# agent_05 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-005 `directory` `prompt-template`
- cursor: `[_]`
- core_role: Template corpus for the RLCR and PR-review-loop state machines. It defines generated prompts, validator block messages, review instructions, goal-tracker contracts, and plan scaffolds consumed by setup scripts and hooks through `hooks/lib/template-loader.sh`.
- algorithmic_behavior: The directory is not passive documentation; its files parameterize workflow transitions. `hooks/lib/template-loader.sh` defines `{{VARIABLE_NAME}}` substitution, single-pass rendering to prevent placeholder injection, missing-variable preservation, and fallback rendering via `load_and_render_safe` at lines 7-20, 50-58, 71-131, and 167-203. `scripts/setup-pr-loop.sh` renders `pr-loop/goal-tracker-initial.md`, `round-0-header.md`, `round-0-task-no-comments.md`, `round-0-task-has-comments.md`, and critical requirements templates while creating the initial PR loop at lines 632, 678, 720, 767, 834, and 860. `hooks/pr-loop-stop-hook.sh` renders block and goal-tracker-update templates for stop-hook decisions at lines 729, 774, and 1248. `hooks/loop-codex-stop-hook.sh` consumes RLCR review/finalize/next-round templates, including Codex review and Claude continuation prompts, as shown by references at lines 744, 784, 800, 914, 1035, 1063, 1114, 1489, 1505, and 1518 from the search output.
- inputs_outputs_state: Inputs are template variables supplied by scripts and hooks, such as `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `RESOLVE_PATH`, `BOT_MENTION_STRING`, `GOAL_TRACKER_FILE`, `NEXT_ROUND`, `CURRENT_ROUND`, and review-result paths. Outputs are rendered markdown prompt/block files or JSON hook block reasons that guide Claude/Codex behavior. State coupling is strongest for `.humanize/rlcr/<timestamp>/round-N-*`, `.humanize/pr-loop/<timestamp>/round-N-*`, `state.md`, `goal-tracker.md`, and PR trigger/review artifacts. The renderer keeps undefined placeholders intact rather than silently deleting them, per `template-loader.sh` lines 12-13 and 119-122.
- gates_or_invariants: The template tree encodes several hard gates. `block/*` templates explain blocked writes, git cleanliness, unpushed commits, wrong round/file paths, stale schema, force-push detection, no trigger comments, Claude-eyes timeout, and large files. PR-loop templates enforce the split between no-comment round zero and existing-comment round zero. `prompt-template/pr-loop/round-0-task-no-comments.md` tells the worker not to trigger first review and to let the stop hook poll, while `round-0-task-has-comments.md` requires fixing issues, committing, pushing, and tagging bots. Codex templates enforce complete acceptance criteria, no deferrals, and stagnation stopping: `codex/full-alignment-review.md` requires `STOP` on stagnation and `COMPLETE` only if all acceptance criteria are fully met at lines 84-93. `codex/regular-review.md` likewise treats deferred and unfinished items as incomplete at lines 24-31 and 51-56.
- dependencies_and_callers: The parent rendering API is `hooks/lib/template-loader.sh`. Shell callers include `scripts/setup-pr-loop.sh`, `hooks/pr-loop-stop-hook.sh`, `hooks/loop-codex-stop-hook.sh`, validators under `hooks/loop-*-validator.sh`, and `scripts/validate-gen-plan-io.sh`. Tests include `tests/test-template-references.sh`, which scans template references and requires missing-template failures to be caught at lines 51-111 and cross-checks common templates at lines 149-167, plus `tests/test-templates-comprehensive.sh`, which loads every markdown template and validates placeholder syntax at lines 61-107 and 114-194.
- edge_cases_or_failure_modes: Missing templates are handled by safe fallbacks in runtime paths, but unsafe direct `load_and_render` can produce empty content if misused; `tests/test-template-references.sh` explicitly guards critical validators to use `load_and_render_safe` at lines 172-200. Template syntax supports only uppercase placeholders; malformed braces, spaces, and lowercase placeholders are tested by `tests/test-templates-comprehensive.sh` lines 122-175 and 197-256. Some PR-loop-specific read-only block text lives in `block/pr-loop-prompt-write.md`, distinct from the general `block/prompt-file-write.md`, so using the wrong block for PR-loop prompt files would produce a less precise reason.
- validation_or_tests: Recursive inventory found 46 markdown templates, 1108 total lines. `tests/test-templates-comprehensive.sh` validates directory existence, loads all markdown templates, checks non-empty content, syntax, malformed placeholder detection, fallback behavior, and real-template rendering. `tests/test-template-references.sh` validates references from hooks and common message functions, including `block/prompt-file-write.md`. `tests/test-pr-loop-system.sh` verifies `pr-loop/goal-tracker-initial.md` schema at lines 641-654 and setup-created goal trackers at lines 1452-1537.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-035 `file` `scripts/check-pr-reviewer-status.sh`
- cursor: `[_]`
- core_role: Runtime PR-review-loop classifier. It determines the startup/re-evaluation case from PR comments, review comments, PR review submissions, and latest commit time, producing JSON consumed by setup and stop-hook state transitions.
- algorithmic_behavior: The script parses `<pr_number> --bots <bot1,bot2>` at lines 46-85, maps local bot names to GitHub authors at lines 91-100, resolves the base repository for fork PR support at lines 109-137, fetches latest commit metadata at lines 139-148, fetches issue comments, inline review comments, and PR review submissions at lines 150-162, combines them at line 165, then loops over configured bots at lines 176-202. For each bot it records whether comments exist, newest review timestamp, whether that bot’s review predates the latest commit, and a global newest timestamp for debugging. Case selection occurs at lines 208-242: no commenters -> case 1; partial commenters with no stale bot -> case 2; all commenters fresh -> case 3; all commenters with any stale bot -> case 4; partial commenters with any stale bot -> case 5. Output JSON is built at lines 248-272.
- inputs_outputs_state: Inputs are positional PR number, `--bots` CSV, current git/GitHub repository context, GitHub CLI responses, and `portable-timeout.sh` sourced at lines 38-40. Outputs are JSON fields `case`, `reviewers_commented`, `reviewers_missing`, `latest_commit_sha`, `latest_commit_at`, `newest_review_at`, and `has_commits_after_reviews`, documented at lines 11-20 and emitted at lines 256-272. It does not write persistent state directly; `scripts/setup-pr-loop.sh` persists its output into `.humanize/pr-loop/<timestamp>/state.md` fields `startup_case`, `latest_commit_sha`, and `latest_commit_at` at lines 418-429 and 535-554. `hooks/pr-loop-stop-hook.sh` re-runs it dynamically and updates `startup_case` at lines 1531-1543.
- gates_or_invariants: The key invariant is per-bot staleness, not global newest-review staleness. Lines 189-193 mark a bot stale when `LATEST_COMMIT_AT` is greater than that bot’s newest review time; case 4/5 can occur even if another bot reviewed later. Argument validation is strict: missing PR or `--bots`, unknown option, missing bot list, or multiple PR numbers exit with errors at lines 49-85. GitHub calls use `run_with_timeout` to avoid hanging, and commit info failure is fatal at lines 139-145. Comment-fetch failures degrade to empty arrays at lines 153-162, which may classify missing reviewers rather than aborting.
- dependencies_and_callers: Depends on `gh`, `jq`, shell arrays, string timestamp comparison on ISO UTC timestamps, and `scripts/portable-timeout.sh`. `scripts/setup-pr-loop.sh` calls it during initialization at lines 418-422, then uses case 4/5 to post trigger comments and verify Claude eyes at lines 443-521. `hooks/pr-loop-stop-hook.sh` calls it at lines 1531-1534 to refresh startup case after new comments arrive. Tests in `tests/test-pr-loop-system.sh` call it directly for cases 1, 2, 3, 4, 5, and PR-review-submission inclusion at lines 151-184, 235-248, 492-505, 660-674, and 1314-1377.
- edge_cases_or_failure_modes: Fork PRs are handled by resolving `CURRENT_REPO`, then base repo, then parent repo, falling back to current repo with a warning at lines 113-137. Empty comment sources are valid and produce case 1. PR review submissions are included because some bots may approve without inline comments, handled at lines 159-162 and tested at `tests/test-pr-loop-system.sh` lines 235-248. Timestamp comparison assumes lexicographically sortable UTC strings; malformed or missing timestamps could avoid stale detection. The initial comment block says case 5 is “for future distinction” and “like case 4” at lines 26-27, but implementation distinguishes partial-plus-stale from all-plus-stale at lines 220-237.
- validation_or_tests: `tests/test-pr-loop-system.sh` verifies no comments -> case 1 at lines 151-169, partial -> case 2 at lines 172-191, PR reviews count as comments at lines 235-248, all commented -> case 3 at lines 492-505, valid JSON for case 4/5 path at lines 660-674, exact case 4 with `has_commits_after_reviews=true` at lines 1314-1339, and exact case 5 at lines 1346-1377. Stop-hook tests also exercise dynamic startup-case refresh via this script, referenced in `tests/test-pr-loop-stophook.sh`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-065 `file` `tests/test-pr-loop-system.sh`
- cursor: `[_]`
- core_role: Executable specification for the PR-loop system, covering setup, reviewer classification, reactions, phase detection, goal-tracker parsing/update semantics, startup trigger rules, stale review cases, monitor output, and stop-hook integration.
- algorithmic_behavior: The test runner sets a mock environment with fake `gh` fixtures and a temporary git repo at lines 50-70, isolates each test in a subshell via `run_test` at lines 79-103, and dispatches named test groups in `main` at lines 1753-1896. It models the PR-loop state machine by creating `.humanize/pr-loop/<timestamp>/state.md`, `approve-state.md`, `cancel-state.md`, `maxiter-state.md`, round files, and goal trackers, then calling production scripts/hooks and asserting outputs. It also mutates fixtures to simulate GitHub comments, PR reviews, reactions, commit timestamps, missing trigger IDs, and bot responses.
- inputs_outputs_state: Inputs are mock fixture files under `tests/fixtures`, mock commands under `tests/mocks`, temporary repositories, environment variables such as `MOCK_GH_PR_NUMBER`, `MOCK_GH_PR_STATE`, `MOCK_GH_LATEST_COMMIT_AT`, `MOCK_GH_HEAD_SHA`, `MOCK_GH_COMMENT_ID_LOOKUP_FAIL`, `TEST_TEMP_DIR`, and `CLAUDE_PROJECT_DIR`. Outputs are pass/fail counters, generated temporary `.humanize/pr-loop` sessions, modified test goal trackers, and process exit status. State transitions under test include active `state.md` -> `approve-state.md`, `state.md` -> `maxiter-state.md`, missing setup cleanup, active-bot removal, goal-tracker row insertion, and round advancement.
- gates_or_invariants: Mutual exclusion is specified: active RLCR blocks PR loop and active PR loop blocks RLCR at lines 109-145. Reviewer status cases and PR review submissions are gates at lines 151-184, 235-248, and 492-505. Phase detection maps state files and round files into monitor phases: approved, waiting initial, waiting reviewer, cancelled, maxiter, and Codex analyzing at lines 255-308, 422-470, 473-485, and 848-898. Trigger requirement invariant is encoded at lines 1111-1173: round 0 cases 1/2/3 do not require a new trigger, cases 4/5 do, and all rounds after 0 do. Case 4/5 setup must fail if Claude eyes verification lacks a trigger comment ID, and cleanup the loop directory, tested at lines 1383-1445.
- dependencies_and_callers: The test file depends on production scripts `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `hooks/pr-loop-stop-hook.sh`, `hooks/lib/loop-common.sh`, `scripts/lib/monitor-common.sh`, and `scripts/humanize.sh`. It also depends on `git`, `jq`, `timeout`, `sed`, `grep`, fixture JSON, and mock CLI behavior. It is included in broader suites by `tests/run-all-tests.sh`, according to search output showing `"test-pr-loop-system.sh"` in that runner.
- edge_cases_or_failure_modes: The tests explicitly model no comments across all comment sources, partial comments with other sources cleared, PR review submissions without comments, new commits after review, partial-review stale state, trigger lookup failure, monitor output without terminal control, old/stalled `round-N-pr-check.md` files, idempotent goal-tracker updates, partial goal-tracker repair, and stop-hook update of goal tracker after bot review. Portability concerns appear in date handling with GNU/BSD fallbacks at lines 1547-1550 and in file-size stat fallbacks at lines 883-884. Some tests restore fixtures manually after mutation, so early hard exits or interrupted runs could leave fixture state dirty in a normal writable checkout.
- validation_or_tests: This file is itself the validation suite. It includes 30-plus named tests and supports filtering by group in `main`: mutual exclusion, reviewer status, reactions, PR reviews, phase detection, goal tracker, PR goal tracker, state file, extended phase, unpushed commits, force-push ancestry, approve state, goal tracker schema, startup case, shared monitor, phase analyzing, monitor output, case1 exception, goal tracker table, partial repair, case 4/5, setup failure, and goal-tracker integration at lines 1765-1879. Final result reporting and nonzero failure exit are at lines 1881-1893.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-095 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: Validator block-message template for attempted writes or edits to generated `round-*-prompt.md` files. It preserves the invariant that generated prompts are instructions from the loop system to the worker and cannot be self-modified.
- algorithmic_behavior: The template states that `round-*-prompt.md` files cannot be written, explains that prompt files contain instructions “FROM Codex TO you (Claude),” and redirects behavior to reading the prompt, executing tasks, and writing results to the summary file at lines 1-12. Its behavioral role is to turn a validator rejection into actionable guidance rather than a silent denial.
- inputs_outputs_state: The template has no variables. It is loaded by `prompt_write_blocked_message()` in `hooks/lib/loop-common.sh` at lines 448-455 with a fallback string. Runtime outputs are stderr block reasons from pre-tool validators or bash validators. It does not mutate state; it protects generated prompt state by preventing writes to prompt files.
- gates_or_invariants: General round prompt files are read-only. `hooks/loop-write-validator.sh` blocks Write calls to `round-*-prompt.md` at lines 65-68. `hooks/loop-edit-validator.sh` blocks Edit calls at lines 48-50. `hooks/loop-bash-validator.sh` blocks shell commands modifying `round-[0-9]+-prompt.md` at lines 360-368. For PR-loop prompt/comment/check/feedback files inside `.humanize/pr-loop`, the system uses the more specific `block/pr-loop-prompt-write.md` via `pr_loop_prompt_blocked_message()` at `hooks/lib/loop-common.sh` lines 892-899, with Write/Edit/Bash call sites at `loop-write-validator.sh` lines 83-87, `loop-edit-validator.sh` lines 66-70, and `loop-bash-validator.sh` lines 417-442.
- dependencies_and_callers: Depends on template loading from `hooks/lib/template-loader.sh` and shared validator functions in `hooks/lib/loop-common.sh`. It is referenced by `tests/test-template-references.sh` as a common template at lines 152-155 and by runtime search result `hooks/lib/loop-common.sh:454`. It is indirectly exercised by write/edit/bash validator tests that attempt prompt-file modification.
- edge_cases_or_failure_modes: Because the template is generic, PR-loop read-only files use a separate PR-loop-specific message. If this file were missing, `prompt_write_blocked_message()` would fall back to a minimal inline message at `hooks/lib/loop-common.sh` lines 449-455; tests are designed to catch missing common templates. It only covers tool-mediated messaging; the actual prevention is in validators, so failure to install hooks would bypass it.
- validation_or_tests: Direct existence is validated in `tests/test-template-references.sh` common templates at lines 152-167. The whole template corpus load and syntax are validated by `tests/test-templates-comprehensive.sh` lines 90-107 and 118-194. Runtime validator call sites are present in Write/Edit/Bash validators as cited above.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-125 `file` `prompt-template/pr-loop/round-0-task-has-comments.md`
- cursor: `[_]`
- core_role: Round-zero PR-loop task template for the branch where fetched PR comments already exist. It defines the initial worker obligations for resolving existing human/bot feedback and triggering re-review.
- algorithmic_behavior: The template orders the worker to analyze comments with human comments first and bot comments newest-first at lines 6-8, fix issues by reading code, making changes, and adding tests where needed at lines 10-13, then commit, push, and post a bot-mention comment at lines 15-21. It also requires a resolution summary at `@{{RESOLVE_PATH}}` listing addressed issues, modified files, and tests at lines 23-26. The rules section forbids state-file modification, requires pushing, requires the correct bot-tag format, and demands all valid concerns be addressed at lines 30-35. Lines 39-43 describe the next transition: stop hook polls reviews, Codex validates approval, issues continue the loop, approval ends it.
- inputs_outputs_state: Inputs are rendered variables `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`; `START_BRANCH` and `ACTIVE_BOTS_DISPLAY` are supplied in the common header rendered before this task. Output is appended to `.humanize/pr-loop/<timestamp>/round-0-prompt.md` by `scripts/setup-pr-loop.sh` at lines 767 and 770-771. Expected worker output is a commit/push, PR comment, and `.humanize/pr-loop/<timestamp>/round-0-pr-resolve.md` summary. State transitions occur later through `hooks/pr-loop-stop-hook.sh`, which verifies trigger rules, polls, writes next-round comment/check/feedback files, updates active bots, and advances or approves.
- gates_or_invariants: This template is chosen only when `scripts/setup-pr-loop.sh` detects fetched comments by absence of the exact sentinel `*No comments found.*` in `round-0-pr-comment.md` at lines 641-649 and enters the “has comments” branch at lines 721-768. Its gate differs from `round-0-task-no-comments.md`: no-comments round zero says not to trigger initial review, while this template requires commit/push/comment. It enforces that `.humanize/pr-loop` state files remain system-managed at line 32, matching validator enforcement in `hooks/lib/loop-common.sh` and `hooks/loop-*-validator.sh`.
- dependencies_and_callers: Called by `scripts/setup-pr-loop.sh` through `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/round-0-task-has-comments.md"` at line 767, after comment fetch at lines 406-412 and comment-count detection at lines 643-649. It coordinates with `prompt-template/pr-loop/round-0-header.md` for PR metadata and fetched comments, `critical-requirements-has-comments.md` for final console requirements, `scripts/fetch-pr-comments.sh` for comment file contents, `hooks/pr-loop-stop-hook.sh` for polling and Codex validation, and `hooks/loop-write-validator.sh`/`loop-edit-validator.sh` for protecting generated files.
- edge_cases_or_failure_modes: The selection relies on a sentinel string from `fetch-pr-comments.sh`; any non-sentinel content, including malformed or empty-but-not-sentinel comment files, is treated as “has comments.” The template assumes worker can push and comment; if commits are not pushed or no trigger comment exists, stop-hook trigger validation can block later. For startup cases 4/5, setup itself may post a re-review trigger before prompt creation, but this template still tells the worker to comment after fixing issues, creating an intentional fresh review trigger after work.
- validation_or_tests: Template existence and syntax are covered by the template tests. Behavior is indirectly specified by `tests/test-pr-loop-system.sh`: setup chooses prompt paths after fixture comments, case 4/5 trigger behavior is tested at lines 1314-1377 and setup failure at lines 1383-1445, and goal tracker/setup integration is tested at lines 1452-1537. Trigger requirement rules that align with this template’s “comment to trigger re-review” instruction are tested at lines 1111-1173.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5/5 section headings present; each assigned item section uses cursor `[_]`
- missing_items: none
- duplicate_items: none in Item Evidence headings
- final_worker_status: `complete`