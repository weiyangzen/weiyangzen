# agent_22 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-022 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR loop. It is a thin command surface over `scripts/setup-rlcr-loop.sh`, with the command frontmatter limiting the allowed tool to `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*)` and passing `$ARGUMENTS` directly to that setup script (`commands/start-rlcr-loop.md:1-14`). It documents the branch feature’s core two-phase algorithm: implementation summary review followed by `codex review` review-loop gating (`commands/start-rlcr-loop.md:16-25`, `commands/start-rlcr-loop.md:60-67`).
- algorithmic_behavior: The command starts an iterative development loop where the agent implements a plan, writes a per-round summary, and is intercepted by the RLCR stop hook. In implementation phase, Codex reviews the summary and either returns feedback, `STOP`, or a final `COMPLETE` marker. `COMPLETE` does not end the loop directly; it transitions into Review Phase, where `codex review --base <branch>` checks git diff quality and the loop continues while review output contains `[P0-9]` findings (`commands/start-rlcr-loop.md:16-25`, `commands/start-rlcr-loop.md:54-67`). The delegated setup script initializes `state.md` with `current_round: 0`, Codex config, `push_every_round`, plan metadata, `start_branch`, `base_branch`, captured `base_commit`, and `review_started: false` (`scripts/setup-rlcr-loop.sh:628-644`), then creates `goal-tracker.md` and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:650-807`). The stop hook later flips `review_started: true`, creates `.review-phase-started`, and runs the review phase when implementation review returns `COMPLETE` (`hooks/loop-codex-stop-hook.sh:1360-1400`).
- inputs_outputs_state: Inputs are a relative markdown plan path or `--plan-file`, plus options `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, `--track-plan-file`, `--push-every-round`, and `--base-branch` (`commands/start-rlcr-loop.md:3`, `scripts/setup-rlcr-loop.sh:44-107`, `scripts/setup-rlcr-loop.sh:111-195`). Outputs are the initialized `.humanize/rlcr/<timestamp>/` session, including copied `plan.md`, `state.md`, `goal-tracker.md`, round prompt files, round summary targets, Codex audit/debug files in cache, and stop-hook JSON block/allow decisions. Key state transitions are `state.md` active loop -> `review_started: true` with `.review-phase-started` -> `finalize-state.md` -> `complete-state.md` (`hooks/loop-codex-stop-hook.sh:996-1079`, `hooks/loop-codex-stop-hook.sh:716-721`). Review issue state is stored by incrementing `current_round` and writing a new round prompt/summary target (`hooks/loop-codex-stop-hook.sh:1082-1127`, `hooks/loop-codex-stop-hook.sh:1471-1528`).
- gates_or_invariants: Setup enforces one active RLCR/PR loop at a time (`scripts/setup-rlcr-loop.sh:211-232`), requires a git repo with at least one commit (`scripts/setup-rlcr-loop.sh:257-267`), rejects unsafe plan paths including absolute paths, spaces, metacharacters, symlinks, parent symlink traversal, missing/unreadable files, out-of-project resolution, and submodule paths (`scripts/setup-rlcr-loop.sh:273-374`). It gates plan tracking: tracked and clean only with `--track-plan-file`; otherwise the plan must be untracked/gitignored (`scripts/setup-rlcr-loop.sh:376-425`). It requires at least 5 total lines and at least 3 meaningful content lines (`scripts/setup-rlcr-loop.sh:431-484`), requires `codex` to be available (`scripts/setup-rlcr-loop.sh:486-492`), validates YAML-safe branch/model/effort names (`scripts/setup-rlcr-loop.sh:504-530`, `scripts/setup-rlcr-loop.sh:588-594`), and resolves a local review base branch by user input > remote default if local > local `main` > local `master` (`scripts/setup-rlcr-loop.sh:533-604`). Runtime stop-hook gates include state schema validation, branch consistency, goal-tracker initialization in round 0, max iteration enforcement outside Review/Finalize phases, git clean state before expensive Codex review, optional push-every-round, and Review Phase marker validation to prevent manual `review_started` toggles (`hooks/loop-codex-stop-hook.sh:88-205`, `hooks/loop-codex-stop-hook.sh:620-721`, `hooks/loop-codex-stop-hook.sh:486-570`, `hooks/loop-codex-stop-hook.sh:1412-1428`).
- dependencies_and_callers: Direct dependency is `scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:12-14`). The setup script depends on `scripts/portable-timeout.sh`, `hooks/lib/loop-common.sh`, git, Codex CLI, and the prompt templates consumed by stop hooks (`scripts/setup-rlcr-loop.sh:26-29`, `scripts/setup-rlcr-loop.sh:203-205`, `scripts/setup-rlcr-loop.sh:486-492`). The runtime loop is implemented by `hooks/loop-codex-stop-hook.sh`; shared state parsing and markers are in `hooks/lib/loop-common.sh`, including `MARKER_COMPLETE`, `MARKER_STOP`, and exit reasons (`hooks/lib/loop-common.sh:29-43`). `README.md` also documents this command and the review-phase behavior, but the command and scripts are the controlling surfaces.
- edge_cases_or_failure_modes: Setup fails closed on unsafe/missing plan paths, missing git history, unavailable `codex`, invalid branch/model/effort names, remote-only base branches, no discoverable local base branch, dirty tracked plan when `--track-plan-file`, tracked plan without `--track-plan-file`, simple/empty plan files, and timed-out git operations (`scripts/setup-rlcr-loop.sh:257-604`). Runtime failures include corrupt/truncated state, outdated schema, branch changes during loop, uninitialized goal tracker, uncommitted changes, unpushed commits when required, Codex exec non-zero exit, missing/empty Codex review result, `codex review` failure, empty review log, manual Review Phase marker inconsistency, max iterations, and `STOP` circuit-breaker output (`hooks/loop-codex-stop-hook.sh:116-154`, `hooks/loop-codex-stop-hook.sh:156-205`, `hooks/loop-codex-stop-hook.sh:1129-1202`, `hooks/loop-codex-stop-hook.sh:1277-1350`, `hooks/loop-codex-stop-hook.sh:1437-1465`).
- validation_or_tests: Related test coverage includes `tests/test-plan-file-validation.sh`, `tests/robustness/test-path-validation-robustness.sh`, `tests/robustness/test-base-branch-detection.sh`, `tests/test-finalize-phase.sh`, `tests/test-state-exit-naming.sh`, and PR/RLCR mutual exclusion tests referenced by repository search. The command itself is documentation/frontmatter plus a single setup-script invocation, so behavior is validated through the setup script and stop-hook test suites rather than by executing the markdown command. I did not modify files.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-052 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for graceful failure behavior in `hooks/lib/template-loader.sh`, which is a shared dependency for RLCR/PR prompt rendering and blocking messages. The test directly sources the loader (`tests/test-error-scenarios.sh:11-15`) and verifies that missing templates/directories, strict shell mode, fallbacks, validation, and malformed placeholders do not crash core hook workflows.
- algorithmic_behavior: The test defines local `pass`/`fail` counters and runs 12 assertions (`tests/test-error-scenarios.sh:22-35`, `tests/test-error-scenarios.sh:42-207`). It exercises `load_template`, `load_and_render`, `render_template`, `load_and_render_safe`, and `validate_template_dir`. The target loader computes `prompt-template` from `hooks/lib`, returns empty output for missing template files while warning on stderr, performs single-pass `{{VAR}}` substitution via `awk`, keeps missing variables as placeholders, and provides safe fallback rendering when a template is missing or renders empty (`hooks/lib/template-loader.sh:24-48`, `hooks/lib/template-loader.sh:50-132`, `hooks/lib/template-loader.sh:134-147`, `hooks/lib/template-loader.sh:167-203`). Directory validation requires `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:205-222`).
- inputs_outputs_state: Inputs are the repository root, resolved template directory, template names, inline template strings, fallback strings, and `VAR=value` assignments. Outputs are console pass/fail lines, aggregate counters, and process exit code 0 when all tests pass or 1 when any test fails (`tests/test-error-scenarios.sh:208-226`). No durable repo state is intentionally changed; all data is local shell variables and command output.
- gates_or_invariants: The file asserts these invariants: missing template file returns empty content and exit 0 (`tests/test-error-scenarios.sh:42-52`); missing template directory returns empty content and exit 0 (`tests/test-error-scenarios.sh:54-65`); `load_and_render` with a missing template returns empty and exit 0 (`tests/test-error-scenarios.sh:67-78`); empty content renders empty (`tests/test-error-scenarios.sh:80-91`); special regex characters in values render literally (`tests/test-error-scenarios.sh:93-106`); missing templates do not terminate a `set -euo pipefail` subshell (`tests/test-error-scenarios.sh:108-131`); `load_and_render_safe` uses fallback and substitutes fallback variables (`tests/test-error-scenarios.sh:133-158`); valid template directories pass and invalid directories fail (`tests/test-error-scenarios.sh:160-180`); empty and unclosed placeholders remain literal (`tests/test-error-scenarios.sh:182-206`).
- dependencies_and_callers: Depends directly on `hooks/lib/template-loader.sh` (`tests/test-error-scenarios.sh:13`). That loader is sourced by `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:138-148`), making its failure behavior relevant to RLCR stop hooks, write/read/bash validators, and PR loop hooks. Repository call sites include `hooks/loop-codex-stop-hook.sh` block prompts for git cleanliness, summary missing, goal tracker validation, review failure, next-round prompts, finalize prompts, and post-alignment inserts (`hooks/loop-codex-stop-hook.sh:307`, `hooks/loop-codex-stop-hook.sh:533`, `hooks/loop-codex-stop-hook.sh:744`, `hooks/loop-codex-stop-hook.sh:914`, `hooks/loop-codex-stop-hook.sh:1489-1522`).
- edge_cases_or_failure_modes: This test focuses on absent files/directories, strict mode resilience, special regex/shell-like characters in values, fallback substitution, malformed placeholders, and invalid template roots. One subtle target behavior is that `render_template` is single-pass, so injected `{{OTHER_VAR}}` in variable values is not recursively expanded (`hooks/lib/template-loader.sh:54-57`, `hooks/lib/template-loader.sh:71-129`). In this read-only branch export, running the test exposed an environment-specific failure: `render_template` uses a here-string at `hooks/lib/template-loader.sh:129`, and Bash attempted to create a temporary file but the sandbox denied it, producing “cannot create temp file for here document: Operation not permitted.” As a result, tests 5, 8, 11, and 12 failed in this environment even though the file under test is intended as a normal writable-temp-shell harness.
- validation_or_tests: I ran `bash tests/test-error-scenarios.sh` read-only. Result: 8 passed, 4 failed. The failures were caused by sandbox temp-file denial at `hooks/lib/template-loader.sh:129`, not by repository edits. The test’s own validation summary exits 1 when failures are present (`tests/test-error-scenarios.sh:218-226`). Related broader coverage exists in `tests/test-template-loader.sh`, which tests normal loader behavior, fallback rendering, directory validation, and shell metacharacter handling (`tests/test-template-loader.sh:41-244`), plus `tests/test-templates-comprehensive.sh` and `tests/test-template-references.sh`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-082 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the RLCR stop-hook git cleanliness gate. It defines the user-facing reason when the agent tries to exit while git has uncommitted changes (`prompt-template/block/git-not-clean.md:1-16`). This is algorithmically important because clean git state is a precondition before expensive Codex summary review or Review Phase execution.
- algorithmic_behavior: The template renders `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}`, then tells the agent to review untracked files, stage real changes, and commit with project conventions (`prompt-template/block/git-not-clean.md:3-10`). In the stop hook, cached git status populates `GIT_ISSUES="uncommitted changes"` when any staged, unstaged, or untracked changes exist (`hooks/loop-codex-stop-hook.sh:492-500`). The hook appends special notes when untracked `.humanize*` local loop directories or other untracked files are present (`hooks/loop-codex-stop-hook.sh:501-521`). If `GIT_ISSUES` is non-empty, the hook renders this template with `load_and_render_safe` and returns a JSON block decision (`hooks/loop-codex-stop-hook.sh:524-545`).
- inputs_outputs_state: Inputs are `GIT_ISSUES` and `SPECIAL_NOTES`. `SPECIAL_NOTES` may include content from `prompt-template/block/git-not-clean-humanize-local.md` and `prompt-template/block/git-not-clean-untracked.md` if those templates exist (`hooks/loop-codex-stop-hook.sh:504-520`). Output is markdown embedded in the hook JSON `reason`, with `systemMessage` set to “Loop: Blocked - uncommitted changes detected, please commit first” (`hooks/loop-codex-stop-hook.sh:537-545`). It does not mutate files; it blocks loop exit until the user commits or otherwise cleans the working tree.
- gates_or_invariants: The invariant is that no RLCR stop attempt proceeds to Codex review while `git status --porcelain` is non-empty. The template reinforces commit hygiene: use project commit conventions, do not include AI authorship, and do not add AI co-author attribution (`prompt-template/block/git-not-clean.md:11-16`). The hook’s fallback provides a minimal equivalent message if the template is missing, preserving the gate even with template failure (`hooks/loop-codex-stop-hook.sh:527-535`).
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh` at the Quick Check “Git Clean and Pushed?” gate (`hooks/loop-codex-stop-hook.sh:486-545`). Rendering depends on `hooks/lib/template-loader.sh` via `load_and_render_safe` (`hooks/lib/template-loader.sh:167-203`). The template is part of the shared `prompt-template/block` directory validated by `validate_template_dir` (`hooks/lib/template-loader.sh:205-222`). The same template path is included in test-reference scans and template-loader integration coverage (`tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh` by repository search).
- edge_cases_or_failure_modes: If `.humanize*` is untracked, the hook adds a special note that these directories are intentionally local rather than simply telling the agent to commit them (`hooks/loop-codex-stop-hook.sh:504-510`). If other untracked files exist, it adds a different artifact-review note (`hooks/loop-codex-stop-hook.sh:513-520`). If the template itself is missing, `load_and_render_safe` uses the inline fallback (`hooks/loop-codex-stop-hook.sh:527-535`). If template rendering fails under an environment that cannot create shell here-string temp files, fallback rendering may also be affected because it uses the same `render_template` implementation; the functional design is still fail-soft at the file-missing level.
- validation_or_tests: Covered by `tests/test-error-scenarios.sh` indirectly through missing template/fallback behavior and directory validation, and by `tests/test-template-loader.sh` / `tests/test-templates-comprehensive.sh` for real template loading and safe rendering. The gate behavior is also observable in stop-hook logic at `hooks/loop-codex-stop-hook.sh:486-545`. I ran `tests/test-error-scenarios.sh`; the run failed under read-only temp restrictions, with 8 pass / 4 fail as described above.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-112 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Next-round instruction snippet appended after Full Goal Alignment Check rounds. It is not executable, but it participates in the RLCR algorithm by carrying Codex alignment audit findings into the following Claude work round (`prompt-template/claude/post-alignment-action-items.md:2-7`).
- algorithmic_behavior: The stop hook builds a next-round prompt whenever Codex finds issues and the loop continues (`hooks/loop-codex-stop-hook.sh:1471-1492`). Immediately after creating that prompt, it detects post-alignment rounds with `CURRENT_ROUND % 5 == 4`; when true, it loads this template and appends it to the next prompt (`hooks/loop-codex-stop-hook.sh:1494-1500`). The snippet tells the agent to prioritize forgotten items, unmet acceptance criteria, and unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:4-7`). This ties into the command-level goal tracker system, where full alignment checks happen at rounds 4, 9, 14, etc. (`commands/start-rlcr-loop.md:35-40`).
- inputs_outputs_state: Inputs are only the current round number and the existence/content of the template. The template has no variables. Output is appended markdown in `round-${NEXT_ROUND}-prompt.md` after the main next-round prompt and before footer/push/goal-tracker-update request sections (`hooks/loop-codex-stop-hook.sh:1476-1522`). It does not directly update `state.md`; state advancement occurs separately by replacing `current_round` with `NEXT_ROUND` (`hooks/loop-codex-stop-hook.sh:1471-1474`).
- gates_or_invariants: The invariant is cadence-based: only the round immediately after a Full Alignment Check receives this extra action-item block. The modulo condition uses the current completed/reviewed round, so rounds 4, 9, 14, etc. trigger the append (`hooks/loop-codex-stop-hook.sh:1494-1496`). If the template is missing or empty, the hook silently skips the appendix because it uses `load_template` and checks non-empty content rather than a hard failure (`hooks/loop-codex-stop-hook.sh:1496-1499`).
- dependencies_and_callers: Single direct caller found: `hooks/loop-codex-stop-hook.sh` while constructing the next round prompt (`hooks/loop-codex-stop-hook.sh:1480-1522`). It depends on `load_template` from `hooks/lib/template-loader.sh`, which returns an empty string for missing templates while warning on stderr (`hooks/lib/template-loader.sh:33-48`). It coordinates with the Codex full-alignment review prompt and goal-tracker update flow: the hook inserts a goal-tracker update section into Codex prompts (`hooks/loop-codex-stop-hook.sh:741-745`) and later asks Claude to update the goal tracker if needed (`hooks/loop-codex-stop-hook.sh:1517-1522`).
- edge_cases_or_failure_modes: If the alignment check found issues but the template is missing, the loop still continues with the standard next-round prompt and footer; no block decision is caused by this missing snippet (`hooks/loop-codex-stop-hook.sh:1496-1500`). Because the snippet has no placeholders, it is not exposed to substitution failures. The main behavioral edge case is cadence correctness: the comments say “round after every 5th,” while the condition is `CURRENT_ROUND % 5 == 4`, matching the documented full alignment rounds 4, 9, 14 rather than multiples of 5.
- validation_or_tests: Template existence/reference coverage is handled by `tests/test-template-references.sh` by scanning `load_template`/`load_and_render` calls, and broader template rendering coverage is in `tests/test-templates-comprehensive.sh`. Runtime behavior is evidenced by the stop-hook append block at `hooks/loop-codex-stop-hook.sh:1494-1500`. I did not run a full stop-hook simulation for this branch research item.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4 assigned item sections appear above, one section per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`