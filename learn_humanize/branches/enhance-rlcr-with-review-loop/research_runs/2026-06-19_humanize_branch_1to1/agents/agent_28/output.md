# agent_28 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-028 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit guard for active RLCR loops. It enforces state-schema validity, branch immutability, and plan-file tracking invariants before Claude can continue user-prompt-driven work. It is registered in `hooks/hooks.json:4-10`.
- algorithmic_behavior: Reads stdin hook input but does not inspect fields (`hooks/loop-plan-file-validator.sh:26-27`). Finds the newest active loop under `$PROJECT_ROOT/.humanize/rlcr` via `find_active_loop` (`hooks/loop-plan-file-validator.sh:29-35`, `hooks/lib/loop-common.sh:155-173`). Selects `finalize-state.md` over `state.md` during finalize phase (`hooks/loop-plan-file-validator.sh:38-42`). Parses state with `parse_state_file_strict`; malformed state fails closed (`hooks/loop-plan-file-validator.sh:44-48`). Then validates schema, current branch, and plan tracking/cleanliness (`hooks/loop-plan-file-validator.sh:79-229`).
- inputs_outputs_state: Inputs are `CLAUDE_PROJECT_DIR` or `pwd`, `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, `plan_file`, `plan_tracked`, `start_branch`, `git rev-parse`, `git ls-files`, and `git status`. Allowed path exits `0` with no output. Policy failures usually emit Claude hook JSON with `"decision": "block"` and exit `0`; malformed state emits stderr and exits `1` (`hooks/loop-plan-file-validator.sh:45-48`). It makes no state transition and does not mutate files.
- gates_or_invariants: Requires strict state frontmatter accepted by `parse_state_file_strict`, including core fields such as `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:251-323`). Additionally requires nonempty `plan_tracked` and `start_branch` (`hooks/loop-plan-file-validator.sh:79-89`). Blocks branch drift from `start_branch` (`hooks/loop-plan-file-validator.sh:95-115`). If `plan_tracked=true`, the plan must remain tracked and have no porcelain status (`hooks/loop-plan-file-validator.sh:123-192`). Otherwise the plan must remain untracked/gitignored (`hooks/loop-plan-file-validator.sh:193-229`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for active-loop discovery, strict state parsing, template directory setup, and `FIELD_*` constants (`hooks/loop-plan-file-validator.sh:16-17`). Depends on `scripts/portable-timeout.sh` for bounded git commands (`hooks/loop-plan-file-validator.sh:19-24`, `scripts/portable-timeout.sh:31-71`). Uses `load_and_render_safe` with `block/schema-outdated.md` fallback for older state schema blocks (`hooks/loop-plan-file-validator.sh:59-77`, `hooks/lib/template-loader.sh:167-203`). The setup side records `START_BRANCH` and validates plan tracking before the loop starts (`scripts/setup-rlcr-loop.sh:376-425`, `scripts/setup-rlcr-loop.sh:494-512`).
- edge_cases_or_failure_modes: No active loop allows exit silently (`hooks/loop-plan-file-validator.sh:33-36`). Git branch resolution failure or timeout blocks because branch consistency cannot be verified (`hooks/loop-plan-file-validator.sh:95-106`). Git tracking/status timeout exits 124 and blocks (`hooks/loop-plan-file-validator.sh:127-147`, `hooks/loop-plan-file-validator.sh:152-172`, `hooks/loop-plan-file-validator.sh:197-217`). Unexpected git exit codes fail closed. YAML quote stripping for `start_branch` and `plan_file` relies on shared parser behavior (`hooks/lib/loop-common.sh:271-275`). `FULL_PLAN_PATH` is computed but unused (`hooks/loop-plan-file-validator.sh:121`), making it a minor dead local rather than behavior.
- validation_or_tests: `tests/test-plan-file-hooks.sh` covers valid pass, quoted `plan_file`, malformed state, missing `start_branch`, and branch changes (`tests/test-plan-file-hooks.sh:93-190`). It also covers quoted `start_branch`, mismatch with quoted branch, and hyphenated `plan_file` paths (`tests/test-plan-file-hooks.sh:347-474`). Template existence/fallback coverage is checked by `tests/test-template-references.sh:83-110` and safe rendering behavior by `tests/test-template-references.sh:172-200`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-058 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR monitor shutdown and terminal cleanup behavior, focused on `_humanize_monitor_codex` internals in `scripts/humanize.sh`.
- algorithmic_behavior: Creates a temp workspace under `/tmp`, writes small generated shell scripts, and asserts monitor cleanup invariants with pass/fail counters (`tests/test-monitor-runtime.sh:45-53`). It verifies graceful-stop messaging, cleanup idempotence, directory deletion detection, terminal restore implementation, signal trap wiring for bash and zsh, trap reset, and summary exit behavior (`tests/test-monitor-runtime.sh:56-512`).
- inputs_outputs_state: Inputs are the repository copy of `scripts/humanize.sh`, a synthetic `.humanize/rlcr/<timestamp>/state.md`, generated helper scripts, optional `zsh`, and shell signals. Outputs are colored PASS/FAIL lines and final exit `0` only if `TESTS_FAILED` is zero (`tests/test-monitor-runtime.sh:492-512`). It mutates only its temp directory and removes it via EXIT trap (`tests/test-monitor-runtime.sh:49-53`).
- gates_or_invariants: `_cleanup` must be idempotent via `cleanup_done`, set `monitor_running=false`, reset INT/TERM traps, kill any `tail_pid`, call `_restore_terminal`, and print stopped state (`scripts/humanize.sh:530-561`). `_graceful_stop` must call `_cleanup` and then print a user-facing reason (`scripts/humanize.sh:563-576`). The monitor loop must detect deletion of `.humanize/rlcr` in its outer, no-log, and incremental paths (`scripts/humanize.sh:620-626`, `scripts/humanize.sh:657-666`, `scripts/humanize.sh:751-759`). Bash must install `trap '_cleanup' INT TERM`; zsh must define `TRAPINT` and `TRAPTERM` (`scripts/humanize.sh:578-588`).
- dependencies_and_callers: Sources `scripts/humanize.sh` in generated scripts (`tests/test-monitor-runtime.sh:64-70`, `tests/test-monitor-runtime.sh:263-269`). The target monitor uses shared monitor helpers from `scripts/lib/monitor-common.sh` when available (`scripts/humanize.sh:6-10`) and is invoked through `humanize monitor rlcr` (`scripts/humanize.sh:883-895`).
- edge_cases_or_failure_modes: Test 1 and Test 2 define local copies of `_cleanup` and `_graceful_stop` rather than calling the real nested functions from `scripts/humanize.sh` (`tests/test-monitor-runtime.sh:78-101`, `tests/test-monitor-runtime.sh:143-168`), so they verify the intended contract but not the actual nested implementation. Test 4 contains a pipeline `grep -q ... | grep -q ...` that cannot prove both patterns in one stream and is effectively bypassed by the later direct source check at `tests/test-monitor-runtime.sh:307-311`. Noninteractive terminal behavior is not fully exercised; it greps for `printf "\033[r"` rather than driving `tput` in a real terminal (`tests/test-monitor-runtime.sh:260-303`). zsh runtime coverage is skipped when `zsh` is absent (`tests/test-monitor-runtime.sh:432-490`).
- validation_or_tests: The file is itself the validation asset. It asserts `_restore_terminal` definition and scroll-region reset (`tests/test-monitor-runtime.sh:293-303`), `_cleanup -> _restore_terminal` (`tests/test-monitor-runtime.sh:305-311`), `_graceful_stop -> _cleanup` (`tests/test-monitor-runtime.sh:313-324`), bash SIGINT handling (`tests/test-monitor-runtime.sh:326-381`), signal handler definitions (`tests/test-monitor-runtime.sh:383-409`), and trap reset (`tests/test-monitor-runtime.sh:411-423`).
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-088 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the pre-review incomplete TodoWrite gate. It prevents progression to Codex review or final stopping while any latest transcript todo is not completed.
- algorithmic_behavior: Template renders a concise block headed “Incomplete Todos Detected,” interpolates `{{INCOMPLETE_LIST}}`, then directs Claude to complete all todos and mark them completed with TodoWrite before writing a summary or stopping (`prompt-template/block/incomplete-todos.md:1-12`).
- inputs_outputs_state: Input variable is `INCOMPLETE_LIST` supplied by `loop-codex-stop-hook.sh` after parsing `check-todos-from-transcript.py` output (`hooks/loop-codex-stop-hook.sh:347-358`). Output is markdown used as the `"reason"` in a JSON block decision (`hooks/loop-codex-stop-hook.sh:360-368`). It performs no state mutation directly.
- gates_or_invariants: The gate fires before git cleanliness, summary file checks, and Codex invocation (`hooks/loop-codex-stop-hook.sh:319-370`). It blocks when the latest TodoWrite set contains any status other than `completed` (`hooks/check-todos-from-transcript.py:113-126`). Missing transcript path or no todos allows progression (`hooks/check-todos-from-transcript.py:99-111`). This keeps the RLCR transition ordering as “todos complete first, then review/summary/stop.”
- dependencies_and_callers: Rendered through `load_and_render_safe "$TEMPLATE_DIR" "block/incomplete-todos.md"` (`hooks/loop-codex-stop-hook.sh:352-358`). Template loader performs single-pass `{{VAR}}` substitution and preserves unresolved placeholders (`hooks/lib/template-loader.sh:50-132`). The data source is `hooks/check-todos-from-transcript.py`, which supports Claude assistant entries, alternative message entries, and direct tool_use entries (`hooks/check-todos-from-transcript.py:51-87`).
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook falls back to a shorter inline message (`hooks/loop-codex-stop-hook.sh:352-358`). If hook input JSON cannot be parsed by the todo checker, the stop hook blocks with a parse-error reason instead of using this template (`hooks/loop-codex-stop-hook.sh:328-345`). Invalid JSONL lines inside the transcript are ignored by the checker (`hooks/check-todos-from-transcript.py:36-39`), so malformed transcript fragments can be skipped if a later valid TodoWrite exists.
- validation_or_tests: `tests/test-todo-checker.sh` verifies all-completed exit, incomplete pending exit, output details, in-progress as incomplete, latest TodoWrite selection, direct format, missing status as incomplete, empty content handling, and unicode handling (`tests/test-todo-checker.sh:106-220`, `tests/test-todo-checker.sh:245-290`). Finalize-phase enforcement verifies incomplete todos block and Codex is not invoked first (`tests/test-finalize-phase.sh:702-741`).
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-118 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Regular implementation-phase Codex review prompt. It defines the non-full-alignment review contract that decides whether Claude continues another RLCR round, enters review/finalize flow, or receives concrete corrective work.
- algorithmic_behavior: Requires Codex to read the original plan first via `@{{PLAN_FILE}}`, compare it with `@{{PROMPT_FILE}}` and Claude’s summary, then perform a skeptical implementation review (`prompt-template/codex/regular-review.md:3-27`). It mandates goal tracker alignment checks against `@{{GOAL_TRACKER_FILE}}` and a summary line reporting AC coverage, forgotten items, and unjustified deferrals (`prompt-template/codex/regular-review.md:33-45`). Output rules require writing findings to `@{{REVIEW_RESULT_FILE}}` when work remains, and only ending with `COMPLETE` when every original-plan task and AC is done with no pending or deferred work (`prompt-template/codex/regular-review.md:49-57`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `DOCS_PATH`, `GOAL_TRACKER_FILE`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`, rendered by `loop-codex-stop-hook.sh` into `round-N-review-prompt.md` (`hooks/loop-codex-stop-hook.sh:735-812`). Output is a prompt consumed by `codex exec`; Codex is expected to create or stdout-copy `round-N-review-result.md` (`hooks/loop-codex-stop-hook.sh:1214-1239`, `hooks/loop-codex-stop-hook.sh:1296-1349`).
- gates_or_invariants: Deferrals are treated as incomplete and must not lead to `COMPLETE` (`prompt-template/codex/regular-review.md:24-31`, `prompt-template/codex/regular-review.md:53-57`). If `COMPLETE` is the last nonempty line, implementation phase transitions to review phase, sets `review_started: true`, creates `.review-phase-started`, and runs `codex review` (`hooks/loop-codex-stop-hook.sh:1351-1400`). If the last line is `STOP`, the stop hook terminates as a circuit breaker path (`hooks/loop-codex-stop-hook.sh:1437-1465`). Otherwise it increments `current_round` and generates the next round prompt with review feedback (`hooks/loop-codex-stop-hook.sh:1467-1506`).
- dependencies_and_callers: Selected for non-full-alignment rounds; every fifth-style alignment check uses `full-alignment-review.md` instead (`hooks/loop-codex-stop-hook.sh:747-800`). It includes the rendered goal-tracker update section from `codex/goal-tracker-update-section.md` (`hooks/loop-codex-stop-hook.sh:741-745`). It depends on Codex CLI availability and configured model/effort from state or defaults (`hooks/loop-codex-stop-hook.sh:21-23`, `hooks/loop-codex-stop-hook.sh:821-864`).
- edge_cases_or_failure_modes: If Codex exits nonzero, fails to create a review result, or creates an empty result, the stop hook blocks with debug details (`hooks/loop-codex-stop-hook.sh:1277-1349`). `COMPLETE` must be the last nonempty line exactly; phrases containing the word do not pass (`hooks/loop-codex-stop-hook.sh:1354-1361`). In review phase, `COMPLETE` from summary review is ignored and `codex review` issue detection controls finalization (`hooks/loop-codex-stop-hook.sh:1361-1365`, `hooks/loop-codex-stop-hook.sh:1406-1435`). The prompt’s requirement to write to `@{{REVIEW_RESULT_FILE}}` is backed by stdout-copy fallback if Codex writes to stdout (`hooks/loop-codex-stop-hook.sh:1296-1312`).
- validation_or_tests: Template references are checked by `tests/test-template-references.sh`, which extracts `load_template`, `load_and_render`, and `load_and_render_safe` references and verifies target files exist (`tests/test-template-references.sh:83-110`). Stop-hook tests in `tests/test-plan-file-hooks.sh` exercise parsing before prompt/review progression enough to ensure quoted state fields do not break downstream stop-hook operation (`tests/test-plan-file-hooks.sh:398-441`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-028, ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-058, ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-088, ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-118
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`