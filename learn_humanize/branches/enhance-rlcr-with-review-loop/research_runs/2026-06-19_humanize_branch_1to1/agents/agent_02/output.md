# agent_02 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: Defines repository-local Claude subagent prompts. Recursive inspection found exactly one child: `agents/draft-relevance-checker.md`. This child is an algorithmic routing/classification asset used by plan generation to decide whether a user draft is semantically relevant before the plan-generation workflow proceeds.
- algorithmic_behavior: `agents/draft-relevance-checker.md:1-6` declares the agent identity, model, and tools: name `draft-relevance-checker`, model `haiku`, tools `Read, Glob, Grep`. Its task body directs the agent to inspect repository docs and structure, compare draft content against repo concepts/files/features, and emit one of two machine-readable verdict prefixes: `RELEVANT:` or `NOT_RELEVANT:` at `agents/draft-relevance-checker.md:27-30`. The classifier is intentionally lenient; ambiguous drafts should pass as relevant per `agents/draft-relevance-checker.md:33-36`.
- inputs_outputs_state: Input is draft document content supplied by the caller. Secondary inputs are repository files available through `Read`, `Glob`, and `Grep`. Output is a single verdict line with a prefix and brief explanation. It has no persistent state and no file writes. It influences downstream command control flow: `commands/gen-plan.md:50-72` says the draft is read, the `humanize:draft-relevance-checker` agent is invoked, `NOT_RELEVANT` stops the command, and `RELEVANT` continues to Phase 3.
- gates_or_invariants: Invariants are mostly prompt-contract level: output must start with one of the two expected prefixes; model should be `haiku`; only read/search tools are permitted; judgment should be semantic and lenient rather than strict lexical matching. This keeps gen-plan from doing expensive or destructive work for unrelated drafts while avoiding false rejection of rough or multilingual drafts.
- dependencies_and_callers: Direct runtime caller is the gen-plan command instructions at `commands/gen-plan.md:56-65`, which specify Task tool invocation using the `haiku` model and the same verdict contract. Test coverage in `tests/test-gen-plan.sh` validates existence at `tests/test-gen-plan.sh:111-116`, frontmatter name at `tests/test-gen-plan.sh:123-129`, model at `tests/test-gen-plan.sh:137-143`, tools at `tests/test-gen-plan.sh:151-155`, naming at `tests/test-gen-plan.sh:241-245`, required frontmatter at `tests/test-gen-plan.sh:289-295`, valid model at `tests/test-gen-plan.sh:391-399`, and ASCII/English-only content at `tests/test-gen-plan.sh:417-422`.
- edge_cases_or_failure_modes: The prompt deliberately accepts informal, multilingual, and rough drafts (`agents/draft-relevance-checker.md:33-35`), which reduces false negatives but can admit weakly related work. If the output omits the exact expected prefix, the gen-plan command has no robust parsing contract beyond the prose instructions. Because the tools are read-only search/read tools, direct repository mutation is not a risk from this agent.
- validation_or_tests: Primary validation is structural/static through `tests/test-gen-plan.sh`; no test here executes the subagent against draft examples. The recursive directory inspection found only `agents/draft-relevance-checker.md`, so the directory’s current algorithmic scope is fully represented by that one classifier prompt.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-032 `file` `scripts/cancel-pr-loop.sh`
- cursor: `[_]`
- core_role: Implements the runtime cancellation transition for active PR loops. It converts the newest active PR-loop session from live state into cancelled state by creating `.cancel-requested` and renaming `state.md` to `cancel-state.md`.
- algorithmic_behavior: Argument parsing accepts `--force`, `-h`, and `--help`; unknown options exit with code 3 (`scripts/cancel-pr-loop.sh:24-66`). The script resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, then scopes itself to `.humanize/pr-loop`, explicitly not `.humanize/rlcr` (`scripts/cancel-pr-loop.sh:72-76`). It selects the newest loop directory by sorted directory listing (`scripts/cancel-pr-loop.sh:76`). A loop is active only if `state.md` exists (`scripts/cancel-pr-loop.sh:88-98`). It extracts `current_round`, `max_iterations`, and `pr_number` from YAML-like state lines (`scripts/cancel-pr-loop.sh:104-112`), touches `.cancel-requested`, renames the active state file to `cancel-state.md`, and prints a `CANCELLED` status with contextual round/PR info (`scripts/cancel-pr-loop.sh:118-130`).
- inputs_outputs_state: Inputs are CLI args, `CLAUDE_PROJECT_DIR`, current directory fallback, the newest child of `.humanize/pr-loop/`, and metadata in `state.md`. Outputs are stdout status tokens (`NO_LOOP`, `NO_ACTIVE_LOOP`, or `CANCELLED`), exit codes 0/1/3, a new `.cancel-requested` signal file, and the state transition `state.md -> cancel-state.md`. Persistent state changes are confined to the newest PR-loop session directory.
- gates_or_invariants: The script requires a newest loop directory and an active `state.md`; directories with only final-state files are not active. It does not cancel RLCR loops. Command wiring restricts the Claude slash command to this script and optional `--force` through `commands/cancel-pr-loop.md:1-4`; the command contract says the script handles all cancellation logic and active status is `state.md` in the newest PR-loop directory (`commands/cancel-pr-loop.md:17-25`). The hook-side gate allows state-file renames only when `.cancel-requested` authorizes cancellation: `hooks/loop-bash-validator.sh:139-155` delegates to `is_cancel_authorized`, and `hooks/lib/loop-common.sh:551-703` requires the signal file, rejects command injection/chaining/substitution, enforces an exact `mv` from `state.md` or `finalize-state.md` to `cancel-state.md`, and rejects symlink sources.
- dependencies_and_callers: Called by `/humanize:cancel-pr-loop` via `commands/cancel-pr-loop.md:13-15`. The PR-loop stop hook observes `.cancel-requested` and exits its polling loop at `hooks/pr-loop-stop-hook.sh:941-945`. Monitoring classifies sessions with `cancel-state.md` as cancelled at `scripts/lib/monitor-common.sh:272-275`. Setup/user guidance references cancellation in `scripts/setup-pr-loop.sh:800-801`.
- edge_cases_or_failure_modes: Newest directory selection is lexicographic reverse sort, so it assumes timestamp-like names sort newest-first. If a loop dir exists but lacks `state.md`, cancellation exits `NO_ACTIVE_LOOP` and does not touch final states. `--force` currently has no behavioral effect (`scripts/cancel-pr-loop.sh:24-31` and help text at `scripts/cancel-pr-loop.sh:39-41`). Metadata extraction defaults missing values to `?`, so cancellation still succeeds if state metadata is absent. A failed `touch` or `mv` exits due to `set -euo pipefail`. There is no explicit concurrency lock around another process changing `state.md` between the active check and `mv`.
- validation_or_tests: `tests/test-pr-loop-scripts.sh:172-245` covers help output, no-loop behavior returning code 1 plus `NO_LOOP`, and active-loop cancellation creating `cancel-state.md` while removing `state.md`. Security and hook authorization are covered separately by cancel-signal tests referenced through search, and the core authorization function is implemented in `hooks/lib/loop-common.sh:545-703`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-062 `file` `tests/test-pr-loop-lib.sh`
- cursor: `[_]`
- core_role: Shared executable test harness for PR-loop behavior. It is not production runtime logic, but it is part of the executable specification surface for PR-loop scripts, hooks, and stop-hook behavior.
- algorithmic_behavior: The library is idempotently loaded via `TEST_PR_LOOP_LIB_LOADED` (`tests/test-pr-loop-lib.sh:10-13`). It derives `SCRIPT_DIR` and `PROJECT_ROOT`, then sources `tests/test-helpers.sh` only if helper function `setup_test_dir` is not already defined (`tests/test-pr-loop-lib.sh:14-21`). It defines `create_mock_gh`, a mocked GitHub CLI dispatcher for auth, repo, PR, and API calls (`tests/test-pr-loop-lib.sh:27-91`). It defines `create_mock_codex`, a minimal successful Codex mock (`tests/test-pr-loop-lib.sh:93-104`). `init_pr_loop_test_env` creates the temporary test directory, prepends the mock binary directory to `PATH`, and installs the mocks (`tests/test-pr-loop-lib.sh:110-124`). `print_test_summary` reports pass/fail counts and returns nonzero if any test failed (`tests/test-pr-loop-lib.sh:130-147`).
- inputs_outputs_state: Inputs are sourced shell context, optional preexisting `SCRIPT_DIR`/`PROJECT_ROOT`, and helper counters/functions. Outputs are shell functions, exported `MOCK_BIN_DIR`, modified `PATH`, a temp `TEST_DIR` from `setup_test_dir`, mock executables `gh` and `codex`, and summary stdout. State is held in shell variables and the temporary test filesystem, with cleanup inherited from `setup_test_dir` in `tests/test-helpers.sh:84-89`.
- gates_or_invariants: Idempotent load prevents duplicate definitions. The mock `gh` contract returns stable JSON/text for expected calls: auth status, repo owner/name, PR base repo, PR commit timestamp, PR number, PR open state, current user, and empty comment/review arrays (`tests/test-pr-loop-lib.sh:36-84`). Unhandled mock `gh` calls fail with exit 1 (`tests/test-pr-loop-lib.sh:87-88`), which is a useful invariant because tests must explicitly model new external interactions. `print_test_summary` gates success on `TESTS_FAILED == 0` (`tests/test-pr-loop-lib.sh:140-146`).
- dependencies_and_callers: Main caller is `tests/test-pr-loop.sh`, which sources `tests/test-helpers.sh` and this library at `tests/test-pr-loop.sh:18-20`, calls `init_pr_loop_test_env` at `tests/test-pr-loop.sh:22-27`, then sources PR-loop script/hook/stop-hook test modules and runs them at `tests/test-pr-loop.sh:32-47`. The library depends on helper functions and counters from `tests/test-helpers.sh`, including `pass`, `fail`, `setup_test_dir`, and temp cleanup (`tests/test-helpers.sh:30-89`).
- edge_cases_or_failure_modes: The mock CLI only supports the exact argument shapes encoded in `create_mock_gh`; new production calls to `gh` can break tests with “unhandled command,” which is desirable for surfacing changed external contracts but may require mock maintenance. Because this file prepends `MOCK_BIN_DIR` to `PATH`, tests depend on correct environment isolation and cleanup. It does not itself assert algorithm behavior; it enables the modules that do.
- validation_or_tests: This file is a test dependency rather than the tested subject. Its behavior is exercised indirectly whenever `tests/test-pr-loop.sh` runs script, hook, and stop-hook tests. The cancel-script tests that rely on this setup are in `tests/test-pr-loop-scripts.sh:169-245`.
- skip_candidate: `yes: test harness/support file rather than core runtime algorithm, but it is still relevant as executable specification infrastructure for PR-loop behavior`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-092 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Prompt/block template for the RLCR stop-hook gate that forbids plan-file changes during an active tracked-plan loop. It defines the user-facing block reason when the plan content differs from the startup backup.
- algorithmic_behavior: The template states that `{{PLAN_FILE}}` changed since the RLCR loop started and that plan modifications are forbidden during an active loop (`prompt-template/block/plan-file-modified.md:1-5`). It gives a required recovery sequence: cancel the current loop, update the plan, start a new loop with the same plan path (`prompt-template/block/plan-file-modified.md:7-10`). It exposes the backup path as `{{BACKUP_PATH}}` (`prompt-template/block/plan-file-modified.md:12`).
- inputs_outputs_state: Inputs are template variables `PLAN_FILE` and `BACKUP_PATH`. Output is rendered markdown used as the `reason` field in a JSON block decision. It does not mutate state; it communicates the invariant violation and recovery path.
- gates_or_invariants: The invariant is plan immutability during an active RLCR loop when tracking is enabled. The runtime hook checks uncommitted modifications first and blocks with a direct JSON reason (`hooks/loop-codex-stop-hook.sh:280-290`). It then always compares the active plan path to the backup and, on any diff, renders this template and emits `{"decision":"block"}` with system message `Loop: Blocked - plan file modified` (`hooks/loop-codex-stop-hook.sh:293-312`). This catches both committed and uncommitted content drift relative to loop start.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:307-309`, through `load_and_render_safe` with fallback content at `hooks/loop-codex-stop-hook.sh:295-306`. It depends on the hook having `FULL_PLAN_PATH`, `BACKUP_PLAN`, `PLAN_FILE`, and `TEMPLATE_DIR` available from the active loop state.
- edge_cases_or_failure_modes: If the template file is missing or rendering fails, the hook has a fallback message with equivalent semantics (`hooks/loop-codex-stop-hook.sh:295-306`). The template recovery command is RLCR-specific and points to `/humanize:cancel-rlcr-loop`, so using it in PR-loop contexts would be incorrect; current caller is RLCR stop-hook only. If backup path is unavailable or stale, the message can direct users to an unusable backup, but the block decision still protects plan immutability.
- validation_or_tests: No direct test reference was found for this exact template name. Runtime coverage is through the stop-hook plan diff path at `hooks/loop-codex-stop-hook.sh:293-312`; the fallback mirrors the template, reducing risk if template loading fails.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-122 `file` `prompt-template/pr-loop/critical-requirements-no-comments.md`
- cursor: `[_]`
- core_role: PR-loop prompt template for round 0 when a newly created PR has no existing review comments. It defines the completion contract for the human/agent before the stop hook begins polling for automatic bot reviews.
- algorithmic_behavior: The template requires the worker to write a resolution summary to `{{RESOLVE_PATH}}`, specifically noting “Round 0 awaiting initial bot reviews” and “No issues to address yet” (`prompt-template/pr-loop/critical-requirements-no-comments.md:6-14`). It then instructs the worker to exit so the Stop Hook can poll for bot reviews (`prompt-template/pr-loop/critical-requirements-no-comments.md:15-20`). It explicitly forbids commenting on the PR to trigger review because bots should review automatically for a new PR (`prompt-template/pr-loop/critical-requirements-no-comments.md:17-18`).
- inputs_outputs_state: Input is template variable `RESOLVE_PATH`. Output is appended/rendered critical instruction text in the initial PR-loop prompt. The expected state transition outside the template is creation/update of the resolution summary at that path, followed by process exit so the stop hook can poll review bots. The template itself has no persistent state.
- gates_or_invariants: The key invariant is that no-comments round 0 should not trigger bot review manually with a PR comment. Completion requires a resolution summary even though there are no issues yet, giving the stop hook a local artifact to verify/consume. This keeps round-0 behavior distinct from later rounds with existing comments, where the alternate template tells the worker to commit/push and comment for review.
- dependencies_and_callers: Direct caller is `scripts/setup-pr-loop.sh:810-835`. When `COMMENT_COUNT` is zero, setup defines a fallback with the same content and calls `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/critical-requirements-no-comments.md" ...` at `scripts/setup-pr-loop.sh:812-834`. A search found only this direct render call; the has-comments sibling is selected at `scripts/setup-pr-loop.sh:836-860`.
- edge_cases_or_failure_modes: If `RESOLVE_PATH` is wrong or unresolved, the worker may write the summary to the wrong location or omit it. If bots do not automatically review a new PR, the loop can stall in polling because the template forbids manual trigger comments for this branch of the workflow. If template loading fails, setup uses the fallback content at `scripts/setup-pr-loop.sh:813-833`.
- validation_or_tests: No direct template-specific test reference was found. Its behavior is indirectly exercised through PR-loop setup paths that render initial prompts. The runtime stop-hook polling path acknowledges cancellation with `.cancel-requested` at `hooks/pr-loop-stop-hook.sh:941-945`, but bot polling behavior itself is separate from this template.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-002`, `ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-032`, `ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-062`, `ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-092`, `ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-122`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`