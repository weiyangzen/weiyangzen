# agent_18 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-018 `file` `commands/cancel-pr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for cancelling an active PR loop, not an RLCR loop. The frontmatter restricts allowed execution to `${CLAUDE_PLUGIN_ROOT}/scripts/cancel-pr-loop.sh` with optional `--force` and hides the command from slash-command tooling (`commands/cancel-pr-loop.md:1-5`).
- algorithmic_behavior: The markdown deliberately delegates all cancellation logic to the script and tells the agent to branch only on the first output line: `NO_LOOP` or `NO_ACTIVE_LOOP` maps to “No active PR loop found”; `CANCELLED` maps to reporting the cancellation message (`commands/cancel-pr-loop.md:17-21`). The referenced script finds the newest `.humanize/pr-loop/*/` directory, requires `state.md`, creates `.cancel-requested`, then renames `state.md` to `cancel-state.md` (`scripts/cancel-pr-loop.sh:72-98`, `scripts/cancel-pr-loop.sh:118-130`).
- inputs_outputs_state: Inputs are `CLAUDE_PLUGIN_ROOT` for command resolution and, inside the script, `CLAUDE_PROJECT_DIR` or `pwd` as project root (`scripts/cancel-pr-loop.sh:72-73`). Outputs are first-line status tokens and exit codes: success `0`, no active loop `1`, option/error `3` (`scripts/cancel-pr-loop.sh:11-15`, `scripts/cancel-pr-loop.sh:78-82`, `scripts/cancel-pr-loop.sh:128-132`). State transition is `state.md` -> `cancel-state.md`, plus `.cancel-requested`; loop artifacts remain preserved (`commands/cancel-pr-loop.md:21-23`).
- gates_or_invariants: Active PR loop is defined by `state.md` existing in the newest `.humanize/pr-loop/` directory (`commands/cancel-pr-loop.md:21`; `scripts/cancel-pr-loop.sh:75-96`). The command must not affect `.humanize/rlcr/`; users are directed to `/humanize:cancel-rlcr-loop` for RLCR (`commands/cancel-pr-loop.md:25`). Hook policy supports this invariant by permitting `mv state.md cancel-state.md` only when the cancel signal exists, according to referenced tests and hook comments (`tests/test-cancel-signal-file.sh:6-7`; `hooks/loop-bash-validator.sh:133`).
- dependencies_and_callers: Direct dependency is `scripts/cancel-pr-loop.sh`. README lists `/cancel-pr-loop` as the PR-loop cancellation command (`README.md:95`). PR and RLCR setup scripts refer users to this command when a PR loop blocks startup (`scripts/setup-pr-loop.sh:100`; `scripts/setup-rlcr-loop.sh:230`). Monitoring/session code treats `cancel-state.md` as a stopped/cancelled state rather than active (`scripts/lib/monitor-common.sh:149-150`, `scripts/lib/monitor-common.sh:274`).
- edge_cases_or_failure_modes: No loop directory returns `NO_LOOP`; loop directory without `state.md` returns `NO_ACTIVE_LOOP`; malformed/missing round fields fall back to `?` in the cancellation message (`scripts/cancel-pr-loop.sh:78-98`, `scripts/cancel-pr-loop.sh:104-112`). `--force` is parsed but currently has no additional effect (`scripts/cancel-pr-loop.sh:24-31`, `scripts/cancel-pr-loop.sh:39-41`). Newest directory selection is lexicographic reverse sort, so timestamp naming is an implicit ordering assumption (`scripts/cancel-pr-loop.sh:75-76`).
- validation_or_tests: `tests/test-pr-loop-scripts.sh` verifies help output, no-loop behavior with exit `1` and `NO_LOOP`, and active-loop cancellation that creates `cancel-state.md` and removes `state.md` (`tests/test-pr-loop-scripts.sh:181-244`). `tests/test-state-exit-naming.sh` and PR-loop system tests validate that `cancel-state.md` is not treated as an active loop.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-048 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for portable ANSI escape stripping and pass/fail summary extraction used by the aggregate test runner. It protects core validation accounting from colored test-suite output on GNU/Linux and BSD/macOS sed (`tests/test-ansi-parsing.sh:3-6`).
- algorithmic_behavior: The script defines local `pass`/`fail` counters, creates colored sample outputs, strips ANSI SGR sequences using `esc=$'\033'` plus `sed "s/${esc}\\[[0-9;]*m//g"`, then extracts numeric counts with `grep -oE 'Passed:[[:space:]]*[0-9]+' | grep -oE '[0-9]+$'` and equivalent `Failed` parsing (`tests/test-ansi-parsing.sh:21-31`, `tests/test-ansi-parsing.sh:45-48`, `tests/test-ansi-parsing.sh:74-90`).
- inputs_outputs_state: Inputs are synthetic colored/plain strings; no filesystem state is mutated. Outputs are terminal test reports and process exit `0` when `TESTS_FAILED == 0`, otherwise `1` (`tests/test-ansi-parsing.sh:163-178`). Internal state is `TESTS_PASSED` and `TESTS_FAILED`, incremented only through helper functions.
- gates_or_invariants: Portable escape handling is the invariant: use ANSI-C quoted ESC rather than nonportable sed escape syntax (`tests/test-ansi-parsing.sh:38-40`). The aggregate parser must tolerate colored counts, plain counts, zero counts, multiple colors, bold+color, and multiline output (`tests/test-ansi-parsing.sh:42-158`). For multiline summaries, it takes the last extracted count using `tail -1`, matching the aggregate runner contract (`tests/test-ansi-parsing.sh:123-125`).
- dependencies_and_callers: This test directly mirrors `tests/run-all-tests.sh`, where `test-ansi-parsing.sh` is included in `TEST_SUITES` and the runner uses the same ESC/sed strip plus grep extraction to accumulate totals (`tests/run-all-tests.sh:32-66`, `tests/run-all-tests.sh:114-121`). It depends on POSIX-ish shell utilities: `bash`, `sed`, `grep`, `tail`.
- edge_cases_or_failure_modes: The regex strips SGR sequences matching `ESC[` plus digits/semicolons ending in `m`; it does not aim to strip all ANSI CSI variants. Count extraction defaults to `0` only when grep finds no match due to `|| echo "0"` (`tests/test-ansi-parsing.sh:76`, `tests/test-ansi-parsing.sh:90`). Numeric comparisons in this file are string equality checks, avoiding arithmetic edge cases.
- validation_or_tests: The file is itself the validation: eight scenarios cover basic color removal, multiple SGR spans, passed/failed count extraction, zero failed count, simulated suite output, no ANSI, and bold+color combined (`tests/test-ansi-parsing.sh:42-158`). The aggregate runner consumes this suite as a guard before declaring all tests passed (`tests/run-all-tests.sh:43`, `tests/run-all-tests.sh:141-152`).
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-078 `file` `prompt-template/block/force-push-detected.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the PR-loop force-push transition. It tells the agent/user that review state was reset because commit history was rewritten and requires a new bot trigger comment before continuing (`prompt-template/block/force-push-detected.md:1-17`).
- algorithmic_behavior: Template variables `{{OLD_COMMIT}}`, `{{NEW_COMMIT}}`, and `{{BOT_MENTION_STRING}}` are rendered into a block message that states the SHA changed non-fast-forward, tracking has been updated, and a fresh trigger comment must mention the configured bot(s) (`prompt-template/block/force-push-detected.md:3-15`). The caller detects the condition when stored `latest_commit_sha` differs from `git rev-parse HEAD` and the old SHA is not an ancestor of the current HEAD (`hooks/pr-loop-stop-hook.sh:368-380`).
- inputs_outputs_state: Inputs are old commit SHA from PR-loop state, current HEAD, bot mention string, and PR number passed to `load_and_render_safe` (`hooks/pr-loop-stop-hook.sh:382-417`). On detection, the hook rewrites `state.md` fields: `latest_commit_sha`, `latest_commit_at`, clears `last_trigger_at`, and clears `trigger_comment_id` (`hooks/pr-loop-stop-hook.sh:396-410`). Output is JSON with `decision: "block"` and a system message telling the user to re-trigger bots (`hooks/pr-loop-stop-hook.sh:419-421`).
- gates_or_invariants: The key invariant is that old trigger comments cannot be reused after a force push. The hook updates commit tracking first, then blocks; later trigger detection filters comments to those created after `latest_commit_at` (`hooks/pr-loop-stop-hook.sh:385-397`, `hooks/pr-loop-stop-hook.sh:523-590`, `hooks/pr-loop-stop-hook.sh:631-639`). Template rendering is single-pass and preserves missing placeholders, preventing nested placeholder injection from values (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-129`).
- dependencies_and_callers: Direct caller is `hooks/pr-loop-stop-hook.sh` via `load_and_render_safe "$TEMPLATE_DIR" "block/force-push-detected.md"` with fallback text (`hooks/pr-loop-stop-hook.sh:412-417`). It depends on `scripts/portable-timeout.sh` indirectly through hook `run_with_timeout` git/gh calls and on `hooks/lib/template-loader.sh` safe rendering/fallback behavior (`hooks/lib/template-loader.sh:167-203`).
- edge_cases_or_failure_modes: If the new commit timestamp cannot be fetched from GitHub, the hook falls back to current UTC time, which may be conservative for trigger filtering (`hooks/pr-loop-stop-hook.sh:388-394`). If `git merge-base --is-ancestor` reports ancestor, this is treated as normal new commits rather than force push. If the template is missing or renders empty, `load_and_render_safe` uses the fallback force-push message (`hooks/lib/template-loader.sh:176-203`).
- validation_or_tests: `tests/test-pr-loop-stophook.sh` verifies two important behaviors: old trigger comments before `latest_commit_at` are rejected after a force-push-like state (`tests/test-pr-loop-stophook.sh:20-135`), and a simulated history rewrite where old SHA is not ancestor blocks with force-push/re-trigger messaging (`tests/test-pr-loop-stophook.sh:538-637`). `tests/test-template-references.sh` scans hook template references and validates referenced templates exist (`tests/test-template-references.sh:51-115`).
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-108 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: Claude-facing finalize-phase prompt used when Codex review is skipped. It defines the fallback review-contract transition from implementation/review to finalization under reduced validation confidence (`prompt-template/claude/finalize-phase-skipped-prompt.md:1-6`).
- algorithmic_behavior: The template warns with `{{REVIEW_SKIP_REASON}}`, requires manual verification, optionally suggests the `code-simplifier:code-simplifier` Task agent, constrains any simplification to functionality-equivalent changes, and requires completion of todos, commit, and writing `{{FINALIZE_SUMMARY_FILE}}` (`prompt-template/claude/finalize-phase-skipped-prompt.md:7-50`). It focuses review/simplification on changes between `{{BASE_BRANCH}}` and `{{START_BRANCH}}` (`prompt-template/claude/finalize-phase-skipped-prompt.md:17-20`).
- inputs_outputs_state: Inputs are template variables `REVIEW_SKIP_REASON`, `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE`. The stop hook moves active `STATE_FILE` to `finalize-state.md`, computes `finalize-summary.md`, renders this template when `skip_reason` is non-empty, and later hands the prompt to Claude (`hooks/loop-codex-stop-hook.sh:1002-1041`). Output is a prompt that requires a finalize summary containing work done, modified files, test confirmation if possible, and manual verification notes (`prompt-template/claude/finalize-phase-skipped-prompt.md:40-50`).
- gates_or_invariants: Non-negotiable invariants are no functionality changes, no existing test failures, no new bugs, and only equivalent cleanup/refactoring (`prompt-template/claude/finalize-phase-skipped-prompt.md:26-34`). Because review was skipped, the prompt weakens automated validation confidence but strengthens manual verification instructions before finalizing (`prompt-template/claude/finalize-phase-skipped-prompt.md:7-14`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh` inside finalize prompt generation for non-empty `skip_reason` (`hooks/loop-codex-stop-hook.sh:1008-1041`). Rendering uses `load_and_render_safe`, so a fallback prompt exists inline if the template is missing (`hooks/loop-codex-stop-hook.sh:1008-1035`; `hooks/lib/template-loader.sh:167-203`). It references plan and goal tracker files via `@{{PLAN_FILE}}` and `@{{GOAL_TRACKER_FILE}}` for Claude context (`prompt-template/claude/finalize-phase-skipped-prompt.md:35-39`).
- edge_cases_or_failure_modes: If review is skipped, the implementation “could not be fully validated,” so finalization relies on manual checks and available tests rather than Codex review pass/fail (`prompt-template/claude/finalize-phase-skipped-prompt.md:3-14`). If variables are missing, template-loader leaves placeholders unchanged rather than failing closed (`hooks/lib/template-loader.sh:119-122`). If the template file is absent, the hook’s fallback has shorter but similar constraints (`hooks/loop-codex-stop-hook.sh:1008-1035`).
- validation_or_tests: Template existence/reference coverage is checked by `tests/test-template-references.sh`, which scans `loop-codex-stop-hook.sh` load calls and validates referenced template files (`tests/test-template-references.sh:56-110`). Finalize-phase behavior is covered broadly by `tests/test-finalize-phase.sh`, including state preservation/finalize-state cases visible in related test assertions.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-138 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for portable timeout behavior used throughout PR/RLCR hooks and scripts to bound git, gh, Codex, and other subprocess calls (`tests/robustness/test-timeout-robustness.sh:3-10`).
- algorithmic_behavior: The test sources `scripts/portable-timeout.sh` and `tests/test-helpers.sh`, then validates detection, timeout exit codes, command argument preservation, pipeline/subshell handling, non-existent/empty commands, signal behavior, repeated invocations, large output, and export visibility (`tests/robustness/test-timeout-robustness.sh:14-18`, `tests/robustness/test-timeout-robustness.sh:31-278`). The implementation selects `gtimeout`, GNU `timeout`, `python3`, `python`, or `none` in priority order (`scripts/portable-timeout.sh:9-29`).
- inputs_outputs_state: Inputs are a timeout value plus command/args passed to `run_with_timeout`. Outputs are the wrapped command’s stdout/stderr and exit code; timed-out commands should return `124` when an implementation exists (`tests/robustness/test-timeout-robustness.sh:62-77`). State is `TIMEOUT_IMPL`, exported by the helper for sourcing scripts/subshell visibility (`scripts/portable-timeout.sh:29`, `scripts/portable-timeout.sh:73-76`).
- gates_or_invariants: Valid implementations are limited to `gtimeout|timeout|python3|python|none` (`tests/robustness/test-timeout-robustness.sh:39-49`, `tests/robustness/test-timeout-robustness.sh:235-247`). Quick commands must preserve output and exit `0`; successful nonzero commands must preserve their command exit code, e.g. `42`; actual timeouts should normalize to `124`; command arguments must stay separated and special characters must not be shell-expanded by the wrapper (`tests/robustness/test-timeout-robustness.sh:51-110`, `tests/robustness/test-timeout-robustness.sh:211-219`).
- dependencies_and_callers: Direct dependency is `scripts/portable-timeout.sh`; summary/report helpers come from `tests/test-helpers.sh` (`tests/robustness/test-timeout-robustness.sh:16-18`; `tests/test-helpers.sh:30-78`). Many runtime paths source this timeout helper, including PR reviewer/reaction scripts, RLCR setup, Codex stop hook, and PR stop hook search results. The aggregate runner includes this robustness suite at the end of `TEST_SUITES` (`tests/run-all-tests.sh:55-66`).
- edge_cases_or_failure_modes: If `TIMEOUT_IMPL=none`, timeout-specific tests are skipped/pass as “no timeout implementation,” and commands run unbounded with a warning (`scripts/portable-timeout.sh:64-69`; `tests/robustness/test-timeout-robustness.sh:65-77`, `tests/robustness/test-timeout-robustness.sh:120-155`). Python fallback uses `subprocess.run(sys.argv[1:], timeout=...)`, so it preserves argv boundaries but does not capture output internally; output streams pass through normally (`scripts/portable-timeout.sh:47-62`). Zero timeout is explicitly allowed to vary between success and immediate timeout (`tests/robustness/test-timeout-robustness.sh:139-152`). Empty command is only smoke-tested as “handled,” not asserted to fail (`tests/robustness/test-timeout-robustness.sh:201-210`).
- validation_or_tests: The file itself defines 18 tests and exits through `print_test_summary`, returning nonzero if any assertion failed (`tests/robustness/test-timeout-robustness.sh:31-278`). It is run by `tests/run-all-tests.sh` as `robustness/test-timeout-robustness.sh` (`tests/run-all-tests.sh:55-66`). The helper under test is small and directly mapped: detection at `scripts/portable-timeout.sh:10-27`, execution at `scripts/portable-timeout.sh:33-71`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-018`, `ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-048`, `ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-078`, `ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-108`, `ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-138`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`