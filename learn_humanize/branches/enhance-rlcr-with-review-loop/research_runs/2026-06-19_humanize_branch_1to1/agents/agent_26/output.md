# agent_26 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-026 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Core RLCR Stop hook and state-machine driver. It is registered as a Claude `Stop` hook in `hooks/hooks.json:52-59`, intercepts exit attempts, runs Codex review, blocks continuation when work is incomplete, and advances RLCR session files through normal, review, finalize, and terminal states.
- algorithmic_behavior: The hook reads hook stdin into `HOOK_INPUT` at `hooks/loop-codex-stop-hook.sh:29`, intentionally does not short-circuit on `stop_hook_active` so iterative stop attempts still review (`hooks/loop-codex-stop-hook.sh:31-37`), then finds the newest active loop under `.humanize/rlcr` via `find_active_loop` (`hooks/loop-codex-stop-hook.sh:43-63`, helper at `hooks/lib/loop-common.sh:155-173`). It distinguishes normal `state.md` from `finalize-state.md` (`hooks/loop-codex-stop-hook.sh:72-81`). Before any expensive Codex call, it fail-closes on malformed state, old schemas, wrong branch, plan-file drift, incomplete todos, broken git status, large changed files, dirty/unpushed git state, missing summary, and uninitialized round-0 goal tracker (`hooks/loop-codex-stop-hook.sh:88-707`). In implementation phase it builds either a regular review prompt or a full alignment prompt every fifth round where `CURRENT_ROUND % 5 == 4` (`hooks/loop-codex-stop-hook.sh:747-812`), invokes `codex exec` with configured model/effort and project root (`hooks/loop-codex-stop-hook.sh:857-863`, `1237-1239`), then interprets Codex’s review result by strict last non-empty line matching for `COMPLETE` or `STOP` (`hooks/loop-codex-stop-hook.sh:1354-1361`, markers defined at `hooks/lib/loop-common.sh:29-31`). After implementation `COMPLETE`, it flips `review_started: true`, creates `.review-phase-started`, and runs `codex review --base` against `BASE_COMMIT` when present, otherwise `BASE_BRANCH` (`hooks/loop-codex-stop-hook.sh:887-949`, `1387-1399`). In review phase, every stop attempt re-runs `codex review`; `[P0-9]` findings continue the loop, while no findings enter finalize (`hooks/loop-codex-stop-hook.sh:965-993`, `1412-1434`; issue scanner at `hooks/lib/loop-common.sh:343-386`).
- inputs_outputs_state: Inputs are hook stdin, `CLAUDE_PROJECT_DIR`, `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, `plan.md` backup, the configured plan file, round summary files, `goal-tracker.md`, git repository state, prompt templates, `codex`, `jq`, `python3`, and `scripts/portable-timeout.sh` (`hooks/loop-codex-stop-hook.sh:43-55`, `245-325`, `821-870`). Outputs are Claude hook JSON blocks with `"decision": "block"` for continuation, silent exit 0 when allowing exit, generated review prompts/results under the loop directory, audit command/log files under `XDG_CACHE_HOME` or `$HOME/.cache`, and state file renames. State transitions include `current_round` update via temp file and `mv` (`hooks/loop-codex-stop-hook.sh:1090-1093`, `1471-1474`), `state.md` to `finalize-state.md` (`hooks/loop-codex-stop-hook.sh:1002`), `finalize-state.md` to `complete-state.md` (`hooks/loop-codex-stop-hook.sh:716-721`), and terminal `maxiter`, `stop`, or `unexpected` states through `end_loop` (`hooks/lib/loop-common.sh:1106-1135`).
- gates_or_invariants: Critical frontmatter fields must be physically present, not merely defaulted (`hooks/loop-codex-stop-hook.sh:88-154`). `codex_model` and `codex_effort` must match safe character patterns (`hooks/loop-codex-stop-hook.sh:116-127`). Modern schema requires `plan_tracked`, `start_branch`, valid `review_started`, and `base_branch` (`hooks/loop-codex-stop-hook.sh:161-205`). Branch switching is blocked (`hooks/loop-codex-stop-hook.sh:212-239`). The plan backup must exist and match the original plan (`hooks/loop-codex-stop-hook.sh:245-312`). Incomplete todos block before Codex (`hooks/loop-codex-stop-hook.sh:321-369`). Git status failures block, not bypass (`hooks/loop-codex-stop-hook.sh:379-400`). Changed code/doc files over 2000 lines block (`hooks/loop-codex-stop-hook.sh:409-483`). Dirty git state blocks, and unpushed commits block when `push_every_round` is true (`hooks/loop-codex-stop-hook.sh:493-577`). Finalize completion still requires a finalize summary and all earlier gates (`hooks/loop-codex-stop-hook.sh:585-721`). Review phase cannot be entered by manually toggling state because `.review-phase-started` must exist (`hooks/loop-codex-stop-hook.sh:1412-1428`).
- dependencies_and_callers: Called by Claude hook configuration at `hooks/hooks.json:52-59`. Shared dependencies are `hooks/lib/loop-common.sh` for state parsing, active-loop discovery, markers, issue detection, protected-path messages, and `end_loop`; `hooks/lib/template-loader.sh` for single-pass safe template rendering (`hooks/lib/template-loader.sh:50-131`, `170-203`); `scripts/portable-timeout.sh`; `hooks/check-todos-from-transcript.py`; prompt templates including `prompt-template/codex/full-alignment-review.md`, `prompt-template/codex/regular-review.md`, review/finalize/next-round Claude templates, and block templates. Tests reference this hook in `tests/test-finalize-phase.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-template-references.sh`, and `tests/test-codex-review-merge.sh` according to repository search.
- edge_cases_or_failure_modes: No active loop or missing state allows exit early (`hooks/loop-codex-stop-hook.sh:59-81`). A nonnumeric `current_round` terminates as unexpected, while nonnumeric `max_iterations` falls back to 42 (`hooks/loop-codex-stop-hook.sh:144-154`). `BASE_BRANCH` is required by schema (`hooks/loop-codex-stop-hook.sh:194-205`), making the later “skip code review when no base_branch” path effectively unreachable under current validation (`hooks/loop-codex-stop-hook.sh:1379-1384`). `codex exec` nonzero, missing result file, or empty result file all block and require retry (`hooks/loop-codex-stop-hook.sh:1277-1349`). Review failure or empty review log is a hard block, not a finalize skip (`hooks/loop-codex-stop-hook.sh:972-985`, `1129-1201`). `COMPLETE` is ignored once already in review phase; absence of priority findings controls finalize (`hooks/loop-codex-stop-hook.sh:1360-1366`, `1407-1434`). `STOP` ends the loop as a circuit breaker, with a special message for full-alignment rounds and an “unexpected” warning for other rounds (`hooks/loop-codex-stop-hook.sh:1437-1464`). Some git checks after the branch-consistency gate call plain `git status` without `-C "$PROJECT_ROOT"` (`hooks/loop-codex-stop-hook.sh:382-386`, `554`), so correctness depends on the hook process running in the project root or Git resolving the intended repository.
- validation_or_tests: The hook has direct test coverage through finalize, plan-file, template-reference, and Codex-review merge tests found by repository references. The template reference test scans `hooks/loop-codex-stop-hook.sh` as a template consumer (`tests/test-template-references.sh:56-64`). I did not execute tests because this worker assignment requests read-only research notes only.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-056 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable specification for escape/protection helpers used by RLCR validators, especially preventing `.humanize` loop state from being staged and ensuring shell-safe directory discovery patterns. It is included in the aggregate test runner at `tests/run-all-tests.sh:35-49`.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh` (`tests/test-humanize-escape.sh:16-18`), defines `pass`, `fail`, `assert_blocks`, and `assert_allows` helpers (`tests/test-humanize-escape.sh:28-70`), then calls `git_adds_humanize` with lowercased command strings to assert whether commands should be blocked (`tests/test-humanize-escape.sh:44-69`). It verifies direct `.humanize` path variants, quoted paths, forced adds, all/broad add flags, chained shell commands, and `git -C` or `--git-dir` variants (`tests/test-humanize-escape.sh:77-166`). It also asserts safe allowed cases such as specific source files, `.gitignore`, `git status`, patch mode, and filenames that merely start with `.humanize` (`tests/test-humanize-escape.sh:168-192`). The second half tests find-based iteration patterns for empty directories, dotfiles-only directories, missing `*-state.md`, existing completed state files, and timestamped session directory discovery (`tests/test-humanize-escape.sh:194-332`).
- inputs_outputs_state: Inputs are the local shell, `/tmp` for temporary test directories, `hooks/lib/loop-common.sh`, and the current working directory for cases where `git_adds_humanize` checks whether `.humanize` exists (`hooks/lib/loop-common.sh:999-1016`). Outputs are colored PASS/FAIL lines, aggregate counters, and final exit code 0 or 1 (`tests/test-humanize-escape.sh:20-38`, `334-352`). The only state mutations are temporary `/tmp/test-humanize-*` directories created and removed during specific scenarios (`tests/test-humanize-escape.sh:130-144`, `204-215`, `225-238`, `248-286`, `303-324`).
- gates_or_invariants: Direct references to `.humanize`, `./.humanize`, nested `.humanize`, quoted `.humanize`, and chained `git add` commands must block (`tests/test-humanize-escape.sh:83-100`, `153-166`). Force plus broad scope or all flags must block because force bypasses ignore rules (`tests/test-humanize-escape.sh:109-119`). `git add -A` and `--all` block when `.humanize` exists (`tests/test-humanize-escape.sh:128-140`). Non-add commands mentioning `.humanize`, patch mode, and similarly named non-loop files must remain allowed (`tests/test-humanize-escape.sh:175-192`). Directory enumeration must use `find` loops rather than glob patterns that can fail in zsh on empty matches (`tests/test-humanize-escape.sh:201-213`, `231-236`, `254-259`, `277-284`, `310-322`).
- dependencies_and_callers: Depends on `to_lower` and `git_adds_humanize` from `hooks/lib/loop-common.sh`; `git_adds_humanize` itself splits command segments on shell operators, detects `git ... add`, strips quotes, blocks direct `.humanize` patterns, handles force/all flags, and conditionally checks `git check-ignore` for broad-scope adds (`hooks/lib/loop-common.sh:924-1020`). Called by `tests/run-all-tests.sh:48`.
- edge_cases_or_failure_modes: The test exercises command classification without executing `git add`, so it verifies parser behavior rather than Git’s actual index changes. The `-A/--all` checks require the test to `cd` into a temporary directory containing `.humanize` because the helper only blocks those forms when that directory exists (`tests/test-humanize-escape.sh:128-140`, helper condition at `hooks/lib/loop-common.sh:999-1008`). There is no `trap` cleanup, so an interruption before each explicit `rm -rf` could leave temporary directories. The zsh-safety tests are simulated by `find` loops in bash; they validate the adopted pattern but do not launch zsh itself.
- validation_or_tests: This file is itself the validation artifact. Its summary gate exits success only when `TESTS_FAILED` is zero (`tests/test-humanize-escape.sh:337-352`). I did not run it because it writes temporary directories and the assignment asks for research notes only.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-086 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Block-message template enforcing the RLCR ownership boundary for `goal-tracker.md` after round 0. It converts a forbidden direct modification into a structured request that Codex can review and apply.
- algorithmic_behavior: The template states that after round 0 only Codex can modify the Goal Tracker, and explicitly forbids direct `Write`, `Edit`, or `Bash` changes (`prompt-template/block/goal-tracker-modification.md:1-6`). It tells Claude to add a `Goal Tracker Update Request` section to the current summary file (`prompt-template/block/goal-tracker-modification.md:7-10`) and provides the required structure: requested changes plus justification (`prompt-template/block/goal-tracker-modification.md:12-23`). The final instruction makes Codex the reviewer/gatekeeper for whether requested changes are justified (`prompt-template/block/goal-tracker-modification.md:25`).
- inputs_outputs_state: Inputs are `{{CURRENT_ROUND}}` and `{{SUMMARY_FILE}}`, rendered by `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh:1092-1104`. Output is a human-facing block reason emitted by validators to stderr. The template itself does not mutate state; it preserves the invariant by rerouting state changes through the round summary and later Codex review.
- gates_or_invariants: The invariant is “no direct goal tracker modification after round 0.” Write and Edit validators enforce that for `goal-tracker.md` when `CURRENT_ROUND > 0` (`hooks/loop-write-validator.sh:189-195`, `hooks/loop-edit-validator.sh:141-148`). Bash validator blocks shell attempts to modify `goal-tracker.md`; in round 0 it tells the agent to use Write/Edit, and after round 0 it uses this template to require summary-based requests (`hooks/loop-bash-validator.sh:343-357`). The required request format forces evidence-oriented changes and explicit alignment with the Ultimate Goal (`prompt-template/block/goal-tracker-modification.md:16-22`).
- dependencies_and_callers: Loaded via `load_and_render_safe` from `goal_tracker_blocked_message` (`hooks/lib/loop-common.sh:1094-1104`). The rendering engine is single-pass, so injected placeholder text inside values is not recursively expanded (`hooks/lib/template-loader.sh:50-131`). Called by `loop-write-validator.sh`, `loop-edit-validator.sh`, and `loop-bash-validator.sh` as cited above.
- edge_cases_or_failure_modes: The template is advisory text; actual enforcement comes from validators. Write/Edit are allowed in round 0, while Bash modifications are blocked even in round 0 with a different message, so users see different remediation depending on tool path (`hooks/loop-bash-validator.sh:343-357`). If a caller omitted `SUMMARY_FILE`, the template would keep the placeholder because missing variables are preserved by the template loader (`hooks/lib/template-loader.sh:7-14`, `116-122`), but current callers pass it.
- validation_or_tests: `tests/test-template-references.sh` includes `block/goal-tracker-modification.md` in the common-template existence list (`tests/test-template-references.sh:149-159`). The validator behavior is also structurally covered by Write/Edit/Bash validator tests elsewhere in the suite. I did not run tests for this research pass.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-116 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Mandatory full-goal alignment review prompt used by the stop hook every five implementation rounds. It defines the strongest Codex review contract: compare implementation against the original plan and goal tracker, audit acceptance criteria and deferrals, detect stagnation, and only allow completion under strict conditions.
- algorithmic_behavior: The template tells Codex this is a mandatory checkpoint every five rounds (`prompt-template/codex/full-alignment-review.md:1-3`), requires reading the original plan first (`prompt-template/codex/full-alignment-review.md:5-10`), includes Claude’s summary (`prompt-template/codex/full-alignment-review.md:13-17`), then mandates a goal tracker audit covering every acceptance criterion, forgotten items, deferred items, and a completion summary (`prompt-template/codex/full-alignment-review.md:19-48`). It adds an implementation review for claim verification and gaps (`prompt-template/codex/full-alignment-review.md:50-55`), embeds the goal-tracker update section (`prompt-template/codex/full-alignment-review.md:57`), and requires historical review of recent rounds for stagnation (`prompt-template/codex/full-alignment-review.md:59-84`). Output rules require findings when issues exist or any acceptance criterion is not met, `STOP` as the last line on stagnation, and `COMPLETE` as the last line only when all original plan tasks and all acceptance criteria are fully met with no deferrals (`prompt-template/codex/full-alignment-review.md:86-93`).
- inputs_outputs_state: Inputs are placeholders rendered by `loop-codex-stop-hook.sh`: `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, prior-round numbers, and `REVIEW_RESULT_FILE` (`hooks/loop-codex-stop-hook.sh:782-795`). Output is `round-N-review-prompt.md`, consumed by `codex exec`; Codex is instructed to write findings to `round-N-review-result.md`. State effects happen indirectly when the stop hook reads the review result: last-line `COMPLETE` triggers review/finalize transition, last-line `STOP` terminates with stop state, and ordinary findings produce the next round prompt (`hooks/loop-codex-stop-hook.sh:1351-1464`, `1471-1537`).
- gates_or_invariants: Full alignment runs when `CURRENT_ROUND % 5 == 4` (`hooks/loop-codex-stop-hook.sh:747-750`). Deferred acceptance criteria are considered incomplete (`prompt-template/codex/full-alignment-review.md:88-93`). Stagnation signs include repeated issues, no meaningful acceptance-criteria progress, repeated mistakes, circular discussion, no new code, or repeated Codex feedback not addressed (`prompt-template/codex/full-alignment-review.md:76-83`). The hook requires exact last non-empty line matching, so incidental mentions of completion or stop words inside prose do not trigger transitions (`hooks/loop-codex-stop-hook.sh:1354-1361`, `1437-1464`).
- dependencies_and_callers: Loaded only by `hooks/loop-codex-stop-hook.sh` for full alignment rounds (`hooks/loop-codex-stop-hook.sh:759-795`). Depends on `prompt-template/codex/goal-tracker-update-section.md` being rendered into `GOAL_TRACKER_UPDATE_SECTION` (`hooks/loop-codex-stop-hook.sh:741-745`) and on `hooks/lib/template-loader.sh` for safe single-pass placeholder substitution (`hooks/lib/template-loader.sh:50-131`). It references `.humanize/rlcr/{{LOOP_TIMESTAMP}}/round-*` history files for longitudinal review (`prompt-template/codex/full-alignment-review.md:63-74`).
- edge_cases_or_failure_modes: The template’s guarantees depend on Codex following the output contract; the shell hook enforces only the last-line protocol and subsequent review loop. Historical files may be absent or sparse if earlier rounds failed before writing summaries/results, but the prompt still asks Codex to inspect available history (`prompt-template/codex/full-alignment-review.md:63-74`). Previous-round placeholders are safe for full alignment because the first full alignment round is round 4 under the hook’s modulo rule, making previous-round references nonnegative (`hooks/loop-codex-stop-hook.sh:747-757`). A `STOP` outside full alignment is still honored by the hook, but logged as unexpected (`hooks/loop-codex-stop-hook.sh:1437-1464`).
- validation_or_tests: `hooks/lib/template-loader.sh` documents this exact template as a load example (`hooks/lib/template-loader.sh:33-35`), and `tests/test-template-references.sh` scans `hooks/loop-codex-stop-hook.sh` for template references (`tests/test-template-references.sh:56-70`). I did not run tests during this read-only research assignment.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `4/4 item headings present; each assigned heading appears once`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`