# agent_30 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-030 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Write` hook that protects RLCR and PR loop state-machine files from direct agent writes. It is registered for the `Write` matcher in `hooks/hooks.json:14`.
- algorithmic_behavior: The script reads hook JSON from stdin, validates JSON shape and depth, exits early for non-`Write` tools, then classifies the target `tool_input.file_path` into protected RLCR/PR loop file families. Core control flow is in `hooks/loop-write-validator.sh:25`, `hooks/loop-write-validator.sh:28`, `hooks/loop-write-validator.sh:33`, `hooks/loop-write-validator.sh:39`, and `hooks/loop-write-validator.sh:44`.
- inputs_outputs_state: Input is Claude hook JSON containing `tool_name` and `tool_input.file_path`; missing or malformed input exits `1`. Allowed writes exit `0`. Blocked writes emit a rendered template/fallback to stderr and exit `2`. It discovers state via `CLAUDE_PROJECT_DIR` or `pwd`, `.humanize/rlcr`, `.humanize/pr-loop`, newest active loop directory, `state.md`, and `finalize-state.md` at `hooks/loop-write-validator.sh:56`, `hooks/loop-write-validator.sh:124`, `hooks/loop-write-validator.sh:126`, and `hooks/loop-write-validator.sh:132`.
- gates_or_invariants: JSON must be valid, UTF-8, null-free, include `tool_name`, include non-empty `tool_input.file_path`, and be no deeper than 30 levels via shared helpers in `hooks/lib/loop-common.sh:58`, `hooks/lib/loop-common.sh:100`, and `hooks/lib/loop-common.sh:118`. `round-*-todos.md` writes are blocked unless allowlisted in the active loop at `hooks/loop-write-validator.sh:55`. `round-*-prompt.md` writes are always blocked at `hooks/loop-write-validator.sh:65`. PR loop `state.md` and read-only generated PR files are blocked at `hooks/loop-write-validator.sh:76`, `hooks/loop-write-validator.sh:78`, and `hooks/loop-write-validator.sh:84`. RLCR `state.md` and `finalize-state.md` writes are blocked after strict state parsing at `hooks/loop-write-validator.sh:141`, `hooks/loop-write-validator.sh:152`, and `hooks/loop-write-validator.sh:157`. `goal-tracker.md` is writable only through Round 0 because later writes are redirected to the current summary file at `hooks/loop-write-validator.sh:192`. Summary writes must target the active loop directory and current round at `hooks/loop-write-validator.sh:202`, `hooks/loop-write-validator.sh:228`, and `hooks/loop-write-validator.sh:254`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `hooks/loop-write-validator.sh:18`. Depends on shared path predicates and messages including `is_round_file_type`, `find_active_loop`, `is_allowlisted_file`, `parse_state_file_strict`, `is_in_pr_loop_dir`, `is_pr_loop_readonly_file`, and `validate_pr_resolve_round`; relevant definitions are in `hooks/lib/loop-common.sh:155`, `hooks/lib/loop-common.sh:251`, `hooks/lib/loop-common.sh:395`, `hooks/lib/loop-common.sh:417`, `hooks/lib/loop-common.sh:777`, `hooks/lib/loop-common.sh:821`, and `hooks/lib/loop-common.sh:837`. Uses template loading through `TEMPLATE_DIR` initialized in `hooks/lib/loop-common.sh:138`.
- edge_cases_or_failure_modes: If there is no active RLCR loop, the validator allows RLCR-family writes after `find_active_loop` returns empty at `hooks/loop-write-validator.sh:128`; this avoids stale-loop blocking. Malformed active state fails closed with “Malformed state file” at `hooks/loop-write-validator.sh:141`. In finalize phase, `finalize-state.md` is parsed instead of `state.md`, and `finalize-summary.md` is allowed only in the active loop at `hooks/loop-write-validator.sh:135` and `hooks/loop-write-validator.sh:168`. `plan.md` backup writes inside `.humanize/rlcr` are blocked at `hooks/loop-write-validator.sh:179`. PR resolve summaries are the PR-loop write exception, but only for the current PR round at `hooks/loop-write-validator.sh:89`; wrong round returns `2` in `hooks/lib/loop-common.sh:862`.
- validation_or_tests: `tests/test-plan-file-hooks.sh:200` checks `plan.md` backup write blocking. `tests/test-finalize-phase.sh:345` checks `finalize-summary.md` allowed and `tests/test-finalize-phase.sh:357` checks `finalize-state.md` blocked. `tests/test-pr-loop-hooks.sh:85`, `tests/test-pr-loop-hooks.sh:118`, and `tests/test-pr-loop-hooks.sh:151` check PR loop state/comment protection and `pr-resolve` allowance. Robust hook JSON behavior is covered by `tests/robustness/test-hook-input-robustness.sh` references found for this validator.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-060 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/setup-rlcr-loop.sh` plan-file admission control. It does not implement the production validator, but encodes expected gates for loop startup before RLCR state is created.
- algorithmic_behavior: The script creates disposable git repositories, plan files, `.gitignore` states, branch names, and a mock `codex` binary, then invokes `scripts/setup-rlcr-loop.sh` with varied arguments and asserts exit code plus diagnostic substrings. Test helpers and temp repo setup are at `tests/test-plan-file-validation.sh:32`, `tests/test-plan-file-validation.sh:37`, `tests/test-plan-file-validation.sh:40`, and `tests/test-plan-file-validation.sh:73`.
- inputs_outputs_state: Inputs are CLI invocations of `setup-rlcr-loop.sh` against temporary plan paths and options such as `--plan-file`, `--track-plan-file`, and `--codex-model`. Outputs are colored PASS/FAIL/SKIP lines and final exit status equal to `TESTS_FAILED` at `tests/test-plan-file-validation.sh:743`. State is isolated under `mktemp -d`, cleaned by trap at `tests/test-plan-file-validation.sh:37`, with git commits and plan files created per scenario.
- gates_or_invariants: Path gates require relative paths, existence, parent directory existence, no spaces, no shell metacharacters, no symlink file, no symlink parents, readability, no project escape, and no submodule placement. The production logic asserted by these tests appears in `scripts/setup-rlcr-loop.sh:273`, `scripts/setup-rlcr-loop.sh:279`, `scripts/setup-rlcr-loop.sh:287`, `scripts/setup-rlcr-loop.sh:299`, `scripts/setup-rlcr-loop.sh:305`, `scripts/setup-rlcr-loop.sh:312`, `scripts/setup-rlcr-loop.sh:335`, and `scripts/setup-rlcr-loop.sh:347`. Git gates require a git repo, at least one commit, correct tracked/untracked status depending on `--track-plan-file`, and clean tracked plans at `scripts/setup-rlcr-loop.sh:257`, `scripts/setup-rlcr-loop.sh:263`, `scripts/setup-rlcr-loop.sh:400`, and `scripts/setup-rlcr-loop.sh:416`. Content gates require at least five lines and at least three meaningful non-comment content lines at `scripts/setup-rlcr-loop.sh:431` and `scripts/setup-rlcr-loop.sh:441`.
- dependencies_and_callers: Calls `scripts/setup-rlcr-loop.sh` repeatedly. Requires `git`, shell utilities, and either real `codex` or the temporary mock created at `tests/test-plan-file-validation.sh:73`. It intentionally unsets `CLAUDE_PROJECT_DIR` so production setup uses the current temp repo rather than the plugin repo at `tests/test-plan-file-validation.sh:19`.
- edge_cases_or_failure_modes: Explicitly exercises absolute path rejection at `tests/test-plan-file-validation.sh:89`, missing file and missing directory at `tests/test-plan-file-validation.sh:104` and `tests/test-plan-file-validation.sh:116`, path and filename spaces at `tests/test-plan-file-validation.sh:128` and `tests/test-plan-file-validation.sh:149`, many shell metacharacters at `tests/test-plan-file-validation.sh:169`, symlinks at `tests/test-plan-file-validation.sh:189`, permission/path resolution failure at `tests/test-plan-file-validation.sh:202`, `../` escape at `tests/test-plan-file-validation.sh:245`, non-git and no-commit repos at `tests/test-plan-file-validation.sh:274` and `tests/test-plan-file-validation.sh:299`, tracked/untracked mismatches at `tests/test-plan-file-validation.sh:329`, `tests/test-plan-file-validation.sh:361`, and `tests/test-plan-file-validation.sh:395`, YAML-unsafe branch characters at `tests/test-plan-file-validation.sh:432`, blank/sparse/comment-only content at `tests/test-plan-file-validation.sh:518`, `tests/test-plan-file-validation.sh:546`, `tests/test-plan-file-validation.sh:567`, and `tests/test-plan-file-validation.sh:590`, and single-line HTML comment handling at `tests/test-plan-file-validation.sh:637`.
- validation_or_tests: This file is itself a validation harness. It is included by `tests/run-all-tests.sh` according to the repository search result. The production startup script creates RLCR state and prompt artifacts only after validation passes, copying the plan to `.humanize/rlcr/.../plan.md` at `scripts/setup-rlcr-loop.sh:618`, writing state frontmatter at `scripts/setup-rlcr-loop.sh:628`, initializing goal tracker at `scripts/setup-rlcr-loop.sh:650`, and generating round-0 prompt at `scripts/setup-rlcr-loop.sh:750`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-090 `file` `prompt-template/block/no-trigger-comment.md`
- cursor: `[_]`
- core_role: Blocking reason template for PR loop transitions when the loop requires a fresh trigger comment before collecting or processing reviews.
- algorithmic_behavior: The template renders a human-facing block explaining that the PR loop is waiting for a trigger comment, includes startup-case context, names the required bot mention string, and provides an example comment. Its placeholders are `{{STARTUP_CASE}}`, `{{STARTUP_CASE_DESC}}`, `{{CURRENT_ROUND}}`, and `{{BOT_MENTION_STRING}}` in `prompt-template/block/no-trigger-comment.md:6`, `prompt-template/block/no-trigger-comment.md:7`, `prompt-template/block/no-trigger-comment.md:10`, and `prompt-template/block/no-trigger-comment.md:14`.
- inputs_outputs_state: Inputs are template variables from `hooks/pr-loop-stop-hook.sh` when `REQUIRE_TRIGGER=true` and no `PR_LAST_TRIGGER_AT` exists. Output is markdown used as the `reason` field in a JSON stop-hook block decision at `hooks/pr-loop-stop-hook.sh:729` and `hooks/pr-loop-stop-hook.sh:733`. It does not mutate state itself.
- gates_or_invariants: Enforces the invariant that PR review cycles cannot proceed on stale or absent triggers in cases requiring explicit re-review. The hook computes `REQUIRE_TRIGGER` for round 0 startup cases and later rounds at `hooks/pr-loop-stop-hook.sh:690`, with cases 4/5 requiring a retrigger at `hooks/pr-loop-stop-hook.sh:699`; missing trigger blocks at `hooks/pr-loop-stop-hook.sh:717`.
- dependencies_and_callers: Loaded through `load_and_render_safe` from `hooks/pr-loop-stop-hook.sh:729`. The rendered block depends on bot mention construction in shared logic (`build_bot_mention_string` in `hooks/lib/loop-common.sh:761`) and startup-case descriptions set immediately before rendering at `hooks/pr-loop-stop-hook.sh:718`.
- edge_cases_or_failure_modes: Unknown startup cases default to not requiring a trigger in `hooks/pr-loop-stop-hook.sh:703`, so this template is not rendered in that branch. For startup cases 4 and 5 the description is specific to new commits after previous bot reviews, while subsequent rounds use “Subsequent round requires trigger” at `hooks/pr-loop-stop-hook.sh:720`. If the template file is unavailable, the hook falls back to a shorter inline missing-trigger message at `hooks/pr-loop-stop-hook.sh:726`.
- validation_or_tests: Template presence and references are covered indirectly by template-reference tests discovered for `tests/test-template-references.sh`; runtime behavior is exercised through PR loop hook tests that inspect stop-hook gating. The concrete caller is visible at `hooks/pr-loop-stop-hook.sh:729`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-120 `file` `prompt-template/pr-loop/codex-goal-tracker-update.md`
- cursor: `[_]`
- core_role: PR-loop prompt fragment instructing Codex how to update `goal-tracker.md` after per-bot review analysis.
- algorithmic_behavior: The template tells Codex to append a row per reviewer to the Issue Summary, increment aggregate issue statistics, and add a per-round Issue Log entry. Required update rules are at `prompt-template/pr-loop/codex-goal-tracker-update.md:3`, `prompt-template/pr-loop/codex-goal-tracker-update.md:7`, `prompt-template/pr-loop/codex-goal-tracker-update.md:12`, and `prompt-template/pr-loop/codex-goal-tracker-update.md:17`. It also provides a markdown example at `prompt-template/pr-loop/codex-goal-tracker-update.md:22`.
- inputs_outputs_state: Inputs are `{{GOAL_TRACKER_FILE}}` and `{{NEXT_ROUND}}`, supplied by `hooks/pr-loop-stop-hook.sh:1240`. Output is prompt text embedded into the generated Codex PR review validation prompt at `hooks/pr-loop-stop-hook.sh:1248`. The intended state mutation is a structured edit to `.humanize/pr-loop/.../goal-tracker.md` by Codex after analysis.
- gates_or_invariants: Preserves PR tracker structure by requiring only three sections to change: Issue Summary, Total Statistics, and Issue Log. It explicitly forbids modifying header sections like PR Information and Ultimate Goal, requires appending rather than replacing tables, and requires a separate Issue Summary row per reviewer at `prompt-template/pr-loop/codex-goal-tracker-update.md:57`.
- dependencies_and_callers: Loaded by `hooks/pr-loop-stop-hook.sh` while generating `round-N-codex-prompt.md`; fallback text exists at `hooks/pr-loop-stop-hook.sh:1245`. The shared helper `update_pr_goal_tracker` provides a consistency backstop if Codex does not update correctly, documented in `hooks/lib/loop-common.sh:1145` and `hooks/lib/loop-common.sh:1159`.
- edge_cases_or_failure_modes: The template relies on Codex correctly counting “issues found” versus “issues resolved” and maintaining table formatting. Duplicate or inconsistent rows are a risk if the same prompt is rerun without idempotency checks; the helper path begins with reviewer extraction for idempotency at `hooks/lib/loop-common.sh:1172`. Status values are constrained to “Issues Found”, “All Resolved”, or “Approved” at `prompt-template/pr-loop/codex-goal-tracker-update.md:10`.
- validation_or_tests: Template reference is exercised by comprehensive/template tests found via repository search, and runtime inclusion is anchored at `hooks/pr-loop-stop-hook.sh:1248`. The production helper’s stated update responsibilities mirror the template at `hooks/lib/loop-common.sh:1154`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-030`, `ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-060`, `ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-090`, `ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-120`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`