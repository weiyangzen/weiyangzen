# agent_06 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 5
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-006 `directory` `scripts`
- cursor: `[_]`
- core_role: Runtime shell layer for Humanize loop workflows. The directory contains RLCR setup/cancel/monitoring, PR review-loop setup/cancel/fetch/poll/status/reaction helpers, shared monitor helpers, portable timeout handling, and gen-plan IO validation. It is core because hooks and slash-command wrappers delegate workflow setup, state-file creation, GitHub review polling, and loop cancellation into this tree.
- algorithmic_behavior: `scripts/setup-rlcr-loop.sh` creates RLCR sessions after validating plan path, git state, plan content, Codex configuration, branch safety, and base branch/base commit capture, then writes `.humanize/rlcr/<timestamp>/state.md`, copies `plan.md`, creates `goal-tracker.md`, and emits `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:234`, `scripts/setup-rlcr-loop.sh:273`, `scripts/setup-rlcr-loop.sh:380`, `scripts/setup-rlcr-loop.sh:431`, `scripts/setup-rlcr-loop.sh:537`, `scripts/setup-rlcr-loop.sh:628`, `scripts/setup-rlcr-loop.sh:750`). `scripts/setup-pr-loop.sh` creates PR-loop sessions after bot flag validation, mutual exclusion against RLCR/PR loops, git/gh/codex checks, fork-aware PR/base repo resolution, startup case classification, initial comment fetch, optional re-review trigger, state creation, goal tracker creation, and prompt generation (`scripts/setup-pr-loop.sh:179`, `scripts/setup-pr-loop.sh:222`, `scripts/setup-pr-loop.sh:244`, `scripts/setup-pr-loop.sh:296`, `scripts/setup-pr-loop.sh:418`, `scripts/setup-pr-loop.sh:443`, `scripts/setup-pr-loop.sh:535`, `scripts/setup-pr-loop.sh:632`, `scripts/setup-pr-loop.sh:678`). `scripts/fetch-pr-comments.sh` materializes all PR comments into markdown for prompts; `scripts/poll-pr-reviews.sh` returns JSON deltas for watched bots; `scripts/check-pr-reviewer-status.sh` classifies startup cases 1-5; `scripts/check-bot-reactions.sh` detects Codex `+1` and Claude `eyes`; cancellation scripts move active state to `cancel-state.md`; `scripts/humanize.sh` and `scripts/lib/monitor-common.sh` provide read-only live monitor views.
- inputs_outputs_state: Inputs include CLI args from command wrappers, `CLAUDE_PROJECT_DIR`, git repository state, plan file path/content, GitHub CLI auth/API responses, bot names, Codex model/effort/timeout, templates under `prompt-template`, and hook state under `.humanize`. Outputs include `.humanize/rlcr/<timestamp>/{state.md,plan.md,goal-tracker.md,round-0-prompt.md}`, `.humanize/pr-loop/<timestamp>/{state.md,goal-tracker.md,round-0-pr-comment.md,round-0-prompt.md,round-N-pr-comment.md,round-N-pr-check.md,round-N-pr-feedback.md}`, markdown prompts, JSON hook decisions through hook callers, and terminal monitor output. State transitions are mostly file renames: RLCR cancel uses `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:134`), PR cancel uses `cancel-state.md` (`scripts/cancel-pr-loop.sh:118`), PR stop hook uses `merged-state.md`, `closed-state.md`, `maxiter-state.md`, `approve-state.md`, and `usage-limit-state.md` through sibling hook coordination (`hooks/pr-loop-stop-hook.sh:246`, `hooks/pr-loop-stop-hook.sh:252`, `hooks/pr-loop-stop-hook.sh:432`, `hooks/pr-loop-stop-hook.sh:1355`, `hooks/pr-loop-stop-hook.sh:1391`). Shared `end_loop` maps RLCR exit reasons to `<reason>-state.md` (`hooks/lib/loop-common.sh:1113`).
- gates_or_invariants: Only one RLCR/PR loop may be active (`scripts/setup-rlcr-loop.sh:211`, `scripts/setup-pr-loop.sh:220`). RLCR plan paths must be relative, no spaces/metacharacters, no symlink traversal, within project, not in submodules, and must satisfy tracked/gitignored policy plus minimum meaningful content (`scripts/setup-rlcr-loop.sh:273`, `scripts/setup-rlcr-loop.sh:280`, `scripts/setup-rlcr-loop.sh:289`, `scripts/setup-rlcr-loop.sh:300`, `scripts/setup-rlcr-loop.sh:356`, `scripts/setup-rlcr-loop.sh:361`, `scripts/setup-rlcr-loop.sh:400`, `scripts/setup-rlcr-loop.sh:431`, `scripts/setup-rlcr-loop.sh:479`). YAML-unsafe branch/model/effort values are rejected before state serialization (`scripts/setup-rlcr-loop.sh:504`, `scripts/setup-pr-loop.sh:365`). PR loop requires at least one monitored bot, gh auth, Codex availability, open PR state, and fork-aware base repo resolution (`scripts/setup-pr-loop.sh:179`, `scripts/setup-pr-loop.sh:255`, `scripts/setup-pr-loop.sh:263`, `scripts/setup-pr-loop.sh:271`, `scripts/setup-pr-loop.sh:340`, `scripts/setup-pr-loop.sh:351`). Stop-hook coordination adds dirty worktree/unpushed commit blocks, trigger requirements, force-push trigger invalidation, per-bot timeout, Codex review markers, and active-bot shrinking/re-adding (`hooks/pr-loop-stop-hook.sh:262`, `hooks/pr-loop-stop-hook.sh:283`, `hooks/pr-loop-stop-hook.sh:350`, `hooks/pr-loop-stop-hook.sh:370`, `hooks/pr-loop-stop-hook.sh:684`, `hooks/pr-loop-stop-hook.sh:900`, `hooks/pr-loop-stop-hook.sh:1355`, `hooks/pr-loop-stop-hook.sh:1418`).
- dependencies_and_callers: Slash-command wrappers call scripts directly: `commands/start-rlcr-loop.md` calls `scripts/setup-rlcr-loop.sh`, `commands/start-pr-loop.md` calls `scripts/setup-pr-loop.sh`, `commands/cancel-rlcr-loop.md` calls `scripts/cancel-rlcr-loop.sh`, `commands/cancel-pr-loop.md` calls `scripts/cancel-pr-loop.sh`, and `commands/gen-plan.md` calls `scripts/validate-gen-plan-io.sh`. Runtime hooks call script helpers: `hooks/pr-loop-stop-hook.sh` uses `scripts/poll-pr-reviews.sh`, `scripts/check-bot-reactions.sh`, and `scripts/check-pr-reviewer-status.sh` (`hooks/pr-loop-stop-hook.sh:801`, `hooks/pr-loop-stop-hook.sh:459`, `hooks/pr-loop-stop-hook.sh:1534`). Setup scripts source `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh`; PR setup also sources `hooks/lib/template-loader.sh` (`scripts/setup-rlcr-loop.sh:28`, `scripts/setup-rlcr-loop.sh:205`, `scripts/setup-pr-loop.sh:35`, `scripts/setup-pr-loop.sh:39`, `scripts/setup-pr-loop.sh:211`). `scripts/humanize.sh` sources `scripts/lib/monitor-common.sh` (`scripts/humanize.sh:6`).
- edge_cases_or_failure_modes: Fork PRs are handled by resolving PR base repos before comments/reactions (`scripts/fetch-pr-comments.sh:126`, `scripts/poll-pr-reviews.sh:122`, `scripts/check-pr-reviewer-status.sh:109`, `scripts/check-bot-reactions.sh:119`). GitHub API fetch helpers retry and often continue with empty arrays, meaning degraded data can produce partial evidence rather than hard failure (`scripts/fetch-pr-comments.sh:183`, `scripts/poll-pr-reviews.sh:209`). `setup-pr-loop.sh` refuses to fall back to local time if trigger timestamp lookup fails, avoiding clock-skew filtering bugs (`scripts/setup-pr-loop.sh:479`). PR stop hook clears trigger timestamps after force pushes or new commits so stale trigger comments cannot be reused (`hooks/pr-loop-stop-hook.sh:396`, `hooks/pr-loop-stop-hook.sh:616`). Bash 3.2/macOS compatibility shapes several implementations: no associative arrays in PR stop hook maps, `portable-timeout.sh` picks `gtimeout`, GNU `timeout`, Python, or no timeout (`hooks/pr-loop-stop-hook.sh:876`, `scripts/portable-timeout.sh:10`). Monitor code is observational and can display stale/missing logs but does not mutate workflow state (`scripts/humanize.sh:641`, `scripts/humanize.sh:746`, `scripts/humanize.sh:1215`). `scripts/validate-gen-plan-io.sh` is a gen-plan gate rather than a review-loop state engine, but remains part of the scripts directory’s command workflow surface (`scripts/validate-gen-plan-io.sh:79`, `scripts/validate-gen-plan-io.sh:148`).
- validation_or_tests: `tests/test-pr-loop.sh` is the top-level PR loop test runner and covers script argument validation, hook behavior, and stop-hook behavior (`tests/test-pr-loop.sh:32`, `tests/test-pr-loop.sh:41`, `tests/test-pr-loop.sh:44`, `tests/test-pr-loop.sh:47`). `tests/test-pr-loop-scripts.sh` directly tests setup/cancel/fetch/poll argument gates (`tests/test-pr-loop-scripts.sh:405`). Fixture tests assert `fetch-pr-comments.sh` returns issue comments, inline review comments, approval-only PR reviews, and honors `--after` (`tests/test-pr-loop-hooks.sh:1242`, `tests/test-pr-loop-hooks.sh:1287`). Plan and robustness suites target `setup-rlcr-loop.sh`, timeout, monitor, git/path/state robustness, and gen-plan IO validation, as referenced in `tests/run-all-tests.sh` and discovered test files.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-036 `file` `scripts/fetch-pr-comments.sh`
- cursor: `[_]`
- core_role: PR-loop intake formatter. It fetches GitHub PR issue comments, inline review comments, and PR review summaries, normalizes them into one stream, and writes the markdown comment file consumed by PR loop round prompts and review validation.
- algorithmic_behavior: Parses `<pr_number> <output_file> [--after <timestamp>] [--bots <bot1,bot2>]` (`scripts/fetch-pr-comments.sh:27`). Validates required args and numeric PR number (`scripts/fetch-pr-comments.sh:92`). Requires `gh` and `jq` (`scripts/fetch-pr-comments.sh:112`). Resolves the base repository for fork PR support by trying current repo then parent repo (`scripts/fetch-pr-comments.sh:126`). Creates temp JSON files with cleanup trap (`scripts/fetch-pr-comments.sh:167`). Fetches three endpoints with retry: issue comments, pull inline comments, and pull reviews (`scripts/fetch-pr-comments.sh:183`, `scripts/fetch-pr-comments.sh:211`, `scripts/fetch-pr-comments.sh:215`, `scripts/fetch-pr-comments.sh:219`). Converts each source into a common JSON object shape, including path/line for inline comments and placeholder body for empty PR reviews (`scripts/fetch-pr-comments.sh:259`, `scripts/fetch-pr-comments.sh:280`, `scripts/fetch-pr-comments.sh:302`). Deduplicates with `unique_by(.id)`, optionally filters by timestamp, sorts humans before bots and newest first, then writes markdown grouped into human and bot sections, optionally per active bot (`scripts/fetch-pr-comments.sh:321`, `scripts/fetch-pr-comments.sh:326`, `scripts/fetch-pr-comments.sh:333`, `scripts/fetch-pr-comments.sh:356`, `scripts/fetch-pr-comments.sh:380`).
- inputs_outputs_state: Inputs are PR number, output markdown path, optional ISO timestamp, optional active bot list, current GitHub repo context, and GitHub API responses. Output is a markdown file with header metadata, human comments, bot comments, optional per-bot grouping, and an API failure warning if any endpoint exhausted retries (`scripts/fetch-pr-comments.sh:244`, `scripts/fetch-pr-comments.sh:347`, `scripts/fetch-pr-comments.sh:394`, `scripts/fetch-pr-comments.sh:423`, `scripts/fetch-pr-comments.sh:444`). It does not mutate loop state directly, but its output path is typically `.humanize/pr-loop/<session>/round-0-pr-comment.md`, created by `setup-pr-loop.sh` (`scripts/setup-pr-loop.sh:406`, `scripts/setup-pr-loop.sh:411`).
- gates_or_invariants: Fails closed on missing/invalid args, missing `gh`/`jq`, and unresolved repo owner/name (`scripts/fetch-pr-comments.sh:93`, `scripts/fetch-pr-comments.sh:103`, `scripts/fetch-pr-comments.sh:112`, `scripts/fetch-pr-comments.sh:158`). API endpoint failures are nonfatal after three retries; they increment `API_FAILURES`, write `[]`, and continue to produce a partial comment file (`scripts/fetch-pr-comments.sh:189`, `scripts/fetch-pr-comments.sh:198`, `scripts/fetch-pr-comments.sh:444`). Sort excludes null `created_at` before `fromdateiso8601` to avoid jq timestamp errors (`scripts/fetch-pr-comments.sh:335`).
- dependencies_and_callers: Depends on `gh`, `jq`, `mktemp`, `date`, `sed`, `cat`, `trap`. Called by PR setup to seed round 0 comment context (`scripts/setup-pr-loop.sh:411`). Its output sentinel `*No comments found.*` controls whether setup uses `prompt-template/pr-loop/round-0-task-no-comments.md` (`scripts/setup-pr-loop.sh:643`, `scripts/setup-pr-loop.sh:687`). Fixture tests mock `gh` using `tests/setup-fixture-mock-gh.sh` (`tests/setup-fixture-mock-gh.sh:3`).
- edge_cases_or_failure_modes: Fork PRs are explicitly supported by switching API calls to the PR base repo (`scripts/fetch-pr-comments.sh:126`). Approval-only PR reviews with empty body are preserved as `[Review state: STATE]` instead of being dropped (`scripts/fetch-pr-comments.sh:300`). `--after` uses strict `created_at > after`, while `poll-pr-reviews.sh` uses `>=`, so boundary timestamps are handled differently (`scripts/fetch-pr-comments.sh:327`, `scripts/poll-pr-reviews.sh:299`). Deduplication by raw numeric `id` across all source types can collapse two different comment-source records if GitHub IDs overlap across endpoint domains (`scripts/fetch-pr-comments.sh:321`). `is_bot` and `format_timestamp` are defined but not used in current formatting flow (`scripts/fetch-pr-comments.sh:226`, `scripts/fetch-pr-comments.sh:238`).
- validation_or_tests: `tests/test-pr-loop-scripts.sh` covers help, missing PR number, missing output path, and invalid PR number (`tests/test-pr-loop-scripts.sh:259`). Fixture tests assert all comment types are present, approval-only reviews get placeholders, and `--after` excludes older comments (`tests/test-pr-loop-hooks.sh:1242`, `tests/test-pr-loop-hooks.sh:1277`, `tests/test-pr-loop-hooks.sh:1287`).
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-066 `file` `tests/test-pr-loop.sh`
- cursor: `[_]`
- core_role: Top-level executable specification for the PR loop. It does not implement runtime workflow logic itself; it assembles the PR-loop test environment and dispatches all PR-loop script, hook, and stop-hook test modules.
- algorithmic_behavior: Runs with `set -euo pipefail`, computes `SCRIPT_DIR` and `PROJECT_ROOT`, sources shared test helpers and PR-loop test library, initializes a mock test environment, sources three test modules, executes their runner functions, and prints a summary (`tests/test-pr-loop.sh:13`, `tests/test-pr-loop.sh:15`, `tests/test-pr-loop.sh:19`, `tests/test-pr-loop.sh:26`, `tests/test-pr-loop.sh:32`, `tests/test-pr-loop.sh:41`, `tests/test-pr-loop.sh:44`, `tests/test-pr-loop.sh:47`, `tests/test-pr-loop.sh:53`).
- inputs_outputs_state: Inputs are repository files plus test fixture/mocking utilities; it accepts no command-line arguments. `init_pr_loop_test_env` creates a temporary test dir and mock `gh`/`codex` commands on `PATH` (`tests/test-pr-loop-lib.sh:111`, `tests/test-pr-loop-lib.sh:115`, `tests/test-pr-loop-lib.sh:120`). Outputs are test pass/fail logs and the aggregate test summary (`tests/test-pr-loop-lib.sh:130`). It mutates only test temp directories, not production `.humanize` state unless individual modules deliberately create fixture state under `TEST_DIR`.
- gates_or_invariants: The runner assumes all sourced modules define `run_script_tests`, `run_hook_tests`, and `run_stophook_tests`; `set -euo pipefail` makes missing modules/functions hard failures. It centralizes initialization before sourcing/running modules, so tests share mock `gh` and `codex` behavior (`tests/test-pr-loop.sh:26`, `tests/test-pr-loop-lib.sh:27`, `tests/test-pr-loop-lib.sh:93`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `tests/test-pr-loop-lib.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-stophook.sh` (`tests/test-pr-loop.sh:19`, `tests/test-pr-loop.sh:20`, `tests/test-pr-loop.sh:32`). It is listed in broader `tests/run-all-tests.sh` according to repository search, making it part of the full test suite.
- edge_cases_or_failure_modes: Because it sources modules into one shell, global variables and shell options can leak between test modules. Mock `gh` in `test-pr-loop-lib.sh` returns simplified JSON and empty comment arrays by default, so deeper behavior relies on fixture-backed mocks in hook tests rather than this runner alone (`tests/test-pr-loop-lib.sh:75`, `tests/setup-fixture-mock-gh.sh:74`). Running it in a strict read-only sandbox may fail if helper setup needs writable temp paths.
- validation_or_tests: The runner validates argument parsing for PR scripts (`tests/test-pr-loop-scripts.sh:405`), hook behavior via `run_hook_tests`, and stop-hook state/polling behavior via `run_stophook_tests` (`tests/test-pr-loop.sh:40`, `tests/test-pr-loop.sh:43`, `tests/test-pr-loop.sh:46`). It also indirectly covers `fetch-pr-comments.sh` fixture behavior through sourced hook tests (`tests/test-pr-loop-hooks.sh:1242`).
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-096 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Block-message template for outdated RLCR loop state schema. It communicates that a required state-field is missing and instructs the user to cancel/update/restart rather than continuing with unsafe or ambiguous state.
- algorithmic_behavior: Static markdown template with one variable, `{{FIELD_NAME}}`, used to render the missing state field into the message (`prompt-template/block/schema-outdated.md:3`). `hooks/loop-plan-file-validator.sh` calls `load_and_render_safe` with this template when required schema fields such as `plan_tracked` or `start_branch` are absent (`hooks/loop-plan-file-validator.sh:59`, `hooks/loop-plan-file-validator.sh:65`, `hooks/loop-plan-file-validator.sh:79`).
- inputs_outputs_state: Input is `FIELD_NAME` from the validator. Output is a markdown reason embedded in a hook JSON block decision (`hooks/loop-plan-file-validator.sh:67`, `hooks/loop-plan-file-validator.sh:71`). The template itself does not change state, but it supports the invariant that outdated state should block prompt submission.
- gates_or_invariants: It is part of schema validation for RLCR state. The caller first parses state strictly, then separately checks required fields and blocks with this rendered reason if a field is empty (`hooks/loop-plan-file-validator.sh:44`, `hooks/loop-plan-file-validator.sh:85`). The broader strict parser also requires `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:284`, `hooks/lib/loop-common.sh:293`, `hooks/lib/loop-common.sh:297`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` placeholder rendering via `load_and_render_safe`; the template loader uses single-pass `{{VAR}}` substitution to avoid placeholder injection (`hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:170`). The RLCR stop hook has similar inline schema-outdated blocks for older state files but does not use this specific template for every schema failure (`hooks/loop-codex-stop-hook.sh:157`, `hooks/loop-codex-stop-hook.sh:180`, `hooks/loop-codex-stop-hook.sh:194`).
- edge_cases_or_failure_modes: The template text says version `1.1.2+` and “terminated as unexpected” (`prompt-template/block/schema-outdated.md:9`, `prompt-template/block/schema-outdated.md:12`), but the direct plan-file-validator caller returns a block decision and exits without renaming state (`hooks/loop-plan-file-validator.sh:71`, `hooks/loop-plan-file-validator.sh:87`). For newer required fields such as `review_started` and `base_branch`, other code paths use version `1.5.0+` inline messages, so this template is specifically aligned with earlier schema fields and may read stale if reused generically.
- validation_or_tests: Related tests check missing required state fields and strict parser rejection (`tests/test-plan-file-hooks.sh:124`, `tests/robustness/test-state-file-robustness.sh:201`). Template reference coverage is also present in the repository’s template tests, though this item’s direct runtime caller is the plan-file validator.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-126 `file` `prompt-template/pr-loop/round-0-task-no-comments.md`
- cursor: `[_]`
- core_role: Round-0 PR-loop task template for startup case with no fetched comments. It defines the initial human/Claude task contract when monitored bots are expected to review automatically and no manual trigger comment should be posted.
- algorithmic_behavior: Tells the agent that the PR has no review comments yet, lists active bots via `{{ACTIVE_BOTS_DISPLAY}}`, instructs it to write an initial summary to `@{{RESOLVE_PATH}}`, and tells it not to comment to trigger review (`prompt-template/pr-loop/round-0-task-no-comments.md:6`, `prompt-template/pr-loop/round-0-task-no-comments.md:8`, `prompt-template/pr-loop/round-0-task-no-comments.md:12`, `prompt-template/pr-loop/round-0-task-no-comments.md:20`). It also explains the downstream stop-hook sequence: poll every 30 seconds up to 15 minutes per bot, run local Codex validation after reviews arrive, continue if issues exist, end if all bots approve (`prompt-template/pr-loop/round-0-task-no-comments.md:26`).
- inputs_outputs_state: Inputs are template variables from `setup-pr-loop.sh`: PR metadata, active bot display, resolve path, and bot mention string (`scripts/setup-pr-loop.sh:651`). Output is appended to `.humanize/pr-loop/<timestamp>/round-0-prompt.md` when the fetched comment file contains the no-comments sentinel (`scripts/setup-pr-loop.sh:643`, `scripts/setup-pr-loop.sh:687`, `scripts/setup-pr-loop.sh:720`, `scripts/setup-pr-loop.sh:770`). It does not mutate state itself, but it steers the user toward creating `round-0-pr-resolve.md`, which the stop hook requires before polling (`hooks/pr-loop-stop-hook.sh:262`).
- gates_or_invariants: Enforces the round-0/no-comments invariant: do not post an `@bot` trigger because first reviews are automatic, do not edit `.humanize/pr-loop` state files, and let the stop hook own polling/Codex validation (`prompt-template/pr-loop/round-0-task-no-comments.md:20`, `prompt-template/pr-loop/round-0-task-no-comments.md:21`, `prompt-template/pr-loop/round-0-task-no-comments.md:22`). The stop hook mirrors this: startup case 1 at round 0 does not require trigger, uses `started_at` for polling, skips Claude eyes verification unless a trigger is required, and may treat Codex `+1` reaction as approval (`hooks/pr-loop-stop-hook.sh:677`, `hooks/pr-loop-stop-hook.sh:693`, `hooks/pr-loop-stop-hook.sh:818`, `hooks/pr-loop-stop-hook.sh:788`, `hooks/pr-loop-stop-hook.sh:442`).
- dependencies_and_callers: Loaded by `setup-pr-loop.sh` through `load_and_render_safe` from `hooks/lib/template-loader.sh` (`scripts/setup-pr-loop.sh:720`). Its complementary critical-requirements template is `prompt-template/pr-loop/critical-requirements-no-comments.md`, emitted immediately after the prompt in the same branch (`scripts/setup-pr-loop.sh:812`, `scripts/setup-pr-loop.sh:834`). Runtime behavior is enforced later by `hooks/pr-loop-stop-hook.sh` and helper scripts `poll-pr-reviews.sh` and `check-bot-reactions.sh`.
- edge_cases_or_failure_modes: Template selection is based on `fetch-pr-comments.sh` writing exactly `*No comments found.*`; any existing comment, including non-bot/human-only comments, switches setup into the “has comments” task path (`scripts/setup-pr-loop.sh:643`, `scripts/setup-pr-loop.sh:687`). If no initial summary is written, the stop hook blocks before polling (`hooks/pr-loop-stop-hook.sh:264`). If bots never respond, stop-hook timeout behavior may remove timed-out bots from `active_bots` or block with bot-timeout guidance depending remaining bot state (`hooks/pr-loop-stop-hook.sh:1057`, `hooks/pr-loop-stop-hook.sh:1080`, `hooks/pr-loop-stop-hook.sh:1144`). If Codex approves by reaction instead of comment, the stop hook can complete or shrink `active_bots` without requiring new comments (`hooks/pr-loop-stop-hook.sh:455`, `hooks/pr-loop-stop-hook.sh:974`).
- validation_or_tests: Stop-hook tests create startup case 1/no-comments state and exercise the transition when comments later arrive (`tests/test-pr-loop-stophook.sh:327`). Monitor/system tests assert startup case 1 displays a waiting phase for initial review (`tests/test-pr-loop-system.sh:1014`). Template reference tests exist in the suite and `setup-pr-loop.sh` fallback text mirrors this template, reducing missing-template risk (`scripts/setup-pr-loop.sh:689`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 5 unique assigned item sections present under Item Evidence; each assigned item is represented once as its own heading.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`