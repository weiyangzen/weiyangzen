# agent_20 enhance-rlcr-with-review-loop 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `dd6c37c45c4836773497f878b8925057e9f5318c`

## Item Evidence

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-020 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Defines the `/humanize:gen-plan` workflow that converts a user draft into a structured implementation plan. It is a command-level algorithm spec, not a shell implementation: it orders IO validation, relevance screening, analysis, user clarification, plan synthesis, file update, review, language handling, and reporting.
- algorithmic_behavior: The workflow is explicitly phased: IO validation at `commands/gen-plan.md:30`, repository relevance check at `commands/gen-plan.md:50`, draft analysis at `commands/gen-plan.md:76`, issue resolution at `commands/gen-plan.md:111`, plan generation at `commands/gen-plan.md:142`, and write/complete handling at `commands/gen-plan.md:251`. It delegates path validation and initial output seeding to `"${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh" $ARGUMENTS` at `commands/gen-plan.md:32-36`. It then invokes `humanize:draft-relevance-checker` via Task/haiku and expects `RELEVANT:` or `NOT_RELEVANT:` output at `commands/gen-plan.md:56-65`.
- inputs_outputs_state: Inputs are `$ARGUMENTS` with `--input <draft>` and `--output <plan>` from `commands/gen-plan.md:3`, draft file content, repository context, Explore-agent findings, and AskUserQuestion clarifications. The main output is an edited plan file containing both the structured plan and preserved original draft; Phase 6 says the output already contains template plus original draft from validation and must be updated with Edit while keeping the draft intact at `commands/gen-plan.md:253-260`. Final reporting outputs path, summary, AC count, and language-unification result at `commands/gen-plan.md:286-292`.
- gates_or_invariants: IO validation is a hard entry gate with exit-code mapping 0-7 at `commands/gen-plan.md:38-47`; non-relevant drafts stop the command at `commands/gen-plan.md:67-72`. Significant analysis issues must be clarified or acknowledged before plan generation at `commands/gen-plan.md:117-124`. Quantitative metrics must be classified as hard requirements vs optimization trends at `commands/gen-plan.md:126-138`. The generated plan must use AC-X format, positive and negative tests per AC, no time estimates, no line numbers, affirmative path boundaries, deterministic-design narrowing, and code-style instructions that implementation code/comments avoid plan workflow terms at `commands/gen-plan.md:225-247`.
- dependencies_and_callers: Depends on `scripts/validate-gen-plan-io.sh`, whose validation/copy behavior checks input existence, non-empty input, output directory existence, output non-existence, write permission, template existence, then copies `prompt-template/plan/gen-plan-template.md` and appends the original draft at `scripts/validate-gen-plan-io.sh:79-160`. Depends on `agents/draft-relevance-checker.md`, which defines the haiku relevance checker and RELEVANT/NOT_RELEVANT contract at `agents/draft-relevance-checker.md:1-6` and `agents/draft-relevance-checker.md:27-36`. Also depends on `prompt-template/plan/gen-plan-template.md`, matching the required plan sections at `prompt-template/plan/gen-plan-template.md:1-74`.
- edge_cases_or_failure_modes: Missing input, empty input, missing output directory, output already exists or is a directory, unwritable output dir, invalid args, and missing template each stop before analysis. Semantically unrelated drafts stop after relevance check. Mixed-language generated plans trigger an AskUserQuestion branch at `commands/gen-plan.md:272-284`. Deterministic designs are handled by converging upper/lower path bounds and fixed allowed choices. A key invariant is that clarifications supplement rather than replace original draft content; Phase 4 and Generation Rule 11 require the final plan to be a superset of draft plus clarified details at `commands/gen-plan.md:113-114` and `commands/gen-plan.md:247`.
- validation_or_tests: `tests/test-gen-plan.sh` validates the command file exists, has description, allowed tools, and argument hint at `tests/test-gen-plan.sh:54-104`; validates the relevance agent file/name/model/tools at `tests/test-gen-plan.sh:107-157`; and tests `validate-gen-plan-io.sh` exit behavior for missing values, unknown flags, missing/empty input, missing output dir, existing output, output directory path, valid paths, and help at `tests/test-gen-plan.sh:426-541`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-050 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable regression specification for the RLCR cancellation authorization path. It defines the expected security contract between `scripts/cancel-rlcr-loop.sh` creating `.cancel-requested`, `hooks/loop-bash-validator.sh` protecting `state.md`, and `hooks/lib/loop-common.sh:is_cancel_authorized` allowing only the intended state-file rename.
- algorithmic_behavior: The test builds a temporary active RLCR loop under `.humanize/rlcr/2024-01-01_12-00-00`, writes a minimal `state.md` frontmatter, exports `CLAUDE_PROJECT_DIR`, and feeds Bash hook JSON to `hooks/loop-bash-validator.sh` through `run_bash_validator` at `tests/test-cancel-signal-file.sh:42-73`. The core positive behavior is: with `$LOOP_DIR/.cancel-requested`, `mv $LOOP_DIR/state.md $LOOP_DIR/cancel-state.md` exits 0 at `tests/test-cancel-signal-file.sh:80-94`; variants with `/./` in the source path and single-quoted paths are also allowed at `tests/test-cancel-signal-file.sh:100-115` and `tests/test-cancel-signal-file.sh:365-380`.
- inputs_outputs_state: Inputs are candidate Bash command strings, hook JSON of shape `{"tool_name":"Bash","tool_input":{"command":...}}`, an active loop state file, and optional `.cancel-requested`. Outputs are validator exit codes and pass/fail counters; the script exits with `$TESTS_FAILED` at `tests/test-cancel-signal-file.sh:1349-1357`. State transitions are test-local: each case resets the temp `.humanize` tree, recreates `state.md`, optionally touches `.cancel-requested`, then validates the command. The validator is tested, not the actual execution of the accepted `mv`.
- gates_or_invariants: The invariant is narrow authorization: the signal file must be in the active loop directory, and even with that signal only a plain `mv` from active `state.md` to active `cancel-state.md` may pass. The test expects exit 2 for no signal at `tests/test-cancel-signal-file.sh:121-135`, signal in wrong directory at `tests/test-cancel-signal-file.sh:201-217`, wrong destination at `tests/test-cancel-signal-file.sh:181-195`, deletion/edit/write attempts at `tests/test-cancel-signal-file.sh:141-175` and `tests/test-cancel-signal-file.sh:223-237`, arbitrary destination bypass at `tests/test-cancel-signal-file.sh:283-297`, and unrelated `/tmp/state.md` at `tests/test-cancel-signal-file.sh:1212-1227`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `tests/test-cancel-signal-file.sh:32-33`, using `is_cancel_authorized` and `to_lower` directly in helper tests. Calls `hooks/loop-bash-validator.sh` via JSON at `tests/test-cancel-signal-file.sh:61-73`. The hook implementation finds active loops and exits 0 when none exists at `hooks/loop-bash-validator.sh:54-67`, strictly parses active state at `hooks/loop-bash-validator.sh:74-85`, and treats `is_cancel_authorized` as the sole exception before blocking state-file modifications at `hooks/loop-bash-validator.sh:139-156` and source-side mv/cp protection at `hooks/loop-bash-validator.sh:286-303`. The helper itself requires `.cancel-requested`, rejects substitution/operators/newlines/hidden variables, normalizes `$loop_dir`, enforces two args, validates source/dest, and rejects symlink sources at `hooks/lib/loop-common.sh:527-704`.
- edge_cases_or_failure_modes: The suite covers shell injection via `&&`, `;`, `$()`, backticks, literal newlines, `||`, pipes, `|&`, background `&`, shell wrappers, command prefixes (`sudo`, `env`, `command --`), mv/cp options and extra args, hidden variables like `${IFS}`, leading whitespace, redirection prefixes including append, fd, `&>`, `&>>`, quoted targets, escaped-space targets, and ANSI-C quoted targets across `tests/test-cancel-signal-file.sh:240-1206`. It also verifies literal `${loop_dir}` syntax normalization at `tests/test-cancel-signal-file.sh:443-486` and that no active loop means commands are allowed at `tests/test-cancel-signal-file.sh:1230-1249`.
- validation_or_tests: This file is itself the validation artifact. It includes direct helper assertions that `is_cancel_authorized` returns true only for signal plus correct command and false for missing signal, wrong command, three args, extra arg before source, and hidden variables at `tests/test-cancel-signal-file.sh:1252-1343`. Related cancellation entrypoint behavior is in `scripts/cancel-rlcr-loop.sh`, which creates `.cancel-requested` and renames `state.md` or `finalize-state.md` to `cancel-state.md` at `scripts/cancel-rlcr-loop.sh:134-139`; `commands/cancel-rlcr-loop.md` documents that the script owns cancellation logic and active-loop detection at `commands/cancel-rlcr-loop.md:36-38`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-080 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Small prompt block used by the RLCR stop hook’s git-clean gate when `.humanize*` appears as untracked local state. Its algorithmic role is not computation; it is a human-facing remediation contract that tells the worker how to keep loop artifacts out of commits.
- algorithmic_behavior: The template emits a special-case note that `.humanize/` is created by `humanize:start-rlcr-loop` and should not be committed, then gives the exact `.gitignore` remediation commands at `prompt-template/block/git-not-clean-humanize-local.md:2-8`. The stop hook loads this template when cached `git status` has untracked paths matching `.humanize` at `hooks/loop-codex-stop-hook.sh:501-510`, appending it to `SPECIAL_NOTES` for the git-not-clean block.
- inputs_outputs_state: Input is not a template variable but the stop hook’s detection state: `UNTRACKED=$(echo "$GIT_STATUS_CACHED" | grep '^??')` and `grep -q '\.humanize'` at `hooks/loop-codex-stop-hook.sh:501-505`. Output is extra prompt text in the block reason when uncommitted changes prevent review. The intended repository state transition is `.humanize*` added to `.gitignore`, then `.gitignore` staged, while `.humanize/rlcr/...` runtime state remains untracked and uncommitted.
- gates_or_invariants: The invariant is that RLCR runtime state under `.humanize/` must remain local. This template participates in the pre-review cleanliness gate: when any uncommitted changes exist, the stop hook blocks before expensive Codex review at `hooks/loop-codex-stop-hook.sh:486-527`. If the special template is missing, the hook falls back to a shorter note at `hooks/loop-codex-stop-hook.sh:506-509`.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh` through `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` at `hooks/loop-codex-stop-hook.sh:506`. Loading semantics come from `hooks/lib/template-loader.sh`, where `load_template` returns file contents or empty string on missing file at `hooks/lib/template-loader.sh:33-48`.
- edge_cases_or_failure_modes: It only addresses untracked `.humanize*`; it does not handle tracked or modified `.humanize` files, nor other untracked artifacts. The suggested `echo '.humanize*' >> .gitignore` can duplicate an existing ignore rule if the user has already added one manually. Because the template has no placeholders, render failure risk is low; missing file degrades to the fallback note rather than an empty block.
- validation_or_tests: Template reference coverage is enforced by `tests/test-template-references.sh`, which scans hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references and fails missing template paths at `tests/test-template-references.sh:56-110`. The same test also inventories all template files and warns on unreferenced ones at `tests/test-template-references.sh:121-140`.
- skip_candidate: `no`

### ENHANCE_RLCR_WITH_REVIEW_LOOP-HZ-110 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Footer template appended to each next-round Claude prompt when Codex review finds issues and the RLCR loop continues. It defines the worker-side contract for completing the round: do not escape the loop improperly, optionally run code simplification, commit, and write the required summary.
- algorithmic_behavior: The template warns that the worker must not exit the RLCR loop by lying, editing loop state, or executing `cancel-rlcr-loop` at `prompt-template/claude/next-round-footer.md:4`. It then orders post-work actions: optional `code-simplifier` review if installed, commit with descriptive message, and write the work summary into `@{{NEXT_SUMMARY_FILE}}` at `prompt-template/claude/next-round-footer.md:6-9`.
- inputs_outputs_state: Input is the `NEXT_SUMMARY_FILE` placeholder. The stop hook sets `NEXT_PROMPT_FILE` and `NEXT_SUMMARY_FILE` after incrementing `current_round` at `hooks/loop-codex-stop-hook.sh:1471-1478`, then renders the footer with `NEXT_SUMMARY_FILE=$NEXT_SUMMARY_FILE` at `hooks/loop-codex-stop-hook.sh:1502-1506`. Output is appended prompt content in `round-${NEXT_ROUND}-prompt.md`; expected subsequent state is a committed worktree and `round-${NEXT_ROUND}-summary.md` written by the worker.
- gates_or_invariants: The key invariant is loop integrity: workers may not self-cancel, falsify completion, or mutate loop state to escape review. The footer reinforces other hook-level protections that block direct state file modification; for example, the bash validator permits only authorized cancel state renames and otherwise blocks `state.md`/`finalize-state.md` writes at `hooks/loop-bash-validator.sh:139-156`. It also makes summary writing part of the next-round contract, complementing validators that protect summary paths from Bash writes and require proper tool usage at `hooks/loop-bash-validator.sh:370-379`.
- dependencies_and_callers: Caller is `hooks/loop-codex-stop-hook.sh` in the “Review Found Issues - Continue Loop” branch. The stop hook first updates `current_round`, builds the next prompt from `claude/next-round-prompt.md`, optionally appends post-alignment instructions, then appends this footer at `hooks/loop-codex-stop-hook.sh:1467-1506`. Rendering behavior is `load_and_render_safe`, which substitutes placeholders single-pass and uses a fallback if the template is missing at `hooks/lib/template-loader.sh:167-203`.
- edge_cases_or_failure_modes: If `code-simplifier` is not installed, action 0 is explicitly conditional and should be skipped. If the template is missing, the fallback only says to commit and write the summary, so the anti-cancel warning and code-simplifier suggestion would be absent at `hooks/loop-codex-stop-hook.sh:1502-1506`. If `NEXT_SUMMARY_FILE` is not supplied, the loader preserves unresolved placeholders by design at `hooks/lib/template-loader.sh:7-14`, making the prompt visibly incomplete rather than silently corrupting content.
- validation_or_tests: Template existence/reference behavior is covered by `tests/test-template-references.sh`, which scans hook template references and fails missing files at `tests/test-template-references.sh:56-110`. Template-loader behavior, including single-pass placeholder substitution and safe fallback semantics, is defined in `hooks/lib/template-loader.sh:50-131` and `hooks/lib/template-loader.sh:167-203`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item sections present, one section per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`