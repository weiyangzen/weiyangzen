# agent_27 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`
- note: inspected assigned files directly. This branch export did not behave as a git checkout under the sandbox (`git rev-parse` reported not a git repository), so validation notes are based on static inspection and existing test coverage references, not newly executed tests.

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-027 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: User-facing shell utility for the Humanize RLCR workflow. It exposes `humanize monitor rlcr-loop`, plus reusable parsing helpers used by the monitor: pipe splitting, goal tracker summarization, git state detection, and git status summarization (`scripts/humanize.sh:12`, `scripts/humanize.sh:26`, `scripts/humanize.sh:110`, `scripts/humanize.sh:160`, `scripts/humanize.sh:871`).
- algorithmic_behavior: `humanize_parse_goal_tracker` parses `goal-tracker.md` sections into a pipe-delimited status tuple: total ACs, completed ACs, active/completed/deferred tasks, open issues, and goal summary (`scripts/humanize.sh:24-105`). `_humanize_monitor_codex` discovers the latest `.humanize/rlcr/<timestamp>` session, maps it to cached Codex logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/<timestamp>`, draws a fixed terminal status bar, tails the latest log incrementally, and switches when newer sessions/logs appear (`scripts/humanize.sh:213-868`).
- inputs_outputs_state: Inputs include current working directory, `.humanize/rlcr`, session `state.md` or `*-state.md`, session `goal-tracker.md`, git porcelain/diff output, terminal dimensions via `tput`, and cached `round-*-codex-run.log` files (`scripts/humanize.sh:218`, `scripts/humanize.sh:262-313`, `scripts/humanize.sh:321-373`, `scripts/humanize.sh:388-427`). Outputs are stdout terminal UI/log stream, return code `1` for missing loop/session, and no repository writes. Runtime state is held in locals such as `current_session_dir`, `current_file`, `current_loop_status`, `last_size`, `last_no_log_status`, `monitor_running`, and `cleanup_done` (`scripts/humanize.sh:538-610`).
- gates_or_invariants: Requires `.humanize/rlcr` before monitor startup and at each polling step; absence triggers an error before startup or graceful stop while running (`scripts/humanize.sh:224-229`, `scripts/humanize.sh:614-618`). Session directories must match timestamp naming before being considered (`scripts/humanize.sh:245-248`, `scripts/humanize.sh:282-285`). `state.md` wins over `*-state.md`, with missing state producing `unknown` status (`scripts/humanize.sh:328-354`). The CLI dispatch only accepts `monitor rlcr-loop` (`scripts/humanize.sh:875-900`).
- dependencies_and_callers: Depends on shell builtins plus `sed`, `grep`, `sort`, `wc`, `tr`, `find`, `basename`, `pwd`, `stat`, `tail`, `tput`, `clear`, and `git`. Designed for bash and zsh; zsh compatibility is explicit through `setopt localoptions ksharrays` and `find`-based discovery to avoid glob errors (`scripts/humanize.sh:15-21`, `scripts/humanize.sh:216`, `scripts/humanize.sh:239-251`). Referenced by README/command usage and tested directly by monitor and robustness tests.
- edge_cases_or_failure_modes: Missing tracker returns zeros and “No goal tracker” (`scripts/humanize.sh:28-30`). Non-repo git status returns zero counts plus `not a git repo` (`scripts/humanize.sh:161-164`). Git-state detection distinguishes not-a-repo, permission error, rebase, merge, shallow clone, detached HEAD, and normal (`scripts/humanize.sh:110-156`). Monitor handles no log yet, deleted loop dir, deleted session/log, newer session/log, and log truncation/rotation (`scripts/humanize.sh:633-732`, `scripts/humanize.sh:747-861`). Possible parsing fragility: pipe-delimited helper output can be confused if `goal_summary` itself contains `|`, and active/completed counts depend on expected markdown headings/table shapes.
- validation_or_tests: Existing tests cover zsh no-glob safety (`tests/test-zsh-monitor-safety.sh:3-9`), real monitor behavior on `.humanize/rlcr` deletion in bash/zsh (`tests/test-monitor-e2e-real.sh:3-13`), and production goal tracker parsing robustness (`tests/robustness/test-goal-tracker-robustness.sh:3-11`). I did not execute tests in this read-only export.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-057 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Specialized blocking-message fragment for the git-clean gate when untracked `.humanize*` artifacts are detected. It teaches the active agent that `.humanize/` is loop-local state and must not be committed (`prompt-template/block/git-not-clean-humanize-local.md:2-8`).
- algorithmic_behavior: This file is not executable code, but it alters the stop-hook decision surface by appending a targeted remediation note to the broader “git not clean” block. `hooks/loop-codex-stop-hook.sh` loads it when cached porcelain status has untracked entries matching `.humanize`; if missing, it falls back to a generic note (`hooks/loop-codex-stop-hook.sh:437-451`).
- inputs_outputs_state: Input is template-loader file content plus git porcelain lines beginning with `??` that include `.humanize` (`hooks/loop-codex-stop-hook.sh:441-446`). Output is markdown inserted into `SPECIAL_NOTES`, then rendered into the JSON block reason for the hook decision (`hooks/loop-codex-stop-hook.sh:450`, `hooks/loop-codex-stop-hook.sh:473-484`). It does not mutate state directly.
- gates_or_invariants: Reinforces invariant that `.humanize/` is generated by `humanize:start-rlcr-loop` and should be ignored, not committed (`prompt-template/block/git-not-clean-humanize-local.md:2-4`). It is advisory inside a hard gate: the actual block is triggered by any uncommitted git status, and the note only specializes remediation (`hooks/loop-codex-stop-hook.sh:464-486`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh` through `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` (`hooks/loop-codex-stop-hook.sh:446`). Loading depends on `hooks/lib/template-loader.sh`, where missing templates yield empty content and safe render paths use fallbacks (`hooks/lib/template-loader.sh:36-47`, `hooks/lib/template-loader.sh:170-203`).
- edge_cases_or_failure_modes: Pattern is broad: any untracked path containing `.humanize` triggers this note, including legacy `.humanize-*` dirs per caller comment (`hooks/loop-codex-stop-hook.sh:444-445`). The suggested `echo '.humanize*' >> .gitignore` can duplicate an existing ignore rule and does not stage removals if `.humanize` was already tracked; still, the parent gate blocks until the repository is clean.
- validation_or_tests: Template existence/reference validation scans shell loader calls and checks all referenced templates exist (`tests/test-template-references.sh:52-115`). The same test also checks template directory completeness and critical safe rendering usage (`tests/test-template-references.sh:117-230`). I did not execute tests in this export.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-087 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Mandatory every-fifth-round Codex review prompt for full goal alignment in the RLCR loop. It defines the audit contract that decides whether the loop continues, enters finalize via `COMPLETE`, or trips the stagnation circuit breaker via `STOP` (`prompt-template/codex/full-alignment-review.md:1-3`, `prompt-template/codex/full-alignment-review.md:84-93`).
- algorithmic_behavior: The prompt forces Codex to read the original implementation plan first, audit every immutable acceptance criterion, detect forgotten tasks, reassess deferred items, summarize remaining work, review implementation claims against reality, inspect historical round files for stagnation, and write a review result file with a strict final marker (`prompt-template/codex/full-alignment-review.md:5-11`, `prompt-template/codex/full-alignment-review.md:19-58`, `prompt-template/codex/full-alignment-review.md:59-93`).
- inputs_outputs_state: Rendered variables include `CURRENT_ROUND`, `PLAN_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, `PREV_ROUND`, `PREV_PREV_ROUND`, and `REVIEW_RESULT_FILE` (`hooks/loop-codex-stop-hook.sh:721-734`). Input artifacts named in the prompt are the plan, goal tracker, Claude summary, docs path, and recent `.humanize/rlcr/<timestamp>/round-*` history (`prompt-template/codex/full-alignment-review.md:7-16`, `prompt-template/codex/full-alignment-review.md:61-74`). Output is a review result at `@{{REVIEW_RESULT_FILE}}` whose last non-empty line drives state transitions.
- gates_or_invariants: Full alignment runs when `CURRENT_ROUND % 5 == 4` (`hooks/loop-codex-stop-hook.sh:686-690`). `COMPLETE` is only allowed when all original plan tasks are done, all ACs are fully met, and no deferrals remain (`prompt-template/codex/full-alignment-review.md:88-93`). Deferred items are explicitly incomplete (`prompt-template/codex/full-alignment-review.md:91-93`). `STOP` must be a single final line only when stagnation is detected (`prompt-template/codex/full-alignment-review.md:76-84`).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` into `round-N-review-prompt.md`; fallback text is used if the template is missing or empty (`hooks/loop-codex-stop-hook.sh:698-734`). Rendering is single-pass through `hooks/lib/template-loader.sh`, so placeholders inside injected values are preserved rather than recursively expanded (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-131`).
- edge_cases_or_failure_modes: Historical paths use `.humanize/rlcr/{{LOOP_TIMESTAMP}}/...`; if early rounds lack enough history, previous-round placeholders can name negative/nonexistent rounds because caller computes `PREV_PREV_ROUND=$((CURRENT_ROUND - 2))`, though full alignment normally first occurs at round 4 (`hooks/loop-codex-stop-hook.sh:686-696`). If Codex writes findings but omits a strict terminal marker, the stop hook treats it as non-complete feedback and continues rather than finalizing; marker parsing requires the last non-empty line to equal `COMPLETE` or `STOP` (`hooks/loop-codex-stop-hook.sh:960-1055`).
- validation_or_tests: Template reference coverage checks that `codex/full-alignment-review.md` is referenced and exists (`tests/test-template-references.sh:52-115`). Finalize-phase tests cover strict `COMPLETE` handling and state transition into `finalize-state.md` (`tests/test-finalize-phase.sh:448-480`). Static search shows no dedicated full-alignment prompt content test beyond template reference/comprehensive template infrastructure; I did not execute tests here.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3` item evidence headings present; each assigned heading is unique
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`