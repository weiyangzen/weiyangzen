# agent_09 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`
- note: repository export is read-only and not a Git checkout; `git status` failed with `fatal: not a git repository`. I inspected files directly and did not modify anything.

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-009 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared shell library layer for RLCR hook algorithms. Recursive inspection found exactly two files: `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`; no nested subdirectories.
- algorithmic_behavior: `loop-common.sh` centralizes state constants, hook JSON validation, active loop discovery, state frontmatter parsing, round-file recognition, protected-path predicates, cancel authorization, `.humanize` git-add detection, shell write-pattern detection, and standard blocked-message emitters. `template-loader.sh` resolves the prompt-template root, loads templates, performs single-pass `{{VAR}}` rendering, provides safe fallback rendering, and validates the expected `block`, `codex`, and `claude` template subdirectories.
- inputs_outputs_state: `validate_hook_input` accepts raw hook JSON, rejects null bytes, invalid UTF-8 when `iconv` exists, invalid JSON, and missing `tool_name`, then sets `VALIDATED_TOOL_NAME` and `VALIDATED_TOOL_INPUT` (`hooks/lib/loop-common.sh:55-92`). `parse_state_file` and `parse_state_file_strict` read YAML frontmatter into `STATE_*` globals, with strict mode returning distinct validation codes for missing file/separators/fields and non-numeric rounds (`hooks/lib/loop-common.sh:202-296`). Template loading returns rendered prompt text or fallback text without changing state (`hooks/lib/template-loader.sh:36-47`, `hooks/lib/template-loader.sh:170-203`).
- gates_or_invariants: Active-loop selection deliberately uses only the newest timestamped loop directory containing `state.md` or `finalize-state.md`, preventing older “zombie” loops from reviving (`hooks/lib/loop-common.sh:147-170`). Strict state parsing requires at least two `---` separators and numeric `current_round`/`max_iterations` (`hooks/lib/loop-common.sh:240-295`). `is_cancel_authorized` requires `.cancel-requested`, exact `mv state.md|finalize-state.md cancel-state.md`, no command substitution/newlines/shell chaining/extra args/hidden `$`, no mixed quote delimiters, and no symlink source (`hooks/lib/loop-common.sh:437-614`). `git_adds_humanize` blocks direct `.humanize`, force-add broad scopes, and broad/all adds when `.humanize` is present and not ignored (`hooks/lib/loop-common.sh:622-740`). `render_template` is single-pass by design, so placeholder-like text inside values is not recursively expanded (`hooks/lib/template-loader.sh:50-132`).
- dependencies_and_callers: `loop-common.sh` sources `template-loader.sh` and initializes `TEMPLATE_DIR` from the library path (`hooks/lib/loop-common.sh:135-145`). Hook callers include read/write/edit/bash/plan validators and the Codex stop hook; examples: `hooks/loop-read-validator.sh:16`, `hooks/loop-write-validator.sh:17`, `hooks/loop-edit-validator.sh:16`, `hooks/loop-bash-validator.sh:16`, `hooks/loop-plan-file-validator.sh:17`, `hooks/loop-codex-stop-hook.sh:48`. `prompt_write_blocked_message` is used by write/edit/bash validators for `round-*-prompt.md` protection (`hooks/loop-write-validator.sh:64`, `hooks/loop-edit-validator.sh:47`, `hooks/loop-bash-validator.sh:343-344`).
- edge_cases_or_failure_modes: `is_deeply_nested` exists but is a separate helper, not automatically invoked by `validate_hook_input` (`hooks/lib/loop-common.sh:112-129`). Lowercased command input means `git -A` becomes `-a`, so `git_adds_humanize` intentionally matches lowercase all flags (`hooks/lib/loop-common.sh:626-640`, `hooks/lib/loop-common.sh:697-704`). `command_modifies_file` is regex based and covers common write forms, but paths with unusual whitespace/quoting may depend on the validator’s lowercased command normalization (`hooks/lib/loop-common.sh:782-810`). `template-loader.sh` keeps unknown placeholders verbatim and warns/returns empty on missing templates (`hooks/lib/template-loader.sh:41-47`, `hooks/lib/template-loader.sh:116-122`).
- validation_or_tests: Direct related specs include `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, `tests/test-template-references.sh`, `tests/test-error-scenarios.sh`, `tests/test-allowlist-validators.sh`, `tests/test-bash-validator-patterns.sh`, `tests/test-cancel-signal-file.sh`, and robustness tests for hook input, session, state file, cancel security, and template stress. I did not execute them because this task requests research notes only and the export is read-only.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-039 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for the `/humanize:gen-plan` command contract, covering command metadata, relevance-checker agent metadata, version consistency, validation helper behavior, content restrictions, and IO validation exit-code semantics.
- algorithmic_behavior: The test initializes project paths (`tests/test-gen-plan.sh:11-15`), keeps pass/fail counters (`tests/test-gen-plan.sh:22-40`), checks positive command/agent structure (`tests/test-gen-plan.sh:54-180`), creates invalid fixtures for negative validation (`tests/test-gen-plan.sh:191-340`), checks model-name acceptance rules (`tests/test-gen-plan.sh:350-400`), checks content for Emoji/CJK when `grep -P` works (`tests/test-gen-plan.sh:404-423`), then exercises `scripts/validate-gen-plan-io.sh` with valid and invalid argument/path combinations (`tests/test-gen-plan.sh:426-542`).
- inputs_outputs_state: Inputs are `commands/gen-plan.md`, `agents/draft-relevance-checker.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`, and `scripts/validate-gen-plan-io.sh`. It creates temporary fixture directories with `mktemp -d`, writes malformed markdown/YAML fixtures, and exits `0` only when `TESTS_FAILED` remains `0` (`tests/test-gen-plan.sh:191-193`, `tests/test-gen-plan.sh:554-562`).
- gates_or_invariants: `commands/gen-plan.md` must exist and include YAML `description`, `allowed-tools`, and `argument-hint` (`tests/test-gen-plan.sh:59-104`). The relevance agent must exist with `name: draft-relevance-checker`, `model: haiku`, and a `tools:` field (`tests/test-gen-plan.sh:111-157`). Versions must match across plugin metadata and README (`tests/test-gen-plan.sh:163-180`). Names must match `^[a-z][a-z0-9-]*$` (`tests/test-gen-plan.sh:195-199`). Model aliases `opus|sonnet|haiku` require exact match, while full IDs may start with `claude-`, `gpt-`, `o[0-9]`, or `gemini-` (`tests/test-gen-plan.sh:350-358`).
- dependencies_and_callers: The tested command defines the generation algorithm: IO validation, lenient relevance check, draft analysis, user issue resolution, quantitative metric confirmation, and structured plan generation (`commands/gen-plan.md:19-26`, `commands/gen-plan.md:30-45`, `commands/gen-plan.md:49-72`, `commands/gen-plan.md:75-136`, `commands/gen-plan.md:139-180`). The command delegates path validation to `scripts/validate-gen-plan-io.sh` (`commands/gen-plan.md:32-36`) and relevance classification to `humanize:draft-relevance-checker` (`commands/gen-plan.md:55-64`), whose own instructions require `RELEVANT:` or `NOT_RELEVANT:` output (`agents/draft-relevance-checker.md:27-36`).
- edge_cases_or_failure_modes: The YAML syntax checker is intentionally lightweight and line-pattern based, not a full YAML parser (`tests/test-gen-plan.sh:304-319`). `grep -P` failures are redirected and effectively make the Emoji/CJK check pass, so unsupported PCRE can reduce coverage (`tests/test-gen-plan.sh:409-423`). The second `trap` for script-test temp cleanup overwrites the first fixture cleanup trap, so running the test may leave the first temp fixture directory behind (`tests/test-gen-plan.sh:192-193`, `tests/test-gen-plan.sh:435-436`). The script tests IO exit codes `1`, `2`, `3`, `4`, `6`, and success `0`, but not no-write-permission exit `5` (`tests/test-gen-plan.sh:438-539`).
- validation_or_tests: This file is itself a validation suite. It indirectly validates `scripts/validate-gen-plan-io.sh`, whose exit contract is documented at `scripts/validate-gen-plan-io.sh:4-11`; the script normalizes paths with `realpath -m`, rejects missing/empty input, missing output dir, existing output path or output directory, and unwritable output directory, then reports success with input line count (`scripts/validate-gen-plan-io.sh:68-130`).
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-069 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: Prompt/block template for the invariant that Claude-side workers must not modify `round-*-prompt.md` instruction files.
- algorithmic_behavior: The template emits a blocked-write explanation: prompt files contain instructions “FROM Codex TO you”, the worker must read the current prompt, execute its tasks, write results to the summary file, and document prompt errors in the summary rather than editing the prompt (`prompt-template/block/prompt-file-write.md:1-12`).
- inputs_outputs_state: It has no placeholders and no dynamic state inputs. It is loaded through `prompt_write_blocked_message`, which falls back to a shorter inline message if the template is unavailable (`hooks/lib/loop-common.sh:358-365`).
- gates_or_invariants: The invariant is separation of instruction surface from result surface: `round-*-prompt.md` is read-only for the worker, while summary files are the write target (`prompt-template/block/prompt-file-write.md:3-10`). This blocks self-modification of instructions and forces error reporting through the summary channel (`prompt-template/block/prompt-file-write.md:12`).
- dependencies_and_callers: `hooks/loop-write-validator.sh` calls the block when a direct write targets a prompt file (`hooks/loop-write-validator.sh:64`). `hooks/loop-edit-validator.sh` calls it for edit attempts (`hooks/loop-edit-validator.sh:47`). `hooks/loop-bash-validator.sh` calls it when `command_modifies_file` detects shell writes to `round-[0-9]+-prompt.md` (`hooks/loop-bash-validator.sh:343-344`). `tests/test-template-references.sh` expects this template path to exist among loop-common message templates.
- edge_cases_or_failure_modes: Because the template is static, there is no substitution failure surface. If the file is missing, `load_and_render_safe` returns the inline fallback from `loop-common.sh`, preserving the gate but with less detailed guidance (`hooks/lib/loop-common.sh:360-365`). The template itself does not specify allowed exceptions; exception logic lives in validators.
- validation_or_tests: Template presence and safe rendering are covered by template reference/comprehensive tests, and the caller path is covered by prompt-file write/edit/bash validator tests. I inspected the template directly and did not execute tests.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-099 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the portable timeout abstraction used by setup and hook scripts to bound Git/Codex/system calls.
- algorithmic_behavior: The test sources `scripts/portable-timeout.sh` and shared helpers (`tests/robustness/test-timeout-robustness.sh:14-17`), validates timeout implementation detection (`tests/robustness/test-timeout-robustness.sh:31-49`), checks normal command execution, timeout exit behavior, argument preservation, exit-code preservation, pipelines, short timeouts, zero timeout tolerance, large output, rapid cycles, nonexistent/empty commands, special characters, signal handling, fallback detection, subshells, and exported `TIMEOUT_IMPL` (`tests/robustness/test-timeout-robustness.sh:51-272`).
- inputs_outputs_state: Inputs are the detected runtime tools (`gtimeout`, GNU `timeout`, `python3`, `python`, or none), shell commands passed to `run_with_timeout`, and `TIMEOUT_IMPL`. Outputs are pass/fail counters via `tests/test-helpers.sh` and final exit status from `print_test_summary` (`tests/robustness/test-timeout-robustness.sh:274-279`; helper summary behavior at `tests/test-helpers.sh:58-78`).
- gates_or_invariants: `detect_timeout_impl` must return only `gtimeout|timeout|python3|python|none` (`tests/robustness/test-timeout-robustness.sh:42-49`, `tests/robustness/test-timeout-robustness.sh:235-247`). Timed-out commands should return `124` when a real timeout implementation exists (`tests/robustness/test-timeout-robustness.sh:62-77`). Successful commands preserve stdout and exit code (`tests/robustness/test-timeout-robustness.sh:51-60`, `tests/robustness/test-timeout-robustness.sh:89-100`). Argument boundaries and shell metacharacters are preserved (`tests/robustness/test-timeout-robustness.sh:79-87`, `tests/robustness/test-timeout-robustness.sh:211-219`).
- dependencies_and_callers: The underlying implementation prioritizes `gtimeout`, GNU `timeout`, `python3`, `python`, then `none` (`scripts/portable-timeout.sh:9-29`). `run_with_timeout` delegates to the selected tool, uses Python `subprocess.run(..., timeout=...)` for Python fallback, returns `124` on `TimeoutExpired`, and runs unbounded with a warning if no timeout exists (`scripts/portable-timeout.sh:31-71`). Callers include `scripts/setup-rlcr-loop.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh` for Git and Codex operations.
- edge_cases_or_failure_modes: If `TIMEOUT_IMPL=none`, timeout-specific cases are recorded as passed skips rather than failures, so the suite allows degraded no-timeout environments (`tests/robustness/test-timeout-robustness.sh:65-77`, `tests/robustness/test-timeout-robustness.sh:120-155`). Zero timeout may succeed or timeout immediately (`tests/robustness/test-timeout-robustness.sh:139-152`). Empty command handling is accepted regardless of exit code (`tests/robustness/test-timeout-robustness.sh:201-209`). The header mentions commands ignoring `SIGTERM`, but the implemented signal test uses a trap that exits gracefully or times out, not a hard SIGTERM-ignoring process (`tests/robustness/test-timeout-robustness.sh:221-233`).
- validation_or_tests: This file is the validation suite for `scripts/portable-timeout.sh`. It ends through `print_test_summary`, so any failed assertion exits nonzero (`tests/robustness/test-timeout-robustness.sh:274-279`). I did not run it due the research-only/read-only constraints.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4 item evidence sections present; item IDs are listed only in their section headers
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`