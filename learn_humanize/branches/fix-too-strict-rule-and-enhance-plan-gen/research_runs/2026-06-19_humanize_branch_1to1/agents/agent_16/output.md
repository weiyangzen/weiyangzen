# agent_16 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-016 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Defines the `gen-plan` command workflow that converts a user draft into a structured implementation plan. It is a command-level algorithm contract rather than executable shell code: it sequences validation, relevance screening, draft analysis, user clarification, plan synthesis, and output reporting. The command frontmatter constrains tools to `validate-gen-plan-io.sh`, `Read`, `Glob`, `Grep`, `Task`, `Write`, and `AskUserQuestion` at `commands/gen-plan.md:1-12`.
- algorithmic_behavior: Phase 1 runs `"${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh" $ARGUMENTS` and branches on exact exit codes `0..6` (`commands/gen-plan.md:30-46`). Phase 2 reads the draft and delegates a lenient repository relevance verdict to `humanize:draft-relevance-checker` using `haiku`, requiring either `RELEVANT:` or `NOT_RELEVANT:` output (`commands/gen-plan.md:49-72`). Phase 3 analyzes clarity, consistency, completeness, and functionality, using Explore agents to inspect affected code paths and dependencies (`commands/gen-plan.md:75-107`). Phase 4 resolves found issues through `AskUserQuestion`, then separately classifies quantitative metrics as hard requirements or optimization directions (`commands/gen-plan.md:110-136`). Phase 5 emits a deterministic markdown plan structure with goal, AC-formatted acceptance criteria, path boundaries, feasibility hints, and dependency sequence (`commands/gen-plan.md:139-213`). Phase 6 writes the plan and reports path, included summary, and AC count (`commands/gen-plan.md:237-243`).
- inputs_outputs_state: Inputs are command arguments `--input <draft>` and `--output <plan>`, plus repository context gathered by Read/Glob/Grep/Task. The validation script normalizes paths with `realpath -m`, derives the output directory, and returns validated absolute target paths through stdout diagnostics, not an exported state file (`scripts/validate-gen-plan-io.sh:68-76`, `scripts/validate-gen-plan-io.sh:125-130`). The command output is a new markdown plan at the requested output path. Intermediate state consists of analysis findings, user decisions, metric classifications, and relevance verdicts that must be reflected in the generated plan context (`commands/gen-plan.md:121-136`, `commands/gen-plan.md:248-254`).
- gates_or_invariants: The IO gate is hard-stop: missing input exits 1, empty input exits 2, missing output directory exits 3, existing output path or directory exits 4, unwritable output directory exits 5, malformed arguments/help exits 6 (`commands/gen-plan.md:38-46`; implementation at `scripts/validate-gen-plan-io.sh:78-123`). The relevance gate stops on `NOT_RELEVANT` (`commands/gen-plan.md:66-71`). Plan formatting invariants include AC IDs using `AC-X` or `AC-X.Y`, no line-number references, no time estimates, no Day/Week/Month/Year terminology, positive and negative tests for every AC, affirmative path boundaries, and narrowed boundaries for deterministic designs (`commands/gen-plan.md:215-233`). The output file must not preexist, so the command protects existing plans from overwrite (`scripts/validate-gen-plan-io.sh:102-115`).
- dependencies_and_callers: Direct dependency is `scripts/validate-gen-plan-io.sh`, allowed in frontmatter and invoked in Phase 1 (`commands/gen-plan.md:5`, `commands/gen-plan.md:35`). Relevance checking depends on `agents/draft-relevance-checker.md`, whose frontmatter sets `name: draft-relevance-checker`, `model: haiku`, and tools `Read, Glob, Grep` (`agents/draft-relevance-checker.md:1-5`). Structural and script behavior is covered by `tests/test-gen-plan.sh`, including command file existence/frontmatter checks, agent metadata checks, content constraints, and validator exit-code tests (`tests/test-gen-plan.sh:55-180`, `tests/test-gen-plan.sh:426-542`). `tests/run-all-tests.sh` includes `test-gen-plan.sh`, so this command participates in the repo’s general validation suite.
- edge_cases_or_failure_modes: Argument values that are omitted or followed by another flag are invalid (`scripts/validate-gen-plan-io.sh:31-45`). Unknown flags and help both route through usage and exit 6 (`scripts/validate-gen-plan-io.sh:47-53`). Empty input is distinct from missing input (`scripts/validate-gen-plan-io.sh:78-91`). Output path that is a directory is treated as output-exists class exit 4, avoiding accidental directory writes (`scripts/validate-gen-plan-io.sh:102-108`). The command’s relevance gate is intentionally lenient and should only reject completely unrelated drafts, reducing false negatives (`commands/gen-plan.md:51-53`; agent note at `agents/draft-relevance-checker.md:31-36`). Missing user clarification can block complete plan generation and must be reported with missing information and improvement suggestions (`commands/gen-plan.md:246-254`).
- validation_or_tests: `tests/test-gen-plan.sh` validates positive command/agent structure and negative malformed fixtures, then directly exercises `validate-gen-plan-io.sh` for missing values, flag-as-value, unknown option, missing input, empty input, missing output directory, existing output file, directory output, valid paths, and help behavior (`tests/test-gen-plan.sh:438-539`). The tests confirm the command metadata and validator contract, but they do not execute a full LLM-mediated draft-to-plan generation path.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-046 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR loop lifecycle state naming. It verifies that only active state filenames are treated as live loops and that terminal state renames encode the loop exit reason. This test file is core because state-file naming drives active-loop detection and prevents completed, cancelled, max-iteration, stopped, or unexpected-error loops from being revived.
- algorithmic_behavior: The script creates an isolated temporary git repo, sources `hooks/lib/loop-common.sh`, then constructs `.humanize/rlcr/<timestamp>/...` fixtures to test `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir` (`tests/test-state-exit-naming.sh:31-67`). Tests 1-6 assert terminal files such as `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` do not count as active, while `state.md` does (`tests/test-state-exit-naming.sh:38-155`). Test 7 asserts the newest timestamp directory with `state.md` wins (`tests/test-state-exit-naming.sh:157-176`). Tests 8-10 exercise `end_loop` invalid reason rejection, valid reason renames, and missing state-file handling (`tests/test-state-exit-naming.sh:182-239`). Tests 11-13 assert the new `.humanize/rlcr` path is recognized while legacy `.humanize-loop.local` is not searched (`tests/test-state-exit-naming.sh:245-287`).
- inputs_outputs_state: Inputs are synthetic loop directories and YAML-frontmatter state files under a `mktemp -d` workspace. The primary state transition under test is `state.md` -> `<reason>-state.md` for `complete`, `cancel`, `maxiter`, `stop`, and `unexpected` (`hooks/lib/loop-common.sh:826-859`). `find_active_loop` outputs the active loop directory path or empty string (`hooks/lib/loop-common.sh:152-170`). Test output is pass/fail counters and process exit equal to failure count (`tests/test-state-exit-naming.sh:289-298`).
- gates_or_invariants: Active-loop detection only checks the newest loop directory and requires `state.md` or `finalize-state.md`; terminal renamed files are not active indicators (`hooks/lib/loop-common.sh:147-170`). `end_loop` accepts only five reasons: `complete`, `cancel`, `maxiter`, `stop`, `unexpected`; invalid reasons return nonzero with an error (`hooks/lib/loop-common.sh:833-846`). Missing `state.md` prevents transition and returns nonzero (`hooks/lib/loop-common.sh:850-858`). The path invariant is that RLCR loop files are under `.humanize/rlcr/`, not legacy `.humanize-loop.local` (`hooks/lib/loop-common.sh:616-620`; tested at `tests/test-state-exit-naming.sh:254-287`).
- dependencies_and_callers: The test sources `hooks/lib/loop-common.sh` directly (`tests/test-state-exit-naming.sh:64-65`). The implementation under test defines exit reason constants and semantics at `hooks/lib/loop-common.sh:30-40`, active-loop discovery at `hooks/lib/loop-common.sh:147-170`, path detection at `hooks/lib/loop-common.sh:616-620`, and loop termination at `hooks/lib/loop-common.sh:826-859`. The state helper is shared by validators listed in its header: read, write, and bash validators (`hooks/lib/loop-common.sh:3-9`). `tests/run-all-tests.sh` includes `test-state-exit-naming.sh`, so this lifecycle behavior is part of the broader suite.
- edge_cases_or_failure_modes: A stale terminal state in the newest directory makes `find_active_loop` return empty even if older directories contain active state, intentionally preventing older loop resurrection (`hooks/lib/loop-common.sh:147-149`, `hooks/lib/loop-common.sh:160-169`). `find_active_loop` sorts timestamp directory names lexically descending, so correctness depends on timestamp-like sortable directory names (`hooks/lib/loop-common.sh:160-162`). The test creates several terminal state files in the same directory without removing prior terminal files; this is acceptable because only `state.md` and `finalize-state.md` are active indicators. The test does not cover `finalize-state.md` active detection, even though the helper supports it (`hooks/lib/loop-common.sh:164-165`). `set -uo pipefail` is used globally, with local `set +e` blocks around expected failures, so unexpected command errors can still surface through assertions rather than immediate exit (`tests/test-state-exit-naming.sh:13`, `tests/test-state-exit-naming.sh:192-199`).
- validation_or_tests: The assigned file is itself validation. It verifies 13 behaviors: terminal state inactivity, active `state.md`, newest active loop precedence, valid/invalid `end_loop` transitions, missing state-file failure, new path detection, legacy path rejection, and legacy search isolation (`tests/test-state-exit-naming.sh:38-287`).
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-076 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Prompt/block template for RLCR guard feedback when an agent targets a valid round filename in the wrong active-loop directory. It is not algorithmic code by itself, but it is a core guard contract surface because read/write validators render it when enforcing active directory correctness.
- algorithmic_behavior: The template emits a concise block message with title, attempted action/path, computed correct path, and a suggested `cat` command (`prompt-template/block/wrong-directory-path.md:1-6`). It uses placeholders `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}`. Rendering is single-pass through `load_and_render_safe`, preserving unresolved placeholders if not supplied and falling back to inline messages if the template is missing (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:167-203`).
- inputs_outputs_state: Inputs are validator-provided substitution values. `loop-read-validator.sh` passes `ACTION=read`, the attempted `FILE_PATH`, and `CORRECT_PATH=$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME` when a current-round summary/prompt path is inside `.humanize/rlcr` but not the active loop directory (`hooks/loop-read-validator.sh:150-164`). `loop-write-validator.sh` passes `ACTION=write to` with analogous path values for write attempts (`hooks/loop-write-validator.sh:220-235`). Output is stderr text plus validator exit code 2, which blocks the attempted tool call.
- gates_or_invariants: The directory gate occurs only after an active loop is found and strict state parsing succeeds. Read validation first blocks todos, ignores non-summary/non-prompt files, resolves active loop/current round, validates file location and round number, then compares exact path equality to `ACTIVE_LOOP_DIR/$CLAUDE_FILENAME` (`hooks/loop-read-validator.sh:48-96`, `hooks/loop-read-validator.sh:115-164`). Write validation similarly permits unrelated files, protects todos/prompts/state/finalize/plan backup/goal tracker/summary round cases first, then performs exact active-directory path comparison (`hooks/loop-write-validator.sh:49-89`, `hooks/loop-write-validator.sh:91-235`). This means same filename in an older or sibling loop directory is blocked even if the round number is otherwise valid.
- dependencies_and_callers: Called by `hooks/loop-read-validator.sh` at `hooks/loop-read-validator.sh:157-164` and `hooks/loop-write-validator.sh` at `hooks/loop-write-validator.sh:226-235`. Both validators depend on `hooks/lib/loop-common.sh` for active-loop discovery, state parsing, round-file detection, and path detection (`hooks/loop-read-validator.sh:14-17`; `hooks/loop-write-validator.sh:15-18`). Rendering depends on `hooks/lib/template-loader.sh`, especially `render_template` and `load_and_render_safe` (`hooks/lib/template-loader.sh:58-132`, `hooks/lib/template-loader.sh:170-203`).
- edge_cases_or_failure_modes: The template’s final line says `cat {{FILE_PATH}}`, which suggests reading the attempted path rather than the computed `{{CORRECT_PATH}}` (`prompt-template/block/wrong-directory-path.md:6`). For write blocks this is also semantically odd because the action is `write to`, yet the suggestion is `cat`. If placeholders are missing, the loader leaves them unchanged by design, so malformed caller substitutions would produce visible `{{...}}` tokens rather than failing closed (`hooks/lib/template-loader.sh:12-13`, `hooks/lib/template-loader.sh:116-122`). Path comparison is exact string equality, so normalization differences, symlinks, case differences, or equivalent paths are not accepted by this gate (`hooks/loop-read-validator.sh:154-164`; `hooks/loop-write-validator.sh:224-235`).
- validation_or_tests: Template rendering behavior is indirectly covered by template loader tests and comprehensive template reference tests; `rg` shows explicit template-loader/comprehensive tests passing `CORRECT_PATH` fixtures. The concrete guard callers are present in read/write validators (`hooks/loop-read-validator.sh:160`, `hooks/loop-write-validator.sh:230`). I did not find a dedicated test named specifically for `wrong-directory-path.md`; nearby allowlist/path tests cover wrong-directory blocking behavior for related round/todos paths.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 unique assigned item sections present; item IDs intentionally not repeated here to keep one evidence header per assigned item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`