# agent_02 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: Prompt-agent specification directory for delegated algorithmic judgment. Recursive inspection found one descendant: `agents/draft-relevance-checker.md`. This file defines the `draft-relevance-checker` subagent used by `commands/gen-plan.md` to decide whether an input draft is semantically relevant before plan generation proceeds.
- algorithmic_behavior: `agents/draft-relevance-checker.md:1-6` defines frontmatter contract: name `draft-relevance-checker`, description, model `haiku`, tools `Read, Glob, Grep`. The body instructs the subagent to explore repo documentation and structure, analyze draft content against repository concepts/files/functions/features, and emit exactly one of two verdict prefixes: `RELEVANT:` or `NOT_RELEVANT:` with a brief reason (`agents/draft-relevance-checker.md:14-29`). The relevance policy is intentionally permissive: informal drafts, any language, rough ideas are allowed; semantic relevance matters more than syntactic similarity; uncertainty should lean relevant (`agents/draft-relevance-checker.md:31-36`).
- inputs_outputs_state: Input is draft document content plus read-only repository context gathered through `Read`, `Glob`, and `Grep`. Output is a text verdict consumed by the gen-plan flow. No persistent state is written by this agent spec itself. The state transition it controls is in `commands/gen-plan.md`: after IO validation, Phase 2 invokes `humanize:draft-relevance-checker`; `NOT_RELEVANT` stops command execution, while `RELEVANT` advances to Phase 3 draft analysis (`commands/gen-plan.md:49-71`).
- gates_or_invariants: The output prefix is an invariant because the caller branches on `RELEVANT` versus `NOT_RELEVANT`. The prompt also establishes a leniency invariant, preventing overly strict rejection of drafts that could reasonably connect to the repo. Tool access is constrained to read/search tools, making this a classification gate rather than a mutating implementation step.
- dependencies_and_callers: Direct caller is the gen-plan command spec, which explicitly says to use the Task tool with model `haiku` and prompt the agent to inspect README/CLAUDE/main files and return one of the two verdicts (`commands/gen-plan.md:55-64`). Validation is tied to `tests/test-gen-plan.sh`, which verifies the agent file exists, its name is `draft-relevance-checker`, model is `haiku`, and a `tools:` field is present (`tests/test-gen-plan.sh:111-156`).
- edge_cases_or_failure_modes: False negatives are the main behavioral risk; the prompt mitigates this by saying to mark relevant when in doubt (`agents/draft-relevance-checker.md:33-36`). Another failure mode is malformed frontmatter or renamed agent file, which would break gen-plan’s reference to `humanize:draft-relevance-checker`. Because the directory currently contains only this one agent, any future sibling agent would need explicit caller/test coverage to avoid becoming orphaned prompt surface.
- validation_or_tests: `tests/test-gen-plan.sh` covers structural and frontmatter invariants for this directory’s child (`tests/test-gen-plan.sh:111-156`). The all-suite runner includes `test-gen-plan.sh` in its ordered test list (`tests/run-all-tests.sh:51`). There is no dynamic execution test of semantic classification quality; current tests validate wiring and metadata, not the subagent’s actual reasoning output.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-032 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Repository-level executable validation orchestrator. It is the aggregate test gate for Humanize plugin behavior, including template loading, hook validation, gen-plan wiring, monitor behavior, and robustness suites.
- algorithmic_behavior: The script initializes counters and failed-suite collection (`tests/run-all-tests.sh:28-30`), declares an ordered `TEST_SUITES` array (`tests/run-all-tests.sh:32-63`), declares a `ZSH_TESTS` exception list (`tests/run-all-tests.sh:65-68`), then iterates through each suite. Missing suite files are skipped without failing (`tests/run-all-tests.sh:73-76`). Suites listed in `ZSH_TESTS` are run with `zsh` if available, otherwise skipped (`tests/run-all-tests.sh:87-104`); all others are executed directly by path (`tests/run-all-tests.sh:105-107`). The runner captures stdout/stderr, strips ANSI escape sequences, extracts the final `Passed:` and `Failed:` counts, and accumulates totals (`tests/run-all-tests.sh:111-118`).
- inputs_outputs_state: Inputs are executable test files under `tests/` and `tests/robustness/`, the local shell environment, executable permissions on suites, and optional availability of `zsh`. Outputs are terminal summary lines, accumulated pass/fail counts, failed-suite names, and process exit code `0` when all considered suites pass or `1` when any suite exits nonzero or reports failed tests (`tests/run-all-tests.sh:120-149`). State is in-process only: `TOTAL_PASSED`, `TOTAL_FAILED`, and `FAILED_SUITES`.
- gates_or_invariants: A suite fails the aggregate gate if either its process exit code is nonzero or its parsed `Failed:` count is greater than zero (`tests/run-all-tests.sh:120`). The runner intentionally uses `set -uo pipefail` rather than global `set -e` at startup (`tests/run-all-tests.sh:12`) and temporarily disables error exit around each suite (`tests/run-all-tests.sh:95-109`) so it can continue running later suites and produce a complete failure report. The pass/fail parser expects test suites to print `Passed: N` and `Failed: N`; suites that do not print those lines can be undercounted as `0`.
- dependencies_and_callers: Depends on every suite named in the `TEST_SUITES` array, including `robustness/test-goal-tracker-robustness.sh` at `tests/run-all-tests.sh:55`. Depends on `sed`, `grep`, shell arrays, executable test scripts, and optionally `zsh`. It is likely the top-level local validation command used by developers/agents because its header documents direct usage as `./tests/run-all-tests.sh` (`tests/run-all-tests.sh:3-9`).
- edge_cases_or_failure_modes: Missing suite files are skipped and do not fail the run (`tests/run-all-tests.sh:73-76`), which is useful for optional tests but can mask accidental deletion. Missing `zsh` skips zsh-only suites (`tests/run-all-tests.sh:98-104`). If a suite emits multiple summaries, the runner uses the last parsed pass/fail counts via `tail -1` (`tests/run-all-tests.sh:114-115`). If a suite exits `0` but prints stale `Failed:` text with a nonzero number, the aggregate run fails; if a suite exits nonzero but prints no summary, the aggregate run still fails because exit code is checked.
- validation_or_tests: This file is itself the validation entrypoint. It validates the assigned robustness parser suite indirectly by including `robustness/test-goal-tracker-robustness.sh` in the ordered suite list (`tests/run-all-tests.sh:55`). It also validates the assigned agent directory indirectly by including `test-gen-plan.sh` (`tests/run-all-tests.sh:51`) and the assigned template indirectly through `test-template-references.sh` and hook/template suites (`tests/run-all-tests.sh:38-42`, `tests/run-all-tests.sh:47`).
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-062 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: Prompt/block template for a mutation gate. It provides the denial text shown when a Bash command attempts to modify `goal-tracker.md` in Round 0, redirecting the actor to use Write or Edit tools so validation hooks are not bypassed.
- algorithmic_behavior: The template states the blocked condition, names the protected file, injects the correct path through `{{CORRECT_PATH}}`, and explains why Bash mutation is disallowed (`prompt-template/block/goal-tracker-bash-write.md:1-8`). It is not executable code, but it is part of the hook algorithm’s user-facing transition: a detected Bash write attempt becomes an exit-2 block with a rendered corrective prompt.
- inputs_outputs_state: Input is a template variable `CORRECT_PATH`, supplied by `goal_tracker_bash_blocked_message` in `hooks/lib/loop-common.sh:397-404`. Output is rendered Markdown/error text sent to stderr by `hooks/loop-bash-validator.sh` when the protected write is detected (`hooks/loop-bash-validator.sh:327-335`). No persistent state is updated by the template; the surrounding validator exits with code `2`, preventing the attempted command from proceeding.
- gates_or_invariants: The invariant is that Bash cannot mutate `goal-tracker.md`; valid mutation must go through Write/Edit tooling. The actual detector is `command_modifies_file`, which matches redirection, append redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec fd>` patterns (`hooks/lib/loop-common.sh:782-810`). For Round 0, the bash validator renders this template with `$ACTIVE_LOOP_DIR/goal-tracker.md` (`hooks/loop-bash-validator.sh:327-331`). For later rounds, it uses a different goal-tracker modification block that routes updates through summary requests (`hooks/loop-bash-validator.sh:331-334`).
- dependencies_and_callers: Called through `goal_tracker_bash_blocked_message` in the shared hook library (`hooks/lib/loop-common.sh:397-404`). That helper uses `load_and_render_safe`, so if the template is missing a fallback message is used instead (`hooks/lib/loop-common.sh:400-404`). Direct runtime caller is `hooks/loop-bash-validator.sh` under the goal-tracker modification section (`hooks/loop-bash-validator.sh:321-336`). Template existence is cross-checked by `tests/test-template-references.sh`, which lists `block/goal-tracker-bash-write.md` as a required common template (`tests/test-template-references.sh:152-159`).
- edge_cases_or_failure_modes: Detection is pattern-based shell text analysis. Tests document that common mutations are caught, including `>`, `>>`, `tee`, in-place editors, `mv`, `cp`, `rm`, `dd`, `truncate`, and fd redirection (`tests/test-bash-validator-patterns.sh:75-133`). Read-only commands should not be caught, including `cat`, `grep`, `head`, `tail`, `wc`, `less`, `echo goal-tracker.md`, `ls`, `file`, `stat`, and `diff` (`tests/test-bash-validator-patterns.sh:142-152`). A known limitation is multi-source `cp file1 file2 goal-tracker.md`, which the tests explicitly note is not detected because the pattern expects `cp src dest` (`tests/test-bash-validator-patterns.sh:165-168`). Variable indirection such as `echo x > $FILE` is not detected unless the literal target appears (`tests/test-bash-validator-patterns.sh:170-172`).
- validation_or_tests: Covered by template reference tests (`tests/test-template-references.sh:152-159`), generic template rendering tests (`tests/test-template-loader.sh:147-164` for real template variable rendering behavior), and bash mutation detector tests (`tests/test-bash-validator-patterns.sh:75-172`). The aggregate runner includes `test-bash-validator-patterns.sh`, `test-template-references.sh`, and `test-template-loader.sh` (`tests/run-all-tests.sh:34-38`).
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-092 `file` `tests/robustness/test-goal-tracker-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for `humanize_parse_goal_tracker`, the production parser that summarizes `goal-tracker.md` for monitor/status behavior. The file is labeled AC-3 and directly sources production code rather than reimplementing the parser (`tests/robustness/test-goal-tracker-robustness.sh:3-22`).
- algorithmic_behavior: The script creates a temporary directory, writes multiple goal-tracker variants, calls `humanize_parse_goal_tracker`, and extracts fields from its pipe-delimited result using `parse_result` (`tests/robustness/test-goal-tracker-robustness.sh:24-51`). The expected production output shape is `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary`, matching `scripts/humanize.sh:24-26` and `scripts/humanize.sh:105`. It validates list-format AC counting, table-format AC counting, active-task calculation excluding completed/deferred rows, completed task row counts, unique completed AC counts, goal summary extraction, missing file defaults, empty files, large AC sets, special characters, malformed headers, truncated content, binary content, open issues, deferred tasks, headers-only files, mixed bold/sub-numbered AC formats, and long goal truncation (`tests/robustness/test-goal-tracker-robustness.sh:60-524`).
- inputs_outputs_state: Inputs are synthetic `goal-tracker.md` files written under `$TEST_DIR`, plus the real parser from `scripts/humanize.sh`. Output is pass/fail lines through shared helpers and final process exit from `print_test_summary` (`tests/robustness/test-goal-tracker-robustness.sh:530-531`). State consists of temporary fixture files and shared counters `TESTS_PASSED`, `TESTS_FAILED`, and `TESTS_SKIPPED` from `tests/test-helpers.sh:22-24`; `setup_test_dir` creates and cleans the temp directory with an EXIT trap (`tests/test-helpers.sh:84-89`).
- gates_or_invariants: The parser must return safe defaults for a nonexistent file: `0|0|0|0|0|0|No goal tracker` (`tests/robustness/test-goal-tracker-robustness.sh:237-244`, `scripts/humanize.sh:27-31`). AC counting must support both list and table formats (`tests/robustness/test-goal-tracker-robustness.sh:60-116`, `scripts/humanize.sh:42-48`). Active task count must equal active-section data rows minus rows whose status column is completed or deferred, floored at zero (`tests/robustness/test-goal-tracker-robustness.sh:118-153`, `scripts/humanize.sh:50-78`). Completed ACs are unique AC identifiers in the Completed section (`scripts/humanize.sh:84-88`). Table section counts subtract header and separator rows (`scripts/humanize.sh:33-40`). Goal summary is the first non-empty non-placeholder line under `### Ultimate Goal`, trimmed to 60 characters (`scripts/humanize.sh:98-103`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for counters, pass/fail reporting, summaries, and temp dir setup (`tests/robustness/test-goal-tracker-robustness.sh:18-24`; `tests/test-helpers.sh:30-89`). Depends on production `scripts/humanize.sh` (`tests/robustness/test-goal-tracker-robustness.sh:21-22`). The parser is consumed by `_humanize_monitor_codex`: the monitor finds `$session_dir/goal-tracker.md`, calls `_parse_goal_tracker`, splits the pipe-delimited result, and uses fields for status display (`scripts/humanize.sh:375-417`). The aggregate runner includes this robustness script at `tests/run-all-tests.sh:55`.
- edge_cases_or_failure_modes: Malformed files without proper `### Acceptance Criteria` headers should produce zero ACs rather than accidental matches (`tests/robustness/test-goal-tracker-robustness.sh:304-326`). Truncated files should still count parseable AC rows (`tests/robustness/test-goal-tracker-robustness.sh:328-347`). Binary content should not make the parser fail completely; the test only requires at least one AC count (`tests/robustness/test-goal-tracker-robustness.sh:349-371`). Mixed AC formats require bold `**AC-2**` to count, while decimal subcriteria such as `AC-3.1` are not expected to count as normal ACs (`tests/robustness/test-goal-tracker-robustness.sh:477-499`). Long goals must be truncated to at most 60 characters (`tests/robustness/test-goal-tracker-robustness.sh:501-524`). The parser’s section extraction is `sed`/regex based, so section-heading spelling and markdown shape are part of the input contract.
- validation_or_tests: This file is itself a validation suite and is included in `tests/run-all-tests.sh`. Its final gate is `print_test_summary`, which returns zero only when no failures were recorded (`tests/test-helpers.sh:58-77`; `tests/robustness/test-goal-tracker-robustness.sh:530-531`). It validates the same parser used by monitor UI code, making it a production behavior gate rather than an isolated fixture test.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 of 4 item sections present, each assigned item section appears once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`