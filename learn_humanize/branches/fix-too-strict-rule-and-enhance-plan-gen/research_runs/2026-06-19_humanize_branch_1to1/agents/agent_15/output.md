# agent_15 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-015 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for safely cancelling an active RLCR loop. This file is a thin command contract: it does not implement cancellation itself, but it defines the allowed tool surface and user-facing decision flow around `scripts/cancel-rlcr-loop.sh`.
- algorithmic_behavior: The command first runs `"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh"` and branches entirely on the first output token from that script. `NO_LOOP` and `NO_ACTIVE_LOOP` map to a no-active-loop response; `CANCELLED` and `CANCELLED_FINALIZE` report the script's cancellation message; `FINALIZE_NEEDS_CONFIRM` triggers an explicit confirmation prompt before any forced finalize cancellation. See `commands/cancel-rlcr-loop.md:11` through `commands/cancel-rlcr-loop.md:22`.
- inputs_outputs_state: Input is the current project loop state under `.humanize/rlcr`, indirectly via the cancel script. Output is a human-facing status message and, if cancellation is accepted, script-side loop state mutation. The implementation script finds the newest loop directory under `$PROJECT_ROOT/.humanize/rlcr`, treats `state.md` as a normal active loop and `finalize-state.md` as Finalize Phase, creates `.cancel-requested`, then renames the active state file to `cancel-state.md`; see `scripts/cancel-rlcr-loop.sh:69` through `scripts/cancel-rlcr-loop.sh:99` and `scripts/cancel-rlcr-loop.sh:134` through `scripts/cancel-rlcr-loop.sh:151`.
- gates_or_invariants: The command's frontmatter allows only two Bash invocations, the normal script and `--force`, plus `AskUserQuestion`; see `commands/cancel-rlcr-loop.md:3`. The key invariant is that cancellation logic is centralized in the script, not duplicated by the command text; this is stated directly at `commands/cancel-rlcr-loop.md:36`. During Finalize Phase, the non-forced path must not mutate state; the user must explicitly choose "Yes, cancel now" before `--force` is run, described at `commands/cancel-rlcr-loop.md:23` through `commands/cancel-rlcr-loop.md:34`.
- dependencies_and_callers: Depends on `scripts/cancel-rlcr-loop.sh` for all real state inspection and mutation. The script depends on `CLAUDE_PROJECT_DIR` or `pwd` to locate the project root and `.humanize/rlcr`; see `scripts/cancel-rlcr-loop.sh:69` through `scripts/cancel-rlcr-loop.sh:73`. It also coordinates with bash-hook protections: the bash validator permits the specific state-file rename only when `.cancel-requested` authorizes it, otherwise state-file edits are blocked; see `hooks/loop-bash-validator.sh:264` through `hooks/loop-bash-validator.sh:280`.
- edge_cases_or_failure_modes: No loop directory returns `NO_LOOP`; newest loop directory without `state.md` or `finalize-state.md` returns `NO_ACTIVE_LOOP`; Finalize Phase without `--force` returns `FINALIZE_NEEDS_CONFIRM` and exit code 2; unknown script options exit with error. These are implemented in `scripts/cancel-rlcr-loop.sh:75` through `scripts/cancel-rlcr-loop.sh:99`, `scripts/cancel-rlcr-loop.sh:117` through `scripts/cancel-rlcr-loop.sh:127`, and `scripts/cancel-rlcr-loop.sh:57` through `scripts/cancel-rlcr-loop.sh:60`. A notable algorithmic edge is newest-directory selection by lexical reverse sort of timestamp-like directory names at `scripts/cancel-rlcr-loop.sh:73`.
- validation_or_tests: `tests/test-cancel-signal-file.sh` validates the state-protection side of cancellation: moving `state.md` to `cancel-state.md` is allowed only with `.cancel-requested`, while arbitrary state edits, wrong destinations, command chaining, substitution, wrong signal directory, and unrelated state files are blocked. Examples include allowed signal rename at `tests/test-cancel-signal-file.sh:78` through `tests/test-cancel-signal-file.sh:90`, blocked no-signal rename at `tests/test-cancel-signal-file.sh:119` through `tests/test-cancel-signal-file.sh:132`, blocked arbitrary destination at `tests/test-cancel-signal-file.sh:281` through `tests/test-cancel-signal-file.sh:294`, and blocked injection patterns at `tests/test-cancel-signal-file.sh:301` through `tests/test-cancel-signal-file.sh:354`. `tests/test-finalize-phase.sh:370` through `tests/test-finalize-phase.sh:392` verifies Finalize Phase summary gating and no Codex invocation when summary is missing.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-045 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for plan-file setup validation in `scripts/setup-rlcr-loop.sh`. It defines the accepted and rejected inputs for starting an RLCR loop from a plan file, including path safety, git repository validity, tracking policy, branch/model serialization safety, and minimum meaningful content.
- algorithmic_behavior: The test constructs temporary git repositories, creates plan files in controlled tracked/untracked/gitignored states, invokes `scripts/setup-rlcr-loop.sh` with specific arguments, captures exit code plus stderr/stdout, and asserts on error substrings. It unsets `CLAUDE_PROJECT_DIR` so the setup script uses the temp repo `pwd`, not the actual plugin repository; see `tests/test-plan-file-validation.sh:19` through `tests/test-plan-file-validation.sh:21`. `setup_test_repo` initializes a repo with one commit, a gitignored `plans/test-plan.md`, and committed `.gitignore`; see `tests/test-plan-file-validation.sh:40` through `tests/test-plan-file-validation.sh:70`. `mock_codex` injects a fake `codex` binary if needed, preventing unrelated CLI availability from dominating early validation paths; see `tests/test-plan-file-validation.sh:73` through `tests/test-plan-file-validation.sh:84`.
- inputs_outputs_state: Inputs are CLI invocations of `setup-rlcr-loop.sh`, including positional plan paths, `--plan-file`, `--track-plan-file`, and `--codex-model`. Output is test pass/fail accounting and process exit equal to the failure count at `tests/test-plan-file-validation.sh:740` through `tests/test-plan-file-validation.sh:749`. The setup script under test, when validation succeeds far enough, creates `.humanize/rlcr/<timestamp>/state.md`, backs up the plan to `plan.md`, writes `goal-tracker.md`, and writes the initial prompt; see `scripts/setup-rlcr-loop.sh:490` through `scripts/setup-rlcr-loop.sh:521` and `scripts/setup-rlcr-loop.sh:527` through `scripts/setup-rlcr-loop.sh:684`.
- gates_or_invariants: The spec requires plan paths to be relative, existing, readable, inside the project, free of spaces and shell metacharacters, not symlinks, and not under git submodules. The corresponding production checks are at `scripts/setup-rlcr-loop.sh:227` through `scripts/setup-rlcr-loop.sh:328`. The tests cover absolute path rejection at `tests/test-plan-file-validation.sh:93` through `tests/test-plan-file-validation.sh:102`, missing file and missing parent directory at `tests/test-plan-file-validation.sh:104` through `tests/test-plan-file-validation.sh:126`, spaces at `tests/test-plan-file-validation.sh:128` through `tests/test-plan-file-validation.sh:167`, metacharacters at `tests/test-plan-file-validation.sh:169` through `tests/test-plan-file-validation.sh:187`, symlink rejection at `tests/test-plan-file-validation.sh:189` through `tests/test-plan-file-validation.sh:200`, path resolution errors at `tests/test-plan-file-validation.sh:202` through `tests/test-plan-file-validation.sh:242`, project escape rejection at `tests/test-plan-file-validation.sh:245` through `tests/test-plan-file-validation.sh:272`, non-git rejection at `tests/test-plan-file-validation.sh:274` through `tests/test-plan-file-validation.sh:297`, and no-commit rejection at `tests/test-plan-file-validation.sh:299` through `tests/test-plan-file-validation.sh:323`.
- dependencies_and_callers: Directly depends on `scripts/setup-rlcr-loop.sh`, which itself sources `scripts/portable-timeout.sh` at `scripts/setup-rlcr-loop.sh:26` through `scripts/setup-rlcr-loop.sh:28` and shells out to git. The test also depends on git being available and writable temp directories. It avoids relying on a real Codex install by injecting a mock binary when absent. Production tracking validation depends on `git status --porcelain` and `git ls-files --error-unmatch`; see `scripts/setup-rlcr-loop.sh:334` through `scripts/setup-rlcr-loop.sh:352`.
- edge_cases_or_failure_modes: The file is regression-heavy. It checks parent-directory permission/path-resolution failures with a conditional skip if `chmod 000` is unsupported; see `tests/test-plan-file-validation.sh:226` through `tests/test-plan-file-validation.sh:242`. It accepts that git itself may reject YAML-unsafe branch names before the setup script sees them; colon, hash, and quote branch cases are handled at `tests/test-plan-file-validation.sh:460` through `tests/test-plan-file-validation.sh:512`. It verifies content validation rejects blank-only, sparse, HTML-comment-only, and hash-comment-only plans; see `tests/test-plan-file-validation.sh:518` through `tests/test-plan-file-validation.sh:608`. It also verifies a single-line HTML comment does not incorrectly put the parser into multiline-comment mode; see `tests/test-plan-file-validation.sh:637` through `tests/test-plan-file-validation.sh:660`.
- validation_or_tests: This file is itself the validation asset. The production content gate requires at least five physical lines and at least three content lines after excluding blank lines, lines starting with `#`, single-line HTML comments, and multiline HTML comments; see `scripts/setup-rlcr-loop.sh:385` through `scripts/setup-rlcr-loop.sh:438`. The tracking policy tests enforce that tracked plans are rejected unless `--track-plan-file` is used, untracked plans are rejected when `--track-plan-file` is used, and modified tracked plans are rejected even with the flag; see `tests/test-plan-file-validation.sh:329` through `tests/test-plan-file-validation.sh:426`. CLI parsing tests verify `--plan-file` is accepted and that combining explicit and positional plan files fails; see `tests/test-plan-file-validation.sh:667` through `tests/test-plan-file-validation.sh:694`. Codex parameter tests reject YAML-unsafe model/effort strings and accept alphanumeric/dot/hyphen model names; see `tests/test-plan-file-validation.sh:700` through `tests/test-plan-file-validation.sh:738`, implemented by `scripts/setup-rlcr-loop.sh:468` through `scripts/setup-rlcr-loop.sh:484`.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-075 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Blocking prompt template used when an agent attempts to exit an active RLCR loop without writing the required summary file. It is a compact review-contract gate: no summary means no Codex review and no loop transition to the next review phase.
- algorithmic_behavior: The template renders a fixed instruction headed "Work Summary Missing", substitutes `{{SUMMARY_FILE}}`, and tells the agent to write a summary containing implementation, files changed, tests passed, and remaining work before attempting exit again. See `prompt-template/block/work-summary-missing.md:1` through `prompt-template/block/work-summary-missing.md:16`.
- inputs_outputs_state: Input is the `SUMMARY_FILE` variable supplied by `hooks/loop-codex-stop-hook.sh`. During normal rounds it points to `round-${CURRENT_ROUND}-summary.md`; during Finalize Phase it points to `finalize-summary.md`; see `hooks/loop-codex-stop-hook.sh:525` through `hooks/loop-codex-stop-hook.sh:530`. Output is a rendered block reason inside the hook's JSON response with `"decision": "block"` and a system message; see `hooks/loop-codex-stop-hook.sh:532` through `hooks/loop-codex-stop-hook.sh:555`.
- gates_or_invariants: The invariant is that a summary file must exist before exit can progress. If missing, the stop hook returns a block decision before building a Codex review prompt or running Codex. The Finalize Phase path shares this invariant but uses `finalize-summary.md`; see `hooks/loop-codex-stop-hook.sh:525` through `hooks/loop-codex-stop-hook.sh:543`. The template loader performs single-pass substitution and preserves missing placeholders rather than recursively expanding injected placeholder-like content; see `hooks/lib/template-loader.sh:50` through `hooks/lib/template-loader.sh:132`.
- dependencies_and_callers: Called through `load_and_render_safe "$TEMPLATE_DIR" "block/work-summary-missing.md"` in `hooks/loop-codex-stop-hook.sh:536` through `hooks/loop-codex-stop-hook.sh:540`. It depends on the template loader fallback behavior: if the template is missing or renders empty, a shorter fallback message is used; see `hooks/lib/template-loader.sh:167` through `hooks/lib/template-loader.sh:203`.
- edge_cases_or_failure_modes: If the template file is absent, the hook still blocks with fallback text, so missing template content degrades messaging but not enforcement. If `SUMMARY_FILE` contains unusual text, substitution is single-pass through environment-backed `awk`, reducing placeholder injection risk. In Finalize Phase, a missing final summary blocks completion and Codex is not invoked; this is validated separately in `tests/test-finalize-phase.sh:370` through `tests/test-finalize-phase.sh:392`.
- validation_or_tests: The template reference is discoverable in template-reference tests through the general `load_and_render_safe` call pattern, but the concrete behavior is covered by stop-hook tests. The Finalize Phase missing-summary test removes `finalize-summary.md`, invokes `hooks/loop-codex-stop-hook.sh`, expects a block decision mentioning summary, and verifies no Codex marker was written; see `tests/test-finalize-phase.sh:370` through `tests/test-finalize-phase.sh:392`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3 item evidence sections present, one per assigned row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`