# agent_17 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-017 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR loop. It is a thin command surface that delegates execution to `scripts/setup-rlcr-loop.sh`, but it defines the user-facing algorithm contract: iterative implementation, summary writing, Codex review, feedback continuation, completion detection, goal tracking, and loop cancellation.
- algorithmic_behavior: Frontmatter declares the command description, argument shape, tool allowlist, and hidden slash-command behavior at `commands/start-rlcr-loop.md:1-5`. The actual command action is the setup script invocation with `$ARGUMENTS` at `commands/start-rlcr-loop.md:10-14`. The workflow contract says the implementing agent works from a plan, writes a summary, Codex reviews on exit, issues cause another iteration, and `COMPLETE` terminates the loop at `commands/start-rlcr-loop.md:16-23`. The goal tracker layer prevents drift by maintaining immutable goal/acceptance criteria and mutable task/progress/evolution sections at `commands/start-rlcr-loop.md:24-42`.
- inputs_outputs_state: Input is `<path/to/plan.md>` plus optional flags `--max`, `--codex-model`, `--codex-timeout`, and `--push-every-round`, declared at `commands/start-rlcr-loop.md:3`. Those arguments are passed directly to `scripts/setup-rlcr-loop.sh` at `commands/start-rlcr-loop.md:13`. The setup script validates plan path/content/git/codex prerequisites, creates `.humanize/rlcr/<timestamp>/`, writes `state.md`, copies `plan.md`, creates `goal-tracker.md`, and emits `round-0-prompt.md`; key setup state writes are in `scripts/setup-rlcr-loop.sh:490-521` and `scripts/setup-rlcr-loop.sh:624-684`. Runtime state advances through round files and hook-controlled review results rather than through this command file itself.
- gates_or_invariants: The command allows only `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*)`, limiting command execution surface at `commands/start-rlcr-loop.md:4`. User-facing invariants require summary writing, goal-tracker maintenance, detailed evidence, no state-file cheating or cancellation bypass, and trusting Codex feedback at `commands/start-rlcr-loop.md:43-49`. Stopping conditions are max iteration count, Codex `COMPLETE`, or `/humanize:cancel-rlcr-loop` at `commands/start-rlcr-loop.md:51-55`. The delegated setup script enforces stricter concrete gates: relative path only, no spaces/metacharacters, no symlink traversal, in-project file, submodule exclusion, git tracking policy, minimum plan content, codex availability, branch/model/effort YAML safety; see `scripts/setup-rlcr-loop.sh:227-248`, `scripts/setup-rlcr-loop.sh:253-328`, `scripts/setup-rlcr-loop.sh:334-379`, `scripts/setup-rlcr-loop.sh:385-446`, and `scripts/setup-rlcr-loop.sh:452-484`.
- dependencies_and_callers: Direct dependency is `scripts/setup-rlcr-loop.sh` via `${CLAUDE_PLUGIN_ROOT}`. The created loop is then enforced by hooks such as `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh`; those hooks share `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`. The cancellation counterpart is `commands/cancel-rlcr-loop.md` and `scripts/cancel-rlcr-loop.sh`, referenced by this command at `commands/start-rlcr-loop.md:55`.
- edge_cases_or_failure_modes: The command file itself has little logic, so failures mostly occur in the setup script. Notable setup failures include missing or duplicate plan args, absolute paths, spaces, metacharacters, symlinked file or parent directories, nonexistent/unreadable files, escaping project root, submodule plans, tracked plan without `--track-plan-file`, untracked plan with `--track-plan-file`, too few lines, insufficient non-comment content, missing Codex CLI, unsafe branch names, and unsafe model/effort values. A workflow-level failure mode is drift: the command mitigates this by requiring the Goal Tracker and full alignment checks after every four work rounds at `commands/start-rlcr-loop.md:33-37`.
- validation_or_tests: Related executable coverage lives primarily in plan/setup tests rather than this command markdown: `tests/test-plan-file-validation.sh`, `tests/robustness/test-plan-file-robustness.sh`, and `tests/robustness/test-path-validation-robustness.sh` exercise the setup script called by this command. The command’s own correctness is mostly validated by whether the allowed tool path, argument passthrough, and documented lifecycle stay aligned with `scripts/setup-rlcr-loop.sh`.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-047 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for the template rendering subsystem used by RLCR hooks and Codex/Claude prompt generation. It validates that templates load from `prompt-template/`, placeholders render safely, missing templates fall back correctly, and hostile or unusual values are inserted literally without shell or placeholder reinterpretation.
- algorithmic_behavior: The test sources `hooks/lib/template-loader.sh` at `tests/test-template-loader.sh:10-12`, initializes pass/fail counters at `tests/test-template-loader.sh:20-34`, then runs 41 focused assertions. Tests 1-14 cover directory resolution, file loading, missing-file empty behavior, single/multiple/multiline substitution, integration rendering, unused variables, unreplaced placeholders, safe fallback, and directory validation at `tests/test-template-loader.sh:41-244`. Tests 15-33 cover literal rendering of shell metacharacters and structured/multiline values at `tests/test-template-loader.sh:247-487`. Tests 34-36 are the key single-pass injection prevention checks: placeholder-shaped content inside values must not be recursively expanded at `tests/test-template-loader.sh:488-543`. Tests 37-41 cover empty substitution, Unicode, underscore-prefixed variables, and numeric variable names at `tests/test-template-loader.sh:545-639`. Summary exit behavior is pass/fail counted at `tests/test-template-loader.sh:641-659`.
- inputs_outputs_state: Inputs are the project root inferred from the test script location, the real template directory from `get_template_dir`, template names such as `block/git-push.md` and `block/wrong-round-number.md`, and `VAR=value` assignment arguments passed to renderer functions. Output is colored console diagnostics and process exit code `0` if `TESTS_FAILED` remains zero, otherwise `1` at `tests/test-template-loader.sh:651-658`. Internal state is only `TESTS_PASSED` and `TESTS_FAILED`. The production loader under test builds environment variables with a `TMPL_VAR_` prefix at `hooks/lib/template-loader.sh:62-69`, scans template content once with `awk` at `hooks/lib/template-loader.sh:71-129`, and emits rendered content at `hooks/lib/template-loader.sh:131`.
- gates_or_invariants: The test enforces these invariants: `get_template_dir` resolves from `hooks/lib` to `prompt-template` (`tests/test-template-loader.sh:41-52`); missing templates return empty for `load_template` (`tests/test-template-loader.sh:67-78`); missing variables remain as placeholders (`tests/test-template-loader.sh:181-194`); missing templates use the provided fallback in `load_and_render_safe` (`tests/test-template-loader.sh:196-208`); existing templates override fallback (`tests/test-template-loader.sh:210-222`); valid template dirs must include expected subdirectories indirectly via `validate_template_dir` (`tests/test-template-loader.sh:224-244`, production check at `hooks/lib/template-loader.sh:205-222`); value content containing `&`, backslashes, `$`, backticks, pipes, semicolons, glob syntax, redirects, quotes, hashes, tildes, regex-like strings, JSON, and newlines must be literal (`tests/test-template-loader.sh:257-487`); injected `{{...}}` inside values must not trigger a second replacement pass (`tests/test-template-loader.sh:499-543`).
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`, especially `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`. Production callers are initialized through `hooks/lib/loop-common.sh`, which sources the loader and sets `TEMPLATE_DIR` at `hooks/lib/loop-common.sh:137-143`. The loaded templates are used broadly by read/write/edit/bash validators and the Codex stop hook; examples include `hooks/loop-read-validator.sh:124-127`, `hooks/loop-write-validator.sh:210-214`, `hooks/loop-edit-validator.sh:144-148`, and `hooks/loop-codex-stop-hook.sh:723-740`.
- edge_cases_or_failure_modes: The strongest covered failure mode is unsafe replacement semantics. A naive `sed` or recursive substitution implementation could interpret `&`, backslashes, shell syntax, or placeholder-shaped review text and corrupt prompts; tests 15-36 make that regression visible. Other edge cases include empty values, non-ASCII text, multiline values, unused variables, unknown placeholders, missing templates, invalid template directories, and variable names beginning with underscore or containing numbers. One notable test naming issue: comments for tests 38 and 39 call some ASCII cases “Unicode,” while 38.1 and 39.1 exercise actual non-ASCII values at `tests/test-template-loader.sh:579-610`.
- validation_or_tests: This file is itself validation. It exits nonzero if any assertion fails, so it is suitable as a direct regression test for prompt/template algorithm changes. It overlaps with broader coverage in `tests/test-templates-comprehensive.sh`, which also checks safe fallback and real template rendering at `tests/test-templates-comprehensive.sh:456-540`.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-077 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Prompt block template for the read validator when a user/agent tries to read a round prompt or summary file from outside the active `.humanize/rlcr/<session>/` loop directory. It is a small but core transition message: the validator blocks the attempted read and redirects attention to the active loop’s current-round files.
- algorithmic_behavior: The template has three placeholders: `{{FILE_PATH}}`, `{{ACTIVE_LOOP_DIR}}`, and `{{CURRENT_ROUND}}`. It renders a title, says the attempted file is outside the active loop directory, enumerates current prompt and summary paths, and gives a `cat {{FILE_PATH}}` command for cases where the user still needs the exact attempted file. See `prompt-template/block/wrong-file-location.md:1-9`. In production, `hooks/loop-read-validator.sh` selects this template only after it has identified a read of a round prompt/summary file and determined the path is not under `.humanize/rlcr/` at `hooks/loop-read-validator.sh:66-79` and `hooks/loop-read-validator.sh:119-128`.
- inputs_outputs_state: Inputs are supplied by `load_and_render_safe` in `hooks/loop-read-validator.sh`: the attempted `FILE_PATH`, resolved `ACTIVE_LOOP_DIR`, and parsed `CURRENT_ROUND` at `hooks/loop-read-validator.sh:124-127`. Output is rendered markdown written to stderr, followed by validator exit code `2` at `hooks/loop-read-validator.sh:127-128`, which means the tool use is blocked with a user-facing reason. The template does not mutate state. State is read from `state.md` or `finalize-state.md`, selected at `hooks/loop-read-validator.sh:85-89`, then parsed with strict state validation at `hooks/loop-read-validator.sh:91-96`.
- gates_or_invariants: The block is guarded by several upstream invariants: only `Read` tool input is processed (`hooks/loop-read-validator.sh:34-38`); `file_path` is required (`hooks/loop-read-validator.sh:40-45`); todos files are separately blocked unless allowlisted (`hooks/loop-read-validator.sh:48-60`); non-round prompt/summary files are allowed (`hooks/loop-read-validator.sh:66-68`); malformed active state blocks safely (`hooks/loop-read-validator.sh:91-94`). For this template specifically, the triggering invariant is `IN_HUMANIZE_LOOP_DIR == false` at `hooks/loop-read-validator.sh:119`. Later gates handle wrong round number and wrong active session directory with different templates at `hooks/loop-read-validator.sh:135-164`.
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh`. It depends on `hooks/lib/loop-common.sh` for path predicates and state parsing, including `is_in_humanize_loop_dir`, which detects `.humanize/rlcr/` paths at `hooks/lib/loop-common.sh:616-620`. Rendering depends on `hooks/lib/template-loader.sh`; `loop-common.sh` sources it and sets/validates `TEMPLATE_DIR` at `hooks/lib/loop-common.sh:137-143`. The closest sibling prompt is `prompt-template/block/wrong-round-file.md`, which shares the “current round files” structure but handles current-round mismatch instead of wrong directory location.
- edge_cases_or_failure_modes: If the template file is missing or empty, `load_and_render_safe` falls back to a shorter inline message at `hooks/loop-read-validator.sh:121-124`. If `ACTIVE_LOOP_DIR` is empty, the validator exits without blocking at `hooks/loop-read-validator.sh:81-83`, so this block is only used when an active loop exists. If the state file is malformed, the validator fails closed before rendering at `hooks/loop-read-validator.sh:91-94`. A subtle content edge case is that the final line says “If you need this file, use: `cat {{FILE_PATH}}`” even though the read is blocked because the path is outside the loop; this appears intentional as a Bash-based escape hatch rather than a Read-tool allowance, but it may be confusing if the intended guidance is to prefer the active-loop current files.
- validation_or_tests: There is no direct assertion for this exact template in `tests/test-template-loader.sh`; that file validates the renderer and uses related real templates such as `block/git-push.md` and `block/wrong-round-number.md` at `tests/test-template-loader.sh:59-64` and `tests/test-template-loader.sh:151-164`. Broader template integration tests similarly cover `block/wrong-round-number.md` at `tests/test-templates-comprehensive.sh:497-514`. This template’s production behavior is indirectly covered where read-validator tests exercise wrong-location behavior, if present in the larger suite, but this assigned file itself has no standalone test assertion observed.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item evidence sections present; each assigned item id is used only as its section heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`