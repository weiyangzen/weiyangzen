# agent_20 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-020 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse validator for Bash commands during an active RLCR loop. It prevents shell-level bypasses of the normal Read/Write/Edit hook contract for loop state, summaries, prompts, todos, goal tracking, plan backups, git pushes, and `.humanize` git staging. It is registered as the Bash PreToolUse hook in `hooks/hooks.json:42`.
- algorithmic_behavior: The script reads hook JSON from stdin, validates it through `validate_hook_input`, rejects overly deep JSON, exits early for non-Bash tools, then extracts `.tool_input.command` (`hooks/loop-bash-validator.sh:22`, `:25`, `:30`, `:36`, `:41`, `:45`). It discovers the newest active loop under `.humanize/rlcr`, allows all Bash if no loop exists, chooses `finalize-state.md` over `state.md` during finalize phase, and strict-parses state before enforcing gates (`:52`, `:54`, `:57`, `:61`, `:67`). Command matching is mostly lowercased regex detection against write-like shell forms via `command_modifies_file` from `hooks/lib/loop-common.sh:785`, with additional special parsing for `mv`/`cp` source-side state file moves.
- inputs_outputs_state: Input is Claude hook JSON with `tool_name` and Bash `command`. Outputs are process exit codes and stderr block messages: `0` allows, `1` signals malformed hook/state validation failure, and `2` blocks the command with a rendered template/fallback. It reads `.humanize/rlcr/<active-loop>/state.md` or `finalize-state.md`; it does not advance RLCR state itself. The relevant state variables are `STATE_CURRENT_ROUND` and `STATE_PUSH_EVERY_ROUND`, populated by `parse_state_file_strict` in `hooks/lib/loop-common.sh:240`.
- gates_or_invariants: Blocks `git push` unless `push_every_round: true` (`hooks/loop-bash-validator.sh:82`). Blocks git add attempts that would stage `.humanize` content (`:100`). Blocks writes or moves targeting `finalize-state.md` and `state.md`, including destination writes and source-side `mv`/`cp` attempts (`:117`, `:127`, `:147`). Source-side detection splits chained shell segments, protects redirection operators with placeholders, strips leading redirections, then checks state/finalize source patterns (`:152`, `:157`, `:186`, `:264`). It also catches `sh -c` / `bash -c` wrappers containing `mv`/`cp` state payloads (`:287`). Plan backup writes under `.humanize/rlcr/.../plan.md` are blocked (`:314`). `goal-tracker.md` writes are blocked with round-aware guidance: round 0 says use Write/Edit; later rounds say request the update in the current summary (`:327`). Prompt writes to `round-N-prompt.md` are blocked (`:343`), Bash writes to `round-N-summary.md` are blocked and redirected to the current round summary path (`:353`), and `round-N-todos.md` Bash writes are blocked unless the command names the active loop directory and `round-1-todos.md` or `round-2-todos.md` (`:363`).
- dependencies_and_callers: Depends on `jq`, `grep`, `sed`, `tr`, `git check-ignore`, and shared hook functions from `hooks/lib/loop-common.sh` loaded at `hooks/loop-bash-validator.sh:16`. Key shared dependencies include JSON validation (`hooks/lib/loop-common.sh:55`), active-loop discovery (`:152`), strict state parsing (`:240`), cancel authorization (`:455`), `.humanize` git-add detection (`:644`), and file-modification pattern matching (`:785`). Message rendering flows through `load_and_render_safe` in `hooks/lib/template-loader.sh:170`. Callers are Claude Code PreToolUse Bash hook registration in `hooks/hooks.json:42`.
- edge_cases_or_failure_modes: No active loop means the validator permits all Bash, so protection is intentionally scoped to active RLCR sessions. Malformed state fails closed with exit `1` (`hooks/loop-bash-validator.sh:68`). Cancel-state moves are permitted only when `.cancel-requested` exists and `is_cancel_authorized` validates an exact `mv state.md|finalize-state.md cancel-state.md` shape with no command substitution, shell operators, extra args, mixed quote styles, or symlink source (`hooks/lib/loop-common.sh:461`, `:467`, `:477`, `:538`, `:579`, `:600`). The generic `command_modifies_file` helper detects common writes (`>`, `>>`, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`, `cp`, `rm`, `dd of=`, `truncate`, `printf >`, `exec >`) but has known pattern limits: tests document that multi-source `cp file1 file2 goal-tracker.md` is not detected and variable-indirect writes like `echo x > $FILE` are not matched (`tests/test-bash-validator-patterns.sh:165`, `:170`). The whole command is lowercased, favoring broad case-insensitive detection over preserving case-sensitive path semantics.
- validation_or_tests: Related coverage includes `tests/test-bash-validator-patterns.sh` for `command_modifies_file`, including state and summary patterns (`tests/test-bash-validator-patterns.sh:181`, `:192`); `tests/test-finalize-phase.sh` for finalize-state handling; `tests/test-cancel-signal-file.sh` for authorized cancel moves; `tests/test-allowlist-validators.sh` for round file allowlisting; `tests/test-plan-file-hooks.sh` for plan backup behavior; and `tests/robustness/test-hook-input-robustness.sh` for hook input hardening. I did not modify files. I did not run the full validator suite; the only attempted execution was the assigned todo test, which failed at sandbox `mktemp`, unrelated to this file.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-050 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for the transcript todo checker used by the RLCR stop hook. It verifies that Claude cannot exit the loop while its latest TodoWrite state still contains pending or in-progress work.
- algorithmic_behavior: The test resolves `hooks/check-todos-from-transcript.py` from the repo root (`tests/test-todo-checker.sh:11`) and runs it with synthetic hook JSON and temporary transcript JSONL files. It asserts the checker’s exit-code protocol: parse errors return `2`, no transcript or no todos returns `0`, all latest todos completed returns `0`, and any latest todo whose status is not exactly `completed` returns `1`. The implementation under test scans JSONL, ignores invalid transcript lines, recognizes assistant-message, message, and direct `tool_use` TodoWrite formats, and keeps the most recent non-empty TodoWrite list (`hooks/check-todos-from-transcript.py:20`, `:51`, `:65`, `:78`).
- inputs_outputs_state: Test input is generated hook JSON on stdin to the checker plus temporary JSONL transcript fixtures. Checker output is exit status plus, for incomplete todos, stdout headed by `INCOMPLETE_TODOS` and per-todo lines like `- [pending] Task` (`hooks/check-todos-from-transcript.py:121`). Test script state is only local counters `TESTS_PASSED` and `TESTS_FAILED` plus files under `mktemp -d`; no repository state is intended to change (`tests/test-todo-checker.sh:20`, `:37`, `:305`).
- gates_or_invariants: Test Group 1 covers invalid JSON and empty stdin as exit `2`, missing `transcript_path` as allow, and nonexistent transcript file as allow (`tests/test-todo-checker.sh:51`, `:63`, `:75`, `:87`). Test Group 2 covers all completed todos, pending todos, output details, and `in_progress` as incomplete (`:106`, `:121`, `:136`, `:144`). Test Group 3 covers empty transcripts, invalid JSONL lines being ignored, multiple TodoWrite calls using the latest one, direct `tool_use`, and alternate `type: message` format (`:166`, `:179`, `:196`, `:212`, `:227`). Test Group 4 covers missing status as incomplete, empty content as still incomplete, and a nominal Unicode-content path (`:249`, `:265`, `:280`).
- dependencies_and_callers: Depends on `bash`, `python3`, `mktemp`, and `hooks/check-todos-from-transcript.py`. The checker is called by `hooks/loop-codex-stop-hook.sh:261`; when checker exit is `1`, the stop hook renders `block/incomplete-todos.md` and blocks before expensive Codex review (`hooks/loop-codex-stop-hook.sh:287`). When checker exit is `2`, the stop hook blocks with a parse-error reason (`:268`).
- edge_cases_or_failure_modes: The spec intentionally allows missing transcript paths, nonexistent files, empty transcript files, and transcripts with no TodoWrite calls; this prevents hook failure from blocking when no todo state exists. Any status other than literal `completed` is incomplete, including missing status and `in_progress`. Invalid JSONL lines inside transcripts are ignored, while invalid hook input JSON is fatal. The checker only updates `latest_todos` when a TodoWrite list is non-empty (`hooks/check-todos-from-transcript.py:62`, `:75`, `:84`), so a later empty TodoWrite call would not clear an earlier list; this edge is not covered by the test. The test named “Unicode in todo content” uses the ASCII string `Task with unicode` (`tests/test-todo-checker.sh:283`), so it does not actually prove non-ASCII handling despite the test name.
- validation_or_tests: Source inspected directly. I attempted to run `bash tests/test-todo-checker.sh`, but this read-only managed sandbox denied `mktemp` under `/var/folders/...` with `Operation not permitted`, so no test result can be claimed from execution here. The failure occurred before checker logic ran and is an environment permission issue, not a test assertion failure.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-080 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Small block-message template used by Write validation when a round summary file is written outside the active RLCR loop directory. It is part of the hook contract surface: it tells the agent where the summary must go.
- algorithmic_behavior: The template is Markdown with one variable, `{{CORRECT_PATH}}` (`prompt-template/block/wrong-summary-location.md:1`). It is loaded by `hooks/loop-write-validator.sh` when `IS_SUMMARY_FILE == true` and the target path is not inside `.humanize/rlcr` (`hooks/loop-write-validator.sh:174`). The write validator computes the correct path as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md`, renders this template through `load_and_render_safe`, writes it to stderr, and exits `2` (`hooks/loop-write-validator.sh:175`, `:179`, `:181`).
- inputs_outputs_state: Input is the `CORRECT_PATH` template variable supplied by the write validator. Output is a rendered Markdown block beginning `# Wrong Summary Location` and containing the correct file path. It has no persistent state transition by itself; it supports the validator’s block decision.
- gates_or_invariants: Enforces the invariant that round summaries must be in the active loop directory, not arbitrary working directories. It is checked after finalize-summary allowance, so `finalize-summary.md` in the active loop directory can still be written during finalize phase (`hooks/loop-write-validator.sh:135`, `:140`). It is separate from the Bash summary-write block, which uses `block/summary-bash-write.md` via `summary_bash_blocked_message` (`hooks/lib/loop-common.sh:387`).
- dependencies_and_callers: Direct caller is `hooks/loop-write-validator.sh:179`. Rendering depends on `hooks/lib/template-loader.sh`, whose renderer performs single-pass `{{VAR}}` substitution and leaves missing variables intact (`hooks/lib/template-loader.sh:50`, `:116`, `:120`). `load_and_render_safe` falls back to inline text if the template is missing or renders empty (`hooks/lib/template-loader.sh:170`, `:179`, `:192`). Repository search found this template path referenced in the write validator, not in the Bash validator.
- edge_cases_or_failure_modes: If `CORRECT_PATH` is not supplied, the placeholder remains visible because the template loader preserves unknown placeholders. If the template is missing, the write validator’s fallback still blocks and reports a correct path. The message is intentionally minimal; it does not explain round-number mismatches or wrong file types, which are handled by sibling templates such as `wrong-round-number.md` and `wrong-file-location.md`.
- validation_or_tests: `tests/test-template-references.sh` performs template reference/existence checks across hook templates, and `rg` confirms this specific template is directly referenced by `hooks/loop-write-validator.sh`. I did not run the template test suite because the sandbox profile is read-only and already blocked the assigned shell test at `mktemp`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item evidence headings present; each assigned item is represented once above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`