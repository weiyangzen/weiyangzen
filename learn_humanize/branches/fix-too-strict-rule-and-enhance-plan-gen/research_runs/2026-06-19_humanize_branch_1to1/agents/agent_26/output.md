# agent_26 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-026 `file` `scripts/cancel-rlcr-loop.sh`
- cursor: `[_]`
- core_role: User-facing cancellation command for an active RLCR loop. It converts the active loop state into a preserved cancellation state by creating a `.cancel-requested` authorization signal and renaming `state.md` or `finalize-state.md` to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:5`, `scripts/cancel-rlcr-loop.sh:134`, `scripts/cancel-rlcr-loop.sh:138`).
- algorithmic_behavior: Parses only `--force` and help (`scripts/cancel-rlcr-loop.sh:24`, `scripts/cancel-rlcr-loop.sh:26`). Resolves `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, then chooses the lexicographically newest child under `.humanize/rlcr` (`scripts/cancel-rlcr-loop.sh:69`, `scripts/cancel-rlcr-loop.sh:73`). It classifies the loop as normal if `state.md` exists, finalize if `finalize-state.md` exists, otherwise inactive (`scripts/cancel-rlcr-loop.sh:85`, `scripts/cancel-rlcr-loop.sh:92`, `scripts/cancel-rlcr-loop.sh:96`). In finalize phase, it refuses to cancel unless `--force` is provided (`scripts/cancel-rlcr-loop.sh:117`). It emits machine-readable first-line statuses: `NO_LOOP`, `NO_ACTIVE_LOOP`, `FINALIZE_NEEDS_CONFIRM`, `CANCELLED`, or `CANCELLED_FINALIZE` (`scripts/cancel-rlcr-loop.sh:76`, `scripts/cancel-rlcr-loop.sh:118`, `scripts/cancel-rlcr-loop.sh:145`, `scripts/cancel-rlcr-loop.sh:149`).
- inputs_outputs_state: Inputs are CLI flags, `CLAUDE_PROJECT_DIR`, `.humanize/rlcr/*/state.md`, and `.humanize/rlcr/*/finalize-state.md`. Outputs are stdout status text, exit code `0/1/2/3`, a touched `.cancel-requested`, and renamed `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:11`, `scripts/cancel-rlcr-loop.sh:87`, `scripts/cancel-rlcr-loop.sh:134`). State transition is `state.md -> cancel-state.md` for normal loops and `finalize-state.md -> cancel-state.md` for forced finalize cancellation. It preserves round metadata only as display text by extracting `current_round` and `max_iterations` before rename (`scripts/cancel-rlcr-loop.sh:105`).
- gates_or_invariants: Finalize cancellation requires explicit force unless the wrapper asks the user first (`scripts/cancel-rlcr-loop.sh:117`). The slash command wrapper only allows this script, the force variant, and `AskUserQuestion`, and tells the agent to branch based on the first output line (`commands/cancel-rlcr-loop.md:3`, `commands/cancel-rlcr-loop.md:17`). Bash hook protection separately permits the state-file rename only when `.cancel-requested` exists and the command exactly matches the safe state/finalize-to-cancel move shape (`hooks/loop-bash-validator.sh:117`, `hooks/loop-bash-validator.sh:127`, `hooks/lib/loop-common.sh:455`, `hooks/lib/loop-common.sh:585`).
- dependencies_and_callers: Called by `/humanize:cancel-rlcr-loop` command definition (`commands/cancel-rlcr-loop.md:11`). Coordinates with `find_active_loop`, which also treats only the newest loop directory as relevant and only `state.md`/`finalize-state.md` as active markers (`hooks/lib/loop-common.sh:147`, `hooks/lib/loop-common.sh:165`). Coordinates with `is_cancel_authorized`, which validates the signal file, rejects command substitution/chaining/newlines, requires exactly two mv args, validates destination `cancel-state.md`, and rejects symlink source state files (`hooks/lib/loop-common.sh:461`, `hooks/lib/loop-common.sh:466`, `hooks/lib/loop-common.sh:579`, `hooks/lib/loop-common.sh:593`, `hooks/lib/loop-common.sh:609`).
- edge_cases_or_failure_modes: Newest-directory selection can ignore an older still-active loop if a newer loop dir exists without `state.md` or `finalize-state.md`; this is consistent with the shared helper’s “newest only” design (`hooks/lib/loop-common.sh:147`). If both `state.md` and `finalize-state.md` exist, the script selects `state.md` first and may leave `finalize-state.md` behind (`scripts/cancel-rlcr-loop.sh:89`). The intended fallback `?` values for missing round fields are fragile because `set -euo pipefail` plus `grep` inside command substitution can exit before default assignment if a malformed state file lacks the fields (`scripts/cancel-rlcr-loop.sh:18`, `scripts/cancel-rlcr-loop.sh:106`, `scripts/cancel-rlcr-loop.sh:110`). A failed `mv` after `touch` may leave `.cancel-requested` behind (`scripts/cancel-rlcr-loop.sh:134`). Directory ordering assumes timestamp-like names sort newest with `sort -r` (`scripts/cancel-rlcr-loop.sh:73`).
- validation_or_tests: `tests/test-cancel-signal-file.sh` validates that state-to-cancel moves are allowed only with `.cancel-requested`, and that other state writes remain blocked (`tests/test-cancel-signal-file.sh:3`, `tests/test-cancel-signal-file.sh:78`, `tests/test-cancel-signal-file.sh:119`, `tests/test-cancel-signal-file.sh:136`). `tests/robustness/test-cancel-security-robustness.sh` exercises the authorization parser against injection and path tricks, per search hits. `tests/test-state-exit-naming.sh` confirms `cancel-state.md` is not treated as an active loop marker (`tests/test-state-exit-naming.sh:93`). Not executed in this read-only research pass.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-056 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: Static block-message template for the Bash pre-tool gate that prevents `.humanize/` loop state from being staged. It is not the detector itself; it is the user-visible contract emitted when `git_adds_humanize` blocks a command (`prompt-template/block/git-add-humanize.md:1`, `hooks/loop-bash-validator.sh:100`).
- algorithmic_behavior: The template explains the invariant that `.humanize/` is local loop state and should not be committed (`prompt-template/block/git-add-humanize.md:3`). It tells the agent to use specific paths or patch mode (`prompt-template/block/git-add-humanize.md:10`) and lists blocked command patterns including direct `.humanize`, `git add -A`, `git add --all`, broad `git add .`, and forced broad add (`prompt-template/block/git-add-humanize.md:18`). The actual runtime path is `loop-bash-validator.sh -> git_adds_humanize -> git_add_humanize_blocked_message -> load_and_render_safe(...)` (`hooks/loop-bash-validator.sh:100`, `hooks/lib/loop-common.sh:742`, `hooks/lib/loop-common.sh:779`).
- inputs_outputs_state: Template has no placeholders and consumes no dynamic inputs. Output is Markdown written to stderr by the Bash validator when the command is blocked; the validator exits with code `2` (`hooks/loop-bash-validator.sh:100`, `hooks/loop-bash-validator.sh:102`). No repository state is modified by the template. The detector reads the current shell command, whether `.humanize` exists, and whether `.humanize` is ignored by Git (`hooks/lib/loop-common.sh:644`, `hooks/lib/loop-common.sh:720`, `hooks/lib/loop-common.sh:733`).
- gates_or_invariants: Direct `.humanize` path references are blocked regardless of flags (`hooks/lib/loop-common.sh:681`). Force plus broad scope/all is blocked because it bypasses ignore rules (`hooks/lib/loop-common.sh:689`, `hooks/lib/loop-common.sh:712`). `git add -A` or `--all` is blocked whenever `.humanize` exists (`hooks/lib/loop-common.sh:724`). Broad `git add .` or `git add *` is blocked only if `.humanize` exists and is not ignored (`hooks/lib/loop-common.sh:730`). The function receives lowercased commands, so it treats lowercase `-a` as the normalized form of original `-A` (`hooks/lib/loop-common.sh:626`, `hooks/lib/loop-common.sh:697`).
- dependencies_and_callers: Depends on the template loader; if the template is missing or empty, `load_and_render_safe` emits an inline fallback with the same behavioral guidance (`hooks/lib/template-loader.sh:167`, `hooks/lib/loop-common.sh:745`). Called only from the Bash validator while an active loop exists; no active loop allows commands through before this gate (`hooks/loop-bash-validator.sh:52`, `hooks/loop-bash-validator.sh:57`).
- edge_cases_or_failure_modes: The template says `.humanize/` is already in `.gitignore`, but the detector’s broad-scope behavior distinguishes ignored and not ignored states; the message can be slightly overgeneral if the repo is not actually ignoring it (`prompt-template/block/git-add-humanize.md:4`, `hooks/lib/loop-common.sh:733`). The current detector intentionally blocks `git add -A src/` whenever `.humanize` exists because the all flag is present, even though the template’s allowed examples include `git add src/`; tests explicitly assert this strict behavior (`tests/test-humanize-escape.sh:139`). Lowercasing allows detection of original `-A`, but it also means any short add flag combination containing `a` is treated as all-like (`hooks/lib/loop-common.sh:697`). The commit-message warning avoids the literal string `.humanize`, while the sample message itself uses “humanize local folder” and is therefore compatible (`prompt-template/block/git-add-humanize.md:32`, `prompt-template/block/git-add-humanize.md:34`).
- validation_or_tests: `tests/test-humanize-escape.sh` directly exercises `git_adds_humanize` for path variants, quoted paths, force flags, all flags, chained commands, `git -C`, allowed specific paths, patch mode, and non-`.humanize` lookalikes (`tests/test-humanize-escape.sh:41`, `tests/test-humanize-escape.sh:83`, `tests/test-humanize-escape.sh:96`, `tests/test-humanize-escape.sh:109`, `tests/test-humanize-escape.sh:135`, `tests/test-humanize-escape.sh:153`, `tests/test-humanize-escape.sh:175`, `tests/test-humanize-escape.sh:185`, `tests/test-humanize-escape.sh:190`). `tests/test-template-references.sh` checks referenced templates exist (`tests/test-template-references.sh:57`, `tests/test-template-references.sh:94`). `tests/test-templates-comprehensive.sh` loads all templates and validates syntax (`tests/test-templates-comprehensive.sh:83`, `tests/test-templates-comprehensive.sh:116`). Not executed in this read-only research pass.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-086 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Static prompt note appended to Claude’s next-round instructions when the loop was started with `--push-every-round`. It reinforces a push-after-commit contract for each round (`prompt-template/claude/push-every-round-note.md:2`).
- algorithmic_behavior: The note itself is one sentence. The controlling algorithm starts in setup arg parsing, where `--push-every-round` sets `PUSH_EVERY_ROUND=true` (`scripts/setup-rlcr-loop.sh:148`). That value is persisted in state frontmatter as `push_every_round` (`scripts/setup-rlcr-loop.sh:515`). During stop-hook prompt generation, if `PUSH_EVERY_ROUND` is true, the hook loads this template and appends it to the next prompt; if missing, it falls back to “Also push your changes after committing” (`hooks/loop-codex-stop-hook.sh:1099`, `hooks/loop-codex-stop-hook.sh:1101`, `hooks/loop-codex-stop-hook.sh:1103`, `hooks/loop-codex-stop-hook.sh:1105`).
- inputs_outputs_state: Input is the parsed/persisted `push_every_round` boolean. Output is extra prompt text in `round-N-prompt.md`; no direct state mutation is performed by the template. The initial round-zero prompt does not load this template but writes the same note inline when the flag is set (`scripts/setup-rlcr-loop.sh:686`, `scripts/setup-rlcr-loop.sh:690`).
- gates_or_invariants: This note is advisory prompt content, but it is paired with hard gates. If `push_every_round` is false, the Bash validator blocks `git push` and points users to restart with the flag if pushing is needed (`hooks/loop-bash-validator.sh:82`, `hooks/loop-bash-validator.sh:84`, `hooks/loop-bash-validator.sh:89`). If `push_every_round` is true, the stop hook blocks exit when `git status -sb` reports the local branch is ahead of remote (`hooks/loop-codex-stop-hook.sh:492`, `hooks/loop-codex-stop-hook.sh:494`, `hooks/loop-codex-stop-hook.sh:508`). README documents the flag as “Require git push after each round” (`README.md:99`).
- dependencies_and_callers: Loaded through `load_template`, not `load_and_render_safe`, because it has no placeholders and the caller supplies a manual fallback (`hooks/loop-codex-stop-hook.sh:1101`). Related block templates are `prompt-template/block/git-push.md` for disallowed pushes when the flag is false and `prompt-template/block/unpushed-commits.md` for stop-time unpushed commits when the flag is true (`prompt-template/block/git-push.md:1`, `prompt-template/block/unpushed-commits.md:1`).
- edge_cases_or_failure_modes: The note does not mention branch or remote, so it relies on general Git defaults. The enforcement gate detects only `git status -sb` text matching `ahead N`; branches without upstream, unusual status formatting, or localized Git output may not trigger the unpushed-commit block even when pushes are expected (`hooks/loop-codex-stop-hook.sh:494`). Template changes affect next-round prompts only; round-zero uses a hardcoded copy and must be updated separately for wording consistency (`scripts/setup-rlcr-loop.sh:686`). If there are no commits in a round, the wording is harmless but imprecise.
- validation_or_tests: Template existence is covered by the template-reference scanner because `loop-codex-stop-hook.sh` references `claude/push-every-round-note.md` (`tests/test-template-references.sh:57`, `tests/test-template-references.sh:94`). All templates are loaded by `tests/test-templates-comprehensive.sh` (`tests/test-templates-comprehensive.sh:90`). The companion `unpushed-commits.md` render path has a targeted template test (`tests/test-templates-comprehensive.sh:517`). I found no direct unit test that asserts this exact note is appended to next-round prompts. Not executed in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 section headers present exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`