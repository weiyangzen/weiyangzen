# agent_13 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-013 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Recursive executable robustness specification for the RLCR loop’s core shell algorithms. The directory is flat and contains ten shell suites: state parsing, active-session selection, goal-tracker parsing, plan path/content validation, git status/state parsing, hook JSON/monitor behavior, template rendering stress, cancel authorization security, and timeout portability.
- algorithmic_behavior: Each script constructs temporary fixtures, invokes production functions or scripts, asserts pass/fail counts through `tests/test-helpers.sh`, and exits nonzero through `print_test_summary` on failed assertions. The directory is wired into the full runner at `tests/run-all-tests.sh:52-62`, after the normal test suites and after `tests/test-monitor-runtime.sh`. Shared pass/fail state and temp cleanup come from `tests/test-helpers.sh:22-44` and `tests/test-helpers.sh:86-89`.
- inputs_outputs_state: Inputs are synthetic markdown state files, loop directories, plan files, git repos, hook JSON payloads, shell commands, template strings, and timeout commands. Outputs are terminal PASS/FAIL lines and process exit status. State transitions under test include `state.md`/`finalize-state.md` parsing into `STATE_*` variables, active-loop discovery from `.humanize/rlcr`, plan validation acceptance vs rejection, hook allow/block exits, cancel move authorization, and timeout exit-code propagation.
- gates_or_invariants: `test-state-file-robustness.sh` validates tolerant vs strict state parsing: tolerant defaults in `parse_state_file` and `get_current_round`, strict rejection for missing YAML, missing required fields, and nonnumeric values (`hooks/lib/loop-common.sh:175-229`, `hooks/lib/loop-common.sh:240-296`). `test-session-robustness.sh` confirms `find_active_loop` only considers the newest immediate session and only active/finalize state files (`hooks/lib/loop-common.sh:147-170`). `test-path-validation-robustness.sh` and `test-plan-file-robustness.sh` drive `scripts/setup-rlcr-loop.sh`, whose plan gate rejects absolute paths, spaces, shell metacharacters, symlink files, symlink parent segments, missing/unreadable/out-of-project files, git-tracking violations, files under five lines, and files with fewer than three content lines (`scripts/setup-rlcr-loop.sh:227-438`). `test-cancel-security-robustness.sh` exercises `is_cancel_authorized`, requiring `.cancel-requested`, no command substitution/chaining/newlines/remaining variables, exactly `mv` from `state.md` or `finalize-state.md` to `cancel-state.md`, no extra args, no mixed quotes, no multiple trailing spaces, and no symlink source (`hooks/lib/loop-common.sh:455-614`). `test-hook-input-robustness.sh` verifies `validate_hook_input`, required tool fields, and deep nesting rejection (`hooks/lib/loop-common.sh:55-129`) plus monitor helper availability.
- dependencies_and_callers: The directory depends on production `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, and `scripts/portable-timeout.sh`. It is called by `tests/run-all-tests.sh:70-129`, which captures each suite’s output, strips ANSI, extracts Passed/Failed counts, and aggregates failures.
- edge_cases_or_failure_modes: The suites cover malformed YAML, missing frontmatter, binary/CRLF/null-containing files, session symlinks, finished/cancelled session files ignored by active-loop discovery, malformed/empty/missing hook JSON fields, very deep JSON, invalid UTF-8, very large templates and plan files, special shell characters, many git file states, detached/rebase/merge/shallow repos, command injection through cancel requests, symlink bypass when a directory name contains `finalize`, no timeout implementation, zero timeout, signal handling, nonexistent commands, and large command output.
- validation_or_tests: Validation is the directory itself. Notable child roles: `test-goal-tracker-robustness.sh` checks `humanize_parse_goal_tracker` returns `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary` and handles list/table AC formats, malformed files, large counts, binary content, and truncation (`scripts/humanize.sh:24-106`). `test-git-operations-robustness.sh` checks `humanize_parse_git_status` and `humanize_detect_git_state` (`scripts/humanize.sh:108-204`). `test-template-stress-robustness.sh` checks the single-pass `awk` renderer and fallback loading (`hooks/lib/template-loader.sh:50-180`). `test-timeout-robustness.sh` checks the fallback chain `gtimeout > timeout > python3 > python > none` and `run_with_timeout` semantics (`scripts/portable-timeout.sh:9-76`). I did not run the suites because the branch export is read-only and the workspace lacks a `.git` checkout; inspection only was requested.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-043 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Executable runtime specification for monitor shutdown and terminal cleanup behavior, specifically AC-1.1 and AC-1.3. It validates the monitor’s graceful-stop contract rather than static parser behavior.
- algorithmic_behavior: The script creates `/tmp/test-monitor-runtime-$$`, builds small temporary runner scripts, sources `scripts/humanize.sh`, stubs or redefines selected monitor helpers, executes the simulated monitor paths, and checks output with grep. It increments local counters through its own `pass`/`fail` functions (`tests/test-monitor-runtime.sh:22-34`) and exits `0` only if all checks pass (`tests/test-monitor-runtime.sh:335-346`).
- inputs_outputs_state: Inputs are a temporary `.humanize/rlcr/<timestamp>/state.md`, generated shell runner files, and source-text checks against `scripts/humanize.sh`. Outputs are PASS/FAIL lines and a final process exit code. State transitions under test include `cleanup_done=false -> true`, `monitor_running=true -> false`, deletion of `.humanize/rlcr`, and the expected call chain `_graceful_stop -> _cleanup -> _restore_terminal`.
- gates_or_invariants: Test 1 asserts `_graceful_stop` prints `Monitoring stopped:` and a user-friendly directory-deleted reason while invoking cleanup and terminal restoration (`tests/test-monitor-runtime.sh:56-134`). Test 2 asserts cleanup idempotence by calling `_graceful_stop` and `_cleanup` repeatedly and expecting exactly one cleanup call (`tests/test-monitor-runtime.sh:137-177`). Test 3 models the monitor loop’s directory-existence check and verifies it continues while `.humanize/rlcr` exists, then stops after deletion (`tests/test-monitor-runtime.sh:180-251`). Test 4 checks that `_restore_terminal` is defined, emits scroll-region reset logic, and is called by `_cleanup` (`tests/test-monitor-runtime.sh:253-310`). Test 5 checks `_graceful_stop` calls `_cleanup` in the production source (`tests/test-monitor-runtime.sh:312-323`).
- dependencies_and_callers: Depends on `scripts/humanize.sh`, temporary shell scripts written under `/tmp/test-monitor-runtime-$$`, `grep`, and basic shell process behavior. It is included in the aggregate runner before robustness tests at `tests/run-all-tests.sh:49`. The production monitor under inspection starts at `scripts/humanize.sh:213`; it checks `.humanize/rlcr` presence at `scripts/humanize.sh:224-229`, finds latest session/logs with safe `find` loops at `scripts/humanize.sh:231-314`, and parses state/log status for the status bar afterward.
- edge_cases_or_failure_modes: The file guards against terminal state leaks after monitor shutdown, double cleanup, stale monitor loops after `.humanize/rlcr` deletion, source drift where `_cleanup` no longer calls `_restore_terminal`, and source drift where `_graceful_stop` no longer invokes `_cleanup`. One minor implementation issue in the test itself: lines `282-287` pipe two `grep -q` commands, but the first `grep -q` produces no stdout, making that nested condition ineffective; the later direct source check at `tests/test-monitor-runtime.sh:306-310` covers the same invariant.
- validation_or_tests: The script is itself the validation. It does some dynamic execution and some source-text verification. It does not fully drive `_humanize_monitor_codex`; instead it isolates and models the key graceful-stop and cleanup state transitions. I did not execute it in this read-only branch export.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-073 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Prompt/block template for the hook-enforced invariant that `round-*-todos.md` files must not be created or accessed directly; agents should use native `TodoWrite` state instead.
- algorithmic_behavior: Static markdown block with a denial heading, a direct prohibition, and the replacement action. The complete file is eight lines: heading at `prompt-template/block/todos-file-access.md:1`, prohibition at `prompt-template/block/todos-file-access.md:3`, native tool instruction at `prompt-template/block/todos-file-access.md:5`, and rationale at `prompt-template/block/todos-file-access.md:7-8`.
- inputs_outputs_state: Input is a blocked Read/Write/Edit/Bash attempt targeting a `round-*-todos.md` path. Output is the rendered block text returned by `todos_blocked_message`; this function takes an action argument but the current template contains no `{{ACTION}}` placeholder, so rendering is effectively static. No persistent state is changed by the template itself.
- gates_or_invariants: The invariant is “Do NOT create or access `round-*-todos.md` files” and “Use the native TodoWrite tool instead” (`prompt-template/block/todos-file-access.md:3-5`). The hook layer enforces this before tool execution: read blocks unauthorized todos reads at `hooks/loop-read-validator.sh:52-59`, write blocks unauthorized todos writes at `hooks/loop-write-validator.sh:53-60`, edit references the same blocker, and bash blocks shell-based todos access through `todos_blocked_message`. The shared message loader is `hooks/lib/loop-common.sh:347-356`.
- dependencies_and_callers: Loaded through `load_and_render_safe "$TEMPLATE_DIR" "block/todos-file-access.md" "$fallback"` in `todos_blocked_message` (`hooks/lib/loop-common.sh:349-355`). Template loading and single-pass rendering are implemented in `hooks/lib/template-loader.sh:36-47` and `hooks/lib/template-loader.sh:58-132`. Template presence is referenced by `tests/test-template-references.sh:153`.
- edge_cases_or_failure_modes: If the template is missing or empty, `todos_blocked_message` falls back to an inline denial string (`hooks/lib/loop-common.sh:351-355`). Because the template is static, action-specific wording is not available even though callers pass `Read`, `Write`, `Edit`, or `Bash`; that is acceptable for a uniform policy message but is a small diagnostic limitation. The rule is intentionally strict and may be a “too strict” surface if a workflow still expects file-backed round todos, but this file clearly documents the current native TodoWrite-only transition.
- validation_or_tests: Direct validation is through hook tests and template reference tests: `tests/robustness/test-hook-input-robustness.sh` verifies hook JSON parsing and that validator paths remain callable, while `tests/test-template-references.sh` includes `block/todos-file-access.md` in the expected template inventory. The actual block behavior is enforced by the read/write/edit/bash validators before filesystem access.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item evidence headings present exactly once`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`