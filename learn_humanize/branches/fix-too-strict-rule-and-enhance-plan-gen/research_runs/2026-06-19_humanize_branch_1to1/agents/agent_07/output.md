# agent_07 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-007 `directory` `tests`
- cursor: `[_]`
- core_role: Recursive executable specification tree for the Humanize/RLCR loop, covering hook validation, plan file protection, plan generation command structure, template rendering, monitor behavior, cancellation, state parsing, git/status parsing, and robustness acceptance criteria. Direct recursive inspection found 31 files total under `tests`, with `tests/robustness` as the only child directory.
- algorithmic_behavior: `tests/run-all-tests.sh:32-63` defines the canonical suite order: top-level functional tests first, then robustness tests AC-1 through AC-11. It captures each suite output, strips ANSI escapes, extracts `Passed:`/`Failed:` counts, aggregates totals, and exits nonzero if any suite fails (`tests/run-all-tests.sh:94-149`). Shared helpers in `tests/test-helpers.sh:30-88` standardize pass/fail/skip counters, summary exit behavior, temp directory setup, and mock git repo setup.
- inputs_outputs_state: Inputs are shell scripts, hook JSON payloads, temporary git repositories, generated `.humanize/rlcr` loop directories, mock plan/state/summary/todo files, prompt templates, and command docs. Outputs are PASS/FAIL counters, suite exit codes, temp filesystem state, and in some tests hook JSON/block messages. State is mostly isolated with `mktemp -d` and `trap` cleanup; several suites create local git repos and fake loop state.
- gates_or_invariants: Directory-level invariants include hook fail-closed behavior for malformed state (`hooks/*validator.sh` through tests), no direct modification of loop-managed files, stable template references and fallbacks, no accidental template placeholders, plan file paths must be relative/safe, `gen-plan` must validate IO before generation, monitor cleanup must restore terminal state, and robustness cases must not crash on malformed input. `tests/test-gen-plan.sh:426-541` also gates `scripts/validate-gen-plan-io.sh` exit codes for missing args, missing input, empty input, bad output dir, existing output, directory output, valid paths, and help.
- dependencies_and_callers: The suite coordinates with `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, all hook validators in `hooks/`, `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, `scripts/validate-gen-plan-io.sh`, `commands/gen-plan.md`, `agents/draft-relevance-checker.md`, and `prompt-template/**`. Branch-relevant plan generation coverage is concentrated in `tests/test-gen-plan.sh`, `tests/test-plan-file-validation.sh`, and `tests/test-plan-file-hooks.sh`.
- edge_cases_or_failure_modes: Covered cases include missing templates, invalid template dirs, malformed placeholders, ANSI parsing, malformed hook JSON, deep JSON nesting, non-UTF8/null bytes, state files with bad YAML or binary/CRLF content, symlink/path/metacharacter plan files, missing/deleted plan backups, command substitution/glob/brace/piped bypass attempts for `plan.md`, stale sessions, deleted `.humanize` dirs, git rebase/merge/detached/non-git states, timeout fallback behavior, and zsh glob safety.
- validation_or_tests: This directory is itself the validation surface. I did not execute the suite in this read-only branch export; evidence is from direct recursive inspection. Notable branch-specific specs: `tests/test-plan-file-hooks.sh:194-335` validates plan backup blocking through Write/Edit/Bash and bypass attempts; `tests/test-plan-file-validation.sh:89-147` validates plan path rejection; `tests/test-gen-plan.sh:1-180` validates command and agent metadata, while `tests/test-gen-plan.sh:426-541` validates IO script behavior.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-037 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for template-loader failure tolerance. It ensures missing or malformed template inputs degrade to empty strings or fallbacks instead of crashing hook validators or returning unusable block messages.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh` (`tests/test-error-scenarios.sh:11-15`), initializes local pass/fail counters, and runs 12 sequential assertions. Tests 1-4 verify missing template files, missing template directories, missing `load_and_render` templates, and empty content return empty output with exit 0 (`tests/test-error-scenarios.sh:42-91`). Tests 5-8 verify safe substitution of special regex characters and fallback behavior (`tests/test-error-scenarios.sh:93-158`). Tests 9-12 verify template directory validation and malformed placeholder preservation (`tests/test-error-scenarios.sh:160-206`).
- inputs_outputs_state: Inputs are `TEMPLATE_DIR`, synthetic template strings, missing template names, missing directory paths, `VAR=value` pairs, and fallback strings. Outputs are rendered strings, exit codes, pass/fail lines, and final process exit 0 when no failures remain (`tests/test-error-scenarios.sh:211-226`). State is local shell variables only; no persistent repository state is changed.
- gates_or_invariants: Missing templates must not abort scripts under `set -euo pipefail`; `load_template` and `load_and_render` return empty content successfully; `load_and_render_safe` must use fallback content and render variables inside fallback; `validate_template_dir` must accept the real template tree and reject nonexistent paths; placeholders with empty names or unclosed delimiters stay literal.
- dependencies_and_callers: Directly depends on `hooks/lib/template-loader.sh`. The tested functions are implemented at `hooks/lib/template-loader.sh:26-48` (`get_template_dir`, `load_template`), `hooks/lib/template-loader.sh:58-132` (`render_template`), `hooks/lib/template-loader.sh:136-147` (`load_and_render`), `hooks/lib/template-loader.sh:170-203` (`load_and_render_safe`), and `hooks/lib/template-loader.sh:208-222` (`validate_template_dir`). These functions feed block messages used by `loop-common.sh` and all hook validators.
- edge_cases_or_failure_modes: Explicitly covers nonexistent template file, nonexistent template directory, empty template content, values containing regex-like characters such as brackets/parentheses/stars, strict-mode subshell execution, fallback variable substitution, invalid template directory, empty `{{}}`, and unclosed `{{UNCLOSED`.
- validation_or_tests: Included in the aggregate runner at `tests/run-all-tests.sh:42`. The script’s own success gate is `TESTS_FAILED -eq 0` before exit 0 (`tests/test-error-scenarios.sh:218-226`).
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-067 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Static prompt/block template for the plan-backup protection gate. It tells the agent that loop-local `plan.md` is a backup of the original plan and must be read, not modified (`prompt-template/block/plan-backup-protected.md:1-7`).
- algorithmic_behavior: The template has no variables and is loaded through `load_and_render_safe`. It is emitted when validators detect a Write/Edit/Bash attempt against `plan.md` inside `.humanize/rlcr`, converting a filesystem/path gate into an actionable denial reason.
- inputs_outputs_state: Input is the template lookup key `block/plan-backup-protected.md`; output is a markdown reason headed `Plan Backup Protected`. It has no state transitions itself, but it supports the state invariant that the loop directory’s backup plan remains immutable during an active RLCR loop.
- gates_or_invariants: The invariant is: the loop-local `plan.md` is integrity evidence, not an editable work target. Write validator blocks loop `plan.md` at `hooks/loop-write-validator.sh:151-156`; Edit validator blocks it at `hooks/loop-edit-validator.sh:103-109`; Bash validator blocks modification commands matching `.humanize/rlcr/.../plan.md` at `hooks/loop-bash-validator.sh:314-318`. Separately, the stop hook requires the backup to exist and match the original plan before review proceeds (`hooks/loop-codex-stop-hook.sh:185-234`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh:170-203` for safe loading/fallback. Direct callers are `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh`. Behavioral coverage is in `tests/test-plan-file-hooks.sh:194-239` for direct Write/Edit/Bash blocking and `tests/test-plan-file-hooks.sh:254-335` for direct path plus command substitution, glob, brace, pipe, and backtick bypass attempts.
- edge_cases_or_failure_modes: If the template is missing, callers use inline fallbacks, preserving the block but reducing message quality. The gate intentionally applies to `.humanize/rlcr` paths only; legacy `.humanize-loop.local` plan paths are allowed by negative tests at `tests/test-plan-file-hooks.sh:1016-1054`. The Bash regex also covers both direct `.humanize/rlcr/plan.md` and one-level session forms.
- validation_or_tests: Main validation is `tests/test-plan-file-hooks.sh`; template existence/reference safety is also covered by `tests/test-template-references.sh`, which scans template loader call sites and checks referenced files exist.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-097 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state file parsing. It distinguishes tolerant parsing used for read/default behavior from strict parsing used by validators that must fail closed on corrupted loop state.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh` and shared test helpers (`tests/robustness/test-state-file-robustness.sh:14-19`), creates a temp directory, writes synthetic `state*.md` files, then calls `parse_state_file`, `get_current_round`, and `parse_state_file_strict`. Positive tests validate full state parsing, round extraction, ignored extra fields, quoted strings, and round zero (`tests/robustness/test-state-file-robustness.sh:33-137`). Negative/edge tests validate strict rejection and tolerant defaults or graceful handling (`tests/robustness/test-state-file-robustness.sh:147-393`).
- inputs_outputs_state: Inputs are temporary markdown state files with YAML frontmatter fields such as `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `plan_file`, `plan_tracked`, and `start_branch`. Outputs are `STATE_*` globals set by `parse_state_file`, raw round strings from `get_current_round`, strict parser return codes, pass/fail counters, and final summary exit via `print_test_summary` (`tests/robustness/test-state-file-robustness.sh:395-400`).
- gates_or_invariants: Valid state with required fields must parse; tolerant mode defaults missing `current_round` to 0 and `max_iterations` to 10; strict mode rejects missing YAML separators, missing required round fields, and non-numeric current/max values. `parse_state_file_strict` enforces those checks in `hooks/lib/loop-common.sh:240-296`; tolerant parsing and defaults live at `hooks/lib/loop-common.sh:202-229`; round extraction defaults to 0 at `hooks/lib/loop-common.sh:175-185`.
- dependencies_and_callers: The strict parser is a shared gate for `hooks/loop-plan-file-validator.sh:44-48`, `hooks/loop-write-validator.sh:112-116`, `hooks/loop-edit-validator.sh:77-81`, `hooks/loop-bash-validator.sh:67-68`, `hooks/loop-read-validator.sh:91-92`, and `hooks/loop-codex-stop-hook.sh:88-89`. The suite is included in the aggregate runner at `tests/run-all-tests.sh:52-54`.
- edge_cases_or_failure_modes: Covers missing frontmatter, non-numeric `current_round`, missing required fields, empty files, partial YAML, malformed YAML with missing colon, very large round numbers, negative round values, quoted/special path values with spaces, trailing whitespace, nonexistent files, binary content after YAML, and CRLF line endings. Some tolerant checks intentionally assert “does not crash” rather than canonicalizing every malformed value.
- validation_or_tests: The file is the validation artifact. It verifies both production parser behavior and validator safety assumptions, but I did not execute it in this read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4 item evidence sections, matching the 4 assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`