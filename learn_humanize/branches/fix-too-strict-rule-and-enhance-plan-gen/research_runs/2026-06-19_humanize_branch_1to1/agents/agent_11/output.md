# agent_11 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-011 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: This directory is the Claude-facing prompt template layer for the RLCR loop. It does not execute shell logic directly, but it defines the markdown control surfaces that drive Claude’s next action after Codex review outcomes: continue to another round, handle post-alignment remediation, finalize after acceptance, push when configured, and request goal-tracker changes through the approved channel.
- algorithmic_behavior: Recursive inspection found six direct templates and no deeper descendants: `prompt-template/claude/push-every-round-note.md`, `next-round-prompt.md`, `post-alignment-action-items.md`, `goal-tracker-update-request.md`, `finalize-phase-prompt.md`, and `next-round-footer.md`. The templates are loaded by `hooks/loop-codex-stop-hook.sh` during stop-hook state transitions. On Codex acceptance, the hook renames `state.md` to `finalize-state.md`, builds the finalize prompt from `claude/finalize-phase-prompt.md`, and returns a blocking decision so Claude enters the Finalize Phase instead of exiting immediately (`hooks/loop-codex-stop-hook.sh:984`, `hooks/loop-codex-stop-hook.sh:1010`, `hooks/loop-codex-stop-hook.sh:1017`). On review failure, the hook increments `current_round`, writes `round-N-prompt.md`, renders `claude/next-round-prompt.md`, optionally appends post-alignment instructions, appends the footer, optionally appends the push note, and finally appends the goal-tracker update request (`hooks/loop-codex-stop-hook.sh:1062`, `hooks/loop-codex-stop-hook.sh:1080`, `hooks/loop-codex-stop-hook.sh:1087`, `hooks/loop-codex-stop-hook.sh:1096`, `hooks/loop-codex-stop-hook.sh:1101`, `hooks/loop-codex-stop-hook.sh:1109`).
- inputs_outputs_state: Inputs are template variables supplied by the hook and rendered by `hooks/lib/template-loader.sh`: `{{PLAN_FILE}}`, `{{REVIEW_CONTENT}}`, `{{GOAL_TRACKER_FILE}}`, `{{NEXT_SUMMARY_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}`. `claude/next-round-prompt.md` consumes the plan, Codex review content, and goal tracker references (`prompt-template/claude/next-round-prompt.md:3`, `prompt-template/claude/next-round-prompt.md:16`, `prompt-template/claude/next-round-prompt.md:22`). `claude/finalize-phase-prompt.md` consumes plan, tracker, and finalize summary paths (`prompt-template/claude/finalize-phase-prompt.md:35`, `prompt-template/claude/finalize-phase-prompt.md:40`). Outputs are markdown prompt bodies persisted into round prompt files or emitted as hook block reasons. State transitions coordinated by these templates are: normal review failure to next-round work, every-fifth-round alignment follow-up, optional push-required workflow, and accepted implementation to Finalize Phase.
- gates_or_invariants: The templates encode several behavioral gates. `next-round-prompt.md` requires Claude to review the original plan before acting and to create Todos for all discovered issues rather than only the highest-priority issue (`prompt-template/claude/next-round-prompt.md:5`, `prompt-template/claude/next-round-prompt.md:12`). It also makes `goal-tracker.md` read-only after Round 0 and routes changes through a summary request (`prompt-template/claude/next-round-prompt.md:22`, `prompt-template/claude/next-round-prompt.md:30`). `next-round-footer.md` forbids escaping the loop by lying, editing loop state, or running `cancel-rlcr-loop`, then requires commit and summary output (`prompt-template/claude/next-round-footer.md:4`, `prompt-template/claude/next-round-footer.md:8`). `finalize-phase-prompt.md` constrains final cleanup to functionality-equivalent simplification and requires tests to remain passing (`prompt-template/claude/finalize-phase-prompt.md:16`, `prompt-template/claude/finalize-phase-prompt.md:20`). `push-every-round-note.md` adds a push requirement only when `push_every_round` is true (`prompt-template/claude/push-every-round-note.md:2`, `hooks/loop-codex-stop-hook.sh:1099`).
- dependencies_and_callers: These templates depend on `hooks/lib/template-loader.sh`, which resolves the template directory relative to the plugin root and requires `prompt-template/block`, `prompt-template/codex`, and `prompt-template/claude` to exist (`hooks/lib/template-loader.sh:24`, `hooks/lib/template-loader.sh:216`). Rendering is single-pass to avoid placeholder injection from inserted values such as review content (`hooks/lib/template-loader.sh:54`, `hooks/lib/template-loader.sh:71`). Primary caller is `hooks/loop-codex-stop-hook.sh`, with the template calls listed above. Sibling coordination is with `prompt-template/codex` for Codex review prompts and `prompt-template/block` for validator block messages; together they form the loop’s review, continuation, and safety message surfaces.
- edge_cases_or_failure_modes: Missing templates are handled with hook-local fallbacks through `load_and_render_safe`, so the loop still produces actionable prompts if a Claude template is absent (`hooks/lib/template-loader.sh:167`, `hooks/lib/template-loader.sh:179`). Missing variables remain as placeholders by design, which prevents accidental substitution but can expose unresolved `{{...}}` markers if callers forget a variable (`hooks/lib/template-loader.sh:12`, `hooks/lib/template-loader.sh:120`). Post-alignment instructions are appended only when `CURRENT_ROUND % 5 == 4`, matching the hook’s “round after every 5th” convention (`hooks/loop-codex-stop-hook.sh:1085`). The finalize template strictly says to use the `code-simplifier:code-simplifier` agent, while the next-round footer softens this to “if installed”; this is a possible operational dependency mismatch between normal rounds and finalize (`prompt-template/claude/finalize-phase-prompt.md:9`, `prompt-template/claude/next-round-footer.md:7`).
- validation_or_tests: `hooks/lib/template-loader.sh` validates that the `claude` template subdirectory exists (`hooks/lib/template-loader.sh:216`). `tests/test-template-references.sh` enumerates template files and checks whether each appears in hook scripts, warning rather than failing for unreferenced templates (`tests/test-template-references.sh:130`, `tests/test-template-references.sh:136`). The direct runtime references found in `hooks/loop-codex-stop-hook.sh` cover all six Claude templates in this directory.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-041 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: This is an executable shell specification for escape-prevention behavior around `.humanize` staging and zsh-safe filesystem iteration. It validates the branch’s core safety algorithm by calling shared hook-library functions directly, especially `git_adds_humanize`, instead of exercising the full Claude hook JSON wrapper.
- algorithmic_behavior: The script sources `hooks/lib/loop-common.sh`, initializes pass/fail counters, and defines `assert_blocks` and `assert_allows` helpers that lowercase command text with `to_lower` before passing it to `git_adds_humanize` (`tests/test-humanize-escape.sh:16`, `tests/test-humanize-escape.sh:18`, `tests/test-humanize-escape.sh:44`, `tests/test-humanize-escape.sh:49`). The behavior under test lives in `hooks/lib/loop-common.sh`: `git_adds_humanize` splits a command line on shell operators, finds `git ... add` segments, extracts arguments after `add`, strips quotes for path matching, detects direct `.humanize` path references, detects `--force` or combined `-f` flags, detects `--all` or lowercased `-a` flags, detects broad scopes like `.` and `*`, and conditionally blocks all-mode or broad-scope adds based on `.humanize` existence and gitignore status (`hooks/lib/loop-common.sh:647`, `hooks/lib/loop-common.sh:668`, `hooks/lib/loop-common.sh:674`, `hooks/lib/loop-common.sh:679`, `hooks/lib/loop-common.sh:685`, `hooks/lib/loop-common.sh:691`, `hooks/lib/loop-common.sh:700`, `hooks/lib/loop-common.sh:708`, `hooks/lib/loop-common.sh:719`). In production, the same detector is invoked by `hooks/loop-bash-validator.sh` after parsing hook JSON, locating an active loop, and strict-parsing the loop state (`hooks/loop-bash-validator.sh:22`, `hooks/loop-bash-validator.sh:54`, `hooks/loop-bash-validator.sh:68`, `hooks/loop-bash-validator.sh:100`).
- inputs_outputs_state: Inputs are synthetic shell command strings and temporary filesystem layouts. Outputs are printed PASS/FAIL lines and final process exit status. The script mutates only ephemeral `/tmp/test-humanize-*` directories, changes into a temp directory for the `git add -A` cases, then returns to the original directory before cleanup (`tests/test-humanize-escape.sh:130`, `tests/test-humanize-escape.sh:132`, `tests/test-humanize-escape.sh:143`, `tests/test-humanize-escape.sh:144`). The internal state is `TESTS_PASSED` and `TESTS_FAILED`, incremented by `pass` and `fail` (`tests/test-humanize-escape.sh:25`, `tests/test-humanize-escape.sh:29`, `tests/test-humanize-escape.sh:34`). The final gate exits 0 only when no failures were recorded (`tests/test-humanize-escape.sh:344`, `tests/test-humanize-escape.sh:347`).
- gates_or_invariants: The test asserts that direct `.humanize` variants are blocked, including `./.humanize`, trailing slash, nested file, relative parent, and path-prefix forms (`tests/test-humanize-escape.sh:83`, `tests/test-humanize-escape.sh:87`). Quoted forms must also block (`tests/test-humanize-escape.sh:96`, `tests/test-humanize-escape.sh:100`). Force with `.humanize`, force plus broad scope, and combined force/all flags must block because force bypasses `.gitignore` (`tests/test-humanize-escape.sh:109`, `tests/test-humanize-escape.sh:119`). `git add -A` and `--all` must block when `.humanize` exists (`tests/test-humanize-escape.sh:128`, `tests/test-humanize-escape.sh:135`). Chained commands and git global-option forms must still be detected (`tests/test-humanize-escape.sh:153`, `tests/test-humanize-escape.sh:164`). Allowed invariants include specific non-humanize files, non-add git commands, patch mode, and lookalike paths such as `.humanizeconfig` and `.humanize-backup` (`tests/test-humanize-escape.sh:175`, `tests/test-humanize-escape.sh:184`, `tests/test-humanize-escape.sh:190`). The zsh-safety invariants are implemented as `find` loops over empty directories, dotfile-only directories, missing `*-state.md`, present `*-state.md`, and timestamped session directories (`tests/test-humanize-escape.sh:204`, `tests/test-humanize-escape.sh:225`, `tests/test-humanize-escape.sh:248`, `tests/test-humanize-escape.sh:271`, `tests/test-humanize-escape.sh:303`).
- dependencies_and_callers: Direct dependency is `hooks/lib/loop-common.sh`, which provides `to_lower` and `git_adds_humanize`. The production caller of the tested detector is `hooks/loop-bash-validator.sh`, where a true result emits the `.humanize` block message and exits with code 2 (`hooks/loop-bash-validator.sh:100`, `hooks/loop-bash-validator.sh:101`). `git_adds_humanize` itself depends on basic POSIX-ish shell utilities (`sed`, `grep`, `git check-ignore`) and current working directory state for the `.humanize` existence/gitignore checks (`hooks/lib/loop-common.sh:719`, `hooks/lib/loop-common.sh:733`). The test’s filesystem-iteration specs correspond to the branch’s stated fix for zsh glob failures by using `find` rather than unguarded globs.
- edge_cases_or_failure_modes: The function receives lowercased input, so it explicitly treats `git add -A` as `-a`; this is documented in the implementation and mirrored by test helper lowercasing (`hooks/lib/loop-common.sh:626`, `tests/test-humanize-escape.sh:49`). Quote stripping is intentionally simple and supports the tested single/double quoted paths, but the shell-operator split is textual and may not fully model operators inside quotes (`hooks/lib/loop-common.sh:647`). The all-mode tests require `.humanize` to exist because non-force `-A` blocking is conditional on current working directory state (`tests/test-humanize-escape.sh:128`, `hooks/lib/loop-common.sh:719`). The script removes temp directories on normal paths, but there is no `trap`, so an unexpected shell error before cleanup could leave `/tmp/test-humanize-*` artifacts. The tests do not cover the `git check-ignore` branch for broad non-force scopes when `.humanize` exists but is ignored (`hooks/lib/loop-common.sh:730`).
- validation_or_tests: This file is itself the validation asset. It runs with `set -euo pipefail`, prints grouped coverage for AC-1.2, AC-2.1, AC-2.2, and AC-2.3, and summarizes pass/fail counts before exiting (`tests/test-humanize-escape.sh:14`, `tests/test-humanize-escape.sh:80`, `tests/test-humanize-escape.sh:106`, `tests/test-humanize-escape.sh:125`, `tests/test-humanize-escape.sh:198`, `tests/test-humanize-escape.sh:337`). Its expected success condition is zero recorded failures (`tests/test-humanize-escape.sh:344`).
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-071 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: This file is a safety-gate block template for attempts to modify `state.md`. It is small, but it is part of the core RLCR guard contract because `state.md` carries loop-control state and must only be managed by the loop system.
- algorithmic_behavior: The template emits a markdown block headed “State File Modification Blocked,” states that `state.md` cannot be modified, and explains that the file contains current round number, max iterations, and Codex configuration (`prompt-template/block/state-file-modification.md:1`, `prompt-template/block/state-file-modification.md:3`, `prompt-template/block/state-file-modification.md:5`). It is loaded by `state_file_blocked_message` in `hooks/lib/loop-common.sh` via `load_and_render_safe`, with an inline fallback if the template is unavailable (`hooks/lib/loop-common.sh:367`, `hooks/lib/loop-common.sh:373`).
- inputs_outputs_state: The template has no placeholders and takes no dynamic inputs. Its output is static markdown written to stderr by validators when they deny an operation. The protected state it refers to is the active loop’s `state.md`, parsed elsewhere for fields such as `current_round`, `max_iterations`, and model/effort configuration (`hooks/lib/loop-common.sh:187`, `hooks/lib/loop-common.sh:216`, `hooks/lib/loop-common.sh:224`). The output does not mutate state; it explains why mutation is forbidden.
- gates_or_invariants: The invariant is that user/agent tools must not write, edit, move, copy, redirect into, or otherwise mutate `state.md`; the loop owns that transition state. `hooks/loop-write-validator.sh` blocks direct Write attempts to `state.md` after checking `finalize-state.md` first (`hooks/loop-write-validator.sh:120`, `hooks/loop-write-validator.sh:129`). `hooks/loop-edit-validator.sh` blocks Edit attempts similarly (`hooks/loop-edit-validator.sh:89`, `hooks/loop-edit-validator.sh:94`). `hooks/loop-bash-validator.sh` blocks Bash commands that modify `state.md`, with a narrow cancel authorization path handled separately (`hooks/loop-bash-validator.sh:106`, `hooks/loop-bash-validator.sh:126`, `hooks/loop-bash-validator.sh:128`).
- dependencies_and_callers: Primary dependency is the shared template loader (`hooks/lib/template-loader.sh`) through `load_and_render_safe`. Primary callers are the write, edit, and bash validators through `state_file_blocked_message`. The template is also part of the common template existence check in `tests/test-template-references.sh`, which includes `block/state-file-modification.md` in the required common template list (`tests/test-template-references.sh:152`, `tests/test-template-references.sh:155`).
- edge_cases_or_failure_modes: The template only names `state.md`; `finalize-state.md` has its own block template and message function. Validators intentionally check finalize-state first because a broad `state.md` pattern can also match `finalize-state.md` (`hooks/loop-write-validator.sh:122`, `hooks/loop-edit-validator.sh:87`, `hooks/loop-bash-validator.sh:110`). If this template is missing, `state_file_blocked_message` still produces a fallback block, so enforcement remains intact but the richer explanation is lost (`hooks/lib/loop-common.sh:369`, `hooks/lib/loop-common.sh:373`). Because the template is static, there is no placeholder injection or missing-variable failure mode.
- validation_or_tests: Template presence is covered by `tests/test-template-references.sh` as a common block template required by loop-common message functions (`tests/test-template-references.sh:149`, `tests/test-template-references.sh:161`). Runtime enforcement is indirectly validated wherever write/edit/bash validator tests exercise state-file blocking; the template itself is content, while the gate decision is implemented in the validator scripts.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3 unique assigned item headings present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`