# agent_29 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-029 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Bootstrap script for `/humanize:start-rlcr-loop`. It validates an implementation plan file, creates a timestamped `.humanize/rlcr/<timestamp>/` loop state directory, snapshots the plan, initializes the goal tracker, writes round 0 prompt/summary paths, and prints the initial instructions that drive the RLCR loop. The slash command invokes it directly through `commands/start-rlcr-loop.md:12`.
- algorithmic_behavior: Parses options for positional/explicit plan file, max iterations, Codex model/effort, Codex timeout, push cadence, and tracked-plan mode at `scripts/setup-rlcr-loop.sh:34`. It rejects invalid CLI combinations such as both `--plan-file` and a positional plan at `scripts/setup-rlcr-loop.sh:188`. It then runs a staged validation pipeline: git repo and HEAD existence at `scripts/setup-rlcr-loop.sh:211`, path shape checks at `scripts/setup-rlcr-loop.sh:227`, symlink and parent-directory checks at `scripts/setup-rlcr-loop.sh:253`, project containment at `scripts/setup-rlcr-loop.sh:301`, optional submodule exclusion at `scripts/setup-rlcr-loop.sh:315`, tracking policy checks at `scripts/setup-rlcr-loop.sh:334`, content sufficiency checks at `scripts/setup-rlcr-loop.sh:385`, Codex CLI availability at `scripts/setup-rlcr-loop.sh:440`, and YAML-safe branch/model/effort checks at `scripts/setup-rlcr-loop.sh:452`. On success it creates loop state, copies the plan, writes YAML-ish `state.md`, seeds `goal-tracker.md`, writes `round-0-prompt.md`, and emits the prompt and completion requirements.
- inputs_outputs_state: Inputs are CLI args, current working directory or `CLAUDE_PROJECT_DIR`, the plan file content, git metadata/status, `.gitmodules` when present, the availability of `codex`, and `scripts/portable-timeout.sh`. Outputs are `.humanize/rlcr/<timestamp>/plan.md`, `state.md`, `goal-tracker.md`, `round-0-prompt.md`, and an expected `round-0-summary.md` path. The state fields include `current_round: 0`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `plan_file`, `plan_tracked`, `start_branch`, and `started_at` at `scripts/setup-rlcr-loop.sh:508`. The round 0 prompt embeds goal-tracker setup steps, appends the copied plan content, and instructs Claude to commit and write the summary at `scripts/setup-rlcr-loop.sh:626`.
- gates_or_invariants: The plan must be relative, inside the project, non-symlinked, readable, not under a git submodule, have no spaces, and avoid shell metacharacters at `scripts/setup-rlcr-loop.sh:227`. Without `--track-plan-file`, the plan must be untracked/gitignored; with `--track-plan-file`, it must be tracked and clean at `scripts/setup-rlcr-loop.sh:354`. Content must be at least 5 total lines and at least 3 meaningful non-comment lines at `scripts/setup-rlcr-loop.sh:385`. Branch names reject YAML-unsafe characters, and Codex model/effort are limited to safe character sets at `scripts/setup-rlcr-loop.sh:458`. The script also uses `set -euo pipefail`, bounded git calls through `run_with_timeout`, and fails closed on git timeout status `124`.
- dependencies_and_callers: Called by `commands/start-rlcr-loop.md:12` with only this script allowlisted in command metadata. Depends on `scripts/portable-timeout.sh`, which selects `gtimeout`, GNU `timeout`, Python, or no-timeout fallback at `scripts/portable-timeout.sh:10`. Downstream runtime state is consumed by `hooks/loop-codex-stop-hook.sh`, which loads `state.md`, requires clean git before exit, renders Codex review prompts, invokes `codex exec`, and advances the loop based on review output. The regular review template is selected at `hooks/loop-codex-stop-hook.sh:739`; `COMPLETE` handling renames `state.md` to `finalize-state.md` at `hooks/loop-codex-stop-hook.sh:967`; non-complete reviews increment `current_round` and create the next prompt at `hooks/loop-codex-stop-hook.sh:1062`.
- edge_cases_or_failure_modes: Absolute paths, whitespace, shell metacharacters, symlinked plan files, symlinked parent dirs, missing parent dirs, unreadable dirs, `../` escape attempts, submodule paths, no git repo, no initial commit, bad tracking mode, too-short/blank/comment-only plans, missing Codex CLI, unsafe branch names, and unsafe Codex parameters all produce explicit hard failures. One noteworthy strictness edge: the content counter treats any line beginning with `#` as a comment, so normal Markdown headings do not count toward the 3 content-line threshold at `scripts/setup-rlcr-loop.sh:425`; tests intentionally use plain text for valid content. Another edge: `PLAN_GIT_STATUS=$(...) || STATUS_EXIT=$?` and `LS_FILES_EXIT=${LS_FILES_EXIT:-0}` rely on unset variables under `set -u`; because each assignment is normalized immediately, this is deliberate but fragile if refactored. The final `trap 'exit 0' PIPE` at `scripts/setup-rlcr-loop.sh:698` prevents broken output pipes from turning a successfully created loop into an error.
- validation_or_tests: `tests/test-plan-file-validation.sh` directly exercises path rejection, git requirements, tracking policy, branch safety, content validation, `--plan-file`, conflicting plan args, and Codex parameter validation. Relevant coverage includes absolute/nonexistent/space/metacharacter/symlink paths at `tests/test-plan-file-validation.sh:89`, project escape and git repo checks at `tests/test-plan-file-validation.sh:245`, tracking mode cases at `tests/test-plan-file-validation.sh:329`, branch YAML safety at `tests/test-plan-file-validation.sh:432`, blank/comment-only content at `tests/test-plan-file-validation.sh:518`, and Codex parameter safety at `tests/test-plan-file-validation.sh:700`. I did not execute the setup script because this task is research-only and the branch export is read-only; running it successfully would create `.humanize/rlcr` state. A read-only `git status --short` probe also failed because the export environment could not create macOS git/xcode cache files under `/tmp` and then reported no `.git` repository, so validation evidence is from direct inspection and tests.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-059 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Blocking prompt template shown by the RLCR stop hook when Claude attempts to exit while the repository has uncommitted git changes. It is a gate template, not executable logic, but it defines the remediation contract that keeps the loop from advancing to Codex review with dirty worktree state.
- algorithmic_behavior: The template accepts `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}` placeholders, then tells the agent to review untracked files, update `.gitignore` for build artifacts, stage real changes, and commit with project conventions. It includes a preliminary `code-simplifier` instruction when available at `prompt-template/block/git-not-clean.md:6`. It forbids AI authorship attribution in commits at `prompt-template/block/git-not-clean.md:11`.
- inputs_outputs_state: Inputs are rendered by `hooks/loop-codex-stop-hook.sh` after cached `git status` detects non-empty status. The hook sets `GIT_ISSUES="uncommitted changes"` at `hooks/loop-codex-stop-hook.sh:438`, appends special notes for untracked `.humanize*` or other untracked files at `hooks/loop-codex-stop-hook.sh:441`, and renders this template at `hooks/loop-codex-stop-hook.sh:473`. Output is JSON hook response with `"decision": "block"` and a system message such as `Loop: Blocked - uncommitted changes detected, please commit first` at `hooks/loop-codex-stop-hook.sh:477`.
- gates_or_invariants: Dirty git status is a hard stop before review prompt generation. The invariant is that Claude must commit all real changes before exiting the RLCR round, and must not include AI co-author trailers. The template does not itself validate commit content or message format; it instructs behavior while the hook enforces the block.
- dependencies_and_callers: Rendered through `load_and_render_safe` from `hooks/lib/template-loader.sh`, whose placeholder syntax is `{{VARIABLE_NAME}}` and single-pass substitution at `hooks/lib/template-loader.sh:7`. Called only in the dirty-git branch of `hooks/loop-codex-stop-hook.sh:464`. It composes with sibling templates `block/git-not-clean-humanize-local.md` and `block/git-not-clean-untracked.md` when special notes are needed.
- edge_cases_or_failure_modes: If this template is missing or empty, the hook uses a fallback message at `hooks/loop-codex-stop-hook.sh:467`, preserving the dirty-git gate but with less detailed guidance. If `SPECIAL_NOTES` is empty, the rendered template leaves no extra explanatory note. Because rendering is single-pass, placeholder-looking text inside injected notes will not be recursively expanded, which avoids prompt corruption. The template tells the agent to add build artifacts to `.gitignore`, but the hook blocks on any non-empty git status; if `.humanize/` is untracked, the sibling special note must clarify that local loop artifacts should not be committed.
- validation_or_tests: Template loading and rendering behavior is covered by `tests/test-templates-comprehensive.sh`, including real-template rendering and an all-template load/render sweep at `tests/test-templates-comprehensive.sh:563`. The template loader validates required template subdirectories at `hooks/lib/template-loader.sh:205`. The dirty-git runtime path is visible in `hooks/loop-codex-stop-hook.sh:432`.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-089 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Codex review prompt template for normal RLCR review rounds. It defines the algorithmic review contract Codex must follow before allowing the loop to advance toward finalization: read the original plan, compare Claude’s summary and implementation against the plan, audit goal-tracker alignment, write review findings when incomplete, and only end with `COMPLETE` when all original work and acceptance criteria are fully satisfied.
- algorithmic_behavior: The template forces Codex to read `@{{PLAN_FILE}}` first at `prompt-template/codex/regular-review.md:5`, use `@{{PROMPT_FILE}}` and Claude’s `{{SUMMARY_CONTENT}}` as the claimed work context at `prompt-template/codex/regular-review.md:11`, and perform a deep implementation review at `prompt-template/codex/regular-review.md:20`. It explicitly rejects Claude-led deferral: deferred tasks must be treated as incomplete and converted into directive implementation plans at `prompt-template/codex/regular-review.md:24`. It also covers honestly pending work and requires singular, definitive implementation guidance for unfinished tasks at `prompt-template/codex/regular-review.md:28`.
- inputs_outputs_state: Inputs are `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, and `REVIEW_RESULT_FILE`, all passed from `hooks/loop-codex-stop-hook.sh:739`. The rendered output becomes `round-N-review-prompt.md`; Codex is expected to write review comments to `round-N-review-result.md`. If the result’s last non-empty trimmed line is exactly `COMPLETE`, the stop hook enters Finalize Phase at `hooks/loop-codex-stop-hook.sh:960`. If not complete, the hook increments `current_round` and writes a next-round prompt containing the review feedback at `hooks/loop-codex-stop-hook.sh:1062`.
- gates_or_invariants: The template establishes a strict completion invariant: `COMPLETE` may be the last line only if all original plan tasks are done, all acceptance criteria are met, and there are no deferrals or pending work at `prompt-template/codex/regular-review.md:53`. It also mandates a Goal Alignment Check: read `@{{GOAL_TRACKER_FILE}}`, verify AC progress, forgotten items, deferrals, and plan evolution, then include `ACs: X/Y addressed | Forgotten items: N | Unjustified deferrals: N` at `prompt-template/codex/regular-review.md:33`. The runtime hook independently enforces exact last-line matching so text like `CANNOT COMPLETE` is not accepted at `hooks/loop-codex-stop-hook.sh:960`.
- dependencies_and_callers: Selected by `hooks/loop-codex-stop-hook.sh` for non-full-alignment rounds; full alignment uses `codex/full-alignment-review.md` every fifth-style cycle when `CURRENT_ROUND % 5 == 4` at `hooks/loop-codex-stop-hook.sh:686`. Uses the shared goal-tracker update section rendered from `prompt-template/codex/goal-tracker-update-section.md` at `hooks/loop-codex-stop-hook.sh:680`. It relies on `hooks/lib/template-loader.sh` for single-pass placeholder rendering. The Codex execution path uses `codex exec` with model/effort from setup state, project root as working directory, and timeout from state at `hooks/loop-codex-stop-hook.sh:818`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, the hook falls back to a shorter regular-review prompt at `hooks/loop-codex-stop-hook.sh:710`, losing much of the stricter anti-deferral and goal-alignment language. If Claude’s summary falsely claims completion, the template explicitly directs Codex to verify reality rather than accept the summary. If Codex exits successfully but does not create the review result file, or creates an empty one, the hook blocks with a retryable failure at `hooks/loop-codex-stop-hook.sh:932` and `hooks/loop-codex-stop-hook.sh:948`. A behavioral mismatch exists between command docs and this template: `commands/start-rlcr-loop.md:53` says completion occurs when all ACs are met or validly deferred, while this template says any deferral prevents `COMPLETE`; the template is stricter and is the active Codex contract for regular rounds.
- validation_or_tests: `tests/test-templates-comprehensive.sh` performs all-template load/render validation at `tests/test-templates-comprehensive.sh:563`, which covers placeholder replacement for this template. Finalize-phase tests exercise `COMPLETE` handling in the stop hook, including the transition to Finalize Phase when Codex writes `COMPLETE`, at `tests/test-finalize-phase.sh:448`. The hook’s strict review-result checks and exact marker parsing provide runtime validation around the template’s completion contract.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 item-evidence headings present
- missing_items: none
- duplicate_items: none detected in Item Evidence headings
- final_worker_status: `complete`