# agent_25 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-025 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: Claude `PreToolUse` Write hook for the RLCR loop. It is registered in `hooks/hooks.json:14-20` for `matcher: "Write"` and enforces which files Claude may create or overwrite inside `.humanize/rlcr`. Its main algorithmic responsibility is preserving loop-owned state while still allowing valid round summaries and finalize summaries.
- algorithmic_behavior: Reads hook JSON from stdin, validates structure with shared helpers, exits early for non-Write tools, then requires `tool_input.file_path` (`hooks/loop-write-validator.sh:23-47`). It classifies the target path as todos, prompt, round summary, finalize summary, loop-local, state, finalize-state, goal-tracker, or `plan.md` backup (`hooks/loop-write-validator.sh:53-86`). Non-loop, non-summary targets pass through (`hooks/loop-write-validator.sh:76-79`). For loop-owned targets it discovers the newest active RLCR loop, switches parsing to `finalize-state.md` during Finalize Phase, and fail-closes on malformed state frontmatter (`hooks/loop-write-validator.sh:95-117`).
- inputs_outputs_state: Input is Claude hook JSON containing `tool_name` and `tool_input.file_path`; shared validation rejects invalid JSON, missing `tool_name`, null bytes, invalid UTF-8, and excessive nesting through `validate_hook_input`, `require_tool_input_field`, and `is_deeply_nested` in `hooks/lib/loop-common.sh:55-129`. Output is only an exit code plus optional stderr block message. Exit `0` allows the Write, exit `1` indicates invalid/malformed hook or state input, and exit `2` blocks a semantically forbidden Write. The only state it reads is under `${CLAUDE_PROJECT_DIR:-pwd}/.humanize/rlcr`, especially `state.md` or `finalize-state.md`; it does not mutate state.
- gates_or_invariants: It blocks prompt files unconditionally (`hooks/loop-write-validator.sh:63-66`), blocks `state.md` and `finalize-state.md` after checking finalize-state first because `state.md` suffix matching also catches finalize-state (`hooks/loop-write-validator.sh:120-132`), allows only the active loop’s `finalize-summary.md` during Finalize Phase (`hooks/loop-write-validator.sh:134-145`), blocks `.humanize/rlcr/**/plan.md` backups (`hooks/loop-write-validator.sh:147-158`), blocks `goal-tracker.md` after round 0 and redirects the worker to the current round summary (`hooks/loop-write-validator.sh:160-168`), rejects summaries outside `.humanize/rlcr` (`hooks/loop-write-validator.sh:170-182`), rejects wrong round-number summaries unless allowlisted (`hooks/loop-write-validator.sh:196-218`), and rejects writes to the wrong loop directory (`hooks/loop-write-validator.sh:220-235`). Todos and historical bootstrap files are governed by a narrow allowlist in `hooks/lib/loop-common.sh:324-345`: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md`.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for JSON validation, path classification, active-loop discovery, strict state parsing, allowlist checks, and rendered block messages (`hooks/loop-write-validator.sh:15-18`). Template rendering flows through `hooks/lib/template-loader.sh`, where `load_and_render_safe` falls back if a template is missing or renders empty (`hooks/lib/template-loader.sh:167-203`). Key message templates include `block/todos-file-access.md`, `block/prompt-file-write.md`, `block/state-file-modification.md`, `block/finalize-state-file-modification.md`, `block/plan-backup-protected.md`, `block/goal-tracker-modification.md`, `block/wrong-summary-location.md`, `block/wrong-round-number.md`, and `block/wrong-directory-path.md`. Registered caller is Claude’s hook config in `hooks/hooks.json:14-20`.
- edge_cases_or_failure_modes: If there is no active loop directory, the hook allows the Write (`hooks/loop-write-validator.sh:100-102`), preventing stale protections from blocking normal work. Malformed active state blocks safely (`hooks/loop-write-validator.sh:112-116`). The active-loop finder only considers the newest timestamp directory and accepts either `state.md` or `finalize-state.md`, avoiding older “zombie” loops (`hooks/lib/loop-common.sh:147-170`). Path membership is substring-based on `.humanize/rlcr/` (`hooks/lib/loop-common.sh:616-620`), and tests assert legacy `.humanize-loop.local` paths are not treated as loop directories (`tests/test-state-exit-naming.sh:245-260`, `tests/test-plan-file-hooks.sh:1030-1040`). Directory validation uses raw string equality, so unnormalized but equivalent absolute paths can be blocked at the final path check. Summary round extraction only handles non-negative numeric `round-N-summary.md` names (`hooks/lib/loop-common.sh:303-321`).
- validation_or_tests: `tests/test-finalize-phase.sh:286-307` verifies `finalize-summary.md` is allowed while `finalize-state.md` is blocked. `tests/test-allowlist-validators.sh:70-107` and `tests/test-allowlist-validators.sh:133-183` cover allowlisted todos/summaries and rejected non-allowlisted round files. `tests/test-plan-file-hooks.sh:194-204` verifies loop-local `plan.md` Write blocking; `tests/test-plan-file-hooks.sh:1030-1040` verifies legacy loop paths are allowed. `tests/robustness/test-hook-input-robustness.sh:35-80` covers valid and invalid hook JSON against production validators. `tests/test-template-references.sh:176-200` checks critical validators use `load_and_render_safe`.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-055 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: Block-message template emitted when a Write or Edit attempts to modify `finalize-state.md`. It encodes the user-facing enforcement contract for Finalize Phase state ownership.
- algorithmic_behavior: The template itself has no executable control flow, but it is algorithmically selected by `finalize_state_file_blocked_message` in `hooks/lib/loop-common.sh:376-383`. The Write validator reaches that helper when `is_finalize_state_file_path` matches the lowercase target path (`hooks/loop-write-validator.sh:120-127`). Its content states that `finalize-state.md` is loop-system managed and redirects work to code simplification, committing, and writing `finalize-summary.md` (`prompt-template/block/finalize-state-file-modification.md:1-8`).
- inputs_outputs_state: Input is the validator’s matched block condition; there are no template variables in this file. Output is static Markdown printed to stderr by the validator’s block path. It does not read or write loop state.
- gates_or_invariants: Reinforces the invariant that `finalize-state.md` cannot be modified by Claude during Finalize Phase. The paired positive allowance is in `hooks/loop-write-validator.sh:140-144`, which permits the active loop’s `finalize-summary.md`, making this template part of the state-vs-summary separation.
- dependencies_and_callers: Loaded through `load_and_render_safe "$TEMPLATE_DIR" "block/finalize-state-file-modification.md"` in `hooks/lib/loop-common.sh:376-383`; the safe loader resolves templates under `prompt-template` and uses fallback text if the file is absent (`hooks/lib/template-loader.sh:24-47`, `hooks/lib/template-loader.sh:167-203`). Direct behavior is exercised by `hooks/loop-write-validator.sh:124-127`; the same shared message helper is also available to the Edit validator via the common library.
- edge_cases_or_failure_modes: If the template is missing or empty, the fallback still blocks with a shorter message (`hooks/lib/loop-common.sh:378-382`), so enforcement does not depend on template availability. Because the template has no placeholders, rendering cannot be corrupted by missing variables. If `finalize-state.md` is matched through the broader `state.md` suffix predicate, the validator’s explicit ordering prevents the generic `state.md` message from taking precedence (`hooks/loop-write-validator.sh:120-124`).
- validation_or_tests: `tests/test-finalize-phase.sh:298-307` verifies the Write validator exits `2` and emits finalize-related output for `finalize-state.md`. `tests/test-template-references.sh:51-64` includes `loop-write-validator.sh` and `loop-common.sh` in template-reference scanning, and `tests/test-template-references.sh:172-200` checks critical validator rendering uses safe fallbacks.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-085 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Claude prompt fragment appended after a Full Goal Alignment Check so the next repair round prioritizes issues Codex found in alignment review. It shapes the next RLCR transition rather than enforcing a filesystem gate.
- algorithmic_behavior: Static Markdown with a heading and three priority bullets: forgotten items, unmet acceptance criteria, and unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:2-7`). The stop hook appends it when the current round satisfies `CURRENT_ROUND % 5 == 4`, meaning the next generated prompt follows a full-alignment round boundary (`hooks/loop-codex-stop-hook.sh:1085-1091`). It is appended after the main next-round prompt and before footer, push, and goal-tracker-update instructions (`hooks/loop-codex-stop-hook.sh:1071-1113`).
- inputs_outputs_state: Input is the stop hook’s current round number and the template file’s presence. Output is appended text in `round-${NEXT_ROUND}-prompt.md` (`hooks/loop-codex-stop-hook.sh:1067-1089`). It does not change checklist or loop state directly; the state transition to the next round is done immediately before prompt generation by rewriting `current_round` in `state.md` (`hooks/loop-codex-stop-hook.sh:1062-1065`).
- gates_or_invariants: The scheduling gate is modulo-based, only after every fifth round boundary as implemented by `if [[ $((CURRENT_ROUND % 5)) -eq 4 ]]` (`hooks/loop-codex-stop-hook.sh:1085-1087`). The fragment is optional at runtime: if `load_template` returns empty, nothing is appended (`hooks/loop-codex-stop-hook.sh:1087-1090`). This makes it advisory prompt conditioning, not a hard validation gate.
- dependencies_and_callers: Called only by `hooks/loop-codex-stop-hook.sh:1087` according to repository search. Loaded with `load_template`, not `load_and_render_safe`; the loader prints a warning and returns empty for missing templates (`hooks/lib/template-loader.sh:33-48`). It depends on the stop hook already having `REVIEW_CONTENT`, `PLAN_FILE`, and `GOAL_TRACKER_FILE` rendered into the main next-round prompt through `claude/next-round-prompt.md` (`hooks/loop-codex-stop-hook.sh:1071-1083`).
- edge_cases_or_failure_modes: Because missing template content is silently skipped after the loader warning, the loop continues but loses the explicit post-alignment prioritization. The modulo condition assumes alignment checks occur on rounds where the just-completed round number leaves remainder 4; if alignment cadence changes elsewhere, this fragment could be appended at the wrong boundary. The template is static and has no variables, so it cannot tailor the action list to the specific unmet ACs; it relies on the preceding review content to carry specifics.
- validation_or_tests: Repository search shows no focused behavioral test asserting this fragment is appended at the modulo boundary. It is indirectly covered by template-reference scanning in `tests/test-template-references.sh:56-69`, which includes `hooks/loop-codex-stop-hook.sh` and extracts `load_template` references. No execution test was found for the exact generated prompt content.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 item headings present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`