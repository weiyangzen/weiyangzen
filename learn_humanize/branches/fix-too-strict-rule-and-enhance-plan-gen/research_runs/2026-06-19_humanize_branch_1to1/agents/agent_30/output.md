# agent_30 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-030 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Front-door IO validator for `/humanize:gen-plan`. The command definition allows and invokes this script before any draft relevance analysis or plan generation, making it the first hard gate in the gen-plan workflow (`commands/gen-plan.md:4`, `commands/gen-plan.md:30`, `commands/gen-plan.md:35`).
- algorithmic_behavior: The script parses only `--input`, `--output`, and `-h/--help` (`scripts/validate-gen-plan-io.sh:28`). Missing values, values that look like another flag, unknown options, and help all route through `usage()` and exit `6` (`scripts/validate-gen-plan-io.sh:15`, `scripts/validate-gen-plan-io.sh:31`, `scripts/validate-gen-plan-io.sh:39`, `scripts/validate-gen-plan-io.sh:47`, `scripts/validate-gen-plan-io.sh:50`). After required argument checks, it normalizes input and output paths with `realpath -m` when available, derives `OUTPUT_DIR`, then prints a diagnostic header (`scripts/validate-gen-plan-io.sh:57`, `scripts/validate-gen-plan-io.sh:68`, `scripts/validate-gen-plan-io.sh:73`). Validation is sequential and fail-fast: input must exist, input must be non-empty, output directory must exist, output target must not be a directory, output target must not already exist, and output directory must be writable (`scripts/validate-gen-plan-io.sh:78`, `scripts/validate-gen-plan-io.sh:86`, `scripts/validate-gen-plan-io.sh:94`, `scripts/validate-gen-plan-io.sh:102`, `scripts/validate-gen-plan-io.sh:110`, `scripts/validate-gen-plan-io.sh:117`). Success prints line count and target path, then exits `0` (`scripts/validate-gen-plan-io.sh:125`).
- inputs_outputs_state: Inputs are CLI flags `--input <draft>` and `--output <plan>`; no stdin, no persistent state. Outputs are human-readable validation diagnostics on stdout plus a process exit code. Exit code contract is declared in comments (`scripts/validate-gen-plan-io.sh:4`) and repeated by the gen-plan command handler (`commands/gen-plan.md:38`): `0` continue, `1` input missing, `2` input empty, `3` output directory missing, `4` output exists or is a directory, `5` no write permission, `6` invalid arguments/help. It does not create the output file and intentionally requires the output path to be new (`scripts/validate-gen-plan-io.sh:102`).
- gates_or_invariants: The generated plan cannot overwrite an existing path (`scripts/validate-gen-plan-io.sh:110`), cannot target a directory (`scripts/validate-gen-plan-io.sh:103`), and cannot proceed from an empty draft (`scripts/validate-gen-plan-io.sh:87`). The output directory must preexist rather than being auto-created (`scripts/validate-gen-plan-io.sh:95`). Required args are strict, but path semantics are filesystem-based after normalization.
- dependencies_and_callers: Depends on standard shell tools: `realpath`, `dirname`, `wc`, `tr`, and Bash conditionals. It is wired into `commands/gen-plan.md` as the only Bash allowed-tool for that command (`commands/gen-plan.md:5`) and is called as `"${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh" $ARGUMENTS` (`commands/gen-plan.md:35`). The broader gen-plan flow then reads the input, runs the draft relevance checker agent, performs draft analysis, resolves issues, and writes the output only after this gate passes (`commands/gen-plan.md:49`, `commands/gen-plan.md:75`, `commands/gen-plan.md:110`).
- edge_cases_or_failure_modes: `--help` exits with invalid-argument code `6`, which is consistent with current tests but unusual for a help path (`scripts/validate-gen-plan-io.sh:21`, `tests/test-gen-plan.sh:532`). `set -e` means unexpected command failures outside guarded expressions can terminate the script. `realpath -m` failure falls back to the original path string (`scripts/validate-gen-plan-io.sh:69`), so behavior can vary mildly by platform/tool availability. Write permission uses `[[ -w "$OUTPUT_DIR" ]]`, which checks directory writability but does not reserve the output path, so a race remains between validation and later Write. If `--input` or `--output` values begin with `--`, they are rejected as missing values (`scripts/validate-gen-plan-io.sh:32`, `scripts/validate-gen-plan-io.sh:40`).
- validation_or_tests: `tests/test-gen-plan.sh` has direct script tests for missing argument values, flag-as-value, unknown flag, input not found, empty input, missing output directory, existing output file, directory output target, valid paths, and `--help` exit code (`tests/test-gen-plan.sh:426`, `tests/test-gen-plan.sh:438`, `tests/test-gen-plan.sh:474`, `tests/test-gen-plan.sh:483`, `tests/test-gen-plan.sh:493`, `tests/test-gen-plan.sh:503`, `tests/test-gen-plan.sh:513`, `tests/test-gen-plan.sh:523`, `tests/test-gen-plan.sh:532`).
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-060 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Policy/error-message template for the RLCR Bash validator’s git-push gate. It is not executable logic, but it defines the user-facing contract when a push is blocked because the active loop is configured to keep commits local.
- algorithmic_behavior: The template states the blocked transition: “Current commits should stay local - no need to push to remote” (`prompt-template/block/git-push.md:3`) and points to the opt-in control `--push-every-round` for workflows that require pushing (`prompt-template/block/git-push.md:6`). The actual gate is in `hooks/loop-bash-validator.sh`: after locating an active loop and parsing strict state, it reads `STATE_PUSH_EVERY_ROUND`; when the value is not `true`, commands matching leading `git push` are blocked and this template is rendered to stderr before exit `2` (`hooks/loop-bash-validator.sh:52`, `hooks/loop-bash-validator.sh:67`, `hooks/loop-bash-validator.sh:80`, `hooks/loop-bash-validator.sh:82`, `hooks/loop-bash-validator.sh:84`, `hooks/loop-bash-validator.sh:89`).
- inputs_outputs_state: The template itself has no inputs and no mutable state. Its runtime input is indirect: the active loop state field `push_every_round`, parsed by `parse_state_file_strict()` from `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh:231`, `hooks/lib/loop-common.sh:265`, `hooks/lib/loop-common.sh:292`). Output is markdown text emitted by `load_and_render_safe` to stderr on block. The related start command advertises the same option in its argument hint (`commands/start-rlcr-loop.md:3`), and the setup script documents the default as local commits unless `--push-every-round` is used (`scripts/setup-rlcr-loop.sh:62`).
- gates_or_invariants: Default invariant: active RLCR loops do not push remote commits unless `push_every_round: true` is present in loop state. The gate only matches a command starting with optional whitespace followed by `git push` (`hooks/loop-bash-validator.sh:84`); it is a policy gate, not a full shell parser for all possible push aliases/wrappers. The template reinforces that the loop handles local commits until completion (`prompt-template/block/git-push.md:4`).
- dependencies_and_callers: Loaded through the shared template loader via `load_and_render_safe` from `hooks/loop-bash-validator.sh` (`hooks/loop-bash-validator.sh:89`). Template loading behavior comes from `hooks/lib/template-loader.sh`, which is sourced by `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:135`). Tests refer to this file as an existing template fixture (`tests/test-template-loader.sh:59`, `tests/test-template-loader.sh:216`) and comprehensive template tests also expect it to override fallback content.
- edge_cases_or_failure_modes: If the template is missing or invalid, the Bash validator has an inline fallback message (`hooks/loop-bash-validator.sh:85`). Since the file contains no placeholders, rendering is simple. It is a skip candidate only if “core algorithm” is interpreted as executable code only; however, for this repository, prompt templates are part of hook gate contracts and user-facing state transitions, so it should remain in coverage.
- validation_or_tests: `tests/test-template-loader.sh` verifies that `load_template` loads `block/git-push.md` and contains “Git Push Blocked” (`tests/test-template-loader.sh:55`, `tests/test-template-loader.sh:61`). It also verifies `load_and_render_safe` uses the real template rather than fallback (`tests/test-template-loader.sh:211`, `tests/test-template-loader.sh:218`). The runtime gate is covered indirectly by Bash-validator tests wherever `push_every_round` and git push behavior are exercised.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-090 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for AC-10 cancel authorization. It directly tests the shared `is_cancel_authorized` gate that permits exactly one otherwise-protected state transition: moving active loop state to `cancel-state.md` after `.cancel-requested` exists.
- algorithmic_behavior: The test script runs with `set -euo pipefail`, sources `hooks/lib/loop-common.sh` and shared test helpers, creates a temp loop directory, and seeds `state.md` (`tests/robustness/test-cancel-security-robustness.sh:13`, `tests/robustness/test-cancel-security-robustness.sh:17`, `tests/robustness/test-cancel-security-robustness.sh:20`, `tests/robustness/test-cancel-security-robustness.sh:27`). It then constructs shell command strings, lowercases them the same way the Bash hook does, and asserts whether `is_cancel_authorized "$LOOP_DIR" "$COMMAND_LOWER"` accepts or rejects. Positive tests cover canonical `mv state.md cancel-state.md`, `finalize-state.md` source, single quotes, unquoted paths, and normalized `/./` paths (`tests/robustness/test-cancel-security-robustness.sh:39`, `tests/robustness/test-cancel-security-robustness.sh:51`, `tests/robustness/test-cancel-security-robustness.sh:65`, `tests/robustness/test-cancel-security-robustness.sh:78`, `tests/robustness/test-cancel-security-robustness.sh:265`). Negative tests cover missing signal, substitution/backticks, shell chaining, wrong source/destination, extra args, newline injection, leftover variables, non-`mv` commands, mixed quotes, IFS manipulation, trailing spaces, non-standard source names, and symlink sources (`tests/robustness/test-cancel-security-robustness.sh:99`, `tests/robustness/test-cancel-security-robustness.sh:109`, `tests/robustness/test-cancel-security-robustness.sh:122`, `tests/robustness/test-cancel-security-robustness.sh:135`, `tests/robustness/test-cancel-security-robustness.sh:174`, `tests/robustness/test-cancel-security-robustness.sh:187`, `tests/robustness/test-cancel-security-robustness.sh:200`, `tests/robustness/test-cancel-security-robustness.sh:213`, `tests/robustness/test-cancel-security-robustness.sh:226`, `tests/robustness/test-cancel-security-robustness.sh:239`, `tests/robustness/test-cancel-security-robustness.sh:278`, `tests/robustness/test-cancel-security-robustness.sh:291`, `tests/robustness/test-cancel-security-robustness.sh:304`, `tests/robustness/test-cancel-security-robustness.sh:317`, `tests/robustness/test-cancel-security-robustness.sh:332`).
- inputs_outputs_state: Inputs are synthesized filesystem state and command strings. The critical state signal is `$LOOP_DIR/.cancel-requested`; accepted commands are expected only when this file exists (`tests/robustness/test-cancel-security-robustness.sh:41`, `tests/robustness/test-cancel-security-robustness.sh:99`). The test mutates temp files to model normal state, finalize state, symlinked state, and loop directory names containing `finalize` (`tests/robustness/test-cancel-security-robustness.sh:55`, `tests/robustness/test-cancel-security-robustness.sh:337`, `tests/robustness/test-cancel-security-robustness.sh:373`, `tests/robustness/test-cancel-security-robustness.sh:393`). Outputs are pass/fail counters through `pass`, `fail`, and `print_test_summary`, exiting with the summary status (`tests/test-helpers.sh:30`, `tests/test-helpers.sh:35`, `tests/test-helpers.sh:58`, `tests/robustness/test-cancel-security-robustness.sh:411`).
- gates_or_invariants: The tested implementation in `hooks/lib/loop-common.sh` requires `.cancel-requested` (`hooks/lib/loop-common.sh:459`, `hooks/lib/loop-common.sh:462`), rejects command substitution, newlines, shell operators, multiple trailing spaces, mixed quote styles, remaining `$` variables after loop-dir normalization, non-`mv` commands, missing or extra args, wrong source/destination, and symlink sources (`hooks/lib/loop-common.sh:466`, `hooks/lib/loop-common.sh:471`, `hooks/lib/loop-common.sh:476`, `hooks/lib/loop-common.sh:481`, `hooks/lib/loop-common.sh:495`, `hooks/lib/loop-common.sh:500`, `hooks/lib/loop-common.sh:538`, `hooks/lib/loop-common.sh:575`, `hooks/lib/loop-common.sh:579`, `hooks/lib/loop-common.sh:585`, `hooks/lib/loop-common.sh:593`, `hooks/lib/loop-common.sh:600`). The exact valid transition is `state.md` or `finalize-state.md` in the active loop directory to `cancel-state.md` in the same directory (`hooks/lib/loop-common.sh:587`, `hooks/lib/loop-common.sh:588`, `hooks/lib/loop-common.sh:595`). The regression tests assert exact path matching prevents a directory name containing `finalize` from bypassing symlink detection (`tests/robustness/test-cancel-security-robustness.sh:369`, `tests/robustness/test-cancel-security-robustness.sh:381`, `hooks/lib/loop-common.sh:600`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for `is_cancel_authorized` and `tests/test-helpers.sh` for temp directory and summary helpers (`tests/robustness/test-cancel-security-robustness.sh:17`, `tests/robustness/test-cancel-security-robustness.sh:18`). The production Bash hook calls the same helper to allow the cancel exception before blocking modifications of `finalize-state.md` or `state.md` (`hooks/loop-bash-validator.sh:117`, `hooks/loop-bash-validator.sh:119`, `hooks/loop-bash-validator.sh:127`, `hooks/loop-bash-validator.sh:129`, `hooks/loop-bash-validator.sh:264`, `hooks/loop-bash-validator.sh:267`, `hooks/loop-bash-validator.sh:274`, `hooks/loop-bash-validator.sh:276`). The user-facing cancel command delegates real cancellation to `scripts/cancel-rlcr-loop.sh`, which creates `.cancel-requested` and then moves `state.md` or `finalize-state.md` to `cancel-state.md` (`commands/cancel-rlcr-loop.md:11`, `scripts/cancel-rlcr-loop.sh:134`, `scripts/cancel-rlcr-loop.sh:138`).
- edge_cases_or_failure_modes: The test intentionally validates conservative rejection of mixed quote styles and multiple trailing spaces, even if some shells would execute them harmlessly (`tests/robustness/test-cancel-security-robustness.sh:278`, `tests/robustness/test-cancel-security-robustness.sh:304`). It uses lowercased absolute temp paths, matching production hook behavior but potentially masking case-sensitive path edge cases outside the test’s assumptions (`hooks/loop-bash-validator.sh:46`). The direct helper tests do not execute the candidate command, so they validate authorization parsing, not filesystem rename success. Symlink tests are important because cancellation is one of the few authorized paths through state-file write protections, and a symlinked source could otherwise move or expose unexpected files (`tests/robustness/test-cancel-security-robustness.sh:332`, `tests/robustness/test-cancel-security-robustness.sh:353`).
- validation_or_tests: This file is itself a validation target and ends with `print_test_summary "Cancel Security Robustness Test Summary"` and `exit $?` (`tests/robustness/test-cancel-security-robustness.sh:411`). Broader related coverage exists in `tests/test-cancel-signal-file.sh`, which also tests `is_cancel_authorized` and hook behavior across many bypass attempts, while this robustness file is focused on AC-10 security hardening and symlink/path-regression cases.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item evidence sections present; each assigned item appears once as a section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`