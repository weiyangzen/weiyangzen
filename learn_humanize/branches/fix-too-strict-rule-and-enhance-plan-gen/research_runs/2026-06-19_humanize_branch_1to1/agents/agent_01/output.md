# agent_01 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`
- inspection_mode: read-only repository research; no files modified

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root is the full Humanize Claude Code plugin surface. It coordinates slash commands, RLCR loop bootstrap, hook-based state guards, Codex review transitions, prompt templates, monitoring helpers, cancellation, gen-plan generation, and executable validation suites. The user-facing purpose is described in `README.md:7-14` and the loop flow is summarized in `README.md:58-65`.
- algorithmic_behavior: The root-level algorithm is a gated feedback loop. `/humanize:start-rlcr-loop` delegates to `scripts/setup-rlcr-loop.sh` through `commands/start-rlcr-loop.md:10-14`; setup validates git and plan-file constraints, writes `.humanize/rlcr/<timestamp>/state.md`, backs up the plan, creates `goal-tracker.md`, and emits the round-0 prompt. Hook registration in `hooks/hooks.json:14-58` then wires `Write`, `Edit`, `Read`, `Bash`, and `Stop` events to validator scripts and the Codex stop hook. On stop, `hooks/loop-codex-stop-hook.sh` validates active state, branch, plan integrity, todos, git cleanliness, summary presence, goal tracker initialization, large files, and Codex review output before either advancing rounds, entering finalize phase, or ending the loop. Gen-plan is a sibling prompt-driven algorithm: `commands/gen-plan.md:21-26` defines validation, relevance checking, draft analysis, user clarification, structured AC plan generation, and output writing.
- inputs_outputs_state: Inputs include slash-command arguments, a markdown plan or draft, Claude hook JSON, transcript JSONL, git repository state, `.humanize/rlcr` state files, prompt templates, and Codex output. Outputs include `.humanize/rlcr/<timestamp>/plan.md`, `state.md`, `goal-tracker.md`, `round-N-prompt.md`, `round-N-summary.md`, `round-N-review-prompt.md`, `round-N-review-result.md`, finalize files, cache/debug files under `XDG_CACHE_HOME`, JSON hook block decisions, and generated plan files. State transitions are mostly file renames and frontmatter updates: setup writes initial state at `scripts/setup-rlcr-loop.sh:508-521`; `end_loop` renames active state to terminal reason files at `hooks/lib/loop-common.sh:833-858`; the stop hook increments `current_round` at `hooks/loop-codex-stop-hook.sh:1062-1065`; `COMPLETE` enters finalize by renaming `state.md` to `finalize-state.md` at `hooks/loop-codex-stop-hook.sh:966-1025`; finalize completion renames to `complete-state.md` at `hooks/loop-codex-stop-hook.sh:654-660`; cancellation creates `.cancel-requested` and renames state in `scripts/cancel-rlcr-loop.sh:134-152`.
- gates_or_invariants: Setup requires a git repo with at least one commit at `scripts/setup-rlcr-loop.sh:211-220`; plan paths must be relative, no spaces/metacharacters, no symlink traversal, exist, be readable, remain inside the project, and avoid submodules at `scripts/setup-rlcr-loop.sh:227-328`; plan tracking mode is enforced at `scripts/setup-rlcr-loop.sh:334-379`. State parsing is strict on frontmatter and numeric fields at `hooks/lib/loop-common.sh:240-295`. Hook JSON rejects null bytes, invalid UTF-8, invalid JSON, missing `tool_name`, and excessive nesting through `hooks/lib/loop-common.sh:55-91` and callers such as `hooks/loop-read-validator.sh:24-43`. Validators block wrong-round files, prompt writes, state edits, protected plan backups, post-round-0 direct goal-tracker edits, and unsafe `.humanize` git adds through `hooks/loop-write-validator.sh:120-235`, `hooks/loop-edit-validator.sh:84-153`, and `hooks/loop-bash-validator.sh:74-373`. The stop hook fails closed on branch mismatch, plan mutation, todo incompletion, git status failure, oversized changed files, dirty git state, unpushed commits when configured, missing summaries, malformed Codex output, and non-terminal review markers at `hooks/loop-codex-stop-hook.sh:149-519` and `hooks/loop-codex-stop-hook.sh:920-1127`. `.humanize*` is ignored at `.gitignore:4-5`, while `git_adds_humanize` detects direct, broad, forced, and chained add forms at `hooks/lib/loop-common.sh:622-740`.
- dependencies_and_callers: External dependencies are `bash`, `zsh` compatibility in several scripts, `git`, `jq`, `python3`, `sed`, `awk`, `grep`, `wc`, `codex`, and timeout support from `scripts/portable-timeout.sh`. `hooks/lib/template-loader.sh:58-132` provides single-pass template rendering. `hooks/check-todos-from-transcript.py:20-129` parses latest TodoWrite calls from transcript JSONL. `commands/cancel-rlcr-loop.md:11-36` delegates cancellation to `scripts/cancel-rlcr-loop.sh`. Gen-plan depends on `scripts/validate-gen-plan-io.sh:78-130` and the `agents/draft-relevance-checker.md:12-37` subagent contract. CI coordinates syntax and focused suites via `.github/workflows/shell-syntax-check.yml:19-60`, `.github/workflows/template-test.yml:26-62`, and `.github/workflows/plan-file-test.yml:37-60`.
- edge_cases_or_failure_modes: Covered cases include missing or old active loop directories, zsh no-match glob behavior in the monitor, non-git directories, detached HEAD, rebase/merge/shallow/permission git states, plan files modified after start even when git status is clean, symlink and shell-metacharacter path attacks, state-file move/copy bypasses through wrappers and redirections, `.humanize` force-add bypasses, malformed hook input, huge or deeply nested input, Codex missing, Codex timeout/nonzero exit, Codex stdout-only result, empty review file, max-iteration completion edge, finalize-phase cancellation confirmation, and stale or wrong-round prompt/summary reads.
- validation_or_tests: `tests/run-all-tests.sh:32-63` enumerates the broad suite, including template, todo, plan-file, hook, cancel, monitor, gen-plan, and robustness suites. Assigned tests below cover monitor deletion and git operations directly. Additional validation includes `tests/test-humanize-escape.sh` for `.humanize` git-add detection, `tests/test-bash-validator-patterns.sh:69-195` for Bash modification detection, `tests/test-allowlist-validators.sh:70-260` for allowlisted round files, `tests/test-gen-plan.sh:54-180` for gen-plan command and agent structure, and `tests/test-plan-file-hooks.sh` for plan integrity and hook protections.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-031 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for monitor shutdown behavior. It verifies AC-1.1 and AC-1.3: deleting `.humanize/` during monitoring should produce a clean, user-friendly stop and restore terminal state without shell glob errors, as stated in `tests/manual-monitor-test.sh:3-14`.
- algorithmic_behavior: The script is a three-command state driver. `setup` creates a synthetic active loop directory at `.humanize/rlcr/2026-01-16_99-99-99`, writes minimal `state.md`, and writes minimal `goal-tracker.md` at `tests/manual-monitor-test.sh:23-45`. `delete` removes `.humanize` to trigger monitor graceful stop and prints the exact observed expectations at `tests/manual-monitor-test.sh:47-56`. `cleanup` removes leftovers at `tests/manual-monitor-test.sh:58-61`. Any other argument prints usage and exits nonzero at `tests/manual-monitor-test.sh:63-70`.
- inputs_outputs_state: Input is one positional subcommand: `setup`, `delete`, or `cleanup`. Output is terminal guidance for the human tester. Persistent state created by `setup` is `.humanize/rlcr/<timestamp>/state.md` with `current_round`, `max_iterations`, Codex model/effort, start timestamp, and `plan_file`; it also creates `goal-tracker.md` with immutable goal and AC seed text. `delete` transitions the project from active-loop state to missing-loop-root state by `rm -rf .humanize`.
- gates_or_invariants: The script assumes execution from or below repository root and normalizes to `PROJECT_ROOT` at `tests/manual-monitor-test.sh:19-21`. `set -euo pipefail` makes unexpected command failures abort. The monitor under test must be run separately as `source scripts/humanize.sh && humanize monitor rlcr-loop`, matching `tests/manual-monitor-test.sh:10-12` and `tests/manual-monitor-test.sh:44-45`.
- dependencies_and_callers: Direct dependencies are shell utilities `mkdir`, `rm`, and `echo`; behavioral dependency is `_humanize_monitor_codex` exposed by `scripts/humanize.sh`. The monitor function checks for `.humanize/rlcr` at `scripts/humanize.sh:224-229`, avoids zsh glob failures by using `find` at `scripts/humanize.sh:231-252`, and reports stopped monitoring during cleanup paths around `scripts/humanize.sh:540-561`.
- edge_cases_or_failure_modes: This manual spec targets deletion of the entire `.humanize` directory while monitor loops are active, terminal scroll-region/cursor restoration, and avoiding zsh/bash path expansion errors. It does not automate assertions; failure is observed by the human in the monitor terminal. Invalid subcommands are rejected with usage.
- validation_or_tests: Complements automated monitor suites listed in `tests/run-all-tests.sh:48-50`: `tests/test-zsh-monitor-safety.sh`, `tests/test-monitor-runtime.sh`, and `tests/test-monitor-e2e-real.sh`. Its expected messages also align with real monitor E2E checks that look for stopped monitoring and terminal cleanup behavior.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-061 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Prompt/block template for a fail-closed git status gate. It defines the user-facing block reason when repository state cannot be verified because `git status --porcelain` failed or timed out.
- algorithmic_behavior: The template renders a markdown block headed “Git Status Failed”, interpolates `{{GIT_STATUS_EXIT}}`, explains likely causes, and instructs manual `git status` recovery at `prompt-template/block/git-status-failed.md:1-10`. It is not logic by itself; it is the message contract for a stop-hook transition.
- inputs_outputs_state: Input variable is `GIT_STATUS_EXIT`. Output is markdown passed as the `reason` field in a JSON hook response. No state is mutated by this file. If the variable is omitted, the single-pass renderer keeps the placeholder because missing variables are preserved by `hooks/lib/template-loader.sh:115-122`.
- gates_or_invariants: The associated invariant is fail-closed repository verification. In the stop hook, `GIT_STATUS_CACHED` is captured once; nonzero `GIT_STATUS_EXIT` blocks exit rather than treating status as clean at `hooks/loop-codex-stop-hook.sh:319-340`. This prevents bypassing dirty-state, large-file, and commit/push checks when git is locked, invalid, or slow.
- dependencies_and_callers: Loaded through `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh:335-336`; rendering semantics come from `hooks/lib/template-loader.sh:58-132`. The inline fallback in `hooks/loop-codex-stop-hook.sh:330-334` mirrors the template so hook behavior survives missing template files.
- edge_cases_or_failure_modes: Timeout and generic git failure share the same block path. The template mentions lock contention, invalid repositories, and large repos at `prompt-template/block/git-status-failed.md:5-8`. It does not distinguish exit code `124` from other failures; that classification remains in calling code. Missing template content falls back to an inline message through `load_and_render_safe`.
- validation_or_tests: Template existence and loader behavior are covered by `tests/test-template-references.sh` through the all-tests list at `tests/run-all-tests.sh:37-40`; comprehensive real-template rendering is covered generally by `tests/test-templates-comprehensive.sh:492-540`. The stop-hook caller is exercised by hook and error scenario suites in `tests/run-all-tests.sh:41-42`.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-091 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for production git state summarization and git state detection. It tests `humanize_parse_git_status` and `humanize_detect_git_state` from `scripts/humanize.sh`, as declared in `tests/robustness/test-git-operations-robustness.sh:3-10` and sourced at `tests/robustness/test-git-operations-robustness.sh:15-20`.
- algorithmic_behavior: The test creates isolated git repositories, mutates file states, calls production functions, parses pipe-delimited results, and checks expected counters or state strings. `parse_result` extracts `modified`, `added`, `deleted`, `untracked`, `insertions`, and `deletions` fields from the production output at `tests/robustness/test-git-operations-robustness.sh:35-47`. `init_test_repo` creates a committed baseline repository at `tests/robustness/test-git-operations-robustness.sh:49-62`.
- inputs_outputs_state: Inputs are generated temp repos and filesystem/git mutations: untracked files, modified files, staged adds, deletions, renamed files, binary files, files with spaces, detached checkout, feature branch, empty repo, and simulated git internals. Outputs are PASS/FAIL counters via `tests/test-helpers.sh:30-78`; final exit status is returned from `print_test_summary` at `tests/robustness/test-git-operations-robustness.sh:502-507`. State transitions include git init/commit, checkout detached and back, branch creation/deletion, staging/resetting, simulated `rebase-merge`, `MERGE_HEAD`, `shallow`, and permission-denied `.git`.
- gates_or_invariants: For `humanize_parse_git_status`, clean repo must yield zero modified/added/untracked at `tests/robustness/test-git-operations-robustness.sh:71-83`; untracked, modified, staged added, deleted, insertions, and deletions must be counted at `tests/robustness/test-git-operations-robustness.sh:98-158`, `tests/robustness/test-git-operations-robustness.sh:218-230`, and `tests/robustness/test-git-operations-robustness.sh:349-367`. Multiple simultaneous states must all be represented at `tests/robustness/test-git-operations-robustness.sh:197-216`. Renames count as modified at `tests/robustness/test-git-operations-robustness.sh:265-282`. For `humanize_detect_git_state`, normal, detached, rebase, merge, shallow, non-git, and permission scenarios must return known state strings at `tests/robustness/test-git-operations-robustness.sh:377-500`.
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, `scripts/humanize.sh`, and `git`. The production function under test returns `0|0|0|0|0|0|not a git repo` outside a repo at `scripts/humanize.sh:160-164`; parses porcelain XY states at `scripts/humanize.sh:168-189`; parses `git diff --shortstat` at `scripts/humanize.sh:191-203`. Git state detection uses `git rev-parse --git-dir`, checks readability, then probes rebase, merge, shallow, and detached HEAD at `scripts/humanize.sh:110-156`.
- edge_cases_or_failure_modes: The spec covers non-git directories, detached HEAD status parsing, empty repos with no commits, feature branches, many untracked files, filenames with spaces, binary files, staged plus unstaged changes in the same file, line deletion counting, simulated rebase/merge/shallow states, and permission errors that may legitimately degrade to `not_a_repo` on some systems at `tests/robustness/test-git-operations-robustness.sh:478-500`. A notable implementation limitation is that `humanize_parse_git_status` captures `git status --porcelain` without explicit timeout or nonzero exit handling at `scripts/humanize.sh:168`; the stop hook has a separate fail-closed status path for lifecycle gating.
- validation_or_tests: This file is itself the validation. It contains 25 numbered tests and is included in the aggregate suite at `tests/run-all-tests.sh:52-58`. It validates production parsing rather than stubs because it sources `scripts/humanize.sh` directly.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4 item headings above; item identifiers are intentionally not repeated here so each assigned identifier string appears once in this output
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`