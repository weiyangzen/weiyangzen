# agent_10 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-010 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: User-facing block-message template library for RLCR hook decisions. The directory does not execute logic itself; it supplies Markdown reason bodies consumed by validators and stop hooks when they block a read/write/bash/stop action. `hooks/lib/template-loader.sh:24-31` resolves the parent `prompt-template` directory, `load_template` reads files by relative path, and `load_and_render_safe` ensures a fallback reason if a template is missing or renders empty (`hooks/lib/template-loader.sh:170-203`).
- algorithmic_behavior: The directory contains 27 short templates grouped by gate type: git hygiene (`git-add-humanize.md`, `git-push.md`, `git-not-clean*.md`, `git-status-failed.md`, `unpushed-commits.md`), protected loop artifacts (`state-file-modification.md`, `finalize-state-file-modification.md`, `plan-backup-protected.md`, `plan-file-modified.md`, `schema-outdated.md`), task/progress readiness (`incomplete-todos.md`, `work-summary-missing.md`, `goal-tracker*.md`), path/round validation (`wrong-*.md`, `prompt-file-write.md`, `summary-bash-write.md`, `todos-file-access.md`), and review/maintainability failures (`codex-review-failed.md`, `large-files.md`). Runtime hooks select one block, substitute placeholders such as `{{CURRENT_ROUND}}`, then either emit JSON block reasons or stderr messages with a nonzero hook exit.
- inputs_outputs_state: Inputs are template path plus `VAR=value` substitutions. Placeholder syntax is uppercase `{{VARIABLE_NAME}}`; missing variables remain literal and values are single-pass rendered, so inserted content containing braces is not recursively expanded (`hooks/lib/template-loader.sh:7-13`, `hooks/lib/template-loader.sh:50-57`, `hooks/lib/template-loader.sh:71-129`). Outputs are rendered Markdown reason strings. State mutation is indirect: templates guide the user while hooks preserve/advance loop state by blocking actions, returning JSON decisions, or preventing final stop progression.
- gates_or_invariants: The blocks encode fail-closed invariants: no `.humanize` staging (`prompt-template/block/git-add-humanize.md:1-34`), no push unless per-round push is enabled (`prompt-template/block/git-push.md:1-9`), clean git state before exit (`prompt-template/block/git-not-clean.md:1-16`), no plan mutation during an active loop (`prompt-template/block/plan-file-modified.md:1-12`), no writes to managed state/prompt/todos files (`prompt-template/block/state-file-modification.md:1-10`, `prompt-template/block/prompt-file-write.md:1-12`, `prompt-template/block/todos-file-access.md:1-8`), current-round summary/read path enforcement (`prompt-template/block/wrong-round-number.md:1-7`, `prompt-template/block/wrong-round-file.md:1-9`), Round 0-only goal-tracker initialization (`prompt-template/block/goal-tracker-not-initialized.md:1-18`), and large-file line-count remediation (`prompt-template/block/large-files.md:1-25`).
- dependencies_and_callers: Direct callers include `hooks/loop-read-validator.sh:119-164` for wrong location/round/directory reads, `hooks/loop-write-validator.sh:151-234` for protected plan, summary location, round number, and directory writes, `hooks/loop-edit-validator.sh:103-150` for plan backup and wrong-round edits, `hooks/loop-bash-validator.sh:82-90` and `hooks/loop-bash-validator.sh:314-318` for push and plan-backup bash gates, shared message helpers in `hooks/lib/loop-common.sh:347-405` and `hooks/lib/loop-common.sh:812-824`, plan schema validation in `hooks/loop-plan-file-validator.sh:59-87`, and stop-hook gates in `hooks/loop-codex-stop-hook.sh:247-251`, `hooks/loop-codex-stop-hook.sh:297-308`, `hooks/loop-codex-stop-hook.sh:335-339`, `hooks/loop-codex-stop-hook.sh:410-422`, `hooks/loop-codex-stop-hook.sh:473-485`, `hooks/loop-codex-stop-hook.sh:504-516`, `hooks/loop-codex-stop-hook.sh:539-556`, and `hooks/loop-codex-stop-hook.sh:619-631`.
- edge_cases_or_failure_modes: Missing templates should not create empty block reasons because `load_and_render_safe` falls back (`hooks/lib/template-loader.sh:179-199`). Missing variables remain visible, which preserves diagnostics but can leak unresolved placeholders into prompts. Some templates are loaded with `load_template` rather than rendered, for example `git-not-clean-humanize-local.md` and `git-not-clean-untracked.md` are appended as static notes (`hooks/loop-codex-stop-hook.sh:445-460`). Template wording can drift from hook behavior; notably `schema-outdated.md` says the loop will be terminated as unexpected, while the plan-file validator emits a block decision and exits without calling loop termination (`hooks/loop-plan-file-validator.sh:71-87`).
- validation_or_tests: `tests/test-template-references.sh:54-111` scans shell callers for referenced templates and fails on missing files; `tests/test-template-references.sh:172-200` checks critical validators use safe rendering. `tests/test-template-loader.sh:55-77` covers existing and missing template loads; `tests/test-template-loader.sh:147-164` renders a real wrong-round template; `tests/test-template-loader.sh:197-221` covers fallback and existing-template safe rendering. `tests/test-templates-comprehensive.sh:90-107` loads every template, `tests/test-templates-comprehensive.sh:446-459` tests safe fallback, and `tests/test-templates-comprehensive.sh:563-590` renders all templates with dummy placeholder values.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-040 `file` `tests/test-helpers.sh`
- cursor: `[_]`
- core_role: Shared Bash test harness for robustness test scripts. It is not production RLCR runtime logic, but it is part of the executable specification surface: it defines common pass/fail accounting, temporary workspace setup, and mock git repository initialization used by robustness tests that validate hook behavior.
- algorithmic_behavior: Defines color constants (`tests/test-helpers.sh:13-16`), initializes global counters (`tests/test-helpers.sh:22-24`), and exposes `pass`, `fail`, and `skip` functions that print formatted result lines and increment counters (`tests/test-helpers.sh:30-52`). `print_test_summary` prints aggregate results and returns success only when `TESTS_FAILED` is zero (`tests/test-helpers.sh:58-77`). `setup_test_dir` creates a temp directory, stores it in `TEST_DIR`, and installs an EXIT cleanup trap (`tests/test-helpers.sh:84-89`). `init_test_git_repo` creates a repository, configures deterministic identity/GPG behavior, writes `file.txt`, and makes an initial commit (`tests/test-helpers.sh:91-104`).
- inputs_outputs_state: Inputs are function arguments: message strings for result functions, optional expected/got details for `fail`, optional summary title for `print_test_summary`, and a directory path for `init_test_git_repo`. Outputs are terminal lines and function exit status from `print_test_summary`. State transitions are global counter increments, assignment of `TEST_DIR`, registration of a cleanup trap, filesystem creation under the temp directory, `cd` into and out of the test repo, and git index/commit creation.
- gates_or_invariants: The main gate is `print_test_summary`: any accumulated failure causes return code 1 (`tests/test-helpers.sh:71-77`). `fail` records a failure but does not immediately exit, letting scripts continue and report multiple defects (`tests/test-helpers.sh:35-44`). `setup_test_dir` assumes the generated temp directory should be removed on process exit (`tests/test-helpers.sh:86-88`). `init_test_git_repo` establishes a clean tracked baseline with a committed file so hook tests can create controlled dirty, untracked, branch, or schema states (`tests/test-helpers.sh:95-104`).
- dependencies_and_callers: Depends on Bash arithmetic, `mktemp`, `trap`, `git init`, `git config`, `git add`, and `git commit`. It is sourced by robustness tests such as `tests/robustness/test-goal-tracker-robustness.sh:19`, `tests/robustness/test-cancel-security-robustness.sh:18`, `tests/robustness/test-git-operations-robustness.sh:17`, `tests/robustness/test-plan-file-robustness.sh:84`, `tests/robustness/test-template-stress-robustness.sh:17`, `tests/robustness/test-session-robustness.sh:17`, `tests/robustness/test-path-validation-robustness.sh:14`, `tests/robustness/test-state-file-robustness.sh:17`, and `tests/robustness/test-hook-input-robustness.sh:17`.
- edge_cases_or_failure_modes: Sourcing the helper resets counters, so it should be sourced once per test process. `setup_test_dir` installs a single EXIT trap and would replace any prior cleanup trap in the caller. The trap uses `rm -rf $TEST_DIR` inside double quotes at trap-registration time; `mktemp -d` paths are normally safe, but this is less robust for unusual whitespace than a quoted runtime cleanup function. `init_test_git_repo` changes process working directory and relies on `cd -` to restore it; under caller `set -e`, any git or cd failure aborts the test. Result functions use `echo -e`, which is acceptable for local shell tests but not fully portable across all shells.
- validation_or_tests: The helper itself is not directly unit-tested in the inspected snippets. Its behavior is indirectly validated by every robustness script that sources it and ends through `print_test_summary`, for example `tests/robustness/test-state-file-robustness.sh:199-213` uses the pass/fail API to assert strict parser rejection of missing required fields.
- skip_candidate: `yes: shared validation harness rather than runtime algorithm implementation; still relevant as the executable test scaffold for core hook behavior`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-070 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Specific block template for outdated RLCR state schema detection. It supplies the user-facing reason when required state fields are missing, especially `plan_tracked` or `start_branch`, indicating a loop created by an older humanize version.
- algorithmic_behavior: The template declares the missing required field with `{{FIELD_NAME}}` (`prompt-template/block/schema-outdated.md:3`), explains the version-mismatch cause (`prompt-template/block/schema-outdated.md:5`), gives recovery options to cancel, update to version 1.1.2+, and restart (`prompt-template/block/schema-outdated.md:7-10`), and states that preservation requires unexpected termination (`prompt-template/block/schema-outdated.md:12`). `hooks/loop-plan-file-validator.sh:59-65` renders it through `load_and_render_safe`, then JSON-escapes the rendered Markdown and returns a hook block decision (`hooks/loop-plan-file-validator.sh:67-76`).
- inputs_outputs_state: Input is the missing field name passed as `FIELD_NAME=$field_name` from `schema_validation_error` (`hooks/loop-plan-file-validator.sh:60-65`). Output is a Markdown reason embedded in JSON as `"decision": "block"` (`hooks/loop-plan-file-validator.sh:71-75`). The template itself has no state mutation; the validator exits successfully after emitting a block response when the field is empty (`hooks/loop-plan-file-validator.sh:79-88`).
- gates_or_invariants: Enforces v1.1.2+ state requirements in the plan-file validator. After strict state parsing succeeds, `PLAN_TRACKED` and `START_BRANCH` are mapped from parsed state (`hooks/loop-plan-file-validator.sh:50-53`), then required fields are checked for emptiness (`hooks/loop-plan-file-validator.sh:79-86`). This gate prevents branch consistency and plan tracking checks from running with an ambiguous legacy state. The underlying strict parser independently requires `current_round` and `max_iterations`, validates numeric values, and returns nonzero for malformed state (`hooks/lib/loop-common.sh:231-296`).
- dependencies_and_callers: Primary caller is `hooks/loop-plan-file-validator.sh:65`. Related schema behavior also exists in `hooks/loop-codex-stop-hook.sh:130-146`, but that path uses an inline reason instead of this template. Both paths depend on state fields populated by `parse_state_file_strict` from `hooks/lib/loop-common.sh:240-296` and JSON formatting through `jq` in the validator.
- edge_cases_or_failure_modes: If the template is missing or empty, the validator fallback contains the same basic recovery options and still blocks (`hooks/loop-plan-file-validator.sh:61-65`). There is a behavior/text mismatch: the template says the loop will be terminated as unexpected (`prompt-template/block/schema-outdated.md:12`), while the plan-file validator returns a block decision and exits without calling a loop-ending function (`hooks/loop-plan-file-validator.sh:71-88`). The stop hook comment also says outdated schema terminates unexpectedly, but the inspected code returns a JSON block and exits (`hooks/loop-codex-stop-hook.sh:130-146`). This may be intentional hook semantics, but the wording is stronger than the visible state transition.
- validation_or_tests: `tests/test-plan-file-hooks.sh:631-653` creates a state file missing `plan_tracked` and expects the stop hook to return a JSON block mentioning schema, missing field, or `plan_tracked`. `tests/robustness/test-state-file-robustness.sh:199-213` verifies strict parsing rejects a state file missing required `current_round` and `max_iterations`. Template loading and rendering safety are covered by `tests/test-template-references.sh:54-111`, `tests/test-template-loader.sh:197-221`, and `tests/test-templates-comprehensive.sh:563-590`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 assigned rows represented as exactly one item section each`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`