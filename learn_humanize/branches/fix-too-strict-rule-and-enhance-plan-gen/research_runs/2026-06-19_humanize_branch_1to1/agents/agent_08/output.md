# agent_08 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-008 `file` `README.md`
- cursor: `[_]`
- core_role: Public behavior contract for the Humanize/RLCR workflow. It defines RLCR as an iterative Claude implementation plus independent Codex review loop and introduces `/humanize:start-rlcr-loop`, `/humanize:cancel-rlcr-loop`, and `/humanize:gen-plan` as the primary algorithm entry points (`README.md:7`, `README.md:13`, `README.md:78`).
- algorithmic_behavior: The documented state machine is: user plan file -> Claude implements and summarizes -> Codex reviews -> feedback returns to Claude until Codex says `COMPLETE` or iteration limits end the loop (`README.md:60`, `README.md:65`). The `gen-plan` workflow is documented as path validation, repository relevance check, clarity/consistency/completeness/functionality analysis, user issue resolution, and structured `plan.md` generation with `AC-X` acceptance criteria (`README.md:103`, `README.md:120`).
- inputs_outputs_state: Inputs are a plan path for `/humanize:start-rlcr-loop <plan.md>` plus options `--max`, `--codex-model <MODEL:EFFORT>`, `--codex-timeout`, and `--push-every-round` (`README.md:90`, `README.md:100`). Loop outputs are progress artifacts under `.humanize/rlcr/<timestamp>/` (`README.md:75`). `gen-plan` consumes `--input <draft.md>` and writes `--output <plan.md>` (`README.md:105`, `README.md:110`).
- gates_or_invariants: The core invariant is iteration over one-shot completion: implementation must continue until acceptance criteria are met or loop termination conditions fire (`README.md:15`, `README.md:21`). Codex is a required review dependency (`README.md:52`, `README.md:55`). `gen-plan` must reject or pause on invalid paths, irrelevant drafts, unclear/inconsistent/incomplete/functionally deficient drafts before output generation (`README.md:114`, `README.md:119`).
- dependencies_and_callers: Depends on Claude Code plugin execution, the Codex CLI, a user-authored plan file, and command definitions under `commands/` and hook logic under `hooks/` that implement the documented loop. The README is caller-facing documentation rather than an executable caller.
- edge_cases_or_failure_modes: Explicitly documented operational exits are user cancellation (`README.md:76`) and max-iteration termination (`README.md:65`, `README.md:94`). User-supplied draft quality issues in `gen-plan` require user engagement before plan generation (`README.md:116`, `README.md:119`).
- validation_or_tests: The README is not itself executable, but it defines behavior exercised by tests such as `tests/test-finalize-phase.sh` for `COMPLETE`, max-iteration, and active-loop state transitions, and by template/rendering tests for prompt-template behavior.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-038 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR Finalize Phase state machine. It validates that Codex `COMPLETE` no longer ends immediately; instead it transitions normal `state.md` into `finalize-state.md`, blocks with a final simplification/refactor prompt, then exits only after finalize gates pass (`tests/test-finalize-phase.sh:3`, `tests/test-finalize-phase.sh:6`, `tests/test-finalize-phase.sh:465`).
- algorithmic_behavior: Test setup creates an isolated git repo, loop state frontmatter, plan backup, and goal tracker (`tests/test-finalize-phase.sh:87`, `tests/test-finalize-phase.sh:123`). Positive behavior includes detecting `finalize-state.md` as active, allowing `finalize-summary.md`, completing by renaming `finalize-state.md` to `complete-state.md`, and preserving normal non-`COMPLETE` round behavior with `state.md` and round increment (`tests/test-finalize-phase.sh:234`, `tests/test-finalize-phase.sh:286`, `tests/test-finalize-phase.sh:417`, `tests/test-finalize-phase.sh:580`). Negative behavior includes max-iteration skip to `maxiter-state.md`, missing summary block, dirty git block, incomplete todo block, complete-state inactivity, and no Codex call during Finalize (`tests/test-finalize-phase.sh:483`, `tests/test-finalize-phase.sh:370`, `tests/test-finalize-phase.sh:395`, `tests/test-finalize-phase.sh:526`, `tests/test-finalize-phase.sh:255`, `tests/test-finalize-phase.sh:387`).
- inputs_outputs_state: Inputs are mocked Codex outputs, hook JSON payloads for Stop/Write/Edit/Bash/Read/UserPromptSubmit validators, git repository state, transcript JSONL with `TodoWrite`, and loop state files (`tests/test-finalize-phase.sh:51`, `tests/test-finalize-phase.sh:287`, `tests/test-finalize-phase.sh:323`, `tests/test-finalize-phase.sh:575`). Outputs are hook exit codes, JSON block decisions, state-file renames, review result files, and pass/fail counters (`tests/test-finalize-phase.sh:36`, `tests/test-finalize-phase.sh:430`, `tests/test-finalize-phase.sh:604`, `tests/test-finalize-phase.sh:672`).
- gates_or_invariants: `finalize-state.md` must be protected from Write/Edit/Bash mutation and from `mv`/`cp` source bypasses (`tests/test-finalize-phase.sh:298`, `tests/test-finalize-phase.sh:334`, `tests/test-finalize-phase.sh:346`). Finalize completion requires `finalize-summary.md`, clean git, and complete todos; it must not invoke Codex because Codex already approved before entering Finalize (`tests/test-finalize-phase.sh:370`, `tests/test-finalize-phase.sh:395`, `tests/test-finalize-phase.sh:526`, `tests/test-finalize-phase.sh:439`). Max-iteration at `COMPLETE` must skip Finalize and write `maxiter-state.md` (`tests/test-finalize-phase.sh:483`, `tests/test-finalize-phase.sh:497`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` for state/path helpers (`tests/test-finalize-phase.sh:48`). Calls `hooks/loop-codex-stop-hook.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-plan-file-validator.sh` (`tests/test-finalize-phase.sh:289`, `tests/test-finalize-phase.sh:313`, `tests/test-finalize-phase.sh:325`, `tests/test-finalize-phase.sh:377`, `tests/test-finalize-phase.sh:648`, `tests/test-finalize-phase.sh:662`). The implementation it verifies uses `find_active_loop` to accept `state.md` or `finalize-state.md` only in the newest loop dir (`hooks/lib/loop-common.sh:147`, `hooks/lib/loop-common.sh:166`), strict state parsing (`hooks/lib/loop-common.sh:231`, `hooks/lib/loop-common.sh:296`), and stop-hook Finalize transition/completion branches (`hooks/loop-codex-stop-hook.sh:966`, `hooks/loop-codex-stop-hook.sh:1025`, `hooks/loop-codex-stop-hook.sh:648`, `hooks/loop-codex-stop-hook.sh:660`).
- edge_cases_or_failure_modes: Covers `complete-state.md` not active, normal `state.md` still active, Finalize validators parsing `finalize-state.md`, missing summary, dirty working tree, incomplete todos before expensive review, max iterations, and false Codex invocation during Finalize (`tests/test-finalize-phase.sh:243`, `tests/test-finalize-phase.sh:270`, `tests/test-finalize-phase.sh:631`, `tests/test-finalize-phase.sh:370`, `tests/test-finalize-phase.sh:395`, `tests/test-finalize-phase.sh:526`, `tests/test-finalize-phase.sh:486`, `tests/test-finalize-phase.sh:541`).
- validation_or_tests: This file is the validation suite. It exits with `TESTS_FAILED`, so any failing assertion produces a nonzero process status (`tests/test-finalize-phase.sh:672`, `tests/test-finalize-phase.sh:681`). I inspected it directly but did not execute it in this read-only research run.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-068 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Prompt block used as a blocking contract when the active RLCR plan file diverges from the loop’s backup. It tells the agent that active-loop plan modification is forbidden and gives the only allowed recovery path (`prompt-template/block/plan-file-modified.md:1`, `prompt-template/block/plan-file-modified.md:5`).
- algorithmic_behavior: The template renders `{{PLAN_FILE}}` and `{{BACKUP_PATH}}`, then instructs cancellation, plan update, and new loop start rather than continuing with a changed plan (`prompt-template/block/plan-file-modified.md:3`, `prompt-template/block/plan-file-modified.md:7`, `prompt-template/block/plan-file-modified.md:12`). It is invoked by `hooks/loop-codex-stop-hook.sh` after comparing the current plan to `$LOOP_DIR/plan.md`; on diff mismatch the hook emits a JSON block decision with system message `Loop: Blocked - plan file modified` (`hooks/loop-codex-stop-hook.sh:185`, `hooks/loop-codex-stop-hook.sh:234`, `hooks/loop-codex-stop-hook.sh:247`, `hooks/loop-codex-stop-hook.sh:251`).
- inputs_outputs_state: Inputs are `PLAN_FILE` and `BACKUP_PATH` substitutions. The upstream state inputs are `PLAN_TRACKED`, `PLAN_FILE`, `PROJECT_ROOT`, `LOOP_DIR`, and the backup file created when the loop started (`hooks/loop-codex-stop-hook.sh:185`, `hooks/loop-codex-stop-hook.sh:186`). Output is a human-readable Markdown reason embedded in hook JSON, not a file mutation.
- gates_or_invariants: The invariant is plan immutability during an active RLCR loop. The stop hook first requires the backup to exist, blocks if the project plan is deleted, checks tracked-plan uncommitted changes, and always diffs current plan content against the backup to catch committed or ignored-file changes (`hooks/loop-codex-stop-hook.sh:188`, `hooks/loop-codex-stop-hook.sh:202`, `hooks/loop-codex-stop-hook.sh:217`, `hooks/loop-codex-stop-hook.sh:233`). The template itself reinforces the cancel-update-restart gate (`prompt-template/block/plan-file-modified.md:7`, `prompt-template/block/plan-file-modified.md:10`).
- dependencies_and_callers: Rendered through `load_and_render_safe` and `render_template` from `hooks/lib/template-loader.sh`, whose renderer performs single-pass `{{VAR}}` substitution and leaves missing variables literal (`hooks/lib/template-loader.sh:50`, `hooks/lib/template-loader.sh:58`, `hooks/lib/template-loader.sh:116`). Direct caller is the stop hook’s plan integrity quick check (`hooks/loop-codex-stop-hook.sh:181`, `hooks/loop-codex-stop-hook.sh:247`).
- edge_cases_or_failure_modes: If `plan.md` backup is missing, a separate backup-missing block fires before this template (`hooks/loop-codex-stop-hook.sh:188`, `hooks/loop-codex-stop-hook.sh:198`). If original plan is deleted, a deletion block fires (`hooks/loop-codex-stop-hook.sh:201`, `hooks/loop-codex-stop-hook.sh:211`). If tracked plan has uncommitted changes, a tracked-plan-specific block fires before content diff (`hooks/loop-codex-stop-hook.sh:217`, `hooks/loop-codex-stop-hook.sh:230`). This template specifically covers content mismatch after those earlier checks.
- validation_or_tests: Covered indirectly by stop-hook plan integrity behavior and by template reference/rendering tests. The template syntax is compatible with the template loader’s uppercase placeholder convention (`hooks/lib/template-loader.sh:7`, `hooks/lib/template-loader.sh:13`).
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-098 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the prompt-template rendering engine under AC-8 stress conditions: large values, large templates, many substitutions, special characters, and placeholder injection prevention (`tests/robustness/test-template-stress-robustness.sh:3`, `tests/robustness/test-template-stress-robustness.sh:10`).
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh`, then tests `render_template`, `load_and_render`, and `load_and_render_safe` over normal substitutions, fallback rendering, 1KB/100KB files, 10KB/100KB variable values, 50 substitutions, regex metacharacters, ampersands, backslashes, newlines, empty values, dollar signs, repeated variables, boundary placeholders, and placeholder-only templates (`tests/robustness/test-template-stress-robustness.sh:16`, `tests/robustness/test-template-stress-robustness.sh:33`, `tests/robustness/test-template-stress-robustness.sh:82`, `tests/robustness/test-template-stress-robustness.sh:111`, `tests/robustness/test-template-stress-robustness.sh:134`, `tests/robustness/test-template-stress-robustness.sh:161`, `tests/robustness/test-template-stress-robustness.sh:274`).
- inputs_outputs_state: Inputs are inline template strings, generated temporary template files, and `VAR=value` arguments. Outputs are rendered strings and pass/fail records via `tests/test-helpers.sh` summary (`tests/robustness/test-template-stress-robustness.sh:17`, `tests/robustness/test-template-stress-robustness.sh:19`, `tests/robustness/test-template-stress-robustness.sh:289`). It creates temporary files such as `standard.md` and `large.md` under `TEST_DIR` only (`tests/robustness/test-template-stress-robustness.sh:64`, `tests/robustness/test-template-stress-robustness.sh:120`).
- gates_or_invariants: Missing templates must render fallback content (`tests/robustness/test-template-stress-robustness.sh:44`). Large inputs must remain renderable and contain substitutions (`tests/robustness/test-template-stress-robustness.sh:96`, `tests/robustness/test-template-stress-robustness.sh:128`). Special characters must be literal-safe, especially sed/regex-sensitive characters even though the implementation uses `awk` (`tests/robustness/test-template-stress-robustness.sh:161`, `tests/robustness/test-template-stress-robustness.sh:172`). Placeholder injection must be prevented: a value containing `{{VAR_B}}` must not be recursively expanded (`tests/robustness/test-template-stress-robustness.sh:218`, `tests/robustness/test-template-stress-robustness.sh:227`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, whose core algorithm builds `TMPL_VAR_` environment variables, scans lines with `awk`, replaces known placeholders once, keeps unknown placeholders unchanged, and prints replaced values without rescanning (`hooks/lib/template-loader.sh:62`, `hooks/lib/template-loader.sh:74`, `hooks/lib/template-loader.sh:88`, `hooks/lib/template-loader.sh:116`, `hooks/lib/template-loader.sh:120`). It also depends on test helper setup/summary functions from `tests/test-helpers.sh`.
- edge_cases_or_failure_modes: Explicitly exercises performance/size pressure but without hard time thresholds beyond reporting elapsed milliseconds (`tests/robustness/test-template-stress-robustness.sh:86`, `tests/robustness/test-template-stress-robustness.sh:104`, `tests/robustness/test-template-stress-robustness.sh:125`). Notes circular references are not applicable because templates do not support variable references between vars (`tests/robustness/test-template-stress-robustness.sh:5`, `tests/robustness/test-template-stress-robustness.sh:8`). A subtle behavior is line-oriented `awk` rendering; multiline variable values are accepted if expected substrings survive, not necessarily if exact byte-for-byte layout is preserved (`tests/robustness/test-template-stress-robustness.sh:195`, `tests/robustness/test-template-stress-robustness.sh:204`).
- validation_or_tests: This file is itself the validation suite for template stress behavior and exits with the helper summary status (`tests/robustness/test-template-stress-robustness.sh:285`, `tests/robustness/test-template-stress-robustness.sh:290`). I inspected it directly but did not execute it in this read-only research run.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-008`, `FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-038`, `FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-068`, `FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-098`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`