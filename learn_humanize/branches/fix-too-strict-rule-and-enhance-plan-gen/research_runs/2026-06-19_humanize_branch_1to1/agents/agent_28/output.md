# agent_28 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-028 `file` `scripts/portable-timeout.sh`
- cursor: `[_]`
- core_role: Sourceable runtime helper that normalizes timeout behavior across macOS and Linux for RLCR setup scripts, hooks, Codex review execution, and robustness tests. Its primary exported interface is `run_with_timeout <seconds> <command> [args...]` at `scripts/portable-timeout.sh:33`.
- algorithmic_behavior: On source, `detect_timeout_impl` chooses the first available implementation in priority order: Homebrew `gtimeout`, GNU `timeout`, `python3`, `python`, then `none` (`scripts/portable-timeout.sh:10-27`). The result is captured once in `TIMEOUT_IMPL` (`scripts/portable-timeout.sh:29`). `run_with_timeout` shifts the first argument into `timeout_secs`, preserves the remaining command as a bash array, then dispatches by implementation (`scripts/portable-timeout.sh:33-70`). The GNU branches call `gtimeout "$timeout_secs" "${cmd[@]}"` or `timeout "$timeout_secs" "${cmd[@]}"` and return the tool exit status (`scripts/portable-timeout.sh:38-46`). The Python branch runs `subprocess.run(sys.argv[1:], timeout=<seconds>)`, preserves normal process return codes, maps timeout expiration to `124`, and maps wrapper exceptions to `1` with an stderr error (`scripts/portable-timeout.sh:47-62`). The `none` branch emits a warning and runs the command without a timeout (`scripts/portable-timeout.sh:64-69`).
- inputs_outputs_state: Inputs are the timeout seconds plus command argv. Outputs are the wrapped command’s stdout/stderr and exit status, except timeout expiration is normalized to exit `124` when an implementation supports time limits. State is only `TIMEOUT_IMPL`, exported for sourcing scripts and child environments (`scripts/portable-timeout.sh:73-76`). The helper does not create files or mutate repository state.
- gates_or_invariants: The detection gate rejects non-GNU `timeout` by requiring `timeout --version` to succeed (`scripts/portable-timeout.sh:13-19`), which avoids accidentally relying on incompatible BSD behavior. The command array invariant preserves argument boundaries through shell metacharacters and spaces (`scripts/portable-timeout.sh:36`, `scripts/portable-timeout.sh:40`, `scripts/portable-timeout.sh:44`, `scripts/portable-timeout.sh:61`). Timeout-capable paths should return `124` on timeout, matching GNU timeout semantics (`scripts/portable-timeout.sh:56-57`).
- dependencies_and_callers: Sourced by `hooks/loop-codex-stop-hook.sh` for git checks and Codex execution (`hooks/loop-codex-stop-hook.sh:50-55`, `hooks/loop-codex-stop-hook.sh:798-845`), by `hooks/loop-plan-file-validator.sh` for branch and plan-file git validation (`hooks/loop-plan-file-validator.sh:19-24`, `hooks/loop-plan-file-validator.sh:96-103`), and by `scripts/setup-rlcr-loop.sh` during loop setup and git validation (`scripts/setup-rlcr-loop.sh:23-29`). It is also sourced by robustness tests (`tests/robustness/test-timeout-robustness.sh:14-17`, `tests/robustness/test-git-operations-robustness.sh:17-19`). The stop hook uses it to enforce `GIT_TIMEOUT=30` around git status/branch checks and `CODEX_TIMEOUT` around `codex exec` (`hooks/loop-codex-stop-hook.sh:153`, `hooks/loop-codex-stop-hook.sh:326`, `hooks/loop-codex-stop-hook.sh:844-845`).
- edge_cases_or_failure_modes: If no timeout implementation exists, commands run unbounded after a warning (`scripts/portable-timeout.sh:64-67`), so callers relying on hard deadlines can hang. `TIMEOUT_IMPL` is computed only when sourced; later PATH changes do not affect dispatch unless the file is re-sourced (`scripts/portable-timeout.sh:29`). The Python implementation embeds `timeout_secs` directly in the `-c` script (`scripts/portable-timeout.sh:49-61`); callers are expected to pass numeric timeout values. If the command name is empty or missing, the underlying implementation determines the failure status. The script assumes bash arrays and is not POSIX-sh compatible despite using portable external strategies.
- validation_or_tests: `tests/robustness/test-timeout-robustness.sh` directly validates detection, accepted implementation names, quick-command success, timeout exit `124`, argv preservation, command exit-code preservation, `sh -c` pipelines, short and zero timeouts, large output, rapid cycles, nonexistent commands, empty commands, special characters, signal handling, subshell commands, and `TIMEOUT_IMPL` export behavior (`tests/robustness/test-timeout-robustness.sh:31-239`; additional export checks are referenced at `tests/robustness/test-timeout-robustness.sh:261-271`). Git robustness tests source it before exercising production git parsing (`tests/robustness/test-git-operations-robustness.sh:17-19`). I did not execute tests because this worker task is read-only research.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-058 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Prompt/block template fragment appended to the git-not-clean blocking message when the stop hook detects untracked files other than `.humanize*`. It guides the agent/operator toward classifying untracked files as ignore patterns versus commit candidates.
- algorithmic_behavior: The template is static markdown. It starts with a titled note (`prompt-template/block/git-not-clean-untracked.md:2`), states that untracked files may be build artifacts, test outputs, or runtime-generated files (`prompt-template/block/git-not-clean-untracked.md:3`), recommends `.gitignore` rather than committing for those classes (`prompt-template/block/git-not-clean-untracked.md:4`), lists examples such as build outputs, dependencies, IDE/editor files, logs, caches, and temporary files (`prompt-template/block/git-not-clean-untracked.md:5-8`), and ends with an instruction to review untracked files and add appropriate ignore patterns (`prompt-template/block/git-not-clean-untracked.md:10`).
- inputs_outputs_state: It has no placeholders and accepts no template variables. Its output is literal markdown loaded by `load_template`. It contributes to `SPECIAL_NOTES` inside the stop hook, which is then rendered into the higher-level `block/git-not-clean.md` reason (`hooks/loop-codex-stop-hook.sh:453-460`, `hooks/loop-codex-stop-hook.sh:467-475`). It does not store state.
- gates_or_invariants: The gate is in the caller: `GIT_STATUS_CACHED` must be non-empty, untracked rows are extracted with `grep '^??'`, `.humanize*` paths are filtered out, and this note is loaded only if other untracked files remain (`hooks/loop-codex-stop-hook.sh:437-456`). The hook then blocks exit for any git issues by returning JSON with `"decision": "block"` (`hooks/loop-codex-stop-hook.sh:477-485`). The invariant this fragment reinforces is that likely generated artifacts should not be blindly committed just to satisfy the clean-git gate.
- dependencies_and_callers: Called only from `hooks/loop-codex-stop-hook.sh` via `load_template "$TEMPLATE_DIR" "block/git-not-clean-untracked.md"` (`hooks/loop-codex-stop-hook.sh:456`). Template directory resolution and raw file loading come from `hooks/lib/template-loader.sh`, where `load_template` returns file contents if present and otherwise logs a warning plus empty output (`hooks/lib/template-loader.sh:33-48`). The stop hook has a fallback note, `"Review untracked files - add to .gitignore or commit them."`, if this template is missing or empty (`hooks/loop-codex-stop-hook.sh:456-459`).
- edge_cases_or_failure_modes: Because it is loaded with `load_template` rather than `load_and_render_safe`, there is no variable rendering and no caller-provided interpolation risk. If the file is missing, the caller uses the shorter fallback (`hooks/loop-codex-stop-hook.sh:456-459`). Since `.humanize*` untracked paths are filtered separately, this block should not be shown for local RLCR state directories alone (`hooks/loop-codex-stop-hook.sh:444-455`). The template gives generic advice and cannot distinguish generated artifacts from source files that should be committed; the decision remains manual.
- validation_or_tests: Template reference coverage is enforced by `tests/test-template-references.sh`, which scans hook/template-loader calls for template paths and fails missing template files (`tests/test-template-references.sh:83-107`). There is no direct content assertion for this specific fragment in the snippets inspected, but it is covered by reference-existence scanning. I did not execute tests because this worker task is read-only research.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-088 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Codex review prompt section that assigns goal-tracker mutation responsibility to Codex/the review owner after Round 0, establishing review-time rules for accepting or rejecting Claude-originated “Goal Tracker Update Request” changes.
- algorithmic_behavior: The section title defines the prompt block as “Goal Tracker Update Requests” and marks it as the reviewer’s responsibility (`prompt-template/codex/goal-tracker-update-section.md:1`). It states that Claude cannot directly modify `goal-tracker.md` after Round 0 and makes the reviewer responsible when Claude’s summary contains a matching request (`prompt-template/codex/goal-tracker-update-section.md:3`). The algorithmic instruction sequence is: evaluate whether the request is justified and serves the Ultimate Goal; if approved, update `@{{GOAL_TRACKER_FILE}}`; move tasks between Active/Completed/Deferred; add Plan Evolution Log entries with round number and justification; add discovered issues to Open Issues; never modify the immutable Ultimate Goal and Acceptance Criteria section; if rejected, explain why in the review (`prompt-template/codex/goal-tracker-update-section.md:5-11`). It then enumerates common request classes: task completion, new issues, plan changes, and strongly justified deferrals (`prompt-template/codex/goal-tracker-update-section.md:13-17`).
- inputs_outputs_state: Input is the `GOAL_TRACKER_FILE` template variable. Rendering substitutes it into the update target path at `prompt-template/codex/goal-tracker-update-section.md:6`. Output is markdown embedded into either full-alignment or regular Codex review prompts (`hooks/loop-codex-stop-hook.sh:680-684`, `hooks/loop-codex-stop-hook.sh:699-708`, `hooks/loop-codex-stop-hook.sh:710-719`). It does not itself update state; it instructs the review actor to update the goal tracker when justified.
- gates_or_invariants: The principal invariant is that the immutable section, specifically Ultimate Goal and Acceptance Criteria, must never be modified (`prompt-template/codex/goal-tracker-update-section.md:10`). Approval is gated by justification and alignment with the Ultimate Goal (`prompt-template/codex/goal-tracker-update-section.md:5`). Deferrals require strong justification (`prompt-template/codex/goal-tracker-update-section.md:17`). Rejections must be explained in the review (`prompt-template/codex/goal-tracker-update-section.md:11`), making rejected plan mutations auditable instead of silent.
- dependencies_and_callers: The stop hook renders this block with `load_and_render_safe`, passing `GOAL_TRACKER_FILE=$GOAL_TRACKER_FILE` and using a short fallback if the template is missing (`hooks/loop-codex-stop-hook.sh:680-684`). The rendered result is passed as `GOAL_TRACKER_UPDATE_SECTION` into both `codex/full-alignment-review.md` and `codex/regular-review.md` prompt rendering (`hooks/loop-codex-stop-hook.sh:721-734`, `hooks/loop-codex-stop-hook.sh:736-750`). Rendering behavior comes from `hooks/lib/template-loader.sh`: templates use `{{VARIABLE_NAME}}` placeholders, single-pass substitution, and preserve missing placeholders as-is (`hooks/lib/template-loader.sh:7-14`, `hooks/lib/template-loader.sh:50-58`). Safe loading falls back when the template is missing or empty (`hooks/lib/template-loader.sh:167-186`).
- edge_cases_or_failure_modes: If the template is missing or empty, the fallback only says to apply requested changes to the goal tracker and omits the stronger evaluation, immutable-section, rejection, and deferral rules (`hooks/loop-codex-stop-hook.sh:680-684`). If `GOAL_TRACKER_FILE` is missing or malformed in hook state, the placeholder may remain or render to an empty target depending on caller state; template rendering itself does not validate path existence. The prompt delegates judgment to the reviewer, so bad reviewer behavior could still approve unjustified changes despite the stated gate.
- validation_or_tests: `tests/test-templates-comprehensive.sh` directly renders this real template with `GOAL_TRACKER_FILE=.humanize/rlcr/20240101/goal-tracker.md` and checks that output contains “Goal Tracker Update Requests” and the rendered file path (`tests/test-templates-comprehensive.sh:531-539`). Reference-existence scanning also covers template references used by hooks (`tests/test-template-references.sh:83-107`). I did not execute tests because this worker task is read-only research.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 section headings present, one for each assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`