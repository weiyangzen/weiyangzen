# agent_22 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-022 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse validator for Claude `Edit` operations during the RLCR loop. It is registered as the Edit hook in `hooks/hooks.json:24-30` and protects loop-owned files from direct mutation.
- algorithmic_behavior: The hook reads JSON from stdin, exits immediately unless `.tool_name` is `Edit`, then lowercases `.tool_input.file_path` for pattern checks (`hooks/loop-edit-validator.sh:22-30`). It blocks `round-*-todos.md` unless the path is allowlisted in the active loop (`:36-43`), blocks all `round-*-prompt.md` edits (`:46-49`), ignores non-`.humanize/rlcr/` paths after those global round-file checks (`:55-57`), finds the active loop and parses either `state.md` or `finalize-state.md` (`:63-82`), blocks state/finalize-state edits (`:89-97`), blocks `.humanize/rlcr/**/plan.md` backup edits (`:103-110`), blocks direct `goal-tracker.md` edits after round 0 (`:117-120`), and rejects summary edits for the wrong round unless allowlisted (`:127-151`).
- inputs_outputs_state: Inputs are hook JSON on stdin, `CLAUDE_PROJECT_DIR` or `pwd` as project root, `.humanize/rlcr/<active>/state.md` or `finalize-state.md`, and templates/messages supplied through `loop-common.sh`. Outputs are process exit codes: `0` allow, `2` policy block with a stderr message, `1` fail-closed malformed state. It does not mutate state; it preserves state-machine integrity by preventing Edit-driven mutation of state files, plan backups, and post-round-0 goal tracker content.
- gates_or_invariants: Non-Edit tools pass through. Todos and prompt file patterns are guarded before `.humanize/rlcr/` scoping, so same-named round prompt/todos files outside the loop are still constrained. Active loop state must strict-parse before sensitive loop files can be edited. `finalize-state.md` is checked before `state.md` because the latter pattern also matches the former (`:87-97`). Goal tracker direct edits are allowed only when `current_round` is not greater than zero; after that, updates must flow through a summary request.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`:14-16`) for `to_lower`, round-file detection, active-loop discovery, allowlisting, strict state parsing, path predicates, and block-message renderers. Shared behavior is coordinated with `loop-write-validator.sh` and `loop-bash-validator.sh`, which enforce the same goal tracker and state-file policy for other tools. The sanctioned replacement path is the Goal Tracker Update Request prompt appended by `hooks/loop-codex-stop-hook.sh:1108-1113`.
- edge_cases_or_failure_modes: Unlike read/write/bash validators, this file directly uses `jq` and does not call `validate_hook_input`, `require_tool_input_field`, or `is_deeply_nested`; malformed JSON exits via `set -e`, but missing `tool_input.file_path` for an Edit can fall through as an allowed empty path. If no active loop exists, `.humanize/rlcr/` edits are allowed after the early todos/prompt gates (`:67-69`). Path checks are string/regex based, not canonicalized. A malformed active state blocks sensitive operations (`:77-80`). Negative numeric rounds would strict-parse in the common parser and would not trigger the `CURRENT_ROUND > 0` goal tracker block.
- validation_or_tests: Existing inspected coverage includes allowlist behavior for Edit in `tests/test-allowlist-validators.sh:189-223`, finalize-state Edit blocking in `tests/test-finalize-phase.sh:310-319`, and plan backup Edit blocking around `tests/test-plan-file-hooks.sh:212-219`. I did not execute the test suite in this read-only research pass.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-052 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared RLCR hook library. It centralizes constants, JSON validation, active-loop/state parsing, path classification, block-message rendering, cancellation authorization, `.humanize` git-add protection, shell modification detection, and loop terminal-state renaming.
- algorithmic_behavior: Constants define state frontmatter fields, review markers, and terminal reasons (`hooks/lib/loop-common.sh:15-40`). JSON helpers reject null bytes, invalid UTF-8 when `iconv` is present, invalid JSON, missing `tool_name`, missing required tool-input fields, and over-deep JSON (`:55-129`). Template loading is initialized through `template-loader.sh` (`:135-145`). Active loop discovery chooses only the newest loop dir containing `state.md` or `finalize-state.md` (`:152-170`). State helpers parse current round and strict/tolerant frontmatter (`:175-296`). Round-file, allowlist, goal/state/finalize path, and message helpers supply validator policy (`:298-429`). Cancel authorization validates a narrow `mv state.md|finalize-state.md cancel-state.md` command with a signal file and injection checks (`:455-614`). Git-add and Bash modification detectors protect `.humanize` and loop files (`:644-810`). `end_loop` renames `state.md` to a reason-specific terminal file (`:833-859`).
- inputs_outputs_state: Inputs include raw hook JSON, state file paths and YAML-like frontmatter, file paths, lowercase shell commands, active loop directories, template paths, and the current git working directory. Outputs are return codes, stderr block messages, exported globals such as `VALIDATED_TOOL_NAME`, `VALIDATED_TOOL_INPUT`, and `STATE_*`, stdout values such as active-loop paths or round numbers, and the `end_loop` filesystem transition from `state.md` to `<reason>-state.md`.
- gates_or_invariants: Strict state parsing requires at least two frontmatter separators and numeric `current_round`/`max_iterations` (`:248-290`). The allowlist is exact-path-only and limited to `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` inside the active loop (`:327-345`). Cancellation requires a `.cancel-requested` file, rejects command substitution, shell chaining, extra args, hidden variables, mixed quotes, and symlinked source state files (`:459-613`). Git-add protection blocks direct `.humanize` adds, force broad adds, all-scope adds when `.humanize` exists, and broad adds when `.humanize` is not ignored (`:644-740`). `end_loop` accepts only `complete`, `cancel`, `maxiter`, `stop`, or `unexpected` (`:833-859`).
- dependencies_and_callers: Sourced by `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-plan-file-validator.sh`, and `loop-codex-stop-hook.sh` per repository search. It depends on `jq`, `sed`, `grep`, `tr`, `wc`, optional `iconv`, `git check-ignore`, and `hooks/lib/template-loader.sh`; the template loader itself resolves `prompt-template` from the plugin root and provides fallback-safe rendering (`hooks/lib/template-loader.sh:24-47`, `:170-203`).
- edge_cases_or_failure_modes: `find_active_loop` intentionally ignores older loop dirs even if they still contain state, preventing stale loop revival but making timestamp ordering authoritative. State parsing is grep/sed frontmatter parsing, not full YAML; quoted handling is partial and numeric validation allows negative values. UTF-8 rejection depends partly on `iconv` availability. `git_adds_humanize` evaluates some broad-scope cases against the current directory’s `.humanize`, so commands with `git -C` can be harder to reason about unless they directly reference `.humanize` or use force broad scope. `command_modifies_file` is regex-based shell analysis, so it is a guardrail with likely false-positive/false-negative boundaries rather than a complete shell parser.
- validation_or_tests: Existing inspected coverage includes hook input robustness in `tests/robustness/test-hook-input-robustness.sh:35-217`, strict/tolerant state parser behavior in `tests/robustness/test-state-file-robustness.sh:147-220`, allowlist checks in `tests/test-allowlist-validators.sh:64-124`, Bash modification pattern tests in `tests/test-bash-validator-patterns.sh`, cancel authorization tests in `tests/test-cancel-signal-file.sh` and `tests/robustness/test-cancel-security-robustness.sh`, `.humanize` git-add tests in `tests/test-humanize-escape.sh`, and template reference checks in `tests/test-template-references.sh:149-200`. I did not execute the test suite in this read-only research pass.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-082 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Static Claude prompt fragment that defines the sanctioned post-round-0 Goal Tracker update contract. It is core to the RLCR transition from “Claude may not directly edit goal-tracker.md” to “Claude may request Codex to update it.”
- algorithmic_behavior: The template tells Claude to include a `Goal Tracker Update Request` section in its summary only when updates are needed (`prompt-template/claude/goal-tracker-update-request.md:2-4`). It specifies two structured subsections: requested changes with examples for completion, open issues, plan evolution, and deferral (`:6-10`), plus a justification tied to the Ultimate Goal (`:12-13`). It ends by assigning review/update authority to Codex (`:16`).
- inputs_outputs_state: It has no variable placeholders and consumes no runtime inputs. Its output is Markdown appended to the next-round prompt by `hooks/loop-codex-stop-hook.sh:1108-1113`, with a fallback sentence if the template cannot be loaded. It does not directly mutate state; it creates the summary evidence surface Codex can later inspect and apply to `goal-tracker.md`.
- gates_or_invariants: The invariant is separation of authority: Claude can request goal tracker changes, but Codex decides whether the request is justified. This matches `prompt-template/claude/next-round-prompt.md:22-31`, which marks the goal tracker read-only after round 0, and `prompt-template/codex/goal-tracker-update-section.md:1-17`, which makes Codex responsible for evaluating, applying, or rejecting requested changes while preserving immutable goal/acceptance-criteria content.
- dependencies_and_callers: Loaded through `load_template` from the template loader and appended by the stop hook after the next-round footer and optional push note (`hooks/loop-codex-stop-hook.sh:1093-1113`). It coordinates with the Edit/Write/Bash validators’ post-round-0 goal-tracker blocks, especially `hooks/loop-edit-validator.sh:117-120`, by preserving an update path without permitting direct writes.
- edge_cases_or_failure_modes: Because the contract is prose Markdown, downstream detection depends on Claude using the expected heading and Codex honoring the request section. If this template is missing, the stop hook falls back to a weaker one-line instruction (`hooks/loop-codex-stop-hook.sh:1109-1112`). The template does not define a machine-parseable schema or required evidence fields beyond examples, so ambiguous or under-justified requests may be skipped or rejected by Codex.
- validation_or_tests: Existing inspected references show the stop hook loads this template directly, and broader template loading/reference checks cover template existence and safe rendering patterns. I did not find a dedicated semantic test that asserts this exact template is appended with the full requested-change/justification structure, and I did not execute tests in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 Item Evidence headings present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`