# agent_19 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-019 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Claude hook manifest for the Humanize RLCR state machine. It wires Claude lifecycle/tool events to repository validators and the Codex review stop hook. The file is configuration, but it is core because it defines which algorithmic gates run at each event: `UserPromptSubmit` plan validation, `PreToolUse` path/command validators, and `Stop` review/round transition handling.
- algorithmic_behavior: `hooks/hooks.json:4-13` runs `hooks/loop-plan-file-validator.sh` on every prompt submit, which checks active loop state schema, branch consistency, and plan-file tracking rules. `hooks/hooks.json:14-51` routes `Write`, `Edit`, `Read`, and `Bash` tool usage to separate validators. `hooks/hooks.json:52-59` runs `hooks/loop-codex-stop-hook.sh` with a 7200 second timeout, making attempted session exit the point where summaries, git state, Codex review, finalization, and next-round prompt generation are enforced.
- inputs_outputs_state: Input is Claude’s hook event JSON plus environment such as `CLAUDE_PLUGIN_ROOT` and `CLAUDE_PROJECT_DIR`. The manifest outputs command invocations, while the invoked scripts output allow/block decisions or stderr messages. Downstream state lives under `.humanize/rlcr/<timestamp>/`: `state.md`, `finalize-state.md`, `round-N-summary.md`, `round-N-review-prompt.md`, `round-N-review-result.md`, and generated next-round prompts. Shared parsing is in `hooks/lib/loop-common.sh`: hook JSON validation at `46-92`, active-loop selection at `147-170`, strict state parsing at `231-296`, and round-file parsing at `303-321`.
- gates_or_invariants: The manifest enforces event separation: prompt submit validates plan/branch state, tool validators protect RLCR files before mutation or access, and stop-time validation performs expensive completion checks. `loop-write-validator.sh:53-66` blocks todos and prompt writes; `200-217` blocks writing a summary for a non-current round unless allowlisted. `loop-edit-validator.sh:36-49` blocks todos and prompt edits; `127-150` applies the same wrong-round summary gate for edits. `loop-read-validator.sh:52-59` blocks todos reads; `119-148` blocks reading round files from wrong locations or old rounds. `loop-bash-validator.sh:82-91` blocks `git push` unless configured, `117-134` blocks state-file modification, `314-319` protects plan backups, and `343-371` blocks shell bypasses for prompt/summary/todos files.
- dependencies_and_callers: Called by Claude Code/plugin hook loading. It depends on `${CLAUDE_PLUGIN_ROOT}` resolving to the plugin root. The referenced scripts depend on `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/portable-timeout.sh`, `jq`, `git`, and for stop-time todo checks `hooks/check-todos-from-transcript.py`. The stop hook uses Codex CLI: it builds review prompts at `hooks/loop-codex-stop-hook.sh:671-751`, checks for `codex` at `758-778`, and runs the review command with selected model/effort at `818-824`.
- edge_cases_or_failure_modes: If `${CLAUDE_PLUGIN_ROOT}` is unset or wrong, no referenced validator path resolves. Hook matching is exact by tool name, so only the listed `Write`, `Edit`, `Read`, and `Bash` tool events are covered by this manifest. The stop hook has a long but finite timeout; Codex hangs beyond 7200 seconds should be killed by the hook runtime. Strict state parsing blocks validators on malformed state in pre-tool hooks, while the stop hook allows exit for malformed state at `hooks/loop-codex-stop-hook.sh:88-91`, then later has additional unexpected-state termination paths for invalid model/round fields at `105-123`.
- validation_or_tests: `jq . hooks/hooks.json` parsed successfully. Static shell syntax check for the assigned comprehensive template test also passed with `bash -n tests/test-templates-comprehensive.sh`. Related behavior is covered indirectly by hook and robustness tests such as `tests/test-template-references.sh`, `tests/test-finalize-phase.sh`, and `tests/robustness/test-hook-input-robustness.sh`.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-049 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Executable specification for the prompt-template subsystem used by RLCR validators and review prompt generation. It asserts that every template can load, placeholder syntax is valid, rendering is single-pass and robust across edge cases, fallback rendering works, and real templates used by validators render with expected substituted values.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh` at `tests/test-templates-comprehensive.sh:15-17`, computes `TEMPLATE_DIR` at `59`, then runs seven validation sections. It verifies required template subdirectories `block`, `codex`, and `claude` at `64-80`; loads every `*.md` template with `find ... -print0 | sort -z` at `90-107`; checks placeholder syntax with regex scans at `118-194`; tests malformed placeholder detection at `199-259`; checks renderer edge cases at `261-439`; checks safe fallback behavior at `440-490`; renders real templates including `block/wrong-round-number.md` at `492-540`; then stress-renders rapid calls and all templates with dummy placeholder assignments at `542-606`.
- inputs_outputs_state: Inputs are the repository root, `prompt-template/**/*.md`, and functions from `hooks/lib/template-loader.sh`. Outputs are colored PASS/FAIL/WARN lines and final counters. The script accumulates `TESTS_PASSED`, `TESTS_FAILED`, and `WARNINGS` at `26-29`, increments via helper functions at `30-57`, and exits `0` only when `TESTS_FAILED` is zero at `619-625`. It creates temporary directories for malformed/incomplete directory checks at `204-206` and `482-490`, intended to be outside the repo.
- gates_or_invariants: Template directory must exist and contain the three expected subdirectories. Template files must not be empty after whitespace removal. Placeholder syntax is constrained to uppercase identifiers in double braces, with checks for extra closing braces, extra opening braces, wrong bracket types, possible unclosed placeholders, spaces inside placeholders, and lowercase placeholders at `128-175`. Rendering must preserve empty lines, handle regex-like characters, quotes, markdown, long values, repeated placeholders, adjacent placeholders, and multiline values. `load_and_render_safe` must use fallback only for missing or empty templates and must prefer the real template when available.
- dependencies_and_callers: It directly tests `hooks/lib/template-loader.sh`. Relevant implementation points are `get_template_dir` at `template-loader.sh:24-31`, `load_template` at `36-48`, `render_template` at `50-132`, `load_and_render_safe` at `167-203`, and `validate_template_dir` at `205-222`. The template loader’s stated contract is single-pass substitution, keeping missing variables unchanged and preventing placeholder injection in values at `template-loader.sh:7-14` and `54-57`.
- edge_cases_or_failure_modes: The script uses `set -uo pipefail` but not `set -e`, so it continues after individual assertion failures and reports aggregate counts. In the read-only branch sandbox, full execution failed because `mktemp` and Bash here-string temp-file creation were denied: `mktemp: ... Operation not permitted` and `template-loader.sh: line 129: cannot create temp file for here document`. That caused rendering-related failures unrelated to template content. The run also exposed a zero-substitution path: `load_and_render_safe "$TEMPLATE_DIR" "block/git-push.md" "$fallback"` at `458` can call `render_template` with no variable assignments, and in this shell produced `env_vars[@]: unbound variable` under `set -u`.
- validation_or_tests: `bash -n tests/test-templates-comprehensive.sh` passed. Running `bash tests/test-templates-comprehensive.sh` in this read-only environment exited `1` with final summary `Passed: 85`, `Failed: 45`, `Warnings: 0`; failures were dominated by sandbox-denied temporary file creation and here-string handling. Before the sandbox-induced render failures, the script confirmed 36 templates found, 0 load failures, and syntax-valid results for all scanned templates.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-079 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: User-facing block template for the RLCR round-number invariant on summary file writes/edits. It tells the agent that it targeted `round-<agent-round>-<file-type>.md` while the active state says a different current round, and directs it to the correct path.
- algorithmic_behavior: The template has title `Wrong Round Number` at `prompt-template/block/wrong-round-number.md:1`, renders the attempted action and target filename at `3`, renders the authoritative current round at `3`, renders `CORRECT_PATH` at `5`, and states the behavioral rule at `7`: do not increment the round number manually. It has no logic itself; it is a deterministic rendering surface for validator decisions.
- inputs_outputs_state: Inputs are five placeholders: `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`. Output is markdown sent to stderr by pre-tool validators. State used to supply values comes from the active loop state file via `parse_state_file_strict`, then `STATE_CURRENT_ROUND`. The attempted round comes from `extract_round_number`, which extracts digits from `round-N-summary.md`, `round-N-prompt.md`, or `round-N-todos.md` patterns in `hooks/lib/loop-common.sh:312-321`.
- gates_or_invariants: The template supports the invariant that summary files must match the active `current_round` and must live in the active loop directory. `hooks/loop-write-validator.sh:200-217` renders it when a `Write` targets a summary file whose extracted round differs from current state and the file is not allowlisted. `hooks/loop-edit-validator.sh:127-150` renders it for the same condition on `Edit`. The separate read path uses `prompt-template/block/wrong-round-file.md`, so this template is specifically the write/edit round correction surface.
- dependencies_and_callers: Loaded through `load_and_render_safe` in the write and edit validators. It depends on template syntax handled by `hooks/lib/template-loader.sh`, which replaces `{{VAR}}` placeholders in a single pass and leaves missing variables unchanged. The comprehensive template test explicitly renders this file at `tests/test-templates-comprehensive.sh:498-510` and asserts the title, attempted `round-3-summary`, current round `5`, and supplied correct path appear.
- edge_cases_or_failure_modes: If the template is missing or empty, both validators have inline fallbacks at `loop-write-validator.sh:205-209` and `loop-edit-validator.sh:139-143`. If a placeholder value is omitted, the template loader keeps the unresolved placeholder literal, which makes the block message visibly degraded but still blocks. The round extractor only matches nonnegative numeric round names, so unusual filenames do not reach this template unless they still match the standard round-file pattern. Allowlisted early files in `hooks/lib/loop-common.sh:331-336` bypass the wrong-round block.
- validation_or_tests: Static inspection confirms all five placeholders follow uppercase `{{VAR}}` syntax. The comprehensive template script’s syntax scan reported `block/wrong-round-number.md` as valid with five placeholders, and its integration section includes a real render test for this exact template. Full render execution could not be trusted in this read-only sandbox because the template loader’s here-string path was denied temporary-file creation.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 unique item headings present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`