# agent_03 fix-too-strict-rule-and-enhance-plan-gen 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 4
- source_commit: `f1c0c6fb48a4eed20dacbe94c5b22992e5f07418`

## Item Evidence

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: Slash-command prompt surface for the plugin’s core workflows. The directory is shallow but was inspected recursively; it contains `commands/gen-plan.md`, `commands/start-rlcr-loop.md`, and `commands/cancel-rlcr-loop.md`. These files do not implement shell logic directly; they define allowed tools, user-facing command protocol, phase sequencing, and delegate stateful behavior to `scripts/*` and hook validators.
- algorithmic_behavior: `commands/gen-plan.md` defines a six-phase draft-to-plan transformation: validate `--input/--output` via `scripts/validate-gen-plan-io.sh`, run a lenient repository relevance gate through `draft-relevance-checker`, analyze clarity/consistency/completeness/functionality, resolve issues and numeric thresholds with `AskUserQuestion`, generate a structured plan with AC-X criteria and positive/negative tests, then write the output plan (`commands/gen-plan.md:30-46`, `49-72`, `75-136`, `139-244`). `commands/start-rlcr-loop.md` is a thin wrapper around `scripts/setup-rlcr-loop.sh`; it starts the RLCR loop, documents the goal-tracker immutable/mutable split, and defines loop termination by max iterations, Codex `COMPLETE`, or cancellation (`commands/start-rlcr-loop.md:10-23`, `24-42`, `51-55`). `commands/cancel-rlcr-loop.md` is a thin wrapper around `scripts/cancel-rlcr-loop.sh`; it interprets sentinel first-line outputs and prompts for explicit confirmation during Finalize Phase (`commands/cancel-rlcr-loop.md:11-21`, `23-35`).
- inputs_outputs_state: `gen-plan` inputs are a draft markdown file and a not-yet-existing output plan path; output is a new plan file whose schema includes goal, acceptance criteria, path boundaries, feasibility hints, and dependencies. Its validation script returns fixed exit codes 0-6 for success, missing/empty input, missing output directory, existing output, permission failure, or invalid args (`scripts/validate-gen-plan-io.sh:4-11`, `78-130`). `start-rlcr-loop` input is one relative plan path plus flags such as `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, and `--push-every-round`; output state is `.humanize/rlcr/<timestamp>/state.md`, `plan.md` backup, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:104-180`, `490-521`, `527-618`, `624-684`). `cancel-rlcr-loop` outputs `.cancel-requested` and renames `state.md` or `finalize-state.md` to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:85-99`, `117-138`).
- gates_or_invariants: `gen-plan` must not proceed unless IO validation succeeds, the draft is semantically relevant, major ambiguities are resolved, quantitative metrics are classified as hard requirements or optimization directions, and generated ACs include positive and negative tests (`commands/gen-plan.md:38-46`, `66-72`, `114-136`, `225-231`). `start-rlcr-loop` requires a git repo with at least one commit, rejects absolute paths, spaces, shell metacharacters, symlinks, paths outside the project, submodule paths, invalid tracking state, too-short plans, missing Codex CLI, YAML-unsafe branch names, and unsafe Codex model/effort strings (`scripts/setup-rlcr-loop.sh:211-248`, `253-328`, `334-379`, `385-446`, `458-484`). `cancel-rlcr-loop` protects Finalize Phase unless `--force` is used (`scripts/cancel-rlcr-loop.sh:117-127`).
- dependencies_and_callers: Command frontmatter restricts execution tools: `gen-plan` allows only the IO validator plus read/search/task/write/user-question tools (`commands/gen-plan.md:1-12`); `start-rlcr-loop` allows only setup script execution (`commands/start-rlcr-loop.md:1-5`); `cancel-rlcr-loop` allows the cancel script, forced cancel script, and `AskUserQuestion` (`commands/cancel-rlcr-loop.md:1-4`). Runtime coordination continues through `hooks/hooks.json`, which wires plan validation on `UserPromptSubmit`, file/command validators on `PreToolUse`, and Codex review on `Stop` (`hooks/hooks.json:4-63`).
- edge_cases_or_failure_modes: `gen-plan` may stop on invalid args, missing files, empty input, existing output target, or non-relevant drafts. `start-rlcr-loop` fails closed on git timeouts, plan path traversal/symlink risks, branch-name YAML injection risk, Codex unavailability, and insufficient plan content. `cancel-rlcr-loop` distinguishes no loop, loop directory with no active state file, normal cancellation, and finalize cancellation requiring confirmation (`scripts/cancel-rlcr-loop.sh:75-99`, `117-152`).
- validation_or_tests: `tests/test-gen-plan.sh` validates `gen-plan.md` frontmatter, agent metadata, English-only content, and all `validate-gen-plan-io.sh` exit-code cases (`tests/test-gen-plan.sh:54-180`, `426-540`). `tests/robustness/test-path-validation-robustness.sh` exercises `setup-rlcr-loop.sh` path acceptance/rejection around relative paths, spaces, shell metacharacters, and symlink/path traversal concerns (`tests/robustness/test-path-validation-robustness.sh:55-89`, `98-240`).
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-033 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for the branch’s “too strict rule” behavior: selected loop-generated todos and historical summaries must be allowlisted, while stale, wrong-directory, old-loop, and same-basename bypasses remain blocked.
- algorithmic_behavior: The test creates a temporary git repo and active loop directory with `state.md` declaring `current_round: 5`, `max_iterations: 42`, `plan_file`, `plan_tracked`, and `start_branch` (`tests/test-allowlist-validators.sh:29-62`). It first tests `is_allowlisted_file()` directly: active-loop `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` are allowed; `round-3-todos.md`, `round-2-summary.md`, and an otherwise valid filename in `/other/path` are blocked (`tests/test-allowlist-validators.sh:70-124`). It then pipes JSON hook inputs into the real Write, Edit, Read, and Bash validators to verify production hook behavior (`tests/test-allowlist-validators.sh:127-381`).
- inputs_outputs_state: Inputs are synthesized Claude hook JSON blobs such as `{"tool_name":"Write","tool_input":{"file_path":"..."}}` and Bash command strings that redirect or tee into loop files. Outputs are validator exit codes and stderr/stdout text checked by the test harness; accepted operations must exit `0`, policy blocks must exit `2`, and the script exits with `$TESTS_FAILED` (`tests/test-allowlist-validators.sh:135-183`, `191-226`, `234-282`, `290-381`, `383-391`).
- gates_or_invariants: The underlying allowlist is exact path based, not basename based: `is_allowlisted_file()` only returns true for four filenames under the active loop directory (`hooks/lib/loop-common.sh:324-345`). Read/Write/Edit allowlisted files may bypass the usual wrong-round/todos restrictions, but only when `find_active_loop()` resolves that active loop. Bash modifications to todos are stricter: they must target the active loop full path and only `round-[12]-todos.md`; generic relative paths, `/tmp`, old loop dirs, and matching timestamp under another root are blocked (`hooks/loop-bash-validator.sh:363-370`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` for `is_allowlisted_file()` (`tests/test-allowlist-validators.sh:15-17`). Invokes `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh`, which are registered by `hooks/hooks.json` under `PreToolUse` for their matching tools (`hooks/hooks.json:14-50`).
- edge_cases_or_failure_modes: Captures the intended relaxation without opening broad bypasses: `round-3-todos.md` remains blocked, `round-2-summary.md` and `round-3-summary.md` remain stale-round blocks, wrong directories remain blocked, old loop directories remain blocked, bare `round-1-todos.md` remains blocked for Bash, and a path under `/tmp/.humanize/rlcr/<same-basename>` remains blocked (`tests/test-allowlist-validators.sh:102-124`, `159-183`, `258-282`, `314-381`).
- validation_or_tests: The file itself is the validation harness. It includes 25 numbered checks across helper-level, Write, Edit, Read, and Bash validator behavior, and fails the process with a nonzero count if any assertion fails (`tests/test-allowlist-validators.sh:64-391`).
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-063 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Policy template for the goal-tracker write gate after Round 0. It converts a blocked direct modification attempt into a structured “Goal Tracker Update Request” workflow, preserving Codex as the only post-Round-0 writer of `goal-tracker.md`.
- algorithmic_behavior: The rendered message states that after Round 0 only Codex can modify the Goal Tracker, blocks direct `Write`, `Edit`, or Bash changes to `goal-tracker.md`, and instructs the worker to place requested changes and justification in the current round summary (`prompt-template/block/goal-tracker-modification.md:1-23`). The template is parameterized by `{{CURRENT_ROUND}}` and `{{SUMMARY_FILE}}` (`prompt-template/block/goal-tracker-modification.md:1`, `9-10`).
- inputs_outputs_state: Input variables are supplied by `goal_tracker_blocked_message()` in `hooks/lib/loop-common.sh`; output is markdown sent to stderr by pre-tool validators as the block reason (`hooks/lib/loop-common.sh:812-824`). The intended state transition is indirect: user/Claude cannot mutate `goal-tracker.md` after Round 0, but can write a request into `round-N-summary.md`; the stop hook later includes a Codex review section that tells Codex to apply justified Goal Tracker Update Requests (`hooks/loop-codex-stop-hook.sh:680-684`).
- gates_or_invariants: Direct `Write` to goal tracker is blocked when `CURRENT_ROUND > 0` (`hooks/loop-write-validator.sh:160-168`); direct `Edit` is blocked under the same condition (`hooks/loop-edit-validator.sh:113-120`); Bash modification patterns against `goal-tracker.md` are blocked in all rounds, with this template used after Round 0 and a separate Bash-specific guidance used in Round 0 (`hooks/loop-bash-validator.sh:321-335`). This enforces the command-level invariant described in `commands/start-rlcr-loop.md`: immutable goal and AC sections are initialized in Round 0 and later changes must be justified through the mutable request path (`commands/start-rlcr-loop.md:24-42`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` through `load_and_render_safe()` and is referenced by `tests/test-template-references.sh` as a required template (`tests/test-template-references.sh:158`). Callers are `goal_tracker_blocked_message()` and the Write/Edit/Bash validators. It coordinates with `prompt-template/codex/goal-tracker-update-section.md`, which gives Codex responsibility for applying summary-file requests.
- edge_cases_or_failure_modes: If the template is missing or invalid, `goal_tracker_blocked_message()` falls back to a shorter inline block message (`hooks/lib/loop-common.sh:817-823`). The template is not used for Round 0 Bash edits; those are redirected to a different message telling the agent to use Write/Edit. If the summary file is never written, the stop hook blocks before review (`hooks/loop-codex-stop-hook.sh:521-557`), so requested goal-tracker updates cannot be silently lost through loop exit.
- validation_or_tests: Covered indirectly by template reference checks, comprehensive template rendering tests, and goal-tracker robustness tests around monitor parsing. The strongest direct runtime coverage is through the Write/Edit/Bash validator tests that assert goal tracker modifications are blocked after Round 0 and route through summary-request semantics.
- skip_candidate: `no`

### FIX_TOO_STRICT_RULE_AND_ENHANCE_PLAN_GEN-HZ-093 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for hook input parsing and monitor resilience. It covers AC-7 JSON/hook input hardening and AC-6 monitor/log edge cases using production validators and production monitor helper functions.
- algorithmic_behavior: The first block pipes JSON into real validators: valid Read, Write, and Bash hook inputs pass; invalid JSON, `{}`, and missing `tool_input.file_path` are rejected with non-signal nonzero exits (`tests/robustness/test-hook-input-robustness.sh:35-115`). It then checks a 10KB Bash command, special shell characters, Unicode paths, unknown tool passthrough, 50-level nesting rejection, invalid UTF-8 rejection, and null-byte graceful handling (`tests/robustness/test-hook-input-robustness.sh:117-235`). Later blocks exercise monitor edge handling, command modification detection, direct production helper calls, and monitor subprocess behavior under missing session, narrow terminal, and ANSI log conditions (`tests/robustness/test-hook-input-robustness.sh:245-370`, `381-433`, `444-664`).
- inputs_outputs_state: Inputs include JSON hook payloads, binary/non-UTF8 byte strings, generated log files, temporary goal-tracker content, temporary git repos, fake `$HOME`/`XDG_CACHE_HOME`, and shimmed `tput`/`clear` functions. Outputs are pass/fail counters via `tests/test-helpers.sh`, validator exit codes, pipe-delimited helper outputs, and monitor wrapper exit codes; the script exits through `print_test_summary` (`tests/robustness/test-hook-input-robustness.sh:12-19`, `666-671`; `tests/test-helpers.sh:58-78`).
- gates_or_invariants: Production `validate_hook_input()` must reject null bytes, invalid UTF-8 when `iconv` is available, invalid JSON syntax, and missing `tool_name`; `require_tool_input_field()` must reject missing required tool fields; `is_deeply_nested()` must reject structures above depth 30 (`hooks/lib/loop-common.sh:55-129`). Read/Write/Bash validators call those gates before enforcing tool-specific policy (`hooks/loop-read-validator.sh:22-43`, `hooks/loop-write-validator.sh:23-44`, `hooks/loop-bash-validator.sh:22-43`). Unknown tool names pass through because validators are matcher-specific and should not block unrelated tool calls (`tests/robustness/test-hook-input-robustness.sh:163-175`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` (`tests/robustness/test-hook-input-robustness.sh:14-18`). Invokes `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-bash-validator.sh`, `command_modifies_file()`, and `scripts/humanize.sh` functions `humanize_parse_goal_tracker`, `humanize_parse_git_status`, `humanize_detect_git_state`, and `_humanize_monitor_codex` (`tests/robustness/test-hook-input-robustness.sh:381-433`). These validators and monitor behavior are wired into runtime by `hooks/hooks.json`.
- edge_cases_or_failure_modes: Explicitly covers malformed JSON without crashes, missing fields, deeply nested JSON DoS shape, non-UTF8 binary input, Bash null-byte stripping, long commands, special characters, Unicode paths, unrecognized tools, log deletion, ANSI logs, binary logs, rapid log growth, narrow/wide terminal formatting, missing `.humanize/rlcr`, and session deletion while monitor is running (`tests/robustness/test-hook-input-robustness.sh:71-235`, `245-341`, `444-664`). A notable coverage distinction is that this file exercises Read/Write/Bash JSON hardening, while `loop-edit-validator.sh` still parses with direct `jq` rather than `validate_hook_input()` (`hooks/loop-edit-validator.sh:22-30`).
- validation_or_tests: The file is a direct test runner with production hooks, not a mock-only spec. It confirms command-modification detection for `sed -i` and redirection via `command_modifies_file()` (`tests/robustness/test-hook-input-robustness.sh:351-370`; implementation at `hooks/lib/loop-common.sh:782-810`) and confirms monitor helper output formats for goal tracker and git status parsing (`scripts/humanize.sh:24-105`, `108-203`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 4/4 section headings present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`