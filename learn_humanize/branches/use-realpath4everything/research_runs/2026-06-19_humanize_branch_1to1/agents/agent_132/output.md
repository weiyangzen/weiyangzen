# agent_132 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-132 `file` `prompt-template/block/methodology-analysis-state-file-modification.md`
- cursor: `[_]`
- core_role: This file is the static block-message template used when a hook blocks modification of `methodology-analysis-state.md`. It is not the state-transition algorithm itself, but it is part of the enforcement contract for the Methodology Analysis Phase: it tells the active agent that the phase state file is loop-managed and redirects work toward the allowed phase outputs. The template title and denial are at `prompt-template/block/methodology-analysis-state-file-modification.md:1` and `prompt-template/block/methodology-analysis-state-file-modification.md:3`; the allowed focus list is at `prompt-template/block/methodology-analysis-state-file-modification.md:5`.

- algorithmic_behavior: The template provides the user-facing reason emitted by `methodology_analysis_state_file_blocked_message()`, which loads `block/methodology-analysis-state-file-modification.md` via `load_and_render_safe` and falls back to an inline denial if the template cannot be loaded (`hooks/lib/loop-common.sh:1012`). The detection predicate is `is_methodology_analysis_state_file_path()`, matching lowercase paths ending in `methodology-analysis-state.md` (`hooks/lib/loop-common.sh:1006`). Write and edit validators check this more specific path before generic `state.md` because generic `state.md` also suffix-matches phase state names (`hooks/loop-write-validator.sh:188`, `hooks/loop-edit-validator.sh:152`). Bash validation applies the same message when shell commands write to, move from, or copy from `methodology-analysis-state.md` (`hooks/loop-bash-validator.sh:253`, `hooks/loop-bash-validator.sh:410`, `hooks/loop-bash-validator.sh:443`).

- inputs_outputs_state: The template has no dynamic placeholders, so its input is only the template lookup path and its output is Markdown text. The lifecycle state it protects is created by `enter_methodology_analysis_phase()`, which renames the current active state file to `methodology-analysis-state.md`, records `.methodology-exit-reason`, creates an empty `methodology-analysis-done.md`, then blocks exit with the rendered methodology prompt (`hooks/lib/methodology-analysis.sh:64`). The normal completion transition is handled elsewhere: `complete_methodology_analysis()` requires non-empty `methodology-analysis-done.md`, non-empty `methodology-analysis-report.md`, and a valid exit reason before renaming `methodology-analysis-state.md` to `<exit_reason>-state.md` (`hooks/lib/methodology-analysis.sh:114`, `hooks/lib/methodology-analysis.sh:155`, `hooks/lib/methodology-analysis.sh:165`). Cancellation is the authorized alternate transition: `cancel-rlcr-loop.sh` detects `methodology-analysis-state.md` as an active phase state and renames it to `cancel-state.md` after creating `.cancel-requested` (`scripts/cancel-rlcr-loop.sh:103`, `scripts/cancel-rlcr-loop.sh:156`, `scripts/cancel-rlcr-loop.sh:165`).

- gates_or_invariants: The key invariant is that agents must not directly write, edit, move, or copy `methodology-analysis-state.md`; only loop code transitions it. `loop-write-validator.sh` explicitly blocks writes to the state file with this template (`hooks/loop-write-validator.sh:192`). `loop-edit-validator.sh` does the same for edits (`hooks/loop-edit-validator.sh:156`). During Methodology Analysis, write access inside the loop directory is restricted to `methodology-analysis-report.md` and `methodology-analysis-done.md` (`hooks/loop-write-validator.sh:120`). Read access from the loop directory is restricted to `methodology-analysis-report.md`, `methodology-analysis-done.md`, and `methodology-analysis-state.md`, while other project reads are blocked for phase isolation (`hooks/loop-read-validator.sh:130`, `hooks/loop-read-validator.sh:152`). The branch’s realpath-sensitive gate appears in these validators: unresolved traversal paths fail closed, and canonicalized paths are compared against the real loop directory prefix (`hooks/loop-read-validator.sh:89`, `hooks/loop-read-validator.sh:101`; `hooks/loop-write-validator.sh:88`, `hooks/loop-write-validator.sh:100`).

- dependencies_and_callers: Direct dependency is `load_and_render_safe` through `methodology_analysis_state_file_blocked_message()` in `hooks/lib/loop-common.sh:1013`. Callers include write, edit, and bash validators: `hooks/loop-write-validator.sh:193`, `hooks/loop-edit-validator.sh:157`, and `hooks/loop-bash-validator.sh:258`. The broader lifecycle dependency is `hooks/lib/methodology-analysis.sh`, which enters and completes the phase (`hooks/lib/methodology-analysis.sh:38`, `hooks/lib/methodology-analysis.sh:114`). The read/write validators depend on `resolve_active_state_file()` resolving `methodology-analysis-state.md` as the active loop state (`hooks/lib/loop-common.sh:271`). Status/monitoring also recognizes this state as `methodology-analysis` (`scripts/lib/monitor-common.sh:163`; `scripts/statusline.sh:40`).

- edge_cases_or_failure_modes: If the template is missing or unreadable, `load_and_render_safe` returns the fallback denial, so blocking behavior can still proceed (`hooks/lib/loop-common.sh:1014`). Because `state.md` suffix matching is broad, the code checks `methodology-analysis-state.md` before `finalize-state.md` and generic `state.md`; otherwise this phase could emit the wrong denial (`hooks/loop-write-validator.sh:190`). Authorized cancel is the main exception: bash validation allows a properly signaled cancel move, but `is_cancel_authorized` requires source to canonicalize exactly to `state.md`, `finalize-state.md`, or `methodology-analysis-state.md`, destination to canonicalize exactly to `cancel-state.md`, and rejects symlink source files (`hooks/lib/loop-common.sh:1210`, `hooks/lib/loop-common.sh:1227`, `hooks/lib/loop-common.sh:1232`). Realpath failures are fail-closed for traversal in read/write methodology-phase checks, preventing `..` bypasses when canonicalization is unavailable (`hooks/loop-read-validator.sh:101`; `hooks/loop-write-validator.sh:100`). Spawned agents with different session IDs are noted as a hook limitation; their sanitization is enforced by the analysis prompt rather than these validators (`hooks/loop-read-validator.sh:79`, `hooks/loop-bash-validator.sh:70`).

- validation_or_tests: I found indirect validation around shared template references in `tests/test-template-references.sh`, but its explicit `COMMON_TEMPLATES` list does not include `block/methodology-analysis-state-file-modification.md` (`tests/test-template-references.sh:152`). I found broad test coverage for analogous `finalize-state.md` blocking behavior in `tests/test-finalize-phase.sh`, including write/edit/bash blocks and source move/copy protection, but no direct test hit for this methodology-specific template path in `tests/` from targeted `rg` searches. Existing runnable coverage therefore appears indirect through shared validator functions and neighboring state-file patterns, with a test gap for the exact `methodology-analysis-state-file-modification.md` template reference and exact emitted message.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: USE_REALPATH4EVERYTHING-HZ-132
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`