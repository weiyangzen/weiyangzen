# agent_103 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-103 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable zsh regression specification for the `humanize monitor rlcr` filesystem-discovery path. Its primary algorithmic role is to prove that monitor discovery logic is safe under zsh’s default unmatched-glob behavior, especially in empty directories, dotfile-only directories, and cache directories with no matching log files. It protects the branch’s monitor path handling against the old pattern of iterating raw globs such as `.humanize/rlcr/*`.
- algorithmic_behavior: The script must run under zsh via `#!/usr/bin/env zsh` and explicitly checks `$ZSH_VERSION` at the end (`tests/test-zsh-monitor-safety.sh:1`, `tests/test-zsh-monitor-safety.sh:282`). It creates an isolated test root under `/tmp/test-zsh-humanize-$$`, enters it, and redirects `XDG_CACHE_HOME` into that root so monitor cache probing does not touch a real user cache (`tests/test-zsh-monitor-safety.sh:48`, `tests/test-zsh-monitor-safety.sh:53`). It first sources `scripts/humanize.sh` and fails if stderr/stdout contains zsh’s `no matches found` diagnostic (`tests/test-zsh-monitor-safety.sh:72`, `tests/test-zsh-monitor-safety.sh:73`). The later blocks reproduce the monitor helper algorithms with `find ... | while read` process substitutions, validating that empty result sets are legal data rather than shell expansion failures (`tests/test-zsh-monitor-safety.sh:105`, `tests/test-zsh-monitor-safety.sh:114`, `tests/test-zsh-monitor-safety.sh:183`, `tests/test-zsh-monitor-safety.sh:191`, `tests/test-zsh-monitor-safety.sh:223`, `tests/test-zsh-monitor-safety.sh:227`).
- inputs_outputs_state: Inputs are the repository’s `scripts/humanize.sh`, the generated `.humanize/rlcr` session tree, synthetic timestamped session directories, synthetic state/log filenames, and `XDG_CACHE_HOME`. Outputs are TAP-like human-readable PASS/FAIL lines, incremented `TESTS_PASSED` / `TESTS_FAILED` counters, and process exit code `0` only when every check passes (`tests/test-zsh-monitor-safety.sh:24`, `tests/test-zsh-monitor-safety.sh:27`, `tests/test-zsh-monitor-safety.sh:32`, `tests/test-zsh-monitor-safety.sh:333`). State transitions are simple: setup creates `.humanize/rlcr`, individual tests mutate it between empty, dotfile-only, and timestamped-session states, then the final summary gates success on zero failures. Cleanup returns to `PROJECT_ROOT` and removes the temp base via `trap cleanup EXIT` (`tests/test-zsh-monitor-safety.sh:56`, `tests/test-zsh-monitor-safety.sh:60`).
- gates_or_invariants: The hard invariant is that no exercised zsh path emits `no matches found`; several checks inspect command output for that exact substring (`tests/test-zsh-monitor-safety.sh:73`, `tests/test-zsh-monitor-safety.sh:119`, `tests/test-zsh-monitor-safety.sh:152`, `tests/test-zsh-monitor-safety.sh:200`). Session discovery only accepts directory basenames matching `YYYY-MM-DD_HH-MM-SS`, and latest selection relies on lexicographic comparison of that timestamp format (`tests/test-zsh-monitor-safety.sh:109`, `tests/test-zsh-monitor-safety.sh:110`, `tests/test-zsh-monitor-safety.sh:259`, `tests/test-zsh-monitor-safety.sh:261`). State discovery must prefer `state.md` as active and otherwise emit `|unknown` when no `*-state.md` exists (`tests/test-zsh-monitor-safety.sh:177`, `tests/test-zsh-monitor-safety.sh:195`). Cache log discovery must tolerate an existing session cache with no `round-*-codex-run.log` files and count zero without error (`tests/test-zsh-monitor-safety.sh:215`, `tests/test-zsh-monitor-safety.sh:227`, `tests/test-zsh-monitor-safety.sh:229`). The final demonstration shows the old glob iteration is hazardous while the `find` pattern remains safe (`tests/test-zsh-monitor-safety.sh:303`, `tests/test-zsh-monitor-safety.sh:318`).
- dependencies_and_callers: The test depends on zsh semantics, local filesystem utilities (`mkdir`, `rm`, `touch`, `find`, `basename`), and `scripts/humanize.sh`. `humanize.sh` sources `scripts/lib/monitor-common.sh` when available (`scripts/humanize.sh:6`, `scripts/humanize.sh:9`) and wraps the shared helpers inside `_humanize_monitor_codex` as `_find_latest_session` and `_find_state_file` (`scripts/humanize.sh:279`, `scripts/humanize.sh:281`, `scripts/humanize.sh:377`, `scripts/humanize.sh:379`). The production implementations being protected use `find` instead of glob expansion for latest-session discovery (`scripts/lib/monitor-common.sh:49`, `scripts/lib/monitor-common.sh:60`) and for `*-state.md` discovery (`scripts/lib/monitor-common.sh:172`, `scripts/lib/monitor-common.sh:185`). The production log search likewise uses `find` for both run and review logs in the cache directory (`scripts/humanize.sh:338`, `scripts/humanize.sh:362`). The suite runner includes this file in the normal test list and separately identifies it as a zsh-only test (`tests/run-all-tests.sh:79`, `tests/run-all-tests.sh:120`, `tests/run-all-tests.sh:122`).
- edge_cases_or_failure_modes: Covered edge cases include missing or empty `.humanize/rlcr`, only hidden files in the loop directory, a session directory with unrelated markdown but no state file, an existing cache session with no codex logs, multiple valid timestamp sessions where latest ordering matters, and accidental execution under a non-zsh shell (`tests/test-zsh-monitor-safety.sh:86`, `tests/test-zsh-monitor-safety.sh:133`, `tests/test-zsh-monitor-safety.sh:166`, `tests/test-zsh-monitor-safety.sh:214`, `tests/test-zsh-monitor-safety.sh:246`, `tests/test-zsh-monitor-safety.sh:282`). A notable limitation is that several checks copy the helper logic locally instead of invoking `monitor_find_latest_session` or `monitor_find_state_file` directly, so they validate the intended algorithmic pattern and zsh behavior more than exact production function wiring (`tests/test-zsh-monitor-safety.sh:98`, `tests/test-zsh-monitor-safety.sh:139`, `tests/test-zsh-monitor-safety.sh:172`). The source test does exercise actual `humanize.sh` loading under zsh (`tests/test-zsh-monitor-safety.sh:72`).
- validation_or_tests: This file is itself validation. It is expected to be run as zsh, either directly or through `tests/run-all-tests.sh`, which classifies `test-zsh-monitor-safety.sh` under `ZSH_TESTS` (`tests/run-all-tests.sh:120`). I did not execute it in this read-only research pass because it creates and removes temporary directories and writes cache/session fixtures; I inspected the executable spec and related production helper implementations directly.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 of 1, represented by the single Item Evidence heading above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`