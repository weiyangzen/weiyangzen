# agent_183 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-183 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Executable security specification for RLCR cancel authorization. The file directly exercises `is_cancel_authorized` from `hooks/lib/loop-common.sh` and defines which Bash `mv` forms may convert an active loop state file into `cancel-state.md`. It is core algorithm coverage because `hooks/loop-bash-validator.sh` uses this helper as the narrow exception to otherwise blocked state-file modifications: `methodology-analysis-state.md`, `finalize-state.md`, and `state.md` modification checks call `is_cancel_authorized` before allowing the command at `hooks/loop-bash-validator.sh:253`, `hooks/loop-bash-validator.sh:262`, and `hooks/loop-bash-validator.sh:272`.

- algorithmic_behavior: The test initializes an isolated temp loop directory with `state.md` at `tests/robustness/test-cancel-security-robustness.sh:20` and `tests/robustness/test-cancel-security-robustness.sh:27`. It then models cancel commands as lowercased command strings, matching runtime behavior where `loop-bash-validator.sh` lowers Bash input with `to_lower` at `hooks/loop-bash-validator.sh:45` and `hooks/loop-bash-validator.sh:46`. Positive cases require a `.cancel-requested` signal and exact `mv` from an allowed state source to `<loop>/cancel-state.md`: normal `state.md` at lines `39-49`, `finalize-state.md` at lines `51-63`, single-quoted paths at lines `65-76`, unquoted paths at lines `78-89`, and `/./` path normalization at lines `265-276`. Negative cases assert rejection for missing signal, command substitution, backticks, shell chaining, wrong source/destination, extra arguments, newline injection, remaining variable expansion, non-`mv`, mixed quotes, trailing spaces, non-standard source names, and symlinked source files at lines `99-367`. Regression cases at lines `369-405` ensure a loop directory whose path contains the substring `finalize` does not bypass exact source-file classification.

- inputs_outputs_state: Inputs are synthetic filesystem state under `$TEST_DIR`, a loop directory path, presence or absence of `.cancel-requested`, optional `state.md` or `finalize-state.md` symlinks, and a lowercased Bash command string. The tested function returns shell status only: success means authorized; non-zero means rejected. The test records each assertion through `pass`/`fail` from `tests/test-helpers.sh:30` and `tests/test-helpers.sh:35`, then exits with the aggregate summary status from `print_test_summary` at `tests/robustness/test-cancel-security-robustness.sh:411`. State transitions in the test are explicit: create signal, call helper, remove signal; for symlink cases it replaces `state.md` or creates `finalize-state.md` as symlink and then restores or removes those files at lines `335-367`.

- gates_or_invariants: The tested authorization invariant is: only a simple `mv` of `<loop>/state.md`, `<loop>/finalize-state.md`, or, in the current implementation, `<loop>/methodology-analysis-state.md` to `<loop>/cancel-state.md` may pass, and only while `<loop>/.cancel-requested` exists. The implementation enforces the signal gate at `hooks/lib/loop-common.sh:1055`, blocks command substitution/newlines/shell operators at `hooks/lib/loop-common.sh:1062`, `hooks/lib/loop-common.sh:1067`, and `hooks/lib/loop-common.sh:1072`, rejects multiple trailing spaces at `hooks/lib/loop-common.sh:1077`, normalizes `$loop_dir`/`${loop_dir}` at `hooks/lib/loop-common.sh:1092`, rejects remaining `$` expansion at `hooks/lib/loop-common.sh:1101`, requires an `mv` command at `hooks/lib/loop-common.sh:1106`, rejects mixed quote delimiters at `hooks/lib/loop-common.sh:1144`, enforces exactly two parsed arguments at `hooks/lib/loop-common.sh:1181` and `hooks/lib/loop-common.sh:1185`, canonicalizes source and destination with prefix-only realpath behavior at `hooks/lib/loop-common.sh:1205` and `hooks/lib/loop-common.sh:1222`, checks exact expected source/destination paths at `hooks/lib/loop-common.sh:1210` and `hooks/lib/loop-common.sh:1227`, and rejects an on-disk source symlink at `hooks/lib/loop-common.sh:1232`. The realpath distinction is important for this branch: `canonicalize_path_prefix` resolves only the parent and preserves the basename at `hooks/lib/project-root.sh:55`, while `canonicalize_path` may dereference an existing leaf and is explicitly unsafe for user-supplied filename authorization at `hooks/lib/project-root.sh:98`.

- dependencies_and_callers: Direct dependencies are `hooks/lib/loop-common.sh` sourced at `tests/robustness/test-cancel-security-robustness.sh:17` and `tests/test-helpers.sh` sourced at line `18`. `setup_test_dir` creates a temp directory with cleanup at `tests/test-helpers.sh:86`; `pass`, `fail`, and `print_test_summary` provide assertion counting and exit status. The core tested helper is `is_cancel_authorized` at `hooks/lib/loop-common.sh:1051`, which depends on `_normalize_path` at `hooks/lib/loop-common.sh:1027`, `canonicalize_path_prefix` at `hooks/lib/project-root.sh:72`, and `canonicalize_path` at `hooks/lib/project-root.sh:113`. Runtime callers include `hooks/loop-bash-validator.sh`, where authorized cancel moves exit `0` instead of blocking state-file modifications at `hooks/loop-bash-validator.sh:253-275`. The standalone cancel script creates `.cancel-requested` and performs the actual rename at `scripts/cancel-rlcr-loop.sh:156-166`; it establishes the intended state transition from active state file to `cancel-state.md`, while this test focuses on the hook-side authorization boundary for direct Bash move forms.

- edge_cases_or_failure_modes: The file covers injection through `$(...)`, backticks, semicolons, `&&`, `||`, pipes, newlines, `${HOME}`, and `${IFS}` at `tests/robustness/test-cancel-security-robustness.sh:109-237` and `291-302`. It covers structural bypasses: wrong destination `/tmp/evil-state.md`, wrong source `/etc/passwd`, extra args, non-`mv`, mixed quoting, and trailing whitespace at lines `174-315`. It covers path ambiguity: `/./` should normalize and pass at lines `265-276`, but a symlink named `state-link.md` must fail because only exact state filenames are valid at lines `317-330`. It covers filesystem symlink rejection for `state.md` and `finalize-state.md` at lines `332-367`. It specifically guards against a historical substring-classification bug where a parent path containing `finalize` could cause `state.md` to be treated like `finalize-state.md`; exact path matching is asserted at lines `369-405`. Coverage gap relative to the current helper: `is_cancel_authorized` now also accepts `methodology-analysis-state.md` as an allowed source at `hooks/lib/loop-common.sh:1212`, but this assigned test file only exercises `state.md` and `finalize-state.md`.

- validation_or_tests: The file is itself a standalone robustness test. It has 26 numbered assertions and exits with non-zero status if any `fail` was recorded through `print_test_summary` at `tests/robustness/test-cancel-security-robustness.sh:411`. It is complemented by broader cancel tests in `tests/test-cancel-signal-file.sh`, including realpath-focused helper regressions for symlinked prefixes and source/destination symlink aliases at `tests/test-cancel-signal-file.sh:1345-1419`. I did not run the test suite because this worker task requested research notes only and the branch export is read-only; inspection was direct from the assigned file and referenced helpers.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-183`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`