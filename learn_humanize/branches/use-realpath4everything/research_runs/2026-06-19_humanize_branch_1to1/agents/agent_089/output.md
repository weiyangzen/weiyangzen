# agent_089 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-089 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR plan-file startup validation. The file is a Bash test harness for `scripts/setup-rlcr-loop.sh`, focused on safe plan path handling, git repository preconditions, plan tracking mode, YAML-safe serialization inputs, content sanity checks, and CLI parsing. It is relevant to the branch because the setup path relies on canonical project-root resolution and path containment checks before writing loop state.
- algorithmic_behavior: The test initializes isolated temporary git repositories, invokes `scripts/setup-rlcr-loop.sh` with controlled plan paths and flags, captures stdout/stderr plus exit status, and asserts that validation fails or passes at the expected gate. Setup begins by deriving `PROJECT_ROOT` from the test file location while explicitly unsetting `CLAUDE_PROJECT_DIR` so the setup script resolves the temp repo from `pwd` instead of the source checkout (`tests/test-plan-file-validation.sh:16`, `tests/test-plan-file-validation.sh:19`). `setup_test_repo` creates a committed git repo, writes an ignored `plans/test-plan.md`, and commits `.gitignore` so default untracked/gitignored plan mode can be tested (`tests/test-plan-file-validation.sh:40`). `mock_codex` prepends a fake `codex` binary to `PATH`, avoiding the real CLI while satisfying dependency checks (`tests/test-plan-file-validation.sh:73`). The suite then exercises absolute-path rejection, missing file and missing parent errors, spaces in path components and filenames, shell metacharacter rejection, symlink rejection, path-resolution failure, `../` project escape, non-git repos, repos without commits, tracked/untracked/modified plan-file mode mismatches, unsafe branch names, blank/comment-only/sparse content rejection, valid content acceptance, `--plan-file`, mutual exclusion with positional plan args, and Codex model/effort validation (`tests/test-plan-file-validation.sh:88`, `tests/test-plan-file-validation.sh:103`, `tests/test-plan-file-validation.sh:115`, `tests/test-plan-file-validation.sh:127`, `tests/test-plan-file-validation.sh:168`, `tests/test-plan-file-validation.sh:188`, `tests/test-plan-file-validation.sh:201`, `tests/test-plan-file-validation.sh:244`, `tests/test-plan-file-validation.sh:273`, `tests/test-plan-file-validation.sh:298`, `tests/test-plan-file-validation.sh:328`, `tests/test-plan-file-validation.sh:360`, `tests/test-plan-file-validation.sh:394`, `tests/test-plan-file-validation.sh:431`, `tests/test-plan-file-validation.sh:517`, `tests/test-plan-file-validation.sh:666`, `tests/test-plan-file-validation.sh:683`, `tests/test-plan-file-validation.sh:700`).
- inputs_outputs_state: Inputs are command-line arguments to `scripts/setup-rlcr-loop.sh`, the current working directory inside each temp repo, git repository state, plan-file contents, branch names, and optional flags such as `--track-plan-file`, `--plan-file`, and `--codex-model`. Outputs are colored `PASS`, `FAIL`, and `SKIP` records plus aggregate counters; the script exits with `TESTS_FAILED`, making the suite itself a binary validation gate (`tests/test-plan-file-validation.sh:23`, `tests/test-plan-file-validation.sh:32`, `tests/test-plan-file-validation.sh:742`). Internal state consists of `TEST_DIR`, per-test temp repos, `PATH` modified to include the mock `codex`, captured `RESULT` and `EXIT_CODE`, and pass/fail/skip counters. The setup script under test resolves the project root via `resolve_project_root` (`scripts/setup-rlcr-loop.sh:326`), which is supplied by `hooks/lib/loop-common.sh` and ultimately `hooks/lib/project-root.sh`; the project-root helper canonicalizes roots with `realpath`/Python fallback to prevent symlink-prefix drift (`hooks/lib/project-root.sh:41`, `hooks/lib/project-root.sh:50`, `hooks/lib/project-root.sh:113`). The setup script then transitions from parsed CLI state into validated plan state: merge explicit/positional plan input (`scripts/setup-rlcr-loop.sh:399`), validate repo and commit existence (`scripts/setup-rlcr-loop.sh:433`), validate path safety (`scripts/setup-rlcr-loop.sh:455`), determine git tracking status (`scripts/setup-rlcr-loop.sh:562`), validate content (`scripts/setup-rlcr-loop.sh:618`), capture branch/model state (`scripts/setup-rlcr-loop.sh:686`), and finally report activated loop metadata including `Plan File`, `Plan Tracked`, branches, model, and loop directory (`scripts/setup-rlcr-loop.sh:1477`).
- gates_or_invariants: The test suite encodes these invariants: plan paths must be relative (`scripts/setup-rlcr-loop.sh:455`), contain no whitespace (`scripts/setup-rlcr-loop.sh:461`), contain no shell metacharacters (`scripts/setup-rlcr-loop.sh:469`), not be symlinks at the leaf (`scripts/setup-rlcr-loop.sh:481`), have an existing parent directory (`scripts/setup-rlcr-loop.sh:487`), not traverse symlink parent directories (`scripts/setup-rlcr-loop.sh:494`), exist and be readable (`scripts/setup-rlcr-loop.sh:517`, `scripts/setup-rlcr-loop.sh:523`), and resolve inside the canonical project root (`scripts/setup-rlcr-loop.sh:529`). The setup script also rejects plan files inside configured submodules if `.gitmodules` and submodule status expose a matching path (`scripts/setup-rlcr-loop.sh:543`), requires a git repo and at least one commit (`scripts/setup-rlcr-loop.sh:433`, `scripts/setup-rlcr-loop.sh:439`), and enforces mutually exclusive plan tracking modes: without `--track-plan-file`, the plan must not be tracked; with `--track-plan-file`, it must be tracked and clean (`scripts/setup-rlcr-loop.sh:582`, `scripts/setup-rlcr-loop.sh:597`). Content must have at least five physical lines and at least three meaningful lines after excluding blank lines, `#` comment lines, single-line HTML comments, and multi-line HTML comment bodies (`scripts/setup-rlcr-loop.sh:618`, `scripts/setup-rlcr-loop.sh:628`, `scripts/setup-rlcr-loop.sh:666`). Branch names and base branches must avoid YAML-unsafe characters, and Codex model/effort fields are restricted before state serialization (`scripts/setup-rlcr-loop.sh:692`, `scripts/setup-rlcr-loop.sh:702`, `scripts/setup-rlcr-loop.sh:711`, `scripts/setup-rlcr-loop.sh:799`). The runtime hook mirrors the persisted-state invariants by strict-parsing state, blocking branch switches, and checking whether the plan file remains tracked/clean or untracked according to `plan_tracked` (`hooks/loop-plan-file-validator.sh:45`, `hooks/loop-plan-file-validator.sh:96`, `hooks/loop-plan-file-validator.sh:124`, `hooks/loop-plan-file-validator.sh:194`).
- dependencies_and_callers: The direct dependency under test is `scripts/setup-rlcr-loop.sh`, invoked repeatedly from the test via absolute path while `cwd` points into temp repos. That setup script depends on `scripts/portable-timeout.sh`, `hooks/lib/loop-common.sh`, `hooks/lib/project-root.sh`, `git`, `jq`, and `codex`; the test only mocks `codex`, so `git` and `jq` remain real prerequisites (`scripts/setup-rlcr-loop.sh:25`, `scripts/setup-rlcr-loop.sh:32`, `scripts/setup-rlcr-loop.sh:340`). `loop-common.sh` sources the shared project-root resolver before callers need `PROJECT_ROOT` (`hooks/lib/loop-common.sh:176`). The runtime caller-side companion is `hooks/loop-plan-file-validator.sh`, a `UserPromptSubmit` hook that reads JSON hook input, extracts `session_id`, finds the active loop, resolves the active state file, strict-parses frontmatter, and enforces branch/tracking consistency (`hooks/loop-plan-file-validator.sh:27`, `hooks/loop-plan-file-validator.sh:31`, `hooks/loop-plan-file-validator.sh:35`, `hooks/loop-plan-file-validator.sh:43`). The test file itself does not call the hook directly; it primarily validates setup-time admission gates that produce safe state for the hook to later consume.
- edge_cases_or_failure_modes: The header claims submodule rejection coverage, but the inspected file does not create a submodule scenario; submodule validation exists in the setup script, but this test file only documents it in the header (`tests/test-plan-file-validation.sh:9`, `scripts/setup-rlcr-loop.sh:543`). The shell metacharacter loop checks for the expected message but does not require a nonzero exit code, and it calls `pass` after the loop even if one metacharacter caused a recorded `fail`; the final exit still reflects `TESTS_FAILED`, but pass counts may be misleading (`tests/test-plan-file-validation.sh:179`, `tests/test-plan-file-validation.sh:186`). The permission-resolution test only skips when `chmod 000` fails; on environments where permissions are changed but access remains possible, behavior may depend on user privileges and filesystem semantics (`tests/test-plan-file-validation.sh:225`). The unsafe branch-name tests accept Git rejecting branch creation as a pass, so on Git versions that disallow a name before checkout, the setup script’s YAML-safety branch may not be exercised (`tests/test-plan-file-validation.sh:459`, `tests/test-plan-file-validation.sh:479`, `tests/test-plan-file-validation.sh:497`). The positive content tests intentionally only assert absence of the phrase `insufficient content`, so they may pass even if later gates fail for unrelated reasons such as working-tree cleanliness or base-branch detection (`tests/test-plan-file-validation.sh:625`, `tests/test-plan-file-validation.sh:629`, `tests/test-plan-file-validation.sh:651`). Because dependency validation happens before plan-path validation, missing real dependencies such as `jq` could mask the intended path-validation assertions despite the mocked `codex` (`scripts/setup-rlcr-loop.sh:340`).
- validation_or_tests: This file is itself the validation artifact. It is a 751-line executable Bash test (`tests/test-plan-file-validation.sh:1`) that creates throwaway repos under `mktemp`, cleans them with a trap, reports counters, and exits with the number of failures (`tests/test-plan-file-validation.sh:36`, `tests/test-plan-file-validation.sh:751`). I inspected it directly and followed the relevant implementation paths in `scripts/setup-rlcr-loop.sh`, `hooks/lib/project-root.sh`, `hooks/lib/loop-common.sh`, and `hooks/loop-plan-file-validator.sh`. I did not modify files. I did not execute the full test suite because the assignment requested read-only research notes and this branch export is explicitly read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 assigned item represented by exactly one Item Evidence section
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`