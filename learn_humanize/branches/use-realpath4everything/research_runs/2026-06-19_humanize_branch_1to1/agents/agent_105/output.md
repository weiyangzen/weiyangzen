# agent_105 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-105 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared RLCR hook library for validators, setup/cancel scripts, and the Codex stop hook. It is not a standalone hook; it centralizes common constants, hook JSON validation, active-loop discovery, state-frontmatter parsing/updating, path classification, block-message rendering, cancellation authorization, git `.humanize` protection, command modification detection, terminal state renaming, and background-task helper export. Direct declared consumers are listed in the file header at `hooks/lib/loop-common.sh:3-13`, and actual callers include `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `scripts/setup-rlcr-loop.sh`, and `scripts/cancel-rlcr-loop.sh`.
- algorithmic_behavior: Hook input handling starts with `validate_hook_input`, which rejects null bytes, invalid UTF-8 when `iconv` exists, invalid JSON via `jq`, and missing `tool_name`; it exports `VALIDATED_TOOL_NAME` and `VALIDATED_TOOL_INPUT` for callers (`hooks/lib/loop-common.sh:91-128`). `require_tool_input_field` enforces required `tool_input` fields (`hooks/lib/loop-common.sh:133-146`), while `is_deeply_nested` computes max JSON depth through `jq '[paths | length] | max'` and rejects DoS-shaped input over the caller-specified depth (`hooks/lib/loop-common.sh:151-165`). Library setup resolves its own paths, sources `project-root.sh`, loads merged config best-effort, validates config-backed Codex defaults, and initializes template rendering fallback (`hooks/lib/loop-common.sh:171-248`).
- algorithmic_behavior: Active state resolution has an explicit phase order: `methodology-analysis-state.md`, then `finalize-state.md`, then `state.md` (`hooks/lib/loop-common.sh:268-280`). `resolve_any_state_file` first uses active states, then any terminal `*-state.md` file (`hooks/lib/loop-common.sh:287-305`). `find_active_loop` has two modes: without a session filter it checks only the single newest loop directory, preserving zombie-loop protection (`hooks/lib/loop-common.sh:342-356`); with a session filter it scans newest-to-oldest for the newest directory belonging to that session, treats empty stored `session_id` as backward-compatible, refuses to revive an older active loop if the session’s newest loop is terminal, and only allows `bg-pending.marker` fallback when a caller explicitly opts in (`hooks/lib/loop-common.sh:359-424`).
- algorithmic_behavior: State parsing is frontmatter-oriented. `_parse_state_fields` extracts fixed YAML keys into `STATE_*` variables (`hooks/lib/loop-common.sh:446-469`). `parse_state_file` is tolerant and defaults non-critical fields such as `current_round`, `max_iterations`, `push_every_round`, `privacy_mode`, `mainline_stall_count`, `last_mainline_verdict`, and `drift_status` (`hooks/lib/loop-common.sh:494-521`). `parse_state_file_strict` is the validator path: it requires at least two `---` separators, requires `current_round`, `max_iterations`, `review_started`, and `base_branch`, validates numeric round/iteration fields and boolean `review_started`, then applies optional defaults (`hooks/lib/loop-common.sh:533-600`). `upsert_state_fields` rewrites or inserts simple frontmatter keys via a temp file and `mv` (`hooks/lib/loop-common.sh:667-716`).
- algorithmic_behavior: Review-loop helpers normalize mainline progress and drift enums (`hooks/lib/loop-common.sh:604-632`), extract the last unambiguous “Mainline Progress Verdict” from review text (`hooks/lib/loop-common.sh:637-662`), and scan Codex review logs for `[P?]` issue markers only in the last 50 lines with the marker inside the first 10 characters, writing extracted issue content to `round-N-review-result.md` (`hooks/lib/loop-common.sh:740-788`). The stop hook uses these to advance review/finalize behavior: issue-free review enters finalize by renaming `state.md` to `finalize-state.md` (`hooks/loop-codex-stop-hook.sh:1250-1278`, `hooks/loop-codex-stop-hook.sh:1281-1365`), while repeated stalled/regressed verdicts update drift fields and may terminate via `end_loop` (`hooks/loop-codex-stop-hook.sh:1779-1824`, `hooks/loop-codex-stop-hook.sh:1909-1957`).
- inputs_outputs_state: Primary inputs are hook JSON from stdin, `.humanize/rlcr/<loop>/...` state files, Codex review logs under the caller’s cache directory, command/path strings from validators, merged plugin/project config, template files, and git state. Outputs are shell return codes, stderr block messages or rendered templates, exported `STATE_*` and `VALIDATED_*` variables, modified state frontmatter from `upsert_state_fields`, terminal state renames from `end_loop`, generated review-result extraction files, and allow/block decisions consumed by hook scripts. The active state machine represented here includes normal implementation (`state.md`), review flag mutation (`review_started: true`), finalize phase (`finalize-state.md`), methodology analysis (`methodology-analysis-state.md`), and terminal states such as `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, and `unexpected-state.md` (`hooks/lib/loop-common.sh:66-76`, `hooks/lib/loop-common.sh:1533-1566`).
- inputs_outputs_state: `end_loop` is the terminal transition helper: it accepts only `complete`, `cancel`, `maxiter`, `stop`, or `unexpected`, renames the active state file to `<reason>-state.md`, emits status to stderr, and fails if the state file is missing (`hooks/lib/loop-common.sh:1540-1566`). `scripts/cancel-rlcr-loop.sh` uses the same active-loop model globally, creates `.cancel-requested`, and renames whichever active state file exists to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:80-91`, `scripts/cancel-rlcr-loop.sh:103-167`). The stop hook uses `end_loop` for corrupted schema, invalid model/effort, max iteration, stop, and drift-circuit outcomes (`hooks/loop-codex-stop-hook.sh:174-204`, `hooks/loop-codex-stop-hook.sh:1833-1841`, `hooks/loop-codex-stop-hook.sh:1940-1945`).
- gates_or_invariants: Project-root and path canonicalization are central to this branch. `loop-common.sh` sources `hooks/lib/project-root.sh`, whose resolver prefers `CLAUDE_PROJECT_DIR`, then git toplevel, and canonicalizes through `realpath` so setup-time and hook-time roots do not diverge through symlinked prefixes (`hooks/lib/project-root.sh:3-20`, `hooks/lib/project-root.sh:41-53`). The companion `canonicalize_path_prefix` resolves only the parent directory and preserves the basename, specifically to compare user-supplied filenames without dereferencing a symlink at the leaf (`hooks/lib/project-root.sh:55-96`). `canonicalize_path` dereferences the leaf and is explicitly warned against for user-supplied authorization (`hooks/lib/project-root.sh:98-144`).
- gates_or_invariants: `is_cancel_authorized` is the tightest realpath-sensitive gate in this file. It requires `.cancel-requested`, rejects command substitution, backticks, newlines, shell operators, hidden variables, mixed quote styles, multiple trailing spaces, non-`mv` commands, missing or extra arguments, and enforces exactly `state.md` or `finalize-state.md` or `methodology-analysis-state.md` to `cancel-state.md` inside the canonical loop directory (`hooks/lib/loop-common.sh:1051-1230`). It canonicalizes the loop directory and both user path arguments using prefix-only canonicalization, then lowercases after canonicalization for case-insensitive filesystem compatibility (`hooks/lib/loop-common.sh:1082-1097`, `hooks/lib/loop-common.sh:1191-1228`). It also rejects an on-disk symlink source file after mapping the canonical source back to the real active state path (`hooks/lib/loop-common.sh:1232-1247`).
- gates_or_invariants: File validators use the shared helpers to enforce phase and round boundaries. Read/write/edit/bash validators call `find_active_loop` with `session_id`, so unrelated sessions are not restricted by another active loop (`hooks/loop-read-validator.sh:49-60`, `hooks/loop-write-validator.sh:50-61`, `hooks/loop-edit-validator.sh:33-44`, `hooks/loop-bash-validator.sh:52-59`). They fail closed on malformed active state via `parse_state_file_strict` (`hooks/loop-read-validator.sh:204-215`, `hooks/loop-write-validator.sh:173-185`, `hooks/loop-edit-validator.sh:137-149`, `hooks/loop-bash-validator.sh:191-199`). Read and write validators use `canonicalize_path_prefix` for final directory-path comparisons to accept symlinked ancestor equivalence without authorizing leaf symlink escapes (`hooks/loop-read-validator.sh:305-322`, `hooks/loop-write-validator.sh:332-352`).
- gates_or_invariants: State and local runtime artifacts are protected from user/tool writes. `command_modifies_file` detects redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec >` against a pattern (`hooks/lib/loop-common.sh:1485-1510`). `git_adds_humanize` blocks direct `.humanize` additions, force-add broad scopes, `git add -A`, and broad `git add .` or `*` when `.humanize` exists and is not ignored (`hooks/lib/loop-common.sh:1280-1376`). `git_has_tracked_humanize_state` fails the stop hook if `.humanize/` is tracked or staged (`hooks/lib/loop-common.sh:1428-1440`, `hooks/loop-codex-stop-hook.sh:639-652`).
- dependencies_and_callers: Hard dependencies are Bash, `jq`, `sed`, `grep`, `awk`, `mv`, `ls`, `sort`, `head`, `tail`, `wc`, `tr`; optional/security dependencies include `iconv`, `perl`, `realpath`, `python3`, `git`, and template/config helpers from `hooks/lib/template-loader.sh`, `hooks/lib/project-root.sh`, and `scripts/lib/config-loader.sh` (`hooks/lib/loop-common.sh:101-112`, `hooks/lib/loop-common.sh:171-240`, `hooks/lib/loop-common.sh:968-991`). Background-task helpers are sourced at EOF, making `loop-bg-tasks.sh` part of the library’s exported behavior (`hooks/lib/loop-common.sh:1568-1578`); the stop hook invokes `handle_bg_task_short_circuit` before normal gates (`hooks/loop-codex-stop-hook.sh:71-84`).
- dependencies_and_callers: `loop-bg-tasks.sh` adds transcript-driven parking semantics. It extracts transcript paths, derives a loop-start UTC timestamp from the loop directory basename, finds launched async Agent/Bash task IDs, subtracts completion notifications, prunes dead tasks through output-file liveness checks, creates `bg-pending.marker` when tasks remain, exits ambiguous or cross-session callers safely, and removes stale markers only after a readable transcript proves no pending task remains (`hooks/lib/loop-bg-tasks.sh:38-95`, `hooks/lib/loop-bg-tasks.sh:193-265`, `hooks/lib/loop-bg-tasks.sh:299-430`).
- edge_cases_or_failure_modes: Missing project root generally causes hooks to allow natural exit or pass-through rather than corrupt state; malformed state causes validators to block and stop hook to terminate as unexpected. `find_active_loop` deliberately ignores stale older loops when the newest relevant loop is terminal or inactive, preventing accidental revival (`hooks/lib/loop-common.sh:308-329`, `hooks/lib/loop-common.sh:393-396`). Empty stored `session_id` is backward-compatible, but marker fallback is restricted to explicit callers to avoid cross-session adoption (`hooks/lib/loop-common.sh:381-420`). `resolve_any_state_file` uses `ls | head -1` for terminal fallback, so terminal-state choice is filesystem/lexicographic rather than semantic if multiple terminal files exist (`hooks/lib/loop-common.sh:302-305`).
- edge_cases_or_failure_modes: Hook JSON null-byte detection is limited by Bash strings because Bash may strip NUL bytes before function logic sees them, but invalid UTF-8 is rejected when `iconv` is available and malformed JSON is rejected by `jq` (`hooks/lib/loop-common.sh:94-115`; corresponding test comment at `tests/robustness/test-hook-input-robustness.sh:219-235`). `upsert_state_fields` accepts assignments space-joined through `$*`, so values with spaces/newlines are outside its intended contract (`hooks/lib/loop-common.sh:664-716`). `preview_edit_result` depends on `perl`; if unavailable, goal-tracker edit validation fails closed through the edit validator (`hooks/lib/loop-common.sh:968-991`, `hooks/loop-edit-validator.sh:218-225`). `detect_review_issues` intentionally ignores issue markers outside the last 50 lines or past the first 10 characters to avoid stale/debug false positives (`hooks/lib/loop-common.sh:728-788`).
- edge_cases_or_failure_modes: The cancel authorization parser is intentionally narrow and may reject some syntactically valid shell forms, but this is a safety tradeoff for the only allowed state-file `mv`. Regression-sensitive edge cases include symlinked project prefixes that should pass after realpath normalization, leaf symlink aliases that must fail, loop directory names containing words like `finalize` or `methodology`, mixed quote delimiters, hidden `${IFS}`-style variables, command chaining, and multiple trailing spaces (`hooks/lib/loop-common.sh:1043-1050`, `hooks/lib/loop-common.sh:1191-1247`).
- validation_or_tests: Relevant test coverage includes hook input validation and command modification detection in `tests/robustness/test-hook-input-robustness.sh:35-235` and `tests/robustness/test-hook-input-robustness.sh:351-370`; active-loop/zombie/session robustness in `tests/robustness/test-session-robustness.sh:33-317` and `tests/test-session-id.sh` references; strict/tolerant state parsing in `tests/robustness/test-state-file-robustness.sh` and `tests/robustness/test-state-transition-robustness.sh`; terminal renaming in `tests/test-state-exit-naming.sh`; Codex issue extraction in `tests/test-codex-review-merge.sh`; `.humanize` git-add protection in `tests/test-humanize-escape.sh`; template reference checks in `tests/test-template-references.sh`; and plan path validation in `tests/robustness/test-path-validation-robustness.sh:76-113`.
- validation_or_tests: The branch-specific realpath/cancel evidence is strongest in `tests/test-cancel-signal-file.sh`: it verifies allowed cancel `mv` only with `.cancel-requested` (`tests/test-cancel-signal-file.sh:80-115`), rejects missing/wrong signal and non-cancel state mutations (`tests/test-cancel-signal-file.sh:121-237`), directly tests `is_cancel_authorized` helper cases (`tests/test-cancel-signal-file.sh:1259-1343`), accepts a symlinked-prefix path after realpath normalization (`tests/test-cancel-signal-file.sh:1345-1374`), and rejects destination/source symlink aliases (`tests/test-cancel-signal-file.sh:1376-1419`). I inspected tests but did not execute them because this assignment requested research notes only in a read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 item section present above`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`