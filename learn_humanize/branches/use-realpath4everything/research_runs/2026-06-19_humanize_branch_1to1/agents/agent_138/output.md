# agent_138 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-138 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: This is a block-message template for legacy RLCR state schema detection. It is not executable algorithm code, but it defines the user-facing contract emitted when a state file lacks a required schema field. The template title is `# State Schema Outdated` at `prompt-template/block/schema-outdated.md:1`, and its core diagnostic is the templated line `State file is missing required field: \`{{FIELD_NAME}}\`` at `prompt-template/block/schema-outdated.md:3`.

- algorithmic_behavior: The template participates in the plan-file validation gate through `schema_validation_error()` in `hooks/loop-plan-file-validator.sh:60`. That function receives a missing field name, renders `block/schema-outdated.md` via `load_and_render_safe` at `hooks/loop-plan-file-validator.sh:66`, JSON-escapes the rendered Markdown at `hooks/loop-plan-file-validator.sh:68-70`, and outputs a JSON block decision at `hooks/loop-plan-file-validator.sh:72-77`. The validator then checks `plan_tracked` and `start_branch` as required fields at `hooks/loop-plan-file-validator.sh:80-90`; if either parsed value is empty, the rendered template becomes the block reason and the hook exits successfully with a blocking JSON response rather than allowing the prompt/action.

- inputs_outputs_state: Input is a single template variable, `FIELD_NAME`, provided as `FIELD_NAME=$field_name` by `hooks/loop-plan-file-validator.sh:66`. The field names come from constants `FIELD_PLAN_TRACKED="plan_tracked"` and `FIELD_START_BRANCH="start_branch"` in `hooks/lib/loop-common.sh:25-26`, and their parsed values come from state frontmatter extraction in `hooks/lib/loop-common.sh:449-450`. Output is Markdown text embedded as the JSON `reason` in a `{"decision":"block"}` hook response. The template itself does not mutate state; it describes the transition that the session will be terminated as `unexpected` to preserve state information at `prompt-template/block/schema-outdated.md:12`. In the plan-file validator path, the actual immediate state transition is a hook block and `exit 0`, not direct loop termination, as shown at `hooks/loop-plan-file-validator.sh:87-89`.

- gates_or_invariants: The relevant invariant is that an active RLCR loop state must include schema-critical fields before branch and plan-file consistency checks continue. The assigned template is the canonical block text for missing `plan_tracked` or `start_branch` in the plan-file validator path. Rendering depends on the template system’s `{{VARIABLE_NAME}}` syntax, where variables are uppercase/underscore names and missing variables are left unreplaced, documented at `hooks/lib/template-loader.sh:7-13`. The safe rendering gate uses `load_and_render_safe`, which falls back to an inline message if the template is missing, empty, or renders empty at `hooks/lib/template-loader.sh:188-210`. A newer strict parser also enforces `current_round`, `max_iterations`, `review_started`, and `base_branch` before this older plan-file-validator schema block can run; see `parse_state_file_strict` requirements at `hooks/lib/loop-common.sh:524-569` and the call site at `hooks/loop-plan-file-validator.sh:45-49`.

- dependencies_and_callers: Direct caller is `hooks/loop-plan-file-validator.sh:66`. The caller gets `TEMPLATE_DIR` from `hooks/lib/loop-common.sh`, which sources the template loader at `hooks/lib/loop-common.sh:240` and initializes/validates the template directory at `hooks/lib/loop-common.sh:242-248`. Rendering behavior depends on `hooks/lib/template-loader.sh`, especially `load_template`, `render_template`, and `load_and_render_safe` at `hooks/lib/template-loader.sh:36-46`, `hooks/lib/template-loader.sh:56-136`, and `hooks/lib/template-loader.sh:188-210`. A related but not direct caller is `hooks/loop-codex-stop-hook.sh`: it has hardcoded schema-outdated JSON block messages for missing `plan_tracked`/`start_branch` at `hooks/loop-codex-stop-hook.sh:225-236`, missing/invalid `review_started` at `hooks/loop-codex-stop-hook.sh:244-255`, and missing `base_branch` at `hooks/loop-codex-stop-hook.sh:258-269`; those paths mirror the same schema gate concept but do not render this specific template.

- edge_cases_or_failure_modes: If `FIELD_NAME` is not supplied or is misspelled, the template loader preserves `{{FIELD_NAME}}` instead of failing, per `hooks/lib/template-loader.sh:12-13`; that would produce a less useful block reason. If the template file is missing or empty, `load_and_render_safe` returns the fallback text defined at `hooks/loop-plan-file-validator.sh:63-66`, so the hook still blocks safely. If the state file is malformed or missing newer strict fields, `parse_state_file_strict` fails earlier and the user sees `Error: Malformed state file, blocking operation for safety` from `hooks/loop-plan-file-validator.sh:45-49`, bypassing this template. The template text says “session” and uses `/humanize:cancel-rlcr-loop`, while the fallback in the validator says “RLCR loop”; this is a wording divergence but not an algorithmic failure. The options in the template require plugin version `1.1.2+` at `prompt-template/block/schema-outdated.md:7-10`, which is accurate for the older `plan_tracked`/`start_branch` gate but not for newer v1.5.0 fields handled elsewhere.

- validation_or_tests: `tests/test-plan-file-hooks.sh:175-193` covers the plan-file validator’s malformed/outdated-state blocking behavior for missing newer schema fields, though this current path is intercepted by strict parsing rather than asserting the exact template render. `tests/test-plan-file-hooks.sh:732-754` covers the Codex stop hook returning a JSON block for old schema state without `plan_tracked`; this validates the same user-visible schema-outdated gate class, but the stop hook uses hardcoded text rather than this template. Template infrastructure is validated more generally by `tests/test-templates-comprehensive.sh:566-606`, which loads and renders all Markdown templates with dummy values and checks for unreplaced placeholders, and by `tests/test-template-loader.sh` coverage of `load_and_render_safe` fallback behavior referenced in search results. I did not run tests because this task requested research notes only and the branch export is read-only.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1 item section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`