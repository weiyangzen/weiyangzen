# agent_164 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-164 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: This is the Claude remediation prompt template for the RLCR Review Phase. Its algorithmic role is to convert Codex review findings into a constrained follow-up round contract, keeping the original mainline objective stable while allowing only review-blocking fixes to drive the next Claude round. The template explicitly anchors the phase at `prompt-template/claude/review-phase-prompt.md:3`, forces re-reading the plan, goal tracker, and round contract at `prompt-template/claude/review-phase-prompt.md:5-12`, and embeds Codex review output through `{{REVIEW_CONTENT}}` at `prompt-template/claude/review-phase-prompt.md:14-16`.
- algorithmic_behavior: The template defines a classification step before any fix work: each finding becomes either `blocking side issue` or `queued side issue` at `prompt-template/claude/review-phase-prompt.md:18-24`. It then enforces lane tagging: `[blocking]` for fixes required now, `[queued]` for follow-up work, and no new `[mainline]` task unless the review proves the old mainline was incomplete, as specified at `prompt-template/claude/review-phase-prompt.md:26-32`. The action sequence is deterministic: refresh the round contract, address blocking issues first, avoid unrelated changes, commit, and write the summary to `{{SUMMARY_FILE}}` at `prompt-template/claude/review-phase-prompt.md:34-40`. It also disables the ordinary completion escape hatch: COMPLETE has no effect in this phase, and Codex review must be rerun until no priority issue markers are found, per `prompt-template/claude/review-phase-prompt.md:53-58`.
- inputs_outputs_state: Inputs are templated variables supplied by the stop hook: `PLAN_FILE`, `GOAL_TRACKER_FILE`, `ROUND_CONTRACT_FILE`, `REVIEW_CONTENT`, `SUMMARY_FILE`, plus currently passed but unused values such as `BITLESSON_FILE` and `CURRENT_ROUND`. The direct caller is `continue_review_loop_with_issues()` in `hooks/loop-codex-stop-hook.sh`, which renders this template through `load_and_render_safe "$TEMPLATE_DIR" "claude/review-phase-prompt.md"` at `hooks/loop-codex-stop-hook.sh:1513-1520`. The rendered prompt is written to `round-N-prompt.md` at `hooks/loop-codex-stop-hook.sh:1469-1471` and returned to Claude as the JSON block `reason` at `hooks/loop-codex-stop-hook.sh:1537-1545`. State transition into this template occurs when Codex review issues are found: `run_and_handle_code_review()` calls `continue_review_loop_with_issues` at `hooks/loop-codex-stop-hook.sh:1262-1274`. That function also updates `current_round` in `state.md` at `hooks/loop-codex-stop-hook.sh:1464-1467` and creates a review-round summary scaffold if absent at `hooks/loop-codex-stop-hook.sh:1470-1493`.
- gates_or_invariants: The template preserves the invariant that review findings do not automatically replace the mainline objective, stated at `prompt-template/claude/review-phase-prompt.md:12`. It gates action on re-anchoring to the original plan, current goal tracker, and current round contract before touching code at `prompt-template/claude/review-phase-prompt.md:5-11`. It requires blocking-vs-queued classification before acting at `prompt-template/claude/review-phase-prompt.md:18-24`, prevents review work from expanding into unrelated feature work at `prompt-template/claude/review-phase-prompt.md:36-39`, and requires summary evidence that fixed, queued, unresolved, and tracker-reconciliation states are captured at `prompt-template/claude/review-phase-prompt.md:42-51`. The broader review gate is implemented by `detect_review_issues()`, which scans the last 50 lines of the Codex review log for priority markers in the first 10 characters of a line, extracts from the first issue line to the end, and returns different statuses for issues found, no issues, and missing or empty output at `hooks/lib/loop-common.sh:720-788`.
- dependencies_and_callers: This template depends on the shared template renderer in `hooks/lib/template-loader.sh`. Rendering is single-pass to prevent placeholder injection from values such as review content, documented at `hooks/lib/template-loader.sh:48-56` and implemented at `hooks/lib/template-loader.sh:69-135`. Missing or empty template files fall back through `load_and_render_safe()` at `hooks/lib/template-loader.sh:185-211`; the stop hook includes an inline fallback at `hooks/loop-codex-stop-hook.sh:1496-1511`. Upstream, `run_codex_code_review()` invokes `codex review --base <review_base>` and stores the log at `hooks/loop-codex-stop-hook.sh:1171-1235`; `run_and_handle_code_review()` routes failures, issue output, and clean review completion at `hooks/loop-codex-stop-hook.sh:1250-1278`. Review phase entry is triggered after implementation COMPLETE when a base branch exists, with `review_started=true` and `.review-phase-started` written at `hooks/loop-codex-stop-hook.sh:1826-1872`. Once in review phase, every exit attempt reruns Codex review and requires the marker file to exist, at `hooks/loop-codex-stop-hook.sh:1878-1907`.
- edge_cases_or_failure_modes: If `codex review` fails or produces no usable output, the system does not render this remediation prompt as a success path; it blocks through `block_review_failure()` instead, as routed at `hooks/loop-codex-stop-hook.sh:1256-1271` and implemented beginning at `hooks/loop-codex-stop-hook.sh:1548`. If the template is missing or empty, the fallback prompt still instructs re-anchoring, blocking-only fixes, commit, and summary writing at `hooks/loop-codex-stop-hook.sh:1496-1511`. If review state is manually toggled without the `.review-phase-started` marker, the hook blocks as inconsistent state at `hooks/loop-codex-stop-hook.sh:1884-1900`. If review output contains issue markers outside the detector’s last-50-line window, or not near the start of a line, it can be treated as no issues by design; this reduces false positives but is a detection boundary. The template itself does not include shell validation or path safety; it relies on surrounding hooks and validators for write restrictions, rendering safety, and review reruns.
- validation_or_tests: `tests/test-codex-review-merge.sh` validates the issue-detection algorithm: marker position, last-50-lines scan window, extraction from first issue line, missing/empty log hard errors, result-file creation, and exactly-50-line behavior are described and tested at `tests/test-codex-review-merge.sh:5-16` and `tests/test-codex-review-merge.sh:57-354`. `tests/test-agent-teams.sh:717-730` verifies that review-phase prompts are generated by `continue_review_loop_with_issues()`, contain review issues, and do not append agent-teams continuation text. `tests/test-finalize-phase.sh:630-688` checks that Codex review failure blocks exit and preserves review state, while `tests/test-finalize-phase.sh:691-724` checks that empty review output blocks instead of finalizing. Template rendering behavior is covered by `tests/test-template-loader.sh` and broader references in `tests/test-templates-comprehensive.sh`; the core renderer behavior is documented in `hooks/lib/template-loader.sh:7-20`. I attempted `git status --short` as a final cleanliness check, but this read-only branch export has no accessible `.git` metadata and the command also hit sandbox cache-write restrictions, so no git cleanliness result was available.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 of 1 assigned item sections present; item id appears in the Item Evidence heading only`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`