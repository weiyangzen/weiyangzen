# agent_101 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-101 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/check-todos-from-transcript.py:1), the hook-side task completion gate used to decide whether a Claude/Codex session may proceed or must be blocked because unfinished work remains. The test file itself is not the production algorithm, but it defines the expected behavior of the checker across stdin hook payload parsing, legacy `TodoWrite` transcript parsing, file-based task-state parsing, lane routing, and failure output. It resolves the checker from its own location via `SCRIPT_DIR`, `PROJECT_ROOT`, and `TODO_CHECKER` at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-todo-checker.sh:12).

- algorithmic_behavior: The script is a Bash harness with `set -euo pipefail` at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-todo-checker.sh:10), local pass/fail counters at lines 21-34, and isolated fixture generation under `mktemp -d` at lines 37-39. Each test temporarily disables `set -e` around an invocation of `python3 "$TODO_CHECKER"` so it can assert exit codes without terminating the suite. The tested algorithm has two extraction paths: legacy transcript JSONL `TodoWrite` calls and file-based task JSON under a per-session tasks directory. In the implementation, transcript entries are normalized by `extract_tool_calls_from_entry()` for assistant/message/direct tool-use shapes at [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/check-todos-from-transcript.py:38), then `find_incomplete_todos_from_transcript()` keeps only the latest non-empty `TodoWrite.todos` list at lines 82-103. File-based tasks are scanned by `find_incomplete_tasks_from_directory()` at line 123, which treats task JSON files as the authoritative task-state source when `session_id` exists.

- inputs_outputs_state: Inputs are JSON hook payloads passed on stdin, plus optional filesystem fixtures referenced by `transcript_path`, `session_id`, and `tasks_base_dir`. The test covers invalid JSON input, empty stdin, missing `transcript_path`, nonexistent transcript files, JSONL transcripts, and task JSON files. Expected outputs are process exit codes and selected stdout/stderr markers. Exit `2` is required for invalid hook JSON at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-todo-checker.sh:52), exit `0` allows continuation when there is no actionable task state at lines 64-98, and exit `1` blocks when incomplete blocking/mainline items remain at lines 122-158. The checker prints `INCOMPLETE_TODOS` plus formatted item lines before exit `1` at [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/check-todos-from-transcript.py:218). Harness state transitions are `TESTS_PASSED` and `TESTS_FAILED`; final suite status exits `0` only when no tests failed at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-todo-checker.sh:548).

- gates_or_invariants: The core gate invariant is conservative: anything not explicitly complete is incomplete unless it is explicitly deleted or explicitly queued. Legacy todos block when `status != "completed"` at [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/check-todos-from-transcript.py:109); file tasks block when `status` is not `completed` or `deleted` at line 150. Missing task status defaults to `pending` in the implementation at line 149 and is asserted by the file-task test at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-todo-checker.sh:339). Lane classification only honors a prefix matching `[mainline]`, `[blocking]`, or `[queued]`; inline lane-looking text must not downgrade a blocker. That invariant is tested for legacy `TodoWrite` content at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-todo-checker.sh:175) and file-task descriptions at line 424, matching `LANE_PREFIX_PATTERN` in [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/check-todos-from-transcript.py:24). Queued tasks are intentionally non-blocking and skipped in both paths: legacy at lines 110-112 and file tasks at lines 156-158.

- dependencies_and_callers: This file directly depends on `bash`, `mktemp`, `python3`, `grep`, temporary filesystem writes, and [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/check-todos-from-transcript.py:1). It constructs transcript JSONL fixtures and task JSON fixtures inline, so it has no external test fixture dependency. The checker is configured in hook surfaces: [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/hooks.json:63) defines Stop hooks, and the repo also has Codex hook config in [config/codex-hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/config/codex-hooks.json:3). A repository search found this test as the direct local specification for `check-todos-from-transcript.py`; production hook orchestration appears elsewhere in stop-hook scripts, while this test targets only the task-completion checker. Path setup uses `cd "$(dirname "${BASH_SOURCE[0]}")" && pwd` rather than a canonicalizing `realpath` call at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-todo-checker.sh:12), so this file is relevant to path-resolution behavior insofar as it locates the checker from the test script path.

- edge_cases_or_failure_modes: The test covers invalid stdin JSON as parse error exit `2` at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-todo-checker.sh:52), empty stdin as allow/exit `0` at line 64, missing or nonexistent transcript path as allow/exit `0` at lines 76 and 88, empty transcript files at line 197, invalid JSONL transcript lines being ignored at line 210, multiple `TodoWrite` calls using the latest list at line 227, direct `tool_use` entry format at line 243, and alternative `type: message` transcript format at line 258. Task-file edge cases include no status defaulting to pending at line 339, completed status allowing at line 356, `in_progress` blocking at line 373, queued prefix not blocking at line 390, explicit blocking prefix blocking at line 407, multiple tasks with one incomplete blocking at line 441, all completed allowing at line 461, deleted tasks ignored at line 481, mixed transcript and file-task inputs being additive at line 498, and nonexistent session directories allowing at line 526. Failure mode not fully asserted: malformed task JSON files are skipped by the implementation at [hooks/check-todos-from-transcript.py](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/check-todos-from-transcript.py:166), but this specific test file does not create a malformed task JSON fixture.

- validation_or_tests: The script contains 25 named tests grouped into input handling, legacy todo detection, transcript format variations, generic edge cases, and file-based task-system behavior. It validates both exit status and selected output content, including incomplete todo names at [tests/test-todo-checker.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-todo-checker.sh:137), `[blocking]` lane output for inline queued text at line 184, and `Task #` output for file-based task IDs at line 518. I attempted to execute `bash tests/test-todo-checker.sh`, but this read-only branch export cannot create the `mktemp` directory under the system temp location, so execution stopped immediately with `mktemp: mkdtemp failed ... Operation not permitted`. I also attempted `git rev-parse HEAD` and `git status --short`; both failed because this branch export is not a live git checkout in the sandbox and the environment could not create git/xcode cache files under `/tmp`. Static inspection was completed directly against the assigned file and the referenced checker implementation.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 item evidence section present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`