# agent_177 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-177 `file` `skills/ask-gemini/SKILL.md`
- cursor: `[_]`
- core_role: Defines the agent-facing workflow contract for the `ask-gemini` skill. The file is not executable itself, but it is core instruction glue: it constrains how an agent invokes `${CLAUDE_PLUGIN_ROOT}/scripts/ask-gemini.sh`, how free-form user text must be quoted, how optional flags are passed, and how returned stdout/stderr/exit codes are interpreted. The front matter names the skill and allowlists only `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/ask-gemini.sh:*)`, making the wrapper script the sole intended execution path (`skills/ask-gemini/SKILL.md:1-5`).
- algorithmic_behavior: The skill’s workflow is: receive a question/task and optional `--gemini-model` / `--gemini-timeout` flags, invoke the wrapper script with the free-form task as a single quoted shell argument, read Gemini’s stdout as the research answer, treat stderr as status/debug output, and report non-zero failures to the user (`skills/ask-gemini/SKILL.md:15-47`). Its most important algorithmic rule is shell argument preservation: plain task text is passed as `"$ARGUMENTS"`; when flags are present, the flags remain separate arguments and the remaining question is reconstructed as one quoted final argument (`skills/ask-gemini/SKILL.md:19-31`). It explicitly forbids unquoted `$ARGUMENTS` because the shell would re-parse metacharacters before the wrapper starts (`skills/ask-gemini/SKILL.md:33-39`).
- inputs_outputs_state: Inputs are agent-supplied arguments matching `[--gemini-model MODEL] [--gemini-timeout SECONDS] [question or task]` (`skills/ask-gemini/SKILL.md:4`). The delegated script parses options until the first non-option or `--`, joins remaining question parts, defaults to model `gemini-3.1-pro-preview` and timeout `3600`, then augments the prompt with a mandatory Google Search instruction (`scripts/ask-gemini.sh:39-43`, `scripts/ask-gemini.sh:88-139`, `scripts/ask-gemini.sh:236-241`). Outputs are Gemini’s response on stdout and status/debug information on stderr, as documented by both the skill and script (`skills/ask-gemini/SKILL.md:41-46`, `scripts/ask-gemini.sh:12-14`, `scripts/ask-gemini.sh:267-288`, `scripts/ask-gemini.sh:388`). Persistent state is written under the resolved project root as `.humanize/skill/<unique-id>/input.md`, `output.md`, and `metadata.md`, plus cache/debug files under `$XDG_CACHE_HOME` or `~/.cache/humanize/<sanitized-project>/skill-<unique-id>/gemini-run.{cmd,out,log}` with local fallback to `.humanize/skill/<unique-id>/cache` (`skills/ask-gemini/SKILL.md:57-61`, `scripts/ask-gemini.sh:16-18`, `scripts/ask-gemini.sh:184-199`, `scripts/ask-gemini.sh:205-218`, `scripts/ask-gemini.sh:247-261`, `scripts/ask-gemini.sh:368-380`).
- gates_or_invariants: The skill’s main invariant is no shell re-parsing of user task text: free-form content containing spaces or metacharacters must remain quoted (`skills/ask-gemini/SKILL.md:17-39`). The script adds runtime gates: `gemini` must exist in `PATH`; the question must be non-empty; `--gemini-model` and `--gemini-timeout` must have values; timeout must match numeric digits; model names may contain only alphanumeric characters, dot, underscore, or hyphen (`scripts/ask-gemini.sh:105-123`, `scripts/ask-gemini.sh:145-168`). Project state is rooted through `resolve_project_root`, which prefers `CLAUDE_PROJECT_DIR`, then `git rev-parse --show-toplevel`, and canonicalizes via `realpath`; failure to resolve a root is fatal for this script (`scripts/ask-gemini.sh:174-178`, `hooks/lib/project-root.sh:41-53`). The branch’s realpath-related invariant is relevant here because project-local `.humanize/skill` state and cache key generation are based on that canonical root, reducing symlink-prefix divergence (`hooks/lib/project-root.sh:14-20`, `scripts/ask-gemini.sh:188-194`).
- dependencies_and_callers: The skill depends directly on `scripts/ask-gemini.sh` through the allowed-tools command (`skills/ask-gemini/SKILL.md:5`, `skills/ask-gemini/SKILL.md:19-31`). The script sources `scripts/portable-timeout.sh` for `run_with_timeout` and `hooks/lib/project-root.sh` for canonical project-root resolution (`scripts/ask-gemini.sh:27-33`). Timeout behavior depends on `gtimeout`, GNU `timeout`, `python3`, `python`, or finally no timeout with a warning (`scripts/portable-timeout.sh:6-27`, `scripts/portable-timeout.sh:33-71`). The actual external execution dependency is Gemini CLI invoked as `gemini "${GEMINI_ARGS[@]}" -p "$AUGMENTED_PROMPT"` (`scripts/ask-gemini.sh:224-234`, `scripts/ask-gemini.sh:282-283`). User-facing documentation advertises `/humanize:ask-gemini` for deep web research and `humanize monitor gemini` for monitoring (`README.md:59-70`). Monitoring support reads `.humanize/skill`, recognizes `tool: gemini` from metadata or `- Tool: gemini` from input, and chooses `gemini-run.log` / `gemini-run.out` cache files for live or completed invocations (`scripts/lib/monitor-skill.sh:43-64`, `scripts/lib/monitor-skill.sh:165-199`). The `humanize` shell dispatcher exposes `monitor gemini` as `_humanize_monitor_skill --tool-filter gemini` (`scripts/humanize.sh:1203-1210`).
- edge_cases_or_failure_modes: Unquoted user input can fail before the script starts or cause shell metacharacter interpretation, which is why the skill forbids the unsafe form (`skills/ask-gemini/SKILL.md:17-39`). Invalid or missing flags exit with validation code `1`; missing Gemini CLI exits `1`; empty question exits `1`; invalid model characters exit `1`; unresolved project root exits `1` (`scripts/ask-gemini.sh:105-168`, `scripts/ask-gemini.sh:174-178`). Timeout returns `124` and writes timeout metadata while suggesting a doubled timeout (`skills/ask-gemini/SKILL.md:50-55`, `scripts/ask-gemini.sh:294-314`). Other Gemini failures preserve stderr tail and exit with Gemini’s code (`scripts/ask-gemini.sh:316-338`). A zero exit with empty stdout is treated as failure and converted to exit `1` with `status: empty_response` metadata (`scripts/ask-gemini.sh:340-362`). If home cache creation fails, the script falls back to a project-local cache and warns (`scripts/ask-gemini.sh:195-199`). If no timeout implementation exists, the timeout helper runs without enforcing the configured timeout, which weakens the skill’s timeout guarantee (`scripts/portable-timeout.sh:64-68`). With `HUMANIZE_GEMINI_YOLO=true` or `1`, the script uses Gemini `--yolo`; otherwise it uses `--sandbox`, so environment can materially change Gemini tool approval behavior (`scripts/ask-gemini.sh:226-231`).
- validation_or_tests: No dedicated `test-ask-gemini.sh` was found in the inspected references. Repository search shows direct tests for the analogous `ask-codex` skill quoting rules, but not for this Gemini skill. The closest validation is structural/runtime validation inside `scripts/ask-gemini.sh`: option parsing, numeric timeout validation, model-character validation, prerequisite checks, root resolution, timeout/error/empty-response handling, and metadata emission (`scripts/ask-gemini.sh:88-168`, `scripts/ask-gemini.sh:174-178`, `scripts/ask-gemini.sh:294-380`). Monitor compatibility is indirectly supported by `tool: gemini` metadata and `- Tool: gemini` input fields written by the script, which `scripts/lib/monitor-skill.sh` knows how to read (`scripts/ask-gemini.sh:205-218`, `scripts/ask-gemini.sh:370-380`, `scripts/lib/monitor-skill.sh:50-64`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence section present for the single assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`