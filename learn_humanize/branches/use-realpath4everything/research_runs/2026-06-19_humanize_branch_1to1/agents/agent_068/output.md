# agent_068 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-068 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for the Bash write-detection algorithm used by RLCR hook validation. The file directly sources `hooks/lib/loop-common.sh` and tests `command_modifies_file`, the shared matcher used by `hooks/loop-bash-validator.sh` to decide whether a Bash command attempts to mutate protected loop artifacts. It is core-adjacent test coverage rather than production logic, but it is not a skip candidate because it defines the expected behavior matrix for the file-modification recognizer.
- algorithmic_behavior: The script lowercases each candidate command with `to_lower` before invoking `command_modifies_file`, matching production behavior in `loop-bash-validator.sh`. It defines two assertion paths: `assert_modifies` expects a regex hit and `assert_not_modifies` expects no hit. The covered mutation classes are output redirection and append redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp` destinations, `rm`, `dd of=...`, `truncate`, `exec fd>file`, and `printf ... > file`. It also asserts read-only operations are ignored, including `cat`, `grep`, `head`, `tail`, `wc`, `less`, `echo`, `ls`, `file`, `stat`, and `diff`.
- inputs_outputs_state: Inputs are static Bash command strings plus an optional target file regex, defaulting to `goal-tracker\.md`. The script has no filesystem fixture mutation beyond sourcing project code; it mutates only in-memory counters `TESTS_PASSED` and `TESTS_FAILED`. Outputs are colored PASS/FAIL lines and a final process exit code: `0` when all assertions pass, `1` when any assertion fails. Key setup is at `tests/test-bash-validator-patterns.sh:11-13`; counters are initialized at `tests/test-bash-validator-patterns.sh:20-21`; assertion helpers are at `tests/test-bash-validator-patterns.sh:36-62`; final exit gating is at `tests/test-bash-validator-patterns.sh:206-213`.
- gates_or_invariants: The invariant is that mutation detection must be broad enough to block common Bash write bypasses but narrow enough not to block read-only inspection. Case-insensitivity is tested by passing an uppercase command and relying on `to_lower` before matching. Regex parameterization is also an invariant: the same helper must detect protected names beyond `goal-tracker.md`, including `state.md` and `round-[0-9]+-summary.md`. The production matcher lives at `hooks/lib/loop-common.sh:1482-1510`; its pattern list is the direct target of this test. Production use in the Bash hook includes blocking state files at `hooks/loop-bash-validator.sh:253-279`, plan backup files at `hooks/loop-bash-validator.sh:478-483`, goal tracker writes at `hooks/loop-bash-validator.sh:491-499`, prompt files at `hooks/loop-bash-validator.sh:506-509`, summary files at `hooks/loop-bash-validator.sh:516-520`, contract files at `hooks/loop-bash-validator.sh:528-537`, and todos files at `hooks/loop-bash-validator.sh:543-551`.
- dependencies_and_callers: This test depends on Bash, `grep`, `tr`, and the repository’s `hooks/lib/loop-common.sh`. `loop-common.sh` in turn sources `hooks/lib/project-root.sh`, which canonicalizes project roots through `realpath` and exposes `canonicalize_path`; that is relevant to this branch’s realpath theme but not directly exercised by this file’s pure string assertions. The production caller is `hooks/loop-bash-validator.sh`, which lowercases `tool_input.command` at `hooks/loop-bash-validator.sh:45-46` and then uses `command_modifies_file` for protected-file gates. The aggregate test runner includes this script in its suite list at `tests/run-all-tests.sh:59-63`.
- edge_cases_or_failure_modes: The script intentionally documents a known false negative: multi-source copy such as `cp file1.md file2.md goal-tracker.md` is not detected because the production regex models `cp src dest`, noted at `tests/test-bash-validator-patterns.sh:165-168`. Variable-indirect targets such as `echo x > $FILE` are expected not to match because the detector only sees literal protected filenames. The matcher is regex and token-pattern based rather than a shell parser, so quoting, command substitution, globbing, and complex shell grammar are only covered where explicit regexes exist elsewhere. The focused file covers simple redirection adjacency such as `>goal-tracker.md`, but not symlink/canonical path behavior, which is handled by other validation paths and `project-root.sh`.
- validation_or_tests: I inspected the assigned file directly and ran `bash tests/test-bash-validator-patterns.sh` from the branch export. Result: 48 passed, 0 failed, exit code 0. The executable bit is present on the assigned file. The covered test groups are redirection at `tests/test-bash-validator-patterns.sh:69-80`, `tee` at `tests/test-bash-validator-patterns.sh:82-94`, in-place editors at `tests/test-bash-validator-patterns.sh:96-107`, file operations at `tests/test-bash-validator-patterns.sh:109-121`, other modifiers at `tests/test-bash-validator-patterns.sh:123-133`, false positives at `tests/test-bash-validator-patterns.sh:135-152`, edge cases at `tests/test-bash-validator-patterns.sh:154-172`, state file patterns at `tests/test-bash-validator-patterns.sh:174-183`, and summary patterns at `tests/test-bash-validator-patterns.sh:185-194`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 assigned item section present
- missing_items: none
- duplicate_items: none; the assigned item_id string appears once in this output
- final_worker_status: `complete`