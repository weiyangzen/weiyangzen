# agent_083 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-083 `file` `tests/test-model-router.sh`
- cursor: `[_]`
- core_role: Executable shell specification for the shared model-provider routing helpers in `scripts/lib/model-router.sh`. It verifies how model names are classified into supported providers, how provider CLI dependencies are checked, and how reasoning effort values are normalized across providers. The test is part of the broader suite via `tests/run-all-tests.sh:98`.
- algorithmic_behavior: The test loads `tests/test-helpers.sh` and `scripts/lib/model-router.sh` directly at `tests/test-model-router.sh:5-8`, then exercises three exported helper functions. `detect_provider` is specified to route `gpt-*` and `o[0-9]*` model names to `codex` (`tests/test-model-router.sh:29-111`) and Claude-family names containing `haiku`, `sonnet`, `opus`, or starting with `claude-` to `claude` (`tests/test-model-router.sh:113-196`). Unknown and empty model names must fail nonzero with useful stderr (`tests/test-model-router.sh:198-230`). `check_provider_dependency` is specified against isolated mock PATHs: mock `codex` and `claude` binaries must satisfy dependency checks, while a safe system-only PATH must fail and mention the missing provider (`tests/test-model-router.sh:232-298`). `map_effort` is specified to translate `xhigh` to `high` only for Claude and emit an info log (`tests/test-model-router.sh:300-318`), while valid efforts pass through for Codex or Claude (`tests/test-model-router.sh:320-386`) and invalid efforts fail (`tests/test-model-router.sh:388-420`).
- inputs_outputs_state: Inputs are literal model names, provider names, effort names, and PATH values constructed by the test. The test sets `SAFE_BASE_PATH=/usr/bin:/bin:/usr/sbin:/sbin` at `tests/test-model-router.sh:10` to suppress ambient provider binaries during negative dependency checks. It creates temporary mock executables with `create_mock_binary` at `tests/test-model-router.sh:17-27`, backed by `setup_test_dir` from `tests/test-helpers.sh`. Function outputs are captured through command substitution into `result`, `stderr_out`, and `exit_code` variables; successful provider detection outputs exactly `codex` or `claude`, successful effort mapping outputs the mapped effort, and failures are expected to return nonzero with diagnostic stderr. The only durable state transitions are the shared test counters incremented by `pass`/`fail` from `tests/test-helpers.sh`; temporary directories are registered for cleanup by that helper. The test ends by delegating process success/failure to `print_test_summary` at `tests/test-model-router.sh:426`, which returns nonzero if any assertion failed.
- gates_or_invariants: `set -euo pipefail` at `tests/test-model-router.sh:3` makes unexpected unset variables or command failures fatal outside guarded assertions. Each assertion explicitly resets `result`, `exit_code`, and sometimes `stderr_out` before invoking the helper under test, preventing stale result reuse. The routing invariant is prefix/pattern based: Codex accepts `gpt-*` and `o` followed by digits, while Claude accepts case-insensitive Claude family terms. The dependency invariant is provider-to-binary identity: `codex` requires `codex`; `claude` requires `claude`. The effort invariant is that accepted effort vocabulary is only `xhigh`, `high`, `medium`, and `low`; Claude cannot receive `xhigh` unchanged and must receive `high` instead. Negative tests assert both failure status and human-readable error content rather than status alone.
- dependencies_and_callers: Direct dependencies are `tests/test-helpers.sh` for `pass`, `fail`, `setup_test_dir`, and `print_test_summary`, and `scripts/lib/model-router.sh` for the tested functions. The implementation under test contains a source guard at `scripts/lib/model-router.sh:6-8`, provider detection at `scripts/lib/model-router.sh:10-30`, dependency checks at `scripts/lib/model-router.sh:32-60`, and effort mapping at `scripts/lib/model-router.sh:62-91`. The live caller found in this branch is `scripts/bitlesson-select.sh`, which sources the router at `scripts/bitlesson-select.sh:9-12`, resolves `BITLESSON_MODEL` and fallback model config at `scripts/bitlesson-select.sh:20-26`, detects the provider at `scripts/bitlesson-select.sh:111-120`, performs dependency check and Codex fallback at `scripts/bitlesson-select.sh:122-131`, then dispatches to Codex or Claude execution paths in `run_selector` at `scripts/bitlesson-select.sh:188-220`.
- edge_cases_or_failure_modes: Empty model input is a hard validation error (`tests/test-model-router.sh:215-230`; implementation at `scripts/lib/model-router.sh:13-16`). Unknown model names fail with an error explaining accepted families (`tests/test-model-router.sh:198-213`; implementation at `scripts/lib/model-router.sh:28-29`). Missing provider binaries fail even if the provider name is otherwise valid (`tests/test-model-router.sh:249-298`; implementation at `scripts/lib/model-router.sh:49-59`). Invalid effort values such as `ultra` fail for both Claude and Codex (`tests/test-model-router.sh:388-420`; implementation at `scripts/lib/model-router.sh:75-81`). A subtle covered edge is Claude model matching case-insensitivity through `claude-3-OPUS-20240229` (`tests/test-model-router.sh:181-196`), backed by `grep -qiE` in `scripts/lib/model-router.sh:23`. A test hygiene issue exists: the numbering labels repeat `Test 10` for empty model and Codex dependency sections (`tests/test-model-router.sh:215-236`), but this is presentation-only and does not merge the assertions.
- validation_or_tests: This file is itself the validation asset for model routing. It is also registered in the aggregate runner at `tests/run-all-tests.sh:98`. I inspected the test and the implementation/caller paths directly; I did not execute the script because the assignment asked for read-only research notes, and the test creates temporary mock binaries even though it does not intend to modify repository files.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section present, matching the assigned checklist row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`