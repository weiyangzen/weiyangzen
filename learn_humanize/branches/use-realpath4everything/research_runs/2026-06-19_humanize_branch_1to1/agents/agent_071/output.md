# agent_071 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-071 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: This file is an executable specification for the RLCR cancel-signal authorization path. Its central contract is stated at `tests/test-cancel-signal-file.sh:3`: it tests the `cancel-rlcr-loop` signal file mechanism, where the only allowed direct state-file mutation is moving the active loop state file to `cancel-state.md` when the active loop directory contains `.cancel-requested`. It is core algorithm evidence because it drives both the public hook entry point, `hooks/loop-bash-validator.sh`, and the lower-level authorization helper, `is_cancel_authorized`, with adversarial command strings and canonicalization regressions.

- algorithmic_behavior: The test builds a temporary active loop in `setup_test_loop` at `tests/test-cancel-signal-file.sh:42`, creates `.humanize/rlcr/2024-01-01_12-00-00/state.md` with loop frontmatter at `tests/test-cancel-signal-file.sh:47`, and exports `CLAUDE_PROJECT_DIR` to force hook project-root resolution toward the temp tree at `tests/test-cancel-signal-file.sh:58`. `run_bash_validator` wraps a shell command in hook JSON using `jq` and pipes it to `hooks/loop-bash-validator.sh` at `tests/test-cancel-signal-file.sh:63`. The expected algorithm is: detect the active loop, identify commands touching `state.md`, delegate possible cancel moves to `is_cancel_authorized`, allow exactly the authorized rename, and block all other state-file edits or exfiltration attempts. The positive path is first proven by a direct `mv <loop>/state.md <loop>/cancel-state.md` with `.cancel-requested` present at `tests/test-cancel-signal-file.sh:80`, and by `./` path normalization at `tests/test-cancel-signal-file.sh:100`. The test then expands to quoted paths, literal `${loop_dir}` substitution, command splitting, shell wrappers, redirection prefixes, unrelated paths, and symlink canonicalization.

- inputs_outputs_state: Inputs are Bash command strings, a synthetic hook JSON envelope, the temporary `CLAUDE_PROJECT_DIR`, the active loop directory, `state.md`, and optional `.cancel-requested`. The validator path returns shell exit status `0` for allowed commands and status `2` for policy blocks; the test increments `TESTS_PASSED` or `TESTS_FAILED` via `pass` and `fail` at `tests/test-cancel-signal-file.sh:25`. The final output is a terminal summary and process exit equal to the failure count at `tests/test-cancel-signal-file.sh:1425`. The primary state transition being specified is active-loop `state.md` to terminal `cancel-state.md`, but the test does not execute the `mv`; it validates whether the hook would allow or block that command. An active loop exists only when `state.md` is present; the no-active-loop case explicitly creates the directory without `state.md` and expects validator exit `0` at `tests/test-cancel-signal-file.sh:1233`.

- gates_or_invariants: The cancel gate requires the signal file in the exact active loop directory, shown by the allowed signal case at `tests/test-cancel-signal-file.sh:81` and wrong-directory signal rejection at `tests/test-cancel-signal-file.sh:201`. It requires the command to be a bare `mv` with exactly two arguments: an allowed active state source and `<loop>/cancel-state.md`; wrong destination is rejected at `tests/test-cancel-signal-file.sh:181`, arbitrary destination at `tests/test-cancel-signal-file.sh:283`, and extra arguments at `tests/test-cancel-signal-file.sh:426`. It forbids other state mutations even with a signal: redirect write at `tests/test-cancel-signal-file.sh:141`, `sed -i` at `tests/test-cancel-signal-file.sh:161`, and `rm` at `tests/test-cancel-signal-file.sh:223`. It also forbids injection or wrapper forms: `&&` at `tests/test-cancel-signal-file.sh:243`, semicolon at `tests/test-cancel-signal-file.sh:263`, command substitution at `tests/test-cancel-signal-file.sh:303`, backticks at `tests/test-cancel-signal-file.sh:323`, newline injection at `tests/test-cancel-signal-file.sh:345`, `sudo` and `env` prefixes at `tests/test-cancel-signal-file.sh:552` and `tests/test-cancel-signal-file.sh:592`, chained operators from `tests/test-cancel-signal-file.sh:652` through `tests/test-cancel-signal-file.sh:795`, and `sh -c`/`bash -c` at `tests/test-cancel-signal-file.sh:812` and `tests/test-cancel-signal-file.sh:832`. The branch-specific realpath invariant is that symlinked parent prefixes should authorize when they resolve to the active loop, but leaf symlink aliases must not authorize; this is covered at `tests/test-cancel-signal-file.sh:1345`, `tests/test-cancel-signal-file.sh:1376`, and `tests/test-cancel-signal-file.sh:1400`.

- dependencies_and_callers: The file sources `hooks/lib/loop-common.sh` directly at `tests/test-cancel-signal-file.sh:33`, depending on `to_lower` and `is_cancel_authorized`. It calls `hooks/loop-bash-validator.sh` as the production hook boundary at `tests/test-cancel-signal-file.sh:72`. The validator obtains the Bash command from JSON at `hooks/loop-bash-validator.sh:45`, resolves the project root through `resolve_project_root`, finds the active RLCR loop, and applies state-file protection when `ACTIVE_LOOP_DIR` exists at `hooks/loop-bash-validator.sh:239`. The state-file gate first checks destination-style modifications via `command_modifies_file`, then calls `is_cancel_authorized` as the sole exception path at `hooks/loop-bash-validator.sh:271`. It separately scans `mv`/`cp` source use to catch exfiltration like `mv state.md /tmp/foo` at `hooks/loop-bash-validator.sh:281`, strips shell operator segments and leading redirections at `hooks/loop-bash-validator.sh:292`, and applies the same cancel exception before blocking at `hooks/loop-bash-validator.sh:430`. Shell wrapper bypasses are handled at `hooks/loop-bash-validator.sh:440`. Realpath behavior comes from `hooks/lib/project-root.sh`: `resolve_project_root` canonicalizes the root at `hooks/lib/project-root.sh:41`, `canonicalize_path_prefix` resolves only the parent directory and preserves the basename at `hooks/lib/project-root.sh:55`, and `canonicalize_path` warns not to use full leaf-dereferencing for user-supplied filename authorization at `hooks/lib/project-root.sh:98`.

- edge_cases_or_failure_modes: The file specifically guards against missing signal files, wrong signal directory, wrong destination, arbitrary destination, extra args before or after `state.md`, option/prefix tricks, command substitution, hidden `${IFS}` style variables, command chaining, background operators, pipe variants, shell wrappers, redirection-prefixed commands, quoted redirection targets, escaped-space redirection targets, ANSI-C quoted redirection targets, unrelated `/tmp/state.md`, and absent active loop. The helper-specific tests at `tests/test-cancel-signal-file.sh:1259` validate direct return behavior for correct command, missing signal, wrong command, three args, quoted paths, extra args before source, and hidden variables. The realpath regressions are the most relevant to this branch: `tests/test-cancel-signal-file.sh:1345` simulates a symlinked path prefix and expects authorization after canonicalization; `tests/test-cancel-signal-file.sh:1376` rejects a destination symlink alias because dereferencing the leaf would let `mv` corrupt state outside the loop; `tests/test-cancel-signal-file.sh:1400` rejects a source symlink alias as defense in depth. In production, `is_cancel_authorized` implements those gates by requiring `.cancel-requested` at `hooks/lib/loop-common.sh:1055`, rejecting substitutions/operators at `hooks/lib/loop-common.sh:1062`, normalizing literal loop-dir variables at `hooks/lib/loop-common.sh:1098`, enforcing leading bare `mv` at `hooks/lib/loop-common.sh:1106`, parsing exactly two args at `hooks/lib/loop-common.sh:1149`, canonicalizing source and destination with `canonicalize_path_prefix` at `hooks/lib/loop-common.sh:1207` and `hooks/lib/loop-common.sh:1224`, and rejecting source symlinks at `hooks/lib/loop-common.sh:1245`.

- validation_or_tests: This file is itself the validation asset. It is executable Bash, 1,433 lines, and includes 53 validator-level positive/negative cases plus 10 direct helper cases. It uses `jq` to avoid malformed JSON when commands include shell metacharacters at `tests/test-cancel-signal-file.sh:61`. It expects block status `2` consistently for policy violations and status `0` for authorized cancel or no active loop. I did not execute the test because the assignment requested research notes only and the branch export is read-only; inspection was direct and line-referenced. A read-only `git rev-parse HEAD` check also could not confirm repository metadata because this export does not present as a normal git checkout in the sandbox.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-071` appears exactly once as an item heading.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`