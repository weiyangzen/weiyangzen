# agent_129 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-129 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: This file is the stop-hook prompt contract for the large-file gate. It does not perform detection itself; it defines the blocking message and remediation instructions emitted when the hook finds staged/tracked changed code or documentation files over the configured line limit. The template title and opening block are in [prompt-template/block/large-files.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/prompt-template/block/large-files.md:1), with two required placeholders: `{{MAX_LINES}}` and `{{LARGE_FILES}}`.
- algorithmic_behavior: The executable behavior is in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:518). The hook sets `MAX_LINES=2000`, reads cached `git status --porcelain`, normalizes renamed paths, resolves them against `PROJECT_ROOT`, classifies supported code/doc extensions, counts lines, and appends over-limit files into `LARGE_FILES`. If `LARGE_FILES` is non-empty, the hook renders this template through `load_and_render_safe` and returns JSON with `"decision": "block"` plus a system message, then exits successfully from the hook process while blocking the agent stop attempt. The template’s sections encode the transition the agent must perform next: split code files into modules, split documentation into logical sections, preserve behavior or narrative flow, update references/navigation, commit, and retry exit.
- inputs_outputs_state: Inputs are `MAX_LINES` and `LARGE_FILES`, passed from [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:590). `LARGE_FILES` entries are generated as markdown bullets containing an absolute resolved file path, line count, and type label at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:576). Output is rendered markdown used as the JSON `reason` field at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:594). The relevant state transition is external: a stop attempt changes into a blocked continuation state until the oversized changed files are split and committed. The template itself has no persistent state and no functions.
- gates_or_invariants: The gate only runs when `GIT_IS_REPO` is true, and it inspects the cached porcelain status rather than the full tree. Only existing files are considered; deleted paths are skipped. Supported code extensions include `py`, `js`, `ts`, `tsx`, `jsx`, `java`, C/C++ headers/sources, `cs`, `go`, `rs`, `rb`, `php`, `swift`, Kotlin/Scala, and shell variants; documentation extensions include `md`, `rst`, `txt`, `adoc`, and `asciidoc` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:553). The threshold comparison is strict greater-than, so files with exactly `MAX_LINES` lines pass, while files with `MAX_LINES + 1` lines block. The renderer invariant is uppercase double-brace placeholder syntax; the shared renderer documents that missing variables remain unchanged and substitution is single-pass in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:7).
- dependencies_and_callers: Direct caller is [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:590), which uses `load_and_render_safe`. Rendering is implemented in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:188); missing or empty templates fall back to an inline large-file message, preserving fail-closed blocking behavior. `TEMPLATE_DIR` is established through loop common/template-loader plumbing; the stop hook sources common loop functions early at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:42). The template also references the optional `code-simplifier` plugin at [prompt-template/block/large-files.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/prompt-template/block/large-files.md:16), but that is advisory remediation text, not a hard runtime dependency.
- edge_cases_or_failure_modes: If `git status` fails or times out, execution blocks earlier through the git-status failure gate, so this template is not rendered in that path. If a changed file has no recognized extension, it is ignored by the large-file detector. If `wc -l` fails or returns nonnumeric output, that file is skipped. Rename status lines are handled by keeping the destination path after ` -> `. Because `LARGE_FILES` uses `PROJECT_ROOT`-resolved paths, the rendered prompt may show absolute local paths instead of repository-relative paths. If this template is missing or empty, `load_and_render_safe` uses the fallback block defined in the hook, but the fallback is less detailed and omits the code/documentation split instructions and optional simplifier guidance.
- validation_or_tests: Broad template loading and nonempty checks cover all markdown templates in [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-templates-comprehensive.sh:90). Placeholder syntax validation checks for malformed double-brace variables, spaces, lowercase names, and brace mismatches in [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-templates-comprehensive.sh:118). Template reference tests verify critical validators use safe rendering patterns in [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-template-references.sh:172). I did not run tests because the branch export is read-only and `git rev-parse` also failed due the export not being a git checkout in this sandbox; inspection was performed directly with `nl`, `rg`, and `sed`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 item section above`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`