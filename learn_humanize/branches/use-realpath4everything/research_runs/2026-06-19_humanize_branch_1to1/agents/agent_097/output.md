# agent_097 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-097 `file` `tests/test-task-tag-routing.sh`
- cursor: `[_]`
- core_role: Executable regression specification for RLCR task-tag routing. It verifies that plan task tags flow into the initial round prompt, the goal tracker schema, and subsequent stop-hook follow-up prompts. The covered routing rule is: `coding` tasks stay with Claude, while `analyze` tasks must be delegated through `/humanize:ask-codex`.
- algorithmic_behavior: The test defines a mock `codex` executable that recognizes `exec` and `review` subcommands, emits deterministic text for each, and fails unsupported invocations; see [`tests/test-task-tag-routing.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-task-tag-routing.sh:25). It creates disposable git repositories with plan fixtures containing a `Tag (`coding`/`analyze`)` column, then invokes [`scripts/setup-rlcr-loop.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/setup-rlcr-loop.sh:1) to generate RLCR artifacts. The first scenario asserts that `round-0-prompt.md` contains `## Task Tag Routing (MUST FOLLOW)`, that the prompt mentions `/humanize:ask-codex`, and that `goal-tracker.md` has an Active Tasks table with `Tag` and `Owner` columns; see [`tests/test-task-tag-routing.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-task-tag-routing.sh:75). The second scenario builds a minimal active loop state and runs [`hooks/loop-codex-stop-hook.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:1), then asserts the generated round-1 prompt retains a `## Task Tag Routing Reminder` and `/humanize:ask-codex`; see [`tests/test-task-tag-routing.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-task-tag-routing.sh:121).
- inputs_outputs_state: Inputs are synthetic plan markdown, a temporary git repository, mock `codex`, environment variables `PATH`, `CLAUDE_PROJECT_DIR`, `XDG_CACHE_HOME`, and JSON hook input. The setup scenario writes plan content under `plans/plan.md`, ignores generated artifacts, initializes git through shared helpers, and runs the setup script from the repo root; see [`tests/test-task-tag-routing.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-task-tag-routing.sh:52). Outputs inspected by the test are generated files below `.humanize/rlcr`: `round-0-prompt.md`, `goal-tracker.md`, and `round-1-prompt.md`. The stop-hook fixture supplies `state.md`, copied `plan.md`, initialized `goal-tracker.md`, `round-0-contract.md`, and `round-0-summary.md`; these are sufficient to move the loop from round 0 to round 1 without relying on real Codex behavior. The hook mutates loop state conceptually by advancing `current_round` through `upsert_state_fields` and creating `round-1-summary.md` and `round-1-prompt.md`; see [`hooks/loop-codex-stop-hook.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:1952).
- gates_or_invariants: The test depends on `set -euo pipefail` to stop on unexpected command failures; see [`tests/test-task-tag-routing.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-task-tag-routing.sh:11). It treats routing text as a hard invariant via `grep -q` assertions rather than parsing markdown tables semantically. The setup script’s intended invariant is explicit in generated prompt text: every task has `coding` or `analyze`, `coding` is executed directly, `analyze` goes through `/humanize:ask-codex`, Goal Tracker `Tag`/`Owner` stay aligned, and missing tags default to `coding`; see [`scripts/setup-rlcr-loop.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/setup-rlcr-loop.sh:1335). The goal tracker schema invariant is the Active Tasks header `| Task | Target AC | Status | Tag | Owner | Notes |`; see [`scripts/setup-rlcr-loop.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/setup-rlcr-loop.sh:1128). The stop hook preserves routing across later rounds by appending a dedicated routing note after the standard next-round footer; see [`hooks/loop-codex-stop-hook.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:2122).
- dependencies_and_callers: Direct dependencies are [`tests/test-helpers.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:30) for `pass`, `fail`, summary reporting, temp directory setup, and git repo initialization; [`scripts/setup-rlcr-loop.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/setup-rlcr-loop.sh:26) for setup artifact generation; and [`hooks/loop-codex-stop-hook.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:42) for follow-up prompt generation. The setup script sources `portable-timeout.sh` and `hooks/lib/loop-common.sh`; the stop hook sources `loop-common.sh`, `portable-timeout.sh`, and `methodology-analysis.sh`. The test is included in the repository’s aggregate test runner at [`tests/run-all-tests.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/run-all-tests.sh:83). Related command documentation describes the same user-facing task-tag routing flow in [`commands/start-rlcr-loop.md`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/commands/start-rlcr-loop.md:119).
- edge_cases_or_failure_modes: The test is intentionally string-sensitive: wording or heading changes in generated prompts can fail even if equivalent routing semantics remain. It only validates presence of routing guidance and schema columns; it does not verify that the setup script actually parses each plan row into goal-tracker task rows with correct per-row `Tag` and `Owner` values. It also does not exercise malformed tags, missing tag columns, case variants, mixed whitespace, or plans without a task table. The mock `codex` avoids real model behavior, so failures here isolate prompt-generation regressions, not delegation execution failures. The stop-hook fixture pre-populates goal tracker and round contract files to bypass Round 0 initialization blockers; real loops can block earlier if placeholders remain in Ultimate Goal, Acceptance Criteria, or Active Tasks, as enforced in [`hooks/loop-codex-stop-hook.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:840).
- validation_or_tests: The file itself defines five checks: round-0 prompt has the routing section, round-0 prompt has `/humanize:ask-codex`, goal tracker Active Tasks table includes `Tag`/`Owner`, stop-hook follow-up prompt has the routing reminder, and stop-hook follow-up prompt has `/humanize:ask-codex`; see [`tests/test-task-tag-routing.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-task-tag-routing.sh:99) and [`tests/test-task-tag-routing.sh`](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-task-tag-routing.sh:227). I did not execute the test because the assignment requested research notes only and the workspace is read-only; inspection was direct with line-numbered reads.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence section present for the assigned file item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`