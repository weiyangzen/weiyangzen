# agent_112 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-112 `file` `prompt-template/block/bitlesson-delta-missing-notes.md`
- cursor: `[_]`
- core_role: This file is a small block prompt template used as the user-facing block reason when the BitLesson Delta validator finds an `add` or `update` action without meaningful `Notes:` text. The template is not executable itself, but it is part of the loop’s validation contract because it defines the exact remediation message for a failed BitLesson delta gate. See `prompt-template/block/bitlesson-delta-missing-notes.md:1` through `prompt-template/block/bitlesson-delta-missing-notes.md:5`.
- algorithmic_behavior: The template states that ``Action: {{ACTION}}`` requires a `Notes:` field explaining “what changed and why” and rejects empty or placeholder notes. At runtime, `scripts/bitlesson-validate-delta.sh` extracts the `## BitLesson Delta` section, parses `Action:`, `Lesson ID(s):`, and `Notes:`, then renders this template only when the action is not `none` and the notes field is blank or placeholder-like. The relevant gate is in `scripts/bitlesson-validate-delta.sh:281` through `scripts/bitlesson-validate-delta.sh:296`.
- inputs_outputs_state: Input variables are supplied by `load_and_render_safe`; this template consumes `ACTION`, substituted into `{{ACTION}}` at `prompt-template/block/bitlesson-delta-missing-notes.md:3`. The validator’s input sources are command-line arguments including `--summary-file`, `--bitlesson-file`, `--bitlesson-relpath`, `--allow-empty-none`, `--template-dir`, and `--current-round` in `scripts/bitlesson-validate-delta.sh:4` through `scripts/bitlesson-validate-delta.sh:13`. Its output on failure is JSON with `"decision": "block"`, `"reason": <rendered template>`, and `"systemMessage": <short diagnostic>`, emitted by `block_exit` in `scripts/bitlesson-validate-delta.sh:78` through `scripts/bitlesson-validate-delta.sh:89`. The template itself holds no persistent state.
- gates_or_invariants: The notes gate applies only inside the non-`none` branch for `Action: add` or `Action: update`; `Action: none` follows separate consistency checks in `scripts/bitlesson-validate-delta.sh:242` through `scripts/bitlesson-validate-delta.sh:267`. The invariant is that an add/update delta must include concrete lesson IDs, then a non-empty `Notes:` value that is not a whole-field placeholder matching bracket or angle-bracket syntax. The validator trims whitespace and uses `NOTES_PLACEHOLDER_REGEX='^(\[.*\]|<.*>)$'`, so values like `[what changed and why]` and `<what changed and why>` are blocked, while normal explanatory text passes. See `scripts/bitlesson-validate-delta.sh:269` through `scripts/bitlesson-validate-delta.sh:297`.
- dependencies_and_callers: The direct caller is `hooks/loop-codex-stop-hook.sh`, which runs `scripts/bitlesson-validate-delta.sh` for non-finalize rounds when `BITLESSON_REQUIRED` is true, then exits with the validator output if any block decision is returned. See `hooks/loop-codex-stop-hook.sh:823` through `hooks/loop-codex-stop-hook.sh:837`. Rendering depends on `hooks/lib/template-loader.sh`; its template syntax is documented as `{{VARIABLE_NAME}}` placeholders with single-pass substitution in `hooks/lib/template-loader.sh:7` through `hooks/lib/template-loader.sh:13`, and `load_and_render_safe` falls back if a template is missing or empty in `hooks/lib/template-loader.sh:185` through `hooks/lib/template-loader.sh:210`.
- edge_cases_or_failure_modes: If the `Notes:` field is whitespace-only, the extracted value becomes empty and this template is rendered as the block reason. If `Notes:` is a full bracketed or angle-bracket placeholder, it is also blocked. If `Notes:` contains normal prose, this specific gate passes and later gates validate file existence, ID syntax, and whether declared lesson IDs exist in the BitLesson file; those later checks appear in `scripts/bitlesson-validate-delta.sh:299` through `scripts/bitlesson-validate-delta.sh:384`. If this template file were missing or empty, `load_and_render_safe` would emit the hardcoded fallback with equivalent text from `scripts/bitlesson-validate-delta.sh:286` through `scripts/bitlesson-validate-delta.sh:292`.
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh` directly covers this behavior. It constructs a BitLesson file and summary fixtures, then asserts that `Action: add` with whitespace-only notes blocks, `Action: update` with `[what changed and why]` blocks, `Action: update` with `<what changed and why>` blocks, and valid explanatory notes pass. See `tests/test-bitlesson-validate-delta.sh:21` through `tests/test-bitlesson-validate-delta.sh:62` for fixtures, `tests/test-bitlesson-validate-delta.sh:75` through `tests/test-bitlesson-validate-delta.sh:84` for the notes-error assertion, and `tests/test-bitlesson-validate-delta.sh:101` through `tests/test-bitlesson-validate-delta.sh:147` for the concrete cases. Template loading robustness is also indirectly guarded by `load_and_render_safe` behavior in `hooks/lib/template-loader.sh:185` through `hooks/lib/template-loader.sh:210`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`