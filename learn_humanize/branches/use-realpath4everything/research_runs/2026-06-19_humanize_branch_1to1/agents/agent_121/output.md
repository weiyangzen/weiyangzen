# agent_121 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-121 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: This prompt block is the stop-hook failure contract for a dirty git working tree. It is not executable code, but it is core to the RLCR loop’s exit gate because `hooks/loop-codex-stop-hook.sh` renders it into the hook’s JSON `reason` when the loop tries to stop with staged, unstaged, or relevant untracked changes still present. The template headline and primary state message are in [prompt-template/block/git-not-clean.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/prompt-template/block/git-not-clean.md:1), and the dynamic issue slot is rendered from `{{GIT_ISSUES}}` at line 3.
- algorithmic_behavior: The template converts a detected dirty-worktree state into prescriptive remediation steps. Its two inputs are `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}` on lines 3-4, supplied by the stop hook. The stop hook initializes those variables, filters cached `git status --porcelain` output, sets `GIT_ISSUES="uncommitted changes"` when relevant entries remain, appends special notes for runtime/untracked cases, then calls `load_and_render_safe` with `block/git-not-clean.md` in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:635). The rendered message is emitted in a blocking JSON decision at lines 702-709, so the state transition is “attempted stop” -> “blocked” until the worker stages specific real changes and commits them.
- inputs_outputs_state: Inputs are the caller-provided `GIT_ISSUES` string, optional concatenated `SPECIAL_NOTES`, the template file, and the fallback text embedded in the stop hook. Output is markdown text inserted into the hook’s JSON `reason`; the accompanying `systemMessage` is `Loop: Blocked - uncommitted changes detected, please commit first` when this path triggers. The template instructs review of untracked files, `.gitignore` updates for artifacts, path-specific staging, and a descriptive commit in [prompt-template/block/git-not-clean.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/prompt-template/block/git-not-clean.md:5). No repository state is mutated by the template itself; mutation is delegated to the human/agent follow-up commands described by the prompt.
- gates_or_invariants: The gate invariant is that loop exit is denied while `GIT_ISSUES` is non-empty. Safety invariants in the template forbid broad staging commands during an active RLCR loop, specifically `git add -A`, `git add --all`, and `git add .` at line 12. It also forbids staging `.humanize/` or legacy `.humanize-*` loop artifacts, requires project-convention commit messages, and prohibits AI authorship trailers in [prompt-template/block/git-not-clean.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/prompt-template/block/git-not-clean.md:13). These prompt-level invariants align with the bash validator’s stronger command-level protection in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/loop-common.sh:1270), which blocks direct `.humanize` adds and broad adds that would include local loop state.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, which renders this template via `load_and_render_safe` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:698). Rendering depends on `hooks/lib/template-loader.sh`; its placeholder rules use single-pass `{{VAR}}` substitution and preserve missing placeholders, documented in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:7), and `load_and_render_safe` falls back when the template is missing or renders empty at line 188. The stop hook may append sibling templates `prompt-template/block/git-not-clean-humanize-local.md` and `prompt-template/block/git-not-clean-untracked.md` before rendering this block, depending on untracked status entries.
- edge_cases_or_failure_modes: The stop hook intentionally excludes untracked `.humanize/` and dash-form legacy `.humanize-*` paths from the dirty decision using `HUMANIZE_UNTRACKED_PATTERN` in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:654), but still appends a special note if those paths are present. Other untracked files trigger the generic untracked-file note. A key edge case is `.humanizeconfig`: it resembles runtime state but is not excluded, so it remains a real untracked file and blocks exit. If the template file is absent, the caller’s fallback still blocks with a minimal dirty-git message, so template loss degrades instructions but does not bypass the gate.
- validation_or_tests: Regression coverage exists around this gate. `tests/test-finalize-phase.sh` creates `dirty.txt` and asserts the finalize stop hook blocks on git-not-clean output in [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-finalize-phase.sh:519). `tests/test-stop-hook-legacy-compat.sh` verifies untracked `.humanize-old` alone is ignored for dirty checks, while `.humanizeconfig` blocks and includes both humanize-runtime and untracked-file notes in [tests/test-stop-hook-legacy-compat.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-stop-hook-legacy-compat.sh:145). `tests/robustness/test-setup-scripts-robustness.sh` separately confirms untracked `.humanizeconfig` is treated as dirty setup state at [tests/robustness/test-setup-scripts-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-setup-scripts-robustness.sh:431). I inspected these tests but did not run them; a `git status --short` read attempt failed because this read-only branch export is not a git checkout with a usable `.git` directory in the sandbox.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence heading present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`