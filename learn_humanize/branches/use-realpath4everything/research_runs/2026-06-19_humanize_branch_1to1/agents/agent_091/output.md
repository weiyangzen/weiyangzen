# agent_091 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-091 `file` `tests/test-session-id.sh`
- cursor: `[_]`
- core_role: Executable regression specification for RLCR `session_id` isolation. The file proves that loop setup, PostToolUse session capture, active-loop discovery, state parsing, validators, and cancellation all agree on the same session ownership model. It is not production code, but it is core algorithm coverage because it encodes the expected state-machine behavior for concurrent or stale RLCR loops.
- algorithmic_behavior: The test starts with strict Bash settings, sources `tests/test-helpers.sh`, and imports `hooks/lib/loop-common.sh` so it can call production helpers directly (`tests/test-session-id.sh:13-20`). It then builds temporary git repos and synthetic `.humanize/rlcr/<timestamp>/` directories to exercise the session algorithm end to end. The first block runs `scripts/setup-rlcr-loop.sh` against a gitignored plan and asserts that generated `state.md` contains an empty `session_id:` field plus a `.humanize/.pending-session-id` signal containing the state path (`tests/test-session-id.sh:34-104`). The next blocks simulate Claude PostToolUse JSON for Bash and assert that `hooks/loop-post-bash-hook.sh` records the hook `session_id` only when a matching setup command consumed the signal (`tests/test-session-id.sh:113-164`, `tests/test-session-id.sh:463-528`). The middle blocks call `find_active_loop` directly with matching, non-matching, empty, absent, and finalized session IDs to define active loop lookup semantics (`tests/test-session-id.sh:202-318`, `tests/test-session-id.sh:576-684`). Later blocks specify stale-loop protection, terminal-state blocking, and multi-session coexistence (`tests/test-session-id.sh:686-784`). Final blocks harden command-signature matching against substring, basename-only, and quoted-prefix bypasses while accepting valid quoted, unquoted, space-delimited, and tab-delimited setup invocations (`tests/test-session-id.sh:787-1053`).
- inputs_outputs_state: Inputs are Bash process environment, temporary filesystem state, hook JSON, hand-written YAML frontmatter, and production scripts. The important environment input is `CLAUDE_PROJECT_DIR`, used by setup, hook, and cancel scripts to resolve the project root (`tests/test-session-id.sh:57`, `tests/test-session-id.sh:146`, `tests/test-session-id.sh:370`). Hook JSON inputs include `tool_name`, `tool_input.command`, and `session_id`; examples include quoted setup commands and non-setup commands (`tests/test-session-id.sh:145`, `tests/test-session-id.sh:489`, `tests/test-session-id.sh:952`). State inputs are `state.md`, `finalize-state.md`, `complete-state.md`, and `cancel-state.md` files with YAML frontmatter containing `current_round`, `max_iterations`, `review_started`, branch fields, and `session_id`. Outputs are pass/fail/skip counters through `test-helpers.sh` (`tests/test-helpers.sh:30-78`), filesystem state changes, and function return strings. The expected state transitions are: setup writes `state.md` with `session_id:` empty and creates `.pending-session-id` (`scripts/setup-rlcr-loop.sh:880-918`); PostToolUse replaces only an empty `session_id:` line and then deletes the signal (`hooks/loop-post-bash-hook.sh:120-138`); cancel creates `.cancel-requested`, removes any pending session signal, and renames active state to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:156-167`). `parse_state_file` must expose `STATE_SESSION_ID` from frontmatter (`tests/test-session-id.sh:320-346`; implementation at `hooks/lib/loop-common.sh:442-463`, `hooks/lib/loop-common.sh:494-521`).
- gates_or_invariants: The central invariant is that hook-driven RLCR restrictions are scoped to the active loop for the current `session_id`, while cancel intentionally stays global. Production `find_active_loop` implements two modes: without a filter it checks only the newest loop directory, preserving zombie-loop protection; with a filter it scans newest-to-oldest until it finds the newest directory for that session, then returns it only if it is active (`hooks/lib/loop-common.sh:308-357`, `hooks/lib/loop-common.sh:359-425`). Empty stored `session_id` is a backward-compatible wildcard (`hooks/lib/loop-common.sh:322-323`, `hooks/lib/loop-common.sh:381-396`; covered by `tests/test-session-id.sh:238-261`). Terminal state files such as `complete-state.md` and `cancel-state.md` block revival of older stale loops for the same session (`tests/test-session-id.sh:633-684`, `tests/test-session-id.sh:747-784`). The PostToolUse hook must not consume a signal unless the Bash command starts with the stored full setup path, quoted or unquoted, followed by end-of-string or POSIX whitespace (`hooks/loop-post-bash-hook.sh:61-106`; tested at `tests/test-session-id.sh:826-1053`). It also normalizes repeated slashes before comparing command path and stored signature (`hooks/loop-post-bash-hook.sh:75-83`). Cancel has a deliberate invariant that it does not filter by session because slash-command invocation lacks hook JSON and is an explicit user action (`scripts/cancel-rlcr-loop.sh:80-91`; tested at `tests/test-session-id.sh:348-383`).
- dependencies_and_callers: The test harness depends on `tests/test-helpers.sh` for `setup_test_dir`, temporary git repo initialization, and summary accounting (`tests/test-session-id.sh:15-20`; helper definitions at `tests/test-helpers.sh:84-104`). It executes `scripts/setup-rlcr-loop.sh`, `hooks/loop-post-bash-hook.sh`, and `scripts/cancel-rlcr-loop.sh` as black-box entry points (`tests/test-session-id.sh:54-57`, `tests/test-session-id.sh:142-146`, `tests/test-session-id.sh:368-370`). It directly imports `hooks/lib/loop-common.sh` for `find_active_loop` and `parse_state_file`. Related hook callers use the same `extract_session_id` plus `find_active_loop` contract: Bash validator extracts hook `session_id` and filters active loop lookup (`hooks/loop-bash-validator.sh:52-60`), read validator does the same for todo and methodology-analysis restrictions (`hooks/loop-read-validator.sh:49-83`), and write validator does the same for todo/prompt and methodology-analysis restrictions (`hooks/loop-write-validator.sh:50-87`). `loop-common.sh` provides `extract_session_id` via `jq` (`hooks/lib/loop-common.sh:250-256`), active state resolution (`hooks/lib/loop-common.sh:264-306`), active loop lookup (`hooks/lib/loop-common.sh:332-425`), and state parsing (`hooks/lib/loop-common.sh:442-521`).
- edge_cases_or_failure_modes: The file explicitly covers a missing hook file by skipping PostToolUse-specific assertions instead of failing the whole test (`tests/test-session-id.sh:162-164`, `tests/test-session-id.sh:506-528`). It validates no-op behavior when there is no `.pending-session-id`, preventing unrelated Bash events from overwriting an existing `session_id` (`tests/test-session-id.sh:167-200`). It covers session IDs containing YAML-safe punctuation (`abc-123_def.456`) and hook-recorded special characters including `/` and `&`, which require safe replacement rather than `sed` substitution (`tests/test-session-id.sh:438-461`, `tests/test-session-id.sh:530-574`; implementation uses `awk -v new_id=...` at `hooks/loop-post-bash-hook.sh:124-134`). It tests race prevention where a pending signal survives unrelated Bash commands (`tests/test-session-id.sh:463-509`). It blocks false positives where a command merely echoes the setup path or mentions the basename (`tests/test-session-id.sh:826-880`). It blocks a quoted-path prefix concatenation boundary bypass (`tests/test-session-id.sh:882-925`). It accepts unquoted setup invocation and tab-delimited arguments, proving command matching is not limited to a single formatting style (`tests/test-session-id.sh:927-1053`). It also asserts that newer terminal loops block stale older active loops, both globally for no-filter lookup and per-session for filtered lookup (`tests/test-session-id.sh:385-436`, `tests/test-session-id.sh:633-784`).
- validation_or_tests: This assigned file is itself the validation artifact. It defines pass/fail assertions through `pass`, `fail`, and `print_test_summary`, ending with `print_test_summary "Session ID Feature Tests"` (`tests/test-session-id.sh:1055-1059`). I inspected it directly and followed its production references, but did not execute it because this task requested read-only research notes and no file modification. The inspected production evidence shows the code under test contains the `session_id:` state field and signal creation in setup (`scripts/setup-rlcr-loop.sh:880-918`), safe session write and signal removal in the PostToolUse hook (`hooks/loop-post-bash-hook.sh:120-138`), session-aware loop discovery in `loop-common.sh` (`hooks/lib/loop-common.sh:308-425`), and session-agnostic cancellation (`scripts/cancel-rlcr-loop.sh:80-91`, `scripts/cancel-rlcr-loop.sh:156-167`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 of 1 Item Evidence sections present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`