# agent_048 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-048 `file` `scripts/ask-gemini.sh`
- cursor: `[_]`
- core_role: Runtime skill adapter that turns `/humanize:ask-gemini` into a bounded, logged, one-shot Gemini CLI consultation. It validates the CLI and user options, resolves the current project root, creates project-local and cache artifacts, runs `gemini` with a web-research prompt, records structured metadata, and emits Gemini stdout back to the caller. The branch-relevant root behavior is delegated to the shared realpath-canonical resolver sourced from [hooks/lib/project-root.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/project-root.sh:41), while the assigned script calls it at [scripts/ask-gemini.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/ask-gemini.sh:174).
- algorithmic_behavior: The script starts with strict shell mode at [scripts/ask-gemini.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/ask-gemini.sh:21), derives `SCRIPT_DIR` from the script path, and sources `portable-timeout.sh` plus `../hooks/lib/project-root.sh` at lines 27-33. Defaults are `gemini-3.1-pro-preview` and `3600` seconds at lines 39-43. Argument parsing accumulates question words into `QUESTION_PARTS`, supports `--gemini-model`, `--gemini-timeout`, `--`, and help, and treats the first non-option as the beginning of the question so later dash-prefixed text is preserved as question text at lines 88-139. Prerequisite gates require `gemini` in `PATH`, a non-empty question, and a model string restricted to alphanumeric, dot, underscore, and hyphen at lines 145-168. The script resolves `PROJECT_ROOT` through `resolve_project_root` at lines 174-178, then creates a unique run id from timestamp, pid, and 4 random bytes at lines 184-185. It writes input metadata to `.humanize/skill/<unique-id>/input.md` at lines 187-218, builds `gemini` arguments at lines 224-234, always prepends a Google Search instruction to the prompt at lines 236-241, writes debug command/prompt data to `gemini-run.cmd` at lines 247-261, runs Gemini through `run_with_timeout` at lines 279-283, then classifies timeout, nonzero exit, empty stdout, or success at lines 294-388.
- inputs_outputs_state: Inputs are shell argv, environment variables, current repository context, external commands, and filesystem writability. User-facing argv inputs are `--gemini-model <MODEL>`, `--gemini-timeout <SECONDS>`, optional `--`, and the free-form question. Environment inputs include `HUMANIZE_GEMINI_YOLO` for approval mode at lines 226-231, `CLAUDE_PROJECT_DIR` via the project-root helper, `XDG_CACHE_HOME`, `HOME`, and `PATH`. External inputs are `gemini`, `date`, `/dev/urandom`, `od`, `tr`, `sed`, `mkdir`, `cp`, `tail`, and timeout implementations. Outputs are Gemini response on stdout at line 388; status, cache path, and errors on stderr at lines 267-269 and error branches; project-local files under `.humanize/skill/<unique-id>/` containing `input.md`, `output.md`, and `metadata.md`; and cache files under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project-path>/skill-<unique-id>/gemini-run.{cmd,out,log}` at lines 191-199 and 247-249. State transitions are: parsed config -> prerequisite-validated -> canonical project-root selected -> run directories created -> input/debug command persisted -> Gemini process executed -> metadata status written as `timeout`, `error`, `empty_response`, or `success` -> successful output copied to project-local `output.md`.
- gates_or_invariants: `set -euo pipefail` makes unset variables and failing commands fatal unless explicitly captured. `gemini` must exist before any storage artifacts are created, because the CLI check happens at lines 145-151 before root resolution and directory creation. The question must not be empty at lines 153-160. Model names are constrained by regex at lines 162-168 to avoid command injection through the model argument, and `GEMINI_ARGS` is an array at lines 224-234, so the model and flags are passed as separate arguments. Timeout must be all digits at lines 113-123; despite the error text saying “positive integer,” `0` is accepted by the regex. `PROJECT_ROOT` must resolve from `CLAUDE_PROJECT_DIR` or git toplevel; `pwd` is intentionally not a fallback in the helper, per [hooks/lib/project-root.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/project-root.sh:10). The helper canonicalizes roots with `realpath` or Python `os.path.realpath`, so symlinked project prefixes converge before `.humanize` and cache paths are computed at [hooks/lib/project-root.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/project-root.sh:41) and [hooks/lib/project-root.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/project-root.sh:113). The main success invariant is nonzero-size Gemini stdout; empty output is treated as failure even with exit code 0 at [scripts/ask-gemini.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/ask-gemini.sh:340).
- dependencies_and_callers: Direct dependency `scripts/portable-timeout.sh` supplies `run_with_timeout`, choosing `gtimeout`, GNU `timeout`, Python, or no-timeout fallback at [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/portable-timeout.sh:10) and [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/portable-timeout.sh:33). Direct dependency `hooks/lib/project-root.sh` supplies realpath-canonical project root resolution and path canonicalizers. The skill wrapper [skills/ask-gemini/SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/skills/ask-gemini/SKILL.md:5) allows Bash execution of this script, instructs callers to quote free-form user text, and documents stdout/stderr interpretation at [skills/ask-gemini/SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/skills/ask-gemini/SKILL.md:17). The monitor path consumes the artifacts produced by this script: `scripts/lib/monitor-skill.sh` identifies `gemini` runs from `metadata.md` or `input.md` at [scripts/lib/monitor-skill.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/monitor-skill.sh:50), locates `gemini-run.log` and `gemini-run.out` cache files at [scripts/lib/monitor-skill.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/monitor-skill.sh:165), and exposes `humanize monitor gemini` through `scripts/humanize.sh` references found near lines 1209-1237. README references `/humanize:ask-gemini` and `humanize monitor gemini` as user entry points.
- edge_cases_or_failure_modes: Missing `gemini` exits 1 before artifacts are created. Empty question exits 1. Unknown options before the question exit 1, while dash-prefixed tokens after the first question token are accepted as part of the question due to `OPTIONS_DONE`. `--gemini-model` and `--gemini-timeout` without following values exit 1. Timeout validation allows `0`, which may cause immediate timeout or implementation-dependent behavior in `run_with_timeout`. If neither `CLAUDE_PROJECT_DIR` nor git toplevel is available, root resolution exits 1. If home cache creation fails, it falls back to `$SKILL_DIR/cache` and warns at lines 195-199. If `realpath` is absent or cannot resolve a path, the helper falls back to Python realpath, then the original input. If no timeout implementation is available, `portable-timeout.sh` warns and runs Gemini without a timeout at [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/portable-timeout.sh:64). Nonzero Gemini exits write `status: error` metadata and echo the last 20 stderr lines. Exit code 124 writes `status: timeout`. Exit code 0 with empty stdout writes `status: empty_response` and exits 1. `UNIQUE_ID` depends on `/dev/urandom`; failure there would abort under strict mode. The debug command file uses `${GEMINI_ARGS[*]}` for display only, so it is not a replay-safe shell-escaped command when arguments contain unusual characters, though model validation and fixed flags keep that low risk here.
- validation_or_tests: Static validation performed by direct inspection of [scripts/ask-gemini.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/ask-gemini.sh:1), [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/portable-timeout.sh:1), [hooks/lib/project-root.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/project-root.sh:1), [skills/ask-gemini/SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/skills/ask-gemini/SKILL.md:1), and [scripts/lib/monitor-skill.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/monitor-skill.sh:1). I did not execute `scripts/ask-gemini.sh` because any meaningful run would create `.humanize/skill` and cache artifacts, and the assignment explicitly says not to modify files. Repository search found related monitor and skill wrapper references but no direct automated test dedicated to `ask-gemini.sh`; the only broad model-name test hit was `tests/test-gen-plan.sh`, which checks model prefixes including `gemini-`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-048`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`