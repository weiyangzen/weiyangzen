# agent_066 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-066 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable regression specification for ANSI escape stripping and pass/fail count extraction used by the aggregate test runner. The file validates that colored suite output can be normalized before summary parsing, especially across GNU/Linux and BSD/macOS `sed` behavior. The script is included in the aggregate suite list at `tests/run-all-tests.sh:73`, and the runner uses the same escape-stripping pattern at `tests/run-all-tests.sh:228` and `tests/run-all-tests.sh:245-248`.
- algorithmic_behavior: The test script defines a portable ESC byte with ANSI-C quoting, `esc=$'\033'`, then repeatedly applies `sed "s/${esc}\\[[0-9;]*m//g"` to remove SGR color/style sequences from sample output. It validates eight cases: basic color removal at `tests/test-ansi-parsing.sh:44-53`, multiple colored spans at `tests/test-ansi-parsing.sh:59-67`, extraction of colored `Passed:` counts at `tests/test-ansi-parsing.sh:73-81`, extraction of colored `Failed:` counts at `tests/test-ansi-parsing.sh:87-95`, zero-count preservation at `tests/test-ansi-parsing.sh:101-109`, multi-line test-suite summary parsing at `tests/test-ansi-parsing.sh:115-130`, plain text with no ANSI codes at `tests/test-ansi-parsing.sh:136-144`, and combined bold/color SGR codes at `tests/test-ansi-parsing.sh:150-158`. Count extraction uses `grep -oE 'Passed:[[:space:]]*[0-9]+'` or `Failed:` followed by `grep -oE '[0-9]+$'`; the multi-line case uses `tail -1` to select the final summary count at `tests/test-ansi-parsing.sh:124-125`.
- inputs_outputs_state: Inputs are fixed in-script Bash strings, some using ANSI-C quoted escape bytes such as `\033[0;32m` and multi-line summary text. Outputs are human-readable PASS/FAIL lines, a colored summary, and process exit code `0` when `TESTS_FAILED` remains zero or `1` otherwise at `tests/test-ansi-parsing.sh:170-178`. State is held only in local shell variables plus global counters `TESTS_PASSED` and `TESTS_FAILED` initialized at `tests/test-ansi-parsing.sh:18-19`. The `pass()` helper increments the passed counter at `tests/test-ansi-parsing.sh:21-24`; `fail()` emits expected/actual diagnostics and increments the failed counter at `tests/test-ansi-parsing.sh:26-31`. There are no repository writes.
- gates_or_invariants: The primary invariant is that stripping ANSI SGR sequences must leave semantically identical plain text for summary parsing. The pattern only targets CSI SGR sequences ending in `m` with numeric/semicolon parameters, so it is deliberately scoped to color/style codes used by the test output, not all possible ANSI control sequences. Numeric extraction must preserve zero as a real value, not fall through to the fallback `echo "0"` path, covered at `tests/test-ansi-parsing.sh:101-109`. The aggregate runner depends on the same invariant when it strips suite logs before adding totals at `tests/run-all-tests.sh:245-251`.
- dependencies_and_callers: Depends on Bash with ANSI-C quoting, `sed`, `grep -E`, `tail`, and standard shell arithmetic. `SCRIPT_DIR` is computed at `tests/test-ansi-parsing.sh:11` but not used by this file’s internal tests. The file is executable (`-rwxr-xr-x`) and is launched directly by the aggregate runner for non-zsh suites via `"$suite_path" >"$out_file" 2>&1` at `tests/run-all-tests.sh:190-195`. Related implementation exists in `tests/run-all-tests.sh`, where `esc=$'\033'` is set before parsing each suite log at `tests/run-all-tests.sh:228`, and in `scripts/statusline.sh:346-349`, which has a separate `strip_ansi()` helper using `sed 's/\x1b\[[0-9;]*m//g'` for visible-length calculation.
- edge_cases_or_failure_modes: Covered edge cases include multiple ANSI spans in one line, bold SGR codes, zero failed counts, multi-line summaries, and uncolored plain text. Potential uncovered cases are non-SGR ANSI sequences, SGR codes with `?` or other private-mode parameters, 24-bit color sequences are actually covered if numeric/semicolon-only and ending in `m`, carriage returns/progress-line rewrites, lowercase labels, labels with punctuation differences, and output containing earlier non-summary `Passed:`/`Failed:` lines where the non-multi-line tests do not use `tail -1`. The script sets `set -uo pipefail` at `tests/test-ansi-parsing.sh:9` but intentionally does not use `set -e`, allowing all assertions to run before final failure reporting.
- validation_or_tests: Direct validation was run read-only with `bash tests/test-ansi-parsing.sh`; it exited `0` and reported `Passed: 8`, `Failed: 0`. This confirms the current shell/sed environment accepts the portable ESC-variable sed expression and the count parsing behavior exercised by the aggregate runner. No files were modified.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: one evidence section for the single assigned item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`