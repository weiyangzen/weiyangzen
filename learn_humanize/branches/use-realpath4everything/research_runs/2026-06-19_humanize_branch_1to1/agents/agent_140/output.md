# agent_140 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-140 `file` `prompt-template/block/stop-hook-direct-execution.md`
- cursor: `[_]`
- core_role: Static Markdown block used as the user-facing denial message when an active RLCR/Humanize loop detects an attempt to manually execute stop-hook infrastructure through the Bash tool. The file itself is only 7 lines: title at `prompt-template/block/stop-hook-direct-execution.md:1`, the violation description at line 3, the managed-hook invariant at line 5, and the required next action at line 7.
- algorithmic_behavior: The template is the presentation layer for a Bash pre-use gate. Runtime detection lives in `hooks/loop-bash-validator.sh`: after validating hook input and extracting the Bash command, it only applies RLCR-specific checks when an active loop is found (`hooks/loop-bash-validator.sh:155-158`). The direct-hook-execution section defines blocked targets `loop-codex-stop-hook.sh` and `rlcr-stop-gate.sh` (`hooks/loop-bash-validator.sh:166`), builds wrapper-aware regex fragments for assignments, `command`, `env`, `timeout`, `nice`, `nohup`, `strace`, and `ltrace` (`hooks/loop-bash-validator.sh:167-179`), matches shell/path launch forms (`hooks/loop-bash-validator.sh:180-181`), then emits this block through `stop_hook_direct_execution_blocked_message` and exits with code 2 (`hooks/loop-bash-validator.sh:181-183`). The message instructs the agent not to continue by invoking hook scripts manually, but to finish normally so the hooks system advances the loop.
- inputs_outputs_state: The template has no placeholders, variables, or direct state reads. Its effective input is the validator’s classified event: a Bash command in an active loop that matches the stop-hook/stop-gate direct execution pattern. Its output is a Markdown block written to stderr by `stop_hook_direct_execution_blocked_message`. The gate causes the Bash tool attempt to fail with exit 2, but this file does not mutate loop state, files, git state, or hook state. The semantic state transition is from “agent attempted manual hook execution” to “manual execution blocked; agent instructed to end response and allow automatic hook transition.”
- gates_or_invariants: The key invariant is that hook scripts are system-managed and must not be invoked manually during an active loop. The exact invariant appears in the template: hook scripts are triggered automatically and should not be manually executed (`prompt-template/block/stop-hook-direct-execution.md:5`). The runtime gate enforces this only after a valid Bash hook event is parsed (`hooks/loop-bash-validator.sh:22-45`) and an active loop is located (`hooks/loop-bash-validator.sh:52-60`, `hooks/loop-bash-validator.sh:155-158`). The template loader also adds a resilience invariant: if the template is missing, empty, or renders empty, `load_and_render_safe` falls back to an embedded message (`hooks/lib/template-loader.sh:188-210`).
- dependencies_and_callers: The direct caller is `stop_hook_direct_execution_blocked_message` in `hooks/lib/loop-common.sh:1470-1480`, which embeds the same fallback text and calls `load_and_render_safe "$TEMPLATE_DIR" "block/stop-hook-direct-execution.md" "$fallback"` at `hooks/lib/loop-common.sh:1479`. The validator caller is `hooks/loop-bash-validator.sh:181-183`. Template loading is provided by `hooks/lib/template-loader.sh`: `load_template` reads files from the resolved template directory (`hooks/lib/template-loader.sh:33-46`), `render_template` performs single-pass `{{VAR}}` substitution (`hooks/lib/template-loader.sh:48-136`), and `load_and_render_safe` supplies fallback behavior (`hooks/lib/template-loader.sh:185-210`). Broader template reference tests scan `load_template`, `load_and_render`, and `load_and_render_safe` calls for existing referenced files (`tests/test-template-references.sh:66-110`).
- edge_cases_or_failure_modes: If no active loop is found, the Bash validator exits 0 before the direct-hook-execution gate, so the template is not used (`hooks/loop-bash-validator.sh:155-158`). If the command targets a different hook script name, it is outside `BLOCKED_HOOK_SCRIPTS` and this specific block is not emitted (`hooks/loop-bash-validator.sh:166`). If the template is missing or empty, the fallback in `hooks/lib/loop-common.sh:1471-1477` preserves the same operator guidance. If rendering fails or returns empty, `load_and_render_safe` emits the fallback (`hooks/lib/template-loader.sh:202-207`). Because the template has no placeholders, placeholder injection and missing-variable behavior are not meaningful for this file, though the shared renderer is single-pass by design (`hooks/lib/template-loader.sh:52-55`).
- validation_or_tests: `tests/test-allowlist-validators.sh` defines `assert_hook_wrapper_blocked`, which expects exit code 2 and output containing “direct execution” (`tests/test-allowlist-validators.sh:488-503`). It covers bare `bash hooks/loop-codex-stop-hook.sh`, `zsh`, `env`, `command`, assignment prefixes, combined wrappers, direct `./scripts/rlcr-stop-gate.sh`, `timeout`, `nice`, `nohup`, `strace`, `ltrace`, and combined utility wrappers (`tests/test-allowlist-validators.sh:507-575`). Template existence is indirectly covered by `tests/test-template-references.sh:93-106`, which checks referenced templates exist, and by comprehensive template loading/rendering over all template files (`tests/test-templates-comprehensive.sh:563-590`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-140`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`