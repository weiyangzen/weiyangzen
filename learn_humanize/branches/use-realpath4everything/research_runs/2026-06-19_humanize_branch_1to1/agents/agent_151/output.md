# agent_151 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-151 `file` `prompt-template/claude/agent-teams-continue.md`
- cursor: `[_]`
- core_role: This file is a Claude prompt-template fragment for RLCR continuation rounds when Agent Teams mode is enabled. It is not executable code, but it defines transition behavior for the next implementation round after Codex review feedback: the previous team is considered expired, the continuing Claude Team Leader must read Codex feedback first, create a new team, avoid redoing already correct work, and cold-start new team members with enough context. The main role is algorithmic orchestration through prompt constraints rather than runtime logic.
- algorithmic_behavior: The template starts a continuation section at `prompt-template/claude/agent-teams-continue.md:1`, then establishes the operating mode at `prompt-template/claude/agent-teams-continue.md:3`: continue as Team Leader in Agent Teams mode within the RLCR development cycle after Codex reviewed the previous round. Its behavioral sequence is: discard old team membership (`:7`), analyze Codex review before spawning workers (`:8`), consult prior outputs and avoid duplicating correct work (`:9`), provide cold-start task context to newly spawned members (`:10`), prioritize high-priority review issues if the work exceeds one round (`:11`), and verify current file state before assigning ownership (`:12`). This creates a human-readable state transition from "review completed" to "new implementation-team allocation."
- inputs_outputs_state: Inputs are implicit prompt context assembled by the RLCR stop hook: Codex review feedback "above," prior summaries, the goal tracker, current repository files, `CLAUDE.md`, and project configuration. The output is not a file artifact by itself; it is appended into the next Claude prompt, where it should cause the Team Leader to form a new team and delegate targeted remediation. State transition is carried in prompt semantics: old teammates are invalidated, new members start with no conversation history, and task ownership must be assigned from the verified current state. The template itself has no placeholders, so template rendering is a direct file load with no variable substitution needs.
- gates_or_invariants: The key invariants are explicit in the template. Old teammates must not be messaged or referenced (`prompt-template/claude/agent-teams-continue.md:7`). Review analysis must happen before spawning team members (`:8`). Correct prior work must not be redone (`:9`). New members must be given previous accomplishments, relevant current file state, review findings, and acceptance criteria (`:10`). Current files must be verified before ownership assignment (`:12`). Runtime gating is handled by `hooks/loop-codex-stop-hook.sh`: continuation instructions are appended only when `AGENT_TEAMS` is `true` and `REVIEW_STARTED` is not `true` (`hooks/loop-codex-stop-hook.sh:2147`). The hook also adds a separate Agent Teams enforcement block earlier when `AGENT_TEAMS` is true, warning the leader not to implement code directly (`hooks/loop-codex-stop-hook.sh:2054-2075`).
- dependencies_and_callers: The direct caller is `hooks/loop-codex-stop-hook.sh`, which loads this file with `load_template "$TEMPLATE_DIR" "claude/agent-teams-continue.md"` (`hooks/loop-codex-stop-hook.sh:2148`) and loads the shared `agent-teams-core.md` alongside it (`hooks/loop-codex-stop-hook.sh:2149`). Both templates must be non-empty for the rich continuation prompt path; if both load, the stop hook appends this continuation fragment, a blank line, then the core Agent Teams template (`hooks/loop-codex-stop-hook.sh:2150-2154`). If either template is empty or missing, the hook appends a short inline fallback (`hooks/loop-codex-stop-hook.sh:2155-2163`). The Agent Teams flag comes from parsed state frontmatter: `STATE_AGENT_TEAMS` is read in `hooks/lib/loop-common.sh:464`, defaulted to `false` in `hooks/lib/loop-common.sh:513`, and assigned to `AGENT_TEAMS` in `hooks/loop-codex-stop-hook.sh:142`. The template loader resolves the template directory from the hooks library path (`hooks/lib/template-loader.sh:24-31`) and returns file contents if the path exists (`hooks/lib/template-loader.sh:36-45`). Initial round setup does not use this file; it uses `agent-teams-instructions.md` plus `agent-teams-core.md` in `scripts/setup-rlcr-loop.sh:1370-1379`, making this file specifically continuation-only.
- edge_cases_or_failure_modes: If this file is missing, empty, or unreadable, or if `agent-teams-core.md` fails to load, the stop hook silently switches to the shorter fallback after suppressing loader warnings with `2>/dev/null` (`hooks/loop-codex-stop-hook.sh:2148-2163`). That fallback preserves high-level delegation but drops the richer continuation rules about old team invalidation, review-first allocation, cold-start context, priority ordering, and current-state verification. If state parsing leaves `agent_teams` unset or false, no continuation instructions are appended (`hooks/lib/loop-common.sh:513`, `hooks/loop-codex-stop-hook.sh:2147`). If `REVIEW_STARTED` is true, the continuation fragment is deliberately skipped because the hook says it is only for implementation phase, not review phase (`hooks/loop-codex-stop-hook.sh:2145-2147`). Because the file has no template variables, placeholder injection is not a relevant edge case for this specific fragment, though the shared loader supports single-pass variable rendering elsewhere (`hooks/lib/template-loader.sh:7-13`).
- validation_or_tests: `tests/test-agent-teams.sh` validates this specific template exists and is at least 200 bytes (`tests/test-agent-teams.sh:372-379`) and checks for the "Continuation Context" section (`tests/test-agent-teams.sh:380-384`). The inspected file is 1767 bytes, matching the assigned metadata and satisfying that size gate. Broader reference tests exist for template loading and references, but I did not run the test suite because this branch export is read-only and the requested task was research-only. A local `git status --short` check could not be used as validation because this export did not present as a git repository in the restricted environment and macOS developer tooling attempted disallowed temp/cache writes.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 evidence section present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`