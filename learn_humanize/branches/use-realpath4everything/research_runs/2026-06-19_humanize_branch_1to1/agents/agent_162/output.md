# agent_162 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-162 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: This file is a Claude prompt-block template for the round immediately after a Full Goal Alignment Check. It does not implement repository runtime logic directly, but it defines a review-to-implementation contract that changes the next agent round’s priorities. Its role is to preserve alignment findings from Codex review and force them into Claude’s next action selection.
- algorithmic_behavior: The template adds a fixed section titled “Post-Alignment Check Action Items” at `prompt-template/claude/post-alignment-action-items.md:2`. It instructs the next Claude round to pay special attention to four classes of alignment output: forgotten items, acceptance criteria marked NOT MET, unjustified deferrals, and queued non-blocking issues. The highest-priority behavioral transition is from “review findings exist” to “Claude must act on neglected or failed acceptance criteria now,” especially lines `5-7`, which direct Claude to address forgotten tasks, prioritize unmet ACs, and un-defer unjustified deferrals. Line `8` preserves queue semantics by keeping non-blocking follow-up work queued unless it becomes blocking.
- inputs_outputs_state: Input is the prior Full Goal Alignment Check result, especially Codex findings about forgotten items, AC status, deferrals, and queued issues. The template itself has no variables or placeholders, so it is static markdown. Its operational input gate is the shell variable `FULL_ALIGNMENT_CHECK`; when true, `hooks/loop-codex-stop-hook.sh:2114-2120` loads this template and appends it to `NEXT_PROMPT_FILE`. Output is appended prompt text for the next Claude implementation round. State transition: a normal next-round prompt becomes a post-alignment prompt with explicit priority rules. It also carries review state from Codex’s full-alignment review prompt, whose output requirements ask for action items classified into Mainline Gaps, Blocking Side Issues, and Queued Side Issues at `prompt-template/codex/full-alignment-review.md:106-110`.
- gates_or_invariants: The inclusion gate is `[[ "$FULL_ALIGNMENT_CHECK" == "true" ]]` in `hooks/loop-codex-stop-hook.sh:2115`; the append gate is `[[ -n "$POST_ALIGNMENT" ]]` at `hooks/loop-codex-stop-hook.sh:2117`, so missing or empty content is silently skipped. The process invariants encoded by the template are: NOT MET acceptance criteria outrank general work; unjustified deferrals must be reversed; forgotten items must be addressed; queued side issues remain queued unless they clearly block mainline progress. This pairs with the full-alignment review invariant at `prompt-template/codex/full-alignment-review.md:112-114`: COMPLETE is only valid when all original ACs are met and no deferrals remain.
- dependencies_and_callers: Direct caller found: `hooks/loop-codex-stop-hook.sh:2116` via `load_template "$TEMPLATE_DIR" "claude/post-alignment-action-items.md"`. Template loading behavior is provided by `hooks/lib/template-loader.sh`; its `append_template` helper documents a similar append-only-if-non-empty contract at `hooks/lib/template-loader.sh:153-168`, though this specific caller performs the load and echo inline. Upstream dependency is the Codex full alignment review template at `prompt-template/codex/full-alignment-review.md:104-114`, which generates the review result categories that this Claude prompt block later tells Claude to consume.
- edge_cases_or_failure_modes: If the template is absent or empty, no fallback text is emitted because the hook only appends when `POST_ALIGNMENT` is non-empty and redirects load errors to `/dev/null`. If `FULL_ALIGNMENT_CHECK` is false, the action-item guidance is not included even if the previous review contained relevant findings. If Codex review output fails to classify action items clearly, this template still tells Claude what kinds of issues to look for, but it cannot supply missing specifics. Because line `8` says queued issues stay queued unless blocking, an ambiguous side issue may be misprioritized unless the review result clearly marks blocking status.
- validation_or_tests: No direct unit test or template-specific test was found in the inspected references. Static validation is straightforward: the file contains only markdown and no render placeholders, so it does not depend on variable substitution. Behavioral validation would be to run or inspect the stop-hook prompt generation path with `FULL_ALIGNMENT_CHECK=true` and verify that `NEXT_PROMPT_FILE` receives the lines from `prompt-template/claude/post-alignment-action-items.md:2-8` after the Codex review result and before the standard footer sequence shown around `hooks/loop-codex-stop-hook.sh:2122-2143`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: USE_REALPATH4EVERYTHING-HZ-162
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`