# agent_161 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-161 `file` `prompt-template/claude/open-question-notice.md`
- cursor: `[_]`
- core_role: This file is a Claude prompt-fragment template used as an RLCR transition gate when Codex reports open questions. The template itself is one static instruction at `prompt-template/claude/open-question-notice.md:1`: it tells Claude that Codex found Open Question(s) and requires use of `AskUserQuestion` before resolving any other Codex findings.
- algorithmic_behavior: The file has no functions or dynamic placeholders; its behavior is activated by `hooks/loop-codex-stop-hook.sh`. After the stop hook has generated the next-round prompt, it checks Codex review output for open-question headings when `ASK_CODEX_QUESTION` is enabled. The detection loop reads `REVIEW_RESULT_FILE` line by line and sets `HAS_OPEN_QUESTION=true` when a line is shorter than 40 characters and contains the case-sensitive substring `Open Question` (`hooks/loop-codex-stop-hook.sh:2078`-`2087`). If detected, the hook loads this template with `load_template "$TEMPLATE_DIR" "claude/open-question-notice.md"` (`hooks/loop-codex-stop-hook.sh:2090`-`2094`) and injects the notice into `NEXT_PROMPT_FILE` using `awk` after the Codex review end marker plus `---` separator (`hooks/loop-codex-stop-hook.sh:2095`-`2110`). This makes the next Claude round user-clarification-first instead of immediately acting on Codex findings.
- inputs_outputs_state: Inputs are the static template text at `prompt-template/claude/open-question-notice.md:1`, the configured `TEMPLATE_DIR`, `REVIEW_RESULT_FILE`, `NEXT_PROMPT_FILE`, and the loop state field `ask_codex_question`. `ask_codex_question` is defined as a state frontmatter field in `hooks/lib/loop-common.sh:38`, parsed from state frontmatter in `hooks/lib/loop-common.sh:455`-`468`, and defaulted to `true` for tolerant and strict parsing in `hooks/lib/loop-common.sh:505`-`518` and `hooks/lib/loop-common.sh:589`-`597`. The stop hook maps that parsed state into `ASK_CODEX_QUESTION` at `hooks/loop-codex-stop-hook.sh:125`-`142`. Output is not a data file from this template directly; the output is an injected instruction in the generated next-round Claude prompt. The state transition is behavioral: Codex review with an open-question marker plus `ask_codex_question=true` transitions the next Claude prompt into a user-question clarification mode before other remediation.
- gates_or_invariants: The primary gate is `if [[ "$ASK_CODEX_QUESTION" == "true" ]]` in `hooks/loop-codex-stop-hook.sh:2080`; if false, the template is not loaded or injected. The second gate is the heuristic open-question detector: line length `< 40` and substring `Open Question` (`hooks/loop-codex-stop-hook.sh:2082`-`2084`). The insertion point also has an implicit structural invariant: the next prompt must contain a line matching `<!-- CODEX.*REVIEW RESULT.*END.*-->` followed by a `---` line, otherwise the `awk` block will not print the notice (`hooks/loop-codex-stop-hook.sh:2095`-`2109`). The template loader invariant is that missing templates yield no content and warn (`hooks/lib/template-loader.sh:33`-`46`); the stop hook protects behavior with an inline fallback that matches the assigned file’s instruction if the template is empty or missing (`hooks/loop-codex-stop-hook.sh:2091`-`2094`). The notice has no `{{VAR}}` placeholders, so render-time variable substitution is not involved.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh` at `hooks/loop-codex-stop-hook.sh:2091`. It depends on `hooks/lib/template-loader.sh`, where `get_template_dir` resolves the plugin `prompt-template` directory (`hooks/lib/template-loader.sh:24`-`31`) and `load_template` reads a named template path (`hooks/lib/template-loader.sh:33`-`46`). `hooks/lib/loop-common.sh` initializes and validates `TEMPLATE_DIR`, warning but allowing inline fallbacks if validation fails (`hooks/lib/loop-common.sh:240`-`248`). User-facing configuration is exposed through `scripts/setup-rlcr-loop.sh`: default `ASK_CODEX_QUESTION="true"` is set near startup (`scripts/setup-rlcr-loop.sh:52` from search evidence), `--claude-answer-codex` disables asking the user and lets Claude answer directly (`scripts/setup-rlcr-loop.sh:119`-`124`), and `--yolo` also disables asking by enabling `--claude-answer-codex` behavior (`scripts/setup-rlcr-loop.sh:128`-`131`). The setup output reports the effective setting as `Ask User for Codex Questions` (`scripts/setup-rlcr-loop.sh:1484`-`1490`). Skill docs also identify the same user-visible control: `skills/humanize/SKILL.md:73`-`85` lists `--claude-answer-codex` as the option that bypasses default `AskUserQuestion`, and `skills/humanize-rlcr/SKILL.md:60`-`68` names open-question handling as a hook-managed gate.
- edge_cases_or_failure_modes: A Codex review can contain real open questions that do not match the exact detector, for example a heading longer than 39 characters, different casing such as `Open questions`, or phrasing without the exact substring; those cases will not inject this template. Conversely, any short line containing `Open Question` can trigger injection even if it is not a formal review section. If `ASK_CODEX_QUESTION=false`, such as from `--claude-answer-codex` or `--yolo`, the user-clarification gate is intentionally bypassed. If the assigned template is missing, empty, or template directory validation fails, the hook falls back to an inline copy of the notice, so runtime behavior is resilient but template-specific wording changes would be ignored in that fallback case. If the generated prompt does not contain the expected Codex review end marker followed by `---`, detection can succeed while insertion silently does not place the notice because the `awk` insertion pattern is not reached.
- validation_or_tests: Template coverage is indirectly validated by template-reference and template-load tests. `tests/test-template-references.sh:66`-`107` scans scripts for `load_template`, `load_and_render`, and `load_and_render_safe` calls using `$TEMPLATE_DIR`, then fails if referenced template files are missing; this covers the caller reference to `claude/open-question-notice.md`. `tests/test-templates-comprehensive.sh:90`-`107` iterates every Markdown template under `prompt-template`, loads it, and fails if loading returns empty; this covers the assigned file’s non-empty invariant. I found no dedicated test that exercises the open-question detection heuristic and verifies exact insertion into `NEXT_PROMPT_FILE`; that behavior appears validated only through the hook code path and general template tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-161`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`