# agent_075 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-075 `file` `tests/test-config-error-handling.sh`
- cursor: `[_]`
- core_role: Executable specification for the negative-path and optional-layer behavior of `scripts/lib/config-loader.sh`. The file is not production logic itself, but it defines the expected contract for the core config merge algorithm when required defaults are missing, optional user/project configs are absent, or optional configs are malformed. It directly sources the loader at `tests/test-config-error-handling.sh:32-33` and exercises `load_merged_config` plus `get_config_value`.

- algorithmic_behavior: The script runs six isolated shell test scenarios. It first resolves `SCRIPT_DIR`, `PROJECT_ROOT`, and `CONFIG_LOADER`, sources `tests/test-helpers.sh`, and fails immediately if `scripts/lib/config-loader.sh` is missing (`tests/test-config-error-handling.sh:16-33`). Each scenario calls `setup_test_dir`, builds a temporary fixture tree, invokes `load_merged_config`, and records pass/fail via the shared helper counters. Test 1 creates a fake plugin root with no `config/default_config.json` and expects `load_merged_config` to return non-zero (`tests/test-config-error-handling.sh:40-53`). Tests 2 and 3 write malformed optional JSON files for project and user config, capture stderr, require a warning-like message, then verify the merged value falls back to the default `bitlesson_model` value `haiku` (`tests/test-config-error-handling.sh:59-110`). Test 4 verifies an empty project object `{}` is accepted and still yields default values (`tests/test-config-error-handling.sh:116-128`). Tests 5 and 6 verify missing project config and missing user config directory are non-fatal and still yield defaults (`tests/test-config-error-handling.sh:134-172`). The script ends with `print_test_summary`, which returns non-zero if any recorded failure exists (`tests/test-config-error-handling.sh:174`; helper behavior in `tests/test-helpers.sh:58-77`).

- inputs_outputs_state: Inputs are filesystem fixtures under `TEST_DIR`, environment overrides, and the repository default config. The important filesystem inputs are fake plugin roots, temporary project roots, optional `$PROJECT_DIR/.humanize/config.json`, optional `$XDG_CONFIG_HOME/humanize/config.json`, and the real `config/default_config.json`. Environment inputs are `XDG_CONFIG_HOME` in every optional-user scenario and the shell `set -euo pipefail` mode (`tests/test-config-error-handling.sh:14`, `tests/test-config-error-handling.sh:64-65`, `tests/test-config-error-handling.sh:93-94`, `tests/test-config-error-handling.sh:121-122`, `tests/test-config-error-handling.sh:139-140`, `tests/test-config-error-handling.sh:161-162`). Outputs are test status lines, final summary, and process exit status. The loader output under test is merged JSON on stdout and warnings/errors on stderr. State transitions are fixture creation, optional config mutation with `printf`, invocation of `load_merged_config`, extraction of `bitlesson_model`, then pass/fail counter increments in `tests/test-helpers.sh:30-44`. The expected default value comes from `config/default_config.json:4`, where `"bitlesson_model": "haiku"`.

- gates_or_invariants: Required default config is a hard invariant: missing `plugin_root/config/default_config.json` must make `load_merged_config` fail, not silently synthesize defaults (`tests/test-config-error-handling.sh:37-53`). Optional config layers are soft: malformed project/user configs must warn and degrade to `{}`, while missing project/user files must also degrade to `{}` without fatal exit (`tests/test-config-error-handling.sh:56-110`, `tests/test-config-error-handling.sh:130-172`). The loader implementation enforces those gates through `_config_loader_prepare_layer`: required missing or malformed config emits an error and exits the loader subshell (`scripts/lib/config-loader.sh:40-56`), while optional missing config writes `{}` and optional malformed config warns then writes `{}` (`scripts/lib/config-loader.sh:48-59`). Another invariant is that optional config must be a JSON object, because `_config_loader_prepare_layer` uses `jq -e 'if type == "object" then . else error(...) end'` before accepting a layer (`scripts/lib/config-loader.sh:52`). The merge invariant is default/user/project object layering with null stripping before jq object multiplication (`scripts/lib/config-loader.sh:113-132`), so optional bad layers should not affect defaults.

- dependencies_and_callers: Direct dependencies are Bash, `mktemp`, `mkdir`, `printf`, `grep`, `jq` via the loader, `tests/test-helpers.sh`, `scripts/lib/config-loader.sh`, and `config/default_config.json`. The sourced helper supplies `setup_test_dir`, `pass`, `fail`, and `print_test_summary` (`tests/test-helpers.sh:30-89`). The loader functions under test are `load_merged_config` and `get_config_value` (`scripts/lib/config-loader.sh:63-159`). The production caller found for this config path is `scripts/bitlesson-select.sh`, which loads merged config and reads `bitlesson_model`, `codex_model`, and `provider_mode`. Related sibling validation is `tests/test-config-merge.sh`, which covers positive merge priority, additive user/project behavior, null stripping, and `HUMANIZE_CONFIG` override, while this assigned file focuses on error handling and optional-layer degradation.

- edge_cases_or_failure_modes: The test covers the most important loader failure boundary: a missing required default config must not be ignored (`tests/test-config-error-handling.sh:40-53`). It also covers malformed optional project config text that is not JSON at all (`tests/test-config-error-handling.sh:59-80`) and malformed optional user config text with invalid JSON syntax (`tests/test-config-error-handling.sh:87-109`). It treats `{}` as a valid project config, preventing empty object configs from being misclassified as malformed (`tests/test-config-error-handling.sh:116-128`). It confirms both absent `.humanize/config.json` and a non-existent `XDG_CONFIG_HOME` path are non-fatal (`tests/test-config-error-handling.sh:134-172`). One subtle test-harness edge case: `setup_test_dir` overwrites a single `trap` each time (`tests/test-helpers.sh:86-89`), so only the most recent temp directory is guaranteed to be cleaned by the final trap; this is test infrastructure behavior rather than the config algorithm contract. Another edge case not directly asserted here but enforced by loader code is missing `jq`, which causes `_config_loader_require_jq` to fail before layer preparation (`scripts/lib/config-loader.sh:17-21`, `scripts/lib/config-loader.sh:75`).

- validation_or_tests: This file is itself the validation artifact. It encodes six pass/fail checks and uses `print_test_summary` as the exit gate. I did not execute the test because the worker instruction is research-only and the branch export is read-only; instead, I inspected the executable spec and the referenced implementation. Related validation coverage exists in `tests/test-config-merge.sh`, especially default-only behavior, priority order, additive merging, null stripping, and `HUMANIZE_CONFIG` override. Together, the two files define the loader contract: required defaults must be present and valid; optional user/project layers may be absent or malformed without blocking the caller; valid higher layers override or add keys; and fallback value extraction through `get_config_value` returns scalar strings suitable for shell callers.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 item evidence heading; the assigned item_id appears exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`