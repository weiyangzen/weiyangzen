# agent_111 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-111 `file` `prompt-template/block/bitlesson-delta-invalid.md`
- cursor: `[_]`
- core_role: This file is a human-facing prompt/block template for the BitLesson stop-gate. It is not executable logic itself, but it is part of the validation contract used when a round summary contains a `## BitLesson Delta` section whose action is missing, duplicated, or outside the allowed action vocabulary. The direct template content is only seven lines: title at `prompt-template/block/bitlesson-delta-invalid.md:1`, failure explanation at `prompt-template/block/bitlesson-delta-invalid.md:3`, and the allowed action set at `prompt-template/block/bitlesson-delta-invalid.md:5` through `prompt-template/block/bitlesson-delta-invalid.md:7`.

- algorithmic_behavior: The actual validation algorithm lives in `scripts/bitlesson-validate-delta.sh`. It extracts a visible `## BitLesson Delta` section from the summary while ignoring headings inside fenced code blocks and HTML comments (`scripts/bitlesson-validate-delta.sh:92` through `scripts/bitlesson-validate-delta.sh:179`). It then parses action candidates with a strict `Action:` line matcher, lowercases the value, counts non-empty matches, and selects the first candidate (`scripts/bitlesson-validate-delta.sh:202` through `scripts/bitlesson-validate-delta.sh:204`). If the action count is not exactly one, or if the action is not `none`, `add`, or `update`, the validator loads this assigned template via `load_and_render_safe` and emits a blocking JSON decision (`scripts/bitlesson-validate-delta.sh:206` through `scripts/bitlesson-validate-delta.sh:218`). In short: this template is the rendered explanation for the invalid-action branch of the BitLesson Delta state machine.

- inputs_outputs_state: Inputs are indirect: the validator receives `--summary-file`, `--bitlesson-file`, `--bitlesson-relpath`, `--allow-empty-none`, `--template-dir`, and `--current-round` (`scripts/bitlesson-validate-delta.sh:7` through `scripts/bitlesson-validate-delta.sh:13`). This template itself takes no placeholder variables; it is static markdown. Output is the markdown reason string loaded from `prompt-template/block/bitlesson-delta-invalid.md`, placed into the `.reason` field of a JSON object with `"decision": "block"` and a `systemMessage` like “Loop: BitLesson Delta must include action none/add/update” (`scripts/bitlesson-validate-delta.sh:78` through `scripts/bitlesson-validate-delta.sh:89`, and `scripts/bitlesson-validate-delta.sh:216` through `scripts/bitlesson-validate-delta.sh:217`). No repository state is mutated by this template. The observable state transition is procedural: a non-finalize loop round with a required BitLesson delta moves from “summary accepted for further stop-hook checks” to “blocked with remediation prompt.”

- gates_or_invariants: The invariant represented by this file is exactly one allowed BitLesson action. The accepted action set is `none`, `add`, and `update`, matching the template lines and the shell condition at `scripts/bitlesson-validate-delta.sh:206`. The summary contract in documentation also defines the required shape as `- Action: none|add|update` (`docs/bitlesson.md:38` through `docs/bitlesson.md:45`). The broader gate only runs when BitLesson is required and the loop is not in finalize phase (`hooks/loop-codex-stop-hook.sh:823` through `hooks/loop-codex-stop-hook.sh:838`). The template-loader fallback behavior is important: if this file is missing or renders empty, `load_and_render_safe` falls back to an inline hardcoded message with the same action requirement (`hooks/lib/template-loader.sh:185` through `hooks/lib/template-loader.sh:210`, `scripts/bitlesson-validate-delta.sh:207` through `scripts/bitlesson-validate-delta.sh:214`).

- dependencies_and_callers: Direct caller is `scripts/bitlesson-validate-delta.sh`, specifically the invalid-action branch loading `block/bitlesson-delta-invalid.md` (`scripts/bitlesson-validate-delta.sh:216`). That validator is invoked by the Codex stop hook for required BitLesson rounds (`hooks/loop-codex-stop-hook.sh:823` through `hooks/loop-codex-stop-hook.sh:830`). Template loading depends on `hooks/lib/template-loader.sh`, whose `load_template`, `render_template`, and `load_and_render_safe` functions provide static or rendered markdown content (`hooks/lib/template-loader.sh:33` through `hooks/lib/template-loader.sh:45`, `hooks/lib/template-loader.sh:56` through `hooks/lib/template-loader.sh:135`, `hooks/lib/template-loader.sh:188` through `hooks/lib/template-loader.sh:210`). Sibling templates in the same gate family are `prompt-template/block/bitlesson-delta-missing.md`, `prompt-template/block/bitlesson-delta-inconsistent.md`, `prompt-template/block/bitlesson-delta-empty-kb.md`, and `prompt-template/block/bitlesson-delta-missing-notes.md`.

- edge_cases_or_failure_modes: The invalid-action branch fires when the summary has zero `Action:` lines inside the extracted delta block, more than one non-empty `Action:` line, or one action whose normalized value is not `none`, `add`, or `update` (`scripts/bitlesson-validate-delta.sh:202` through `scripts/bitlesson-validate-delta.sh:218`). Because the action matcher only captures alphabetic values, malformed values containing digits, punctuation, slashes, or compound tokens may count as no valid candidate rather than as a parsed invalid value; the gate still blocks through this same template. Action headings inside fenced code or HTML comments do not count because extraction ignores those sections (`scripts/bitlesson-validate-delta.sh:104` through `scripts/bitlesson-validate-delta.sh:151`). If the assigned template is absent, the validator still blocks using its inline fallback, so the algorithmic gate does not fail open.

- validation_or_tests: Focused tests exist for the validator in `tests/test-bitlesson-validate-delta.sh`. The current visible coverage verifies related BitLesson Delta gate behavior: missing visible section when inside fenced code or HTML comments blocks (`tests/test-bitlesson-validate-delta.sh:116` through `tests/test-bitlesson-validate-delta.sh:142`), missing or placeholder notes for `add`/`update` blocks (`tests/test-bitlesson-validate-delta.sh:101` through `tests/test-bitlesson-validate-delta.sh:114`), and valid `add`/`update` summaries pass (`tests/test-bitlesson-validate-delta.sh:144` through `tests/test-bitlesson-validate-delta.sh:152`). I did not find a direct test case in that file specifically asserting that an invalid or duplicated `Action:` value renders this exact template. I did not run the test suite because this task requested read-only research notes only; inspection was performed with read-only shell commands.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section present exactly once above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`