# agent_189 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-189 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: This file is an executable black-box specification for RLCR plan-file path and content validation. It does not implement the validator itself; it drives the production entrypoint [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/setup-rlcr-loop.sh:445) with controlled path inputs and asserts whether validation accepts or rejects each case. The test is part of the full test matrix via [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/run-all-tests.sh:105).
- algorithmic_behavior: The script creates an isolated temporary Git repository, installs a mock `codex`, generates plan files, invokes the production setup script, and classifies the result from stderr/stdout patterns. Initialization happens at [tests/robustness/test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-path-validation-robustness.sh:12): it resolves `SCRIPT_DIR`/`PROJECT_ROOT`, sources shared test helpers, calls `setup_test_dir`, and builds a clean Git repo with `.gitignore` set so plan files stay untracked/ignored. The mock `codex` at line 22 prevents the test from calling the real external tool after validation passes. `create_valid_plan` at line 50 writes a plan with enough non-comment content to satisfy production content checks. The central classifier `test_path_validation` at line 78 removes any prior `.humanize/rlcr` state, runs `scripts/setup-rlcr-loop.sh` with `CLAUDE_PROJECT_DIR="$TEST_DIR"`, then returns rejected if output matches validation-error patterns, accepted if output reaches later non-path gates such as “requires codex” or “must be gitignored,” and failed for other nonzero exits.
- inputs_outputs_state: Inputs are candidate plan paths passed as a single positional argument to `setup-rlcr-loop.sh` through `test_path_validation` at line 88. The tested paths include safe relative names (`docs/plan.md`, `plan.md`, dash/underscore names, nested paths, dotted filenames), dangerous shell/path syntax, symlinks, long/deep paths, missing files, directories, and insufficient plan contents. Outputs are TAP-like human-readable `PASS`/`FAIL` lines produced by shared helper functions from [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:30), accumulated counters, and a final process exit from `print_test_summary` at [tests/robustness/test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-path-validation-robustness.sh:486). State transitions are deliberately reset per subtest by deleting `$TEST_DIR/.humanize/rlcr` at line 84, preventing “loop already active” state from masking path-validation outcomes.
- gates_or_invariants: The file encodes the expected validation contract of the production setup algorithm. Valid relative paths are accepted for normal names, root filenames, dash/underscore, nested directories, dotted filenames, long filenames where the filesystem permits, and ten-level nested paths at lines 122-169 and 387-412. Absolute paths must be rejected at line 179, spaces at line 188, and shell metacharacters from semicolon through exclamation mark at lines 199-332. File symlinks, parent-directory symlinks, and symlink chains must be rejected at lines 342-377. Empty, comments-only, short, missing, and directory “plan files” must be rejected at lines 425-480. These assertions correspond directly to production gates: relative-only path validation at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/setup-rlcr-loop.sh:455), no spaces at line 461, no shell metacharacters at line 469, no plan-file symlink at line 481, parent directory existence at line 487, no parent symlink traversal at line 494, file/readability checks at lines 517 and 523, in-project resolved path containment at line 529, minimum five lines at line 618, and at least three meaningful content lines at line 666.
- dependencies_and_callers: Direct dependencies are Bash with `set -euo pipefail`, `git`, `ln`, `grep`, `mktemp`, `chmod`, and the production script [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/setup-rlcr-loop.sh:445). It depends on [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:86) for temp directory setup and cleanup, plus `pass`, `fail`, and summary counters. The test uses `CLAUDE_PROJECT_DIR="$TEST_DIR"` when invoking setup so the production script validates paths against the isolated test repository rather than the source tree. It is called by the aggregate test runner as `robustness/test-path-validation-robustness.sh` in [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/run-all-tests.sh:105).
- edge_cases_or_failure_modes: The classifier intentionally treats some later setup failures as proof that path validation passed, especially “requires codex” and “must be gitignored” at lines 97-104. Because this is output-pattern based, changes to production error wording can cause false rejections or false acceptances unless the regex at line 93 is kept in sync. The mock `codex` is created on `PATH`, but the classifier still contains a “requires codex” fallback, which suggests compatibility with environments where the setup script detects codex differently. The long filename case at lines 387-400 is conditional: if the filesystem refuses the filename, the test records a pass for a skipped condition rather than failing. The symlink-chain case at lines 367-377 is mostly covered by the leaf `-L` check in production, while parent symlink traversal at lines 352-365 exercises the more security-relevant directory-walk guard. Unicode paths are noted as allowed at lines 414-415 but are not actively tested in this file, leaving a coverage gap for the “now ALLOWED” policy.
- validation_or_tests: I directly inspected the assigned file with line numbers and its production validation target. I also ran `bash -n tests/robustness/test-path-validation-robustness.sh`; it exited successfully with no syntax errors. A `git status --short` probe could not validate worktree cleanliness because this read-only branch export is not a Git checkout in the sandbox and macOS developer tooling attempted to create denied cache files under `/tmp`; no repository files were modified. The file itself provides 31 named subtests covering positive paths, invalid characters, symlink traversal, long/deep paths, and content validation.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 unique item section present; no additional assigned item sections emitted
- missing_items: 0
- duplicate_items: 0
- final_worker_status: `complete`