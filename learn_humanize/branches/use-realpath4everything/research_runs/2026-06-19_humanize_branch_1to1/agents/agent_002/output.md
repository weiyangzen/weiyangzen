# agent_002 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role:
  - `agents/` is a prompt-specification directory for Humanize subagents, not an executable algorithm directory. Recursive inspection found exactly four files and no subdirectories: `agents/bitlesson-selector.md`, `agents/draft-relevance-checker.md`, `agents/plan-compliance-checker.md`, and `agents/plan-understanding-quiz.md`.
  - The directory’s algorithmic responsibility is to encode decision procedures for pre-flight validation and per-task lesson selection in markdown agent frontmatter plus deterministic output contracts. The files are consumed by command workflows and packaging/install surfaces, while the actual shell execution and path handling live outside this directory.
  - For the branch theme, this directory does not implement realpath/canonicalization itself. A focused search found canonical path behavior in the related runtime script, not under `agents/`: `scripts/bitlesson-select.sh` uses `resolve_project_root` at line 15 and `pwd -P` for the BitLesson file directory at line 137.

- algorithmic_behavior:
  - `agents/draft-relevance-checker.md` defines a lenient semantic relevance classifier for draft documents. It instructs the agent to inspect repository docs/structure, compare the draft against repository technologies, paths, functions, and features, then output exactly `RELEVANT: <brief explanation>` or `NOT_RELEVANT: <brief explanation>` (`agents/draft-relevance-checker.md:14`, `agents/draft-relevance-checker.md:16`, `agents/draft-relevance-checker.md:21`, `agents/draft-relevance-checker.md:27`). Its core gate is intentionally permissive: uncertain cases should be marked relevant (`agents/draft-relevance-checker.md:33`, `agents/draft-relevance-checker.md:36`).
  - `agents/plan-compliance-checker.md` defines a two-stage validator before RLCR setup: repository relevance and branch-switch detection (`agents/plan-compliance-checker.md:14`, `agents/plan-compliance-checker.md:16`, `agents/plan-compliance-checker.md:31`). It accepts plans related to the repository, rejects clearly unrelated plans, and rejects instructions that require switching, checking out, creating branches, or creating worktrees during implementation (`agents/plan-compliance-checker.md:23`, `agents/plan-compliance-checker.md:33`, `agents/plan-compliance-checker.md:37`). It also defines safe disambiguations, such as `git checkout -- <file>`, negated branch-switching instructions, descriptive branch references, and `--base-branch` parameters (`agents/plan-compliance-checker.md:40`).
  - `agents/plan-understanding-quiz.md` defines an advisory comprehension-check generator. It analyzes the plan and repository context, then emits exactly two multiple-choice questions, each with four options and one answer, plus a short technical plan summary (`agents/plan-understanding-quiz.md:14`, `agents/plan-understanding-quiz.md:24`, `agents/plan-understanding-quiz.md:31`, `agents/plan-understanding-quiz.md:53`). Its required output has 13 fixed fields, including `QUESTION_1`, `OPTION_1A-D`, `ANSWER_1`, `QUESTION_2`, `OPTION_2A-D`, `ANSWER_2`, and `PLAN_SUMMARY` (`agents/plan-understanding-quiz.md:57`, `agents/plan-understanding-quiz.md:61`, `agents/plan-understanding-quiz.md:74`, `agents/plan-understanding-quiz.md:79`).
  - `agents/bitlesson-selector.md` defines lesson-selection logic for sub-tasks. Inputs are the current sub-task, related file paths, and configured BitLesson content, normally `.humanize/bitlesson.md` (`agents/bitlesson-selector.md:10`, `agents/bitlesson-selector.md:12`, `agents/bitlesson-selector.md:14`). Its selection rule is precision-first: include only directly relevant lessons, avoid weak matches, and return `NONE` when nothing applies (`agents/bitlesson-selector.md:26`, `agents/bitlesson-selector.md:28`, `agents/bitlesson-selector.md:30`). It requires a stable two-line output: `LESSON_IDS` and `RATIONALE` (`agents/bitlesson-selector.md:32`, `agents/bitlesson-selector.md:34`, `agents/bitlesson-selector.md:37`).

- inputs_outputs_state:
  - Directory inputs are not files read directly by shell code at runtime in this branch export; they are prompt specs selected by command/task systems and packaged as runtime dependencies. Each file declares frontmatter with `name`, `description`, `model`, and `tools` where applicable (`agents/draft-relevance-checker.md:1`, `agents/plan-compliance-checker.md:1`, `agents/bitlesson-selector.md:1`, `agents/plan-understanding-quiz.md:1`).
  - Draft relevance input: a draft document body plus lightweight repository exploration. Output is one verdict line, either `RELEVANT` or `NOT_RELEVANT` (`agents/draft-relevance-checker.md:14`, `agents/draft-relevance-checker.md:27`).
  - Plan compliance input: a plan file body plus repository exploration. Output is exactly one of `PASS`, `FAIL_RELEVANCE`, or `FAIL_BRANCH_SWITCH` (`agents/plan-compliance-checker.md:14`, `agents/plan-compliance-checker.md:48`, `agents/plan-compliance-checker.md:50`).
  - Plan quiz input: a plan file body plus repository context. Output is a structured quiz/summary block with exactly all 13 fields; malformed output is handled by the caller as non-blocking advisory failure (`agents/plan-understanding-quiz.md:57`, `commands/start-rlcr-loop.md:86`).
  - BitLesson input: sub-task description, related paths, and BitLesson knowledge-base content. Output is a stable two-line lesson selection (`agents/bitlesson-selector.md:14`, `agents/bitlesson-selector.md:36`). The actual runtime script enforces equivalent output parsing by extracting the first `LESSON_IDS:` and `RATIONALE:` lines and failing if either is absent (`scripts/bitlesson-select.sh:242`, `scripts/bitlesson-select.sh:250`, `scripts/bitlesson-select.sh:258`).
  - State transitions are mostly outside `agents/`. These specs influence workflow gates: `gen-plan` proceeds from draft validation to plan creation only on `RELEVANT` (`commands/gen-plan.md:171`, `commands/gen-plan.md:176`); `start-rlcr-loop` proceeds only on `PASS` for compliance but treats the quiz as advisory (`commands/start-rlcr-loop.md:52`, `commands/start-rlcr-loop.md:60`, `commands/start-rlcr-loop.md:62`); BitLesson selection feeds implementation notes and later BitLesson delta validation (`commands/start-rlcr-loop.md:168`, `commands/start-rlcr-loop.md:173`).

- gates_or_invariants:
  - Frontmatter invariant: each agent file declares a stable name/model/tools contract. For example, `draft-relevance-checker` uses `haiku` and `Read, Glob, Grep` (`agents/draft-relevance-checker.md:2`, `agents/draft-relevance-checker.md:4`, `agents/draft-relevance-checker.md:5`); `plan-compliance-checker` uses `sonnet` (`agents/plan-compliance-checker.md:4`); `plan-understanding-quiz` uses `opus` (`agents/plan-understanding-quiz.md:4`); `bitlesson-selector` uses `haiku` and `Read, Grep` (`agents/bitlesson-selector.md:4`, `agents/bitlesson-selector.md:5`).
  - Relevance gates are intentionally lenient. Both draft and plan relevance checks reject only clearly unrelated content and pass uncertain cases (`agents/draft-relevance-checker.md:33`, `agents/draft-relevance-checker.md:36`, `agents/plan-compliance-checker.md:29`, `agents/plan-compliance-checker.md:59`).
  - Branch-switch gate is the strongest blocking invariant in the directory. RLCR requires the working branch to remain constant across rounds, so plans mandating branch changes fail with `FAIL_BRANCH_SWITCH` (`agents/plan-compliance-checker.md:46`, `agents/plan-compliance-checker.md:54`).
  - Quiz output invariant: always output all 13 fields; answers must be exactly `A`, `B`, `C`, or `D`; questions should match the plan language and implementation substance (`agents/plan-understanding-quiz.md:79`, `agents/plan-understanding-quiz.md:80`, `agents/plan-understanding-quiz.md:82`).
  - BitLesson invariant: select only directly relevant lessons and return `NONE` when no direct match exists (`agents/bitlesson-selector.md:28`, `agents/bitlesson-selector.md:30`). The shell runtime adds stricter runtime invariants: required non-empty `--task`, non-empty `--paths`, existing non-empty BitLesson file, placeholder short-circuit to `LESSON_IDS: NONE`, provider routing, timeout handling, and stable output extraction (`scripts/bitlesson-select.sh:76`, `scripts/bitlesson-select.sh:82`, `scripts/bitlesson-select.sh:94`, `scripts/bitlesson-select.sh:99`, `scripts/bitlesson-select.sh:105`, `scripts/bitlesson-select.sh:115`, `scripts/bitlesson-select.sh:186`, `scripts/bitlesson-select.sh:227`).

- dependencies_and_callers:
  - `commands/gen-plan.md` calls `humanize:draft-relevance-checker` after IO validation and before output plan creation (`commands/gen-plan.md:154`, `commands/gen-plan.md:160`, `commands/gen-plan.md:161`). Its caller-side state transition is stop on `NOT_RELEVANT`, otherwise copy the template and append the draft (`commands/gen-plan.md:171`, `commands/gen-plan.md:176`).
  - `commands/start-rlcr-loop.md` calls `humanize:plan-compliance-checker` during the Plan Compliance Pre-Check (`commands/start-rlcr-loop.md:13`, `commands/start-rlcr-loop.md:41`). It fails closed on malformed output and blocks on relevance or branch-switch failures (`commands/start-rlcr-loop.md:52`, `commands/start-rlcr-loop.md:54`, `commands/start-rlcr-loop.md:55`, `commands/start-rlcr-loop.md:56`).
  - `commands/start-rlcr-loop.md` calls `humanize:plan-understanding-quiz` after compliance, unless skipped by `--skip-impl`, `--yolo`, `--skip-quiz`, help flags, or missing plan content (`commands/start-rlcr-loop.md:64`, `commands/start-rlcr-loop.md:75`). It asks the user two questions and allows proceeding or stopping after wrong answers (`commands/start-rlcr-loop.md:90`, `commands/start-rlcr-loop.md:100`).
  - `agents/bitlesson-selector.md` documents that runtime execution happens via `scripts/bitlesson-select.sh`, routed to Codex or Claude based on configured `bitlesson_model` (`agents/bitlesson-selector.md:19`, `agents/bitlesson-selector.md:21`, `agents/bitlesson-selector.md:22`). `commands/start-rlcr-loop.md` requires running `bitlesson-selector` for each task/sub-task and recording lesson IDs or `NONE` (`commands/start-rlcr-loop.md:173`, `commands/start-rlcr-loop.md:175`).
  - Packaging surfaces include `agents/` as a runtime dependency for Codex installs (`docs/install-for-codex.md:48`, `docs/install-for-codex.md:54`, `docs/install-for-codex.md:66`) and Kimi installs (`docs/install-for-kimi.md:56`, `docs/install-for-kimi.md:63`). `scripts/install-skill.sh` installs a PATH shim for `bitlesson-selector` that dispatches to installed `scripts/bitlesson-select.sh` under candidate runtime roots (`scripts/install-skill.sh:334`, `scripts/install-skill.sh:354`, `scripts/install-skill.sh:368`, `scripts/install-skill.sh:370`).

- edge_cases_or_failure_modes:
  - `draft-relevance-checker` can false-pass unrelated but superficially connected drafts because its rule explicitly leans relevant on doubt (`agents/draft-relevance-checker.md:33`, `agents/draft-relevance-checker.md:36`). That is by design to avoid rejecting plausible planning input.
  - `plan-compliance-checker` can false-pass ambiguous branch-related wording because it leans pass on branch-switch uncertainty (`agents/plan-compliance-checker.md:60`). It specifically avoids false positives for file restore, negated branch-switch instructions, descriptive branch references, and `--base-branch` settings (`agents/plan-compliance-checker.md:40`).
  - `plan-understanding-quiz` degrades gracefully at the caller: malformed quiz output does not block RLCR setup; the command warns and continues (`commands/start-rlcr-loop.md:86`). A very short or technically thin plan still requires derived questions from available implementation hints (`agents/plan-understanding-quiz.md:84`).
  - `bitlesson-selector` has divergence risk between the markdown prompt spec and `scripts/bitlesson-select.sh`, because the runtime script embeds its own prompt text and adds a fourth decision rule forbidding tools/repository inspection (`scripts/bitlesson-select.sh:166`, `scripts/bitlesson-select.sh:171`). The markdown spec says the agent may use `Read, Grep` (`agents/bitlesson-selector.md:5`), while the runtime prompt says to use only prompt information. This is not a branch-path issue, but it is a maintenance edge case for behavior consistency.
  - Runtime BitLesson failures outside `agents/` include missing required args, missing or whitespace-only BitLesson file, unsupported provider, model binary dependency fallback, timeout, nonzero selector exit, and malformed output (`scripts/bitlesson-select.sh:76`, `scripts/bitlesson-select.sh:88`, `scripts/bitlesson-select.sh:94`, `scripts/bitlesson-select.sh:100`, `scripts/bitlesson-select.sh:220`, `scripts/bitlesson-select.sh:227`, `scripts/bitlesson-select.sh:232`, `scripts/bitlesson-select.sh:258`).
  - For the use-realpath branch concern, `agents/` has no direct symlink/realpath behavior. The nearest related canonicalization is `BITLESSON_DIR="$(cd "$(dirname "$BITLESSON_FILE")" && pwd -P)"` in the selector runtime (`scripts/bitlesson-select.sh:137`), followed by git root discovery or fallback to that physical directory (`scripts/bitlesson-select.sh:138`, `scripts/bitlesson-select.sh:141`).

- validation_or_tests:
  - Recursive coverage validation: `find agents -type f -print | sort` returned all four files, and `wc -c agents/*` totaled `12261`, matching the assigned directory byte count.
  - `tests/test-gen-plan.sh` validates that `agents/draft-relevance-checker.md` exists (`tests/test-gen-plan.sh:111`), has the expected `name` (`tests/test-gen-plan.sh:254`, `tests/test-gen-plan.sh:256`), uses `haiku` (`tests/test-gen-plan.sh:268`, `tests/test-gen-plan.sh:270`), has a `tools` field (`tests/test-gen-plan.sh:282`, `tests/test-gen-plan.sh:283`), follows naming rules (`tests/test-gen-plan.sh:372`), has required frontmatter (`tests/test-gen-plan.sh:420`, `tests/test-gen-plan.sh:422`), uses a valid model (`tests/test-gen-plan.sh:523`, `tests/test-gen-plan.sh:526`), and avoids emoji/CJK content (`tests/test-gen-plan.sh:548`, `tests/test-gen-plan.sh:552`).
  - `tests/test-bitlesson-select-routing.sh` validates the runtime script associated with `agents/bitlesson-selector.md`: it creates mock BitLesson files and mock Codex/Claude binaries (`tests/test-bitlesson-select-routing.sh:18`, `tests/test-bitlesson-select-routing.sh:67`, `tests/test-bitlesson-select-routing.sh:119`), checks provider routing, checks `codex-only` behavior (`tests/test-bitlesson-select-routing.sh:400`, `tests/test-bitlesson-select-routing.sh:409`), and verifies placeholder BitLesson files short-circuit to `LESSON_IDS: NONE` without invoking a model (`tests/test-bitlesson-select-routing.sh:416`, `tests/test-bitlesson-select-routing.sh:433`).
  - I did not run the test suite because the task requested research notes only and the branch export is read-only. A `git status --short` attempt failed because this read-only export is not a git repository and the restricted environment could not create Xcode temp cache files.

- skip_candidate: `yes: this directory is a set of markdown agent prompt specifications and packaging inputs, not executable core path-canonicalization logic. It is relevant to Humanize workflow gating, but it does not itself implement realpath behavior; the directly related canonical path handling observed during research is in scripts/bitlesson-select.sh, outside the assigned directory.`

## Worker Self-Test
- assigned_items_seen: `1/1`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`