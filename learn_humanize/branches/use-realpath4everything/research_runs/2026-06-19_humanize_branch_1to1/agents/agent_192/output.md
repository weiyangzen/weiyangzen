# agent_192 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-192 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: This file is a black-box executable specification for the setup side of the RLCR loop. It drives [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/setup-rlcr-loop.sh:1) through adversarial argument, path, git, dependency, timeout, YAML-safety, and `--skip-impl` scenarios. It is core algorithm coverage because it asserts the setup algorithm’s acceptance/rejection gates and the state files that must be created before the loop can run.

- algorithmic_behavior: The harness initializes strict shell mode, resolves its own root paths, sources shared pass/fail helpers and portable timeout logic, then creates a temporary test workspace via `setup_test_dir` at [tests/robustness/test-setup-scripts-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-setup-scripts-robustness.sh:13). Its helper flow is: `create_minimal_plan` writes a markdown implementation plan with goal, acceptance criteria, and steps at line 31; `init_basic_git_repo` creates a clean one-commit git repo on `main` at line 51; `prepare_runtime_bin` symlinks a constrained runtime toolset for restricted-`PATH` tests at line 67; `run_rlcr_setup` `cd`s into the synthetic repo and invokes the real setup script with `CLAUDE_PROJECT_DIR` set to that repo at line 84. Each test mutates only its temp repo, captures `OUTPUT` and `EXIT_CODE`, and calls `pass`, `fail`, or `skip`.

- inputs_outputs_state: Inputs are command-line arguments passed to `setup-rlcr-loop.sh`, synthetic plan file contents and paths, synthetic git states, mocked `codex`/`jq`/`timeout` binaries, branch names, and `PATH`/`CLAUDE_PROJECT_DIR`. Outputs are test-result counters printed through [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:30), plus the subprocess output and exit status used as assertions. The file expects setup to create `.humanize/rlcr/<timestamp>/` state in success-path cases, especially `state.md`, `.review-phase-started`, `goal-tracker.md`, `round-0-summary.md`, `round-0-contract.md`, and `round-0-prompt.md` during `--skip-impl` tests at [tests/robustness/test-setup-scripts-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-setup-scripts-robustness.sh:885). Final state is summarized by `print_test_summary` and the script exits with that status at line 1191.

- gates_or_invariants: Argument gates cover help output, missing plans, nonnumeric/negative/empty `--max`, invalid `--codex-timeout`, missing `--codex-model` value, unknown options, and duplicate positional plus `--plan-file` at lines 102-220. Plan path/content gates reject comment-only plans, plans under five lines, paths with spaces, shell metacharacters, absolute paths, direct plan-file symlinks, and symlinked parent directories at lines 230-347 and 540-593. Git gates reject non-git directories, repos without commits, tracked plans without `--track-plan-file`, dirty working trees including untracked `.humanizeconfig`, and active loop directories at lines 418-530. YAML/state serialization gates reject branch names with YAML-unsafe characters and codex model values with invalid characters at lines 357-408. Numeric invariants include `--full-review-round >= 2` and numeric-only review-round/timeout values at lines 770-859. Dependency gates verify missing `codex`, missing `jq`, multiple missing dependencies, and all-dependencies-present behavior at lines 1059-1185.

- dependencies_and_callers: Direct dependencies are Bash, git, standard Unix tools, [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:86) for temp-dir cleanup and result accounting, [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/portable-timeout.sh:33) for timeout behavior, and the production [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/setup-rlcr-loop.sh:190). The production setup script sections under test include dependency collection at lines 340-360, plan-file merge and missing-plan handling at lines 399-427, git validation at lines 433-443, path and symlink validation at lines 455-541, plan tracking/content validation at lines 562-676, YAML-safe branch/model validation at lines 686-716, and skip-implementation state generation at lines 829-1285. This robustness test is included in the full suite list at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/run-all-tests.sh:101), specifically line 113.

- edge_cases_or_failure_modes: The file probes several fail-closed boundaries: timeout command returning 124 during git rev-parse at lines 695-750; portable mock behavior that avoids hardcoded `/usr/bin/git` at lines 752-760; restricted `PATH` cases where interpreter/runtime tools must remain available while `codex` or `jq` is hidden at lines 1059-1158; platform-dependent branch-name and symlink creation, which are tolerated through conditional pass/skip behavior at lines 370-385 and 555-593; and environmental variance where real installed `codex`/`jq` may make a missing-dependency scenario untestable, handled with `skip` at lines 1081 and 1111. One notable encoded behavior is that `--codex-timeout 0` is accepted because the current regex allows non-negative numeric strings, despite error text saying “positive integer,” at lines 657-673.

- validation_or_tests: The file itself is the validation asset. It contains 49 numbered scenarios with some gaps in numbering, grouped as argument parsing, plan-file validation, YAML safety, git edge cases, mutual exclusion, symlink protection, positive paths, timeout scenarios, full-review-round validation, skip-implementation mode, and dependency checks. I inspected the assigned file directly and followed references to helper, timeout, suite, workflow, and target setup-script sections. I did not execute the test because this worker task is read-only research and the script intentionally creates temp repos and mocked executables; no repository files were modified.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1; the single assigned item appears once as the Item Evidence heading.
- missing_items: 0
- duplicate_items: 0
- final_worker_status: `complete`