# agent_125 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-125 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: This is a blocking-message template used by the RLCR Bash pre-tool validation path when an agent tries to modify `goal-tracker.md` through shell commands. The file is not executable algorithm code, but it is part of the algorithmic guard contract: it defines the user-facing remediation for a denied Bash write and directs the agent to use hook-visible Write/Edit tooling instead. Direct file content is at `prompt-template/block/goal-tracker-bash-write.md:1-8`.
- algorithmic_behavior: The template emits a Markdown block titled `Bash Write Blocked: Use Write or Edit Tool`, forbids Bash-based `goal-tracker.md` modification, renders the active tracker path through `{{CORRECT_PATH}}`, and explains that shell commands such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks. At runtime, `hooks/loop-bash-validator.sh:491-499` detects a command that modifies `goal-tracker.md`; if `CURRENT_ROUND` is `0`, it calls `goal_tracker_bash_blocked_message`, writes this rendered template to stderr, and exits with code `2`. For rounds after `0`, the same detector routes to `goal_tracker_blocked_message` instead, so this specific template is the Round 0 Bash remediation.
- inputs_outputs_state: Inputs are the template variable `CORRECT_PATH` and the validator context that computes it as `$ACTIVE_LOOP_DIR/goal-tracker.md` in `hooks/loop-bash-validator.sh:491-494`. The renderer is called from `hooks/lib/loop-common.sh:905-911`, where `correct_path` is passed into `load_and_render_safe` for `block/goal-tracker-bash-write.md`. Output is a rendered Markdown denial message to stderr; the broader validator output is paired with exit code `2` from `hooks/loop-bash-validator.sh:498`. State is intentionally unchanged: the blocked Bash command should not modify the tracker, and the instruction pushes future mutation through Write/Edit so downstream validators can inspect content.
- gates_or_invariants: The key invariant is that `goal-tracker.md` must not be modified through Bash, even in Round 0 initialization/update flows. The gate exists because Write/Edit validators can validate goal-tracker changes while raw Bash redirection or in-place shell edits can bypass those content checks. `hooks/loop-bash-validator.sh:486-489` documents the round split: Round 0 prompts Write/Edit; Round greater than 0 uses the stricter goal-tracker modification message. The template itself reinforces the invariant at `prompt-template/block/goal-tracker-bash-write.md:3-8`.
- dependencies_and_callers: Direct caller is `goal_tracker_bash_blocked_message` in `hooks/lib/loop-common.sh:905-911`. That function depends on `load_and_render_safe`, `TEMPLATE_DIR`, and a fallback message if the template cannot be loaded. Runtime caller is the active-loop Bash validator branch in `hooks/loop-bash-validator.sh:491-499`, which depends on `command_modifies_file`, `ACTIVE_LOOP_DIR`, and parsed `CURRENT_ROUND`. Related sibling templates include `prompt-template/block/goal-tracker-modification.md`, used after Round 0, and `prompt-template/block/summary-bash-write.md`, which has the same Bash-denial pattern for summary files.
- edge_cases_or_failure_modes: If the template is missing or unreadable, `hooks/lib/loop-common.sh:907-911` provides a shorter fallback message, so the gate still blocks but with less complete guidance. If `CORRECT_PATH` is wrong because active-loop discovery or realpath normalization is wrong, the denial message can point to the wrong tracker even though the Bash write remains blocked. Detection accuracy depends on `command_modifies_file`; `tests/test-bash-validator-patterns.sh:75-133` covers common redirection, `tee`, in-place editors, file operations, `dd`, `truncate`, and `exec` cases, while `tests/test-bash-validator-patterns.sh:142-152` verifies read-only commands are not treated as writes. A documented limitation appears at `tests/test-bash-validator-patterns.sh:165-168`: multi-source `cp file1 file2 goal-tracker.md` is not detected by the helper pattern.
- validation_or_tests: Existing validation is indirect but concrete. `tests/test-template-references.sh:152-158` includes `block/goal-tracker-bash-write.md` in the cross-reference list so missing templates are caught. `tests/test-bash-validator-patterns.sh:75-168` validates the command-pattern detector that feeds this template path. `tests/robustness/test-hook-system-robustness.sh:402-414` checks that Bash modification of an active loop `goal-tracker.md` after Round 0 exits with code `2`; that exercises the same detector but the post-Round-0 message path. I did not run the tests because this branch export is read-only and the task requested research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1; the single assigned item is present once in Item Evidence`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`