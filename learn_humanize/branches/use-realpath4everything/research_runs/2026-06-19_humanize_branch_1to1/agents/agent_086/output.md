# agent_086 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-086 `file` `tests/test-monitor-e2e-sigint.sh`
- cursor: `[_]`
- core_role: This is an executable test-split wrapper for monitor SIGINT behavior. It does not implement the monitor algorithm itself; it selects and runs the SIGINT subset from the shared real e2e monitor test suite. The file enables strict shell execution, resolves its own directory, sources `tests/test-monitor-e2e-real.sh`, runs `monitor_test_bash_sigint` and `monitor_test_zsh_sigint`, prints the shared pass/fail counters, and exits according to `TESTS_FAILED` at `tests/test-monitor-e2e-sigint.sh:1-21`.
- algorithmic_behavior: The wrapper’s algorithm is linear and stateful through sourced globals. It computes `SCRIPT_DIR` from `${BASH_SOURCE[0]}` so it can source the sibling shared suite regardless of caller cwd (`tests/test-monitor-e2e-sigint.sh:4-5`). It prints a SIGINT-specific banner (`tests/test-monitor-e2e-sigint.sh:7-10`), invokes the bash and zsh SIGINT scenarios in order (`tests/test-monitor-e2e-sigint.sh:12-13`), prints aggregate counters (`tests/test-monitor-e2e-sigint.sh:15-20`), then returns success only when `TESTS_FAILED` is zero (`tests/test-monitor-e2e-sigint.sh:21`). The actual scenario behavior is in the sourced file: `monitor_test_bash_sigint` constructs a fake RLCR project, fake HOME/cache tree, wrapper script, starts the real `_humanize_monitor_codex`, sends SIGINT, waits boundedly, and validates output (`tests/test-monitor-e2e-real.sh:377-550`). `monitor_test_zsh_sigint` repeats the same pattern under zsh when available (`tests/test-monitor-e2e-real.sh:555-686`).
- inputs_outputs_state: Inputs are the shell environment, the repository layout, executable access to `tests/test-monitor-e2e-real.sh`, `scripts/humanize.sh`, and optionally `zsh`. The sourced suite initializes shared mutable counters `TESTS_PASSED=0` and `TESTS_FAILED=0` plus `pass`/`fail` functions (`tests/test-monitor-e2e-real.sh:20-37`). Scenario inputs include generated `state.md`, `goal-tracker.md`, fake `HOME`, fake `XDG_CACHE_HOME`, and cached `round-1-codex-run.log` files (`tests/test-monitor-e2e-real.sh:382-425`, `tests/test-monitor-e2e-real.sh:563-606`). Outputs are human-readable PASS/FAIL lines, debug output, a final summary, temporary captured monitor output files, and process exit code 0 or 1 from the wrapper (`tests/test-monitor-e2e-sigint.sh:15-21`). State transitions include creating `/tmp/test-monitor-e2e-real-$$`, installing an EXIT cleanup trap that kills lingering matching monitor processes and removes the temp base (`tests/test-monitor-e2e-real.sh:43-51`), launching monitor subprocesses (`tests/test-monitor-e2e-real.sh:478-479`, `tests/test-monitor-e2e-real.sh:643-645`), signal delivery, bounded wait loops, and counter increments by `pass`/`fail`.
- gates_or_invariants: The wrapper enforces `set -euo pipefail` (`tests/test-monitor-e2e-sigint.sh:3`) and relies on the sourced file to define both test functions and counters before invocation. The bash scenario requires the monitor process to still be running after startup sleep before SIGINT is sent; otherwise it records a start failure with captured output (`tests/test-monitor-e2e-real.sh:481-527`). Bash sends SIGINT to the process group first, then falls back to the process PID (`tests/test-monitor-e2e-real.sh:492-497`). It waits up to 20 half-second iterations, then may use SIGTERM/SIGKILL cleanup; notably, the bash test still counts forced SIGTERM handling as a pass because the comments treat SIGINT delivery as complex in bash (`tests/test-monitor-e2e-real.sh:498-518`). Bash cleanup evidence must include `Stopped`, `Monitoring stopped`, `interrupt`, `signal`, or a clean `EXIT_CODE:[01]` (`tests/test-monitor-e2e-real.sh:529-541`). Both bash and zsh reject zsh/bash glob errors matching `no matches found|bad pattern` (`tests/test-monitor-e2e-real.sh:544-548`, `tests/test-monitor-e2e-real.sh:680-683`). The zsh scenario is gated by `command -v zsh`; missing zsh produces a skip message rather than a failure (`tests/test-monitor-e2e-real.sh:560-562`).
- dependencies_and_callers: Direct dependency is `tests/test-monitor-e2e-real.sh`, sourced at `tests/test-monitor-e2e-sigint.sh:5`. That shared suite sources and exercises the real monitor implementation through generated runner scripts that call `source "$PROJECT_ROOT/scripts/humanize.sh"` and `_humanize_monitor_codex` (`tests/test-monitor-e2e-real.sh:464-471`, `tests/test-monitor-e2e-real.sh:632-637`). The monitor entry point is `_humanize_monitor_codex` in `scripts/humanize.sh:261`, and the command dispatcher exposes it through `humanize monitor rlcr` at `scripts/humanize.sh:1195-1202`. The implementation depends on shared monitor helpers such as `monitor_find_latest_session`, `monitor_get_file_size`, and `monitor_find_state_file` (`scripts/humanize.sh:279-380`; `scripts/lib/monitor-common.sh:29-63`, `scripts/lib/monitor-common.sh:147-180`). The monitor installs cleanup and signal behavior directly: `_cleanup` stops loops, kills `tail_pid`, restores terminal state, and prints `Stopped monitoring.` (`scripts/humanize.sh:731-762`); bash uses `trap '_cleanup' INT TERM` and zsh defines `TRAPINT`/`TRAPTERM` (`scripts/humanize.sh:784-795`). The test wrapper is included in the aggregate runner list at `tests/run-all-tests.sh:80-83`.
- edge_cases_or_failure_modes: If `tests/test-monitor-e2e-real.sh` cannot be sourced, strict mode causes the wrapper to fail before any test runs. If `monitor_test_bash_sigint` or `monitor_test_zsh_sigint` is absent after sourcing, `set -u/e` behavior will fail the script at invocation. If the real monitor exits before the startup sleep, bash records a start failure and includes up to 300 bytes of output (`tests/test-monitor-e2e-real.sh:520-526`); zsh records a simpler start failure (`tests/test-monitor-e2e-real.sh:668-669`). If zsh is absent, zsh coverage is skipped without incrementing failures (`tests/test-monitor-e2e-real.sh:560-562`). If bash SIGINT does not terminate the process, the test forcibly terminates it but still passes the process-exit portion after SIGTERM fallback (`tests/test-monitor-e2e-real.sh:505-518`), so the stronger validation comes from cleanup/exit-code and no-glob-error checks. If zsh SIGINT does not terminate, the test kills the process and fails that exit gate (`tests/test-monitor-e2e-real.sh:660-663`). The real monitor has additional relevant failure boundaries: missing `.humanize/rlcr` before start returns error (`scripts/humanize.sh:272-277`), deleted loop directory during monitoring triggers graceful stop and return 0 (`scripts/humanize.sh:838-844`, `scripts/humanize.sh:1022-1030`), and cleanup resets traps to avoid repeated signal handling (`scripts/humanize.sh:738-747`, `scripts/humanize.sh:1181-1187`).
- validation_or_tests: This file is itself the executable validation entry for the SIGINT e2e split. It validates real monitor signal behavior rather than stubs: generated runners source `scripts/humanize.sh` and call `_humanize_monitor_codex` directly (`tests/test-monitor-e2e-real.sh:464-471`, `tests/test-monitor-e2e-real.sh:632-637`). The wrapper participates in the full test suite through `tests/run-all-tests.sh:82`. I did not execute it during this research pass because the user requested research notes only, the branch export is read-only, and the test intentionally creates `/tmp` projects and launches/kills background monitor processes.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-086`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`