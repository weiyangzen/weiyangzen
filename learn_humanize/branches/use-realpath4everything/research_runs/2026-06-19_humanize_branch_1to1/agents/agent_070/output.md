# agent_070 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-070 `file` `tests/test-bitlesson-validate-delta.sh`
- cursor: `[_]`
- core_role: Executable regression specification for BitLesson Delta validation behavior. It exercises [tests/test-bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-bitlesson-validate-delta.sh:1), which shells out to [scripts/bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-validate-delta.sh:1). This is part of the loop stop-gate path used by [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:823) when BitLesson validation is enabled.
- algorithmic_behavior: The test initializes `SCRIPT_DIR`, `PROJECT_ROOT`, `VALIDATOR`, `TEMPLATE_DIR`, and a fixed `TEST_LESSON_ID` at lines 8-14. It defines `make_bitlesson_file` to create a minimal `.humanize/bitlesson.md` containing one concrete `Lesson ID`, and `make_summary_file` to create a round summary with a `## BitLesson Delta` block at lines 21-49. `run_validator` invokes the validator with `--summary-file`, `--bitlesson-file`, `--bitlesson-relpath`, `--allow-empty-none false`, `--template-dir`, and `--current-round 1` at lines 51-62. Assertions then classify validator output as either a JSON block decision or an empty pass signal.
- inputs_outputs_state: Inputs are temporary markdown files under `TEST_DIR`: one BitLesson knowledge-base file and multiple round summary variants. The test harness state is created by `setup_test_dir` from [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:86), which uses `mktemp -d` and registers cleanup. Outputs are console PASS/FAIL lines plus the final summary from `print_test_summary`; failure state is accumulated in `TESTS_FAILED` and returned nonzero by the summary helper when any assertion fails. The validator itself returns blocking output as JSON with `.decision == "block"` via `block_exit` in [scripts/bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-validate-delta.sh:78), and returns no stdout on success by exiting 0 at line 387.
- gates_or_invariants: The test enforces that `Action: add` and `Action: update` require a meaningful `Notes:` value, not whitespace or placeholder text. This maps to validator logic that trims notes, defines `NOTES_PLACEHOLDER_REGEX='^(\[.*\]|<.*>)$'`, and blocks empty or placeholder notes at [scripts/bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-validate-delta.sh:281). It also enforces that a `## BitLesson Delta` heading inside a fenced code block or HTML comment is not accepted as the real summary section, matching `extract_bitlesson_delta_block` fence/comment tracking at lines 92-179. Successful cases assert that real text outside fences/comments and meaningful notes produce no block output.
- dependencies_and_callers: Direct dependencies are Bash with `set -euo pipefail`, `jq` for JSON assertions at lines 68-80, shared test helpers at line 10, the validator script at line 12, and `prompt-template` block templates at line 13. The validator depends on `hooks/lib/template-loader.sh` at [scripts/bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-validate-delta.sh:76) and uses templates such as [prompt-template/block/bitlesson-delta-missing-notes.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/prompt-template/block/bitlesson-delta-missing-notes.md:1). Runtime caller evidence is the stop hook, which runs `bitlesson-validate-delta.sh` for non-finalize rounds when `BITLESSON_REQUIRED == true`, then exits with the validator JSON if any block output exists at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:823). Note: [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/run-all-tests.sh:59) does not list this test in its `TEST_SUITES` array in the inspected range, so this may be a standalone regression script rather than part of the aggregate suite.
- edge_cases_or_failure_modes: Covered failure cases are whitespace-only notes for `add` at lines 101-104, bracket placeholder notes for `update` at lines 106-109, angle-bracket placeholder notes for `update` at lines 111-114, a delta section quoted inside a fenced markdown block at lines 116-128, and a delta section hidden in an HTML comment at lines 130-142. The assertion helpers fail if a block case lacks JSON `.decision == "block"` or if notes-specific failures do not mention `Notes`; pass cases fail if the validator emits any stdout. External failure modes include missing `jq`, missing validator/template files, or a validator execution error, all of which would terminate under `set -euo pipefail`.
- validation_or_tests: The file is itself validation evidence. It defines seven checks: three Notes-field block checks, two hidden-section block checks, and two pass checks for valid visible BitLesson Delta content. The tested contract aligns with the documented BitLesson Delta format and strict rules in [docs/bitlesson.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/docs/bitlesson.md:40), although the docs excerpt mentions action/id rules and not the newer Notes placeholder/fence/comment edge cases.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 evidence sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`