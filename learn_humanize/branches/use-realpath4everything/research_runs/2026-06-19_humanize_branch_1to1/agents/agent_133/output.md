# agent_133 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-133 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: This is a reusable block-message template for the RLCR loop’s plan-backup protection gate. It defines the user-facing contract that `.humanize/rlcr/.../plan.md` is a backup copy, not an editable working plan. The file is short but part of the core guard surface because several tool validators render it when blocking mutation attempts. Its local content is a heading plus three policy sentences at `prompt-template/block/plan-backup-protected.md:1-7`.
- algorithmic_behavior: The template itself has no executable logic and no variable placeholders. Its algorithmic role is declarative: when a validator classifies a target as the loop-local `plan.md` backup, it emits this message and blocks the operation. Direct Write calls are classified in `hooks/loop-write-validator.sh`: `basename "$FILE_PATH"` sets `IS_PLAN_BACKUP` for `plan.md`, and paths under `/.humanize/rlcr/` render this template, print to stderr, and exit `2` at `hooks/loop-write-validator.sh:151-156` and `hooks/loop-write-validator.sh:230-235`. Direct Edit calls perform the same filename/path gate and exit `2` at `hooks/loop-edit-validator.sh:180-187`. Bash attempts are caught by `command_modifies_file "$COMMAND_LOWER" "\.humanize/rlcr(/[^/]+)?/plan\.md"`, then this template is rendered and the command is blocked with exit `2` at `hooks/loop-bash-validator.sh:472-482`.
- inputs_outputs_state: Input is not data passed into this markdown file; it is the validator’s detected unsafe action against a loop backup path. The rendered output is markdown text beginning with `# Plan Backup Protected` and stating that `plan.md` in the loop directory is a backup and should be read, not modified. There are no template variables, so `load_and_render_safe` receives only the template path and a fallback string from the callers. State transition is external: attempted mutation of backup plan moves the hook decision from allow to block, returning exit `2` for PreToolUse validators. The backup itself is created during loop setup by copying or creating `"$LOOP_DIR/plan.md"` at `scripts/setup-rlcr-loop.sh:829-832`; later stop-hook integrity checks require it to exist and match the original plan before continuing, as shown in `hooks/loop-codex-stop-hook.sh:329-356` and `hooks/loop-codex-stop-hook.sh:377-396`.
- gates_or_invariants: Primary invariant: the loop-local `plan.md` is immutable during the active RLCR loop and must be used only as a reference backup. Secondary invariant: plan integrity is checked by comparing the project plan against the backup, with missing backup, deleted original, uncommitted tracked changes, or content drift becoming stop-hook block reasons in `hooks/loop-codex-stop-hook.sh:332-396`. The template reinforces the gate instruction at `prompt-template/block/plan-backup-protected.md:3-7`. The validators intentionally scope the block to `plan.md` under `/.humanize/rlcr/`, so an unrelated project file named `plan.md` outside the loop directory is not blocked by this specific template path.
- dependencies_and_callers: Loaded through the shared template system. `hooks/lib/template-loader.sh` resolves the template directory relative to hook code with `get_template_dir` at `hooks/lib/template-loader.sh:24-31`, loads files with `load_template` at `hooks/lib/template-loader.sh:33-46`, and provides `load_and_render_safe` fallback behavior at `hooks/lib/template-loader.sh:185-211`. Direct callers are `hooks/loop-write-validator.sh:232-234`, `hooks/loop-edit-validator.sh:183-185`, and `hooks/loop-bash-validator.sh:479-481`. The broader plan-backup lifecycle depends on loop setup creating the backup and stop-hook integrity enforcement validating it against the original plan. Tests exercising the callers live in `tests/test-plan-file-hooks.sh`.
- edge_cases_or_failure_modes: If the template is missing or empty, the validators do not fail open; `load_and_render_safe` emits the caller-provided fallback message instead, per `hooks/lib/template-loader.sh:194-210`. If the file is present but has no placeholders, rendering is deterministic. If an unsafe Bash command avoids `command_modifies_file` pattern recognition, this specific block may not fire; the Bash guard’s documented purpose is to catch common bypasses such as redirection, `tee`, `sed -i`, and similar mutation patterns at `hooks/loop-bash-validator.sh:3-10`. If the backup is deleted rather than modified through an unobserved path, the stop hook separately blocks because `"$BACKUP_PLAN"` is missing at `hooks/loop-codex-stop-hook.sh:332-343`. In review phase, the stop hook skips plan integrity checks, but the PreToolUse mutation guards still contain all-round plan-backup modification blocks.
- validation_or_tests: `tests/test-plan-file-hooks.sh` covers direct Write blocking for loop `plan.md` at `tests/test-plan-file-hooks.sh:252-263`, direct Edit blocking at `tests/test-plan-file-hooks.sh:269-280`, Bash write blocking at `tests/test-plan-file-hooks.sh:286-297`, and Bash remove blocking beginning at `tests/test-plan-file-hooks.sh:299-305`. The same test file checks stop-hook behavior when the backup is missing at `tests/test-plan-file-hooks.sh:641-658` and validates the security case where a tracked plan has committed content changes that differ from the backup at `tests/test-plan-file-hooks.sh:756-785`. I inspected the tests and call sites only; I did not execute the test suite because this task requested read-only research notes.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 evidence section present for the single assigned row`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`