# agent_110 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-110 `file` `prompt-template/block/bitlesson-delta-inconsistent.md`
- cursor: `[_]`
- core_role: This file is a stop-gate block template for BitLesson Delta consistency failures. It is not executable code; its algorithmic role is to provide the human-facing remediation message when `scripts/bitlesson-validate-delta.sh` detects that a round summary's `## BitLesson Delta` declaration contradicts the expected BitLesson state. The template title is defined at `prompt-template/block/bitlesson-delta-inconsistent.md:1`, the mismatch statement at `prompt-template/block/bitlesson-delta-inconsistent.md:3`, and the corrective contract at `prompt-template/block/bitlesson-delta-inconsistent.md:5-7`.
- algorithmic_behavior: The template encodes a two-branch consistency contract. For `Action: none`, `Lesson ID(s)` must be `NONE` or omitted, as stated at `prompt-template/block/bitlesson-delta-inconsistent.md:6`. For `Action: add|update`, concrete lesson IDs must be supplied and each must already exist in `.humanize/bitlesson.md`, as stated at `prompt-template/block/bitlesson-delta-inconsistent.md:7`. The actual validation logic lives in `scripts/bitlesson-validate-delta.sh`: it extracts the `## BitLesson Delta` block while ignoring headings inside fenced code blocks and HTML comments (`scripts/bitlesson-validate-delta.sh:92-179`), parses `Action:` and `Lesson ID(s):` (`scripts/bitlesson-validate-delta.sh:202-225`), then emits this template through `load_and_render_safe` for multiple inconsistency branches (`scripts/bitlesson-validate-delta.sh:250`, `scripts/bitlesson-validate-delta.sh:276-277`, `scripts/bitlesson-validate-delta.sh:306-308`, `scripts/bitlesson-validate-delta.sh:349-350`, `scripts/bitlesson-validate-delta.sh:364-365`, `scripts/bitlesson-validate-delta.sh:379-382`). The stop hook calls the validator for non-finalize rounds when BitLesson is required, and any non-empty validator output becomes the hook decision output (`hooks/loop-codex-stop-hook.sh:823-837`).
- inputs_outputs_state: Inputs are indirect: a round summary file containing `## BitLesson Delta`, the BitLesson knowledge base path, the relative BitLesson path, the empty-knowledge-base policy, the template directory, and the current round passed into `scripts/bitlesson-validate-delta.sh` (`scripts/bitlesson-validate-delta.sh:6-13`, `scripts/bitlesson-validate-delta.sh:24-67`). The template itself has no placeholders, so rendering currently returns static Markdown content. Output is embedded as the JSON `reason` value in a block decision via `block_exit`, with `decision: "block"` and a short `systemMessage` (`scripts/bitlesson-validate-delta.sh:78-90`). State transition is external to this file: a valid BitLesson Delta lets the validator exit silently with status 0 (`scripts/bitlesson-validate-delta.sh:387`), while an inconsistent delta returns a block JSON object, causing `loop-codex-stop-hook.sh` to stop the loop before later checks (`hooks/loop-codex-stop-hook.sh:834-837`).
- gates_or_invariants: The key invariant is that the summary action and lesson ID field must describe the same BitLesson state. `none` cannot name concrete IDs (`scripts/bitlesson-validate-delta.sh:242-252`). `add` and `update` cannot omit IDs or use `NONE` (`scripts/bitlesson-validate-delta.sh:269-279`), must include non-placeholder notes (`scripts/bitlesson-validate-delta.sh:281-297`), require the BitLesson file to exist (`scripts/bitlesson-validate-delta.sh:299-310`), require at least one non-empty ID after comma splitting (`scripts/bitlesson-validate-delta.sh:315-352`), require the `BL-YYYYMMDD-short-name` format (`scripts/bitlesson-validate-delta.sh:322-367`), and require each ID to appear as a `Lesson ID:` entry in the BitLesson file (`scripts/bitlesson-validate-delta.sh:328-339`, `scripts/bitlesson-validate-delta.sh:369-384`). The public documentation mirrors the same contract at `docs/bitlesson.md:36-51`.
- dependencies_and_callers: Direct dependency is the shared template loader, especially `load_and_render_safe`, which loads `prompt-template/block/bitlesson-delta-inconsistent.md` and falls back to branch-specific inline text if the template is missing or renders empty (`hooks/lib/template-loader.sh:185-211`). The loader performs single-pass `{{VAR}}` substitution and preserves missing placeholders (`hooks/lib/template-loader.sh:7-13`, `hooks/lib/template-loader.sh:56-135`), though this particular template has no variables. Direct caller is `scripts/bitlesson-validate-delta.sh`; operational caller is `hooks/loop-codex-stop-hook.sh` during required BitLesson stop-gate validation. The broader workflow is documented in `docs/bitlesson.md:25-35`: project knowledge lives at `.humanize/bitlesson.md`, selectors run before work, and the stop gate validates every required round summary.
- edge_cases_or_failure_modes: Since this template is generic, validator branches that pass contextual substitutions such as `ACTION`, `BITLESSON_FILE`, `INVALID_IDS`, or `MISSING_IDS` lose those specifics when the template exists, because the file contains no placeholders. That means detailed inline fallbacks in `scripts/bitlesson-validate-delta.sh:271-274`, `scripts/bitlesson-validate-delta.sh:301-304`, `scripts/bitlesson-validate-delta.sh:356-362`, and `scripts/bitlesson-validate-delta.sh:371-377` are shadowed by the generic template. The behavior is still safe because the stop gate blocks, but remediation is less precise than the fallback messages. Other edge cases handled by the validator include `## BitLesson Delta` inside fenced code or HTML comments being ignored (`scripts/bitlesson-validate-delta.sh:95-179`), comma-separated IDs being trimmed and empty entries skipped (`scripts/bitlesson-validate-delta.sh:315-320`), missing BitLesson files blocking add/update declarations (`scripts/bitlesson-validate-delta.sh:299-310`), and empty knowledge bases optionally blocking `Action: none` through a different template (`scripts/bitlesson-validate-delta.sh:254-267`).
- validation_or_tests: Regression coverage exists in `tests/test-bitlesson-validate-delta.sh`. The test constructs a BitLesson file and summary files (`tests/test-bitlesson-validate-delta.sh:21-62`), asserts block JSON output for invalid summaries (`tests/test-bitlesson-validate-delta.sh:64-83`), and asserts no output for passing summaries (`tests/test-bitlesson-validate-delta.sh:86-95`). Covered cases include missing or placeholder notes for add/update (`tests/test-bitlesson-validate-delta.sh:101-114`), false positives inside fenced code blocks and HTML comments (`tests/test-bitlesson-validate-delta.sh:116-142`), and passing add/update summaries with valid notes and known IDs (`tests/test-bitlesson-validate-delta.sh:144-152`). I did not run tests because this task requested read-only research notes only, and the branch export is restricted read-only. A `git status --short` probe also failed because the export is not a Git checkout with `.git`, and macOS tooling attempted denied cache writes under `/tmp`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence heading present for the single assigned item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`