# agent_008 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-008 `directory` `scripts`
- cursor: `[_]`
- core_role: `scripts` is the operational shell layer for the Humanize plugin. It contains 21 shell files under `scripts/` and `scripts/lib/`, totaling about 7,951 lines. Its core algorithm responsibilities are plan/RLCR loop bootstrap, stop-gate reuse, one-shot model consultation, BitLesson memory selection and delta validation, skill/runtime installation, config/model routing, timeout portability, terminal monitoring, and status-line reporting. It coordinates with sibling `hooks/`, `prompt-template/`, `templates/`, `config/`, `agents/`, `docs/`, and `tests/` paths, but the executable algorithm entry points live here.

- algorithmic_behavior:
  - Directory structure and child roles:
    - `scripts/setup-rlcr-loop.sh` is the primary RLCR bootstrap/state-machine initializer. It parses loop options, resolves project root through shared hook logic, validates tool dependencies, rejects overlapping active loops, validates plan paths/content/git state, captures branch/base commit, creates `.humanize/rlcr/<timestamp>/` state, initializes BitLesson, writes goal tracker/round contract/summary scaffold, and emits the initial prompt. Key path and state points: `scripts/setup-rlcr-loop.sh:26`, `scripts/setup-rlcr-loop.sh:326`, `scripts/setup-rlcr-loop.sh:369`, `scripts/setup-rlcr-loop.sh:455`, `scripts/setup-rlcr-loop.sh:529`, `scripts/setup-rlcr-loop.sh:723`, `scripts/setup-rlcr-loop.sh:821`, `scripts/setup-rlcr-loop.sh:880`, `scripts/setup-rlcr-loop.sh:929`, `scripts/setup-rlcr-loop.sh:1195`, `scripts/setup-rlcr-loop.sh:1290`.
    - `scripts/cancel-rlcr-loop.sh` cancels the active RLCR loop by resolving the project root, finding the newest active loop via shared hook library, creating `.cancel-requested`, deleting pending session-id signal, and renaming the active phase state file to `cancel-state.md`. It refuses normal cancellation during finalize unless `--force` is provided. See `scripts/cancel-rlcr-loop.sh:69`, `scripts/cancel-rlcr-loop.sh:91`, `scripts/cancel-rlcr-loop.sh:103`, `scripts/cancel-rlcr-loop.sh:139`, `scripts/cancel-rlcr-loop.sh:156`, `scripts/cancel-rlcr-loop.sh:166`.
    - `scripts/rlcr-stop-gate.sh` wraps `hooks/loop-codex-stop-hook.sh` so non-hook workflows can reuse the same RLCR stop decision. It builds a Stop-hook-shaped JSON payload with `jq`, forwards `CLAUDE_PROJECT_DIR`, maps hook block decisions to exit code `10`, allow decisions to `0`, and wrapper errors to `20`. See `scripts/rlcr-stop-gate.sh:19`, `scripts/rlcr-stop-gate.sh:25`, `scripts/rlcr-stop-gate.sh:81`, `scripts/rlcr-stop-gate.sh:108`, `scripts/rlcr-stop-gate.sh:128`, `scripts/rlcr-stop-gate.sh:148`, `scripts/rlcr-stop-gate.sh:152`.
    - `scripts/validate-gen-plan-io.sh` validates gen-plan input/output paths. It parses `--input`, `--output`, mode flags, normalizes paths with `realpath -m`, requires input file existence and non-empty content, requires output directory writability, rejects existing output paths, and confirms the gen-plan template exists. See `scripts/validate-gen-plan-io.sh:35`, `scripts/validate-gen-plan-io.sh:76`, `scripts/validate-gen-plan-io.sh:98`, `scripts/validate-gen-plan-io.sh:108`, `scripts/validate-gen-plan-io.sh:124`, `scripts/validate-gen-plan-io.sh:140`, `scripts/validate-gen-plan-io.sh:162`.
    - `scripts/validate-refine-plan-io.sh` validates refine-plan inputs and schema. Its AWK scanners count valid comment blocks while ignoring fenced code and HTML comments, detect nested/stray/missing comment markers, scan visible markdown headings, require the gen-plan section set, validate in-place or output-dir writability, and create/check the QA directory. See `scripts/validate-refine-plan-io.sh:16`, `scripts/validate-refine-plan-io.sh:62`, `scripts/validate-refine-plan-io.sh:115`, `scripts/validate-refine-plan-io.sh:227`, `scripts/validate-refine-plan-io.sh:303`, `scripts/validate-refine-plan-io.sh:318`, `scripts/validate-refine-plan-io.sh:561`, `scripts/validate-refine-plan-io.sh:589`, `scripts/validate-refine-plan-io.sh:608`, `scripts/validate-refine-plan-io.sh:639`, `scripts/validate-refine-plan-io.sh:662`.
    - `scripts/ask-codex.sh` runs one-shot Codex consultations. It parses model/timeout/question, validates Codex availability and model/effort strings, resolves project root, creates `.humanize/skill/<unique-id>/` plus cache storage, saves input/debug command, runs `codex exec` through `portable-timeout`, classifies timeout/error/empty/success, saves metadata/output, and prints only the model response to stdout. See `scripts/ask-codex.sh:26`, `scripts/ask-codex.sh:89`, `scripts/ask-codex.sh:153`, `scripts/ask-codex.sh:172`, `scripts/ask-codex.sh:192`, `scripts/ask-codex.sh:202`, `scripts/ask-codex.sh:244`, `scripts/ask-codex.sh:296`, `scripts/ask-codex.sh:309`, `scripts/ask-codex.sh:334`, `scripts/ask-codex.sh:361`, `scripts/ask-codex.sh:391`.
    - `scripts/ask-gemini.sh` mirrors `ask-codex` for Gemini CLI. It validates model/question, resolves project root, writes invocation records/cache, augments the prompt with a mandatory Google Search instruction, runs `gemini -p` with sandbox or yolo mode, classifies result states, and saves metadata/output. See `scripts/ask-gemini.sh:27`, `scripts/ask-gemini.sh:91`, `scripts/ask-gemini.sh:145`, `scripts/ask-gemini.sh:162`, `scripts/ask-gemini.sh:174`, `scripts/ask-gemini.sh:184`, `scripts/ask-gemini.sh:224`, `scripts/ask-gemini.sh:236`, `scripts/ask-gemini.sh:281`, `scripts/ask-gemini.sh:294`, `scripts/ask-gemini.sh:316`, `scripts/ask-gemini.sh:340`.
    - `scripts/bitlesson-init.sh` creates `.humanize/bitlesson.md` from `templates/bitlesson.md` only if missing. It rejects missing arguments, non-directory project roots, missing templates, absolute BitLesson relpaths, and relpaths containing parent traversal; it canonicalizes project root with physical `pwd -P`. See `scripts/bitlesson-init.sh:22`, `scripts/bitlesson-init.sh:60`, `scripts/bitlesson-init.sh:70`, `scripts/bitlesson-init.sh:75`, `scripts/bitlesson-init.sh:78`, `scripts/bitlesson-init.sh:83`.
    - `scripts/bitlesson-select.sh` selects relevant lessons for a task. It loads merged config, chooses a model/provider, falls back to Codex in Codex-only or missing-provider cases, builds a constrained selector prompt, runs Codex or Claude through timeout, and enforces exactly two output lines: `LESSON_IDS` and `RATIONALE`. See `scripts/bitlesson-select.sh:9`, `scripts/bitlesson-select.sh:20`, `scripts/bitlesson-select.sh:76`, `scripts/bitlesson-select.sh:99`, `scripts/bitlesson-select.sh:105`, `scripts/bitlesson-select.sh:115`, `scripts/bitlesson-select.sh:126`, `scripts/bitlesson-select.sh:137`, `scripts/bitlesson-select.sh:148`, `scripts/bitlesson-select.sh:188`, `scripts/bitlesson-select.sh:242`, `scripts/bitlesson-select.sh:258`.
    - `scripts/bitlesson-validate-delta.sh` validates the required summary `## BitLesson Delta` section. It extracts the section while ignoring fenced code and HTML comments, requires exactly one `Action: none|add|update`, enforces ID/notes consistency, verifies lesson ID format `BL-YYYYMMDD-short-name`, confirms referenced IDs exist in `.humanize/bitlesson.md`, and emits hook-compatible block JSON through `jq` on violations. See `scripts/bitlesson-validate-delta.sh:78`, `scripts/bitlesson-validate-delta.sh:92`, `scripts/bitlesson-validate-delta.sh:181`, `scripts/bitlesson-validate-delta.sh:200`, `scripts/bitlesson-validate-delta.sh:202`, `scripts/bitlesson-validate-delta.sh:227`, `scripts/bitlesson-validate-delta.sh:242`, `scripts/bitlesson-validate-delta.sh:269`, `scripts/bitlesson-validate-delta.sh:281`, `scripts/bitlesson-validate-delta.sh:322`, `scripts/bitlesson-validate-delta.sh:328`.
    - `scripts/install-skill.sh` installs/upgrades skills for Kimi, Codex, or both. It detects source-checkout versus installed-runtime layout, validates required runtime components, syncs skills and runtime bundles, hydrates `{{HUMANIZE_RUNTIME_ROOT}}`, strips Claude-only frontmatter, seeds Codex user config, installs native Codex hooks, and writes a `bitlesson-selector` shim. See `scripts/install-skill.sh:75`, `scripts/install-skill.sh:90`, `scripts/install-skill.sh:120`, `scripts/install-skill.sh:155`, `scripts/install-skill.sh:167`, `scripts/install-skill.sh:194`, `scripts/install-skill.sh:232`, `scripts/install-skill.sh:253`, `scripts/install-skill.sh:269`, `scripts/install-skill.sh:334`, `scripts/install-skill.sh:471`.
    - `scripts/install-codex-hooks.sh` merges Humanize native Codex Stop hooks into `${CODEX_HOME:-~/.codex}/hooks.json`. It requires Codex hook feature support, loads `config/codex-hooks.json`, JSON-escapes runtime root before template parse, shell-quotes hook commands, removes older managed Humanize hooks, appends current managed hook groups, and enables `codex_hooks`. See `scripts/install-codex-hooks.sh:71`, `scripts/install-codex-hooks.sh:75`, `scripts/install-codex-hooks.sh:85`, `scripts/install-codex-hooks.sh:94`, `scripts/install-codex-hooks.sh:105`, `scripts/install-codex-hooks.sh:112`, `scripts/install-codex-hooks.sh:138`, `scripts/install-codex-hooks.sh:163`, `scripts/install-codex-hooks.sh:175`, `scripts/install-codex-hooks.sh:201`.
    - `scripts/install-skills-codex.sh` and `scripts/install-skills-kimi.sh` are thin wrappers that resolve their own directory and invoke `install-skill.sh --target codex|kimi`. See `scripts/install-skills-codex.sh:8`, `scripts/install-skills-kimi.sh:8`.
    - `scripts/portable-timeout.sh` abstracts timeout behavior across macOS/Linux. It chooses `gtimeout`, GNU `timeout`, Python subprocess timeout, or no-timeout fallback; callers rely on exit code `124` for timeouts. See `scripts/portable-timeout.sh:10`, `scripts/portable-timeout.sh:29`, `scripts/portable-timeout.sh:33`, `scripts/portable-timeout.sh:38`, `scripts/portable-timeout.sh:47`, `scripts/portable-timeout.sh:64`.
    - `scripts/lib/config-loader.sh` is the config merge algorithm. It requires `jq`, validates JSON-object layers, loads default config, user config, and project config or `HUMANIZE_CONFIG`, strips nulls recursively, merges layers in increasing precedence, and returns scalar values with `get_config_value`. See `scripts/lib/config-loader.sh:17`, `scripts/lib/config-loader.sh:24`, `scripts/lib/config-loader.sh:63`, `scripts/lib/config-loader.sh:77`, `scripts/lib/config-loader.sh:84`, `scripts/lib/config-loader.sh:100`, `scripts/lib/config-loader.sh:113`, `scripts/lib/config-loader.sh:119`, `scripts/lib/config-loader.sh:139`.
    - `scripts/lib/model-router.sh` maps model names to providers and validates provider binaries. Codex models match `gpt-*` or `oN*`; Claude models match `claude-*`, `haiku`, `sonnet`, or `opus`; effort mapping constrains to `xhigh|high|medium|low` and maps `xhigh` to `high` for Claude. See `scripts/lib/model-router.sh:10`, `scripts/lib/model-router.sh:32`, `scripts/lib/model-router.sh:62`.
    - `scripts/humanize.sh`, `scripts/lib/monitor-common.sh`, `scripts/lib/monitor-skill.sh`, and `scripts/statusline.sh` are operator-observability algorithms. They parse goal trackers, state files, git status, skill metadata, cache logs, and terminal dimensions; then they render live RLCR/skill monitors or compact status-line fields. See `scripts/humanize.sh:24`, `scripts/humanize.sh:71`, `scripts/humanize.sh:158`, `scripts/humanize.sh:208`, `scripts/humanize.sh:261`, `scripts/humanize.sh:838`, `scripts/humanize.sh:1191`; `scripts/lib/monitor-common.sh:40`, `scripts/lib/monitor-common.sh:154`, `scripts/lib/monitor-common.sh:201`, `scripts/lib/monitor-common.sh:265`, `scripts/lib/monitor-skill.sh:53`, `scripts/lib/monitor-skill.sh:79`, `scripts/lib/monitor-skill.sh:121`, `scripts/lib/monitor-skill.sh:169`, `scripts/lib/monitor-skill.sh:339`, `scripts/statusline.sh:37`, `scripts/statusline.sh:67`, `scripts/statusline.sh:178`, `scripts/statusline.sh:278`, `scripts/statusline.sh:384`.

- inputs_outputs_state:
  - Main external inputs are CLI flags, environment variables, current repo state, `.humanize` runtime files, config JSON, templates, hook JSON, plan/summary markdown, tool availability, and model command outputs.
  - Important env inputs include `CLAUDE_PROJECT_DIR`, `CLAUDE_SESSION_ID`, `CLAUDE_TRANSCRIPT_PATH`, `CODEX_HOME`, `CODEX_MODEL`, `CODEX_PERMISSION_MODE`, `XDG_CONFIG_HOME`, `XDG_CACHE_HOME`, `HUMANIZE_CONFIG`, `HUMANIZE_CODEX_BYPASS_SANDBOX`, `HUMANIZE_GEMINI_YOLO`, `HUMANIZE_COMMAND_BIN_DIR`, and `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`.
  - Persistent state outputs include `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, `round-0-summary.md`, `round-0-contract.md`, `round-0-prompt.md`, `.humanize/.pending-session-id`, `.humanize/bitlesson.md`, and `.humanize/skill/<unique-id>/{input.md,output.md,metadata.md}`.
  - Cache/log outputs include `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/...` for RLCR and skill runs; `ask-codex` writes `codex-run.{cmd,out,log}`, and `ask-gemini` writes `gemini-run.{cmd,out,log}`.
  - Installer outputs target external skill/config locations such as Kimi skills dir, Codex skills dir, `${CODEX_HOME:-~/.codex}/hooks.json`, `${XDG_CONFIG_HOME:-~/.config}/humanize/config.json`, and `$HOME/.local/bin/bitlesson-selector`.
  - State transitions:
    - RLCR setup starts from no active loop, then creates active `state.md`; skip-impl can start with `review_started: true` and `.review-phase-started` marker.
    - Cancel transitions any active phase state file into `cancel-state.md`.
    - Stop gate delegates transition decisions to `hooks/loop-codex-stop-hook.sh` and only maps its JSON decision into wrapper exit status.
    - Skill invocations transition from running/no metadata to terminal metadata states `success`, `error`, `timeout`, or `empty_response`.
    - BitLesson validation does not mutate state itself; it returns allow by exit `0` with no output or block JSON through `block_exit`.

- gates_or_invariants:
  - Path/root invariant: several scripts delegate project-root resolution to `hooks/lib/project-root.sh`, whose resolver prioritizes `CLAUDE_PROJECT_DIR`, then `git rev-parse --show-toplevel`, and explicitly avoids `pwd` fallback because working-directory drift can miss active loop state. It canonicalizes through `realpath`/Python fallback. See `hooks/lib/project-root.sh:5`, `hooks/lib/project-root.sh:10`, `hooks/lib/project-root.sh:41`, `hooks/lib/project-root.sh:113`.
  - The branch’s realpath theme is present directly in gen/refine validators via `realpath -m` on input/output paths (`scripts/validate-gen-plan-io.sh:98`, `scripts/validate-refine-plan-io.sh:561`) and indirectly in `setup-rlcr-loop`, `ask-codex`, `ask-gemini`, `bitlesson-select`, and `cancel-rlcr-loop` through `resolve_project_root`.
  - `setup-rlcr-loop.sh` enforces one active loop at a time (`scripts/setup-rlcr-loop.sh:369`), required dependencies `codex`, `jq`, `git` (`scripts/setup-rlcr-loop.sh:340`), relative plan paths only (`scripts/setup-rlcr-loop.sh:455`), no spaces (`scripts/setup-rlcr-loop.sh:461`), no shell metacharacters (`scripts/setup-rlcr-loop.sh:469`), no symlink leaf or symlink parent segments (`scripts/setup-rlcr-loop.sh:481`, `scripts/setup-rlcr-loop.sh:494`), no path escape after resolution (`scripts/setup-rlcr-loop.sh:529`), no submodule plan files (`scripts/setup-rlcr-loop.sh:543`), tracked/ignored policy depending on `--track-plan-file` (`scripts/setup-rlcr-loop.sh:582`), sufficient content (`scripts/setup-rlcr-loop.sh:618`, `scripts/setup-rlcr-loop.sh:633`), YAML-safe branch/model/effort (`scripts/setup-rlcr-loop.sh:692`, `scripts/setup-rlcr-loop.sh:702`, `scripts/setup-rlcr-loop.sh:711`), and clean working tree except untracked `.humanize` artifacts (`scripts/setup-rlcr-loop.sh:723`).
  - Gen/refine IO validators enforce non-empty readable inputs, valid output destinations, write permissions, and required template/schema availability. Refine additionally enforces valid non-empty comment blocks and required sections (`scripts/validate-refine-plan-io.sh:589`, `scripts/validate-refine-plan-io.sh:608`).
  - BitLesson selector invariant: if no `## Lesson:` entries exist, it returns `LESSON_IDS: NONE` deterministically without invoking a model (`scripts/bitlesson-select.sh:105`). If it invokes a model, it hard-parses and rejects nonconforming output (`scripts/bitlesson-select.sh:238`).
  - BitLesson delta invariant: `Action: none` must not name concrete IDs, while `add|update` must include concrete existing IDs and non-placeholder notes (`scripts/bitlesson-validate-delta.sh:242`, `scripts/bitlesson-validate-delta.sh:269`, `scripts/bitlesson-validate-delta.sh:281`, `scripts/bitlesson-validate-delta.sh:328`).
  - Installer invariant: required source layout must contain skills and runtime components before install (`scripts/install-skill.sh:75`), and Codex hook install requires `codex_hooks` feature support before merging (`scripts/install-codex-hooks.sh:75`).

- dependencies_and_callers:
  - `setup-rlcr-loop.sh`, `ask-codex.sh`, `cancel-rlcr-loop.sh`, and `bitlesson-select.sh` source `hooks/lib/loop-common.sh` or `hooks/lib/project-root.sh`; `loop-common.sh` in turn sources `scripts/lib/config-loader.sh` and loads config-backed defaults, linking scripts to `config/default_config.json` and project/user config.
  - `rlcr-stop-gate.sh` calls `hooks/loop-codex-stop-hook.sh` and passes Stop-hook JSON; that hook calls `scripts/bitlesson-validate-delta.sh` during summary validation, visible in `hooks/loop-codex-stop-hook.sh:824`.
  - `setup-rlcr-loop.sh` calls `scripts/bitlesson-init.sh` at `scripts/setup-rlcr-loop.sh:863`.
  - `install-skill.sh` calls `scripts/install-codex-hooks.sh` at `scripts/install-skill.sh:253` and generates a `bitlesson-selector` shim that dispatches to installed `scripts/bitlesson-select.sh` (`scripts/install-skill.sh:334`).
  - `scripts/humanize.sh` sources `scripts/lib/monitor-common.sh` and `scripts/lib/monitor-skill.sh` (`scripts/humanize.sh:7`, `scripts/humanize.sh:1243`), while `monitor-skill.sh` depends on `monitor_get_yaml_value`, `monitor_format_timestamp`, and `humanize_split_to_array` (`scripts/lib/monitor-skill.sh:8`).
  - Documentation references confirm intended entry points: `docs/bitlesson.md` describes selector and delta behavior, `docs/install-for-codex.md` and `docs/install-for-kimi.md` describe install wrappers, `docs/usage.md` documents `ask-codex` and monitoring, and README references `ask-gemini` plus `humanize monitor`.
  - Tests directly exercise this directory: config merge/error tests target `scripts/lib/config-loader.sh`; model-router tests target `scripts/lib/model-router.sh`; installer tests target `scripts/install-skill.sh` and generated shim; robustness tests target `setup-rlcr-loop.sh`, `portable-timeout.sh`, and `humanize.sh`.

- edge_cases_or_failure_modes:
  - This read-only branch export did not expose a usable `.git` repository to the sandbox, and `git status` failed due blocked macOS cache creation plus no visible `.git`; research therefore used file inspection, not repository metadata verification.
  - `setup-rlcr-loop.sh` fails closed on git timeout during repo/plan/status checks and differentiates timeout from normal untracked-file status (`scripts/setup-rlcr-loop.sh:433`, `scripts/setup-rlcr-loop.sh:562`, `scripts/setup-rlcr-loop.sh:571`, `scripts/setup-rlcr-loop.sh:723`).
  - Plan path containment relies on `RESOLVED_PLAN_DIR=$(cd "$PLAN_DIR" && pwd)` rather than `pwd -P` or shared `canonicalize_path`; the surrounding checks reject explicit symlink leaves/parents first, but this is a relevant branch-review point because the branch theme is realpath canonicalization everywhere (`scripts/setup-rlcr-loop.sh:494`, `scripts/setup-rlcr-loop.sh:532`).
  - `humanize.sh` still uses logical `pwd` to locate project cache logs in `_find_latest_codex_log` (`scripts/humanize.sh:303`), and `monitor-skill.sh` falls back to `pwd` if git top-level lookup fails (`scripts/lib/monitor-skill.sh:158`). These are observability paths, not mutation paths, but they can mismatch caches under symlinked working directories.
  - `validate-gen-plan-io.sh` and `validate-refine-plan-io.sh` use GNU-style `realpath -m`; fallback is the raw input if unsupported (`scripts/validate-gen-plan-io.sh:99`, `scripts/validate-refine-plan-io.sh:562`). That preserves execution but may weaken canonical behavior on platforms without compatible `realpath`.
  - `portable-timeout.sh` falls back to running commands without timeout if no GNU timeout, Homebrew `gtimeout`, Python 3, or Python exists (`scripts/portable-timeout.sh:64`), so timeout guarantees degrade in minimal environments.
  - `ask-codex.sh` can deliberately bypass Codex sandboxing via `HUMANIZE_CODEX_BYPASS_SANDBOX`; `ask-gemini.sh` can use `HUMANIZE_GEMINI_YOLO`. Both are controlled by explicit env gates (`scripts/ask-codex.sh:250`, `scripts/ask-gemini.sh:226`).
  - Installer paths are intentionally outside the repo in normal operation. Dry-run mode avoids writes, but normal mode mutates user skill/config directories and hook JSON (`scripts/install-skill.sh:240`, `scripts/install-codex-hooks.sh:193`).
  - Refine comment parser rejects nested/wrong/stray/missing markers and ignores markers in fenced code/HTML comments; malformed comment syntax becomes validation error rather than silent misparse (`scripts/validate-refine-plan-io.sh:139`, `scripts/validate-refine-plan-io.sh:202`, `scripts/validate-refine-plan-io.sh:227`, `scripts/validate-refine-plan-io.sh:308`).
  - Statusline and monitors are best-effort views; they parse markdown/YAML with grep/sed/awk and can misclassify unusual formatting, but they are not authoritative state mutation paths.

- validation_or_tests:
  - Recursive inspection found all script files under `scripts` with no deeper subdirectories beyond `scripts/lib`: `ask-codex.sh`, `ask-gemini.sh`, BitLesson scripts, RLCR scripts, install scripts, validators, monitor/status scripts, and four shared library files.
  - `tests/robustness/test-path-validation-robustness.sh` runs production `scripts/setup-rlcr-loop.sh` against valid/invalid plan paths and checks path validation outcomes (`tests/robustness/test-path-validation-robustness.sh:5`, `tests/robustness/test-path-validation-robustness.sh:76`, `tests/robustness/test-path-validation-robustness.sh:88`).
  - `tests/robustness/test-plan-file-robustness.sh` runs production `scripts/setup-rlcr-loop.sh` against plan content edge cases, captures output safely, and treats loop creation or activation as validation success (`tests/robustness/test-plan-file-robustness.sh:5`, `tests/robustness/test-plan-file-robustness.sh:46`, `tests/robustness/test-plan-file-robustness.sh:61`, `tests/robustness/test-plan-file-robustness.sh:82`).
  - `tests/test-config-merge.sh` validates default/user/project/custom config precedence, additive merge, and null stripping for `scripts/lib/config-loader.sh` (`tests/test-config-merge.sh:3`, `tests/test-config-merge.sh:43`, `tests/test-config-merge.sh:102`, `tests/test-config-merge.sh:141`, `tests/test-config-merge.sh:164`).
  - `tests/test-config-error-handling.sh` targets malformed config handling in `scripts/lib/config-loader.sh`; `tests/test-model-router.sh` targets provider detection and effort mapping; `tests/test-codex-hook-install.sh` targets `install-skill.sh`, Codex hook install, Codex user config seeding, and the generated `bitlesson-selector` shim.
  - `tests/test-unified-codex-config.sh` references `setup-rlcr-loop.sh` and `ask-codex.sh` to assert config-backed Codex model/effort defaults and runtime override behavior.
  - `tests/test-monitor-runtime.sh` and `tests/robustness/test-goal-tracker-robustness.sh` exercise `scripts/humanize.sh` monitor cleanup, terminal restoration, signal handling, and goal-tracker parsing.
  - I did not execute test suites because the assignment requested research notes only and the branch export is read-only; the evidence above is from direct recursive inspection and line-referenced source/test reads.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-008`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`