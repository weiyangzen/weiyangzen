# agent_106 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-106 `file` `hooks/lib/methodology-analysis.sh`
- cursor: `[_]`
- core_role: `hooks/lib/methodology-analysis.sh` is a stop-hook phase library for the RLCR state machine. It inserts a methodology-analysis phase immediately before terminal loop exit so the system can analyze RLCR process quality without continuing normal implementation work. The file is explicitly sourced by `hooks/loop-codex-stop-hook.sh` at lines 53-54, after the stop hook resolves project and loop context.
- algorithmic_behavior: The file defines three phase functions. `enter_methodology_analysis_phase` at `hooks/lib/methodology-analysis.sh:38` is the transition function: it checks skip/re-entry gates, renames the active state file into `methodology-analysis-state.md`, records the original terminal reason in `.methodology-exit-reason`, creates an empty done marker, renders the analysis prompt, and emits JSON with `"decision": "block"` at lines 90-97. `complete_methodology_analysis` at line 114 is the exit validator: it accepts only when both `methodology-analysis-done.md` and `methodology-analysis-report.md` exist with non-whitespace content, the saved exit-reason marker exists, and the exit reason is one of `complete`, `stop`, or `maxiter`; then it renames `methodology-analysis-state.md` to the matching terminal state file at lines 165-168 and removes the marker at line 171. `block_methodology_analysis_incomplete` at line 183 emits block JSON instructing the agent to finish the phase when completion gates have not passed.
- inputs_outputs_state: Inputs are the exit reason and description passed to `enter_methodology_analysis_phase`, plus globals `PRIVACY_MODE`, `STATE_FILE`, `LOOP_DIR`, `CURRENT_ROUND`, `MAX_ITERATIONS`, and `TEMPLATE_DIR` documented in `hooks/lib/methodology-analysis.sh:22-32`. The transition mutates RLCR state by moving `$STATE_FILE` to `$LOOP_DIR/methodology-analysis-state.md` at line 65, writing `$LOOP_DIR/.methodology-exit-reason` at line 69, and touching `$LOOP_DIR/methodology-analysis-done.md` at line 72 as an empty placeholder. Outputs are hook-compatible JSON block responses and stderr diagnostics. The completion path consumes `methodology-analysis-done.md`, `methodology-analysis-report.md`, and `.methodology-exit-reason`, then produces one of `complete-state.md`, `stop-state.md`, or `maxiter-state.md` through the `target_name="${exit_reason}-state.md"` calculation at lines 165-167.
- gates_or_invariants: Privacy mode is a hard skip gate at lines 42-46. Re-entry is blocked if `methodology-analysis-state.md` already exists at lines 48-52. A previous non-empty `methodology-analysis-done.md` skips the phase at lines 55-61. Completion fails closed if the done marker is missing or whitespace-only at lines 118-129, if the report is missing or empty at lines 131-143, if `.methodology-exit-reason` is absent at lines 145-149, or if the exit reason is outside the explicit `complete|stop|maxiter` set at lines 155-163. Shared loop state resolution treats `methodology-analysis-state.md` as the highest-priority active state in `hooks/lib/loop-common.sh:264-280`, so the phase cleanly supersedes `finalize-state.md` or `state.md` while active. Direct edits to the methodology state file are identified by `is_methodology_analysis_state_file_path` in `hooks/lib/loop-common.sh:1006-1009` and blocked with a standard message at lines 1012-1019.
- dependencies_and_callers: The direct caller is `hooks/loop-codex-stop-hook.sh`. It detects methodology phase by checking whether the active state path ends with `methodology-analysis-state.md` at lines 100-101, then runs the completion handler before git-clean checks at lines 617-625. The phase is entered on max-iteration exit at lines 927-934, successful finalize completion at lines 943-952, max-iteration after COMPLETE at lines 1835-1841, and stagnation/circuit-breaker stop at lines 1940-1945. Prompt rendering depends on `load_and_render_safe` and template `prompt-template/claude/methodology-analysis-prompt.md`, with fallback content embedded at `hooks/lib/methodology-analysis.sh:75-87`. The template instructs an Opus analysis agent to read round summaries and review results, produce `methodology-analysis-report.md`, avoid project-identifying details, and write a meaningful done marker; see `prompt-template/claude/methodology-analysis-prompt.md:12-20`, sanitization rules at lines 22-30, report consumption rule at lines 43-45, and completion marker rule at lines 71-73. The validators coordinate with this phase: read access in the originating session is restricted to methodology artifacts in `hooks/loop-read-validator.sh:86-150`, write and edit access are restricted to `methodology-analysis-report.md` and `methodology-analysis-done.md` in `hooks/loop-write-validator.sh:88-132` and `hooks/loop-edit-validator.sh:71-113`, and bash validation blocks git/file-manipulation writes during the phase while allowing a direct cancel command in `hooks/loop-bash-validator.sh:81-94`.
- edge_cases_or_failure_modes: The empty placeholder created at `hooks/lib/methodology-analysis.sh:72` intentionally does not count as completion because `complete_methodology_analysis` trims leading whitespace and rejects empty content at lines 123-129. A non-empty stale `methodology-analysis-done.md` before entry causes the phase to be skipped at lines 55-61, which assumes prior completion rather than re-running analysis. Missing or empty `methodology-analysis-report.md` blocks even if the done marker is populated, preventing a marker-only exit at lines 131-143. If `.methodology-exit-reason` is missing or corrupted, completion blocks rather than guessing terminal state at lines 145-163. The `mv` operations at lines 65 and 167 are not explicitly checked in this library; under normal hook execution, failure would be visible through shell errors, but the function itself does not validate that the rename succeeded before later steps. The validators include realpath fallback logic and fail closed on unresolved traversal segments; for example, read validation rejects unresolved `..` paths and symlink paths when canonicalization fails in `hooks/loop-read-validator.sh:89-116`, while write/edit validators reject unresolved traversal at `hooks/loop-write-validator.sh:89-105` and `hooks/loop-edit-validator.sh:72-87`.
- validation_or_tests: I directly inspected `hooks/lib/methodology-analysis.sh` with line numbers and traced references with `rg`. Template reference coverage exists in `tests/test-template-references.sh`, which scans `load_template`, `load_and_render`, and `load_and_render_safe` calls for existing template files at lines 83-110. The same test file also verifies critical validators use safe template rendering rather than unsafe `load_and_render` calls at lines 172-201. There appears to be no dedicated unit test specifically exercising `enter_methodology_analysis_phase` or `complete_methodology_analysis`; coverage is indirect through template reference tests and the surrounding stop-hook/validator behavior. I attempted `git status --short`, but this branch export is not an active git checkout in the read-only sandbox, and the command failed with “not a git repository”; no repository state was modified.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1 item evidence section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`