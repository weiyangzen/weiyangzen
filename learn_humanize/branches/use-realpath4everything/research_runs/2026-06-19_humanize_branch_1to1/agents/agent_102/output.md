# agent_102 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-102 `file` `tests/test-unified-codex-config.sh`
- cursor: `[_]`
- core_role: This file is an executable behavioral specification for the repository’s unified Codex configuration algorithm. It verifies that `codex_model` and `codex_effort` are the canonical configuration fields across default config, merged project config, loop hook defaults, RLCR state parsing, setup state generation, stop-hook validation, and `ask-codex` runtime behavior. It also guards removal of legacy `loop_reviewer_*` fields. The stated contract appears directly in the test header at `tests/test-unified-codex-config.sh:3` through `tests/test-unified-codex-config.sh:11`.

- algorithmic_behavior: The test proceeds as a linear shell spec with small assertion helpers, then exercises each configuration propagation layer. It first checks `config/default_config.json` for default `codex_model=gpt-5.4` and `codex_effort=high`, and verifies `loop_reviewer_model` / `loop_reviewer_effort` are absent at `tests/test-unified-codex-config.sh:61` through `tests/test-unified-codex-config.sh:80`. It then sources `scripts/lib/config-loader.sh` and tests the merge path using synthetic project roots: default-only config and `.humanize/config.json` project overrides at `tests/test-unified-codex-config.sh:90` through `tests/test-unified-codex-config.sh:122`. The underlying merge algorithm loads default, user, and project layers and combines them in order at `scripts/lib/config-loader.sh:63` through `scripts/lib/config-loader.sh:136`, with value extraction via `get_config_value` at `scripts/lib/config-loader.sh:139` through `scripts/lib/config-loader.sh:159`.

  The next section checks `hooks/lib/loop-common.sh` as the shared runtime source of `DEFAULT_CODEX_MODEL` and `DEFAULT_CODEX_EFFORT`. It verifies default loading, absence of legacy reviewer constants, project config override behavior, caller-pre-set default precedence, invalid config fallback, and unsupported non-Codex model prefix fallback at `tests/test-unified-codex-config.sh:130` through `tests/test-unified-codex-config.sh:248`. The corresponding implementation declares only `FIELD_CODEX_MODEL` and `FIELD_CODEX_EFFORT` at `hooks/lib/loop-common.sh:33` through `hooks/lib/loop-common.sh:35`, resolves the project root before loading config at `hooks/lib/loop-common.sh:176` through `hooks/lib/loop-common.sh:199`, and applies precedence `caller preset > config value > fallback` at `hooks/lib/loop-common.sh:208` through `hooks/lib/loop-common.sh:230`.

  The stop-hook fallback chain is tested with generated `state.md` frontmatter. State values win when present; bare state falls back to defaults; config-backed defaults fill missing state fields when `CLAUDE_PROJECT_DIR` points to a project config at `tests/test-unified-codex-config.sh:252` through `tests/test-unified-codex-config.sh:373`. The implementation parses frontmatter into `STATE_CODEX_MODEL`, `STATE_CODEX_EFFORT`, and `STATE_CODEX_TIMEOUT` at `hooks/lib/loop-common.sh:446` through `hooks/lib/loop-common.sh:469`, then the stop hook maps `STATE_CODEX_*` to execution settings with fallback to `DEFAULT_CODEX_*` at `hooks/loop-codex-stop-hook.sh:125` through `hooks/loop-codex-stop-hook.sh:140`.

  Setup behavior is checked both statically and behaviorally. Static checks assert `scripts/setup-rlcr-loop.sh` has no `loop_reviewer` references and includes `codex_model:` / `codex_effort:` in the state template at `tests/test-unified-codex-config.sh:377` through `tests/test-unified-codex-config.sh:388`. Runtime setup creates a temporary git repo, writes `.humanize/config.json`, invokes setup with `--codex-model gpt-5.3:xhigh`, and checks generated `state.md`, round-0 summary scaffold, and output wording at `tests/test-unified-codex-config.sh:547` through `tests/test-unified-codex-config.sh:632`. The setup script initializes `CODEX_MODEL` and `CODEX_EFFORT` from shared defaults at `scripts/setup-rlcr-loop.sh:39` through `scripts/setup-rlcr-loop.sh:45`, parses `--codex-model MODEL:EFFORT` at `scripts/setup-rlcr-loop.sh:205` through `scripts/setup-rlcr-loop.sh:218`, validates model and effort at `scripts/setup-rlcr-loop.sh:702` through `scripts/setup-rlcr-loop.sh:716`, writes the two fields to state at `scripts/setup-rlcr-loop.sh:880` through `scripts/setup-rlcr-loop.sh:907`, and prints “Codex Model” / “Codex Effort” status labels at `scripts/setup-rlcr-loop.sh:1451` through `scripts/setup-rlcr-loop.sh:1491`.

  The file also covers stale-key handling. Project configs containing `loop_reviewer_model` and `loop_reviewer_effort` must not influence defaults, and state files containing those stale keys must not set `STATE_LOOP_REVIEWER_*`; valid `codex_*` fields in the same stale state still parse correctly at `tests/test-unified-codex-config.sh:391` through `tests/test-unified-codex-config.sh:468`.

  Finally, the test verifies `ask-codex` does not pre-set defaults, consumes `DEFAULT_CODEX_MODEL` / `DEFAULT_CODEX_EFFORT` from `loop-common.sh`, advertises “default from config” in help, reports config-backed model and effort at runtime, and lets `--codex-model` override both values at `tests/test-unified-codex-config.sh:676` through `tests/test-unified-codex-config.sh:771`. The implementation sources `loop-common.sh` at `scripts/ask-codex.sh:26` through `scripts/ask-codex.sh:33`, initializes runtime values from defaults at `scripts/ask-codex.sh:39` through `scripts/ask-codex.sh:43`, parses overrides at `scripts/ask-codex.sh:105` through `scripts/ask-codex.sh:118`, builds `codex exec` arguments at `scripts/ask-codex.sh:244` through `scripts/ask-codex.sh:257`, and emits the observed stderr status at `scripts/ask-codex.sh:282` through `scripts/ask-codex.sh:284`.

- inputs_outputs_state: Inputs include the repository default config file, optional project `.humanize/config.json`, synthetic RLCR `state.md` files, CLI flags such as `--codex-model MODEL:EFFORT`, environment variables including `CLAUDE_PROJECT_DIR`, `XDG_CONFIG_HOME`, `PATH`, and temporary mock/stub `codex` executables. The test also consumes helper functions from `tests/test-helpers.sh`, including pass/fail/skip counters at `tests/test-helpers.sh:30` through `tests/test-helpers.sh:52`, temporary directory setup at `tests/test-helpers.sh:84` through `tests/test-helpers.sh:89`, and temporary git repo initialization at `tests/test-helpers.sh:91` through `tests/test-helpers.sh:105`.

  Outputs are pass/fail/skip lines and a final summary emitted through `print_test_summary` at `tests/test-unified-codex-config.sh:775` through `tests/test-unified-codex-config.sh:779`. Behavioral subtests also inspect generated files in temporary projects, especially `.humanize/rlcr/.../state.md`, `round-0-summary.md`, and `ask-codex` stderr. State transitions under test include: merged config values becoming `DEFAULT_CODEX_*`; state frontmatter becoming `STATE_CODEX_*`; stop-hook execution values resolving as `STATE_CODEX_*` first, then defaults; setup CLI overrides becoming persisted state fields; and `ask-codex` defaults becoming runtime command arguments and stderr metadata.

- gates_or_invariants: The primary invariant is that `codex_model` and `codex_effort` are the only supported model/effort field names for this path; legacy `loop_reviewer_*` keys must be absent or ignored. Default config must contain `gpt-5.4` and `high` at `config/default_config.json:1` through `config/default_config.json:8`. Config precedence must behave as default < user < project, with caller-pre-set shell defaults outranking config. This is asserted in the test at `tests/test-unified-codex-config.sh:161` through `tests/test-unified-codex-config.sh:194` and implemented at `hooks/lib/loop-common.sh:208` through `hooks/lib/loop-common.sh:230`.

  Validation gates include requiring `jq` for JSON config tests at `tests/test-unified-codex-config.sh:65` through `tests/test-unified-codex-config.sh:80`, skipping runtime setup when `codex` is missing at `tests/test-unified-codex-config.sh:553` through `tests/test-unified-codex-config.sh:557`, rejecting shell-unsafe model names via `^[a-zA-Z0-9._-]+$` at `tests/test-unified-codex-config.sh:642` through `tests/test-unified-codex-config.sh:655`, and restricting setup/stop-hook effort values to `xhigh|high|medium|low` at `tests/test-unified-codex-config.sh:657` through `tests/test-unified-codex-config.sh:672`. The stop hook must reject invalid state effort before invoking Codex; the test stubs `codex`, runs the hook, expects “Invalid codex effort,” and verifies no invocation log exists at `tests/test-unified-codex-config.sh:472` through `tests/test-unified-codex-config.sh:543`. The stop-hook implementation enforces this at `hooks/loop-codex-stop-hook.sh:172` through `hooks/loop-codex-stop-hook.sh:184`.

- dependencies_and_callers: This test depends on Bash, `grep`, `sed`, `jq` for JSON assertions, `git` for runtime setup fixtures, `timeout` through scripts under test, and optionally a real or mocked `codex` command. Repository paths exercised are `config/default_config.json`, `scripts/lib/config-loader.sh`, `hooks/lib/loop-common.sh`, `hooks/loop-codex-stop-hook.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/ask-codex.sh`, and `tests/test-helpers.sh`. The test is a caller of `load_merged_config`, `get_config_value`, `parse_state_file`, setup CLI parsing, stop-hook state validation, and `ask-codex` invocation construction. The main production callers protected by this test are RLCR setup/start flow, RLCR stop hook review flow, and one-shot `/humanize:ask-codex`.

- edge_cases_or_failure_modes: Missing `jq` causes config, stop-hook effort, and setup-execution sections to skip rather than fail. Missing `codex` skips setup execution, while the `ask-codex` runtime section injects a mock `codex` into `PATH` to avoid a real external call. Malformed or unsupported config values are expected to warn and fall back: shell-unsafe `haiku!` and invalid effort `superhigh` are covered at `tests/test-unified-codex-config.sh:196` through `tests/test-unified-codex-config.sh:222`, and shell-safe but unsupported non-Codex model names `haiku`, `false`, and `claude-3` are covered at `tests/test-unified-codex-config.sh:223` through `tests/test-unified-codex-config.sh:247`. Stale reviewer keys in either config or state are ignored without poisoning Codex defaults at `tests/test-unified-codex-config.sh:400` through `tests/test-unified-codex-config.sh:468`. A manually edited state with invalid `codex_effort` must end the loop unexpectedly and avoid invoking Codex, as tested at `tests/test-unified-codex-config.sh:489` through `tests/test-unified-codex-config.sh:542`. The setup execution path also depends on a valid local base branch and clean temporary repo; its fixture creates a `master` branch and local bare remote to avoid network use at `tests/test-unified-codex-config.sh:558` through `tests/test-unified-codex-config.sh:587`.

- validation_or_tests: The assigned file is itself the validation artifact. It includes static assertions against source files, direct function-level shell assertions, state parser checks, setup-script integration checks, stop-hook behavioral checks, regex validation checks, and `ask-codex` runtime checks. I inspected the file directly from `tests/test-unified-codex-config.sh:1` through `tests/test-unified-codex-config.sh:779` and followed the directly referenced implementation surfaces listed above. I did not execute the full test because this branch export is read-only and several sections intentionally create temporary projects, generated `.humanize` state, mock executables, and setup artifacts; additionally, a read-only `git status` probe failed because the export is not seen as a writable git checkout in this sandbox.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 evidence section present; exact assigned id appears once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`