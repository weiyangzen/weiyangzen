# agent_119 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-119 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: This file is a stop-hook prompt block used as a contextual remediation note when local Humanize runtime state appears in `git status` during an RLCR stop attempt. It is not executable algorithm code itself; it is a small template fragment consumed by the git-clean gate to explain that `.humanize/` is local state created by `humanize:start-rlcr-loop` and should be ignored rather than committed. The direct content is at `prompt-template/block/git-not-clean-humanize-local.md:2-8`.
- algorithmic_behavior: The surrounding algorithm is implemented in `hooks/loop-codex-stop-hook.sh`. During the quick git-clean gate, the hook starts with cached porcelain status and removes untracked `.humanize/` and legacy `.humanize-*` entries from the dirty determination using `HUMANIZE_UNTRACKED_PATTERN='^\?\? \.humanize[-/]'` at `hooks/loop-codex-stop-hook.sh:659-660`. If the remaining filtered status is non-empty, the hook blocks exit for “uncommitted changes” at `hooks/loop-codex-stop-hook.sh:661-663`. It then looks back at the original unfiltered untracked status, and if any `.humanize[-/]` path was present, it loads this template with `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` at `hooks/loop-codex-stop-hook.sh:665-673`. The loaded block is appended to `SPECIAL_NOTES`, which is rendered into the broader `block/git-not-clean.md` prompt at `hooks/loop-codex-stop-hook.sh:698-700`. The final gate output is JSON with `"decision": "block"`, the rendered reason, and the system message `"Loop: Blocked - uncommitted changes detected, please commit first"` at `hooks/loop-codex-stop-hook.sh:702-710`.
- inputs_outputs_state: Inputs are the cached `git status --porcelain --untracked-files=all` output, the template directory resolved through shared loop common code, and the file content of `prompt-template/block/git-not-clean-humanize-local.md`. The template has no placeholders, so it is loaded as static Markdown rather than rendered with variables. Its output is a Markdown note appended to `SPECIAL_NOTES`, then interpolated into the main git-not-clean prompt through `{{SPECIAL_NOTES}}` in `prompt-template/block/git-not-clean.md:3-4`. The hook state transition is from “stop requested while active loop exists” to a blocked stop response when there are non-Humanize dirty changes. This template does not mutate repository state; it instructs the agent/user to mutate `.gitignore` and stage only `.gitignore` with the commands shown at `prompt-template/block/git-not-clean-humanize-local.md:4-8`.
- gates_or_invariants: The key invariant is that untracked `.humanize/` and `.humanize-*` runtime state should not itself make the working tree dirty for stop-hook purposes. That invariant is encoded in the filter at `hooks/loop-codex-stop-hook.sh:654-660` and mirrored in setup at `scripts/setup-rlcr-loop.sh:729-731`. The template is only a special note; it is not the gate trigger. A stop is blocked only when `GIT_STATUS_FOR_BLOCK` remains non-empty after Humanize-local paths are filtered, as shown at `hooks/loop-codex-stop-hook.sh:660-663`. Another invariant is that local Humanize state should never be committed: the main git-not-clean template says not to stage `.humanize/` or legacy `.humanize-*` artifacts at `prompt-template/block/git-not-clean.md:11-14`, while stronger tracked-state protection lives in `prompt-template/block/git-tracked-humanize.md:1-19`.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:669`, inside the git-clean stop gate. Loading is performed by `load_template` from `hooks/lib/template-loader.sh:36-46`, which returns file contents when the template exists and emits a warning to stderr when missing. The main template composition depends on `prompt-template/block/git-not-clean.md:1-18`, especially `{{SPECIAL_NOTES}}` at line 4. Related sibling notes include `prompt-template/block/git-not-clean-untracked.md:2-10`, loaded for other untracked files at `hooks/loop-codex-stop-hook.sh:676-683`. Related hard protections include `prompt-template/block/git-add-humanize.md:1-34` for blocked broad `git add` behavior and `prompt-template/block/git-tracked-humanize.md:1-19` for already tracked or staged Humanize state. Setup-time cleanliness uses a related but non-template path in `scripts/setup-rlcr-loop.sh:723-740`.
- edge_cases_or_failure_modes: If only untracked `.humanize/` or `.humanize-*` paths exist, they are filtered out and this template is not emitted because the stop hook does not enter the dirty block. If a non-Humanize dirty file exists alongside `.humanize-old/`, the hook blocks and includes this note; this is covered by `tests/test-stop-hook-legacy-compat.sh:162-177`. If the template is missing or empty, the hook falls back to `Note: .humanize/ and .humanize-* directories are intentionally untracked.` at `hooks/loop-codex-stop-hook.sh:670-671`, so the gate still functions. The file begins with a blank line at `prompt-template/block/git-not-clean-humanize-local.md:1`; because `SPECIAL_NOTES` concatenation does not insert its own separator at `hooks/loop-codex-stop-hook.sh:673`, that leading newline is part of the formatting contract. The template’s suggested `echo '.humanize*' >> .gitignore` at `prompt-template/block/git-not-clean-humanize-local.md:6` is broad and intentionally covers both `.humanize/` and legacy `.humanize-*`; however, repeated use may append duplicate ignore lines because the prompt block itself does not perform idempotence checks.
- validation_or_tests: The most specific behavioral validation found is `tests/test-stop-hook-legacy-compat.sh`. Test 1 verifies that untracked legacy `.humanize-old` paths alone do not trigger the uncommitted-changes block at `tests/test-stop-hook-legacy-compat.sh:145-159`. Test 1b verifies that `.humanizeconfig` still blocks as a real untracked file and, when `.humanize-old/` is also present, the output includes `Special Case - \.humanize directory detected` plus the generic untracked-file note at `tests/test-stop-hook-legacy-compat.sh:162-177`. Generic template reference and load coverage also applies: `tests/test-template-references.sh:83-107` scans shell template references and fails missing templates, while `tests/test-templates-comprehensive.sh:90-107` loads every Markdown template and checks it is non-empty. Template-loader behavior is covered by `tests/test-template-loader.sh:55-78` for existing and missing template loading, and by `tests/test-template-loader.sh:197-220` for safe fallback behavior. I did not run the test suite because this task is read-only research; no files were modified.
- skip_candidate: `yes: this is a static prompt-template fragment, not a standalone core algorithm implementation. It is still relevant to algorithmic behavior because the stop-hook git-clean gate depends on it for the user-facing transition message when Humanize-local state is detected alongside real dirty files.`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-119`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`