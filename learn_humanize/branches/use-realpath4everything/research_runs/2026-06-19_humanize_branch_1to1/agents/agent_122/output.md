# agent_122 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-122 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: User-facing block template for the RLCR loop Bash validator’s “no remote push by default” policy. The template explains that commits should remain local during the loop and gives the operator-facing escape hatch: restart the loop with `--push-every-round`. The file is a prompt/block contract, not executable logic, but it is part of a hard command gate because `hooks/loop-bash-validator.sh` renders it immediately before exiting with a blocking status.
- algorithmic_behavior: The template has three functional sections: title at `prompt-template/block/git-push.md:1`, policy rationale at `prompt-template/block/git-push.md:3`-`prompt-template/block/git-push.md:4`, and remediation command hint at `prompt-template/block/git-push.md:6`-`prompt-template/block/git-push.md:9`. It contains no `{{VAR}}` placeholders, so rendering is deterministic and independent of runtime substitutions. Runtime behavior is supplied by `hooks/loop-bash-validator.sh:191`-`hooks/loop-bash-validator.sh:220`: when an RLCR loop is active, the state file parses successfully, `STATE_PUSH_EVERY_ROUND` is not exactly `true`, and the lowercased command begins with `git push`, the validator renders `block/git-push.md` via `load_and_render_safe` and exits `2`.
- inputs_outputs_state: Direct template inputs are only the template directory and relative template name, passed to `load_and_render_safe "$TEMPLATE_DIR" "block/git-push.md" ...` at `hooks/loop-bash-validator.sh:217`; no template variables are consumed. The surrounding gate’s inputs are `ACTIVE_LOOP_DIR`, resolved state file, parsed `STATE_PUSH_EVERY_ROUND`, and `COMMAND_LOWER` from the Bash tool invocation. Output is the rendered Markdown block on stderr plus validator exit code `2`, which blocks the attempted shell command. The template itself performs no state transition, but the caller’s state transition is “allow shell command evaluation” to “deny command before execution,” preserving local-only commit state when `push_every_round` is false. The state value originates from setup: `--push-every-round` sets `PUSH_EVERY_ROUND=true` in `scripts/setup-rlcr-loop.sh:232`-`scripts/setup-rlcr-loop.sh:234`, and setup writes `push_every_round: $PUSH_EVERY_ROUND` to `.humanize/loop/state.md` at `scripts/setup-rlcr-loop.sh:880`-`scripts/setup-rlcr-loop.sh:887`.
- gates_or_invariants: The invariant encoded by this block is that an active RLCR loop should not push to remote unless explicitly configured. The hard gate is exact in `hooks/loop-bash-validator.sh:210`-`hooks/loop-bash-validator.sh:218`: `PUSH_EVERY_ROUND != "true"` and command regex `^[[:space:]]*git[[:space:]]+push` produces the block. The state parser fails closed before this check: malformed state causes an error and exit at `hooks/loop-bash-validator.sh:195`-`hooks/loop-bash-validator.sh:198`. The template-loader invariant is safe fallback: if the real template is missing or empty, `load_and_render_safe` emits the fallback instead of leaving the block blank, implemented at `hooks/lib/template-loader.sh:188`-`hooks/lib/template-loader.sh:210`.
- dependencies_and_callers: Primary caller is `hooks/loop-bash-validator.sh:217`. It depends on `hooks/lib/template-loader.sh`, where `load_template` reads the file from `$template_dir/$template_name` if present at `hooks/lib/template-loader.sh:36`-`hooks/lib/template-loader.sh:46`, and `load_and_render_safe` falls back on missing or empty content at `hooks/lib/template-loader.sh:194`-`hooks/lib/template-loader.sh:207`. The common state field is declared as `FIELD_PUSH_EVERY_ROUND="push_every_round"` in `hooks/lib/loop-common.sh:32`, and parsing populates `STATE_PUSH_EVERY_ROUND` with default `false` behavior noted around `hooks/lib/loop-common.sh:456`, `hooks/lib/loop-common.sh:510`, and `hooks/lib/loop-common.sh:590`. Related sibling policy template `prompt-template/block/unpushed-commits.md` handles the opposite stop-hook case: when `--push-every-round` is enabled and commits remain unpushed, `hooks/loop-codex-stop-hook.sh:714`-`hooks/loop-codex-stop-hook.sh:735` blocks loop exit until push occurs.
- edge_cases_or_failure_modes: If no RLCR loop is active, the template is not consulted because the whole check is under `[[ -n "$ACTIVE_LOOP_DIR" ]]` at `hooks/loop-bash-validator.sh:191`. If state parsing fails, the validator blocks earlier with a malformed-state error, so this template is not rendered. If `push_every_round` is missing or anything other than exact string `true`, parser defaults/non-true semantics make `git push` blocked. The command match only targets commands starting with `git push` after whitespace; chained commands where `git push` appears after `;`, `&&`, or `|` are not covered by this specific regex, though earlier generic git-command checks may apply elsewhere. If the template file is missing, unreadable, or empty, the caller’s fallback still blocks with a shorter message from `hooks/loop-bash-validator.sh:213`-`hooks/loop-bash-validator.sh:216`.
- validation_or_tests: Template loading is directly covered by `tests/test-template-loader.sh:55`-`tests/test-template-loader.sh:65`, which loads `block/git-push.md` and expects “Git Push Blocked.” Fallback-vs-real-template behavior is covered by `tests/test-template-loader.sh:211`-`tests/test-template-loader.sh:222`, expecting the real template to be used instead of fallback. Comprehensive template tests repeat the same real-template assertion at `tests/test-templates-comprehensive.sh:456`-`tests/test-templates-comprehensive.sh:463`. Error handling for missing template directories using this path is covered at `tests/test-error-scenarios.sh:55`-`tests/test-error-scenarios.sh:64`. I did not run tests because this assignment requested read-only research notes and no modifications.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence section present for the single assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`