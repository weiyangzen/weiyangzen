# agent_188 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-188 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the RLCR hook gate layer. The file drives the hook scripts as black-box command-line programs and asserts the safety contract for JSON parsing, protected RLCR state files, goal-tracker mutability, active-loop stop behavior, and concurrency/no-crash behavior. It specifically covers hooks called out in the header: `loop-edit-validator.sh`, `loop-plan-file-validator.sh`, and `loop-codex-stop-hook.sh`, while also exercising `loop-bash-validator.sh`, `loop-write-validator.sh`, and `loop-read-validator.sh` for cross-hook invariants (`tests/robustness/test-hook-system-robustness.sh:3-14`).
- algorithmic_behavior: The script creates isolated temporary fixtures with `setup_test_dir`, then feeds synthetic Claude/Codex hook JSON on stdin to each validator with `CLAUDE_PROJECT_DIR` pointing at the fixture root (`tests/robustness/test-hook-system-robustness.sh:19-25`). It validates behavior by checking Unix exit codes and, where hooks use Claude-style blocking, JSON output containing `"decision": "block"`. The tests define three result classes: allow/pass-through as exit `0` with no block decision, hard pre-tool block as exit `2`, and graceful malformed-input handling as any non-crash exit below `128`.
- algorithmic_behavior: Edit validator coverage starts with a valid `Edit` payload accepted, then confirms active-loop `state.md` writes are blocked, malformed JSON is rejected without a signal crash, missing `file_path` is handled without crashing, outside `.humanize/rlcr` path traversal is delegated to the host sandbox, and traversal that canonicalizes back to an RLCR `state.md` is blocked with exit `2` (`tests/robustness/test-hook-system-robustness.sh:39-154`). This is directly relevant to the branch theme because the tested hook side uses path canonicalization/realpath logic before protected-file comparisons (`hooks/loop-edit-validator.sh:71-85`, `hooks/lib/project-root.sh:98-143`).
- algorithmic_behavior: Plan-file validator coverage builds a real temporary git repo, commits a baseline, writes an RLCR state file with schema fields, and verifies a plan-file edit is allowed when state/git invariants hold, a non-plan file passes through, and empty JSON does not crash (`tests/robustness/test-hook-system-robustness.sh:164-241`). The called validator resolves project root from `CLAUDE_PROJECT_DIR`, finds the active loop, strict-parses state, checks schema fields, verifies branch consistency, and checks plan tracking/cleanliness through timeout-wrapped git commands (`hooks/loop-plan-file-validator.sh:18-49`, `hooks/loop-plan-file-validator.sh:80-116`, `hooks/loop-plan-file-validator.sh:122-230`).
- algorithmic_behavior: State parser coverage calls `get_current_round` directly against state frontmatter variants: extra whitespace, quoted/special path values, and missing closing delimiter (`tests/robustness/test-hook-system-robustness.sh:252-306`). The expected invariant is tolerant extraction of `current_round` or fallback to `0` rather than aborting; the implementation extracts YAML frontmatter with `sed`, matches `current_round:`, strips spaces, and defaults to `0` (`hooks/lib/loop-common.sh:430-440`).
- algorithmic_behavior: Command-injection and protected-file coverage asserts that Bash cannot bypass Write/Edit validators for RLCR-owned state and goal-tracker files. It constructs current and old loop directories, then verifies Bash blocks `state.md` modification, blocks `goal-tracker.md` modification after round 0, and blocks reads of old-session goal trackers, while allowing unrelated dangerous shell text through because host sandboxing owns unrelated command risk (`tests/robustness/test-hook-system-robustness.sh:316-417`, `tests/robustness/test-hook-system-robustness.sh:538-565`). The Bash hook validates JSON, rejects overly deep input, finds the active loop, strict-parses state, and pattern-matches write-like shell constructs via `command_modifies_file` (`hooks/loop-bash-validator.sh:22-46`, `hooks/loop-bash-validator.sh:191-279`, `hooks/lib/loop-common.sh:1485-1510`).
- algorithmic_behavior: Goal-tracker mutability is the main state-transition sub-contract. After round 0, Write/Edit may change only the mutable section of the active `goal-tracker.md`; immutable-section writes are blocked. The script tests mutable Write, immutable Write, mutable Edit, mutable deletion by Edit, immutable Edit, and old-session Read blocking (`tests/robustness/test-hook-system-robustness.sh:418-550`). The implementation previews Edit results with Perl, extracts immutable sections using an `awk` boundary scanner, compares old/new immutable content, and normalizes active goal-tracker paths before allowing changes (`hooks/lib/loop-common.sh:923-963`, `hooks/lib/loop-common.sh:968-991`, `hooks/loop-write-validator.sh:243-265`, `hooks/loop-edit-validator.sh:194-229`).
- algorithmic_behavior: Stop-hook state behavior is exercised across missing, corrupt, incomplete, and valid active-loop states. No `.humanize` state must allow natural exit; corrupt state must not crash; missing critical fields should end/allow as unexpected; a complete active state with a mocked `codex` executable must emit exit `0` with a JSON block decision (`tests/robustness/test-hook-system-robustness.sh:644-780`). The stop hook finds the active loop with session-aware lookup, exits early if none, resolves active state, parses frontmatter, terminates unexpected malformed states, and emits JSON block responses for active-loop continuation (`hooks/loop-codex-stop-hook.sh:61-95`, `hooks/loop-codex-stop-hook.sh:107-123`, `hooks/loop-codex-stop-hook.sh:186-205`, `hooks/loop-codex-stop-hook.sh:1640-1669`).
- algorithmic_behavior: Robustness stress coverage verifies concurrent read-validator invocations do not mutate/corrupt state, and large/deep/special JSON payloads do not crash validators. It spawns ten parallel `loop-read-validator.sh` processes and then re-reads `current_round`, applies a 5-second timeout to a 100KB Write payload, checks escaped null handling, and submits nested JSON (`tests/robustness/test-hook-system-robustness.sh:604-632`, `tests/robustness/test-hook-system-robustness.sh:790-831`). Timeout portability is provided by `run_with_timeout`, which selects `gtimeout`, GNU `timeout`, Python, or no-timeout fallback (`scripts/portable-timeout.sh:9-71`).
- inputs_outputs_state: Inputs are synthetic hook JSON objects piped through stdin, temporary RLCR state/goal-tracker/plan fixtures, temporary git repositories, environment variable `CLAUDE_PROJECT_DIR`, and a mocked `codex` binary for the active-loop stop-hook case (`tests/robustness/test-hook-system-robustness.sh:41-43`, `tests/robustness/test-hook-system-robustness.sh:167-177`, `tests/robustness/test-hook-system-robustness.sh:760-771`). Outputs are test harness `pass`/`fail` counters, hook stdout/stderr captures, exit-code assertions, and final process exit from `print_test_summary` (`tests/test-helpers.sh:30-78`, `tests/robustness/test-hook-system-robustness.sh:838-839`). State transitions are fixture-local: creation of `.humanize/rlcr/<timestamp>/state.md`, optional `unexpected-state.md` rename by the stop hook for incomplete state, git initialization/commit, and no expected mutation in concurrent read tests.
- gates_or_invariants: Protected RLCR state files are never user-editable through Edit/Write/Bash; path traversal that resolves to `state.md` must be blocked; unrelated non-RLCR paths pass through to sandbox enforcement; malformed JSON and unusual content must fail gracefully, not terminate by signal; active loop stop attempts block with JSON decision rather than nonzero hook failure; missing/no active state allows natural exit; after round 0 only the goal-tracker mutable section may change; old loop goal-trackers are inaccessible; concurrent validators must preserve state. The test repeatedly encodes these gates as exit `0` allow/no block, exit `2` pre-tool block, or exit `<128` graceful error (`tests/robustness/test-hook-system-robustness.sh:72-77`, `tests/robustness/test-hook-system-robustness.sh:149-154`, `tests/robustness/test-hook-system-robustness.sh:653-657`, `tests/robustness/test-hook-system-robustness.sh:774-780`).
- dependencies_and_callers: Direct dependencies are `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, and `tests/test-helpers.sh` (`tests/robustness/test-hook-system-robustness.sh:19-23`). Runtime dependencies include `jq`, `git`, `bash`, `sed`, `grep`, `perl` for Edit preview behavior, and a timeout implementation or Python fallback. Hook dependencies exercised are `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. The shared root/canonicalization dependency is `hooks/lib/project-root.sh`, where `resolve_project_root` prioritizes `CLAUDE_PROJECT_DIR` and canonicalizes via `realpath` (`hooks/lib/project-root.sh:41-53`).
- edge_cases_or_failure_modes: The spec intentionally probes malformed JSON, missing JSON fields, `..` traversal, missing frontmatter delimiter, corrupted non-YAML state, missing required state fields, concurrent access, very large JSON, escaped null path content, and nested JSON (`tests/robustness/test-hook-system-robustness.sh:79-105`, `tests/robustness/test-hook-system-robustness.sh:123-154`, `tests/robustness/test-hook-system-robustness.sh:289-306`, `tests/robustness/test-hook-system-robustness.sh:660-724`, `tests/robustness/test-hook-system-robustness.sh:790-831`). One notable test-label inconsistency exists: after Test 16 the numbering jumps to Test 18, and the mutable deletion echo says `Test 12ea`; this is cosmetic and does not change execution order or assertions (`tests/robustness/test-hook-system-robustness.sh:510-526`, `tests/robustness/test-hook-system-robustness.sh:660-683`).
- validation_or_tests: This file is itself validation. It accumulates pass/fail counters through `tests/test-helpers.sh`, exercises hooks as subprocesses, and exits nonzero if any assertion fails (`tests/test-helpers.sh:30-78`, `tests/robustness/test-hook-system-robustness.sh:838-839`). I did not execute the test suite because this branch export is read-only and the assignment requested research notes only; direct inspection shows the script would create temp directories, temporary git repos, and a mock `codex` under `mktemp` rather than editing the repository.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`