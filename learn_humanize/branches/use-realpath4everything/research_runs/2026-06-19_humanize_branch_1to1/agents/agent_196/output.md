# agent_196 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-196 `file` `tests/robustness/test-template-stress-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the template rendering subsystem. The file does not implement production logic; it defines behavioral gates for [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:56), especially `render_template`, `load_and_render`, and `load_and_render_safe`. It is part of the broader suite through [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/run-all-tests.sh:108), where it is listed under robustness tests.
- algorithmic_behavior: The script starts with strict Bash mode, resolves `SCRIPT_DIR` and `PROJECT_ROOT`, sources the template loader plus shared test helpers, and creates an isolated temp directory via `setup_test_dir` at [test-template-stress-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-template-stress-robustness.sh:12) and [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:86). It then runs 18 ordered checks. Normal-operation checks verify basic `{{VAR}}` substitution, missing-template fallback rendering, and file-backed rendering of a roughly 1KB template. Stress checks verify 10KB and 100KB variable values, a roughly 100KB template file, and 50 substitutions in one template. Edge checks verify regex metacharacters, ampersands, backslashes, embedded newlines, empty values, placeholder-in-value injection prevention, dollar signs, repeated placeholders, boundary placeholders, and a template consisting only of one placeholder.
- inputs_outputs_state: Inputs are inline template strings, `VAR=value` argument pairs, and generated temporary template files under `$TEST_DIR`. Outputs are rendered strings captured in `RESULT`, pass/fail counter mutations through `pass` and `fail`, and a process exit code from `print_test_summary` at [test-template-stress-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-template-stress-robustness.sh:289). State transitions are simple and deterministic: each check computes a result, compares it to either exact expected output or a size/substring predicate, increments shared counters in [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:22), then the final summary returns success only when `TESTS_FAILED == 0` at [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:71).
- gates_or_invariants: The central invariant is single-pass template replacement: a value containing `{{VAR_B}}` must not be rescanned after insertion, asserted at [test-template-stress-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-template-stress-robustness.sh:221). This matches loader comments and implementation in [template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:52). Other gates require replacement values to be treated literally rather than as regex or `sed` replacement syntax, so `.*+?^${}()[]|\`, `&`, `\`, and `$` survive unchanged. Size gates require outputs above 10,000, 100,000, and 100KB-scale thresholds, proving the renderer does not truncate large values or files. Fallback gating requires `load_and_render_safe` to render fallback variables when the named template is absent.
- dependencies_and_callers: Direct dependencies are `hooks/lib/template-loader.sh`, `tests/test-helpers.sh`, and standard tools `bash`, `awk` indirectly through `render_template`, `env`, `mktemp`, `printf`, `seq`, `date`, `wc`, and `grep`. The production dependency being specified is the template loader’s `awk` scanner, which builds `TMPL_VAR_` environment variables and scans each line for `{{...}}` delimiters at [template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:73). Production callers include common hook initialization in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/loop-common.sh:240), read/write/bash validators that render block messages such as [hooks/loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-read-validator.sh:236), and scripts such as [scripts/bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-validate-delta.sh:196). Related tests include baseline template behavior in `tests/test-template-loader.sh`, missing/error handling in `tests/test-error-scenarios.sh`, and robustness error cases in `tests/robustness/test-template-error-robustness.sh`.
- edge_cases_or_failure_modes: The test guards against historical failure modes common to shell templating: replacement via regex/sed corrupting metacharacters, ampersand expanding to the matched text, backslashes being consumed, dollar signs being shell-expanded, multiline values being collapsed or truncated, empty values leaving placeholders behind, repeated placeholders only replacing once, and variables at string boundaries being missed. It also exposes two design limits. First, `render_template` is line-oriented because the loader’s `awk` block processes `$0` line by line and prints each result; multiline values embedded into one placeholder are expected to be present but the test only checks for `Line1` and `Line3`, not exact formatting. Second, the test comment says circular references are not applicable because variables do not reference one another, which is enforced by single-pass substitution rather than dependency resolution.
- validation_or_tests: The file itself is the validation asset. Its executable contract covers 18 checks and ends by returning nonzero if any check failed. I performed read-only syntax validation with `bash -n tests/robustness/test-template-stress-robustness.sh`, `bash -n hooks/lib/template-loader.sh`, and `bash -n tests/test-helpers.sh`; all exited 0. I did not run the full stress script because it creates temporary files and this assignment explicitly says not to modify files in the read-only branch export. A `git status --short` attempt was not usable in this export: the directory is not a git checkout and the sandbox also blocked macOS cache writes under `/tmp`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`