# agent_096 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-096 `file` `tests/test-stop-hook-legacy-compat.sh`
- cursor: `[_]`
- core_role: This file is an executable compatibility specification for `hooks/loop-codex-stop-hook.sh`. It verifies that legacy Humanize runtime paths do not falsely trip the stop-hook git-dirty gate, that a similarly named real file still blocks, and that legacy loop state without `bitlesson_required` preserves the old disabled behavior. It is core-adjacent test coverage for the stop-hook gate pipeline rather than production logic itself.
- algorithmic_behavior: The script runs under strict Bash mode (`set -euo pipefail`, line 12), resolves its own directory and project root (lines 14-18), creates an isolated temp test area via `setup_test_dir` (line 20), and points `XDG_CACHE_HOME` inside that temp area so hook debug/cache files do not leak to the user cache (lines 22-23). It then installs a fake `codex` executable ahead of the real `PATH` (lines 25-38). The fake `codex` writes a marker file when invoked and prints controllable mock feedback, giving the tests a binary signal for whether the stop hook reached the Codex review stage. `create_stop_hook_fixture` builds a temporary git repo, commits `.gitignore`, creates an ignored `plans/test-plan.md`, captures current branch and base commit, and constructs `.humanize/rlcr/2026-03-01_00-00-00` with `plan.md`, `state.md`, `round-0-summary.md`, and `goal-tracker.md` (lines 40-118). `run_stop_hook` executes the real stop hook from inside the fixture repo with `CLAUDE_PROJECT_DIR` set to that repo, mock Codex environment variables, JSON `{}` on stdin, and captures both exit code and output without aborting the test process (lines 120-136). Three assertions then encode the behavioral contract: untracked `.humanize-old/` must be ignored for dirty checks and allow Codex invocation (lines 145-160); untracked `.humanizeconfig` must still be treated as a normal dirty file and block before Codex while preserving notes for both Humanize local dirs and generic untracked files (lines 162-183); and a legacy state file that omits `bitlesson_required` must still reach Codex even if `.humanize/bitlesson.md` exists (lines 185-197).
- inputs_outputs_state: Inputs are the real stop hook at `hooks/loop-codex-stop-hook.sh` (line 18), helper functions from `tests/test-helpers.sh` (line 16), the generated fixture repo state, hook stdin `{}` (line 132), `CLAUDE_PROJECT_DIR`, `XDG_CACHE_HOME`, `MOCK_CODEX_MARKER`, and `MOCK_CODEX_OUTPUT`. The key fixture state is `state.md` frontmatter with `current_round: 0`, `max_iterations: 42`, `codex_model: gpt-5.4`, `codex_effort: high`, `push_every_round: false`, `full_review_round: 5`, `plan_file: "plans/test-plan.md"`, `plan_tracked: false`, branch/base commit fields, `review_started: false`, `ask_codex_question: false`, and `agent_teams: false` (lines 67-85). Notably, it omits `bitlesson_required`. Outputs are pass/fail counters through `pass`, `fail`, and `print_test_summary` (lines 152-199), plus captured `RUN_OUTPUT`, `RUN_EXIT_CODE`, and the marker file `codex-called.marker` used as an observable Codex-invocation side effect (lines 120-135). The hook under test may also create cache files under the temp `XDG_CACHE_HOME`; the test does not assert those files directly.
- gates_or_invariants: The test assumes `.humanize/rlcr/.../state.md` is the active loop state and that `CLAUDE_PROJECT_DIR` drives root resolution (lines 62-64, 129). In the branch under review, that root is canonicalized via `hooks/lib/project-root.sh`, which documents `CLAUDE_PROJECT_DIR -> git rev-parse -> failure` and realpath canonicalization (project-root lines 5-16, 41-52). The dirty-check invariant under test is implemented in the stop hook by filtering untracked `.humanize/` and dash-separated `.humanize-*` entries with `HUMANIZE_UNTRACKED_PATTERN='^\?\? \.humanize[-/]'` before deciding whether to block (stop hook lines 654-660). That pattern intentionally matches `.humanize-old/` and `.humanize/bitlesson.md`, but not `.humanizeconfig`, because `.humanizeconfig` lacks `-` or `/` after `.humanize`. If any non-filtered status remains, the hook emits `"Loop: Blocked - uncommitted changes detected, please commit first"` and exits before Codex (stop hook lines 687-710). The legacy BitLesson invariant is that `BITLESSON_REQUIRED` defaults to `false` and is only populated when raw frontmatter contains `bitlesson_required:` (stop hook lines 144-147), so the existence of `.humanize/bitlesson.md` alone must not activate BitLesson delta validation. The BitLesson validator only runs when `BITLESSON_REQUIRED == "true"` and not in finalize phase (stop hook lines 823-838).
- dependencies_and_callers: Direct dependencies are `tests/test-helpers.sh` for temp directory setup, git fixture initialization, result reporting, and cleanup (helper lines 86-104); system `git`; `jq` via the stop hook; and the real stop-hook libraries sourced by `hooks/loop-codex-stop-hook.sh`, including `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, and `hooks/lib/methodology-analysis.sh` (stop hook lines 42-54). The fixture depends on stop-hook active-loop discovery through `.humanize/rlcr/<timestamp>/state.md`; active state resolution prefers methodology, finalize, then normal state files (loop-common lines 264-280), and active-loop discovery returns the newest active loop when no session filter is present (loop-common lines 332-356). The test is included in the aggregate runner list at `tests/run-all-tests.sh`, which names `test-stop-hook-legacy-compat.sh` as part of the test suite. The stop hook also uses prompt templates checked by this test’s string expectations: `prompt-template/block/git-not-clean.md` contains the overall dirty-block copy and warning not to stage `.humanize/` or legacy `.humanize-*` artifacts (template lines 1-18); `prompt-template/block/git-not-clean-humanize-local.md` contains “Special Case - .humanize directory detected” (lines 1-8); and `prompt-template/block/git-not-clean-untracked.md` contains “Note on Untracked Files” (lines 1-10).
- edge_cases_or_failure_modes: A regression that changes the untracked filter from `.humanize[-/]` to a broader `.humanize` prefix would incorrectly ignore `.humanizeconfig`; Test 1b catches this by requiring a dirty block and no Codex marker (lines 162-183). A regression that stops ignoring dash-separated legacy runtime directories would block Test 1 before Codex (lines 145-160). A regression that infers BitLesson enforcement from the presence of `.humanize/bitlesson.md` instead of the explicit state field would block Test 2 before the mocked Codex call because the summary intentionally omits a BitLesson Delta section (lines 87-93, 110-117, 185-197). The test intentionally uses `plan_tracked: false` and a gitignored `plans/` path (lines 49-55, 77) so plan-file dirtiness does not dominate the compatibility cases. It also avoids anti-drift `drift_status`, so the round-contract gate remains disabled for this legacy fixture; the stop hook only enforces a round contract when raw state contains `drift_status:` (stop hook lines 789-817). If `codex --help` behavior changed substantially, the fake `codex` still exits successfully and prints output for both the feature probe and review call, but because the mock ignores arguments, the test only proves reachability, not real Codex CLI semantics.
- validation_or_tests: This file is itself the validation asset. Its pass conditions are concrete: Test 1 requires exit code `0`, marker present, and no “Loop: Blocked - uncommitted changes” text (lines 152-160). Test 1b requires exit code `0`, marker absent, dirty-block text present, Humanize special-case note present, and generic untracked-file note present (lines 173-183). Test 2 requires exit code `0` and marker present (lines 190-197). The script exits with the helper summary status (lines 199-200). I did not execute the test because the assignment requested research notes only and the branch export is read-only; inspection was direct against the assigned file and referenced hook/template/helper code.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1, represented by the single Item Evidence section above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`