# agent_078 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-078 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for graceful error handling in the RLCR prompt-template loader. The file is test-only, but it defines core behavioral contracts for `hooks/lib/template-loader.sh`: missing templates must degrade safely, rendering must be literal and non-crashing, safe wrappers must emit fallbacks, and malformed placeholders must remain stable. The test sources the loader at [tests/test-error-scenarios.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-error-scenarios.sh:13) and resolves the template root through `get_template_dir` at [tests/test-error-scenarios.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-error-scenarios.sh:15).
- algorithmic_behavior: The script runs a 12-case assertion sequence with explicit pass/fail accounting instead of immediate abort. It initializes counters at [tests/test-error-scenarios.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-error-scenarios.sh:22), increments them through `pass` and `fail`, then exits according to `TESTS_FAILED` at [tests/test-error-scenarios.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-error-scenarios.sh:218). Cases 1-3 assert missing file, missing directory, and missing `load_and_render` template paths return empty stdout and status 0. Case 4 asserts empty template content renders to empty output. Case 5 asserts values containing regex-like characters such as brackets, parentheses, and asterisks render literally. Case 6 runs a strict-mode subshell with `set -euo pipefail` to prove a missing template does not terminate the caller. Cases 7-8 assert `load_and_render_safe` falls back, including variable substitution inside fallback text. Cases 9-10 validate template directory acceptance/rejection. Cases 11-12 assert empty and unclosed placeholders are preserved.
- inputs_outputs_state: Inputs are shell function calls, template names, fallback strings, and `VAR=value` render assignments. The only repository dependency loaded directly is `hooks/lib/template-loader.sh`; the only real template filesystem dependency is the resolved `prompt-template` directory. Outputs are colored PASS/FAIL lines, expected/got diagnostics, a summary, and process status 0 or 1. State is local to the shell process: `TESTS_PASSED`, `TESTS_FAILED`, transient `CONTENT`, `RESULT`, `EXIT_CODE`, and strict-mode subshell output. The script does not intentionally write repository files or mutate persistent project state.
- gates_or_invariants: Missing template paths must not be fatal: `load_template` and `load_and_render` are expected to produce empty stdout and exit 0 when files are absent, as asserted starting at [tests/test-error-scenarios.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-error-scenarios.sh:45). `render_template` must treat replacement values as literal text rather than regex or shell syntax. `load_and_render_safe` must provide useful fallback text when a template is missing, and must render variables inside the fallback when supplied. `validate_template_dir` must distinguish the real template tree from a non-existent path. Placeholder parsing must be conservative: `{{}}` and unclosed `{{...` forms stay unchanged rather than being deleted or throwing.
- dependencies_and_callers: This suite depends on `hooks/lib/template-loader.sh`, whose relevant implementation includes `get_template_dir` at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:26), `load_template` at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:36), `render_template` at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:56), `load_and_render_safe` at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:188), and `validate_template_dir` at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:216). The renderer uses `env` plus `awk` for single-pass placeholder substitution at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:73). The aggregate test runner includes this assigned suite at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/run-all-tests.sh:72). Adjacent coverage exists in `tests/test-template-loader.sh` for normal rendering and `tests/robustness/test-template-error-robustness.sh` for broader malformed/error scenarios.
- edge_cases_or_failure_modes: The file specifically protects against absent template files, absent template directories, empty template bodies, strict shell mode interaction, fallback rendering, invalid template roots, empty placeholders, unclosed placeholders, and regex-like characters in replacement values. A notable observed limitation is environmental: direct execution in this managed read-only export failed render-dependent cases because Bash could not create the temporary file backing the here-string used by `render_template` at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:128). That produced 7 passes and 5 failures in this sandbox, with the common error `cannot create temp file for here document: Operation not permitted`. This is a validation-environment failure, not evidence that the spec itself expects failure. It also shows that `load_and_render_safe` fallback-with-variables depends on `render_template`; if rendering cannot run at all, the “safe” fallback path with substitutions can still return empty.
- validation_or_tests: Inspected `tests/test-error-scenarios.sh` directly with line numbers. `bash -n tests/test-error-scenarios.sh` passed with exit 0. `bash tests/test-error-scenarios.sh` was attempted and reached the suite summary, but failed 5 render-dependent assertions due the read-only sandbox’s inability to create shell temporary here-string files. `git rev-parse HEAD` was not usable in this branch export because the export is not a normal `.git` checkout in this sandbox and the macOS git shim attempted blocked temp/cache writes; the source commit above is therefore taken from the provided run metadata.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-078` appears as one Item Evidence section
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`