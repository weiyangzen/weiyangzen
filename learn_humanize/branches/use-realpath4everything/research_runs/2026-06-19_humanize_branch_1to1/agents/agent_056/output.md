# agent_056 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-056 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Initializes a repository-local RLCR loop session. It is not the long-running scheduler itself; it is the bootstrapper that validates the repo/plan/branch conditions, creates `.humanize/rlcr/<timestamp>/` state, writes the first round prompt/contract/summary scaffolds, and emits the activation prompt that downstream hooks use to enforce the loop. Key setup starts with shared realpath-aware project-root resolution via `loop-common.sh` at `scripts/setup-rlcr-loop.sh:25-33`, then creates loop state under `.humanize/rlcr` at `scripts/setup-rlcr-loop.sh:821-850`.
- algorithmic_behavior: The script follows a strict fail-fast sequence. It loads defaults and helpers, including `portable-timeout.sh` and `hooks/lib/loop-common.sh` (`scripts/setup-rlcr-loop.sh:17-33`). It defines plan-content extraction helpers for goal and acceptance criteria (`scripts/setup-rlcr-loop.sh:57-88`), parses CLI flags and positional plan input (`scripts/setup-rlcr-loop.sh:188-320`), resolves the project root (`scripts/setup-rlcr-loop.sh:326-330`), checks required commands (`scripts/setup-rlcr-loop.sh:340-363`), rejects starting when another active loop exists (`scripts/setup-rlcr-loop.sh:369-379`), validates agent-teams prerequisites (`scripts/setup-rlcr-loop.sh:385-397`), normalizes explicit versus positional plan arguments (`scripts/setup-rlcr-loop.sh:399-427`), validates git repository state (`scripts/setup-rlcr-loop.sh:433-443`), hardens the plan path (`scripts/setup-rlcr-loop.sh:455-556`), validates plan tracking policy (`scripts/setup-rlcr-loop.sh:562-607`), validates plan content (`scripts/setup-rlcr-loop.sh:618-671`), records safe branch/model settings (`scripts/setup-rlcr-loop.sh:686-716`), requires a clean working tree except untracked `.humanize` runtime files (`scripts/setup-rlcr-loop.sh:723-741`), resolves and snapshots the base branch commit (`scripts/setup-rlcr-loop.sh:748-815`), writes session files (`scripts/setup-rlcr-loop.sh:827-923`), builds the goal tracker (`scripts/setup-rlcr-loop.sh:929-1156`), builds the round summary/contract/prompt (`scripts/setup-rlcr-loop.sh:1162-1441`), and finally prints the activation message and initial prompt (`scripts/setup-rlcr-loop.sh:1447-1541`).
- inputs_outputs_state: Inputs are the plan path or `--plan-file`, `--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, `--base-branch`, `--full-review-round`, `--skip-impl`, `--claude-answer-codex`, `--agent-teams`, BitLesson flags, and `--privacy` (`scripts/setup-rlcr-loop.sh:91-140`, `scripts/setup-rlcr-loop.sh:188-320`). Environmental/config inputs include `CLAUDE_PROJECT_DIR`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, and config-backed `DEFAULT_CODEX_MODEL`, `DEFAULT_CODEX_EFFORT`, and `DEFAULT_AGENT_TEAMS` from `hooks/lib/loop-common.sh:208-235`. Repository inputs include git HEAD, branch refs, remote default branch, `.gitmodules`, plan file tracking status, working tree status, and existing `.humanize/rlcr` state. Outputs are `.humanize/rlcr/<timestamp>/plan.md`, `state.md`, `goal-tracker.md`, `round-0-summary.md`, `round-0-contract.md`, `round-0-prompt.md`, optional `.review-phase-started`, `.humanize/.pending-session-id`, initialized `.humanize/bitlesson.md`, stderr validation messages, stdout activation text, and exit status. State transition is from no active loop to active loop; in normal mode `review_started` is false, while `--skip-impl` starts review mode immediately (`scripts/setup-rlcr-loop.sh:872-907`, `scripts/setup-rlcr-loop.sh:920-923`).
- gates_or_invariants: Requires `codex`, `jq`, and `git` before setup continues (`scripts/setup-rlcr-loop.sh:340-363`). Requires one active loop at most via `find_active_loop` (`scripts/setup-rlcr-loop.sh:369-379`; implementation in `hooks/lib/loop-common.sh:308-420`). Requires git repo plus at least one commit (`scripts/setup-rlcr-loop.sh:433-443`). Plan path must be relative, contain no spaces or shell metacharacters, not be a symlink, have existing parent dirs, avoid symlinked parent path segments, resolve within the canonical project root, and not be inside a submodule (`scripts/setup-rlcr-loop.sh:455-556`). Tracking invariant is mode-dependent: with `--track-plan-file`, the plan must be tracked and clean; without it, the plan must be untracked/gitignored (`scripts/setup-rlcr-loop.sh:562-607`). Content gate requires at least five physical lines and at least three non-comment content lines; blank lines, HTML comments, and leading-hash lines are ignored (`scripts/setup-rlcr-loop.sh:618-671`). Branch/base/model values must be YAML-safe, Codex effort must be one of the allowed values, working tree must be clean, and the base branch must exist as a local ref (`scripts/setup-rlcr-loop.sh:686-815`). Agent teams require `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` (`scripts/setup-rlcr-loop.sh:385-397`). The late `PIPE` trap is intentionally installed only after state setup to make final display robust without masking internal pipeline failures (`scripts/setup-rlcr-loop.sh:1447-1449`).
- dependencies_and_callers: The script is the command backend for `/humanize:start-rlcr-loop`, documented in `docs/usage.md` and referenced throughout tests. It sources `scripts/portable-timeout.sh`, whose `run_with_timeout` abstracts `gtimeout`, GNU `timeout`, Python subprocess timeout, or no-timeout fallback (`scripts/portable-timeout.sh:9-71`). It sources `hooks/lib/loop-common.sh`, which itself uses `hooks/lib/project-root.sh`; that resolver intentionally canonicalizes project roots through `realpath` so setup-time and hook-time paths do not diverge under symlinked prefixes (`hooks/lib/project-root.sh:14-20`, `hooks/lib/project-root.sh:41-53`). It calls `scripts/bitlesson-init.sh` with `templates/bitlesson.md` (`scripts/setup-rlcr-loop.sh:860-866`). It writes `.humanize/.pending-session-id`; `hooks/loop-post-bash-hook.sh` consumes that one-shot signal after the setup Bash command and patches `session_id` into `state.md` only when the command starts with the recorded setup path (`hooks/loop-post-bash-hook.sh:8-21`, `hooks/loop-post-bash-hook.sh:61-130`). Runtime enforcement then shifts to hooks such as `hooks/loop-codex-stop-hook.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-plan-file-validator.sh`, which consume the generated state rather than being implemented in this setup file.
- edge_cases_or_failure_modes: `--skip-impl` with no plan bypasses plan validation, creates a placeholder plan, disables BitLesson summary enforcement, and starts review mode immediately (`scripts/setup-rlcr-loop.sh:408-418`, `scripts/setup-rlcr-loop.sh:830-848`, `scripts/setup-rlcr-loop.sh:875-923`). `--skip-impl` with a plan anchors review-only work to extracted plan goal/criteria (`scripts/setup-rlcr-loop.sh:678-679`, `scripts/setup-rlcr-loop.sh:931-1000`, `scripts/setup-rlcr-loop.sh:1202-1285`). Remote-only base branches are rejected because later `codex review --base` needs a local branch (`scripts/setup-rlcr-loop.sh:748-765`). If no remote default, local `main`, or local `master` exists, setup stops (`scripts/setup-rlcr-loop.sh:766-797`). If git checks time out, exit code `124` is treated as a hard failure (`scripts/setup-rlcr-loop.sh:562-578`, `scripts/setup-rlcr-loop.sh:723-727`). The content validator intentionally treats leading-hash markdown headings as non-content, so valid-looking plans composed only of headings can fail unless they include body text (`scripts/setup-rlcr-loop.sh:628-663`). Partial failures after creating `LOOP_DIR` could leave a timestamped directory, but active-loop detection requires an active state file, reducing accidental revival risk. Path handling is hardened against leaf and parent symlinks; project-root canonicalization comes from the shared realpath helper, which is central to this branch’s path-consistency theme.
- validation_or_tests: I inspected the file directly and ran `bash -n scripts/setup-rlcr-loop.sh`, which completed with exit code 0. Existing focused tests cover the file’s core gates in `tests/test-plan-file-validation.sh`: absolute path rejection, nonexistent parents, spaces/metacharacters, symlink rejection, project escape, non-git/no-commit repos, tracked/untracked plan policy, YAML-unsafe branch names, insufficient content, `--plan-file`, conflicting plan arguments, and Codex model/effort validation (`tests/test-plan-file-validation.sh:88-740`). Related hook tests cover the session-id and active-loop interaction surfaces this setup script seeds, including `tests/test-stop-hook-bg-allow.sh` references to setup timestamp behavior and `find_active_loop`, plus `hooks/loop-post-bash-hook.sh` for `.pending-session-id` consumption.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 Item Evidence section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`