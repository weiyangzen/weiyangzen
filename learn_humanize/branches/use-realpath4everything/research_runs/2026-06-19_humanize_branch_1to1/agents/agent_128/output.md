# agent_128 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-128 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: This is a stop-hook block prompt template for the RLCR loop’s “incomplete tasks” gate. It is not executable algorithm code by itself; its core role is to define the user-facing contract when the hook detects unfinished task state before allowing the agent to stop or move into Codex review. The template’s title and warning are at `prompt-template/block/incomplete-todos.md:1-5`, and the required remediation contract is at `prompt-template/block/incomplete-todos.md:7-12`.

- algorithmic_behavior: The surrounding algorithm checks task/todo completion before expensive review work. In `hooks/loop-codex-stop-hook.sh:401-407`, the hook declares a quick check for incomplete tasks before Codex review. It runs `hooks/check-todos-from-transcript.py` at `hooks/loop-codex-stop-hook.sh:408-413`. If that checker exits `1`, the hook strips the checker’s first marker line with `tail -n +2` into `INCOMPLETE_LIST` at `hooks/loop-codex-stop-hook.sh:434-437`, renders this template at `hooks/loop-codex-stop-hook.sh:439-445`, and returns a JSON block decision at `hooks/loop-codex-stop-hook.sh:447-455`. The template itself injects `{{INCOMPLETE_LIST}}` at `prompt-template/block/incomplete-todos.md:5`, then instructs the agent to complete all remaining tasks, mark them completed with `TaskUpdate`, and only then summarize/stop at `prompt-template/block/incomplete-todos.md:7-10`. It also explicitly prevents transition to Codex review while unfinished work remains at `prompt-template/block/incomplete-todos.md:12`.

- inputs_outputs_state: Input is the render variable `INCOMPLETE_LIST`, supplied by `loop-codex-stop-hook.sh` from the checker output. The checker builds that output from two possible sources: task files under the Claude task directory for the session, read in `find_incomplete_tasks_from_directory()` at `hooks/check-todos-from-transcript.py:123-170`, and legacy `TodoWrite` calls from a transcript JSONL file, read in `find_incomplete_todos_from_transcript()` at `hooks/check-todos-from-transcript.py:73-120`. The checker emits a first-line marker `INCOMPLETE_TODOS` followed by formatted bullet rows at `hooks/check-todos-from-transcript.py:204-220`; the hook discards the marker and passes only the bullet list into this template. Output is rendered Markdown stored as the JSON `reason` field for a hook response with `"decision": "block"` and a system message saying incomplete tasks were detected. The template does not directly mutate state. Instead, it defines the required external state transition: incomplete task statuses must be changed to `completed` before the stop/review transition may continue.

- gates_or_invariants: The principal gate is “all non-deferred work must be complete before stopping.” The template states that all remaining tasks must be completed before attempting to stop at `prompt-template/block/incomplete-todos.md:8`, each must be marked completed at `prompt-template/block/incomplete-todos.md:9`, and stopping/summary is allowed only after all tasks are complete at `prompt-template/block/incomplete-todos.md:10`. The implementation’s stricter details live in the checker: legacy todos whose status is not exactly `completed` are incomplete unless their lane is `[queued]` at `hooks/check-todos-from-transcript.py:106-118`; task-file statuses other than `completed` or `deleted` are incomplete unless lane `[queued]` is detected at `hooks/check-todos-from-transcript.py:149-165`. Empty hook input allows proceeding at `hooks/check-todos-from-transcript.py:175-180`, JSON parse errors return exit code `2` at `hooks/check-todos-from-transcript.py:181-184`, and the hook treats parse errors as a separate blocking condition at `hooks/loop-codex-stop-hook.sh:415-431`. If the checker file itself is missing, the hook skips this gate because the check is guarded by `[[ -f "$TODO_CHECKER" ]]` at `hooks/loop-codex-stop-hook.sh:410`.

- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, specifically the incomplete-task branch at `hooks/loop-codex-stop-hook.sh:434-456`. The template depends on `hooks/lib/template-loader.sh` for safe loading and rendering: `load_and_render_safe()` loads the template, falls back if content is missing or empty, and renders variables at `hooks/lib/template-loader.sh:188-210`. Rendering is single-pass, preserving missing placeholders and avoiding recursive placeholder substitution, documented and implemented at `hooks/lib/template-loader.sh:48-136`. Upstream detection depends on `hooks/check-todos-from-transcript.py`, whose exit-code contract is documented at `hooks/check-todos-from-transcript.py:9-13`. Template reference coverage is checked by `tests/test-template-references.sh`, which scans `load_template`, `load_and_render`, and `load_and_render_safe` calls and verifies referenced template files exist at `tests/test-template-references.sh:83-110`.

- edge_cases_or_failure_modes: If `INCOMPLETE_LIST` is empty despite checker exit `1`, the template still renders a block message with an empty task list because it is a passive template. If `INCOMPLETE_LIST` contains Markdown, shell metacharacters, or placeholder-like text, `render_template()` injects it as a single-pass value and does not rescan injected `{{...}}` content, reducing prompt corruption risk; this behavior is described at `hooks/lib/template-loader.sh:52-55` and implemented at `hooks/lib/template-loader.sh:69-128`. If the template is missing or empty, `load_and_render_safe()` uses the fallback message from `loop-codex-stop-hook.sh:439-443` via the fallback path at `hooks/lib/template-loader.sh:197-207`. If task JSON files are malformed or unreadable, the checker skips them at `hooks/check-todos-from-transcript.py:166-168`, which can produce a false allow for corrupted task records. If transcript JSONL lines are malformed, legacy todo parsing skips those lines at `hooks/check-todos-from-transcript.py:91-95`. `[queued]` tasks/todos are intentionally excluded from blocking at `hooks/check-todos-from-transcript.py:110-112` and `hooks/check-todos-from-transcript.py:156-158`, while missing todo status defaults to incomplete because it is not equal to `completed` at `hooks/check-todos-from-transcript.py:106-110`.

- validation_or_tests: I inspected tests but did not execute them, because this worker was instructed to produce read-only research notes only. Relevant validation coverage exists in `tests/test-todo-checker.sh`: incomplete legacy todos exit `1` and include details at `tests/test-todo-checker.sh:122-143`; `in_progress` counts as incomplete at `tests/test-todo-checker.sh:145-158`; missing status is treated as incomplete at `tests/test-todo-checker.sh:280-293`; empty content is still incomplete at `tests/test-todo-checker.sh:296-309`; multiple task-file records with one pending task exit `1` at `tests/test-todo-checker.sh:441-459`; deleted task files are ignored at `tests/test-todo-checker.sh:481-496`; mixed legacy TodoWrite and task-file sources detect incomplete task state at `tests/test-todo-checker.sh:498-516`. Stop-hook integration coverage exists in `tests/test-finalize-phase.sh`, where a finalize phase with incomplete todos must block at `tests/test-finalize-phase.sh:786-804` and must not invoke Codex during that check at `tests/test-finalize-phase.sh:806-811`. Template infrastructure coverage exists in `tests/test-templates-comprehensive.sh`, which loads all templates at `tests/test-templates-comprehensive.sh:87-107`, validates placeholder syntax expectations at `tests/test-templates-comprehensive.sh:118-160`, and checks safe fallback behavior at `tests/test-templates-comprehensive.sh:445-460`.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 item evidence section present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`