# agent_182 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-182 `file` `tests/robustness/test-base-branch-detection.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR setup base-branch auto-detection. The file isolates the base-branch selection behavior used by `scripts/setup-rlcr-loop.sh` and validates that RLCR can start without an `origin` remote when a usable local `main` or `master` branch exists. It is not production code, but it encodes expected behavior for a core setup algorithm that later controls review base state written into loop metadata.
- algorithmic_behavior: The test defines a local `detect_base_branch()` helper at `tests/robustness/test-base-branch-detection.sh:37` that mirrors the setup script’s auto-detection path. It accepts `project_root` and optional `git_timeout`, then tries three ordered branches of logic. First, it runs `git -C "$project_root" remote show origin`, extracts `HEAD branch:` with `grep` and `sed`, and accepts a non-empty, non-`(unknown)` value at `tests/robustness/test-base-branch-detection.sh:43`. Second, it checks local `refs/heads/main` via `git show-ref --verify --quiet` at `tests/robustness/test-base-branch-detection.sh:51`. Third, it checks local `refs/heads/master` at `tests/robustness/test-base-branch-detection.sh:57`. If none of those produce a branch, the function returns `1` at `tests/robustness/test-base-branch-detection.sh:63`.
- inputs_outputs_state: Inputs are temporary Git repositories created under `TEST_DIR`, the repository path argument passed to `detect_base_branch`, and a timeout value such as `5`. Outputs are stdout branch names (`main`, `master`, or a remote default branch name) plus shell exit status. The test state is managed by `setup_test_dir` from `tests/test-helpers.sh`, which creates a temp directory and registers cleanup with `trap` at `tests/test-helpers.sh:86`. Each scenario mutates only its throwaway repository: initializes Git, configures local identity, creates a branch, commits `file.txt`, optionally deletes `main`, and optionally creates a bare remote. Test result state is accumulated through `TESTS_PASSED`, `TESTS_FAILED`, and `TESTS_SKIPPED` counters in `tests/test-helpers.sh:22`, reported by `print_test_summary` at `tests/test-helpers.sh:58`.
- gates_or_invariants: The branch priority invariant in this test is remote default, then local `main`, then local `master`, then failure. The remote-default pipeline is explicitly guarded with `|| true` at `tests/robustness/test-base-branch-detection.sh:44` so `set -euo pipefail` does not abort when `origin` is absent. Remote default values equal to `(unknown)` are rejected at `tests/robustness/test-base-branch-detection.sh:45`. Local branches are accepted only if exact local refs exist under `refs/heads/`. The test’s expected invariant for missing `origin` is graceful fallback, not a hard failure. The production setup script adds additional gates not present in the test helper: user-supplied `--base-branch` parsing at `scripts/setup-rlcr-loop.sh:248`, local existence validation for explicit branches at `scripts/setup-rlcr-loop.sh:751`, remote-only explicit branch diagnostics at `scripts/setup-rlcr-loop.sh:755`, remote-default local-ref validation at `scripts/setup-rlcr-loop.sh:773`, YAML-unsafe branch-name rejection at `scripts/setup-rlcr-loop.sh:799`, and base commit capture via `git rev-parse "$BASE_BRANCH"` at `scripts/setup-rlcr-loop.sh:810`.
- dependencies_and_callers: The test sources `tests/test-helpers.sh` for temporary directory setup, result counters, `pass`, `fail`, and summary behavior at `tests/robustness/test-base-branch-detection.sh:18`. It sources `scripts/portable-timeout.sh` at `tests/robustness/test-base-branch-detection.sh:19`; that script chooses `gtimeout`, GNU `timeout`, Python, or no timeout at `scripts/portable-timeout.sh:10`, and exposes `run_with_timeout` at `scripts/portable-timeout.sh:33`. Runtime dependencies are Bash, Git, `grep`, `sed`, `mktemp`, and a timeout implementation if available. The production algorithm under test is in `scripts/setup-rlcr-loop.sh:743`, with the auto-detection path specifically at `scripts/setup-rlcr-loop.sh:767`. Downstream production callers consume the resolved `BASE_BRANCH` by writing it into loop state at `scripts/setup-rlcr-loop.sh:892`, using it in generated review prompts and status surfaces later in the same setup script, and requiring `base_branch` during hook state validation in `hooks/lib/loop-common.sh:566`.
- edge_cases_or_failure_modes: Test 1 covers no `origin` plus local `main`, expecting `main` at `tests/robustness/test-base-branch-detection.sh:73`. Test 2 covers no `origin` plus local `master` and no `main`, expecting `master` at `tests/robustness/test-base-branch-detection.sh:100`. Test 3 covers no `origin` and only `develop`, expecting detection failure at `tests/robustness/test-base-branch-detection.sh:129`. Test 4 creates a bare `origin`, pushes `main`, and accepts `main`; the assertion text allows either remote detection or local fallback because `git remote show origin` behavior can vary for a freshly created file remote at `tests/robustness/test-base-branch-detection.sh:181`. A notable coverage gap is that the file header claims user-specified `--base-branch` priority is tested at `tests/robustness/test-base-branch-detection.sh:6`, but the executable tests never invoke the setup script’s CLI parser or explicit branch validation path. Another gap is that the replicated helper does not verify that a remote default branch exists locally, while production does that at `scripts/setup-rlcr-loop.sh:773`; therefore this test is a close robustness spec for missing-origin fallback, not a complete executable mirror of current production detection.
- validation_or_tests: The file itself is the validation asset. It creates four concrete Git fixtures and calls `pass` or `fail` for each outcome, then exits through `print_test_summary "Base Branch Detection Summary"` at `tests/robustness/test-base-branch-detection.sh:198`, returning nonzero if any counter failed via `tests/test-helpers.sh:71`. The detection helper wraps Git operations with `run_with_timeout`, which prevents hanging `git remote show origin` or ref checks from blocking the robustness suite indefinitely. I inspected the file directly and compared its replicated algorithm to the production setup implementation, but did not execute the test because this branch export is explicitly read-only and the script creates temporary repositories.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 Item Evidence section present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`