# agent_187 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-187 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for hook input validation and monitor resilience. The file is not a production algorithm, but it directly drives production hook validators and monitor helpers through their public shell interfaces, making it core evidence for the branch’s safety behavior around canonical paths, JSON parsing, malformed input, shell command detection, and RLCR monitor edge cases. It sources production shared logic from `hooks/lib/loop-common.sh` and test counters from `tests/test-helpers.sh` at `tests/robustness/test-hook-input-robustness.sh:14-19`.

- algorithmic_behavior: The script runs 26 numbered checks in four main phases. First, it pipes crafted JSON payloads into `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, and `hooks/loop-bash-validator.sh` and asserts accept/reject exit semantics at `tests/robustness/test-hook-input-robustness.sh:35-235`. These validators call `validate_hook_input`, `is_deeply_nested`, and `require_tool_input_field` before applying tool-specific gates; those shared functions validate UTF-8, reject invalid JSON, require `tool_name`, require tool-specific fields, and compute JSON path depth with `jq` in `hooks/lib/loop-common.sh:91-165`. Second, it simulates monitor/log/terminal edge cases with width values, file growth/deletion, ANSI stripping, binary log line counting, narrow/wide rendering primitives, and rapid append loops at `tests/robustness/test-hook-input-robustness.sh:245-341`. Third, it tests command modification pattern detection for `sed -i` and redirection by calling `command_modifies_file` from production shared code at `tests/robustness/test-hook-input-robustness.sh:351-369`; the implementation matches redirects, `tee`, in-place editors, `mv`/`cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec >` patterns in `hooks/lib/loop-common.sh:1482-1510`. Fourth, it sources `scripts/humanize.sh` and invokes production monitor helpers and the real `_humanize_monitor_codex` monitor loop under controlled fake projects/home/cache dirs at `tests/robustness/test-hook-input-robustness.sh:381-664`.

- inputs_outputs_state: Inputs are inline JSON strings, shell command strings, synthetic log files, temporary `.humanize/rlcr/<session>` directories, temporary state/goal-tracker markdown files, fake XDG cache paths, and shimmed terminal functions. Hook-input JSON covers valid Read/Write/Bash payloads, malformed JSON, `{}` without `tool_name`, missing `tool_input.file_path`, 10KB Bash commands, shell metacharacters, Unicode path escapes, unknown tools, 50-level nested JSON, invalid UTF-8 bytes, and null-byte cases at `tests/robustness/test-hook-input-robustness.sh:35-235`. Outputs are pass/fail counters through `pass`, `fail`, and `print_test_summary` from `tests/test-helpers.sh:30-77`; the final process exits with the summary status at `tests/robustness/test-hook-input-robustness.sh:670-671`. State transitions are temporary and test-local: `setup_test_dir` creates `TEST_DIR` and registers cleanup at `tests/test-helpers.sh:86-89`; monitor tests create fake active RLCR sessions, then background-delete `.humanize/rlcr` to force `_humanize_monitor_codex` from active polling into graceful stop paths at `tests/robustness/test-hook-input-robustness.sh:531-560` and `tests/robustness/test-hook-input-robustness.sh:621-647`.

- gates_or_invariants: Hook validators must accept well-formed non-loop paths for matching tools with exit `0`, reject malformed or incomplete required fields with nonzero exit below signal range, ignore unrecognized tool names, reject JSON depth greater than 30, reject invalid UTF-8 with exit `1`, and never signal-crash on null-byte-stripped input. These expectations are encoded at `tests/robustness/test-hook-input-robustness.sh:35-235`. Production read/write/bash validators share a fixed gate order: read stdin, call `validate_hook_input`, reject deep nesting, check tool name, require the relevant field, then apply tool-specific protections in `hooks/loop-read-validator.sh:23-46`, `hooks/loop-write-validator.sh:24-47`, and `hooks/loop-bash-validator.sh:22-45`. The branch’s realpath-related invariant is visible in the production dependencies this test exercises indirectly: project roots are resolved with `CLAUDE_PROJECT_DIR` or Git toplevel and canonicalized via `realpath` in `hooks/lib/project-root.sh:41-53`; directory comparisons use `canonicalize_path_prefix`, which resolves only the parent directory and preserves the basename to avoid approving symlink-leaf bypasses in `hooks/lib/project-root.sh:55-96`. Read and write validators apply this parent-prefix canonical comparison before accepting loop file paths in `hooks/loop-read-validator.sh:307-322` and `hooks/loop-write-validator.sh:334-352`.

- dependencies_and_callers: Direct dependencies are `hooks/lib/loop-common.sh`, `tests/test-helpers.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, and `scripts/humanize.sh`. `loop-common.sh` itself sources `hooks/lib/project-root.sh`, `hooks/lib/template-loader.sh`, `scripts/lib/config-loader.sh`, and `hooks/lib/loop-bg-tasks.sh`, so test behavior also depends on project-root canonicalization, template fallback loading, config default loading, and background-task helpers. Monitor tests depend on `scripts/humanize.sh` helpers: `humanize_parse_goal_tracker` at `scripts/humanize.sh:71-154`, `humanize_detect_git_state` at `scripts/humanize.sh:158-204`, `humanize_parse_git_status` at `scripts/humanize.sh:208-252`, and `_humanize_monitor_codex` at `scripts/humanize.sh:261-1188`. The test is likely called by the repository’s aggregate robustness or all-tests runner, though this assigned file itself does not declare a caller. The local branch export was not a Git checkout, and the managed read-only sandbox blocked local Git/toolchain cache writes, so I relied on direct file inspection plus provided source metadata rather than repository status.

- edge_cases_or_failure_modes: The most security-relevant input edge cases are malformed JSON, missing fields, excessive nesting, invalid UTF-8 byte sequences, Bash-stripped null bytes, shell metacharacters inside JSON strings, very long commands, and Unicode path values. The monitor edge cases include terminal width changes, narrow terminal layout, ANSI log content, binary log content, file deletion, log rotation/truncation, rapid log growth, absent session dirs, and background removal of active `.humanize/rlcr`. A notable test-harness limitation is explicit in the null-byte case: Bash command substitution strips null bytes before the hook sees them, so the test only verifies graceful behavior after stripping rather than end-to-end null-byte rejection at `tests/robustness/test-hook-input-robustness.sh:219-235`. Another practical fragility is platform tooling: JSON behavior depends on `jq`; invalid UTF-8 rejection depends on `iconv` when present in `hooks/lib/loop-common.sh:101-109`; monitor tests shim `tput` and `clear` to isolate terminal behavior at `tests/robustness/test-hook-input-robustness.sh:465-469`, `tests/robustness/test-hook-input-robustness.sh:524-527`, and `tests/robustness/test-hook-input-robustness.sh:615-617`.

- validation_or_tests: This file is itself the validation asset. It validates hook JSON parsing, required field enforcement, non-crashing rejection behavior, command mutation detection, parser availability, and monitor graceful exit. Concrete production assertions include valid Read/Write/Bash JSON passing at `tests/robustness/test-hook-input-robustness.sh:35-69`, malformed and incomplete payload rejection at `tests/robustness/test-hook-input-robustness.sh:71-115`, DoS/binary robustness at `tests/robustness/test-hook-input-robustness.sh:177-217`, monitor helper parser contract checks for pipe-delimited outputs at `tests/robustness/test-hook-input-robustness.sh:381-433`, missing-session graceful monitor exit at `tests/robustness/test-hook-input-robustness.sh:444-486`, narrow-terminal no-signal-crash behavior at `tests/robustness/test-hook-input-robustness.sh:488-578`, and ANSI-log no-signal-crash behavior at `tests/robustness/test-hook-input-robustness.sh:580-664`. I did not run the test because the task requests research notes only, the branch export is read-only, and the script creates/removes temporary files and directories.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-187`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`