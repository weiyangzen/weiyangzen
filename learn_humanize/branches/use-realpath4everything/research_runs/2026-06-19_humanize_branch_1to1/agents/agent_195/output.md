# agent_195 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-195 `file` `tests/robustness/test-template-error-robustness.sh`
- cursor: `[_]`
- core_role: This is an executable robustness specification for the shell template loading/rendering subsystem. It does not implement production behavior itself; it verifies that `hooks/lib/template-loader.sh` remains resilient under missing files, malformed placeholders, unusual variable names/values, filesystem variation, concurrency, and discovery edge cases. It is included in the full robustness suite via `tests/run-all-tests.sh:101-117`, specifically at `tests/run-all-tests.sh:116`.
- algorithmic_behavior: The script runs under `set -euo pipefail` at `tests/robustness/test-template-error-robustness.sh:13`, resolves `SCRIPT_DIR` and `PROJECT_ROOT`, then sources `hooks/lib/template-loader.sh` and `tests/test-helpers.sh` at `tests/robustness/test-template-error-robustness.sh:15-18`. It creates an isolated temp directory through `setup_test_dir` at `tests/robustness/test-template-error-robustness.sh:20`. The test sequence then exercises `load_and_render_safe` and `render_template` across 20 scenarios. Missing templates/directories must return caller-provided fallbacks at `tests/robustness/test-template-error-robustness.sh:34-51`. Empty templates are allowed to resolve either empty or fallback at `tests/robustness/test-template-error-robustness.sh:53-63`. Malformed syntax tests assert non-crashing, non-empty output for unclosed, nested, opening-only, and empty-placeholder cases at `tests/robustness/test-template-error-robustness.sh:73-115`. Invalid variable names with spaces, special characters, and numeric-only names are treated as tolerated inputs at `tests/robustness/test-template-error-robustness.sh:125-155`. Value handling verifies single-pass non-recursive substitution, newline preservation, and very long names at `tests/robustness/test-template-error-robustness.sh:165-198`. Filesystem cases cover BOM, whitespace-only templates, filenames with spaces, permission denial, subdirectories, and symlinked templates at `tests/robustness/test-template-error-robustness.sh:208-260` and `tests/robustness/test-template-error-robustness.sh:309-334`. Concurrent behavior is checked by spawning 10 background render jobs and requiring all results to match while preserving the source file at `tests/robustness/test-template-error-robustness.sh:270-299`.
- inputs_outputs_state: Inputs are temporary template files under `$TEST_DIR`, explicit fallback strings, variable assignments such as `NAME=World`, malformed template strings such as `Hello {{NAME, welcome!`, filesystem state including chmod and symlink support, and the current UID for permission behavior. Outputs are human-readable PASS/FAIL lines from `pass`/`fail`, a summary from `print_test_summary`, and the process exit code. Shared test state is maintained by `TESTS_PASSED`, `TESTS_FAILED`, and `TESTS_SKIPPED` in `tests/test-helpers.sh:22-24`; `pass` increments passed at `tests/test-helpers.sh:30-33`, `fail` increments failed without immediately exiting at `tests/test-helpers.sh:35-44`, and `print_test_summary` returns success only when failures are zero at `tests/test-helpers.sh:58-77`. Temporary filesystem state is created by `setup_test_dir` with cleanup trap at `tests/test-helpers.sh:86-89`. The concurrency section maintains `PIDS` and `FAILURES` state at `tests/robustness/test-template-error-robustness.sh:274-292`.
- gates_or_invariants: The main invariant is that template rendering must degrade predictably instead of aborting the shell script under strict mode. Exact gates enforce fallback behavior for missing template paths at `tests/robustness/test-template-error-robustness.sh:36-50`, non-recursive value expansion at `tests/robustness/test-template-error-robustness.sh:168-174`, special filename rendering at `tests/robustness/test-template-error-robustness.sh:235-240`, concurrent exact output at `tests/robustness/test-template-error-robustness.sh:278-299`, and subdirectory loading at `tests/robustness/test-template-error-robustness.sh:313-318`. Portability gates are intentionally tolerant: empty templates may return empty or fallback, whitespace-only templates unconditionally pass if no crash occurs, permission behavior may vary by UID/filesystem, and symlink support may vary. The final invariant is delegated to `print_test_summary` and `exit $?` at `tests/robustness/test-template-error-robustness.sh:340-341`.
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`, sourced at `tests/robustness/test-template-error-robustness.sh:17`. The production loader’s key behavior is `load_template`, which cats existing files and warns on missing ones at `hooks/lib/template-loader.sh:36-46`; `render_template`, which builds `TMPL_VAR_` environment entries and performs single-pass AWK replacement at `hooks/lib/template-loader.sh:56-135`; and `load_and_render_safe`, which emits fallback content when loading or rendering produces empty output at `hooks/lib/template-loader.sh:188-211`. The single-pass invariant tested here comes from the loader comments and AWK loop at `hooks/lib/template-loader.sh:52-55` and `hooks/lib/template-loader.sh:87-128`. Test harness dependency is `tests/test-helpers.sh`, especially temp setup and result accounting. The suite caller is `tests/run-all-tests.sh:116`.
- edge_cases_or_failure_modes: Covered edge cases include nonexistent template file, nonexistent template directory, empty file, unclosed placeholder, nested placeholder braces, opening braces without closing braces, empty placeholder name, spaces/special characters/numeric-only variable names, variable values containing `{{...}}`, multiline values, 500-character variable names, UTF-8 BOM, whitespace-only content, spaces in filenames, permission-denied files, concurrent readers, subdirectory template lookup, and symlinked templates. Failure modes are mostly observable as non-empty mismatch or fallback mismatch rather than immediate abort. A notable portability design choice is that some checks record pass for either strict expected output or acceptable fallback/system variation, so the test is a robustness guard more than a precise parser conformance suite for every malformed case.
- validation_or_tests: This file is itself the validation asset. It validates by executing each scenario, recording pass/fail counters, then returning nonzero if any hard assertion failed. The production behavior it specifies maps to `load_and_render_safe` fallback semantics, `render_template` single-pass substitution, and safe handling of unusual input. It is also wired into the repository-wide runner, so normal full-suite execution includes this robustness file.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`