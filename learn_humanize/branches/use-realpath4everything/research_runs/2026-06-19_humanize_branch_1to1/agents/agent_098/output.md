# agent_098 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-098 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable shell specification for the prompt template loading/rendering algorithm implemented in `hooks/lib/template-loader.sh`. The file is not production runtime code, but it is core validation coverage for prompt rendering behavior used by RLCR hook blockers, prompt builders, and validators. It sources the loader at `tests/test-template-loader.sh:10-12`, computes the repository-local template root, then verifies path resolution, file loading, safe fallback behavior, template directory validation, literal metacharacter preservation, placeholder injection prevention, and variable-name edge cases.
- algorithmic_behavior: The test script runs as a strict Bash program with `set -euo pipefail` at `tests/test-template-loader.sh:8`. It initializes `SCRIPT_DIR`, `PROJECT_ROOT`, and sources `hooks/lib/template-loader.sh` at `tests/test-template-loader.sh:10-12`. It maintains two counters, `TESTS_PASSED` and `TESTS_FAILED`, and helper functions `pass()` / `fail()` mutate those counters and print diagnostic output at `tests/test-template-loader.sh:20-34`. Its behavioral coverage is sequential:
  - Path discovery: `get_template_dir "$PROJECT_ROOT/hooks/lib"` must resolve to `$PROJECT_ROOT/prompt-template` at `tests/test-template-loader.sh:41-52`. This corresponds to `hooks/lib/template-loader.sh:26-31`, where the loader walks two levels up from `hooks/lib` and appends `prompt-template`.
  - File loading: `load_template` must emit real template content for `block/git-push.md` and must emit no stdout for a missing file when stderr is redirected at `tests/test-template-loader.sh:54-78`. The implementation checks `[[ -f "$template_path" ]]`, cats existing content, and warns to stderr otherwise at `hooks/lib/template-loader.sh:36-46`.
  - Basic rendering: tests 4-7 cover single placeholders, multiple placeholders, multiline templates, and path-like values at `tests/test-template-loader.sh:80-144`. This targets the loader’s single-pass AWK scanner at `hooks/lib/template-loader.sh:56-136`.
  - Load-plus-render integration: test 8 loads `prompt-template/block/wrong-round-number.md`, supplies `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`, and asserts rendered output includes the expected title, generated filename, and current-round emphasis at `tests/test-template-loader.sh:146-164`. The fixture has placeholders in `prompt-template/block/wrong-round-number.md:1-7`.
  - Unused and missing variable behavior: unused assignments must be ignored, while unreplaced placeholders must remain literal at `tests/test-template-loader.sh:166-194`. This maps to `hooks/lib/template-loader.sh:114-121`, where missing entries fall back to the original `{{VAR}}`.
  - Safe fallback behavior: `load_and_render_safe` must render fallback text for missing templates and use real template content when present at `tests/test-template-loader.sh:196-222`. Implementation loads with stderr suppressed, uses `_emit_fallback` for empty content, and falls back again if rendering produces empty output at `hooks/lib/template-loader.sh:174-211`.
  - Template directory validation: a real `prompt-template` directory must pass and `/non/existing/path` must fail at `tests/test-template-loader.sh:224-244`. The loader requires the directory and subdirectories `block`, `codex`, `claude`, and `plan` at `hooks/lib/template-loader.sh:216-238`; the inspected tree contains those directories.
  - Shell metacharacter literal safety: tests 15-33 verify values containing `&`, backslashes, `$`, backticks, pipes, semicolons, globs, parentheses, redirection syntax, quotes, hash, tilde, combined operators, regex-like text, JSON-like text, and multiline content are rendered literally at `tests/test-template-loader.sh:246-486`. These specifically guard against shell evaluation and AWK replacement hazards such as the classic `gsub` ampersand expansion issue; the implementation avoids global substitution and instead appends `vars[var_name]` directly at `hooks/lib/template-loader.sh:115-117`.
  - Placeholder injection prevention: tests 34-36 verify that a replacement value containing `{{OTHER_VAR}}` is not rescanned or expanded, regardless of variable ordering, at `tests/test-template-loader.sh:488-543`. This validates the single-pass invariant documented in `hooks/lib/template-loader.sh:12-13` and implemented by the scanner that moves past only original template placeholders at `hooks/lib/template-loader.sh:123-128`.
  - Additional edge cases: tests 37-41 verify empty values, ASCII “Unicode” placeholder cases, actual non-ASCII bytes using Bash ANSI-C quoting, underscore-prefixed variable names, and numeric suffixes at `tests/test-template-loader.sh:545-639`.
  - Final state transition: after all checks, the script prints a summary and exits `0` only if `TESTS_FAILED` remains zero; otherwise it exits `1` at `tests/test-template-loader.sh:641-659`.
- inputs_outputs_state: Inputs are the current repository layout, the loader library `hooks/lib/template-loader.sh`, fixture templates under `prompt-template`, Bash function arguments expressed as `VAR=value` pairs, and expected strings embedded in the test file. The main fixture inputs inspected were `prompt-template/block/git-push.md`, which begins with `# Git Push Blocked` at `prompt-template/block/git-push.md:1`, and `prompt-template/block/wrong-round-number.md`, which uses `{{ACTION}}`, `{{CLAUDE_ROUND}}`, `{{FILE_TYPE}}`, `{{CURRENT_ROUND}}`, and `{{CORRECT_PATH}}` at `prompt-template/block/wrong-round-number.md:1-7`. Outputs are human-readable PASS/FAIL lines, a final summary, and process status. Internal state is limited to `TESTS_PASSED`, `TESTS_FAILED`, local `TEMPLATE`, `RESULT`, `EXPECTED`, and fixture variables such as `MULTILINE_VAL` / `REVIEW`. The loader under test converts `VAR=value` arguments into `TMPL_VAR_*` environment entries at `hooks/lib/template-loader.sh:60-67`; AWK builds a lookup table from `ENVIRON` at `hooks/lib/template-loader.sh:73-81`; each input line is scanned left-to-right and transformed into output at `hooks/lib/template-loader.sh:83-128`.
- gates_or_invariants: The script’s success gate is zero recorded failures at `tests/test-template-loader.sh:651-659`. The key behavioral invariants are: template directory resolution must be repository-relative; missing templates must not emit stdout when stderr is suppressed; fallback rendering must be available for missing or empty templates; existing templates take precedence over fallbacks; missing placeholders are preserved rather than deleted; unused variable assignments are no-ops; replacement values are literal data, not shell code; metacharacters are never interpreted; replacement values containing placeholder syntax are not recursively expanded; empty values are valid replacements; and variable names with `_` and digits are supported when passed as exact names. The loader comments claim variable names are “uppercase letters, numbers, underscores only” at `hooks/lib/template-loader.sh:9-11`, but the actual scanner does not enforce that syntax; it extracts whatever appears between `{{` and `}}` and looks for a matching environment variable at `hooks/lib/template-loader.sh:110-121`. This test file covers valid uppercase/underscore/digit names rather than invalid-name rejection.
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`, sourced at `tests/test-template-loader.sh:12`. Fixture dependency is `prompt-template`, especially `block/git-push.md` and `block/wrong-round-number.md`. The broader caller surface is large: `hooks/lib/loop-common.sh` sources `template-loader.sh`, initializes `TEMPLATE_DIR`, and validates it at `hooks/lib/loop-common.sh:240-248`; it also uses `load_and_render_safe` for common blocker messages such as todos access, prompt writes, state-file modification, finalize contract access, summary Bash blocking, tracked `.humanize` blocking, and direct hook execution blocking at `hooks/lib/loop-common.sh:840-900` and `hooks/lib/loop-common.sh:1442-1479`. Runtime hook examples include `hooks/loop-bash-validator.sh:210-218`, where `block/git-push.md` is rendered to block `git push`, and `hooks/loop-write-validator.sh:311-324`, where `block/wrong-round-number.md` is rendered for wrong round summary/contract writes. The test is included in the aggregate runner at `tests/run-all-tests.sh:59-62`. Related broader coverage exists in `tests/test-templates-comprehensive.sh`, `tests/test-error-scenarios.sh`, `tests/test-template-references.sh`, and robustness tests under `tests/robustness`, but this assigned file is the focused executable spec for the loader’s core happy-path and safety invariants.
- edge_cases_or_failure_modes: The file explicitly exercises missing files, invalid directories, unused variables, unresolved placeholders, shell metacharacters, ampersand-heavy replacements, Windows paths, regex-like backslashes, JSON braces/quotes, multiline replacement values, placeholder injection through values, empty replacement values, non-ASCII bytes, underscore-prefixed names, and numeric suffix names. Important failure modes covered by the implementation and partially by this test include AWK failure, which returns non-zero and prints `Error: Template rendering failed` at `hooks/lib/template-loader.sh:130-133`; missing templates, which warn to stderr from `hooks/lib/template-loader.sh:43-45`; empty template content, which causes `load_and_render_safe` to use fallback at `hooks/lib/template-loader.sh:197-200`; and invalid template roots or missing required subdirectories, which fail validation at `hooks/lib/template-loader.sh:219-235`. A notable environment-sensitive failure observed during this research: running `bash tests/test-template-loader.sh` in this read-only sandbox failed at test 4 because `render_template` uses a here-string into AWK at `hooks/lib/template-loader.sh:128`, and Bash could not create the temporary file needed for that here-string: `cannot create temp file for here document: Operation not permitted`. That appears to be sandbox I/O restriction, not a test assertion failure in the algorithm itself.
- validation_or_tests: Direct inspection completed for `tests/test-template-loader.sh` with line numbers, `hooks/lib/template-loader.sh`, the two template fixtures, and the `prompt-template` subdirectory layout. I ran `bash tests/test-template-loader.sh`; tests 1-3 passed, then the run exited non-zero on test 4 because the read-only sandbox blocked temporary file creation for the loader’s here-string. The observed partial output was: PASS for `get_template_dir`, PASS for existing `load_template`, PASS for missing `load_template`, then `hooks/lib/template-loader.sh: line 128: cannot create temp file for here document: Operation not permitted` followed by `Error: Template rendering failed (awk exit code: 1)`. I also confirmed the assigned file is executable (`-rwxr-xr-x`) and 21,298 bytes, matching the scheduler metadata. No files were modified.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1, represented by exactly one item evidence section above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`