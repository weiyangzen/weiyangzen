# agent_166 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-166 `file` `prompt-template/codex/commit-history-section.md`
- cursor: `[_]`
- core_role: This file is a Codex prompt-section template, not executable algorithm code. Its algorithmic role is to inject the loop’s historical context into Codex review prompts so review decisions can account for accumulated branch commits and the last few round artifacts. It is explicitly referenced by `hooks/loop-codex-stop-hook.sh` as the “Integral component: accumulated commit history and recent round references” at `hooks/loop-codex-stop-hook.sh:997`.
- algorithmic_behavior: The template renders a fixed markdown block titled `Development History (Integral Context)` at `prompt-template/codex/commit-history-section.md:1`. It places `{{COMMIT_HISTORY}}` inside a fenced code block at lines 3-6, then places `{{RECENT_ROUND_FILES}}` under `Recent Round Files` at lines 8-10. Its final instruction at line 12 changes review behavior by directing Codex to look for cross-round patterns, stalled progress, recurring issues, and drift from the main objective, with higher weight on recent rounds while still considering systemic trends.
- inputs_outputs_state: Inputs are two template variables: `COMMIT_HISTORY` and `RECENT_ROUND_FILES`. The stop hook computes `COMMIT_HISTORY` from Git history in oldest-first order using `git log --oneline --no-decorate --reverse "$BASE_COMMIT"..HEAD | tail -80` when `BASE_COMMIT` is a valid ancestor, otherwise it falls back to the latest 30 commits with an annotation; see `hooks/loop-codex-stop-hook.sh:999-1007`. `RECENT_ROUND_FILES` is generated from up to three prior rounds as repository-relative `@.humanize/rlcr/<loop>/round-N-summary.md` and `round-N-review-result.md` references; see `hooks/loop-codex-stop-hook.sh:1009-1015`. Output is a rendered markdown string assigned to `COMMIT_HISTORY_SECTION` at `hooks/loop-codex-stop-hook.sh:1024-1026`, then embedded into both full-alignment and regular Codex review prompts via `{{COMMIT_HISTORY_SECTION}}` in `prompt-template/codex/full-alignment-review.md:19` and `prompt-template/codex/regular-review.md:20`.
- gates_or_invariants: The file itself enforces no runtime gate, but it participates in review gates by feeding the historical context that supports mainline drift and stagnation checks. The enclosing review templates require a `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED` line, and the stop hook blocks if that verdict is missing at `hooks/loop-codex-stop-hook.sh:1421-1453`. Drift can stop the loop through `stop_for_mainline_drift` when stalled progress persists, with the circuit-breaker state set at `hooks/loop-codex-stop-hook.sh:1383-1418`. Rendering invariants come from `hooks/lib/template-loader.sh`: placeholders use `{{VARIABLE_NAME}}` syntax, missing variables remain literal, and substitution is single-pass to avoid recursive placeholder injection; see `hooks/lib/template-loader.sh:7-14` and `hooks/lib/template-loader.sh:48-56`.
- dependencies_and_callers: Direct caller is `load_and_render_safe "$TEMPLATE_DIR" "codex/commit-history-section.md"` in `hooks/loop-codex-stop-hook.sh:1024`. Rendering depends on `hooks/lib/template-loader.sh`, especially `load_and_render_safe` at `hooks/lib/template-loader.sh:188-211` and `render_template` at `hooks/lib/template-loader.sh:56-136`. Upstream data dependencies are `BASE_COMMIT`, `PROJECT_ROOT`, `CURRENT_ROUND`, and `LOOP_TIMESTAMP` from the stop-hook state. Downstream callers are the prompt builders for full-alignment review and regular review at `hooks/loop-codex-stop-hook.sh:1055-1081`, which pass the rendered section into `full-alignment-review.md` or `regular-review.md`.
- edge_cases_or_failure_modes: If `BASE_COMMIT` is missing, corrupted, or not an ancestor of `HEAD`, the stop hook avoids a failing range log and uses a recent-commit fallback annotated with “base commit unavailable”; see `hooks/loop-codex-stop-hook.sh:999-1006`. If no commits are available, `COMMIT_HISTORY` becomes `(no commits yet)` at `hooks/loop-codex-stop-hook.sh:1007`. On round 0, no prior files exist, so `RECENT_ROUND_FILES` becomes `(first round, no prior history)` at `hooks/loop-codex-stop-hook.sh:1015`. If the template file is missing or renders empty, `load_and_render_safe` falls back to `COMMIT_HISTORY_SECTION_FALLBACK` defined at `hooks/loop-codex-stop-hook.sh:1017-1023`. A subtle prompt-quality failure remains possible if the injected commit history contains fenced-code delimiters or confusing text, but the source is `git log --oneline`, not arbitrary multiline file content. Template variable values are not recursively expanded because the loader is single-pass.
- validation_or_tests: Dedicated coverage exists in `tests/test-commit-history-section.sh`. It validates round 0 placeholders at lines 33-61, round 3 commit and prior-round references at lines 63-114, corrupted `BASE_COMMIT` fallback at lines 116-147, unrelated valid commit fallback at lines 149-179, missing-template fallback behavior at lines 180-222, round 1 boundary behavior at lines 224-244, and empty `BASE_COMMIT` fallback at lines 246-266. The test sources `hooks/lib/template-loader.sh` at `tests/test-commit-history-section.sh:17` and renders this exact template at lines 53-55 and 88-90. I did not run the test because this branch export is read-only and the test creates temporary Git repositories/files.
- skip_candidate: `yes: this file is a small prompt fragment rather than a core implementation algorithm. It is still relevant to the branch’s review-control behavior because it shapes Codex’s historical-context input and feeds drift/stagnation review gates.`

## Worker Self-Test
- assigned_items_seen: `1 item section`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`