# agent_100 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-100 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: This is an executable Bash specification for the repository’s prompt template subsystem. It validates that `prompt-template/**` is structurally present, that every Markdown template can be loaded, that placeholder syntax follows the expected `{{UPPER_CASE_NAME}}` contract, and that `hooks/lib/template-loader.sh` renders and falls back correctly. It is included in the aggregate test runner at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/run-all-tests.sh:60), where `test-templates-comprehensive.sh` is one of the standard suites.

- algorithmic_behavior: The script derives `SCRIPT_DIR` and `PROJECT_ROOT`, then sources [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:17) through [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-templates-comprehensive.sh:15). It resolves `TEMPLATE_DIR` through `get_template_dir "$PROJECT_ROOT/hooks/lib"` at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-templates-comprehensive.sh:59). The execution flow is sectioned: directory structure checks at lines 64-80; all-template loading at lines 85-111; syntax validation over every `*.md` at lines 116-194; malformed-placeholder detector self-tests at lines 199-259; rendering edge cases at lines 264-438; fallback and directory validation at lines 443-490; integration rendering against real templates at lines 495-540; stress/load-and-render sweep at lines 545-606; and final exit-code summary at lines 611-625.

- inputs_outputs_state: Inputs are repository-local files only: `hooks/lib/template-loader.sh`, the resolved `prompt-template` directory, and all Markdown templates returned by `find "$TEMPLATE_DIR" -name "*.md" -type f -print0 | sort -z` at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-templates-comprehensive.sh:107). The test also uses hard-coded sample template strings and `VAR=value` substitutions for edge cases. Outputs are colored stdout diagnostics, summary counts, warnings, and process exit status. State is held in `TESTS_PASSED`, `TESTS_FAILED`, and `WARNINGS` counters defined at lines 26-28 and mutated only by `pass`, `fail`, and `warn` helpers at lines 31-50. The final gate exits `0` only when `TESTS_FAILED == 0`; warnings do not fail the run at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-templates-comprehensive.sh:619).

- gates_or_invariants: The first hard gate is that the template directory must exist; otherwise the script exits immediately at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-templates-comprehensive.sh:66). It then requires `block`, `codex`, and `claude` subdirectories in Section 1, while the delegated loader’s `validate_template_dir` requires `block`, `codex`, `claude`, and `plan` in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:224). Template load success means `load_template` returns non-empty content and, for Section 2, not whitespace-only; whitespace-only templates produce warnings instead of failures at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-templates-comprehensive.sh:96). Placeholder syntax is constrained to uppercase identifiers with optional digits/underscores after the first character. The script flags extra closing braces, extra opening braces, wrong bracket types, unclosed placeholder candidates, spaces inside placeholders, and lowercase placeholders at lines 128-175. Rendering invariants include preserved empty content, multiline output, repeated variables, adjacent variables, variables at line boundaries, long values, and no crash on backslashes, quotes, Markdown, and multiline values.

- dependencies_and_callers: Its direct dependency is `hooks/lib/template-loader.sh`, especially `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`. The loader itself computes the template root by walking two directories up from `hooks/lib` and appending `prompt-template` at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:26). `render_template` is implemented as a single-pass `awk` scan over `{{...}}` placeholders using `TMPL_VAR_` environment variables at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:56). Real template integration probes use `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-templates-comprehensive.sh:499). The broader repository depends on this template loader heavily from hooks and scripts, including `loop-common.sh`, `loop-codex-stop-hook.sh`, validators, and `scripts/bitlesson-validate-delta.sh`, so this test protects prompt/block-message behavior used by many hook gates.

- edge_cases_or_failure_modes: Missing `prompt-template` exits early and prevents later checks. Missing required subdirectories increment failures. Missing individual templates, empty load results, malformed placeholders, failed render output, or failed real-template substring checks increment `TESTS_FAILED`. Whitespace-only templates and unreplaced placeholders in the final all-template sweep are warnings, not hard failures, at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-templates-comprehensive.sh:596). The script intentionally skips single-brace `{VAR}` validation because templates may document such syntax, which reduces false positives but leaves single-brace misuse outside this suite’s hard gates at lines 160-163. The unclosed-placeholder detector is heuristic: it greps candidate fragments, extracts a variable stem with `sed`, and suppresses findings if a valid `{{VAR}}` also appears, so it can miss or soften ambiguous cases. Temporary directories are created with `mktemp -d` and removed with `rm -rf` at lines 205-259 and 484-490; in a restricted read-only sandbox, executing the suite may fail if temp creation or deletion is blocked.

- validation_or_tests: This file is itself a validation command intended to be run as `tests/test-templates-comprehensive.sh` or through `tests/run-all-tests.sh`. Its validation coverage includes corpus enumeration and load checks; regex-level malformed placeholder detection; direct renderer calls for empty, single-line, multiline, empty-line, special-character, quote, CJK-safe ASCII, Markdown, long, multiline, repeated, and adjacent substitutions; safe fallback behavior for missing templates; valid/invalid/incomplete template directory checks; real-template integration; and a 100-call rapid render loop. I did not execute the script because the task explicitly required research notes only and the environment is read-only; the script performs temporary filesystem writes via `mktemp`, `mkdir`, and `rm -rf`.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 Item Evidence section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`