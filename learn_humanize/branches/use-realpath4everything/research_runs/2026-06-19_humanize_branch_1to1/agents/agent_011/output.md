# agent_011 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-011 `directory` `tests`
- cursor: `[_]`
- core_role: The `tests` directory is the repository’s shell-based validation harness for RLCR loop setup, hook validation, stop/cancel transitions, prompt/template routing, monitor behavior, model/provider selection, config merging, GitHub-review parsing, and robustness/security edge cases. It is not production runtime code, but it is core algorithm evidence for this branch because it encodes the expected path-resolution, realpath, symlink, session, and state-machine behavior across the production scripts under `scripts/` and `hooks/`.

- algorithmic_behavior: The directory is coordinated by [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/run-all-tests.sh:59), which enumerates the suite list, throttles parallel jobs via `HUMANIZE_TEST_JOBS`, handles zsh-only tests separately, captures per-suite output/exit/time files, strips ANSI, aggregates pass/fail counts, and prints failing suite detail before returning nonzero. Shared primitives live in [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:30): `pass`, `fail`, `skip`, `print_test_summary`, `setup_test_dir`, and `init_test_git_repo`.

  The branch-relevant path algorithm is validated in two layers. [tests/test-plan-file-validation.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-plan-file-validation.sh:88) drives `scripts/setup-rlcr-loop.sh` against temp git repositories and checks absolute-path rejection, missing parent errors, spaces, shell metacharacters, symlink file rejection, permission/path resolution failure, `../` escape rejection, non-git/no-commit rejection, plan tracking mode, YAML-unsafe branch/model values, and content sufficiency. [tests/robustness/test-path-validation-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-path-validation-robustness.sh:76) expands that into a table-like validator helper over valid nested/dotted paths, invalid metacharacters, file symlink rejection, parent-directory symlink rejection, symlink-chain rejection, long/deep paths, and content/file-type failures. These tests align with production gates in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/setup-rlcr-loop.sh:455), especially relative-only plan paths, no spaces/metacharacters, no symlink leaf, no symlink parent components, within-project resolution, git tracking rules, and plan content checks.

  The other key realpath behavior is cancel authorization. [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-cancel-signal-file.sh:42) constructs loop state, sends JSON to `loop-bash-validator.sh`, and asserts that only an authorized `mv state.md cancel-state.md` or equivalent active-state move passes when `.cancel-requested` exists. Its late helper tests are directly branch-specific: a command path through a symlinked prefix must authorize after realpath normalization, while source or destination symlink aliases at the leaf must reject to avoid moving through an alias outside the loop directory ([tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-cancel-signal-file.sh:1345), [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-cancel-signal-file.sh:1376), [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-cancel-signal-file.sh:1400)). [tests/robustness/test-cancel-security-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-cancel-security-robustness.sh:39) adds injection, quote, operator, wrong-source/destination, `/./` normalization, symlink source, and “path contains finalize” exact-match regressions. These map to [hooks/lib/project-root.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/project-root.sh:41), [hooks/lib/project-root.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/project-root.sh:55), and [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/loop-common.sh:1051), where project roots are canonicalized, parent-prefix canonicalization preserves leaf basenames, and `is_cancel_authorized` compares canonicalized paths while rejecting symlinked active state files.

  The rest of the directory validates sibling algorithm surfaces: state-file resolution and terminal state naming in `test-state-exit-naming.sh`; session ID propagation, post-bash hook behavior, cancellation, and command signatures in `test-session-id.sh`; background-task stop-hook short-circuit, transcript parsing, tilde expansion, cross-session markers, and marker cleanup in [tests/test-stop-hook-bg-allow.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-stop-hook-bg-allow.sh:3); stop-gate wrapper/project-root behavior in `test-stop-gate.sh`; finalize phase and code-review transition behavior in `test-finalize-phase.sh`; hook input and hook-system robustness in `tests/robustness`; template rendering/reference/comprehensive checks in `test-template-loader.sh`, `test-template-references.sh`, and `test-templates-comprehensive.sh`; prompt generation/refinement/tag routing in `test-gen-plan.sh`, `test-refine-plan.sh`, and `test-task-tag-routing.sh`; model/provider routing in `test-model-router.sh` and `test-bitlesson-select-routing.sh`; monitor runtime/e2e behavior in `test-monitor-runtime.sh`, `test-monitor-e2e-real.sh`, split deletion/SIGINT wrappers, and `test-zsh-monitor-safety.sh`; config merge/error behavior; GitHub review parsing with JSON fixtures; and a `tests/mocks/gh` executable that simulates `gh` API/repo/pr/reaction behavior for review tests.

- inputs_outputs_state: Inputs are shell invocations, environment variables such as `CLAUDE_PROJECT_DIR`, `HUMANIZE_TEST_JOBS`, `HOME`, `XDG_CONFIG_HOME`, fake `PATH` entries, mocked `codex` and `gh` binaries, temp git repositories, markdown plan files, hook JSON payloads, JSONL transcripts, config JSON, and fixture API JSON under `tests/fixtures`. Outputs are per-suite PASS/FAIL/SKIP counters, process exit codes, temporary `.humanize/rlcr/...` loop directories, `state.md` and terminal `*-state.md` files, `.cancel-requested` and `bg-pending.marker`, generated prompt/goal/review artifacts, captured mock invocation markers, and runner summary/failure detail. State transitions under test include setup creating active loop state, stop hooks moving from implementation to review/finalize or blocking exit, cancel moving active state to `cancel-state.md`, background markers parking/resuming loops, monitor cleanup on deletion/SIGINT, and validator decisions allowing or blocking read/write/edit/bash actions.

- gates_or_invariants: The test directory encodes these invariants:
  - Project root resolution must prefer `CLAUDE_PROJECT_DIR`, then git toplevel, never drifting `pwd`, and must realpath-canonicalize root paths ([hooks/lib/project-root.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/project-root.sh:5)).
  - Plan files must be repo-relative, inside the project, readable regular files, not symlinks, not under symlinked parent directories, not submodule paths, not unsafe names, and must satisfy git tracking/content rules.
  - Cancel authorization requires `.cancel-requested`, a plain `mv`, exactly two arguments, valid active-state source name, `cancel-state.md` destination, no shell injection/operator/hidden variable, canonicalized comparable prefixes, and no symlinked source or leaf alias bypass.
  - Hook validators must preserve `.humanize/rlcr` state integrity, isolate sessions, parse JSON robustly, and fail closed on malformed or dangerous inputs.
  - Background-task stop handling must not run normal Codex review while current-session background work is pending, must preserve foreign parked loops, and must treat marker adoption as opt-in for the stop hook only.
  - Runner-level isolation assumes each suite owns its temp directory and can run in parallel without shared state.

- dependencies_and_callers: The suite is normally called by humans or CI through `tests/run-all-tests.sh`. Individual test files call production siblings under `scripts/`, `hooks/`, `hooks/lib/`, `prompt-template/`, `commands/`, `agents/`, `config/`, and `skills/`. Common external dependencies are `bash`, `zsh` for one suite, `git`, `jq`, `sed`, `awk`, `grep`, `mktemp`, `realpath` or `python3` fallback through production code, timeout wrappers, and mocked `codex`/`gh` binaries. Fixtures under `tests/fixtures/*.json` feed GitHub/review/reaction parsers; `tests/mocks/gh` provides deterministic CLI/API behavior.

- edge_cases_or_failure_modes: Covered cases include absolute paths, `../` escape, missing dirs/files, unreadable dirs, spaces, shell metacharacters, tilde/backslash/globs, symlinked plan files, parent symlink traversal, symlink chains, long/deep paths, empty/comment-only/too-short plans, non-git/no-commit repos, dirty/tracked/untracked plan-mode mismatches, YAML-unsafe branch/model values, git timeouts, invalid JSON/null bytes/deep JSON, stale or terminal loop directories, cross-session loops, missing/unreadable/malformed transcripts, tilde transcript paths, mixed legacy/current background-completion records, command substitution, pipes, redirects, prefix wrappers like `sudo`/`env`/`sh -c`, mixed quotes, hidden `${IFS}`, symlinked cancel source/destination aliases, path casing after canonicalization, and substring bugs when loop paths contain words such as `finalize`.

- validation_or_tests: I inspected the directory recursively via inventory (`68` files: shell suites, JSON fixtures, and mock executable), line-count/type scans, broad `rg` searches, and targeted line-numbered reads of the runner, helpers, plan path tests, path robustness tests, cancel tests, cancel security tests, and relevant production helpers. I did not run the suite: this branch export is read-only, and the tests intentionally create temp repos/files and run git/mock binaries. A `git status` probe also failed because this export has no usable `.git` and the sandbox blocked git’s temporary cache writes, so the scheduler-provided source commit is the authoritative commit reference.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-011`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`