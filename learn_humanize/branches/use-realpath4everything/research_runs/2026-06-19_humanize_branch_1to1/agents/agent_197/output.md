# agent_197 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-197 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Black-box robustness specification for the repository’s portable timeout abstraction. The file does not implement timeout behavior itself; it validates the contract exposed by [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/portable-timeout.sh:9), especially `detect_timeout_impl`, `TIMEOUT_IMPL`, and `run_with_timeout`. It is core-adjacent because many setup, ask, git, hook, and robustness paths rely on timeout-bounded subprocess execution.
- algorithmic_behavior: The script bootstraps from its own location via `SCRIPT_DIR` and `PROJECT_ROOT`, then sources `scripts/portable-timeout.sh` and `tests/test-helpers.sh` at [tests/robustness/test-timeout-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-timeout-robustness.sh:14). It runs 18 sequential checks. Tests 1-2 validate implementation discovery and allowed values: `gtimeout`, GNU `timeout`, `python3`, `python`, or `none` at lines 31-49. Tests 3, 5, 6, 7, 10, 11, 14, and 17 verify normal command execution, argument preservation, exit-code preservation, shell pipeline use through `sh -c`, large stdout streaming, repeated quick cycles, literal special-character arguments, and subshell execution at lines 51-110, 157-186, 211-219, and 249-257. Tests 4, 8, 9, 12, 13, and 15 exercise timeout exits, short elapsed-time enforcement, zero timeout variance, missing commands, empty commands, and signal-handling variance at lines 62-77, 120-155, and 188-233. Tests 16 and 18 re-check fallback detection and exported state at lines 235-247 and 259-272.
- inputs_outputs_state: Inputs are the host PATH/tooling, `TIMEOUT_IMPL` from the sourced wrapper, timeout seconds passed to `run_with_timeout`, and commands such as `echo`, `sleep`, `printf`, `sh -c`, `wc`, and a deliberately nonexistent command. Observable outputs are stdout captures like `hello`, `arg1 arg2`, `tEst`, `subshell`, line counts from a 1000-line generator, and process exit codes. State is limited to shell locals and test counters from [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:22): `TESTS_PASSED`, `TESTS_FAILED`, and `TESTS_SKIPPED`. The script transitions by accumulating pass/fail counts and exits with `print_test_summary`’s return code at [tests/robustness/test-timeout-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-timeout-robustness.sh:278), so any failed assertion makes the script exit non-zero.
- gates_or_invariants: `TIMEOUT_IMPL` must be non-empty and one of the accepted implementation names. A quick command must preserve stdout and return zero. Timed-out sleeps must return `124` when an implementation other than `none` exists. A short timeout must finish in less than 5 seconds and return `124`. Command arguments must remain array-safe, including spaces, shell metacharacters, and quotes. Non-timeout command exit status must propagate, demonstrated by `sh -c 'exit 42'`. Zero timeout is explicitly permissive: exit `0` or `124` both pass. Signal handling is also permissive: exit `0` or `124` both pass. `detect_timeout_impl` must be stable enough to return a valid enum when called after initial sourcing. `TIMEOUT_IMPL` must exist and is expected to be exported, though a mismatch in a plain `sh` subshell is downgraded to pass with a weaker message at lines 262-269.
- dependencies_and_callers: Direct dependencies are Bash with `set -euo pipefail`, the timeout wrapper [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/portable-timeout.sh:31), and helper functions `pass`, `fail`, and `print_test_summary` from [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:30). The wrapper’s discovery order is `gtimeout`, GNU `timeout`, `python3`, `python`, then `none` at [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/portable-timeout.sh:9). Runtime consumers found by reference include `scripts/setup-rlcr-loop.sh`, `scripts/ask-codex.sh`, `scripts/ask-gemini.sh`, `scripts/bitlesson-select.sh`, and other robustness tests, so this file guards a shared process-control primitive rather than an isolated test helper.
- edge_cases_or_failure_modes: The script deliberately covers no timeout binary being available, long-running commands, zero-second timeout ambiguity, commands that ignore or trap termination, high-volume stdout, nonexistent executables, empty command strings, shell-special arguments, pipelines, and subshells. Potential weakness: the initial `RESULT=$(run_with_timeout 5 echo "hello")` is executed under `set -e`, so if `run_with_timeout` unexpectedly fails there, the script can terminate before recording a helper-level failure. Another edge is the large-output assertion uses string comparison syntax with `[[ "$RESULT" -ge "1000" ]]`, which works for integer strings but depends on `wc -l` producing a clean numeric token after command substitution. Under `TIMEOUT_IMPL=none`, actual timeout enforcement tests are skipped as passes rather than skipped counters, so this file treats absence of timeout support as acceptable for portability.
- validation_or_tests: This file is itself validation. It validates `portable-timeout.sh` against success-path behavior, failure-path behavior, enum stability, exit-code contracts, elapsed-time bounds, argument preservation, and exported environment state. I inspected the full assigned file directly and also inspected the sourced wrapper and helper files. I did not execute the robustness test because the branch export is read-only and the environment showed restricted temp/cache behavior during a git metadata probe; no file modifications were made.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 heading present for the sole assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`