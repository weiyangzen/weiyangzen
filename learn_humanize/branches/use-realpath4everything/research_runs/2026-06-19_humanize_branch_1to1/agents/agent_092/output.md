# agent_092 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-092 `file` `tests/test-skill-monitor.sh`
- cursor: `[_]`
- core_role: Executable shell specification for the non-interactive skill invocation monitor, `_humanize_monitor_skill --once`. It validates how the monitor reads `.humanize/skill/<timestamp-id>/` records, computes aggregate status counts, chooses the focused invocation, extracts display fields, prints watched output, lists recent invocations, and rejects invalid/missing input surfaces. The test is registered in the aggregate runner at `tests/run-all-tests.sh:99-100`.
- algorithmic_behavior: The script creates a fresh temporary repo for each scenario with `setup_test_env`, initializes git, commits a dummy file, and sources `scripts/humanize.sh`, which in turn sources `scripts/lib/monitor-common.sh` and `scripts/lib/monitor-skill.sh` (`tests/test-skill-monitor.sh:35-54`, `scripts/humanize.sh:6-15`, `scripts/humanize.sh:1243-1245`). Test fixture creation is centralized in `create_skill_invocation`, which writes `input.md`, optional `metadata.md`, and success-only `output.md` under `.humanize/skill/<unique_id>` (`tests/test-skill-monitor.sh:56-104`). Each scenario invokes `_humanize_monitor_skill --once`, captures stdout/stderr plus exit code, then validates exact behavioral signals with `grep`.
- inputs_outputs_state: Inputs are timestamp-like invocation directory names, markdown input files with `## Question` and `## Configuration`, YAML frontmatter metadata keys `model`, `effort`, `timeout`, `exit_code`, `duration`, `status`, and optionally `output.md` content (`tests/test-skill-monitor.sh:69-98`). Output is a textual one-shot monitor report containing total counts, per-status counts, focused invocation details, watched output, and a recent invocation table (`scripts/lib/monitor-skill.sh:339-425`). State transitions are simulated by file presence: missing `.humanize/skill` is an error, empty `.humanize/skill` is an error, no `metadata.md` means `running`, completed statuses come from `metadata.md`, and non-empty `output.md` becomes watchable content (`scripts/lib/monitor-skill.sh:43-48`, `scripts/lib/monitor-skill.sh:121-143`, `scripts/lib/monitor-skill.sh:169-217`).
- gates_or_invariants: The monitor must reject a missing skill directory with nonzero exit and “directory not found” (`tests/test-skill-monitor.sh:110-118`). It must reject an existing but invocation-empty skill directory with nonzero exit and “No skill invocations found” (`tests/test-skill-monitor.sh:124-133`). Valid skill directories must start with `YYYY-MM-DD_HH-MM-SS`; non-matching child directories are ignored (`tests/test-skill-monitor.sh:371-389`, `scripts/lib/monitor-skill.sh:77-90`). The focused invocation is newest with watchable non-empty content, falling back to newest when no content exists (`scripts/lib/monitor-skill.sh:97-117`, `scripts/lib/monitor-skill.sh:349-355`). Question extraction is intentionally first non-empty line under `## Question`, excluding later context (`tests/test-skill-monitor.sh:295-346`, `scripts/lib/monitor-skill.sh:145-152`).
- dependencies_and_callers: The test depends on `git`, `mktemp`, `grep`, `sed`, `cut`, `tr`, `mkdir`, `cat`, and Bash arithmetic/capture semantics. Runtime dependencies are `scripts/humanize.sh` for loader glue and `humanize_split_to_array` (`scripts/humanize.sh:22-34`), `scripts/lib/monitor-common.sh` for `monitor_get_yaml_value` and timestamp formatting (`scripts/lib/monitor-common.sh:198-213`, `scripts/lib/monitor-common.sh:219-232`), and `scripts/lib/monitor-skill.sh` for the monitor implementation. Public callers route through `humanize monitor skill`, `humanize monitor codex`, and `humanize monitor gemini` (`scripts/humanize.sh:1190-1211`), with README usage documented at `README.md:64-70`. Producer scripts create the real `.humanize/skill` schema: `scripts/ask-codex.sh:205-238` writes Codex input/tool fields and `scripts/ask-codex.sh:319-405` writes status metadata; `scripts/ask-gemini.sh:187-218` and `scripts/ask-gemini.sh:302-379` do the same for Gemini.
- edge_cases_or_failure_modes: The test covers absent state root, empty root, mixed success/error/timeout counters, a running invocation without metadata, empty-response metadata without output, multi-line questions, and filtering out invalid directory names (`tests/test-skill-monitor.sh:106-389`). It does not exercise `--tool-filter`, cache lookup precedence, global cache path display, local cache fallback, terminal drawing, live tailing, signal cleanup, or path behavior when invoked outside a git repo. Because `setup_test_env` sources the monitor inside a synthetic git repo, it does not validate repository-root discovery for real project layouts beyond current-directory `.humanize/skill`.
- validation_or_tests: The file itself is the validation asset. It prints pass/fail counters and exits `1` if any assertion failed (`tests/test-skill-monitor.sh:391-404`). It verifies one-shot success for a single invocation, total/status counters, status/model/duration/question/output rendering, newest-focused selection, recent invocation listing, first-line question extraction, no-output fallback, and timestamp-directory filtering (`tests/test-skill-monitor.sh:139-389`). I did not execute the test because this assignment requested research notes only and the branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 item evidence sections present; unique assigned item heading count verified manually`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`