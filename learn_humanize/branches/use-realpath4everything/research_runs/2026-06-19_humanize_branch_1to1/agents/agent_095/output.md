# agent_095 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-095 `file` `tests/test-stop-hook-bg-allow.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR Stop-hook background-task algorithm. The file defines the required behavior when Claude Code has launched asynchronous Agent or Bash work that has not completed: the stop hook must allow the stop with a user-facing `systemMessage`, avoid Codex review and later gates, and preserve loop state until a subsequent stop can prove the background work is finished. This role is stated in the file header at [tests/test-stop-hook-bg-allow.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-stop-hook-bg-allow.sh:3), with acceptance criteria AC-1 through AC-20 enumerated at lines 14-33 and additional AC-21 through AC-24 test blocks later in the file.
- algorithmic_behavior: The test constructs isolated git fixtures, synthetic Claude transcript JSONL, mock `codex`, and mock `lsof`, then drives `hooks/loop-codex-stop-hook.sh` and `scripts/rlcr-stop-gate.sh`. Normal no-background cases must reach Codex, proven by a marker file from the mock Codex binary at lines 82-94 and asserted by `assert_reached_codex` at lines 331-339. Pending background cases must return exit 0, emit JSON containing `.systemMessage`, skip Codex, and keep the state file hash unchanged via `assert_systemmessage_only` at lines 291-329.
- algorithmic_behavior: Transcript generation covers both recognized launch families: background subagents via `toolUseResult.isAsync` plus `agentId` at lines 208-222, and background shell tasks via `toolUseResult.backgroundTaskId` at lines 224-238. Completion recognition covers legacy queue-operation XML task notifications at lines 240-247 and current SDK/system `task_notification` records at lines 249-253. The pending-set algorithm being specified is effectively `pending := launched - completed`, with optional liveness pruning and loop-start timestamp filtering.
- algorithmic_behavior: AC-1 through AC-8 verify the core stop-hook flow: no background dispatch reaches Codex at lines 354-363; a pending subagent and pending shell short-circuit at lines 365-395; a launched subagent with completion notification reaches Codex at lines 397-409; multiple pending tasks count correctly in the message at lines 411-432; missing, empty, or absent `transcript_path` fails closed into normal Codex flow at lines 434-454; no active loop exits silently at lines 456-474; finalize phase also short-circuits when background work is pending at lines 476-489.
- algorithmic_behavior: AC-9, AC-10, AC-10b, and AC-10c verify the same behavior through the wrapper and path normalization. `rlcr-stop-gate.sh` must forward `--transcript-path` and print `ALLOW:` for pending background work at lines 491-518. A literal `~/...` transcript path must expand under `HOME`, both when routed through the hook and when calling `list_pending_background_task_ids` directly at lines 520-563. The wrapper must also preserve that behavior for tilde-form paths at lines 565-604.
- algorithmic_behavior: AC-11 through AC-14 define parked-loop/session-isolation transitions around `bg-pending.marker`. A foreign session hitting a marker-bearing loop must receive a parked-loop message while preserving marker and state bytes at lines 606-668. A foreign session without a marker must not adopt the loop at lines 670-716. The normal short-circuit must write `bg-pending.marker` at lines 718-740. `find_active_loop` must prefer an exact same-session loop over a newer foreign marker at lines 742-790. Same-session resume with no pending work removes a stale marker while preserving `session_id` at lines 793-843. Cross-session stops must preserve both marker and stored `session_id` at lines 845-899.
- algorithmic_behavior: AC-15 and AC-16 specify completion parsing compatibility. A current `type:"system", subtype:"task_notification"` record must remove a matching launch from the pending list at lines 901-924. Mixed legacy XML queue-operation completions and SDK task notifications must union together so both launched tasks resolve to an empty pending set at lines 926-953.
- algorithmic_behavior: AC-17 through AC-20 enforce fail-closed cleanup safety. If the transcript is missing, absent from hook input, points to a nonexistent file, or is malformed JSON, the hook must not treat that as authoritative “no pending” evidence. Existing `bg-pending.marker` and stored `session_id` must be preserved at lines 955-1050 and 1155-1203. AC-18 additionally proves marker-based adoption is opt-in: default `find_active_loop` ignores a foreign marker, while the stop hook’s explicit opt-in can return it at lines 1052-1094. AC-19 requires an empty-session caller plus existing marker to exit 0 silently with no state mutation at lines 1096-1153.
- algorithmic_behavior: AC-21 through AC-24 cover newer edge conditions. Transcript scans must ignore background launches before the RLCR loop start timestamp and include launches after it at lines 1205-1240. `derive_loop_start_iso_ts` must convert loop-dir local wall-clock names to UTC correctly for UTC, Asia/Tokyo, and America/Los_Angeles at lines 1242-1287. End-to-end hook behavior must ignore pre-loop launches and proceed to Codex at lines 1289-1310. Wrapper calls without `--session-id` must short-circuit only when there is no prior marker, and must silently allow when a marker already exists at lines 1312-1408. `lsof` liveness probes must retain alive pending tasks and prune dead/orphaned tasks at lines 1410-1459.
- inputs_outputs_state: Inputs are temporary project roots, `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, `goal-tracker.md`, plan and summary files, hook input JSON containing `transcript_path` and optionally `session_id`, transcript JSONL records, environment variables such as `CLAUDE_PROJECT_DIR`, `HOME`, `MOCK_CODEX_MARKER`, `MOCK_CODEX_OUTPUT`, and `LSOF_BIN`, plus wrapper flags `--project-root`, `--transcript-path`, and optional `--session-id`. Fixture creation is centralized in `create_full_fixture` at lines 101-178 and `run_stop_hook_with_input` at lines 271-289.
- inputs_outputs_state: Outputs asserted by this spec include hook exit code 0, empty output for silent allow paths, JSON with `.systemMessage` for pending or parked background work, wrapper text beginning `ALLOW:`, presence or absence of `codex-called.marker`, creation/removal/preservation of `bg-pending.marker`, and byte-level preservation of `state.md` via SHA-256 hashes. The final test outcome is aggregated through `print_test_summary` at line 1461, supplied by [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:58).
- inputs_outputs_state: The state machine specified is: no active loop -> allow silently; active loop with no pending background tasks -> continue to normal stop-hook gates/Codex and remove a same-session stale marker only when the transcript parses successfully; active loop with pending same-session background work -> write marker, emit system message, exit 0 before gates; foreign session with marker -> emit parked message and preserve artifacts; no-session caller with existing marker -> silent allow and preserve artifacts; unreadable or malformed transcript -> fail closed, never remove marker.
- gates_or_invariants: The background short-circuit must run before normal stop-hook gates, Codex review, git cleanliness checks, summary checks, phase handling, and max-iteration logic. This invariant is called out in the implementation dependency [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:71), where `handle_bg_task_short_circuit` is invoked before state parsing at lines 83-91.
- gates_or_invariants: State mutation is intentionally narrow. Pending background work may create `bg-pending.marker`; same-session verified completion may remove it; cross-session, empty-session, unreadable-transcript, and malformed-transcript paths must not rewrite `state.md`, must not erase a marker, and must not invoke Codex. The file repeatedly enforces this by hash comparisons and marker checks in AC-11, AC-14, AC-17, AC-19, AC-20, and AC-22b.
- gates_or_invariants: Session isolation is a hard invariant. `find_active_loop` should return exact `session_id` matches before marker fallback, and marker fallback should be available only to the stop hook, not default validators. The test calls `find_active_loop` directly for these contracts at lines 774-778 and 1072-1088; the implementation dependency documents the third `allow_bg_marker_fallback` argument in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/loop-common.sh:325).
- gates_or_invariants: Transcript path handling must expand bare `~` and `~/...` without relying on shell expansion. The direct helper and wrapper regression tests at lines 520-604 correspond to the helper dependency [hooks/lib/loop-bg-tasks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/loop-bg-tasks.sh:23), which defines `expand_leading_tilde` and `extract_transcript_path`.
- dependencies_and_callers: Direct dependencies are `tests/test-helpers.sh` for temporary directories, git fixtures, pass/fail counters, and summaries; `jq` for JSON fixture generation and assertions; `git` for minimal repos and branch/base commits; `sha256sum`, `grep`, `sed`, `awk`, `sort`, `wc`, `id`, and `date`; `hooks/loop-codex-stop-hook.sh` as the primary executable under test; `scripts/rlcr-stop-gate.sh` as the wrapper under test; and `hooks/lib/loop-common.sh`/`hooks/lib/loop-bg-tasks.sh` for direct helper calls.
- dependencies_and_callers: The production helper dependency implements the behaviors this test pins: launch/completion extraction, timestamp filtering, tilde expansion, liveness pruning, marker guards, and stale-marker cleanup in [hooks/lib/loop-bg-tasks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/loop-bg-tasks.sh:193) and [hooks/lib/loop-bg-tasks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/loop-bg-tasks.sh:311). The wrapper dependency builds real Stop-hook JSON and maps hook JSON without a `decision` field to `ALLOW:` output in [scripts/rlcr-stop-gate.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/rlcr-stop-gate.sh:108) and lines 162-173.
- edge_cases_or_failure_modes: The suite explicitly guards against false negatives from missing tilde expansion, false positives from pre-loop session-wide background tasks, stale marker deletion when transcript evidence is unavailable, loop hijacking by a different session, ambiguous wrapper calls lacking `session_id`, completion-format drift between legacy and SDK transcript records, and dead background task IDs that would otherwise pin the loop forever.
- edge_cases_or_failure_modes: There are portability assumptions: `jq` is required; `sha256sum` is used rather than `shasum`; temporary mock `lsof` binaries are created inside `$TEST_DIR/bin`; and AC-23/AC-24 create task output fixtures under `/tmp/claude-<uid>/...` based on the transcript directory slug. The file mitigates home-directory side effects by using `FAKE_HOME` under the temp test dir at lines 50-57.
- validation_or_tests: This file is itself the validation asset. It is executable Bash with `set -euo pipefail`, constructs independent AC-specific fixtures, records pass/fail counts through `tests/test-helpers.sh`, and exits with the summary status at lines 1461-1462. I inspected it directly and did not execute it, consistent with the read-only branch research instruction. A read-only `git status` check was attempted for context but failed because this export/sandbox could not create required temporary xcrun cache files and did not expose usable git metadata; no repository files were modified.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 Item Evidence section for the single assigned item`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`