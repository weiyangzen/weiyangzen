# agent_137 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-137 `file` `prompt-template/block/round-contract-missing.md`
- cursor: `[_]`
- core_role: This is a block-message prompt template used by the RLCR/Codex stop-hook path when a non-finalize round tries to exit without the required round contract file. It is part of the anti-drift control surface: the template tells the worker to create `{{ROUND_CONTRACT_FILE}}` before continuing, and explains that the loop uses the contract to prevent goal drift. Direct file evidence: `prompt-template/block/round-contract-missing.md:1` defines the block title, `:3-4` names the required output path placeholder, and `:13` states the hard stop.
- algorithmic_behavior: The template itself is declarative, but its rendered content drives a stop-hook transition from “round may exit” to “blocked until contract exists.” The stop hook sets `ROUND_CONTRACT_FILE="$LOOP_DIR/round-${CURRENT_ROUND}-contract.md"` for normal rounds at `hooks/loop-codex-stop-hook.sh:751-757`, checks summary existence first at `:759-784`, then enforces round contract existence only when anti-drift metadata is present. The anti-drift gate reads raw frontmatter for `drift_status:` at `:789-792`; if the current round is not finalize and the contract file is absent, it renders this assigned template via `load_and_render_safe "$TEMPLATE_DIR" "block/round-contract-missing.md"` at `:793-805`, emits JSON with `"decision": "block"` and system message `"Loop: Round contract missing for round $CURRENT_ROUND"` at `:807-814`, then exits successfully from the hook process at `:815` after returning the block decision.
- inputs_outputs_state: Inputs are the current loop phase, `CURRENT_ROUND`, `LOOP_DIR`, raw state frontmatter, and the file existence state of `round-${CURRENT_ROUND}-contract.md`. Template input is `ROUND_CONTRACT_FILE`, substituted into `{{ROUND_CONTRACT_FILE}}` at `prompt-template/block/round-contract-missing.md:4`. Output is rendered Markdown embedded as the `reason` field in the stop hook’s JSON block response. State transition is external to this file: missing contract plus active anti-drift state causes the hook to block exit; writing the contract at the expected path allows subsequent stop-hook checks to proceed past this gate.
- gates_or_invariants: The contract must restate exactly the round-scoping data listed by the template: single mainline objective, target ACs, truly blocking issues, queued out-of-scope issues, and concrete success criteria at `prompt-template/block/round-contract-missing.md:6-11`. The stop-hook invariant is conditional: no finalize-phase enforcement and no legacy-loop enforcement when `drift_status:` is absent, per `hooks/loop-codex-stop-hook.sh:789-792`. A sibling validator protects the same contract artifact from Bash mutation: `hooks/loop-bash-validator.sh:523-536` blocks commands that modify `round-[0-9]+-contract.md` and routes users toward Write/Edit tooling instead, keeping contract creation aligned with hook validation.
- dependencies_and_callers: Direct caller found: `hooks/loop-codex-stop-hook.sh:804` through `load_and_render_safe`. Related write-path guard: `hooks/loop-bash-validator.sh:534` renders `prompt-template/block/round-contract-bash-write.md`, not this file, for Bash modification attempts. Rendering depends on `hooks/lib/template-loader.sh`; placeholder syntax is documented at `:7-13`, `load_template` reads `$template_dir/$template_name` at `:36-45`, and `load_and_render_safe` falls back if the template is missing or renders empty at `:185-210`. The next-round prompt creates the positive-side requirement before implementation: `prompt-template/claude/next-round-prompt.md:12-28` tells the worker to write `@{{ROUND_CONTRACT_FILE}}` and not start implementation until it exists.
- edge_cases_or_failure_modes: If this template is missing or empty, the stop hook still blocks using its inline fallback because `load_and_render_safe` falls back at `hooks/lib/template-loader.sh:197-207`; behavior survives template loss but wording may diverge. If `drift_status:` is absent from raw frontmatter, the missing-contract gate is skipped for legacy loops, so this template is not rendered. If `IS_FINALIZE_PHASE` is true, `ROUND_CONTRACT_FILE` is set empty and contract enforcement is skipped, so finalize cannot be blocked by this template. If the summary file is missing, the summary gate at `hooks/loop-codex-stop-hook.sh:759-784` blocks earlier and this template is not reached. If `ROUND_CONTRACT_FILE` contains unusual characters, substitution is single-pass through the template loader, limiting placeholder-injection behavior as documented at `hooks/lib/template-loader.sh:48-55`.
- validation_or_tests: There is direct behavioral coverage around the missing-contract stop gate in `tests/test-plan-file-hooks.sh`: the test setup creates anti-drift state with `drift_status: normal` at `:130-135`, defines `create_round_contract` at `:63-70`, and has a named negative case “Stop hook blocks when round contract is missing” referenced by search at `tests/test-plan-file-hooks.sh:495-524`. Template reference integrity is covered generically by `tests/test-template-references.sh:83-110`, which scans `load_template`, `load_and_render`, and `load_and_render_safe` calls and fails missing template paths; fallback-safe rendering policy is checked at `tests/test-template-references.sh:169-200` for critical validators. Additional validator tests cover contract path access and mutation boundaries in `tests/test-allowlist-validators.sh`, including current-round Write/Edit allowance, stale-round blocks, Read behavior, and Bash block cases referenced at lines `:169-179`, `:208-218`, `:251-274`, `:320-369`, and `:402-412`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence section present for the single assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`