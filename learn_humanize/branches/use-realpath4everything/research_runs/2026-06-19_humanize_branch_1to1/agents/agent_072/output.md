# agent_072 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-072 `file` `tests/test-codex-hook-install.sh`
- cursor: `[_]`
- core_role: Executable specification for Codex-target Humanize installation: it verifies that `scripts/install-skill.sh --target codex` syncs runtime skills, installs Codex-native Stop hooks, enables the `codex_hooks` feature, seeds Codex-friendly Humanize config, installs a `bitlesson-selector` shim, preserves unrelated hooks, removes stale managed hooks, remains idempotent, and fails on Codex builds without native hook support. Key setup and preflight are in `tests/test-codex-hook-install.sh:8`, `tests/test-codex-hook-install.sh:19`, and `tests/test-codex-hook-install.sh:24`.

- algorithmic_behavior: The test creates an isolated temp layout with fake `codex`, Codex home, XDG config, and command-bin directories (`tests/test-codex-hook-install.sh:29`). The fake Codex CLI exposes `features list`, `features enable codex_hooks`, and `exec` behaviors (`tests/test-codex-hook-install.sh:40`). It seeds an existing `hooks.json` containing a custom `SessionStart`, one stale Humanize Stop hook, and one unrelated Stop hook (`tests/test-codex-hook-install.sh:71`). It then runs `install-skill.sh` with explicit Codex config, skills, and command-bin paths (`tests/test-codex-hook-install.sh:110`). After install, it checks installed skill bundles (`tests/test-codex-hook-install.sh:118`, `tests/test-codex-hook-install.sh:124`), hook/config/feature artifacts (`tests/test-codex-hook-install.sh:130`, `tests/test-codex-hook-install.sh:136`, `tests/test-codex-hook-install.sh:142`), and shim executability (`tests/test-codex-hook-install.sh:148`). A Python JSON inspection extracts Stop hook commands and asserts the managed runtime Stop hook exists, unrelated Stop hook remains, stale old Humanize hook is absent, `SessionStart` is unchanged, and exactly one managed Humanize Stop hook exists (`tests/test-codex-hook-install.sh:168`). It invokes the installed `bitlesson-selector` shim against a placeholder BitLesson file and expects stable `LESSON_IDS: NONE` output (`tests/test-codex-hook-install.sh:228`). It reruns install to prove managed Stop hooks are not duplicated and feature enable runs each time (`tests/test-codex-hook-install.sh:251`, `tests/test-codex-hook-install.sh:278`, `tests/test-codex-hook-install.sh:284`). Finally, it swaps in an unsupported fake Codex CLI that lacks `codex_hooks`, expects nonzero installer exit, and verifies the failure message mentions the missing feature (`tests/test-codex-hook-install.sh:290`, `tests/test-codex-hook-install.sh:310`, `tests/test-codex-hook-install.sh:326`).

- inputs_outputs_state: Inputs are the repository installer at `scripts/install-skill.sh`, a preexisting `$CODEX_HOME/hooks.json`, fake `codex` command behavior on `PATH`, `XDG_CONFIG_HOME`, `--codex-config-dir`, `--codex-skills-dir`, `--command-bin-dir`, and a placeholder BitLesson file. Outputs are temp-only install artifacts: `$CODEX_HOME/skills/humanize/SKILL.md`, `$CODEX_HOME/skills/humanize-rlcr/SKILL.md`, `$CODEX_HOME/hooks.json`, `$CODEX_HOME/.codex-hooks-enabled`, `$XDG_CONFIG_HOME/humanize/config.json`, `$COMMAND_BIN_DIR/bitlesson-selector`, install logs, and feature-enable log entries. The main state transition is `hooks.json` Stop hook merge: stale managed commands matching old Humanize hook paths are removed, unrelated Stop groups are preserved, template-managed Stop hook is appended, and non-Stop hook state remains unchanged. User config transitions from absent to JSON containing `bitlesson_model: gpt-5.4` and `provider_mode: codex-only` (`tests/test-codex-hook-install.sh:154`, `tests/test-codex-hook-install.sh:161`).

- gates_or_invariants: The test hard-gates on executable installer presence and `python3` availability (`tests/test-codex-hook-install.sh:19`, `tests/test-codex-hook-install.sh:24`). It assumes `jq` is available for config assertions but does not preflight it (`tests/test-codex-hook-install.sh:154`). Hook merge invariants are exact: managed Stop hook found, custom Stop hook kept, stale managed hook removed, SessionStart preserved, and managed hook count equals one (`tests/test-codex-hook-install.sh:198` through `tests/test-codex-hook-install.sh:225`). Idempotency invariant is one managed Humanize Stop hook after a second install (`tests/test-codex-hook-install.sh:278`). Feature enable invariant is two log entries after two successful installs (`tests/test-codex-hook-install.sh:284`). Unsupported Codex invariant is nonzero exit with an explanatory `codex_hooks feature` message (`tests/test-codex-hook-install.sh:320`, `tests/test-codex-hook-install.sh:326`).

- dependencies_and_callers: Direct test dependency is `tests/test-helpers.sh`, especially `setup_test_dir`, `pass`, `fail`, and `print_test_summary` (`tests/test-helpers.sh:30`, `tests/test-helpers.sh:35`, `tests/test-helpers.sh:86`). The test exercises `scripts/install-skill.sh`, whose Codex path syncs skills/runtime (`scripts/install-skill.sh:232`), seeds user config (`scripts/install-skill.sh:269`), installs native hooks (`scripts/install-skill.sh:253`), and writes the shim (`scripts/install-skill.sh:334`). Native hook behavior is delegated to `scripts/install-codex-hooks.sh`: Codex feature support check (`scripts/install-codex-hooks.sh:75`), JSON merge/filter/append algorithm (`scripts/install-codex-hooks.sh:94`), and feature enable (`scripts/install-codex-hooks.sh:175`). The managed hook source is `config/codex-hooks.json:4`, with command template at `config/codex-hooks.json:9`. The shim dispatch target is `scripts/bitlesson-select.sh`; for this test’s placeholder BitLesson file, the runtime exits through its no-recorded-lessons path (`scripts/bitlesson-select.sh:105`) rather than needing real model selection.

- edge_cases_or_failure_modes: Missing installer or `python3` causes immediate fatal exit; missing `jq` would fail later under `set -euo pipefail` without a tailored diagnostic. The test covers stale managed hook cleanup, custom Stop hook preservation, SessionStart preservation, idempotent hook count, and unsupported Codex feature rejection. It does not cover malformed existing `hooks.json`, invalid `hooks`/`Stop` shapes, runtime roots containing spaces or shell metacharacters, symlinked repo/config roots, or path canonicalization-specific behavior. A notable test isolation risk: the second successful install omits `--command-bin-dir` (`tests/test-codex-hook-install.sh:251`), so if executed without a redirected `HOME` or `HUMANIZE_COMMAND_BIN_DIR`, `install-skill.sh` can write the shim to the user default command bin (`scripts/install-skill.sh:35`, `scripts/install-skill.sh:477`). The unsupported-Codex case also omits `XDG_CONFIG_HOME` (`tests/test-codex-hook-install.sh:310`), while the installer writes user config before native hook support failure (`scripts/install-skill.sh:386`), so that path can touch the user default config location in an unwrapped environment.

- validation_or_tests: This file is itself the validation asset. It performs black-box install validation by running the installer with fake Codex CLI implementations and then inspecting filesystem artifacts, JSON state, command shim output, idempotency, feature-enable side effects, and unsupported feature errors. I did not execute it in this research pass because the assignment requested research notes only and the branch export is read-only/no-modification.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1; the single Item Evidence heading above is present once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`