# agent_041 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-041 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: RLCR Stop-hook state machine and completion gate. `hooks/loop-codex-stop-hook.sh` is registered as the Claude Code `Stop` hook in `hooks/hooks.json:63-70`, and `scripts/rlcr-stop-gate.sh:3-14` wraps the same hook for non-hook skill workflows. Its top-level contract is explicit at `hooks/loop-codex-stop-hook.sh:3-12`: intercept exit attempts, ask Codex to review work, block when Codex does not confirm completion, and preserve per-round state under `.humanize/rlcr/<timestamp>/`.

- algorithmic_behavior: The hook reads the Stop-hook JSON from stdin (`hooks/loop-codex-stop-hook.sh:24-28`), resolves a canonical project root through `resolve_project_root` (`hooks/loop-codex-stop-hook.sh:42-47`; helper realpath behavior in `hooks/lib/project-root.sh:41-53` and `hooks/lib/project-root.sh:113-143`), extracts `session_id`, and locates an active loop with `find_active_loop "$LOOP_BASE_DIR" "$HOOK_SESSION_ID" true` (`hooks/loop-codex-stop-hook.sh:61-68`). Loop lookup is session-aware and zombie-safe: without a session it only checks the newest loop, with a session it stops at that session's newest terminal state, and only the Stop hook opts into `bg-pending.marker` fallback (`hooks/lib/loop-common.sh:308-425`). Before normal gates, it delegates background-task short-circuits to `handle_bg_task_short_circuit` (`hooks/loop-codex-stop-hook.sh:71-83`), which implements ambiguous-caller, cross-session parked-loop, pending-background-task, and stale-marker cleanup guards (`hooks/lib/loop-bg-tasks.sh:299-435`). It then resolves phase by active state file: `methodology-analysis-state.md`, `finalize-state.md`, or `state.md` (`hooks/loop-codex-stop-hook.sh:85-101`; helper order in `hooks/lib/loop-common.sh:264-280`).

  Normal implementation phase gates run before Codex: state frontmatter extraction and tolerant parse (`hooks/loop-codex-stop-hook.sh:103-171`; parser in `hooks/lib/loop-common.sh:442-522`), Codex model/effort validation (`hooks/loop-codex-stop-hook.sh:172-183`), required `current_round`/`max_iterations` checks (`hooks/loop-codex-stop-hook.sh:186-215`), schema checks for `plan_tracked`, `start_branch`, `review_started`, and `base_branch` (`hooks/loop-codex-stop-hook.sh:220-270`), branch consistency (`hooks/loop-codex-stop-hook.sh:285-316`), plan-file backup/integrity checks unless already in review phase (`hooks/loop-codex-stop-hook.sh:318-399`), Todo/Task completion parsing via `hooks/check-todos-from-transcript.py` (`hooks/loop-codex-stop-hook.sh:401-457`), fail-closed git status caching (`hooks/loop-codex-stop-hook.sh:485-516`), large modified code/docs file rejection above 2000 lines (`hooks/loop-codex-stop-hook.sh:518-604`), methodology-analysis completion handling (`hooks/loop-codex-stop-hook.sh:606-626`), git clean and optional push enforcement (`hooks/loop-codex-stop-hook.sh:628-744`), summary file requirement (`hooks/loop-codex-stop-hook.sh:746-784`), anti-drift round-contract requirement (`hooks/loop-codex-stop-hook.sh:786-817`), BitLesson delta validation (`hooks/loop-codex-stop-hook.sh:819-838`), Round 0 goal-tracker placeholder checks (`hooks/loop-codex-stop-hook.sh:840-916`), and max-iteration handling (`hooks/loop-codex-stop-hook.sh:918-935`).

  Finalize phase is a separate post-review state: if `finalize-state.md` is active and all earlier gates pass, Codex is skipped and the hook either enters methodology analysis or renames `finalize-state.md` to `complete-state.md` (`hooks/loop-codex-stop-hook.sh:937-953`). Implementation review builds `round-N-review-prompt.md`, including commit history from `BASE_COMMIT..HEAD` when the base commit is an ancestor, or recent branch commits otherwise (`hooks/loop-codex-stop-hook.sh:961-1087`). It requires `codex`, creates cache/debug files outside the project tree, probes and caches support for `--disable codex_hooks` to avoid nested hook recursion, and builds `codex exec`/`codex review` arguments (`hooks/loop-codex-stop-hook.sh:1089-1162`). The implementation review runs `codex exec` with timeout and stores command/stdout/stderr audit files (`hooks/loop-codex-stop-hook.sh:1623-1662`). It requires a nonzero-free exit, a created review-result file or stdout copy, and non-empty review content (`hooks/loop-codex-stop-hook.sh:1664-1768`). It treats only the last non-empty line exactly equal to `COMPLETE` or `STOP` as a terminal marker (`hooks/loop-codex-stop-hook.sh:1770-1778`).

  Completion is two-stage: `COMPLETE` in implementation phase sets `review_started: true`, clears drift counters, writes `.review-phase-started`, and immediately runs `codex review`; review failure is blocking, review issues continue the loop, and no review issues enter finalize (`hooks/loop-codex-stop-hook.sh:1826-1876`; review helpers `hooks/loop-codex-stop-hook.sh:1167-1280`). In review phase, a manually toggled `review_started: true` is rejected unless `.review-phase-started` exists (`hooks/loop-codex-stop-hook.sh:1878-1907`). Review issue detection is delegated to `detect_review_issues`, which scans only the last 50 lines of the Codex review log for `[P0-9]` markers in the first 10 characters, extracts from the first issue line to EOF, and treats missing/empty log as hard error (`hooks/lib/loop-common.sh:719-788`). Non-complete implementation reviews update `current_round`, drift fields, prompt/summary stubs, optional drift-replan prompts, Agent Teams notices, open-question notices, push notes, and then return JSON `decision: block` (`hooks/loop-codex-stop-hook.sh:1948-2183`).

- inputs_outputs_state: Inputs are hook JSON stdin (`session_id`, `transcript_path`, Stop metadata), project root from `CLAUDE_PROJECT_DIR` or git toplevel, `.humanize/rlcr/<timestamp>/*-state.md`, plan backup `plan.md`, project plan file, round summaries/contracts, goal tracker, git status/logs, Codex CLI output, BitLesson config/files, templates, and optional transcript background-task events. Outputs are either no stdout and `exit 0` to allow natural stop, JSON with `"decision": "block"` plus `reason`/`systemMessage`, or JSON with only `systemMessage` for allow-with-message background cases. Persistent state transitions include `state.md -> finalize-state.md` on review success (`hooks/loop-codex-stop-hook.sh:1281-1365`), `finalize-state.md -> complete-state.md` after final summary gates (`hooks/loop-codex-stop-hook.sh:943-952`), `state.md/finalize-state.md -> methodology-analysis-state.md -> <complete|stop|maxiter>-state.md` when privacy allows methodology analysis (`hooks/lib/methodology-analysis.sh:38-100`, `hooks/lib/methodology-analysis.sh:114-174`), `state.md -> maxiter-state.md|stop-state.md|unexpected-state.md` through `end_loop` (`hooks/lib/loop-common.sh:1533-1566`), `review_started: false -> true` plus `.review-phase-started` on implementation `COMPLETE` (`hooks/loop-codex-stop-hook.sh:1854-1871`), and `current_round`/drift-field updates on issue continuation (`hooks/loop-codex-stop-hook.sh:1952-1958`).

- gates_or_invariants: The hook is deliberately fail-closed. It blocks or terminates unexpectedly on malformed critical state fields, invalid Codex model/effort, missing required schema, branch drift from `start_branch`, missing/modified plan backup, incomplete native tasks, git status failure, tracked `.humanize` state, dirty worktree, unpushed commits when `push_every_round=true`, missing summary, missing anti-drift contract, invalid BitLesson delta, uninitialized Round 0 goal tracker, and missing `Mainline Progress Verdict` in implementation reviews (`hooks/loop-codex-stop-hook.sh:172-215`, `hooks/loop-codex-stop-hook.sh:220-457`, `hooks/loop-codex-stop-hook.sh:485-935`, `hooks/loop-codex-stop-hook.sh:1785-1791`). It never trusts `stop_hook_active` as a bypass; the comments state iterative loops must review on each blocked continuation (`hooks/loop-codex-stop-hook.sh:30-36`). It only recognizes terminal markers on their own last non-empty line to avoid false positives like "CANNOT COMPLETE" (`hooks/loop-codex-stop-hook.sh:1773-1778`). Mainline anti-drift requires Codex to classify progress as `ADVANCED`, `STALLED`, or `REGRESSED`; two consecutive stalls/regressions set `replan_required`, three trigger a stop circuit breaker (`hooks/loop-codex-stop-hook.sh:1793-1810`, `hooks/loop-codex-stop-hook.sh:1909-1912`). Review phase cannot be skipped after it starts: `codex review` failures and missing stdout block instead of finalizing (`hooks/loop-codex-stop-hook.sh:1248-1280`, `hooks/loop-codex-stop-hook.sh:1548-1621`). Background-task handling allows a natural stop but does not mark the loop complete; it writes `bg-pending.marker` and resumes normal gates only after transcript evidence shows no pending tasks (`hooks/lib/loop-bg-tasks.sh:369-435`).

- dependencies_and_callers: Direct shell dependencies are `bash`, `jq`, `git`, `sed`, `awk`, `grep`, `wc`, `tail`, `cp`, `mv`, `date`, `codex`, `python3`, and optional timeout tools. Shared libraries are `hooks/lib/loop-common.sh` for constants, state parsing, active-loop selection, drift/verdict parsing, review issue detection, and terminal renames; `hooks/lib/project-root.sh` for realpath-canonical project roots and path canonicalization; `hooks/lib/loop-bg-tasks.sh` for transcript/background task parking; `hooks/lib/methodology-analysis.sh` for post-exit analysis phase; and `scripts/portable-timeout.sh` for bounded git/Codex calls (`hooks/loop-codex-stop-hook.sh:42-54`). External scripts are `hooks/check-todos-from-transcript.py` for Todo/Task completion and `scripts/bitlesson-validate-delta.sh` for BitLesson summary delta validation (`hooks/loop-codex-stop-hook.sh:408-457`, `hooks/loop-codex-stop-hook.sh:823-837`). Primary caller is Claude Code via `hooks/hooks.json:63-70`; secondary caller is `scripts/rlcr-stop-gate.sh`, which builds a Stop-like JSON payload, forwards `CLAUDE_PROJECT_DIR`, maps block JSON to exit 10, and treats no decision as allow (`scripts/rlcr-stop-gate.sh:99-178`).

- edge_cases_or_failure_modes: No active loop, no project root, or no active state file exits 0 and allows stop (`hooks/loop-codex-stop-hook.sh:46-68`, `hooks/loop-codex-stop-hook.sh:91-95`). `find_active_loop` prevents stale older loop revival when the newest loop for the session is terminal (`hooks/lib/loop-common.sh:308-425`). Background-task transcript errors fail closed for cleanup: unreadable/malformed transcript does not remove `bg-pending.marker`, while confirmed pending tasks short-circuit before all other gates (`hooks/lib/loop-bg-tasks.sh:389-435`). Git status timeouts remove stale `.git/index.lock` before blocking, preventing a timeout-created lock from trapping later commits (`hooks/loop-codex-stop-hook.sh:459-516`). Large-file detection only examines changed tracked/new files with code/doc extensions and skips deleted/unknown extension files (`hooks/loop-codex-stop-hook.sh:524-604`). `BASE_COMMIT` is used for commit history and review base only if it is an ancestor for prompt history, while `codex review` prefers `BASE_COMMIT` over `BASE_BRANCH` to avoid comparing a moving branch to itself (`hooks/loop-codex-stop-hook.sh:997-1007`, `hooks/loop-codex-stop-hook.sh:1176-1183`). If Codex does not create `round-N-review-result.md`, stdout is copied as fallback; copy failure, absent result, or empty result blocks (`hooks/loop-codex-stop-hook.sh:1715-1768`). If `BASE_BRANCH` is missing at `COMPLETE`, the code has a skip-reason path initialized (`hooks/loop-codex-stop-hook.sh:1844-1852`), but earlier schema validation normally blocks missing `base_branch` (`hooks/loop-codex-stop-hook.sh:258-270`), so that branch appears legacy/defensive. Methodology analysis is skipped when `privacy_mode=true`; otherwise it requires both non-empty done marker and non-empty report before final terminal rename (`hooks/lib/methodology-analysis.sh:42-72`, `hooks/lib/methodology-analysis.sh:118-168`).

- validation_or_tests: I did not execute tests because this assignment is read-only research notes only. Existing coverage is substantial and directly references this file. `tests/test-finalize-phase.sh` covers `COMPLETE` to finalize, finalize-state detection, Codex review failure blocking, empty review output blocking, finalize skipping Codex, non-complete review loop continuation, stalled/regressed drift behavior, and missing `Mainline Progress Verdict` blocking. `tests/test-plan-file-hooks.sh` covers stop-hook schema blocking, plan-file modification/deletion/tracked-plan cases, and plan backup integrity. `tests/test-stop-hook-bg-allow.sh` covers pending background subagents/shells, finalize-phase pending background tasks, cross-session `bg-pending.marker`, same-session stale marker cleanup, transcript tilde expansion, malformed/missing transcript preservation, pre-loop launch filtering, and dead/orphan task pruning. `tests/test-disable-nested-codex-hooks.sh` verifies nested `codex exec` and `codex review` include `--disable codex_hooks` when supported. `tests/test-codex-review-merge.sh` covers `detect_review_issues` marker scanning and missing/empty log hard errors. Robustness coverage includes `tests/robustness/test-hook-system-robustness.sh` for no-state/corrupt/incomplete-state hook behavior and `tests/robustness/test-state-transition-robustness.sh` for active/finalize/terminal state resolution. `.github/workflows/plan-file-test.yml:8-47` runs plan-file validation/hook tests when this hook changes.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-041`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`