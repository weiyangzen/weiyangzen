# agent_157 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-157 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: This is a Claude-facing prompt block that standardizes the fallback path for Goal Tracker reconciliation. Its role is not to update `goal-tracker.md` directly; it defines a review handoff contract where Claude can request tracker mutations in its summary when direct mutation is unsafe or blocked. See `prompt-template/claude/goal-tracker-update-request.md:2` and `prompt-template/claude/goal-tracker-update-request.md:17`.

- algorithmic_behavior: The template tells Claude to include a fenced Markdown section titled `Goal Tracker Update Request` only as an optional fallback. The section schema is fixed: `Requested Changes` plus `Justification`, with examples for completion evidence, blocking issues, queued issues, plan evolution, and deferrals at `prompt-template/claude/goal-tracker-update-request.md:4` through `prompt-template/claude/goal-tracker-update-request.md:14`. This turns a blocked direct write into a structured intent record that Codex can review later. It has no `{{VAR}}` placeholders, so rendering behavior is simple file load and append rather than variable substitution. The stop hook appends it into Claude’s next prompt via `load_template "$TEMPLATE_DIR" "claude/goal-tracker-update-request.md"` at `hooks/loop-codex-stop-hook.sh:2138` through `hooks/loop-codex-stop-hook.sh:2143`; if the file is missing or empty, the hook falls back to a one-line instruction at `hooks/loop-codex-stop-hook.sh:2140` through `hooks/loop-codex-stop-hook.sh:2141`.

- inputs_outputs_state: Inputs are Claude’s assessment that it cannot safely update the mutable section of `goal-tracker.md`, plus the desired tracker changes and their justification. The template’s output is a structured summary section, not an on-disk state mutation. The downstream state transition is delegated to Codex: `prompt-template/codex/goal-tracker-update-section.md:3` says Codex must handle a summary containing this section or detected tracker drift, and `prompt-template/codex/goal-tracker-update-section.md:5` through `prompt-template/codex/goal-tracker-update-section.md:11` define the transition: evaluate tracker state, update the active tracker if needed, never touch the immutable section, and explain rejected requests. The specific tracker state changes expected downstream are task movement, side-issue insertion, plan evolution logging, and justified deferral handling, listed at `prompt-template/codex/goal-tracker-update-section.md:13` through `prompt-template/codex/goal-tracker-update-section.md:18`.

- gates_or_invariants: The main invariant is that Claude should normally maintain only the mutable section directly, and should not change the immutable section after Round 0. This is stated in the surrounding next-round prompt at `prompt-template/claude/next-round-prompt.md:63` through `prompt-template/claude/next-round-prompt.md:65`. The write validator enforces the active tracker path and immutable-section preservation for `Write` operations after Round 0 at `hooks/loop-write-validator.sh:243` through `hooks/loop-write-validator.sh:265`. The edit validator applies the same active-path and mutable-update checks for `Edit` at `hooks/loop-edit-validator.sh:194` through `hooks/loop-edit-validator.sh:229`. Bash mutation of `goal-tracker.md` is blocked in all rounds at `hooks/loop-bash-validator.sh:485` through `hooks/loop-bash-validator.sh:499`, which makes this fallback template part of the safe recovery path when direct edits are not accepted. The blocking template reinforces the same invariant: after Round 0, update only the mutable section and use this request path if Codex help is needed, at `prompt-template/block/goal-tracker-modification.md:3` through `prompt-template/block/goal-tracker-modification.md:14`.

- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, which appends the template to `NEXT_PROMPT_FILE` at `hooks/loop-codex-stop-hook.sh:2138` through `hooks/loop-codex-stop-hook.sh:2143`. The template loader dependency is `hooks/lib/template-loader.sh`: `load_template` reads template files and emits a warning if missing at `hooks/lib/template-loader.sh:36` through `hooks/lib/template-loader.sh:45`; `load_and_render_safe` exists for placeholder-bearing templates and fallback behavior at `hooks/lib/template-loader.sh:188` through `hooks/lib/template-loader.sh:210`, though this specific Claude template is loaded without rendering. Related prompt contracts are `prompt-template/claude/next-round-prompt.md:54` through `prompt-template/claude/next-round-prompt.md:65`, which tells Claude to read and maintain the tracker, and `prompt-template/claude/review-phase-prompt.md:44` through `prompt-template/claude/review-phase-prompt.md:51`, which says a Goal Tracker Update Request belongs in the summary only if reconciliation still needs Codex help. Codex’s paired reconciliation instructions are loaded into review prompts through `hooks/loop-codex-stop-hook.sh:971` through `hooks/loop-codex-stop-hook.sh:975`.

- edge_cases_or_failure_modes: If this template is missing or empty, Claude still receives the weaker fallback string `Include a Goal Tracker Update Request section in your summary if needed.` from `hooks/loop-codex-stop-hook.sh:2140` through `hooks/loop-codex-stop-hook.sh:2141`, but loses the richer schema and examples. If Claude emits vague requested changes without evidence or justification, Codex is explicitly allowed to reject them and explain why at `prompt-template/codex/goal-tracker-update-section.md:11`. If Claude tries to use Bash to modify the tracker rather than the request path, the Bash validator blocks the operation at `hooks/loop-bash-validator.sh:491` through `hooks/loop-bash-validator.sh:498`. If Claude targets an old session’s tracker or changes immutable content after Round 0, the write/edit validators block based on normalized active path and immutable-section comparison at `hooks/loop-write-validator.sh:244` through `hooks/loop-write-validator.sh:260` and `hooks/loop-edit-validator.sh:195` through `hooks/loop-edit-validator.sh:224`. Deferrals are a sensitive edge case: the template permits requesting them at `prompt-template/claude/goal-tracker-update-request.md:11`, but the Codex side says deferrals should be allowed only with strong justification at `prompt-template/codex/goal-tracker-update-section.md:18`.

- validation_or_tests: The exact target template is referenced by the stop hook and found by repository search at `hooks/loop-codex-stop-hook.sh:2139`. The broad template reference validator scans hook scripts for template references and verifies referenced template files exist; its scan targets include `hooks/loop-codex-stop-hook.sh` at `tests/test-template-references.sh:56` through `tests/test-template-references.sh:64`, and it searches quoted template paths under hooks at `tests/test-template-references.sh:134` through `tests/test-template-references.sh:140`. The paired Codex reconciliation template has direct comprehensive test coverage in `tests/test-templates-comprehensive.sh`, which checks for `Goal Tracker Update Requests` and rendered tracker path according to the search result. I did not run the full test suite because the assignment requested research notes only and the workspace is read-only. A `git status --short` check also failed because this branch export has no `.git` metadata visible in the restricted workspace and the environment could not create Xcode cache files under `/tmp`; no files were modified.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-157`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`