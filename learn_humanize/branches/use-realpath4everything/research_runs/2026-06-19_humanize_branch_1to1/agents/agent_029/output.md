# agent_029 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-029 `file` `agents/plan-understanding-quiz.md`
- cursor: `[_]`
- core_role: This file is a Claude-style subagent prompt/policy definition for `plan-understanding-quiz`. It is not executable shell/Python code, but it is algorithmically important because it defines the pre-RLCR “Plan Understanding Quiz” generator used by `commands/start-rlcr-loop.md` before loop setup. Its role is to transform plan-file content plus repository context into a strict two-question comprehension check and a short educational summary. The frontmatter declares `name: plan-understanding-quiz`, model `opus`, and tool permissions `Read, Glob, Grep` at `agents/plan-understanding-quiz.md:1-6`.
- algorithmic_behavior: The prompt implements a two-phase generation algorithm. First, it analyzes the supplied plan file for modified components, technical mechanism, interconnections, and reused repository patterns at `agents/plan-understanding-quiz.md:14-23`. Second, it explores repository context by checking docs such as `README.md` or `CLAUDE.md`, directory structure, referenced files, and existing architecture at `agents/plan-understanding-quiz.md:24-28`. It then emits exactly two multiple-choice questions, each with four options and one correct answer, with `QUESTION_1` focused on changed components/core mechanism and `QUESTION_2` focused on integration, existing patterns, or constraints at `agents/plan-understanding-quiz.md:31-35`. The prompt also requires a 2-3 sentence technical summary for remediation when the user misses questions at `agents/plan-understanding-quiz.md:53-55`.
- inputs_outputs_state: The primary input is the content of a plan file supplied by the caller, plus read-only repository context gathered through the allowed tools. The output is a fixed 13-field line-oriented record: `QUESTION_1`, four `OPTION_1*` fields, `ANSWER_1`, `QUESTION_2`, four `OPTION_2*` fields, `ANSWER_2`, and `PLAN_SUMMARY`, specified at `agents/plan-understanding-quiz.md:57-75`. The file itself maintains no persistent state. Runtime state transitions are managed by the caller: `commands/start-rlcr-loop.md` invokes the agent after the compliance pre-check, parses the 13 fields, presents each question via `AskUserQuestion`, marks each answer internally as pass/wrong, and either proceeds, asks whether to proceed despite wrong answers, or stops if the user chooses to review first at `commands/start-rlcr-loop.md:60-106`.
- gates_or_invariants: The strongest invariant is output shape: all 13 fields are mandatory, each field must be on its own line, and answers must be exactly one of `A`, `B`, `C`, or `D` at `agents/plan-understanding-quiz.md:59-80`. The prompt also constrains content quality: questions must be plan-specific, implementation-focused, not generic, not trivial line/syntax trivia, and plausible enough that skimming users may fail while careful readers can answer at `agents/plan-understanding-quiz.md:36-43`. It requires language matching: if the plan is written in another language, questions and options should use that same language at `agents/plan-understanding-quiz.md:81-82`. In the caller, the quiz is advisory rather than a hard gate: malformed quiz output logs a warning and continues, while wrong user answers trigger an explicit proceed-or-stop choice rather than automatic failure at `commands/start-rlcr-loop.md:86-105`.
- dependencies_and_callers: The direct caller is `commands/start-rlcr-loop.md`, which instructs use of the Task tool to invoke `humanize:plan-understanding-quiz` with model `opus` and plan content at `commands/start-rlcr-loop.md:71-84`. The caller depends on exact field names from this prompt for parsing at `commands/start-rlcr-loop.md:86`. The behavior is documented for users in `docs/usage.md`: the quiz runs before RLCR work starts, asks two technical implementation questions, proceeds immediately if both are correct, and offers proceed-or-review if either is missed at `docs/usage.md:16-35`. Bypass paths exist in the command and setup flow: `--skip-quiz`, `--yolo`, `--skip-impl`, help mode, or unavailable plan content skip the quiz at `commands/start-rlcr-loop.md:64-69`; `commands/gen-plan.md` auto-starts RLCR with `--skip-quiz` after convergence discussion at `commands/gen-plan.md:594-608`; `scripts/setup-rlcr-loop.sh` documents and parses `--skip-quiz` and `--yolo` in its CLI help/argument handling according to the repository search results.
- edge_cases_or_failure_modes: A very short or underspecified plan is explicitly handled by deriving questions from whatever implementation hints are available at `agents/plan-understanding-quiz.md:84`. A malformed agent response is handled outside this file: missing fields or invalid answers cause `start-rlcr-loop` to warn that the quiz is unavailable and continue to setup at `commands/start-rlcr-loop.md:86`, so quiz-generation failure does not block implementation. The prompt gives no machine-readable escaping rules for colons, multiline values, or option text, so the caller’s line-oriented parser may be sensitive to deviations despite the strict format. The prompt asks for randomized correct-answer positions at `agents/plan-understanding-quiz.md:81`, but there is no deterministic enforcement in this file. The model may also produce generic or weak distractors if the plan lacks detail; the prompt mitigates that by defining good/bad question characteristics at `agents/plan-understanding-quiz.md:36-52`.
- validation_or_tests: No dedicated test file for `plan-understanding-quiz` was found in the targeted search. Validation is primarily contract-based through `commands/start-rlcr-loop.md`, which checks that all 13 fields exist and that answers are valid letters before presenting the questions at `commands/start-rlcr-loop.md:86-95`. Documentation in `docs/usage.md:16-41` confirms the intended user-facing flow and skip semantics. I also inspected the file directly with line numbers and confirmed its byte size as `5410`, matching the assignment metadata. Attempting `git rev-parse` was not usable in this read-only export because the directory is not a Git checkout and the environment blocked Git/Xcode cache writes; this did not affect direct file inspection.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1; the single assigned item appears once in the Item Evidence heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`