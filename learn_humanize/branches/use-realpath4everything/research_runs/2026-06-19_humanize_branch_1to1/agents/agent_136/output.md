# agent_136 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-136 `file` `prompt-template/block/round-contract-bash-write.md`
- cursor: `[_]`
- core_role: This is a block-message template used by the RLCR Bash pre-tool validator when a shell command attempts to modify `round-N-contract.md`. Its role is not to implement the detector itself, but to provide the user-facing enforcement text for a core anti-bypass gate: round contracts must be modified through structured Write/Edit tool paths rather than shell redirection, `tee`, in-place editors, or file-moving commands. The template content is short: title at `prompt-template/block/round-contract-bash-write.md:1`, prohibition at line 3, remediation instruction at line 5, and the rendered target path placeholder at line 7.
- algorithmic_behavior: The behavioral transition is driven by `hooks/loop-bash-validator.sh`. The Bash hook parses hook JSON, exits early unless `tool_name == "Bash"` (`hooks/loop-bash-validator.sh:34-38`), lowercases the command (`hooks/loop-bash-validator.sh:45-46`), resolves the active loop directory (`hooks/loop-bash-validator.sh:52-59`), and then checks whether the command modifies a round contract. The round-contract branch matches `round-[0-9]+-contract\.md` through `command_modifies_file` (`hooks/loop-bash-validator.sh:528`). If matched, it computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-contract.md"` (`hooks/loop-bash-validator.sh:529`), renders this template through `load_and_render_safe` (`hooks/loop-bash-validator.sh:534-535`), writes the rendered message to stderr, and exits `2` (`hooks/loop-bash-validator.sh:536`). The template therefore participates in the gate’s final state transition from “Bash tool request pending” to “Bash request blocked with recovery path”.
- inputs_outputs_state: Inputs are the Bash command string from `.tool_input.command`, active loop state including `ACTIVE_LOOP_DIR` and `CURRENT_ROUND`, and this Markdown template. The template has one substitution input, `{{CORRECT_PATH}}`, rendered from the active current-round contract path. Output is a Markdown block message sent to stderr, for example instructing the agent to use Write/Edit on the correct `round-${CURRENT_ROUND}-contract.md`. Persistent repository state is not changed by this template. Runtime state change is limited to the validator rejecting the Bash command with exit code `2`; no contract file is written or mutated by the template path.
- gates_or_invariants: The invariant enforced is that `round-N-contract.md` is protected from Bash-based mutation even when it is the current round’s contract. The correct write path is still surfaced, but only for Write/Edit tool use. The detector covers write-like Bash forms through `command_modifies_file`: redirection, append redirection, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`/`cp` to target, `rm`, `dd of=`, `truncate`, `printf ... >`, and `exec fd>` (`hooks/lib/loop-common.sh:1485-1502`). The template-rendering invariant is that missing or empty template content must not yield an empty block response: `load_and_render_safe` falls back to an inline message if loading/rendering fails (`hooks/lib/template-loader.sh:188-210`). The gate is case-insensitive at the command-text level because the Bash validator lowercases the command before matching (`hooks/loop-bash-validator.sh:45-46`; `hooks/lib/loop-common.sh:790-792`).
- dependencies_and_callers: Direct runtime caller is `hooks/loop-bash-validator.sh:528-536`. That caller depends on `hooks/lib/loop-common.sh` for command normalization and `command_modifies_file`, and on `hooks/lib/template-loader.sh` for `load_and_render_safe`. Related sibling templates include `prompt-template/block/summary-bash-write.md` and `prompt-template/block/goal-tracker-bash-write.md`, both used for similar “do not modify controlled loop files via Bash” messages (`hooks/lib/loop-common.sh:892-911`). Related non-Bash contract protections exist in write/edit/read validators: current round contract Write/Edit is allowed, stale round contract access is blocked, and finalize phase contract access is blocked, as seen in references around `hooks/loop-write-validator.sh:220-350`, `hooks/loop-edit-validator.sh:248-259`, and `hooks/loop-read-validator.sh:271-320`.
- edge_cases_or_failure_modes: If the template file is missing or renders empty, `load_and_render_safe` emits the fallback message embedded in `hooks/loop-bash-validator.sh:530-533`, preserving the block but with less polished formatting. If `CORRECT_PATH` is unavailable because active loop resolution/current round state is unavailable, the surrounding validator state is the likely failure point; this template only renders the value it receives. Detection is regex-based over the lowercased command and can miss write methods not represented in `command_modifies_file`, especially complex shell constructs, quoted paths with spaces, or indirect scripts that mutate the file without an obvious target string in the original command. The detector intentionally operates on command text rather than filesystem side effects, so it prevents common bypasses but is not a complete shell sandbox. Since the template only says “Use the Write or Edit tool,” it assumes the separate Write/Edit validators will enforce current-round and correct-location invariants.
- validation_or_tests: `tests/test-allowlist-validators.sh:402-412` directly verifies that a Bash command like `echo test > $LOOP_DIR/round-5-contract.md` exits `2` and emits a contract-related error. `tests/test-template-references.sh:1-9` describes the broader validation goal of preventing missing templates from causing empty validator messages, and the same script scans shell template references including `hooks/loop-bash-validator.sh` (`tests/test-template-references.sh:56-64`). Template loading/rendering behavior is covered by `tests/test-template-loader.sh:147-164`, which exercises `load_and_render` with placeholder substitution; `load_and_render_safe` itself provides fallback behavior at `hooks/lib/template-loader.sh:188-210`. The template is directly referenced in the Bash validator via literal path `block/round-contract-bash-write.md`, so reference scanning can catch accidental removal or rename.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 item evidence sections present; exactly one assigned item heading emitted`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`