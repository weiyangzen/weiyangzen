# agent_058 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-058 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: This script is the `gen-plan` workflow’s preflight gate. It validates CLI shape, draft input readability, output target safety, output directory writability, and availability of the plan template before the command proceeds to draft relevance checks and plan generation. The command wrapper calls it in Phase 1 and treats exit code `0` as permission to continue, parsing its `TEMPLATE_FILE:` stdout line for the template path; see [commands/gen-plan.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/commands/gen-plan.md:132).
- algorithmic_behavior: The script is a linear shell validator with `set -e` at [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/validate-gen-plan-io.sh:14). It first defines usage and exit-code semantics, then parses flags in a `while [[ $# -gt 0 ]]` loop with a `case` over `--input`, `--output`, `--auto-start-rlcr-if-converged`, `--discussion`, `--direct`, and help at [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/validate-gen-plan-io.sh:16) and [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/validate-gen-plan-io.sh:36). Missing values, values that look like flags, unknown options, missing required `--input` or `--output`, and mutually enabled `--discussion` plus `--direct` all stop with exit code `6`. After parsing, it normalizes input and output using `realpath -m ... || echo "$path"` at [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/validate-gen-plan-io.sh:99), derives `OUTPUT_DIR` with `dirname`, and emits a validation header. It then checks, in order: input file exists, input file is non-empty, output directory exists, output target is not a directory, output target does not already exist, and output directory is writable at [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/validate-gen-plan-io.sh:108). On success, it reports line count and output target, resolves the template either from `CLAUDE_PLUGIN_ROOT/prompt-template/plan/gen-plan-template.md` or from a script-relative fallback, then exits `0` after printing `TEMPLATE_FILE:`; see [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/validate-gen-plan-io.sh:155) and [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/validate-gen-plan-io.sh:162).
- inputs_outputs_state: Inputs are command-line flags, filesystem state for the draft file and output path, the process environment variable `CLAUDE_PLUGIN_ROOT`, the script location via `BASH_SOURCE`, and the plan template file. Outputs are stdout diagnostics, stable `VALIDATION_ERROR:*` markers, `VALIDATION_SUCCESS`, `TEMPLATE_FILE: <path>`, and process exit codes. It does not create the output file or mutate repo state; [commands/gen-plan.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/commands/gen-plan.md:150) explicitly documents the validator as side-effect-free. Its only internal state is shell variables: `INPUT_FILE`, `OUTPUT_FILE`, `AUTO_START_RLCR_IF_CONVERGED`, `GEN_PLAN_MODE_DISCUSSION`, `GEN_PLAN_MODE_DIRECT`, `OUTPUT_DIR`, `INPUT_LINE_COUNT`, `SCRIPT_DIR`, and `TEMPLATE_FILE`.
- gates_or_invariants: Required arguments are exactly `--input` and `--output`; their values cannot be absent or another flag. `--discussion` and `--direct` are mutually exclusive at [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/validate-gen-plan-io.sh:76). Direct mode plus `--auto-start-rlcr-if-converged` is allowed but emits a note because auto-start only triggers in discussion mode at [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/validate-gen-plan-io.sh:93). The output target must be a new file path: existing files and directories both map to exit code `4` at [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/validate-gen-plan-io.sh:132). The template file is a required plugin invariant; missing template exits `7` as a configuration error at [scripts/validate-gen-plan-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/validate-gen-plan-io.sh:171).
- dependencies_and_callers: The primary caller is `/humanize:gen-plan`, whose allowed Bash tools include this exact script and whose Phase 1 invokes `"${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh" $ARGUMENTS`; see [commands/gen-plan.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/commands/gen-plan.md:4) and [commands/gen-plan.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/commands/gen-plan.md:134). The flow-level skill diagram also routes through this validator before reading the draft; see [skills/humanize-gen-plan/SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/skills/humanize-gen-plan/SKILL.md:19). General Humanize documentation lists the script as the “Generate Plan from Draft” preflight and repeats its exit-code contract at [skills/humanize/SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/skills/humanize/SKILL.md:97) and [skills/humanize/SKILL.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/skills/humanize/SKILL.md:221). Runtime dependencies are standard POSIX/Bash utilities: `realpath`, `dirname`, `wc`, `tr`, `cd`, `pwd`, and filesystem test operators. It depends on `prompt-template/plan/gen-plan-template.md` existing; the template begins with the plan title skeleton at [prompt-template/plan/gen-plan-template.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/prompt-template/plan/gen-plan-template.md:1).
- edge_cases_or_failure_modes: The branch-relevant path behavior is best-effort canonicalization. The script says “Get absolute paths,” but uses GNU-style `realpath -m`; on this environment, `realpath -m README.md` fails with “illegal option -- m”, so the fallback keeps raw relative paths. That means absolute/canonical reporting is not guaranteed on BSD/macOS unless GNU `realpath` is present. Other failure modes are intentionally distinguished by exit code: input missing `1`, empty input `2`, missing output directory `3`, existing output or output directory `4`, non-writable output directory `5`, invalid args/help `6`, and missing template `7`. Because existence checks run after normalization fallback, traversal and symlink behavior depends on the local `realpath` implementation. A direct read-only probe of `scripts/validate-gen-plan-io.sh --input README.md --output docs/__agent_058_probe_plan.md --direct` exited `5` with `VALIDATION_ERROR: NO_WRITE_PERMISSION`, confirming the output writability gate triggers before template resolution in this branch export.
- validation_or_tests: Static syntax validation passed with `bash -n scripts/validate-gen-plan-io.sh`. The repo’s focused test block checks executable presence and the validator’s major exit-code paths: missing `--input` value, missing `--output` value, flag-as-value, unknown option, missing input, empty input, missing output directory, existing output file, output directory path, valid path success, auto-start flag acceptance, `--discussion` acceptance, `--direct` acceptance, mutual exclusion, and help exit code; see [tests/test-gen-plan.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-gen-plan.sh:557). I did not run the full test suite because the assigned branch export is read-only and the tests create temp files and exercise script paths beyond this single evidence item.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item; the single Item Evidence heading above corresponds to the assigned file item.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`