# agent_042 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-042 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: `hooks/loop-edit-validator.sh` is the Claude `PreToolUse` gate for `Edit` operations in the RLCR loop. It is registered in `hooks/hooks.json:24-30` under matcher `Edit`. Its algorithmic responsibility is to decide whether an attempted edit may proceed, with special protection for `.humanize/rlcr` state-machine artifacts: todos, prompts, state files, finalize contracts, loop-local `plan.md` backups, goal trackers, and stale round summary/contract files.
- algorithmic_behavior: The hook reads JSON from stdin, extracts `.tool_name`, and exits immediately for non-`Edit` tools at `hooks/loop-edit-validator.sh:23-28`. For `Edit`, it extracts `.tool_input.file_path`, lowercases it for pattern checks, and extracts `session_id` for session-aware loop lookup at `hooks/loop-edit-validator.sh:30-34`. It first blocks `round-*-todos.md` unless the path is one of the active-loop allowlisted historical files, and blocks all `round-*-prompt.md` writes at `hooks/loop-edit-validator.sh:40-53`. Before the normal `.humanize/rlcr` early exit, it enforces methodology-analysis mode: if the session-matched active loop has `methodology-analysis-state.md`, only `methodology-analysis-report.md` and `methodology-analysis-done.md` inside that loop may be edited; all other edits are blocked at `hooks/loop-edit-validator.sh:62-114`. For normal RLCR files it finds the active loop, resolves the active state file, detects finalize mode, strictly parses the state file, and stores `CURRENT_ROUND` at `hooks/loop-edit-validator.sh:120-149`. It then blocks state-file edits, finalize-state edits, methodology-analysis-state edits, finalize-phase round contract edits, loop-local `plan.md` backup edits, old or immutable goal-tracker edits, and wrong-round summary/contract edits at `hooks/loop-edit-validator.sh:156-260`. Successful validation reaches `exit 0` at `hooks/loop-edit-validator.sh:265`.
- inputs_outputs_state: Primary input is Claude hook JSON on stdin with fields `tool_name`, `session_id`, and `tool_input.file_path`; goal-tracker edit validation also requires `tool_input.old_string`, `tool_input.new_string`, and optional `tool_input.replace_all` after round 0 at `hooks/loop-edit-validator.sh:204-218`. The hook reads RLCR state from the active loop under `$PROJECT_ROOT/.humanize/rlcr`, using `resolve_project_root` and `find_active_loop`; `resolve_project_root` prioritizes `CLAUDE_PROJECT_DIR`, then `git rev-parse`, and canonicalizes through `realpath` at `hooks/lib/project-root.sh:41-52`. Output is process status plus stderr block messages: `0` allows the edit, `2` blocks expected policy violations, and `1` is used for malformed state or missing required goal-tracker edit fields. State transitions are not performed by this hook; it is a guard that preserves RLCR state-machine ownership. It derives active phase from `resolve_active_state_file`, which prefers `methodology-analysis-state.md`, then `finalize-state.md`, then `state.md` at `hooks/lib/loop-common.sh:268-280`.
- gates_or_invariants: Tool gate: only `Edit` is handled; other tools pass through. Session gate: active-loop discovery is filtered by hook `session_id`; without a session filter it only considers the newest loop directory to avoid reviving stale loops, while with a filter it stops at that session’s newest terminal or active loop at `hooks/lib/loop-common.sh:308-425`. Methodology gate: during methodology analysis, source-code edits are blocked and only the two methodology artifacts are allowed at `hooks/loop-edit-validator.sh:71-113`. RLCR scope gate: paths outside `.humanize/rlcr/` pass through after methodology checks at `hooks/loop-edit-validator.sh:120-122`; the helper is substring-based at `hooks/lib/loop-common.sh:1253-1256`. State invariant: state files are parsed strictly and malformed state blocks operation for safety at `hooks/loop-edit-validator.sh:144-148`; strict parsing requires frontmatter, `current_round`, `max_iterations`, `review_started`, and `base_branch`, and validates numeric fields at `hooks/lib/loop-common.sh:533-575`. File ownership gates: state files are never editable at `hooks/loop-edit-validator.sh:156-169`; finalize mode blocks round contracts at `hooks/loop-edit-validator.sh:171-174`; loop `plan.md` backups are blocked at `hooks/loop-edit-validator.sh:180-188`. Round invariant: summary and contract filenames must match `CURRENT_ROUND` unless allowlisted at `hooks/loop-edit-validator.sh:236-260`. Goal-tracker invariant: only the active loop’s `goal-tracker.md` may be edited, and after round 0 the immutable section must remain byte-equivalent after applying the proposed literal edit preview at `hooks/loop-edit-validator.sh:194-229`.
- dependencies_and_callers: The hook sources `hooks/lib/loop-common.sh` at `hooks/loop-edit-validator.sh:15-17`. Key dependencies include `extract_session_id`, `resolve_active_state_file`, `find_active_loop`, `parse_state_file_strict`, round-file helpers, allowlist helpers, block-message renderers, `preview_edit_result`, and goal-tracker immutable-section comparison in `hooks/lib/loop-common.sh:253-256`, `hooks/lib/loop-common.sh:268-280`, `hooks/lib/loop-common.sh:332-425`, `hooks/lib/loop-common.sh:533-575`, and `hooks/lib/loop-common.sh:790-1031`. Template rendering flows through `load_and_render_safe` in `hooks/lib/template-loader.sh:188`, used by the shared block-message helpers. The direct runtime caller is Claude’s hook dispatcher via `hooks/hooks.json:24-30`. Tests invoke it directly with synthetic hook JSON in `tests/robustness/test-hook-system-robustness.sh:43`, `tests/robustness/test-hook-system-robustness.sh:69`, `tests/test-allowlist-validators.sh:229`, and `tests/test-finalize-phase.sh:425`.
- edge_cases_or_failure_modes: Malformed JSON fails via `jq` under `set -euo pipefail`; robustness tests expect a non-crashing nonzero exit below signal range at `tests/robustness/test-hook-system-robustness.sh:79-90`. Missing `file_path` is tolerated or fails gracefully because the path becomes empty and generally passes non-RLCR checks; tested at `tests/robustness/test-hook-system-robustness.sh:93-105`. Paths outside `.humanize/rlcr` are intentionally delegated to Claude’s sandbox and allowed at `tests/robustness/test-hook-system-robustness.sh:108-121`. Traversal-like paths that still contain `.humanize/rlcr` and target `state.md` are blocked by the state-file basename pattern, tested at `tests/robustness/test-hook-system-robustness.sh:123-154`. Methodology-analysis path handling tries full `realpath`, then parent `realpath` for nonexistent files, then rejects unresolved paths containing `..` before falling back to absolute/raw path construction at `hooks/loop-edit-validator.sh:72-100`. Goal-tracker edit preview depends on `perl`; if `perl` is unavailable or the literal replacement cannot be rendered, the edit is blocked at `hooks/loop-edit-validator.sh:218-225` and `hooks/lib/loop-common.sh:968-991`. The simple `_normalize_path` used for active goal-tracker equality collapses `/./` and `//` but does not canonicalize all traversal or symlinks at `hooks/lib/loop-common.sh:1027-1031`; methodology checks are more explicitly `realpath`-aware. Allowlisted files are exact active-loop path matches only for `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` at `hooks/lib/loop-common.sh:819-837`.
- validation_or_tests: Existing validation covers core hook behavior. `tests/test-allowlist-validators.sh:225-287` verifies edit allowlisting for `round-2-todos.md`, historical `round-1-summary.md`, current `round-5-contract.md`, stale `round-0-contract.md`, and blocked non-allowlisted todos. `tests/test-finalize-phase.sh:422-443` verifies the edit validator blocks `finalize-state.md` and round contracts during finalize phase. `tests/robustness/test-hook-system-robustness.sh:39-77` covers valid edit JSON and state-file blocking; `tests/robustness/test-hook-system-robustness.sh:79-121` covers malformed JSON, missing `file_path`, and out-of-scope paths; `tests/robustness/test-hook-system-robustness.sh:496-536` covers allowed mutable goal-tracker edits/deletions and blocked immutable-section edits. I did not execute the test suite; this was a read-only branch research pass using direct file inspection and repository searches.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 item heading present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`