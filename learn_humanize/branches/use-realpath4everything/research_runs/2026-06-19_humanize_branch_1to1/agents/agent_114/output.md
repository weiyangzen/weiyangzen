# agent_114 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-114 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: User-facing block-message template for the timeout state where an expected GitHub Claude bot acknowledgement never arrives. The file is a small Markdown contract, not executable logic: it defines what the system tells the operator after a retry loop has exhausted without receiving an `eyes` reaction. The title at `prompt-template/block/claude-eyes-timeout.md:1` names the terminal condition, and line 3 binds the message to the retry budget via `{{RETRY_COUNT}}` and `{{TOTAL_WAIT_SECONDS}}`.
- algorithmic_behavior: The implied algorithmic transition is `trigger comment posted / wait for Claude bot reaction -> poll/retry for eyes reaction -> timeout block -> require external repository or PR configuration remediation -> post a new trigger comment to restart`. The template enumerates likely causes at `prompt-template/block/claude-eyes-timeout.md:5-8`, required operator actions at `prompt-template/block/claude-eyes-timeout.md:10-13`, and restart semantics at `prompt-template/block/claude-eyes-timeout.md:18`. It does not itself poll GitHub or evaluate reactions; it is the rendered explanation for the block state.
- inputs_outputs_state: Inputs are two template variables: `RETRY_COUNT` and `TOTAL_WAIT_SECONDS`, both consumed on `prompt-template/block/claude-eyes-timeout.md:3`. The output is Markdown intended for a block/error channel. State is external: a caller must have maintained retry count, accumulated wait time, and observed absence of the required reaction. The template expresses no persistent state mutation. Rendering would normally use the shared template-loader path: `load_template`, `render_template`, `load_and_render`, or `load_and_render_safe` in `hooks/lib/template-loader.sh:33-210`. The renderer performs single-pass `{{VAR}}` replacement, leaving unknown placeholders as-is per `hooks/lib/template-loader.sh:48-56` and `hooks/lib/template-loader.sh:114-121`.
- gates_or_invariants: The operational gate is receipt of an `eyes` reaction from `claude[bot]`; absence after the configured retry budget blocks continuation. The repository/PR invariants implied by the message are: Claude GitHub App installed, bot has repository permissions, and PR is in a state where the bot can respond, as stated in `prompt-template/block/claude-eyes-timeout.md:6-13`. Template-level invariants are valid uppercase placeholder names and non-empty template content. The comprehensive template test extracts placeholders matching uppercase syntax and renders dummy assignments across all templates at `tests/test-templates-comprehensive.sh:575-602`. The shared loader validates that `prompt-template` has required subdirectories including `block` at `hooks/lib/template-loader.sh:213-230`.
- dependencies_and_callers: Direct static caller search for `claude-eyes-timeout.md`, `RETRY_COUNT`, `TOTAL_WAIT_SECONDS`, and the title found only this file, so no literal shell caller is present in the checked tree. The template may be reserved for a dynamic path or an external scheduler/GitHub automation path. General dependencies are the `prompt-template/block` convention and `hooks/lib/template-loader.sh`, whose API loads files by relative template name and renders placeholders. Related evidence for the expected reaction shape exists in `tests/fixtures/comment-reactions.json:1-9`, containing a `claude[bot]` reaction with `"content": "eyes"`. The static template-reference test scans known hook files for literal `load_template` / `load_and_render_safe` calls and warns, rather than fails, when a template is not directly referenced, which allows dynamic templates; see `tests/test-template-references.sh:130-140`.
- edge_cases_or_failure_modes: If the caller omits either variable, the output will contain raw `{{RETRY_COUNT}}` or `{{TOTAL_WAIT_SECONDS}}` because unknown placeholders are preserved by the renderer. If the retry count or wait seconds are inaccurate, the template has no validation layer and will faithfully print bad values. If the bot is installed but delayed beyond the caller’s retry budget, this message can produce a false operational block. If no caller exists in the deployed runtime, the template is inert documentation-like content. If a caller uses `load_and_render_safe` and the template is missing or empty, fallback text would be emitted instead, per `hooks/lib/template-loader.sh:185-210`.
- validation_or_tests: General template loading/rendering is covered by `tests/test-templates-comprehensive.sh`: it verifies the template directory exists, checks `block` subdirectory presence, loads every Markdown template, and attempts rendering all detected uppercase placeholders. Static reference validation in `tests/test-template-references.sh:56-110` checks literal template references in core hooks and `tests/test-template-references.sh:121-140` reports unreferenced templates as warnings. No targeted test was found for this exact timeout template or for a Claude bot retry loop. No tests were run because this task requested read-only research notes only.
- skip_candidate: `yes: this file is a message template rather than executable algorithm code, and no direct static caller was found in the repository. It is still relevant to the core workflow as a block-state contract documenting the timeout transition and remediation path for Claude bot acknowledgement failures.`

## Worker Self-Test
- assigned_items_seen: 1/1 evidence section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`