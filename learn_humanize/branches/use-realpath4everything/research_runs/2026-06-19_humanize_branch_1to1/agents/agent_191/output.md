# agent_191 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-191 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR active-session discovery. The file does not implement the production algorithm itself; it constructs controlled loop directory states and asserts the expected behavior of `find_active_loop` from `hooks/lib/loop-common.sh`. This is core because active-loop lookup gates hook enforcement, cancellation, status display, and protection against reviving stale loop sessions.
- algorithmic_behavior: The test initializes a temporary workspace through `setup_test_dir`, sources production loop helpers, then runs 18 scenario checks. It repeatedly rebuilds `$TEST_DIR/rlcr`, creates immediate child session directories, adds or omits marker files, calls `find_active_loop "$TEST_DIR/rlcr"`, and compares the returned path or empty result. The positive path verifies newest active directory selection, finalize-phase detection, scaling to 15 directories under a 1000 ms budget, and active sessions containing both `state.md` and `finalize-state.md` (`tests/robustness/test-session-robustness.sh:33`, `:48`, `:63`, `:105`). The negative and edge path verifies empty/nonexistent bases, files-only bases, missing state markers, unexpected names, symlinked directories, spaces, non-recursive lookup, rapid creation, terminal-state suppression, and stale older loop suppression when the newest directory is inactive or finished (`:128`, `:140`, `:150`, `:164`, `:179`, `:194`, `:210`, `:224`, `:240`, `:259`, `:273`, `:287`).
- inputs_outputs_state: Inputs are filesystem shapes under a temporary `rlcr` directory plus marker files: `state.md`, `finalize-state.md`, `complete-state.md`, and `cancel-state.md`. The tested production call has no `session_id` filter, so it exercises the no-filter branch of `find_active_loop`. Outputs are either the selected session directory path or an empty string in `RESULT`, plus test counter mutations through `pass`/`fail`. Test state is reset with `rm -rf "$TEST_DIR/rlcr"` between cases; cleanup is registered by `setup_test_dir` in `tests/test-helpers.sh:86`. Final process status comes from `print_test_summary` and `exit $?` at `tests/robustness/test-session-robustness.sh:322`.
- gates_or_invariants: The main invariant is zombie-loop protection: without a session filter, only the single newest immediate child directory is considered, so an older `state.md` must not be revived if the newest directory lacks an active marker or contains only terminal state (`tests/robustness/test-session-robustness.sh:89`, `:296`). Active markers accepted by this test are `state.md` and `finalize-state.md`; terminal-only `complete-state.md` and `cancel-state.md` must return empty (`:259`, `:273`). Lookup is intentionally shallow: a nested `a/b/c/.../state.md` must not be discovered from the base directory (`:224`). The test also encodes tolerances for non-standard names and spaces, and only requires non-empty success for symlinked directories rather than canonical-path equality (`:179`, `:194`, `:210`).
- dependencies_and_callers: Direct dependencies are `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`, sourced at `tests/robustness/test-session-robustness.sh:16` and `:17`. The production implementation under test is `find_active_loop` in `hooks/lib/loop-common.sh:332`; its no-filter branch lists `"$loop_base_dir"/*/`, reverse-sorts, checks only `head -1`, then calls `resolve_active_state_file` (`hooks/lib/loop-common.sh:342`). `resolve_active_state_file` currently accepts `methodology-analysis-state.md`, `finalize-state.md`, and `state.md` (`hooks/lib/loop-common.sh:268`), so this test covers two of the three active markers. Related callers include global cancellation through `scripts/cancel-rlcr-loop.sh:91`, status display logic that mirrors active-loop lookup in `scripts/statusline.sh:65`, and hook validators/stop hooks that call the same library with session-aware filtering, visible in references such as `hooks/loop-codex-stop-hook.sh:62`, `hooks/loop-read-validator.sh:50`, `hooks/loop-write-validator.sh:51`, and `hooks/loop-edit-validator.sh:34`.
- edge_cases_or_failure_modes: Covered edge cases include empty base, missing base, base with only files, active-looking older directory masked by inactive newest directory, all sessions lacking active markers, arbitrary directory names, symlink entries, spaces in names, deep nested state files, rapid directory creation, finished/cancelled loops, and mixed finished-newest plus active-older sessions. Notable gaps: the file header mentions permission changes and deletion races, but there is no chmod-denied case and no concurrent deletion during lookup. The Unicode test label is misleading because it creates `session-test`, not a directory with Unicode characters (`tests/robustness/test-session-robustness.sh:304`). The test does not exercise the session-filter branch, `bg-pending.marker` fallback, terminal newest handling for a specific `session_id`, or `methodology-analysis-state.md`, all of which exist in production `find_active_loop`.
- validation_or_tests: This file is itself the validation asset. It uses helper assertions from `tests/test-helpers.sh:30` and `:35`, accumulates failures, prints a summary, and exits nonzero if any scenario fails (`tests/test-helpers.sh:58`). I inspected the file and related production implementation only; I did not execute the script because this run is research-only and the branch export was provided as read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1 item evidence section present; the assigned item id appears only in its section heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`