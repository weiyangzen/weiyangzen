# agent_185 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-185 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: This executable robustness test is the direct shell-level specification for git status and git repository-state behavior used by the Humanize monitor. It sources production code from [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/humanize.sh:19), then exercises `humanize_parse_git_status` and `humanize_detect_git_state` against temporary repositories. The file is included in the aggregate test runner at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/run-all-tests.sh:106), so it is part of the normal robustness suite rather than a standalone note.
- algorithmic_behavior: The first algorithm under test is `humanize_parse_git_status`, whose contract is `modified|added|deleted|untracked|insertions|deletions` as stated in [tests/robustness/test-git-operations-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-git-operations-robustness.sh:32). The test creates controlled git worktrees, mutates file states, calls the production parser, and uses `parse_result` to select positional fields from the pipe-delimited output at [tests/robustness/test-git-operations-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-git-operations-robustness.sh:36). It validates clean state, untracked files, modified files, staged additions, deleted files, line insertions, line deletions, renamed files as modified, binary file counting, filenames with spaces, many untracked files, simultaneous staged and unstaged changes, detached HEAD parsing, empty repositories, and feature branches. The second algorithm under test is `humanize_detect_git_state`, validated for `normal`, `detached`, `rebase`, `merge`, `shallow`, `not_a_repo`, and permission-error-compatible behavior at [tests/robustness/test-git-operations-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-git-operations-robustness.sh:377).
- inputs_outputs_state: Inputs are synthetic filesystem and git states built under `TEST_DIR`, which is allocated by `setup_test_dir` from [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:86). `init_test_repo` creates a repository with user config, a committed `file.txt`, and a clean baseline at [tests/robustness/test-git-operations-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-git-operations-robustness.sh:50). Outputs are pass/fail counter transitions through `pass`, `fail`, and final `print_test_summary`, with process exit status determined at [tests/robustness/test-git-operations-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-git-operations-robustness.sh:506). Test state transitions are explicit: `git add`, `git reset`, `git checkout`, branch creation, file removal, simulated `.git/rebase-merge`, simulated `.git/MERGE_HEAD`, simulated `.git/shallow`, and temporary `.git` permission changes. The production parser’s monitor-facing output is consumed as six fields in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/humanize.sh:464), so these tests guard status-bar data shape as well as counts.
- gates_or_invariants: The file runs with `set -euo pipefail` at [tests/robustness/test-git-operations-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-git-operations-robustness.sh:13), so unset variables and failing simple commands are hard failures unless explicitly tolerated. The parser invariant is stable pipe-field ordering: modified field 1, added field 2, deleted field 3, untracked field 4, insertions field 5, deletions field 6. Count gates are exact where deterministic, such as clean `0` counts and 20 untracked files, and lower-bound where git diff behavior can vary, such as insertions/deletions `>=3` and renamed/combined states `>=1`. State-detection precedence is also encoded by the production implementation: rebase marker before merge marker, merge before shallow, shallow before detached, then normal, visible in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/humanize.sh:178).
- dependencies_and_callers: Direct dependencies are `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, and `scripts/humanize.sh`, sourced at [tests/robustness/test-git-operations-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-git-operations-robustness.sh:17). `portable-timeout.sh` is sourced but not materially used by this file; its presence aligns the robustness test harness with other tests. Production dependencies inside `humanize_parse_git_status` are `git rev-parse --git-dir`, `git status --porcelain`, and `git diff --shortstat HEAD` fallback behavior at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/humanize.sh:208). A related smoke test in [tests/robustness/test-hook-input-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-hook-input-robustness.sh:410) checks that the same production git parser is callable and pipe-delimited, but this assigned file is the deeper behavioral suite.
- edge_cases_or_failure_modes: Non-git handling expects the parser to return a string containing `not a git repo` at [tests/robustness/test-git-operations-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-git-operations-robustness.sh:168), matching the production non-repo output in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/humanize.sh:210). Empty repositories must produce a non-empty parser result rather than crashing. Permission simulation accepts either `permission_error` or `not_a_repo`, acknowledging platform differences around unreadable `.git` directories at [tests/robustness/test-git-operations-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/robustness/test-git-operations-robustness.sh:478). Cleanup is mostly manual between cases; a missed cleanup could contaminate subsequent count assertions, so each mutation is followed by `rm`, `git checkout`, `git reset`, branch deletion, or marker-file removal. One notable fragility is branch restoration using `master` then `main`, which is intentionally compatible with either default branch name but assumes one exists after init.
- validation_or_tests: The file itself contains 25 numbered tests and exits through `print_test_summary`, which returns nonzero if any `fail` call occurred. It validates production code directly rather than mocking the parser. Key validation clusters are normal operations in tests 1-6, edge cases in tests 7-18, and repository-state detection in tests 19-25. The executable bit is present (`-rwxr-xr-x`) and the file size matches the assignment metadata at 14,275 bytes with 507 lines. I did not run the test suite because this task requested read-only research notes only, and the branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1; the single assigned item section is present exactly once above`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`