# agent_194 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-194 `file` `tests/robustness/test-state-transition-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR loop state transition/discovery behavior. The file does not implement production state transitions; it creates synthetic `.humanize/rlcr/<timestamp>/...-state.md` fixtures and asserts that shared state helpers from `hooks/lib/loop-common.sh` parse, validate, and discover active loop state correctly. It is part of the robustness suite listed by `tests/run-all-tests.sh:101-117`, specifically at `tests/run-all-tests.sh:117`.
- algorithmic_behavior: The script starts with strict Bash mode and sources `hooks/lib/loop-common.sh` plus `tests/test-helpers.sh` at `tests/robustness/test-state-transition-robustness.sh:12-17`. It creates a temporary test root via `setup_test_dir` at line `19`. Local fixture builders define three state shapes: active `state.md` with full frontmatter at lines `30-52`, `finalize-state.md` with `finalize_mode: true` and `review_started: true` at lines `54-72`, and terminal `cancel-state.md` with `cancelled: true` at lines `74-89`. The test cases then exercise round parsing, strict schema validation, active/finalize/cancel discovery, newest-directory selection, lexicographic directory ordering, and one-level state location rules across 16 numbered checks at lines `98-417`.
- inputs_outputs_state: Inputs are generated Markdown state files under temporary directories shaped like `.humanize/rlcr/2026-01-19_00-00-00/state.md`, `finalize-state.md`, and `cancel-state.md`. Outputs are TAP-like human-readable `PASS`/`FAIL` lines through `pass`/`fail` helpers from `tests/test-helpers.sh:30-44`, ending in `print_test_summary` and `exit $?` at `tests/robustness/test-state-transition-robustness.sh:423-424`. State under test is not persistent repo state; all fixtures live under `$TEST_DIR`, which is created by `mktemp -d` and cleaned with a trap in `tests/test-helpers.sh:86-89`. The script reads helper outputs from `parse_state_file_strict`, `get_current_round`, and `find_active_loop`; it does not mutate production `.humanize` loop state.
- gates_or_invariants: Valid active states must have parseable YAML frontmatter and required fields accepted by `parse_state_file_strict`; line `103-112` asserts round `0` is strict-valid. Round extraction must preserve `5` and `42` at lines `120-138`. Finalize state must be considered active by `find_active_loop` when `finalize-state.md` exists at lines `148-158`; a coexistence scenario confirms `finalize-state.md` can sit beside `state.md` at lines `160-172`, reflecting production active-state priority in `resolve_active_state_file` where `finalize-state.md` is checked before `state.md` at `hooks/lib/loop-common.sh:271-276`. Cancel state must not be considered active at lines `182-192`. New active state after an older cancel must be discovered if it is in the newest loop directory at lines `194-207`. Strict schema gates reject non-numeric `current_round`, missing `current_round`, missing `max_iterations`, and missing `base_branch` at lines `260-361`. Directory discovery must select only the newest lexicographic child directory and ignore nested state files at lines `371-417`.
- dependencies_and_callers: Direct dependencies are `hooks/lib/loop-common.sh` and `tests/test-helpers.sh`, sourced at lines `16-17`. Production helpers under test include `resolve_active_state_file`, which prioritizes `methodology-analysis-state.md`, then `finalize-state.md`, then `state.md` at `hooks/lib/loop-common.sh:268-279`; `find_active_loop`, whose no-session path checks only the single lexicographically newest child directory using `ls -1d ... | sort -r | head -1` at `hooks/lib/loop-common.sh:342-356`; `get_current_round`, which extracts `current_round` from YAML frontmatter and defaults to `0` at `hooks/lib/loop-common.sh:430-439`; and `parse_state_file_strict`, which validates separators, required fields, numeric values, and boolean `review_started` at `hooks/lib/loop-common.sh:533-599`. Production callers of these helpers include `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh`, as shown by the references to `find_active_loop` and `parse_state_file_strict` in those files. The robustness file is coordinated with sibling tests such as `tests/robustness/test-concurrent-state-robustness.sh`, `tests/robustness/test-state-file-robustness.sh`, and `tests/test-state-exit-naming.sh`, which cover concurrency, parser tolerance, and terminal-state naming.
- edge_cases_or_failure_modes: The script explicitly documents two permissive parser behaviors: negative rounds are parsed because strict validation accepts `^-?[0-9]+$` at `tests/robustness/test-state-transition-robustness.sh:230-235` and `hooks/lib/loop-common.sh:571-575`, and rounds above `max_iterations` are parsed without enforcement at `tests/robustness/test-state-transition-robustness.sh:238-257`. This means transition legality such as “round must not be negative” or “round must not exceed max” is not enforced by `get_current_round` or `parse_state_file_strict`; enforcement is expected elsewhere. Non-numeric rounds are rejected by the strict parser at lines `260-282`. Missing schema fields are rejected for `current_round`, `max_iterations`, and `base_branch` at lines `292-361`; production strict parsing also requires `review_started` at `hooks/lib/loop-common.sh:562-565`, though this assigned test does not include a dedicated missing-`review_started` case. Directory edge cases include terminal newest directories suppressing active-loop discovery, nested `state.md` ignored because only direct child loop directories are checked, and lexicographic rather than timestamp-validated ordering at lines `385-402`.
- validation_or_tests: This file is itself the validation artifact. It is executable Bash, 424 lines, and uses `print_test_summary` to fail the process when any assertion failed. It belongs to the full test runner’s robustness list at `tests/run-all-tests.sh:101-117`. I did not run it because this worker task requested research notes only and the branch export is read-only; inspection was direct with line-numbered reads. The concrete covered assertions are 16 tests: valid round `0`, valid round `5`, max round `42`, finalize detection, finalize coexistence, cancel inactive, newer active after cancel, negative round parsing, over-max round parsing, non-numeric rejection, missing `current_round`, missing `max_iterations`, missing `base_branch`, newest directory selection, lexicographic ordering, and nested state ignored.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-194`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`