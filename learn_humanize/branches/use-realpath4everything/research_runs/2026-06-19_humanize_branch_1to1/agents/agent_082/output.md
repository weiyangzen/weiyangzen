# agent_082 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-082 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Executable regression specification for Humanize shell safety around protected runtime state. It verifies that `.humanize/` loop artifacts cannot be staged through `git add` path variants, force flags, all/broad-scope flags, chained shell commands, or `git` global options, and it separately verifies zsh-safe directory/file discovery patterns that avoid unmatched glob failures. The file directly sources `hooks/lib/loop-common.sh` at `tests/test-humanize-escape.sh:16-18`, so its primary production target is `git_adds_humanize`.
- algorithmic_behavior: The script initializes strict shell mode with `set -euo pipefail` at `tests/test-humanize-escape.sh:14`, then wraps assertions around `git_adds_humanize`. `assert_blocks` lowercases each candidate command via `to_lower` and expects `git_adds_humanize` to return success for blocked commands at `tests/test-humanize-escape.sh:45-55`; `assert_allows` expects the inverse at `tests/test-humanize-escape.sh:59-69`. Production `to_lower` is a simple `tr` wrapper in `hooks/lib/loop-common.sh:790-793`. Production `git_adds_humanize` splits commands on shell operators, detects segments containing `git ... add`, extracts arguments after `add`, strips quotes for path matching, blocks direct `.humanize` path references, detects force/all/broad-scope flags, and conditionally blocks all/broad adds when `.humanize` exists at `hooks/lib/loop-common.sh:1280-1376`.
- inputs_outputs_state: Inputs are literal shell command strings, temporary test directories under `/tmp`, and the current working directory during `git_adds_humanize` checks. Outputs are colored PASS/FAIL lines and process exit status: zero only when `TESTS_FAILED` remains zero at `tests/test-humanize-escape.sh:349-356`. Test state is maintained in integer counters `TESTS_PASSED` and `TESTS_FAILED` at `tests/test-humanize-escape.sh:25-37`. The `git add -A` group creates `/tmp/test-humanize-git-add-$$/.humanize`, changes into that directory, verifies `.humanize`-existence-gated blocking, returns to the original directory, and removes the fixture at `tests/test-humanize-escape.sh:133-149`. The zsh-safety groups create and remove separate `/tmp/test-humanize-*` fixtures at `tests/test-humanize-escape.sh:209-299` and `tests/test-humanize-escape.sh:308-337`.
- gates_or_invariants: Direct `.humanize` references must block when the path appears as `.humanize`, `./.humanize`, nested `path/to/.humanize`, parent-relative `../project/.humanize`, or `.humanize/rlcr/...` at `tests/test-humanize-escape.sh:83-90`. Quoted forms must normalize and block at `tests/test-humanize-escape.sh:99-104`. Force with `.humanize`, force with broad scope, and combined flags like `-fA`/`-Af` must block at `tests/test-humanize-escape.sh:113-124`. `-A`/`--all` must block only under a fixture where `.humanize` exists, matching the production gate `[[ ! -d ".humanize" ]] && continue` at `hooks/lib/loop-common.sh:1355-1358` and the all-flag return at `hooks/lib/loop-common.sh:1360-1364`. Non-add git commands and unrelated files that merely start with `.humanize` must not block at `tests/test-humanize-escape.sh:180-197`, preserving the production regex boundary that avoids over-blocking `.humanizeconfig` and `.humanize-backup` at `hooks/lib/loop-common.sh:1317-1323`.
- dependencies_and_callers: The test depends on Bash, `tr`, `sed`, `grep`, `find`, `mkdir`, `touch`, `rm`, and `basename`, plus `hooks/lib/loop-common.sh` for `to_lower` and `git_adds_humanize`. `loop-common.sh` itself is a shared hook library used by validators and setup/cancel/stop-hook scripts, documented at `hooks/lib/loop-common.sh:5-13`. The `.humanize` staging protection connects to the user-facing blocked message in `prompt-template/block/git-add-humanize.md` and the production message function at `hooks/lib/loop-common.sh:1378-1416`. The zsh-safe iteration examples mirror production monitor helpers: `monitor_find_latest_session` uses `find "$loop_dir" -mindepth 1 -maxdepth 1 -type d` instead of globs at `scripts/lib/monitor-common.sh:37-63`; `monitor_find_state_file` uses `find "$session_dir" -maxdepth 1 -name '*-state.md' -type f` at `scripts/lib/monitor-common.sh:147-192`; `_find_latest_codex_log` in `scripts/humanize.sh` uses `find` with explicit log filename predicates at `scripts/humanize.sh:338-362`.
- edge_cases_or_failure_modes: The test covers shell command segmentation through `&&` and `;` at `tests/test-humanize-escape.sh:158-160`, and `git -C`/`--git-dir` global options before `add` at `tests/test-humanize-escape.sh:169-171`, which map to production comments and matching behavior at `hooks/lib/loop-common.sh:1265-1305`. It checks patch mode is allowed at `tests/test-humanize-escape.sh:189-192`; this is important because production force/all detection treats lowercased `-a` as all but should not treat `-p` as broad staging. The test’s zsh safety section exercises empty directories, directories containing only dotfiles, absence of `*-state.md`, positive state detection, and timestamped session selection at `tests/test-humanize-escape.sh:209-337`. One untested production branch is broad `git add .` without force when `.humanize` exists and is not gitignored, handled by `git check-ignore -q .humanize` at `hooks/lib/loop-common.sh:1366-1371`; this file only asserts force broad-scope blocking and all-flag blocking.
- validation_or_tests: This file is itself a shell test. Its validation oracle is return-code based: every assertion increments pass/fail counters, then the script exits `0` only if no failures occurred at `tests/test-humanize-escape.sh:342-356`. It validates the core `.humanize` protection behavior against production `git_adds_humanize` and validates find-based monitor iteration patterns by simulation. I did not execute the test because this task requested research notes only and the branch export is read-only; direct inspection was sufficient to characterize its algorithmic role.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`