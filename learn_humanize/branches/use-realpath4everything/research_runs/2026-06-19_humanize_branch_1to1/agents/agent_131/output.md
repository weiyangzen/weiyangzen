# agent_131 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-131 `file` `prompt-template/block/mainline-verdict-missing.md`
- cursor: `[_]`
- core_role: This file is a stop-hook block-message template for the “implementation review missing mainline verdict” safety gate. It is not executable code, but it defines the human/system-facing remediation contract used when Codex review output omits the required `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED` line. The assigned file states the missing required line at `prompt-template/block/mainline-verdict-missing.md:3-5`, explains that Humanize cannot safely update drift state or choose the next-round prompt without it at `:7`, and names the relevant review artifacts via placeholders at `:11-13`.

- algorithmic_behavior: The template is consumed by `block_missing_mainline_verdict()` in `hooks/loop-codex-stop-hook.sh:1421-1454`. That function renders `block/mainline-verdict-missing.md` with `REVIEW_RESULT_FILE` and `REVIEW_PROMPT_FILE`, wraps the rendered Markdown in a JSON response with `"decision": "block"`, and emits the system message `Loop: Blocked - implementation review missing Mainline Progress Verdict` at `hooks/loop-codex-stop-hook.sh:1445-1452`. The gate is reached after the stop hook reads the review result, extracts the last non-empty marker line, and calls `extract_mainline_progress_verdict` when review mode has not started (`hooks/loop-codex-stop-hook.sh:1770-1788`). If the last marker is not `STOP` and the extracted verdict is `unknown`, the hook blocks immediately through this template path (`hooks/loop-codex-stop-hook.sh:1788-1790`).

- inputs_outputs_state: Inputs are the rendered placeholder values `{{REVIEW_RESULT_FILE}}` and `{{REVIEW_PROMPT_FILE}}` from `prompt-template/block/mainline-verdict-missing.md:11-13`, passed by the caller at `hooks/loop-codex-stop-hook.sh:1441-1443`. Template rendering uses `load_and_render_safe`, which loads the template and falls back if missing or empty (`hooks/lib/template-loader.sh:186-207`). Variable substitution is single-pass to avoid placeholder injection or accidental recursive substitution (`hooks/lib/template-loader.sh:49-55`, `:69-83`). Output is a Markdown reason body embedded into the stop-hook JSON block decision. State transition is intentionally prevented: the missing-verdict path blocks before writing new drift counters or generating a next prompt. The normal state machine, when a verdict exists, resets `mainline_stall_count` on `advanced`, increments it on `stalled` or `regressed`, sets `drift_status` to `replan_required` after repeated stalls, and eventually triggers a drift stop (`hooks/loop-codex-stop-hook.sh:1793-1810`). The missing-verdict gate avoids entering that ambiguous transition.

- gates_or_invariants: The core invariant is that implementation review feedback must contain exactly one parseable verdict value from `ADVANCED`, `STALLED`, or `REGRESSED`; otherwise drift state and next-round prompt choice are unsafe. The extractor accepts a line matching `Mainline Progress Verdict:` followed by one of those three words (`hooks/lib/loop-common.sh:634-642`), normalizes the verdict to lowercase enum values (`hooks/lib/loop-common.sh:602-615`), and rejects ambiguous placeholder-style lines containing multiple verdict keywords (`hooks/lib/loop-common.sh:648-659`). Optional drift state fields default to `mainline_stall_count=0`, `last_mainline_verdict=unknown`, and `drift_status=normal` when absent (`hooks/lib/loop-common.sh:589-598`). The review prompt templates themselves declare the verdict mandatory: regular review specifies the one-line verdict and says omission will cause the stop hook to block (`prompt-template/codex/regular-review.md:56-61`), and full alignment review repeats the same mandatory drift-audit contract (`prompt-template/codex/full-alignment-review.md:59-66`).

- dependencies_and_callers: Direct caller is `block_missing_mainline_verdict()` in `hooks/loop-codex-stop-hook.sh:1421-1454`. The branch path depends on `extract_mainline_progress_verdict()` from `hooks/lib/loop-common.sh:634-662`, the shared renderer `load_and_render_safe()` from `hooks/lib/template-loader.sh:186-207`, and the Codex review-result lifecycle in `hooks/loop-codex-stop-hook.sh:1770-1790`. Upstream prompt dependencies are `prompt-template/codex/regular-review.md` and `prompt-template/codex/full-alignment-review.md`, which instruct Codex to output the mandatory verdict line. Sibling block template `prompt-template/block/mainline-drift-stop.md` handles the later circuit-breaker case once valid `stalled` or `regressed` verdicts have accumulated, but this assigned template handles the earlier “cannot classify the review result” failure.

- edge_cases_or_failure_modes: If the review result omits the verdict line, uses an unsupported verdict, or leaves the placeholder-style `ADVANCED / STALLED / REGRESSED` line intact, extraction returns `unknown` and this template blocks the exit. If the final marker is `STOP`, the hook exempts the missing-verdict block through the `LAST_LINE_TRIMMED != "$MARKER_STOP"` guard (`hooks/loop-codex-stop-hook.sh:1788`). If Codex outputs `COMPLETE`, the hook forces mainline state to advanced and resets drift tracking (`hooks/loop-codex-stop-hook.sh:1817-1823`). If this template file is absent or renders empty, `load_and_render_safe()` uses the inline fallback with the same semantics (`hooks/loop-codex-stop-hook.sh:1427-1439`, `hooks/lib/template-loader.sh:197-207`). The assigned template’s line `Humanize cannot safely update the drift state or choose the correct next-round prompt without this verdict` (`prompt-template/block/mainline-verdict-missing.md:7`) is the key failure-mode rationale.

- validation_or_tests: Negative coverage exists in `tests/test-finalize-phase.sh`. Test `T-NEG-10a` creates review feedback without `Mainline Progress Verdict`, runs the stop hook, and expects a JSON block decision mentioning the verdict (`tests/test-finalize-phase.sh:948-979`). It then asserts the missing-verdict path does not generate `round-4-prompt.md` (`tests/test-finalize-phase.sh:982-986`) and preserves prior drift state as `round=3`, `mainline_stall_count=1`, `last_mainline_verdict=stalled`, `drift_status=normal` (`tests/test-finalize-phase.sh:988-994`). Positive/adjacent tests show valid verdicts drive normal continuation and drift handling, including `ADVANCED` in `tests/test-agent-teams.sh:597-606` and `STALLED` in `tests/test-agent-teams.sh:638-647`.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 of 1 Item Evidence headings`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`