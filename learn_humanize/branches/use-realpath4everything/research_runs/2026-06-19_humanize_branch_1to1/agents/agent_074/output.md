# agent_074 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-074 `file` `tests/test-commit-history-section.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR stop-hook “Integral Context” prompt section. The test validates that commit history and recent round references are rendered into the Codex review prompt correctly, including degraded modes when `BASE_COMMIT` cannot be trusted. It directly mirrors production logic in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:997) through line 1026.
- algorithmic_behavior: The script is a Bash test with `set -euo pipefail` at [tests/test-commit-history-section.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-commit-history-section.sh:12), so each scenario also proves the algorithm avoids unhandled command failures. It resolves `SCRIPT_DIR`, `PROJECT_ROOT`, sources shared test helpers and the template loader at lines 14-17, then creates an isolated temporary Git repo at lines 29-30. Test 1 sets `CURRENT_ROUND=0`, computes `git log "$BASE_COMMIT"..HEAD`, expects empty history to become `(no commits yet)`, and expects no prior round files to become `(first round, no prior history)` at lines 37-57. Test 2 creates three commits, renders the real `prompt-template/codex/commit-history-section.md`, and asserts all commit messages plus round 2, 1, and 0 summary/review references appear at lines 69-114. Tests 3 and 3b model the production gate: only use a base revision range when `git merge-base --is-ancestor "$BASE_COMMIT" HEAD` succeeds; otherwise render the last 30 commits with an annotation at lines 124-132 and 164-172. Test 4 forces a missing-template path and verifies the fallback section includes the development-history heading, fenced commit log, recent round files, and read directive at lines 196-221. Test 5 verifies the recent-round loop boundary for `CURRENT_ROUND=1`, ensuring only round 0 is referenced at lines 230-243. Test 6 validates the legacy empty `BASE_COMMIT` path also receives the fallback annotation at lines 252-265.
- inputs_outputs_state: Inputs are a temporary Git repository, `CURRENT_ROUND`, `BASE_COMMIT` or invalid substitutes, `LOOP_TIMESTAMP`, the template directory, and the rendered variables `COMMIT_HISTORY` and `RECENT_ROUND_FILES`. Output is not a persisted artifact; it is pass/fail test reporting through `pass`, `fail`, and `print_test_summary` from [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:30). State transitions are local shell variables: `COMMIT_HISTORY` moves from raw `git log` output to either the no-commit placeholder, annotated fallback log, or recent branch log; `RECENT_ROUND_FILES` moves from empty to a bounded list of round summary/review markdown references, or to the first-round placeholder. Git state transitions inside the temp repo are initial commit, three feature/fix commits, then an orphan branch commit used to prove unrelated commits are rejected as a base.
- gates_or_invariants: The key invariant is that a non-empty `BASE_COMMIT` is insufficient by itself; it must be an ancestor of `HEAD` before the algorithm uses `BASE_COMMIT..HEAD`. That is asserted by the dead SHA case at lines 122-147 and the valid-but-unrelated orphan commit case at lines 155-179. Commit history ordering is oldest-first through `git log --reverse`, matching production at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:1000). Normal base-range output is capped with `tail -80`; fallback output is capped to `-30` recent branch commits. Recent round references are bounded to at most the previous three rounds by `r >= CURRENT_ROUND - 3` and never below 0, as shown at lines 46-51, 80-86, and 230-237. Missing templates must not erase the full section; `load_and_render_safe` must render the supplied fallback instead.
- dependencies_and_callers: The test depends on `tests/test-helpers.sh` for temp repo setup, Git identity setup, pass/fail counters, and cleanup. It depends on `hooks/lib/template-loader.sh`, specifically `load_and_render_safe`, which loads a template, performs single-pass `{{VAR}}` substitution, and emits a fallback when a template is missing or renders empty. It uses `prompt-template/codex/commit-history-section.md`, whose placeholders are `{{COMMIT_HISTORY}}` and `{{RECENT_ROUND_FILES}}` at lines 5 and 10. The production caller is the Codex stop hook section that builds `COMMIT_HISTORY_SECTION` before embedding it into full-alignment or regular review prompt templates, visible around [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:1024). The run setup captures `base_commit` earlier in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/setup-rlcr-loop.sh:810), which is the upstream state consumed by the hook.
- edge_cases_or_failure_modes: Covered edge cases include no commits since loop start, first round with no prior history, normal later round with multiple commits, corrupted/nonexistent base SHA, valid but unrelated base commit from an orphan branch, missing template file, round-1 lower boundary, and empty legacy base commit. A subtle failure mode guarded here is Bash `set -e` termination from `git log BAD..HEAD`; the test avoids that by requiring `merge-base --is-ancestor` before using the range and explicitly records “did not crash” at line 147. Another covered failure mode is a missing prompt template silently reducing context; Test 4 requires the fallback to still contain both commit history and round-file navigation.
- validation_or_tests: This file is itself the validation asset. It statically verifies the exact algorithm used in production rather than invoking the whole stop hook. I inspected it directly and cross-checked against production lines 997-1026, the template loader, and the commit-history template. I did not execute the test in this read-only branch export: even read-oriented `git` inspection attempted to create cache/temp files outside the permitted filesystem and failed with sandbox permission errors, so running a test that creates temp Git repos would not be reliable in this environment.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 evidence heading present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`