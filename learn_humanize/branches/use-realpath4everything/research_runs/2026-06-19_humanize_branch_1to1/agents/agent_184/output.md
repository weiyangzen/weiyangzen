# agent_184 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-184 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR loop state-file algorithms under parse, discovery, concurrency, stale-loop, and filesystem-error conditions. The file is a 504-line executable shell test, marked executable at `-rwxr-xr-x`, and is included in the aggregate suite by `tests/run-all-tests.sh:101-117`, specifically `tests/run-all-tests.sh:114`.
- algorithmic_behavior: The script initializes a disposable test directory via `setup_test_dir` from `tests/test-helpers.sh` at `tests/robustness/test-concurrent-state-robustness.sh:17-19`, then sources the production loop library at `tests/robustness/test-concurrent-state-robustness.sh:72`. Its helper `create_state_file` builds minimal YAML-frontmatter state documents with `current_round`, iteration limits, branches, and Codex settings at `tests/robustness/test-concurrent-state-robustness.sh:30-50`. The test then exercises three production algorithms from `hooks/lib/loop-common.sh`: `get_current_round`, `find_active_loop`, and `parse_state_file_strict`. It validates tolerant parsing behavior in `tests/robustness/test-concurrent-state-robustness.sh:59-160`; active-loop selection and terminal-state handling in `tests/robustness/test-concurrent-state-robustness.sh:170-240` and `397-429`; concurrent read/write behavior in `tests/robustness/test-concurrent-state-robustness.sh:250-322`; strict state rejection in `tests/robustness/test-concurrent-state-robustness.sh:332-371`; and numeric/permission edge cases in `tests/robustness/test-concurrent-state-robustness.sh:439-497`.
- inputs_outputs_state: Inputs are synthetic state trees under `$TEST_DIR`, including `state.md`, `finalize-state.md`, and `cancel-state.md`, plus loop-base directories named with sortable timestamps such as `2026-01-20_00-00-00`. The script’s expected outputs are pass/fail counters emitted through `pass`, `fail`, and `print_test_summary` from `tests/test-helpers.sh:30-78`; its final process exit is the summary return at `tests/robustness/test-concurrent-state-robustness.sh:503-504`. The tested production state transition model distinguishes active states from terminal states: `resolve_active_state_file` accepts `methodology-analysis-state.md`, `finalize-state.md`, or `state.md` in that priority order at `hooks/lib/loop-common.sh:268-280`; `resolve_any_state_file` also falls back to terminal `*-state.md` files at `hooks/lib/loop-common.sh:287-305`. The test checks that `finalize-state.md` remains active at `tests/robustness/test-concurrent-state-robustness.sh:203-219`, while `cancel-state.md` is terminal and not active at `tests/robustness/test-concurrent-state-robustness.sh:413-429`.
- gates_or_invariants: The main invariants are: `get_current_round` should default safely to `0` on absent or unreadable parse content, while preserving valid numeric values, as implemented at `hooks/lib/loop-common.sh:430-440`; frontmatter extraction depends on exact `---` separator lines, which the CRLF test calls out at `tests/robustness/test-concurrent-state-robustness.sh:100-121`; `find_active_loop` without a session filter must inspect only the newest timestamp directory to avoid reviving stale older loops, documented and implemented at `hooks/lib/loop-common.sh:308-357`; directories without active state files are ignored or return empty rather than falling back to older active state, asserted at `tests/robustness/test-concurrent-state-robustness.sh:186-201` and `397-411`; concurrent readers must all recover the non-default value `5`, preventing false success masked by default `0`, at `tests/robustness/test-concurrent-state-robustness.sh:250-277`; read-during-write safety assumes atomic writer behavior through temp-file plus `mv` at `tests/robustness/test-concurrent-state-robustness.sh:285-295`, and requires all 20 sampled reads to be numeric at `tests/robustness/test-concurrent-state-robustness.sh:300-322`.
- dependencies_and_callers: Direct dependencies are Bash, `sed`, `grep`, `awk`, `sort`, `ls`, `wc`, `tr`, `seq`, `sleep`, `mv`, `chmod`, `id`, and `mktemp`. Test harness dependency is `tests/test-helpers.sh`, which provides counters, pass/fail reporting, temp-directory setup, and cleanup trap at `tests/test-helpers.sh:22-89`. Production dependency is `hooks/lib/loop-common.sh`; that library sources `hooks/lib/project-root.sh` at `hooks/lib/loop-common.sh:176-178`, then uses config/template loader setup before function definitions. The branch’s realpath theme is visible in `hooks/lib/project-root.sh`, where project roots are resolved through `canonicalize_path` and `realpath` to prevent symlink-prefix divergence at `hooks/lib/project-root.sh:14-20` and `41-53`; the target test itself computes `SCRIPT_DIR` and `PROJECT_ROOT` using `cd ... && pwd` at `tests/robustness/test-concurrent-state-robustness.sh:15-16`, then calls the production library by stable path. Production callers of the tested functions include hook validators such as `hooks/loop-codex-stop-hook.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-bash-validator.sh`, which rely on active loop detection and strict parsing before allowing or blocking operations.
- edge_cases_or_failure_modes: Covered parsing edge cases include trailing whitespace around frontmatter, leading whitespace before `current_round`, CRLF separators, empty `current_round`, inline comments, Unicode body content, very large round numbers, zero, and unreadable state files at `tests/robustness/test-concurrent-state-robustness.sh:59-160`, `373-387`, and `439-497`. Discovery edge cases include non-existent loop base, empty loop base, newest directory with no `state.md`, older stale state blocked by a newer inactive directory, active `finalize-state.md`, and terminal `cancel-state.md` at `tests/robustness/test-concurrent-state-robustness.sh:170-240` and `397-429`. A subtle validation limitation is that Test 13 labels the input as malformed YAML, but `parse_state_file_strict` is not a full YAML parser; it uses frontmatter regex extraction and field checks at `hooks/lib/loop-common.sh:541-589`. The test only requires non-zero rejection at `tests/robustness/test-concurrent-state-robustness.sh:342-350`, so failure may come from missing required fields or nonnumeric `current_round`, not YAML grammar parsing. Test numbering jumps from 15 to 18 at `tests/robustness/test-concurrent-state-robustness.sh:373-397`; this is harmless for execution but worth noting for traceability.
- validation_or_tests: This file is itself the validation artifact. It ends with `print_test_summary "Concurrent State Access Robustness Test Summary"` and exits with the summary status at `tests/robustness/test-concurrent-state-robustness.sh:503-504`. It is also wired into the repository-wide shell suite by `tests/run-all-tests.sh:114`. I did not execute it in this worker pass because the branch export and tool sandbox are read-only, while the script intentionally creates temp directories, writes state files, performs background writes, and changes file permissions.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-184`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`