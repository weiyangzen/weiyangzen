# agent_032 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-032 `file` `commands/refine-plan.md`
- cursor: `[_]`
- core_role: `commands/refine-plan.md` is the command-level workflow definition for refining an annotated `gen-plan` implementation plan into a comment-free refined plan plus a QA ledger. It is core algorithm material because it defines the ordered state machine, validation gates, comment parsing semantics, classification rules, plan rewrite invariants, QA output contract, optional translation behavior, and atomic write protocol for plan/RLCR-adjacent preparation. Its frontmatter exposes the command purpose, CLI shape, and allowed tools, including the validator script and `AskUserQuestion` for discussion mode (`commands/refine-plan.md:1`, `commands/refine-plan.md:3`, `commands/refine-plan.md:4`, `commands/refine-plan.md:11`). It explicitly constrains the command to planning artifacts only and forbids source implementation, unrelated writes, automatic RLCR startup, and schema invention (`commands/refine-plan.md:19`, `commands/refine-plan.md:21`, `commands/refine-plan.md:23`, `commands/refine-plan.md:28`).

- algorithmic_behavior: The file defines a strict sequential pipeline: parse execution mode, load merged config, validate IO, extract comments, classify comments, process them, generate the refined plan, generate QA, and write outputs atomically (`commands/refine-plan.md:30`, `commands/refine-plan.md:32`, `commands/refine-plan.md:34`). Phase 0 parses `--input`, optional `--output`, optional `--qa-dir`, optional `--alt-language`, and mutually exclusive `--discussion`/`--direct`, then derives in-place mode and the QA path from the input basename rather than the output basename (`commands/refine-plan.md:46`, `commands/refine-plan.md:50`, `commands/refine-plan.md:57`, `commands/refine-plan.md:65`, `commands/refine-plan.md:68`). Phase 0.5 reuses `config-loader.sh` semantics to merge default, user, and project config layers and resolve `alternative_plan_language` and `gen_plan_mode` without creating a refine-specific config model (`commands/refine-plan.md:86`, `commands/refine-plan.md:88`, `commands/refine-plan.md:92`, `commands/refine-plan.md:104`). Mode resolution prioritizes CLI flags, then valid config, then discussion default (`commands/refine-plan.md:109`). Alternative language resolution prioritizes CLI, then config, normalizes supported language names/codes, treats English as no-op, rejects unsupported CLI values, and warns/disables unsupported config values (`commands/refine-plan.md:118`, `commands/refine-plan.md:126`, `commands/refine-plan.md:140`, `commands/refine-plan.md:145`). Phase 1 shells out to `${CLAUDE_PLUGIN_ROOT}/scripts/validate-refine-plan-io.sh`, intentionally excluding `--alt-language` because the validator does not accept it (`commands/refine-plan.md:157`, `commands/refine-plan.md:159`, `commands/refine-plan.md:162`). Phase 2 defines a stateful scanner rather than regex extraction; it tracks code fences, HTML comments, active comment block state, and nearest heading (`commands/refine-plan.md:184`, `commands/refine-plan.md:186`, `commands/refine-plan.md:190`). The scanner supports `CMT:/ENDCMT`, `<cmt></cmt>`, and `<comment></comment>` in inline and multiline forms, ignores markers inside fences or HTML comments, assigns `CMT-N` IDs only to non-empty blocks, preserves surrounding inline text, and emits both extracted comment metadata and `PLAN_WITH_COMMENTS_REMOVED` (`commands/refine-plan.md:199`, `commands/refine-plan.md:203`, `commands/refine-plan.md:223`, `commands/refine-plan.md:226`, `commands/refine-plan.md:227`, `commands/refine-plan.md:265`). Phase 3 classifies every raw comment as exactly one of `question`, `change_request`, or `research_request`, splits multi-intent raw comments into deterministic sub-items `CMT-N.1`, `CMT-N.2`, and uses dominant precedence `research_request > change_request > question` for the ledger (`commands/refine-plan.md:276`, `commands/refine-plan.md:282`, `commands/refine-plan.md:296`, `commands/refine-plan.md:301`). Phase 4 processes comments in document order: questions are answered with only minimal clarifying edits, change requests mutate the plan while preserving schema consistency, and research requests use only read/search tools before integrating evidence or adding pending decisions (`commands/refine-plan.md:332`, `commands/refine-plan.md:336`, `commands/refine-plan.md:351`, `commands/refine-plan.md:368`, `commands/refine-plan.md:378`). Phase 5 applies accepted refinements to comment-removed text, preserving required `gen-plan` sections, optional sections, identifiers, routing tags, language, and cross references (`commands/refine-plan.md:395`, `commands/refine-plan.md:399`, `commands/refine-plan.md:401`, `commands/refine-plan.md:413`, `commands/refine-plan.md:419`). Phase 6 populates the QA template with exactly one ledger row per raw comment and section-specific details (`commands/refine-plan.md:457`, `commands/refine-plan.md:459`, `commands/refine-plan.md:473`). Phase 7 prepares all outputs in memory, writes temp files in destination directories, replaces auxiliary outputs before the main in-place plan, and reports final paths, counts, mode, and convergence status (`commands/refine-plan.md:508`, `commands/refine-plan.md:548`, `commands/refine-plan.md:553`, `commands/refine-plan.md:563`, `commands/refine-plan.md:574`).

- inputs_outputs_state: Inputs are CLI arguments, the annotated `gen-plan` file, merged config values, and optionally user decisions in discussion mode. Required input is `--input`; omitted `--output` means in-place rewrite, omitted `--qa-dir` defaults to `.humanize/plan_qa`, and `--discussion`/`--direct` are mutually exclusive (`commands/refine-plan.md:50`, `commands/refine-plan.md:59`, `commands/refine-plan.md:60`, `commands/refine-plan.md:61`, `commands/refine-plan.md:63`). Config inputs are loaded with the same three-layer merge order implemented by `scripts/lib/config-loader.sh`: required default config, optional user config, optional project config (`commands/refine-plan.md:92`, `scripts/lib/config-loader.sh:77`, `scripts/lib/config-loader.sh:78`, `scripts/lib/config-loader.sh:84`, `scripts/lib/config-loader.sh:113`). The default config sets `alternative_plan_language` to empty and `gen_plan_mode` to `discussion` (`config/default_config.json:6`, `config/default_config.json:7`). Internal state variables called out by the workflow include `INPUT_FILE`, `OUTPUT_FILE`, `QA_DIR`, mode booleans, `IN_PLACE_MODE`, `QA_FILE`, `ORIGINAL_PLAN_TEXT`, `EXTRACTED_COMMENTS`, `PLAN_WITH_COMMENTS_REMOVED`, `REFINED_PLAN_TEXT`, `QA_TEXT`, optional translated variants, and final convergence status (`commands/refine-plan.md:48`, `commands/refine-plan.md:67`, `commands/refine-plan.md:179`, `commands/refine-plan.md:269`, `commands/refine-plan.md:397`, `commands/refine-plan.md:548`, `commands/refine-plan.md:502`). Outputs are the main refined plan, the main QA document, optional language variants with `_<code>` inserted before the extension or appended for extensionless paths, and a final report (`commands/refine-plan.md:514`, `commands/refine-plan.md:519`, `commands/refine-plan.md:524`, `commands/refine-plan.md:529`, `commands/refine-plan.md:574`). The QA output path is derived from the input basename, so `docs/my-plan.md` becomes `<QA_DIR>/my-plan-qa.md` regardless of output filename (`commands/refine-plan.md:68`, `commands/refine-plan.md:69`). The QA template requires `Summary`, `Comment Ledger`, `Answers`, `Research Findings`, `Plan Changes Applied`, `Remaining Decisions`, and `Refinement Metadata` (`prompt-template/plan/refine-plan-qa-template.md:1`, `prompt-template/plan/refine-plan-qa-template.md:3`, `prompt-template/plan/refine-plan-qa-template.md:7`, `prompt-template/plan/refine-plan-qa-template.md:19`, `prompt-template/plan/refine-plan-qa-template.md:38`, `prompt-template/plan/refine-plan-qa-template.md:60`, `prompt-template/plan/refine-plan-qa-template.md:86`, `prompt-template/plan/refine-plan-qa-template.md:108`).

- gates_or_invariants: Hard invariant: planning-only writes are limited to the refined plan, QA document, and optional translated variants (`commands/refine-plan.md:19`, `commands/refine-plan.md:23`). The workflow itself is sequential and must not parallelize across phases (`commands/refine-plan.md:32`). CLI invariants reject missing `--input`, missing `--alt-language` value, and simultaneous `--discussion` and `--direct` (`commands/refine-plan.md:57`, `commands/refine-plan.md:59`, `commands/refine-plan.md:62`, `commands/refine-plan.md:63`). Scope invariants forbid adding `--language`, `--qa-output`, new config keys, or auto-starting RLCR (`commands/refine-plan.md:78`). Validator exit codes are mapped to specific stop messages for missing input, empty input, no comment blocks, missing required schema sections, output directory failure, QA directory failure, and invalid arguments (`commands/refine-plan.md:165`). The executable validator enforces these gates with real exit codes 1-7, path normalization via `realpath -m`, required section checks outside ignored regions, output/input directory writeability for new-file or in-place mode, QA directory creation/writeability, and success mode reporting (`scripts/validate-refine-plan-io.sh:4`, `scripts/validate-refine-plan-io.sh:561`, `scripts/validate-refine-plan-io.sh:573`, `scripts/validate-refine-plan-io.sh:589`, `scripts/validate-refine-plan-io.sh:608`, `scripts/validate-refine-plan-io.sh:639`, `scripts/validate-refine-plan-io.sh:662`, `scripts/validate-refine-plan-io.sh:680`). Comment extraction invariants include ignoring fences/HTML comments, rejecting nested starts, rejecting stray or wrong end markers, rejecting EOF while in a comment block, skipping empty comments from the ledger, and stopping if no non-empty comments remain (`commands/refine-plan.md:223`, `commands/refine-plan.md:244`, `commands/refine-plan.md:248`, `commands/refine-plan.md:272`). Plan validation before QA requires required sections, no comment markers, valid `AC-*` references, valid task dependencies, exactly one valid routing tag per task, and agreement between pending decisions and convergence status (`commands/refine-plan.md:442`). Atomic write invariants require preparing every output first, deleting temps and preserving finals on pre-finalization failure, replacing primary in-place plan last, and reporting partial-finalization risk if final replacement fails (`commands/refine-plan.md:546`, `commands/refine-plan.md:559`, `commands/refine-plan.md:563`, `commands/refine-plan.md:564`, `commands/refine-plan.md:565`).

- dependencies_and_callers: Direct dependency is `${CLAUDE_PLUGIN_ROOT}/scripts/validate-refine-plan-io.sh`, allowlisted in frontmatter and invoked during Phase 1 (`commands/refine-plan.md:5`, `commands/refine-plan.md:159`). The command also depends on `${CLAUDE_PLUGIN_ROOT}/scripts/lib/config-loader.sh` semantics for merged config, without necessarily exposing a separate script call in the command file (`commands/refine-plan.md:88`; implementation semantics in `scripts/lib/config-loader.sh:63`). It depends on `${CLAUDE_PLUGIN_ROOT}/config/default_config.json` for baseline `alternative_plan_language` and `gen_plan_mode` (`commands/refine-plan.md:94`, `config/default_config.json:6`). It depends on `${CLAUDE_PLUGIN_ROOT}/prompt-template/plan/refine-plan-qa-template.md` for mandatory QA population (`commands/refine-plan.md:459`). The hidden flow skill `skills/humanize-refine-plan/SKILL.md` mirrors this command as an installer-hydrated workflow: parse args/config, validate IO, extract/classify/process comments, validate plan, populate QA, optionally translate, atomically write, and report success (`skills/humanize-refine-plan/SKILL.md:18`, `skills/humanize-refine-plan/SKILL.md:20`, `skills/humanize-refine-plan/SKILL.md:22`, `skills/humanize-refine-plan/SKILL.md:26`, `skills/humanize-refine-plan/SKILL.md:30`, `skills/humanize-refine-plan/SKILL.md:41`, `skills/humanize-refine-plan/SKILL.md:45`). User-facing docs describe `/humanize:refine-plan` with the same arguments, comment formats, QA structure, and variant naming (`docs/usage.md:131`, `docs/usage.md:151`, `docs/usage.md:168`, `docs/usage.md:216`, `docs/usage.md:232`). Tests in `tests/test-refine-plan.sh` are the main verification caller: they assert command metadata, phase headings/order, validator exit handling, extraction/classification requirements, alternate language rules, atomic write rules, template coverage, install wiring, reference extractor/classifier/path helpers, and validator behavior (`tests/test-refine-plan.sh:640`, `tests/test-refine-plan.sh:660`, `tests/test-refine-plan.sh:677`, `tests/test-refine-plan.sh:704`, `tests/test-refine-plan.sh:716`, `tests/test-refine-plan.sh:733`, `tests/test-refine-plan.sh:751`, `tests/test-refine-plan.sh:767`, `tests/test-refine-plan.sh:791`, `tests/test-refine-plan.sh:804`, `tests/test-refine-plan.sh:891`, `tests/test-refine-plan.sh:956`, `tests/test-refine-plan.sh:988`, `tests/test-refine-plan.sh:1048`).

- edge_cases_or_failure_modes: CLI edge cases include missing values for `--input`, `--output`, `--qa-dir`, and `--alt-language`; unknown flags; and mutually exclusive mode flags (`commands/refine-plan.md:59`, `commands/refine-plan.md:63`, `commands/refine-plan.md:145`, `scripts/validate-refine-plan-io.sh:500`, `scripts/validate-refine-plan-io.sh:526`, `scripts/validate-refine-plan-io.sh:537`, `scripts/validate-refine-plan-io.sh:544`). The command explicitly avoids passing `--alt-language` to the validator, and the validator treats unexpected `--alt-language` as invalid arguments, so the command layer must strip it before validation (`commands/refine-plan.md:72`, `tests/test-refine-plan.sh:1074`). Parser failure modes include nested comments, stray/wrong end markers, unterminated blocks, empty-only blocks, markers hidden in fenced code or HTML comments, and section headings hidden in those ignored regions (`commands/refine-plan.md:223`, `commands/refine-plan.md:228`, `commands/refine-plan.md:244`, `scripts/validate-refine-plan-io.sh:139`, `scripts/validate-refine-plan-io.sh:165`, `scripts/validate-refine-plan-io.sh:227`, `scripts/validate-refine-plan-io.sh:303`, `scripts/validate-refine-plan-io.sh:318`). The validator’s implementation has careful state for fences, HTML comments, comment format, current heading, and context excerpts; it counts only valid non-empty blocks (`scripts/validate-refine-plan-io.sh:115`, `scripts/validate-refine-plan-io.sh:182`, `scripts/validate-refine-plan-io.sh:209`, `scripts/validate-refine-plan-io.sh:212`). Plan consistency failure modes include missing required sections, dangling AC references, invalid task dependencies, invalid routing tags, or convergence status inconsistent with unresolved decisions (`commands/refine-plan.md:442`). Processing edge cases include ambiguous classification, mixed-intent comments requiring sub-items, unresolved decisions in discussion/direct mode, and research that narrows an issue but leaves a human choice (`commands/refine-plan.md:296`, `commands/refine-plan.md:306`, `commands/refine-plan.md:387`, `commands/refine-plan.md:375`). Language edge cases include unsupported CLI language as fatal, unsupported config language as warning/no variant, English as no-op, alternate language same as main language, ambiguous main language, and extensionless variant filenames (`commands/refine-plan.md:145`, `commands/refine-plan.md:147`, `commands/refine-plan.md:440`, `commands/refine-plan.md:524`). Write failure modes include temp write or translation failure, finalization after partial replacement, stale temp files, and update ordering for in-place mode (`commands/refine-plan.md:559`, `commands/refine-plan.md:563`, `commands/refine-plan.md:565`, `commands/refine-plan.md:572`). The tests cover many of these explicitly: ignored HTML/fence markers, empty comments, unterminated/nested parse errors, missing sections, non-writable output/input directories, non-directory QA path, QA auto-create, mixed valid/ignored/empty markers, in-place/new-file mode reporting, and resolved output path reporting (`tests/test-refine-plan.sh:1128`, `tests/test-refine-plan.sh:1137`, `tests/test-refine-plan.sh:1146`, `tests/test-refine-plan.sh:1155`, `tests/test-refine-plan.sh:1176`, `tests/test-refine-plan.sh:1191`, `tests/test-refine-plan.sh:1220`, `tests/test-refine-plan.sh:1227`, `tests/test-refine-plan.sh:1245`, `tests/test-refine-plan.sh:1276`, `tests/test-refine-plan.sh:1287`, `tests/test-refine-plan.sh:1307`, `tests/test-refine-plan.sh:1323`).

- validation_or_tests: The primary validation artifact is `scripts/validate-refine-plan-io.sh`, which implements IO and parse preflight with documented exit codes (`scripts/validate-refine-plan-io.sh:1`, `scripts/validate-refine-plan-io.sh:4`). The command file itself documents the exact exit-code-to-command-action mapping (`commands/refine-plan.md:165`). `tests/test-refine-plan.sh` checks the command definition for frontmatter, allowed tool allowlist, hidden slash-command metadata, ultrathink instruction, planning-only constraint, sequential phases, phase ordering, validator invocation rules, comment extraction requirements, classification and disposition vocabularies, schema preservation, config/alt-language behavior, atomic transaction requirements, QA template coverage, install docs/skill wiring, reference behavior for extraction/classification/language/path helpers, and validator script scenarios (`tests/test-refine-plan.sh:669`, `tests/test-refine-plan.sh:678`, `tests/test-refine-plan.sh:699`, `tests/test-refine-plan.sh:705`, `tests/test-refine-plan.sh:717`, `tests/test-refine-plan.sh:734`, `tests/test-refine-plan.sh:752`, `tests/test-refine-plan.sh:768`, `tests/test-refine-plan.sh:792`, `tests/test-refine-plan.sh:805`, `tests/test-refine-plan.sh:891`, `tests/test-refine-plan.sh:956`, `tests/test-refine-plan.sh:988`, `tests/test-refine-plan.sh:1052`). I did not execute tests because the assignment requested read-only research notes only, and the validator tests create fixtures and mutate permissions under test directories.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence section present for the assigned file item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`