# agent_080 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-080 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: `tests/test-gen-plan.sh` is an executable specification for the `/humanize:gen-plan` command surface. It does not implement planning itself; it guards the command contract across command metadata, the relevance-checker agent, the plan template, version surfaces, and `scripts/validate-gen-plan-io.sh`. This is core-adjacent algorithm coverage because `gen-plan` defines the repository’s structured planning workflow, Claude/Codex deliberation phases, auto-start gate, task routing schema, and IO preflight behavior.
- algorithmic_behavior: The script runs as a sequential Bash assertion suite under `set -euo pipefail` at `tests/test-gen-plan.sh:9`. It derives `SCRIPT_DIR`, `PROJECT_ROOT`, `COMMANDS_DIR`, and `AGENTS_DIR` from the script location at `tests/test-gen-plan.sh:11-14`, then accumulates results in `TESTS_PASSED` and `TESTS_FAILED` at `tests/test-gen-plan.sh:22-40`. Positive checks validate real repository files: `commands/gen-plan.md` exists and has frontmatter fields at `tests/test-gen-plan.sh:59-103`; `agents/draft-relevance-checker.md` exists at `tests/test-gen-plan.sh:111-116`; the Claude/Codex deliberation workflow appears in the command and template at `tests/test-gen-plan.sh:123-246`; agent name/model/tools are pinned at `tests/test-gen-plan.sh:249-287`; version strings match across `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and `README.md` at `tests/test-gen-plan.sh:290-310`. Negative checks create intentionally invalid temporary fixtures and verify local validators reject them at `tests/test-gen-plan.sh:322-532`. The script then runs `scripts/validate-gen-plan-io.sh` against temp inputs to pin argument and path exit codes at `tests/test-gen-plan.sh:562-705`, and finally asserts the embedded `markdown` plan structure block in `commands/gen-plan.md` exactly equals `prompt-template/plan/gen-plan-template.md` at `tests/test-gen-plan.sh:708-715`.
- inputs_outputs_state: Inputs are repository-relative files discovered from `PROJECT_ROOT`: `commands/gen-plan.md`, `agents/draft-relevance-checker.md`, `prompt-template/plan/gen-plan-template.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`, and `scripts/validate-gen-plan-io.sh`. Runtime inputs also include temp fixture files under `mktemp -d` at `tests/test-gen-plan.sh:323` and `tests/test-gen-plan.sh:566` if executed. Outputs are colored PASS/FAIL lines from `pass()` and `fail()`, summary counts at `tests/test-gen-plan.sh:721-726`, and process exit `0` when `TESTS_FAILED == 0` or `1` otherwise at `tests/test-gen-plan.sh:728-736`. State transitions are simple counter increments in `pass()`/`fail()`; command-specific probes store transient values such as `DESC`, `NAME`, `MODEL`, `PLUGIN_VER`, `MARKETPLACE_VER`, `README_VER`, `EXIT_CODE`, and `OUTPUT`.
- gates_or_invariants: Metadata gates require `description:`, `allowed-tools:`, and `argument-hint:` in `commands/gen-plan.md` at `tests/test-gen-plan.sh:71-103`; `commands/gen-plan.md` itself shows those fields and allowed scripts at `commands/gen-plan.md:1-14`. Workflow gates require `scripts/ask-codex.sh`, `--auto-start-rlcr-if-converged`, `ultrathink`, `## Pending User Decisions`, Phase 3 before Phase 4, Phase 5, max three rounds, task breakdown, task tags, and question consolidation at `tests/test-gen-plan.sh:125-246`; the command content anchors these at `commands/gen-plan.md:18`, `commands/gen-plan.md:184`, `commands/gen-plan.md:216`, `commands/gen-plan.md:255-257`, `commands/gen-plan.md:291`, `commands/gen-plan.md:316-322`, and `commands/gen-plan.md:594-599`. Agent gates require `name: draft-relevance-checker`, `model: haiku`, and `tools:` at `tests/test-gen-plan.sh:254-287`, matching `agents/draft-relevance-checker.md:1-6`. Naming gates enforce `^[a-z][a-z0-9-]*$` at `tests/test-gen-plan.sh:326-330`. Frontmatter gates require first-line `---` plus `description:` for commands and `name:` plus `description:` for agents at `tests/test-gen-plan.sh:332-345`. Model gates accept exact `opus|sonnet|haiku` or prefixes `claude-|gpt-|o[0-9]|gemini-` at `tests/test-gen-plan.sh:481-489`. Content gates reject CJK/emoji with `grep -P` at `tests/test-gen-plan.sh:540-552`. IO gates pin validator exit codes: invalid args/help `6`, input missing `1`, input empty `2`, output dir missing `3`, output exists or directory `4`, valid paths `0`, auto-start accepted, discussion/direct recognized, and discussion plus direct rejected at `tests/test-gen-plan.sh:569-700`.
- dependencies_and_callers: Direct dependencies are Bash plus standard CLI tools `sed`, `grep`, `awk`, `mktemp`, `cat`, `touch`, `mkdir`, `wc`, `tr`, and executable permission on `scripts/validate-gen-plan-io.sh`. The IO validator itself parses `--input`, `--output`, `--auto-start-rlcr-if-converged`, `--discussion`, and `--direct` at `scripts/validate-gen-plan-io.sh:35-80`; normalizes input/output with `realpath -m` fallback at `scripts/validate-gen-plan-io.sh:98-101`; validates existence, emptiness, output directory, output collision, and write permission at `scripts/validate-gen-plan-io.sh:108-153`; and locates the plan template via `CLAUDE_PLUGIN_ROOT` or script-relative fallback at `scripts/validate-gen-plan-io.sh:162-179`. The plan template schema being compared includes required top-level sections, task tags, deliberation status, pending decisions, implementation notes, and translated variant rules at `prompt-template/plan/gen-plan-template.md:1-120`. Aggregate test wiring references this file from `tests/run-all-tests.sh:83`.
- edge_cases_or_failure_modes: The script is structural and grep-based, so it can pass while deeper semantics are wrong or fail after harmless wording changes. The YAML syntax check at `tests/test-gen-plan.sh:435-449` is a heuristic, not a YAML parser, and only accepts simple key/list/quoted frontmatter lines. The direct-mode check at `tests/test-gen-plan.sh:137-140` depends on `GEN_PLAN_MODE=direct` and `PLAN_CONVERGENCE_STATUS=partially_converged` being discoverable through a short grep pipeline, so formatting changes can break it. The auto-start gate check only searches 12 lines after `Optional Direct Work Start` at `tests/test-gen-plan.sh:143-146`, which is intentionally narrow but brittle. The CJK/emoji check uses `grep -P` at `tests/test-gen-plan.sh:540-552`; on systems without PCRE `grep`, stderr is suppressed and the check can falsely pass. The second `trap` at `tests/test-gen-plan.sh:567` replaces the first cleanup trap from `tests/test-gen-plan.sh:324`, so an executed run can leave the first fixture directory behind after the validator-test temp dir is introduced. The version extraction at `tests/test-gen-plan.sh:299-302` assumes simple JSON formatting and numeric dotted README versions. The branch-relevant path behavior is mostly delegated to `validate-gen-plan-io.sh`, whose `realpath -m` usage at `scripts/validate-gen-plan-io.sh:99-100` has a fallback path for platforms where that GNU option is unavailable; the test asserts exit-code outcomes, not normalized path strings.
- validation_or_tests: This assigned file is itself the validation artifact. I inspected it directly with line-numbered reads and followed its direct dependencies. I did not execute it because the worker instruction says not to modify files; execution would create temporary fixtures and validator temp files via `mktemp`, `cat >`, `touch`, and `mkdir` at `tests/test-gen-plan.sh:322-567` and `tests/test-gen-plan.sh:614-665`. If run in a writable test environment, its final gate is `TESTS_FAILED == 0` at `tests/test-gen-plan.sh:728-736`. The underlying IO validator’s documented exit-code contract is visible at `scripts/validate-gen-plan-io.sh:4-12` and is exercised by the script at `tests/test-gen-plan.sh:569-700`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`