# agent_001 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository-level RLCR orchestration system for Humanize: it installs and runs Claude/Codex loop commands, enforces hook-time path and state safety, drives implementation, review, finalize, cancel, and optional methodology-analysis phases, renders prompts/block messages, loads configuration, routes model/tool invocations, and validates the behavior through shell/Python tests. The branch’s core algorithmic responsibility centers on deterministic project-root resolution and realpath-aware path comparison across the entire hook/script surface.
- algorithmic_behavior: The directory coordinates slash-command entrypoints, setup scripts, validators, stop hooks, support libraries, prompt templates, plugin/config metadata, skills, docs, and tests. `hooks/lib/project-root.sh:41` resolves project root from `CLAUDE_PROJECT_DIR`, then `git rev-parse --show-toplevel`, intentionally avoiding `pwd`; it canonicalizes the result so setup-time and hook-time roots do not diverge through symlinked prefixes. `hooks/lib/project-root.sh:72` provides `canonicalize_path_prefix`, which resolves only the parent directory and preserves the basename, while `hooks/lib/project-root.sh:113` provides full `canonicalize_path` and explicitly warns against using it for leaf authorization. That distinction is the branch’s key security algorithm: compare equivalent ancestor paths without dereferencing attacker-controlled leaf symlinks.
  
  Setup begins in `scripts/setup-rlcr-loop.sh:326`, requiring a resolvable project root, then checks `codex`, `jq`, and `git` at `scripts/setup-rlcr-loop.sh:340`. It enforces single active-loop mutual exclusion at `scripts/setup-rlcr-loop.sh:369`. Plan-file validation rejects absolute paths, spaces, shell metacharacters, symlink files, symlink parent directories, missing files, and invalid repository state at `scripts/setup-rlcr-loop.sh:455` through `scripts/setup-rlcr-loop.sh:520`. Initialization writes `.humanize/rlcr/<timestamp>/state.md`, captures branch/base/plan/model settings, prepares a pending session-id signal, and initializes BitLesson/goal-tracker artifacts at `scripts/setup-rlcr-loop.sh:860` through `scripts/setup-rlcr-loop.sh:918`.
  
  Hook validators consume Claude hook JSON, validate structure, locate the active loop, parse strict state, and allow/block operations. Shared JSON guards in `hooks/lib/loop-common.sh:91` reject null bytes, invalid UTF-8, invalid JSON, missing `tool_name`, and excessive nesting. Active-loop discovery at `hooks/lib/loop-common.sh:332` protects against stale loop resurrection by selecting the newest active loop, optionally filtering by session id, and allowing `bg-pending.marker` fallback only for the stop hook. Strict state parsing at `hooks/lib/loop-common.sh:533` requires YAML frontmatter, required fields, numeric rounds, and boolean `review_started`.
  
  `hooks/loop-read-validator.sh:56` blocks todos reads unless allowlisted and, during methodology-analysis phase, restricts loop reads to analysis artifacts while blocking project-file reads at `hooks/loop-read-validator.sh:86` through `hooks/loop-read-validator.sh:168`. It validates round-file location/round number and compares prefix-canonical paths at `hooks/loop-read-validator.sh:305` through `hooks/loop-read-validator.sh:313`. `hooks/loop-write-validator.sh:57` blocks todos/prompt writes; at `hooks/loop-write-validator.sh:88` it restricts methodology-analysis writes to report/done artifacts; at `hooks/loop-write-validator.sh:192` through `hooks/loop-write-validator.sh:205` it blocks state-file writes; at `hooks/loop-write-validator.sh:230` it blocks plan backup writes; at `hooks/loop-write-validator.sh:243` it validates goal-tracker mutations; and at `hooks/loop-write-validator.sh:341` it uses parent-only canonical path comparison for write destinations. `hooks/loop-edit-validator.sh:120` limits edit validation to `.humanize/rlcr`, blocks state/finalize/methodology state edits at `hooks/loop-edit-validator.sh:156` through `hooks/loop-edit-validator.sh:168`, protects plan backup edits at `hooks/loop-edit-validator.sh:180`, and previews/validates goal-tracker edits at `hooks/loop-edit-validator.sh:204` through `hooks/loop-edit-validator.sh:224`.
  
  `hooks/loop-bash-validator.sh:81` blocks methodology-analysis write-like Bash behavior, including mutating git commands, file operations, in-place edits, interpreters, shell scripts, build tools, sourcing, direct script execution, and file redirection. It prevents direct execution of stop hooks through wrappers at `hooks/loop-bash-validator.sh:166` through `hooks/loop-bash-validator.sh:183`. It blocks state mutation via shell patterns at `hooks/loop-bash-validator.sh:253` through `hooks/loop-bash-validator.sh:279` and routes the narrow cancel exception through `is_cancel_authorized`.
  
  Cancel authorization in `hooks/lib/loop-common.sh:1051` is the most sensitive path algorithm: it requires `.cancel-requested`, rejects command substitution, newlines, shell chaining, hidden variables, mixed quote styles, extra args, and non-`mv` forms, normalizes literal loop-dir variables, then canonicalizes source and destination with `canonicalize_path_prefix` at `hooks/lib/loop-common.sh:1207` and `hooks/lib/loop-common.sh:1224`. This permits symlinked directory prefixes but rejects leaf symlink aliases for `state.md` or `cancel-state.md`. `scripts/cancel-rlcr-loop.sh:73` resolves project root, globally finds the newest active loop by design, requires `--force` for finalize cancellation at `scripts/cancel-rlcr-loop.sh:139`, creates the cancel signal, and renames the active state to `cancel-state.md` at `scripts/cancel-rlcr-loop.sh:156` through `scripts/cancel-rlcr-loop.sh:166`.
  
  The stop hook is the state-machine driver. `hooks/loop-codex-stop-hook.sh:64` finds an active loop with session-aware background-marker fallback. It delegates background-task parking before normal gates at `hooks/loop-codex-stop-hook.sh:83`; `hooks/lib/loop-bg-tasks.sh:193` parses transcript JSONL for async Agent/Bash launches and completion notifications, prunes dead tasks via `lsof`, and `hooks/lib/loop-bg-tasks.sh:311` runs ambiguous-caller, cross-session, pending-task, and stale-marker cleanup guards. The stop hook then parses phase/state at `hooks/loop-codex-stop-hook.sh:91` through `hooks/loop-codex-stop-hook.sh:140`, enforces branch/plan/git/summary/BitLesson/goal-tracker gates, runs Codex summary review in implementation phase, transitions to review phase when Codex returns `COMPLETE`, and runs `codex review` in review phase. `hooks/loop-codex-stop-hook.sh:1250` treats review command failure or missing stdout as blocking, keeps review issues in the loop, and calls finalize only when no severity markers are detected. Finalize transition renames `state.md` to `finalize-state.md` at `hooks/loop-codex-stop-hook.sh:1287`.
  
  Optional methodology analysis is a terminal pre-exit phase. `hooks/lib/methodology-analysis.sh:38` skips when privacy mode is true, otherwise renames active state to `methodology-analysis-state.md`, records exit reason, creates a done placeholder, and emits a blocking prompt. Completion at `hooks/lib/methodology-analysis.sh:114` requires non-empty done/report artifacts, a valid saved exit reason, and then renames state to the matching terminal state at `hooks/lib/methodology-analysis.sh:165` through `hooks/lib/methodology-analysis.sh:171`.
  
  Supporting algorithms include single-pass prompt rendering in `hooks/lib/template-loader.sh:56`, preventing substituted values from being re-scanned as placeholders; config merge layering in `scripts/lib/config-loader.sh:63`, merging default, user, and project config while stripping nulls and ignoring malformed optional configs; and `scripts/rlcr-stop-gate.sh:108`, which wraps the same stop-hook logic for skill/non-hook contexts with explicit hook JSON.
- inputs_outputs_state: Inputs include CLI arguments to commands/scripts, Claude hook JSON from PreToolUse/PostToolUse/Stop events, `CLAUDE_PROJECT_DIR`, optional session/transcript IDs, git repository state, plan file paths/content, `.humanize/config.json` plus default/user config layers, prompt templates, BitLesson knowledge base/delta content, goal-tracker content, task/transcript JSONL, and command strings from Bash tool invocations. Outputs include hook JSON block/allow decisions, rendered block reasons and next-round prompts, `.humanize/rlcr/<loop>/state.md` and terminal state files, `plan.md` backups, `goal-tracker.md`, round summaries/contracts/prompts/review logs, `bg-pending.marker`, `.cancel-requested`, methodology analysis report/done markers, config-derived defaults, and status/monitor output. State transitions are file-name based: `state.md` for implementation/review, `finalize-state.md` for finalize, `methodology-analysis-state.md` for optional methodology analysis, and terminal `complete-state.md`, `stop-state.md`, `maxiter-state.md`, or `cancel-state.md`.
- gates_or_invariants: All hook/script project-root decisions must use canonical root resolution, not current working directory. User-supplied leaf paths that are being authorized against expected filenames must use parent-only canonicalization, not full leaf dereferencing. Active-loop selection must not revive older zombie loops. Hook JSON must be well-formed, UTF-8, shallow enough, and contain required fields. State frontmatter must contain required schema fields and valid scalar types before protected operations continue. Setup accepts only relative, no-space, no-metacharacter, non-symlink, in-project plan paths. State files, prompt files, plan backups, finalize contracts, and immutable goal-tracker sections are hook-managed and blocked from direct tool mutation. Methodology-analysis phase allows only methodology artifacts and blocks project-file reads/writes for the owning session. Stop-hook gates require consistent branch/plan/git/summary/review/BitLesson/goal-tracker evidence before advancing phase or exiting. Cancel is only authorized through the signal-file plus exact two-argument `mv` path form, with symlink-leaf aliases rejected. Background work parks the loop and must not run Codex/git gates until the owning session transcript proves pending work is complete.
- dependencies_and_callers: Top-level callers are Claude slash commands in `commands/`, Codex hook config in `config/codex-hooks.json`, Claude hook config in `hooks/hooks.json`, plugin metadata in `.claude-plugin/`, and skills under `skills/`. Runtime dependencies include `bash`, `jq`, `git`, `codex`, `realpath`, `python3` fallback for realpath behavior, `awk`, `sed`, `grep`, `tr`, `date`, optional `lsof`, and optional model/provider CLIs used by ask/BitLesson flows. Core libraries under `hooks/lib/` are shared by read/write/edit/bash validators, stop hook, setup, cancel, status/monitor helpers, and stop-gate wrapper. Prompt templates under `prompt-template/` are consumed by validators and stop-hook phase prompts. Tests under `tests/` and `tests/robustness/` exercise the hooks/scripts with mocks and fixtures.
- edge_cases_or_failure_modes: Missing project root makes hooks allow natural stop but makes setup/cancel hard-error. Missing `jq`, `git`, or `codex` blocks setup or wrappers. Malformed hook JSON, null bytes, invalid UTF-8, or deeply nested JSON fail closed. Malformed or partially edited state files block protected operations. Full realpath on a symlink leaf would authorize unsafe source/destination aliases, so the branch explicitly avoids it for leaf comparisons. Symlinked parent prefixes are accepted when they canonicalize to the same real directory. Plan paths with shell metacharacters, parent symlinks, absolute paths, or spaces are rejected before loop creation. Background transcripts can be missing, unreadable, stale, or cross-session; guards fail conservatively and preserve loop state. Methodology-analysis phase has a known limitation that spawned agents with different session ids are not restricted by the same hooks, so prompts enforce sanitization. Review command failure, empty Codex review stdout, missing review marker, branch drift, dirty git state, missing summaries/contracts, invalid BitLesson delta, uninitialized goal tracker, direct stop-hook execution, and forbidden state mutation all block progress.
- validation_or_tests: Validation here was by source inspection only; no tests were run in this read-only research pass. Existing test coverage includes path validation robustness in `tests/robustness/test-path-validation-robustness.sh:76`, which runs production setup against valid and invalid plan paths; realpath/cancel regression tests in `tests/test-cancel-signal-file.sh:1345` through `tests/test-cancel-signal-file.sh:1418`, covering symlinked prefixes and rejecting source/destination symlink aliases; hook allowlist and plan-file tests in `tests/test-allowlist-validators.sh` and `tests/test-plan-file-hooks.sh`; background stop-hook behavior in `tests/test-stop-hook-bg-allow.sh`; session filtering in `tests/test-session-id.sh`; finalize behavior in `tests/test-finalize-phase.sh`; template/config/model routing tests in `tests/test-template-loader.sh`, `tests/test-config-merge.sh`, and `tests/test-model-router.sh`; and broad shell/robustness suites under `tests/run-all-tests.sh` and `tests/robustness/`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`