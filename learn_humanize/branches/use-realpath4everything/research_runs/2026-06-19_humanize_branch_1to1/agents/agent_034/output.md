# agent_034 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-034 `file` `config/codex-hooks.json`
- cursor: `[_]`
- core_role: `config/codex-hooks.json` is the Codex-native hook template that wires Humanize RLCR exit gating into the Codex hook lifecycle. Its only event binding is `Stop`, so it participates in runtime routing by making normal Codex session exit trigger `hooks/loop-codex-stop-hook.sh` after RLCR round summaries are written. The assigned file is small but core-adjacent because it is the declarative entrypoint from Codex runtime configuration into the repository’s shell-based gating algorithm.
- algorithmic_behavior: The file is pure JSON configuration, not executable code. Lines `3-16` define a `hooks` object with a single `Stop` array. Lines `4-14` contain one hook group, and lines `7-12` contain one command hook: `type: "command"`, command path `{{HUMANIZE_RUNTIME_ROOT}}/hooks/loop-codex-stop-hook.sh`, `timeout: 7200`, and status text `humanize RLCR stop hook`. The algorithmic effect is event-to-command routing: when Codex fires a `Stop` hook, Codex runs the configured command and lets that command decide whether to allow exit or block with instructions. The actual gate behavior lives in `hooks/loop-codex-stop-hook.sh`, which reads hook input from stdin at `hooks/loop-codex-stop-hook.sh:28`, resolves the project and active loop at `hooks/loop-codex-stop-hook.sh:43-68`, and can emit a JSON `"decision": "block"` on review failure at `hooks/loop-codex-stop-hook.sh:1687-1693`.
- inputs_outputs_state: Template inputs are the static JSON plus the `{{HUMANIZE_RUNTIME_ROOT}}` placeholder at `config/codex-hooks.json:9`. Installation supplies the runtime root from `scripts/install-codex-hooks.sh:10-14` or from the unified installer via `scripts/install-skill.sh:253-267`. During merge, the installer reads this template, JSON-escapes the runtime root, substitutes it, parses JSON, and shell-quotes command paths at `scripts/install-codex-hooks.sh:105-118`. The output state is the user’s Codex hooks file, `$CODEX_HOME/hooks.json`, set at `scripts/install-codex-hooks.sh:73` and written at `scripts/install-codex-hooks.sh:170-171`. Runtime state is not stored in this JSON; the invoked Stop hook reads `.humanize/rlcr/<timestamp>/state.md`, summary files, review prompts/results, and related loop files, described in `hooks/loop-codex-stop-hook.sh:8-12`.
- gates_or_invariants: The template invariant is exactly one managed Humanize Codex Stop hook group with a command hook and a two-hour timeout. Install-time guards require the template file to exist at `scripts/install-codex-hooks.sh:71`, require `codex` to be installed and expose `codex_hooks` at `scripts/install-codex-hooks.sh:75-82`, and require `python3` for merging at `scripts/install-codex-hooks.sh:90-92`. The merge invariant removes stale managed Humanize Stop commands matching `loop-codex-stop-hook.sh` or `pr-loop-stop-hook.sh` while preserving unrelated hooks at `scripts/install-codex-hooks.sh:138-165`. Feature enablement is a gate: unless skipped, `codex features enable codex_hooks` must succeed at `scripts/install-codex-hooks.sh:175-184`. Runtime gates enforced after this routing include state/schema validation, branch consistency, incomplete Task/Todo blocking, git cleanliness, summary presence, max iteration handling, marker handling, and code review gating, summarized in `skills/humanize-rlcr/SKILL.md:52-68`.
- dependencies_and_callers: Direct caller is `scripts/install-codex-hooks.sh`, which uses `HOOKS_TEMPLATE="$REPO_ROOT/config/codex-hooks.json"` at `scripts/install-codex-hooks.sh:14` and merges it into Codex config at `scripts/install-codex-hooks.sh:201-202`. The unified installer calls that hook installer for Codex installs at `scripts/install-skill.sh:386-390`. Documentation states Codex installation will install/update native Humanize Stop hooks in `${CODEX_HOME:-~/.codex}/hooks.json` at `docs/install-for-codex.md:25-29`, and verification expects `hooks.json` to contain `loop-codex-stop-hook.sh` at `docs/install-for-codex.md:70-80`. The runtime dependency is `hooks/loop-codex-stop-hook.sh`, which sources shared loop utilities at `hooks/loop-codex-stop-hook.sh:42-55`, resolves the active project/loop at `hooks/loop-codex-stop-hook.sh:46-64`, and performs the RLCR review/exit decision.
- edge_cases_or_failure_modes: If `{{HUMANIZE_RUNTIME_ROOT}}` is not replaced, the installed command path would be invalid and Codex Stop routing would fail to reach the runtime hook. The installer mitigates paths with spaces or shell metacharacters by JSON-escaping before `json.loads` and shell-quoting commands at `scripts/install-codex-hooks.sh:105-118`. Existing malformed hooks config fails parsing or object-shape checks at `scripts/install-codex-hooks.sh:120-136`. Existing stale Humanize hook commands are removed before the new managed group is appended at `scripts/install-codex-hooks.sh:138-165`, preventing duplicated managed Stop hooks. Codex builds without `codex_hooks` support fail installation at `scripts/install-codex-hooks.sh:75-82`. At runtime, if no active loop or no state file is found, the Stop hook allows exit at `hooks/loop-codex-stop-hook.sh:66-69` and `hooks/loop-codex-stop-hook.sh:91-95`; if Codex review execution fails, the hook blocks exit with debug file references at `hooks/loop-codex-stop-hook.sh:1668-1694`.
- validation_or_tests: `tests/test-codex-hook-install.sh` validates the installed hook surface. It seeds an existing `hooks.json` with a stale Humanize Stop hook, a custom Stop hook, and a `SessionStart` hook at `tests/test-codex-hook-install.sh:71-108`. It then verifies the Codex install writes hooks, enables `codex_hooks`, adds the managed Humanize Stop command, preserves unrelated Stop hooks, removes stale Humanize commands, preserves `SessionStart`, and writes exactly one managed Humanize Stop hook at `tests/test-codex-hook-install.sh:130-226`. It also verifies idempotency on a second install at `tests/test-codex-hook-install.sh:258-282`, repeated feature enablement at `tests/test-codex-hook-install.sh:284-288`, and rejection of unsupported Codex builds without the `codex_hooks` feature at `tests/test-codex-hook-install.sh:290-332`. Documentation verification steps are listed in `docs/install-for-codex.md:70-80`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 Item Evidence sections present`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`