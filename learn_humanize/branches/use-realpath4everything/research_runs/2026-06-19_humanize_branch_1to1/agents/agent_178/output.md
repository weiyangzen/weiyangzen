# agent_178 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-178 `file` `skills/humanize-gen-plan/SKILL.md`
- cursor: `[_]`
- core_role: This file is the flow-level algorithm specification for generating a Humanize implementation plan from a draft. It is not ordinary executable code, but it defines the ordered workflow, stop gates, input/output contract, output schema, and user-interaction checkpoints for the `humanize-gen-plan` skill. Its frontmatter identifies it as `type: flow`, non-user-invocable in source form, and with model invocation disabled by metadata (`skills/humanize-gen-plan/SKILL.md:1-7`). The body states the transformation goal: turn a rough draft into a structured implementation plan with goals, AC-style acceptance criteria, path boundaries, and feasibility suggestions (`skills/humanize-gen-plan/SKILL.md:9-12`).

- algorithmic_behavior: The main algorithm is encoded as a Mermaid flowchart (`skills/humanize-gen-plan/SKILL.md:19-47`). It starts by validating input/output paths through the hydrated runtime script path, `{{HUMANIZE_RUNTIME_ROOT}}/scripts/validate-gen-plan-io.sh --input <draft> --output <plan>` (`skills/humanize-gen-plan/SKILL.md:21`). If validation fails, it reports the validation error and stops (`skills/humanize-gen-plan/SKILL.md:22-24`). If validation succeeds, it reads the draft, checks repository relevance, rejects unrelated drafts, analyzes the draft for clarity, consistency, completeness, and functionality, loops through user issue resolution until no issues remain, optionally confirms quantitative metrics, generates the structured plan, writes it using an edit-preserving operation, reviews the generated plan, fixes inconsistencies until clean, optionally asks the user to unify multiple languages, then reports success with plan path, AC count, and language-unification status (`skills/humanize-gen-plan/SKILL.md:25-46`). The explicit state transitions are: `BEGIN -> VALIDATE -> CHECK`; failed validation goes to `REPORT_ERROR -> END_FAIL`; passed validation goes through draft read and relevance check; failed relevance goes to `REPORT_IRRELEVANT -> END_FAIL`; successful relevance goes through analysis and issue-resolution loops; successful generation goes through write/review/fix loops; final success goes to `END_SUCCESS`.

- inputs_outputs_state: Required inputs are exactly `--input <path/to/draft.md>` and `--output <path/to/plan.md>` (`skills/humanize-gen-plan/SKILL.md:49-54`). The input draft must exist and be non-empty according to the delegated validation script, and the output target must be a new file in an existing writable directory. The principal output is a generated plan document with sections for title, goal description, acceptance criteria, path boundaries, dependencies/sequence, and implementation notes (`skills/humanize-gen-plan/SKILL.md:55-94`). The assigned file’s output sketch is shorter than the repository template, but it establishes the required AC shape: `AC-1`, positive tests, and negative tests (`skills/humanize-gen-plan/SKILL.md:65-72`). The more complete repository template at `prompt-template/plan/gen-plan-template.md` expands the same output contract with feasibility hints, task breakdown, Claude-Codex deliberation, pending user decisions, and optional translated-language variant behavior (`prompt-template/plan/gen-plan-template.md:46-120`). State is not persisted by this skill file directly; instead, flow state is represented by validation pass/fail, relevance pass/fail, analysis issue presence, metric presence, review consistency, and language-unification branches.

- gates_or_invariants: The first hard gate is IO validation via the hydrated runtime root (`skills/humanize-gen-plan/SKILL.md:13-21`). The delegated script defines the concrete invariant set: required arguments, mutually exclusive `--discussion` and `--direct`, input file exists, input file is not empty, output directory exists, output path is not an existing file or directory, output directory is writable, and the plan template file exists (`scripts/validate-gen-plan-io.sh:35-80`, `scripts/validate-gen-plan-io.sh:82-179`). The skill’s exit-code table mirrors those gates: code `0` continues; `1` input missing; `2` input empty; `3` output directory missing; `4` output exists; `5` no write permission; `6` invalid arguments; `7` template missing (`skills/humanize-gen-plan/SKILL.md:96-108`). The relevance gate rejects drafts unrelated to the repository (`skills/humanize-gen-plan/SKILL.md:25-28`), and the associated agent is intentionally lenient, marking drafts relevant when they could reasonably connect to the repo (`agents/draft-relevance-checker.md:21-37`). The analysis loop invariant is that clarity, consistency, completeness, and functionality issues must be resolved before plan generation continues (`skills/humanize-gen-plan/SKILL.md:29-36`). The review loop invariant is that the written plan must be free of inconsistencies before success reporting (`skills/humanize-gen-plan/SKILL.md:37-45`). A final language gate asks the user to unify language if multiple languages are detected (`skills/humanize-gen-plan/SKILL.md:42-45`). A content invariant in the output structure says implementation code should not contain plan terminology (`skills/humanize-gen-plan/SKILL.md:92-94`).

- dependencies_and_callers: The skill depends on installer hydration of `{{HUMANIZE_RUNTIME_ROOT}}` into an absolute runtime root (`skills/humanize-gen-plan/SKILL.md:13-17`). `scripts/install-skill.sh` installs the skill set including `humanize-gen-plan` (`scripts/install-skill.sh:39-44`), validates the source layout contains all skill files and runtime directories (`scripts/install-skill.sh:75-87`), copies runtime bundle directories such as `scripts`, `hooks`, `prompt-template`, `templates`, `config`, and `agents` (`scripts/install-skill.sh:155-164`), and hydrates the runtime-root placeholder inside every `SKILL.md` (`scripts/install-skill.sh:167-191`). Kimi installation docs describe `humanize-gen-plan` as a Flow skill and show `/flow:humanize-gen-plan` and `/skill:humanize-gen-plan` as usage paths (`docs/install-for-kimi.md:7-14`, `docs/install-for-kimi.md:132-140`). The assigned skill’s executable validation dependency is `scripts/validate-gen-plan-io.sh`, and that script in turn locates `prompt-template/plan/gen-plan-template.md` through `CLAUDE_PLUGIN_ROOT` or a script-relative fallback (`scripts/validate-gen-plan-io.sh:162-179`). The richer Claude command counterpart, `commands/gen-plan.md`, uses the same validation script, relevance checker, Codex analysis, config loader, and output template (`commands/gen-plan.md:32-45`, `commands/gen-plan.md:132-180`). The default config sets `alternative_plan_language` to disabled and `gen_plan_mode` to `discussion` (`config/default_config.json:1-8`), with merge behavior supplied by `scripts/lib/config-loader.sh` (`scripts/lib/config-loader.sh:63-137`).

- edge_cases_or_failure_modes: Failure modes include missing or empty input draft, nonexistent output directory, output path already existing or being a directory, unwritable output directory, invalid arguments, missing plan template, failed repository relevance, unresolved draft issues, inconsistent generated plan, and mixed-language output needing user intervention (`skills/humanize-gen-plan/SKILL.md:21-46`, `skills/humanize-gen-plan/SKILL.md:96-108`). The validation script also handles `--input` or `--output` without values, unknown flags, help invocation as invalid-argument exit `6`, mutually exclusive `--discussion` and `--direct`, and a no-op note when `--auto-start-rlcr-if-converged` is used in direct mode (`scripts/validate-gen-plan-io.sh:16-27`, `scripts/validate-gen-plan-io.sh:35-80`, `scripts/validate-gen-plan-io.sh:93-96`). Path normalization uses `realpath -m` when available and falls back to the original path otherwise, so environments without compatible `realpath` still proceed with less canonicalized paths (`scripts/validate-gen-plan-io.sh:98-101`). A subtle mismatch exists between the assigned skill’s abbreviated output block and the fuller canonical template: the skill lists core plan sections only (`skills/humanize-gen-plan/SKILL.md:55-94`), while the template adds task routing, deliberation, pending decisions, and alternative-language output conventions (`prompt-template/plan/gen-plan-template.md:69-120`). This is not necessarily a bug, but it means the assigned file is a high-level flow contract rather than the complete output schema.

- validation_or_tests: Direct validation coverage exists in `tests/test-gen-plan.sh` for the delegated IO script. The tests assert exit code `6` for missing values and unknown options, `1` for missing input, `2` for empty input, `3` for missing output directory, `4` for existing output file or output directory, `0` for valid paths, acceptance of `--auto-start-rlcr-if-converged`, recognition of `--discussion` and `--direct`, rejection of using both mode flags together, and `6` for `--help` (`tests/test-gen-plan.sh:557-705`). The same test file also compares the command’s embedded plan-structure block against `prompt-template/plan/gen-plan-template.md` (`tests/test-gen-plan.sh:708-715`). I did not run tests because this task requested research notes only and the workspace is a read-only branch export. A `git status` probe also failed because this export is not a normal Git checkout in the restricted environment, so commit/tree verification relies on the run metadata supplied in the prompt.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-178`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`