# agent_094 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-094 `file` `tests/test-stop-gate.sh`
- cursor: `[_]`
- core_role: `tests/test-stop-gate.sh` is an executable specification for `scripts/rlcr-stop-gate.sh`, focused on stop-gate project-root selection, active-loop blocking, tracked `.humanize/` protection, and hook-input JSON construction. It is part of the normal suite via `tests/run-all-tests.sh:60-68`, where `test-stop-gate.sh` is listed with the other RLCR hook tests.
- algorithmic_behavior: The test starts with strict shell mode and shared test helpers at `tests/test-stop-gate.sh:6-11`. Its `setup_active_loop_fixture` helper at `tests/test-stop-gate.sh:18-59` creates a temporary git repo, writes a minimal `plan.md`, mirrors that plan into `.humanize/rlcr/2026-03-01_00-00-00/plan.md`, and writes `state.md` frontmatter with round, branch, model, plan, review, session, and agent-team fields. Test 1 at `tests/test-stop-gate.sh:64-89` runs the wrapper from inside the project and expects exit `10` plus `BLOCK:`, proving the default root resolves to the caller’s repo rather than the plugin install directory. Test 2 at `tests/test-stop-gate.sh:91-116` runs from outside the target repo with `--project-root` and expects the same active-loop block. Test 3 at `tests/test-stop-gate.sh:118-145` stages `.humanize/rlcr/.../goal-tracker.md` and expects the dedicated “Tracked Humanize State Blocked” message. Test 4 at `tests/test-stop-gate.sh:147-178` stages `.humanize-backup` and `.humanizeconfig` and verifies those similarly named files do not trigger the tracked-state reason. Test 5 at `tests/test-stop-gate.sh:180-204` runs in a non-git empty project and expects `ALLOW:` with exit `0`. Test 6 at `tests/test-stop-gate.sh:206-287` installs a mock `loop-codex-stop-hook.sh`, copies the gate wrapper and `project-root.sh` into a fake plugin layout, passes `--transcript-path` without a session id, and verifies the wrapper forwards `transcript_path` while encoding empty `session_id` as JSON `null`.
- inputs_outputs_state: Inputs are temporary directories from `setup_test_dir`, synthetic git repos from `init_test_git_repo`, the real gate script path `tests/test-stop-gate.sh:11`, generated `.humanize/rlcr/<timestamp>/state.md`, optional staged files, and in Test 6 a mock hook plus `CLAUDE_PROJECT_DIR`, `MOCK_HOOK_INPUT_LOG`, `--transcript-path`, and `--json`. Outputs are per-test `out.txt` captures, Test 6’s captured hook JSON log, pass/fail counters from `tests/test-helpers.sh:30-44`, and the final summary/exit from `print_test_summary` at `tests/test-stop-gate.sh:289-290`. State transitions are fixture-local: `setup_test_dir` creates one temp root and an EXIT cleanup trap (`tests/test-helpers.sh:86-89`), each test creates an isolated subproject, Test 3 moves `.humanize/` state into the git index, Test 4 moves only unrelated dot-prefixed files into the index, and Test 6 substitutes a mock hook so only wrapper JSON forwarding is exercised.
- gates_or_invariants: The implementation under test resolves `PROJECT_ROOT` from `hooks/lib/project-root.sh` before argument parsing (`scripts/rlcr-stop-gate.sh:25-27`), then lets `--project-root` override it (`scripts/rlcr-stop-gate.sh:60-63`). The resolver prefers `CLAUDE_PROJECT_DIR`, then `git rev-parse --show-toplevel`, then failure, and canonicalizes with `realpath` (`hooks/lib/project-root.sh:41-52`). If no root is resolved, the wrapper must allow stop with exit `0` (`scripts/rlcr-stop-gate.sh:81-87`). The wrapper requires an executable hook and `jq`, otherwise exits `20` (`scripts/rlcr-stop-gate.sh:89-97`). Hook input JSON must keep each optional field independent: empty `session_id` and empty `transcript_path` become `null` individually rather than collapsing the whole object (`scripts/rlcr-stop-gate.sh:108-123`). Hook output mapping is invariant: empty hook output allows exit `0`, `decision:"block"` exits `10`, a JSON response without `decision` allows exit `0`, and unexpected decisions exit `20` (`scripts/rlcr-stop-gate.sh:125-178`). The stop hook finds the active loop via `find_active_loop` (`hooks/loop-codex-stop-hook.sh:61-69`), resolves the active state file (`hooks/loop-codex-stop-hook.sh:91-95`), blocks tracked `.humanize/` state before generic dirty checks (`hooks/loop-codex-stop-hook.sh:639-652`), then later reaches generic git cleanliness (`hooks/loop-codex-stop-hook.sh:687-710`) and missing-summary validation (`hooks/loop-codex-stop-hook.sh:747-784`).
- dependencies_and_callers: Direct test dependencies are Bash, `git`, `jq`, `mktemp`, `grep`, `cp`, `chmod`, and `tests/test-helpers.sh`. Behavioral dependencies are `scripts/rlcr-stop-gate.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/lib/project-root.sh`, and `hooks/lib/loop-common.sh`. Active-loop selection is defined by `find_active_loop` at `hooks/lib/loop-common.sh:332-425`; the no-session path checks only the newest loop directory and requires an active state file. Tracked-state detection is `git_has_tracked_humanize_state` at `hooks/lib/loop-common.sh:1428-1440`, which intentionally scopes `git ls-files` to `.humanize` so `.humanize-backup` and `.humanizeconfig` are not treated as Humanize runtime state. The dedicated block text is generated by `git_tracked_humanize_blocked_message` at `hooks/lib/loop-common.sh:1442-1466`.
- edge_cases_or_failure_modes: Test 1 and Test 2 only require a real `BLOCK:` line, not a specific block reason. The fixture comment at `tests/test-stop-gate.sh:18` says the minimal loop should block on a missing summary file, but the generated `plan.md` is untracked, so the real hook may block earlier on generic git cleanliness before reaching the missing-summary gate; the assertions are broad enough for that. Test 3 protects ordering: tracked `.humanize/` must produce the dedicated reason before normal loop validation or generic dirty checks. Test 4 protects path scoping: a prefix-like filename must not match `.humanize/` runtime state, even though those staged files can still make the repo dirty through the generic git gate. Test 5 ensures no project context is benign rather than a wrapper error. Test 6 covers a historical `jq select(length > 0)` failure mode where an empty `session_id` could erase sibling fields such as `transcript_path`; its mock hook makes this observable without depending on pending-background-task logic.
- validation_or_tests: Read-only syntax validation was run with `bash -n tests/test-stop-gate.sh`, `bash -n scripts/rlcr-stop-gate.sh`, `bash -n hooks/lib/project-root.sh`, and `bash -n hooks/loop-codex-stop-hook.sh`; all exited `0` with no syntax output. I did not run `tests/test-stop-gate.sh` end-to-end because it intentionally creates temporary git repositories and files, while this assignment requested research notes only and no file modifications.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1; single Item Evidence section present once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`