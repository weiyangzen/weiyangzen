# agent_021 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-021 `directory` `skills/humanize`
- cursor: `[_]`
- core_role: `skills/humanize` is the general Humanize skill manifest and installed runtime anchor for the Humanize workflow family. Recursive inspection found exactly one descendant file: `skills/humanize/SKILL.md`. The directory does not contain executable code itself, but it is core because installer logic treats `humanize` as the runtime bundle root where `scripts/`, `hooks/`, `prompt-template/`, `templates/`, `config/`, and `agents/` are copied during install. The manifest describes RLCR, plan generation, ask-codex, monitoring, storage layout, state files, gate expectations, and user-facing command contracts. Its frontmatter declares `name: humanize`, describes iterative development with AI review, disables direct user invocation, and disables model invocation at `skills/humanize/SKILL.md:1-5`.

- algorithmic_behavior: The manifest defines Humanize as an iterative feedback-loop coordinator: implementation work is summarized, Codex independently reviews that summary, feedback blocks continuation until corrected, and a later review phase uses `codex review` severity markers before finalization. The two-phase RLCR state machine is documented at `skills/humanize/SKILL.md:32-48`: Phase 1 performs implementation, writes a summary, runs Codex completeness/correctness review, loops on issues, and transitions when Codex outputs `COMPLETE`; Phase 2 runs `codex review --base <branch>`, loops on `[P0-9]` findings, and completes when no issues remain. The same section notes that Codex CLI `0.114.0+` with `codex_hooks` uses a native `Stop` hook for automatic exit gating. The manifest also defines a plan-generation workflow that transforms drafts into structured plans with goals, AC-X acceptance criteria, path boundaries, feasibility hints, dependencies, and milestones at `skills/humanize/SKILL.md:50-57`.

- inputs_outputs_state: Inputs are plan files for RLCR (`setup-rlcr-loop.sh path/to/plan.md`), optional review-only mode via `--skip-impl`, draft files for `validate-gen-plan-io.sh --input ... --output ...`, one-shot ask-codex questions, and loop options such as `--max`, model/timeout, base branch, plan tracking, push policy, agent teams, quiz/yolo flags, and privacy at `skills/humanize/SKILL.md:61-88` and `skills/humanize/SKILL.md:97-109`. Outputs are project-local `.humanize/` artifacts. RLCR state is stored under `.humanize/rlcr/<timestamp>/` with files including `state.md`, `goal-tracker.md`, per-round summaries and review results, finalize state/summary, methodology analysis files, and `complete-state.md`; one-shot skill results are stored under `.humanize/skill/<timestamp>/` with `input.md`, `output.md`, and `metadata.md` at `skills/humanize/SKILL.md:180-203`. The goal-tracker state model splits immutable ultimate goal/acceptance criteria from mutable active/completed/deferred tasks and plan evolution logs at `skills/humanize/SKILL.md:152-164`.

- gates_or_invariants: Core invariants are described as process rules rather than implemented locally: always write the required summary before exit, maintain `goal-tracker.md`, include implementation/test detail, do not cheat by editing state files or running cancel commands, and rely on the native Codex `Stop` hook after writing the summary at `skills/humanize/SKILL.md:166-173`. Plan files are expected to include goal description, acceptance criteria with positive/negative tests, path boundaries, allowed/prohibited choices, dependencies/milestones, and implementation notes that prevent leaking plan terminology like `AC-`, `Milestone`, or `Step` into code at `skills/humanize/SKILL.md:111-150`. Exit-code contracts are listed for `ask-codex.sh` and `validate-gen-plan-io.sh`, including validation failures for missing/empty input, missing output directory, pre-existing output file, no write permission, invalid args, and missing template at `skills/humanize/SKILL.md:214-229`.

- dependencies_and_callers: `skills/humanize/SKILL.md` depends on installer hydration of `{{HUMANIZE_RUNTIME_ROOT}}` into an absolute runtime path, explicitly described at `skills/humanize/SKILL.md:12-20`. The installer’s runtime-root hydration and bundle-copy behavior is implemented in `scripts/install-skill.sh`: it validates the presence of all skill manifests and runtime directories at `scripts/install-skill.sh:75-87`, syncs runtime components into `<skills-dir>/humanize` at `scripts/install-skill.sh:155-164`, and replaces `{{HUMANIZE_RUNTIME_ROOT}}` in all skill files via awk at `scripts/install-skill.sh:167-191`. Codex install documentation confirms this directory is the runtime dependency root containing `scripts/`, `hooks/`, `prompt-template/`, `templates/`, `config/`, and `agents/` at `docs/install-for-codex.md:25-32` and `docs/install-for-codex.md:48-66`. The sibling `skills/humanize-rlcr/SKILL.md` is the Codex flow entrypoint and calls the same runtime setup script at `skills/humanize-rlcr/SKILL.md:24-32`, then relies on `.humanize/rlcr/...` prompts/summaries and native Stop-hook blocking at `skills/humanize-rlcr/SKILL.md:36-68`. Sibling flow manifests cover plan generation and refine-plan: `skills/humanize-gen-plan/SKILL.md:19-47` describes validation, relevance checks, issue resolution, plan generation, and review loops; `skills/humanize-refine-plan/SKILL.md:18-48` describes annotated-plan parsing, CMT classification, refinement, QA generation, alternate-language variants, and atomic writes. The command-layer Claude entrypoint `commands/start-rlcr-loop.md` adds pre-setup plan compliance and quiz gates before invoking `setup-rlcr-loop.sh` at `commands/start-rlcr-loop.md:13-57`, `commands/start-rlcr-loop.md:60-105`, and `commands/start-rlcr-loop.md:109-115`.

- edge_cases_or_failure_modes: Manifest-level edge cases include missing Codex CLI prerequisite (`skills/humanize/SKILL.md:175-178`), invalid ask-codex or plan-generation IO exit codes (`skills/humanize/SKILL.md:214-229`), review-only mode where no plan is required (`skills/humanize/SKILL.md:67-69`, `skills/humanize/SKILL.md:80`), maximum-iteration auto-stop (`skills/humanize/SKILL.md:74`), plan-file immutability when tracked (`skills/humanize/SKILL.md:81`), required pushes when `--push-every-round` is enabled (`skills/humanize/SKILL.md:82`), direct Claude handling of Codex open questions via `--claude-answer-codex` (`skills/humanize/SKILL.md:83`), and privacy disabling methodology analysis (`skills/humanize/SKILL.md:87`). Because the directory is only a manifest in this branch export, actual enforcement failures are delegated to scripts/hooks such as `setup-rlcr-loop.sh`, `cancel-rlcr-loop.sh`, `ask-codex.sh`, `validate-gen-plan-io.sh`, `humanize.sh`, and the Stop hook wrapper. The wrapper `scripts/rlcr-stop-gate.sh` maps allow/block/runtime errors to `0`, `10`, and `20` respectively at `scripts/rlcr-stop-gate.sh:8-15`, allows benign no-project contexts at `scripts/rlcr-stop-gate.sh:81-87`, and fails if the hook is missing/non-executable or `jq` is absent at `scripts/rlcr-stop-gate.sh:89-97`.

- validation_or_tests: No tests live under `skills/humanize` itself. Validation is primarily external and contract-based. The manifest points to command exit codes for `ask-codex.sh` and `validate-gen-plan-io.sh` at `skills/humanize/SKILL.md:214-229`; sibling and repo tests cover the runtime components referenced by the manifest, including ask-codex, stop gate, monitor runtime, skill monitor, plan validation, finalize phase, config merging/error handling, hook install, template references, state transitions, and robustness suites under `tests/`. The install documentation’s verification steps assert that `humanize` plus sibling skills are installed and that runtime dependencies exist under the installed `humanize/` directory at `docs/install-for-codex.md:36-80`. Git metadata checks were attempted but this read-only branch export did not expose a usable `.git` repository; `git status` failed with “not a git repository,” and diff checks against the source commit could not be used as validation evidence.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 assigned item section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`