# agent_156 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-156 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: This file is a Claude-facing finalize-phase prompt template used when the normal code-review phase is skipped. Its algorithmic role is not runtime business logic; it is a control-surface contract for the RLCR loop’s phase transition from implementation completion into finalize when automated review cannot run. It tells the agent that validation is incomplete, requires manual verification, optionally allows simplification-only refactoring, and requires a finalize summary artifact.

- algorithmic_behavior: The template starts with a review-skip warning and interpolates `{{REVIEW_SKIP_REASON}}` at lines 1-5. It then converts the skipped review into a manual verification workflow: review changes, run available tests, and check quality issues at lines 7-13. Lines 15-24 define optional use of the `code-simplifier:code-simplifier` subagent and scope it to changes between `{{BASE_BRANCH}}` and `{{START_BRANCH}}`. Lines 26-34 impose functionality-preservation constraints: no behavioral change, no existing-test failure, no new bugs, and only equivalent cleanup. Lines 35-39 provide reference links to the original plan and goal tracker. Lines 40-51 define final exit obligations: complete `[mainline]` and `[blocking]` tasks, document non-blocking queued tasks, commit changes, and write the summary to `{{FINALIZE_SUMMARY_FILE}}`.

- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-codex-stop-hook.sh`: `FINALIZE_SUMMARY_FILE`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, `REVIEW_SKIP_REASON`, `BASE_BRANCH`, and `START_BRANCH` at `hooks/loop-codex-stop-hook.sh:1320-1326`. Output is rendered markdown embedded into the hook’s JSON block decision as the `reason` field at `hooks/loop-codex-stop-hook.sh:1356-1363`. The state transition happens inside `enter_finalize_phase`: the active state file is moved to `finalize-state.md` at `hooks/loop-codex-stop-hook.sh:1287-1288`, and the finalize summary target is set to `$LOOP_DIR/finalize-summary.md` at lines 1290-1291. The skipped-review branch is selected only when `skip_reason` is non-empty at line 1293.

- gates_or_invariants: The key gate is that skipped automated review does not mean fully validated completion; the prompt explicitly says the implementation “could not be fully validated” at `prompt-template/claude/finalize-phase-skipped-prompt.md:5`. The invariant is conservative finalization: manual verification and tests are requested before finalizing at lines 9-13, functionality-equivalent changes only are allowed at lines 26-34, and finalize cannot exit cleanly without completing blocking/mainline tasks plus writing a summary at lines 40-51. In the caller, successful code review uses the normal finalize prompt, while skipped review uses this specific prompt; the no-issue review path calls `enter_finalize_phase ""` at `hooks/loop-codex-stop-hook.sh:1275-1278`, while a non-empty skip reason routes to `finalize-phase-skipped-prompt.md` at lines 1293-1326. The execution-cron protocol also matters for this research run: workers may only report the worker-self-tested cursor, and the master lane owns final acceptance; the relevant skill instructions define that split at `/Users/wangweiyang/.codex/skills/execution-cron-builder/SKILL.md:15-31`.

- dependencies_and_callers: The direct caller is `enter_finalize_phase` in `hooks/loop-codex-stop-hook.sh:1283-1365`. The only direct reference found by repository search is `hooks/loop-codex-stop-hook.sh:1320`. The skip reason currently originates when no `BASE_BRANCH` is configured for code review: `REVIEW_SKIPPED=true` and `REVIEW_SKIP_REASON="No base_branch configured for code review"` are set at `hooks/loop-codex-stop-hook.sh:1844-1852`. Template loading/rendering is handled by `hooks/lib/template-loader.sh`: placeholders use `{{VARIABLE_NAME}}` syntax, missing variables remain literal, and substitution is single-pass per lines 7-14 and 48-56. `load_and_render_safe` returns a rendered fallback if this template is missing or empty at `hooks/lib/template-loader.sh:185-210`. A near-equivalent inline fallback exists in the caller at `hooks/loop-codex-stop-hook.sh:1294-1318`.

- edge_cases_or_failure_modes: If the template file is missing or empty, the hook still proceeds using the inline fallback via `load_and_render_safe`, so the finalize transition is resilient but may lose details present only in the file, such as the reference-file section and precise task-state wording. If a variable is not supplied, the template renderer leaves the placeholder unchanged rather than failing, based on `hooks/lib/template-loader.sh:7-14` and `hooks/lib/template-loader.sh:115-121`; this could expose unresolved `{{...}}` text in the agent prompt. Because substitution is single-pass, placeholder-like text inside variable values is not recursively expanded, which prevents prompt corruption but also means nested template values are intentionally inert. The prompt asks for a commit at line 44, so in read-only or no-write contexts this instruction is not directly satisfiable; the surrounding system must ensure the prompt is only used in writable finalize contexts or that the agent reports the limitation. The template is a prompt contract, so its behavioral guarantees rely on the agent following the rendered instructions rather than a hard shell-level enforcement in this file.

- validation_or_tests: No direct unit or integration test reference for `finalize-phase-skipped-prompt.md` was found by searching for its filename, heading, and `REVIEW_SKIP_REASON`. The surrounding validation mechanism is indirect: `load_and_render_safe` provides fallback behavior and placeholder rendering; the stop hook emits the rendered prompt as a JSON block decision. Repository search found the direct call site and related variable usage in `hooks/loop-codex-stop-hook.sh`, plus generic template-loader behavior in `hooks/lib/template-loader.sh`. Git metadata validation was not available in this read-only export: `git rev-parse HEAD && git status --short` failed because the export is not a Git repository and the sandbox also blocked creation of temporary xcrun cache files.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section present for the assigned prompt-template file
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`