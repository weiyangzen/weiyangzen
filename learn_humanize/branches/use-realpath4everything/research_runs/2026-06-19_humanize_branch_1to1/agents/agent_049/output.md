# agent_049 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-049 `file` `scripts/bitlesson-init.sh`
- cursor: `[_]`
- core_role: Repository/project BitLesson knowledge-base initializer. The script is the setup-time gate that guarantees a project has a BitLesson file before RLCR loop state and later BitLesson enforcement are enabled. It is called from `scripts/setup-rlcr-loop.sh:863` with `--project-root "$PROJECT_ROOT"`, `--template "$SCRIPT_DIR/../templates/bitlesson.md"`, and `--bitlesson-relpath ".humanize/bitlesson.md"`; the setup script then records that relative path in loop state at `scripts/setup-rlcr-loop.sh:899`-`901`.
- algorithmic_behavior: It is a small deterministic Bash workflow with `set -euo pipefail` at `scripts/bitlesson-init.sh:3`. Argument parsing at `scripts/bitlesson-init.sh:22`-`46` accepts `--project-root`, `--template`, optional `--bitlesson-relpath`, and help. Defaults are initialized at `scripts/bitlesson-init.sh:18`-`20`, with `.humanize/bitlesson.md` as the default BitLesson target. After validation, it canonicalizes the project root with physical path resolution via `PROJECT_ROOT_ABS="$(cd "$PROJECT_ROOT" && pwd -P)"` at `scripts/bitlesson-init.sh:75`, builds the target path at `scripts/bitlesson-init.sh:76`, rejects non-file collisions at `scripts/bitlesson-init.sh:78`-`81`, creates the parent directory and copies the template only when the file is absent at `scripts/bitlesson-init.sh:83`-`86`, then prints the resolved target path at `scripts/bitlesson-init.sh:88`.
- inputs_outputs_state: Inputs are CLI arguments only: required `--project-root`, required `--template`, and optional `--bitlesson-relpath`. The template file is expected to be `templates/bitlesson.md`, whose initial knowledge-base scaffold contains the `# BitLesson Knowledge Base` heading, strict entry format, and `## Entries` placeholder at `templates/bitlesson.md:1`-`23`. Output on success is a single stdout line containing the resolved BitLesson file path. Error/help output goes to stderr via `usage()` at `scripts/bitlesson-init.sh:5`-`16` and validation messages at `scripts/bitlesson-init.sh:48`-`72` and `78`-`80`. Persistent state transition is intentionally narrow: absent target file becomes a copied template; existing regular target file is preserved without overwrite; existing non-regular target path causes a hard failure. It does not modify loop state directly; `setup-rlcr-loop.sh` writes `bitlesson_required`, `bitlesson_file`, and `bitlesson_allow_empty_none` after initialization at `scripts/setup-rlcr-loop.sh:880`-`907`.
- gates_or_invariants: Required argument gates: missing `--project-root` fails at `scripts/bitlesson-init.sh:48`-`52`; missing `--template` fails at `scripts/bitlesson-init.sh:54`-`58`. Filesystem gates: project root must be an existing directory at `scripts/bitlesson-init.sh:60`-`63`; template must be an existing regular file at `scripts/bitlesson-init.sh:65`-`68`. Path safety gate: `--bitlesson-relpath` must not be absolute and must not contain a `..` path segment at `scripts/bitlesson-init.sh:70`-`73`. Non-overwrite invariant: the script copies only when `[[ ! -f "$BITLESSON_FILE" ]]` at `scripts/bitlesson-init.sh:83`; a pre-existing regular file is left unchanged. Collision invariant: an existing target that is not a regular file fails before any copy at `scripts/bitlesson-init.sh:78`-`81`. Realpath-relevant invariant: project root is physical-canonicalized with `pwd -P`, so the printed and used base path resolves symlinks for the root component.
- dependencies_and_callers: Direct runtime dependencies are POSIX-ish shell utilities available to Bash: `cd`, `pwd -P`, `dirname`, `mkdir -p`, `cp`, and shell conditionals/regex. The direct caller found in the repository is `scripts/setup-rlcr-loop.sh:863`-`866`, under the “Initialize BitLesson File” section at `scripts/setup-rlcr-loop.sh:857`-`867`. Downstream consumers are not called by this script but rely on the initialized file path: docs describe the workflow at `docs/bitlesson.md:27`-`34`; `hooks/loop-codex-stop-hook.sh:148`-`158` reads/sanitizes the `bitlesson_file` state value and constructs the BitLesson file path; `scripts/bitlesson-validate-delta.sh` validates round summaries against the BitLesson file; `scripts/bitlesson-select.sh` selects applicable lesson IDs from that file. This initializer therefore coordinates setup-time file existence with later selector and stop-gate behavior, but it does not validate lesson content itself.
- edge_cases_or_failure_modes: Unknown arguments fail immediately after printing usage at `scripts/bitlesson-init.sh:40`-`44`. Supplying an option without a following value sets the variable to empty through `${2:-}` and then `shift 2`; with `set -e`, a missing second argument may also produce a shell shift failure depending on argument count. Empty required values are caught by the later required-argument checks. A relative `--template` is accepted and checked relative to the caller’s current directory; unlike `PROJECT_ROOT`, the template path is not canonicalized before `cp`. A `--bitlesson-relpath` such as `subdir/../file` is rejected, but the script does not restrict characters beyond absolute path and `..` segment checks; whitespace or unusual shell-safe characters can pass because variables are quoted. If the target parent directory cannot be created, or `cp` fails due to permissions/read-only filesystem, `set -e` exits nonzero. Existing symlink behavior is determined by Bash `-f`: a symlink to a regular file is treated as a file and preserved; a broken symlink or directory at the target path triggers the non-regular collision error through `[[ -e "$BITLESSON_FILE" && ! -f "$BITLESSON_FILE" ]]`.
- validation_or_tests: I inspected the file directly with line numbers and searched repository references. A syntax check `bash -n scripts/bitlesson-init.sh` completed successfully with exit code 0. I did not run a create/copy behavior test because this branch export is read-only and the instructions prohibit modifying files. `git rev-parse HEAD` and `git status --short` were attempted only for environment confirmation, but both failed because this export is not a git checkout in the sandbox and the toolchain attempted to create `/tmp/xcrun_db-*` cache files, which the read-only environment denied. The closest repository-level behavior coverage is indirect: docs specify initialization from `templates/bitlesson.md` at `docs/bitlesson.md:29`-`34`, while BitLesson routing and delta validation have separate tests such as `tests/test-bitlesson-select-routing.sh` and `tests/test-bitlesson-validate-delta.sh`; I did not find a dedicated direct unit test for `scripts/bitlesson-init.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 item evidence section present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`