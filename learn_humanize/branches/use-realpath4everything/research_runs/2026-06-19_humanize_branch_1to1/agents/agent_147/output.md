# agent_147 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-147 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Read-hook block template for the RLCR loop guard when a model tries to read loop-owned files from the wrong place. The template is small but sits on a core safety path: it turns a validator denial into actionable guidance that re-anchors the agent on the active loop directory and current-round prompt/summary files. Direct file content inspected at `prompt-template/block/wrong-file-location.md:1-9`.
- algorithmic_behavior: The template renders a `# Wrong File Location` denial message, substitutes `{{FILE_PATH}}`, `{{ACTIVE_LOOP_DIR}}`, and `{{CURRENT_ROUND}}`, lists the current prompt and summary paths as `{{ACTIVE_LOOP_DIR}}/round-{{CURRENT_ROUND}}-prompt.md` and `{{ACTIVE_LOOP_DIR}}/round-{{CURRENT_ROUND}}-summary.md`, then gives a literal command hint, `cat {{FILE_PATH}}`. It does not itself decide allow/block; `hooks/loop-read-validator.sh` does that and writes the rendered template to stderr before exiting with code `2`.
- inputs_outputs_state: Inputs are render variables passed through `load_and_render_safe`: `FILE_PATH`, `ACTIVE_LOOP_DIR`, `CURRENT_ROUND`; one call site also passes `CORRECT_PATH`, but this template does not consume it. Output is markdown text for stderr, with no filesystem writes and no state mutation. State used by callers comes from hook JSON input, project-root resolution, active-loop discovery, and parsed loop state: `FILE_PATH` is read from `.tool_input.file_path` at `hooks/loop-read-validator.sh:41-47`; `ACTIVE_LOOP_DIR` is selected via `find_active_loop` at `hooks/loop-read-validator.sh:76-84` and `hooks/loop-read-validator.sh:197-201`; `CURRENT_ROUND` is populated from strict state parsing at `hooks/loop-read-validator.sh:204-217`.
- gates_or_invariants: The template is emitted only after the read validator has identified a protected RLCR path and selected an active loop. Main gate 1: old or wrong-location `goal-tracker.md` inside `.humanize/rlcr` is blocked when its normalized path differs from `$ACTIVE_LOOP_DIR/goal-tracker.md`, then this template is rendered at `hooks/loop-read-validator.sh:227-242`. Main gate 2: current round prompt/summary/contract reads outside the active humanize loop directory are blocked at `hooks/loop-read-validator.sh:270-279`. Related invariants before this block include `Read`-tool-only handling (`hooks/loop-read-validator.sh:35-43`), malformed hook JSON/deep nesting rejection (`hooks/loop-read-validator.sh:23-33`), active-loop isolation by session (`hooks/lib/loop-common.sh:308-331`), strict state parsing fail-closed (`hooks/loop-read-validator.sh:211-215`), and allowlisted historical files bypassing round-number rejection (`hooks/lib/loop-common.sh:816-835`).
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-file-location.md" ...` at lines `236` and `275`. Rendering depends on `hooks/lib/template-loader.sh`: placeholders use `{{VAR}}` syntax, missing variables remain literal, and replacement is single-pass to avoid placeholder injection (`hooks/lib/template-loader.sh:7-14`, `hooks/lib/template-loader.sh:48-56`, `hooks/lib/template-loader.sh:69-135`). Fallback behavior is defined by `load_and_render_safe`, which emits caller fallback text only if the template is missing, empty, or render output is empty (`hooks/lib/template-loader.sh:185-210`). The read validator also depends on `hooks/lib/loop-common.sh` for active-loop discovery, path/file classification, round extraction, allowlisting, and state parsing; project-root resolution is intentionally realpath-based in `hooks/lib/project-root.sh:3-20` and `hooks/lib/project-root.sh:41-53`.
- edge_cases_or_failure_modes: Because the template exists and is non-empty, caller fallbacks containing more specific text are normally suppressed. For the goal-tracker wrong-path case, `CORRECT_PATH` is passed at `hooks/loop-read-validator.sh:237-240`, but `prompt-template/block/wrong-file-location.md` never references `{{CORRECT_PATH}}`, so the rendered message points to active prompt/summary files and a `cat {{FILE_PATH}}` hint rather than explicitly naming `$ACTIVE_LOOP_DIR/goal-tracker.md`; that is a UX precision issue in this specific call path. The final hint says to use `cat {{FILE_PATH}}`, even though the read attempt has just been blocked for protected-path misuse; this is consistent with sibling templates such as `wrong-round-file.md`, but it may be confusing if the intended remediation is to read the active-loop canonical path instead. If `CURRENT_ROUND` or `ACTIVE_LOOP_DIR` were absent due to upstream failure, placeholders would remain literal by renderer design, but current caller flow exits earlier when no active loop exists and fails closed on malformed state. Symlink and canonical-prefix behavior is handled outside this template by validator helpers; this file does not enforce canonicalization itself.
- validation_or_tests: No direct test assertion was found for `prompt-template/block/wrong-file-location.md` by name beyond caller references. Renderer behavior is covered generally in `tests/test-template-loader.sh`, including special-character substitution and real-template rendering for another block at `tests/test-template-loader.sh:132-164`. Comprehensive template tests similarly validate the template directory and real-template rendering patterns at `tests/test-templates-comprehensive.sh:480-514`. The read-validator goal-tracker old-session block path is exercised at `tests/robustness/test-hook-system-robustness.sh:538-550`, checking exit code `2`, though it does not assert this exact rendered text. I did not run the full test suite; this was a read-only research pass. A `git status`/rev-parse attempt failed because this export is not a writable git checkout and macOS toolchain cache creation under `/tmp` was blocked, so the supplied source commit metadata is taken from the task prompt.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 of 1 item evidence sections present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`