# agent_069 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-069 `file` `tests/test-bitlesson-select-routing.sh`
- cursor: `[_]`
- core_role: This file is an executable specification for BitLesson selector provider routing. It verifies that [scripts/bitlesson-select.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-select.sh:1) selects Codex or Claude based on merged config, handles provider fallback, short-circuits empty lesson stores, and invokes Codex in a constrained helper mode. The test is part of the main suite via [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/run-all-tests.sh:95).

- algorithmic_behavior: The test derives `SCRIPT_DIR`, `PROJECT_ROOT`, and `BITLESSON_SELECT` from its own location, then sources shared assertions from [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:30). It builds temporary project fixtures with `.humanize/config.json`, BitLesson files, and mock `codex`/`claude` binaries. Key fixture sections are placeholder BitLesson content at [tests/test-bitlesson-select-routing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-bitlesson-select-routing.sh:18), real root-level BitLesson content at [tests/test-bitlesson-select-routing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-bitlesson-select-routing.sh:29), real `.humanize/bitlesson.md` content at [tests/test-bitlesson-select-routing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-bitlesson-select-routing.sh:48), Codex mocks at [tests/test-bitlesson-select-routing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-bitlesson-select-routing.sh:67), and Claude mocks at [tests/test-bitlesson-select-routing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-bitlesson-select-routing.sh:119). Tests 1 and 1b prove `gpt-*` models route to `codex exec`, require a trailing `-`, and pass the selector prompt on stdin. Tests 2-4 prove `haiku`, `sonnet`, and uppercase `OPUS` route to Claude. Test 5 proves unknown model names fail clearly. Test 6 proves a Codex-routed model cannot proceed without `codex`. Test 7 proves a Claude-routed model falls back to Codex when `claude` is unavailable. Test 8 proves `provider_mode: "codex-only"` forces Codex even when `bitlesson_model` is `haiku`. Test 9 proves a placeholder BitLesson file returns `LESSON_IDS: NONE` without invoking model binaries. Test 10 proves Codex helper invocation disables hooks, uses read-only sandboxing, uses skip-git/ephemeral flags when supported, and avoids `--full-auto`.

- inputs_outputs_state: Inputs are command-line arguments `--task`, `--paths`, `--bitlesson-file`; temp project config under `$TEST_DIR/.humanize/config.json`; BitLesson markdown content; `CLAUDE_PROJECT_DIR`; `XDG_CONFIG_HOME`; and a deliberately controlled `PATH`. The expected stdout contract is exactly the two-line selector shape `LESSON_IDS:` and `RATIONALE:`, which mirrors the runtime selector output enforcement in [scripts/bitlesson-select.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-select.sh:242). State is intentionally per-test and temporary: `setup_test_dir` creates a fresh `TEST_DIR` and installs cleanup through `trap` in [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:86). Test-local state includes generated config files, mock binaries under `$TEST_DIR/bin` or mode-specific bin dirs, captured stdin in `codex-stdin.txt`, and captured Codex CLI args in `codex-args.txt`. The test’s observable state transition is from a configured provider/model scenario to either pass/fail counter increments through `pass`/`fail`, then final suite success/failure through `print_test_summary`.

- gates_or_invariants: The test enforces strict PATH isolation for missing-binary cases with `SAFE_BASE_PATH="/usr/bin:/bin:/usr/sbin:/sbin"` at [tests/test-bitlesson-select-routing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-bitlesson-select-routing.sh:9), preventing accidental success from user-local Codex or Claude installations. The Codex mocks require a trailing `-` and non-empty stdin, so the selector must stream the prompt rather than relying on implicit CLI input. The target selector’s provider gate is `detect_provider`, where `gpt-*` and `o[0-9]*` map to Codex, Claude family names map to Claude case-insensitively, and unknown names are hard errors in [scripts/lib/model-router.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/model-router.sh:10). The dependency gate is `check_provider_dependency`, which checks the required binary with `command -v` and emits provider-specific install guidance on failure in [scripts/lib/model-router.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/model-router.sh:32). Runtime selector invariants include required non-empty task/paths, existing non-empty BitLesson file, placeholder short-circuit if no `## Lesson:` exists, a 120-second timeout, and output normalization back to stable `LESSON_IDS`/`RATIONALE` lines in [scripts/bitlesson-select.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-select.sh:76).

- dependencies_and_callers: Direct dependencies are `bash`, `mktemp`, `grep`, `printf`, `cat`, `chmod`, and the repo’s shared test helper. The test exercises `scripts/bitlesson-select.sh`, which depends on `scripts/lib/config-loader.sh`, `scripts/lib/model-router.sh`, `hooks/lib/project-root.sh`, `scripts/portable-timeout.sh`, and `hooks/lib/loop-common.sh`. Config merging comes from default, user, and project config layers via `load_merged_config` in [scripts/lib/config-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/config-loader.sh:63), while `DEFAULT_CODEX_MODEL` is established by `loop-common.sh` with fallback `gpt-5.4` in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/loop-common.sh:204). Project root resolution is canonicalized through `resolve_project_root`, which prioritizes `CLAUDE_PROJECT_DIR`, then git root, and realpaths the result in [hooks/lib/project-root.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/project-root.sh:41). Caller context is the BitLesson selector agent contract: [agents/bitlesson-selector.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/agents/bitlesson-selector.md:21) states runtime execution happens through `scripts/bitlesson-select.sh`, and [docs/bitlesson.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/docs/bitlesson.md:7) documents the same automatic provider routing and Codex-only override.

- edge_cases_or_failure_modes: Covered edge cases include unknown model config, missing provider binaries, Claude-to-Codex fallback, Codex missing after fallback, uppercase Claude-family names, placeholder BitLesson files with no recorded lessons, root-level versus `.humanize` BitLesson paths, stdin prompt delivery to Codex, and Codex CLI feature probing for `--disable`, `--skip-git-repo-check`, and `--ephemeral`. A subtle edge is that fallback is asymmetric in the selector: if a Claude provider binary is missing, the selector rewrites to the default Codex model/provider and checks Codex; if Codex is missing, the second dependency check fails and the command exits non-zero. Another important failure mode guarded by Test 10 is accidental Codex hook recursion or broad autonomy: the expected args include `--disable codex_hooks`, `-s read-only`, and no `--full-auto`.

- validation_or_tests: The assigned file itself is the validation asset. It uses manual assertions: each scenario captures stdout/stderr and exit code, then calls `pass` or `fail`; final status is returned by `print_test_summary` in [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-helpers.sh:58). I did not execute it because this branch export is read-only and the task asked for research notes only, but direct inspection shows it is wired into the top-level test runner and is executable as `bash tests/test-bitlesson-select-routing.sh` in a writable/testable environment with required base tools such as `jq`.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`