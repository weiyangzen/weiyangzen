# agent_088 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-088 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR plan-file protection. The file is a shell test harness that verifies hook behavior around the plan file copy stored under `.humanize/rlcr/.../plan.md`, the original project plan path recorded in loop state, branch/state schema invariants, stop-hook integrity checks, command-bypass blocking, and legacy path exclusions. It is not runtime production logic, but it is core algorithm evidence because it defines required safety behavior for the hooks that enforce plan immutability and loop-state coherence.
- algorithmic_behavior: The script builds isolated temporary git repositories and loop directories, then feeds synthetic Claude hook JSON into the hook entry points. Its setup path creates a temporary repo, a gitignored `plans/test-plan.md`, a loop backup at `.humanize/rlcr/2024-01-01_12-00-00/plan.md`, and `state.md` frontmatter with `current_round`, `max_iterations`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, `review_started`, and anti-drift fields at [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-plan-file-hooks.sh:78). It mocks `codex` so stop-hook review paths do not call the real CLI at [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-plan-file-hooks.sh:40). The first block verifies `loop-plan-file-validator.sh` permits valid state, strips YAML quotes from `plan_file`, blocks malformed v1.5.0 state, and blocks branch drift at [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-plan-file-hooks.sh:145) through [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-plan-file-hooks.sh:243). The Write/Edit/Bash blocks assert that direct writes, edits, redirections, removals, and shell expansion bypasses against `.humanize/rlcr/.../plan.md` deny with exit code `2` at [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-plan-file-hooks.sh:252), [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-plan-file-hooks.sh:269), and [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-plan-file-hooks.sh:286). Stop-hook tests assert plan-source immutability, backup presence, tracked-file race detection, committed tracked-plan mutation detection, outdated schema JSON blocking, round-contract presence, and section-specific goal-tracker placeholder diagnostics at [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-plan-file-hooks.sh:450) through [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-plan-file-hooks.sh:1126). Final negative tests assert `.humanize-loop.local/.../plan.md` is no longer treated as protected RLCR loop state at [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-plan-file-hooks.sh:1129).
- inputs_outputs_state: Inputs are synthetic hook payloads on stdin, mostly `{}` for prompt/stop hooks and JSON objects with `tool_name` plus `tool_input.file_path` or `tool_input.command` for PreToolUse validators. The script also controls environment via `CLAUDE_PROJECT_DIR`, `XDG_CACHE_HOME`, `PATH`, temporary git branches, tracked vs gitignored plan files, and generated loop artifacts. Expected outputs are hook exit codes plus stderr/stdout messages. Prompt and stop hooks signal “block” as JSON with a `"decision"` field while still exiting `0`; Write/Edit/Bash validators use exit `2` for hook denial and nonzero validation errors for malformed hook input. State transitions are simulated by rewriting `state.md`, switching branches, mutating or deleting the project plan, deleting the backup, creating or omitting `round-0-summary.md`, `round-0-contract.md`, and `goal-tracker.md`, and committing tracked-plan changes to test both dirty and clean-git mutation cases. The test counters `TESTS_PASSED`, `TESTS_FAILED`, and `TESTS_SKIPPED` are incremented by `pass`, `fail`, and `skip`, and the script exits with the failure count at [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-plan-file-hooks.sh:1173).
- gates_or_invariants: Required state frontmatter must include current round, max iterations, plan path, plan tracking flag, start/base branch, and review status. The hook-side parser strips double quotes from `plan_file`, `start_branch`, `base_branch`, and related fields in `hooks/lib/loop-common.sh:447-453`, which the YAML quote tests exercise. Branch consistency is a hard gate in `hooks/loop-plan-file-validator.sh:96-116` and `hooks/loop-codex-stop-hook.sh:286-316`. The plan backup inside `.humanize/rlcr` is read-only: Write blocks `basename plan.md` inside `.humanize/rlcr` at `hooks/loop-write-validator.sh:226-236`, Edit does the same at `hooks/loop-edit-validator.sh:176-187`, and Bash blocks commands modifying `\.humanize/rlcr(/[^/]+)?/plan\.md` at `hooks/loop-bash-validator.sh:472-483`. Stop-hook plan integrity requires the backup to exist, the original plan file to exist, tracked plans to have no uncommitted status, and original-vs-backup content to match by `diff -q` at `hooks/loop-codex-stop-hook.sh:318-399`. Round contracts are required when anti-drift state is present at `hooks/loop-codex-stop-hook.sh:786-817`. Goal-tracker placeholder diagnostics must be section-specific, using separate Ultimate Goal, Acceptance Criteria, and Active Tasks extraction at `hooks/loop-codex-stop-hook.sh:840-916`. Legacy `.humanize-loop.local` paths are explicitly allowed by these tests because the active loop classifier checks `.humanize/rlcr/` rather than the old dash-separated directory form at `hooks/lib/loop-common.sh:1252-1256`.
- dependencies_and_callers: Direct hook dependencies invoked by the test are `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. Shared behavior comes from `hooks/lib/loop-common.sh`, `hooks/lib/project-root.sh`, `hooks/lib/template-loader.sh`, and `scripts/portable-timeout.sh`. The project-root helper prioritizes `CLAUDE_PROJECT_DIR`, falls back to `git rev-parse --show-toplevel`, and canonicalizes through `realpath` at `hooks/lib/project-root.sh:41-53`; this is relevant to the branch theme because hook path comparisons are intended to operate against canonical project roots. The command-detection dependency for Bash denial is `command_modifies_file`, which checks redirection, append, `tee`, in-place editors, `mv`/`cp`, `rm`, `dd`, `truncate`, `printf`, and `exec` redirection patterns at `hooks/lib/loop-common.sh:1482-1510`. `find_active_loop` selects the active `.humanize/rlcr` session directory, optionally filtered by session id, at `hooks/lib/loop-common.sh:332-425`. The test itself is likely run by repo test aggregators such as `tests/run-all-tests.sh`, but this assigned file is self-contained and can also be executed directly as `bash tests/test-plan-file-hooks.sh` in a writable environment.
- edge_cases_or_failure_modes: The file targets several failure modes: malformed or legacy state missing v1.5.0 fields, missing `start_branch`, quoted YAML values that would otherwise compare literally, branch switching during an active loop, write/edit/bash attempts against the backup plan, direct `.humanize/rlcr/plan.md` without timestamp subdir, command substitution, glob, brace expansion, piped `tee`, and backtick substitution bypasses. Stop-hook cases include modified gitignored plan, deleted plan, missing backup, tracked plan with uncommitted mutation, tracked plan with committed mutation where `git status` is clean but content differs from the backup, missing round contract, outdated schema returning a JSON block instead of silently exiting, and goal-tracker placeholder false positives across unrelated sections. Negative tests cover a deliberate non-blocking path: `.humanize-loop.local/.../plan.md` must not be protected as current RLCR state. One notable edge in the implementation under test is that `is_in_humanize_loop_dir` is substring-based, so broader path normalization and traversal behavior is likely covered by sibling robustness tests rather than this file. I did not run the script because it creates temporary git repositories and writes under temp/cache paths, while this worker environment is read-only/restricted.
- validation_or_tests: This file is itself validation. It contains 17 numbered/lettered test groups plus subcases, all using isolated temp directories and a mock `codex`. It verifies both allow and deny paths by checking exit codes, JSON block markers, and message substrings. The key expected denial code for PreToolUse validators is `2`, while prompt/stop blocking is generally JSON on stdout with exit `0`. The final harness prints pass/fail/skip counts and exits with `TESTS_FAILED`, making it suitable for CI-style execution when the filesystem and git tooling are writable. Related hook code indicates this executable spec validates real production gates rather than mocks of those gates, except for external Codex review output.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`