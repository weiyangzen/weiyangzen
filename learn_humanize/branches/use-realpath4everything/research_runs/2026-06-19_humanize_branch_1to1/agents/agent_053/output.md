# agent_053 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-053 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Runtime shell entrypoint for the `humanize` command’s monitoring surface. It exposes `humanize monitor rlcr|skill|codex|gemini`, implements the RLCR/Codex live monitor in-file, and delegates skill/codex/gemini invocation monitoring to `scripts/lib/monitor-skill.sh`. It also provides public parser helpers used by tests and other monitor code: `humanize_split_to_array`, `humanize_parse_goal_tracker_issue_counts`, `humanize_parse_goal_tracker`, `humanize_detect_git_state`, and `humanize_parse_git_status` (`scripts/humanize.sh:24`, `scripts/humanize.sh:38`, `scripts/humanize.sh:71`, `scripts/humanize.sh:158`, `scripts/humanize.sh:208`, `scripts/humanize.sh:1191`).
- algorithmic_behavior: Startup resolves `HUMANIZE_SCRIPT_DIR` from the script path and conditionally sources `scripts/lib/monitor-common.sh`; it also resolves `../hooks/lib` and conditionally sources `hooks/lib/loop-common.sh` for shared constants (`scripts/humanize.sh:7`, `scripts/humanize.sh:13`). Goal-tracker parsing counts unique AC IDs in the Acceptance Criteria section, active/completed/deferred task table rows, Completed and Verified rows, issue tables, and first Ultimate Goal content line, returning a pipe-delimited summary (`scripts/humanize.sh:91`, `scripts/humanize.sh:103`, `scripts/humanize.sh:127`, `scripts/humanize.sh:133`, `scripts/humanize.sh:143`, `scripts/humanize.sh:148`). Git parsing shells out to `git status --porcelain` and `git diff --shortstat`, classifies modified/added/deleted/untracked states, and returns numeric counters (`scripts/humanize.sh:216`, `scripts/humanize.sh:221`, `scripts/humanize.sh:240`, `scripts/humanize.sh:251`). The RLCR monitor requires `.humanize/rlcr`, finds the newest timestamped session through `monitor_find_latest_session`, locates logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project-path>/<session>`, chooses the highest round log among `round-*-codex-run.log` and `round-*-codex-review.log`, and validates that review rounds start after run rounds (`scripts/humanize.sh:266`, `scripts/humanize.sh:272`, `scripts/humanize.sh:280`, `scripts/humanize.sh:291`, `scripts/humanize.sh:295`, `scripts/humanize.sh:305`, `scripts/humanize.sh:338`, `scripts/humanize.sh:364`). The monitor parses `state.md`/stop-state files into round/model/effort/plan/review/team/push/drift fields, draws a fixed terminal status bar, tails log content incrementally, and switches to newer sessions/logs as they appear (`scripts/humanize.sh:377`, `scripts/humanize.sh:382`, `scripts/humanize.sh:414`, `scripts/humanize.sh:474`, `scripts/humanize.sh:521`, `scripts/humanize.sh:607`, `scripts/humanize.sh:838`, `scripts/humanize.sh:1075`, `scripts/humanize.sh:1095`, `scripts/humanize.sh:1134`, `scripts/humanize.sh:1161`). Final command routing maps `monitor rlcr` to `_humanize_monitor_codex`, `monitor skill` to `_humanize_monitor_skill`, and codex/gemini filters to `_humanize_monitor_skill --tool-filter ...` (`scripts/humanize.sh:1195`, `scripts/humanize.sh:1200`, `scripts/humanize.sh:1203`, `scripts/humanize.sh:1206`, `scripts/humanize.sh:1209`).
- inputs_outputs_state: Inputs are command arguments to `humanize`, the current working directory, `.humanize/rlcr/<timestamp>/` session directories, session `goal-tracker.md`, active or terminal state files found via `monitor_find_state_file`, log files in the user cache, current git repository status, terminal size/signals, and optional `XDG_CACHE_HOME` (`scripts/humanize.sh:261`, `scripts/humanize.sh:421`, `scripts/humanize.sh:425`, `scripts/humanize.sh:464`, `scripts/humanize.sh:787`). Outputs are terminal UI/status-bar text, live tailed log bytes, usage/error messages, and pipe-delimited helper summaries; the script does not mutate repo files. Runtime state is held in shell locals: `current_session_dir`, `current_file`, `last_size`, `last_no_log_status`, `monitor_running`, `cleanup_done`, and `resize_needed` (`scripts/humanize.sh:267`, `scripts/humanize.sh:731`, `scripts/humanize.sh:779`, `scripts/humanize.sh:833`). State transitions include no session -> error, no log -> centered wait loop, active log -> incremental tail loop, truncated/deleted log -> reset and rediscover, newer session/log -> switch target, deleted `.humanize/rlcr` -> graceful stop (`scripts/humanize.sh:803`, `scripts/humanize.sh:883`, `scripts/humanize.sh:997`, `scripts/humanize.sh:1022`, `scripts/humanize.sh:1081`, `scripts/humanize.sh:1100`, `scripts/humanize.sh:1135`).
- gates_or_invariants: Missing tracker files return zero/default summaries rather than failing (`scripts/humanize.sh:40`, `scripts/humanize.sh:73`). Missing `.humanize/rlcr` is a hard monitor gate with return 1 (`scripts/humanize.sh:272`). `_find_latest_codex_log` requires an explicit existing session dir and refuses to search globally, preventing cross-session log bleed (`scripts/humanize.sh:297`). It enforces the run/review ordering invariant: max run round must be strictly less than min review round when review logs exist (`scripts/humanize.sh:364`). Terminal height must be at least `status_bar_height + 3`; otherwise it displays a resize prompt and waits (`scripts/humanize.sh:661`, `scripts/humanize.sh:815`, `scripts/humanize.sh:921`, `scripts/humanize.sh:1042`). Cleanup is guarded by `cleanup_done`, resets traps, kills a tail process if one exists, and restores the scroll region (`scripts/humanize.sh:736`). Zsh compatibility gates include `setopt localoptions ksharrays` and TRAP functions; bash uses normal `trap` (`scripts/humanize.sh:262`, `scripts/humanize.sh:787`). A branch-relevant path invariant gap: this file still derives paths with `cd ... && pwd` and uses `project_root="$(pwd)"` for cache-key sanitization, not explicit `realpath`; symlinked CWDs can therefore produce a different cache directory than canonical project-root logic elsewhere (`scripts/humanize.sh:7`, `scripts/humanize.sh:13`, `scripts/humanize.sh:305`).
- dependencies_and_callers: Depends on shell utilities `sed`, `grep`, `sort`, `wc`, `tr`, `head`, `find`, `basename`, `date`, `tput`, `tail`, `stat`, `git`, `kill`, `sleep`, and shell process substitution. It uses `monitor_get_file_size`, `monitor_find_latest_session`, and `monitor_find_state_file` from `scripts/lib/monitor-common.sh`; that library defines cross-platform `stat` file sizing and timestamp-pattern session ordering (`scripts/lib/monitor-common.sh:32`, `scripts/lib/monitor-common.sh:40`, `scripts/lib/monitor-common.sh:154`). It sources `scripts/lib/monitor-skill.sh` after defining `humanize`, which provides `_humanize_monitor_skill` and expects `humanize_split_to_array` plus common monitor helpers (`scripts/humanize.sh:1243`, `scripts/lib/monitor-skill.sh:8`, `scripts/lib/monitor-skill.sh:18`). Upstream producers include `scripts/setup-rlcr-loop.sh`, which creates `.humanize/rlcr`, session `state.md`, copied `plan.md`, and `goal-tracker.md` (`scripts/setup-rlcr-loop.sh:821`, `scripts/setup-rlcr-loop.sh:880`, `scripts/setup-rlcr-loop.sh:929`). Documented callers are README/docs usage for `humanize monitor ...` (`README.md:67`, `docs/usage.md:319`).
- edge_cases_or_failure_modes: Markdown parsing is regex/table-shape dependent: table row counts subtract two header rows, AC totals rely on `AC-?[0-9]+(\.[0-9]+)?`, and pipe-delimited return values can be corrupted if `goal_summary` itself contains `|` (`scripts/humanize.sh:47`, `scripts/humanize.sh:91`, `scripts/humanize.sh:153`). `_count_table_data_rows` is defined inside two functions without `local -f`, so sourcing/invoking may leave a global helper definition in shell scope (`scripts/humanize.sh:45`, `scripts/humanize.sh:80`). Git status classification groups renames as modified and does not explicitly classify conflicts/unmerged states beyond generic M/A/D character checks (`scripts/humanize.sh:229`, `scripts/humanize.sh:230`). `humanize_detect_git_state` checks shallow clones before detached HEAD, so a detached shallow clone reports `shallow`, not `detached` (`scripts/humanize.sh:190`, `scripts/humanize.sh:197`). GNU-only `date -d` is attempted for UTC conversion and falls back to raw timestamp on macOS/BSD (`scripts/humanize.sh:480`). The unused `check_interval=2` local is overridden by fixed `sleep 0.5` loops, so changing that variable has no runtime effect (`scripts/humanize.sh:269`, `scripts/humanize.sh:900`, `scripts/humanize.sh:1024`). Because log cache lookup sanitizes `pwd`, symlinked or non-canonical working directories are a likely failure mode in a `use-realpath4everything` branch: the monitor may not find logs written under a canonicalized project path (`scripts/humanize.sh:303`).
- validation_or_tests: Direct tests source this production file. Goal tracker robustness tests cover standard/mixed/large/malformed tracker shapes and issue breakdown parsing (`tests/robustness/test-goal-tracker-robustness.sh:5`, `tests/robustness/test-goal-tracker-robustness.sh:21`, `tests/robustness/test-goal-tracker-robustness.sh:35`). Git robustness tests cover clean, modified, added, deleted, untracked, detached, rebase/merge, and non-git scenarios (`tests/robustness/test-git-operations-robustness.sh:5`, `tests/robustness/test-git-operations-robustness.sh:19`, `tests/robustness/test-git-operations-robustness.sh:32`). Skill monitor tests source `humanize.sh` and exercise `_humanize_monitor_skill --once`, while RLCR monitor runtime/e2e tests call the real `_humanize_monitor_codex` for deletion, resize, SIGINT, bash, and zsh paths (`tests/test-skill-monitor.sh:3`, `tests/test-skill-monitor.sh:52`, `tests/test-monitor-e2e-real.sh:5`, `tests/test-monitor-e2e-real.sh:54`, `tests/test-zsh-monitor-safety.sh:3`). I did not run tests or modify files; this was a read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`