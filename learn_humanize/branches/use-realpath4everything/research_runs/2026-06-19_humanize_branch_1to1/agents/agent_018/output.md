# agent_018 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-018 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: `scripts/lib` is a shared shell-library directory, not a standalone executable. It provides reusable algorithms for configuration resolution, model/provider routing, and terminal monitoring used by higher-level commands and hooks. Recursive inspection found exactly four files and no child subdirectories: `config-loader.sh`, `model-router.sh`, `monitor-common.sh`, and `monitor-skill.sh`. The directory byte total matches the assignment metadata at 42,166 bytes.
- algorithmic_behavior: `config-loader.sh` implements the repository’s merged-config algorithm. It requires `jq`, prepares config layers as JSON objects, treats the default config as required, treats user/project config as optional, strips nulls recursively, then merges `{}` + default + user + project with later layers winning (`scripts/lib/config-loader.sh:17`, `scripts/lib/config-loader.sh:24`, `scripts/lib/config-loader.sh:63`, `scripts/lib/config-loader.sh:113`). `get_config_value` provides scalar extraction from the merged JSON and stringifies non-string values for shell callers (`scripts/lib/config-loader.sh:139`). `model-router.sh` maps model names to providers by pattern: `gpt-*` and `o[0-9]*` route to `codex`; `claude-*`, `haiku`, `sonnet`, and `opus` route to `claude`; unknown or empty names fail (`scripts/lib/model-router.sh:10`). It also validates provider CLI availability and maps unsupported Claude `xhigh` effort down to `high` while passing supported efforts through (`scripts/lib/model-router.sh:32`, `scripts/lib/model-router.sh:62`). `monitor-common.sh` supplies shared monitor primitives: ANSI color functions, cross-platform file size, latest timestamped session discovery, terminal scroll-region setup/restore, status-to-color mapping, state-file discovery, YAML frontmatter extraction, timestamp formatting, string truncation, and goal-tracker table parsing (`scripts/lib/monitor-common.sh:32`, `scripts/lib/monitor-common.sh:40`, `scripts/lib/monitor-common.sh:71`, `scripts/lib/monitor-common.sh:130`, `scripts/lib/monitor-common.sh:154`, `scripts/lib/monitor-common.sh:201`, `scripts/lib/monitor-common.sh:237`, `scripts/lib/monitor-common.sh:265`, `scripts/lib/monitor-common.sh:300`). `monitor-skill.sh` implements the `.humanize/skill` monitor. It parses `--once` and `--tool-filter`, filters timestamp-named invocation directories, derives tool metadata from `metadata.md` or `input.md`, selects the best watchable invocation/file, counts statuses, renders a one-shot report, or runs an interactive tail loop with fixed status bar and cleanup traps (`scripts/lib/monitor-skill.sh:18`, `scripts/lib/monitor-skill.sh:31`, `scripts/lib/monitor-skill.sh:53`, `scripts/lib/monitor-skill.sh:79`, `scripts/lib/monitor-skill.sh:100`, `scripts/lib/monitor-skill.sh:121`, `scripts/lib/monitor-skill.sh:169`, `scripts/lib/monitor-skill.sh:229`, `scripts/lib/monitor-skill.sh:340`, `scripts/lib/monitor-skill.sh:428`).
- inputs_outputs_state: `load_merged_config` inputs are `plugin_root`, `project_root`, optional `XDG_CONFIG_HOME`, `HOME`, and `HUMANIZE_CONFIG`; outputs merged JSON on stdout and warnings/errors on stderr. State is temporary only: it creates a `mktemp -d` directory, writes layer JSON files, and removes the temp directory on subshell exit (`scripts/lib/config-loader.sh:77`, `scripts/lib/config-loader.sh:84`, `scripts/lib/config-loader.sh:100`). `model-router.sh` inputs are model/provider/effort strings plus `PATH`; outputs provider names, mapped effort names, or stderr errors. It mutates no persistent state (`scripts/lib/model-router.sh:18`, `scripts/lib/model-router.sh:49`, `scripts/lib/model-router.sh:84`). `monitor-common.sh` inputs are filesystem paths, terminal dimensions, markdown/YAML files, and status strings; outputs pipe-delimited summaries or display strings, while terminal setup functions mutate terminal scroll/cursor state (`scripts/lib/monitor-common.sh:78`, `scripts/lib/monitor-common.sh:91`, `scripts/lib/monitor-common.sh:294`, `scripts/lib/monitor-common.sh:382`). `monitor-skill.sh` reads `.humanize/skill`, local invocation files, optional cache files under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize`, and git root for cache path derivation; outputs either a report to stdout in `--once` mode or live terminal UI/tail output in interactive mode. Its loop state is local variables such as `current_skill_dir`, `current_file`, `monitor_running`, `cleanup_done`, and `TAIL_PID` (`scripts/lib/monitor-skill.sh:23`, `scripts/lib/monitor-skill.sh:156`, `scripts/lib/monitor-skill.sh:434`).
- gates_or_invariants: `config-loader.sh` enforces object-only JSON layers; missing/malformed default config is fatal, while missing/malformed optional user/project config becomes `{}` with a warning (`scripts/lib/config-loader.sh:40`, `scripts/lib/config-loader.sh:52`). It uses `exit 1` inside `_config_loader_prepare_layer` for required-layer failures because `set -e` does not reliably propagate nested failures from the subshell (`scripts/lib/config-loader.sh:43`). Null values are stripped before merge, so higher layers cannot delete lower-layer values by setting `null` (`scripts/lib/config-loader.sh:119`). `model-router.sh` gates on known providers, known efforts, and installed `codex`/`claude` binaries (`scripts/lib/model-router.sh:36`, `scripts/lib/model-router.sh:75`). `monitor-common.sh` requires timestamp session names of `YYYY-MM-DD_HH-MM-SS` for latest-session selection and prioritizes active state files before terminal stop-state files (`scripts/lib/monitor-common.sh:55`, `scripts/lib/monitor-common.sh:162`). `monitor-skill.sh` gates on `.humanize/skill` existing, timestamp-prefixed invocation directory names, optional `codex`/`gemini` filters, and watchable file priority: running invocations prefer live cache logs, completed invocations prefer `output.md` before cache output/logs (`scripts/lib/monitor-skill.sh:43`, `scripts/lib/monitor-skill.sh:85`, `scripts/lib/monitor-skill.sh:186`, `scripts/lib/monitor-skill.sh:202`).
- dependencies_and_callers: `scripts/bitlesson-select.sh` sources `config-loader.sh` and `model-router.sh`, loads merged defaults, detects the provider for `bitlesson_model`, optionally falls back from Claude to Codex in `provider_mode=codex-only`, checks provider dependencies, then runs either `codex exec` or `claude --print` (`scripts/bitlesson-select.sh:9`, `scripts/bitlesson-select.sh:20`, `scripts/bitlesson-select.sh:115`, `scripts/bitlesson-select.sh:126`, `scripts/bitlesson-select.sh:188`). `hooks/lib/loop-common.sh` sources `config-loader.sh` after preserving shell options, loads config best-effort once a project root is available, and derives `DEFAULT_BITLESSON_MODEL`, `DEFAULT_CODEX_MODEL`, `DEFAULT_CODEX_EFFORT`, and `DEFAULT_AGENT_TEAMS` (`hooks/lib/loop-common.sh:180`, `hooks/lib/loop-common.sh:189`, `hooks/lib/loop-common.sh:204`). `scripts/humanize.sh` sources `monitor-common.sh` near startup and `monitor-skill.sh` after defining `humanize_split_to_array`; its `humanize monitor` command dispatches to RLCR monitor, all skill monitor, codex-only skill monitor, or gemini-only skill monitor (`scripts/humanize.sh:6`, `scripts/humanize.sh:22`, `scripts/humanize.sh:1195`, `scripts/humanize.sh:1243`). Command docs also specify that `gen-plan` and `refine-plan` must reuse `config-loader.sh` semantics instead of directly reading `.humanize/config.json` (`commands/gen-plan.md:64`, `commands/refine-plan.md:88`). The default config consumed by the merge algorithm lives at `config/default_config.json` with keys including `codex_model`, `codex_effort`, `bitlesson_model`, `agent_teams`, `alternative_plan_language`, and `gen_plan_mode` (`config/default_config.json:1`).
- edge_cases_or_failure_modes: In `config-loader.sh`, lack of `jq` is fatal; missing required default config is fatal; malformed optional configs warn and are ignored; missing optional configs become `{}`; an empty key passed to `get_config_value` is a usage error (`scripts/lib/config-loader.sh:17`, `scripts/lib/config-loader.sh:40`, `scripts/lib/config-loader.sh:57`, `scripts/lib/config-loader.sh:143`). Because the file sets `set -euo pipefail` when sourced, callers such as `loop-common.sh` explicitly preserve and restore shell options around sourcing to avoid unwanted option leakage (`hooks/lib/loop-common.sh:180`). In `model-router.sh`, modern model names outside the hard-coded patterns are rejected; only `codex` and `claude` providers are supported; missing CLI binaries block execution unless a caller implements fallback (`scripts/lib/model-router.sh:28`, `scripts/lib/model-router.sh:44`, `scripts/bitlesson-select.sh:126`). In `monitor-common.sh`, YAML parsing is simple grep/sed frontmatter extraction and does not parse nested YAML or escaped values; state-file stop reason selection uses first `find` result among `*-state.md`, so multiple terminal state files are not explicitly ordered (`scripts/lib/monitor-common.sh:176`, `scripts/lib/monitor-common.sh:209`). `monitor_truncate_string` subtracts three from `max_len`, so very small max lengths could produce awkward shell substring behavior (`scripts/lib/monitor-common.sh:249`). In `monitor-skill.sh`, `--tool-filter` consumes the next argument without validating it, legacy invocations with unknown tool are treated as Codex for codex filtering, cache path derivation depends on git root or `pwd`, and interactive mode assumes usable `tput`, `tail -f`, and terminal alternate-screen behavior (`scripts/lib/monitor-skill.sh:35`, `scripts/lib/monitor-skill.sh:72`, `scripts/lib/monitor-skill.sh:158`, `scripts/lib/monitor-skill.sh:429`, `scripts/lib/monitor-skill.sh:509`).
- validation_or_tests: Direct tests cover config merge precedence, additive merging, null stripping, `HUMANIZE_CONFIG`, malformed optional JSON fallback, missing optional configs, and missing default config fatal behavior (`tests/test-config-merge.sh:3`, `tests/test-config-merge.sh:43`, `tests/test-config-merge.sh:95`, `tests/test-config-merge.sh:138`, `tests/test-config-merge.sh:156`, `tests/test-config-error-handling.sh:37`, `tests/test-config-error-handling.sh:56`). Model-router tests cover Codex and Claude model pattern detection, unknown/empty model rejection, mocked dependency success/failure, and effort mapping/pass-through/rejection (`tests/test-model-router.sh:30`, `tests/test-model-router.sh:199`, `tests/test-model-router.sh:233`, `tests/test-model-router.sh:301`, `tests/test-model-router.sh:389`). Skill-monitor tests exercise `--once` mode for missing directory, empty directory, success/error/timeout/running/empty response counts, latest invocation focus, recent invocation listing, question extraction, and non-timestamp directory filtering (`tests/test-skill-monitor.sh:107`, `tests/test-skill-monitor.sh:136`, `tests/test-skill-monitor.sh:195`, `tests/test-skill-monitor.sh:246`, `tests/test-skill-monitor.sh:296`, `tests/test-skill-monitor.sh:348`, `tests/test-skill-monitor.sh:371`). Additional monitor runtime/e2e/robustness tests reference the real monitor functions for deletion, SIGINT, narrow terminals, and ANSI safety (`tests/test-monitor-runtime.sh`, `tests/test-monitor-e2e-real.sh`, `tests/robustness/test-hook-input-robustness.sh`). I did not execute the test suites because this task requested research notes only and the workspace is read-only; a `git` metadata check was also not usable because this branch export did not expose a working `.git` repository under the sandbox.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section present for the single assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`