# agent_062 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-062 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: `tests/run-all-tests.sh` is the repository-level executable test orchestrator for the Humanize plugin. It is not the implementation of the realpath/path-resolution algorithm itself, but it is a core validation surface: it enumerates the canonical shell test suites, runs them with bounded parallelism, normalizes per-suite output, aggregates pass/fail counts, and returns the repository-wide test status. The header explicitly defines its purpose and exit contract at `tests/run-all-tests.sh:3` and `tests/run-all-tests.sh:10`.

- algorithmic_behavior: The script computes its own `SCRIPT_DIR` from `BASH_SOURCE[0]` at `tests/run-all-tests.sh:17`, then derives a parallelism cap through `default_jobs()` at `tests/run-all-tests.sh:21`. `default_jobs()` prefers `getconf _NPROCESSORS_ONLN`, validates numeric output, clamps the default maximum to 8, and enforces a lower bound of 1 at `tests/run-all-tests.sh:23` through `tests/run-all-tests.sh:30`. It accepts an override from `HUMANIZE_TEST_JOBS` at `tests/run-all-tests.sh:33` and rejects non-positive or non-integer values at `tests/run-all-tests.sh:34` through `tests/run-all-tests.sh:37`.

  The central suite list is a fixed ordered array at `tests/run-all-tests.sh:60` through `tests/run-all-tests.sh:118`. It includes baseline tests, config tests, hook tests, monitor tests, model/router tests, skill monitor tests, and robustness tests, including path-focused coverage such as `robustness/test-path-validation-robustness.sh` at `tests/run-all-tests.sh:105`. A small `ZSH_TESTS` array at `tests/run-all-tests.sh:121` through `tests/run-all-tests.sh:123` marks `test-zsh-monitor-safety.sh` as requiring zsh execution.

  Runtime behavior is a producer/throttler/collector pipeline. For each suite, the runner resolves `suite_path`, creates safe per-suite temp filenames, skips missing files, selects `zsh` only for suites listed in `ZSH_TESTS`, launches each suite in a subshell, records stdout/stderr, exit code, and elapsed milliseconds, then stores the child PID in associative state at `tests/run-all-tests.sh:166` through `tests/run-all-tests.sh:199`. The throttle loop at `tests/run-all-tests.sh:201` through `tests/run-all-tests.sh:217` keeps active child processes below `MAX_JOBS`, using `wait -n` when Bash is at least 4.3 and falling back to waiting on the oldest active PID otherwise.

  Result collection waits per suite in original suite order at `tests/run-all-tests.sh:229` through `tests/run-all-tests.sh:265`, reads the `.exit`, `.out`, and `.time` files, strips ANSI SGR color codes with `sed`, extracts the last `Passed:` and `Failed:` counts, increments global totals, records failed suite names, and writes sortable display lines keyed by elapsed milliseconds. Final reporting prints skipped suites, sorted pass/fail lines, total passed/failed counts, and detailed logs for failing suites at `tests/run-all-tests.sh:267` through `tests/run-all-tests.sh:301`.

- inputs_outputs_state: Primary inputs are the script path, the hardcoded `TEST_SUITES` array, the optional `HUMANIZE_TEST_JOBS` environment variable, the availability of external commands (`getconf`, `codex`, `zsh`, `date`, `sed`, `grep`, `sort`, `cut`, `kill`, `mktemp`), and the executable test files under `tests/`. The script also depends on each suite emitting summary lines in the form `Passed: <number>` and `Failed: <number>`, because aggregation uses regex extraction at `tests/run-all-tests.sh:247` and `tests/run-all-tests.sh:248`.

  Outputs are human-readable colored terminal status lines, aggregate totals, full detail logs for failed suites, and a process exit status. Exit `0` means no failed suites were detected; exit `1` means at least one suite returned nonzero or reported failures. The explicit final branch is at `tests/run-all-tests.sh:285` through `tests/run-all-tests.sh:301`.

  Internal mutable state is maintained in `MAX_JOBS`, `OUTPUT_DIR`, `PIDS`, `SKIPPED`, `ACTIVE_PIDS`, `TOTAL_PASSED`, `TOTAL_FAILED`, `FAILED_SUITES`, and `SORT_FILE`. Per-suite state transitions are: queued in `TEST_SUITES`; skipped if missing or if zsh is unavailable for a zsh-only suite; launched as a background process; active while its PID is in `ACTIVE_PIDS`; completed when its `.exit`, `.out`, and `.time` files have been written by the child subshell; classified as passed or failed during collection; optionally expanded into a `.detail` file for failing logs. Global state transitions are: create temp output directory at `tests/run-all-tests.sh:126`; optionally add a mock `codex` binary at `tests/run-all-tests.sh:132` through `tests/run-all-tests.sh:140`; launch/throttle all suites; collect and sort results; remove the temp directory via the EXIT trap at `tests/run-all-tests.sh:127`.

- gates_or_invariants: The main preflight gate is `HUMANIZE_TEST_JOBS` validation: it must be an integer greater than or equal to 1, otherwise the runner exits immediately with an error at `tests/run-all-tests.sh:34` through `tests/run-all-tests.sh:37`. The default job count invariant is `1 <= default_jobs <= 8` at `tests/run-all-tests.sh:26` through `tests/run-all-tests.sh:29`. Bash feature compatibility is gated by `supports_wait_n()` at `tests/run-all-tests.sh:40` through `tests/run-all-tests.sh:44`, avoiding mandatory Bash 4.3 behavior on older shells.

  Suite availability is a soft gate: missing test files do not fail the run; they are recorded in `SKIPPED` as `not found` at `tests/run-all-tests.sh:173` through `tests/run-all-tests.sh:176`. zsh availability is also a soft gate for zsh-only suites: if zsh is unavailable, the suite is skipped with reason `zsh not available` at `tests/run-all-tests.sh:178` through `tests/run-all-tests.sh:181`. This means the runner treats some environmental coverage gaps as skips rather than hard failures.

  The completion invariant is stricter for executed suites: a suite is failed if its captured exit code is nonzero or its parsed `Failed:` count is greater than zero at `tests/run-all-tests.sh:253`. A zero exit with an unparsable nonzero failure count would pass as long as the parsed failed count is `0`, so the output-summary format is an implicit invariant shared by suite scripts. ANSI normalization is intentionally portable: `esc=$'\033'` plus `sed "s/${esc}\\[[0-9;]*m//g"` at `tests/run-all-tests.sh:228` and `tests/run-all-tests.sh:246`.

- dependencies_and_callers: Direct dependencies include Bash with associative arrays, standard Unix tools, and every script listed in the `TEST_SUITES` array. The runner provides a fallback `codex` binary only when `command -v codex` fails, because some setup scripts only require `codex` to exist; this mock is created under `$OUTPUT_DIR/mock-bin` and prepended to `PATH` at `tests/run-all-tests.sh:132` through `tests/run-all-tests.sh:140`. zsh is required only for `test-zsh-monitor-safety.sh`, selected by `needs_zsh()` at `tests/run-all-tests.sh:143` through `tests/run-all-tests.sh:151`.

  A direct repository search found only self/documentation references to the runner plus `tests/test-ansi-parsing.sh`, which explicitly tests the runner’s ANSI stripping and count extraction assumptions. That validation file states its purpose at `tests/test-ansi-parsing.sh:3` through `tests/test-ansi-parsing.sh:6`, exercises the same `sed` expression at `tests/test-ansi-parsing.sh:45` through `tests/test-ansi-parsing.sh:48`, and tests `Passed:`/`Failed:` extraction at `tests/test-ansi-parsing.sh:74` through `tests/test-ansi-parsing.sh:90` and multiline tail-selection at `tests/test-ansi-parsing.sh:116` through `tests/test-ansi-parsing.sh:125`.

  Most suite scripts appear to follow the shared summary contract either directly or through `tests/test-helpers.sh`. The helper’s `print_test_summary()` emits `Passed:` and `Failed:` lines at `tests/test-helpers.sh:58` through `tests/test-helpers.sh:68`, which matches the runner parser. Direct inspection of the repository file list showed all suite paths named by this runner exist in the read-only export; the runner therefore should not produce `not found` skips for its current hardcoded list.

- edge_cases_or_failure_modes: The runner is intentionally tolerant of some missing dependencies, but that tolerance can hide coverage loss: missing suites and unavailable zsh-only coverage become skips rather than failures. The mock `codex` fallback is safe for tests that only check command availability, but it can mask real CLI absence unless specific tests provide their own stricter mocks. Since the mock exits `0`, any suite that accidentally relies on real Codex behavior without installing its own mock could produce false confidence.

  The elapsed-time measurement uses `date +%s%3N` at `tests/run-all-tests.sh:184`, `tests/run-all-tests.sh:187`, `tests/run-all-tests.sh:191`, and `tests/run-all-tests.sh:194`. On platforms where `%3N` is unsupported or returns a non-numeric literal, arithmetic expansion could fail inside the child subshell. The script is likely intended for environments where this GNU-style millisecond format works; there is no explicit fallback for BSD `date`.

  The throttle implementation with `wait -n` prunes `ACTIVE_PIDS` by `kill -0` at `tests/run-all-tests.sh:203` through `tests/run-all-tests.sh:211`. A just-finished but unreaped process can still be visible briefly, so throttling may be conservative, but correctness is preserved because final collection waits each recorded PID. If a child subshell exits before writing `.exit` due to shell-level arithmetic or command execution errors, collection defaults the exit code to `1` at `tests/run-all-tests.sh:240`, making that suite fail.

  Parsing is summary-format dependent. If a suite exits `0` but omits `Passed:`/`Failed:` lines, it contributes zero counts and is treated as passed unless the exit code is nonzero. If a suite emits multiple summaries, the runner intentionally uses the last matching line via `tail -1` at `tests/run-all-tests.sh:247` and `tests/run-all-tests.sh:248`. If a suite emits unrelated text matching `Failed: <number>` after its summary, that later line could affect classification.

  File-name sanitization for temp output replaces `/` with `_` at `tests/run-all-tests.sh:168`; this is enough for the current suite names but would collide if future suite names differ only by slash placement after normalization. The `trap "rm -rf $OUTPUT_DIR" EXIT` at `tests/run-all-tests.sh:127` is unquoted inside the trap string; current `mktemp -d` paths normally avoid whitespace, but robust quoting would be safer.

  I did not execute the full runner in this read-only branch export. The repository root is not a full Git checkout in this environment, and some tests intentionally create repositories, temp files, hooks, and monitor processes. Direct evidence here is therefore static inspection plus targeted reference inspection.

- validation_or_tests: The runner is a validation harness rather than a unit under direct test, but it has targeted validation coverage in `tests/test-ansi-parsing.sh`, which checks the portable ANSI stripping and pass/fail count extraction that the runner uses. The shared suite summary convention is also encoded in `tests/test-helpers.sh:58` through `tests/test-helpers.sh:78`. The runner itself enumerates 42 suites, including robustness suites and path validation coverage, and aggregates them into a single process exit gate. The executable bit is present on `tests/run-all-tests.sh`, and sampled referenced suites such as `tests/test-template-loader.sh`, `tests/test-zsh-monitor-safety.sh`, and `tests/robustness/test-state-file-robustness.sh` are executable in the export.

- skip_candidate: `no`. Although this file is orchestration/test infrastructure rather than the path canonicalization implementation, the assignment labels it as an executable specification for core algorithm behavior. It is the repository’s top-level validation entry point and includes the robustness/path-validation test suite list, so it is relevant evidence for the branch’s algorithmic correctness gates.

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-062`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`