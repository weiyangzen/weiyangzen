# agent_079 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-079 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable regression specification for the RLCR finalize-phase algorithm. It verifies that the loop can transition from normal implementation review into a protected `finalize-state.md` phase, then complete only after final summary, todo, and git-clean gates pass. It also verifies that the branch’s realpath/canonical active-loop behavior still lets all hooks find and parse finalize state through `find_active_loop` and `resolve_active_state_file`.

- algorithmic_behavior: The test builds an isolated temporary Git repo and RLCR loop directory, then drives hook scripts with JSON hook payloads. Setup helpers create mock `codex` commands, a synthetic repo, `plans/test-plan.md`, `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, and round contracts at `tests/test-finalize-phase.sh:41`, `tests/test-finalize-phase.sh:53`, `tests/test-finalize-phase.sh:84`, `tests/test-finalize-phase.sh:116`, `tests/test-finalize-phase.sh:146`, `tests/test-finalize-phase.sh:172`, and `tests/test-finalize-phase.sh:208`. The test first asserts helper predicates for finalize paths (`is_finalize_state_file_path`, `is_finalize_summary_path`, and `finalize_state_file_blocked_message`) at `tests/test-finalize-phase.sh:279`. It then validates active-loop detection: `finalize-state.md` is active, `complete-state.md` is terminal, and normal `state.md` remains active at `tests/test-finalize-phase.sh:323`, `tests/test-finalize-phase.sh:343`, and `tests/test-finalize-phase.sh:363`.

- inputs_outputs_state: Inputs are shell-created state files, mocked Codex stdout/review stdout, JSON PreToolUse/Stop/UserPromptSubmit hook payloads, and transcript JSONL todo data. Observable outputs are hook exit codes, JSON block/allow decisions, marker files, review logs, generated prompt/result files, and state-file renames. Key state transitions are `state.md -> finalize-state.md` after Codex emits `COMPLETE` and review passes (`tests/test-finalize-phase.sh:591`), `finalize-state.md -> complete-state.md` after final gates pass (`tests/test-finalize-phase.sh:541`), `state.md -> maxiter-state.md` when at max iterations (`tests/test-finalize-phase.sh:609`), and `state.md -> stop-state.md` after drift circuit breaker (`tests/test-finalize-phase.sh:996`). The stop hook implements these transitions in `hooks/loop-codex-stop-hook.sh:927`, `hooks/loop-codex-stop-hook.sh:943`, `hooks/loop-codex-stop-hook.sh:1283`, and `hooks/loop-codex-stop-hook.sh:1385`.

- gates_or_invariants: Finalize phase must require `finalize-summary.md`, a clean Git worktree, completed todos, and no further Codex call before completion; the test covers missing summary at `tests/test-finalize-phase.sh:494`, dirty Git at `tests/test-finalize-phase.sh:519`, incomplete todos at `tests/test-finalize-phase.sh:770`, and “Codex not invoked” at `tests/test-finalize-phase.sh:511` and `tests/test-finalize-phase.sh:563`. `finalize-state.md` is system-owned: Write/Edit/Bash mutation and source-copy/move attempts must be blocked with finalize-specific messaging at `tests/test-finalize-phase.sh:398`, `tests/test-finalize-phase.sh:422`, `tests/test-finalize-phase.sh:446`, `tests/test-finalize-phase.sh:458`, and `tests/test-finalize-phase.sh:470`. Round contracts are invalid during finalize: Write/Edit/Read validators must block contract access at `tests/test-finalize-phase.sh:410`, `tests/test-finalize-phase.sh:434`, and `tests/test-finalize-phase.sh:1078`. Max iterations must skip finalize even if completion text appears, asserted at `tests/test-finalize-phase.sh:609`. Normal rounds must remain unaffected: non-complete review feedback increments the round and creates review artifacts at `tests/test-finalize-phase.sh:814`.

- dependencies_and_callers: The test sources `hooks/lib/loop-common.sh` at `tests/test-finalize-phase.sh:49`. That library sources `hooks/lib/project-root.sh`, whose resolver deliberately canonicalizes the project root with `realpath` and avoids `pwd` drift (`hooks/lib/project-root.sh:1`). Active state resolution prioritizes methodology-analysis state, then `finalize-state.md`, then `state.md` (`hooks/lib/loop-common.sh:264`), while active loop discovery treats terminal state files as inactive (`hooks/lib/loop-common.sh:308`). Strict parsing requires `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:524`). Finalize path helpers and messages live at `hooks/lib/loop-common.sh:868`, `hooks/lib/loop-common.sh:1000`, and `hooks/lib/loop-common.sh:1021`. The invoked validators are `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. The write/read validators use canonical path comparison for loop files (`hooks/loop-read-validator.sh:307`), consistent with the branch’s realpath focus.

- edge_cases_or_failure_modes: The test covers review failure before finalize (`tests/test-finalize-phase.sh:630`), empty review output and empty cached review log (`tests/test-finalize-phase.sh:690`), missing required `Mainline Progress Verdict` preserving drift state (`tests/test-finalize-phase.sh:948`), two stalled rounds producing drift-recovery prompt (`tests/test-finalize-phase.sh:890`), third stalled/regressed round stopping the loop (`tests/test-finalize-phase.sh:996`), and validators parsing `finalize-state.md` when no `state.md` exists (`tests/test-finalize-phase.sh:1041`). Bash bypass handling includes direct redirection to `finalize-state.md` plus `mv`/`cp` source protection; the implementation checks destination modification first and then segment-splits shell commands to catch source `mv`/`cp` patterns at `hooks/loop-bash-validator.sh:262`, `hooks/loop-bash-validator.sh:292`, `hooks/loop-bash-validator.sh:420`, and shell-wrapper payloads at `hooks/loop-bash-validator.sh:440`.

- validation_or_tests: This file is itself the executable validation artifact. It totals 1,113 lines and exits with `TESTS_FAILED`, making it suitable for CI-style pass/fail use (`tests/test-finalize-phase.sh:1104`). It verifies hook behavior by constructing hook JSON, piping it to hook scripts, checking exact exit codes (`0` for allow, `2` for blocked tool use), inspecting emitted JSON for `"decision": "block"`, grepping for expected domain messages, and checking file-system side effects. I did not run the test because the branch export is read-only and the request was research-only. A non-mutating `git status` probe also failed because this export is not a Git checkout and macOS toolchain cache writes were blocked by the sandbox; this does not affect the source-level inspection.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1; the single Item Evidence heading contains the assigned item id exactly once`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`