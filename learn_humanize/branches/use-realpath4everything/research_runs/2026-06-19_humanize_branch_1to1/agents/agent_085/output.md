# agent_085 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-085 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: This file is an executable end-to-end specification for the real RLCR monitor path, especially `_humanize_monitor_codex` in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/humanize.sh:261). It is not a production algorithm implementation itself, but it exercises the real monitor function rather than stubs, making it core behavioral evidence for monitor lifecycle safety. Its header states the tested contract: graceful stop when `.humanize/rlcr` is deleted, no shell glob errors, terminal restoration, and bash/zsh compatibility in [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-real.sh:3).

- algorithmic_behavior: The test harness builds isolated temporary RLCR project layouts under `/tmp/test-monitor-e2e-real-$$`, synthesizes `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, and matching cache log files, then launches small generated runner scripts that source the repository’s real `scripts/humanize.sh` and call `_humanize_monitor_codex` directly. The bash deletion path is assembled in [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-real.sh:56), with state and goal tracker fixtures at lines 61-93, fake cache/log setup at lines 95-103, runner generation at lines 107-152, background monitor launch at lines 156-159, deletion trigger at line 165, and bounded process wait at lines 167-182. The zsh deletion path mirrors that flow in [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-real.sh:233), with a zsh runner at lines 287-320 and directory deletion at line 333. The SIGINT paths create equivalent fixtures and runner scripts, then send interrupts to bash and zsh monitor processes in [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-real.sh:377) and [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-real.sh:555).

- inputs_outputs_state: Inputs are synthetic project directories, RLCR session directories named like `2026-01-16_10-00-00`, YAML frontmatter in `state.md`, `goal-tracker.md`, fake `HOME`, fake `XDG_CACHE_HOME`, generated runner scripts, shell availability, `tput`/`clear` shim functions, process signals, and filesystem deletion events. Outputs are captured monitor stdout/stderr files under the test base, pass/fail counters, process exit codes, and final script exit status. State transitions validated by the test are: session exists and monitor starts; monitor discovers session and cache log; external deletion removes `.humanize/rlcr`; production monitor detects the missing loop directory and transitions through cleanup to graceful stop; runner records `EXIT_CODE:0`. For SIGINT, state transitions are: monitor starts, signal is delivered, cleanup path runs or the process is terminated after fallback, and output must show cleanup/signal evidence or a clean exit code. The production state transition being exercised is visible in `_humanize_monitor_codex`: `monitor_running=true` and `cleanup_done=false` are initialized at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/humanize.sh:731), `_cleanup` sets `cleanup_done=true` and `monitor_running=false` at lines 738-742, restores terminal state at lines 759-761, and `_graceful_stop` wraps cleanup plus user-facing stop text at lines 764-777. The main monitor loop checks for deleted `.humanize/rlcr` and returns success at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/humanize.sh:838), with additional deletion checks inside no-log and follow loops at lines 904-908 and 1027-1030.

- gates_or_invariants: The file enforces several concrete gates. The monitor must exit within bounded wait loops after deletion or signal; deletion wait loops use a max of 20 half-second polls in [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-real.sh:167) and [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-real.sh:336). Deletion output must contain `Monitoring stopped:` and `directory no longer exists` in [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-real.sh:187). Output must not contain zsh/bash glob failure strings `no matches found` or `bad pattern`, checked at lines 200-205, 360-364, 544-549, and 680-684. Terminal cleanup is checked by requiring `Stopped monitoring` in output and a source-level `printf "\033[r"` scroll-region reset in [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-real.sh:207). Graceful deletion must produce `EXIT_CODE:0`, checked for bash at lines 222-227 and zsh at lines 366-370. The production invariant behind the glob-safety gate is that session/log enumeration uses `find`, not unguarded shell globs: `monitor_find_latest_session` explicitly says it uses `find` to avoid zsh no-match errors in [scripts/lib/monitor-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/monitor-common.sh:49), and `_find_latest_codex_log` uses `find "$cache_dir" -maxdepth 1 ...` in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/humanize.sh:338).

- dependencies_and_callers: The assigned file depends on bash, optional zsh, POSIX process controls (`kill`, `wait`, `pkill`), `sed`, `grep`, `mkdir`, `rm`, `chmod`, `cat`, `head`, and repository scripts. Its generated runners source [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/humanize.sh:1), which sources [scripts/lib/monitor-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/monitor-common.sh:1) at [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/humanize.sh:6). The production monitor in turn uses `monitor_find_latest_session`, `monitor_find_state_file`, and `monitor_get_file_size` from `monitor-common.sh`, plus local parsing and rendering helpers. The assigned file is also used as a source library by focused split wrappers: [tests/test-monitor-e2e-deletion.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-deletion.sh:5) runs only the bash/zsh deletion tests, and [tests/test-monitor-e2e-sigint.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-sigint.sh:5) runs only the bash/zsh SIGINT tests. Those split wrappers are listed in [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/run-all-tests.sh:81), while direct execution of the assigned file runs all four tests from [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-real.sh:691).

- edge_cases_or_failure_modes: The main edge case is deletion of `.humanize/rlcr` while the monitor is live, including when a log is present or when the monitor is polling. The production monitor treats top-level loop directory deletion as a graceful terminal condition, not as a session switch, by calling `_graceful_stop` and returning `0` in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/humanize.sh:840). The test also covers shell-specific behavior where zsh would normally error on unmatched globs; this is why it runs the real function under zsh and scans output for `no matches found` and `bad pattern`. Signal handling is intentionally tolerant: the bash SIGINT test sends to the process group first, then the process, and if the monitor remains alive it escalates to SIGTERM/SIGKILL while still counting a SIGTERM-managed shutdown as pass because SIGINT delivery is noted as complex in [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-real.sh:492). The zsh SIGINT path is stricter and fails if the process does not exit after SIGINT within the bounded wait in [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-real.sh:649). Potential failure modes include missing `zsh` causing a skip rather than failure at lines 238-240 and 560-562, monitor startup failure leading to explicit fail output, lingering processes cleaned by `pkill -f "test-monitor-e2e-real-$$"` at lines 46-50, and platform differences around process groups or terminal commands mitigated by `tput` and `clear` shims in generated runners.

- validation_or_tests: I inspected the assigned file directly and ran read-only shell syntax validation with `bash -n tests/test-monitor-e2e-real.sh`, which returned `bash -n OK`. I did not run the full e2e script because it intentionally creates temporary projects, writes generated runner scripts under `/tmp`, starts background monitor processes, deletes directories, and sends signals; that is beyond the requested research-only, no-modification posture. The file’s built-in validation matrix is four functions: `monitor_test_bash_deletion` at [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-real.sh:56), `monitor_test_zsh_deletion` at line 233, `monitor_test_bash_sigint` at line 377, and `monitor_test_zsh_sigint` at line 555. The direct-run summary exits `0` only when `TESTS_FAILED` is zero and prints verified properties for graceful messages, glob safety, terminal restoration, bash/zsh behavior, and SIGINT handling in [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-monitor-e2e-real.sh:702). I also attempted read-only git metadata checks, but `git rev-parse HEAD` and `git ls-tree` failed because this branch export is not a git checkout in the sandbox and macOS developer tooling attempted to create blocked `/tmp/xcrun_db-*` cache files; therefore the source commit is recorded from the scheduler-provided metadata rather than local git output.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1 item section present; item section count checked as one
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`