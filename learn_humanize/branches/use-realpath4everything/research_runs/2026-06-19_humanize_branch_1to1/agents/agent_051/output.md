# agent_051 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-051 `file` `scripts/bitlesson-validate-delta.sh`
- cursor: `[_]`
- core_role: Runtime validation gate for the RLCR/BitLesson workflow. It verifies that a round summary contains a live, syntactically valid `## BitLesson Delta` section and that the declared BitLesson action is consistent with the configured BitLesson knowledge base. It is invoked by [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:823) for non-finalize rounds when `bitlesson_required: true`.

- algorithmic_behavior: The script parses required CLI arguments at [scripts/bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-validate-delta.sh:24), validates that the summary file exists, resolves its own plugin root from `BASH_SOURCE`, then sources `hooks/lib/template-loader.sh` at line 76. Its primary extraction routine, `extract_bitlesson_delta_block`, scans the summary with `awk` and only recognizes `## BitLesson Delta` headings outside fenced code blocks and outside HTML comments, using explicit fence/comment state tracking at lines 92-179. If extraction fails, it emits a JSON block decision through `block_exit` at lines 78-90. Once a delta block is found, it extracts exactly one `Action:` candidate, lowercases it, and only permits `none`, `add`, or `update` at lines 202-218. It then extracts `Lesson ID(s):` or legacy `Lesson IDs:` at lines 220-224 and counts concrete `Lesson ID:` entries in the BitLesson file at lines 227-240. For `Action: none`, it requires empty/`NONE` lesson IDs and can optionally block an empty KB depending on `--allow-empty-none` at lines 242-267. For `add`/`update`, it requires concrete IDs, non-placeholder `Notes:`, an existing BitLesson file, valid `BL-YYYYMMDD-short-name` ID syntax, and matching `Lesson ID:` entries in the KB at lines 268-384.

- inputs_outputs_state: Inputs are `--summary-file`, `--bitlesson-file`, `--bitlesson-relpath`, `--allow-empty-none`, `--template-dir`, and `--current-round`, all mandatory at lines 4-13 and 62-67. Output on success is no stdout and exit `0` at line 387, allowing the caller to continue. Output on a validation block is JSON with `decision: "block"`, `reason`, and `systemMessage`, followed by exit `0`, so validation failures are represented as hook decisions rather than shell failures. Hard script errors, such as missing required arguments, missing summary file, unknown arguments, or an unexpected nonzero validator invocation in the caller, use stderr and nonzero exit. The script does not mutate repository state; its only state transition is external workflow control: continue vs block the stop hook.

- gates_or_invariants: The strongest invariant is that BitLesson evidence must be in normal summary text, not hidden in quoted/template content. The scanner ignores markdown fences opened with ``` or `~~~` and ignores multi-line HTML comments before recognizing the delta heading at lines 104-170. The delta section ends on the next level-2 markdown heading at lines 161-163. Exactly one action is allowed; multiple `Action:` lines or invalid values block. `Action: none` cannot name concrete IDs. `Action: add|update` must name at least one comma-separated concrete ID, must include meaningful `Notes:`, and every declared ID must already exist as a `Lesson ID:` record in the KB. Placeholder ID records such as `<BL-YYYYMMDD-short-name>` are excluded from the concrete KB count at lines 227-239.

- dependencies_and_callers: Direct dependency is [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/lib/template-loader.sh:188), especially `load_and_render_safe`, which loads prompt-template block messages with fallback content. The stop hook passes summary path, resolved BitLesson file path, relative BitLesson path, empty-none policy, template dir, and current round at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:824). The caller prints any nonempty JSON result and exits the hook cleanly at lines 834-837. Upstream state parsing determines `BITLESSON_FILE_REL`, rejects absolute/traversal/invalid relative paths, defaults to `.humanize/bitlesson.md`, and normalizes `bitlesson_allow_empty_none` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/hooks/loop-codex-stop-hook.sh:144). Documentation in [docs/bitlesson.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/docs/bitlesson.md:36) defines the same required summary contract. The script itself does not use the branch’s shared `realpath` helpers; it uses `cd ... && pwd` for `SCRIPT_DIR`/`PROJECT_ROOT`, while path safety for the BitLesson relative path is handled by the caller.

- edge_cases_or_failure_modes: Missing `## BitLesson Delta` blocks produce a block decision, including when the apparent section is inside a fenced code block or HTML comment. A one-line HTML comment containing both `<!--` and `-->` does not enter comment state before heading detection on that same line, but a valid markdown heading cannot normally coexist with the comment marker at line start, so practical impact is limited. The action extraction only matches alphabetic values; values with punctuation or inline comments become invalid. Multiple `Action:` lines block because count must equal one. `Lesson ID(s)` parsing uses the first matching line only and splits only on commas, so newline-separated IDs are not accepted as multiple IDs. `Notes:` is first-line only; multiline notes without text on the `Notes:` line are treated as missing. Placeholder notes that are entirely bracketed or angle-bracketed are rejected by `^(\[.*\]|<.*>)$`. A missing BitLesson file is tolerated only for `Action: none`; it blocks for `add`/`update`. Because `block_exit` exits `0`, callers must inspect stdout, which the stop hook correctly does.

- validation_or_tests: Regression coverage exists in [tests/test-bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-bitlesson-validate-delta.sh:1). The test constructs a mock KB with a concrete `BL-20260313-notes-validation` ID, runs the validator with `--allow-empty-none false`, and verifies block JSON vs empty success output. Covered cases include whitespace-only notes, bracket placeholder notes, angle-bracket placeholder notes, delta inside a fenced block, delta inside an HTML comment, valid `add` notes, and normal text validation at lines 101-152. I did not run the test suite because this worker is constrained to a read-only branch export and must not modify files; the inspected tests create temporary files/directories.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 item evidence section present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`