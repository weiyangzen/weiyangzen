# agent_181 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-181 `file` `skills/humanize/SKILL.md`
- cursor: `[_]`
- core_role: `skills/humanize/SKILL.md` is the top-level Humanize workflow contract for iterative AI-assisted development. It is not executable code; it defines the operator-facing algorithm for RLCR, plan generation, one-shot Codex consultation, runtime command entrypoints, expected state layout, and loop invariants. The metadata marks it as `user-invocable: false` and `disable-model-invocation: true`, so it functions as embedded process documentation and command routing guidance rather than a directly invoked agent skill (`skills/humanize/SKILL.md:1-6`).
- algorithmic_behavior: The file defines two main workflows. First, the RLCR loop runs a two-phase algorithm: implementation rounds produce summaries, Codex reviews each summary, feedback continues when issues remain, and a `COMPLETE` marker advances into code review; then `codex review --base <branch>` searches for `[P0-9]` findings, fixes loop until none remain, and completion proceeds to finalization (`skills/humanize/SKILL.md:32-49`). Second, Generate Plan turns a draft into a structured implementation plan with goal, AC-style acceptance criteria, path boundaries, feasibility hints, dependencies, and milestones (`skills/humanize/SKILL.md:50-58`). It also exposes an `ask-codex.sh` one-shot consultation path (`skills/humanize/SKILL.md:105-109`). The concrete RLCR setup implementation mirrors the skill contract by parsing options, validating prerequisites, creating `.humanize/rlcr/<timestamp>`, copying or synthesizing the plan, writing `state.md`, and initializing `goal-tracker.md` (`scripts/setup-rlcr-loop.sh:188-320`, `scripts/setup-rlcr-loop.sh:821-907`, `scripts/setup-rlcr-loop.sh:926-1156`).
- inputs_outputs_state: Inputs are a plan file path or `--skip-impl` for RLCR startup (`skills/humanize/SKILL.md:61-69`), options such as max iterations, Codex model/effort, timeout, base branch, full-review interval, push policy, quiz/open-question behavior, agent-teams, and privacy (`skills/humanize/SKILL.md:73-88`), draft/output file paths for plan generation (`skills/humanize/SKILL.md:97-103`), and a free-form question for one-shot Codex consultation (`skills/humanize/SKILL.md:105-109`). Outputs are persisted under `.humanize/rlcr/<timestamp>/` for RLCR state, goal tracker, round summaries, review results, finalize/methodology/complete state files, and under `.humanize/skill/<timestamp>/` for one-shot skill input/output/metadata (`skills/humanize/SKILL.md:180-203`). Setup writes frontmatter fields including `current_round`, `max_iterations`, Codex config, plan metadata, start/base branch, captured base commit, review phase status, session fields, privacy, BitLesson state, and drift/mainline tracking (`scripts/setup-rlcr-loop.sh:880-907`). `ask-codex.sh` writes `.humanize/skill/<unique-id>/input.md` and uses a project-local or cache-backed output/log layout (`scripts/ask-codex.sh:15-17`, `scripts/ask-codex.sh:202-238`).
- gates_or_invariants: The skill’s explicit invariants are: always write the required summary before exit, keep `goal-tracker.md` updated, include implementation/files/tests detail, do not bypass the loop by editing state or running cancel commands, and rely on the native Codex Stop hook after writing the summary (`skills/humanize/SKILL.md:166-174`). The Goal Tracker invariant divides immutable Ultimate Goal/Acceptance Criteria from mutable active/completed/deferred/evolution sections; tasks must map to ACs, deferrals require justification, and full alignment checks occur every N rounds (`skills/humanize/SKILL.md:152-165`). Referenced setup gates enforce one active loop only, required `codex`, `jq`, and `git`, valid Agent Teams environment, a git repo with at least one commit, clean working tree, valid local base branch, YAML-safe branch/model fields, and Codex effort in `xhigh|high|medium|low` (`scripts/setup-rlcr-loop.sh:326-397`, `scripts/setup-rlcr-loop.sh:682-815`). Plan-file gates reject absolute paths, spaces, shell metacharacters, symlink files or symlink parent segments, missing/unreadable/out-of-project paths, submodule paths, wrong tracked/gitignored status, too-short plans, and comment-only content (`scripts/setup-rlcr-loop.sh:445-676`). The branch’s realpath-oriented invariant is implemented below the skill layer: `resolve_project_root` intentionally avoids `pwd`, prefers `CLAUDE_PROJECT_DIR` then git toplevel, and canonicalizes via `realpath` to avoid symlink-prefix divergence (`hooks/lib/project-root.sh:3-20`, `hooks/lib/project-root.sh:41-53`). Generated-plan IO validation canonicalizes input/output with `realpath -m` before checking existence, emptiness, output directory, non-existence, write permission, and template availability (`scripts/validate-gen-plan-io.sh:98-179`).
- dependencies_and_callers: The skill depends on installer hydration of `{{HUMANIZE_RUNTIME_ROOT}}`; all command examples are written against that runtime root (`skills/humanize/SKILL.md:12-20`). The referenced commands are `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/validate-gen-plan-io.sh`, `scripts/ask-codex.sh`, and `scripts/humanize.sh` monitor (`skills/humanize/SKILL.md:61-109`, `skills/humanize/SKILL.md:205-213`). In this branch export, the `skills/humanize/` directory contains only `SKILL.md`; the command bodies live at repository top-level `scripts/`, with shared hook logic in `hooks/` and `hooks/lib/`. Codex Stop hook installation is described in the skill (`skills/humanize/SKILL.md:48`, `skills/humanize/SKILL.md:171-172`) and configured by `config/codex-hooks.json`, which points the `Stop` hook to `{{HUMANIZE_RUNTIME_ROOT}}/hooks/loop-codex-stop-hook.sh` with a 7200s timeout (`config/codex-hooks.json:1-16`). The stop hook finds the active loop, parses state, validates Codex fields, and gates exit on summary/review/review-phase/finalization behavior (`hooks/loop-codex-stop-hook.sh:1-14`, `hooks/loop-codex-stop-hook.sh:61-180`). Shared loop helpers define key constants and state operations, including `MARKER_COMPLETE`, exit reason constants, `resolve_active_state_file`, `find_active_loop`, `parse_state_file`, immutable goal-tracker extraction/preservation helpers, command modification detection, and `end_loop` (`hooks/lib/loop-common.sh:63-76`, `hooks/lib/loop-common.sh:268`, `hooks/lib/loop-common.sh:332`, `hooks/lib/loop-common.sh:494`, `hooks/lib/loop-common.sh:922-960`, `hooks/lib/loop-common.sh:1485`, `hooks/lib/loop-common.sh:1540`).
- edge_cases_or_failure_modes: If `{{HUMANIZE_RUNTIME_ROOT}}` is not hydrated or points at a layout without `scripts/` and `hooks/`, the skill’s command examples fail before workflow setup. Native stop gating requires Codex CLI `0.114.0+` with `codex_hooks` enabled; otherwise the documented automatic exit gate is unavailable (`skills/humanize/SKILL.md:48`). Missing `codex`, `jq`, or `git` hard-fails RLCR setup with a combined dependency error (`scripts/setup-rlcr-loop.sh:340-363`). Existing active loops block new startup (`scripts/setup-rlcr-loop.sh:369-379`). `--skip-impl` without a plan bypasses plan validation, creates a placeholder plan, and ignores `--track-plan-file`; `--skip-impl` with a plan anchors review-only work to extracted plan goal/AC content (`scripts/setup-rlcr-loop.sh:408-427`, `scripts/setup-rlcr-loop.sh:829-849`, `scripts/setup-rlcr-loop.sh:931-1000`). Cancel is global to the current project rather than session-filtered, creates `.cancel-requested`, removes pending setup markers, and renames the active state file to `cancel-state.md`; finalize cancellation requires `--force` (`scripts/cancel-rlcr-loop.sh:80-97`, `scripts/cancel-rlcr-loop.sh:139-167`). Monitoring fails if `.humanize/rlcr` does not exist and defensively errors on inconsistent log ordering where implementation run rounds overlap review rounds (`scripts/humanize.sh:272-277`, `scripts/humanize.sh:364-371`). Generate-plan validation fails on missing/empty input, missing output directory, existing output, unwritable output directory, invalid args, or missing plan template with the documented exit codes (`skills/humanize/SKILL.md:221-229`, `scripts/validate-gen-plan-io.sh:108-179`). `ask-codex.sh` fails on missing Codex CLI, empty question, invalid model characters, invalid effort characters, or unresolved project root (`scripts/ask-codex.sh:153-196`).
- validation_or_tests: The skill itself lists expected exit-code behavior for `ask-codex.sh` and `validate-gen-plan-io.sh` (`skills/humanize/SKILL.md:214-229`). Related validation coverage in the repository includes monitor e2e/runtime tests that synthesize `.humanize/rlcr` state and `goal-tracker.md`, template reference tests for goal-tracker blocks, cancel signal tests, setup robustness tests, state-transition/session robustness tests, and path-validation robustness tests for `scripts/setup-rlcr-loop.sh` (`tests/test-monitor-e2e-real.sh:62-81`, `tests/test-monitor-runtime.sh:61-73`, `tests/test-template-references.sh:157-158`, `tests/test-cancel-signal-file.sh:3-45`, `tests/robustness/test-path-validation-robustness.sh:5-19`, `tests/robustness/test-state-transition-robustness.sh:100-120`, `tests/robustness/test-session-robustness.sh:35-56`). The test runner includes `test-ask-codex.sh`, indicating one-shot Codex behavior is part of the validation surface (`tests/run-all-tests.sh:94`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-181`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`