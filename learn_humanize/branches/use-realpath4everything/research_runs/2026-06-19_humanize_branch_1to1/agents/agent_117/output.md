# agent_117 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-117 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: This prompt block is the user-facing denial message for attempts to modify `finalize-state.md`, the RLCR loop state file used during Finalize Phase. The file itself is not executable logic, but it is part of the hook enforcement contract: it tells the agent that `finalize-state.md` is system-managed and redirects work to the allowed finalize tasks. Direct contents are in `prompt-template/block/finalize-state-file-modification.md:1-8`.
- algorithmic_behavior: The template is rendered by `finalize_state_file_blocked_message()` in `hooks/lib/loop-common.sh:868-875`. That function supplies this file as the primary template and an inline fallback if template loading fails. The message establishes the transition policy for a blocked state mutation: do not edit `finalize-state.md`; continue Finalize Phase by running the code-simplifier agent, committing changes, and writing `finalize-summary.md` (`prompt-template/block/finalize-state-file-modification.md:5-8`). Enforcement is performed by validators, not by the markdown file itself.
- inputs_outputs_state: Input is an attempted Write/Edit/Bash operation targeting a path that matches `finalize-state.md`, usually inside `.humanize/rlcr/<session>/`. The rendered output is a markdown block headed `Finalize State File Modification Blocked` and emitted to stderr by validators. State transition on this path is intentionally none: the validator exits before the tool can modify `finalize-state.md`. The allowed adjacent output is `finalize-summary.md`, which the write validator permits only in the active loop directory during Finalize Phase (`hooks/loop-write-validator.sh:208-218`).
- gates_or_invariants: The key invariant is that Finalize Phase state is owned by the loop system, not the worker/agent. `is_finalize_state_file_path()` recognizes any lowercased path ending in `finalize-state.md` (`hooks/lib/loop-common.sh:1000-1004`). Validators check this before the broader `state.md` pattern because `state\.md$` also matches `finalize-state.md`; this specificity rule is called out in write/edit/bash validators (`hooks/loop-write-validator.sh:188-204`, `hooks/loop-edit-validator.sh:151-168`, `hooks/loop-bash-validator.sh:245-268`). The write and edit validators fail closed on malformed active state before allowing loop-sensitive operations (`hooks/loop-write-validator.sh:180-184`, `hooks/loop-edit-validator.sh:144-148`).
- dependencies_and_callers: The template depends on the common template loader initialized from `hooks/lib/loop-common.sh:240-248`, then `load_and_render_safe` resolves `block/finalize-state-file-modification.md` (`hooks/lib/loop-common.sh:874`). Direct callers are `hooks/loop-write-validator.sh:197-199`, `hooks/loop-edit-validator.sh:161-163`, and `hooks/loop-bash-validator.sh:262-268`, plus bash source-protection paths for `mv`/`cp` from `finalize-state.md` (`hooks/loop-bash-validator.sh:420-427`) and shell-wrapper payloads (`hooks/loop-bash-validator.sh:452-459`). Related finalize flow templates are `prompt-template/claude/finalize-phase-prompt.md`, which instructs creation of `finalize-summary.md`, and `prompt-template/block/finalize-contract-access.md`, which blocks historical round contracts during Finalize Phase.
- edge_cases_or_failure_modes: If the template directory cannot be validated, the common library warns and uses inline fallbacks (`hooks/lib/loop-common.sh:245-248`), so enforcement does not depend on this markdown file being readable. Case sensitivity is handled by lowercasing paths before matching in validators; matching is suffix-based, so full paths ending in `finalize-state.md` are covered. The bash validator accounts for direct redirects/modifications, `mv`/`cp` destination checks, `mv`/`cp` source checks, and `sh -c`/`bash -c` wrapper attempts (`hooks/loop-bash-validator.sh:249-283`, `hooks/loop-bash-validator.sh:420-459`). Authorized cancellation is an explicit exception: bash operations can proceed when `is_cancel_authorized` allows the state file to be renamed to terminal cancel state (`hooks/loop-bash-validator.sh:262-266`, `hooks/loop-bash-validator.sh:421-425`).
- validation_or_tests: `tests/test-finalize-phase.sh` covers helper matching for `finalize-state.md`, non-matching `state.md`, full-path matching, and existence of `finalize_state_file_blocked_message()` (`tests/test-finalize-phase.sh:279-321`). The same test script verifies that Write allows `finalize-summary.md` but blocks `finalize-state.md` with exit 2 and a finalize-related message (`tests/test-finalize-phase.sh:386-407`), Edit blocks `finalize-state.md` (`tests/test-finalize-phase.sh:422-431`), Bash blocks redirection writes (`tests/test-finalize-phase.sh:446-455`), and Bash blocks `mv`/`cp` from `finalize-state.md` (`tests/test-finalize-phase.sh:458-480`). I did not run tests; this was a read-only research assignment.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1 item evidence heading present for the single assigned item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`