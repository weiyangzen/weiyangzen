# agent_173 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-173 `file` `scripts/lib/model-router.sh`
- cursor: `[_]`
- core_role: Shared Bash routing library that maps configured model names to the supported local agent provider, verifies that provider’s CLI is available, and normalizes reasoning-effort labels across providers. It is sourced by [scripts/bitlesson-select.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-select.sh:11) before BitLesson selector execution, so it gates whether the selector runs via Codex or Claude.
- algorithmic_behavior: The file defines a source guard via `_MODEL_ROUTER_LOADED` to prevent duplicate function definitions when sourced repeatedly ([scripts/lib/model-router.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/model-router.sh:6)). `detect_provider` rejects an empty model, classifies `gpt-*` and `o[0-9]*` as `codex`, classifies `claude-*` or names containing `haiku`, `sonnet`, or `opus` as `claude`, and fails closed for unknown names ([scripts/lib/model-router.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/model-router.sh:10)). `check_provider_dependency` maps `codex` to the `codex` binary and `claude` to the `claude` binary, then uses `command -v` as the availability gate ([scripts/lib/model-router.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/model-router.sh:32)). `map_effort` validates provider and effort vocabulary, then maps `xhigh` to `high` only for Claude while passing all supported effort values through for Codex and non-`xhigh` Claude cases ([scripts/lib/model-router.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/model-router.sh:62)).
- inputs_outputs_state: Inputs are positional function arguments: model name for `detect_provider`, provider name for `check_provider_dependency`, and effort plus target provider for `map_effort`. Successful outputs are single stdout tokens: `codex` or `claude` from `detect_provider`, no stdout from `check_provider_dependency`, and the normalized effort from `map_effort`. Errors are emitted to stderr with nonzero returns, including empty model, unknown model, unknown provider, missing binary, or unsupported effort. State is limited to the shell variable `_MODEL_ROUTER_LOADED`; the library has no persistent files, no network calls, no subprocess state other than `grep` and `command -v`, and no repository mutation.
- gates_or_invariants: Provider vocabulary is exactly `codex` or `claude`; unknown providers fail in both `check_provider_dependency` and `map_effort` ([scripts/lib/model-router.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/model-router.sh:36), [scripts/lib/model-router.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/model-router.sh:66)). Effort vocabulary is exactly `xhigh`, `high`, `medium`, or `low` ([scripts/lib/model-router.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/lib/model-router.sh:75)). Model routing is precedence-based: Codex prefixes are checked before Claude substring detection, so a model beginning with `gpt-` or `o<digit>` routes to Codex even if later text contains a Claude-family token. Dependency validation is PATH-based and does not inspect CLI versions or command behavior.
- dependencies_and_callers: Direct production caller found: [scripts/bitlesson-select.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-select.sh:115), which derives `BITLESSON_PROVIDER` from `BITLESSON_MODEL`, optionally forces Claude-family models to Codex when `provider_mode` is `codex-only`, then checks the selected provider binary before running the selector ([scripts/bitlesson-select.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-select.sh:117), [scripts/bitlesson-select.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-select.sh:126)). That caller gets default model values through merged JSON config, where `bitlesson_model` defaults to `haiku` and `codex_model` defaults to `gpt-5.4` ([config/default_config.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/config/default_config.json:2)). The caller’s runtime branches then execute either `codex exec` with read-only sandbox arguments or `claude --print` ([scripts/bitlesson-select.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-select.sh:188)). `map_effort` has no non-test caller in the inspected tree; it is currently a shared helper contract covered by tests.
- edge_cases_or_failure_modes: Empty model names fail early with a clear error. Unknown names such as non-OpenAI/non-Claude aliases fail instead of defaulting. Claude detection is intentionally broad and case-insensitive for `haiku|sonnet|opus`, so any model string containing those substrings routes to Claude. Codex model matching is case-sensitive and limited to `gpt-*` or lowercase `o` followed by a digit. Missing selected provider binary causes failure unless the caller chooses a fallback; in `bitlesson-select.sh`, a missing Claude binary falls back to Codex, but a missing Codex binary still fails after rechecking Codex ([scripts/bitlesson-select.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-select.sh:126)). The dependency check does not validate that the binary can actually run the needed subcommand. The source guard is safe for normal sourcing; if the script is executed directly, it simply defines functions and exits without invoking them.
- validation_or_tests: Dedicated router tests cover Codex routing for `gpt-5.3-codex`, `gpt-4o`, `o3-mini`, `o1-pro`, and `o4-mini`; Claude routing for `haiku`, `sonnet`, `opus`, `claude-sonnet-4-6`, and uppercase `OPUS`; unknown and empty model failures; dependency success/failure using mock `codex` and `claude`; and effort mapping/failure cases ([tests/test-model-router.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-model-router.sh:37), [tests/test-model-router.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-model-router.sh:243), [tests/test-model-router.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-model-router.sh:311)). Integration-style routing tests for the caller cover gpt-to-Codex, Claude-family routing, unknown model errors, missing binary behavior, Claude-to-Codex fallback, `codex-only` provider mode, placeholder BitLesson short-circuiting, and safe Codex selector flags ([tests/test-bitlesson-select-routing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-bitlesson-select-routing.sh:137), [tests/test-bitlesson-select-routing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-bitlesson-select-routing.sh:349), [tests/test-bitlesson-select-routing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-bitlesson-select-routing.sh:440)). The repository aggregate test list includes both `test-bitlesson-select-routing.sh` and `test-model-router.sh` ([tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/run-all-tests.sh:95)). I inspected tests but did not execute them because this assignment requested research notes only and the branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-173`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`