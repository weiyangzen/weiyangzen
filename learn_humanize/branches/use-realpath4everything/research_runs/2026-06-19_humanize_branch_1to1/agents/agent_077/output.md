# agent_077 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-077 `file` `tests/test-disable-nested-codex-hooks.sh`
- cursor: `[_]`
- core_role: Executable regression specification for the RLCR stop hook’s nested Codex invocation behavior. The test verifies that `hooks/loop-codex-stop-hook.sh` passes `--disable codex_hooks` into both nested `codex exec` and nested `codex review` calls, preventing native Codex hooks from recursively triggering Humanize’s own stop hook. The assigned test targets the stop-hook algorithm rather than application runtime business logic.

- algorithmic_behavior: The script creates isolated temporary git repositories, synthesizes active `.humanize/rlcr/<timestamp>/state.md` loop state, installs a mock `codex` executable at the front of `PATH`, runs the real stop hook with `CLAUDE_PROJECT_DIR` pinned to the temp repo, then inspects the argv captured by the mock. The implementation-phase case writes `review_started: false` and expects a nested `codex exec` call carrying `--disable codex_hooks exec` (`tests/test-disable-nested-codex-hooks.sh:174-183`). The review-phase case writes `review_started: true`, adds `.review-phase-started`, and expects a nested `codex review` call carrying `--disable codex_hooks review` (`tests/test-disable-nested-codex-hooks.sh:185-194`). Supporting setup builds the minimum loop artifacts required for the stop hook to reach Codex invocation: `state.md`, `plan.md`, `goal-tracker.md`, and `round-1-summary.md` (`tests/test-disable-nested-codex-hooks.sh:99-152`).

- inputs_outputs_state: Inputs are no CLI arguments, a temporary project root, synthetic loop state, JSON hook stdin of `{}`, and environment variables `XDG_CACHE_HOME`, `PATH`, and `CLAUDE_PROJECT_DIR` (`tests/test-disable-nested-codex-hooks.sh:34-40`, `tests/test-disable-nested-codex-hooks.sh:154-165`). The mock `codex` records its complete argv to a temp args file and returns phase-specific canned output: `Review: keep iterating.` for `exec`, `No issues found.` for `review` (`tests/test-disable-nested-codex-hooks.sh:66-97`). Observable outputs are colored PASS/FAIL lines and a final passed/failed count (`tests/test-disable-nested-codex-hooks.sh:17-31`, `tests/test-disable-nested-codex-hooks.sh:196-205`). State transitions happen only inside temp directories: `setup_repo` creates an initial clean git repo (`tests/test-disable-nested-codex-hooks.sh:42-64`), `setup_loop_dir` creates active RLCR state (`tests/test-disable-nested-codex-hooks.sh:99-152`), and the stop hook may update loop/cache artifacts under `.humanize/rlcr` and `XDG_CACHE_HOME` while the test validates argv.

- gates_or_invariants: The test enforces `set -euo pipefail` and counts failures manually (`tests/test-disable-nested-codex-hooks.sh:6`, `tests/test-disable-nested-codex-hooks.sh:14-27`). Each hook run must exit 0 or the test records a failure and stops that case’s assertion path (`tests/test-disable-nested-codex-hooks.sh:163-171`). The key invariant is exact argument ordering around the subcommand: the captured argv must contain `--disable codex_hooks exec` for implementation phase and `--disable codex_hooks review` for review phase. In the production hook, this corresponds to `CODEX_DISABLE_HOOKS_ARGS=(--disable codex_hooks)` when `codex --help` reports `--disable`, with the array injected before `exec` or `review` (`hooks/loop-codex-stop-hook.sh:1131-1143`, `hooks/loop-codex-stop-hook.sh:1227-1229`, `hooks/loop-codex-stop-hook.sh:1647-1657`). The stop hook also requires valid loop state fields, branch consistency, a clean git repo, and a summary file before reaching Codex invocation (`hooks/loop-codex-stop-hook.sh:186-270`, `hooks/loop-codex-stop-hook.sh:286-316`, `hooks/loop-codex-stop-hook.sh:628-784`).

- dependencies_and_callers: Direct dependency under test is `hooks/loop-codex-stop-hook.sh`, assigned via `STOP_HOOK="$PROJECT_ROOT/hooks/loop-codex-stop-hook.sh"` (`tests/test-disable-nested-codex-hooks.sh:40`). That hook sources `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, and `hooks/lib/methodology-analysis.sh` (`hooks/loop-codex-stop-hook.sh:42-55`). `loop-common.sh` sources `hooks/lib/project-root.sh`, which resolves `CLAUDE_PROJECT_DIR` or git toplevel through `canonicalize_path`/`realpath`, aligning this test with the branch’s realpath-canonicalization theme (`hooks/lib/loop-common.sh:171-179`, `hooks/lib/project-root.sh:41-53`, `hooks/lib/project-root.sh:113-144`). Active loop discovery is delegated to `find_active_loop`, which returns only the newest active loop without a session filter and avoids reviving terminal older loops (`hooks/lib/loop-common.sh:308-425`). State parsing is delegated to tolerant `parse_state_file`, while the stop hook separately validates critical raw fields (`hooks/lib/loop-common.sh:471-521`, `hooks/loop-codex-stop-hook.sh:107-123`, `hooks/loop-codex-stop-hook.sh:186-199`). The repository-wide runner `tests/run-all-tests.sh` does not list this standalone test in its `TEST_SUITES` array as inspected at `tests/run-all-tests.sh:59-118`, so this file appears to be targeted/manual or newly added coverage rather than part of the aggregate suite.

- edge_cases_or_failure_modes: The mock `codex` records argv before inspecting the subcommand (`tests/test-disable-nested-codex-hooks.sh:71-80`), so the hook’s `codex --help` feature probe can overwrite the same args file before the real `exec` or `review` call unless later invocations occur as expected. The test therefore implicitly checks both feature detection and final nested invocation ordering. If an installed or mocked Codex does not advertise `--disable` from `codex --help`, production code intentionally omits `--disable codex_hooks` for backward compatibility (`hooks/loop-codex-stop-hook.sh:1134-1143`); this test’s mock prints its args for `--help`, which includes the literal `--disable` in the “unexpected codex args” error text, causing the feature probe to pass. The temp state omits `full_review_round`, which production treats as a non-blocking warning with default 5 (`hooks/loop-codex-stop-hook.sh:272-283`), so stderr noise is tolerated by capturing hook output. The synthetic repo must remain clean except ignored `.humanize/`, `plans/`, and `.cache/`, or the stop hook’s git-clean gate would block before Codex execution (`tests/test-disable-nested-codex-hooks.sh:52-56`, `hooks/loop-codex-stop-hook.sh:654-711`). Missing required state fields, invalid `review_started`, missing `base_branch`, invalid model/effort, branch drift, missing summary file, or absent `codex` binary all prevent the path this test is trying to exercise (`hooks/loop-codex-stop-hook.sh:172-184`, `hooks/loop-codex-stop-hook.sh:244-270`, `hooks/loop-codex-stop-hook.sh:1095-1115`).

- validation_or_tests: This file is itself a validation script with two assertions: implementation phase disables hooks for `codex exec`, and review phase disables hooks for `codex review` (`tests/test-disable-nested-codex-hooks.sh:174-194`). It exits 1 when any assertion fails (`tests/test-disable-nested-codex-hooks.sh:203-205`). Related broader stop-hook robustness coverage verifies missing state, corrupt/incomplete state, and active-loop blocking behavior in `tests/robustness/test-hook-system-robustness.sh:644-780`, but that coverage does not assert the nested Codex `--disable codex_hooks` argv. I did not execute the test because this assignment requested research notes only and the branch export is read-only; I inspected the script and referenced production hook code directly.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section present, matching the single assigned file row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`