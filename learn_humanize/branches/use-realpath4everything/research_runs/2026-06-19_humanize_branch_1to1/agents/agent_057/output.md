# agent_057 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-057 `file` `scripts/statusline.sh`
- cursor: `[_]`
- core_role: `scripts/statusline.sh` is a Bash status-line renderer for a CLI session, with one repository-specific responsibility: exposing the Humanize RLCR loop state as the final status field. The header says the generic status fields are community/template material and only the final RLCR field is tied to this repo (`scripts/statusline.sh:7`, `scripts/statusline.sh:12`). Algorithmically it consumes a JSON status payload from stdin, queries local session/project files, computes display fields, then emits one or more colored terminal lines (`scripts/statusline.sh:19`, `scripts/statusline.sh:390`).
- algorithmic_behavior: The script first reads all stdin into `input`, then `get_value` extracts JSON values through `jq` with empty fallback (`scripts/statusline.sh:19`, `scripts/statusline.sh:22`). It formats cost, duration, line counts, model, session display name, fast mode, CWD, git branch, context usage, and RLCR state into parallel colored/plain field arrays (`scripts/statusline.sh:247`, `scripts/statusline.sh:251`, `scripts/statusline.sh:351`). The RLCR resolver has two layers: `_resolve_rlcr_display` maps a chosen loop directory to `Analyzing`, `Finalizing`, `Active`, a capitalized terminal reason, or `Off` based on state-file presence (`scripts/statusline.sh:37`); `get_rlcr_status` chooses the relevant loop directory under `.humanize/rlcr`, preferring session-aware state files and matching the current `session_id` when available (`scripts/statusline.sh:67`). The context bar builder converts `.context_window.used_percentage` into a 20-cell visible bar and colors remaining capacity green/yellow/red by threshold (`scripts/statusline.sh:278`). Final layout uses terminal width from `/dev/tty`, caps line width at 75%, and greedily wraps fields when plain-text length would exceed that cap (`scripts/statusline.sh:379`, `scripts/statusline.sh:384`).
- inputs_outputs_state: Inputs are the status JSON fields `.model.display_name`, `.cwd`, `.session_id`, `.transcript_path`, `.cost.total_cost_usd`, `.cost.total_duration_ms`, `.cost.total_lines_added`, `.cost.total_lines_removed`, and `.context_window.used_percentage` (`scripts/statusline.sh:169`, `scripts/statusline.sh:242`, `scripts/statusline.sh:326`). File-system inputs include `$CWD/.humanize/rlcr/*/*-state.md` for RLCR state (`scripts/statusline.sh:258`), transcript JSONL at either supplied `.transcript_path` or `$HOME/.claude/projects/<project>/<session>.jsonl` (`scripts/statusline.sh:189`), `$HOME/.claude/projects/<project>/sessions-index.json` (`scripts/statusline.sh:204`), `$HOME/.claude/settings.json` for `fastMode` (`scripts/statusline.sh:221`), and the git repo at `$CWD` for branch name (`scripts/statusline.sh:238`). Output is terminal text with ANSI color sequences, split across wrapped lines (`scripts/statusline.sh:398`, `scripts/statusline.sh:403`). The script does not mutate repository state; it only reads JSON, repo/session metadata, git branch state, and user settings.
- gates_or_invariants: Missing or invalid JSON values collapse to empty/defaults through `jq -r "$1 // empty"` and shell fallbacks (`scripts/statusline.sh:22`, `scripts/statusline.sh:253`). RLCR is gated by both a valid `$CWD` directory and `$CWD/.humanize` existence; otherwise status is `Off` (`scripts/statusline.sh:257`). If `.humanize/rlcr` is absent, `get_rlcr_status` returns `Off` immediately (`scripts/statusline.sh:71`). If any RLCR markdown state file contains non-empty `session_id`, session-unaware entries are ignored for status selection (`scripts/statusline.sh:76`, `scripts/statusline.sh:142`). Without a current `session_id`, the resolver either checks only the newest directory in fully session-unaware mode for zombie-loop protection (`scripts/statusline.sh:82`) or picks the newest directory that has any stored session id (`scripts/statusline.sh:94`). With a current `session_id`, it scans newest-to-oldest for a matching stored session id (`scripts/statusline.sh:121`, `scripts/statusline.sh:148`). Visible width is measured from plain copies of fields, with ANSI stripped from the context bar (`scripts/statusline.sh:347`, `scripts/statusline.sh:376`).
- dependencies_and_callers: Runtime dependencies are `bash`, `jq`, `git`, `grep`, `awk`, `sed`, `sort`, `head`, `tail`, `basename`, `cut`, `tr`, `stty`, and `tput`. There is no checked-in direct caller found for `scripts/statusline.sh`; it appears intended to be configured externally as a Claude Code status-line command. Its RLCR directory-selection comment says it mirrors `find_active_loop` from `hooks/lib/loop-common.sh` (`scripts/statusline.sh:65`). The shared implementation resolves active state priority as `methodology-analysis-state.md`, `finalize-state.md`, then `state.md` (`hooks/lib/loop-common.sh:268`) and its session-filter branch returns only active state for the matched session, stopping on terminal newest state (`hooks/lib/loop-common.sh:385`). `statusline.sh` also mirrors state-file display semantics used by monitoring helpers such as `scripts/lib/monitor-common.sh`, which treats `methodology-analysis-state.md` and `state.md` as active statuses (`scripts/lib/monitor-common.sh:162`).
- edge_cases_or_failure_modes: The script does not use `realpath` or canonical path normalization in this file; `$CWD`, transcript paths, and `$CWD/.humanize/rlcr` are used as provided after simple existence checks (`scripts/statusline.sh:171`, `scripts/statusline.sh:258`). That means symlinked working directories can display and inspect symlink-relative `.humanize` paths rather than canonical project paths. The RLCR display chooser checks `methodology-analysis-state.md` before `finalize-state.md` and `state.md` (`scripts/statusline.sh:40`), but the session-id-filtered `any_state` selection omits `methodology-analysis-state.md` and starts with `finalize-state.md`, then `state.md`, then wildcard terminal states (`scripts/statusline.sh:127`); a methodology-analysis session with a session id may still be found through wildcard fallback, but after lower-priority named files if multiple state files coexist. Compared with `hooks/lib/loop-common.sh`, this script’s session-filter mode returns a display for terminal state files too, because `_resolve_rlcr_display` maps any `*-state.md` to a terminal reason (`scripts/statusline.sh:47`), while the core `find_active_loop` returns empty if the matched session’s newest loop is terminal (`hooks/lib/loop-common.sh:387`, `hooks/lib/loop-common.sh:393`). The project directory name for Claude files is built with `sed 's|[/.]|-|g'`, so unusual path normalization, symlinks, or spaces are not canonicalized (`scripts/statusline.sh:185`). `COST_STR=$(printf "%.2f" "${COST:-0}")` and context rounding assume numeric values; malformed JSON strings may trigger shell/printf diagnostics or arithmetic errors (`scripts/statusline.sh:248`, `scripts/statusline.sh:329`). If `/dev/tty` is unavailable, width falls back through `tput` or `80` (`scripts/statusline.sh:379`). `ls | sort -r` assumes timestamp-like directory names sort newest-first lexicographically (`scripts/statusline.sh:86`, `scripts/statusline.sh:152`).
- validation_or_tests: I inspected the file directly with line numbers and ran `bash -n scripts/statusline.sh`; syntax validation exited `0`. I found no dedicated checked-in `statusline.sh` test by repository search. Related behavioral coverage exists around the shared RLCR loop resolver and state files rather than this statusline wrapper, including session-id and finalization tests that exercise `find_active_loop`, `state.md`, and `finalize-state.md` paths. The closest directly relevant implementation reference is `hooks/lib/loop-common.sh:332`, whose documented resolver behavior the statusline claims to mirror.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `USE_REALPATH4EVERYTHING-HZ-057`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`