# agent_113 use-realpath4everything 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `cf17140050c4e063f27924c2d56cc2279d81f4cd`

## Item Evidence

### USE_REALPATH4EVERYTHING-HZ-113 `file` `prompt-template/block/bitlesson-delta-missing.md`
- cursor: `[_]`
- core_role: This is a fail-closed prompt/block template used when a round summary does not contain the required `## BitLesson Delta` section. Its role is not path canonicalization itself, but it is part of the loop validation contract: when the validator cannot find the BitLesson delta section, this template becomes the user-facing block reason that tells the agent the exact section it must add. The file is short and declarative: [prompt-template/block/bitlesson-delta-missing.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/prompt-template/block/bitlesson-delta-missing.md:1).
- algorithmic_behavior: The template supplies a deterministic remediation message. It declares the missing condition at lines 1-3, then defines the minimum accepted markdown shape at lines 5-12: heading `## BitLesson Delta`, `Action: none|add|update`, `Lesson ID(s): <IDs or NONE>`, and `Notes: <what changed and why>`. The runtime algorithm that consumes it is in `scripts/bitlesson-validate-delta.sh`: `extract_bitlesson_delta_block detect` scans the summary, ignores fenced code blocks and HTML comments, and exits nonzero if no real `## BitLesson Delta` heading is found; on that failure path the script loads this template through `load_and_render_safe` and returns a JSON block decision.
- inputs_outputs_state: Input to this template is effectively the missing-section validator state; the template itself has no variables and no rendered substitutions. The caller receives summary path, BitLesson file path, relative BitLesson path, `allow-empty-none`, template directory, and current round as CLI inputs in [scripts/bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-validate-delta.sh:6). Output is embedded into a JSON object with `"decision": "block"`, `"reason": <rendered template>`, and a `systemMessage` of `Loop: Summary missing BitLesson Delta section (round $CURRENT_ROUND)` via `block_exit` at [scripts/bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-validate-delta.sh:78) and the missing-section call site at [scripts/bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-validate-delta.sh:181). No persistent state is updated by the template; the state transition is control-flow only: summary accepted path continues to parse action/IDs/notes, missing-section path blocks exit with remediation text.
- gates_or_invariants: The invariant enforced by this block is that every accepted summary must include an actual top-level `## BitLesson Delta` section outside code fences and outside HTML comments. The minimal schema in the template is also the downstream parser contract: exactly one action among `none`, `add`, or `update` is required, and later validation enforces action/ID/notes consistency. The missing-section gate is fail-closed because the fallback embedded in the caller mirrors this same template text if the template file cannot be loaded, so the block reason remains actionable even if template loading fails.
- dependencies_and_callers: Direct caller: `scripts/bitlesson-validate-delta.sh` uses `load_and_render_safe "$TEMPLATE_DIR" "block/bitlesson-delta-missing.md" "$FALLBACK"` at [scripts/bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-validate-delta.sh:196). It depends on `hooks/lib/template-loader.sh` being sourced at [scripts/bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/scripts/bitlesson-validate-delta.sh:76). Related sibling templates handle later gates: invalid action, inconsistent IDs, empty knowledge base, and missing notes. Reference coverage also exists through repository-wide template reference validation in `tests/test-template-references.sh`, though that script’s hard-coded critical script list focuses mostly on hook validators and may not fully enumerate this standalone script.
- edge_cases_or_failure_modes: A fake delta heading inside a fenced code block or HTML comment must not satisfy the gate; the extractor tracks both states before detecting headings. Heading matching is case-insensitive for the exact text `BitLesson Delta`, but requires a level-two markdown heading shape. If the template file is missing, the caller’s fallback text preserves the same required minimal format, avoiding an empty or unclear block. If the summary file itself is missing, the validator exits with a shell error before template use. If the section exists but is malformed, this template is no longer used; control passes to later block templates for invalid action, inconsistent IDs, or missing notes.
- validation_or_tests: Specific behavioral tests in [tests/test-bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-bitlesson-validate-delta.sh:116) cover the missing-section-equivalent edge cases where `## BitLesson Delta` appears only inside a fenced code block or HTML comment and must block. The same test file covers valid normal text at [tests/test-bitlesson-validate-delta.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-bitlesson-validate-delta.sh:149). Template existence/reference checks are implemented in [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/use-realpath4everything/tests/test-template-references.sh:1), including missing-template reporting and `load_and_render_safe` expectations, but I did not run tests in this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 evidence section for the single assigned item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`