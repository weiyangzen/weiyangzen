# agent_23 templify-prompt 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `0c107abc65600bca4e18b04265b48c91da772491`

## Item Evidence

### TEMPLIFY_PROMPT-HZ-023 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Initializes an RLCR loop session for `/humanize:start-rlcr-loop`. It is the setup boundary that converts a user-supplied implementation plan into local loop state under `.humanize-loop.local/<timestamp>/`, including `state.md`, `goal-tracker.md`, and `round-0-prompt.md`. It does not perform Codex review itself; later stop-hook logic consumes the files it creates.
- algorithmic_behavior: The script runs with `set -euo pipefail` (`scripts/setup-rlcr-loop.sh:11`), parses one plan file plus loop options (`--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`) (`scripts/setup-rlcr-loop.sh:93`), validates prerequisites, creates a timestamped loop directory, seeds persistent state, builds a goal tracker, and emits the initial prompt to stdout. The parser enforces a single plan file and rejects unknown options (`scripts/setup-rlcr-loop.sh:141`, `scripts/setup-rlcr-loop.sh:147`). Relative plan paths are anchored to `${CLAUDE_PROJECT_DIR:-$(pwd)}` (`scripts/setup-rlcr-loop.sh:173`). The state file starts `current_round: 0` and records max iteration, Codex model/effort/timeout, push policy, plan path, and start time (`scripts/setup-rlcr-loop.sh:223`). The goal tracker is seeded with immutable and mutable sections (`scripts/setup-rlcr-loop.sh:243`), using heuristics to extract a goal and acceptance criteria from headings (`scripts/setup-rlcr-loop.sh:263`, `scripts/setup-rlcr-loop.sh:285`). The initial prompt requires Round 0 goal-tracker initialization, Todo creation for all tasks, implementation from the plan, optional push behavior, a commit, and a summary at `round-0-summary.md` (`scripts/setup-rlcr-loop.sh:336`, `scripts/setup-rlcr-loop.sh:389`, `scripts/setup-rlcr-loop.sh:401`).
- inputs_outputs_state: Inputs are CLI arguments, the plan markdown file, `${CLAUDE_PROJECT_DIR}` or current directory, current time, and availability of the `codex` CLI. Outputs are a new loop directory at `$PROJECT_ROOT/.humanize-loop.local/$TIMESTAMP` (`scripts/setup-rlcr-loop.sh:207`), `state.md` (`scripts/setup-rlcr-loop.sh:223`), `goal-tracker.md` (`scripts/setup-rlcr-loop.sh:240`), `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:336`), and user-facing setup/completion instructions on stdout (`scripts/setup-rlcr-loop.sh:401`). The state transition is inactive/no newest active loop to active loop with `current_round: 0`; later `hooks/loop-codex-stop-hook.sh` increments the round by rewriting `state.md` (`hooks/loop-codex-stop-hook.sh:763`) or terminates by removing `state.md` on `COMPLETE`, `STOP`, max iteration, or cancellation (`hooks/loop-codex-stop-hook.sh:725`, `hooks/loop-codex-stop-hook.sh:755`, `commands/cancel-rlcr-loop.md:21`).
- gates_or_invariants: Setup gates include required plan file (`scripts/setup-rlcr-loop.sh:163`), existing file (`scripts/setup-rlcr-loop.sh:179`), minimum five lines (`scripts/setup-rlcr-loop.sh:185`), installed Codex CLI (`scripts/setup-rlcr-loop.sh:195`), positive integer values for `--max` and `--codex-timeout` (`scripts/setup-rlcr-loop.sh:103`, `scripts/setup-rlcr-loop.sh:130`), and at most one plan file (`scripts/setup-rlcr-loop.sh:149`). Loop invariants established by the generated prompt are: Round 0 may initialize immutable goal content, after Round 0 the immutable section is read-only (`scripts/setup-rlcr-loop.sh:339`, `scripts/setup-rlcr-loop.sh:349`), every task must be represented in Todos (`scripts/setup-rlcr-loop.sh:355`), the agent must not bypass the loop by editing state or cancelling (`scripts/setup-rlcr-loop.sh:380`), and completion requires commit plus summary, with push only if enabled (`scripts/setup-rlcr-loop.sh:382`, `scripts/setup-rlcr-loop.sh:430`).
- dependencies_and_callers: The slash command wrapper calls this script directly through `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:4`, `commands/start-rlcr-loop.md:12`). Hook registration is external in `hooks/hooks.json`: PreToolUse validators for Write/Edit/Read/Bash and a Stop hook for Codex review (`hooks/hooks.json:4`, `hooks/hooks.json:42`). Shared loop discovery treats only the newest timestamp directory containing `state.md` as active (`hooks/lib/loop-common.sh:23`). Stop-hook behavior consumes `state.md`, the current round prompt/summary, and `goal-tracker.md`; it blocks for missing summaries, uncommitted changes, unpushed commits if enabled, uninitialized Round 0 goal tracker, and large files before running Codex review (`hooks/loop-codex-stop-hook.sh:62`, `hooks/loop-codex-stop-hook.sh:180`, `hooks/loop-codex-stop-hook.sh:316`, `hooks/loop-codex-stop-hook.sh:343`). Validators protect the files created by setup: prompt files are read-only, state files are system-managed, summaries must match current round and active loop directory, and goal tracker edits are blocked after Round 0 (`hooks/loop-write-validator.sh:42`, `hooks/loop-write-validator.sh:85`, `hooks/loop-write-validator.sh:94`, `hooks/loop-edit-validator.sh:72`, `hooks/loop-read-validator.sh:103`, `hooks/loop-bash-validator.sh:72`).
- edge_cases_or_failure_modes: Heading extraction for goal and acceptance criteria is heuristic and may miss valid plans; it only scans limited windows and uses regex forms like `^\s*##` that may be shell/toolchain dependent (`scripts/setup-rlcr-loop.sh:263`, `scripts/setup-rlcr-loop.sh:285`). If extraction fails, placeholders are written and Round 0 must fill them; the stop hook blocks if those placeholders remain (`hooks/loop-codex-stop-hook.sh:348`). An existing older loop is ignored if a newer timestamp directory exists, even if the older one still has `state.md` (`hooks/lib/loop-common.sh:23`). The script creates local state files but does not itself verify `.humanize-loop.local` ignore status; stop-hook git-clean checks special-case untracked `.humanize-loop.local` as intentionally local (`hooks/loop-codex-stop-hook.sh:198`). `PLAN_CONTENT=$(cat "$PLAN_FILE")` is assigned but unused (`scripts/setup-rlcr-loop.sh:241`). The generated prompt embeds absolute paths; if paths contain spaces, the natural-language `@path` references may be less robust for Claude file-link parsing, though the shell writes them correctly.
- validation_or_tests: Direct setup-script tests were not found in the inspected test set. Existing validation around this workflow is mostly hook/template focused: `tests/test-template-references.sh` scans hook scripts and ensures referenced templates exist (`tests/test-template-references.sh:51`), while `tests/test-templates-comprehensive.sh` loads all templates, checks placeholder syntax, and exercises rendering edge cases (`tests/test-templates-comprehensive.sh:61`, `tests/test-templates-comprehensive.sh:82`, `tests/test-templates-comprehensive.sh:114`). Runtime validation is enforced by the stop hook and PreToolUse validators rather than a dedicated setup unit test.
- skip_candidate: `no`

### TEMPLIFY_PROMPT-HZ-053 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Claude-facing continuation template used when Codex review finds unresolved work and blocks exit. It defines the transition contract from a failed/blocked review into the next RLCR round by restating the original plan, injecting Codex feedback, requiring complete Todo coverage, and constraining goal-tracker access.
- algorithmic_behavior: The template opens with an explicit continuation signal, “Your work is not finished,” then instructs Claude to review the original implementation plan via `@{{PLAN_FILE}}` (`prompt-template/claude/next-round-prompt.md:1`, `prompt-template/claude/next-round-prompt.md:5`). It mandates Todo creation for all tasks and all discovered issues, and explicitly disallows only addressing the most important issues (`prompt-template/claude/next-round-prompt.md:12`). It injects the full Codex review payload between review delimiters (`prompt-template/claude/next-round-prompt.md:16`). It then points Claude at `@{{GOAL_TRACKER_FILE}}` for read-only context on ultimate goal, acceptance criteria, active/completed/deferred tasks, plan evolution, and open issues (`prompt-template/claude/next-round-prompt.md:22`). The final invariant says the goal tracker cannot be directly modified after Round 0; updates must be requested in the round summary (`prompt-template/claude/next-round-prompt.md:30`).
- inputs_outputs_state: Inputs are the placeholders `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE` (`prompt-template/claude/next-round-prompt.md:6`, `prompt-template/claude/next-round-prompt.md:18`, `prompt-template/claude/next-round-prompt.md:24`). The rendered output is written by the stop hook as `.humanize-loop.local/<timestamp>/round-${NEXT_ROUND}-prompt.md` after incrementing `current_round` (`hooks/loop-codex-stop-hook.sh:763`, `hooks/loop-codex-stop-hook.sh:768`, `hooks/loop-codex-stop-hook.sh:781`). The state transition is Codex review not ending in `COMPLETE` or `STOP` to blocked exit plus next-round instructions returned in hook JSON (`hooks/loop-codex-stop-hook.sh:712`, `hooks/loop-codex-stop-hook.sh:819`). The template is not the complete prompt alone: the hook appends post-alignment action items for rounds after full alignment checks, then a footer with commit/summary instructions, optional push note, and goal-tracker update request guidance (`hooks/loop-codex-stop-hook.sh:786`, `hooks/loop-codex-stop-hook.sh:794`, `hooks/loop-codex-stop-hook.sh:800`, `hooks/loop-codex-stop-hook.sh:809`).
- gates_or_invariants: The template preserves three major invariants: alignment with the original plan (`prompt-template/claude/next-round-prompt.md:5`), exhaustive issue/task tracking rather than priority-only repair (`prompt-template/claude/next-round-prompt.md:12`), and goal-tracker read-only behavior after Round 0 (`prompt-template/claude/next-round-prompt.md:30`). The stop hook enforces the surrounding gate: if Codex’s last non-empty review line is exactly `COMPLETE`, state is removed and exit is allowed; if it is `STOP`, the circuit breaker terminates; otherwise this template is rendered and exit is blocked (`hooks/loop-codex-stop-hook.sh:712`, `hooks/loop-codex-stop-hook.sh:718`, `hooks/loop-codex-stop-hook.sh:729`, `hooks/loop-codex-stop-hook.sh:760`). The Write/Edit/Bash validators enforce the prompt’s goal-tracker rule after Round 0 (`hooks/loop-write-validator.sh:94`, `hooks/loop-edit-validator.sh:81`, `hooks/loop-bash-validator.sh:83`).
- dependencies_and_callers: Called only through `load_and_render_safe "$TEMPLATE_DIR" "claude/next-round-prompt.md"` in the stop hook (`hooks/loop-codex-stop-hook.sh:781`). Rendering uses the template loader’s single-pass `{{VAR}}` substitution, intentionally preventing placeholder injection from values such as `REVIEW_CONTENT` (`hooks/lib/template-loader.sh:35`, `hooks/lib/template-loader.sh:39`, `hooks/lib/template-loader.sh:56`). Template directory discovery comes from `hooks/lib/loop-common.sh`, which sources `template-loader.sh` and sets `TEMPLATE_DIR` (`hooks/lib/loop-common.sh:11`, `hooks/lib/loop-common.sh:15`). Prompt files are protected as generated instructions: reads must target the active/current round prompt, and writes/edits/bash modifications are blocked (`hooks/loop-read-validator.sh:45`, `hooks/loop-read-validator.sh:103`, `hooks/loop-write-validator.sh:42`, `hooks/loop-edit-validator.sh:41`, `hooks/loop-bash-validator.sh:99`).
- edge_cases_or_failure_modes: The base template references “see below” for Goal Tracker Update Request instructions (`prompt-template/claude/next-round-prompt.md:31`), but that section is appended separately by the hook; if the append template is missing, the hook uses a fallback sentence (`hooks/loop-codex-stop-hook.sh:809`). The template depends on unquoted `@{{PLAN_FILE}}` and `@{{GOAL_TRACKER_FILE}}` references, so paths with spaces may be less reliable in Claude’s file-reference syntax, even though rendering itself is safe. If the Codex review content is extremely large, it is embedded directly into the next prompt; there is no truncation in this template path. The single-pass renderer prevents accidental substitution of `{{...}}` sequences inside review content, which is a positive guard against prompt corruption (`hooks/lib/template-loader.sh:39`).
- validation_or_tests: Template existence is covered by `tests/test-template-references.sh`, which scans hook `load_template` / `load_and_render_safe` calls and verifies referenced files exist (`tests/test-template-references.sh:51`, `tests/test-template-references.sh:93`). General template loading, placeholder syntax, and rendering behavior are covered by `tests/test-templates-comprehensive.sh` (`tests/test-templates-comprehensive.sh:82`, `tests/test-templates-comprehensive.sh:114`, `tests/test-templates-comprehensive.sh:262`). The hook also has a runtime fallback for this template if missing or empty (`hooks/loop-codex-stop-hook.sh:772`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TEMPLIFY_PROMPT-HZ-023`, `TEMPLIFY_PROMPT-HZ-053`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`