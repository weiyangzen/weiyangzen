# agent_21 templify-prompt 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `0c107abc65600bca4e18b04265b48c91da772491`

## Item Evidence

### TEMPLIFY_PROMPT-HZ-021 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Shell-facing runtime monitor for Humanize RLCR loop sessions. It exposes `humanize monitor rlcr-loop`, reads loop state from `.humanize-loop.local`, reads Codex run logs from `$HOME/.cache/humanize/<sanitized-project-path>/<session>/`, renders a fixed terminal status bar, and streams the current round log.
- algorithmic_behavior: Main entrypoint `humanize()` dispatches only the `monitor rlcr-loop` command to `_humanize_monitor_codex()` and rejects other commands with usage text (`scripts/humanize.sh:521`, `scripts/humanize.sh:525`, `scripts/humanize.sh:530`, `scripts/humanize.sh:545`). The monitor first gates on `.humanize-loop.local` existing (`scripts/humanize.sh:16`). It selects the latest timestamp-named session by lexicographic timestamp ordering (`scripts/humanize.sh:23`) and selects the latest `round-*-codex-run.log` by newest session, then highest round number (`scripts/humanize.sh:43`, `scripts/humanize.sh:75`, `scripts/humanize.sh:84`). It parses `state.md`, `goal-tracker.md`, and git status into pipe-delimited values, splits them into arrays, then repeatedly redraws a status bar while tailing new log bytes (`scripts/humanize.sh:98`, `scripts/humanize.sh:116`, `scripts/humanize.sh:200`, `scripts/humanize.sh:248`, `scripts/humanize.sh:263`, `scripts/humanize.sh:456`).
- inputs_outputs_state: Inputs are current working directory, `.humanize-loop.local/<timestamp>/state.md`, `.humanize-loop.local/<timestamp>/goal-tracker.md`, cached Codex logs, terminal size via `tput`, and repo status via git. Output is terminal UI only: status lines for session, round/model, AC/task/issue counts, git diff summary, goal, plan, log path, and streamed log content (`scripts/humanize.sh:339`, `scripts/humanize.sh:352`, `scripts/humanize.sh:358`, `scripts/humanize.sh:372`). It does not write repository state; its only state transitions are local monitor variables such as `current_file`, `current_session_dir`, `last_size`, and switching to newer logs when discovered (`scripts/humanize.sh:493`, `scripts/humanize.sh:499`).
- gates_or_invariants: Requires `.humanize-loop.local` in the current directory or exits with error (`scripts/humanize.sh:16`). Session directories must match `YYYY-MM-DD_HH-MM-SS` (`scripts/humanize.sh:34`, `scripts/humanize.sh:65`). Log discovery is restricted to session timestamps present locally, even though logs live in cache, so stale unrelated cache logs are ignored (`scripts/humanize.sh:57`). Missing `state.md` returns `N/A` fields; missing `goal-tracker.md` returns zero counts and “No goal tracker” (`scripts/humanize.sh:101`, `scripts/humanize.sh:120`). The monitor traps `INT`/`TERM`, kills its background tail PID if present, and restores the terminal scroll region (`scripts/humanize.sh:407`, `scripts/humanize.sh:417`, `scripts/humanize.sh:422`).
- dependencies_and_callers: Intended caller is the sourced shell function documented in README: source `scripts/humanize.sh`, then run `humanize monitor rlcr-loop` (`README.md:90`, `README.md:95`, `README.md:103`). Runtime dependencies are bash or zsh-compatible shell, `sed`, `grep`, `sort`, `wc`, `tr`, `cut`, `head`, `tail`, `stat`, `tput`, `basename`, `seq`, `git`, terminal ANSI support, and the loop directory layout created by setup (`README.md:243`). It relies on cache-path sanitization matching `loop-codex-stop-hook.sh`, noted in-script (`scripts/humanize.sh:51`).
- edge_cases_or_failure_modes: If no logs exist, it waits indefinitely for the first log while polling every two seconds (`scripts/humanize.sh:434`). If `tput` is unavailable or terminal dimensions are too small, drawing/truncation can fail or produce poor layout; no explicit guard exists. Goal tracker parsing is regex/table-shape dependent, so changed heading names or table formats can undercount ACs/tasks/issues (`scripts/humanize.sh:138`, `scripts/humanize.sh:150`, `scripts/humanize.sh:174`, `scripts/humanize.sh:188`). `git diff --shortstat HEAD` may fail in unborn/nonstandard repos and falls back to unstaged diff only (`scripts/humanize.sh:234`). The `tail_pid` cleanup path is mostly inert because the current implementation uses direct `tail`, not a captured background tail process (`scripts/humanize.sh:401`, `scripts/humanize.sh:471`, `scripts/humanize.sh:488`).
- validation_or_tests: No direct test reference for `scripts/humanize.sh` or `_humanize_monitor_codex` was found by repository search. README documents manual validation by sourcing and running the monitor (`README.md:88`). Script-internal runtime validation covers missing loop dir, missing state/goal files, non-git repos, log presence, and cross-platform `stat` fallback (`scripts/humanize.sh:16`, `scripts/humanize.sh:101`, `scripts/humanize.sh:120`, `scripts/humanize.sh:203`, `scripts/humanize.sh:448`).
- skip_candidate: `no`

### TEMPLIFY_PROMPT-HZ-051 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Claude prompt fragment that defines the sanctioned transition path for goal tracker changes after the tracker becomes Codex-controlled: Claude must request changes in its summary, and Codex decides whether to apply them.
- algorithmic_behavior: The template instructs Claude to include a `## Goal Tracker Update Request` section only when updates are needed (`prompt-template/claude/goal-tracker-update-request.md:2`). The requested schema has two fields: `Requested Changes` as bullets for task completion, open issues, plan evolution, or deferrals, and `Justification` explaining why the changes serve the Ultimate Goal (`prompt-template/claude/goal-tracker-update-request.md:4`, `prompt-template/claude/goal-tracker-update-request.md:6`, `prompt-template/claude/goal-tracker-update-request.md:12`). It ends by assigning review/application responsibility to Codex (`prompt-template/claude/goal-tracker-update-request.md:16`).
- inputs_outputs_state: Input is Claude’s observed work outcome and any need to update `goal-tracker.md`. Output is a structured markdown section embedded in Claude’s round summary, not a direct file mutation. State transition is indirect: Claude emits a request; Codex review may later mutate the goal tracker if justified. This pairs with the next-round prompt’s explicit read-only rule after Round 0 (`prompt-template/claude/next-round-prompt.md:22`, `prompt-template/claude/next-round-prompt.md:30`).
- gates_or_invariants: The invariant is “Claude cannot directly modify `goal-tracker.md` after Round 0; request through summary instead.” Write validator enforces this for direct write/edit attempts after Round 0 (`hooks/loop-write-validator.sh:91`, `hooks/loop-write-validator.sh:94`). Bash validator blocks shell commands that modify `goal-tracker.md` in all rounds; for Round 0 it directs use of proper Write/Edit tools, and after Round 0 it redirects to summary request flow (`hooks/loop-bash-validator.sh:78`, `hooks/loop-bash-validator.sh:83`, `hooks/loop-bash-validator.sh:88`). Codex stop hook loads this exact template into the next Claude prompt, falling back to a one-line request instruction if missing (`hooks/loop-codex-stop-hook.sh:809`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` when building `NEXT_PROMPT_FILE` (`hooks/loop-codex-stop-hook.sh:810`, `hooks/loop-codex-stop-hook.sh:814`). It coordinates with `prompt-template/codex/goal-tracker-update-section.md`, which tells Codex to process Claude’s request, and the stop hook loads that Codex-side section for full alignment and regular reviews (`hooks/loop-codex-stop-hook.sh:434`, `hooks/loop-codex-stop-hook.sh:437`). The underlying goal tracker is created during loop setup with immutable and mutable sections plus task-state rules (`scripts/setup-rlcr-loop.sh:240`, `scripts/setup-rlcr-loop.sh:250`, `scripts/setup-rlcr-loop.sh:362`).
- edge_cases_or_failure_modes: If the template is missing, the stop hook silently degrades to a minimal fallback, losing the structured `Requested Changes` and `Justification` schema (`hooks/loop-codex-stop-hook.sh:810`, `hooks/loop-codex-stop-hook.sh:811`). If Claude omits the section despite needing updates, Codex has less structured evidence for goal tracker maintenance. If Claude includes vague requests without evidence, Codex may reject or ignore them under the “if justified” contract (`prompt-template/claude/goal-tracker-update-request.md:16`). The template itself has no placeholders, so it is robust to rendering context but cannot encode the actual target tracker path.
- validation_or_tests: Repository tests cover the Codex-side goal tracker update section rendering, not this Claude fragment directly (`tests/test-templates-comprehensive.sh:531`, `tests/test-templates-comprehensive.sh:535`). Runtime coverage comes from `loop-codex-stop-hook.sh` loading `claude/goal-tracker-update-request.md` into generated next-round prompts and from validators enforcing that request path (`hooks/loop-codex-stop-hook.sh:810`, `hooks/loop-write-validator.sh:94`, `hooks/loop-bash-validator.sh:83`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 Item Evidence sections present, one per assigned row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`