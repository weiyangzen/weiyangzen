# agent_25 templify-prompt 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `0c107abc65600bca4e18b04265b48c91da772491`

## Item Evidence

### TEMPLIFY_PROMPT-HZ-025 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for the prompt-template loading/rendering layer. The script sources `hooks/lib/template-loader.sh` at `tests/test-template-loader.sh:10-12` and verifies the public shell API used by validators and loop hooks: `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`.
- algorithmic_behavior: The test suite defines the expected template algorithm as path resolution plus single-pass placeholder substitution. It first validates that `get_template_dir "$PROJECT_ROOT/hooks/lib"` resolves to `$PROJECT_ROOT/prompt-template` at `tests/test-template-loader.sh:42-52`, matching the implementation in `hooks/lib/template-loader.sh:11-16`. It then asserts that `load_template` returns file contents for existing templates and empty output for missing ones at `tests/test-template-loader.sh:55-78`, corresponding to `hooks/lib/template-loader.sh:21-33`. Rendering behavior is specified through direct strings and real templates: single variable replacement at `tests/test-template-loader.sh:81-93`, multiple variables at `tests/test-template-loader.sh:96-108`, multiline templates at `tests/test-template-loader.sh:111-129`, and real `block/wrong-round-number.md` integration at `tests/test-template-loader.sh:147-164`. The implementation uses `awk` to scan for `{{VAR}}` placeholders once per input line, replacing names from `TMPL_VAR_...` environment variables without re-scanning replacement values (`hooks/lib/template-loader.sh:56-114`).
- inputs_outputs_state: Inputs are shell function arguments: template directory paths, template-relative names such as `block/git-push.md`, template content strings, and `VAR=value` substitution pairs. Outputs are rendered strings on stdout, warnings/errors on stderr in missing/invalid cases, and process exit status from the test script. The script maintains only local test counters `TESTS_PASSED` and `TESTS_FAILED` at `tests/test-template-loader.sh:20-33`, then exits `0` if no failures or `1` otherwise at `tests/test-template-loader.sh:545-563`. It creates no persistent state. The algorithm under test is stateless except for temporarily exporting substitution values through `env` into the `awk` process in `hooks/lib/template-loader.sh:47-59`.
- gates_or_invariants: Key invariants are: missing templates must not crash the caller (`tests/test-template-loader.sh:68-78`); unused variables are no-ops (`tests/test-template-loader.sh:167-179`); unreplaced placeholders stay literal (`tests/test-template-loader.sh:181-194`); safe rendering must use fallback content when a template is missing (`tests/test-template-loader.sh:197-208`) and must prefer the real template when present (`tests/test-template-loader.sh:210-222`); a valid template directory must contain `block`, `codex`, and `claude` subdirectories, as implemented at `hooks/lib/template-loader.sh:190-207` and tested at `tests/test-template-loader.sh:224-244`. The strongest invariant is single-pass injection prevention: values containing `{{...}}` must not trigger second-stage replacement, tested at `tests/test-template-loader.sh:488-543` and documented in the implementation at `hooks/lib/template-loader.sh:35-42`.
- dependencies_and_callers: The test depends on Bash, `grep`, `awk`, `env`, and the repository’s `hooks/lib/template-loader.sh`. It also depends on real prompt templates `prompt-template/block/git-push.md` and `prompt-template/block/wrong-round-number.md`, inspected at `prompt-template/block/git-push.md:1-9` and `prompt-template/block/wrong-round-number.md:1-7`. Callers of the tested loader include `hooks/lib/loop-common.sh:13-16`, `hooks/loop-codex-stop-hook.sh` for many block/review prompt renders including `hooks/loop-codex-stop-hook.sh:781-807`, `hooks/loop-bash-validator.sh:62`, `hooks/loop-write-validator.sh:109-160`, `hooks/loop-edit-validator.sh:108`, and `hooks/loop-read-validator.sh:92-128`. Related coverage exists in `tests/test-templates-comprehensive.sh:261-545`, which broadens edge-case and real-template checks, and `tests/test-template-references.sh:172-199`, which checks critical callers use safe rendering.
- edge_cases_or_failure_modes: The assigned test stresses shell metacharacter safety in values: ampersand, backslash, dollar sign, backticks, pipes, semicolons, globs, parentheses, redirection, quotes, hash, tilde, combined metacharacters, Windows paths, regex-like strings, JSON-like content, and multiline values at `tests/test-template-loader.sh:247-486`. These cases protect against the classic `awk`/`gsub` replacement pitfall where `&` or backslashes are interpreted instead of emitted literally; the current implementation avoids `gsub` and appends values directly (`hooks/lib/template-loader.sh:100-104`). A malformed placeholder without closing braces is treated literally by the implementation at `hooks/lib/template-loader.sh:87-94`, though this specific assigned test focuses more on unreplaced well-formed placeholders. A subtle input constraint is that variable assignments are split on the first `=` by `hooks/lib/template-loader.sh:50-53`, so values may contain `=` but variable names cannot. Empty rendered results trigger fallback in `load_and_render_safe` (`hooks/lib/template-loader.sh:174-184`), which means intentionally empty template files behave like missing templates for safe callers.
- validation_or_tests: This file is itself the validation. It defines 36 numbered checks: baseline loader behavior in tests 1-14, metacharacter literal rendering in tests 15-33, and placeholder-injection prevention in tests 34-36. I inspected it directly and followed its implementation and real-template references. I did not execute the test script during this read-only research pass.
- skip_candidate: `no`

### TEMPLIFY_PROMPT-HZ-055 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: Conditional Claude prompt fragment for the `--push-every-round` mode. The entire template is a short note at `prompt-template/claude/push-every-round-note.md:2`, instructing the worker to push commits to remote after each round when that mode is enabled. It is small, but it participates in the loop’s transition contract between rounds.
- algorithmic_behavior: The file contains no placeholders and no executable logic. Its algorithmic behavior comes from caller-side gating: `hooks/loop-codex-stop-hook.sh` appends the template to the next-round prompt only when `PUSH_EVERY_ROUND` equals `true` (`hooks/loop-codex-stop-hook.sh:800-807`). If the file is missing or empty, the hook falls back to a hardcoded instruction, “Also push your changes after committing,” at `hooks/loop-codex-stop-hook.sh:802-805`. Thus the template is a data-driven branch in prompt generation: append remote-push instruction for push-enabled loops, omit it otherwise.
- inputs_outputs_state: Input state is the loop state field `push_every_round`, read from the active state file in the stop hook at `hooks/loop-codex-stop-hook.sh:245-248`. That state is initialized by `scripts/setup-rlcr-loop.sh`: default `PUSH_EVERY_ROUND="false"` at `scripts/setup-rlcr-loop.sh:27-32`, changed by `--push-every-round` at `scripts/setup-rlcr-loop.sh:137-139`, and written into `state.md` at `scripts/setup-rlcr-loop.sh:224-231`. Output is appended markdown in the generated next prompt file `round-${NEXT_ROUND}-prompt.md`, created at `hooks/loop-codex-stop-hook.sh:768-784` and then extended with footer and push note at `hooks/loop-codex-stop-hook.sh:794-807`. The prompt fragment itself has no internal state.
- gates_or_invariants: The template must only appear when push-every-round mode is enabled; otherwise the default workflow keeps commits local. That invariant is reinforced by the bash validator, which blocks `git push` commands when `push_every_round` is not true (`hooks/loop-bash-validator.sh:48-64`) and uses `prompt-template/block/git-push.md` as the user-facing block message (`prompt-template/block/git-push.md:1-9`). The complementary stop-hook gate requires no unpushed commits when `push_every_round` is true: it detects `git status -sb` “ahead” output and blocks exit with `block/unpushed-commits.md` at `hooks/loop-codex-stop-hook.sh:242-270`. Together, this note is part of a bidirectional contract: no push unless opted in, mandatory push before exit when opted in.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:800-807` through `load_template "$TEMPLATE_DIR" "claude/push-every-round-note.md"`. Template directory resolution is supplied by `hooks/lib/loop-common.sh:13-16`, which sources `hooks/lib/template-loader.sh`. Setup-time related behavior is in `scripts/setup-rlcr-loop.sh`, which writes the same note inline into the round-0 prompt when the flag is enabled (`scripts/setup-rlcr-loop.sh:389-395`) and prints push requirements in the activation message (`scripts/setup-rlcr-loop.sh:432-436`). User-facing flag documentation appears in `commands/start-rlcr-loop.md:3` and `README.md:198`.
- edge_cases_or_failure_modes: Because the template has no variables, rendering cannot corrupt placeholders, but loading can fail. The stop hook handles missing/empty content with a fallback at `hooks/loop-codex-stop-hook.sh:802-805`, so prompt generation remains functional if the template is absent. A behavioral edge case is drift between the template and setup’s inline round-0 text at `scripts/setup-rlcr-loop.sh:389-395`; both currently carry the same instruction, but only later rounds use this template file. Another edge case is stale or malformed `push_every_round` state: only exact string `true` enables the note and the unpushed-commit gate (`hooks/loop-codex-stop-hook.sh:248`, `hooks/loop-codex-stop-hook.sh:801`).
- validation_or_tests: I did not find a dedicated assertion that this exact template is referenced or appended, but related validation exists around template reference discovery in `tests/test-template-references.sh:68-94` and safe rendering usage in `tests/test-template-references.sh:172-199`. The push mode behavior is indirectly specified by setup and hook code: flag parsing at `scripts/setup-rlcr-loop.sh:137-139`, state persistence at `scripts/setup-rlcr-loop.sh:224-231`, prompt append at `hooks/loop-codex-stop-hook.sh:800-807`, push blocking when disabled at `hooks/loop-bash-validator.sh:55-64`, and unpushed commit blocking when enabled at `hooks/loop-codex-stop-hook.sh:248-270`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 item evidence sections
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`