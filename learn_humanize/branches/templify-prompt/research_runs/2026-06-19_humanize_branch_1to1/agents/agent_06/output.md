# agent_06 templify-prompt 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `0c107abc65600bca4e18b04265b48c91da772491`

## Item Evidence

### TEMPLIFY_PROMPT-HZ-006 `directory` `tests`
- cursor: `[_]`
- core_role: The `tests` directory is the validation surface for the prompt-template loading and guard-message algorithm. It recursively contains four shell suites: `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, and `tests/test-error-scenarios.sh`. These tests verify that templates under `prompt-template/` can be found, rendered, syntax-checked, cross-referenced from hooks, and safely degraded to fallback messages when missing.
- algorithmic_behavior: `tests/test-template-loader.sh` is the focused unit/integration suite for `hooks/lib/template-loader.sh`. It checks `get_template_dir` path derivation, `load_template` success and missing-file behavior, `render_template` replacement semantics, `load_and_render` integration with real templates, `load_and_render_safe` fallback behavior, and `validate_template_dir` acceptance/rejection gates (`tests/test-template-loader.sh:41`, `tests/test-template-loader.sh:55`, `tests/test-template-loader.sh:80`, `tests/test-template-loader.sh:147`, `tests/test-template-loader.sh:197`, `tests/test-template-loader.sh:225`). Its core algorithmic coverage is single-pass template rendering: values containing shell metacharacters are expected to render literally (`tests/test-template-loader.sh:247`) and injected `{{VAR}}` patterns inside values must not be recursively substituted (`tests/test-template-loader.sh:488`). This directly validates the implementation in `hooks/lib/template-loader.sh`, where `render_template` builds `TMPL_VAR_` environment bindings and uses an awk scanner to replace placeholders once (`hooks/lib/template-loader.sh:43`, `hooks/lib/template-loader.sh:59`, `hooks/lib/template-loader.sh:73`, `hooks/lib/template-loader.sh:101`).
- algorithmic_behavior: `tests/test-template-references.sh` is a static cross-reference gate. It scans selected hook scripts for `load_template`, `load_and_render`, and `load_and_render_safe` calls (`tests/test-template-references.sh:56`, `tests/test-template-references.sh:83`) and fails if referenced template files are missing (`tests/test-template-references.sh:102`). It also validates a hard-coded common-template inventory, including `block/goal-tracker-modification.md` (`tests/test-template-references.sh:152`), and enforces that critical validators use `load_and_render_safe` rather than unsafe direct rendering (`tests/test-template-references.sh:172`, `tests/test-template-references.sh:185`). This coordinates the `tests` directory with sibling paths `hooks/` and `prompt-template/` by preventing hook blockers from emitting empty feedback when a template is absent.
- algorithmic_behavior: `tests/test-templates-comprehensive.sh` is the broad CI-style suite. It validates the template directory structure (`block`, `codex`, `claude`) (`tests/test-templates-comprehensive.sh:61`, `tests/test-templates-comprehensive.sh:74`), loads every Markdown template found under `prompt-template/` (`tests/test-templates-comprehensive.sh:82`, `tests/test-templates-comprehensive.sh:90`), checks placeholder syntax against uppercase `{{VAR_NAME}}` conventions (`tests/test-templates-comprehensive.sh:114`, `tests/test-templates-comprehensive.sh:122`), runs malformed-placeholder detector self-tests (`tests/test-templates-comprehensive.sh:197`), covers render edge cases such as empty content, empty lines, long values, adjacent variables, and multiline values (`tests/test-templates-comprehensive.sh:262`), validates fallback behavior (`tests/test-templates-comprehensive.sh:440`), renders real templates (`tests/test-templates-comprehensive.sh:492`), and stress-tests repeated calls plus all-template renderability (`tests/test-templates-comprehensive.sh:542`, `tests/test-templates-comprehensive.sh:562`).
- algorithmic_behavior: `tests/test-error-scenarios.sh` documents and exercises failure behavior rather than using strict pass/fail counters. It probes missing template file, missing template directory, missing `load_and_render` target, empty render content, special regex characters in values, and behavior under `set -euo pipefail` (`tests/test-error-scenarios.sh:21`, `tests/test-error-scenarios.sh:34`, `tests/test-error-scenarios.sh:47`, `tests/test-error-scenarios.sh:60`, `tests/test-error-scenarios.sh:73`, `tests/test-error-scenarios.sh:86`). Its summary explicitly records the risk that empty reasons lead to empty agent feedback (`tests/test-error-scenarios.sh:114`, `tests/test-error-scenarios.sh:120`), which is the failure mode the safer fallback tests address.
- inputs_outputs_state: Inputs are local shell scripts, Markdown templates, synthetic template strings, and variable assignments in `VAR=value` form. The tests source `hooks/lib/template-loader.sh` (`tests/test-template-loader.sh:12`, `tests/test-templates-comprehensive.sh:17`) and derive `PROJECT_ROOT` from the test script location (`tests/test-template-loader.sh:10`, `tests/test-template-references.sh:14`). Outputs are console pass/fail/warn summaries and process exit codes. `test-template-loader.sh`, `test-template-references.sh`, and `test-templates-comprehensive.sh` maintain in-process counters (`TESTS_PASSED`, `TESTS_FAILED`, `WARNINGS`) and exit nonzero when failures exist (`tests/test-template-loader.sh:20`, `tests/test-template-loader.sh:555`; `tests/test-template-references.sh:25`, `tests/test-template-references.sh:221`; `tests/test-templates-comprehensive.sh:26`, `tests/test-templates-comprehensive.sh:619`). `test-error-scenarios.sh` captures `EXIT_CODE` values for display but does not enforce assertions with counters (`tests/test-error-scenarios.sh:25`, `tests/test-error-scenarios.sh:27`).
- gates_or_invariants: Core invariants include: template directory resolves to `prompt-template` from `hooks/lib` (`tests/test-template-loader.sh:44`); missing `load_template` targets return empty content (`tests/test-template-loader.sh:68`); `load_and_render_safe` must use fallback content for missing/empty templates (`tests/test-template-loader.sh:197`, `hooks/lib/template-loader.sh:155`); unreplaced placeholders remain as literal `{{ID}}` (`tests/test-template-loader.sh:181`); injected placeholders inside values are not recursively rendered (`tests/test-template-loader.sh:488`); template placeholders should be uppercase and unspaced (`tests/test-templates-comprehensive.sh:122`, `tests/test-templates-comprehensive.sh:165`, `tests/test-templates-comprehensive.sh:171`); critical validators must use safe rendering (`tests/test-template-references.sh:174`); common blocker templates must exist (`tests/test-template-references.sh:152`).
- dependencies_and_callers: The tests depend on standard Bash tools (`find`, `grep`, `sed`, `awk` through the loader, `sort`, `wc`, `mktemp`, `mkdir`, `rm`) and repository scripts. Main code under test is `hooks/lib/template-loader.sh`, whose key functions are `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` (`hooks/lib/template-loader.sh:11`, `hooks/lib/template-loader.sh:21`, `hooks/lib/template-loader.sh:43`, `hooks/lib/template-loader.sh:121`, `hooks/lib/template-loader.sh:155`, `hooks/lib/template-loader.sh:193`). Static reference validation also checks hook callers including `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/lib/loop-common.sh` (`tests/test-template-references.sh:57`).
- edge_cases_or_failure_modes: The directory specifically targets shell metacharacter corruption, awk replacement bugs with ampersands, backslash and Windows path handling, dollar signs, backticks, pipes, semicolons, globs, redirection-like strings, quotes, hashes, tildes, JSON-like values, regex-like values, multiline values, and long values (`tests/test-template-loader.sh:247`, `tests/test-template-loader.sh:425`, `tests/test-template-loader.sh:437`, `tests/test-template-loader.sh:461`, `tests/test-template-loader.sh:473`; `tests/test-templates-comprehensive.sh:318`, `tests/test-templates-comprehensive.sh:382`). It also targets placeholder injection and malformed placeholder syntax (`tests/test-template-loader.sh:488`, `tests/test-templates-comprehensive.sh:197`). Failure modes include missing templates causing empty messages (`tests/test-error-scenarios.sh:114`) and incomplete template directories being rejected by `validate_template_dir` (`tests/test-templates-comprehensive.sh:481`).
- validation_or_tests: This item is itself the validation directory. The runnable entry points are `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, and `tests/test-error-scenarios.sh`. I inspected all four files recursively under `tests`. I did not execute the suites because the task requested research notes only and the branch export is read-only; static inspection shows the scripts are intended to exit `0` only when their modeled checks pass, except `test-error-scenarios.sh`, which is primarily observational.
- skip_candidate: `no`

### TEMPLIFY_PROMPT-HZ-036 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: This file is a blocker-message template for the RLCR loop’s goal-tracker authority gate. It defines the agent-facing response emitted when a non-Codex actor attempts to modify `goal-tracker.md` after Round 0. Its role is algorithmic even though it is Markdown because hook validators render it as part of a state-dependent access-control transition.
- algorithmic_behavior: The template begins with a round-specific heading using `{{CURRENT_ROUND}}` (`prompt-template/block/goal-tracker-modification.md:1`), states that after Round 0 only Codex can modify the Goal Tracker (`prompt-template/block/goal-tracker-modification.md:3`), forbids direct `goal-tracker.md` changes through Write, Edit, or Bash (`prompt-template/block/goal-tracker-modification.md:5`), and redirects the actor to include a structured `Goal Tracker Update Request` in the current summary file (`prompt-template/block/goal-tracker-modification.md:7`, `prompt-template/block/goal-tracker-modification.md:9`). The requested format contains `Requested Changes` and `Justification` sections (`prompt-template/block/goal-tracker-modification.md:14`, `prompt-template/block/goal-tracker-modification.md:16`, `prompt-template/block/goal-tracker-modification.md:21`). The terminal rule is that Codex reviews the request and applies justified goal-tracker changes (`prompt-template/block/goal-tracker-modification.md:25`).
- inputs_outputs_state: Inputs are `CURRENT_ROUND` and `SUMMARY_FILE`, passed by `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:180`, `hooks/lib/loop-common.sh:182`, `hooks/lib/loop-common.sh:189`). Output is rendered Markdown written to stderr by validators before they exit with blocking status. State transition: an attempted direct mutation of `goal-tracker.md` when `CURRENT_ROUND > 0` is transformed from a file write/edit/bash operation into a summary-file update request. The template does not mutate state itself; it defines the required indirect state-change protocol.
- gates_or_invariants: The invariant is “Round 0 may initialize or update the goal tracker via controlled paths, but after Round 0 only Codex may directly modify it.” The Write validator enforces this for `Write` when `is_goal_tracker_path` is true and `CURRENT_ROUND > 0` (`hooks/loop-write-validator.sh:90`, `hooks/loop-write-validator.sh:94`). The Edit validator enforces the same for `Edit` (`hooks/loop-edit-validator.sh:77`, `hooks/loop-edit-validator.sh:81`). The Bash validator checks shell commands for goal-tracker modification patterns and uses this template for rounds greater than 0 (`hooks/loop-bash-validator.sh:78`, `hooks/loop-bash-validator.sh:83`, `hooks/loop-bash-validator.sh:87`). Bash Round 0 uses the separate `goal-tracker-bash-write.md` template to direct the actor to Write/Edit instead (`hooks/loop-bash-validator.sh:84`, `hooks/lib/loop-common.sh:126`).
- dependencies_and_callers: The direct rendering function is `goal_tracker_blocked_message` in `hooks/lib/loop-common.sh`, which calls `load_and_render_safe "$TEMPLATE_DIR" "block/goal-tracker-modification.md"` with a short fallback (`hooks/lib/loop-common.sh:185`, `hooks/lib/loop-common.sh:189`). That helper depends on `hooks/lib/template-loader.sh` for safe loading and placeholder substitution (`hooks/lib/template-loader.sh:155`). Callers are `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh` when their parsed tool inputs target `goal-tracker.md` in an active loop. Existence of this template is included in the common-template inventory test (`tests/test-template-references.sh:152`, `tests/test-template-references.sh:158`) and real all-template loading/rendering is covered by the comprehensive suite (`tests/test-templates-comprehensive.sh:90`, `tests/test-templates-comprehensive.sh:562`).
- edge_cases_or_failure_modes: Missing or empty template content would normally risk blank blocker feedback; this caller uses `load_and_render_safe`, so the fallback message still includes the current round and summary-file pointer (`hooks/lib/loop-common.sh:185`, `hooks/lib/loop-common.sh:189`; `hooks/lib/template-loader.sh:164`). Incorrect `CURRENT_ROUND` extraction would route the wrong policy: `get_current_round` defaults to `0` if state parsing fails (`hooks/lib/loop-common.sh:46`, `hooks/lib/loop-common.sh:57`), which could prevent this post-Round-0 template from being used. Path matching is suffix-based via `is_goal_tracker_path`, so any path ending in `goal-tracker.md` is treated as a target (`hooks/lib/loop-common.sh:137`). Bash mutation detection is pattern-based and may miss command forms outside its enumerated redirection, tee, in-place edit, copy/move, or `dd of=` patterns (`hooks/lib/loop-common.sh:155`, `hooks/lib/loop-common.sh:162`).
- validation_or_tests: `tests/test-template-references.sh` explicitly checks that `block/goal-tracker-modification.md` exists as a common template (`tests/test-template-references.sh:152`, `tests/test-template-references.sh:158`). The same test enforces safe rendering for critical validators, reducing the missing-template blank-message risk (`tests/test-template-references.sh:172`). `tests/test-templates-comprehensive.sh` loads every template, validates placeholder syntax, and attempts rendering all templates with dummy placeholder values (`tests/test-templates-comprehensive.sh:90`, `tests/test-templates-comprehensive.sh:118`, `tests/test-templates-comprehensive.sh:562`). The template uses uppercase placeholders `{{CURRENT_ROUND}}` and `{{SUMMARY_FILE}}`, matching the syntax gate (`prompt-template/block/goal-tracker-modification.md:1`, `prompt-template/block/goal-tracker-modification.md:10`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 unique item evidence headings present above
- missing_items: 0
- duplicate_items: 0
- final_worker_status: `complete`