# agent_02 templify-prompt 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `0c107abc65600bca4e18b04265b48c91da772491`

## Item Evidence

### TEMPLIFY_PROMPT-HZ-002 `directory` `commands`
- cursor: `[_]`
- core_role: `commands/` is the Claude slash-command entry surface for the RLCR workflow. It contains exactly two command markdown files, `commands/start-rlcr-loop.md` and `commands/cancel-rlcr-loop.md`, matching the README’s command table and plugin layout at `README.md:180` and `README.md:220`. The directory itself does not contain the loop engine; it dispatches into `scripts/setup-rlcr-loop.sh` for setup and relies on hooks under `hooks/` for runtime loop enforcement.
- algorithmic_behavior: `commands/start-rlcr-loop.md` defines a hidden slash command with argument shape `<path/to/plan.md> [--max N] [--codex-model MODEL:EFFORT] [--codex-timeout SECONDS] [--push-every-round]` at `commands/start-rlcr-loop.md:3`, permits only the setup script tool at `commands/start-rlcr-loop.md:4`, and executes `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh" $ARGUMENTS` at `commands/start-rlcr-loop.md:12`. Its documented transition is: user implements a plan, writes a round summary, Stop hook invokes Codex review, issues block continuation, and a terminal `COMPLETE` review ends the loop at `commands/start-rlcr-loop.md:16`. It also defines the Goal Tracker contract: immutable goal and acceptance criteria, mutable tasks/completed/deferred/log sections, and periodic alignment audits at `commands/start-rlcr-loop.md:24`. `commands/cancel-rlcr-loop.md` defines the manual cancellation path: list `.humanize-loop.local/*/state.md`, return `NO_LOOP` if absent, otherwise read state, remove state files, and report the cancelled round at `commands/cancel-rlcr-loop.md:11`.
- inputs_outputs_state: Start inputs are the plan file path plus optional max iteration, Codex model/effort, timeout, and push policy. The delegated setup script validates and normalizes those inputs: one plan file only, plan existence, at least 5 lines, numeric options, and `codex` availability at `scripts/setup-rlcr-loop.sh:93`, `scripts/setup-rlcr-loop.sh:163`, `scripts/setup-rlcr-loop.sh:179`, `scripts/setup-rlcr-loop.sh:185`, and `scripts/setup-rlcr-loop.sh:195`. Setup outputs include `.humanize-loop.local/<timestamp>/state.md` with `current_round`, `max_iterations`, Codex settings, push policy, plan file, and start time at `scripts/setup-rlcr-loop.sh:207` and `scripts/setup-rlcr-loop.sh:223`; `goal-tracker.md` with immutable and mutable sections at `scripts/setup-rlcr-loop.sh:240`; and `round-0-prompt.md` plus `round-0-summary.md` path instructions at `scripts/setup-rlcr-loop.sh:334`. Runtime state advances in the Stop hook: it finds the active loop at `hooks/loop-codex-stop-hook.sh:52`, parses frontmatter at `hooks/loop-codex-stop-hook.sh:285`, requires `round-N-summary.md` at `hooks/loop-codex-stop-hook.sh:319`, runs Codex review, removes the state file on `COMPLETE` or `STOP`, or increments `current_round` and writes `round-(N+1)-prompt.md` at `hooks/loop-codex-stop-hook.sh:718` and `hooks/loop-codex-stop-hook.sh:763`. Cancel input is current `.humanize-loop.local/*/state.md`; its output is deletion of the state file while preserving the loop directory and artifacts, per `commands/cancel-rlcr-loop.md:21`.
- gates_or_invariants: Start enforces exactly one plan file, valid numeric options, a sufficiently detailed plan, and installed Codex before creating state (`scripts/setup-rlcr-loop.sh:99`, `scripts/setup-rlcr-loop.sh:150`, `scripts/setup-rlcr-loop.sh:185`, `scripts/setup-rlcr-loop.sh:195`). The Goal Tracker invariant is that Round 0 initializes ultimate goal, acceptance criteria, and active tasks, while later rounds should preserve the immutable section (`commands/start-rlcr-loop.md:28`, `scripts/setup-rlcr-loop.sh:339`). Stop-hook gates include no incomplete todos before review (`hooks/loop-codex-stop-hook.sh:67`), no large changed files above 2000 lines (`hooks/loop-codex-stop-hook.sh:104`), clean git before exit (`hooks/loop-codex-stop-hook.sh:185`), pushed commits when `--push-every-round` is enabled (`hooks/loop-codex-stop-hook.sh:245`), summary-file presence (`hooks/loop-codex-stop-hook.sh:321`), Round 0 goal-tracker placeholder removal (`hooks/loop-codex-stop-hook.sh:348`), and max-iteration termination (`hooks/loop-codex-stop-hook.sh:410`). Cancel’s invariant is intentionally narrower: it only removes loop state, preserving summaries and review results (`commands/cancel-rlcr-loop.md:24`).
- dependencies_and_callers: `commands/start-rlcr-loop.md` depends directly on `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh` through its allowed tool and command body (`commands/start-rlcr-loop.md:4`, `commands/start-rlcr-loop.md:13`). The loop then depends on hook registration in `hooks/hooks.json`, especially the Stop hook invoking `${CLAUDE_PLUGIN_ROOT}/hooks/loop-codex-stop-hook.sh` with a 7200-second timeout at `hooks/hooks.json:42`. Setup and Stop both depend on the `.humanize-loop.local/<timestamp>/` state convention documented in `README.md:241`. The Stop hook depends on `hooks/lib/loop-common.sh`, the template-loader library, `hooks/check-todos-from-transcript.py`, prompt templates under `prompt-template/`, `jq`, `git`, and `codex` (`hooks/loop-codex-stop-hook.sh:46`, `hooks/loop-codex-stop-hook.sh:67`, `hooks/loop-codex-stop-hook.sh:231`, `hooks/loop-codex-stop-hook.sh:512`). README references these commands as user-facing invocation points at `README.md:184`.
- edge_cases_or_failure_modes: Start fails on no plan, multiple plan files, unknown options, nonnumeric `--max` or `--codex-timeout`, missing plan file, plan shorter than five lines, or missing Codex CLI (`scripts/setup-rlcr-loop.sh:98`, `scripts/setup-rlcr-loop.sh:141`, `scripts/setup-rlcr-loop.sh:150`, `scripts/setup-rlcr-loop.sh:163`, `scripts/setup-rlcr-loop.sh:179`, `scripts/setup-rlcr-loop.sh:185`, `scripts/setup-rlcr-loop.sh:195`). Goal extraction is heuristic and may leave placeholders if the plan lacks expected headings, which then triggers Round 0 Stop-hook blocking until filled (`scripts/setup-rlcr-loop.sh:263`, `hooks/loop-codex-stop-hook.sh:348`). Cancel can remove all matching state files if multiple loop directories contain `state.md`, because the command uses `.humanize-loop.local/*/state.md` at `commands/cancel-rlcr-loop.md:21`; it reports based on read state but does not itself arbitrate multiple active loops. Runtime failure modes include Codex nonzero exit, no review file, empty review file, missing template fallback paths, corrupted numeric `current_round`, untracked files, uncommitted work, unpushed commits when push mode is active, and max-iteration stop (`hooks/loop-codex-stop-hook.sh:303`, `hooks/loop-codex-stop-hook.sh:635`, `hooks/loop-codex-stop-hook.sh:672`, `hooks/loop-codex-stop-hook.sh:700`).
- validation_or_tests: The command files are not directly unit-tested as command markdown, but their delegated template/hook surfaces are covered by tests. `tests/test-template-references.sh` scans hook scripts for referenced templates and fails on missing files (`tests/test-template-references.sh:52`, `tests/test-template-references.sh:93`, `tests/test-template-references.sh:213`). `tests/test-templates-comprehensive.sh` loads all prompt templates, validates placeholder syntax, and renders all templates with sample args (`tests/test-templates-comprehensive.sh:83`, `tests/test-templates-comprehensive.sh:114`, `tests/test-templates-comprehensive.sh:563`). `tests/test-template-loader.sh` verifies loader path resolution, existing/missing template loading, rendering, fallback behavior, and special-character literal handling (`tests/test-template-loader.sh:42`, `tests/test-template-loader.sh:55`, `tests/test-template-loader.sh:197`). Validation gap: there is no direct test asserting that `/humanize:start-rlcr-loop` and `/humanize:cancel-rlcr-loop` command metadata invokes only the intended Bash tools or that cancel handles multiple state files deterministically.
- skip_candidate: `no`

### TEMPLIFY_PROMPT-HZ-032 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: This file is a reusable block template inserted into the Stop-hook “Git Not Clean” decision when the worktree has untracked files other than `.humanize-loop.local`. It is not executable by itself, but it shapes the algorithm’s blocking feedback for a specific git-cleanliness edge case.
- algorithmic_behavior: The template text begins with a “Note on Untracked Files” at `prompt-template/block/git-not-clean-untracked.md:2`, classifies likely untracked artifacts as build outputs, dependencies, IDE/editor files, logs, caches, and temporary files at `prompt-template/block/git-not-clean-untracked.md:3`, and ends by instructing review plus `.gitignore` patterns at `prompt-template/block/git-not-clean-untracked.md:10`. The runtime algorithm lives in `hooks/loop-codex-stop-hook.sh`: after `git status --porcelain`, it extracts untracked rows with `grep '^??'`, removes `.humanize-loop.local` from that set, and loads this template only when other untracked rows remain (`hooks/loop-codex-stop-hook.sh:190`, `hooks/loop-codex-stop-hook.sh:196`, `hooks/loop-codex-stop-hook.sh:207`). The resulting note is appended into `SPECIAL_NOTES` and rendered into `block/git-not-clean.md` at `hooks/loop-codex-stop-hook.sh:214` and `hooks/loop-codex-stop-hook.sh:227`.
- inputs_outputs_state: Input to the template selection path is `GIT_STATUS=$(git status --porcelain)`, specifically lines beginning with `??` after excluding `.humanize-loop.local` (`hooks/loop-codex-stop-hook.sh:191`, `hooks/loop-codex-stop-hook.sh:208`). This template has no placeholders, so its rendered output is static markdown. Its output becomes part of the larger Stop-hook JSON block reason, which is emitted with `decision: block`, a rendered `reason`, and system message “Loop: Blocked - uncommitted changes detected, please commit first” at `hooks/loop-codex-stop-hook.sh:231`. State is not directly mutated by this template; the Stop hook exits early with a block decision and leaves the active loop state file in place.
- gates_or_invariants: The gate is active only inside a git repository and only when `git status --porcelain` is nonempty (`hooks/loop-codex-stop-hook.sh:185`, `hooks/loop-codex-stop-hook.sh:192`). The invariant is that Claude cannot exit the RLCR loop while the worktree is dirty; untracked artifact-like files should be either ignored or intentionally staged/committed through the parent `git-not-clean.md` instructions. `.humanize-loop.local` receives a separate special-case template and is intentionally not treated as a normal untracked artifact (`hooks/loop-codex-stop-hook.sh:198`, `prompt-template/block/git-not-clean-humanize-local.md:2`). If this template is missing, the hook falls back to the hardcoded note “Review untracked files - add to .gitignore or commit them.” (`hooks/loop-codex-stop-hook.sh:210`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh` through `load_template "$TEMPLATE_DIR" "block/git-not-clean-untracked.md"` at `hooks/loop-codex-stop-hook.sh:210`. Template directory resolution and loading are provided by `hooks/lib/template-loader.sh`: `get_template_dir` resolves plugin-root-relative `prompt-template`, `load_template` returns file contents or empty with warning, and rendering is single-pass for placeholder safety (`hooks/lib/template-loader.sh:11`, `hooks/lib/template-loader.sh:21`, `hooks/lib/template-loader.sh:43`). The surrounding message composition depends on `prompt-template/block/git-not-clean.md`, which injects `{{SPECIAL_NOTES}}` at `prompt-template/block/git-not-clean.md:4`.
- edge_cases_or_failure_modes: The template is static and cannot list actual untracked filenames; it relies on the parent block to say only that uncommitted changes exist. Users may need to inspect `git status` separately to distinguish real source files from generated artifacts. Pattern matching uses a simple grep exclusion for `.humanize-loop.local`; paths containing that string elsewhere could be excluded from `OTHER_UNTRACKED` unintentionally (`hooks/loop-codex-stop-hook.sh:208`). If both `.humanize-loop.local` and other untracked files exist, both special notes are concatenated without an explicit separator beyond each template’s own leading/trailing newlines (`hooks/loop-codex-stop-hook.sh:204`, `hooks/loop-codex-stop-hook.sh:214`). Because the template has no placeholders, malformed placeholder risk is low, but loss or empty content degrades to the fallback note.
- validation_or_tests: `tests/test-template-references.sh` directly checks that templates referenced by hooks exist, including this file through the Stop-hook reference scanning logic (`tests/test-template-references.sh:56`, `tests/test-template-references.sh:93`). `tests/test-templates-comprehensive.sh` loads every `prompt-template/**/*.md`, validates placeholder syntax, and attempts rendering all templates, which covers this static block as a no-placeholder template (`tests/test-templates-comprehensive.sh:90`, `tests/test-templates-comprehensive.sh:118`, `tests/test-templates-comprehensive.sh:563`). `tests/test-template-loader.sh` validates template loading, missing-template fallback semantics, and literal rendering behavior used by this path (`tests/test-template-loader.sh:55`, `tests/test-template-loader.sh:197`). Validation gap: I did not find a focused behavioral test that simulates `git status --porcelain` with non-`.humanize-loop.local` untracked files and asserts this exact note is included in the Stop-hook JSON.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2` section headings only
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`