# agent_27 templify-prompt 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `0c107abc65600bca4e18b04265b48c91da772491`

## Item Evidence

### TEMPLIFY_PROMPT-HZ-027 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Executable specification for the template loading/rendering subsystem. It validates that every Markdown template under `prompt-template/` is discoverable, syntactically valid under the project’s `{{VAR_NAME}}` placeholder contract, renderable through `hooks/lib/template-loader.sh`, and supported by fallback/error handling behavior. The script sources the loader at `tests/test-templates-comprehensive.sh:17` and derives the canonical template root through `get_template_dir` at `tests/test-templates-comprehensive.sh:59`.

- algorithmic_behavior: The script is a sectioned test runner with accumulated pass/fail/warn counters, not a fail-fast shell test. It uses `set -uo pipefail` at `tests/test-templates-comprehensive.sh:13`, then increments `TESTS_PASSED`, `TESTS_FAILED`, and `WARNINGS` via helper functions at `tests/test-templates-comprehensive.sh:30-57`. Its algorithm proceeds through seven gates: directory shape validation, load-all validation, placeholder syntax scanning, malformed-placeholder detector self-tests, render edge cases, fallback behavior, real-template integration checks, and stress rendering. Final success is determined only by `TESTS_FAILED == 0` at `tests/test-templates-comprehensive.sh:619-624`; warnings are reported but do not fail the run.

- inputs_outputs_state: Inputs are the repository-relative template directory, all `*.md` files found with `find "$TEMPLATE_DIR" -name "*.md" -type f -print0 | sort -z` at `tests/test-templates-comprehensive.sh:107`, loader functions from `hooks/lib/template-loader.sh`, and shell tools including `grep`, `sed`, `awk` through the loader, `mktemp`, `rm`, and `wc`. Outputs are colored terminal diagnostics, pass/fail/warn counts, and process exit status. Internal state transitions are counter increments on each assertion, plus temporary directories created for malformed-placeholder and incomplete-directory checks at `tests/test-templates-comprehensive.sh:205-206` and `tests/test-templates-comprehensive.sh:484`. Temporary state is removed manually at `tests/test-templates-comprehensive.sh:258-259` and `tests/test-templates-comprehensive.sh:490`.

- gates_or_invariants: Template directory invariants require `prompt-template/` plus `block/`, `codex/`, and `claude/` subdirectories at `tests/test-templates-comprehensive.sh:66-80`. Load invariants require every Markdown template to return non-empty content through `load_template`; whitespace-only content is a warning path at `tests/test-templates-comprehensive.sh:96-105`. Syntax invariants accept uppercase placeholders matching `{{VAR}}`, `{{VAR_NAME}}`, or `{{VAR_NAME_123}}`, and reject extra closing braces, extra opening braces, wrong bracket families, suspected unclosed placeholders, spaces inside placeholders, and lowercase placeholder names at `tests/test-templates-comprehensive.sh:122-175`. Single-brace placeholders are intentionally skipped as too noisy at `tests/test-templates-comprehensive.sh:160-164`.

- dependencies_and_callers: The direct dependency is `hooks/lib/template-loader.sh`. That loader defines `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` at `hooks/lib/template-loader.sh:11-207`. The tested rendering algorithm is single-pass, environment-backed `awk`; the loader explicitly states that variable values containing `{{OTHER_VAR}}` are not rescanned at `hooks/lib/template-loader.sh:35-42`, and implements character-by-character placeholder replacement at `hooks/lib/template-loader.sh:56-116`. This test also exercises real templates that are used by hooks, including `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` at `tests/test-templates-comprehensive.sh:497-540`.

- edge_cases_or_failure_modes: Render edge cases include empty input, single and multi-line templates, empty line preservation, regex-like characters in values, backslashes, quotes, markdown formatting, long values, multiline values, variables at line start/end, repeated variables, and adjacent variables at `tests/test-templates-comprehensive.sh:266-439`. Several assertions are deliberately “no crash” rather than exact equality, notably backslashes, quotes, CJK-labelled content, markdown, and multiline values at `tests/test-templates-comprehensive.sh:329-402`. The script claims CJK and emoji coverage in its header at `tests/test-templates-comprehensive.sh:8`, but the concrete CJK/emoji checks use ASCII-safe substitute strings at `tests/test-templates-comprehensive.sh:348-369`, so true Unicode rendering is not strongly validated here. Because cleanup is manual rather than trap-based, interruption between `mktemp` and `rm -rf` can leave temporary directories outside the repo. The final all-template render loop warns on unreplaced placeholders but does not convert those warnings into failures at `tests/test-templates-comprehensive.sh:590-597`.

- validation_or_tests: This file is itself the validation surface. It validates malformed placeholder detection with known-bad samples at `tests/test-templates-comprehensive.sh:204-256`, validates fallback behavior for missing and existing templates at `tests/test-templates-comprehensive.sh:445-463`, validates `validate_template_dir` against valid, missing, and incomplete directories at `tests/test-templates-comprehensive.sh:465-490`, performs real-template rendering checks at `tests/test-templates-comprehensive.sh:497-540`, and stress-tests 100 successive render calls plus all-template dummy rendering at `tests/test-templates-comprehensive.sh:547-606`.

- skip_candidate: `no`

### TEMPLIFY_PROMPT-HZ-057 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Codex review prompt contract for controlled goal-tracker mutation. It is a Markdown template, not executable code, but it defines the runtime decision procedure Codex must follow when Claude requests updates to `goal-tracker.md` after Claude is no longer allowed to edit that file directly. The rendered section is inserted into both regular and full-alignment Codex review prompts.

- algorithmic_behavior: The template defines a three-step transition algorithm. First, Codex evaluates whether a Claude “Goal Tracker Update Request” is justified and serves the Ultimate Goal at `prompt-template/codex/goal-tracker-update-section.md:3-5`. Second, if approved, Codex updates `@{{GOAL_TRACKER_FILE}}` by moving tasks among Active, Completed, and Deferred sections, adding Plan Evolution Log entries with round number and justification, and adding Open Issues as needed at `prompt-template/codex/goal-tracker-update-section.md:6-9`. Third, if rejected, Codex records why in its review at `prompt-template/codex/goal-tracker-update-section.md:11`. It also enumerates supported request classes: task completion, new issues, plan changes, and deferrals at `prompt-template/codex/goal-tracker-update-section.md:13-17`.

- inputs_outputs_state: Its only template input is `GOAL_TRACKER_FILE`, referenced as `@{{GOAL_TRACKER_FILE}}` at `prompt-template/codex/goal-tracker-update-section.md:6`. At runtime, `hooks/loop-codex-stop-hook.sh` renders it with the active loop’s goal-tracker path at `hooks/loop-codex-stop-hook.sh:434-438`, then passes the rendered text into `GOAL_TRACKER_UPDATE_SECTION` for the parent review template at `hooks/loop-codex-stop-hook.sh:477-505`. Outputs are not direct program output; the intended outputs are Codex-authored review text and, when approved, modifications to the mutable parts of `goal-tracker.md`. State transitions include Active to Completed, Active to Deferred, new Open Issues rows, and Plan Evolution Log entries. The immutable Ultimate Goal and Acceptance Criteria must remain unchanged.

- gates_or_invariants: The central invariant is explicit: “NEVER modify the IMMUTABLE SECTION” at `prompt-template/codex/goal-tracker-update-section.md:10`. Approval is gated on justification and alignment with the Ultimate Goal at `prompt-template/codex/goal-tracker-update-section.md:5`. Deferrals have an elevated threshold: they are allowed only with strong justification at `prompt-template/codex/goal-tracker-update-section.md:17`. The surrounding hook system enforces the ownership boundary: Write blocks goal-tracker changes after round 0 at `hooks/loop-write-validator.sh:91-98`, Edit blocks the same at `hooks/loop-edit-validator.sh:77-84`, and Bash blocks shell-based goal-tracker mutation attempts at `hooks/loop-bash-validator.sh:78-91`. The shared blocked-message helper tells Claude to put the request in its summary instead at `hooks/lib/loop-common.sh:180-191`.

- dependencies_and_callers: The template depends on the template loader’s `load_and_render_safe` path, used by the stop hook at `hooks/loop-codex-stop-hook.sh:437`. It is embedded into `prompt-template/codex/full-alignment-review.md` through `{{GOAL_TRACKER_UPDATE_SECTION}}` at `prompt-template/codex/full-alignment-review.md:57`, and into `prompt-template/codex/regular-review.md` at `prompt-template/codex/regular-review.md:47`. It coordinates with Claude-facing request guidance in `prompt-template/claude/goal-tracker-update-request.md`, which tells Claude to include requested changes and justification in a summary section at `prompt-template/claude/goal-tracker-update-request.md:2-16`.

- edge_cases_or_failure_modes: The contract is prompt-enforced, not mechanically verified. There is no parser here that confirms Claude’s request format, no automatic check that Codex actually preserves the immutable section, and no automated validation that a Plan Evolution Log entry includes a round number. If the template is missing, the stop hook falls back to a shorter generic instruction at `hooks/loop-codex-stop-hook.sh:435-438`, which preserves the broad behavior but loses the detailed transition rules. Parent templates prefix the insertion with `## Part 3:` while this template itself starts with a `##` heading, so rendered Markdown can have awkward heading composition even though the behavioral instructions remain present. If `GOAL_TRACKER_FILE` is not supplied, the placeholder can remain unresolved; the comprehensive template test is intended to catch normal rendering of this path.

- validation_or_tests: The comprehensive test has a dedicated real-template check for this file at `tests/test-templates-comprehensive.sh:530-540`. It renders `codex/goal-tracker-update-section.md` with `GOAL_TRACKER_FILE=.humanize-loop.local/20240101/goal-tracker.md` and asserts that the rendered output contains both the “Goal Tracker Update Requests” heading and the concrete goal-tracker path. The same test suite also includes global placeholder syntax validation for all templates at `tests/test-templates-comprehensive.sh:118-194`, so this file’s uppercase `{{GOAL_TRACKER_FILE}}` placeholder participates in repository-wide syntax gating.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 item sections present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`