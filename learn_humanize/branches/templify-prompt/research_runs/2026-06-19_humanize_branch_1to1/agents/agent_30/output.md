# agent_30 templify-prompt 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `0c107abc65600bca4e18b04265b48c91da772491`

## Item Evidence

### TEMPLIFY_PROMPT-HZ-030 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: This file is a Markdown block template for reporting a failed Codex review attempt in the RLCR loop. It is structured as a user-facing gate message: title, failure fact, exit code, expected review-result file, debug file paths, stderr excerpt, and retry instruction. The relevant local structure is `prompt-template/block/codex-review-failed.md:1-18`.
- algorithmic_behavior: The template does not execute logic itself; it defines the intended failure-output contract for a Codex review process that fails to produce usable review output. Its placeholder contract expects the renderer to substitute `CODEX_EXIT_CODE`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, `CODEX_STDOUT_FILE`, `CODEX_STDERR_FILE`, and `STDERR_CONTENT` at `prompt-template/block/codex-review-failed.md:5-15`. The message encodes a state transition from “Codex review attempted” to “exit blocked pending retry,” ending with “The system will attempt another review when you exit” at `prompt-template/block/codex-review-failed.md:18`.
- inputs_outputs_state: Inputs are template variable bindings from the Codex review runner: the Codex process exit code, intended review result path, command/stdout/stderr debug paths, and a bounded stderr tail. The output is a rendered Markdown block suitable for inclusion in a hook-block response. In the surrounding runtime, analogous state is held in `hooks/loop-codex-stop-hook.sh`: `REVIEW_RESULT_FILE` is set at `hooks/loop-codex-stop-hook.sh:428-430`, debug paths are set at `hooks/loop-codex-stop-hook.sh:546-548`, `CODEX_EXIT_CODE` is initialized and populated at `hooks/loop-codex-stop-hook.sh:595-597`, and stderr tails are collected on failure at `hooks/loop-codex-stop-hook.sh:637-651` and `hooks/loop-codex-stop-hook.sh:674-697`.
- gates_or_invariants: The implied gate is “Codex review must produce a non-empty review result file.” The runtime hook enforces related invariants directly: Codex CLI must exist before review runs at `hooks/loop-codex-stop-hook.sh:512-532`; non-zero Codex exit blocks at `hooks/loop-codex-stop-hook.sh:635-652`; missing review file triggers stdout fallback-copy first at `hooks/loop-codex-stop-hook.sh:654-670`; still-missing result blocks at `hooks/loop-codex-stop-hook.sh:672-698`; empty result blocks at `hooks/loop-codex-stop-hook.sh:700-707`. The generic renderer preserves unresolved placeholders when no binding is supplied, per `hooks/lib/template-loader.sh:100-107`, so this template depends on complete variable binding for a clean message.
- dependencies_and_callers: The repository’s template system is `hooks/lib/template-loader.sh`, which locates `prompt-template` via `get_template_dir` at `hooks/lib/template-loader.sh:11-16`, loads files at `hooks/lib/template-loader.sh:21-33`, and renders `{{VARIABLE_NAME}}` placeholders in a single pass at `hooks/lib/template-loader.sh:35-117`. `hooks/lib/loop-common.sh` sources the loader and validates the template directory at `hooks/lib/loop-common.sh:11-20`. However, direct reference searches found no call loading `block/codex-review-failed.md`. Instead, `hooks/loop-codex-stop-hook.sh` currently builds a separate inline `# Codex Review Failed` reason inside `codex_failure_exit()` at `hooks/loop-codex-stop-hook.sh:607-633`. The closest runtime caller is therefore semantic, not direct: the hook implements the same failure contract inline rather than rendering this file.
- edge_cases_or_failure_modes: If used via `render_template`, missing variable bindings would leave raw `{{...}}` tokens in the delivered message because unresolved placeholders are intentionally preserved. Multiline stderr content is supported by the renderer’s environment-variable substitution path, but very long stderr may produce a verbose block unless the caller bounds it; the current hook uses `tail -30`, while this template labels the excerpt as “last 50 lines” at `prompt-template/block/codex-review-failed.md:13`, creating a potential mismatch if wired to the existing hook without adjusting either template text or caller behavior. The template says the review result file was “not created” at `prompt-template/block/codex-review-failed.md:6`, so it fits the missing-output case better than all Codex failure cases; non-zero exit with partial output, failed stdout copy, or empty review file may need distinct error-type context like the current inline helper provides at `hooks/loop-codex-stop-hook.sh:613-624`.
- validation_or_tests: `tests/test-templates-comprehensive.sh` loads every Markdown template under `prompt-template` and checks placeholder syntax at `tests/test-templates-comprehensive.sh:83-194`, so this file is covered for loadability and `{{UPPER_CASE}}` syntax. The same test renders all templates with dummy placeholder values and warns on leftovers at `tests/test-templates-comprehensive.sh:562-606`. `tests/test-template-references.sh` scans shell scripts for explicit template references at `tests/test-template-references.sh:51-111` and separately warns on unreferenced templates at `tests/test-template-references.sh:117-142`; this file would be treated as not directly referenced unless dynamically used. I did not execute tests because this task is read-only research, and the branch export is not a writable implementation workspace.
- skip_candidate: `yes: the file is a valid prompt/block template and matches the review-failure domain, but current runtime behavior appears to use an inline Codex failure message rather than loading this template directly. It is core-adjacent as a declared contract artifact, but likely an orphan or future-intended template unless another external packager references it outside this repository export.`

## Worker Self-Test
- assigned_items_seen: 1 item section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`