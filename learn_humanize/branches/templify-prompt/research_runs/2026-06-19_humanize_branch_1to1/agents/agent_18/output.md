# agent_18 templify-prompt 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `0c107abc65600bca4e18b04265b48c91da772491`

## Item Evidence

### TEMPLIFY_PROMPT-HZ-018 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse validator for Claude `Edit` tool calls during the RLCR loop. It protects loop-owned control files and enforces that edits only target the current round’s summary file when operating inside `.humanize-loop.local`. It is registered in `hooks/hooks.json:15-20` under the `Edit` matcher.
- algorithmic_behavior: The script reads the hook JSON payload from stdin, extracts `.tool_name` and `.tool_input.file_path` with `jq`, and exits immediately for non-`Edit` tools (`hooks/loop-edit-validator.sh:22-30`). It lowercases the path through `to_lower`, then applies a sequence of gates: block round todos files (`:36-39`), block round prompt files (`:41-44`), ignore files outside `.humanize-loop.local` (`:50-52`), locate the active loop (`:58-64`), parse the current round from `state.md` (`:66`), block state edits (`:72-75`), block goal tracker edits after round 0 (`:81-85`), and validate that summary edits target the current round (`:91-116`). A valid edit reaches `exit 0` at `:119`; a blocked edit writes a rendered message to stderr and exits `2`.
- inputs_outputs_state: Input is Claude hook JSON on stdin, especially `tool_name` and `tool_input.file_path` (`:22-30`). It reads process/environment state from `CLAUDE_PROJECT_DIR` or `pwd` to locate `.humanize-loop.local` (`:58-60`). It reads loop state via `get_current_round "$ACTIVE_LOOP_DIR/state.md"` (`:66`), which in `hooks/lib/loop-common.sh:48-57` extracts `current_round:` from YAML frontmatter and defaults to `0`. Output is only the hook decision: `0` allow, `2` block with stderr guidance. It does not mutate repository or loop state.
- gates_or_invariants: Invariant 1: `round-*-todos.md` must not be edited through `Edit`; the user/agent should use native TodoWrite (`hooks/loop-edit-validator.sh:36-39`, helper message at `hooks/lib/loop-common.sh:88-95`). Invariant 2: `round-*-prompt.md` is read-only because it is generated instruction content (`hooks/loop-edit-validator.sh:41-44`, helper at `hooks/lib/loop-common.sh:98-104`). Invariant 3: `state.md` is hook-managed and blocked from direct edits (`hooks/loop-edit-validator.sh:72-75`). Invariant 4: `goal-tracker.md` is editable only in round 0; after that, the agent must request changes in the current summary (`hooks/loop-edit-validator.sh:81-85`, message helper at `hooks/lib/loop-common.sh:182-192`). Invariant 5: summary file edits inside the loop must use the current round number, enforced by comparing `extract_round_number` output to `CURRENT_ROUND` (`hooks/loop-edit-validator.sh:91-115`; extractor in `hooks/lib/loop-common.sh:77-84`).
- dependencies_and_callers: Called by Claude Code hook config `hooks/hooks.json:15-20`. Depends on Bash strict mode (`hooks/loop-edit-validator.sh:12`), `jq`, `sed`, and shared hook helpers from `hooks/lib/loop-common.sh` (`hooks/loop-edit-validator.sh:14-17`). `loop-common.sh` in turn sources `hooks/lib/template-loader.sh` and initializes `TEMPLATE_DIR` (`hooks/lib/loop-common.sh:11-20`). The wrong-round edit block renders `prompt-template/block/wrong-round-number.md` using `load_and_render_safe` (`hooks/loop-edit-validator.sh:101-114`), whose fallback behavior is implemented in `hooks/lib/template-loader.sh:155-188`.
- edge_cases_or_failure_modes: If the hook payload has no `tool_name` or `file_path`, `jq -r` yields an empty string and most path checks do not match, so the edit is allowed unless later commands fail (`hooks/loop-edit-validator.sh:22-30`). If no `.humanize-loop.local` active loop exists, the hook allows the edit (`:50-64`), preventing stale hooks from blocking normal work. If `.humanize-loop.local` appears anywhere in the path string, `is_in_humanize_loop_dir` treats it as in-loop (`hooks/lib/loop-common.sh:149-153`), so path classification is regex/string-based rather than canonicalized. If the filename extraction cannot parse a loop-relative filename, round validation is skipped (`hooks/loop-edit-validator.sh:93-98`). If `current_round` is missing or unparsable, the helper defaults to `0` (`hooks/lib/loop-common.sh:46-57`); the numeric comparison for goal tracker uses `[[ "$CURRENT_ROUND" -gt 0 ]]`, so non-numeric state content could make the hook fail under `set -euo pipefail`. Missing templates degrade to inline fallback messages through `load_and_render_safe`, not empty errors (`hooks/lib/template-loader.sh:155-188`).
- validation_or_tests: Template-reference coverage scans `hooks/loop-edit-validator.sh` for template calls and checks referenced template files exist (`tests/test-template-references.sh:57-64`, `:83-111`). The same test treats `loop-edit-validator.sh` as a critical validator and verifies it uses `load_and_render_safe`, not unsafe rendering (`tests/test-template-references.sh:172-200`). Comprehensive template tests cover `wrong-round-number.md` rendering with all variables (`tests/test-templates-comprehensive.sh:498-514`) and general load/fallback rendering behavior (`:446-463`, `:562-606`). I did not run tests because this research task is read-only; evidence is from direct source inspection.
- skip_candidate: `no`

### TEMPLIFY_PROMPT-HZ-048 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: Block-message template used when the read validator catches an attempted read of a prompt or summary file from a non-current RLCR round. It is part of the validator feedback path, not an executable algorithm itself, but it defines the corrective contract shown to the agent at a state-transition guard.
- algorithmic_behavior: The template renders a `# Wrong Round File` message, reports the attempted `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md`, states the current round as `{{CURRENT_ROUND}}`, lists the active prompt and summary files for the current round, and includes an explicit escape hatch for the originally requested file via `cat {{FILE_PATH}}` (`prompt-template/block/wrong-round-file.md:1-9`). It is loaded by `hooks/loop-read-validator.sh` when `CLAUDE_ROUND != CURRENT_ROUND` (`hooks/loop-read-validator.sh:103-115`).
- inputs_outputs_state: Inputs are render variables passed by the read validator: `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH` (`hooks/loop-read-validator.sh:109-114`). Output is markdown text written to stderr by the hook before `exit 2` (`hooks/loop-read-validator.sh:109-115`). It reads no files directly and maintains no state; state context comes from the read validator’s active loop lookup and current-round parsing (`hooks/loop-read-validator.sh:56-64`).
- gates_or_invariants: It supports the read-side invariant that agents should not consume stale prompt/summary files from old rounds. The validator first narrows to round prompt/summary files (`hooks/loop-read-validator.sh:45-47`), locates the active loop and current round (`:56-64`), extracts the requested round (`:70-73`), determines file type (`:75-81`), and blocks if the requested round differs (`:103-115`). The template reinforces the invariant by showing only the current round prompt and summary as the normal path (`prompt-template/block/wrong-round-file.md:5-7`). The final line permits explicit shell access to the requested file (`:9`), meaning the gate blocks direct `Read` use but does not claim historical files are forbidden in all circumstances.
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh:109`. Rendering depends on `load_and_render_safe` from `hooks/lib/template-loader.sh:155-188`, which preserves fallback behavior if the template is missing or empty. The read validator is registered in `hooks/hooks.json:23-29` under the `Read` matcher. Shared helper dependencies include `is_round_file_type` (`hooks/lib/loop-common.sh:65-72`), `extract_round_number` (`:74-84`), `find_active_loop` (`:23-44`), and `get_current_round` (`:46-58`).
- edge_cases_or_failure_modes: If the requested filename does not contain a parseable round number, the read validator allows it before this template is used (`hooks/loop-read-validator.sh:70-73`). If the file is a round prompt/summary outside `.humanize-loop.local`, the read validator emits `wrong-file-location.md` instead, before the round-number gate (`:87-97`). If the round number matches but the directory is stale or otherwise not exactly `ACTIVE_LOOP_DIR`, the read validator emits `wrong-directory-path.md` instead (`:122-132`). The template’s `cat {{FILE_PATH}}` guidance intentionally suggests a Bash read path; this can be useful for exceptional historical inspection, but it also means stale-round content can still be read if the agent follows that explicit shell instruction. Placeholder safety relies on the single-pass renderer in `hooks/lib/template-loader.sh:35-117`, which avoids recursive placeholder injection.
- validation_or_tests: `tests/test-template-references.sh` scans hook scripts, finds `block/wrong-round-file.md` through the `loop-read-validator.sh` reference, and fails if the file is missing (`tests/test-template-references.sh:83-111`). The same test checks all template files for direct hook references and reports unreferenced templates as warnings (`:119-142`). `tests/test-templates-comprehensive.sh` loads every markdown template, validates placeholder syntax, and tries rendering every template with dummy values (`tests/test-templates-comprehensive.sh:90-107`, `:118-194`, `:562-606`). There is direct integration coverage for `wrong-round-number.md` (`:498-514`), but no similarly specific assertion for `wrong-round-file.md` beyond the all-template load/render sweep. I did not run tests because this research task is read-only; evidence is from direct source inspection.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 Item Evidence sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`