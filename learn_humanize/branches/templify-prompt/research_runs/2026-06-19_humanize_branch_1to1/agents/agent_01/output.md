# agent_01 templify-prompt 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `0c107abc65600bca4e18b04265b48c91da772491`

## Item Evidence

### TEMPLIFY_PROMPT-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root for the Humanize/RLCR Claude Code plugin. It implements an iterative loop where `/humanize:start-rlcr-loop` creates a local session under `.humanize-loop.local/<timestamp>/`, Claude implements a plan and writes a round summary, and a Stop hook invokes Codex to review the work before allowing termination or generating the next round. The README describes the main flow at `README.md:31`-`README.md:39`, the active state directory and artifacts at `README.md:241`-`README.md:257`, and the goal tracker model at `README.md:130`-`README.md:151`.
- algorithmic_behavior: Slash commands are the entry points: `commands/start-rlcr-loop.md:1`-`commands/start-rlcr-loop.md:14` delegates setup to `scripts/setup-rlcr-loop.sh`, while `commands/cancel-rlcr-loop.md:11`-`commands/cancel-rlcr-loop.md:22` removes active `state.md` files. Hook registration in `hooks/hooks.json:4`-`hooks/hooks.json:52` wires PreToolUse validators for Write/Edit/Read/Bash and a Stop hook with a 7200s timeout. Setup parses options and validates the plan file, Codex availability, max iterations, model/effort, timeout, and push policy at `scripts/setup-rlcr-loop.sh:93`-`scripts/setup-rlcr-loop.sh:201`; it then creates `state.md`, `goal-tracker.md`, and round-0 prompt/summary paths at `scripts/setup-rlcr-loop.sh:223`-`scripts/setup-rlcr-loop.sh:386`. The Stop hook is the main state machine: it checks transcript todos, large files, git cleanliness, push requirements, state validity, summary presence, goal tracker initialization, max iterations, builds a Codex review prompt, runs `codex exec`, validates review output, and either ends the loop or writes the next-round prompt at `hooks/loop-codex-stop-hook.sh:61`-`hooks/loop-codex-stop-hook.sh:829`.
- inputs_outputs_state: Primary inputs are a markdown plan path plus options (`--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`) parsed at `scripts/setup-rlcr-loop.sh:93`-`scripts/setup-rlcr-loop.sh:157`; Claude Code hook JSON on stdin, parsed with `jq` in validators such as `hooks/loop-write-validator.sh:22`-`hooks/loop-write-validator.sh:31`; transcript JSONL consumed by `hooks/check-todos-from-transcript.py:87`-`hooks/check-todos-from-transcript.py:125`; git status and filesystem state consumed by the Stop hook at `hooks/loop-codex-stop-hook.sh:106`-`hooks/loop-codex-stop-hook.sh:275`; and prompt templates under `prompt-template/`. Outputs are local loop artifacts (`state.md`, `goal-tracker.md`, `round-N-prompt.md`, `round-N-summary.md`, `round-N-review-prompt.md`, `round-N-review-result.md`) described in `README.md:243`-`README.md:251`, Codex debug/cache files under `$HOME/.cache/humanize/...` created at `hooks/loop-codex-stop-hook.sh:536`-`hooks/loop-codex-stop-hook.sh:548`, and hook JSON block decisions such as `hooks/loop-codex-stop-hook.sh:86`-`hooks/loop-codex-stop-hook.sh:94`. State advances by replacing `current_round` in `state.md` at `hooks/loop-codex-stop-hook.sh:763`-`hooks/loop-codex-stop-hook.sh:766`; terminal success or circuit-breaker removal of `state.md` happens at `hooks/loop-codex-stop-hook.sh:719`-`hooks/loop-codex-stop-hook.sh:756`.
- gates_or_invariants: The root enforces a single active newest loop via `find_active_loop` in `hooks/lib/loop-common.sh:23`-`hooks/lib/loop-common.sh:44`; stale older loops are ignored. Plans must exist and have at least five lines at `scripts/setup-rlcr-loop.sh:179`-`scripts/setup-rlcr-loop.sh:193`. Round files must match active session/current round and avoid manual prompt/state edits; shared path helpers and block-message functions are in `hooks/lib/loop-common.sh:65`-`hooks/lib/loop-common.sh:191`. Write/Edit/Read/Bash validators block todos files, prompt files, `state.md`, wrong round summaries, wrong loop paths, post-round-0 goal tracker edits, and Bash-based bypass attempts; examples are `hooks/loop-write-validator.sh:37`-`hooks/loop-write-validator.sh:97`, `hooks/loop-read-validator.sh:36`-`hooks/loop-read-validator.sh:133`, and `hooks/loop-bash-validator.sh:49`-`hooks/loop-bash-validator.sh:123`. Stop-hook gates include incomplete TodoWrite items at `hooks/loop-codex-stop-hook.sh:67`-`hooks/loop-codex-stop-hook.sh:95`, >2000-line changed code/docs files at `hooks/loop-codex-stop-hook.sh:104`-`hooks/loop-codex-stop-hook.sh:176`, clean git at `hooks/loop-codex-stop-hook.sh:185`-`hooks/loop-codex-stop-hook.sh:240`, optional push-every-round at `hooks/loop-codex-stop-hook.sh:243`-`hooks/loop-codex-stop-hook.sh:273`, summary existence at `hooks/loop-codex-stop-hook.sh:319`-`hooks/loop-codex-stop-hook.sh:340`, and round-0 goal tracker initialization at `hooks/loop-codex-stop-hook.sh:343`-`hooks/loop-codex-stop-hook.sh:401`.
- dependencies_and_callers: `.claude-plugin/plugin.json:1`-` .claude-plugin/plugin.json:18` and `.claude-plugin/marketplace.json:1`-` .claude-plugin/marketplace.json:13` package the plugin. `.claude/CLAUDE.md:4`-` .claude/CLAUDE.md:8` defines repo rules, including version bump and user confirmation for commits/pushes. `hooks/lib/template-loader.sh:9`-`hooks/lib/template-loader.sh:206` is the shared template dependency, used by `hooks/lib/loop-common.sh:11`-`hooks/lib/loop-common.sh:17` and Stop/validator scripts. The template loader uses `cat`, `env`, and `awk` for single-pass placeholder rendering at `hooks/lib/template-loader.sh:43`-`hooks/lib/template-loader.sh:117`, with safe fallback rendering at `hooks/lib/template-loader.sh:155`-`hooks/lib/template-loader.sh:188`. `scripts/portable-timeout.sh:9`-`scripts/portable-timeout.sh:71` supplies timeout portability for Codex execution. `scripts/humanize.sh:9`-`scripts/humanize.sh:553` is an operational monitor that parses local loop state, goal tracker tables, git status, and cached Codex logs. CI workflows cover shell syntax, templates, Claude automation, and version checks in `.github/workflows/shell-syntax-check.yml:19`-` .github/workflows/shell-syntax-check.yml:61`, `.github/workflows/template-test.yml:26`-` .github/workflows/template-test.yml:62`, and `.github/workflows/version-bump-check.yml:56`-` .github/workflows/version-bump-check.yml:123`.
- edge_cases_or_failure_modes: Missing templates normally degrade through inline fallbacks via `load_and_render_safe`, but `load_template` itself emits empty output for missing files at `hooks/lib/template-loader.sh:21`-`hooks/lib/template-loader.sh:33`; `tests/test-error-scenarios.sh:114`-`tests/test-error-scenarios.sh:120` explicitly notes the risk of empty feedback. The renderer uses a Bash here-string at `hooks/lib/template-loader.sh:59`-`hooks/lib/template-loader.sh:114`; in this read-only sandbox, runtime tests failed because the shell could not create temp files for that here document. The Stop hook removes `state.md` if `current_round` is corrupt (`hooks/loop-codex-stop-hook.sh:303`-`hooks/loop-codex-stop-hook.sh:308`), silently defaults invalid `max_iterations` to 42 (`hooks/loop-codex-stop-hook.sh:310`-`hooks/loop-codex-stop-hook.sh:313`), copies Codex stdout into the expected review result if the file is missing (`hooks/loop-codex-stop-hook.sh:654`-`hooks/loop-codex-stop-hook.sh:670`), and blocks with debug paths on nonzero Codex exit, missing result, or empty result (`hooks/loop-codex-stop-hook.sh:607`-`hooks/loop-codex-stop-hook.sh:707`). `find_active_loop` ignores older state files by design (`hooks/lib/loop-common.sh:23`-`hooks/lib/loop-common.sh:44`), which prevents zombie sessions but means only the lexicographically newest timestamp can be active.
- validation_or_tests: Shell syntax validation passed for all shell scripts using `bash -n`. `tests/test-template-references.sh` passed with 67 passes, 0 failures, and 1 warning; it specifically verified `block/git-not-clean-humanize-local.md` exists and is referenced. `tests/test-template-loader.sh` failed in this managed read-only sandbox after 5 passes and 31 failures because `hooks/lib/template-loader.sh:114` could not create temp files for here-doc/here-string execution; this is an environment limitation observed during validation, not a source edit. `python3 -m py_compile hooks/check-todos-from-transcript.py` failed because Python attempted to write cache files under `/tmp` and the user cache path, both blocked by the sandbox. Test coverage also includes loader behavior and placeholder injection prevention in `tests/test-template-loader.sh:81`-`tests/test-template-loader.sh:245`, template reference existence and safe-render usage in `tests/test-template-references.sh:51`-`tests/test-template-references.sh:230`, error scenarios in `tests/test-error-scenarios.sh:21`-`tests/test-error-scenarios.sh:120`, and comprehensive template syntax/render/stress checks in `tests/test-templates-comprehensive.sh:61`-`tests/test-templates-comprehensive.sh:620`.
- skip_candidate: `no`

### TEMPLIFY_PROMPT-HZ-031 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: A specialized prompt block injected into the Stop hook’s git-clean failure response when `.humanize-loop.local/` appears as an untracked file or directory. It is a narrow but algorithmically relevant transition contract: the loop should still require a clean git state before review, but it tells Claude to ignore/track the generated loop directory by adding an ignore pattern rather than committing runtime state.
- algorithmic_behavior: The file contains a short Markdown note at `prompt-template/block/git-not-clean-humanize-local.md:2`-`prompt-template/block/git-not-clean-humanize-local.md:8`. The Stop hook loads it only when `git status --porcelain` has untracked entries matching `.humanize-loop.local` at `hooks/loop-codex-stop-hook.sh:196`-`hooks/loop-codex-stop-hook.sh:205`. Its content is concatenated into `SPECIAL_NOTES`, then rendered into the broader git-clean block via `block/git-not-clean.md` at `hooks/loop-codex-stop-hook.sh:218`-`hooks/loop-codex-stop-hook.sh:239`. The surrounding block decision prevents exit/Codex review until the worktree is clean.
- inputs_outputs_state: Input is the untracked subset of `git status --porcelain`, computed at `hooks/loop-codex-stop-hook.sh:190`-`hooks/loop-codex-stop-hook.sh:199`. The template itself has no placeholders and no local state mutation. Output is advisory Markdown appended to the hook’s JSON `reason` field. The recommended user-facing transition is to append `.humanize*local*` to `.gitignore` and stage `.gitignore`, as written at `prompt-template/block/git-not-clean-humanize-local.md:4`-`prompt-template/block/git-not-clean-humanize-local.md:8`; the actual hook still blocks because `GIT_ISSUES` remains `uncommitted changes` until the git state is resolved.
- gates_or_invariants: The invariant is that `.humanize-loop.local/` is generated loop state and should not be committed. This is consistent with repository ignore rules at `.gitignore:3`-`.gitignore:4`, which already ignore `.humanize-loop.local/`. The template functions as a special-case explanation inside the strict git-clean gate, not as a bypass. If the template is missing, the Stop hook falls back to a plain note at `hooks/loop-codex-stop-hook.sh:200`-`hooks/loop-codex-stop-hook.sh:203`, so the gate remains functional.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:199`-`hooks/loop-codex-stop-hook.sh:205` through `load_template`. The loaded note is merged into `block/git-not-clean.md`, whose `{{SPECIAL_NOTES}}` insertion point is at `prompt-template/block/git-not-clean.md:1`-`prompt-template/block/git-not-clean.md:4`. Template existence is covered by `tests/test-template-references.sh`, which scans shell template references at `tests/test-template-references.sh:51`-`tests/test-template-references.sh:111` and reported this file as present in the validation run.
- edge_cases_or_failure_modes: If `.humanize-loop.local` is already in `.gitignore`, the directory should not appear in `git status --porcelain`; the block is then not loaded. If `.humanize-loop.local` appears alongside other untracked files, this note is appended together with `block/git-not-clean-untracked.md`, because other untracked entries are computed at `hooks/loop-codex-stop-hook.sh:207`-`hooks/loop-codex-stop-hook.sh:215`. If `.humanize-loop.local` is modified as a tracked path, this special note may not trigger because it only inspects the `??` untracked subset. The template’s suggested ignore pattern is broader than the repo’s current exact ignore entry, which can catch variant names but may also hide similarly named runtime artifacts.
- validation_or_tests: Direct file inspection confirmed 8 lines and no placeholders. `tests/test-template-references.sh` passed and included `block/git-not-clean-humanize-local.md` as an existing referenced template. Broader runtime rendering of templates could not be fully validated in this read-only sandbox because the template renderer’s here-string path attempted blocked temp-file creation, but this specific template is loaded with `load_template` and has no variable substitution.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TEMPLIFY_PROMPT-HZ-001`, `TEMPLIFY_PROMPT-HZ-031`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`