# agent_06 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 9
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-006 `directory` `hooks`
- cursor: `[_]`
- core_role: Claude Code hook layer for the RLCR loop: it validates tool use, protects loop-owned files, enforces state-machine phase gates, and drives stop-hook transitions between implementation, review, finalize, methodology-analysis, cancel, and terminal states.
- algorithmic_behavior: `hooks/hooks.json:2-74` wires `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, and `Stop` events to validators. `hooks/loop-codex-stop-hook.sh` is the transition engine: it resolves the active loop, parses state, enforces branch/plan/git/todo/large-file/BitLesson/contract gates, launches Codex reviews, interprets `COMPLETE` / `STOP`, advances rounds, and creates next-round prompts/summaries/contracts. `hooks/lib/loop-common.sh` supplies shared state parsing, active-loop lookup, review issue extraction, protected-path logic, cancel authorization, and git protection. Validators block writes/edits/reads/bash that would bypass the state machine.
- inputs_outputs_state: Inputs include hook JSON on stdin, `.humanize/rlcr/<loop>` state files, transcripts, task records, git status, config defaults, prompt templates, Codex review logs, and round artifacts. Outputs include hook allow/block JSON, updated state fields, state-file renames such as active to finalize/methodology/cancel states, generated prompts and summaries, review-result files, debug logs, and background-task markers.
- gates_or_invariants: `hooks/lib/loop-common.sh:91-128` validates hook JSON and UTF-8/null-byte safety; `hooks/lib/loop-common.sh:494-600` strictly parses state; `hooks/loop-codex-stop-hook.sh:225-270` enforces schema fields; `hooks/loop-codex-stop-hook.sh:289-316` checks branch consistency; `hooks/loop-codex-stop-hook.sh:325-399` verifies plan integrity; `hooks/loop-codex-stop-hook.sh:408-457` checks incomplete todos/tasks; `hooks/loop-codex-stop-hook.sh:680-789` gates git cleanliness, tracked `.humanize`, and unpushed changes; `hooks/loop-codex-stop-hook.sh:834-862` requires round contracts when drift tracking exists. PreTool validators enforce phase-specific file ownership: read/write/edit validators block old or wrong-round prompt/summary/contract access and loop-owned state writes.
- dependencies_and_callers: Called by Claude Code hook runtime through `hooks/hooks.json`. Depends on `jq`, `git`, shell utilities, Python for transcript todo scanning, Codex CLI for review execution, `hooks/lib/project-root.sh`, `hooks/lib/template-loader.sh`, `hooks/lib/loop-bg-tasks.sh`, `hooks/lib/methodology-analysis.sh`, prompt templates under `prompt-template/`, setup/runtime scripts under `scripts/`, and tests under `tests/`.
- edge_cases_or_failure_modes: The directory is fail-closed in several places: invalid hook JSON, missing or malformed state, missing git metadata, empty/missing review logs, plan drift, wrong branch, dirty git state, tracked `.humanize`, unpushed changes, incomplete todos, background tasks still pending, missing round contract, missing mainline verdict, and repeated mainline `STALLED` / `REGRESSED` verdicts. Background-task handling deliberately avoids advancing the loop while async work is pending and has guards for stale or cross-session markers.
- validation_or_tests: Covered directly by `tests/test-codex-review-merge.sh` for review-log issue detection, `tests/robustness/test-setup-scripts-robustness.sh` for setup/state initialization contracts that the hooks later consume, and hook-specific behavior implied by validators and stop-hook gates. The directory also coordinates with prompt templates `prompt-template/block/round-contract-missing.md` and `prompt-template/codex/full-alignment-review.md`.
- skip_candidate: `no`

### H2_DEV-HZ-036 `file` `commands/refine-plan.md`
- cursor: `[_]`
- core_role: Command workflow definition for the plan-refinement algorithm. It is planning-only and specifies how annotated draft plans are parsed, classified, answered, transformed, validated, and written atomically.
- algorithmic_behavior: Frontmatter at `commands/refine-plan.md:1-13` declares a hidden command with allowed tools. The hard constraint at `commands/refine-plan.md:19-29` says no implementation work, tests, or code edits. The workflow is phase-based: argument parsing, config merge, IO validation, comment scanning, classification, processing, plan generation, QA document generation, atomic writes, and final reporting. Comment scanning at `commands/refine-plan.md:184-272` is stateful across Markdown fences, HTML comments, custom CMT blocks, headings, and inline/multiline comment syntaxes.
- inputs_outputs_state: Inputs are `--input`, optional `--output`, `--qa-dir`, mode flags such as discussion/direct, config values from `.humanizeconfig` / environment / CLI, and annotated Markdown comments. Outputs are a refined plan file, optional QA/discussion artifacts, optional appendix content, validation summaries, and final reporting. State is local to the command transaction: scanned comments, classifications, dispositions, generated plan content, QA docs, and temp-file replacement plan.
- gates_or_invariants: Argument rules require `--input`; discussion/direct are mutually exclusive; output may be in-place or separate. IO validation has an exit-code contract at `commands/refine-plan.md:157-180`. Config merge semantics at `commands/refine-plan.md:86-153` define priority and alternative-plan language mapping. Classification precedence at `commands/refine-plan.md:276-330` is research request over change request over question. Plan validation at `commands/refine-plan.md:395-453` requires generated plans to preserve expected `gen-plan` sections, acceptance criteria, dependency tags, no unresolved comment markers, and convergence handling.
- dependencies_and_callers: Uses the validator script for IO checking, the config-loader semantics named in `commands/refine-plan.md:86-153`, and only read/search tools for research handling. It is a command surface that feeds later RLCR planning by producing a cleaner plan artifact rather than mutating implementation code.
- edge_cases_or_failure_modes: Scanner errors are fatal for nested, stray, or unclosed comment blocks; comments inside fences and HTML comments are ignored; ambiguous direct-mode questions are assumed according to direct processing rules; deferred or unresolved comments must be represented with dispositions instead of silently dropped. Atomic write rules at `commands/refine-plan.md:508-572` prevent partial outputs and stale temp files.
- validation_or_tests: The command defines its own validation contract, including IO validation exit codes, no-comment-marker checks, plan section validation, QA document requirements at `commands/refine-plan.md:457-505`, and error handling at `commands/refine-plan.md:589-607`.
- skip_candidate: `no`

### H2_DEV-HZ-066 `file` `scripts/validate-explore-idea-io.sh`
- cursor: `[_]`
- core_role: Runtime preflight and routing validator for the explore-idea workflow. It validates input shape, direction selection, git anchoring, runtime capacity settings, and emits a structured key-value contract for downstream execution.
- algorithmic_behavior: The script is read-only preflight: exit codes are declared at `scripts/validate-explore-idea-io.sh:19-29`, success output keys at `scripts/validate-explore-idea-io.sh:31-52`, defaults/caps at `scripts/validate-explore-idea-io.sh:60-69`, argument parsing at `scripts/validate-explore-idea-io.sh:143-170`, and required `--input` handling at `scripts/validate-explore-idea-io.sh:176-180`. It resolves either a directions JSON file or a draft Markdown file with a companion `.directions.json`, validates schema, selects directions, caps concurrency, anchors to git, creates deterministic run metadata, and prints final values.
- inputs_outputs_state: Inputs include CLI flags, draft or directions JSON path, optional explicit direction selectors, concurrency and cap values, git repository state, `BASE_COMMIT`, and timestamp/nonce environment values. Outputs are key-value lines such as selected directions, run id, output directories, base commit/branch, concurrency, model, and effort. It does not write workflow state itself; it prepares state for the caller.
- gates_or_invariants: Numeric caps are validated by `validate_int_cap` at `scripts/validate-explore-idea-io.sh:186-201`. Input resolution at `scripts/validate-explore-idea-io.sh:207-235` rejects wrong file types. Schema validation requires `jq` and `validate-directions-json.sh` at `scripts/validate-explore-idea-io.sh:266-275`. Default direction selection sorts by display order and caps to six at `scripts/validate-explore-idea-io.sh:287-296`; explicit `--directions` handling rejects duplicate or unknown selectors and caps at ten at `scripts/validate-explore-idea-io.sh:299-351`. Git anchoring at `scripts/validate-explore-idea-io.sh:367-403` requires a git checkout, `BASE_COMMIT`, and clean tracked files.
- dependencies_and_callers: Depends on `jq`, `git`, companion schema validator `scripts/validate-directions-json.sh`, plugin/template locations, shell utilities, and optional randomness sources for nonce generation. It is intended to be called before the explore-idea runner allocates work.
- edge_cases_or_failure_modes: Missing input exits early; nonnumeric or oversized caps fail; direct JSON and draft-companion routing are distinct; unknown direction IDs or duplicate source indexes fail; selected concurrency is reduced to selected direction count; dirty tracked files block execution; existing run-id paths cause collision failure; absent template/plugin files block final routing.
- validation_or_tests: The script contains its own structured exit-code and output contract; downstream tests can assert exact emitted keys. Hardcoded review runtime values are emitted at `scripts/validate-explore-idea-io.sh:449-451` as `gpt-5.5` and `xhigh`, matching scheduler metadata expectations for this run family.
- skip_candidate: `no`

### H2_DEV-HZ-096 `file` `tests/test-codex-review-merge.sh`
- cursor: `[_]`
- core_role: Executable specification for Codex review-log issue extraction, specifically `detect_review_issues` in `hooks/lib/loop-common.sh`.
- algorithmic_behavior: The test creates isolated loop/cache directories, sources `hooks/lib/loop-common.sh`, writes synthetic `round-N-codex-review.log` files, calls `detect_review_issues`, and asserts return codes and generated `round-N-review-result.md` behavior. Setup is at `tests/test-codex-review-merge.sh:20-43`.
- inputs_outputs_state: Inputs are synthetic review logs in `$CACHE_DIR`, current round values, and loop directory paths. Outputs are function return codes and optional `$LOOP_DIR/round-N-review-result.md` files. State is temporary per test case and reset through helper setup.
- gates_or_invariants: The spec fixes the issue marker algorithm: `[P#]`-style markers must appear within the first ten characters of a line to count; only the last 50 log lines are scanned; missing or empty logs return hard-error code `2`; no issue markers return `1`; detected issues return `0` and write the review result from the first matching issue line to the end.
- dependencies_and_callers: Depends on Bash, temporary filesystem, and `hooks/lib/loop-common.sh`. It validates behavior consumed by `hooks/loop-codex-stop-hook.sh` when deciding whether to continue review rounds or advance.
- edge_cases_or_failure_modes: Tests cover markers outside the allowed prefix ignored at `tests/test-codex-review-merge.sh:84-107`, no markers at `tests/test-codex-review-merge.sh:110-131`, missing/empty logs at `tests/test-codex-review-merge.sh:134-169`, late markers detected at `tests/test-codex-review-merge.sh:172-197`, early markers outside the last-50-line window ignored at `tests/test-codex-review-merge.sh:199-227`, multiple issues extracted from the first match at `tests/test-codex-review-merge.sh:229-255`, and position-zero/dash-prefix variants at `tests/test-codex-review-merge.sh:257-303`.
- validation_or_tests: This file is itself the validation artifact. It finishes with result-file creation checks at `tests/test-codex-review-merge.sh:305-330`, exact 50-line boundary coverage at `tests/test-codex-review-merge.sh:332-354`, and a failure summary/exit contract at `tests/test-codex-review-merge.sh:356-368`.
- skip_candidate: `no`

### H2_DEV-HZ-126 `file` `tests/test-streaming.sh`
- cursor: `[_]`
- core_role: Executable specification for the log streaming and monitor state behavior used by the visualization/server side of RLCR observation.
- algorithmic_behavior: The test drives `viz/server/log_streamer.py` and related parser behavior through Python snippets embedded in Bash. It verifies startup snapshots, append polling, truncation, rotation, missing/recreated files, EOF handling, replay windows, chunking, registry sharing, reconnects, and idle eviction. Its contract summary is declared at `tests/test-streaming.sh:3-18`.
- inputs_outputs_state: Inputs are temporary log files, synthetic append/truncate/rotate operations, source/session records, and cache paths. Outputs are event lists with event types such as snapshot, append, resync, eof/no-op, and replay; registry state; cached log ordering; and eviction outcomes.
- gates_or_invariants: Initial missing files produce no snapshot and later `resync missing`; existing files produce snapshots; append events advance offsets and byte counts; truncation and inode change cause resync; recreated files snapshot anew; replay is bounded by the configured window; snapshots chunk at 64 KiB; registry acquisition returns shared stream objects for the same source and cleans idle streams.
- dependencies_and_callers: Depends on `viz/server/log_streamer.py`, `viz/server/parser.py`, temporary filesystem, Python runtime, and RLCR source/cache conventions. It supports the monitor/routing side rather than the write-protect hook side, but it still specifies core runtime observation behavior.
- edge_cases_or_failure_modes: Covers missing startup at `tests/test-streaming.sh:56-80`, small existing snapshot at `tests/test-streaming.sh:82-112`, append offsets at `tests/test-streaming.sh:114-136`, truncation at `tests/test-streaming.sh:138-159`, rotation/inode change at `tests/test-streaming.sh:161-189`, missing-then-reappear at `tests/test-streaming.sh:191-217`, EOF one-shot behavior at `tests/test-streaming.sh:219-243`, replay and overflow at `tests/test-streaming.sh:245-314`, chunking at `tests/test-streaming.sh:316-340`, parser cache ordering at `tests/test-streaming.sh:342-380`, reconnect sharing at `tests/test-streaming.sh:382-472`, and idle eviction at `tests/test-streaming.sh:474-520`.
- validation_or_tests: This file is the validation artifact for streaming semantics and ends with a summary/exit gate at `tests/test-streaming.sh:522-532`.
- skip_candidate: `no`

### H2_DEV-HZ-156 `file` `hooks/lib/project-root.sh`
- cursor: `[_]`
- core_role: Shared path-resolution library for hook authorization. It gives validators and stop-hook code a deterministic project root and canonical path primitives so loop-owned file checks are stable across symlinks, missing leaves, and linked worktrees.
- algorithmic_behavior: The priority order is documented at `hooks/lib/project-root.sh:5-9`: linked git worktree root, then `CLAUDE_PROJECT_DIR`, then `git rev-parse --show-toplevel`, else fail. `resolve_project_root` at `hooks/lib/project-root.sh:51-76` canonicalizes the root and special-cases linked worktree `.git` files when git’s root differs from the environment root. `canonicalize_path_prefix` at `hooks/lib/project-root.sh:95-119` canonicalizes the existing parent while preserving the basename. `canonicalize_path` at `hooks/lib/project-root.sh:136-167` resolves the leaf when it exists and otherwise falls back to parent-plus-basename.
- inputs_outputs_state: Inputs are optional path arguments, `CLAUDE_PROJECT_DIR`, current working directory, git metadata, filesystem realpaths, and path strings from hook JSON. Outputs are absolute canonical paths printed to stdout or nonzero failures. It has no persistent state.
- gates_or_invariants: Root resolution must return a directory or fail; canonicalization avoids authorizing path escapes through symlinks; nonexisting target paths are normalized by resolving the nearest existing parent, which is necessary for validating intended writes before files exist.
- dependencies_and_callers: Depends on `git`, `pwd -P`, `realpath` or Python fallback behavior, and standard shell utilities. It is sourced by hook validators and shared libraries including path-protection code in `hooks/lib/loop-common.sh`.
- edge_cases_or_failure_modes: Handles missing leaf files, linked worktree `.git` files, unset `CLAUDE_PROJECT_DIR`, git failure outside a repository, and symlinked parents. Failure to resolve a root or parent makes validators fail closed rather than accidentally allow writes.
- validation_or_tests: Its behavior is indirectly exercised by setup robustness tests for symlink rejection and hook tests that rely on canonical loop paths, especially `tests/robustness/test-setup-scripts-robustness.sh:540-593`.
- skip_candidate: `no`

### H2_DEV-HZ-186 `file` `prompt-template/block/round-contract-missing.md`
- cursor: `[_]`
- core_role: Block prompt template for the missing round-contract gate. It tells the agent exactly what artifact must be written before the stop hook can advance when drift tracking is active.
- algorithmic_behavior: The template instructs the model to create `{{ROUND_CONTRACT_FILE}}`, restating mainline objective, target acceptance criteria, blocking versus queued issues, and success criteria. It closes by stating that the loop uses this contract to prevent goal drift (`prompt-template/block/round-contract-missing.md:13`).
- inputs_outputs_state: Input is the rendered `ROUND_CONTRACT_FILE` variable supplied by the stop hook/template loader. Output is a block message to the active agent; the required follow-up state is the creation of the round contract file in the current loop directory.
- gates_or_invariants: The stop hook invokes this template when not in finalize and raw `drift_status` exists but the round contract is missing. The invariant is that any drift-aware round must have an explicit contract before exit/transition.
- dependencies_and_callers: Consumed by `hooks/loop-codex-stop-hook.sh:834-862` through the template-loading path in `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: Missing or unrendered `ROUND_CONTRACT_FILE` would make the block less actionable, but the underlying hook gate still prevents advancement. If an agent writes the wrong round’s contract, write/read validators and stop-hook path checks prevent it from satisfying the current-round invariant.
- validation_or_tests: Validated indirectly by stop-hook contract checks and setup tests that require skip-implementation paths to create round-0 contracts and prompts referencing those contracts.
- skip_candidate: `no`

### H2_DEV-HZ-216 `file` `prompt-template/codex/full-alignment-review.md`
- cursor: `[_]`
- core_role: Full-alignment Codex review prompt template. It defines the review algorithm for periodic deep alignment checks against the original plan, goal tracker, summary, commit history, and implementation state.
- algorithmic_behavior: The template requires reading the original plan at `prompt-template/codex/full-alignment-review.md:5-10`, reviews current summary at `prompt-template/codex/full-alignment-review.md:13-17`, includes commit history at `prompt-template/codex/full-alignment-review.md:19`, audits goal tracker integrity at `prompt-template/codex/full-alignment-review.md:21-50`, checks mainline drift at `prompt-template/codex/full-alignment-review.md:52-67`, performs implementation review at `prompt-template/codex/full-alignment-review.md:68-73`, and requires goal-tracker updates at `prompt-template/codex/full-alignment-review.md:75`.
- inputs_outputs_state: Inputs are rendered paths and context values such as plan file, goal tracker, summary, review result path, commit history, and current loop/round. Outputs are a Codex review result file, issue list when applicable, goal-tracker updates when needed, a mandatory `Mainline Progress Verdict`, and a final line marker of `COMPLETE` or `STOP` only under strict conditions.
- gates_or_invariants: The template mandates `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED`; the stop hook blocks omission. It instructs `STOP` on stagnation at `prompt-template/codex/full-alignment-review.md:77-103`. Output requirements at `prompt-template/codex/full-alignment-review.md:104-114` require issues to be written to `{{REVIEW_RESULT_FILE}}` and allow `COMPLETE` only when all original plan tasks and acceptance criteria are fully met with no deferrals.
- dependencies_and_callers: Consumed by `hooks/loop-codex-stop-hook.sh:1100-1114` for full-alignment review rounds, rendered by the template loader, and interpreted by stop-hook marker/verdict logic at `hooks/loop-codex-stop-hook.sh:1823-1874`.
- edge_cases_or_failure_modes: Missing mainline verdict blocks progress; a non-final-line marker is ignored by the stop hook; `STALLED` / `REGRESSED` increments mainline stall accounting; unjustified `COMPLETE` is constrained by the prompt and then subject to code-review gates; explicit `STOP` enters the loop stop path.
- validation_or_tests: Validated indirectly by stop-hook review-phase logic, mainline verdict enforcement, and setup robustness tests for `full_review_round` options at `tests/robustness/test-setup-scripts-robustness.sh:770-859`.
- skip_candidate: `no`

### H2_DEV-HZ-246 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for `scripts/setup-rlcr-loop.sh`, the initializer that creates the RLCR loop state and artifacts consumed by the hook state machine.
- algorithmic_behavior: The test builds temporary git repositories, creates minimal plans, mocks runtime dependencies, calls setup with many argument/environment combinations, and asserts validation failures or generated state/artifact contents. Helpers are defined at `tests/robustness/test-setup-scripts-robustness.sh:31-92`.
- inputs_outputs_state: Inputs include CLI args for setup, plan paths, git repo state, dependency availability, mocked `codex` / `jq` / timeout tools, branch names, symlinks, and skip-implementation flags. Outputs are setup exit codes, stderr/stdout diagnostics, `.humanize/rlcr/<loop>` state files, review-start markers, goal tracker, round-0 summary, round-0 contract, and prompt scaffolds.
- gates_or_invariants: Argument validation covers help, missing plan, invalid `--max`, invalid `--codex-timeout`, missing `--codex-model`, unknown options, and conflicting positional/`--plan-file` inputs at `tests/robustness/test-setup-scripts-robustness.sh:102-220`. Plan validation rejects comment-only, too-short, spaces, shell metacharacters, and absolute paths at `tests/robustness/test-setup-scripts-robustness.sh:230-347`. YAML safety rejects unsafe branch/model values at `tests/robustness/test-setup-scripts-robustness.sh:357-408`. Git gates reject non-git repos, dirty untracked config, repos without commits, and tracked plans without explicit tracking at `tests/robustness/test-setup-scripts-robustness.sh:418-492`.
- dependencies_and_callers: Depends on Bash, git, mocked runtime binaries, and `scripts/setup-rlcr-loop.sh`. The initialized state is consumed by `hooks/loop-codex-stop-hook.sh`, validators in `hooks/`, and prompt/template generation paths.
- edge_cases_or_failure_modes: Tests mutual exclusion with active loops at `tests/robustness/test-setup-scripts-robustness.sh:502-530`, plan-file symlink and parent-symlink rejection at `tests/robustness/test-setup-scripts-robustness.sh:540-593`, valid and extreme numeric/timeout behavior at `tests/robustness/test-setup-scripts-robustness.sh:603-760`, timeout command return `124`, and absence of hardcoded `/usr/bin/git`. It also tests dependency failures for missing Codex, missing `jq`, multiple missing deps, and all-deps-present behavior at `tests/robustness/test-setup-scripts-robustness.sh:1059-1185`.
- validation_or_tests: This file is the validation artifact. It additionally specifies full-review round validation at `tests/robustness/test-setup-scripts-robustness.sh:770-859` and skip-implementation initialization at `tests/robustness/test-setup-scripts-robustness.sh:869-1049`, including `review_started: true`, `.review-phase-started`, placeholder-free goal tracker, round-0 summary with BitLesson Delta action `none`, round-0 contract with mainline objective, and prompt references to the contract. Summary/exit appears at `tests/robustness/test-setup-scripts-robustness.sh:1191`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 9 item sections present in the same order as assigned
- missing_items: none
- duplicate_items: none
- item_section_ids: H2_DEV-HZ-006; H2_DEV-HZ-036; H2_DEV-HZ-066; H2_DEV-HZ-096; H2_DEV-HZ-126; H2_DEV-HZ-156; H2_DEV-HZ-186; H2_DEV-HZ-216; H2_DEV-HZ-246
- final_worker_status: `complete`