# agent_23 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-023 `directory` `skills/humanize`
- cursor: `[_]`
- core_role: The directory is the installed user-facing Humanize skill surface. Recursive inspection found only `skills/humanize/SKILL.md`; its role is to document and route the core RLCR loop, plan generation, monitoring, storage layout, and stop-hook expectations.
- algorithmic_behavior: `skills/humanize/SKILL.md:32-49` defines RLCR as a two-phase algorithm: implementation rounds with summary review until Codex says `COMPLETE`, then code review rounds using `codex review --base <branch>` until no `[P0-9]` issues remain, then finalize. `skills/humanize/SKILL.md:50-58` separately defines structured plan generation from a draft. `skills/humanize/SKILL.md:153-165` defines the goal-tracker mechanism: immutable goal/ACs plus mutable task/progress/deferral/evolution state, with periodic full alignment checks.
- inputs_outputs_state: Inputs are plan files, optional review-only mode, loop options, and runtime root hydration via `{{HUMANIZE_RUNTIME_ROOT}}` at `skills/humanize/SKILL.md:12-20`. Outputs/state live under `.humanize/`, including `state.md`, `goal-tracker.md`, round summaries/review results, finalize state/summary, methodology reports, and skill output metadata at `skills/humanize/SKILL.md:181-204`. Commands emit or consume RLCR summaries and monitor output via `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/validate-gen-plan-io.sh`, `scripts/ask-codex.sh`, and `scripts/humanize.sh` at `skills/humanize/SKILL.md:61-110` and `206-213`.
- gates_or_invariants: Important invariants are explicit: always write summaries, maintain goal tracker, avoid manual state cheating, use the native Codex Stop hook, and trust review gating (`skills/humanize/SKILL.md:167-174`). Plan structure is constrained around goal, ACs, path boundaries, milestones, and implementation notes (`skills/humanize/SKILL.md:112-151`). Option defaults include max iterations 42, Codex model `gpt-5.5:high`, review timeout 5400, full review interval 5, and flags for plan tracking, push-per-round, agent teams, yolo, and privacy (`skills/humanize/SKILL.md:73-88`).
- dependencies_and_callers: This skill coordinates sibling runtime scripts under `scripts/`, prompt templates under `prompt-template/`, hooks under `hooks/`, and state under `.humanize/`. It is hydrated by the installer through the runtime-root template mechanism; `scripts/install-skill.sh` also manages related skill variants and target-specific installation.
- edge_cases_or_failure_modes: Skip implementation mode is documented as `--skip-impl` and enters review directly (`skills/humanize/SKILL.md:67-69`, `80`). Plan tracking, push-per-round, max-iteration, and full-review behavior are delegated to the loop/hook runtime. The directory itself is a workflow contract rather than executable implementation, so misconfiguration risk is stale documentation versus script behavior.
- validation_or_tests: The skill’s documented behaviors are covered indirectly by executable specs such as `tests/workflow-first-party-flows.test.ts` for first-party RLCR graph shape, `tests/test-monitor-e2e-real.sh` for monitor behavior, `tests/test-stop-gate.sh` for stop-gate enforcement, and `tests/test-codex-hook-install.sh` for install-time skill behavior.
- skip_candidate: `no`

### H2_DEV-HZ-053 `file` `scripts/bitlesson-init.sh`
- cursor: `[_]`
- core_role: Initializes the persistent BitLesson knowledge-base file used by RLCR rounds. It is a workflow setup helper with path validation and idempotent creation semantics.
- algorithmic_behavior: The script parses `--project-root`, `--template`, and optional `--bitlesson-relpath`; defaults the relative path to `.humanize/bitlesson.md`; validates required arguments and filesystem types; resolves the project root physically; refuses unsafe relative paths; copies the template only when the target file does not exist; then prints the resolved target path (`scripts/bitlesson-init.sh:5-88`).
- inputs_outputs_state: Inputs are an existing project root directory, an existing template file, and a safe relative BitLesson path (`scripts/bitlesson-init.sh:18-46`, `60-73`). Output is stdout containing the absolute BitLesson file path (`scripts/bitlesson-init.sh:88`). State transition is missing target file -> parent directory created -> template copied; existing regular file -> no overwrite (`scripts/bitlesson-init.sh:78-86`).
- gates_or_invariants: It rejects missing `--project-root`, missing `--template`, non-directory roots, non-file templates, absolute `--bitlesson-relpath`, and `..` path traversal (`scripts/bitlesson-init.sh:48-73`). It also rejects a preexisting non-regular target such as a directory or symlink target that is not a regular file (`scripts/bitlesson-init.sh:78-81`). The no-overwrite invariant protects accumulated lesson history (`scripts/bitlesson-init.sh:12-14`, `83-86`).
- dependencies_and_callers: `scripts/setup-rlcr-loop.sh:865-871` invokes it during RLCR setup with project root, `templates/bitlesson.md`, and `.humanize/bitlesson.md`, suppressing stdout. Hook prompt generation later references `BITLESSON_FILE`; drift and next-round prompts add BitLesson selector requirements when enabled (`hooks/loop-codex-stop-hook.sh:2036-2049`, `2089-2095`).
- edge_cases_or_failure_modes: Empty option values are caught after parsing. A caller that passes an option without a value can hit `set -u` style shell issues because parsing uses `${2:-}` then shifts by 2, so malformed argv is a usage/error path. A target path with an existing directory fails. It does not validate template contents, only existence.
- validation_or_tests: Behavior is covered through setup-level tests that exercise RLCR initialization and BitLesson routing, including `tests/test-bitlesson-select-routing.sh`, `tests/test-bitlesson-validate-delta.sh`, and setup robustness tests. The direct caller line is `scripts/setup-rlcr-loop.sh:868`.
- skip_candidate: `no`

### H2_DEV-HZ-083 `file` `tests/storage.test.ts`
- cursor: `[_]`
- core_role: Executable specification for persistent hub run storage. It verifies state-dir resolution, session metadata, run JSONL logging, output event persistence, schema versioning, and recovery of abandoned running runs.
- algorithmic_behavior: The test defines a fake backend that emits stdout/stderr events and returns a successful `AgentResult` (`tests/storage.test.ts:24-53`). It creates an `AgentRunCoordinator` with a `FileRunStore`, starts a run, waits for completion, then verifies persisted run/session records can be reloaded from a fresh store (`tests/storage.test.ts:66-116`). A second case records a run as `running` and expects a later session load to classify it as `interrupted` (`tests/storage.test.ts:118-159`).
- inputs_outputs_state: Inputs are temporary state directories, deterministic session IDs, deterministic clocks, fake agent requests, and manually recorded run records. Outputs are `sessions/<session>/session.json` and per-run JSONL logs under `sessions/<session>/runs/`. The underlying implementation defines `RUN_SCHEMA_VERSION = "humanize2.run.v1"` and `SESSION_SCHEMA_VERSION = "humanize2.session.v1"` (`src/hub/storage.ts:9-10`), writes `session.json` on create (`src/hub/storage.ts:62-66`), appends run log entries (`src/hub/storage.ts:109-119`, `161-164`), and reconstructs output events from `run.created`, `run.output`, and `run.finished` entries (`src/hub/storage.ts:132-158`).
- gates_or_invariants: `resolveStateDir` priority is `HUMANIZE2_STATE_DIR`, then `HUMANIZE2_CACHE_DIR`, then `$HOME/.h2/cache` (`tests/storage.test.ts:60-64`, `src/hub/storage.ts:31-40`). Created runs must carry schema version, session ID, derived short name, default timeout `21_600_000`, project path, and ordered output events (`tests/storage.test.ts:79-90`). Unfinished `running` records must not reload as still running; they become `interrupted` with error `Hub session ended before this run finished` (`tests/storage.test.ts:152-157`, `src/hub/storage.ts:94-104`).
- dependencies_and_callers: Depends on `AgentRunCoordinator`, `FileRunStore`, `HumanizeService`, and agent backend interfaces (`tests/storage.test.ts:7-10`). `AgentRunCoordinator.createRun` records `run.created` before async execution (`src/hub/runs.ts:163-199`), and hub startup loads persisted runs via `store.loadRuns()` as shown by references in `src/hub-server.ts`.
- edge_cases_or_failure_modes: Missing `sessions` directory returns no runs (`src/hub/storage.ts:88-92`). Session directories without `session.json` are skipped for session listing (`src/hub/storage.ts:75-84`). Empty JSONL lines are ignored (`src/hub/storage.ts:132-136`). The test does not cover corrupt JSON, duplicate run logs, or write failure, so those remain residual risks.
- validation_or_tests: This file is itself the validation. It also complements `tests/workflow-storage.test.ts`, which applies similar persistence expectations to workflow runs.
- skip_candidate: `no`

### H2_DEV-HZ-113 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: True end-to-end executable specification for the real `_humanize_monitor_codex` monitor function. It validates live behavior when RLCR state/log directories disappear or monitor processes receive interrupts.
- algorithmic_behavior: The script creates isolated test projects with `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, fake cache logs, and shimmed terminal functions, then sources `scripts/humanize.sh` and calls `_humanize_monitor_codex` directly in bash and zsh (`tests/test-monitor-e2e-real.sh:56-158`, `233-327`, `377-479`, `555-645`). It deletes `.humanize/rlcr` or sends SIGINT, waits bounded loops, then inspects captured output and exit behavior (`tests/test-monitor-e2e-real.sh:164-227`, `332-370`, `492-549`, `649-684`).
- inputs_outputs_state: Inputs are synthetic RLCR state files with fields like `current_round`, `max_iterations`, `codex_model`, `plan_file`, `review_started`, and matching cache log paths (`tests/test-monitor-e2e-real.sh:65-78`, `246-259`, `387-400`, `568-581`). Outputs are PASS/FAIL counters and captured monitor stdout/stderr. The monitor reads `.humanize/rlcr`, state files, goal tracker, git status, and cache logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/<session>` (`scripts/humanize.sh:266-375`, `382-405`, `449-472`).
- gates_or_invariants: Required invariants are clean exit with `Monitoring stopped:`, user-friendly deletion reason, no zsh/bash glob errors, terminal scroll-region reset, cleanup message, and exit code 0 for graceful deletion (`tests/test-monitor-e2e-real.sh:187-227`, `353-370`). SIGINT paths must show cleanup/interrupt/signal indication or clean exit code and no glob errors (`tests/test-monitor-e2e-real.sh:532-549`, `672-684`). The monitor implementation sets zsh `ksharrays`, uses `find` instead of unsafe globs, validates run/review log round ordering, and resets scroll region with `printf "\033[r"` (`scripts/humanize.sh:261-264`, `338-372`, `706-712`).
- dependencies_and_callers: Depends on `scripts/humanize.sh` real monitor, shared helpers such as `monitor_find_latest_session`, `monitor_find_state_file`, `monitor_get_file_size`, `humanize_parse_goal_tracker`, and terminal commands `tput`, `clear`, `tail`, `find`, `grep`, `sed`. The monitor is exposed through the `humanize monitor rlcr` command path (`skills/humanize/SKILL.md:206-213`) and command dispatch in `scripts/humanize.sh`.
- edge_cases_or_failure_modes: Handles missing `.humanize/rlcr` at startup as an error (`scripts/humanize.sh:272-277`), deletion during main loop as graceful stop with exit 0 (`scripts/humanize.sh:838-844`), deletion while waiting for logs (`scripts/humanize.sh:900-908`), session directory deletion by switching to latest session or waiting (`scripts/humanize.sh:955-980`), too-small terminal by displaying a centered message until resize (`scripts/humanize.sh:661-688`, `815-823`), and SIGINT/SIGTERM cleanup in bash/zsh (`scripts/humanize.sh:736-796`). Bash SIGINT delivery is treated pragmatically: forced SIGTERM still counts as handled if monitor ran (`tests/test-monitor-e2e-real.sh:505-518`).
- validation_or_tests: This file runs four tests and exits 0 only when no failures occur, printing verified claims for clean exit, no glob errors, terminal restoration, bash/zsh support, and SIGINT handling (`tests/test-monitor-e2e-real.sh:691-724`).
- skip_candidate: `no`

### H2_DEV-HZ-143 `file` `tests/workflow-first-party-flows.test.ts`
- cursor: `[_]`
- core_role: Executable specification for bundled first-party workflow cartridges and their runtime execution through `WorkflowCoordinator`. It validates graph shape, routing, loops, agent backend override, artifact-driven progression, human waits, and run completion.
- algorithmic_behavior: The test loads HTML cartridges for `gen-idea`, `gen-plan`, `rlcr`, and `refine-plan`, then inspects parsed agent, loop, branch, and template nodes (`tests/workflow-first-party-flows.test.ts:12-93`). It then starts workflows, delivers artifacts in expected order, waits for agent-start events, and verifies terminal run status, completed vertices, waiting state, and final artifacts (`tests/workflow-first-party-flows.test.ts:95-387`).
- inputs_outputs_state: Inputs are `flow/*/workflow.html` cartridges, fake `codex`/`claude` backends, delivered artifacts such as `idea-input`, `plan-draft`, `plan-convergence-verdict`, `plan-human-decision`, and `comment-human-decision`. Outputs are workflow run records with `status`, `waitingFor`, `events`, `artifacts`, and `nodeRunIds`. `WorkflowCoordinator.start` seeds the frontier with the graph start vertex and records `workflow.started` (`src/workflows/coordinator.ts:300-351`). `deliverArtifact` records artifacts, validation status, duplicate delivery warnings, and wakes artifact/predicate waiters (`src/workflows/coordinator.ts:452-502`).
- gates_or_invariants: `gen-idea` must have exactly six orthogonal explorer agents and read-only/evidence/synthesis prompt content (`tests/workflow-first-party-flows.test.ts:12-33`; cartridge agents at `flow/gen-idea/workflow.html:47-62`). `gen-plan` must have relevance, first-pass, candidate, convergence, final writer nodes; convergence loop max 3; `revise` loops and `converged` routes to final writer, with default human decision (`tests/workflow-first-party-flows.test.ts:35-60`; `flow/gen-plan/workflow.html:46-54`). `rlcr` must model plan compliance, quiz, builder, reviewer, code-review, finalizer; implementation and code-review loops max 42; branch values `revise` and `complete` must route correctly (`tests/workflow-first-party-flows.test.ts:62-93`; `flow/rlcr/workflow.html:62-97`). `refine-plan` must route through human decision and produce both `refined-plan` and `refinement-qa` (`tests/workflow-first-party-flows.test.ts:328-387`; `flow/refine-plan/workflow.html:26-30`).
- dependencies_and_callers: Depends on `WorkflowCoordinator`, `AgentRunCoordinator`, `HumanizeService`, workflow parser/types, and first-party HTML cartridge files. Runtime dependencies include graph compilation, manifest validation, expression evaluation, schema validation, and agent run coordination (`src/workflows/coordinator.ts:12-16`, `187-199`, `202-260`). Agent override behavior is guarded by manifest and authorized tool checks (`src/workflows/coordinator.ts:354-360`).
- edge_cases_or_failure_modes: Human-decision branch must put the run into `waitingFor` until a human artifact arrives (`tests/workflow-first-party-flows.test.ts:237-247`; `src/workflows/coordinator.ts:540-560`). Branches without matched values route to default or throw if no default exists (`src/workflows/coordinator.ts:754-777`). Loops stop at `max` and emit `loop.continue`/`loop.exit` (`src/workflows/coordinator.ts:711-751`). Agent vertices fail if backend status is not succeeded or expected artifacts remain missing after soft retries (`src/workflows/coordinator.ts:833-920`). The tests cover happy and decision paths but not malformed cartridges or schema mismatch failures.
- validation_or_tests: This file is the validation. Helper functions assert node presence, template presence, deterministic IDs/time, fake backend execution, and bounded waits (`tests/workflow-first-party-flows.test.ts:390-513`).
- skip_candidate: `no`

### H2_DEV-HZ-173 `file` `prompt-template/block/git-tracked-humanize.md`
- cursor: `[_]`
- core_role: Blocking prompt template for the invariant that `.humanize/` loop state must not be tracked, staged, or committed.
- algorithmic_behavior: The template emits a stop message titled `Tracked Humanize State Blocked`, explains that tracked or staged `.humanize/` files are local loop state, instructs removal from the index with `git rm --cached -r .humanize`, and forbids force-adding or committing RLCR trackers, summaries, contracts, and cancel/finalize markers (`prompt-template/block/git-tracked-humanize.md:1-19`).
- inputs_outputs_state: Input is the detection condition from hook/runtime logic, not template parameters. Output is a markdown block used as the stop/gate message. The hook helper `git_tracked_humanize_blocked_message` loads this template with a fallback copy (`hooks/lib/loop-common.sh:1447-1470`).
- gates_or_invariants: The core invariant is that `.humanize/` is local state and must remain outside version control (`prompt-template/block/git-tracked-humanize.md:3-6`). The required fix preserves real project staged files while removing only Humanize state from the index (`prompt-template/block/git-tracked-humanize.md:7-14`). The important section forbids `git add -f` on Humanize state and forbids committing RLCR internal files (`prompt-template/block/git-tracked-humanize.md:16-19`).
- dependencies_and_callers: Called through `hooks/lib/loop-common.sh`, which detects tracked/staged `.humanize` via git status/ls-files and then renders this block (`hooks/lib/loop-common.sh:1440-1470`). `scripts/rlcr-stop-gate.sh` reaches the same hook logic through the gate wrapper.
- edge_cases_or_failure_modes: It deliberately targets `.humanize/`, not similarly named paths. `tests/test-stop-gate.sh` includes a negative case for `.humanize-backup` not being confused with loop state, and positive blocking for actual tracked Humanize state.
- validation_or_tests: `tests/test-stop-gate.sh:133-145` asserts `rlcr-stop-gate` exits 10 and reports `Tracked Humanize State Blocked` when `.humanize` state is tracked. The same test suite verifies `.humanize-backup` does not trigger this reason.
- skip_candidate: `no`

### H2_DEV-HZ-203 `file` `prompt-template/claude/drift-replan-prompt.md`
- cursor: `[_]`
- core_role: Recovery-mode prompt template used when recent implementation rounds stall or regress the mainline. It changes the next round’s algorithm from normal issue clearing to explicit mainline re-anchoring and contract rewrite.
- algorithmic_behavior: The prompt declares drift recovery mode, injects `{{STALL_COUNT}}` and `{{LAST_MAINLINE_VERDICT}}`, anchors the worker on `@{{PLAN_FILE}}`, requires rereading the plan, goal tracker, recent summaries/reviews, and rewriting `@{{ROUND_CONTRACT_FILE}}` before coding (`prompt-template/claude/drift-replan-prompt.md:1-33`). It mandates a recovery contract with exactly one mainline objective, 1-2 target ACs, drift root cause, blocking versus queued issue separation, and success criteria to restore `ADVANCED` (`prompt-template/claude/drift-replan-prompt.md:25-31`).
- inputs_outputs_state: Inputs are rendered variables `PLAN_FILE`, `GOAL_TRACKER_FILE`, `ROUND_CONTRACT_FILE`, `STALL_COUNT`, `LAST_MAINLINE_VERDICT`, and `REVIEW_CONTENT`. Output is the next `round-N-prompt.md` used by Claude/Kimi/Codex-style agents. The stop hook renders this template when `DRIFT_REPLAN_REQUIRED=true` (`hooks/loop-codex-stop-hook.sh:2067-2076`).
- gates_or_invariants: The template forbids normal issue-clearing behavior during recovery (`prompt-template/claude/drift-replan-prompt.md:10`), forbids implementation before the recovery contract exists (`prompt-template/claude/drift-replan-prompt.md:33`), requires Task system lane tags `[mainline]`, `[blocking]`, and `[queued]` (`prompt-template/claude/drift-replan-prompt.md:35-47`), and requires goal tracker updates without changing the immutable section (`prompt-template/claude/drift-replan-prompt.md:55-62`). Guardrails prohibit queued cleanup taking over, scope broadening, silent plan changes, and vague inability reports (`prompt-template/claude/drift-replan-prompt.md:63-68`).
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh` during next-round prompt generation. If BitLesson enforcement is also required and the drift template lacks selector text, the hook appends a required BitLesson selection section (`hooks/loop-codex-stop-hook.sh:2089-2095`). It coordinates with `goal-tracker.md`, `round-N-contract.md`, review results, and state fields such as mainline stall count and drift status.
- edge_cases_or_failure_modes: If the template is missing or render fails, the hook has a `DRIFT_REPLAN_FALLBACK` with a shorter recovery prompt (`hooks/loop-codex-stop-hook.sh:2050-2065`). A worker unable to produce a credible recovered objective must say so with concrete blockers rather than fake progress (`prompt-template/claude/drift-replan-prompt.md:68`).
- validation_or_tests: `tests/test-finalize-phase.sh:923-944` asserts that two stalled rounds create a blocked next prompt, that the generated prompt contains `Drift Recovery Mode`, and that state records `mainline_stall_count=2`, last verdict `stalled`, and `drift_status=replan_required`.
- skip_candidate: `no`

### H2_DEV-HZ-233 `file` `skills/humanize-rlcr/SKILL-kimi.md`
- cursor: `[_]`
- core_role: Kimi-specific RLCR skill entrypoint that reproduces hook-mode enforcement by explicitly calling `scripts/rlcr-stop-gate.sh` after each work round.
- algorithmic_behavior: The skill instructs Kimi environments without native hooks to start RLCR via `setup-rlcr-loop.sh`, then for each round read the generated prompt, implement changes, commit, write the required summary, run the stop-gate wrapper, and branch on gate exit code (`skills/humanize-rlcr/SKILL-kimi.md:28-64`). Exit `0` allows loop exit, `10` means RLCR logic blocked and returned instructions must be followed, and `20` is infrastructure failure that must not be faked (`skills/humanize-rlcr/SKILL-kimi.md:60-64`).
- inputs_outputs_state: Inputs are `/flow:humanize-rlcr` arguments, optional plan path or `--skip-impl`, runtime root hydration, generated `round-N-prompt.md` or finalize prompts, summary files, optional `CLAUDE_SESSION_ID`, and optional `CLAUDE_TRANSCRIPT_PATH` (`skills/humanize-rlcr/SKILL-kimi.md:18-27`, `44-58`, `91-112`). Outputs/state are commits, `round-N-summary.md` or `finalize-summary.md`, and stop-gate results. It treats generated loop files as source of truth (`skills/humanize-rlcr/SKILL-kimi.md:84-90`).
- gates_or_invariants: It requires using `rlcr-stop-gate.sh`, not manual review logic (`skills/humanize-rlcr/SKILL-kimi.md:9-16`). Enforced behavior includes state/schema validation, branch consistency, plan integrity, incomplete task blocking, git-clean exit requirement, push-per-round blocking, summary checks, max iterations, full-alignment rounds, strict `COMPLETE`/`STOP` marker handling, review-phase transition, `[P0-9]` code-review gating, Codex review failure blocking, and open-question handling (`skills/humanize-rlcr/SKILL-kimi.md:65-83`). Critical rules forbid manually editing state, declaring completion after a blocked gate, substituting ad-hoc Codex commands, or ignoring generated prompt/review files (`skills/humanize-rlcr/SKILL-kimi.md:84-90`).
- dependencies_and_callers: Installed by `scripts/install-skill.sh` for Kimi targets: it hydrates `{{HUMANIZE_RUNTIME_ROOT}}` and overwrites `humanize-rlcr/SKILL.md` with this file (`scripts/install-skill.sh:410-426`). It depends on `scripts/setup-rlcr-loop.sh`, `scripts/rlcr-stop-gate.sh`, `hooks/loop-codex-stop-hook.sh`, and `scripts/cancel-rlcr-loop.sh`.
- edge_cases_or_failure_modes: Setup nonzero is a hard stop (`skills/humanize-rlcr/SKILL-kimi.md:38`). Gate exit `20` is an infrastructure error and must be reported, not bypassed (`skills/humanize-rlcr/SKILL-kimi.md:63`). Review-only mode is supported with `--skip-impl` (`skills/humanize-rlcr/SKILL-kimi.md:120-121`). The skill is intentionally different from native Stop-hook instructions because Kimi lacks that hook path.
- validation_or_tests: `tests/test-codex-hook-install.sh:500-528` verifies Kimi install produces `humanize-rlcr/SKILL.md`, includes `rlcr-stop-gate.sh`, avoids native Stop-hook wording, and documents current default `gpt-5.5:high`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8/8 section headings above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`