# agent_15 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-015 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Claude-facing prompt templates that drive RLCR continuation, review repair, drift recovery, finalize, methodology-analysis, agent-team delegation, goal-tracker reconciliation, open-question handling, and push/summary obligations.
- algorithmic_behavior: The stop hook renders these templates through `load_and_render_safe` to produce blocking hook responses that become the next Claude instruction payload. `next-round-prompt.md` requires re-reading plan and goal tracker, writing a round contract, lane-tagging tasks as `mainline`, `blocking`, or `queued`, and using BitLesson selection before work; see [prompt-template/claude/next-round-prompt.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/prompt-template/claude/next-round-prompt.md:12). `drift-replan-prompt.md` switches the next round into recovery mode after repeated stalled/regressed mainline verdicts and requires a recovered objective before implementation; see [prompt-template/claude/drift-replan-prompt.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/prompt-template/claude/drift-replan-prompt.md:3). `review-phase-prompt.md` constrains work after code review findings and states that `COMPLETE` has no effect in review phase; see [prompt-template/claude/review-phase-prompt.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/prompt-template/claude/review-phase-prompt.md:55). `finalize-phase-prompt.md` and `finalize-phase-skipped-prompt.md` define post-review simplification-only work and require writing `finalize-summary.md`; see [prompt-template/claude/finalize-phase-prompt.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/prompt-template/claude/finalize-phase-prompt.md:41).
- inputs_outputs_state: Inputs are template variables injected by hooks: `PLAN_FILE`, `GOAL_TRACKER_FILE`, `ROUND_CONTRACT_FILE`, `REVIEW_CONTENT`, `CURRENT_ROUND`, `STALL_COUNT`, `LAST_MAINLINE_VERDICT`, `FINALIZE_SUMMARY_FILE`, `BASE_BRANCH`, `START_BRANCH`, and related paths. Outputs are rendered Markdown prompts placed in loop files such as `round-N-prompt.md` or returned as JSON `reason` text by the stop hook. State effects are indirect: the prompt content tells Claude what artifacts must exist before the next stop attempt.
- gates_or_invariants: Templates encode invariants rather than enforcing them directly: single mainline objective per round, queued work must not replace mainline work, review phase is fix-only, finalize phase must preserve functionality and tests, agent-teams mode forbids the team leader from implementing directly, and methodology analysis must sanitize project-specific details. `next-round-footer.md` forbids lying, editing loop state files, or invoking cancel as an exit bypass; see [prompt-template/claude/next-round-footer.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/prompt-template/claude/next-round-footer.md:4).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` renders `finalize-phase-prompt.md` and `finalize-phase-skipped-prompt.md` in `enter_finalize_phase` at lines 1370 and 1398, renders `review-phase-prompt.md` at line 1563, renders `drift-replan-prompt.md` or `next-round-prompt.md` at lines 2068 and 2078, appends `next-round-footer.md` at line 2175, `push-every-round-note.md` at line 2181, `goal-tracker-update-request.md` at line 2189, `open-question-notice.md` at line 2141, and agent-team continuation/core prompts at lines 2198-2204. `methodology-analysis-prompt.md` is tied to `hooks/lib/methodology-analysis.sh`.
- edge_cases_or_failure_modes: Missing templates are not fatal where the hook supplies fallback strings; this keeps the state machine running but can degrade prompt specificity. `open-question-notice.md` is inserted only when `ASK_CODEX_QUESTION=true` and a short line contains `Open Question`, so verbose or differently phrased questions may not trigger the notice. Agent-team prompts warn about silent overwrites from shared file ownership, which is a coordination failure mode rather than a shell-enforced gate.
- validation_or_tests: Template wiring is indirectly covered by finalize and drift tests in [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:591), [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:923), and [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:936), plus template reference/comprehensive tests listed under `tests/test-template-references.sh` and `tests/test-templates-comprehensive.sh`.
- skip_candidate: `no`

### H2_DEV-HZ-045 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Main RLCR stop-hook state machine. It intercepts Claude exit attempts, validates loop state and artifacts, runs Codex summary/code review, advances rounds, enters review/finalize/methodology terminal phases, and emits Claude Code hook JSON block/allow decisions.
- algorithmic_behavior: Reads hook JSON from stdin, resolves the project and active loop by session-aware lookup, short-circuits for background tasks, resolves `state.md`, `finalize-state.md`, or `methodology-analysis-state.md`, and parses frontmatter. Active phase detection is at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/loop-codex-stop-hook.sh:91). It validates model/effort values, required state fields, schema fields, branch consistency, plan backup integrity, transcript task completion, git cleanliness, large-file limits, summary presence, round contract presence, BitLesson delta, goal-tracker initialization, and max-iteration budget before review. Normal implementation phase builds a Codex prompt, runs `codex exec`, expects a review result, reads the last non-empty marker line, and either enters review/finalize, stops, or creates the next round prompt. Review phase runs `codex review --base` and finalizes only when no `[P0-9]` issues are detected.
- inputs_outputs_state: Inputs include stdin hook JSON, `.humanize/rlcr/<timestamp>/state.md`, `finalize-state.md`, `round-N-summary.md`, `round-N-contract.md`, `goal-tracker.md`, plan backup, git status, Codex CLI output, and templates. Outputs include hook JSON with `decision: block` for required continuation, prompt files, summary stubs, Codex review prompt/result files, cache debug files under `XDG_CACHE_HOME`, and state renames such as `state.md` to `finalize-state.md`, `complete-state.md`, `maxiter-state.md`, or `stop-state.md`.
- gates_or_invariants: Fails closed when state fields are missing or corrupted, when branch differs from `start_branch`, when tracked plan files changed, when todos are incomplete, when git status fails, when tracked `.humanize` state is present, when non-humanize uncommitted changes exist, when `push_every_round` has unpushed commits, or when summaries/contracts are missing. It requires a `Mainline Progress Verdict` in implementation reviews unless the marker is `STOP`; missing verdict blocks without advancing state at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/loop-codex-stop-hook.sh:1835). Finalize phase skips Codex but still requires prior gates and `finalize-summary.md`, then renames to `complete-state.md`; see [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/loop-codex-stop-hook.sh:988).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, `hooks/lib/loop-bg-tasks.sh` through loop-common, `scripts/portable-timeout.sh`, `hooks/lib/methodology-analysis.sh`, `hooks/check-todos-from-transcript.py`, `scripts/bitlesson-validate-delta.sh`, `jq`, `git`, `python3`, `codex`, and prompt templates under `prompt-template/claude`, `prompt-template/codex`, and `prompt-template/block`. It is called by Claude Code stop-hook configuration, not by normal shell users; validators also protect against direct execution.
- edge_cases_or_failure_modes: If `parse_state_file` tolerantly defaults missing fields, the hook separately checks raw frontmatter to prevent accidental round zero behavior. If Codex writes review output to stdout rather than the expected file, the hook copies stdout into the result file; if no file or empty file remains, it blocks with debug paths. `codex review` failures or empty stdout are hard blockers and do not skip to finalize. Repeated `STALLED` or `REGRESSED` verdicts set `replan_required` at two rounds and stop at three rounds. The hook caches `git status`; git timeouts can leave `index.lock`, so `cleanup_stale_index_lock` removes it before asking Claude to commit.
- validation_or_tests: [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:486) verifies finalize summary/git/todo gates and no Codex invocation during finalize; [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:591) verifies COMPLETE leads to finalize prompt/state; [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:633) and [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:694) verify code-review failure/empty output block; [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:895) verifies drift recovery and circuit breaker behavior.
- skip_candidate: `no`

### H2_DEV-HZ-075 `file` `tests/dev-client.test.ts`
- cursor: `[_]`
- core_role: Executable specification for the development MCP client’s tool argument parser, specifically whether tool-call arguments can be supplied inline or through an `@file` indirection.
- algorithmic_behavior: Uses Vitest to call `parseToolArguments` with a raw JSON object string and expects the object to be parsed unchanged at [tests/dev-client.test.ts](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/dev-client.test.ts:10). It then creates a temporary `arguments.json`, passes `@<path>`, and expects the file contents to be parsed as JSON at [tests/dev-client.test.ts](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/dev-client.test.ts:17).
- inputs_outputs_state: Inputs are the raw argument string and, for the second test, a temporary JSON file. Output is a resolved `Record<string, unknown>` object. Test-local state is a temp directory under `tmpdir()` that is removed in a `finally` block.
- gates_or_invariants: The tested implementation in [src/dev-client.ts](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/src/dev-client.ts:28) treats a leading `@` as file indirection, otherwise parses the raw string. It rejects JSON values that are `null`, arrays, or primitives by throwing `Tool arguments must be a JSON object` at [src/dev-client.ts](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/src/dev-client.ts:34), although this assigned test file only covers success paths.
- dependencies_and_callers: Depends on Node `fs/promises`, `path`, `os`, Vitest, and `../src/dev-client.js`. The parser feeds `callHumanizeTool`, which sends parsed arguments as the JSON-RPC `tools/call.arguments` object.
- edge_cases_or_failure_modes: No coverage here for invalid JSON, missing `@file`, non-object JSON, arrays, or paths that intentionally start with `@` but should be treated literally. Because `@` detection is unconditional on the first character, a raw JSON string cannot begin with `@` unless it is intended as a file path.
- validation_or_tests: The file is itself the validation. It asserts exact object equality for inline JSON and file-backed JSON and cleans up temporary files after the `@file` case.
- skip_candidate: `no`

### H2_DEV-HZ-105 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Shell integration specification for finalize-phase behavior across the RLCR stop hook, loop-common helpers, and read/write/edit/bash/plan validators.
- algorithmic_behavior: Builds an isolated git repo and loop directory, writes representative `state.md`, plan backup, goal tracker, and round contract fixtures, then exercises helper functions, active-loop detection, validator protections, stop-hook finalize completion, COMPLETE handling, review failure handling, incomplete todo blocking, normal round continuation, and mainline drift state transitions. The test setup writes the state fixture at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:218) and mocks `codex` behavior at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:53).
- inputs_outputs_state: Inputs are generated hook JSON payloads, mock Codex executables, temporary transcripts, and loop files. Outputs are pass/fail counters, hook JSON output captures, generated prompt/result files, cache logs, and state-file transitions. It mutates only the temp test repo and exits with `TESTS_FAILED`.
- gates_or_invariants: Specifies that `finalize-state.md` is active but `complete-state.md` is terminal, `finalize-summary.md` is writable, `finalize-state.md` is protected from Write/Edit/Bash/mv/cp, historical round contracts are blocked during finalize, finalize still requires clean git and completed todos, and finalize completion must not invoke Codex. It also specifies that COMPLETE at max iterations creates `maxiter-state.md`, not finalize, and that review failure preserves `state.md` with `review_started: true`.
- dependencies_and_callers: Sources [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/lib/loop-common.sh:48), calls `hooks/loop-codex-stop-hook.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-read-validator.sh`, and `loop-plan-file-validator.sh`, and relies on git plus mocked `codex`.
- edge_cases_or_failure_modes: Tests review failure with non-zero exit at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:633), empty review stdout at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:694), missing mainline verdict preserving prior drift state at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:950), two-round drift replan at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:895), and third-round circuit breaker at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:998).
- validation_or_tests: This file is a comprehensive validation harness for finalize-phase contracts. It is not a unit-only test; it verifies cross-script behavior and state-file side effects such as `state.md` to `finalize-state.md` at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:597) and `finalize-state.md` to `complete-state.md` at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:551).
- skip_candidate: `no`

### H2_DEV-HZ-135 `file` `tests/test-validate-gen-idea-io.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/validate-gen-idea-io.sh`, focused on output `.md` enforcement, derived companion `.directions.json` paths, collision prevention, and inline idea/path disambiguation.
- algorithmic_behavior: Creates a mock git repo and minimal plugin template tree, then runs the validator inside the repo through `run_validate` at [tests/test-validate-gen-idea-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-validate-gen-idea-io.sh:38). Positive cases expect `VALIDATION_SUCCESS` and `DIRECTIONS_JSON_FILE` for `foo.md` and `subdir/bar.md`. Negative cases assert no `.md` suffix and `.txt` return exit 6, companion JSON collision returns exit 8, existing output draft returns exit 4, missing idea returns exit 1, and slash-containing ideas such as `undo/redo` and `CI/CD` are inline text.
- inputs_outputs_state: Inputs are positional idea text, `--output` paths, a mock `CLAUDE_PLUGIN_ROOT`, and created collision files. Outputs are exit codes and stdout fields from the validator, including `OUTPUT_FILE`, `DIRECTIONS_JSON_FILE`, `SLUG`, `TEMPLATE_FILE`, and inline idea sentinel body. Test state is under a helper-managed temp directory.
- gates_or_invariants: Specifies that every explicit output must end in `.md` so the validator can derive `${OUTPUT_FILE%.md}.directions.json`; see implementation at [scripts/validate-gen-idea-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/scripts/validate-gen-idea-io.sh:152). Existing output is checked before companion collision in implementation lines 172-181. Slash characters alone must not trigger path mode; only whitespace-free missing inputs ending in `.md` become path-like failures, as documented at [scripts/validate-gen-idea-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/scripts/validate-gen-idea-io.sh:90).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, git initialization helpers, `scripts/validate-gen-idea-io.sh`, and a valid `prompt-template/idea/gen-idea-template.md` through `CLAUDE_PLUGIN_ROOT`.
- edge_cases_or_failure_modes: The tests document a prior regression where slash-only inline ideas were misclassified as missing paths. The implementation limitation remains that a nonexistent path containing whitespace and not ending `.md` is treated as inline text; this is noted in the validator comments, but not asserted here. It does not test unreadable files, non-empty file inputs, `--n` bounds, output permission failures, or missing template exit 7.
- validation_or_tests: The file is itself the validation suite for this contract. It checks success stdout shape at [tests/test-validate-gen-idea-io.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-validate-gen-idea-io.sh:49), subdir derivation at line 66, companion collision at line 104, and slash-inline regression cases at lines 137 and 150.
- skip_candidate: `no`

### H2_DEV-HZ-165 `file` `prompt-template/block/finalize-contract-access.md`
- cursor: `[_]`
- core_role: Block-message template for the finalize-phase invariant that there is no active `round-N-contract.md` once the loop has entered Finalize Phase.
- algorithmic_behavior: Renders a concise denial message using `{{ACTION}}`, telling Claude not to read or write historical round contracts and to use `finalize-summary.md` or `goal-tracker.md` instead. Template lines are at [prompt-template/block/finalize-contract-access.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/prompt-template/block/finalize-contract-access.md:1).
- inputs_outputs_state: Input is an action phrase such as `read` or `write to`; output is Markdown surfaced on stderr by validators or in hook block reasons. It does not mutate state.
- gates_or_invariants: The invariant is enforced by callers: `finalize_contract_blocked_message` in loop-common loads this template at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/lib/loop-common.sh:882). Write validator blocks contracts in finalize at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/loop-write-validator.sh:230), read validator blocks them at [hooks/loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/loop-read-validator.sh:218), and finalize tests assert both.
- dependencies_and_callers: Called through `load_and_render_safe` from `finalize_contract_blocked_message`, then by `loop-read-validator.sh`, `loop-write-validator.sh`, and related edit/bash validation paths covered by finalize tests.
- edge_cases_or_failure_modes: The template is advisory and relies on the caller to identify finalize phase and round-contract paths correctly. If rendered with an awkward `ACTION`, line 5 may be grammatically odd but still blocks. It does not include the attempted or correct path, only policy guidance.
- validation_or_tests: [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:410), [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:434), and [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:1080) verify finalize-phase contract access is blocked and mentions contract.
- skip_candidate: `no`

### H2_DEV-HZ-195 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Block-message template for enforcing that RLCR round files are accessed from the active loop directory, not from stale or incorrect directories.
- algorithmic_behavior: Renders attempted action/path and the correct path using `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}`; see [prompt-template/block/wrong-directory-path.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/prompt-template/block/wrong-directory-path.md:1). It is loaded when validators compare canonicalized attempted paths against the active loop’s expected path.
- inputs_outputs_state: Inputs are attempted file path, correct active-loop path, and action verb. Output is Markdown on stderr from validators. It has no direct state transition.
- gates_or_invariants: Read validator computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"` and blocks if prefix-canonical attempted/correct paths differ at [hooks/loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/loop-read-validator.sh:305). Write validator applies the same parent-prefix canonicalization to avoid symlink leaf bypasses at [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/loop-write-validator.sh:345). The invariant is that round files must be read/written only at the corresponding file in the active loop directory.
- dependencies_and_callers: Called by `hooks/loop-read-validator.sh` and `hooks/loop-write-validator.sh` via `load_and_render_safe`; `loop-common.sh` supplies canonicalization helpers and active-loop discovery.
- edge_cases_or_failure_modes: The template line 6 says `If you need this file, use: cat {{FILE_PATH}}`, which repeats the attempted path rather than `{{CORRECT_PATH}}`. Since callers’ fallback text uses the correct path in prose, this template may produce confusing remediation text even while the validator correctly blocks.
- validation_or_tests: Directory path validation is covered by broader plan/read/write validator tests, including `tests/test-plan-file-validation.sh`, `tests/test-plan-file-hooks.sh`, and robustness path-validation suites. Direct callers are visible at [hooks/loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/loop-read-validator.sh:314) and [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/loop-write-validator.sh:355).
- skip_candidate: `no`

### H2_DEV-HZ-225 `file` `scripts/lib/config-loader.sh`
- cursor: `[_]`
- core_role: Shared Bash config loader implementing Humanize’s layered runtime configuration merge for hooks and scripts.
- algorithmic_behavior: Source-guards itself, requires `jq`, validates each config layer as a JSON object, and merges four layers with later layers overriding earlier ones: empty object, required plugin default config, optional user config, optional project config. Project config path is `HUMANIZE_CONFIG` when set, otherwise `$project_root/.humanize/config.json`; user config comes from `XDG_CONFIG_HOME/humanize/config.json` or `$HOME/.config/humanize/config.json`; see [scripts/lib/config-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/scripts/lib/config-loader.sh:63).
- inputs_outputs_state: Inputs are `plugin_root`, `project_root`, environment variables `XDG_CONFIG_HOME`, `HOME`, and `HUMANIZE_CONFIG`, plus JSON files. Output of `load_merged_config` is merged JSON on stdout. `get_config_value` takes merged JSON plus a top-level key and emits a scalar string, stringified non-string, or empty when absent/null.
- gates_or_invariants: `jq` is mandatory. Missing or malformed required default config is fatal; missing optional user/project configs become `{}`; malformed optional configs are warned and ignored. The jq merge recursively strips nulls from objects and arrays before applying `*`, so null values do not erase lower-layer defaults; see [scripts/lib/config-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/scripts/lib/config-loader.sh:119). Required-layer failures call `exit 1` inside the subshell because Bash `set -e` is unreliable through nested function calls in this context.
- dependencies_and_callers: Used by `hooks/lib/loop-common.sh` to resolve defaults for BitLesson, Codex model/effort, and agent teams at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/lib/loop-common.sh:183), and by `scripts/bitlesson-select.sh` at [scripts/bitlesson-select.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/scripts/bitlesson-select.sh:113). Tests and command docs reference its merge semantics.
- edge_cases_or_failure_modes: If `mktemp -d` fails, the function will fail under `set -e`. Optional malformed configs can silently fall back to defaults except for stderr warnings. `get_config_value` only supports top-level keys, not dotted paths. Null stripping means users cannot intentionally set a config key to JSON null to erase a default.
- validation_or_tests: `tests/test-config-merge.sh` covers default/user/project precedence and `HUMANIZE_CONFIG`; `tests/test-config-error-handling.sh` covers missing default fatal behavior and malformed optional warning/fallback; `tests/test-unified-codex-config.sh` verifies Codex config keys are loaded through this path.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8 headings above, one per assigned item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`