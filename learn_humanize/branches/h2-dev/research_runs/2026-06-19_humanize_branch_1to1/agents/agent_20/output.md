# agent_20 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-020 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: Shared shell-library layer for Humanize monitors, configuration resolution, and provider/model routing. The directory contains four direct child modules: `monitor-common.sh`, `config-loader.sh`, `monitor-skill.sh`, and `model-router.sh`.
- algorithmic_behavior: `monitor-common.sh` provides status colors, latest RLCR session discovery, active/terminal state-file detection, YAML frontmatter reads, timestamp/string formatting, and goal-tracker counting/parsing (`scripts/lib/monitor-common.sh:32`, `scripts/lib/monitor-common.sh:40`, `scripts/lib/monitor-common.sh:154`, `scripts/lib/monitor-common.sh:201`, `scripts/lib/monitor-common.sh:265`, `scripts/lib/monitor-common.sh:300`). `config-loader.sh` validates JSON config layers, strips nulls, and deep-merges default/user/project config (`scripts/lib/config-loader.sh:24`, `scripts/lib/config-loader.sh:63`, `scripts/lib/config-loader.sh:113`, `scripts/lib/config-loader.sh:119`). `monitor-skill.sh` implements the terminal monitor for `.humanize/skill` invocations, including tool filtering, newest-first selection, status counts, cache/output file selection, `--once` summary mode, and interactive tail mode (`scripts/lib/monitor-skill.sh:18`, `scripts/lib/monitor-skill.sh:53`, `scripts/lib/monitor-skill.sh:79`, `scripts/lib/monitor-skill.sh:121`, `scripts/lib/monitor-skill.sh:169`, `scripts/lib/monitor-skill.sh:339`, `scripts/lib/monitor-skill.sh:428`). `model-router.sh` maps model names to Codex/Claude, checks required binaries, and maps `xhigh` to `high` for Claude (`scripts/lib/model-router.sh:10`, `scripts/lib/model-router.sh:32`, `scripts/lib/model-router.sh:62`).
- inputs_outputs_state: Inputs include filesystem state under `.humanize/rlcr`, `.humanize/skill`, optional global cache under `~/.cache/humanize`, default/user/project JSON config, model names, effort values, and PATH. Outputs are shell-returned strings: merged JSON, scalar config values, provider names, mapped effort names, monitor summaries, and parsed progress counts. Persistent state is read-only for this directory; it does not own durable writes.
- gates_or_invariants: Config loading requires `jq`, requires the default config to exist and be a JSON object, ignores malformed optional user/project configs with warnings, and strips null values before merge (`scripts/lib/config-loader.sh:17`, `scripts/lib/config-loader.sh:40`, `scripts/lib/config-loader.sh:52`, `scripts/lib/config-loader.sh:119`). Model routing fails closed for empty/unknown models and missing provider binaries (`scripts/lib/model-router.sh:13`, `scripts/lib/model-router.sh:28`, `scripts/lib/model-router.sh:49`). Skill monitor accepts timestamp-like invocation dirs only and treats legacy unknown-tool invocations as Codex when filtering for Codex (`scripts/lib/monitor-skill.sh:68`, `scripts/lib/monitor-skill.sh:79`).
- dependencies_and_callers: `hooks/lib/loop-common.sh` sources `config-loader.sh` and uses merged values for defaults (`hooks/lib/loop-common.sh:183`, `hooks/lib/loop-common.sh:199`, `hooks/lib/loop-common.sh:205`). `scripts/humanize.sh` sources `monitor-common.sh` and dispatches `humanize monitor skill|codex|gemini` to `_humanize_monitor_skill` (`scripts/humanize.sh:6`, `scripts/humanize.sh:1363`, `scripts/humanize.sh:1408`). `scripts/bitlesson-select.sh` sources `config-loader.sh` and `model-router.sh`, then uses both to resolve provider/model behavior (`scripts/bitlesson-select.sh:9`, `scripts/bitlesson-select.sh:113`, `scripts/bitlesson-select.sh:125`). `commands/gen-plan.md` and `commands/refine-plan.md` document config-loader as the authoritative config semantics.
- edge_cases_or_failure_modes: Missing `jq` is fatal for config loading; missing or malformed required default config is fatal, while malformed optional layers are ignored. `monitor_get_yaml_value` is simple frontmatter text extraction, not a full YAML parser. `monitor_find_state_file` prioritizes methodology analysis, then active state, then first `*-state.md`, so multiple terminal state files are not strongly ordered. `monitor-skill.sh` depends on `humanize_split_to_array` from `humanize.sh`; direct sourcing without that function would break stats splitting. Cache discovery is display-only and may fall back to project-local files when global cache is absent.
- validation_or_tests: Covered by `tests/test-config-merge.sh` for layer priority/null stripping/env override (`tests/test-config-merge.sh:3`, `tests/test-config-merge.sh:43`, `tests/test-config-merge.sh:75`, `tests/test-config-merge.sh:164`), `tests/test-model-router.sh` for provider, binary, and effort behavior (`tests/test-model-router.sh:37`, `tests/test-model-router.sh:243`, `tests/test-model-router.sh:311`), and `tests/test-skill-monitor.sh` for `--once` monitor behavior (`tests/test-skill-monitor.sh:112`, `tests/test-skill-monitor.sh:145`, `tests/test-skill-monitor.sh:207`, `tests/test-skill-monitor.sh:384`). I inspected tests but did not execute them in this read-only research pass.
- skip_candidate: `no`

### H2_DEV-HZ-050 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse write gate for the RLCR loop. It decides whether a Claude `Write` operation can touch RLCR-controlled files and blocks writes that would corrupt loop state, write stale round artifacts, mutate immutable goal-tracker content, or bypass methodology-analysis restrictions.
- algorithmic_behavior: Reads hook JSON from stdin, validates structure and nesting, ignores non-`Write` tools, requires `tool_input.file_path`, and extracts `session_id` for session-scoped active-loop lookup (`hooks/loop-write-validator.sh:24`, `hooks/loop-write-validator.sh:27`, `hooks/loop-write-validator.sh:32`, `hooks/loop-write-validator.sh:38`, `hooks/loop-write-validator.sh:43`, `hooks/loop-write-validator.sh:51`). It blocks non-allowlisted todos writes, blocks all prompt writes, restricts methodology-analysis phase writes to `methodology-analysis-report.md` and `methodology-analysis-done.md`, computes file classes, parses the active state strictly, blocks direct state-file writes, validates finalize summary/contract behavior, validates goal-tracker path and immutable-section preservation, blocks summary/contract files outside `.humanize/rlcr`, enforces current round number, and checks canonicalized target directory (`hooks/loop-write-validator.sh:57`, `hooks/loop-write-validator.sh:67`, `hooks/loop-write-validator.sh:88`, `hooks/loop-write-validator.sh:149`, `hooks/loop-write-validator.sh:191`, `hooks/loop-write-validator.sh:202`, `hooks/loop-write-validator.sh:223`, `hooks/loop-write-validator.sh:231`, `hooks/loop-write-validator.sh:253`, `hooks/loop-write-validator.sh:283`, `hooks/loop-write-validator.sh:318`, `hooks/loop-write-validator.sh:352`).
- inputs_outputs_state: Inputs are hook JSON, `tool_input.file_path`, optional `tool_input.content`, `session_id`, project root, `.humanize/rlcr/<session>` files, and state frontmatter fields such as `current_round`. Outputs are exit status and stderr block messages: exit `0` allow, exit `1` malformed/unsafe input or malformed state, exit `2` policy block. It does not mutate state; it preserves RLCR state by refusing dangerous writes.
- gates_or_invariants: JSON must be valid UTF-8/object-like input accepted by shared helpers; depth over 30 is blocked (`hooks/lib/loop-common.sh:91`, `hooks/lib/loop-common.sh:151`). Active loop lookup is session-aware and intentionally does not fall back to other sessions for validators (`hooks/lib/loop-common.sh:332`, `hooks/lib/loop-common.sh:399`). State parsing is fail-closed for missing frontmatter/required fields/non-numeric rounds (`hooks/lib/loop-common.sh:533`). Goal-tracker edits after round 0 must preserve the immutable section (`hooks/loop-write-validator.sh:264`, `hooks/lib/loop-common.sh:956`). Path comparisons use parent-prefix canonicalization for final directory validation to avoid symlink-leaf escape (`hooks/loop-write-validator.sh:345`, `hooks/loop-write-validator.sh:352`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`hooks/loop-write-validator.sh:17`), which in turn sources `project-root.sh`, `config-loader.sh`, and the template loader (`hooks/lib/loop-common.sh:178`, `hooks/lib/loop-common.sh:183`, `hooks/lib/loop-common.sh:240`). Shares concepts with read/edit/bash validators. Block messages come from `prompt-template/block/*` via `load_and_render_safe`.
- edge_cases_or_failure_modes: If no project root or no active loop is found after the initial round-file checks, many writes are allowed to avoid restricting unrelated sessions (`hooks/loop-write-validator.sh:79`, `hooks/loop-write-validator.sh:179`). Methodology-analysis fallback rejects unresolved `..` paths and symlink leaves when `realpath` cannot canonicalize (`hooks/loop-write-validator.sh:100`, `hooks/loop-write-validator.sh:111`). Spawned agents with different `session_id` are explicitly noted as not restricted by hooks and are expected to be governed by prompt sanitization (`hooks/loop-write-validator.sh:84`). Finalize-summary allow uses exact active-loop path comparison before later canonical directory checks (`hooks/loop-write-validator.sh:223`).
- validation_or_tests: Covered by finalize-phase tests invoking the write validator (`tests/test-finalize-phase.sh:389`, `tests/test-finalize-phase.sh:401`, `tests/test-finalize-phase.sh:413`), plan-file hook tests (`tests/test-plan-file-hooks.sh:256`, `tests/test-plan-file-hooks.sh:1150`), allowlist validator tests (`tests/test-allowlist-validators.sh:147`, `tests/test-allowlist-validators.sh:212`), robustness hook-system tests for goal-tracker writes (`tests/robustness/test-hook-system-robustness.sh:453`, `tests/robustness/test-hook-system-robustness.sh:488`, `tests/robustness/test-hook-system-robustness.sh:805`), and hook-input robustness tests (`tests/robustness/test-hook-input-robustness.sh:51`). I inspected these references but did not execute the suites.
- skip_candidate: `no`

### H2_DEV-HZ-080 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Aggregate executable specification runner for the shell-side Humanize plugin behavior. It coordinates many independent shell/robustness test suites and returns a single pass/fail gate.
- algorithmic_behavior: Computes default parallelism from CPU count capped at 8, allows `HUMANIZE_TEST_JOBS`, validates it as integer >=1, defines the ordered `TEST_SUITES` list, marks zsh-only and serial suites, creates a temporary output directory, injects a mock `codex` binary if needed, injects a portable `timeout` shim if needed, runs suites in parallel with throttling, collects suite output/exit/time, runs serial suites after the parallel batch, prints skips and sorted results, and fails if any suite fails (`tests/run-all-tests.sh:21`, `tests/run-all-tests.sh:33`, `tests/run-all-tests.sh:59`, `tests/run-all-tests.sh:140`, `tests/run-all-tests.sh:147`, `tests/run-all-tests.sh:152`, `tests/run-all-tests.sh:158`, `tests/run-all-tests.sh:170`, `tests/run-all-tests.sh:225`, `tests/run-all-tests.sh:292`, `tests/run-all-tests.sh:366`, `tests/run-all-tests.sh:398`).
- inputs_outputs_state: Inputs are executable test files under `tests/`, environment variable `HUMANIZE_TEST_JOBS`, optional `zsh`, optional real `codex`, optional `timeout`, and platform shell capabilities. Outputs are colored per-suite lines, skipped-suite notices, detailed failing logs, total pass/fail counts, and exit `0` or `1`. Runtime state lives only in `OUTPUT_DIR` and is cleaned by trap (`tests/run-all-tests.sh:151`, `tests/run-all-tests.sh:153`).
- gates_or_invariants: Missing individual suites are skipped rather than fatal (`tests/run-all-tests.sh:299`). zsh-only tests are skipped if zsh is unavailable (`tests/run-all-tests.sh:310`). Signal-heavy runtime tests run serially after the parallel phase (`tests/run-all-tests.sh:145`, `tests/run-all-tests.sh:304`). A suite is failed if exit code is nonzero or parsed `Failed:` count is greater than zero (`tests/run-all-tests.sh:272`). It preserves full failing detail logs for CI (`tests/run-all-tests.sh:276`).
- dependencies_and_callers: Relies on bash, `mktemp`, `sort`, `sed`, `grep`, optional zsh, and python3 for millisecond timing and timeout shim (`tests/run-all-tests.sh:170`, `tests/run-all-tests.sh:220`). It includes `test-humanize-escape.sh`, config/model/skill monitor tests, RLCR source tests, and many robustness tests (`tests/run-all-tests.sh:78`, `tests/run-all-tests.sh:86`, `tests/run-all-tests.sh:108`, `tests/run-all-tests.sh:110`, `tests/run-all-tests.sh:119`, `tests/run-all-tests.sh:122`). It does not run the TypeScript Vitest suite; `package.json` owns that via `npm test` (`package.json:20`).
- edge_cases_or_failure_modes: Older bash without `wait -n` falls back to waiting on the oldest PID (`tests/run-all-tests.sh:39`, `tests/run-all-tests.sh:336`). Test suites that do not print `Passed:`/`Failed:` parse as zero counts, so exit status remains the reliable failure signal (`tests/run-all-tests.sh:264`). The mock `codex` only satisfies `command -v`; suites needing behavior create their own mocks (`tests/run-all-tests.sh:155`). Full execution writes temp files and possibly mock binaries, so I did not run it during research.
- validation_or_tests: This file is itself the validation orchestrator. It is the shell test gate for most RLCR/hook/monitor behavior in the repo, but not for Vitest-based TypeScript tests.
- skip_candidate: `no`

### H2_DEV-HZ-110 `file` `tests/test-humanize-escape.sh`
- cursor: `[_]`
- core_role: Focused regression spec for preventing `.humanize` local state from escaping into git tracking and for using find-based iteration patterns that avoid zsh glob failures.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh`, defines `assert_blocks`/`assert_allows`, lowercases candidate shell commands, and calls `git_adds_humanize` to decide expected block/allow outcomes (`tests/test-humanize-escape.sh:16`, `tests/test-humanize-escape.sh:18`, `tests/test-humanize-escape.sh:45`, `tests/test-humanize-escape.sh:59`). It tests direct `.humanize` path variants, quoted variants, force and all flags, chained commands, `git -C` forms, allowed specific paths, patch mode, similarly named non-`.humanize` files, and find-based empty/dotfile/session/state iteration (`tests/test-humanize-escape.sh:83`, `tests/test-humanize-escape.sh:99`, `tests/test-humanize-escape.sh:113`, `tests/test-humanize-escape.sh:133`, `tests/test-humanize-escape.sh:158`, `tests/test-humanize-escape.sh:169`, `tests/test-humanize-escape.sh:180`, `tests/test-humanize-escape.sh:209`, `tests/test-humanize-escape.sh:230`, `tests/test-humanize-escape.sh:253`, `tests/test-humanize-escape.sh:308`).
- inputs_outputs_state: Inputs are command strings and temporary directory contents. Outputs are PASS/FAIL lines, counters, and exit `0`/`1` (`tests/test-humanize-escape.sh:29`, `tests/test-humanize-escape.sh:34`, `tests/test-humanize-escape.sh:342`). It creates and removes temp dirs under `/tmp` for `.humanize` existence and iteration scenarios (`tests/test-humanize-escape.sh:135`, `tests/test-humanize-escape.sh:211`, `tests/test-humanize-escape.sh:231`, `tests/test-humanize-escape.sh:254`, `tests/test-humanize-escape.sh:309`).
- gates_or_invariants: Direct `.humanize` and `.humanize/rlcr/...` adds must block, including quoted and path-prefixed forms. Force plus broad scope must block even if gitignore would otherwise hide the directory. `git add -A/--all` blocks when `.humanize` exists. Specific non-`.humanize` paths and patch mode are allowed. Names such as `.humanizeconfig`, `.humanize-backup`, and `.humanizerc` are not overblocked (`tests/test-humanize-escape.sh:180`, `tests/test-humanize-escape.sh:190`, `tests/test-humanize-escape.sh:195`).
- dependencies_and_callers: Validates `git_adds_humanize` from `hooks/lib/loop-common.sh`, whose implementation splits chained commands, detects `git ... add`, strips quotes for path matching, detects force/all/broad scope flags, checks `.humanize` existence, and consults `git check-ignore` for broad non-force scope (`hooks/lib/loop-common.sh:1285`, `hooks/lib/loop-common.sh:1291`, `hooks/lib/loop-common.sh:1309`, `hooks/lib/loop-common.sh:1320`, `hooks/lib/loop-common.sh:1330`, `hooks/lib/loop-common.sh:1360`, `hooks/lib/loop-common.sh:1373`).
- edge_cases_or_failure_modes: The parser is regex-based, not a full shell parser; it deliberately splits on `&&`, `||`, `|`, and `;`, and strips simple quotes. `git add -A` blocking depends on `.humanize` existing. `git add .` and `git add *` block only when `.humanize` exists and is not ignored. Comments in `loop-common.sh` acknowledge it is scoped to `.humanize/`, not similarly named paths (`hooks/lib/loop-common.sh:1423`).
- validation_or_tests: Included in the aggregate runner at `tests/run-all-tests.sh:78`. I inspected the test but did not execute it.
- skip_candidate: `no`

### H2_DEV-HZ-140 `file` `tests/vendor-metadata.test.ts`
- cursor: `[_]`
- core_role: Vitest specification for the hub’s vendor-session metadata loader. It verifies that dashboard/session metadata can be reconstructed from Codex and Claude local JSONL session logs.
- algorithmic_behavior: Creates a temporary home, writes a Codex session file under `.codex/sessions/...`, calls `loadVendorSessionMetadataForRuns`, and expects model, reasoning effort, token breakdown, latest context load, and context window values (`tests/vendor-metadata.test.ts:11`, `tests/vendor-metadata.test.ts:13`, `tests/vendor-metadata.test.ts:15`, `tests/vendor-metadata.test.ts:64`, `tests/vendor-metadata.test.ts:66`). It then creates a Claude project JSONL under `.claude/projects/...`, includes a duplicate usage record, calls the same loader, and expects deduplicated token totals plus latest context load (`tests/vendor-metadata.test.ts:78`, `tests/vendor-metadata.test.ts:80`, `tests/vendor-metadata.test.ts:82`, `tests/vendor-metadata.test.ts:130`, `tests/vendor-metadata.test.ts:132`).
- inputs_outputs_state: Inputs are synthetic JSONL log lines and `AgentRunRecord` values with `agent` and `backendSessionId` (`tests/vendor-metadata.test.ts:149`). Output is a metadata map keyed by vendor session id. Runtime state is temp-home filesystem data created under `tmpdir()` (`tests/vendor-metadata.test.ts:143`).
- gates_or_invariants: Codex metadata must read `turn_context` model/effort and `event_msg` token counts; token math subtracts cached input from input tokens and reasoning output from output tokens via `tokenStatsFromUsageObject` (`src/hub/vendor-metadata.ts:73`, `src/hub/vendor-metadata.ts:87`, `src/hub/token-stats.ts:75`, `src/hub/token-stats.ts:102`). Claude usage records are deduplicated by message id plus request id before summing (`src/hub/vendor-metadata.ts:140`, `src/hub/vendor-metadata.ts:204`). Malformed/blank JSONL lines are ignored in the implementation (`src/hub/vendor-metadata.ts:62`, `src/hub/vendor-metadata.ts:67`, `src/hub/vendor-metadata.ts:114`, `src/hub/vendor-metadata.ts:119`).
- dependencies_and_callers: Imports Vitest, Node fs/path/os helpers, `loadVendorSessionMetadataForRuns`, and `AgentRunRecord` (`tests/vendor-metadata.test.ts:1`, `tests/vendor-metadata.test.ts:5`, `tests/vendor-metadata.test.ts:7`). The loader is used by the hub HTTP server for `metadataByVendorSessionId` (`src/hub/http-server.ts:10`, `src/hub/http-server.ts:80`) and consumed in agent-session summaries.
- edge_cases_or_failure_modes: `findFileContainingName` recursively returns the first matching `.jsonl` whose filename includes the session id; traversal order is filesystem/readdir dependent and not timestamp-sorted (`src/hub/vendor-metadata.ts:171`). The test does not cover multiple matching files, missing logs, malformed lines, synthetic Claude model filtering, or cleanup of temp homes. Claude context used tokens are overwritten by each non-duplicate usage record; the test expects the latest usage context (`tests/vendor-metadata.test.ts:138`).
- validation_or_tests: Run through the TypeScript test script, not the shell aggregate: `package.json` defines `test` as `vitest run --config vitest.config.ts` (`package.json:20`). I inspected but did not execute Vitest.
- skip_candidate: `no`

### H2_DEV-HZ-170 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the RLCR dirty-worktree gate. It provides the human/agent remediation contract when loop exit is attempted with uncommitted changes or after methodology analysis with dirty tracked changes.
- algorithmic_behavior: Renders `{{GIT_ISSUES}}` and `{{SPECIAL_NOTES}}`, then instructs the agent to review untracked files, stage only specific paths, commit with project conventions, avoid broad git-add commands, never stage `.humanize/` or legacy loop artifacts, and avoid AI authorship trailers (`prompt-template/block/git-not-clean.md:3`, `prompt-template/block/git-not-clean.md:5`, `prompt-template/block/git-not-clean.md:11`). It also conditionally recommends `code-simplifier` if installed (`prompt-template/block/git-not-clean.md:6`).
- inputs_outputs_state: Inputs are template variables supplied by the stop hook. Output is rendered markdown used as the `reason` field in a JSON block decision. It has no direct state writes.
- gates_or_invariants: The template’s invariant is that RLCR exit cannot proceed until real changes are intentionally staged and committed, `.humanize` runtime state remains out of git, broad staging commands are avoided, and commit authorship remains human/tool-neutral (`prompt-template/block/git-not-clean.md:12`, `prompt-template/block/git-not-clean.md:13`, `prompt-template/block/git-not-clean.md:15`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` loads this template after methodology-analysis completion if the tree is still dirty and in the normal git-clean gate when cached status has non-`.humanize` changes (`hooks/loop-codex-stop-hook.sh:634`, `hooks/loop-codex-stop-hook.sh:642`, `hooks/loop-codex-stop-hook.sh:733`, `hooks/loop-codex-stop-hook.sh:743`). Special notes may come from companion templates for untracked `.humanize` and other untracked files (`hooks/loop-codex-stop-hook.sh:714`, `hooks/loop-codex-stop-hook.sh:724`).
- edge_cases_or_failure_modes: This is a prompt/template gate, not an executable validator; enforcement depends on the stop hook’s git-status classification and the agent following the rendered instructions. The wording says “trying to stop,” which still applies when rendered for methodology-analysis terminal gating, but the caller supplies more specific context through variables and fallback.
- validation_or_tests: Template reference tests include `hooks/loop-write-validator.sh` and template existence/reference checks; stop-hook tests exercise the dirty-tree block path indirectly. Direct caller references were confirmed by search. I did not execute tests.
- skip_candidate: `no`

### H2_DEV-HZ-200 `file` `prompt-template/claude/agent-teams-continue.md`
- cursor: `[_]`
- core_role: RLCR next-round prompt block that preserves Agent Teams mode semantics across rounds while preventing stale team/context assumptions.
- algorithmic_behavior: Instructs the Team Leader to create a brand-new team, review Codex feedback before spawning members, avoid redoing completed work, cold-start each member with relevant prior accomplishments/current file state/review findings/acceptance criteria, prioritize critical issues when work exceeds one round, and verify current file state before assigning ownership (`prompt-template/claude/agent-teams-continue.md:3`, `prompt-template/claude/agent-teams-continue.md:7`, `prompt-template/claude/agent-teams-continue.md:8`, `prompt-template/claude/agent-teams-continue.md:9`, `prompt-template/claude/agent-teams-continue.md:10`, `prompt-template/claude/agent-teams-continue.md:11`, `prompt-template/claude/agent-teams-continue.md:12`).
- inputs_outputs_state: Inputs are the surrounding next-round prompt context and prior Codex review text. Output is markdown appended to the next prompt. It does not write state itself; it influences the next implementation round’s delegation state.
- gates_or_invariants: Old teammates are invalid after a round boundary; new members have no conversation history; the leader must base assignments on current file reads/greps and Codex review feedback; prior correct work should not be redone. These are behavioral gates enforced by prompt contract rather than shell logic.
- dependencies_and_callers: Loaded by `hooks/loop-codex-stop-hook.sh` only when `AGENT_TEAMS=true` and not in review phase, and paired with `claude/agent-teams-core.md` when both templates exist (`hooks/loop-codex-stop-hook.sh:2195`, `hooks/loop-codex-stop-hook.sh:2198`, `hooks/loop-codex-stop-hook.sh:2200`). If missing, a shorter fallback is appended (`hooks/loop-codex-stop-hook.sh:2206`). `tests/test-agent-teams.sh` verifies the continuation template exists and contains continuation context (`tests/test-agent-teams.sh:372`, `tests/test-agent-teams.sh:381`).
- edge_cases_or_failure_modes: Because enforcement is prompt-only, a Team Leader can still ignore it. If either continuation or core template is missing, the fallback loses the detailed “review first/cold start/current state” safeguards. The template has no placeholders, so it is robust to missing variables but cannot adapt text to specific round data.
- validation_or_tests: `tests/test-agent-teams.sh` checks presence, size, and “Continuation Context” content (`tests/test-agent-teams.sh:372`, `tests/test-agent-teams.sh:375`, `tests/test-agent-teams.sh:381`). I inspected but did not execute it.
- skip_candidate: `no`

### H2_DEV-HZ-230 `file` `skills/ask-gemini/SKILL.md`
- cursor: `[_]`
- core_role: Skill instruction contract for invoking Gemini as an external research agent through the repo’s `scripts/ask-gemini.sh` wrapper. It is workflow-core for the skill subsystem, though not an RLCR state-file validator.
- algorithmic_behavior: Declares the skill name, description, argument hint, and allowed Bash tool, then instructs callers to run `ask-gemini.sh` with the full free-form task quoted as one argument unless explicit flags are present (`skills/ask-gemini/SKILL.md:1`, `skills/ask-gemini/SKILL.md:4`, `skills/ask-gemini/SKILL.md:5`, `skills/ask-gemini/SKILL.md:17`, `skills/ask-gemini/SKILL.md:19`). If flags such as `--gemini-model` or `--gemini-timeout` are supplied, it requires reconstructing the command with flags as separate shell arguments and the remaining question as one quoted final argument (`skills/ask-gemini/SKILL.md:25`). It explicitly forbids unquoted `$ARGUMENTS` because shell reparsing can fail or misinterpret metacharacters (`skills/ask-gemini/SKILL.md:33`).
- inputs_outputs_state: Inputs are skill arguments, optional Gemini model/timeout flags, and the free-form user question. Outputs are Gemini stdout for incorporation into the answer, stderr status, exit code, and saved `.humanize/skill/<timestamp>/output.md` (`skills/ask-gemini/SKILL.md:41`, `skills/ask-gemini/SKILL.md:43`, `skills/ask-gemini/SKILL.md:59`). The backing script also writes input, output, metadata, and cache logs under `.humanize/skill` and `~/.cache/humanize` (`scripts/ask-gemini.sh:16`, `scripts/ask-gemini.sh:187`, `scripts/ask-gemini.sh:191`, `scripts/ask-gemini.sh:368`, `scripts/ask-gemini.sh:370`).
- gates_or_invariants: Gemini must perform Google Search/up-to-date web research per instruction (`skills/ask-gemini/SKILL.md:10`, `skills/ask-gemini/SKILL.md:61`; backing prompt at `scripts/ask-gemini.sh:237`). Exit code `0` means stdout contains the answer; `1` is validation error; `124` is timeout and should prompt a larger timeout; other nonzero codes should be reported (`skills/ask-gemini/SKILL.md:48`, `skills/ask-gemini/SKILL.md:50`). Default model and timeout are `gemini-3.1-pro-preview` and 3600 seconds (`skills/ask-gemini/SKILL.md:60`; `scripts/ask-gemini.sh:39`).
- dependencies_and_callers: Allows only `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/ask-gemini.sh:*)`. The script depends on `gemini`, `portable-timeout.sh`, and `project-root.sh`, validates model names, resolves project root, chooses sandbox or yolo mode, and records status metadata (`scripts/ask-gemini.sh:27`, `scripts/ask-gemini.sh:30`, `scripts/ask-gemini.sh:33`, `scripts/ask-gemini.sh:145`, `scripts/ask-gemini.sh:162`, `scripts/ask-gemini.sh:174`, `scripts/ask-gemini.sh:224`, `scripts/ask-gemini.sh:281`, `scripts/ask-gemini.sh:302`, `scripts/ask-gemini.sh:326`, `scripts/ask-gemini.sh:350`, `scripts/ask-gemini.sh:370`).
- edge_cases_or_failure_modes: The skill file’s major safety edge is shell metacharacter handling; unsafe unquoted invocation can fail before the wrapper script starts. The wrapper fails if `gemini` is missing, question is empty, model contains unsafe characters, or project root cannot be resolved (`scripts/ask-gemini.sh:145`, `scripts/ask-gemini.sh:153`, `scripts/ask-gemini.sh:162`, `scripts/ask-gemini.sh:174`). Home cache write failure falls back to project-local cache (`scripts/ask-gemini.sh:195`). Empty Gemini stdout records `empty_response` and exits nonzero (`scripts/ask-gemini.sh:340`).
- validation_or_tests: No dedicated `test-ask-gemini.sh` appears in `tests/run-all-tests.sh`; analogous `ask-codex` and skill-monitor tests cover the shared `.humanize/skill` output/metadata monitoring path. I inspected the skill and backing script but did not execute Gemini.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8/8 item sections present, each assigned item heading appears once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`