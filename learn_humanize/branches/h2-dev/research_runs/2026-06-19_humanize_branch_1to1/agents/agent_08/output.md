# agent_08 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 9
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-008 `directory` `scripts`
- cursor: `[_]`
- core_role: Runtime command, validation, installation, monitor, state-control, and smoke-test layer for Humanize/RLCR workflows. The directory is a core algorithm coordinator because it contains setup, stop-gate, BitLesson, model/provider routing, config merge, command preflight, and runtime installation scripts.
- algorithmic_behavior: The directory is organized around several algorithmic lanes. RLCR setup/control lives in `scripts/setup-rlcr-loop.sh`, `cancel-rlcr-loop.sh`, `cancel-rlcr-session.sh`, and `rlcr-stop-gate.sh`. `setup-rlcr-loop.sh` parses loop options, validates prerequisites, rejects unsafe plan paths, captures git/base state, initializes `.humanize/rlcr/<timestamp>/state.md`, creates `goal-tracker.md`, creates round prompts, initializes BitLesson state, and writes a pending session-id signal; key gates are at `scripts/setup-rlcr-loop.sh:189`, `scripts/setup-rlcr-loop.sh:331`, `scripts/setup-rlcr-loop.sh:375`, `scripts/setup-rlcr-loop.sh:450`, `scripts/setup-rlcr-loop.sh:728`, `scripts/setup-rlcr-loop.sh:826`, `scripts/setup-rlcr-loop.sh:885`, and prompt/task routing at `scripts/setup-rlcr-loop.sh:1340`. Stop-gate reuse is provided by `scripts/rlcr-stop-gate.sh`, which wraps `hooks/loop-codex-stop-hook.sh` and maps hook JSON decisions to CLI exits `0`, `10`, or `20` at `scripts/rlcr-stop-gate.sh:8` and `scripts/rlcr-stop-gate.sh:152`.
- inputs_outputs_state: Inputs include CLI args, plan paths, git checkout state, `CLAUDE_PROJECT_DIR`, `CLAUDE_PLUGIN_ROOT`, `CODEX_HOME`, `XDG_CONFIG_HOME`, `HUMANIZE_CONFIG`, model/effort flags, and JSON hook input. Outputs include validation key/value lines, generated loop state files under `.humanize/rlcr`, skill outputs under `.humanize/skill`, cache logs under `~/.cache/humanize`, installed skills/hooks, and smoke output files under `temp/`. State transitions include active `state.md` creation, review/finalize markers, cancellation by renaming active state to `cancel-state.md`, and stop-hook driven completion/follow-up through hook wrappers.
- gates_or_invariants: The directory enforces clean git working tree before RLCR start (`scripts/setup-rlcr-loop.sh:728`), no concurrent active loop (`scripts/setup-rlcr-loop.sh:375`), relative non-symlink in-project plan paths (`scripts/setup-rlcr-loop.sh:460`, `scripts/setup-rlcr-loop.sh:486`, `scripts/setup-rlcr-loop.sh:499`, `scripts/setup-rlcr-loop.sh:543`), local base-branch availability (`scripts/setup-rlcr-loop.sh:753`), YAML-safe branch/model/effort values (`scripts/setup-rlcr-loop.sh:697`, `scripts/setup-rlcr-loop.sh:707`), and hook stop-gate JSON validity (`scripts/rlcr-stop-gate.sh:142`). `scripts/lib/config-loader.sh` requires `jq`, treats missing required defaults as fatal, and ignores malformed optional layers with warnings (`scripts/lib/config-loader.sh:17`, `scripts/lib/config-loader.sh:40`, `scripts/lib/config-loader.sh:52`). `scripts/lib/model-router.sh` and `scripts/bitlesson-select.sh` enforce provider selection/dependency gates and stable two-line selector output (`scripts/bitlesson-select.sh:125`, `scripts/bitlesson-select.sh:136`, `scripts/bitlesson-select.sh:262`). Timeout invariants are centralized in `scripts/portable-timeout.sh:10` and `scripts/portable-timeout.sh:38`.
- dependencies_and_callers: `commands/gen-plan.md` calls `scripts/validate-gen-plan-io.sh` and `scripts/setup-rlcr-loop.sh`; `commands/start-rlcr-loop.md` calls `setup-rlcr-loop.sh`; `skills/humanize*.md` reference setup and validator scripts; `hooks/loop-codex-stop-hook.sh` calls `scripts/bitlesson-validate-delta.sh` and renders `prompt-template/codex/regular-review.md`; `scripts/install-skill.sh` calls `scripts/install-codex-hooks.sh` for Codex installs (`scripts/install-skill.sh:281`). Shared dependencies include `hooks/lib/loop-common.sh`, `hooks/lib/project-root.sh`, `hooks/lib/template-loader.sh`, `scripts/lib/config-loader.sh`, `scripts/lib/model-router.sh`, `jq`, `git`, `codex`, `claude`, `gemini`, `python3`, and optional `rsync`.
- edge_cases_or_failure_modes: Important failures include no git repo or no commits, missing dependencies, active loop collision, unsafe/absolute/space/metacharacter plan paths, symlink traversal, untracked/tracked plan mismatch, dirty checkout, missing local base branch, malformed config, missing templates, missing hook support, timeouts, empty LLM responses, selector output not matching the stable contract, and state cancellation during finalize requiring `--force`.
- validation_or_tests: Validation coverage is distributed across shell tests and smoke scripts. Assigned tests cover config errors, task routing, and state transitions. Other referenced tests include `tests/test-gen-plan.sh` for `validate-gen-plan-io.sh`, `tests/test-stop-gate.sh` for `rlcr-stop-gate.sh`, `tests/test-bitlesson-validate-delta.sh`, and smoke scripts `scripts/real-btc-smoke.ts` and `scripts/nested-market-smoke.ts`. The directory itself contains two TypeScript smoke runners: `scripts/real-btc-smoke.ts` tests agent run output creation (`scripts/real-btc-smoke.ts:35`) and `scripts/nested-market-smoke.ts` tests logical child run propagation through JSON-RPC (`scripts/nested-market-smoke.ts:51`, `scripts/nested-market-smoke.ts:166`).
- skip_candidate: `no`

### H2_DEV-HZ-038 `file` `config/codex-hooks.json`
- cursor: `[_]`
- core_role: Codex native hooks template that registers Humanize’s RLCR Stop hook into Codex runtime configuration.
- algorithmic_behavior: Defines a `Stop` hook group with a single command hook that executes `{{HUMANIZE_RUNTIME_ROOT}}/hooks/loop-codex-stop-hook.sh` with a 7200-second timeout and status message (`config/codex-hooks.json:3`, `config/codex-hooks.json:8`, `config/codex-hooks.json:10`). The placeholder is hydrated by `scripts/install-codex-hooks.sh`.
- inputs_outputs_state: Input is the installed runtime root path supplied to the installer. Output is merged Codex `hooks.json` content under the target Codex config directory. State impact is indirect: it causes stop events to invoke the RLCR state machine and write/rewrite loop state through the hook.
- gates_or_invariants: The JSON assumes the native Codex hooks schema and requires the runtime hook script path to resolve after placeholder substitution. `scripts/install-codex-hooks.sh` validates native `hooks` feature support, rejects legacy `codex_hooks`, requires `--disable` support to avoid recursive hooks, removes old managed hook commands, and shell-quotes hydrated command paths (`scripts/install-codex-hooks.sh:84`, `scripts/install-codex-hooks.sh:89`, `scripts/install-codex-hooks.sh:102`, `scripts/install-codex-hooks.sh:146`, `scripts/install-codex-hooks.sh:172`).
- dependencies_and_callers: Consumed by `scripts/install-codex-hooks.sh` via `HOOKS_TEMPLATE="$REPO_ROOT/config/codex-hooks.json"` (`scripts/install-codex-hooks.sh:14`). Installed by `scripts/install-skill.sh` during Codex target install (`scripts/install-skill.sh:281`).
- edge_cases_or_failure_modes: Missing template fails installer (`scripts/install-codex-hooks.sh:72`). Unsupported Codex CLI, legacy feature key, missing `--disable`, malformed existing `hooks.json`, invalid `hooks.Stop` shape, or missing `python3` all block installation.
- validation_or_tests: Installation behavior is covered indirectly by install docs/tests and template reference tests; `tests/test-template-references.sh` checks hook/template references, and `tests/test-disable-nested-codex-hooks.sh` exercises the nested hook prevention behavior referenced by the installer.
- skip_candidate: `no`

### H2_DEV-HZ-068 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Preflight validator for the `/humanize:gen-plan` workflow. It prevents malformed input/output routing before the prompt/template driven plan generation starts.
- algorithmic_behavior: Parses `--input`, `--output`, `--auto-start-rlcr-if-converged`, `--discussion`, and `--direct`; rejects unknown or missing options; rejects simultaneous `--discussion` and `--direct`; normalizes paths; validates input existence/non-emptiness, output directory existence, output path nonexistence and non-directory status, output directory writeability, and plan template availability (`scripts/validate-gen-plan-io.sh:35`, `scripts/validate-gen-plan-io.sh:76`, `scripts/validate-gen-plan-io.sh:98`, `scripts/validate-gen-plan-io.sh:108`, `scripts/validate-gen-plan-io.sh:124`, `scripts/validate-gen-plan-io.sh:132`, `scripts/validate-gen-plan-io.sh:147`, `scripts/validate-gen-plan-io.sh:162`).
- inputs_outputs_state: Inputs are CLI args and optionally `CLAUDE_PLUGIN_ROOT`. Outputs are human-readable validation messages plus `VALIDATION_SUCCESS` and `TEMPLATE_FILE: ...` on success (`scripts/validate-gen-plan-io.sh:155`, `scripts/validate-gen-plan-io.sh:177`). It does not mutate workflow state.
- gates_or_invariants: Exit codes are part of the command contract: `1` input missing, `2` empty input, `3` missing output dir, `4` existing output or directory output, `5` no write permission, `6` argument errors, `7` missing plan template (`scripts/validate-gen-plan-io.sh:4`). Direct mode with `--auto-start-rlcr-if-converged` is allowed but emits a note because auto-start only applies to discussion convergence (`scripts/validate-gen-plan-io.sh:93`).
- dependencies_and_callers: Called by `commands/gen-plan.md` and referenced by `skills/humanize-gen-plan/SKILL.md` and `skills/humanize/SKILL.md`. It depends on standard shell tools `realpath`, `dirname`, `wc`, and the template path `prompt-template/plan/gen-plan-template.md`.
- edge_cases_or_failure_modes: `realpath -m` fallback keeps original paths if `realpath` is unavailable or incompatible. The validator checks write permission but not final atomic write behavior. It also uses `set -e`, so unexpected shell errors fail closed.
- validation_or_tests: `tests/test-gen-plan.sh` has a dedicated section for this script, checking missing option values, unknown options, missing input, empty input, missing output directory, existing output, output-is-directory, valid paths, auto-start flag acceptance, and help behavior. The `rg` reference showed those checks around `tests/test-gen-plan.sh:557` through `tests/test-gen-plan.sh:705`.
- skip_candidate: `no`

### H2_DEV-HZ-098 `file` `tests/test-config-error-handling.sh`
- cursor: `[_]`
- core_role: Executable specification for config loading error semantics, especially which config failures are fatal versus warning-and-fallback.
- algorithmic_behavior: Sources `scripts/lib/config-loader.sh`, creates isolated temporary project/user config trees, and exercises `load_merged_config` plus `get_config_value` under malformed, missing, and empty config cases (`tests/test-config-error-handling.sh:16`, `tests/test-config-error-handling.sh:32`, `tests/test-config-error-handling.sh:40`).
- inputs_outputs_state: Inputs are synthetic config files under `$TEST_DIR`, `XDG_CONFIG_HOME`, `PROJECT_ROOT/config/default_config.json`, and fake plugin roots. Outputs are pass/fail lines and final `print_test_summary`. State is confined to temp dirs via `setup_test_dir` from `tests/test-helpers.sh`.
- gates_or_invariants: Missing required `default_config.json` must cause non-zero exit (`tests/test-config-error-handling.sh:37`, `tests/test-config-error-handling.sh:48`). Malformed project or user config must emit a warning and fall back to defaults (`tests/test-config-error-handling.sh:56`, `tests/test-config-error-handling.sh:64`, `tests/test-config-error-handling.sh:84`, `tests/test-config-error-handling.sh:93`). Empty project config `{}`, missing project config, and missing user config directory must be non-fatal and preserve default `bitlesson_model=haiku` (`tests/test-config-error-handling.sh:113`, `tests/test-config-error-handling.sh:130`, `tests/test-config-error-handling.sh:153`).
- dependencies_and_callers: Depends on `scripts/lib/config-loader.sh`, `tests/test-helpers.sh`, `jq`, `mktemp`, and default config values from `config/default_config.json` (`config/default_config.json:2` through `config/default_config.json:7`).
- edge_cases_or_failure_modes: The test explicitly distinguishes required default config errors from optional user/project config errors. It captures stderr to ensure warnings are visible, not silently swallowed (`tests/test-config-error-handling.sh:64`, `tests/test-config-error-handling.sh:93`).
- validation_or_tests: This file is itself validation. It is complementary to broader config tests such as `tests/test-config-merge.sh` and `tests/test-unified-codex-config.sh`.
- skip_candidate: `no`

### H2_DEV-HZ-128 `file` `tests/test-task-tag-routing.sh`
- cursor: `[_]`
- core_role: Executable specification that RLCR prompt generation and follow-up prompts preserve task routing tags for `coding` versus `analyze` work.
- algorithmic_behavior: Builds mock repositories and a mock `codex` executable, starts `scripts/setup-rlcr-loop.sh`, then inspects generated `round-0-prompt.md` and `goal-tracker.md` for the routing section and `Tag`/`Owner` columns (`tests/test-task-tag-routing.sh:25`, `tests/test-task-tag-routing.sh:52`, `tests/test-task-tag-routing.sh:75`, `tests/test-task-tag-routing.sh:93`). It then fabricates an active loop directory and calls `hooks/loop-codex-stop-hook.sh` to ensure a follow-up `round-1-prompt.md` contains the routing reminder (`tests/test-task-tag-routing.sh:121`, `tests/test-task-tag-routing.sh:223`).
- inputs_outputs_state: Inputs are synthetic plan markdown with a Task Breakdown table containing `coding` and `analyze` tags, mock Codex output, hook JSON, and temp git repos. Outputs are generated RLCR files under `.humanize/rlcr` and pass/fail assertions.
- gates_or_invariants: Round 0 prompt must include `## Task Tag Routing (MUST FOLLOW)` and `/humanize:ask-codex` instruction for `analyze` tasks (`tests/test-task-tag-routing.sh:99`, `tests/test-task-tag-routing.sh:105`). Goal tracker Active Tasks table must include `Tag` and `Owner` columns (`tests/test-task-tag-routing.sh:111`). Stop-hook follow-up prompts must include `## Task Tag Routing Reminder` and `/humanize:ask-codex` (`tests/test-task-tag-routing.sh:227`, `tests/test-task-tag-routing.sh:233`).
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, `hooks/loop-codex-stop-hook.sh`, `tests/test-helpers.sh`, git, and a mocked `codex`. The behavior under test is implemented in `scripts/setup-rlcr-loop.sh:1133`, `scripts/setup-rlcr-loop.sh:1340`, and `hooks/loop-codex-stop-hook.sh:1417`.
- edge_cases_or_failure_modes: The stop-hook fixture intentionally omits `codex_model` in state so the hook must derive defaults from config, which protects compatibility with older state files (`tests/test-task-tag-routing.sh:155`). The hook is allowed to return non-zero/blocked behavior during the test (`|| true`) because the assertion is about generated prompt content (`tests/test-task-tag-routing.sh:224`).
- validation_or_tests: This file is itself validation for routing persistence across initial setup and iterative hook-generated prompts.
- skip_candidate: `no`

### H2_DEV-HZ-158 `file` `prompt-template/block/bitlesson-delta-empty-kb.md`
- cursor: `[_]`
- core_role: Stop-hook block message template enforcing BitLesson learning capture when the knowledge base has no concrete entries and `Action: none` is disallowed.
- algorithmic_behavior: Provides the user-facing block reason that says `Action: none` is not allowed while `.humanize/bitlesson.md` has no concrete lesson entries, and instructs the worker to add or update at least one reusable lesson if the round resolved prior issues (`prompt-template/block/bitlesson-delta-empty-kb.md:1`, `prompt-template/block/bitlesson-delta-empty-kb.md:3`, `prompt-template/block/bitlesson-delta-empty-kb.md:5`).
- inputs_outputs_state: Inputs are template variables passed by `scripts/bitlesson-validate-delta.sh` for current round and BitLesson relative path, though this concrete file does not use the placeholders. Output is a rendered block reason embedded in a JSON stop-hook response.
- gates_or_invariants: The gate is triggered when BitLesson action is `none`, the BitLesson file has zero concrete `Lesson ID:` entries, and `--allow-empty-none` is not `true` (`scripts/bitlesson-validate-delta.sh:227`, `scripts/bitlesson-validate-delta.sh:242`, `scripts/bitlesson-validate-delta.sh:254`). It produces `decision: block` JSON through `block_exit` (`scripts/bitlesson-validate-delta.sh:78`).
- dependencies_and_callers: Loaded via `load_and_render_safe` in `scripts/bitlesson-validate-delta.sh:263`; the validator is called by `hooks/loop-codex-stop-hook.sh:868`.
- edge_cases_or_failure_modes: If this template is absent, the validator uses an inline fallback with `{{CURRENT_ROUND}}` and `{{BITLESSON_FILE}}` placeholders (`scripts/bitlesson-validate-delta.sh:255`). If the BitLesson file has only placeholder IDs, they are not counted as concrete (`scripts/bitlesson-validate-delta.sh:229`).
- validation_or_tests: Covered by `tests/test-bitlesson-validate-delta.sh` and referenced by template reference checks. The assigned template directly defines the block contract for the empty knowledge-base edge case.
- skip_candidate: `no`

### H2_DEV-HZ-188 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Block message template for preventing direct edits to RLCR `state.md`, preserving the loop state machine’s integrity.
- algorithmic_behavior: Tells the worker that `state.md` is managed by the loop system and must not be modified; if work is done, the worker should stop and let review auto-trigger (`prompt-template/block/state-file-modification.md:1`, `prompt-template/block/state-file-modification.md:3`).
- inputs_outputs_state: Input is the block renderer call from hook validators. Output is the reason shown when write/edit/bash validators detect state-file mutation attempts. It prevents user/tool writes from corrupting active state.
- gates_or_invariants: `hooks/lib/loop-common.sh` renders this template in `state_file_blocked_message` (`hooks/lib/loop-common.sh:864`, `hooks/lib/loop-common.sh:870`). Related validators call strict state parsing before allowing loop operations, and the robustness test confirms malformed state is rejected by `parse_state_file_strict` (`hooks/lib/loop-common.sh:525`).
- dependencies_and_callers: Referenced by `hooks/lib/loop-common.sh`, and covered by `tests/test-template-references.sh` according to the reference scan. It interacts with read/write/edit/bash validators that protect `.humanize/rlcr` files.
- edge_cases_or_failure_modes: If the template is missing, the fallback still blocks state edits (`hooks/lib/loop-common.sh:866`). The message specifically names `state.md`; separate templates exist for `finalize-state.md` and methodology-analysis state, so this file is scoped to normal active state.
- validation_or_tests: Template reference tests check the file exists and is referenced. State transition robustness tests validate strict parser expectations around state fields.
- skip_candidate: `no`

### H2_DEV-HZ-218 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Core Codex review prompt template for non-full-alignment RLCR rounds. It defines the review contract that decides whether the loop advances, blocks, continues, or eventually stops.
- algorithmic_behavior: Requires Codex to read the original plan first (`prompt-template/codex/regular-review.md:5`), review Claude’s summary against the plan and prompt (`prompt-template/codex/regular-review.md:11`), classify findings into Mainline Gaps, Blocking Side Issues, and Queued Side Issues (`prompt-template/codex/regular-review.md:49`), provide mandatory `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED` (`prompt-template/codex/regular-review.md:56`), perform goal alignment against `GOAL_TRACKER_FILE` (`prompt-template/codex/regular-review.md:35`), and only output `COMPLETE` as the final line when every original task and AC is done with no deferrals or pending work (`prompt-template/codex/regular-review.md:72`).
- inputs_outputs_state: Inputs are rendered variables such as current round, plan file, prompt file, summary content, commit history, docs path, goal tracker, and review result path. Output is a review prompt written to `round-N-review-prompt.md`; Codex’s response becomes `round-N-review-result.md`. The stop hook then interprets review verdict and final `COMPLETE`.
- gates_or_invariants: This template makes deferred and unfinished items incomplete by definition (`prompt-template/codex/regular-review.md:26`, `prompt-template/codex/regular-review.md:72`). It also makes the verdict line mandatory; omission blocks the round per line `61`. The stop hook renders this template in regular rounds at `hooks/loop-codex-stop-hook.sh:1117`.
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` using `load_and_render_safe`, alongside commit history and goal tracker update sections (`hooks/loop-codex-stop-hook.sh:1016`, `hooks/loop-codex-stop-hook.sh:1062`, `hooks/loop-codex-stop-hook.sh:1118`). It depends on state fields parsed from `state.md` for round, plan, and review phase context.
- edge_cases_or_failure_modes: If the template is missing, the stop hook uses a simpler inline regular-review fallback (`hooks/loop-codex-stop-hook.sh:1087`). If Codex omits the verdict, the loop blocks and requires rerun; if Codex follows Claude’s deferral, the template explicitly instructs Codex to reject that as incomplete work.
- validation_or_tests: Behavior is exercised indirectly by stop-hook tests, task-tag routing tests, and any tests that mock Codex review output. The required verdict is consumed by loop-common verdict parsing (`hooks/lib/loop-common.sh:634`).
- skip_candidate: `no`

### H2_DEV-HZ-248 `file` `tests/robustness/test-state-transition-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR loop state discovery, state parsing, finalize/cancel handling, and malformed state behavior.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh`, creates synthetic loop directories and state files, then verifies valid round states, finalize state detection, cancel state exclusion, invalid field handling, and loop directory discovery semantics (`tests/robustness/test-state-transition-robustness.sh:14`, `tests/robustness/test-state-transition-robustness.sh:30`, `tests/robustness/test-state-transition-robustness.sh:95`).
- inputs_outputs_state: Inputs are generated `state.md`, `finalize-state.md`, and `cancel-state.md` files under temp `.humanize/rlcr` trees. Outputs are pass/fail assertions and final summary. State mutations are local to `$TEST_DIR`.
- gates_or_invariants: Round 0, round 5, and max round values must parse (`tests/robustness/test-state-transition-robustness.sh:98`, `tests/robustness/test-state-transition-robustness.sh:114`, `tests/robustness/test-state-transition-robustness.sh:127`). Finalize state must be considered active (`tests/robustness/test-state-transition-robustness.sh:148`). Cancel state must not be active (`tests/robustness/test-state-transition-robustness.sh:182`). Non-numeric round, missing `current_round`, missing `max_iterations`, and missing `base_branch` must be rejected by strict parser (`tests/robustness/test-state-transition-robustness.sh:259`, `tests/robustness/test-state-transition-robustness.sh:292`, `tests/robustness/test-state-transition-robustness.sh:315`, `tests/robustness/test-state-transition-robustness.sh:339`). Newest lexicographic loop directory selection and wrong-location nested state ignoring are asserted (`tests/robustness/test-state-transition-robustness.sh:371`, `tests/robustness/test-state-transition-robustness.sh:404`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` functions `parse_state_file_strict`, `get_current_round`, and `find_active_loop`; also uses `tests/test-helpers.sh`. The implementation under test is in `hooks/lib/loop-common.sh:332`, `hooks/lib/loop-common.sh:430`, and `hooks/lib/loop-common.sh:525`.
- edge_cases_or_failure_modes: Negative `current_round` and over-max round are intentionally parsed rather than rejected, documenting that semantic enforcement is elsewhere (`tests/robustness/test-state-transition-robustness.sh:217`, `tests/robustness/test-state-transition-robustness.sh:238`). Directory discovery is lexicographic and does not validate timestamp shape (`tests/robustness/test-state-transition-robustness.sh:385`). Nested `state.md` under a subdirectory is ignored (`tests/robustness/test-state-transition-robustness.sh:404`).
- validation_or_tests: This file is itself validation and complements other robustness tests such as concurrent state robustness. It specifically proves the strict parser schema and active-loop lookup invariants.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 9 unique item evidence sections present above, matching the 9 assigned non-`none` rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`