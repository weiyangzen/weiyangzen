# agent_27 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-027 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: Recursive robustness suite for Humanize/RLCR shell algorithms, covering state parsing, session discovery, goal tracker parsing, git status summarization, hook validator inputs, path and plan validation, cancel authorization, timeout handling, base branch detection, setup script guards, template rendering, and state transitions.
- algorithmic_behavior: The directory contains 16 shell specs, 8,155 total lines. `tests/run-all-tests.sh` includes every robustness script in the main suite at lines 122-137. The child roles are: base branch selection in `test-base-branch-detection.sh`; cancel authorization in `test-cancel-security-robustness.sh`; concurrent state access in `test-concurrent-state-robustness.sh`; git status parsing in `test-git-operations-robustness.sh`; goal tracker markdown parsing in `test-goal-tracker-robustness.sh`; hook JSON/monitor edge cases in `test-hook-input-robustness.sh`; hook validator coverage in `test-hook-system-robustness.sh`; setup path validation in `test-path-validation-robustness.sh`; plan content/tracking validation in `test-plan-file-robustness.sh`; concurrent sessions in `test-session-robustness.sh`; setup script argument/git/YAML/concurrency guards in `test-setup-scripts-robustness.sh`; state frontmatter parsing in `test-state-file-robustness.sh`; state machine progressions in `test-state-transition-robustness.sh`; template error handling in `test-template-error-robustness.sh`; template stress behavior in `test-template-stress-robustness.sh`; timeout fallback/termination behavior in `test-timeout-robustness.sh`.
- inputs_outputs_state: Inputs are temporary repos, temporary `.humanize/rlcr` session trees, mocked plan files, mock `codex` binaries, synthetic JSON hook payloads, state markdown, goal tracker markdown, and git worktree states. Outputs are pass/fail counters via `tests/test-helpers.sh`, exit status from each shell script, and diagnostic output. State is isolated under test temp dirs via helper setup rather than modifying the repository.
- gates_or_invariants: The suite enforces fail-closed handling for malformed state, unsafe plan paths, symlinks, command injection, unsafe YAML serialization characters, invalid hook JSON, active session races, git edge states, and timeout behavior. Production state parsing has strict and tolerant modes in `hooks/lib/loop-common.sh`: tolerant defaults at lines 494-521, strict required field/frontmatter validation at lines 533-570. Cancel authorization invariants are documented in `hooks/lib/loop-common.sh` lines 1039-1056.
- dependencies_and_callers: Robustness scripts source production shell logic instead of duplicating it: `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/humanize.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, and `hooks/loop-*` validators. `scripts/humanize.sh` provides goal tracker parsing at lines 71-153 and git state/status parsing at lines 158-240. `scripts/setup-rlcr-loop.sh` plan validation is the main target for path/plan/setup tests at lines 450-710.
- edge_cases_or_failure_modes: Covered cases include missing YAML delimiters, missing fields, non-numeric rounds, binary/CRLF state files, concurrent reads while writing, deleted session dirs, symlink state files, shell metacharacters, command substitution, escaped paths, unreadable dirs, non-git repos, detached/rebase/merge states, malformed/deep JSON, binary hook payloads, narrow terminals, deleted logs, missing templates, huge templates, and commands ignoring `SIGTERM`.
- validation_or_tests: This directory is itself validation. Main suite registration is explicit in `tests/run-all-tests.sh` lines 122-137, so these tests run with the broader shell test harness.
- skip_candidate: `no`

### H2_DEV-HZ-057 `file` `scripts/cancel-rlcr-session.sh`
- cursor: `[_]`
- core_role: Session-scoped RLCR cancellation helper used by the dashboard/API to cancel one named session without touching other active sessions in the same project.
- algorithmic_behavior: Parses `--session-id`, optional `--project`, optional `--force`, and legacy positional session ID at lines 28-49. It validates the session ID against path traversal and injection forms at lines 57-76, resolves project root from `--project`, `CLAUDE_PROJECT_DIR`, or `pwd` at lines 78-84, finds `.humanize/rlcr/<session-id>` at line 86, selects the active state file in priority order `state.md`, `methodology-analysis-state.md`, then `finalize-state.md` at lines 99-112, and performs cancellation by touching `.cancel-requested` then renaming the active state file to `cancel-state.md` at lines 120-121.
- inputs_outputs_state: Inputs are CLI args and filesystem state under `$PROJECT_ROOT/.humanize/rlcr/$SESSION_ID`. Outputs are sentinel strings on stdout: `NO_SESSION`, `NO_ACTIVE_LOOP`, `FINALIZE_NEEDS_CONFIRM`, or `CANCELLED <id>` at lines 88-117 and 123-124. Persistent state transition is active loop file to `cancel-state.md` plus `.cancel-requested`.
- gates_or_invariants: Exit codes are defined at lines 16-20. The script refuses missing session IDs at lines 51-55; rejects `/`, backslash, leading dot, `.`/`..`, parent-token prefixes, and non `[A-Za-z0-9._-]` IDs at lines 65-76; refuses non-existent session dirs at lines 88-92; refuses sessions with no active state at lines 108-112; requires `--force` for finalize phase at lines 114-118.
- dependencies_and_callers: The script is referenced by app auth/migration tests in `tests/test-app-auth.sh` lines 123-149 and is discovered by `viz/server/app.py` through `_find_session_cancel_script` and cancellation routes. It mirrors older project-scoped cancel mechanics noted in comments at lines 5-10.
- edge_cases_or_failure_modes: Methodology analysis cancellation is treated as an active phase at lines 102-104. Finalize cancellation is gated because it may be destructive or late-stage. If `mv "$ACTIVE_STATE_FILE" "$SESSION_DIR/cancel-state.md"` overwrites an existing cancel-state file, the script does not add a collision guard. It also assumes no concurrent actor changes the active state file between detection and `mv`.
- validation_or_tests: Covered by `tests/test-app-auth.sh` route wiring checks and directly related to `tests/robustness/test-cancel-security-robustness.sh`, though that robustness file targets the hook-level `is_cancel_authorized` validator rather than this helper’s full CLI flow.
- skip_candidate: `no`

### H2_DEV-HZ-087 `file` `tests/test-app-auth.sh`
- cursor: `[_]`
- core_role: Executable shell specification for dashboard Flask app auth, remote bind safety, project mutation deprecation, session cancellation routing, CSRF, and fail-closed authorization behavior.
- algorithmic_behavior: Uses static grep checks plus a small AST/runtime probe against `viz/server/app.py`. Sections validate CLI flags at lines 35-42; remote bind token guard and configurable `app.run(host=BIND_HOST)` at lines 44-56; auth middleware/token extraction at lines 58-75; remote-mode WebSocket rejection at lines 77-82; legacy project persistence removal at lines 84-111; mutation routes returning 410 at lines 113-121; session-scoped cancel helper wiring at lines 123-149; missing session ID route at lines 157-168; CSRF and cancellable statuses at lines 170-200; request-relative origin handling and `--project PROJECT_DIR` forwarding at lines 202-213.
- inputs_outputs_state: Input is `viz/server/app.py` at line 23 and optional `VIZ_TEST_VENV` for Flask runtime probing at line 267. Outputs are PASS/FAIL counters via `_pass`/`_fail` at lines 29-33, final exit 1 if failures exist at lines 307-314, and success text at line 316. It does not mutate app state.
- gates_or_invariants: The auth predicate must not allow `not AUTH_TOKEN` on non-loopback binds. The AST check extracts `_request_authorized` and rejects the old combined allow clause at lines 221-255. The behavioral matrix, when Flask is available, requires non-loopback/no-token deny, wrong-token deny, bearer-token grant, and loopback/no-token grant at lines 275-305.
- dependencies_and_callers: Depends on `grep`, `python3` AST parsing, and optionally `/tmp/viz-routes-test-venv` or `VIZ_TEST_VENV` with Flask. Targets `viz/server/app.py` functions and constants found in production around `_request_authorized`, `_enforce_csrf_protection`, `_origin_matches_request`, `_find_session_cancel_script`, `_CANCELLABLE_STATUSES`, and `app.run`.
- edge_cases_or_failure_modes: The runtime probe intentionally skips as PASS if the dedicated venv lacks Flask, relying on static checks instead at lines 267-305. Static grep checks can miss semantic regressions if equivalent strings remain while behavior changes, partly mitigated by the AST and optional Flask probe.
- validation_or_tests: It is a test file included in the repository’s test surface; it exits with accumulated `FAIL_COUNT`, making each grep/probe a gate.
- skip_candidate: `no`

### H2_DEV-HZ-117 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/setup-rlcr-loop.sh` plan-file, git tracking, branch, content, CLI, and Codex model validation.
- algorithmic_behavior: Builds temporary git repos and mock plan files, unsets `CLAUDE_PROJECT_DIR` so tests run against temp repo cwd at lines 19-21, creates a mock `codex` binary at lines 73-83, and invokes `setup-rlcr-loop.sh` under many path and repo conditions. It validates absolute path rejection at lines 88-101, non-existent files/dirs at lines 103-125, spaces and shell metacharacters at lines 127-186, symlinks at lines 188-199, path resolution failures at lines 201-242, project escape at lines 244-271, non-git and no-commit repos at lines 273-321, tracking mode at lines 328-425, branch YAML safety at lines 431-511, content sufficiency at lines 517-660, CLI option conflicts at lines 666-694, and Codex model/effort safety at lines 700-740.
- inputs_outputs_state: Inputs are temp directories from `mktemp -d`, synthetic git repos, plan files with controlled content, and command args to `setup-rlcr-loop.sh`. Outputs are pass/fail counters, diagnostic command output captured in `RESULT`, and final exit equal to `TESTS_FAILED` at lines 742-751. State remains under the temp directory with cleanup trap at line 38.
- gates_or_invariants: Mirrors production validation in `scripts/setup-rlcr-loop.sh`: plan paths must be relative at lines 460-464; no spaces at lines 466-472; no shell metacharacters at lines 474-481; no symlink file or symlink parent path at lines 486-520; file must exist/readable and resolve under project at lines 522-546; not inside submodule at lines 548-561; tracking mode must match git state at lines 567-612; content requires enough real non-comment lines at lines 623-676; branch and Codex strings must be YAML-safe at lines 697-710.
- dependencies_and_callers: Calls `scripts/setup-rlcr-loop.sh` repeatedly. Uses `git`, `chmod`, shell heredocs, and a temp-bin mock `codex` to avoid real agent startup. It depends on the setup script’s error messages including phrases like `relative path`, `not found`, `cannot contain spaces`, `shell metacharacters`, `gitignored`, `tracked in git`, `clean`, `YAML-unsafe`, and `insufficient content`.
- edge_cases_or_failure_modes: Tests allow git itself to reject some invalid branch names before the setup script does at lines 459-474 and 476-511. Some permission tests can skip when `chmod 000` is not supported at lines 225-241. Several “accept” cases only assert no specific validation error, because later setup phases may still fail once parsing succeeds.
- validation_or_tests: This file is the validation harness for plan setup hardening; the final summary at lines 742-751 makes any failed assertion a nonzero script exit.
- skip_candidate: `no`

### H2_DEV-HZ-147 `file` `tests/workflow-parser.test.ts`
- cursor: `[_]`
- core_role: Vitest specification for HTML workflow cartridge parsing into executable Humanize2 workflow nodes, declarations, templates, views, and parse-time safety errors.
- algorithmic_behavior: Imports `parseWorkflowCartridge` at line 4. The first test parses an inline `<h2-workflow>` with manifest, board, templates, script, agent, parallel, sleep, message, await, branch, and view, then checks normalized cartridge fields and node objects at lines 7-81. Additional tests reject misplaced `h2-expect` at lines 83-93, parse agent inputs at lines 95-124, reject `h2-input` outside agents at lines 126-136, require branch defaults at lines 138-149, require exactly one workflow root at lines 151-153, parse check/human/loop/transform primitives at lines 155-184, parse event/artifact declarations and hooks at lines 186-209, downgrade hard hooks with load warnings at lines 211-229, reject misplaced hooks and unknown executable elements at lines 231-253, and parse bundled flow files at lines 255-316. Event expressions are restricted so `event.<type>` is rejected on branch/loop but accepted on await at lines 319-360.
- inputs_outputs_state: Inputs are inline HTML strings and bundled `flow/.../workflow.html` files read with `readFile` at lines 255-287. Outputs are parsed cartridge objects, node arrays, load events, and thrown `CartridgeParseError`s asserted by Vitest. No persistent state is changed.
- gates_or_invariants: Production parser requires one `h2-workflow` root and an `h2-flow` child in `src/workflows/parser.ts` lines 43-58; validates direct-child placement for expect/hook/input at lines 144-181; maps known executable elements at lines 300-390; rejects unknown `h2-*` executable elements at lines 381-386; parses agent inputs/hooks at lines 392-485; requires exactly one `h2-default` for branches and blocks event roots outside await at lines 488-509.
- dependencies_and_callers: Depends on Vitest and `src/workflows/parser.ts`. Runtime caller is `src/workflows/coordinator.ts`, which imports the parser and loads cartridges. Bundled cartridges under `flow/gen-idea`, `flow/gen-plan`, `flow/refine-plan`, `flow/rlcr`, and `flow/experimental/team-intervention-smoke` act as fixture coverage.
- edge_cases_or_failure_modes: Protects against silently ignored executable tags, misplaced agent-only metadata, branch nodes without default exits, unsupported hard hook enforcement being misrepresented, and event-root expressions being used in unsupported control contexts. Helper flatteners at lines 363-389 avoid shallow-only assertions for nested nodes.
- validation_or_tests: This is a TypeScript unit test suite; expected behavior is asserted with `expect(...).toMatchObject`, `toThrow`, and bundled fixture parsing.
- skip_candidate: `no`

### H2_DEV-HZ-177 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Prompt block that gates agent shutdown/review when local task tracking still contains incomplete items.
- algorithmic_behavior: Renders `{{INCOMPLETE_LIST}}` into a stop-time warning at line 5, then instructs the agent to complete remaining tasks, mark each task completed using `TaskUpdate`, and only proceed to summary/stop after all are complete at lines 7-10. It explicitly blocks Codex review until tasks are finished at line 12.
- inputs_outputs_state: Input is the templated incomplete task list. Output is a prompt message that should alter agent control flow by preventing premature review or exit. State transition expected by the prompt is task statuses moving to `completed` through `TaskUpdate`.
- gates_or_invariants: Invariant is “no stop/review while incomplete tasks remain.” The gate depends on the runtime prompt assembler detecting incomplete tasks and inserting this block.
- dependencies_and_callers: Depends on the template rendering system that substitutes `{{INCOMPLETE_LIST}}`; likely coordinated with stop/review hooks that inspect task state. It is a prompt-level algorithmic guard rather than executable code.
- edge_cases_or_failure_modes: If the incomplete list is stale or incorrectly generated, the prompt can block unnecessarily or miss unfinished work. If the runtime agent lacks or ignores `TaskUpdate`, enforcement is advisory rather than mechanical.
- validation_or_tests: No direct test discovered for this exact file in assigned inspection; template robustness more broadly is covered under `tests/robustness/test-template-error-robustness.sh` and `test-template-stress-robustness.sh`.
- skip_candidate: `no`

### H2_DEV-HZ-207 `file` `prompt-template/claude/methodology-analysis-prompt.md`
- cursor: `[_]`
- core_role: RLCR exit-phase prompt that routes a completed loop into sanitized methodology analysis before fully exiting.
- algorithmic_behavior: Receives exit metadata placeholders `{{EXIT_REASON}}`, `{{EXIT_REASON_DESCRIPTION}}`, `{{CURRENT_ROUND}}`, `{{MAX_ITERATIONS}}`, and `{{LOOP_DIR}}` at lines 3-7 and 16-20. It instructs spawning an Opus analysis agent to read `round-*-summary.md` and `round-*-review-result.md`, write `methodology-analysis-report.md`, and follow strict sanitization rules at lines 12-31. It defines methodology focus areas at lines 32-40, requires subsequent user-facing content to be derived only from the sanitized report at lines 43-45, branches on no-improvements vs improvements at lines 47-69, optionally asks the user to file a GitHub issue, and requires `methodology-analysis-done.md` before exit at lines 71-73.
- inputs_outputs_state: Inputs are loop records in `{{LOOP_DIR}}`, exit reason metadata, and user responses via `AskUserQuestion`. Outputs are `methodology-analysis-report.md`, optional `gh issue create --repo PolyArch/humanize ...`, and mandatory `methodology-analysis-done.md`. State transition is RLCR loop exit -> methodology analysis phase -> completion marker.
- gates_or_invariants: Sanitization is the primary invariant: no paths, identifiers, branch names, hashes, domain terms, code snippets, raw errors, project URLs, or project-identifying information at lines 22-30. Another gate requires all later user-facing content to derive solely from the sanitized report at lines 43-45. Exit is blocked until meaningful completion marker content is written at lines 71-73.
- dependencies_and_callers: Depends on Claude-style Agent tooling with `model: "opus"`, `AskUserQuestion`, filesystem access to the loop dir, and optionally GitHub CLI `gh` at lines 57-68. It coordinates with session cancellation because `scripts/cancel-rlcr-session.sh` recognizes `methodology-analysis-state.md` as cancellable active state at lines 95-104.
- edge_cases_or_failure_modes: If the spawned agent leaks project-specific details, the prompt’s downstream-only-from-report rule can propagate sanitized output only if the report is reviewed correctly. If `gh` is unavailable, it falls back to manual title/body text at lines 67-68. If user declines, it still requires the done marker at line 62.
- validation_or_tests: No direct executable test found for this exact prompt; behavior is contractually specified in the template and indirectly covered by template rendering robustness tests.
- skip_candidate: `no`

### H2_DEV-HZ-237 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Focused robustness specification for the hook-level RLCR cancel authorization algorithm, especially command/path injection and symlink bypass prevention.
- algorithmic_behavior: Sources `hooks/lib/loop-common.sh` and test helpers at lines 15-18, creates a mock active loop directory and `state.md` at lines 27-30, then calls `is_cancel_authorized "$LOOP_DIR" "$COMMAND_LOWER"` with controlled `mv` command strings. Positive cases accept a valid cancel with signal file, `finalize-state.md`, single quotes, and unquoted paths at lines 39-89. Negative cases reject missing signal, command substitution, backticks, semicolon/AND/OR/pipe chaining, wrong destination/source, extra args, newline injection, variable/IFS expansion, non-`mv` commands, mixed quotes, trailing spaces, non-standard source names, symlinked `state.md`/`finalize-state.md`, and paths containing `finalize` that previously risked substring misclassification at lines 99-405.
- inputs_outputs_state: Inputs are temporary loop dirs, `.cancel-requested` marker, mock `state.md`/`finalize-state.md` files, symlinks, and crafted command strings lowercased before validation. Outputs are pass/fail records through `pass`, `fail`, `print_test_summary`, and final exit from the summary at lines 407-412. It creates/removes temp files inside the test dir.
- gates_or_invariants: Mirrors `is_cancel_authorized` contract in `hooks/lib/loop-common.sh`: signal file required, command substitution/backticks/newlines/operators rejected, multiple trailing spaces rejected, `$loop_dir` normalized, remaining `$` rejected, command must be `mv`, exactly source and destination are allowed, quote style must be consistent, and source symlink is rejected. These invariants are documented at `hooks/lib/loop-common.sh` lines 1039-1056 and implemented starting lines 1060-1125.
- dependencies_and_callers: Directly depends on `is_cancel_authorized` from `hooks/lib/loop-common.sh`. Production callers include `hooks/loop-bash-validator.sh`, which checks cancel commands before permitting shell execution. Test helpers provide `setup_test_dir`, `pass`, `fail`, and summary behavior.
- edge_cases_or_failure_modes: The test intentionally accepts `/./` path normalization at lines 265-276 while rejecting symlink and command-substitution bypasses. It protects a regression where a loop directory containing the word `finalize` could cause incorrect source classification at lines 369-405. It validates authorization parsing, not the full `scripts/cancel-rlcr-session.sh` CLI flow.
- validation_or_tests: This file is itself registered in the main robustness list at `tests/run-all-tests.sh` line 130 and exits nonzero on any failed assertion.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `8/8 unique item evidence headings above`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`