# agent_02 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 9
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: The `agents` directory is a prompt-specification layer for repository-aware preflight, quiz, and BitLesson selection agents. It contains exactly four direct files: `agents/bitlesson-selector.md`, `agents/draft-relevance-checker.md`, `agents/plan-compliance-checker.md`, and `agents/plan-understanding-quiz.md`; recursive inspection found no nested directories.
- algorithmic_behavior: `bitlesson-selector` selects only directly relevant lessons and returns a stable two-line result, `LESSON_IDS` plus `RATIONALE` (`agents/bitlesson-selector.md:26-41`). `draft-relevance-checker` explores repo context, compares a draft to repository purpose, and emits either `RELEVANT` or `NOT_RELEVANT` (`agents/draft-relevance-checker.md:14-30`). `plan-compliance-checker` performs two RLCR entry gates: repository relevance and branch-switch detection, then emits exactly one of `PASS`, `FAIL_RELEVANCE`, or `FAIL_BRANCH_SWITCH` (`agents/plan-compliance-checker.md:16-55`). `plan-understanding-quiz` generates exactly two four-option implementation-comprehension questions plus a plan summary (`agents/plan-understanding-quiz.md:29-75`).
- inputs_outputs_state: Inputs are prompt-provided task, path, plan, draft, and BitLesson content. Outputs are strict text verdicts consumed by slash-command workflows and workflow cartridges. These files do not mutate state directly; they determine whether later stateful RLCR setup/progress may continue. `bitlesson-selector` is also mirrored by `scripts/bitlesson-select.sh`, which validates required CLI inputs and BitLesson file existence (`scripts/bitlesson-select.sh:64-85`), returns `LESSON_IDS: NONE` if no lessons exist (`scripts/bitlesson-select.sh:93-97`), and builds a no-tools selector prompt (`scripts/bitlesson-select.sh:147-179`).
- gates_or_invariants: The plan compliance agent is fail-closed at command level: malformed or non-matching output stops `/start-rlcr-loop` (`commands/start-rlcr-loop.md:52-56`). Branch switching is explicitly incompatible with RLCR because the working branch must remain constant (`agents/plan-compliance-checker.md:31-47`). Quiz output requires all 13 fields, and malformed quiz output degrades to a warning rather than blocking (`commands/start-rlcr-loop.md:75-86`). BitLesson selection prefers precision, allows `NONE`, and has a stable output contract (`agents/bitlesson-selector.md:28-39`).
- dependencies_and_callers: `/gen-plan` invokes `humanize:draft-relevance-checker` after IO validation (`commands/gen-plan.md:154-168`). `/start-rlcr-loop` invokes `plan-compliance-checker` and `plan-understanding-quiz` (`commands/start-rlcr-loop.md:37-86`). The first-party RLCR workflow cartridge includes `plan-compliance-checker` as a Claude agent node and branches on its pass/fail artifact (`flow/rlcr/workflow.html:48-55`), then runs the quiz generator/human confirmation path (`flow/rlcr/workflow.html:56-60`). BitLesson selector requirements are injected into round prompts (`commands/start-rlcr-loop.md:168-177`, `prompt-template/claude/next-round-prompt.md:42-45`) and agent-team coordination (`prompt-template/claude/agent-teams-core.md:23`).
- edge_cases_or_failure_modes: Relevance checks intentionally lean lenient to avoid false rejections (`agents/draft-relevance-checker.md:31-36`, `agents/plan-compliance-checker.md:56-62`). Branch-switch detection disambiguates safe `git checkout -- <file>` and negated instructions (`agents/plan-compliance-checker.md:40-45`). Quiz agent must keep the plan language and can derive questions from sparse implementation hints (`agents/plan-understanding-quiz.md:77-85`). BitLesson routing can fall back from Claude to Codex when dependencies/config require it (`scripts/bitlesson-select.sh:113-141`, `scripts/bitlesson-select.sh:187-220`).
- validation_or_tests: Agent file existence, frontmatter, model, and naming are covered by tests referenced for gen-plan and install paths, including `tests/test-gen-plan.sh` references to `draft-relevance-checker` and `tests/test-codex-hook-install.sh` references to the `bitlesson-selector` shim. Workflow-level behavior for `plan-compliance-checker` is exercised in workflow projection/gap tests referenced by `rg`, and the cartridge itself encodes the branch point in `flow/rlcr/workflow.html:48-55`.
- skip_candidate: `no`

### H2_DEV-HZ-032 `file` `commands/cancel-rlcr-loop.md`
- cursor: `[_]`
- core_role: This slash-command definition is the user-facing cancellation algorithm for active RLCR loops. It constrains allowed tools to the cancellation script, the forced variant, and `AskUserQuestion`, with model invocation disabled (`commands/cancel-rlcr-loop.md:1-5`).
- algorithmic_behavior: The command runs `${CLAUDE_PLUGIN_ROOT}/scripts/cancel-rlcr-loop.sh`, inspects the first output line, and maps machine statuses into user-facing actions (`commands/cancel-rlcr-loop.md:11-23`). If the script returns `FINALIZE_NEEDS_CONFIRM`, it asks the user whether to force-cancel or let finalize complete (`commands/cancel-rlcr-loop.md:24-35`).
- inputs_outputs_state: Inputs are the active project context, plugin root, script output status, and optionally a user confirmation. Outputs are either a no-loop message, a cancellation report, or a finalize-continuation message. The underlying script creates `.cancel-requested`, removes pending session/methodology markers, and renames the active state file to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:156-167`).
- gates_or_invariants: Finalize Phase is protected by a confirmation gate unless `--force` is explicitly used (`commands/cancel-rlcr-loop.md:24-33`, `scripts/cancel-rlcr-loop.sh:139-149`). The command states the key invariant: active loops are represented only by `state.md`, `methodology-analysis-state.md`, or `finalize-state.md` in the newest loop directory (`commands/cancel-rlcr-loop.md:37-39`).
- dependencies_and_callers: Depends directly on `scripts/cancel-rlcr-loop.sh`, which resolves project root, calls `find_active_loop`, and operates globally in the current project rather than filtering by session (`scripts/cancel-rlcr-loop.sh:69-91`). Other prompts and hooks point users to this command when loop state is invalid or needs manual termination.
- edge_cases_or_failure_modes: No active directory returns `NO_LOOP`; an existing loop directory with no active state file returns `NO_ACTIVE_LOOP` (`scripts/cancel-rlcr-loop.sh:93-120`). Finalize cancellation without confirmation returns exit code 2 and `FINALIZE_NEEDS_CONFIRM` (`scripts/cancel-rlcr-loop.sh:139-149`). Unknown CLI options exit as script errors (`scripts/cancel-rlcr-loop.sh:57-61`).
- validation_or_tests: Behavior is specified through the command status table (`commands/cancel-rlcr-loop.md:17-23`) and by shared state naming tests that verify terminal names like `cancel-state.md` are not active cursors (`tests/test-state-exit-naming.sh:97-159`).
- skip_candidate: `no`

### H2_DEV-HZ-062 `file` `scripts/rlcr-stop-gate.sh`
- cursor: `[_]`
- core_role: This script adapts `hooks/loop-codex-stop-hook.sh` into a reusable non-hook gate for skills and external workflows, preserving the same RLCR stop/continue decisions (`scripts/rlcr-stop-gate.sh:3-15`).
- algorithmic_behavior: It resolves the plugin root and default project root, parses optional `--session-id`, `--transcript-path`, `--project-root`, and `--json`, then constructs a Claude Stop-hook-compatible JSON payload (`scripts/rlcr-stop-gate.sh:19-34`, `scripts/rlcr-stop-gate.sh:48-79`, `scripts/rlcr-stop-gate.sh:99-123`). It invokes the real stop hook with `CLAUDE_PROJECT_DIR` pinned to the resolved target (`scripts/rlcr-stop-gate.sh:125-129`).
- inputs_outputs_state: Inputs come from CLI flags and `CLAUDE_SESSION_ID`, `CLAUDE_TRANSCRIPT_PATH`, `CODEX_MODEL`, and `CODEX_PERMISSION_MODE` environment defaults (`scripts/rlcr-stop-gate.sh:29-33`). Outputs are shell exit codes: 0 allow, 10 block, 20 wrapper/runtime error (`scripts/rlcr-stop-gate.sh:8-11`). It does not mutate loop state itself; state transitions are delegated to `loop-codex-stop-hook.sh`.
- gates_or_invariants: If no project root resolves, the gate allows exit as benign outside-project usage (`scripts/rlcr-stop-gate.sh:81-87`). Missing hook executable or missing `jq` is a wrapper error (`scripts/rlcr-stop-gate.sh:89-97`). Non-JSON hook output and unexpected decisions are hard wrapper errors (`scripts/rlcr-stop-gate.sh:142-146`, `scripts/rlcr-stop-gate.sh:176-178`). A hook `decision: block` maps to exit 10 and either raw JSON or formatted `BLOCK:` output (`scripts/rlcr-stop-gate.sh:152-159`).
- dependencies_and_callers: Depends on `hooks/lib/project-root.sh`, `hooks/loop-codex-stop-hook.sh`, and `jq` (`scripts/rlcr-stop-gate.sh:25-27`, `scripts/rlcr-stop-gate.sh:94-97`). It is referenced by Kimi RLCR skill wiring and tests; stop-hook background tests exercise transcript forwarding and allow/block behavior.
- edge_cases_or_failure_modes: Empty `session_id` is intentionally serialized as JSON `null` without dropping `transcript_path`, avoiding a prior jq object-collapse bug (`scripts/rlcr-stop-gate.sh:103-123`). Empty hook output means allow (`scripts/rlcr-stop-gate.sh:136-140`). JSON output without a `decision` also allows while surfacing `systemMessage` (`scripts/rlcr-stop-gate.sh:162-174`).
- validation_or_tests: `tests/test-stop-gate.sh` verifies default project-root/cwd blocking (`tests/test-stop-gate.sh:64-89`), `--project-root` override (`tests/test-stop-gate.sh:91-116`, `tests/test-stop-gate.sh:298-318`), tracked Humanize-state block reporting (`tests/test-stop-gate.sh:118-145`), no-loop allow (`tests/test-stop-gate.sh:180-204`), and the empty-session transcript forwarding regression (`tests/test-stop-gate.sh:206-287`).
- skip_candidate: `no`

### H2_DEV-HZ-092 `file` `tests/test-bitlesson-validate-delta.sh`
- cursor: `[_]`
- core_role: This is an executable specification for `scripts/bitlesson-validate-delta.sh`, especially the rule that add/update BitLesson Delta summaries require meaningful `Notes` and that hidden/template-only deltas do not count.
- algorithmic_behavior: The test builds a temporary BitLesson file and round summary files, then runs the validator with fixed options: summary path, BitLesson path, relative BitLesson path, `--allow-empty-none false`, template dir, and current round (`tests/test-bitlesson-validate-delta.sh:21-62`).
- inputs_outputs_state: Inputs are synthetic `# Round Summary` Markdown and `.humanize/bitlesson.md` content. Expected outputs are either empty stdout for pass (`tests/test-bitlesson-validate-delta.sh:86-95`) or Claude-hook-style block JSON where `.decision == "block"` (`tests/test-bitlesson-validate-delta.sh:64-84`). No persistent repo state is changed beyond temp files.
- gates_or_invariants: `Action: add` with whitespace-only notes blocks (`tests/test-bitlesson-validate-delta.sh:101-104`). `Action: update` with bracket or angle-bracket placeholder notes blocks (`tests/test-bitlesson-validate-delta.sh:106-114`). Delta sections inside fenced code blocks or HTML comments block as invalid/missing real summary content (`tests/test-bitlesson-validate-delta.sh:116-142`). Valid explanatory notes pass (`tests/test-bitlesson-validate-delta.sh:144-152`).
- dependencies_and_callers: Directly depends on `scripts/bitlesson-validate-delta.sh`, `prompt-template`, `jq`, and `tests/test-helpers.sh` (`tests/test-bitlesson-validate-delta.sh:8-13`). The stop hook invokes the same validator during RLCR exit checks (`hooks/loop-codex-stop-hook.sh:869-876` from reference search).
- edge_cases_or_failure_modes: The validator ignores `## BitLesson Delta` headings when inside fences/comments via fence/comment state tracking (`scripts/bitlesson-validate-delta.sh:92-179`). It validates one action only (`scripts/bitlesson-validate-delta.sh:202-218`), requires concrete lesson IDs for add/update (`scripts/bitlesson-validate-delta.sh:269-279`), rejects empty or placeholder notes (`scripts/bitlesson-validate-delta.sh:281-297`), validates ID format (`scripts/bitlesson-validate-delta.sh:322-367`), and rejects IDs missing from the BitLesson file (`scripts/bitlesson-validate-delta.sh:369-384`).
- validation_or_tests: This file is the validation. It ends with `print_test_summary "BitLesson Delta Validator Test Summary"` and exits through helper summary behavior (`tests/test-bitlesson-validate-delta.sh:154`).
- skip_candidate: `no`

### H2_DEV-HZ-122 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: This executable spec defines RLCR state-file cursor semantics: active state names versus terminal renamed state files, plus new `.humanize/rlcr` path detection.
- algorithmic_behavior: It creates temp git-backed loop directories, sources `hooks/lib/loop-common.sh`, and exercises `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir` (`tests/test-state-exit-naming.sh:31-67`).
- inputs_outputs_state: Inputs are synthetic loop directories containing `state.md` or terminal files such as `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md`. Outputs are pass/fail assertions and actual state-file renames when `end_loop` runs.
- gates_or_invariants: `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` are not active loops (`tests/test-state-exit-naming.sh:51-159`). `state.md` is active (`tests/test-state-exit-naming.sh:76-95`). The newest active directory wins (`tests/test-state-exit-naming.sh:161-180`). `end_loop` accepts only `complete`, `cancel`, `maxiter`, `stop`, and `unexpected` (`tests/test-state-exit-naming.sh:186-230`).
- dependencies_and_callers: Depends on `find_active_loop`, which checks only the newest directory without a session filter to prevent stale older loops from being revived (`hooks/lib/loop-common.sh:308-357`). Depends on `end_loop`, which renames `state.md` to `<reason>-state.md` (`hooks/lib/loop-common.sh:1538-1571`). Depends on `is_in_humanize_loop_dir`, which matches `.humanize/rlcr/` only (`hooks/lib/loop-common.sh:1257-1261`).
- edge_cases_or_failure_modes: Invalid `end_loop` reason fails with an error (`tests/test-state-exit-naming.sh:186-204`). Missing source state file fails gracefully (`tests/test-state-exit-naming.sh:232-243`). Legacy `.humanize-loop.local` paths must not be detected or searched (`tests/test-state-exit-naming.sh:258-291`).
- validation_or_tests: This file is the validation. It exits with the failure count (`tests/test-state-exit-naming.sh:293-302`) and is included in the broader test runner list from `tests/run-all-tests.sh` reference search.
- skip_candidate: `no`

### H2_DEV-HZ-152 `file` `tests/workflow-storage.test.ts`
- cursor: `[_]`
- core_role: This Vitest suite specifies workflow cartridge/run/scheduler snapshot persistence, reload preference order, schema compatibility, and dashboard restore behavior.
- algorithmic_behavior: It creates temporary state directories, uses `FileWorkflowStore.create`, starts workflows through `WorkflowCoordinator`, records artifacts/boards, and reloads persisted runs/snapshots (`tests/workflow-storage.test.ts:21-99`).
- inputs_outputs_state: Inputs include workflow HTML cartridges, optional `sourcePath`, artifact deliveries, board patches, and manually written snapshot JSON. Outputs are persisted cartridge HTML/metadata, run JSON, scheduler snapshot JSON, and restored dashboard-rendered views. The storage schema must be `humanize2.workflow.storage.v1` (`tests/workflow-storage.test.ts:97-125`).
- gates_or_invariants: Current `sourcePath` HTML is preferred over stored fallback HTML (`tests/workflow-storage.test.ts:22-40`). Bundled first-party cartridge source is preferred over stale stored HTML for `rlcr` (`tests/workflow-storage.test.ts:42-53`). Unsupported snapshot storage schema versions reject with an error (`tests/workflow-storage.test.ts:101-126`). Terminal stored runs can still be imported for dashboard listing and rendered views, even with older records missing `loopIterations` and `vars` (`tests/workflow-storage.test.ts:128-177`).
- dependencies_and_callers: Directly covers `src/workflows/storage.ts`: directory creation (`src/workflows/storage.ts:27-32`), cartridge recording (`src/workflows/storage.ts:35-49`), run recording (`src/workflows/storage.ts:51-54`), snapshot recording (`src/workflows/storage.ts:56-59`), run loading/sorting (`src/workflows/storage.ts:61-69`), snapshot schema check (`src/workflows/storage.ts:71-80`), and cartridge HTML resolution order (`src/workflows/storage.ts:83-127`). Recovery imports/restores persisted runs via `restoreWorkflowRunsFromStore` (`src/workflows/recovery.ts:16-39`).
- edge_cases_or_failure_modes: Missing snapshot returns undefined, but present snapshot with wrong schema throws (`src/workflows/storage.ts:71-80`). Bad cartridge IDs cannot be used for bundled path traversal because bundled lookup requires a safe directory name (`src/workflows/storage.ts:118-127`, `src/workflows/storage.ts:142-144`). Terminal runs import without scheduler snapshot replay (`src/workflows/recovery.ts:26-39`, `src/workflows/recovery.ts:77-79`).
- validation_or_tests: This file is the validation. It uses fake Codex/Claude backends to avoid external agent calls while preserving run-coordinator contracts (`tests/workflow-storage.test.ts:186-246`), deterministic IDs (`tests/workflow-storage.test.ts:248-256`), and deterministic timestamps (`tests/workflow-storage.test.ts:258-261`).
- skip_candidate: `no`

### H2_DEV-HZ-182 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: This block template is the user-facing policy message for protecting `.humanize/rlcr/<loop>/plan.md`, which is a backup of the original plan and must not be modified (`prompt-template/block/plan-backup-protected.md:1-7`).
- algorithmic_behavior: The template itself is static text. Runtime validators load it when a tool attempt would write, edit, or shell-modify the loop-local `plan.md` backup, then block the tool operation.
- inputs_outputs_state: Input is only the validator’s detection that a targeted path is the RLCR plan backup. Output is a block reason explaining to read the backup instead of modifying it. No variables are required by the template.
- gates_or_invariants: The invariant is plan integrity: the loop backup must remain an immutable reference to the original plan (`prompt-template/block/plan-backup-protected.md:3-5`). The allowed behavior is read/reference only (`prompt-template/block/plan-backup-protected.md:7`).
- dependencies_and_callers: `loop-write-validator.sh` loads this template for writes to loop `plan.md` (`hooks/loop-write-validator.sh:236-246`). `loop-edit-validator.sh` loads it for edits (`hooks/loop-edit-validator.sh:186-197`). `loop-bash-validator.sh` loads it for shell commands that modify `.humanize/rlcr/.../plan.md` (`hooks/loop-bash-validator.sh:490-500`).
- edge_cases_or_failure_modes: If the template is missing, callers use a fallback message via `load_and_render_safe`; because this template has no substitutions, variable-rendering failure is not a concern. Path checks are scoped to `.humanize/rlcr/`, so normal project plan files remain governed by separate plan-file validation rather than this backup block.
- validation_or_tests: Template-reference tests scan `load_template` and `load_and_render_safe` call sites for missing templates, and hook robustness tests broadly exercise state/goal-tracker write blocking. This template’s correctness is mainly enforced by the validators that call it.
- skip_candidate: `no`

### H2_DEV-HZ-212 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: This prompt fragment injects the per-round push requirement when RLCR is configured with `--push-every-round` (`prompt-template/claude/push-every-round-note.md:1-2`).
- algorithmic_behavior: The stop hook appends the template to the next round prompt only when `PUSH_EVERY_ROUND` is true (`hooks/loop-codex-stop-hook.sh:2179-2185`). That turns state-file configuration into an explicit next-agent instruction.
- inputs_outputs_state: Input is the parsed loop state value `push_every_round`. Output is a short instruction in the generated prompt requiring remote push after each round. It does not change git state itself; it influences the next round’s required behavior.
- gates_or_invariants: The invariant is that a round is not operationally complete under push-every-round mode until commits are pushed. If the template is absent, the stop hook falls back to “Also push your changes after committing.” (`hooks/loop-codex-stop-hook.sh:2181-2184`).
- dependencies_and_callers: The same policy is inlined for round-0 prompt generation in `setup-rlcr-loop.sh` when `PUSH_EVERY_ROUND` is true (`scripts/setup-rlcr-loop.sh:1438-1444`). Later rounds use this template through `loop-codex-stop-hook.sh`.
- edge_cases_or_failure_modes: The template starts with a blank line, which is intentional spacing when appended to prompt files (`prompt-template/claude/push-every-round-note.md:1-2`). Missing-template fallback keeps the algorithmic instruction alive, but with less specific wording.
- validation_or_tests: Template existence is covered by template-reference/comprehensive template tests from the test suite. Runtime behavior is covered indirectly by stop-hook prompt-generation paths.
- skip_candidate: `no`

### H2_DEV-HZ-242 `file` `tests/robustness/test-hook-system-robustness.sh`
- cursor: `[_]`
- core_role: This is a broad adversarial executable spec for hook robustness across edit, write, read, bash, plan-file, and stop-hook flows. It focuses on JSON input validation, command injection boundaries, state parsing, race safety, and stop-hook behavior (`tests/robustness/test-hook-system-robustness.sh:3-15`).
- algorithmic_behavior: The script sets up temp fixtures, sources shared loop helpers and timeout support, and invokes hook scripts by piping synthetic Claude hook JSON with `CLAUDE_PROJECT_DIR` pinned to test projects (`tests/robustness/test-hook-system-robustness.sh:17-25`).
- inputs_outputs_state: Inputs are JSON tool events, temporary `.humanize/rlcr` state files, temporary plan/goal-tracker files, git repositories, and a mock `codex` binary for active-loop stop-hook testing (`tests/robustness/test-hook-system-robustness.sh:769-782`). Outputs are hook exit codes, optional JSON block decisions, and pass/fail summaries.
- gates_or_invariants: Edit validator accepts valid JSON, blocks `state.md`, handles malformed/missing fields without crashing, allows paths outside `.humanize` to the sandbox, and blocks traversal back into loop `state.md` (`tests/robustness/test-hook-system-robustness.sh:39-154`). Plan-file validator allows valid plan edits/non-plan files and handles empty JSON (`tests/robustness/test-hook-system-robustness.sh:164-242`). Bash validator blocks `state.md` and post-round goal-tracker modifications while letting unrelated commands pass to sandbox policy (`tests/robustness/test-hook-system-robustness.sh:316-416`, `tests/robustness/test-hook-system-robustness.sh:559-572`).
- dependencies_and_callers: Depends on `hooks/loop-edit-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, `jq`, and git. It validates shared parser functions such as `get_current_round` (`hooks/lib/loop-common.sh:430-440`) and strict stop-hook state requirements (`hooks/lib/loop-common.sh:533-599`).
- edge_cases_or_failure_modes: Tests whitespace and quoted/special path values in state frontmatter (`tests/robustness/test-hook-system-robustness.sh:252-287`), malformed state with missing closing delimiter (`tests/robustness/test-hook-system-robustness.sh:289-306`), mutable versus immutable goal-tracker changes after round 0 (`tests/robustness/test-hook-system-robustness.sh:418-543`), old-session goal-tracker read blocking (`tests/robustness/test-hook-system-robustness.sh:545-557`), concurrent hook invocation state preservation (`tests/robustness/test-hook-system-robustness.sh:611-641`), no-state/corrupt-state/missing-required-field active-loop stop-hook behavior (`tests/robustness/test-hook-system-robustness.sh:651-734`), and large/null/deep JSON payload handling (`tests/robustness/test-hook-system-robustness.sh:800-842`).
- validation_or_tests: This file is the validation and exits through `print_test_summary "Hook System Robustness Test Summary"` (`tests/robustness/test-hook-system-robustness.sh:844-849`). Its active-loop stop-hook scenario asserts that a valid loop must produce a block decision rather than silently allowing exit (`tests/robustness/test-hook-system-robustness.sh:736-790`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `9/9 item evidence sections present, in the same order as the assignment table; item ids are only used as section headings to preserve one evidence section per assigned id`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`