# agent_19 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-019 `directory` `prompt-template/plan`
- cursor: `[_]`
- core_role: Canonical prompt-template directory for planning outputs. Recursively inspected children: [prompt-template/plan/gen-plan-template.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/prompt-template/plan/gen-plan-template.md:1) and [prompt-template/plan/refine-plan-qa-template.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/prompt-template/plan/refine-plan-qa-template.md:1).
- algorithmic_behavior: `gen-plan-template.md` defines the required plan schema: goal, acceptance criteria with positive and negative tests, path boundaries, allowed choices, feasibility hints, dependency sequence, and task breakdown. It also forces each task to carry exactly one routing tag, `coding` or `analyze`, with `analyze` routed through `/humanize:ask-codex` at lines 71-79. `refine-plan-qa-template.md` defines the refinement QA artifact: comment ledger, answers, research findings, applied plan changes, remaining decisions, and metadata.
- inputs_outputs_state: Inputs are draft requirements, Codex analysis, user comments, config such as `alternative_plan_language`, and plan validation state. Outputs are the main plan, optional translated plan variant, and mandatory QA document. The plan template preserves workflow state through AC IDs, task IDs, dependencies, convergence status, and pending decision records; the QA template preserves comment disposition state through `CMT-*` rows and refinement metadata.
- gates_or_invariants: The plan template requires deterministic positive and negative verification for ACs, explicit upper/lower scope bounds, exactly one routing tag per task, and no plan-specific workflow terms in implementation code at [gen-plan-template.md:101](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/prompt-template/plan/gen-plan-template.md:101). The language variant gate only writes translated variants for supported non-English configured languages at lines 110-120. The QA template requires one ledger row per extracted comment and complete population of all sections.
- dependencies_and_callers: `commands/gen-plan.md` invokes `ask-codex.sh` before Claude plan synthesis and embeds the same task-routing contract at [commands/gen-plan.md:190](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/commands/gen-plan.md:190) and [commands/gen-plan.md:447](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/commands/gen-plan.md:447). `commands/refine-plan.md` requires reading and fully populating the QA template at [commands/refine-plan.md:457](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/commands/refine-plan.md:457).
- edge_cases_or_failure_modes: Deterministic designs may collapse upper and lower bounds into a narrow fixed choice. Unsupported or English `alternative_plan_language` produces no variant. Mixed-language refinement must keep QA language aligned with the refined plan. Missing AC/task references or unresolved user decisions block refinement rather than allowing invented requirements.
- validation_or_tests: [tests/test-gen-plan.sh:708](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-gen-plan.sh:708) verifies the plan structure block in `commands/gen-plan.md` matches `gen-plan-template.md`. [tests/test-refine-plan.sh:25](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-refine-plan.sh:25) tracks the refine QA template path for refine-plan validation coverage. Tests were inspected, not executed, because this branch export is read-only.
- skip_candidate: `no`

### H2_DEV-HZ-049 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Read` validator for RLCR loop safety. It prevents agents from reading stale, misplaced, or phase-inappropriate RLCR artifacts while allowing normal non-loop reads.
- algorithmic_behavior: The script reads hook JSON from stdin, validates JSON structure and nesting, exits early for non-`Read` tools, requires `tool_input.file_path`, resolves session-aware active loop state, and applies layered read gates. Main branches: todos-file blocking at [loop-read-validator.sh:56](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/loop-read-validator.sh:56), methodology-analysis read isolation at line 76, restricted RLCR file detection at line 175, finalize-phase contract blocking at line 218, goal-tracker path validation at line 227, round-number validation at line 286, and symlink-aware directory validation at line 311.
- inputs_outputs_state: Inputs are hook JSON fields `tool_name`, `tool_input.file_path`, optional `session_id`, plus project/loop state resolved from `.humanize/rlcr`. Outputs are exit `0` to allow, exit `1` for malformed input or malformed state, and exit `2` with a rendered block message for policy denial. It does not mutate state; it reads active state to derive current round and phase.
- gates_or_invariants: Fails closed on invalid JSON, missing `file_path`, overly deep JSON, malformed state frontmatter, stale round summary/prompt/contract files, wrong loop directory, finalize contract reads, and methodology-analysis leakage. During methodology analysis, only `methodology-analysis-report.md`, `methodology-analysis-done.md`, and `methodology-analysis-state.md` are readable from the loop directory; other project files are blocked. It uses `canonicalize_path_prefix` so a symlink leaf cannot authorize a different underlying file.
- dependencies_and_callers: Registered as the `Read` PreToolUse hook in [hooks/hooks.json:34](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/hooks.json:34). Depends on `hooks/lib/loop-common.sh` for JSON validation, active-loop discovery, strict state parsing, allowlist checks, and block messages; these helpers are defined around [loop-common.sh:91](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/lib/loop-common.sh:91), [loop-common.sh:332](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/lib/loop-common.sh:332), and [loop-common.sh:533](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/lib/loop-common.sh:533). Template rendering uses `load_and_render_safe` at [template-loader.sh:188](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/lib/template-loader.sh:188).
- edge_cases_or_failure_modes: If no project root or no active loop is found, it allows natural reads to avoid false blocking. Session filtering prevents stale loops from other sessions from taking over. If `realpath` cannot resolve methodology paths, traversal segments and unresolved symlinks are blocked. A missing or malformed state file blocks with exit `1`; stale historical files may pass only if explicitly allowlisted by `is_allowlisted_file`.
- validation_or_tests: [tests/robustness/test-hook-input-robustness.sh:35](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/robustness/test-hook-input-robustness.sh:35) covers valid/malformed/missing-field JSON, deep nesting, and binary input handling. [tests/test-allowlist-validators.sh:294](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-allowlist-validators.sh:294) covers read allowlist behavior and stale round blocking. [tests/test-finalize-phase.sh:1066](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-finalize-phase.sh:1066) covers finalize-state parsing and contract blocking. [tests/test-template-references.sh:176](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-template-references.sh:176) checks critical validators use safe template fallbacks.
- skip_candidate: `no`

### H2_DEV-HZ-079 `file` `tests/mcp.test.ts`
- cursor: `[_]`
- core_role: Vitest executable specification for the Humanize2 MCP stdio surface and its JSON-RPC hub parity.
- algorithmic_behavior: Starts the MCP server via `node` plus `tsx` with fake agent env in `serverOptions()` at [tests/mcp.test.ts:15](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/mcp.test.ts:15). It then verifies tool-list parity, `agent_run`, long-running RPC timeout behavior, child-run spawn/wait/message flow, and workflow load/start/artifact/wait behavior.
- inputs_outputs_state: Inputs are MCP tool calls, JSON-RPC hub calls, fake backend env vars, deterministic ID factories, and workflow HTML. Outputs are MCP text content containing JSON payloads. State transitions covered include parent run creation, child run creation, child continuation via message, workflow cartridge load, workflow run start, artifact delivery, and workflow success.
- gates_or_invariants: Every workflow-shaped JSON-RPC method reported by `system.info` must be exposed as an MCP tool by replacing dots with underscores at [tests/mcp.test.ts:47](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/mcp.test.ts:47). Baseline tools must include `agent_run`, `agent_send_message`, `agent_spawn_child`, `agent_status`, `agent_wait`, `claude_run`, and `codex_run`. Long-running tool calls must respect a caller-supplied RPC timeout. Child messages must carry origin metadata from `HUMANIZE2_RUN_ID` and `HUMANIZE2_RUN_SHORT_NAME`.
- dependencies_and_callers: Imports `callHumanizeTool` and `listHumanizeTools` from `src/dev-client.ts`, `createHubHttpServer` from `src/hub/http-server.ts`, `AgentRunCoordinator`, `HumanizeService`, and `WorkflowCoordinator`. The implementation side registers the relevant MCP tools in [src/server.ts:154](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/src/server.ts:154) and workflow forwarding tools in [src/server.ts:238](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/src/server.ts:238). JSON-RPC method exposure comes from [src/hub/http-server.ts:156](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/src/hub/http-server.ts:156).
- edge_cases_or_failure_modes: The helper ID factory throws on exhausted IDs, which catches unexpected extra run creation. `afterEach` closes opened HTTP servers to avoid cross-test state. Fake backend behavior depends on `HUMANIZE2_FAKE_AGENT_RESPONSE`; delayed fake responses use `HUMANIZE2_FAKE_AGENT_DELAY_MS` as seen in [src/agents/registry.ts:11](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/src/agents/registry.ts:11).
- validation_or_tests: This file is itself the validation surface. It was inspected, not executed, due read-only export constraints and likely runtime cache writes.
- skip_candidate: `no`

### H2_DEV-HZ-109 `file` `tests/test-helpers.sh`
- cursor: `[_]`
- core_role: Shared Bash test harness for repository shell tests. It is validation infrastructure rather than production RLCR logic.
- algorithmic_behavior: Defines pass/fail/skip functions that print colored status and increment counters at [tests/test-helpers.sh:30](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-helpers.sh:30). `print_test_summary()` emits aggregate counts and returns success only when the failure counter is zero. `setup_test_dir()` creates a temporary directory with cleanup trap, and `init_test_git_repo()` creates a minimal Git repo with one committed file.
- inputs_outputs_state: Inputs are assertion labels, expected/actual strings, temp directory targets, and Git repo target directory. Outputs are stdout diagnostics and process return status from `print_test_summary`. State is stored in global counters `TESTS_PASSED`, `TESTS_FAILED`, and `TESTS_SKIPPED`, plus `TEST_DIR`.
- gates_or_invariants: Any call to `fail()` increments `TESTS_FAILED`, causing `print_test_summary()` to return nonzero at [tests/test-helpers.sh:71](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-helpers.sh:71). `setup_test_dir()` installs an EXIT trap to remove its temp directory. `init_test_git_repo()` configures local user identity and disables GPG signing to make commits deterministic in tests.
- dependencies_and_callers: Used by many shell tests, including robustness suites and command/hook tests. `rg` references show callers such as `tests/test-ask-codex.sh`, `tests/test-gen-plan.sh`, `tests/test-model-router.sh`, and `tests/robustness/*`.
- edge_cases_or_failure_modes: `init_test_git_repo()` assumes writable filesystem and available Git; those assumptions do not hold in this read-only branch export. The single `TEST_DIR` trap is simple and can be overwritten if nested temp setup is attempted in one process.
- validation_or_tests: Indirectly validated by every shell test that sources it. No separate self-test file was found for the helper itself.
- skip_candidate: `yes: shared test harness, not a core runtime algorithm; retained because it controls shell-test pass/fail semantics for core validators`

### H2_DEV-HZ-139 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Zsh-specific executable safety specification for monitor discovery loops. It ensures monitor code avoids zsh `nomatch` glob failures in empty or dotfile-only directories.
- algorithmic_behavior: Runs under zsh, creates an isolated `/tmp/test-zsh-humanize-$$` workspace, sources `scripts/humanize.sh`, then simulates monitor helper logic using `find` loops instead of unguarded globs. Test cases cover empty RLCR directories, dotfile-only directories, missing state files, empty Codex cache logs, latest-session selection, zsh runtime verification, and old glob-pattern demonstration.
- inputs_outputs_state: Inputs are zsh runtime, project root, temporary `.humanize/rlcr` content, and `XDG_CACHE_HOME`. Outputs are colored PASS/FAIL lines and final exit `0` or `1`. State transitions create and remove temporary loop/cache directories and increment local pass/fail counters.
- gates_or_invariants: Sourcing `humanize.sh` must not emit “no matches found” at [tests/test-zsh-monitor-safety.sh:72](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-zsh-monitor-safety.sh:72). Session discovery must return empty safely for no sessions and dotfile-only directories. State-file discovery must return `|unknown` when no `*-state.md` exists. Latest valid session is selected lexicographically by timestamp. The test must actually run under zsh, checked at line 282.
- dependencies_and_callers: Depends on `scripts/humanize.sh`, zsh, `find`, `/tmp`, and writable cache/temp directories. Included in the aggregate test list at [tests/run-all-tests.sh:79](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/run-all-tests.sh:79).
- edge_cases_or_failure_modes: The test intentionally does not source `test-helpers.sh`; it carries its own counters. It disables zsh `nomatch` only inside the old-pattern demonstration so the test can illustrate the historical failure without aborting. It requires a writable `/tmp`, so it was not executed in this read-only export.
- validation_or_tests: This file is the validation case for zsh monitor safety and is included in the main shell test runner.
- skip_candidate: `no`

### H2_DEV-HZ-169 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Stop-hook block-message template that explains how to handle untracked non-Humanize files when Git status is not clean.
- algorithmic_behavior: Provides a static note telling the agent that untracked files may be build artifacts, dependencies, editor files, logs, caches, or temporary files, and should usually be added to `.gitignore` rather than committed. It is loaded into `SPECIAL_NOTES` when the stop hook detects untracked files other than local `.humanize` artifacts.
- inputs_outputs_state: Input is the stop hook’s dirty Git classification; the template itself has no variables. Output is stderr/user-facing markdown appended to the Git-not-clean block. It does not mutate state but influences the next agent action by steering toward `.gitignore` hygiene.
- gates_or_invariants: The stop hook excludes `.humanize/` and legacy `.humanize-*` paths from dirty determination, then loads this template only for other untracked files at [hooks/loop-codex-stop-hook.sh:721](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/loop-codex-stop-hook.sh:721). The invariant is that generated artifacts and dependencies should not be blindly committed.
- dependencies_and_callers: Called via `load_template "$TEMPLATE_DIR" "block/git-not-clean-untracked.md"` in [hooks/loop-codex-stop-hook.sh:724](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/loop-codex-stop-hook.sh:724). Depends on template directory resolution in `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook falls back to “Review untracked files - add to .gitignore or commit them.” The template is advisory and does not distinguish which untracked file is safe; the stop hook’s Git status data provides that context.
- validation_or_tests: Template reference coverage scans stop/read/write/edit/bash validators and hook template loads in [tests/test-template-references.sh:52](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-template-references.sh:52). Stop-hook Git-clean behavior is covered elsewhere by stop-hook test suites, but this exact template content is not independently executed.
- skip_candidate: `no`

### H2_DEV-HZ-199 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Write-validator block-message template for summary files written outside the active RLCR loop directory.
- algorithmic_behavior: Emits a concise denial with the correct path placeholder `{{CORRECT_PATH}}`. The write validator uses it when a summary file target is outside `.humanize/rlcr` while an active loop exists.
- inputs_outputs_state: Input variable is `CORRECT_PATH`, supplied by the write validator. Output is rendered markdown on stderr. The gate prevents accidental summary artifacts outside the loop directory and preserves the RLCR state machine’s canonical summary location.
- gates_or_invariants: Summary files must be in the loop directory. The write validator computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"` and renders this template at [hooks/loop-write-validator.sh:292](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/hooks/loop-write-validator.sh:292).
- dependencies_and_callers: Depends on `load_and_render_safe` for variable substitution and fallback safety. The template is specifically used by `hooks/loop-write-validator.sh`; sibling contract-location handling uses a separate template.
- edge_cases_or_failure_modes: If the template is unavailable, the inline fallback says “Write summary to the correct path: {{CORRECT_PATH}}”. The template is intentionally minimal and assumes the validator has already determined the active loop and current round.
- validation_or_tests: Template reference tests include `loop-write-validator.sh` among critical scripts at [tests/test-template-references.sh:176](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-template-references.sh:176). The path-location behavior is validated through write-validator tests, though this small template is mainly a user-facing policy surface.
- skip_candidate: `no`

### H2_DEV-HZ-229 `file` `skills/ask-codex/SKILL.md`
- cursor: `[_]`
- core_role: Skill instruction file for safe one-shot Codex consultation through `scripts/ask-codex.sh`.
- algorithmic_behavior: Defines the allowed tool as the wrapper script, instructs callers to quote free-form task text, preserve supported flags as separate shell arguments, pass the remaining question as one quoted final argument, and interpret stdout/stderr plus exit codes. It explicitly warns against unquoted `$ARGUMENTS` expansion at [skills/ask-codex/SKILL.md:30](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/skills/ask-codex/SKILL.md:30).
- inputs_outputs_state: Inputs are optional `--codex-model`, optional `--codex-timeout`, and a question/task string. Outputs are Codex response on stdout, status information on stderr, and a saved `.humanize/skill/<timestamp>/output.md` response. Exit state is classified as success, validation error, timeout, or generic Codex process error.
- gates_or_invariants: Shell safety is the primary invariant: never pass free-form user text unquoted; if flags are present, reconstruct the command with flags as separate arguments and one quoted final question. Exit code `0` means usable response, `1` means validation error, `124` means timeout, and other codes should be reported with stderr.
- dependencies_and_callers: Depends on `${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh`. `commands/gen-plan.md` routes first-pass analysis through this script at [commands/gen-plan.md:190](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/commands/gen-plan.md:190). The plan template routes `analyze` tasks through `/humanize:ask-codex` at [prompt-template/plan/gen-plan-template.md:71](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/prompt-template/plan/gen-plan-template.md:71).
- edge_cases_or_failure_modes: Shell metacharacters in user text can break or alter execution if unquoted. Missing Codex, empty questions, invalid flags, and timeouts must be surfaced instead of silently ignored. The documented default is `gpt-5.5:high` with a 3600-second timeout.
- validation_or_tests: [tests/test-ask-codex.sh:481](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-ask-codex.sh:481) verifies the skill warns against bare `$ARGUMENTS`; [tests/test-ask-codex.sh:488](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-ask-codex.sh:488) verifies the safe quoted simple invocation; [tests/test-ask-codex.sh:495](/Users/wangweiyang/GitHub/humanize_branch_worktrees/h2-dev/tests/test-ask-codex.sh:495) verifies the one-quoted-final-argument guidance.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: H2_DEV-HZ-019, H2_DEV-HZ-049, H2_DEV-HZ-079, H2_DEV-HZ-109, H2_DEV-HZ-139, H2_DEV-HZ-169, H2_DEV-HZ-199, H2_DEV-HZ-229
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`