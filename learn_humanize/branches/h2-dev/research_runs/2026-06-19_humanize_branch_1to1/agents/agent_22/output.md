# agent_22 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-022 `directory` `skills/ask-gemini`
- cursor: `[_]`
- core_role: Skill entrypoint directory for the Ask Gemini workflow. Recursive inspection found one child, `skills/ask-gemini/SKILL.md`, so the directory’s responsibility is to expose a Claude/Kimi-style skill contract that delegates execution to `scripts/ask-gemini.sh`.
- algorithmic_behavior: The child skill defines a one-shot external research consultation flow. It requires quoted shell invocation, preserves recognized flags as separate arguments, passes the remaining question as one quoted final argument, and explicitly forbids unsafe `$ARGUMENTS` expansion. The skill instructs consumers to interpret stdout as Gemini’s answer and stderr as status/debug output.
- inputs_outputs_state: Inputs are optional `--gemini-model`, optional `--gemini-timeout`, and a free-form question or task. Outputs are Gemini’s stdout, status/errors on stderr, and persisted response state under `.humanize/skill/<timestamp>/output.md`. The skill states the default model and timeout at `skills/ask-gemini/SKILL.md:59-61`.
- gates_or_invariants: The command must be called through the allowed tool path in frontmatter, `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/ask-gemini.sh:*)`, at `skills/ask-gemini/SKILL.md:5`. The main invariant is shell-safety: free-form text containing spaces or shell metacharacters must not be re-parsed by the shell, documented at `skills/ask-gemini/SKILL.md:17-39`.
- dependencies_and_callers: Depends directly on `scripts/ask-gemini.sh`. Repository references show user docs route `/humanize:ask-gemini` to this behavior, while monitor tooling treats Gemini skill runs as `.humanize/skill` cache artifacts; e.g. `scripts/lib/monitor-skill.sh` supports `codex` and `gemini` cache files.
- edge_cases_or_failure_modes: Skill-level exit-code interpretation covers validation errors, timeout, and process errors at `skills/ask-gemini/SKILL.md:48-55`. The unsafe invocation form is called out as a pre-start failure risk because metacharacters can break before the script runs.
- validation_or_tests: No dedicated `tests/test-ask-gemini.sh` was present in assigned coverage, but repository references and monitor-skill support validate that Gemini skill artifacts are part of the broader skill-monitor surface. The executable behavior is primarily in `scripts/ask-gemini.sh`, covered separately below.
- skip_candidate: `no`

### H2_DEV-HZ-052 `file` `scripts/ask-gemini.sh`
- cursor: `[_]`
- core_role: Runtime script implementing the Ask Gemini skill workflow: argument parsing, prerequisite validation, project-root resolution, prompt augmentation, execution through Gemini CLI, timeout behavior, artifact persistence, and result classification.
- algorithmic_behavior: The script parses flags until `--` or first non-option argument, accumulates question parts, and joins them into a single prompt string at `scripts/ask-gemini.sh:88-139`. It validates that `gemini` exists, the question is non-empty, the model contains only safe characters, and a project root can be resolved at `scripts/ask-gemini.sh:145-178`. It creates a unique run directory and cache directory, writes `input.md`, builds Gemini args with sandbox/yolo mode, appends text output mode, prepends a mandatory Google Search instruction, writes debug command metadata, runs with timeout, then writes `output.md` and `metadata.md`.
- inputs_outputs_state: Inputs are CLI args, `HUMANIZE_GEMINI_YOLO`, `CLAUDE_PROJECT_DIR` or git context via `resolve_project_root`, `XDG_CACHE_HOME` or `HOME`, and the Gemini CLI. Outputs include stdout response, stderr status lines, `.humanize/skill/<unique-id>/input.md`, `.humanize/skill/<unique-id>/output.md`, `.humanize/skill/<unique-id>/metadata.md`, and cache files `gemini-run.cmd`, `gemini-run.out`, `gemini-run.log` at `scripts/ask-gemini.sh:187-199` and `scripts/ask-gemini.sh:247-261`.
- gates_or_invariants: Timeout must be numeric at `scripts/ask-gemini.sh:113-123`; model name must match `^[a-zA-Z0-9._-]+$` at `scripts/ask-gemini.sh:162-168`; empty questions fail at `scripts/ask-gemini.sh:153-160`; missing Gemini CLI fails at `scripts/ask-gemini.sh:145-151`. Approval mode is gated by `HUMANIZE_GEMINI_YOLO`, using `--yolo` only for `true` or `1`, otherwise `--sandbox`, at `scripts/ask-gemini.sh:226-231`.
- dependencies_and_callers: Sources `scripts/portable-timeout.sh` for `run_with_timeout` at `scripts/ask-gemini.sh:29-30`; that helper chooses `gtimeout`, GNU `timeout`, Python, or no-timeout fallback in `scripts/portable-timeout.sh:9-76`. It also sources `hooks/lib/project-root.sh` at `scripts/ask-gemini.sh:32-33`, whose resolver prioritizes linked worktree, `CLAUDE_PROJECT_DIR`, then git top-level at `hooks/lib/project-root.sh:51-76`. Called by `skills/ask-gemini/SKILL.md`.
- edge_cases_or_failure_modes: Home cache may be unwritable, falling back to project-local cache at `scripts/ask-gemini.sh:195-199`. Exit code `124` writes timeout metadata and suggests a larger timeout at `scripts/ask-gemini.sh:294-314`. Nonzero Gemini exit writes error metadata and tails stderr at `scripts/ask-gemini.sh:316-338`. Empty stdout is treated as failure with `status: empty_response` at `scripts/ask-gemini.sh:340-362`. If no timeout implementation exists, `portable-timeout.sh` warns and runs without timeout at `scripts/portable-timeout.sh:69-73`.
- validation_or_tests: Behavior is indirectly exercised by the skill contract and monitor-skill artifact discovery. The script itself contains explicit self-validation branches and metadata states for `success`, `timeout`, `error`, and `empty_response`.
- skip_candidate: `no`

### H2_DEV-HZ-082 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Intended helper for monitor test environment setup, but currently acts as a stub/guard rather than a real setup algorithm.
- algorithmic_behavior: Parses `TEST_DIR` and optional `TEST_NAME`, requires `TEST_DIR`, then rejects every `TEST_NAME` through a catch-all case. It never creates monitor state despite the header comment saying it creates directory structure and state files.
- inputs_outputs_state: Inputs are positional args `<test_dir> <test_name>`. Output on missing `TEST_DIR` is a usage message and exit `1` at `tests/setup-monitor-test-env.sh:15-18`. Output on any test name is “Unknown test name” and exit `1` at `tests/setup-monitor-test-env.sh:20-25`. The final `echo "$TEST_DIR"` at `tests/setup-monitor-test-env.sh:28` is unreachable for all current inputs because the case always exits.
- gates_or_invariants: `set -euo pipefail` enforces shell strictness at `tests/setup-monitor-test-env.sh:10`. There are no accepted test names; “Available: (none currently)” is the effective invariant at `tests/setup-monitor-test-env.sh:22-23`.
- dependencies_and_callers: No direct callers were found in assigned files. The real monitor setup for deletion tests is embedded in `tests/test-monitor-e2e-real.sh`, not this helper.
- edge_cases_or_failure_modes: Misleading header comments are the main failure mode: a caller expecting setup side effects will always receive an unknown-test error. Default `TEST_NAME=default` still fails because the catch-all rejects it.
- validation_or_tests: The file is itself a test helper, but in current form it is a negative validator only. No positive setup behavior exists to validate.
- skip_candidate: `yes: currently a stub/placeholder helper with no accepted setup path, so it is probably misclassified as core algorithm unless future tests wire in named cases`

### H2_DEV-HZ-112 `file` `tests/test-monitor-e2e-deletion.sh`
- cursor: `[_]`
- core_role: E2E wrapper that selects and runs monitor deletion behavior tests from the larger real monitor test suite.
- algorithmic_behavior: Sources `tests/test-monitor-e2e-real.sh`, prints a deletion-suite banner, runs `monitor_test_bash_deletion` and `monitor_test_zsh_deletion`, then exits according to aggregate `TESTS_FAILED`. The wrapper’s executable algorithm is only dispatch and result aggregation at `tests/test-monitor-e2e-deletion.sh:4-21`.
- inputs_outputs_state: Inputs are repository scripts and the shell environment needed by the sourced real test. Outputs are pass/fail summary lines and process exit `0` only when `TESTS_FAILED` is zero. State counters `TESTS_PASSED` and `TESTS_FAILED` are initialized in the sourced file at `tests/test-monitor-e2e-real.sh:25-26`.
- gates_or_invariants: The invariant is that both bash and zsh deletion tests must pass for the wrapper to return success. The zsh test self-skips if zsh is unavailable at `tests/test-monitor-e2e-real.sh:238-240`.
- dependencies_and_callers: Depends on `tests/test-monitor-e2e-real.sh`, which creates temporary `.humanize/rlcr/<session>` directories, `state.md`, `goal-tracker.md`, fake cache directories, and runner scripts. Those runner scripts source the real `scripts/humanize.sh` and call `_humanize_monitor_codex` under bash or zsh at `tests/test-monitor-e2e-real.sh:107-158` and `tests/test-monitor-e2e-real.sh:287-326`.
- edge_cases_or_failure_modes: The deletion tests force removal of `.humanize/rlcr` while the monitor is running, then bound wait loops to avoid hangs. Bash failure conditions include missing graceful message, missing deletion reason, glob errors such as “no matches found” or “bad pattern”, missing cleanup message, missing scroll-region reset source, and nonzero exit at `tests/test-monitor-e2e-real.sh:181-227`. Zsh checks the same core graceful-stop and glob-safety behavior at `tests/test-monitor-e2e-real.sh:340-370`.
- validation_or_tests: Validates real monitor implementation paths in `scripts/humanize.sh`: initial missing-loop check at `scripts/humanize.sh:272-277`, terminal restore at `scripts/humanize.sh:706-712`, cleanup at `scripts/humanize.sh:736-762`, graceful stop at `scripts/humanize.sh:764-777`, and directory-deletion exits in the main, no-log, and follow loops at `scripts/humanize.sh:838-844`, `scripts/humanize.sh:899-908`, and `scripts/humanize.sh:1022-1030`.
- skip_candidate: `no`

### H2_DEV-HZ-142 `file` `tests/workflow-coordinator.test.ts`
- cursor: `[_]`
- core_role: Executable specification for the Humanize2 workflow state machine implemented by `src/workflows/coordinator.ts`. It covers cartridge loading, run lifecycle, agent dispatch, artifacts, boards, waits, branches, loops, retries, human requests, checks, transforms, and event predicates.
- algorithmic_behavior: The suite starts workflows from inline `<h2-workflow>` cartridges, drives them through `WorkflowCoordinator.start`, `deliverArtifact`, `patchBoard`, `emitEvent`, and `waitForRun`, then asserts state transitions and event records. The first smoke flow verifies captain agent dispatch, expected artifact delivery, board transform, and terminal success at `tests/workflow-coordinator.test.ts:10-69`. Board patch and latest-artifact semantics are specified at `tests/workflow-coordinator.test.ts:71-134`. Terminal stored runs must clear stale wait targets at `tests/workflow-coordinator.test.ts:136-163`.
- inputs_outputs_state: Inputs are inline workflow HTML, deterministic ID factories, fixed clocks, fake agent backends, delivered artifacts, board patches, and emitted events. Outputs are `WorkflowRunRecord` state, `BoardRecord` values, `ArtifactRecord` values, event lists, agent run requests, and terminal statuses. The fake `slowBackend` records prompts, emits a session-start JSON line, handles abort signals, and returns a successful `AgentResult` at `tests/workflow-coordinator.test.ts:512-550`.
- gates_or_invariants: Manifest and authorization gates are enforced by the coordinator implementation: agent tools and scripts must be declared and authorized at `src/workflows/coordinator.ts:202-263`. Artifact schema validation can yield accepted, manifest-undeclared, or schema-mismatch at `src/workflows/coordinator.ts:1422-1461`. Agent expectations require artifact name, current iteration, accepted validation status, expected schema, and matching producer alias at `src/workflows/coordinator.ts:1497-1523`.
- dependencies_and_callers: Imports `AgentRunCoordinator`, `HumanizeService`, `WorkflowCoordinator`, `buildAgentSessions`, and agent types at `tests/workflow-coordinator.test.ts:1-7`. It exercises coordinator methods whose core behavior is in `src/workflows/coordinator.ts`: constructor/load/start at `src/workflows/coordinator.ts:130-199` and `src/workflows/coordinator.ts:265-330`, artifact/board/event APIs at `src/workflows/coordinator.ts:452-594`, loop/branch execution at `src/workflows/coordinator.ts:711-777`, agent soft-enforcement at `src/workflows/coordinator.ts:779-930`, and wait-target status updates at `src/workflows/coordinator.ts:1188-1224`.
- edge_cases_or_failure_modes: Branch routing must use artifact data and record `branch.routed` (`tests/workflow-coordinator.test.ts:165-213`). Loop artifacts are scoped to iterations and support `@iter-N` qualifiers (`tests/workflow-coordinator.test.ts:215-253`). Duplicate current-iteration artifacts should route using the latest accepted artifact, including content normalized from JSON text to object (`tests/workflow-coordinator.test.ts:255-305`). Soft-enforcement retries must stop at the configured cap and fail with `agent.expectation_unmet` (`tests/workflow-coordinator.test.ts:307-342`). Producer short-name aliases must satisfy expectations (`tests/workflow-coordinator.test.ts:344-394`). Event awaits only satisfy from events emitted after the await baseline, not before it (`tests/workflow-coordinator.test.ts:396-442`). Primitive checks, human waits, transforms, and bounded loops compose successfully (`tests/workflow-coordinator.test.ts:444-488`).
- validation_or_tests: This file is the validation suite. It uses deterministic `ids`, `fixedClock`, and bounded `waitUntil` helpers at `tests/workflow-coordinator.test.ts:562-585` to make async workflow behavior reproducible.
- skip_candidate: `no`

### H2_DEV-HZ-172 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Stop-hook/block template that explains a failure to verify repository cleanliness when `git status` fails or times out.
- algorithmic_behavior: Renders a blocking reason with `{{GIT_STATUS_EXIT}}`, states that repository state cannot be verified, lists likely causes, and asks the operator to manually check git status. The whole template is at `prompt-template/block/git-status-failed.md:1-10`.
- inputs_outputs_state: Input is the template variable `GIT_STATUS_EXIT`. Output is markdown text consumed as a stop reason. It does not mutate state directly.
- gates_or_invariants: The invariant is conservative: if git status cannot be checked, the loop cannot safely continue. This is wired in `hooks/loop-codex-stop-hook.sh`, where `git -C "$PROJECT_ROOT" status --porcelain` is run with timeout, nonzero exit loads this template, and the hook emits a block JSON response at `hooks/loop-codex-stop-hook.sh:498-512`.
- dependencies_and_callers: Called through `load_and_render_safe "$TEMPLATE_DIR" "block/git-status-failed.md"` from the loop codex stop hook. A fallback copy of the text exists inline in the same hook, so the block reason remains available if template loading fails.
- edge_cases_or_failure_modes: Covers lock contention, invalid repository state, and large or slow repositories at `prompt-template/block/git-status-failed.md:5-8`. If the template is missing, the hook fallback preserves the same contract.
- validation_or_tests: Template reference coverage appears in stop-hook and template tests outside this assigned file. The hook callsite is the strongest behavior evidence because it shows the template is used to block the loop rather than as documentation only.
- skip_candidate: `no`

### H2_DEV-HZ-202 `file` `prompt-template/claude/agent-teams-instructions.md`
- cursor: `[_]`
- core_role: Prompt header template that switches the initial RLCR implementation prompt into Agent Teams mode and defines the Claude actor as Team Leader.
- algorithmic_behavior: The template tells Claude it is in Agent Teams mode within an RLCR cycle, that this is the initial round, and that it must read the implementation plan before creating a team. It names key files and obligations: plan file, `goal-tracker.md`, and the required work summary after teammates finish at `prompt-template/claude/agent-teams-instructions.md:1-8`.
- inputs_outputs_state: Inputs are the setup process deciding `agent_teams=true` and assembling the initial prompt. Output is injected prompt text; no runtime state mutation happens inside the template.
- gates_or_invariants: Agent Teams mode is gated by `--agent-teams` plus the environment variable `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, enforced in setup tests and setup script references. The setup script injects this template only when `AGENT_TEAMS == true`, using `AGENT_TEAMS_HEADER="$TEMPLATE_DIR/claude/agent-teams-instructions.md"` at `scripts/setup-rlcr-loop.sh:1375-1378`.
- dependencies_and_callers: Used with `prompt-template/claude/agent-teams-core.md` for initial prompts. Continuation prompts use sibling `agent-teams-continue.md` and core instructions in the stop hook, with implementation-phase-only insertion referenced at `hooks/loop-codex-stop-hook.sh:2195-2199`.
- edge_cases_or_failure_modes: Main failure mode is accidental prompt injection when mode is disabled, or omission when enabled. The template also requires the team leader to read the goal tracker before task splitting, preventing stale acceptance criteria from driving team assignment.
- validation_or_tests: `tests/test-agent-teams.sh` checks env gating, state recording, initial prompt inclusion with Agent Teams enabled, absence without it, and existence/content of this template. Relevant checks include template path setup at `tests/test-agent-teams.sh:331-340` and initial prompt coverage around `tests/test-agent-teams.sh:245-324`.
- skip_candidate: `no`

### H2_DEV-HZ-232 `file` `skills/humanize-refine-plan/SKILL.md`
- cursor: `[_]`
- core_role: Flow skill specification for refining an annotated `gen-plan` document into a comment-free refined plan plus a QA ledger while preserving schema and convergence state.
- algorithmic_behavior: Defines a staged flow: parse args and paths, load merged config, validate IO via `scripts/validate-refine-plan-io.sh`, extract `CMT:`/`ENDCMT` blocks with a stateful scanner, classify comments as question/change request/research request, ask minimal user questions in discussion mode when ambiguous, process comments in order, repair plan consistency until valid, populate QA, optionally translate variants, then atomically write outputs and report success. The mermaid flow is at `skills/humanize-refine-plan/SKILL.md:18-48`.
- inputs_outputs_state: Required input is `--input <annotated-plan.md>` containing a gen-plan schema and at least one comment block. Optional inputs are `--output`, `--qa-dir`, `--alt-language`, `--discussion`, and `--direct` at `skills/humanize-refine-plan/SKILL.md:50-65`. Outputs are a refined plan with comment blocks removed, a QA ledger, and optional translated variants at `skills/humanize-refine-plan/SKILL.md:96-107`.
- gates_or_invariants: Required sections must remain intact, including goal, acceptance criteria, path boundaries, feasibility hints, dependencies, task breakdown, Claude-Codex deliberation, pending decisions, and implementation notes at `skills/humanize-refine-plan/SKILL.md:71-83`. Task routing tags remain restricted to `coding` or `analyze` at `skills/humanize-refine-plan/SKILL.md:84`. `--discussion` and `--direct` are mutually exclusive, and `--alt-language` must not be passed to the validator at `skills/humanize-refine-plan/SKILL.md:62-65`.
- dependencies_and_callers: Depends on `scripts/validate-refine-plan-io.sh` and `prompt-template/plan/refine-plan-qa-template.md`. The validator scans comment blocks while ignoring fenced code and HTML comments, detects nested/stray/missing markers, counts only non-empty blocks, and checks required sections at `scripts/validate-refine-plan-io.sh:16-315` and `scripts/validate-refine-plan-io.sh:608-637`. The QA template requires summary, ledger, answers, research findings, applied changes, remaining decisions, and metadata at `prompt-template/plan/refine-plan-qa-template.md:1-119`.
- edge_cases_or_failure_modes: Validation exit codes are explicitly mapped from success through invalid args at `skills/humanize-refine-plan/SKILL.md:131-143`. Unsupported or same-language alternate language cases are handled by normalization rules at `skills/humanize-refine-plan/SKILL.md:108-129`. Validator failures include input missing, empty input, no valid comments, malformed comment syntax, missing required sections, unwritable output/input directory, and unwritable QA directory at `scripts/validate-refine-plan-io.sh:573-678`.
- validation_or_tests: `tests/test-refine-plan.sh` is the main executable specification for this skill family. It covers command/skill installation wiring, comment extraction examples, parse errors, alt-language behavior, validator exit codes, ignored markers in fences/HTML comments, empty/nested/unterminated blocks, required section checks, output and QA path permissions, and successful in-place/new-file validation.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `8 unique item sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`