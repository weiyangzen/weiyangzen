# agent_07 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 9
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-007 `directory` `prompt-template`
- cursor: `[_]`
- core_role: Central prompt/template corpus for the Humanize RLCR, gen-plan/gen-idea, explore-idea, Codex review, Claude continuation, and hook block-message workflows. Recursive inspection covered the whole tree: block, claude, codex, explore, idea, and plan templates, totaling 73,063 bytes and 1,812 lines by direct `wc`.
- algorithmic_behavior: The directory is not executable by itself; it defines the contracts that executable hooks and commands render. `prompt-template/block/*` defines fail-closed validator messages such as dirty git, protected state files, wrong round files, missing round contracts, mainline drift stops, and BitLesson delta failures. `prompt-template/codex/*` defines review algorithms: regular review, full alignment review, code review phase audit, commit-history context, and goal-tracker reconciliation. `prompt-template/claude/*` defines next-round, drift-replan, review-phase, finalize-phase, methodology-analysis, and agent-team delegation prompts. `prompt-template/explore/*` defines prototype-worker, report, and final-idea contracts. `prompt-template/idea/gen-idea-template.md` and `prompt-template/plan/*` define generated artifact shapes.
- inputs_outputs_state: Inputs are placeholder values in two syntaxes: `{{VAR}}` for hook-rendered templates and `<PLACEHOLDER>` for command-level generation templates. Outputs are rendered prompts, block reasons, audit files, idea drafts, plan drafts, explore reports, and worker-result instructions. State is indirect: templates tell agents and hooks how to move RLCR through implementation, review, drift recovery, finalize, and methodology-analysis phases, while actual state mutation happens in scripts/hooks.
- gates_or_invariants: Block templates encode hard gates: no direct state-file modification (`prompt-template/block/state-file-modification.md:1`), no Bash writes to summary/goal-tracker (`prompt-template/block/summary-bash-write.md:1`, `prompt-template/block/goal-tracker-bash-write.md:1`), no wrong round file access (`prompt-template/block/wrong-round-file.md:1`), round contract required (`prompt-template/block/round-contract-missing.md:1`), mandatory mainline verdict (`prompt-template/block/mainline-verdict-missing.md:1`), and outdated state schema handling (`prompt-template/block/schema-outdated.md:1`). Review templates require no deferrals for completion and mandatory verdicts (`prompt-template/codex/regular-review.md:49`, `prompt-template/codex/full-alignment-review.md:52`).
- dependencies_and_callers: Loaded through `hooks/lib/template-loader.sh` and initialized by `hooks/lib/loop-common.sh:240-247`. Direct callers include read/write/edit/bash validators, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-plan-file-validator.sh`, BitLesson validation, gen-plan/gen-idea validation, explore validation, and refine-plan. `tests/test-template-references.sh:56-110` scans hook references against this directory; `tests/test-template-loader.sh:147-222` verifies render and fallback behavior.
- edge_cases_or_failure_modes: Missing templates degrade through `load_and_render_safe` fallbacks instead of empty block messages. Missing variables are intentionally preserved by the renderer, so an unprovided placeholder remains visible rather than silently blanking. The explore worker prompt treats direction data as untrusted and places hard constraints before it (`prompt-template/explore/worker-prompt.md:16-31`). The gen-plan template intentionally contains plan terminology forbidden in generated implementation code, and `tests/test-style-compliance.sh` excludes templates from runtime style scanning.
- validation_or_tests: Coverage includes `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh`, `tests/test-commit-history-section.sh`, `tests/test-worker-result-contract.sh`, `tests/test-explore-command-structure.sh`, and gen-plan/gen-idea/refine-plan tests. I inspected these references but did not execute tests because this assignment requested read-only research notes.
- skip_candidate: `no`

### H2_DEV-HZ-037 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR loop. It performs human/agent pre-flight checks, then delegates executable setup to `scripts/setup-rlcr-loop.sh`.
- algorithmic_behavior: The command first performs a plan compliance pre-check unless help or skip-implementation mode is requested (`commands/start-rlcr-loop.md:13-20`). It extracts the plan file from `--plan-file` or first positional argument, applies path-safety filters, reads the plan, then invokes a plan-compliance agent that returns only `PASS`, `FAIL_RELEVANCE`, or `FAIL_BRANCH_SWITCH` (`commands/start-rlcr-loop.md:21-57`). It then optionally runs a plan-understanding quiz agent, asks two user questions, and lets the user proceed or stop if answers are wrong (`commands/start-rlcr-loop.md:60-106`). Finally it runs the setup script (`commands/start-rlcr-loop.md:109-115`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, plan file content, user quiz answers, and optional flags such as `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, `--push-every-round`, `--base-branch`, `--full-review-round`, `--skip-impl`, `--agent-teams`, `--yolo`, `--skip-quiz`, and privacy flags. Output is the setup-script invocation that creates `.humanize/rlcr/<timestamp>/` state, prompt, tracker, contract, and summary files. The command itself defines no shell state, but it establishes the control-flow contract for setup.
- gates_or_invariants: Fail-closed parsing of the compliance agent blocks on plan irrelevance, branch-switching instructions, or malformed output (`commands/start-rlcr-loop.md:52-57`). The quiz is advisory except when the user chooses to stop (`commands/start-rlcr-loop.md:86-105`). Round semantics are strict: one round means the agent believes the entire plan is finished, not one task or milestone (`commands/start-rlcr-loop.md:130-138`). Goal tracker immutable/mutable sections and acceptance criteria anchoring are required (`commands/start-rlcr-loop.md:139-159`).
- dependencies_and_callers: Allowed tools are the setup script, Read, Task, and AskUserQuestion (`commands/start-rlcr-loop.md:4-8`). It depends on two named subagents in prompt text: `humanize:plan-compliance-checker` and `humanize:plan-understanding-quiz`. Runtime setup is delegated to `scripts/setup-rlcr-loop.sh`, which creates the state and prompt artifacts.
- edge_cases_or_failure_modes: If no plan path is extractable, the pre-check is skipped and the setup script handles the error (`commands/start-rlcr-loop.md:26`). Unsafe paths are also deferred to setup validation (`commands/start-rlcr-loop.md:28-35`). Quiz generation with missing fields warns and continues (`commands/start-rlcr-loop.md:86`). `--skip-impl` bypasses plan validation and enters code-review mode (`commands/start-rlcr-loop.md:197-209`). `--yolo` skips the quiz and lets Claude answer Codex open questions indirectly via setup flags.
- validation_or_tests: Behavior is indirectly specified by setup and hook tests, especially plan-file validation and stop-hook tests. The command’s workflow text also aligns with setup-script branches around argument parsing, state creation, skip-impl handling, and goal tracker initialization in `scripts/setup-rlcr-loop.sh:189-325`, `scripts/setup-rlcr-loop.sh:450-681`, and `scripts/setup-rlcr-loop.sh:885-1161`.
- skip_candidate: `no`

### H2_DEV-HZ-067 `file` `scripts/validate-gen-idea-io.sh`
- cursor: `[_]`
- core_role: Deterministic IO validator and metadata resolver for `/humanize:gen-idea`; it prevents partial or colliding draft/companion writes before the command launches exploratory agents.
- algorithmic_behavior: Parses one idea input plus `--n` and `--output` (`scripts/validate-gen-idea-io.sh:32-67`). It validates direction count as an integer from 2 to 10 (`scripts/validate-gen-idea-io.sh:75-84`). It chooses input mode: existing `.md` file, missing `.md` path error, or inline text (`scripts/validate-gen-idea-io.sh:90-141`). It resolves default output under `.humanize/ideas/<slug>-<timestamp>.md`, derives `<output>.directions.json`, checks collisions and write permission, validates that the idea template exists, and emits a structured success block (`scripts/validate-gen-idea-io.sh:143-218`).
- inputs_outputs_state: Inputs are CLI args, current repo root from `git rev-parse --show-toplevel` or `pwd`, optional `CLAUDE_PLUGIN_ROOT`, filesystem readability/writability, and the template file. Outputs on success are stdout keys: `VALIDATION_SUCCESS`, `INPUT_MODE`, optional `IDEA_BODY_FILE`, `OUTPUT_FILE`, `DIRECTIONS_JSON_FILE`, `SLUG`, `TEMPLATE_FILE`, `N`, and for inline mode an `IDEA_BODY_BEGIN/END` sentinel block (`scripts/validate-gen-idea-io.sh:203-217`). The only intentional state mutation is creating the default output directory when default output is used (`scripts/validate-gen-idea-io.sh:162-164`).
- gates_or_invariants: Exit codes are part of the command contract (`scripts/validate-gen-idea-io.sh:4-13`). File inputs must be `.md`, readable, and non-empty (`scripts/validate-gen-idea-io.sh:104-119`). User-supplied output must end in `.md`, its parent must exist, output and companion JSON must not exist, and output dir must be writable (`scripts/validate-gen-idea-io.sh:152-188`). Template absence is a plugin configuration failure (`scripts/validate-gen-idea-io.sh:190-201`).
- dependencies_and_callers: Called by `commands/gen-idea.md:50-64`, which consumes the emitted keys and later writes both the draft and `DIRECTIONS_JSON_FILE`. It depends on `prompt-template/idea/gen-idea-template.md`, `git`, `date`, `realpath`, `sed`, and standard shell tools.
- edge_cases_or_failure_modes: Path detection is deliberately narrow: only whitespace-free inputs ending in `.md` are considered missing paths; slash-only ideas such as `undo/redo` and `CI/CD` remain inline (`scripts/validate-gen-idea-io.sh:90-102`). Inline ideas under 10 characters produce a warning but pass (`scripts/validate-gen-idea-io.sh:132-135`). Whitespace-containing nonexistent paths that do not end in `.md` are treated as inline, which is documented as a limitation (`scripts/validate-gen-idea-io.sh:95-96`). `realpath` failures fall back to raw path strings (`scripts/validate-gen-idea-io.sh:121`, `scripts/validate-gen-idea-io.sh:158`).
- validation_or_tests: `tests/test-validate-gen-idea-io.sh:44-162` checks success metadata, `.md` suffix enforcement, companion collision, existing output collision, missing idea, and slash-containing inline ideas. `tests/test-gen-idea-dual-write.sh:45-125` verifies the dual-write contract with `commands/gen-idea.md`.
- skip_candidate: `no`

### H2_DEV-HZ-097 `file` `tests/test-commit-history-section.sh`
- cursor: `[_]`
- core_role: Executable specification for the integral “development history” section inserted into Codex review prompts.
- algorithmic_behavior: Builds a temporary git repo, captures a base commit, generates commit history and recent round-file references, renders `prompt-template/codex/commit-history-section.md` via `load_and_render_safe`, and asserts rendered content across normal and fallback paths (`tests/test-commit-history-section.sh:14-55`).
- inputs_outputs_state: Inputs are a temp git repo, `CURRENT_ROUND`, `BASE_COMMIT`, `LOOP_TIMESTAMP`, template directory, and helper functions. Outputs are PASS/FAIL records and final test summary. Temporary repo state is mutated by adding commits and an orphan branch to model review-history edge cases (`tests/test-commit-history-section.sh:69-74`, `tests/test-commit-history-section.sh:155-162`).
- gates_or_invariants: Round 0 must show “no commits yet” and “first round, no prior history” placeholders (`tests/test-commit-history-section.sh:35-61`). Round 3 must show all three commits and round 0-2 summary/review references (`tests/test-commit-history-section.sh:76-114`). Missing templates must render a full fallback section, not an empty prompt fragment (`tests/test-commit-history-section.sh:181-222`). Round 1 must reference only round 0 (`tests/test-commit-history-section.sh:225-244`).
- dependencies_and_callers: Sources `tests/test-helpers.sh` and `hooks/lib/template-loader.sh` (`tests/test-commit-history-section.sh:14-19`). It mirrors `hooks/loop-codex-stop-hook.sh:1042-1071`, where production validates `BASE_COMMIT` as an ancestor, collects up to 80 commits, creates recent round references, and renders `codex/commit-history-section.md`.
- edge_cases_or_failure_modes: Corrupted/nonexistent base commit falls back to recent branch commits with an annotation and must not trip `set -e` (`tests/test-commit-history-section.sh:117-147`). A valid but unrelated orphan commit is treated the same way (`tests/test-commit-history-section.sh:150-179`). Empty base commit for legacy loops also falls back with annotation (`tests/test-commit-history-section.sh:247-266`).
- validation_or_tests: This file is itself the validation surface. I inspected it and its production mirror; I did not execute it in the read-only research run.
- skip_candidate: `no`

### H2_DEV-HZ-127 `file` `tests/test-style-compliance.sh`
- cursor: `[_]`
- core_role: Narrow executable style gate for plan-authored files, enforcing that workflow/planning tokens do not leak into runtime implementation code.
- algorithmic_behavior: Builds a file list from all `.sh` and `.py` files under `viz/` plus `scripts/cancel-rlcr-session.sh` if present (`tests/test-style-compliance.sh:43-63`). It fails immediately if no scoped files exist (`tests/test-style-compliance.sh:65-68`). It then scans every file for four literal substrings and reports one pass/fail per pattern with offending paths and matching lines (`tests/test-style-compliance.sh:74-93`).
- inputs_outputs_state: Inputs are the repository tree rooted from the test script and the scoped files. Outputs are colored PASS/FAIL lines, offending grep output, counts, and exit code 1 if any forbidden token is found (`tests/test-style-compliance.sh:95-104`). It does not mutate repository state.
- gates_or_invariants: The forbidden literals are `AC-`, `Milestone`, `Step `, and `Phase ` (`tests/test-style-compliance.sh:6-10`, `tests/test-style-compliance.sh:76`). Scope is intentionally restricted: broader `scripts/`, `commands/`, `hooks/`, and tests are excluded because upstream workflow text legitimately contains those terms (`tests/test-style-compliance.sh:12-27`).
- dependencies_and_callers: Uses only `find`, `grep`, `sed`, and shell arrays. It is likely run by the repository’s test aggregator (`tests/run-all-tests.sh` references validation scripts generally), but this specific file is self-contained.
- edge_cases_or_failure_modes: Missing `viz/` and missing cancel script produce “no plan-scope files found” and fail, which turns absent implementation surface into a test failure (`tests/test-style-compliance.sh:65-68`). Matches in comments are failures just like matches in code because the test uses plain `grep -nF` over whole files (`tests/test-style-compliance.sh:80-90`).
- validation_or_tests: This file is its own validation logic. It was inspected, not executed.
- skip_candidate: `yes: style-only validation gate, not a runtime RLCR state machine or scheduling algorithm component`

### H2_DEV-HZ-157 `file` `hooks/lib/template-loader.sh`
- cursor: `[_]`
- core_role: Shared rendering library that lets hooks and scripts load Markdown templates, substitute variables safely, and fall back when a template is missing.
- algorithmic_behavior: `get_template_dir` derives `<plugin-root>/prompt-template` from a hooks/lib path (`hooks/lib/template-loader.sh:24-31`). `load_template` cats an existing file or warns to stderr (`hooks/lib/template-loader.sh:33-46`). `render_template` builds `TMPL_VAR_*` environment entries and uses awk to scan each line once, replacing `{{VAR}}` with provided values and preserving unknown placeholders (`hooks/lib/template-loader.sh:56-136`). `load_and_render` composes load and render (`hooks/lib/template-loader.sh:138-151`). `append_template` appends optional template content (`hooks/lib/template-loader.sh:153-168`). `load_and_render_safe` emits a rendered fallback when loading or rendering fails (`hooks/lib/template-loader.sh:185-211`). `validate_template_dir` requires `block`, `codex`, `claude`, and `plan` subdirectories (`hooks/lib/template-loader.sh:213-238`).
- inputs_outputs_state: Inputs are a template directory, template name, raw content, fallback message, and `VAR=value` assignments. Outputs are rendered text on stdout and warnings/errors on stderr. There is no persistent state; all variables are local except environment values passed into awk for a single render.
- gates_or_invariants: The documented syntax is `{{VARIABLE_NAME}}`; substitution is single-pass; missing variables stay as literal placeholders (`hooks/lib/template-loader.sh:7-13`). Single-pass rendering prevents placeholder injection where review content containing `{{PLAN_FILE}}` would be substituted a second time (`hooks/lib/template-loader.sh:52-55`). Awk failure returns nonzero (`hooks/lib/template-loader.sh:130-133`).
- dependencies_and_callers: Sourced by `hooks/lib/loop-common.sh:240`; call sites include the stop hook, read/write/edit/bash validators, plan-file validator, methodology analysis, and BitLesson validation. `tests/test-template-loader.sh:42-244` covers directory resolution, load/missing behavior, safe fallback, and directory validation; later sections cover shell metacharacters and placeholder injection (`tests/test-template-loader.sh:247-544`).
- edge_cases_or_failure_modes: Missing templates produce warnings in `load_template`, but safe callers suppress those and render fallback. Empty template content also triggers fallback in `load_and_render_safe` (`hooks/lib/template-loader.sh:194-199`). Values containing shell metacharacters, JSON-like text, Windows paths, multiline text, and placeholder-shaped substrings are specified by tests to render literally (`tests/test-template-loader.sh:247-544`).
- validation_or_tests: Covered by `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-error-scenarios.sh`, and robustness template tests. I inspected those references, not executed them.
- skip_candidate: `no`

### H2_DEV-HZ-187 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Block-message template for RLCR sessions whose state file predates required schema fields.
- algorithmic_behavior: Renders a short failure reason naming the missing field via `{{FIELD_NAME}}`, explains that the loop was started with an older Humanize version, offers cancel/update/restart options, and states the session will terminate unexpectedly to preserve state (`prompt-template/block/schema-outdated.md:1-12`).
- inputs_outputs_state: Input is `FIELD_NAME`. Output is Markdown block text, usually embedded as a JSON `reason` by a validator. The template itself does not mutate state; it defines the user-facing contract for a schema gate.
- gates_or_invariants: Missing required state schema fields are treated as blocking rather than silently defaulted. The direct caller in `hooks/loop-plan-file-validator.sh` maps missing `plan_tracked` or `start_branch` to this template (`hooks/loop-plan-file-validator.sh:60-88`).
- dependencies_and_callers: Loaded through `load_and_render_safe` in `hooks/loop-plan-file-validator.sh:66`. It depends on the template-loader preserving `{{FIELD_NAME}}` substitution and fallback behavior.
- edge_cases_or_failure_modes: If this template is unavailable, the validator has an inline fallback string that still blocks (`hooks/loop-plan-file-validator.sh:63-66`). The message text says the session will be terminated as unexpected, but the plan-file validator itself returns a block decision; termination behavior is handled by the broader hook/loop control path, not by this template.
- validation_or_tests: Indirectly covered by template reference tests and plan-file hook/schema tests. The state robustness test verifies strict parser failures that feed schema-style gates.
- skip_candidate: `no`

### H2_DEV-HZ-217 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Codex review prompt section that authorizes and constrains goal-tracker reconciliation during RLCR reviews.
- algorithmic_behavior: Tells Codex that if Claude’s summary contains a “Goal Tracker Update Request” section, or if review detects tracker drift, Codex must evaluate current tracker alignment, update `@{{GOAL_TRACKER_FILE}}` if correction is needed, or explain rejected requests (`prompt-template/codex/goal-tracker-update-section.md:1-12`). It enumerates common update classes: task completion, blocking issues, queued issues, plan changes, and deferrals (`prompt-template/codex/goal-tracker-update-section.md:13-18`).
- inputs_outputs_state: Input is `GOAL_TRACKER_FILE`. Output is a rendered prompt subsection inserted into regular and full alignment review prompts. It can lead Codex to mutate the mutable section of `goal-tracker.md`, moving tasks between sections or appending plan evolution/issue entries. It explicitly forbids modifying the immutable section (`prompt-template/codex/goal-tracker-update-section.md:6-10`).
- gates_or_invariants: Tracker updates must remain aligned with the ultimate goal and acceptance-criteria progress. Deferrals require strong justification (`prompt-template/codex/goal-tracker-update-section.md:17`). Rejected requests require review explanation (`prompt-template/codex/goal-tracker-update-section.md:11`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh:1016-1020` renders this section, then injects it into `prompt-template/codex/full-alignment-review.md:75` or `prompt-template/codex/regular-review.md:65`. Claude-side fallback request guidance lives in `prompt-template/claude/goal-tracker-update-request.md`.
- edge_cases_or_failure_modes: If the template is missing, the stop hook uses a fallback that still instructs Codex to apply summary-requested changes (`hooks/loop-codex-stop-hook.sh:1016-1020`). The prompt also covers drift detected by Codex even without an explicit Claude request.
- validation_or_tests: `tests/test-templates-comprehensive.sh:531-539` renders this real template and checks the goal-tracker path substitution. Template reference tests also cover existence.
- skip_candidate: `no`

### H2_DEV-HZ-247 `file` `tests/robustness/test-state-file-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for RLCR state-file parsing, validating strict and tolerant behavior under malformed, partial, legacy, and extended state files.
- algorithmic_behavior: Creates temporary state files, sources `hooks/lib/loop-common.sh`, then exercises `parse_state_file`, `parse_state_file_strict`, and `get_current_round` across positive, negative, and compatibility cases (`tests/robustness/test-state-file-robustness.sh:14-30`). It finishes with a shared test summary and exits with the summary status (`tests/robustness/test-state-file-robustness.sh:525-530`).
- inputs_outputs_state: Inputs are generated Markdown state files with YAML frontmatter. Outputs are parser globals such as `STATE_CURRENT_ROUND`, `STATE_MAX_ITERATIONS`, `STATE_PLAN_FILE`, `STATE_FULL_REVIEW_ROUND`, `STATE_MAINLINE_STALL_COUNT`, `STATE_LAST_MAINLINE_VERDICT`, and `STATE_DRIFT_STATUS`, plus PASS/FAIL lines. State mutation is limited to temp files.
- gates_or_invariants: Valid files with required fields must parse (`tests/robustness/test-state-file-robustness.sh:33-60`). Strict mode rejects missing frontmatter, nonnumeric current round, and missing required fields (`tests/robustness/test-state-file-robustness.sh:149-215`). Tolerant mode supplies defaults for missing fields (`tests/robustness/test-state-file-robustness.sh:217-228`). `full_review_round` defaults to 5 and accepts minimum 2 (`tests/robustness/test-state-file-robustness.sh:399-474`). Drift fields default safely to stall count 0, verdict unknown, and drift normal (`tests/robustness/test-state-file-robustness.sh:476-523`).
- dependencies_and_callers: Depends on parser implementation in `hooks/lib/loop-common.sh:430-600` and helper output functions in `tests/test-helpers.sh`. The strict parser requires frontmatter separators and required fields including `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:541-569`), while tolerant parsing applies defaults (`hooks/lib/loop-common.sh:501-521`).
- edge_cases_or_failure_modes: The suite covers extra fields, quoted values, round 0, empty files, partial YAML, malformed YAML, huge and negative round numbers, spaces in string values, non-existent files, binary suffix content, and CRLF line endings (`tests/robustness/test-state-file-robustness.sh:72-397`). Some tolerant expectations are intentionally broad, such as allowing partial YAML to return 0 or 5 and CRLF to return any non-crashing result (`tests/robustness/test-state-file-robustness.sh:251-257`, `tests/robustness/test-state-file-robustness.sh:386-397`).
- validation_or_tests: This file is the validation surface. It was inspected in full; no tests were executed because the assignment was read-only research.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `9/9 unique assigned item sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`