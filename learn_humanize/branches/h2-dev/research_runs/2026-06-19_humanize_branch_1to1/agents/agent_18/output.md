# agent_18 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-018 `directory` `prompt-template/idea`
- cursor: `[_]`
- core_role: Idea-synthesis output template directory for `/humanize:gen-idea`. It currently contains one core descendant, `prompt-template/idea/gen-idea-template.md`, which defines the canonical markdown shape for generated idea drafts.
- algorithmic_behavior: The template is a pure structured-output contract: title, original idea, primary direction, evidence, risks, alternatives, and synthesis notes are represented by placeholders at `prompt-template/idea/gen-idea-template.md:1-31`. `commands/gen-idea.md:165-190` defines the replacement algorithm and rendering rules, including byte-identical preservation of the original idea, primary selection fields, alternative numbering, and fallback evidence sentinel rendering.
- inputs_outputs_state: Inputs are synthesized proposal fields from `gen-idea`: `<TITLE>`, `<ORIGINAL_IDEA>`, `<PRIMARY_NAME>`, `<PRIMARY_RATIONALE>`, `<PRIMARY_APPROACH_SUMMARY>`, `<PRIMARY_OBJECTIVE_EVIDENCE>`, `<PRIMARY_KNOWN_RISKS>`, `<ALTERNATIVES>`, and `<SYNTHESIS_NOTES>`. Output is the generated draft markdown file; companion `directions.json` is built by the command after the template write, not by this directory. The directory holds no mutable state.
- gates_or_invariants: Template existence is a hard IO gate in `scripts/validate-gen-idea-io.sh:190-200`; missing template exits as `TEMPLATE_NOT_FOUND`. The generation command requires a single write of finalized content, not progressive edits, at `commands/gen-idea.md:192-194`.
- dependencies_and_callers: Called by `scripts/validate-gen-idea-io.sh:190-194` via `CLAUDE_PLUGIN_ROOT` or script-relative fallback, then consumed by `commands/gen-idea.md:167-190`. Tests use fixture touches for this template in `tests/test-gen-idea-dual-write.sh` and `tests/test-validate-gen-idea-io.sh`.
- edge_cases_or_failure_modes: If fewer than two proposal responses survive parsing, `commands/gen-idea.md:140-143` stops before this template is populated. Dropped proposals force alternative renumbering with no gaps per `commands/gen-idea.md:145` and `commands/gen-idea.md:159`. Missing template stops validation with exit 7 from `scripts/validate-gen-idea-io.sh:197-200`.
- validation_or_tests: IO validation confirms the template path before generation. The direct structure is simple enough that most behavior is enforced through the command contract and gen-idea IO tests rather than a dedicated template-render test.
- skip_candidate: `no`

### H2_DEV-HZ-048 `file` `hooks/loop-post-bash-hook.sh`
- cursor: `[_]`
- core_role: Claude Code PostToolUse Bash hook that binds the newly created RLCR loop state to the leader session id immediately after `setup-rlcr-loop.sh` runs. This is part of the session-aware RLCR state machine, preventing unrelated sessions or spawned agents from being governed as the loop leader.
- algorithmic_behavior: Reads hook JSON from stdin at `hooks/loop-post-bash-hook.sh:26-37`, extracts `.tool_input.command`, `.cwd`, and `.session_id` with `jq`, locates a pending `.humanize/.pending-session-id` signal in the hook cwd, `CLAUDE_PROJECT_DIR`, or process cwd at `hooks/loop-post-bash-hook.sh:131-139`, validates the setup command signature, and patches `session_id:` in `state.md` using `awk` at `hooks/loop-post-bash-hook.sh:180-195`. It then removes the signal file at `hooks/loop-post-bash-hook.sh:197-198`.
- inputs_outputs_state: Input JSON fields are `tool_input.command`, `cwd`, and `session_id`. Signal input is `.humanize/.pending-session-id`, line 1 = target state path and line 2 = setup script signature, matching `scripts/setup-rlcr-loop.sh:914-923`. Output is an updated RLCR `state.md` with `session_id: <id>` plus deletion of the one-shot signal file.
- gates_or_invariants: `matches_setup_command_signature` at `hooks/loop-post-bash-hook.sh:43-86` is the main authorization gate. It accepts only quoted or unquoted commands that start with the exact setup script path and have an end or whitespace boundary; it rejects `echo setup...` and concatenated script-path strings. It normalizes repeated slashes before comparison at `hooks/loop-post-bash-hook.sh:57-65`. Existing non-empty `session_id` is not overwritten due to the check at `hooks/loop-post-bash-hook.sh:182-184`.
- dependencies_and_callers: Registered as the PostToolUse Bash hook in `hooks/hooks.json:52-59`. It sources `hooks/lib/project-root.sh` for path canonicalization at `hooks/loop-post-bash-hook.sh:29-30`. It depends on `git rev-parse` in `resolve_candidate_root` at `hooks/loop-post-bash-hook.sh:88-101` and optionally on `jq`.
- edge_cases_or_failure_modes: Missing or unparsable `jq` leaves command/session extraction empty, so the hook no-ops rather than corrupting state. Empty or missing signal exits cleanly at `hooks/loop-post-bash-hook.sh:141-143`. Empty/missing state path removes the signal as cleanup at `hooks/loop-post-bash-hook.sh:156-159`. No session id leaves the signal for a later attempt at `hooks/loop-post-bash-hook.sh:175-177`. Non-matching command preserves the signal at `hooks/loop-post-bash-hook.sh:162-167`.
- validation_or_tests: `tests/test-session-id.sh:110-165` verifies recording and signal removal; `tests/test-session-id.sh:464-509` verifies non-setup commands do not consume the signal; `tests/test-session-id.sh:512-528` verifies setup command acceptance; `tests/test-session-id.sh:531-574` verifies session ids with `/`, `&`, and `.` are safely written.
- skip_candidate: `no`

### H2_DEV-HZ-078 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for RLCR monitor deletion behavior and terminal restoration. It documents how the monitor should react when `.humanize/` disappears while `humanize monitor rlcr` is active.
- algorithmic_behavior: Dispatches on one positional command at `tests/manual-monitor-test.sh:23-72`. `setup` creates `.humanize/rlcr/2026-01-16_99-99-99`, writes minimal `state.md` and `goal-tracker.md`, and instructs the tester to start the monitor. `delete` removes `.humanize` and describes expected monitor output. `cleanup` removes `.humanize`.
- inputs_outputs_state: Input is one of `setup`, `delete`, or `cleanup`. State created is a fake RLCR session directory with `current_round`, `max_iterations`, model fields, `started_at`, `plan_file`, and a goal tracker at `tests/manual-monitor-test.sh:26-38`. Output is console guidance and filesystem setup/deletion of `.humanize`.
- gates_or_invariants: The expected invariant is that deletion of `.humanize/rlcr` produces a clean monitor exit message and restores terminal scroll/cursor state; the exact expected text is documented at `tests/manual-monitor-test.sh:51-54`. Unknown commands exit 1 with usage at `tests/manual-monitor-test.sh:63-70`.
- dependencies_and_callers: Depends on being run from or under the repo root; it resolves `PROJECT_ROOT` from the script path at `tests/manual-monitor-test.sh:19-21`. It coordinates manually with `source scripts/humanize.sh && humanize monitor rlcr` per `tests/manual-monitor-test.sh:10-14` and `tests/manual-monitor-test.sh:43-45`.
- edge_cases_or_failure_modes: It is intentionally destructive to local `.humanize` test state, so it is manual-only. It does not isolate into a temp repo and would remove real local `.humanize` if run in a working checkout. It also uses a synthetic timestamp containing `99-99-99`, which is adequate for monitor path testing but not a real clock value.
- validation_or_tests: The script itself is the manual test. It asserts behavior through human observation rather than machine assertions, specifically clean exit, terminal restoration, and absence of shell glob errors at `tests/manual-monitor-test.sh:51-54`.
- skip_candidate: `no`

### H2_DEV-HZ-108 `file` `tests/test-gen-plan.sh`
- cursor: `[_]`
- core_role: Large executable contract for `/humanize:gen-plan`, its command frontmatter, planning workflow structure, template synchronization, model/name validation, and IO validator semantics.
- algorithmic_behavior: Runs positive structure checks first, then negative fixture checks. It validates command file existence and frontmatter at `tests/test-gen-plan.sh:54-104`, relevance-agent structure at `tests/test-gen-plan.sh:107-116`, Claude/Codex deliberation workflow requirements at `tests/test-gen-plan.sh:119-247`, version consistency at `tests/test-gen-plan.sh:290-311`, invalid fixture rejection at `tests/test-gen-plan.sh:347-473`, model validation at `tests/test-gen-plan.sh:476-532`, no Emoji/CJK content at `tests/test-gen-plan.sh:534-554`, and `validate-gen-plan-io.sh` exit-code behavior at `tests/test-gen-plan.sh:556-706`.
- inputs_outputs_state: Inputs are repository files: `commands/gen-plan.md`, `agents/draft-relevance-checker.md`, `prompt-template/plan/gen-plan-template.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`, and `scripts/validate-gen-plan-io.sh`. It creates temporary invalid fixtures under `mktemp -d` at `tests/test-gen-plan.sh:322-324` and a second temp script-test directory at `tests/test-gen-plan.sh:564-567`. Output is PASS/FAIL lines and process exit 0 only if `TESTS_FAILED` is zero at `tests/test-gen-plan.sh:728-735`.
- gates_or_invariants: Enforces that direct mode cannot mark convergence as complete by checking `PLAN_CONVERGENCE_STATUS=partially_converged` at `tests/test-gen-plan.sh:137-140`; auto-start must require discussion mode at `tests/test-gen-plan.sh:143-147`; command must require ultrathink at `tests/test-gen-plan.sh:149-153`; pending user decisions and convergence loop sections must exist at `tests/test-gen-plan.sh:155-177`; Codex first-pass must precede Claude candidate plan at `tests/test-gen-plan.sh:179-186`; task breakdown tags are mandatory at `tests/test-gen-plan.sh:219-235`; pending decision consolidation must reference both Codex questions and convergence needs at `tests/test-gen-plan.sh:237-247`.
- dependencies_and_callers: Mirrors implementation in `commands/gen-plan.md:1-45` and `commands/gen-plan.md:255-329`. The IO exit-code expectations correspond to `scripts/validate-gen-plan-io.sh:1-12`, option parsing at `scripts/validate-gen-plan-io.sh:35-80`, and path/template validation at `scripts/validate-gen-plan-io.sh:108-178`. It also checks the extracted Plan Structure block against `prompt-template/plan/gen-plan-template.md` at `tests/test-gen-plan.sh:708-715`.
- edge_cases_or_failure_modes: The YAML syntax checker is heuristic, not a full YAML parser, at `tests/test-gen-plan.sh:436-449`. `grep -P` no-CJK checks are skipped implicitly on systems without compatible PCRE behavior because stderr is redirected but the condition may simply not match. The test uses `real` temp writes and cleanup traps; multiple `trap ... EXIT` assignments at `tests/test-gen-plan.sh:324` and `tests/test-gen-plan.sh:567` mean the latter trap can override the former in a plain shell, leaving some temp files possible.
- validation_or_tests: This is the validation harness. It specifically exercises `validate-gen-plan-io.sh` for missing option values, unknown options, missing/empty input, missing output directory, existing output, directory output, valid paths, auto-start flag, `--discussion`, `--direct`, mutual exclusion, and help behavior at `tests/test-gen-plan.sh:569-703`.
- skip_candidate: `no`

### H2_DEV-HZ-138 `file` `tests/test-worker-result-contract.sh`
- cursor: `[_]`
- core_role: Executable structural contract for `prompt-template/explore/worker-prompt.md`, ensuring explore-idea workers produce parseable result JSON and respect hard safety boundaries.
- algorithmic_behavior: Checks template existence at `tests/test-worker-result-contract.sh:26-34`, sentinel presence and order at `tests/test-worker-result-contract.sh:37-61`, required render placeholders at `tests/test-worker-result-contract.sh:63-91`, required JSON result fields at `tests/test-worker-result-contract.sh:93-130`, and hard constraints at `tests/test-worker-result-contract.sh:132-198`.
- inputs_outputs_state: Input is `prompt-template/explore/worker-prompt.md` selected at `tests/test-worker-result-contract.sh:19`. Output is test pass/fail reporting through `tests/test-helpers.sh` and a final summary via `print_test_summary` at `tests/test-worker-result-contract.sh:200-201`. It does not mutate repo state.
- gates_or_invariants: Enforces result sentinels `EXPLORE_RESULT_JSON_BEGIN/END` and their ordering. Requires fields such as `schema_version`, `task_status`, Codex review metadata fields, worktree/branch/commit fields, test counts, dirty/commit status, summary, and error. Requires hard constraints to appear before untrusted direction data at `tests/test-worker-result-contract.sh:143-152`, forbids untrusted `<DIRECTION_NAME>` interpolation inside bash code fences at `tests/test-worker-result-contract.sh:154-160`, and requires no nested skills, no push, `CLAUDE_PROJECT_DIR` scoping, explicit review model placeholder, and branch naming format at `tests/test-worker-result-contract.sh:162-198`.
- dependencies_and_callers: Validates the prompt whose actual contract appears in `prompt-template/explore/worker-prompt.md:16-27` for hard constraints, setup behavior at `prompt-template/explore/worker-prompt.md:67-90`, Codex review metadata checking at `prompt-template/explore/worker-prompt.md:104-115`, and result JSON at `prompt-template/explore/worker-prompt.md:128-160`.
- edge_cases_or_failure_modes: It is grep-based and validates structural presence, not semantic JSON validity after rendering. It guards against prompt injection by requiring hard constraints before untrusted direction data and checking that untrusted direction name is not interpolated into bash snippets.
- validation_or_tests: The whole file is the test. It is especially tied to the worker-result parser contract through sentinel and required-field checks at `tests/test-worker-result-contract.sh:40-130`.
- skip_candidate: `no`

### H2_DEV-HZ-168 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Stop-hook block-message addendum for the git-clean gate when untracked `.humanize/` or `.humanize-*` runtime artifacts are detected alongside other dirty state.
- algorithmic_behavior: Static markdown template at `prompt-template/block/git-not-clean-humanize-local.md:2-8` instructs that `.humanize/` is created by `humanize:start-rlcr-loop`, should not be committed, and should be added to `.gitignore`. It is appended into `SPECIAL_NOTES` by the stop hook when the untracked Humanize runtime pattern is present.
- inputs_outputs_state: No runtime inputs or placeholders. Output is text appended to a stop-hook block response. In `hooks/loop-codex-stop-hook.sh:699-719`, the hook filters untracked `.humanize[-/]` entries out of the main dirty determination, then loads this note if such entries are present in the original git status.
- gates_or_invariants: The invariant is commit hygiene: `.humanize/` runtime state must remain local and uncommitted. The stop hook separately blocks tracked Humanize state earlier at `hooks/loop-codex-stop-hook.sh:690-696`, while this note handles untracked local runtime state as explanatory guidance.
- dependencies_and_callers: Loaded via `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` in `hooks/loop-codex-stop-hook.sh:714`. Template-loading behavior is defined in `hooks/lib/template-loader.sh:36-46`.
- edge_cases_or_failure_modes: If the template is missing, `hooks/loop-codex-stop-hook.sh:715-717` falls back to a shorter note. The git-status regex `^\?\? \.humanize[-/]` intentionally matches `.humanize/` and dash-separated legacy variants but not `.humanizeconfig`, as tested in `tests/test-stop-hook-legacy-compat.sh:162-183`.
- validation_or_tests: `tests/test-stop-hook-legacy-compat.sh:173-176` expects the stop hook output to include the “Special Case - .humanize directory detected” note when `.humanize-old/` is present together with a normal untracked dirty file.
- skip_candidate: `no`

### H2_DEV-HZ-198 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Guard message template for RLCR validators when a tool attempts to write or edit a round-specific file whose embedded round number does not match the active loop round.
- algorithmic_behavior: Renders `{{ACTION}}`, `{{CLAUDE_ROUND}}`, `{{FILE_TYPE}}`, `{{CURRENT_ROUND}}`, and `{{CORRECT_PATH}}` into a short block message at `prompt-template/block/wrong-round-number.md:1-7`. Write and edit validators use it with fallback text when stale round summary/contract paths are targeted.
- inputs_outputs_state: Inputs are validator-derived path metadata. `hooks/loop-write-validator.sh:318-335` extracts the target round and file type, computes `CORRECT_PATH`, and renders this template for wrong-round writes. `hooks/loop-edit-validator.sh:254-271` does the same for edits. Output is stderr block text and exit 2 from the validator.
- gates_or_invariants: The invariant is that current-round summary/contract files are the only writable/editable active round artifacts unless allowlisted. Wrong-round attempts are blocked when `CLAUDE_ROUND` is non-empty, differs from `CURRENT_ROUND`, and the path is not allowlisted, at `hooks/loop-write-validator.sh:322` and `hooks/loop-edit-validator.sh:258`.
- dependencies_and_callers: Depends on shared template rendering through `load_and_render_safe` in `hooks/lib/template-loader.sh:188-210`, which falls back if the template is missing or renders empty. Rendering is single-pass in `render_template` to prevent placeholder injection, described at `hooks/lib/template-loader.sh:48-56`.
- edge_cases_or_failure_modes: Read validation uses sibling template `block/wrong-round-file.md` rather than this assigned file at `hooks/loop-read-validator.sh:286-298`, so this template is write/edit focused. If the target filename lacks a round number, validators skip this gate. If a file is allowlisted, wrong-round blocking is bypassed.
- validation_or_tests: `tests/test-template-loader.sh:151-161` and `tests/test-templates-comprehensive.sh:498-510` render this real template and assert title, stale round filename, current round, and correct path substitution.
- skip_candidate: `no`

### H2_DEV-HZ-228 `file` `scripts/lib/monitor-skill.sh`
- cursor: `[_]`
- core_role: Runtime monitor implementation for `.humanize/skill` invocations from `ask-codex` and `ask-gemini`. It provides `_humanize_monitor_skill`, which powers `humanize monitor skill`, `humanize monitor codex`, and `humanize monitor gemini`.
- algorithmic_behavior: Parses `--once` and `--tool-filter` at `scripts/lib/monitor-skill.sh:31-41`, validates `.humanize/skill` exists at `scripts/lib/monitor-skill.sh:43-48`, derives invocation tool from `metadata.md` or `input.md` at `scripts/lib/monitor-skill.sh:50-64`, filters and sorts timestamp-named invocation directories at `scripts/lib/monitor-skill.sh:77-90`, selects the newest invocation with watchable content at `scripts/lib/monitor-skill.sh:97-117`, counts status buckets at `scripts/lib/monitor-skill.sh:119-143`, chooses the best output/log/cache file at `scripts/lib/monitor-skill.sh:165-217`, and renders either a one-shot report or an interactive terminal view.
- inputs_outputs_state: Inputs are `.humanize/skill/<timestamp...>/input.md`, optional `metadata.md`, optional `output.md`, local `cache/`, and global cache under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize/<sanitized-project>/skill-<id>` at `scripts/lib/monitor-skill.sh:154-163`. Output is terminal UI or a one-shot text report. It does not modify invocation state; it only starts/stops a local `tail -f` child in interactive mode.
- gates_or_invariants: Directory names must start with `YYYY-MM-DD_HH-MM-SS` to count at `scripts/lib/monitor-skill.sh:84-86`. Missing `metadata.md` means running at `scripts/lib/monitor-skill.sh:130-140` and `scripts/lib/monitor-skill.sh:173-174`. Legacy invocations with unknown tool count as codex when codex filter is active at `scripts/lib/monitor-skill.sh:66-74`.
- dependencies_and_callers: Sourced by `scripts/humanize.sh:1408-1410`; command routing calls it at `scripts/humanize.sh:1355-1370`. It depends on `monitor_get_yaml_value` and `monitor_format_timestamp` from `scripts/lib/monitor-common.sh:198-232`, and `humanize_split_to_array` from `scripts/humanize.sh:22-34`.
- edge_cases_or_failure_modes: Missing `.humanize/skill` returns 1 with guidance. Empty filtered result returns 1 in once mode at `scripts/lib/monitor-skill.sh:339-347`. Running invocations prefer live logs over output files at `scripts/lib/monitor-skill.sh:202-207`; completed invocations prefer `output.md` then cache at `scripts/lib/monitor-skill.sh:209-214`. Interactive cleanup restores scroll region and screen state, kills/disowns tail, and handles bash/zsh traps at `scripts/lib/monitor-skill.sh:428-469` and `scripts/lib/monitor-skill.sh:521-526`.
- validation_or_tests: `tests/test-skill-monitor.sh:112-133` covers missing/empty directory behavior; `tests/test-skill-monitor.sh:141-192` covers a single successful invocation; `tests/test-skill-monitor.sh:200-244` covers mixed statuses and newest focus; `tests/test-skill-monitor.sh:251-267` covers running status; `tests/test-skill-monitor.sh:299-346` covers question extraction; `tests/test-skill-monitor.sh:375-389` verifies non-timestamp directories are ignored.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8 unique Item Evidence headings, each corresponding to one scheduler-assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`