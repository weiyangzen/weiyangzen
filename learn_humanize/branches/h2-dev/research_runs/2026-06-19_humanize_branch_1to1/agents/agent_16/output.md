# agent_16 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-016 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex-side review prompt templates for the RLCR loop. This directory defines the algorithmic review contract that decides whether the loop continues, transitions toward finalize, or blocks due to mainline drift.
- algorithmic_behavior: Contains five recursive child files: `code-review-phase.md`, `commit-history-section.md`, `full-alignment-review.md`, `goal-tracker-update-section.md`, and `regular-review.md`. `regular-review.md` forces plan-first review, goal alignment, finding lanes, a mandatory `Mainline Progress Verdict`, and `COMPLETE` only when all plan tasks/ACs are fully done with no pending/deferred work (`prompt-template/codex/regular-review.md:22`, `:35`, `:49`, `:56`, `:67`). `full-alignment-review.md` adds interval-based acceptance-criteria audit, forgotten/deferred item detection, drift audit, historical-round stagnation analysis, and `STOP` vs `COMPLETE` output rules (`prompt-template/codex/full-alignment-review.md:21`, `:52`, `:77`, `:94`, `:102`, `:106`). `code-review-phase.md` is an audit-only template for `codex review --base`, including expected severity marker parsing and generated artifact names (`prompt-template/codex/code-review-phase.md:3`, `:14`, `:15`, `:30`).
- inputs_outputs_state: Inputs are rendered variables from `hooks/loop-codex-stop-hook.sh`: current round, plan file, prompt file, summary content, goal tracker, docs path, commit-history section, review result file, loop timestamp, and previous-round indices (`hooks/loop-codex-stop-hook.sh:1010`, `:1014`, `:1019`, `:1034`, `:1069`, `:1100`, `:1118`). Outputs are review prompt files such as `round-N-review-prompt.md`, review results written by Codex, audit command/output/log files, goal-tracker edits, and terminal markers `COMPLETE` or `STOP`.
- gates_or_invariants: Review output must include `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED`; omission is a stop-hook block (`prompt-template/codex/regular-review.md:56`, `:61`; `prompt-template/codex/full-alignment-review.md:59`, `:66`; `hooks/loop-codex-stop-hook.sh:1835`). `COMPLETE` is only allowed when all tasks/ACs are fully met and no deferrals/pending work remain (`regular-review.md:72`, `full-alignment-review.md:112`). Goal tracker updates must never mutate the immutable section (`goal-tracker-update-section.md:10`).
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe`: goal-tracker section at `hooks/loop-codex-stop-hook.sh:1019`, commit-history section at `:1069`, full alignment at `:1102`, regular review at `:1118`, code-review audit at `:1252`. Uses template loader fallback behavior if templates are missing.
- edge_cases_or_failure_modes: Codex 0.130.0 compatibility is explicitly handled by treating `code-review-phase.md` as audit-only and avoiding prompt input when `--base` is used (`code-review-phase.md:4`). Full review interval config is sanitized if invalid and defaults to 5 (`hooks/loop-codex-stop-hook.sh:1024`). Prior-round references clamp at zero to avoid negative paths (`:1037`). Missing mainline verdict blocks exit unless last marker is `STOP` (`:1838`).
- validation_or_tests: `tests/test-commit-history-section.sh` validates commit-history rendering, round placeholders, recent round references, and fallback rendering (`tests/test-commit-history-section.sh:53`, `:88`, `:206`). Template reference/comprehensive tests also scan Codex templates via `load_and_render_safe` usage (`tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh` from reference search).
- skip_candidate: `no`

### H2_DEV-HZ-046 `file` `hooks/loop-edit-validator.sh`
- cursor: `[_]`
- core_role: Claude `PreToolUse` `Edit` validator for RLCR state-machine safety. It prevents edits to generated prompts, todos, state files, wrong-round loop artifacts, and immutable goal-tracker content.
- algorithmic_behavior: Reads hook JSON from stdin, extracts `.tool_name`, exits early unless it is `Edit`, extracts `tool_input.file_path`, lowercases it, and filters by session id (`hooks/loop-edit-validator.sh:23`, `:26`, `:30`, `:34`). It blocks non-allowlisted `round-*-todos.md` edits, all prompt-file edits, methodology-analysis edits outside two allowed report files, loop state-file edits, finalize contract edits, protected `plan.md` backups, wrong goal-tracker paths, immutable goal-tracker changes, and wrong-round summary/contract edits (`:40`, `:50`, `:71`, `:166`, `:181`, `:190`, `:204`, `:234`, `:247`).
- inputs_outputs_state: Input is Claude hook JSON with `tool_name`, `session_id`, and `tool_input.file_path`; goal-tracker edits also require `old_string`, `new_string`, and optional `replace_all` (`:215`, `:226`). Output is process exit status: `0` allow, `2` block with rendered message, `1` malformed/unsafe input or state. It reads active loop state via `resolve_active_state_file` and `parse_state_file_strict`, setting `CURRENT_ROUND` from `STATE_CURRENT_ROUND` (`:148`, `:155`, `:159`).
- gates_or_invariants: State files are hook-managed and blocked, with specific checks ordered before generic `state.md` matching (`:161`, `:166`, `:171`, `:176`). During methodology-analysis phase, only `methodology-analysis-report.md` and `methodology-analysis-done.md` under the active loop are editable (`:111`, `:113`). Goal-tracker immutable section must remain byte-equivalent after previewing the edit (`:229`, `:234`). Wrong-round edits are blocked unless allowlisted (`:258`).
- dependencies_and_callers: Installed in `hooks/hooks.json` under `PreToolUse` matcher `Edit` (`hooks/hooks.json:24`). Sources `hooks/lib/loop-common.sh` (`hooks/loop-edit-validator.sh:16`), using `extract_session_id`, `find_active_loop`, `resolve_active_state_file`, `parse_state_file_strict`, `is_round_file_type`, `is_allowlisted_file`, `preview_edit_result`, and template-backed block messages. `loop-common.sh` provides session isolation and zombie-loop protection in `find_active_loop` (`hooks/lib/loop-common.sh:308`) and strict state validation (`:524`).
- edge_cases_or_failure_modes: If project root cannot resolve, the validator exits open (`hooks/loop-edit-validator.sh:62`). If state is malformed, it fails closed with exit `1` (`:155`). If `realpath` is unavailable, methodology path handling rejects traversal and unresolved symlink targets before raw-prefix fallback (`:79`, `:82`, `:92`). Spawned agents with different session ids are noted as not restricted by hooks in methodology mode, relying on prompt sanitization (`:65`).
- validation_or_tests: Referenced by `tests/test-finalize-phase.sh` for finalize-state edit blocking (`tests/test-finalize-phase.sh:425`), `tests/test-allowlist-validators.sh` for allowlisted loop files, and robustness suites for hook input/session/path/state failures (`tests/robustness/test-hook-system-robustness.sh` from reference search).
- skip_candidate: `no`

### H2_DEV-HZ-076 `file` `tests/hub-client.test.ts`
- cursor: `[_]`
- core_role: Vitest executable specification for hub JSON-RPC client environment propagation. It verifies child-run and message routing defaults used by agent/workflow continuation.
- algorithmic_behavior: Tests `applyRunEnvironmentDefaults` from `src/hub-client.ts` for three cases: add ambient `parentRunId` to `run.spawn_child`, preserve explicit `parentRunId`, and add ambient `runId` plus agent `messageOrigin` to `run.send_message` (`tests/hub-client.test.ts:6`, `:25`, `:43`).
- inputs_outputs_state: Inputs are method names, param objects, and fake env maps containing `HUMANIZE2_RUN_ID`. Outputs are returned params objects with `parentRunId`, `runId`, and `messageOrigin` fields (`:18`, `:38`, `:54`). No persistent state is mutated.
- gates_or_invariants: Ambient run id must not override an explicit parent (`:25`). `run.send_message` must identify the current run as both destination and origin when `runId` is absent (`:54`). Origin defaults to `{ kind: "agent", sender: run id, sourceRunId: run id }` (`:57`).
- dependencies_and_callers: Covers `src/hub-client.ts`, where `applyRunEnvironmentDefaults` implements spawn-child defaulting at lines `43-47`, send-message defaulting at `50-55`, and origin-only injection at `58-66`. The CLI applies these defaults before `callHubRpc` (`src/hub-client.ts:103`). Server-side JSON-RPC supports `run.spawn_child` and `run.send_message` (`src/hub/http-server.ts:180`, `:188`) and validates `messageOrigin` shape (`:372`).
- edge_cases_or_failure_modes: The test does not cover empty `HUMANIZE2_RUN_ID`, `HUMANIZE2_RUN_SHORT_NAME`, explicit `messageOrigin`, invalid params, or `run.send_message` with explicit `runId` but missing origin; implementation has branches for several of these (`src/hub-client.ts:58`, `:71`). Mis-propagation would break parent-child lineage or agent-origin auditability.
- validation_or_tests: Run through `npm test` / `vitest` per `package.json:20`; this file is itself the direct validation surface. Related behavior is also exercised in hub/MCP tests that use `HUMANIZE2_RUN_ID` and `messageOrigin`.
- skip_candidate: `no`

### H2_DEV-HZ-106 `file` `tests/test-frontend-migration.sh`
- cursor: `[_]`
- core_role: Pattern-based executable spec for frontend migration behavior in the visualization dashboard. It validates UI algorithm contracts around project switcher removal, auth propagation, live-log SSE lifecycle, targeted refresh, and drag listener singleton behavior.
- algorithmic_behavior: Defines pass/fail counters, then greps implementation files for required and forbidden patterns (`tests/test-frontend-migration.sh:24`). Groups cover removal of `switchProject`/add-project chrome and project API calls (`:30`), token propagation through `_resolveAuthToken`, `Authorization: Bearer`, `window.authedFetch`, and `_withToken` SSE query injection (`:70`), live-log EventSource event handling (`:104`), lifecycle hardening and remote WebSocket avoidance (`:152`), cancellable statuses and UTF-8 decoding (`:194`), pipeline drag listener singleton (`:219`), and session-detail targeted refresh/race guards (`:260`).
- inputs_outputs_state: Inputs are static files under `viz/static/js/app.js`, `viz/static/js/actions.js`, `viz/static/js/pipeline.js`, and `viz/static/css/layout.css` (`:17`, `:18`, `:223`, `:145`). Outputs are colored PASS/FAIL lines and process exit `1` when `FAIL_COUNT > 0` (`:309`, `:314`).
- gates_or_invariants: Raw project switching/adding APIs must be absent (`:34`, `:46`, `:52`). Authenticated frontend calls must use `authedFetch` and SSE token query propagation (`:92`, `:98`). SSE clients must handle `snapshot`, `append`, `resync`, and `eof` (`:120`). `renderPipeline` must not add window listeners directly and must call `_ensureDragListeners()` (`:239`, `:246`). Route-change race guard must re-check route and skeleton after await (`:275`).
- dependencies_and_callers: Verifies `app.js` implementation symbols such as `_resolveAuthToken`, `_withToken`, `_scheduleHomeRefresh`, `_refreshHomeCards`, `_refreshSessionPartial`, `_startRemotePolling`, `_mountLiveLogPane`, and `_ensureSessionLogPane` (implementation references found in `viz/static/js/app.js:22`, `:60`, `:127`, `:161`, `:266`, `:327`, `:687`, `:935`). Verifies actions use `window.authedFetch` (`viz/static/js/actions.js:27` et seq.) and pipeline singleton implementation (`viz/static/js/pipeline.js:25`).
- edge_cases_or_failure_modes: This is grep/awk-based, so it can produce false positives or negatives if equivalent behavior is refactored without matching symbol names. It intentionally avoids browser execution (`tests/test-frontend-migration.sh:11`), so DOM/runtime regressions outside the checked patterns are residual risk. It also creates no state except counters.
- validation_or_tests: The script is the validation gate; it exits nonzero on any failed pattern (`:314`). It is likely part of broader shell test execution via the repository test scripts, but direct invocation is sufficient for this contract.
- skip_candidate: `no`

### H2_DEV-HZ-136 `file` `tests/test-viz-isolation.sh`
- cursor: `[_]`
- core_role: Pattern and small behavioral shell spec for per-project viz dashboard isolation, tmux session naming, port/url state, host probing, readiness failure, and IPv6 URL correctness.
- algorithmic_behavior: Sources `viz/scripts/viz-session-name.sh`, asserts `viz_tmux_session_name` exists, compares stable/distinct names for different project paths, verifies empty input legacy fallback, then checks `viz-start.sh`, `viz-stop.sh`, and `viz-status.sh` source the helper and derive `TMUX_SESSION` from `PROJECT_DIR` (`tests/test-viz-isolation.sh:33`, `:40`, `:49`, `:59`, `:73`, `:81`, `:99`). Later groups validate `viz.url` persistence, configured-host port probing, readiness probe fail-closed behavior, and IPv6 bracketing (`:108`, `:136`, `:175`, `:210`).
- inputs_outputs_state: Inputs are launcher scripts and helper paths (`:18`, `:19`, `:20`, `:21`). It creates a temporary URL probe script with `mktemp`, registers cleanup trap, and runs a matrix over host values (`:226`, `:228`, `:230`, `:254`). Outputs are PASS/FAIL counts and exit `1` when failures exist (`:268`, `:273`).
- gates_or_invariants: Session names must be per-project and stable, with `humanize-viz-` prefix, but empty project path must yield legacy `humanize-viz` (`:48`, `:58`, `:66`, `:73`). Scripts must not hard-code `TMUX_SESSION="humanize-viz"` (`:90`). Health/status must use persisted `viz.url` with localhost fallback for older deployments (`:112`, `:118`, `:130`). Readiness must track success, clean stale files, and exit nonzero on failure (`:191`, `:197`, `:204`). Specific IPv6 addresses must be bracketed while loopback/wildcards normalize to localhost (`:219`, `:255`).
- dependencies_and_callers: Targets `viz/scripts/viz-session-name.sh`, `viz-start.sh`, `viz-stop.sh`, and `viz-status.sh`. Implementation references confirm `URL_FILE="$HUMANIZE_DIR/viz.url"` and derived `TMUX_SESSION` in start/status/stop (`viz/scripts/viz-start.sh:59`, `:64`; `viz/scripts/viz-status.sh:28`, `:29`; `viz/scripts/viz-stop.sh:28`, `:29`), configured `probe_host` in `find_port` (`viz/scripts/viz-start.sh:140`), URL writing/bracketing (`:169`, `:187`, `:192`), and readiness cleanup (`:223`, `:234`, `:236`).
- edge_cases_or_failure_modes: This spec relies heavily on textual shape. It does include one behavioral replay for URL construction, but does not start tmux or a server. `mktemp`/`chmod` require writable temp space; in strict read-only environments, this group may fail for environment reasons rather than implementation correctness.
- validation_or_tests: Direct shell validation; exits nonzero on `FAIL_COUNT > 0` (`tests/test-viz-isolation.sh:273`). Covers T9/AC-8 plus later Round 11/14/16/18 fixes documented in comments.
- skip_candidate: `no`

### H2_DEV-HZ-166 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: Block-message template for the invariant that `finalize-state.md` is system-managed during Finalize Phase and cannot be user/agent modified.
- algorithmic_behavior: Renders a concise denial message and redirects the actor toward allowed finalize actions: run code-simplifier, commit changes, and write `finalize-summary.md` (`prompt-template/block/finalize-state-file-modification.md:1`, `:3`, `:5`).
- inputs_outputs_state: Has no dynamic placeholders. Input is the hook/template loader selecting this template. Output is stderr block content used by validators; no state mutation.
- gates_or_invariants: `finalize-state.md` cannot be modified (`:3`). The allowed writable finalize artifact is `finalize-summary.md` (`:8`).
- dependencies_and_callers: Rendered by `finalize_state_file_blocked_message` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:873`, `:879`). Called from edit validator (`hooks/loop-edit-validator.sh:171`), write validator (`hooks/loop-write-validator.sh:207`), and bash validator for direct or wrapped modification attempts (`hooks/loop-bash-validator.sh:280`, `:438`, `:470`).
- edge_cases_or_failure_modes: Template absence falls back to inline message in `loop-common.sh` (`hooks/lib/loop-common.sh:875`). The template itself does not mention cancel authorization; bash validator separately allows authorized cancel operations before blocking (`hooks/loop-bash-validator.sh:281`).
- validation_or_tests: `tests/test-finalize-phase.sh` checks the block function exists and that finalize-state modification attempts are blocked; reference search shows direct hook invocations around `tests/test-finalize-phase.sh:425` and `:437`.
- skip_candidate: `no`

### H2_DEV-HZ-196 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Block-message template for enforcing that loop file reads target the active RLCR loop directory and current round artifacts.
- algorithmic_behavior: Renders the attempted path, active loop directory, current prompt/summary paths, and a fallback shell `cat` instruction (`prompt-template/block/wrong-file-location.md:1`, `:3`, `:5`, `:9`). It is a user-facing contract message for path-location violations rather than a validator itself.
- inputs_outputs_state: Inputs are template variables `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND`; `CORRECT_PATH` may be passed by callers but this template does not render it (`hooks/loop-read-validator.sh:236`, `:275`). Output is block text on stderr and the caller exits `2`.
- gates_or_invariants: Current loop files are expected under `{{ACTIVE_LOOP_DIR}}/` (`wrong-file-location.md:3`). Current round prompt/summary filenames are derived from `CURRENT_ROUND` (`:6`, `:7`). The message distinguishes normal tool read from explicit shell `cat` when the user really needs the wrong-location file (`:9`).
- dependencies_and_callers: Used by `hooks/loop-read-validator.sh` for wrong goal-tracker path (`hooks/loop-read-validator.sh:227`) and round file read outside the active loop directory (`:270`). Sibling write/edit validators use more specific wrong-location templates for writes/contracts/summaries.
- edge_cases_or_failure_modes: The template says “read” and is read-validator oriented, so it is a skip candidate only for edit/write core paths; still core to the broader RLCR path gate. Because `CORRECT_PATH` is not interpolated by this template, callers that pass it rely on active-dir/current-round lines instead.
- validation_or_tests: Template reference tests scan `load_and_render_safe` calls and verify referenced templates exist. Path validation/robustness suites exercise wrong-location read behavior via `loop-read-validator.sh`.
- skip_candidate: `no`

### H2_DEV-HZ-226 `file` `scripts/lib/model-router.sh`
- cursor: `[_]`
- core_role: Shared model/provider routing helper for runtime scripts that choose Codex vs Claude and normalize reasoning effort.
- algorithmic_behavior: Source guard prevents double-loading (`scripts/lib/model-router.sh:6`). `detect_provider` rejects empty model names, routes `gpt-*` and `o[0-9]*` to `codex`, routes `claude-*` or names containing `haiku|sonnet|opus` case-insensitively to `claude`, and errors on unknown names (`:10`, `:18`, `:23`, `:28`). `check_provider_dependency` maps provider to required binary and verifies it exists in `PATH` (`:32`, `:36`, `:49`). `map_effort` validates provider and effort, maps `xhigh` to `high` for Claude, otherwise passes effort through (`:62`, `:75`, `:84`, `:90`).
- inputs_outputs_state: Inputs are model name, provider, and effort strings. Outputs are stdout values (`codex`, `claude`, mapped effort) or stderr errors/info with nonzero returns. No persistent state beyond `_MODEL_ROUTER_LOADED`.
- gates_or_invariants: Only providers `codex` and `claude` are accepted (`:66`). Only efforts `xhigh`, `high`, `medium`, and `low` are valid (`:75`). Missing CLI dependencies are hard errors with install hints (`:53`, `:55`). Unknown models do not silently default (`:28`).
- dependencies_and_callers: Sourced by `scripts/bitlesson-select.sh` (`scripts/bitlesson-select.sh:11`). That caller uses `detect_provider` for `BITLESSON_MODEL`, honors `provider_mode=codex-only`, falls back to Codex if the configured provider binary is absent, then rechecks dependency (`scripts/bitlesson-select.sh:113`, `:125`, `:127`, `:136`). It is validated by `tests/test-model-router.sh`.
- edge_cases_or_failure_modes: Claude detection matches any model containing `haiku`, `sonnet`, or `opus`, which may overmatch non-Claude names containing those substrings. Codex detection only catches lowercase `gpt-` and `o` followed by a digit; uppercase GPT/O names fail. `check_provider_dependency` is runtime-environment sensitive. `map_effort xhigh claude` emits an info line to stderr, which callers must tolerate.
- validation_or_tests: `tests/test-model-router.sh` covers Codex routes (`gpt-5.3-codex`, `gpt-4o`, `o3-mini`, `o1-pro`, `o4-mini`), Claude routes (`haiku`, `sonnet`, `opus`, `claude-sonnet-4-6`, uppercase OPUS in full model), unknown/empty model errors, dependency success/failure with mock binaries, and effort mapping/errors (`tests/test-model-router.sh:37`, `:71`, `:122`, `:173`, `:207`, `:224`, `:243`, `:258`, `:311`, `:397`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8/8 item evidence sections present; each assigned row is represented once as a `###` evidence heading.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`