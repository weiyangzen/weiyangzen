# agent_03 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 9
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: Slash-command workflow layer for the Humanize planning, exploration, RLCR startup, and cancellation algorithms. Recursive inspection found six flat files: `cancel-rlcr-loop.md`, `explore-idea.md`, `gen-idea.md`, `gen-plan.md`, `refine-plan.md`, and `start-rlcr-loop.md`.
- algorithmic_behavior: The directory defines user-facing state machines rather than shell implementations. `gen-idea.md` converts loose input into repo-grounded idea drafts plus companion directions JSON via validation, direction generation, parallel read-only Explore subagents, synthesis, and JSON validation (`commands/gen-idea.md:48`, `commands/gen-idea.md:111`, `commands/gen-idea.md:196`). `explore-idea.md` launches bounded prototype workers and synthesizes canonical explore artifacts (`commands/explore-idea.md:23`, `commands/explore-idea.md:125`, `commands/explore-idea.md:201`). `gen-plan.md` transforms drafts into implementation plans through validation, relevance checks, Codex-first analysis, Claude/Codex convergence, manual decision gates, and optional RLCR start (`commands/gen-plan.md:32`, `commands/gen-plan.md:184`, `commands/gen-plan.md:255`, `commands/gen-plan.md:594`). `refine-plan.md` scans annotated plans, classifies comments, applies/refuses edits, writes QA, and performs atomic output replacement (`commands/refine-plan.md:184`, `commands/refine-plan.md:276`, `commands/refine-plan.md:508`). `start-rlcr-loop.md` wraps RLCR setup with plan compliance, quiz, and setup script invocation (`commands/start-rlcr-loop.md:13`, `commands/start-rlcr-loop.md:60`, `commands/start-rlcr-loop.md:109`). `cancel-rlcr-loop.md` delegates cancellation to the script and only asks confirmation for finalize-phase cancellation (`commands/cancel-rlcr-loop.md:11`, `commands/cancel-rlcr-loop.md:24`).
- inputs_outputs_state: Inputs are command arguments, draft files, annotated plans, directions JSON, implementation plans, and config. Outputs are draft `.md`, `.directions.json`, explore run directories, `manifest.json`, `worker-results.jsonl`, `explore-report.md`, `final-idea.md`, refined plans, QA docs, and RLCR loop state created by `scripts/setup-rlcr-loop.sh`. Directory-level state progression is loose idea -> directions -> prototype exploration -> final idea -> plan -> RLCR loop -> review/finalize/cancel.
- gates_or_invariants: Command frontmatter constrains tools per workflow. Planning commands explicitly forbid implementation (`commands/gen-plan.md:20`, `commands/refine-plan.md:19`). Explore requires explicit user confirmation, no remote pushes, manifest-before-dispatch, no nested slash commands/Skills, exact branch naming, targeted tests, and no dropped worker results (`commands/explore-idea.md:23`). Start command applies plan path safety, compliance, branch-switch detection, and advisory understanding quiz before invoking setup (`commands/start-rlcr-loop.md:28`, `commands/start-rlcr-loop.md:41`, `commands/start-rlcr-loop.md:71`).
- dependencies_and_callers: These are invoked as Humanize slash commands. They depend on sibling scripts such as `validate-gen-idea-io.sh`, `validate-directions-json.sh`, `validate-explore-idea-io.sh`, `validate-gen-plan-io.sh`, `ask-codex.sh`, `validate-refine-plan-io.sh`, `setup-rlcr-loop.sh`, and `cancel-rlcr-loop.sh`; they also coordinate with `prompt-template/explore/*`, `prompt-template/plan/*`, and RLCR hooks.
- edge_cases_or_failure_modes: Malformed validator output, degraded direction/proposal counts, JSON schema failure with no-partial-output cleanup, user denial before explore dispatch, all prototype workers failing, pending decisions blocking auto-start, direct mode forcing partial convergence, comment parse errors, and finalize-phase cancellation requiring confirmation.
- validation_or_tests: `tests/test-explore-command-structure.sh` asserts explore command phases, allowed tools, hard constraints, worktree dispatch, sentinel parsing, and two-tier report structure (`tests/test-explore-command-structure.sh:49`, `tests/test-explore-command-structure.sh:102`, `tests/test-explore-command-structure.sh:187`). `tests/test-explore-manifest.sh` asserts manifest and JSONL schema surfaces (`tests/test-explore-manifest.sh:48`, `tests/test-explore-manifest.sh:149`).
- skip_candidate: `no`

### H2_DEV-HZ-033 `file` `commands/explore-idea.md`
- cursor: `[_]`
- core_role: Coordinator specification for bounded parallel prototype exploration from an idea draft or directions JSON, producing canonical decision artifacts for later plan generation.
- algorithmic_behavior: Phase 1 validates IO and parses validator-emitted keys, including `RUN_ID`, `RUN_DIR`, `BASE_COMMIT`, selected directions, concurrency, worker caps, templates, and Codex review model settings (`commands/explore-idea.md:52`). Phase 2 shows a dispatch plan and requires explicit confirmation before any write/worker launch (`commands/explore-idea.md:84`). Phase 3 creates `<RUN_DIR>/dispatch-prompts`, materializes one prompt per direction, hashes prompts, then writes `manifest.json` before dispatch (`commands/explore-idea.md:125`, `commands/explore-idea.md:161`). Phase 4 batches Agent workers by `EFFECTIVE_CONCURRENCY` and requires isolated git worktrees (`commands/explore-idea.md:201`). Phase 5 extracts sentinel-delimited JSON from workers into `worker-results.jsonl`, records coordinator failures, and updates manifest statuses (`commands/explore-idea.md:228`). Phase 6 ranks product direction separately from prototype readiness, writes report/final idea artifacts, and emits cleanup/adoption paths (`commands/explore-idea.md:265`).
- inputs_outputs_state: Inputs are `<draft-or-directions-json>`, optional direction selection/concurrency/timeouts, validation stdout, the directions JSON body, worker prompt template, report template, final idea template, and worker Agent outputs. Outputs include `<RUN_DIR>/dispatch-prompts/*.md`, `<RUN_DIR>/manifest.json`, `<RUN_DIR>/worker-results.jsonl`, `<REPORT_PATH>` as `explore-report.md`, `<FINAL_IDEA_PATH>` as `final-idea.md`, optional `.failed`, and local worktree branches named `explore/<RUN_ID>/<dir_slug>`.
- gates_or_invariants: Hard constraints forbid remote push, require user confirmation, require manifest before dispatch, forbid nested Skills/slash commands in worker prompts, forbid unsupported `--effort max`, require workers to branch from current `HEAD == <BASE_COMMIT>`, require targeted tests, require `CLAUDE_PROJECT_DIR="$PWD"` for worker Codex calls, require exact validation-provided `CODEX_REVIEW_MODEL_SPEC`, and require every worker result to be recorded (`commands/explore-idea.md:23`). Result collection must append a row for valid JSON, failed start, parse failure, or missing summary; no result may vanish (`commands/explore-idea.md:221`, `commands/explore-idea.md:242`).
- dependencies_and_callers: Called by the `/humanize:explore-idea` workflow and normally follows `/humanize:gen-idea` output (`commands/gen-idea.md:260`). Depends on `scripts/validate-explore-idea-io.sh`, `scripts/validate-directions-json.sh`, `Agent`, git, `jq`, `shasum`/`sha256sum`, `prompt-template/explore/worker-prompt.md`, `report-template.md`, and `final-idea-template.md`.
- edge_cases_or_failure_modes: Validation exit codes distinguish missing input, missing companion JSON, schema failure, cap/argument errors, dirty/missing Git base state, run directory collision, and missing templates (`commands/explore-idea.md:59`). User denial stops before manifest/worktrees (`commands/explore-idea.md:121`). Manifest write failure writes `.failed` and aborts (`commands/explore-idea.md:197`). One worker failure is recorded while others continue; all-worker failure writes `.failed`, patches manifest, and still emits failure artifacts (`commands/explore-idea.md:248`, `commands/explore-idea.md:252`, `commands/explore-idea.md:368`).
- validation_or_tests: `tests/test-explore-command-structure.sh` checks required command structure, tools, hard constraints, worktree isolation, branch naming, sentinel parsing, and two-tier ranking (`tests/test-explore-command-structure.sh:123`, `tests/test-explore-command-structure.sh:162`, `tests/test-explore-command-structure.sh:187`). `tests/test-explore-manifest.sh` verifies documented manifest fields, per-worker fields, run directory structure, `.failed`, and JSONL schema (`tests/test-explore-manifest.sh:48`, `tests/test-explore-manifest.sh:82`, `tests/test-explore-manifest.sh:117`).
- skip_candidate: `no`

### H2_DEV-HZ-063 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Runtime initializer for the RLCR state machine. It validates a plan/review-only invocation, enforces repository safety, creates a timestamped `.humanize/rlcr/<session>` directory, and seeds state, prompt, contract, goal tracker, summary, BitLesson, and session-id capture artifacts.
- algorithmic_behavior: Parses flags for plan file, iteration caps, Codex model/effort/timeout, push policy, base branch, skip-impl, Codex question handling, agent teams, BitLesson, and privacy (`scripts/setup-rlcr-loop.sh:39`, `scripts/setup-rlcr-loop.sh:189`). It resolves project root, checks dependencies (`codex`, `jq`, `git`), blocks if another active loop exists, validates agent-teams env, merges explicit/positional plan paths, and handles skip-impl no-plan placeholder mode (`scripts/setup-rlcr-loop.sh:331`, `scripts/setup-rlcr-loop.sh:345`, `scripts/setup-rlcr-loop.sh:374`, `scripts/setup-rlcr-loop.sh:390`, `scripts/setup-rlcr-loop.sh:404`). It then validates git repo state, plan path, plan tracking policy, content minimums, branch/model YAML safety, clean working tree, and base branch/local base commit (`scripts/setup-rlcr-loop.sh:438`, `scripts/setup-rlcr-loop.sh:460`, `scripts/setup-rlcr-loop.sh:567`, `scripts/setup-rlcr-loop.sh:620`, `scripts/setup-rlcr-loop.sh:691`, `scripts/setup-rlcr-loop.sh:728`, `scripts/setup-rlcr-loop.sh:753`). After gates pass, it writes loop artifacts and prints the initial prompt plus completion requirements (`scripts/setup-rlcr-loop.sh:826`, `scripts/setup-rlcr-loop.sh:885`, `scripts/setup-rlcr-loop.sh:934`, `scripts/setup-rlcr-loop.sh:1200`, `scripts/setup-rlcr-loop.sh:1452`).
- inputs_outputs_state: Inputs are plan path/flags, environment defaults from `loop-common.sh`, project git state, config-backed defaults, and templates. Outputs include copied or placeholder `plan.md`, `state.md`, `.pending-session-id`, optional `.review-phase-started`, `goal-tracker.md`, `round-0-summary.md`, `round-0-contract.md`, `round-0-prompt.md`, and `.humanize/bitlesson.md`. Normal mode starts with `review_started: false`; skip-impl starts with `review_started: true` and `.review-phase-started` (`scripts/setup-rlcr-loop.sh:877`, `scripts/setup-rlcr-loop.sh:925`).
- gates_or_invariants: Only one active loop is allowed (`scripts/setup-rlcr-loop.sh:374`). Plan paths must be relative, no spaces, no shell metacharacters, not symlinks, no symlink parent segments, inside project, not in submodules, existing/readable, and tracking status must match `--track-plan-file` (`scripts/setup-rlcr-loop.sh:460`, `scripts/setup-rlcr-loop.sh:486`, `scripts/setup-rlcr-loop.sh:499`, `scripts/setup-rlcr-loop.sh:534`, `scripts/setup-rlcr-loop.sh:548`, `scripts/setup-rlcr-loop.sh:587`). Plan content must have at least five lines and at least three non-comment content lines (`scripts/setup-rlcr-loop.sh:623`, `scripts/setup-rlcr-loop.sh:633`). Codex effort is restricted to `xhigh|high|medium|low` (`scripts/setup-rlcr-loop.sh:716`). Working tree must be clean except ignored `.humanize` runtime paths (`scripts/setup-rlcr-loop.sh:728`). Base branch must be a local branch and base commit is captured at setup time (`scripts/setup-rlcr-loop.sh:753`, `scripts/setup-rlcr-loop.sh:812`).
- dependencies_and_callers: Sourced dependencies are `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` (`scripts/setup-rlcr-loop.sh:25`, `scripts/setup-rlcr-loop.sh:32`). It calls `scripts/bitlesson-init.sh` (`scripts/setup-rlcr-loop.sh:868`) and may include agent-team templates. It is invoked by `commands/start-rlcr-loop.md:114` and optional auto-start in `commands/gen-plan.md:613`. It coordinates with `hooks/loop-post-bash-hook.sh` via `.pending-session-id`.
- edge_cases_or_failure_modes: Handles skip-impl without a plan by creating a private placeholder, ignores `--track-plan-file` in that mode, supports skip-impl with explicit plan anchor, rejects remote-only base branches, fails closed on git timeout, rejects YAML-unsafe branch/model fields, disables BitLesson enforcement for review-only mode, traps final output SIGPIPE only after all important writes, and filters untracked `.humanize` runtime paths from clean-tree checks.
- validation_or_tests: `tests/robustness/test-path-validation-robustness.sh` directly runs this script against path/content fixtures (`tests/robustness/test-path-validation-robustness.sh:76`). Additional referenced suites include `tests/test-plan-file-validation.sh`, `tests/robustness/test-base-branch-detection.sh`, `tests/robustness/test-setup-scripts-robustness.sh`, `tests/test-agent-teams.sh`, and `tests/test-unified-codex-config.sh`.
- skip_candidate: `no`

### H2_DEV-HZ-093 `file` `tests/test-cancel-session.sh`
- cursor: `[_]`
- core_role: Executable specification for session-scoped RLCR cancellation behavior implemented by `scripts/cancel-rlcr-session.sh`.
- algorithmic_behavior: Builds a temp project with three session directories, two normal active `state.md` sessions and one `finalize-state.md` session (`tests/test-cancel-session.sh:35`, `tests/test-cancel-session.sh:42`). It then runs the helper under missing, nonexistent, normal, finalize, malicious, and legacy positional session-id cases.
- inputs_outputs_state: Inputs are a temporary `.humanize/rlcr` tree, session ids, `--project`, `--session-id`, and `--force`. Expected outputs are exit codes, output sentinels such as `CANCELLED <session>`, creation of `.cancel-requested`, and renaming active state file to `cancel-state.md`.
- gates_or_invariants: Missing `--session-id` exits code 3 (`tests/test-cancel-session.sh:51`). Nonexistent session exits code 1 (`tests/test-cancel-session.sh:63`). Normal cancellation mutates only the target session (`tests/test-cancel-session.sh:75`, `tests/test-cancel-session.sh:98`). Finalize phase requires `--force`, exiting code 2 otherwise (`tests/test-cancel-session.sh:105`). Unsafe session ids with traversal, absolute paths, backslashes, slashes, leading dot, or bare dot are rejected with code 3 and must not mutate sibling state (`tests/test-cancel-session.sh:126`). Legacy positional session id remains supported (`tests/test-cancel-session.sh:152`).
- dependencies_and_callers: Depends on `scripts/cancel-rlcr-session.sh` being executable (`tests/test-cancel-session.sh:18`, `tests/test-cancel-session.sh:24`). This helper is used by the visualization/dashboard per-session cancel path, not by the global slash command.
- edge_cases_or_failure_modes: Verifies finalize cancellation confirmation, sibling session isolation, traversal resistance, and backward compatibility. It explicitly creates a sibling `.humanize/sibling/state.md` to prove traversal attempts do not rename external state (`tests/test-cancel-session.sh:129`).
- validation_or_tests: The file is itself the validation. It counts pass/fail and exits nonzero on any failure (`tests/test-cancel-session.sh:164`).
- skip_candidate: `no`

### H2_DEV-HZ-123 `file` `tests/test-stop-gate.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/rlcr-stop-gate.sh`, the non-hook wrapper that reuses the RLCR stop-hook enforcement path.
- algorithmic_behavior: Creates minimal active-loop fixtures with `state.md` but no round summary so the stop gate should block with exit 10 (`tests/test-stop-gate.sh:18`). It exercises default root resolution, explicit `--project-root`, tracked Humanize state pre-blocking, benign dot-prefixed files, no-loop allow behavior, and JSON forwarding to the underlying hook.
- inputs_outputs_state: Inputs are temp git repos, active loop state, `CLAUDE_PROJECT_DIR`, `--project-root`, `--transcript-path`, `--json`, and a mock `loop-codex-stop-hook.sh` for the JSON-forwarding regression. Outputs are exit codes 0/10, textual `BLOCK:` or `ALLOW:`, and captured hook input JSON containing `transcript_path` and `session_id`.
- gates_or_invariants: Default project root uses caller cwd when env is empty and blocks active loop (`tests/test-stop-gate.sh:64`). `--project-root` targets a repo from outside it (`tests/test-stop-gate.sh:91`) and later is asserted to override cwd plus inherited env (`tests/test-stop-gate.sh:298`). Tracked `.humanize/rlcr/.../goal-tracker.md` blocks before normal loop validation with a dedicated reason (`tests/test-stop-gate.sh:118`). `.humanize-backup` and `.humanizeconfig` must not be treated as loop state (`tests/test-stop-gate.sh:147`). No active loop exits 0 and reports `ALLOW:` (`tests/test-stop-gate.sh:180`). Empty `session_id` must become JSON null without removing `transcript_path` (`tests/test-stop-gate.sh:206`, `tests/test-stop-gate.sh:267`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/rlcr-stop-gate.sh`, `hooks/lib/project-root.sh`, `jq`, and a staged mock hook for test 6. It indirectly specifies behavior of `hooks/loop-codex-stop-hook.sh` as wrapped by the gate.
- edge_cases_or_failure_modes: Regression coverage for `jq` object collapse when a field used `select(length > 0)` incorrectly; wrapper must reach the hook decision branch and exit 10 with empty session id plus real transcript path (`tests/test-stop-gate.sh:278`). The test documents that inherited `CLAUDE_PROJECT_DIR` is intentionally honored unless `--project-root` is supplied (`tests/test-stop-gate.sh:289`).
- validation_or_tests: The file is an executable test suite with `print_test_summary` at the end (`tests/test-stop-gate.sh:320`).
- skip_candidate: `no`

### H2_DEV-HZ-153 `file` `hooks/lib/loop-bg-tasks.sh`
- cursor: `[_]`
- core_role: Shared stop-hook background-task guard library. It decides whether an RLCR stop attempt should short-circuit because Claude Code still has asynchronous Agent/Bash work pending, and it manages parked-loop marker behavior across sessions.
- algorithmic_behavior: Provides utilities to expand leading tilde (`hooks/lib/loop-bg-tasks.sh:29`), extract transcript path from hook JSON (`hooks/lib/loop-bg-tasks.sh:41`), convert loop directory local timestamp to UTC transcript boundary (`hooks/lib/loop-bg-tasks.sh:65`), derive Claude task-output directory from transcript path (`hooks/lib/loop-bg-tasks.sh:107`), probe task liveness with output-file plus `lsof` (`hooks/lib/loop-bg-tasks.sh:131`), and enumerate pending background task ids (`hooks/lib/loop-bg-tasks.sh:193`). The pending-task algorithm extracts async launch ids from `toolUseResult`, extracts terminal task notifications from SDK and legacy XML queue records, computes launched minus completed, then prunes dead ids when task output files prove no open holders (`hooks/lib/loop-bg-tasks.sh:210`, `hooks/lib/loop-bg-tasks.sh:232`, `hooks/lib/loop-bg-tasks.sh:250`, `hooks/lib/loop-bg-tasks.sh:254`). `handle_bg_task_short_circuit` runs marker/session guards before the normal stop-hook logic (`hooks/lib/loop-bg-tasks.sh:311`).
- inputs_outputs_state: Inputs are loop directory, hook JSON, hook session id, transcript JSONL, optional since timestamp, task output files under `/tmp/claude-<uid>/.../tasks`, `jq`, `date`, and optional `LSOF_BIN`. Outputs are pending ids/counts, JSON `systemMessage` for allow-short-circuit cases, `bg-pending.marker` creation, and stale marker deletion.
- gates_or_invariants: Missing transcript, unreadable transcript, or missing `jq` returns nonzero from pending-id listing, so callers treat unknown as no short-circuit for normal stop flow and do not clear markers without proof (`hooks/lib/loop-bg-tasks.sh:202`, `hooks/lib/loop-bg-tasks.sh:205`, `hooks/lib/loop-bg-tasks.sh:418`). Liveness probing fails open when the output file is absent or `lsof` is unavailable (`hooks/lib/loop-bg-tasks.sh:120`). Pending background task guard must run before phase/state/git/summary/Codex gates (`hooks/lib/loop-bg-tasks.sh:389`). Cross-session marker guard leaves foreign parked loops untouched (`hooks/lib/loop-bg-tasks.sh:354`). Stale marker cleanup requires a successful fresh pending-id parse and empty result (`hooks/lib/loop-bg-tasks.sh:428`).
- dependencies_and_callers: Depends on `FIELD_SESSION_ID` and `resolve_active_state_file` from `hooks/lib/loop-common.sh` (`hooks/lib/loop-bg-tasks.sh:15`). It is sourced through `loop-common.sh`, and `hooks/loop-codex-stop-hook.sh` invokes `handle_bg_task_short_circuit` immediately after finding a loop (`hooks/loop-codex-stop-hook.sh:64`, `hooks/loop-codex-stop-hook.sh:83`).
- edge_cases_or_failure_modes: Handles local-time loop dirs versus UTC transcript events, absent or malformed transcript paths, older transcript records without timestamps, legacy task notifications embedded in XML, orphaned tasks killed without completion events, missing `lsof`, ambiguous callers with marker but no session id, and foreign sessions parked on background work.
- validation_or_tests: Covered by `tests/test-stop-hook-bg-allow.sh` references for pending task listing, tilde expansion, since timestamp filtering, marker fallback, and validator isolation; `tests/test-stop-gate.sh` exercises wrapper-level forwarding that feeds this library.
- skip_candidate: `no`

### H2_DEV-HZ-183 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Stop-hook block template for plan integrity violations during an active RLCR loop.
- algorithmic_behavior: Renders a concise block reason stating that `{{PLAN_FILE}}` changed since session start, plan modification is forbidden, and the user must cancel, update the plan, and start a new session. It also exposes the backup path (`prompt-template/block/plan-file-modified.md:1`).
- inputs_outputs_state: Inputs are template variables `PLAN_FILE` and `BACKUP_PATH`. Output is a human-readable block reason embedded in the stop hook JSON `reason`; it does not write state itself.
- gates_or_invariants: Enforces immutable plan scope during active implementation. In `hooks/loop-codex-stop-hook.sh`, tracked-plan uncommitted changes block first (`hooks/loop-codex-stop-hook.sh:361`), then content drift against the loop backup renders this template and blocks exit (`hooks/loop-codex-stop-hook.sh:377`, `hooks/loop-codex-stop-hook.sh:391`).
- dependencies_and_callers: Loaded via `load_and_render_safe "$TEMPLATE_DIR" "block/plan-file-modified.md"` in the stop hook (`hooks/loop-codex-stop-hook.sh:391`). Depends on template loader behavior from `loop-common.sh`.
- edge_cases_or_failure_modes: If template loading fails, stop hook has an inline fallback with equivalent cancel/update/restart instructions (`hooks/loop-codex-stop-hook.sh:379`). The tracked-plan status gate has a separate message for uncommitted tracked modifications, so this template primarily covers backup-content mismatch.
- validation_or_tests: Indirectly covered by plan-file and stop-hook validation suites that assert plan mutation blocks; no standalone template test was found in assigned scope.
- skip_candidate: `no`

### H2_DEV-HZ-213 `file` `prompt-template/claude/review-phase-prompt.md`
- cursor: `[_]`
- core_role: Claude-facing prompt template for the RLCR Review Phase when Codex code review finds `[P0-9]` issues and the loop must continue.
- algorithmic_behavior: Forces a required re-anchor on original plan, goal tracker, and round contract before code changes (`prompt-template/claude/review-phase-prompt.md:5`). Injects `{{REVIEW_CONTENT}}`, asks Claude to classify findings as blocking side issues or queued side issues, prevents queued work from becoming the round objective, and restricts task tags to `[blocking]` or `[queued]` unless review proves the previous mainline objective incomplete (`prompt-template/claude/review-phase-prompt.md:14`, `prompt-template/claude/review-phase-prompt.md:18`, `prompt-template/claude/review-phase-prompt.md:26`). It instructs contract refresh, blocking fixes first, no unrelated features, commit, and summary write (`prompt-template/claude/review-phase-prompt.md:34`).
- inputs_outputs_state: Inputs are `PLAN_FILE`, `GOAL_TRACKER_FILE`, `ROUND_CONTRACT_FILE`, `REVIEW_CONTENT`, and `SUMMARY_FILE`. Output is the next review-round prompt written to `.humanize/rlcr/<session>/round-N-prompt.md`; the stop hook also creates a matching summary scaffold and updates current round.
- gates_or_invariants: During review phase, COMPLETE has no effect, findings must be addressed until no `[P0-9]` issues remain, and summary must include fixed blocking issues, queued reclassifications, validation, and goal tracker reconciliation (`prompt-template/claude/review-phase-prompt.md:42`, `prompt-template/claude/review-phase-prompt.md:53`).
- dependencies_and_callers: Rendered by `continue_review_loop_with_issues` in `hooks/loop-codex-stop-hook.sh` (`hooks/loop-codex-stop-hook.sh:1506`, `hooks/loop-codex-stop-hook.sh:1563`). That path is reached from review-phase code review handling when `review_started=true`, base branch exists, and `.review-phase-started` marker validates the state (`hooks/loop-codex-stop-hook.sh:1934`, `hooks/loop-codex-stop-hook.sh:1956`).
- edge_cases_or_failure_modes: If the review phase marker is missing while state says review started, the stop hook blocks as a manual-toggle/state inconsistency (`hooks/loop-codex-stop-hook.sh:1935`). If the template omits BitLesson instructions and BitLesson is required, the hook appends them after rendering (`hooks/loop-codex-stop-hook.sh:1571`).
- validation_or_tests: Indirectly covered by review-phase/finalize/agent-team tests; `tests/test-agent-teams.sh` references review-phase prompt behavior around agent-team exclusion in review phase.
- skip_candidate: `no`

### H2_DEV-HZ-243 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for production plan path and content validation in `scripts/setup-rlcr-loop.sh`.
- algorithmic_behavior: Creates a temp git repo, installs a mock `codex` executable to avoid real Codex calls, writes gitignore rules to keep the worktree clean, and defines `create_valid_plan` plus `test_path_validation` helpers (`tests/robustness/test-path-validation-robustness.sh:18`, `tests/robustness/test-path-validation-robustness.sh:37`, `tests/robustness/test-path-validation-robustness.sh:49`, `tests/robustness/test-path-validation-robustness.sh:76`). The helper deletes prior RLCR state, runs `setup-rlcr-loop.sh` with `CLAUDE_PROJECT_DIR` pinned to the temp repo, and treats specific path/content validation messages as rejection (`tests/robustness/test-path-validation-robustness.sh:83`, `tests/robustness/test-path-validation-robustness.sh:88`, `tests/robustness/test-path-validation-robustness.sh:91`).
- inputs_outputs_state: Inputs are generated plan files and candidate path strings. Outputs are pass/fail assertions and the final test summary. The test mutates only its temp repo and cleans via shared test helper.
- gates_or_invariants: Positive cases accept normal relative path, root filename, dash/underscore, nested directories, and dots in filename (`tests/robustness/test-path-validation-robustness.sh:122`, `tests/robustness/test-path-validation-robustness.sh:131`, `tests/robustness/test-path-validation-robustness.sh:141`, `tests/robustness/test-path-validation-robustness.sh:151`, `tests/robustness/test-path-validation-robustness.sh:161`). Negative path cases reject absolute paths, spaces, semicolon, pipe, dollar, backticks, angle brackets, ampersand, asterisk, question mark, backslash, tilde, parentheses, braces, square brackets, hash, and exclamation (`tests/robustness/test-path-validation-robustness.sh:179`, `tests/robustness/test-path-validation-robustness.sh:188`, `tests/robustness/test-path-validation-robustness.sh:199`, `tests/robustness/test-path-validation-robustness.sh:325`). Symlink cases reject symlink plan files, parent-directory symlinks, and symlink chains (`tests/robustness/test-path-validation-robustness.sh:342`, `tests/robustness/test-path-validation-robustness.sh:352`, `tests/robustness/test-path-validation-robustness.sh:367`). Content cases reject empty, comments-only, too-short, nonexistent, and directory-as-file inputs (`tests/robustness/test-path-validation-robustness.sh:425`, `tests/robustness/test-path-validation-robustness.sh:434`, `tests/robustness/test-path-validation-robustness.sh:450`, `tests/robustness/test-path-validation-robustness.sh:463`, `tests/robustness/test-path-validation-robustness.sh:472`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, git, shell, production `scripts/setup-rlcr-loop.sh`, and a mock `codex` inserted at the front of `PATH`.
- edge_cases_or_failure_modes: Long filenames are accepted when the filesystem allows creation, deep ten-level nested paths are accepted, and a comment notes Unicode/CJK/emoji paths are allowed by current policy even though no explicit Unicode fixture is included in this file (`tests/robustness/test-path-validation-robustness.sh:387`, `tests/robustness/test-path-validation-robustness.sh:402`, `tests/robustness/test-path-validation-robustness.sh:414`). The helper’s acceptance classification also treats later dependency/gitignore errors as proof path validation passed (`tests/robustness/test-path-validation-robustness.sh:97`, `tests/robustness/test-path-validation-robustness.sh:102`).
- validation_or_tests: The file is the validation suite; it exits through `print_test_summary` (`tests/robustness/test-path-validation-robustness.sh:486`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 9 unique item sections above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`