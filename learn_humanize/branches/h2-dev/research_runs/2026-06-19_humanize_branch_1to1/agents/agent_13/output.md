# agent_13 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-013 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared shell-library layer for the RLCR hook state machine. The directory centralizes state parsing, active-loop discovery, project-root canonicalization, template rendering, background-task stop-hook parking, methodology-analysis transitions, and common gate/block-message helpers used by sibling hook scripts.
- algorithmic_behavior: `loop-common.sh` is the primary coordinator library. It validates hook JSON and UTF-8/null-byte safety at `hooks/lib/loop-common.sh:91`, loads config-backed Codex defaults at `hooks/lib/loop-common.sh:211`, resolves active state files in priority order at `hooks/lib/loop-common.sh:268`, finds the active loop with session isolation and zombie-loop prevention at `hooks/lib/loop-common.sh:332`, parses tolerant and strict YAML frontmatter state at `hooks/lib/loop-common.sh:494` and `hooks/lib/loop-common.sh:533`, extracts Codex review findings from the tail of review logs at `hooks/lib/loop-common.sh:740`, protects immutable goal-tracker sections at `hooks/lib/loop-common.sh:928`, authorizes only narrowly shaped cancel-state `mv` commands at `hooks/lib/loop-common.sh:1056`, blocks accidental `.humanize` staging at `hooks/lib/loop-common.sh:1285`, and terminates loops by renaming active state to terminal state at `hooks/lib/loop-common.sh:1545`.
- inputs_outputs_state: Inputs include Claude hook JSON, `.humanize/rlcr/<timestamp>/*state.md`, Codex review logs under the loop cache, project/user/default config JSON, shell command strings, file paths, transcript JSONL, and templates. Outputs include exported/default variables such as `DEFAULT_CODEX_MODEL`, `DEFAULT_CODEX_EFFORT`, `STATE_*` fields, rendered block messages, active loop directory paths, review issue markdown, terminal state file names, and stop-hook JSON emitted by background/methodology helpers.
- gates_or_invariants: Root resolution intentionally prefers `CLAUDE_PROJECT_DIR` except linked worktree cases and never falls back to `pwd`, preventing state lookup drift (`hooks/lib/project-root.sh:5`, `hooks/lib/project-root.sh:20`). Canonicalization distinguishes full leaf dereference from parent-only canonicalization to avoid symlink authorization bypasses (`hooks/lib/project-root.sh:78`, `hooks/lib/project-root.sh:121`). Template rendering is single-pass, preserving missing placeholders and preventing injected `{{VAR}}` in values from being re-expanded (`hooks/lib/template-loader.sh:48`). Active loop lookup refuses older zombie loops when the newest matching session is terminal (`hooks/lib/loop-common.sh:308`). Strict state parsing requires frontmatter, `current_round`, `max_iterations`, `review_started`, and `base_branch` (`hooks/lib/loop-common.sh:541`). Cancel authorization rejects command substitution, shell operators, mixed quote styles, extra args, path traversal via noncanonical aliases, and symlinked state leaves (`hooks/lib/loop-common.sh:1067`, `hooks/lib/loop-common.sh:1237`).
- dependencies_and_callers: `loop-common.sh` sources `project-root.sh`, `scripts/lib/config-loader.sh`, `template-loader.sh`, and then `loop-bg-tasks.sh` (`hooks/lib/loop-common.sh:178`, `hooks/lib/loop-common.sh:183`, `hooks/lib/loop-common.sh:240`, `hooks/lib/loop-common.sh:1583`). The hook scripts registered in `hooks/hooks.json` source these libraries: `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-plan-file-validator.sh`, and `loop-codex-stop-hook.sh`. `methodology-analysis.sh` is sourced by the stop hook at `hooks/loop-codex-stop-hook.sh:53`.
- edge_cases_or_failure_modes: JSON with null bytes, invalid UTF-8, missing `tool_name`, or excessive nesting fails closed. Malformed strict state blocks operations. Missing project root in hooks generally exits naturally to avoid false blocking. Missing or malformed config is best-effort in `loop-common.sh`, but required default config remains fatal inside `config-loader.sh`. Background-task liveness fails open when task output is missing or `lsof` is unavailable (`hooks/lib/loop-bg-tasks.sh:120`). Transcript parsing failures fail closed for stale-marker cleanup so a parked loop marker is not removed without evidence (`hooks/lib/loop-bg-tasks.sh:416`). Methodology analysis skips in privacy mode and blocks completion if the done marker or report is empty (`hooks/lib/methodology-analysis.sh:42`, `hooks/lib/methodology-analysis.sh:118`).
- validation_or_tests: This directory is exercised indirectly by assigned tests `tests/test-unified-codex-config.sh`, `tests/test-explore-command-structure.sh`, and `tests/claude-cache-stability.test.ts`, and by broader hook tests referenced in the tree. `tests/test-unified-codex-config.sh` directly sources `hooks/lib/loop-common.sh` and asserts config-backed defaults, stale-key ignoring, state parsing, and invalid effort rejection.
- skip_candidate: `no`

### H2_DEV-HZ-043 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Claude Code hook registration manifest for the RLCR state machine. It maps tool lifecycle events to the validator and stop-hook scripts that enforce RLCR loop boundaries.
- algorithmic_behavior: Registers `UserPromptSubmit` to run `loop-plan-file-validator.sh`, `PreToolUse` validators for `Write`, `Edit`, `Read`, and `Bash`, a `PostToolUse` Bash hook for session-id capture, and a `Stop` hook for Codex review/finalization with a long timeout (`hooks/hooks.json:4`, `hooks/hooks.json:14`, `hooks/hooks.json:52`, `hooks/hooks.json:63`).
- inputs_outputs_state: Inputs are Claude Code hook events. Outputs are delegated script decisions: allow, shell exit errors, block JSON, or stop-hook system messages. The manifest itself has no mutable state; it routes event state into `${CLAUDE_PLUGIN_ROOT}/hooks/*`.
- gates_or_invariants: Tool-specific matching ensures write/edit/read/bash constraints execute before tool use, while Bash post-processing runs only after Bash events. The Stop hook timeout is `7200` seconds, allowing long Codex review/finalization flows (`hooks/hooks.json:68`).
- dependencies_and_callers: Depends on `CLAUDE_PLUGIN_ROOT` being set by the plugin install/runtime. Registered scripts then depend on `hooks/lib`. For example, `loop-read-validator.sh` validates JSON and blocks todos/outdated RLCR files (`hooks/loop-read-validator.sh:25`, `hooks/loop-read-validator.sh:56`); `loop-write-validator.sh` blocks prompt/todos/state/invalid summary writes (`hooks/loop-write-validator.sh:57`, `hooks/loop-write-validator.sh:67`); `loop-bash-validator.sh` blocks Bash bypasses and direct hook execution (`hooks/loop-bash-validator.sh:24`, `hooks/loop-bash-validator.sh:200`); `loop-codex-stop-hook.sh` finds the active loop and delegates background-task guards before normal gates (`hooks/loop-codex-stop-hook.sh:64`, `hooks/loop-codex-stop-hook.sh:74`).
- edge_cases_or_failure_modes: If `${CLAUDE_PLUGIN_ROOT}` is unset or points to a stale install, hooks cannot resolve. Misregistration can bypass a whole class of gates. Stop hook timeout must be high enough for review, but a hung stop hook can hold the stop path until timeout.
- validation_or_tests: Hook installation/structure is covered elsewhere by tests such as `tests/test-codex-hook-install.sh`; this assigned file itself is a declarative manifest, not executable logic.
- skip_candidate: `no`

### H2_DEV-HZ-073 `file` `tests/claude-cache-stability.test.ts`
- cursor: `[_]`
- core_role: Executable cache-stability specification for Humanize2 workflow-agent prompts. It protects prompt layout so dynamic workflow context does not destroy Claude prompt-cache reuse across repeated turns.
- algorithmic_behavior: Generates 100 prompts with the same Claude Code envelope and stable task body but varying workflow run ids, vertex ids, ports, timestamps, artifact inputs, and board values (`tests/claude-cache-stability.test.ts:7`, `tests/claude-cache-stability.test.ts:68`). It estimates cache reuse by computing common prefix length between consecutive prompts and dividing average reusable prefix bytes by average prompt bytes (`tests/claude-cache-stability.test.ts:50`, `tests/claude-cache-stability.test.ts:122`).
- inputs_outputs_state: Inputs are synthetic `WorkflowAgentLaunchContext` objects and `promptWithWorkflowContext` from `src/agents/workflow-context.ts`. Output is a Vitest assertion result with metrics: Claude Code version, rounds, average prompt bytes, average reusable prefix bytes, and cache hit ratio.
- gates_or_invariants: The test requires the version to remain fixed, exactly 100 rounds, average prompt size above 10 KB, reusable prefix above 10 KB, and cache hit ratio above `0.9` (`tests/claude-cache-stability.test.ts:23`). The underlying source keeps stable instructions before the task/context block and sorts JSON keys recursively in `stableJsonValue`, making object key order deterministic (`src/agents/workflow-context.ts:7`, `src/agents/workflow-context.ts:64`).
- dependencies_and_callers: Depends on Vitest, `src/agents/workflow-context.ts`, and `src/agents/types.ts`. The same workflow-context helper is used by agent launchers in `src/agents/codex.ts` and `src/agents/claude.ts`, so this test indirectly constrains runtime agent prompt composition.
- edge_cases_or_failure_modes: Moving dynamic fields earlier in the prompt, adding variable envelope fields before stable content, unsorted JSON serialization, or shrinking the stable task/instruction prefix would reduce reusable prefix bytes and fail the ratio. The metric uses JavaScript string length rather than tokenizer bytes, so it is a deterministic proxy rather than exact provider billing telemetry.
- validation_or_tests: This file is itself the validation. It is narrowly focused on prompt-cache prefix stability rather than semantic agent correctness.
- skip_candidate: `no`

### H2_DEV-HZ-103 `file` `tests/test-explore-command-structure.sh`
- cursor: `[_]`
- core_role: Shell executable specification for `/humanize:explore-idea` command structure. It verifies that the exploration workflow keeps its intended phases, worker isolation, validation scripts, report artifacts, and productization handoff.
- algorithmic_behavior: Uses grep-based structural assertions against `commands/explore-idea.md`, `scripts/validate-explore-idea-io.sh`, report/final-idea templates, and usage docs. It checks command file/script existence, allowed tools, six workflow phases, hard constraints, Agent worktree dispatch, result sentinel collection, report placeholders, final-idea placeholders, and validator exit-code coverage (`tests/test-explore-command-structure.sh:30`, `tests/test-explore-command-structure.sh:102`, `tests/test-explore-command-structure.sh:159`, `tests/test-explore-command-structure.sh:187`, `tests/test-explore-command-structure.sh:350`).
- inputs_outputs_state: Inputs are repository files: `commands/explore-idea.md`, `scripts/validate-explore-idea-io.sh`, `prompt-template/explore/report-template.md`, `prompt-template/explore/final-idea-template.md`, and `docs/usage.md`. Outputs are pass/fail counters emitted via `tests/test-helpers.sh`.
- gates_or_invariants: Pins `validate-explore-idea-io.sh`, `validate-directions-json.sh`, `Agent`, `Write`, `Read`, `jq`, and `AskUserQuestion` as required allowed tools (`tests/test-explore-command-structure.sh:49`). Requires all phases `Phase 1` through `Phase 6` (`tests/test-explore-command-structure.sh:102`). Requires no remote push, manifest-before-dispatch, no nested skills, explicit user confirmation, and `isolation: "worktree"` (`tests/test-explore-command-structure.sh:130`, `tests/test-explore-command-structure.sh:151`, `tests/test-explore-command-structure.sh:162`). Requires result parsing via `EXPLORE_RESULT_JSON_BEGIN` and appending to `worker-results.jsonl` (`tests/test-explore-command-structure.sh:187`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` for `pass`, `fail`, and summary behavior. It validates command content that in turn references the IO validator and direction schema validator. The referenced command defines worker branch naming as `explore/<RUN_ID>/<dir_slug>`, worktree isolation, result JSON rows, final report synthesis, and cleanup guidance (`commands/explore-idea.md:31`, `commands/explore-idea.md:203`, `commands/explore-idea.md:237`, `commands/explore-idea.md:337`).
- edge_cases_or_failure_modes: This is a structural grep test, so it can miss semantic regressions if required phrases remain but behavior changes. Conversely, harmless wording changes can fail it. The test intentionally guards command-prompt contracts rather than executing workers.
- validation_or_tests: The file is an executable validator. It ends with `print_test_summary "explore-idea Command Structure Test Summary"` (`tests/test-explore-command-structure.sh:367`).
- skip_candidate: `no`

### H2_DEV-HZ-133 `file` `tests/test-unified-codex-config.sh`
- cursor: `[_]`
- core_role: Executable migration/regression specification for unified Codex configuration. It ensures RLCR, setup, stop hook, and ask-codex all use `codex_model` and `codex_effort` rather than stale `loop_reviewer_*` keys.
- algorithmic_behavior: Runs layered shell assertions over default config, config merging, `loop-common.sh` defaults, state parsing, stop-hook fallback, setup script output, invalid value handling, and `ask-codex` runtime logging. It creates temporary projects and state files, stubs Codex where needed, and uses `run_with_timeout` for script execution (`tests/test-unified-codex-config.sh:64`, `tests/test-unified-codex-config.sh:91`, `tests/test-unified-codex-config.sh:133`, `tests/test-unified-codex-config.sh:257`, `tests/test-unified-codex-config.sh:479`, `tests/test-unified-codex-config.sh:683`).
- inputs_outputs_state: Inputs include `config/default_config.json`, `scripts/lib/config-loader.sh`, `hooks/lib/loop-common.sh`, `hooks/loop-codex-stop-hook.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/ask-codex.sh`, generated temp `.humanize/config.json` files, and generated temp `state.md` files. Outputs are pass/fail/skip counters and temporary state/config artifacts.
- gates_or_invariants: Requires default config `codex_model` = `gpt-5.5` and `codex_effort` = `high`, with `loop_reviewer_*` absent (`tests/test-unified-codex-config.sh:69`). Requires project config overrides to beat defaults (`tests/test-unified-codex-config.sh:110`). Requires `loop-common.sh` caller presets to beat config (`tests/test-unified-codex-config.sh:181`). Invalid model characters, unsupported non-Codex model prefixes, and invalid effort values must warn and fall back (`tests/test-unified-codex-config.sh:197`, `tests/test-unified-codex-config.sh:224`). Stop hook must reject invalid state effort before invoking Codex (`tests/test-unified-codex-config.sh:524`). Setup state must include `codex_model`/`codex_effort` and omit `loop_reviewer` (`tests/test-unified-codex-config.sh:616`).
- dependencies_and_callers: Directly exercises `config-loader.sh`, which merges default, user, and project layers via jq with null stripping (`scripts/lib/config-loader.sh:63`, `scripts/lib/config-loader.sh:113`). Aligns with `loop-common.sh` config validation (`hooks/lib/loop-common.sh:211`), stop-hook fallback and validation (`hooks/loop-codex-stop-hook.sh:136`, `hooks/loop-codex-stop-hook.sh:172`), setup validation and state writing (`scripts/setup-rlcr-loop.sh:707`, `scripts/setup-rlcr-loop.sh:889`), and `ask-codex` default assignment/logging (`scripts/ask-codex.sh:41`, `scripts/ask-codex.sh:300`).
- edge_cases_or_failure_modes: Skips some sections if `jq`, `codex`, or target scripts are unavailable. Uses temporary git repositories, so failures can come from missing git/codex tooling rather than logic. It deliberately treats stale reviewer keys in config/state as ignored, not errors (`tests/test-unified-codex-config.sh:396`).
- validation_or_tests: This file is the validator and ends with `print_test_summary "Unified Codex Config Test Summary"` (`tests/test-unified-codex-config.sh:780`).
- skip_candidate: `no`

### H2_DEV-HZ-163 `file` `prompt-template/block/claude-eyes-timeout.md`
- cursor: `[_]`
- core_role: Block-message template for a Claude GitHub bot review wait timeout. It tells the operator that the bot did not acknowledge a trigger with an `eyes` reaction after configured retries.
- algorithmic_behavior: Renders `{{RETRY_COUNT}}` and `{{TOTAL_WAIT_SECONDS}}` into a timeout explanation, lists likely causes, and gives required remediation steps (`prompt-template/block/claude-eyes-timeout.md:3`, `prompt-template/block/claude-eyes-timeout.md:10`).
- inputs_outputs_state: Inputs are retry count and total wait seconds supplied by a caller. Output is human-facing markdown. No repository state transition is performed by the template itself.
- gates_or_invariants: The template is a failure gate message: it should only be shown when the external bot acknowledgement did not arrive. It does not define the retry algorithm, only the block/remediation text.
- dependencies_and_callers: `rg` found only the template itself referencing `claude-eyes-timeout`, `RETRY_COUNT`, or `TOTAL_WAIT_SECONDS` in this branch export. No direct caller was found under `scripts`, `hooks`, `commands`, `prompt-template`, or `tests`.
- edge_cases_or_failure_modes: If the template is orphaned, it can drift from the actual review-trigger path without test coverage. If later wired, missing variables would remain as placeholders because `template-loader.sh` preserves unknown `{{VAR}}` tokens by design (`hooks/lib/template-loader.sh:12`).
- validation_or_tests: No direct assigned or discovered test references this template.
- skip_candidate: `yes: likely orphaned or dormant block template in this branch; it is algorithm-adjacent as a gate message but no direct caller was found`

### H2_DEV-HZ-193 `file` `prompt-template/block/work-summary-missing.md`
- cursor: `[_]`
- core_role: Stop-hook block template that enforces the RLCR invariant that Claude must write a round/finalize work summary before exiting.
- algorithmic_behavior: Renders `{{SUMMARY_FILE}}` into a required-action block and enumerates expected summary content: implementation, created/modified files, tests, and remaining items (`prompt-template/block/work-summary-missing.md:5`, `prompt-template/block/work-summary-missing.md:10`).
- inputs_outputs_state: Input is the expected summary path. Output is markdown embedded in Stop-hook block JSON. It does not mutate state; it causes the hook to block natural exit until the file exists.
- gates_or_invariants: Enforces summary presence before downstream stop-hook review/finalization. In `hooks/loop-codex-stop-hook.sh`, the hook selects `finalize-summary.md` during finalize or `round-${CURRENT_ROUND}-summary.md` otherwise, checks `[[ ! -f "$SUMMARY_FILE" ]]`, renders this template, returns `{"decision":"block"}`, and exits naturally for Claude Code hook semantics (`hooks/loop-codex-stop-hook.sh:797`, `hooks/loop-codex-stop-hook.sh:804`, `hooks/loop-codex-stop-hook.sh:811`, `hooks/loop-codex-stop-hook.sh:824`).
- dependencies_and_callers: Called through `load_and_render_safe` in the stop hook, using `template-loader.sh` single-pass rendering. Related prompt templates instruct Claude to write summaries, including `prompt-template/claude/review-phase-prompt.md` and next-round/finalize templates.
- edge_cases_or_failure_modes: The gate checks file existence at this point; content quality is handled later by summary parsing/review behavior. If the template is missing or empty, the stop hook uses its inline fallback (`hooks/loop-codex-stop-hook.sh:808`).
- validation_or_tests: Covered indirectly by stop-hook tests that exercise missing-summary behavior; the assigned file itself is the rendered block contract.
- skip_candidate: `no`

### H2_DEV-HZ-223 `file` `prompt-template/plan/gen-plan-template.md`
- cursor: `[_]`
- core_role: Canonical structured plan-output template for `/humanize:gen-plan`. It defines the plan document schema that later RLCR setup, task routing, and plan QA consume.
- algorithmic_behavior: Requires sections for goal description, acceptance criteria with deterministic positive and negative tests, path boundaries, feasibility hints, dependency/sequence milestones, task breakdown, Claude-Codex deliberation, pending user decisions, implementation notes, and output-file convention (`prompt-template/plan/gen-plan-template.md:3`, `prompt-template/plan/gen-plan-template.md:6`, `prompt-template/plan/gen-plan-template.md:25`, `prompt-template/plan/gen-plan-template.md:57`, `prompt-template/plan/gen-plan-template.md:80`, `prompt-template/plan/gen-plan-template.md:91`, `prompt-template/plan/gen-plan-template.md:99`).
- inputs_outputs_state: Input is the draft/user intent consumed by `commands/gen-plan.md` and IO validation. Output is the main plan file such as `plan.md`; optionally, a translated sibling variant is produced when merged config resolves `alternative_plan_language` to a supported language (`prompt-template/plan/gen-plan-template.md:106`, `prompt-template/plan/gen-plan-template.md:112`).
- gates_or_invariants: Every task must include exactly one routing tag, either `coding` or `analyze` (`prompt-template/plan/gen-plan-template.md:71`). Acceptance criteria should include both passing and failing tests (`prompt-template/plan/gen-plan-template.md:8`). Implementation code/comments must not contain plan-specific workflow terminology such as `AC-`, `Milestone`, `Step`, or `Phase` (`prompt-template/plan/gen-plan-template.md:101`). Alternative language variants preserve identifiers, paths, APIs, and flags, and are disabled for empty, English, or unsupported values (`prompt-template/plan/gen-plan-template.md:118`, `prompt-template/plan/gen-plan-template.md:120`).
- dependencies_and_callers: `scripts/validate-gen-plan-io.sh` locates this template (`scripts/validate-gen-plan-io.sh:165`). `commands/gen-plan.md` embeds a matching Plan Structure block and repeats the routing-tag and variant rules (`commands/gen-plan.md:447`, `commands/gen-plan.md:529`). `tests/test-gen-plan.sh` asserts the command plan-structure block matches this template and that task tags are present. `commands/start-rlcr-loop.md` and `scripts/setup-rlcr-loop.sh` consume the `coding`/`analyze` tags for RLCR execution routing (`commands/start-rlcr-loop.md:149`, `scripts/setup-rlcr-loop.sh:1342`).
- edge_cases_or_failure_modes: Unsupported `alternative_plan_language` disables variant generation rather than failing. Output paths without extensions append `_<code>` directly (`prompt-template/plan/gen-plan-template.md:116`). A plan without valid task tags can still be prose-valid but violates downstream routing expectations; separate tests such as `tests/test-task-tag-routing.sh` and `tests/test-gen-plan.sh` guard that integration.
- validation_or_tests: `tests/test-gen-plan.sh` validates that `commands/gen-plan.md` stays synchronized with this template and includes mandatory `coding`/`analyze` tags. `tests/workflow-first-party-flows.test.ts` also expects workflow plan prompts to contain Task Breakdown and both routing tags.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8 item evidence headings present; each assigned item is represented once in `## Item Evidence`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`