# agent_11 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 9
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-011 `directory` `tests`
- cursor: `[_]`
- core_role: Recursive executable specification suite for Humanize’s shell hooks, RLCR loop state machine, planning/explore workflows, hub/session APIs, workflow graph/runtime, dashboard, config, timeout portability, and validator behavior. The directory contains 108 files total: 75 shell tests, 23 TypeScript/Vitest tests, 9 JSON fixtures, and 1 mock executable. Top-level tests cover command contracts and module behavior; `tests/robustness/` adds failure-mode and adversarial-state coverage.
- algorithmic_behavior: The main shell runner, `tests/run-all-tests.sh`, defines the suite list at lines 59-138, including template, stop-gate, plan validation, monitor, explore, BitLesson, provider routing, dashboard, and robustness scripts. It computes bounded parallelism from `HUMANIZE_TEST_JOBS` or CPU count with a default cap of 8 at lines 19-37; runs signal-heavy `test-monitor-runtime.sh` serially via `SERIAL_TESTS` at lines 145-149 and 366-375; supports zsh-only suites via `ZSH_TESTS` at lines 140-143 and 190-199; captures each suite’s output, exit code, and elapsed time at lines 225-240; parses pass/fail counters from ANSI-stripped logs at lines 264-275; then prints skipped suites, sorted results, and a final aggregate pass/fail exit at lines 377-415. Shared shell assertions live in `tests/test-helpers.sh`, which tracks passed, failed, and skipped counts at lines 22-24, exposes `pass`, `fail`, `skip` at lines 30-52, and returns process status from `print_test_summary` at lines 58-78.
- inputs_outputs_state: Inputs are local repo scripts, hooks, commands, prompt templates, TypeScript modules, fixtures under `tests/fixtures/`, and generated temp repos/session directories. Outputs are stdout pass/fail summaries and process exit status. State is intentionally isolated: the runner allocates a temp output directory at lines 151-153, creates mock binaries only inside that directory at lines 155-188, and individual shell suites typically use `mktemp -d` plus traps. TypeScript tests exercise in-memory and temp-backed service state through Vitest. Fixtures model GitHub reactions/comments/reviews and directions JSON contracts.
- gates_or_invariants: Directory-level invariants include no missing listed test file (`run-all-tests.sh` lines 299-302), valid job count (`HUMANIZE_TEST_JOBS` integer >=1, lines 33-37), shell portability across bash 3.2 by avoiding associative arrays and checking `wait -n` support at lines 39-44 and 286-288, zsh availability before zsh suites at lines 310-314, and failure if any suite exits non-zero or reports failed assertions at lines 272-278 and 398-415. Robustness suites gate malformed state files, path traversal, command injection-like shell strings, git edge cases, concurrent state writes, template errors, timeout semantics, and hook input parsing.
- dependencies_and_callers: The test directory coordinates with `hooks/` validators and loop libraries, `scripts/` setup/validation/runtime helpers, `commands/` slash-command markdown contracts, `prompt-template/` message/rendering contracts, `src/` TypeScript hub/workflow/service modules, `viz/` dashboard code, and package scripts. `package.json` exposes `npm test` as `vitest run --config vitest.config.ts` and `typecheck` via `tsc`. Shell suites invoke production helpers directly, such as `hooks/loop-codex-stop-hook.sh`, `hooks/lib/template-loader.sh`, `scripts/portable-timeout.sh`, `scripts/validate-explore-idea-io.sh`, `scripts/bitlesson-validate-delta.sh`, and `scripts/humanize.sh`.
- edge_cases_or_failure_modes: The suite intentionally covers missing Codex by injecting a mock `codex` binary at `run-all-tests.sh` lines 155-166, missing GNU timeout by injecting a Python timeout shim at lines 168-188, unavailable zsh via skips, ANSI-colored summary parsing, deleted monitor directories, SIGINT/SIGTERM cleanup, symlinks, absolute paths, spaces/metacharacters in paths, stale sessions, corrupted state frontmatter, invalid JSON hook payloads, duplicate explore directions, run ID collisions, and worker outputs without sentinel JSON.
- validation_or_tests: Validation is the directory’s purpose. Key children include Vitest suites for `src/hub/agent-sessions.ts`, workflow parser/graph/coordinator/storage/RPC/projection, service/MCP/hub behavior, and config; shell suites for RLCR stop hooks, plan file hooks, BitLesson, explore/gen-idea/gen-plan/refine-plan, dashboard auth/routes/streaming/cancellation, template loading, and command structure; plus `tests/robustness/test-timeout-robustness.sh` for timeout fallback behavior. Directory coordination is explicitly recursive because the runner includes nested `robustness/...` suites at lines 121-137.
- skip_candidate: `no`

### H2_DEV-HZ-041 `file` `docs/usage.md`
- cursor: `[_]`
- core_role: Behavior-defining user documentation for Humanize’s RLCR loop, planning commands, explore worker workflow, configuration precedence, dashboard monitoring, cancellation, and sandbox/security modes.
- algorithmic_behavior: Lines 5-12 define the core two-phase RLCR loop: implementation phase where Claude works on the plan and Codex reviews summaries until COMPLETE, followed by review phase using `codex review --base <branch>` with `[P0-9]` markers. Lines 16-35 define an advisory Plan Understanding Quiz before `start-rlcr-loop`; it generates two multiple-choice questions and can explain misses before the user proceeds or stops. Lines 43-56 define the typical `gen-plan` to `refine-plan` to `start-rlcr-loop` flow. Lines 88-116 define `explore-idea` as bounded parallel prototype workers in isolated worktrees producing `manifest.json`, dispatch prompts, `worker-results.jsonl`, `explore-report.md`, and `final-idea.md`. Lines 118-148 define `start-rlcr-loop` flags and defaults, including max iterations, Codex model/timeout, plan tracking, push-every-round, base branch detection priority, full review cadence, agent teams, yolo, and quiz skipping.
- inputs_outputs_state: Inputs include idea text/draft paths, directions JSON, plan files, annotated plans, CLI options, project/user/default config, and environment variables. Outputs include `.humanize/ideas/<slug>-<timestamp>.md` plus companion `.directions.json` at lines 80-82, explore artifacts under `.humanize/explore/<RUN_ID>/` at lines 105-110, refined plan and QA ledger outputs at lines 264-282, ask-codex artifacts under `.humanize/skill/<timestamp>/` at lines 301-302, and RLCR session progress under `.humanize/rlcr/<timestamp>/` at line 376. State transitions include draft to directions, directions to explore run, final idea to plan, plan to RLCR loop, implementation to review phase, and active loop to canceled/completed state.
- gates_or_invariants: The Plan Understanding Quiz is advisory rather than blocking, stated at lines 33-35. `gen-plan` validates input/output paths and repository relevance before producing a structured plan at lines 168-174. `refine-plan` requires at least one non-empty comment block and ignores markers inside code fences or HTML comments at lines 257-260. `start-rlcr-loop` defines base branch auto-detection priority at lines 131-133 and full-review cadence bounds at lines 134-136. Config precedence is fixed as defaults, user config, project config, then CLI flags at lines 304-310. Remote dashboard binding requires a token for non-loopback hosts and rejects WebSocket transport in remote mode at lines 400-410. Sandbox bypass is documented as dangerous and opt-in via `HUMANIZE_CODEX_BYPASS_SANDBOX=true|1` at lines 459-468.
- dependencies_and_callers: This documentation aligns with `commands/gen-idea.md`, `commands/explore-idea.md`, `commands/start-rlcr-loop.md`, `commands/gen-plan.md`, `commands/refine-plan.md`, `scripts/validate-explore-idea-io.sh`, `scripts/ask-codex.sh`, `scripts/humanize.sh`, config loaders, and the dashboard server. It also documents config keys used by script/module implementations: `codex_model`, `codex_effort`, `bitlesson_model`, `provider_mode`, `agent_teams`, `alternative_plan_language`, and `gen_plan_mode` at lines 312-323.
- edge_cases_or_failure_modes: The guide explicitly warns against “wishful coding” failure before RLCR execution at lines 20-24, supports skip paths for quiz/yolo/auto-start at lines 37-41, documents direct versus tunneled remote dashboard access at lines 412-451, warns against sandbox bypass in untrusted/shared/production contexts at lines 469-483, and notes legacy `loop_reviewer_model` / `loop_reviewer_effort` are ignored at lines 353-355.
- validation_or_tests: Behavior is cross-checked by shell suites for plan generation/refinement, explore validation and command structure, unified Codex config, monitor/dashboard auth and streaming, cancellation, base branch detection, and sandbox/hook behavior. Documentation itself is not a runtime gate, but it defines user-visible defaults and workflows that tests assert through command files and scripts.
- skip_candidate: `no`

### H2_DEV-HZ-071 `file` `tests/agent-sessions.test.ts`
- cursor: `[_]`
- core_role: Vitest executable specification for `buildAgentSessions`, the hub/dashboard folding algorithm that turns raw agent run attempts into user-visible session records with parent linkage, input history, model labels, token usage, and context-window stats.
- algorithmic_behavior: The first test constructs interrupted and continued captain/worker runs at lines 7-77 and expects two folded sessions at lines 79-132. It verifies continuation grouping by `continuedFromRunId`, stable titles (`captain`, `worker-1`), vendor session IDs, current/previous run IDs, final status, duration spanning all attempts, token totals, context windows, and prompt/intervention input history. The second test at lines 134-176 confirms a captain without an intervention remains a single prompt entry while a continued worker records prompt then intervention. The third test at lines 178-216 preserves workflow-origin metadata for dashboard attribution. Later tests verify metadata/default-derived model labels and context windows at lines 218-276, latest context load from streamed Claude usage at lines 278-302, Codex aggregate usage not being used as latest context at lines 304-327, and aggregate result usage overriding totals while message usage supplies latest context at lines 329-353.
- inputs_outputs_state: Inputs are arrays of synthetic `AgentRunRecord` objects produced by the helper at lines 356-380, with fields such as `id`, `shortName`, `parentRunId`, `continuedFromRunId`, `interventionMessage`, `messageOrigin`, `agent`, `model`, `reasoningEffort`, timestamps, backend session IDs, and JSON output events. Outputs are `AgentSessionRecord[]` from `buildAgentSessions`. State transitions under test are raw run attempts to root continuation groups, interrupted to succeeded continuation chains, parent run ID to parent session ID mapping, output event streams to token stats, and prompt/intervention fields to input history.
- gates_or_invariants: The tests require grouped sessions to sort by `startedAt` and derive titles from worker/captain naming rules. They assert context window inference: `gpt-5` maps to 400,000 tokens at line 102, `claude-opus-4-7` maps to 1,000,000 at lines 127-129, and `gpt-5.5` metadata maps to 258,400 at lines 256-270. They require duplicated Claude message usage with the same message ID not to double count, shown by identical `msg-a` events at lines 337-339 and expected aggregate totals at lines 344-352. They also require Codex aggregate `turn.completed` usage to produce totals but not `contextUsedTokens`, asserted at lines 317-326.
- dependencies_and_callers: The test imports `buildAgentSessions` from `src/hub/agent-sessions.ts` and `AgentRunRecord` from `src/hub/runs.ts` at lines 3-4. The implementation groups runs by `continuedFromRunId` roots at `src/hub/agent-sessions.ts` lines 79-101 and 173-191, builds sessions at lines 103-171, generates input history at lines 202-224, derives titles at lines 226-239, parses usage JSON at lines 241-353, resolves model metadata at lines 387-413, and infers context windows at lines 505-530.
- edge_cases_or_failure_modes: Covered cases include worker parent pointing to a later captain continuation (`parentRunId: captain-b` while the root session is `captain-a`), intervention prompt duplication avoided when `prompt === interventionMessage` by implementation lines 202-214, workflow-origin metadata preservation, metadata overriding stream/run defaults, streamed assistant usage deduplication by message ID, non-JSON stderr/stdout ignored by parsing, and Codex aggregate usage not being misrepresented as live context load.
- validation_or_tests: The file is itself the focused validation. It uses deterministic timestamps and explicit expected objects so regressions in folding, attribution, token accounting, context-window inference, or model label normalization fail directly under Vitest.
- skip_candidate: `no`

### H2_DEV-HZ-101 `file` `tests/test-disable-nested-codex-hooks.sh`
- cursor: `[_]`
- core_role: Shell executable specification ensuring Humanize’s nested Codex calls from the RLCR stop hook disable native Codex hooks, avoiding recursive hook execution, and ensuring review-phase Codex invocation respects CLI compatibility around `--base`.
- algorithmic_behavior: The script creates isolated repos with `.humanize/rlcr/<timestamp>` loop state and summaries. `setup_repo` initializes a git repo and tracked baseline at lines 42-64. `setup_mock_codex` writes a mock `codex` binary at lines 66-128; its `--help` advertises `--disable`, its `exec` path records args and exits with a review-like response, and its `review` path fails if `--base` is combined with prompt stdin. `setup_loop_dir` writes `state.md`, copies the plan, creates `goal-tracker.md`, writes `round-1-summary.md`, and optionally marks review phase with `.review-phase-started` at lines 130-183. `run_loop_hook` invokes `hooks/loop-codex-stop-hook.sh` with mocked `PATH` and `CLAUDE_PROJECT_DIR` at lines 185-203.
- inputs_outputs_state: Inputs are mock git repos, mock Codex CLI args, loop state frontmatter fields (`current_round`, `max_iterations`, `plan_file`, `base_branch`, `codex_model`, `codex_effort`, `review_started`, etc.) at lines 141-159, and hook stdin `{}`. Outputs are captured hook output, exit code, and argument files (`impl.args`, `review.args`) that record nested Codex invocations. State transition under test is implementation mode (`review_started=false`) to `codex exec`, and review mode (`review_started=true` plus `.review-phase-started`) to `codex review`.
- gates_or_invariants: Implementation phase must call `codex exec --disable hooks`, asserted at lines 209-214. Review phase must call `codex review --disable hooks`, asserted at lines 220-225. The stop hook must capture `codex --help` before grepping rather than using a fragile pipeline, asserted by absence of a direct `codex --help 2>&1 | grep -q` pattern at lines 227-233. Review phase must use `--base` without trailing `-` prompt input and without stdin, asserted at lines 235-240. The generated review audit prompt must document the `--base` plus prompt incompatibility, asserted at lines 242-250.
- dependencies_and_callers: Direct dependency is `hooks/loop-codex-stop-hook.sh` via `STOP_HOOK` at line 40. The mock depends on `git`, shell, and hook-generated review prompt files. The test complements `tests/test-ask-codex.sh` nested hook disable checks and broader stop-hook compatibility tests. It also indirectly pins Codex feature probing behavior in the stop hook.
- edge_cases_or_failure_modes: It simulates a Codex version that rejects `codex review --base -` with stdin, failing with exit 64/65 in the mock at lines 101-119. It verifies both implementation and review paths, and catches recursion risk if `--disable hooks` is omitted. It also catches a previous shell-pipeline fragility where `grep` in a pipeline could behave incorrectly under strict shell settings.
- validation_or_tests: The script accumulates pass/fail counts and exits 1 if any assertion fails at lines 253-262. It is intended for shell execution and is included in the aggregate runner at `tests/run-all-tests.sh` line 90.
- skip_candidate: `no`

### H2_DEV-HZ-131 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Broad shell validation suite for prompt-template loading, syntax, rendering semantics, fallback behavior, real-template integration, and stress behavior. It is a core contract for all hook/command prompt blocks that drive state-machine gates and user-facing block messages.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh` at lines 15-17 and computes `TEMPLATE_DIR` at line 59. Section 1 validates template directory existence and required subdirectories `block`, `codex`, and `claude` at lines 64-80. Section 2 recursively loads every `*.md` template via `find "$TEMPLATE_DIR" -name "*.md"` at lines 90-107. Section 3 scans every template for malformed placeholder syntax: extra closing braces, extra opening braces, wrong bracket types, potentially unclosed placeholders, spaces inside placeholders, and lowercase placeholders at lines 118-194. Section 3.5 self-tests the regex detectors against intentionally malformed patterns at lines 199-259. Section 4 validates rendering edge cases for empty content, single and multiple lines, empty-line preservation, regex characters, backslashes, quotes, CJK-like content, markdown, long values, multiline values, start/end placeholders, repeated placeholders, and adjacent variables at lines 261-439. Section 5 validates missing-template fallback, real-template preference, and directory validation at lines 440-490. Section 6 renders real templates (`wrong-round-number`, `unpushed-commits`, `goal-tracker-update-section`) with required variables at lines 492-540. Section 7 stress-tests 100 rapid renders and load/render of every template with dummy placeholder values at lines 542-606.
- inputs_outputs_state: Inputs are the full `prompt-template/` tree, `hooks/lib/template-loader.sh`, and synthetic template strings. Outputs are pass/fail/warn counters and exit status. Rendering state is single-pass substitution: placeholders provided as `VAR=value` arguments become `TMPL_VAR_*` environment variables in the loader, while missing placeholders remain literal. The suite tracks `TESTS_PASSED`, `TESTS_FAILED`, and `WARNINGS` at lines 26-29 and exits non-zero if failures exist at lines 619-625.
- gates_or_invariants: Placeholders must be uppercase `{{VAR_NAME}}` without spaces or extra braces. Every template file must load non-empty content unless whitespace-only warning is acceptable. `load_and_render_safe` must use fallback for missing templates and the real template when present. `validate_template_dir` must reject non-existent and incomplete directories. Rendering must preserve empty lines and support shell/regex/metacharacter-heavy values without crashing. By checking remaining placeholders after dummy substitution at lines 590-597, it warns if a template still contains unresolved uppercase placeholders.
- dependencies_and_callers: The suite depends on `hooks/lib/template-loader.sh`. The loader defines `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `append_template`, `load_and_render_safe`, and `validate_template_dir` at lines 24-238. Important implementation details are single-pass awk replacement at `template-loader.sh` lines 56-135, fallback emission at lines 174-211, and directory validation requiring `block`, `codex`, `claude`, and `plan` at lines 213-238. The test’s real-template examples connect to hook block messages and Codex prompt sections.
- edge_cases_or_failure_modes: Detects malformed template syntax that could leak into prompts; missing templates; empty templates; invalid template directories; unsafe substitution behavior with regex/shell characters; multiline value handling; repeated or adjacent placeholders; high-volume repeated render calls; and possible unresolved placeholders across the entire template tree. One notable invariant mismatch is that the test’s early subdir loop checks `block codex claude` at lines 74-80, while `validate_template_dir` requires `plan` too in the loader at line 224; the later `validate_template_dir` call still exercises the stricter implementation.
- validation_or_tests: This file is itself the CI/CD comprehensive template validation suite and is included by `tests/run-all-tests.sh` at line 68. It also complements narrower `tests/test-template-loader.sh`, `tests/test-template-references.sh`, and robustness template-error/stress suites.
- skip_candidate: `no`

### H2_DEV-HZ-161 `file` `prompt-template/block/bitlesson-delta-missing-notes.md`
- cursor: `[_]`
- core_role: User-facing block template for a BitLesson Delta validation gate: `Action: add` or `Action: update` cannot pass unless the round summary includes a meaningful `Notes:` field explaining what changed and why.
- algorithmic_behavior: The template contains a title at line 1, renders the violating action through `{{ACTION}}` at line 3, and states the concrete invariant at line 5: notes must not be empty or placeholder text. It is loaded by `scripts/bitlesson-validate-delta.sh` when the extracted BitLesson Delta action is non-`none` and notes are missing, whitespace-only, or match placeholder syntax.
- inputs_outputs_state: Input is the `ACTION` placeholder, supplied from `BITLESSON_ACTION` in `scripts/bitlesson-validate-delta.sh` line 295. Output is a rendered markdown reason embedded into the hook JSON block response. The state transition is summary validation from “candidate exit allowed” to “blocked” when `BITLESSON_NOTES` fails the required notes gate.
- gates_or_invariants: The validator extracts notes at `scripts/bitlesson-validate-delta.sh` lines 281-282, defines placeholder text as an entire bracketed or angle-bracketed value via `NOTES_PLACEHOLDER_REGEX='^(\[.*\]|<.*>)$'` at line 283, and blocks if notes are empty or placeholder-like at lines 285-297. This template is the preferred message; a same-content fallback is embedded at lines 286-292 if the template cannot load.
- dependencies_and_callers: Primary caller is `scripts/bitlesson-validate-delta.sh` via `load_and_render_safe "$TEMPLATE_DIR" "block/bitlesson-delta-missing-notes.md"` at lines 294-295. That script is invoked by `hooks/loop-codex-stop-hook.sh` with `--allow-empty-none` and template dir parameters, referenced at `hooks/loop-codex-stop-hook.sh` line 873. The template uses `hooks/lib/template-loader.sh` rendering semantics through the validator.
- edge_cases_or_failure_modes: Blocks whitespace-only notes, square-bracket placeholder notes such as `[what changed and why]`, and angle-bracket placeholder notes such as `<what changed and why>`. It does not by itself validate Lesson ID existence or action consistency; those gates are handled by adjacent BitLesson templates and validator branches. If the template is missing or empty, `load_and_render_safe` emits the fallback, so the validation remains fail-closed.
- validation_or_tests: `tests/test-bitlesson-validate-delta.sh` asserts add with whitespace-only notes blocks at lines 101-104, update with square-bracket placeholder blocks at lines 106-109, update with angle-bracket placeholder blocks at lines 111-114, and valid explanatory notes pass at lines 144-152. `tests/test-templates-comprehensive.sh` also loads and syntax-validates all templates recursively.
- skip_candidate: `no`

### H2_DEV-HZ-191 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Block-message template enforcing the RLCR invariant that agents must not create or access legacy `round-*-todos.md` files and must use native task tools instead.
- algorithmic_behavior: Lines 1-5 declare the block and replacement action: do not create/access `round-*-todos.md`; use `TaskCreate`, `TaskUpdate`, and `TaskList`. Lines 7-8 explain the state-management rationale: native task tools provide UI-visible tracking and integration with Claude Code task management. The template is returned by the common hook message function when read/write/bash/edit validators detect disallowed todos-file access.
- inputs_outputs_state: It has no placeholders. Input is the triggering attempted action/path in caller logic, though the `action` parameter to `todos_blocked_message` is currently not interpolated. Output is a stable markdown block reason used in hook JSON decisions. State transition is attempted file access to blocked hook decision, preserving task state in native task APIs rather than filesystem todo snapshots.
- gates_or_invariants: `hooks/lib/loop-common.sh` defines `todos_blocked_message` at lines 844-853, with this template loaded through `load_and_render_safe` at line 852 and a fallback at lines 848-850. The surrounding helper recognizes round file names, including `todos`, via `extract_round_number` at lines 804-814 and allowlists only specific early files in `is_allowlisted_file` at lines 816-842. The template encodes the invariant that legacy todos files are not a valid state surface.
- dependencies_and_callers: Called through shared hook library `hooks/lib/loop-common.sh`, then consumed by loop read/write/edit/bash validators. `tests/test-template-references.sh` treats `block/todos-file-access.md` as a common template that must exist at lines 149-167. It also depends on `hooks/lib/template-loader.sh` for fallback-safe rendering.
- edge_cases_or_failure_modes: If the template is absent, the fallback still blocks legacy todos access. The message is path-pattern oriented and does not decide allowlisting itself; validator logic must correctly detect `round-*-todos.md` and avoid blocking unrelated files. The template explicitly covers both create and access, so it applies to reads as well as writes.
- validation_or_tests: `tests/test-template-references.sh` checks the common template exists at lines 152-167. `tests/test-templates-comprehensive.sh` validates syntax/load/render for all templates. Related allowlist behavior is exercised by `tests/test-allowlist-validators.sh`, and todo/task state behavior by `tests/test-todo-checker.sh`.
- skip_candidate: `no`

### H2_DEV-HZ-221 `file` `prompt-template/explore/worker-prompt.md`
- cursor: `[_]`
- core_role: Core prompt contract for `/humanize:explore-idea` prototype workers. It defines worker isolation, trusted versus untrusted inputs, base-commit anchoring, bounded implementation/review iterations, Codex review metadata verification, commit behavior, and the machine-readable result schema consumed by the coordinator.
- algorithmic_behavior: Run context placeholders are declared at lines 6-15. Hard constraints at lines 16-27 require staying in the assigned worktree, no nested skills/slash commands, no subagents, no git push, no sibling worktree access, use only `ask-codex.sh`, scope Codex with `CLAUDE_PROJECT_DIR="$PWD"`, fail closed on review metadata mismatch, and emit the result sentinel last. Lines 28-65 isolate direction data as untrusted input in fenced text blocks. Setup at lines 69-90 verifies the worktree root, asserts `HEAD == <BASE_COMMIT>`, creates `explore/<RUN_ID>/<DIR_SLUG>`, exports `CLAUDE_PROJECT_DIR`, and fails immediately on coordinator checkout mismatch. Per-iteration behavior at lines 92-115 reads relevant files, implements minimally, runs targeted tests only, calls `ask-codex.sh` with `--codex-model "<CODEX_REVIEW_MODEL_SPEC>"`, reads metadata, and applies feedback until LGTM or iteration cap. Commit behavior is defined at lines 117-126. Result emission with sentinel markers and JSON schema is defined at lines 128-160, with enum guidance at lines 162-182.
- inputs_outputs_state: Inputs are rendered placeholders: run/direction IDs, slug, direction name/rationale/approach/evidence/risks/confidence/original idea, max iterations, Codex timeout/model spec, base branch, and base commit. Runtime inputs are repository files in the isolated worktree and Codex review metadata generated under `.humanize/skill/<unique-id>/metadata.md`. Outputs are optional local commit(s) on `explore/<RUN_ID>/<DIR_SLUG>` and a final JSON object between `EXPLORE_RESULT_JSON_BEGIN` and `EXPLORE_RESULT_JSON_END`. State transitions are pending worker to setup-validated, branch-created, iteration N implementation/test/review, optional feedback loop, committed/none/wip/failed commit status, and final emitted result.
- gates_or_invariants: Critical invariants include no filesystem access outside the assigned worktree at lines 18 and 22, no nested automation at lines 19-20, no remote push at line 21, no direct Codex CLI at line 23, Codex scoped to current worktree at lines 24 and 86-90, base commit equality before branch creation at lines 71-85, targeted tests rather than full suite at lines 98-103, and review metadata must prove model `gpt-5.5` and effort `xhigh` for the expected model spec at lines 25 and 112-114. The final action must be printing sentinel JSON with nothing afterward, lines 26 and 130.
- dependencies_and_callers: Used by `/humanize:explore-idea`. `commands/explore-idea.md` states worker constraints must stay in sync with `WORKER_PROMPT_TEMPLATE` at lines 37-40, substitutes every placeholder at lines 137-160, dispatches workers with `isolation: "worktree"` at lines 201-217, and parses the sentinel JSON at lines 228-245. `scripts/validate-explore-idea-io.sh` locates this template at lines 248-255, validates it exists, enforces the base-anchor contract comments at lines 367-377, and emits `CODEX_REVIEW_MODEL_SPEC: gpt-5.5:xhigh` at lines 449-477.
- edge_cases_or_failure_modes: Explicit failure cases include base commit mismatch, coordinator checkout root mismatch, missing/unreadable/wrong Codex metadata, Codex unavailable, Codex not reached, no changes to commit, dirty state remaining, commit failure, and unrecoverable errors before loop completion. Direction data is marked untrusted so conflicting instructions inside idea fields must not override hard constraints. The JSON schema includes `task_status`, `codex_final_verdict`, `dirty_state`, `commit_status`, `error`, and arrays for `what_worked` / `what_didnt` so partial and failed prototypes remain collectible.
- validation_or_tests: `tests/test-worker-result-contract.sh` verifies the file exists, contains sentinel markers in order, contains all required placeholders, contains required JSON fields, has hard constraints before untrusted direction data, avoids untrusted `<DIRECTION_NAME>` inside bash fences, forbids nested skills and git push, requires `CLAUDE_PROJECT_DIR`, uses `--codex-model "<CODEX_REVIEW_MODEL_SPEC>"`, and enforces branch naming. `tests/test-explore-command-structure.sh` verifies the command side of dispatch, result collection, and two-tier report structure. `tests/test-validate-explore-idea-io.sh` validates template presence and emitted model-spec fields.
- skip_candidate: `no`

### H2_DEV-HZ-251 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable specification for the portable timeout abstraction used by Humanize scripts across macOS/Linux, ensuring timeouts have stable detection, execution, exit codes, fallback behavior, and argument handling.
- algorithmic_behavior: The script sources `scripts/portable-timeout.sh` and `tests/test-helpers.sh` at lines 14-17. Positive tests at lines 31-110 verify timeout implementation detection, valid implementation enum, quick command completion, timed-out command exit code 124, argument passing, preservation of non-timeout command exit code 42, and pipeline support through `sh -c`. Negative/edge tests at lines 120-272 verify short timeout elapsed bounds, zero timeout behavior, large output, rapid cycles, nonexistent commands, empty commands, special characters, signal-handling commands, fallback chain detection, subshell command execution, and `TIMEOUT_IMPL` availability in subshells.
- inputs_outputs_state: Inputs are the detected timeout implementation from `portable-timeout.sh`, command/argument arrays passed to `run_with_timeout`, and environmental command availability (`gtimeout`, GNU `timeout`, `python3`, `python`, or none). Outputs are command stdout, command exit status, elapsed time measurements, and pass/fail counters. State transition is from environment probing to `TIMEOUT_IMPL`, then per-call dispatch to the selected timeout backend.
- gates_or_invariants: `TIMEOUT_IMPL` must be one of `gtimeout`, `timeout`, `python3`, `python`, or `none`, asserted at lines 42-49 and 239-247. A timed-out command must return 124 when an implementation exists, asserted at lines 62-77. Successful commands must preserve stdout and exit status, lines 51-60 and 89-100. Short timeouts must stop well before the long sleep completes, lines 120-134. Special characters in arguments must remain literal, lines 211-219. The implementation must export or at least expose `TIMEOUT_IMPL`, lines 259-272.
- dependencies_and_callers: Depends directly on `scripts/portable-timeout.sh`. That script selects implementations in priority order `gtimeout > timeout > python3 > python > none` at lines 9-32, assigns `TIMEOUT_IMPL` at line 34, dispatches `run_with_timeout` through the selected backend at lines 38-76, maps Python `subprocess.TimeoutExpired` to 124 at lines 52-67, and exports `TIMEOUT_IMPL` at line 81. The robustness test uses shared pass/fail summary helpers from `tests/test-helpers.sh`.
- edge_cases_or_failure_modes: Covers no native timeout implementation, non-GNU timeout shims rejected by `timeout --version` pattern in `portable-timeout.sh` lines 15-21, commands ignoring or trapping SIGTERM, zero timeout variations, empty command strings, large stdout, rapid repeated invocations, nonexistent commands, subshells, pipelines, and shell metacharacters in arguments. If `TIMEOUT_IMPL=none`, timeout-specific assertions are skipped as passes rather than requiring impossible behavior.
- validation_or_tests: The file is itself the timeout robustness validation and exits with `print_test_summary` status at lines 274-279. It is included in the aggregate runner at `tests/run-all-tests.sh` line 131.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `9 unique item evidence sections; each assigned item_id appears once as a section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`