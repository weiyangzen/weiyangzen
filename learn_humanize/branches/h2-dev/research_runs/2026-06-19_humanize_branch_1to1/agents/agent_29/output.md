# agent_29 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-029 `file` `agents/draft-relevance-checker.md`
- cursor: `[_]`
- core_role: Subagent prompt/policy used by the gen-plan flow to classify whether a draft belongs to the current repository before planning proceeds. Frontmatter declares `name: draft-relevance-checker`, `model: haiku`, and tools `Read, Glob, Grep` at `agents/draft-relevance-checker.md:1-5`.
- algorithmic_behavior: The prompt tells the agent to quickly inspect repository docs/structure, compare draft semantics against repo concepts/files/features, then return exactly one verdict prefix: `RELEVANT:` or `NOT_RELEVANT:` with a brief explanation (`agents/draft-relevance-checker.md:14-29`). Its policy biases toward recall over precision: informal/multilingual drafts are acceptable, semantic relevance matters more than syntax, and uncertainty should be marked relevant (`agents/draft-relevance-checker.md:31-36`).
- inputs_outputs_state: Input is draft document content plus repository filesystem access through read/search tools. Output is a single textual verdict. No persistent state is owned by this file.
- gates_or_invariants: Verdict prefix is a contract surface (`RELEVANT:`/`NOT_RELEVANT:`); lenient interpretation is an invariant so marginally related drafts are not prematurely rejected (`agents/draft-relevance-checker.md:27-36`).
- dependencies_and_callers: `commands/gen-plan.md:154-169` invokes `humanize:draft-relevance-checker` after IO validation and explicitly repeats the haiku model and verdict contract. `tests/test-gen-plan.sh` validates the file exists, has the correct name/model/tools/frontmatter, valid naming, and English-only content.
- edge_cases_or_failure_modes: Ambiguous drafts should be accepted as relevant; unrelated content such as recipes is the example negative case (`agents/draft-relevance-checker.md:33-46`). Misclassification risk is asymmetric by design: false positives are tolerated more than false negatives.
- validation_or_tests: `tests/test-gen-plan.sh` covers presence, frontmatter, model `haiku`, tools field, naming convention, required fields, valid model, and content language. This assigned file is prompt-policy rather than executable logic.
- skip_candidate: `no`

### H2_DEV-HZ-059 `file` `scripts/nested-market-smoke.ts`
- cursor: `[_]`
- core_role: End-to-end smoke script for Humanize2 hub nested run routing: a parent agent run must use JSON-RPC to spawn a logical child run, and both must produce market-data artifacts. It is wired as `npm run smoke:nested-market`, which first builds then runs this script (`package.json:18-20`).
- algorithmic_behavior: Defines four parent/child agent combinations (`codex/codex`, `codex/claude`, `claude/codex`, `claude/claude`) at `scripts/nested-market-smoke.ts:19-24`; selects all or one named combination from CLI input (`scripts/nested-market-smoke.ts:37-49`); for each, writes a child JSON-RPC request file, constructs a shell-quoted `humanize2-rpc run.spawn_child @request.json` command, creates the parent run through `run.create`, waits for parent success, polls `run.list` until a child with matching `parentRunId` and agent appears, waits for child success, and validates output files contain expected markers (`scripts/nested-market-smoke.ts:51-115`).
- inputs_outputs_state: Inputs are optional CLI selection (`all` default), `HUMANIZE2_JSONRPC_URL` defaulting to `http://127.0.0.1:4772/jsonrpc`, built `dist/hub-client.js`, and a running hub (`scripts/nested-market-smoke.ts:14-18`, `scripts/nested-market-smoke.ts:147-153`). Outputs are temp markdown files and child request JSON under repo `temp/`, plus console success lines (`scripts/nested-market-smoke.ts:53-56`, `scripts/nested-market-smoke.ts:58-73`, `scripts/nested-market-smoke.ts:111-114`).
- gates_or_invariants: Hub availability is checked first via `system.info` (`scripts/nested-market-smoke.ts:26-34`, `scripts/nested-market-smoke.ts:147-153`). Invalid combination names hard-fail with usage (`scripts/nested-market-smoke.ts:47-49`). Parent and child statuses must be `succeeded` or errors include status payloads (`scripts/nested-market-smoke.ts:101-109`). Output files must be non-empty and contain `BTC`/`ETH` for parent, `NVDA`/`GOOG` for child (`scripts/nested-market-smoke.ts:111-140`).
- dependencies_and_callers: Imports `callHubRpc` from `src/hub-client.js` (`scripts/nested-market-smoke.ts:5`). Depends on hub JSON-RPC methods `system.info`, `run.create`, `run.wait`, `run.list`, and on the generated `dist/hub-client.js` binary path. Uses permissive agent options for real smoke execution: Codex gets `sandbox: danger-full-access` and `--skip-git-repo-check`; Claude gets `permissionMode: bypassPermissions` (`scripts/nested-market-smoke.ts:117-128`).
- edge_cases_or_failure_modes: Fails if hub is absent, build artifact missing, agent runners unavailable, network market-data fetch unavailable, parent does not spawn a logical child, child agent mismatch, child discovery times out after 30 minutes, output files are absent/empty, or marker strings are omitted (`scripts/nested-market-smoke.ts:155-185`). `shellQuote` escapes single quotes for safe command embedding (`scripts/nested-market-smoke.ts:187-189`).
- validation_or_tests: This file is itself a smoke validation workflow. It tests actual nested routing behavior rather than mocked unit behavior and is invoked via `npm run smoke:nested-market`.
- skip_candidate: `no`

### H2_DEV-HZ-089 `file` `tests/test-ask-codex.sh`
- cursor: `[_]`
- core_role: Executable shell specification for `scripts/ask-codex.sh`, the one-shot Codex consultation path. It replaces the real `codex` binary with mocks, so it validates wrapper behavior without external Codex calls (`tests/test-ask-codex.sh:1-18`, `tests/test-ask-codex.sh:35-53`).
- algorithmic_behavior: Creates a temporary git project, injects mock Codex binaries, runs `ask-codex.sh` under controlled `CLAUDE_PROJECT_DIR`, `XDG_CACHE_HOME`, and `PATH`, then asserts validation, storage, metadata, cache, argument parsing, concurrency uniqueness, skill guidance, and nested-hook auto-probe behavior (`tests/test-ask-codex.sh:29-119`).
- inputs_outputs_state: Inputs are CLI flags/questions, mock env vars `MOCK_CODEX_EXIT_CODE`, `MOCK_CODEX_STDOUT`, `MOCK_CODEX_STDERR`, and cache writability (`tests/test-ask-codex.sh:5-9`, `tests/test-ask-codex.sh:55-71`). Expected outputs include stdout response, project-local `.humanize/skill/<id>/input.md`, `output.md`, `metadata.md`, cache files `codex-run.cmd` and `codex-run.out`, and `.codex-disable-hooks-supported` probe cache (`tests/test-ask-codex.sh:218-243`, `tests/test-ask-codex.sh:448-470`, `tests/test-ask-codex.sh:548-604`).
- gates_or_invariants: Empty question, unknown option, missing flag args, non-numeric timeout, and unsafe model/effort characters must exit 1 with expected diagnostics (`tests/test-ask-codex.sh:128-198`). Successful calls must echo Codex stdout, persist artifacts, and exit 0 (`tests/test-ask-codex.sh:208-254`). Nonzero Codex exit propagates; empty stdout maps to `empty_response`; timeout exit 124 maps to timeout status and diagnostic (`tests/test-ask-codex.sh:264-320`).
- dependencies_and_callers: Sources `tests/test-helpers.sh`; targets `scripts/ask-codex.sh` and `skills/ask-codex/SKILL.md` (`tests/test-ask-codex.sh:14-18`). `tests/run-all-tests.sh:103-104` includes it in the suite. Production behavior under test includes option parsing and safety regexes in `scripts/ask-codex.sh:89-186`, unique storage/cache creation in `scripts/ask-codex.sh:202-218`, command construction and hook disable probing in `scripts/ask-codex.sh:244-274`, and result metadata handling in `scripts/ask-codex.sh:327-420`.
- edge_cases_or_failure_modes: Covers concurrent rapid calls producing distinct project and cache directories (`tests/test-ask-codex.sh:330-364`), `--` separator preserving flag-like question text (`tests/test-ask-codex.sh:398-407`), readonly home cache fallback (`tests/test-ask-codex.sh:420-438`), large/noisy `codex --help` output during `--disable` probing (`tests/test-ask-codex.sh:526-560`), and CLIs without `--disable` support (`tests/test-ask-codex.sh:561-604`).
- validation_or_tests: This file is a self-contained validation script and ends through `print_test_summary` (`tests/test-ask-codex.sh:614-618`). It also validates skill documentation against unsafe bare `$ARGUMENTS` expansion (`tests/test-ask-codex.sh:481-500`).
- skip_candidate: `no`

### H2_DEV-HZ-119 `file` `tests/test-rlcr-sources.sh`
- cursor: `[_]`
- core_role: Executable parity and behavior test for `viz/server/rlcr_sources.py`, which supplies RLCR dashboard/session/log source discovery. The header states coverage for path sanitization parity, session enumeration/partitioning, live log discovery, and deterministic ordering (`tests/test-rlcr-sources.sh:1-16`).
- algorithmic_behavior: Uses shell and Python side by side: `_shell_sanitize` implements the exact `sed` pipeline from `scripts/humanize.sh`, `_py_sanitize` imports `sanitize_project_path`, then compares representative paths (`tests/test-rlcr-sources.sh:39-86`). It builds a temporary `.humanize/rlcr` tree, seeds active/historical/unknown session states, imports `enumerate_sessions` and `partition_sessions`, and asserts classification/order (`tests/test-rlcr-sources.sh:97-207`). It seeds cache logs and asserts `live_log_paths` filters and sorts only valid round logs (`tests/test-rlcr-sources.sh:209-259`). It asserts `cache_dir_for_session` respects `XDG_CACHE_HOME` plus sanitization (`tests/test-rlcr-sources.sh:261-280`).
- inputs_outputs_state: Inputs are a temp fixture tree, `XDG_CACHE_HOME`, and imported Python module functions. Outputs are pass/fail counters and process exit 1 if any failures occur (`tests/test-rlcr-sources.sh:33-37`, `tests/test-rlcr-sources.sh:282-292`). Fixture state is removed by trap (`tests/test-rlcr-sources.sh:101-102`).
- gates_or_invariants: Sanitization must match the shell pipeline for spaces, slashes, tildes, unicode, repeated special chars, and empty string (`tests/test-rlcr-sources.sh:64-95`). Session listing must be chronological and skip non-directory files (`tests/test-rlcr-sources.sh:118-138`). `methodology-analysis-state.md` and `finalize-state.md` are active; terminal `complete/cancel/maxiter` states are historical (`tests/test-rlcr-sources.sh:158-191`). Missing RLCR/cache dirs must return `[]` instead of raising (`tests/test-rlcr-sources.sh:196-207`, `tests/test-rlcr-sources.sh:248-259`).
- dependencies_and_callers: Targets `viz/server/rlcr_sources.py`; `viz/server/rlcr_sources.py:58-70` mirrors sanitization, `viz/server/rlcr_sources.py:87-96` builds cache paths, `viz/server/rlcr_sources.py:137-183` enumerates and partitions sessions, and `viz/server/rlcr_sources.py:190-215` filters logs. `tests/run-all-tests.sh:119-120` includes it in the suite.
- edge_cases_or_failure_modes: Handles unavailable Python by skipping with exit 0 (`tests/test-rlcr-sources.sh:28-31`). It explicitly covers startup races/missing dirs, partial unknown sessions, unicode paths collapsing through sanitization, invalid log names, and nonnumeric round IDs.
- validation_or_tests: The file is the validation surface; it has four named groups and a hard summary gate (`tests/test-rlcr-sources.sh:64-292`).
- skip_candidate: `no`

### H2_DEV-HZ-149 `file` `tests/workflow-restart.test.ts`
- cursor: `[_]`
- core_role: Vitest specification for workflow scheduler restart recovery. It proves a workflow blocked on an `h2-await` predicate can be persisted, restored into a new `WorkflowCoordinator`, receive the awaited artifact, and complete (`tests/workflow-restart.test.ts:19-67`).
- algorithmic_behavior: Creates a temp `FileWorkflowStore`, starts a coordinator with deterministic IDs/time, loads inline workflow HTML containing `<h2-await id="done-wait" on="exists(artifact.done)">`, starts the workflow, waits until the run reports a predicate wait, loads the persisted snapshot, constructs a second coordinator using the same store, calls `restoreRun`, delivers artifact `done`, waits for terminal success, and asserts a `workflow.restored` event exists (`tests/workflow-restart.test.ts:20-67`).
- inputs_outputs_state: Inputs are inline workflow HTML, deterministic ID factories (`workflow-run`, then `artifact-done`), fixed clock, fake Codex/Claude backends, and temporary filesystem storage (`tests/workflow-restart.test.ts:21-38`, `tests/workflow-restart.test.ts:70-127`). State outputs include persisted run/snapshot records in `FileWorkflowStore`; the test asserts snapshot is defined and `inflight` contains `done-wait` (`tests/workflow-restart.test.ts:42-46`).
- gates_or_invariants: Before restart, the run must reach `waitingFor.kind === "predicate"` (`tests/workflow-restart.test.ts:40`). Snapshot frontier/inflight must be restorable. After restart, artifact delivery must unblock the predicate and the run status must become `succeeded` (`tests/workflow-restart.test.ts:59-66`). Production restore re-adds await/human vertices to the frontier and removes them from completed during restore (`src/workflows/coordinator.ts:1305-1419`).
- dependencies_and_callers: Imports `AgentRunCoordinator`, `HumanizeService`, `WorkflowCoordinator`, and `FileWorkflowStore` (`tests/workflow-restart.test.ts:7-11`). Persistence behavior depends on `FileWorkflowStore.recordSnapshot/loadSnapshot` (`src/workflows/storage.ts:56-80`) and `WorkflowCoordinator.persistSnapshot/restoreRun` (`src/workflows/coordinator.ts:1274-1419`).
- edge_cases_or_failure_modes: The fixture targets an in-flight await predicate specifically; agent-vertex restart has separate failure paths if persisted run IDs are absent or missing (`src/workflows/coordinator.ts:1373-1400`). The test timeout helper fails after 1 second if waiting state is not reached (`tests/workflow-restart.test.ts:129-137`).
- validation_or_tests: This is a Vitest test and is covered by the repo `npm test` script (`package.json:20`). It uses fake backends so no external agent call is required (`tests/workflow-restart.test.ts:76-111`).
- skip_candidate: `no`

### H2_DEV-HZ-179 `file` `prompt-template/block/mainline-drift-stop.md`
- cursor: `[_]`
- core_role: Prompt/block template for the RLCR mainline-drift circuit breaker. It defines the stop message when implementation fails to advance the mainline for a configured number of rounds (`prompt-template/block/mainline-drift-stop.md:1-14`).
- algorithmic_behavior: Renders placeholders for `STALL_COUNT`, `LAST_VERDICT`, and `PLAN_FILE`; sets `Drift status: replan_required`; instructs that the loop should not continue automatically and that the next action is to re-read the original plan, diagnose stalling/regression, and restart with a narrower recovered mainline objective (`prompt-template/block/mainline-drift-stop.md:3-14`).
- inputs_outputs_state: Inputs are template variables supplied by the hook. Output is a rendered block used as the JSON `reason` in a blocking stop response. It also corresponds to state fields updated by the stop function.
- gates_or_invariants: The invariant is a hard stop, not a next-round continuation: “This loop should not continue automatically” (`prompt-template/block/mainline-drift-stop.md:9`). Drift status must be `replan_required` (`prompt-template/block/mainline-drift-stop.md:7`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh:1433-1468` implements `stop_for_mainline_drift`, updates mainline stall/verdict/drift fields, renders this template through `load_and_render_safe`, calls `end_loop`, emits `decision: block`, and exits 0. A fallback message is embedded at `hooks/loop-codex-stop-hook.sh:1444-1451`.
- edge_cases_or_failure_modes: If template loading fails, the hook fallback still stops the loop. If variables are absent or stale, the stop reason may be less useful, but the control-flow gate remains a block because the hook exits after `end_loop`.
- validation_or_tests: Covered indirectly by hook/template robustness tests, not a standalone test in this assigned set. Its behavior is validated through the hook path that renders `block/mainline-drift-stop.md`.
- skip_candidate: `no`

### H2_DEV-HZ-209 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Prompt template that drives the next Claude implementation round after Codex review in an RLCR loop. It re-anchors the round to the original plan, current tracker, recent reviews, and a newly written round contract (`prompt-template/claude/next-round-prompt.md:1-28`).
- algorithmic_behavior: Requires the agent to read `PLAN_FILE`, `GOAL_TRACKER_FILE`, recent summaries/reviews, and write `ROUND_CONTRACT_FILE` before coding. The contract must define exactly one mainline objective, 1-2 target ACs, blocking issues, queued out-of-scope issues, and success criteria (`prompt-template/claude/next-round-prompt.md:12-28`). It then imposes task lane tags `[mainline]`, `[blocking]`, `[queued]` with rules that queued issues must not replace the round objective (`prompt-template/claude/next-round-prompt.md:29-41`).
- inputs_outputs_state: Inputs are placeholders `PLAN_FILE`, `GOAL_TRACKER_FILE`, `ROUND_CONTRACT_FILE`, `BITLESSON_FILE`, and `REVIEW_CONTENT` (`prompt-template/claude/next-round-prompt.md:3-51`). Outputs are a next-round prompt file, a required round contract, Task-system updates, and updates to the mutable section of `goal-tracker.md` when safe (`prompt-template/claude/next-round-prompt.md:54-75`).
- gates_or_invariants: No implementation before round contract exists (`prompt-template/claude/next-round-prompt.md:27`). Exactly one mainline objective per round (`prompt-template/claude/next-round-prompt.md:20-21`). Before each task/sub-task, the agent must read BitLesson file, run `bitlesson-selector`, and follow selected lesson IDs or `NONE` (`prompt-template/claude/next-round-prompt.md:42-45`). Mainline guardrails prevent queued work from taking over (`prompt-template/claude/next-round-prompt.md:67-75`).
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh:2067-2087` renders this template when drift replan is not required; drift mode instead renders `claude/drift-replan-prompt.md`. The rendered prompt receives review content and tracker/contract paths from the hook.
- edge_cases_or_failure_modes: If Codex review reports many findings, this template forces classification into mainline gaps, blocking side issues, and queued side issues (`prompt-template/claude/next-round-prompt.md:71-75`). If the tracker cannot be safely reconciled, the prompt allows an optional update request instead of unsafe mutation (`prompt-template/claude/next-round-prompt.md:63-65`).
- validation_or_tests: Validated indirectly by hook rendering and template stress/robustness coverage; no standalone test in assigned items. It is a core transition contract rather than executable code.
- skip_candidate: `no`

### H2_DEV-HZ-239 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: Robustness test for production git state/status helpers in `scripts/humanize.sh`, specifically `humanize_parse_git_status` and `humanize_detect_git_state` (`tests/robustness/test-git-operations-robustness.sh:1-20`).
- algorithmic_behavior: Sources production code, creates temporary git repositories, mutates them into many status/state configurations, calls the real helper functions, parses pipe-delimited status output, and asserts expected counters or state strings (`tests/robustness/test-git-operations-robustness.sh:15-47`). Production status returns `modified|added|deleted|untracked|insertions|deletions`; git-state returns `normal`, `detached`, `rebase`, `merge`, `shallow`, `permission_error`, or `not_a_repo` (`scripts/humanize.sh:156-252`).
- inputs_outputs_state: Inputs are temp repos and filesystem/git metadata: clean repo, untracked/modified/staged/deleted files, branch changes, detached HEAD, simulated rebase/merge/shallow markers, non-git dir, and unreadable `.git` dir (`tests/robustness/test-git-operations-robustness.sh:49-500`). Outputs are pass/fail summary and process exit via `print_test_summary` (`tests/robustness/test-git-operations-robustness.sh:502-507`).
- gates_or_invariants: Clean repo counts are zero (`tests/robustness/test-git-operations-robustness.sh:71-84`). Untracked, modified, staged added, deleted, insertions, and deletions must be counted (`tests/robustness/test-git-operations-robustness.sh:98-157`, `tests/robustness/test-git-operations-robustness.sh:218-230`, `tests/robustness/test-git-operations-robustness.sh:349-367`). Renames count as modified by implementation contract (`tests/robustness/test-git-operations-robustness.sh:265-282`). State detection must identify normal, detached, rebase, merge, shallow, non-git, and permission cases (`tests/robustness/test-git-operations-robustness.sh:377-500`).
- dependencies_and_callers: Sources `tests/test-helpers.sh`, `scripts/portable-timeout.sh`, and `scripts/humanize.sh` (`tests/robustness/test-git-operations-robustness.sh:15-20`). `tests/run-all-tests.sh:121-127` includes it in the robustness suite.
- edge_cases_or_failure_modes: Covers empty repository without commits (`tests/robustness/test-git-operations-robustness.sh:233-246`), feature branches (`tests/robustness/test-git-operations-robustness.sh:248-263`), many untracked files (`tests/robustness/test-git-operations-robustness.sh:284-299`), filenames with spaces (`tests/robustness/test-git-operations-robustness.sh:301-314`), binary files (`tests/robustness/test-git-operations-robustness.sh:316-329`), and staged plus unstaged changes to the same file (`tests/robustness/test-git-operations-robustness.sh:331-347`). Permission error may degrade to `not_a_repo` on some systems and is accepted (`tests/robustness/test-git-operations-robustness.sh:478-500`).
- validation_or_tests: This file is the validation. It imports the production helpers directly, so failures indicate real monitor/git-summary behavior drift rather than fixture-only mismatch.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8/8 item evidence sections present in the scheduler-provided order, one section per assigned row.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`