# agent_26 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-026 `directory` `skills/humanize-rlcr`
- cursor: `[_]`
- core_role: Flow-skill entrypoint for RLCR. The directory contains `skills/humanize-rlcr/SKILL.md` and `skills/humanize-rlcr/SKILL-kimi.md`; both define the user-facing protocol for starting, iterating, gating, and cancelling the Ralph-Loop with Codex Review.
- algorithmic_behavior: `SKILL.md` is the Codex native-hook path: run `scripts/setup-rlcr-loop.sh`, read generated `.humanize/rlcr/<timestamp>/round-<N>-prompt.md`, implement, commit, write `round-<N>-summary.md` or `finalize-summary.md`, then stop so the native Humanize `Stop` hook advances or blocks the loop (`SKILL.md:24-48`). `SKILL-kimi.md` is the hook-equivalent skill-mode path for environments without native hooks; it explicitly calls `scripts/rlcr-stop-gate.sh`, maps exit `0` to done, `10` to blocked/continue, and `20` to infrastructure failure (`SKILL-kimi.md:28-64`).
- inputs_outputs_state: Inputs are flow arguments passed to setup, including plan path, `--track-plan-file`, `--max`, `--codex-model`, `--base-branch`, `--full-review-round`, `--skip-impl`, `--push-every-round`, `--agent-teams`, and quiz/yolo options (`SKILL.md:76-97`; `SKILL-kimi.md:91-112`). State is generated under `.humanize/rlcr/<timestamp>/`, especially `state.md`, `finalize-state.md`, prompts, summaries, review results, and phase markers (`SKILL.md:40-47`, `SKILL.md:71-74`). Outputs are committed code changes plus required summary files; cancellation delegates to `scripts/cancel-rlcr-loop.sh` (`SKILL.md:109-113`; `SKILL-kimi.md:124-128`).
- gates_or_invariants: Both skill files declare that the loop gates branch consistency, plan-file integrity, incomplete Task/Todo work, git cleanliness, unpushed commits when `--push-every-round` is active, summary presence, max-iteration handling, full-review rounds, strict `COMPLETE`/`STOP` markers, review-phase marker transitions, code-review markers, review failure/empty-output blocking, and open-question handling (`SKILL.md:50-68`; `SKILL-kimi.md:65-83`). Critical invariant: never manually edit state files, never bypass blocked hook results, never replace hook-managed transitions with ad hoc Codex commands, and use generated prompt/review files as source of truth (`SKILL.md:69-75`; `SKILL-kimi.md:84-90`).
- dependencies_and_callers: Depends on installer hydration of `{{HUMANIZE_RUNTIME_ROOT}}` (`SKILL.md:14-23`), `scripts/setup-rlcr-loop.sh`, native `hooks/loop-codex-stop-hook.sh` through Stop hooks, and `scripts/rlcr-stop-gate.sh` for hook-equivalent mode (`SKILL-kimi.md:9-17`, `SKILL-kimi.md:50-58`). Coordinates with sibling runtime scripts and prompt templates, not with code directly.
- edge_cases_or_failure_modes: Setup nonzero means stop and report (`SKILL.md:34`; `SKILL-kimi.md:38`). A blocked native hook or gate exit `10` must be followed exactly and continue to the next round (`SKILL.md:47-48`; `SKILL-kimi.md:60-63`). Gate exit `20` is infrastructure failure and must not be treated as completion (`SKILL-kimi.md:63`). Review-only mode starts with `--skip-impl` (`SKILL.md:105-106`).
- validation_or_tests: The directory itself is instruction surface rather than an executable test. Its contracts are exercised by related assigned tests for plan hooks, cancellation, base-branch detection, and stop-hook prompt templates.
- skip_candidate: `no`

### H2_DEV-HZ-056 `file` `scripts/cancel-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Runtime cancellation transition for active RLCR loops. It converts the active loop state into `cancel-state.md`, creates an explicit cancel signal, and returns machine-readable status lines (`scripts/cancel-rlcr-loop.sh:1-16`, `scripts/cancel-rlcr-loop.sh:156-184`).
- algorithmic_behavior: Parses only `--force` and help; unknown options exit `3` (`scripts/cancel-rlcr-loop.sh:24-63`). Resolves the project root via `hooks/lib/loop-common.sh`, then finds the newest active loop globally with `find_active_loop "$LOOP_BASE_DIR"` (`scripts/cancel-rlcr-loop.sh:69-92`). Selects active state priority as `state.md`, then `methodology-analysis-state.md`, then `finalize-state.md` (`scripts/cancel-rlcr-loop.sh:103-121`). It extracts `current_round` and `max_iterations` from YAML-like lines for user output (`scripts/cancel-rlcr-loop.sh:127-133`).
- inputs_outputs_state: Input is current project context plus optional `--force`. State read/write surfaces are `$PROJECT_ROOT/.humanize/rlcr/<timestamp>`, `.cancel-requested`, active state file, `cancel-state.md`, `$PROJECT_ROOT/.humanize/.pending-session-id`, and `.methodology-exit-reason` (`scripts/cancel-rlcr-loop.sh:78`, `scripts/cancel-rlcr-loop.sh:103-106`, `scripts/cancel-rlcr-loop.sh:156-166`). Outputs are status tokens: `NO_LOOP`, `NO_ACTIVE_LOOP`, `FINALIZE_NEEDS_CONFIRM`, `CANCELLED`, `CANCELLED_METHODOLOGY_ANALYSIS`, or `CANCELLED_FINALIZE` (`scripts/cancel-rlcr-loop.sh:93-96`, `scripts/cancel-rlcr-loop.sh:118-120`, `scripts/cancel-rlcr-loop.sh:139-149`, `scripts/cancel-rlcr-loop.sh:172-184`).
- gates_or_invariants: Finalize phase requires explicit `--force` because finalize is the post-completion ending phase (`scripts/cancel-rlcr-loop.sh:139-149`). Cancellation is intentionally global, not session-filtered, because standalone slash-command invocation lacks the caller’s session id (`scripts/cancel-rlcr-loop.sh:80-91`). The script creates `.cancel-requested` before moving state so validators can distinguish authorized cancellation from arbitrary state mutation (`scripts/cancel-rlcr-loop.sh:156-166`; shared authorization is in `hooks/lib/loop-common.sh:1039-1246`).
- dependencies_and_callers: Depends on `resolve_project_root` and `find_active_loop` from `hooks/lib/loop-common.sh` (`scripts/cancel-rlcr-loop.sh:69-77`). The skill flow advertises this script as the cancel command. Bash validators special-case authorized state-file movement only when `.cancel-requested` exists and the command is exactly the allowed move/cancel transition (`hooks/loop-bash-validator.sh:271-297`, `hooks/loop-bash-validator.sh:428-456`).
- edge_cases_or_failure_modes: No active directory or missing active state exits `1` (`scripts/cancel-rlcr-loop.sh:93-121`). Project root resolution failure exits `3` (`scripts/cancel-rlcr-loop.sh:73-77`). Missing round metadata is tolerated and displayed as `?` (`scripts/cancel-rlcr-loop.sh:127-133`). Existing `cancel-state.md` would be overwritten by `mv` semantics if present; no explicit guard is present in this script.
- validation_or_tests: Cancel signal/state authorization is covered by `tests/test-cancel-signal-file.sh` and robustness security tests referenced by the shared helper search; this assigned file itself is not a test.
- skip_candidate: `no`

### H2_DEV-HZ-086 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for portable ANSI-stripping and summary-count extraction used by the parallel test runner’s result aggregation.
- algorithmic_behavior: Defines pass/fail counters and tests `sed "s/${esc}\\[[0-9;]*m//g"` using ANSI-C escape `$'\033'` (`tests/test-ansi-parsing.sh:38-48`). It verifies stripping single color codes, multiple codes, bold/color combined codes, and no-ANSI plain text (`tests/test-ansi-parsing.sh:41-67`, `tests/test-ansi-parsing.sh:132-158`). It then extracts numeric `Passed:` and `Failed:` counts with `grep -oE` and `tail -1` for multi-line summaries (`tests/test-ansi-parsing.sh:70-130`).
- inputs_outputs_state: Inputs are synthetic strings containing ANSI SGR sequences and test-summary-like lines. Outputs are colored PASS/FAIL lines and final `Passed:`/`Failed:` summary; exit `0` iff no failures, else `1` (`tests/test-ansi-parsing.sh:160-178`). No persistent state.
- gates_or_invariants: The stripping regex intentionally handles SGR-style color/bold codes with numeric/semicolon parameters ending in `m`; it is portable across GNU and BSD sed (`tests/test-ansi-parsing.sh:3-6`, `tests/test-ansi-parsing.sh:38-40`). Counts must parse after color removal and default to `0` when grep finds no match (`tests/test-ansi-parsing.sh:76`, `tests/test-ansi-parsing.sh:90`, `tests/test-ansi-parsing.sh:104`).
- dependencies_and_callers: Mirrors `tests/run-all-tests.sh`, where `collect_suite_result` strips ANSI and aggregates `Passed:`/`Failed:` counts (`tests/run-all-tests.sh:243-283`, especially `tests/run-all-tests.sh:264-270`). Included in the aggregate suite list at `tests/run-all-tests.sh:59-138`.
- edge_cases_or_failure_modes: Covers zero failed count, mixed multi-line output, and text with no ANSI sequences (`tests/test-ansi-parsing.sh:97-144`). It only targets SGR `...m` escape sequences; cursor/control sequences with other final bytes are outside this test’s contract.
- validation_or_tests: Self-validating shell test; no run was executed during this read-only research pass.
- skip_candidate: `no`

### H2_DEV-HZ-116 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Broad executable spec for RLCR hook enforcement around plan-file immutability, state schema, branch consistency, protected loop artifacts, command-bypass resistance, goal-tracker initialization, and legacy path behavior.
- algorithmic_behavior: Builds isolated git repos under `mktemp`, mocks `codex`, writes loop state under `.humanize/rlcr/2024-01-01_12-00-00`, and supplies round contracts plus plan backups (`tests/test-plan-file-hooks.sh:30-140`). It exercises `hooks/loop-plan-file-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, and `loop-codex-stop-hook.sh` with JSON hook payloads and stop-hook stdin (`tests/test-plan-file-hooks.sh:145-244`, `tests/test-plan-file-hooks.sh:252-324`, `tests/test-plan-file-hooks.sh:450-525`, `tests/test-plan-file-hooks.sh:567-754`).
- inputs_outputs_state: Inputs are synthetic hook JSON payloads for `UserPromptSubmit`, `Write`, `Edit`, and `Bash`, plus temporary repo branch/state/plan mutations. Expected outputs are exit codes and JSON/text diagnostics: exit `0` allow, exit `2` pre-tool block for Write/Edit/Bash, stop-hook JSON with `"decision":"block"` for loop blocks, and final shell exit equal to failure count (`tests/test-plan-file-hooks.sh:151-158`, `tests/test-plan-file-hooks.sh:254-263`, `tests/test-plan-file-hooks.sh:1172-1181`).
- gates_or_invariants: Requires strict state fields including `plan_tracked`, `start_branch`, `review_started`, and `base_branch`; malformed or old schemas must block (`tests/test-plan-file-hooks.sh:175-215`, `tests/test-plan-file-hooks.sh:732-754`; implementation in `hooks/lib/loop-common.sh:524-600`). Branch switching blocks (`tests/test-plan-file-hooks.sh:220-244`; implementation in `hooks/loop-plan-file-validator.sh:92-116` and `hooks/loop-codex-stop-hook.sh:286-316`). Plan backup/original integrity is enforced for missing, deleted, uncommitted, and committed modifications (`tests/test-plan-file-hooks.sh:567-835`; implementation in `hooks/loop-codex-stop-hook.sh:318-399`).
- dependencies_and_callers: Depends on shared parsing and loop lookup in `hooks/lib/loop-common.sh`, especially quote stripping for YAML-like fields (`hooks/lib/loop-common.sh:446-469`), strict parser (`hooks/lib/loop-common.sh:524-600`), and active-loop resolution (`hooks/lib/loop-common.sh:264-425`). The concrete validators block direct plan backup writes (`hooks/loop-write-validator.sh:237-247`; `hooks/loop-edit-validator.sh:186-198`) and shell modifications/bypasses (`hooks/loop-bash-validator.sh:490-501`).
- edge_cases_or_failure_modes: Explicitly covers YAML-quoted `plan_file` and `start_branch`, hyphenated plan paths, branch mismatch with quoted values, direct `.humanize/rlcr/plan.md` with no timestamp, command substitution, glob, brace expansion, pipes to `tee`, backticks, tracked-plan race conditions, committed tracked changes that leave git status clean, missing round contract, and section-specific goal-tracker placeholder reporting (`tests/test-plan-file-hooks.sh:160-173`, `tests/test-plan-file-hooks.sh:312-393`, `tests/test-plan-file-hooks.sh:399-558`, `tests/test-plan-file-hooks.sh:660-835`, `tests/test-plan-file-hooks.sh:837-1126`). Legacy `.humanize-loop.local` paths are intentionally allowed and treated as negative tests (`tests/test-plan-file-hooks.sh:1128-1170`).
- validation_or_tests: This file is itself the executable validation suite. Its assertions connect to assigned templates: goal-tracker placeholder diagnostics are rendered through `prompt-template/block/goal-tracker-not-initialized.md` from `hooks/loop-codex-stop-hook.sh:900-960`.
- skip_candidate: `no`

### H2_DEV-HZ-146 `file` `tests/workflow-manifest.test.ts`
- cursor: `[_]`
- core_role: Vitest executable spec for H2 workflow cartridge manifest enforcement, ensuring workflow agents/scripts cannot use undeclared or hub-unauthorized capabilities and that missing human-input declarations produce warnings.
- algorithmic_behavior: Constructs in-memory workflow HTML cartridges and calls `WorkflowCoordinator.loadHtml`. Tests reject an `h2-agent` whose `tool="codex"` is absent from manifest `tools`, reject an `h2-script uses="git.detectBase"` absent from manifest `allow`, reject a declared-but-unregistered script adapter, reject a manifest-declared tool not authorized by hub config, and verify `h2-human` without capability emits a warning at load and run start (`tests/workflow-manifest.test.ts:8-91`).
- inputs_outputs_state: Inputs are HTML strings, fake agent backends for `codex` and `claude`, and optional `authorizedAgentTools` config (`tests/workflow-manifest.test.ts:93-133`). Outputs are rejected promises with error codes/messages or a loaded cartridge/run event list containing `manifest.warning.human_input_undeclared` (`tests/workflow-manifest.test.ts:23`, `tests/workflow-manifest.test.ts:39`, `tests/workflow-manifest.test.ts:55`, `tests/workflow-manifest.test.ts:72`, `tests/workflow-manifest.test.ts:87-89`).
- gates_or_invariants: Hard-tier manifest rule: any agent/script/check/transform use must be explicitly declared when corresponding vertices exist; missing declarations fail cartridge load (`src/workflows/coordinator.ts:202-240`). Agent tools must also be in hub-authorized tools (`src/workflows/coordinator.ts:227-232`). Script adapters must be registered and support the requested kind (`src/workflows/coordinator.ts:241-255`). Human input is warning-tier, not hard fail (`src/workflows/parser.ts:183-199`).
- dependencies_and_callers: Depends on `WorkflowCoordinator`, `AgentRunCoordinator`, `HumanizeService`, fake `AgentBackend`s, default script registry, and parse/compile pipeline (`tests/workflow-manifest.test.ts:1-7`, `tests/workflow-manifest.test.ts:93-133`; implementation imports at `src/workflows/coordinator.ts:1-47`). Default registered adapters include `git.statusClean` and `git.detectBase`, which is why undeclared `git.detectBase` and unknown `git.deleteEverything` test distinct paths (`src/workflows/script-registry.ts:71-87`).
- edge_cases_or_failure_modes: Separates undeclared from unregistered from unauthorized; a script can be manifest-declared yet still fail if the runtime registry lacks it (`tests/workflow-manifest.test.ts:42-56`). A tool can be manifest-declared yet fail if hub config excludes it (`tests/workflow-manifest.test.ts:58-73`). Human input omission is intentionally surfaced as event telemetry, not a load blocker (`tests/workflow-manifest.test.ts:75-90`).
- validation_or_tests: Vitest suite; no run was executed during this read-only research pass.
- skip_candidate: `no`

### H2_DEV-HZ-176 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: Stop-hook block template for Round 0 goal-tracker initialization failure. It is a user-facing contract that turns detected placeholder sections into required corrective actions.
- algorithmic_behavior: Template renders `{{GOAL_TRACKER_FILE}}` and `{{MISSING_ITEMS}}`, then instructs the agent to read the tracker, replace placeholders with real Ultimate Goal, 3-7 testable Acceptance Criteria, and Active Tasks mapped to ACs, write `goal-tracker.md`, then try exiting again (`prompt-template/block/goal-tracker-not-initialized.md:1-18`).
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-codex-stop-hook.sh`: `GOAL_TRACKER_FILE` and a generated missing-item list (`hooks/loop-codex-stop-hook.sh:927-949`). Output is the `reason` field in a stop-hook JSON block with system message `Loop: Goal Tracker not initialized in Round 0` (`hooks/loop-codex-stop-hook.sh:951-960`).
- gates_or_invariants: The immutable section can only be set in Round 0; after Round 0 it is read-only (`prompt-template/block/goal-tracker-not-initialized.md:16`). Stop hook only runs this placeholder gate in non-finalize, non-review, Round 0, when `goal-tracker.md` exists (`hooks/loop-codex-stop-hook.sh:886-895`). Detection is section-specific: Ultimate Goal, Acceptance Criteria, and Active Tasks are checked independently for placeholder text (`hooks/loop-codex-stop-hook.sh:900-940`).
- dependencies_and_callers: Called through `load_and_render_safe "$TEMPLATE_DIR" "block/goal-tracker-not-initialized.md"` in the stop hook (`hooks/loop-codex-stop-hook.sh:942-949`). Validated by the section-specific placeholder tests in `tests/test-plan-file-hooks.sh:837-1126`.
- edge_cases_or_failure_modes: If the template is missing, the stop hook has a fallback message (`hooks/loop-codex-stop-hook.sh:942-947`). Placeholder matching is generic within each section, so variant placeholder wording beginning with `[To be ...]` is caught without requiring exact full strings (`hooks/loop-codex-stop-hook.sh:904-924`).
- validation_or_tests: Covered by tests that assert only the relevant missing section is reported and all three are reported when all placeholders remain (`tests/test-plan-file-hooks.sh:841-1126`).
- skip_candidate: `no`

### H2_DEV-HZ-206 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Next-round prompt footer template that gives Claude a controlled fallback protocol for requesting goal-tracker mutable-section changes via the summary when direct update is unsafe.
- algorithmic_behavior: Provides a markdown section titled `Goal Tracker Update Request` with `Requested Changes` bullets and a `Justification` field (`prompt-template/claude/goal-tracker-update-request.md:1-17`). The stop hook appends this template to the next round prompt after the standard footer and optional push note (`hooks/loop-codex-stop-hook.sh:2172-2193`).
- inputs_outputs_state: No dynamic variables in the template itself. Input is the stop-hook prompt-generation phase; output is appended text in the next `round-<N>-prompt.md` that instructs the agent how to request task completion marks, blocking side issues, queued issues, plan evolution, or deferrals (`prompt-template/claude/goal-tracker-update-request.md:2-15`).
- gates_or_invariants: Direct mutable updates are optional fallback only; the template asks for summary inclusion when safe direct update is not possible, preserving stop-hook review/reconciliation as the authority (`prompt-template/claude/goal-tracker-update-request.md:2`, `prompt-template/claude/goal-tracker-update-request.md:17`). This pairs with validators that block unsafe direct goal-tracker edits after Round 0 unless the immutable section is preserved (`hooks/loop-write-validator.sh:253-276`; `hooks/loop-edit-validator.sh:204-240`; shared immutable comparison in `hooks/lib/loop-common.sh:955-1008`).
- dependencies_and_callers: Loaded with `load_template "$TEMPLATE_DIR" "claude/goal-tracker-update-request.md"` and falls back to a one-line instruction if unavailable (`hooks/loop-codex-stop-hook.sh:2188-2193`).
- edge_cases_or_failure_modes: Template is advisory but algorithmically important because it provides the sanctioned path when Write/Edit validators reject unsafe mutations. If missing, the stop hook still appends a fallback instruction (`hooks/loop-codex-stop-hook.sh:2188-2193`).
- validation_or_tests: Indirectly exercised by prompt-generation/goal-tracker update behavior in broader stop-hook suites; this assigned file is not itself executable.
- skip_candidate: `no`

### H2_DEV-HZ-236 `file` `tests/robustness/test-base-branch-detection.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for RLCR setup’s base-branch auto-detection logic, especially missing-origin behavior and fallback ordering.
- algorithmic_behavior: Sources test helpers and `scripts/portable-timeout.sh`, then defines `detect_base_branch(project_root, timeout)` that mirrors setup logic: try `git remote show origin | grep "HEAD branch:"`, ignore missing-origin failure with `|| true`, then fallback to local `main`, then local `master`, else return failure (`tests/robustness/test-base-branch-detection.sh:14-64`). It creates isolated git repos for local-main, local-master, no-main/master, and simulated-origin cases (`tests/robustness/test-base-branch-detection.sh:70-191`).
- inputs_outputs_state: Inputs are temporary git repositories with selected branch/remote topology. Output is pass/fail test reporting via sourced `test-helpers.sh`, ending with `print_test_summary` (`tests/robustness/test-base-branch-detection.sh:18-29`, `tests/robustness/test-base-branch-detection.sh:193-198`). State lives only under the test temp directory created by `setup_test_dir`.
- gates_or_invariants: Priority order is user-specified branch in setup, then remote default branch, then local `main`, then local `master`, then hard failure (`tests/robustness/test-base-branch-detection.sh:5-11`; implementation in `scripts/setup-rlcr-loop.sh:748-802`). Setup’s full implementation additionally validates that user-specified and remote-default branches exist locally, rejects YAML-unsafe branch names, and captures `base_commit` to avoid self-diff issues when working on the base branch (`scripts/setup-rlcr-loop.sh:753-820`).
- dependencies_and_callers: Depends on `run_with_timeout` from `scripts/portable-timeout.sh` for portable bounded git commands (`tests/robustness/test-base-branch-detection.sh:19-20`, `tests/robustness/test-base-branch-detection.sh:35-44`). Mirrors `scripts/setup-rlcr-loop.sh:772-802`, not a separately exported production function.
- edge_cases_or_failure_modes: Explicitly verifies origin absence does not terminate under `set -euo pipefail` because remote detection is guarded with `|| true` (`tests/robustness/test-base-branch-detection.sh:41-45`, `tests/robustness/test-base-branch-detection.sh:70-122`). Verifies failure when only a nonstandard branch such as `develop` exists (`tests/robustness/test-base-branch-detection.sh:124-149`). Simulated remote test accepts detection from origin or fallback to local `main` if `remote show` is not definitive (`tests/robustness/test-base-branch-detection.sh:155-191`).
- validation_or_tests: Self-contained robustness test; no run was executed during this read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8 unique item headings above; each assigned heading appears once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`