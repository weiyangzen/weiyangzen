# agent_25 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-025 `directory` `skills/humanize-refine-plan`
- cursor: `[_]`
- core_role: Defines the non-user-invocable `humanize-refine-plan` flow for converting annotated `gen-plan` documents with `CMT:` / `ENDCMT` blocks into a comment-free refined plan plus QA ledger. Recursive inspection found one child: `skills/humanize-refine-plan/SKILL.md`.
- algorithmic_behavior: The embedded flowchart drives argument parsing, merged config load, IO validation through `scripts/validate-refine-plan-io.sh`, stateful comment extraction, comment classification, ordered processing, schema-preserving refinement, consistency repair, QA ledger population, optional translation, atomic writes, and success reporting (`skills/humanize-refine-plan/SKILL.md:18`-`47`).
- inputs_outputs_state: Inputs are `--input` annotated plan plus optional `--output`, `--qa-dir`, `--alt-language`, `--discussion`, and `--direct` (`skills/humanize-refine-plan/SKILL.md:50`-`65`). Outputs are the refined plan, QA ledger, and optional language variants (`skills/humanize-refine-plan/SKILL.md:96`-`107`). State is convergence metadata captured in QA and the preserved plan structure.
- gates_or_invariants: Requires the `gen-plan` schema sections to remain intact, removes all resolved comment blocks, restricts task routing tags to `coding` or `analyze`, and requires atomic writes (`skills/humanize-refine-plan/SKILL.md:67`-`87`). Validator exit codes distinguish missing input, empty input, no comments, missing required sections, unwritable output/QA dirs, and invalid args (`skills/humanize-refine-plan/SKILL.md:131`-`143`).
- dependencies_and_callers: Depends on hydrated `{{HUMANIZE_RUNTIME_ROOT}}`, `scripts/validate-refine-plan-io.sh`, and `prompt-template/plan/refine-plan-qa-template.md` (`skills/humanize-refine-plan/SKILL.md:12`-`22`, `41`). It is exposed as `/flow:humanize-refine-plan` and `/skill:humanize-refine-plan` (`skills/humanize-refine-plan/SKILL.md:144`-`160`).
- edge_cases_or_failure_modes: Ambiguous comments branch either to user questions in discussion mode or safe assumptions in direct mode (`skills/humanize-refine-plan/SKILL.md:30`-`34`, `59`-`64`). Parse failures must report line, column, heading, and context (`skills/humanize-refine-plan/SKILL.md:26`-`29`). English/no-op and same-language alternate output are skipped (`skills/humanize-refine-plan/SKILL.md:124`-`129`).
- validation_or_tests: Validation is specified as a required pre-processing gate and encoded through exit-code semantics. Related executable coverage exists in `tests/test-refine-plan.sh` and `scripts/validate-refine-plan-io.sh`, though not assigned here.
- skip_candidate: `no`

### H2_DEV-HZ-055 `file` `scripts/bitlesson-validate-delta.sh`
- cursor: `[_]`
- core_role: Runtime stop/loop validator that enforces a required `## BitLesson Delta` contract in round summaries before allowing the RLCR loop to exit cleanly.
- algorithmic_behavior: Parses mandatory CLI args, verifies the summary file, loads `hooks/lib/template-loader.sh`, emits block decisions as JSON through `block_exit`, and exits `0` even when blocking so the hook system receives a structured decision (`scripts/bitlesson-validate-delta.sh:4`-`90`). It extracts the delta block with an awk state machine that ignores headings inside fenced code blocks or HTML comments (`scripts/bitlesson-validate-delta.sh:92`-`179`).
- inputs_outputs_state: Inputs are `--summary-file`, `--bitlesson-file`, `--bitlesson-relpath`, `--allow-empty-none`, `--template-dir`, and `--current-round` (`scripts/bitlesson-validate-delta.sh:4`-`23`, `62`-`67`). Outputs are either silence with exit `0` on pass, normal argument/file errors with exit `1`, or JSON `{decision:"block", reason, systemMessage}` on policy violations (`scripts/bitlesson-validate-delta.sh:78`-`90`).
- gates_or_invariants: Requires exactly one action of `none`, `add`, or `update` (`scripts/bitlesson-validate-delta.sh:202`-`218`). `Action: none` requires empty/NONE IDs and, unless explicitly allowed, an existing concrete BitLesson entry (`scripts/bitlesson-validate-delta.sh:242`-`267`). `add`/`update` require concrete IDs, non-placeholder `Notes`, an existing BitLesson file, valid ID syntax, and entries present in that file (`scripts/bitlesson-validate-delta.sh:269`-`384`).
- dependencies_and_callers: Depends on `jq`, `awk`, `sed`, `tr`, and `load_and_render_safe` from `hooks/lib/template-loader.sh` (`scripts/bitlesson-validate-delta.sh:74`-`88`). Uses prompt-template block fallbacks such as `block/bitlesson-delta-missing.md`, `block/bitlesson-delta-invalid.md`, `block/bitlesson-delta-inconsistent.md`, `block/bitlesson-delta-empty-kb.md`, and `block/bitlesson-delta-missing-notes.md` (`scripts/bitlesson-validate-delta.sh:196`, `216`, `263`, `276`, `294`).
- edge_cases_or_failure_modes: Missing `## BitLesson Delta` blocks, multiple/invalid actions, `none` with IDs, non-zero round with no concrete KB entry, add/update with `NONE`, missing or placeholder notes, missing BitLesson file, malformed IDs, and declared IDs absent from the KB all block with distinct messages. Fenced/commented delta text is intentionally ignored by the extractor.
- validation_or_tests: Covered by `tests/test-bitlesson-validate-delta.sh`, which asserts whitespace/placeholder notes block, fenced/commented delta blocks fail detection, and normal add/update with notes passes (`tests/test-bitlesson-validate-delta.sh:101`-`152`).
- skip_candidate: `no`

### H2_DEV-HZ-085 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR hook allowlist behavior across direct allowlist helper logic and Read/Write/Edit/Bash validators.
- algorithmic_behavior: Creates a temporary git repo and active loop directory with `state.md` declaring `current_round: 5`, then feeds JSON hook inputs into validators and checks expected exit codes/output text (`tests/test-allowlist-validators.sh:29`-`64`, `140`-`149`). It increments pass/fail counters and exits with the failure count (`tests/test-allowlist-validators.sh:19`-`27`, `577`-`585`).
- inputs_outputs_state: Inputs are synthetic hook payloads for `Read`, `Write`, `Edit`, and `Bash`, plus loop state at `.humanize/rlcr/<timestamp>/state.md`. Outputs are PASS/FAIL lines and process exit status equal to failed assertions. State created during tests includes temporary `.git`, loop dir, state file, and old/different loop directories.
- gates_or_invariants: Specifies the stable allowlist: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, `round-1-summary.md` are allowed only in the active loop dir (`tests/test-allowlist-validators.sh:72`-`134`). Current-round contract access is allowed via Read/Write/Edit for round 5, stale contracts are blocked, and Bash modification of contracts remains blocked (`tests/test-allowlist-validators.sh:169`-`179`, `251`-`274`, `320`-`369`, `402`-`412`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` and exercises `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh` (`tests/test-allowlist-validators.sh:15`-`17`, `147`, `229`, `298`, `380`). The helper under test is `is_allowlisted_file`, implemented with canonical active-loop path comparison and a fixed four-file allowlist (`hooks/lib/loop-common.sh:816`-`842`).
- edge_cases_or_failure_modes: Tests wrong directory, old loop directory, same basename under a different root, generic relative todos filename, stale round summaries/contracts, and direct hook execution bypasses wrapped by `env`, `command`, assignment prefixes, `timeout`, `nice`, `nohup`, `strace`, and `ltrace` (`tests/test-allowlist-validators.sh:415`-`575`). The Bash hook implements those wrapper patterns in `HOOK_WRAPPER_PREFIX_PATTERN`/`HOOK_LAUNCH_PATTERN` (`hooks/loop-bash-validator.sh:184`-`202`).
- validation_or_tests: This file is itself the validation surface. It also documents production validator behavior: todos checks in Write/Edit/Read use `is_allowlisted_file` (`hooks/loop-write-validator.sh:57`-`64`, `hooks/loop-edit-validator.sh:40`-`47`, `hooks/loop-read-validator.sh:56`-`63`), while wrong-round summary/contract checks exempt allowlisted files (`hooks/loop-write-validator.sh:318`-`336`, `hooks/loop-edit-validator.sh:247`-`272`, `hooks/loop-read-validator.sh:286`-`299`).
- skip_candidate: `no`

### H2_DEV-HZ-115 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Runtime verification spec for the `humanize monitor rlcr` terminal monitor cleanup, graceful stop, loop-directory deletion handling, and signal behavior.
- algorithmic_behavior: Builds temporary scripts that source or mimic relevant `scripts/humanize.sh` functions, then asserts output markers for cleanup, terminal restoration, graceful stop messages, idempotency, directory deletion detection, and signal traps (`tests/test-monitor-runtime.sh:45`-`109`, `143`-`233`, `333`-`395`).
- inputs_outputs_state: Inputs are temporary `.humanize/rlcr` directories, generated shell scripts, simulated monitor variables, and self-sent SIGINT/SIGTERM paths. Outputs are PASS/FAIL lines and exit `0` only when all runtime checks pass (`tests/test-monitor-runtime.sh:511`-`532`).
- gates_or_invariants: `_cleanup` must be idempotent, reset traps, kill active tail process, call `_restore_terminal`, and mark `monitor_running=false` (`scripts/humanize.sh:731`-`762`). `_graceful_stop` must call `_cleanup` and print a user-facing reason (`scripts/humanize.sh:764`-`777`). Bash must install `trap '_cleanup' INT TERM`; zsh must define `TRAPINT`/`TRAPTERM` (`scripts/humanize.sh:784`-`795`).
- dependencies_and_callers: Targets `scripts/humanize.sh`, especially monitor internals around `_restore_terminal`, `_cleanup`, `_graceful_stop`, and the main loop. Depends on shell builtins, `grep`, `kill`, `sleep`, optional `zsh`, and terminal commands via source verification rather than full interactive terminal emulation.
- edge_cases_or_failure_modes: Covers deleted `.humanize/rlcr`, double cleanup calls, source-level absence of scroll-region reset, SIGINT ignored in background/parallel CI contexts, missing zsh, and cleanup trap reentrancy (`tests/test-monitor-runtime.sh:173`-`177`, `347`-`364`, `451`-`509`). Production monitor checks loop-dir deletion in the main, no-log, and log-follow loops (`scripts/humanize.sh:839`-`843`, `904`-`908`, `1027`-`1030`).
- validation_or_tests: This file is the validation artifact. It cross-checks both dynamic behavior and source structure: `_restore_terminal()` and `printf "\033[r"` (`tests/test-monitor-runtime.sh:263`-`307`), `_graceful_stop -> _cleanup` (`tests/test-monitor-runtime.sh:314`-`324`), bash/zsh signal handlers (`tests/test-monitor-runtime.sh:402`-`428`), and trap reset (`tests/test-monitor-runtime.sh:430`-`442`).
- skip_candidate: `no`

### H2_DEV-HZ-145 `file` `tests/workflow-graph.test.ts`
- cursor: `[_]`
- core_role: Vitest executable spec for workflow graph compilation legality around loop gotos, `continue` edges, parallel branch isolation, and invalid non-loop cycles.
- algorithmic_behavior: Parses inline H2 workflow HTML with `parseWorkflowCartridge`, compiles it via `compileGraph`, and asserts either successful compilation or specific `GraphCompileError` messages (`tests/workflow-graph.test.ts:1`-`8`). The cases construct minimal cartridges to isolate graph-edge validation behavior.
- inputs_outputs_state: Inputs are `<h2-workflow>` HTML strings containing flow, loop, branch, parallel, sequence, script, case/default, `goto`, and `continue` tags. Outputs are thrown/not-thrown assertions; no persistent state.
- gates_or_invariants: Rejects external `goto` into loop body (`tests/workflow-graph.test.ts:11`-`26`), accepts `continue` to immediate or outer enclosing loops (`tests/workflow-graph.test.ts:28`-`61`), rejects `continue` to unrelated loops (`tests/workflow-graph.test.ts:63`-`80`), rejects branch escapes from parallel branches (`tests/workflow-graph.test.ts:82`-`103`), accepts same-branch parallel targets (`tests/workflow-graph.test.ts:105`-`127`), and rejects goto-created cycles not closed by loop/continue edges (`tests/workflow-graph.test.ts:129`-`142`).
- dependencies_and_callers: Depends on `src/workflows/parser.ts` and `src/workflows/graph.ts`. `compileGraph` creates synthetic start/end vertices, compiles child nodes, resolves branch/continue targets, validates branch scoping, then validates cycles (`src/workflows/graph.ts:67`-`99`).
- edge_cases_or_failure_modes: Production validation rejects `continue` to missing/non-enclosing loops (`src/workflows/graph.ts:405`-`420`), `goto` into a loop body from outside the loop chain (`src/workflows/graph.ts:448`-`459`), parallel branch escapes unless targeting the same branch or join vertex (`src/workflows/graph.ts:462`-`505`), and SCC cycles after filtering `backedge` and `continue-edge` (`src/workflows/graph.ts:509`-`530`).
- validation_or_tests: This file is the targeted regression suite for graph compiler control-flow invariants. It validates semantics used by workflow runtime/coordinator code that imports `compileGraph` (`src/workflows/coordinator.ts:13`, `190`, `400`).
- skip_candidate: `no`

### H2_DEV-HZ-175 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Block-message template for enforcing RLCR goal-tracker mutation policy after Round 0.
- algorithmic_behavior: Provides user-facing instructions rendered when validators reject unsafe goal-tracker changes. It tells the actor that after Round 0 only the active tracker’s mutable section may be updated, and points to `{{CORRECT_PATH}}` (`prompt-template/block/goal-tracker-modification.md:1`-`7`).
- inputs_outputs_state: Inputs are template variables `CURRENT_ROUND` and `CORRECT_PATH`; output is markdown block text used as stderr/system guidance by hook validators. It does not mutate state itself.
- gates_or_invariants: Encodes three invariants: keep the immutable section unchanged, do not modify `goal-tracker.md` via Bash, and do not write to old loop session trackers (`prompt-template/block/goal-tracker-modification.md:8`-`14`).
- dependencies_and_callers: Loaded through `load_and_render_safe` by goal-tracker blocked-message helpers in `hooks/lib/loop-common.sh`, then invoked by Write/Edit/Bash validators when path or immutable-section checks fail. The assigned allowlist tests indirectly exercise related hook blocking but not this template directly.
- edge_cases_or_failure_modes: Handles drift that cannot be safely resolved by directing the actor to include an optional `Goal Tracker Update Request` in the round summary instead of modifying protected content directly (`prompt-template/block/goal-tracker-modification.md:14`).
- validation_or_tests: Related coverage appears in hook robustness and goal-tracker tests, and in validator code paths that call `goal_tracker_blocked_message` from Write/Edit/Bash (`hooks/loop-write-validator.sh:253`-`276`, `hooks/loop-edit-validator.sh:204`-`240`, `hooks/loop-bash-validator.sh:509`-`517`).
- skip_candidate: `no`

### H2_DEV-HZ-205 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase instruction template used when code review was skipped, preserving the RLCR transition to finalization while explicitly weakening validation confidence.
- algorithmic_behavior: Renders a warning with `{{REVIEW_SKIP_REASON}}`, instructs manual verification, optionally suggests a simplifier agent, states non-negotiable refactor constraints, references plan/tracker files, and defines required pre-exit tasks and final summary content (`prompt-template/claude/finalize-phase-skipped-prompt.md:1`-`51`).
- inputs_outputs_state: Inputs are `REVIEW_SKIP_REASON`, `BASE_BRANCH`, `START_BRANCH`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, and `FINALIZE_SUMMARY_FILE`. Output is a Claude prompt that moves state into Finalize Phase and requires writing a finalize summary; it does not itself update state files.
- gates_or_invariants: Existing functionality must not change, existing tests must not fail, no new bugs may be introduced, and any simplification must be functionality-equivalent (`prompt-template/claude/finalize-phase-skipped-prompt.md:26`-`34`). Before exit, all `[mainline]` and `[blocking]` tasks must be completed; `[queued]` items may remain only as documented non-blocking follow-up (`prompt-template/claude/finalize-phase-skipped-prompt.md:40`-`45`).
- dependencies_and_callers: Consumed by finalize/review skip orchestration in setup/stop-hook paths. It references the Task tool simplifier and completion surfaces: original plan, goal tracker, and `finalize-summary.md` (`prompt-template/claude/finalize-phase-skipped-prompt.md:15`-`24`, `35`-`45`).
- edge_cases_or_failure_modes: Explicitly covers the degraded path where implementation “could not be fully validated” because review was skipped (`prompt-template/claude/finalize-phase-skipped-prompt.md:3`-`10`). It mitigates that by requiring manual review/tests and summary confirmation of tests or manual verification (`prompt-template/claude/finalize-phase-skipped-prompt.md:47`-`51`).
- validation_or_tests: Related executable coverage is in `tests/test-finalize-phase.sh`, which exercises finalize prompt/state behavior and finalize contract restrictions. The assigned allowlist test also verifies current/stale contract access behavior that changes around active/finalize phases.
- skip_candidate: `no`

### H2_DEV-HZ-235 `file` `skills/humanize/SKILL.md`
- cursor: `[_]`
- core_role: Primary skill instruction for Humanize RLCR, defining the iterative implementation/review loop, plan generation conventions, state surfaces, monitor entrypoint, and required operator behavior.
- algorithmic_behavior: Defines RLCR as two phases: implementation rounds where AI writes summaries and Codex reviews for completeness, then code review where `codex review --base <branch>` emits `[P0-9]` issues until no issues remain and Finalize Phase begins (`skills/humanize/SKILL.md:30`-`49`). It also defines plan generation structure and acceptance-criteria conventions (`skills/humanize/SKILL.md:50`-`58`, `112`-`151`).
- inputs_outputs_state: Main inputs are a plan file or `--skip-impl`, loop options such as `--max`, `--codex-model`, `--base-branch`, `--full-review-round`, plan tracking, push policy, answer mode, agent teams, quiz/privacy flags (`skills/humanize/SKILL.md:61`-`89`). Outputs/state live under `.humanize/rlcr/<timestamp>/`: `state.md`, `goal-tracker.md`, round summaries/reviews, finalize/methodology/complete state files, plus `.humanize/skill` one-shot results (`skills/humanize/SKILL.md:181`-`203`).
- gates_or_invariants: Important rules require summaries before exit, goal-tracker maintenance, thorough implementation/test details, no state-file/cancel-command cheating, normal exit through the native Stop hook, and trust in external review (`skills/humanize/SKILL.md:167`-`174`). Goal tracker invariants split immutable Ultimate Goal/Acceptance Criteria from mutable active/completed/deferred/evolution sections (`skills/humanize/SKILL.md:153`-`165`).
- dependencies_and_callers: Depends on hydrated `{{HUMANIZE_RUNTIME_ROOT}}`, `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/validate-gen-plan-io.sh`, `scripts/ask-codex.sh`, `scripts/humanize.sh`, Codex CLI, and native Codex Stop hook (`skills/humanize/SKILL.md:12`-`20`, `61`-`110`, `206`-`213`). Monitor runtime behavior is validated by the assigned monitor test.
- edge_cases_or_failure_modes: Supports review-only mode via `--skip-impl`, optional plan immutability, local-only vs push-every-round behavior, methodology analysis opt-in, and Finalize/complete states (`skills/humanize/SKILL.md:67`-`89`, `193`-`198`). It explicitly warns against bypassing loop gates by editing state or running cancel commands (`skills/humanize/SKILL.md:169`-`173`).
- validation_or_tests: The skill documents exit codes for `ask-codex.sh` and `validate-gen-plan-io.sh` (`skills/humanize/SKILL.md:215`-`230`). Behavior is broadly covered by tests in this assignment: allowlist validators, monitor runtime, finalize prompt behavior, workflow graph tests, and BitLesson validation.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8 item sections above; each assigned item appears as exactly one `###` heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`