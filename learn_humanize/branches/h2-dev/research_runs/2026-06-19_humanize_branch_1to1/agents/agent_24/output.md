# agent_24 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-024 `directory` `skills/humanize-gen-plan`
- cursor: `[_]`
- core_role: Flow-skill wrapper for generating an implementation plan from a draft; the directory recursively contains only `skills/humanize-gen-plan/SKILL.md`, so its behavior is entirely defined there.
- algorithmic_behavior: Defines a staged plan-generation pipeline: validate IO, read draft, reject irrelevant drafts, analyze clarity/consistency/completeness/functionality, ask the user to resolve issues, confirm quantitative metrics, generate a structured plan, write it with an edit/write path, review and repair inconsistencies, then report plan path, AC count, and language state. See `skills/humanize-gen-plan/SKILL.md:19-47`.
- inputs_outputs_state: Inputs are `--input <draft.md>` and `--output <plan.md>` (`skills/humanize-gen-plan/SKILL.md:49-54`). Output is a plan with goal description, AC-style acceptance criteria, path boundaries, dependencies/milestones, and implementation notes (`skills/humanize-gen-plan/SKILL.md:55-94`). Runtime root is hydrated into `{{HUMANIZE_RUNTIME_ROOT}}` (`skills/humanize-gen-plan/SKILL.md:13-17`).
- gates_or_invariants: Starts with `scripts/validate-gen-plan-io.sh --input <draft> --output <plan>` and stops on validation failure (`skills/humanize-gen-plan/SKILL.md:21-24`). Exit codes distinguish missing input, empty input, missing output directory, existing output, permission failure, bad args, and missing template (`skills/humanize-gen-plan/SKILL.md:96-108`). Generated implementation code must not contain plan terminology (`skills/humanize-gen-plan/SKILL.md:92-94`).
- dependencies_and_callers: Depends on `scripts/validate-gen-plan-io.sh`, referenced directly in the flow (`skills/humanize-gen-plan/SKILL.md:21`). It is invoked as `/flow:humanize-gen-plan` or `/skill:humanize-gen-plan` (`skills/humanize-gen-plan/SKILL.md:109-124`), and is referenced by exploration templates as the plan-ready next step.
- edge_cases_or_failure_modes: Irrelevant drafts are explicitly stopped (`skills/humanize-gen-plan/SKILL.md:25-28`); unclear/incomplete/inconsistent drafts loop through user resolution (`skills/humanize-gen-plan/SKILL.md:29-32`); multiple-language output asks the user to unify language before success (`skills/humanize-gen-plan/SKILL.md:42-45`); existing output path is a validation failure.
- validation_or_tests: The skill itself is declarative; validation is delegated to `validate-gen-plan-io.sh` and the post-write review loop in the flow. No direct assigned test file targets this skill directory.
- skip_candidate: `no`

### H2_DEV-HZ-054 `file` `scripts/bitlesson-select.sh`
- cursor: `[_]`
- core_role: Runtime selector that chooses applicable BitLesson IDs for a sub-task and related paths using either Codex or Claude, with strict output normalization.
- algorithmic_behavior: Parses `--task`, `--paths`, and `--bitlesson-file` (`scripts/bitlesson-select.sh:23-62`), validates required inputs and file content (`scripts/bitlesson-select.sh:64-91`), short-circuits to `LESSON_IDS: NONE` when no lesson headings exist (`scripts/bitlesson-select.sh:93-97`), resolves the BitLesson project root (`scripts/bitlesson-select.sh:103-111`), loads merged config and model/provider settings (`scripts/bitlesson-select.sh:113-125`), builds a constrained selector prompt (`scripts/bitlesson-select.sh:147-179`), runs the selector, then extracts exactly `LESSON_IDS` and `RATIONALE` lines (`scripts/bitlesson-select.sh:246-271`).
- inputs_outputs_state: Inputs are CLI task string, comma-separated paths, and a BitLesson markdown file. Outputs are exactly two stdout lines: selected IDs or `NONE`, and one rationale sentence (`scripts/bitlesson-select.sh:23-31`, `scripts/bitlesson-select.sh:270-271`). It reads config from plugin/user/project layers via `load_merged_config` and `get_config_value`; it does not mutate repository state.
- gates_or_invariants: Fails on unknown args, missing/empty task, missing paths, missing bitlesson file, or whitespace-only file (`scripts/bitlesson-select.sh:56-91`). Selector prompt forbids tools, shell, browser, MCP, and repo inspection, limiting the model to provided prompt content (`scripts/bitlesson-select.sh:165-177`). Output format is enforced and raw unexpected output becomes a hard error (`scripts/bitlesson-select.sh:262-268`).
- dependencies_and_callers: Sources `config-loader.sh`, `model-router.sh`, `project-root.sh`, `portable-timeout.sh`, and `loop-common.sh` (`scripts/bitlesson-select.sh:9-21`). Model routing comes from `detect_provider` and `check_provider_dependency` in `scripts/lib/model-router.sh:10-60`; config merge comes from `scripts/lib/config-loader.sh:63-155`; timeout behavior comes from `scripts/portable-timeout.sh:38-76`.
- edge_cases_or_failure_modes: `provider_mode=codex-only` remaps Claude models to the Codex fallback (`scripts/bitlesson-select.sh:127-130`). Missing provider binary falls back to default Codex and then requires Codex dependency success (`scripts/bitlesson-select.sh:136-140`). Codex CLI capability probing avoids assuming `--disable`, `--skip-git-repo-check`, or `--ephemeral` support (`scripts/bitlesson-select.sh:191-208`). Timeout exits 124 with a specific message (`scripts/bitlesson-select.sh:231-234`); unsupported providers fail in `run_selector` (`scripts/bitlesson-select.sh:219-225`).
- validation_or_tests: No assigned direct test file names `bitlesson-select.sh`, but its behavior is guarded by script-level validation and stable-output checks. The selector’s config/model dependencies have reusable helpers used elsewhere in RLCR scripts.
- skip_candidate: `no`

### H2_DEV-HZ-084 `file` `tests/test-agent-teams.sh`
- cursor: `[_]`
- core_role: Executable shell specification for RLCR `--agent-teams` configuration, state persistence, prompt injection, and stop-hook continuation behavior.
- algorithmic_behavior: Creates temporary git projects and plan files, runs `scripts/setup-rlcr-loop.sh` with and without `--agent-teams`, validates `agent_teams` in `state.md`, tests `parse_state_file`, verifies prompt templates, and exercises `hooks/loop-codex-stop-hook.sh` with mocked Codex output. Top-level coverage is declared in `tests/test-agent-teams.sh:3-12`.
- inputs_outputs_state: Inputs are CLI flags, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, project `.humanize/config.json`, generated loop state, mock Codex responses, and hook JSON. Outputs are pass/fail test events via `test-helpers.sh`, generated `.humanize/rlcr/.../state.md`, `round-N-prompt.md`, summaries, contracts, and mocked binaries under temp dirs.
- gates_or_invariants: `--agent-teams` must fail unless `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`; bad values like `0`, `false`, `yes`, and `true` are rejected (`tests/test-agent-teams.sh:54-79`). Default state must be `agent_teams: false` without the flag (`tests/test-agent-teams.sh:123-153`). Project config can enable teams when the env gate is satisfied (`tests/test-agent-teams.sh:155-186`). The underlying setup gate is implemented in `scripts/setup-rlcr-loop.sh:390-401`, and state write occurs at `scripts/setup-rlcr-loop.sh:885-912`.
- dependencies_and_callers: Sources `tests/test-helpers.sh` and `hooks/lib/loop-common.sh` (`tests/test-agent-teams.sh:17-22`). Uses `parse_state_file` and `find_active_loop`, whose `agent_teams` parsing/defaulting is in `hooks/lib/loop-common.sh:442-522`, and whose active-loop lookup is in `hooks/lib/loop-common.sh:332-425`. Stop-hook tests call `hooks/loop-codex-stop-hook.sh` (`tests/test-agent-teams.sh:427-428`).
- edge_cases_or_failure_modes: Missing template files become skips rather than hard failures for template-existence checks (`tests/test-agent-teams.sh:330-388`). Implementation-phase continuation must include Agent Teams guidance only when enabled (`tests/test-agent-teams.sh:592-701`). Drift recovery must preserve Agent Teams guidance (`tests/test-agent-teams.sh:631-669`). Review phase must not include Agent Teams continuation even when state has `agent_teams: true` (`tests/test-agent-teams.sh:703-734`).
- validation_or_tests: This file is itself validation. It uses realistic state frontmatter, plan backup, goal tracker, round summary, and round contract setup (`tests/test-agent-teams.sh:431-540`), plus mock Codex exec/review scripts (`tests/test-agent-teams.sh:542-590`) to validate phase-specific prompt generation.
- skip_candidate: `no`

### H2_DEV-HZ-114 `file` `tests/test-monitor-e2e-sigint.sh`
- cursor: `[_]`
- core_role: Thin executable test shard for monitor SIGINT behavior in bash and zsh shells.
- algorithmic_behavior: Sources the real monitor E2E test implementation, then invokes `monitor_test_bash_sigint` and `monitor_test_zsh_sigint` (`tests/test-monitor-e2e-sigint.sh:4-13`). It prints a summary and exits nonzero when any sourced test increments `TESTS_FAILED` (`tests/test-monitor-e2e-sigint.sh:15-21`).
- inputs_outputs_state: Inputs come from `tests/test-monitor-e2e-real.sh`; this shard itself has no CLI args. Outputs are human-readable pass/fail counts and process exit status.
- gates_or_invariants: Requires `set -euo pipefail` (`tests/test-monitor-e2e-sigint.sh:3`) and treats `TESTS_FAILED == 0` as the only success condition (`tests/test-monitor-e2e-sigint.sh:19-21`).
- dependencies_and_callers: Depends entirely on `tests/test-monitor-e2e-real.sh` (`tests/test-monitor-e2e-sigint.sh:5`). The sourced functions create fake RLCR loop dirs, run the real `_humanize_monitor_codex`, send SIGINT, check cleanup/exit behavior, and ensure no glob errors; relevant definitions are in `tests/test-monitor-e2e-real.sh:377-548` for bash and `tests/test-monitor-e2e-real.sh:555-683` for zsh.
- edge_cases_or_failure_modes: Bash SIGINT delivery may require fallback SIGTERM and still pass if the monitor ran and terminated cleanly enough (`tests/test-monitor-e2e-real.sh:493-518`). zsh absence is skipped in the real test (`tests/test-monitor-e2e-real.sh:555-563`). Both shell paths fail on startup failure, no cleanup indication, or glob errors.
- validation_or_tests: The shard validates only by delegating to the sourced real test functions. It is useful for parallel test splitting but does not itself encode the monitor algorithm.
- skip_candidate: `yes: wrapper shard only; the core SIGINT monitor assertions live in the sourced real test file`

### H2_DEV-HZ-144 `file` `tests/workflow-gap-coverage.test.ts`
- cursor: `[_]`
- core_role: Vitest regression suite for workflow scheduler semantics, manifest enforcement, artifact expectation matching, persistence/recovery, and RLCR cartridge routing.
- algorithmic_behavior: Constructs `WorkflowCoordinator` and `AgentRunCoordinator` instances with fake/slow backends, loads inline `<h2-workflow>` HTML or `flow/rlcr/workflow.html`, starts runs, delivers artifacts/events, and asserts terminal run states plus event traces. Imports target `src/hub/runs.js`, `src/service.js`, `src/workflows/coordinator.js`, and `src/workflows/storage.js` (`tests/workflow-gap-coverage.test.ts:5-12`).
- inputs_outputs_state: Inputs are inline workflow HTML, fake backend results, delivered artifacts/events, temp storage dirs, and deterministic ID/clock helpers. Outputs are workflow run records, event streams, loop iteration counters, persisted snapshots, and Vitest assertions. Helpers are defined at `tests/workflow-gap-coverage.test.ts:802-929`.
- gates_or_invariants: Nested loops must reset by compiled `bodyVertexIds` and produce four inner iterations for a 2x2 loop (`tests/workflow-gap-coverage.test.ts:19-55`), matching implementation in `src/workflows/coordinator.ts:1160-1178`. `event.<type>` is valid for await-entry predicates after entry and rejected in branch/loop expressions (`tests/workflow-gap-coverage.test.ts:57-110`), with parser errors in `src/workflows/parser.ts:317` and `src/workflows/parser.ts:507`. Manifest declarations are mandatory for agent/script/check vertices (`tests/workflow-gap-coverage.test.ts:112-155`), matching `src/workflows/coordinator.ts:202-250`.
- dependencies_and_callers: Exercises `AgentRunCoordinator`, `HumanizeService`, `WorkflowCoordinator`, and `FileWorkflowStore`. RLCR tests depend on `flow/rlcr/workflow.html`, whose code-review and implementation loops are declared at `flow/rlcr/workflow.html:62-102`, with finalizer and stopped sink at `flow/rlcr/workflow.html:103-114`.
- edge_cases_or_failure_modes: Artifact expectations reject wrong producer and wrong schema, accept matching producer/schema, and record duplicate delivery while still allowing later valid producer delivery (`tests/workflow-gap-coverage.test.ts:157-314`), matching expectation failure handling in `src/workflows/coordinator.ts:867-905`. An interrupted running agent can hand off terminal waiting to an `h2-message` continuation (`tests/workflow-gap-coverage.test.ts:316-367`), implemented at `src/workflows/coordinator.ts:840-853`. Restore fails unrecoverable agent vertices whose managed run is missing (`tests/workflow-gap-coverage.test.ts:369-413`, `src/workflows/coordinator.ts:1362-1400`). Human wait snapshots must persist and restore (`tests/workflow-gap-coverage.test.ts:415-455`, `src/workflows/coordinator.ts:1287-1302`).
- validation_or_tests: This file is the validation. It covers RLCR complete route to code-review then finalizer without falling through to stopped (`tests/workflow-gap-coverage.test.ts:457-572`), code-review revise returning to implementation before finalizing (`tests/workflow-gap-coverage.test.ts:574-713`), and stop route reaching stopped without finalizer (`tests/workflow-gap-coverage.test.ts:715-800`).
- skip_candidate: `no`

### H2_DEV-HZ-174 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: Prompt block enforcing the goal-tracker write gate: shell writes to `goal-tracker.md` are forbidden because they bypass validation hooks.
- algorithmic_behavior: Emits a blocking instruction telling the agent not to modify `goal-tracker.md` through Bash and to use the Write/Edit tool at `{{CORRECT_PATH}}` instead (`prompt-template/block/goal-tracker-bash-write.md:1-8`).
- inputs_outputs_state: Input is the hydrated `{{CORRECT_PATH}}` placeholder. Output is a small prompt fragment used by hook/blocking logic to redirect state mutation through validated tool paths. It does not maintain state itself.
- gates_or_invariants: Preserves the invariant that goal tracker mutations must pass validation hooks; specifically names `cat`, `echo`, `sed`, and `awk` as disallowed bypass paths (`prompt-template/block/goal-tracker-bash-write.md:3-8`).
- dependencies_and_callers: Depends on whatever hook/template loader inserts `{{CORRECT_PATH}}`; likely used by write/bash guard flows around RLCR goal tracker updates. No direct caller was required to inspect to understand this assigned file’s contract.
- edge_cases_or_failure_modes: The template is only effective if the caller correctly detects Bash write attempts and hydrates the correct path. If a mutation path is not recognized as Bash-based, this block will not fire.
- validation_or_tests: No direct assigned test targets this block, but it participates in the broader RLCR invariant that goal tracker state should be updated through managed mechanisms rather than arbitrary shell writes.
- skip_candidate: `no`

### H2_DEV-HZ-204 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase prompt contract that routes completed RLCR implementation into a simplification/refactor pass after Codex review has passed.
- algorithmic_behavior: Tells Claude it is in Finalize Phase, invokes the `code-simplifier:code-simplifier` subagent via Task, constrains changes to functionality-equivalent simplification, and requires completion of blocking/mainline tasks, commit, and summary file write (`prompt-template/claude/finalize-phase-prompt.md:1-52`).
- inputs_outputs_state: Inputs are hydrated `{{BASE_BRANCH}}`, `{{START_BRANCH}}`, `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}` placeholders (`prompt-template/claude/finalize-phase-prompt.md:29-46`). Outputs are simplified code changes, a commit, and a finalize summary containing files changed, simplifications, test confirmation, and refactoring notes (`prompt-template/claude/finalize-phase-prompt.md:41-52`).
- gates_or_invariants: Non-negotiable invariants: do not change functionality, do not fail existing tests, do not introduce bugs, and only perform cleanup/simplification (`prompt-template/claude/finalize-phase-prompt.md:16-24`). Mainline and blocking tasks must be completed; queued tasks may remain only when documented as non-blocking (`prompt-template/claude/finalize-phase-prompt.md:41-45`).
- dependencies_and_callers: Depends on the Task tool and `code-simplifier:code-simplifier` agent (`prompt-template/claude/finalize-phase-prompt.md:7-14`). References the plan and goal tracker as authoritative context (`prompt-template/claude/finalize-phase-prompt.md:36-40`). This prompt aligns with RLCR finalization after review success.
- edge_cases_or_failure_modes: Refactor may not proceed if tests fail or if simplification would alter behavior. Queued tasks are allowed only as explicit non-blocking follow-up, preventing hidden incomplete blocking work.
- validation_or_tests: Validation is prompt-level: requires running tests and recording that tests still pass in the summary (`prompt-template/claude/finalize-phase-prompt.md:20-22`, `prompt-template/claude/finalize-phase-prompt.md:48-52`). No direct assigned test inspects this file.
- skip_candidate: `no`

### H2_DEV-HZ-234 `file` `skills/humanize-rlcr/SKILL.md`
- cursor: `[_]`
- core_role: Codex flow entrypoint for RLCR, delegating setup and phase transitions to Humanize native Stop hooks rather than ad-hoc Codex commands.
- algorithmic_behavior: Starts with `setup-rlcr-loop.sh $ARGUMENTS`, stops on setup failure, then repeats rounds by reading generated round/finalize prompts, implementing changes, committing, writing the required summary, exiting normally, and letting the Stop hook determine continuation or completion (`skills/humanize-rlcr/SKILL.md:24-49`).
- inputs_outputs_state: Inputs are setup arguments including plan path, max iterations, model/effort, timeout, base branch, review cadence, skip/review-only flags, push policy, question answering, agent teams, yolo, and quiz skipping (`skills/humanize-rlcr/SKILL.md:76-97`). State and prompt files live under `.humanize/rlcr/<timestamp>/`, including `round-<N>-prompt.md`, `round-<N>-summary.md`, and `finalize-summary.md` (`skills/humanize-rlcr/SKILL.md:36-46`).
- gates_or_invariants: Enforces native Stop-hook control over schema/state validation, branch consistency, plan integrity, incomplete Task/Todo blocking, git-clean exit, optional push-every-round blocking, summary presence, max iterations, full alignment, strict completion/stop markers, review-phase transition marker, code-review gating on priority findings, Codex review failure blocking, and open-question handling (`skills/humanize-rlcr/SKILL.md:50-68`).
- dependencies_and_callers: Depends on hydrated `{{HUMANIZE_RUNTIME_ROOT}}`, `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, generated loop files, and native Humanize Stop hooks (`skills/humanize-rlcr/SKILL.md:14-23`, `skills/humanize-rlcr/SKILL.md:30-32`, `skills/humanize-rlcr/SKILL.md:109-113`). Review phase uses `codex review` with `gpt-5.5:high` (`skills/humanize-rlcr/SKILL.md:97`).
- edge_cases_or_failure_modes: Critical rules forbid manual edits to state files, manual completion after a blocked hook, ad-hoc `codex exec`/`codex review` replacing hook-managed transitions, and ignoring generated prompt/review files as source of truth (`skills/humanize-rlcr/SKILL.md:69-75`). Setup nonzero exit is an immediate stop (`skills/humanize-rlcr/SKILL.md:28-35`).
- validation_or_tests: Covered indirectly by `tests/test-agent-teams.sh`, which verifies RLCR setup state and stop-hook prompt behavior, and by `tests/workflow-gap-coverage.test.ts`, which validates the newer workflow-cartridge RLCR loop routing through implementation, review, code review, finalizer, and stop sink.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: H2_DEV-HZ-024, H2_DEV-HZ-054, H2_DEV-HZ-084, H2_DEV-HZ-114, H2_DEV-HZ-144, H2_DEV-HZ-174, H2_DEV-HZ-204, H2_DEV-HZ-234
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`