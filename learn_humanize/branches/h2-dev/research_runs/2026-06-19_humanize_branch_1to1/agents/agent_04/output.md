# agent_04 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 9
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-004 `directory` `config`
- cursor: `[_]`
- core_role: Repository-wide configuration and Codex hook registration defaults. The directory has two files only: `config/default_config.json` and `config/codex-hooks.json`.
- algorithmic_behavior: `default_config.json` seeds model/runtime defaults: `codex_model: gpt-5.5`, `codex_effort: high`, `bitlesson_model: haiku`, `agent_teams: false`, empty `alternative_plan_language`, and `gen_plan_mode: discussion` at `config/default_config.json:2-7`. These are merged by `scripts/lib/config-loader.sh`, which requires the default config and overlays user/project config in order at `scripts/lib/config-loader.sh:77-132`. `codex-hooks.json` declares the native Codex `Stop` hook command, timeout, and status message at `config/codex-hooks.json:3-16`.
- inputs_outputs_state: Inputs are static JSON defaults and the `{{HUMANIZE_RUNTIME_ROOT}}` placeholder. Outputs are merged runtime config values consumed by shell libraries and installed Codex `hooks.json` entries. The state effect is indirect: defaults determine fields placed into RLCR state files and Codex review settings.
- gates_or_invariants: Default config must exist and be a JSON object; `load_merged_config` treats missing/malformed required default config as fatal at `scripts/lib/config-loader.sh:40-55`. `loop-common.sh` validates configured Codex model/effort, requiring safe model token syntax and effort in `xhigh|high|medium|low` at `hooks/lib/loop-common.sh:211-230`. Hook install requires the hook template file and native Codex hooks support at `scripts/install-codex-hooks.sh:72-117`.
- dependencies_and_callers: `loop-common.sh` loads merged config for `DEFAULT_BITLESSON_MODEL`, `DEFAULT_CODEX_MODEL`, `DEFAULT_CODEX_EFFORT`, and `DEFAULT_AGENT_TEAMS` at `hooks/lib/loop-common.sh:189-236`. `scripts/install-codex-hooks.sh` reads `config/codex-hooks.json` and substitutes the runtime root at `scripts/install-codex-hooks.sh:14` and `scripts/install-codex-hooks.sh:128-153`. `scripts/install-skill.sh` checks and reads the default config during Codex install at `scripts/install-skill.sh:297-328`.
- edge_cases_or_failure_modes: Missing `jq` blocks config loading at `scripts/lib/config-loader.sh:17-21`. Malformed optional user/project config is ignored with warnings, but malformed required default config is fatal. Bad `codex_model` or `codex_effort` in merged config is ignored with warnings and falls back to caller preset or defaults.
- validation_or_tests: `tests/test-unified-codex-config.sh:64-80` asserts default Codex keys and absence of old reviewer keys, then confirms project overrides at `tests/test-unified-codex-config.sh:102-122`. `tests/test-config-merge.sh:43-63` checks default-only merge values for `bitlesson_model`, `agent_teams`, and `gen_plan_mode`.
- skip_candidate: `no`

### H2_DEV-HZ-034 `file` `commands/gen-idea.md`
- cursor: `[_]`
- core_role: Command workflow specification for directed-swarm idea generation, producing a draft and companion directions JSON for downstream `/humanize:gen-plan` or `/humanize:explore-idea`.
- algorithmic_behavior: Defines a strict five-phase pipeline: parse input, validate IO, generate orthogonal directions, run parallel Explore subagents, synthesize/write output at `commands/gen-idea.md:25-33`. It enforces draft-only behavior and read-only exploration at `commands/gen-idea.md:19-23`. Phase 2 generates exactly `N` orthogonal directions, with retry/degradation rules at `commands/gen-idea.md:76-107`. Phase 3 dispatches all directions in one Task-tool message and requires structured fields from each subagent at `commands/gen-idea.md:111-145`. Phase 4 picks a primary direction by evidence density, fit, surface area, and confidence, then writes draft plus JSON at `commands/gen-idea.md:151-249`.
- inputs_outputs_state: Inputs are `$ARGUMENTS`, either inline idea text or a `.md` file, optional `--n`, and optional `--output`. Phase 1 parses stdout from `scripts/validate-gen-idea-io.sh`, including `INPUT_MODE`, `OUTPUT_FILE`, `DIRECTIONS_JSON_FILE`, `TEMPLATE_FILE`, and `N` at `commands/gen-idea.md:48-72`. Outputs are exactly one markdown draft and one `.directions.json` companion, plus a user report at `commands/gen-idea.md:251-262`.
- gates_or_invariants: Hard no-implementation/no-commit invariant at `commands/gen-idea.md:19-21`. No partial output on Phase 1-3 errors at `commands/gen-idea.md:266-273`. At least two directions/proposals must survive degradation at `commands/gen-idea.md:101-105` and `commands/gen-idea.md:138-143`. Companion JSON must pass `validate-directions-json.sh`; on failure both files are deleted at `commands/gen-idea.md:244-249`.
- dependencies_and_callers: Allowed tools are constrained to validation scripts, `Read`, `Glob`, `Grep`, `Task`, `Write`, and cleanup `rm` at `commands/gen-idea.md:4-12`. It depends on `scripts/validate-gen-idea-io.sh`, which emits `DIRECTIONS_JSON_FILE` and inline idea sentinel blocks at `scripts/validate-gen-idea-io.sh:203-217`, and on `scripts/validate-directions-json.sh`, which enforces schema version 1 at `scripts/validate-directions-json.sh:38-121`.
- edge_cases_or_failure_modes: Validation exit codes 1-8 map to clear stop messages at `commands/gen-idea.md:55-64`. Warnings before `VALIDATION_SUCCESS` must be surfaced but are non-fatal at `commands/gen-idea.md:66`. Direction generation may proceed with reduced count only after one retry and only if at least two survive. Subagent responses missing required fields are dropped. Fabricated repo evidence is forbidden; sentinel `exploratory, no concrete precedent` must be preserved at `commands/gen-idea.md:123-136` and `commands/gen-idea.md:271`.
- validation_or_tests: `tests/test-gen-idea-dual-write.sh` checks that validation emits `DIRECTIONS_JSON_FILE` and that this command references it. `tests/test-validate-gen-idea-io.sh` exercises companion derivation/collision handling. `tests/test-directions-json-schema.sh` validates the JSON schema contract enforced by `validate-directions-json.sh`.
- skip_candidate: `no`

### H2_DEV-HZ-064 `file` `scripts/statusline.sh`
- cursor: `[_]`
- core_role: Runtime statusline renderer with an RLCR status field. It is not a state mutator; it mirrors active-loop lookup rules to present session-aware RLCR state in the CLI status line.
- algorithmic_behavior: Reads JSON from stdin at `scripts/statusline.sh:19`, extracts fields with `jq` via `get_value` at `scripts/statusline.sh:21-24`, and formats model/context/cost/session/fast/RLCR data. `_resolve_rlcr_display` maps loop files to display states: methodology analysis, finalize, active, terminal `*-state.md`, or off at `scripts/statusline.sh:36-63`. `get_rlcr_status` mirrors `find_active_loop` by scanning `.humanize/rlcr`, honoring optional `session_id`, preferring session-aware state files, and avoiding zombie loops at `scripts/statusline.sh:65-155`.
- inputs_outputs_state: Input is Claude Code status JSON containing model, cwd, session id, transcript path, cost, duration, lines, and context usage. Output is one or more ANSI-colored statusline rows emitted to stdout at `scripts/statusline.sh:390-403`. No repository state is written.
- gates_or_invariants: If `.humanize/rlcr` does not exist, RLCR status is `Off` at `scripts/statusline.sh:71-74` and `scripts/statusline.sh:257-262`. If no session id is provided and no session-aware files exist, only the newest directory is considered for zombie-loop protection at `scripts/statusline.sh:82-92`. Terminal states are represented from `*-state.md` filenames, capitalizing the reason at `scripts/statusline.sh:47-58`.
- dependencies_and_callers: Depends on `jq`, `git`, `stty`/`tput`, `grep`, `awk`, and local `.humanize/rlcr` state files. It explicitly comments that its RLCR lookup mirrors `hooks/lib/loop-common.sh` at `scripts/statusline.sh:65-66`. It reads transcript JSONL and sessions index for custom session title resolution at `scripts/statusline.sh:175-218`.
- edge_cases_or_failure_modes: Missing or unreadable transcript falls back to `$HOME/.claude/projects/<project>/<session>.jsonl` at `scripts/statusline.sh:188-191`. Missing title falls back to `sessions-index.json`, then full session id. Bad/missing terminal width falls back to 80 columns at `scripts/statusline.sh:379-382`. Without `jq`, many fields become empty; the script generally formats defaults like `?`, `0`, or `Off`.
- validation_or_tests: No assigned direct test file targets statusline, but its critical RLCR lookup behavior is coupled to `find_active_loop` semantics tested in `tests/test-stop-hook-bg-allow.sh`, especially exact-session precedence and marker isolation at `tests/test-stop-hook-bg-allow.sh:742-791` and `tests/test-stop-hook-bg-allow.sh:1052-1094`.
- skip_candidate: `no`

### H2_DEV-HZ-094 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable specification for the cancel-signal authorization gate that permits only the intentional `state.md`/phase-state to `cancel-state.md` transition during an active RLCR loop.
- algorithmic_behavior: Creates an isolated loop with `state.md` and `CLAUDE_PROJECT_DIR`, then exercises `hooks/loop-bash-validator.sh` through JSON hook input at `tests/test-cancel-signal-file.sh:42-74`. It verifies successful cancel only when `.cancel-requested` exists in the active loop and the command is exactly an allowed `mv` shape at `tests/test-cancel-signal-file.sh:80-115`. It also invokes `is_cancel_authorized` directly for helper-level behavior at `tests/test-cancel-signal-file.sh:1251-1419`.
- inputs_outputs_state: Inputs are synthetic Bash commands, active loop state files, and optional `.cancel-requested` signal files. Expected outputs are validator exit 0 for authorized cancel or exit 2 for blocked mutations. State is temporary under `mktemp`; test helper creates/removes `.humanize/rlcr` for each scenario.
- gates_or_invariants: Requires the signal file in the active loop directory, not the parent, at `tests/test-cancel-signal-file.sh:197-217`. Allows only `mv <loop>/state.md <loop>/cancel-state.md` style, including `./` normalization and quoted paths. Blocks redirection, command chaining, wrappers, `sudo`, `env`, `command`, `sh -c`, `bash -c`, extra args, wrong destinations, hidden variables, and unrelated `state.md` paths across tests at `tests/test-cancel-signal-file.sh:239-1227`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `tests/test-cancel-signal-file.sh:32-33`, calls `hooks/loop-bash-validator.sh` at `tests/test-cancel-signal-file.sh:63-74`, and directly calls `to_lower` plus `is_cancel_authorized`. Production behavior under test lives in `hooks/lib/loop-common.sh:1056-1255` and `hooks/loop-bash-validator.sh:263-296`, `hooks/loop-bash-validator.sh:428-486`.
- edge_cases_or_failure_modes: Covers no active loop allow path at `tests/test-cancel-signal-file.sh:1229-1248`. Covers symlinked prefix acceptance, but rejects source/destination symlink aliases to avoid moving state through an alias path at `tests/test-cancel-signal-file.sh:1345-1419`. It also blocks newline, `$()`, backtick, `${IFS}`, and redirection-prefix attempts.
- validation_or_tests: This file is itself the validation asset. It exits with the failed-test count at `tests/test-cancel-signal-file.sh:1425-1433`. I did not execute it because the branch export is read-only and the sandbox forbids writes/approvals.
- skip_candidate: `no`

### H2_DEV-HZ-124 `file` `tests/test-stop-hook-bg-allow.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR stop hook’s background-task short-circuit, marker parking, session isolation, transcript parsing, and liveness-probe behavior.
- algorithmic_behavior: Builds minimal git-backed RLCR fixtures that satisfy pre-Codex gates at `tests/test-stop-hook-bg-allow.sh:96-178`, emits JSONL transcript events for async Agent/Bash launches and completions at `tests/test-stop-hook-bg-allow.sh:186-262`, invokes `hooks/loop-codex-stop-hook.sh` or `scripts/rlcr-stop-gate.sh`, and asserts either Codex reached or JSON `systemMessage` short-circuit at `tests/test-stop-hook-bg-allow.sh:271-339`.
- inputs_outputs_state: Inputs are synthetic repositories, hook input JSON with `transcript_path` and optional `session_id`, transcript JSONL, mocked `codex`, mocked `lsof`, and loop state files. Outputs are hook exit status, optional JSON `systemMessage`, Codex invocation marker, and marker/state file preservation or cleanup.
- gates_or_invariants: Pending background work must exit 0 with `systemMessage`, no Codex, and unchanged state at `tests/test-stop-hook-bg-allow.sh:291-329`. Missing transcript fails closed to normal Codex flow at `tests/test-stop-hook-bg-allow.sh:434-454`. No active loop exits 0 without Codex or message at `tests/test-stop-hook-bg-allow.sh:456-474`. Cross-session parked loops preserve marker and stored `session_id` at `tests/test-stop-hook-bg-allow.sh:606-716` and `tests/test-stop-hook-bg-allow.sh:845-899`. Same-session verified completion removes stale marker at `tests/test-stop-hook-bg-allow.sh:793-843`.
- dependencies_and_callers: Exercises `hooks/loop-codex-stop-hook.sh`, which delegates background guards before normal gate logic at `hooks/loop-codex-stop-hook.sh:61-83`. It also exercises `hooks/lib/loop-bg-tasks.sh`, where `list_pending_background_task_ids` computes launched minus completed tasks and prunes dead tasks at `hooks/lib/loop-bg-tasks.sh:156-265`, and `handle_bg_task_short_circuit` implements ambiguous caller, cross-session guard, short-circuit, and stale-marker cleanup at `hooks/lib/loop-bg-tasks.sh:299-435`.
- edge_cases_or_failure_modes: Tests tilde transcript paths at `tests/test-stop-hook-bg-allow.sh:520-604`; exact-session loop precedence over newer foreign marker at `tests/test-stop-hook-bg-allow.sh:742-791`; SDK and legacy completion formats at `tests/test-stop-hook-bg-allow.sh:901-953`; missing/malformed transcripts preserving markers at `tests/test-stop-hook-bg-allow.sh:955-1203`; pre-loop transcript launches filtered by loop start timestamp and timezone conversion at `tests/test-stop-hook-bg-allow.sh:1205-1310`; wrapper behavior with missing session id at `tests/test-stop-hook-bg-allow.sh:1312-1408`; liveness positive/negative via `lsof` at `tests/test-stop-hook-bg-allow.sh:1410-1459`.
- validation_or_tests: This file is itself the validation suite and ends with `print_test_summary`/exit status at `tests/test-stop-hook-bg-allow.sh:1461-1462`. I did not execute it because it creates temp repos/files and the current branch export is read-only.
- skip_candidate: `no`

### H2_DEV-HZ-154 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared RLCR hook library containing core state-machine primitives, config defaults, loop discovery, state parsing/updating, review issue detection, validator block messages, cancel authorization, git-state protection, and terminal state transitions.
- algorithmic_behavior: Establishes constants for state fields, review markers, exit reasons, mainline verdicts, and drift status at `hooks/lib/loop-common.sh:20-76`. Validates hook JSON and rejects null bytes/invalid UTF-8/deep nesting at `hooks/lib/loop-common.sh:82-165`. Loads config and templates at `hooks/lib/loop-common.sh:171-248`. Resolves active/any state files and finds active loops with session-aware and marker fallback behavior at `hooks/lib/loop-common.sh:264-425`. Parses state tolerantly or strictly at `hooks/lib/loop-common.sh:430-600`. Detects Codex review issues by scanning recent log lines for `[P?]` markers at `hooks/lib/loop-common.sh:719-788`.
- inputs_outputs_state: Inputs are hook JSON, `.humanize/rlcr/<timestamp>` directories, YAML frontmatter state files, Codex logs, command strings, and repo config. Outputs are shell variables (`STATE_*`, defaults), block messages, parsed verdicts, boolean returns, review-result files, updated state frontmatter, or terminal `*-state.md` files. `upsert_state_fields` writes frontmatter updates at `hooks/lib/loop-common.sh:664-717`; `end_loop` renames active state files at `hooks/lib/loop-common.sh:1538-1571`.
- gates_or_invariants: Strict parser rejects missing state file, missing YAML separators, missing required fields, non-numeric rounds/max iterations, and invalid `review_started` at `hooks/lib/loop-common.sh:524-600`. `find_active_loop` protects against stale zombie loops and only uses marker fallback when explicitly requested at `hooks/lib/loop-common.sh:308-425`. Cancel authorization requires `.cancel-requested`, safe `mv` shape, no shell operators/substitution/newlines, exact source/destination paths, prefix-only canonicalization, and non-symlink source at `hooks/lib/loop-common.sh:1038-1255`. `.humanize` state must not be force-added/tracked at `hooks/lib/loop-common.sh:1263-1471`.
- dependencies_and_callers: Used by read/write/edit/bash/plan validators, stop hook, setup and cancel scripts per header at `hooks/lib/loop-common.sh:5-13`. Sources `project-root.sh`, `scripts/lib/config-loader.sh`, `template-loader.sh`, and finally `loop-bg-tasks.sh` at `hooks/lib/loop-common.sh:171-240` and `hooks/lib/loop-common.sh:1573-1583`.
- edge_cases_or_failure_modes: Invalid/missing config can warn or fall back; invalid configured Codex model/effort is ignored at `hooks/lib/loop-common.sh:211-230`. Review detection treats missing/empty log as hard error return 2, but no markers as return 1. `is_cancel_authorized` returns distinct codes for missing signal, security violation, mixed quote styles, trailing spaces, invalid structure, and symlink source at `hooks/lib/loop-common.sh:1038-1047`.
- validation_or_tests: Covered by `tests/test-cancel-signal-file.sh` for cancel authorization, `tests/test-stop-hook-bg-allow.sh` for `find_active_loop` and background helpers, plus config tests for default loading. I did not execute tests under the read-only sandbox.
- skip_candidate: `no`

### H2_DEV-HZ-184 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: Block template used by validators when a worker/assistant attempts to write `round-*-prompt.md`, preserving instruction-file immutability in the RLCR loop.
- algorithmic_behavior: Provides a user-facing refusal message: prompt files are instructions from Codex to Claude and must be read, not modified; results belong in the summary file at `prompt-template/block/prompt-file-write.md:1-12`.
- inputs_outputs_state: Input is a validator request to render a block message. Output is markdown text. No state is modified.
- gates_or_invariants: Enforces the invariant that prompt files are not writable by the agent receiving those prompts. If the prompt has errors, the allowed path is documenting that in the summary file, not editing the prompt at `prompt-template/block/prompt-file-write.md:7-12`.
- dependencies_and_callers: `prompt_write_blocked_message` loads this template with fallback text at `hooks/lib/loop-common.sh:855-862`. Bash/file validators use `command_modifies_file` and path checks to block prompt modifications; the shared template renderer supplies the final message.
- edge_cases_or_failure_modes: If the template is missing or template directory validation fails, `loop-common.sh` uses inline fallback text rather than failing the hook, per `hooks/lib/loop-common.sh:245-248` and `hooks/lib/loop-common.sh:856-862`.
- validation_or_tests: Related blocking behavior is covered indirectly by validator tests around prompt/round-file protections, and by the shared block-message path in `loop-common.sh`.
- skip_candidate: `no`

### H2_DEV-HZ-214 `file` `prompt-template/codex/code-review-phase.md`
- cursor: `[_]`
- core_role: Audit-only template documenting a Codex code-review phase invocation and the transition rule from review issues to remediation or finalization.
- algorithmic_behavior: Records review configuration placeholders (`BASE_BRANCH`, `REVIEW_ROUND`, `TIMESTAMP`) and specifies the code-review algorithm: run `codex review --base`, scan output for `[P0-9]` severity markers, return a fix prompt if issues exist, otherwise transition to Finalize Phase at `prompt-template/codex/code-review-phase.md:6-18`.
- inputs_outputs_state: Inputs are template variables and the Codex review output. Outputs are audit files listed at `prompt-template/codex/code-review-phase.md:30-36`: review prompt, review result, command invocation, stdout capture, and stderr/log capture. The template itself does not mutate state.
- gates_or_invariants: Contains a compatibility invariant: Codex 0.130.0 rejects prompt input when `--base` is used, so Humanize must not pass prompt stdin with `--base`; the file is audit-only at `prompt-template/codex/code-review-phase.md:3-4`. Expected issue marker format starts with `[P0]`, `[P1]`, etc. at `prompt-template/codex/code-review-phase.md:19-28`.
- dependencies_and_callers: Used by the Codex review phase in the RLCR stop hook path. The issue marker semantics align with `detect_review_issues`, which scans the tail of Codex logs for `[P?]` markers and writes `round-<n>-review-result.md` at `hooks/lib/loop-common.sh:719-788`.
- edge_cases_or_failure_modes: If Codex output includes severity markers outside the expected tail/first-character range, `detect_review_issues` may not classify them as actionable. If no markers are found, the intended transition is Finalize Phase. The template guards against a CLI-level failure mode around `codex review --base` and stdin.
- validation_or_tests: Covered by tests that exercise code-review phase parsing and by `detect_review_issues` behavior in `loop-common.sh`. This file is a template/audit contract, not an executable spec.
- skip_candidate: `no`

### H2_DEV-HZ-244 `file` `tests/robustness/test-plan-file-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR setup plan-file validation, especially content counting, size handling, path validity, and disappearance/readability edge cases.
- algorithmic_behavior: Creates an isolated git repo and mocked `codex`, then runs production `scripts/setup-rlcr-loop.sh` against candidate plan files through `test_plan_validation` at `tests/robustness/test-plan-file-robustness.sh:46-106`. The helper interprets known validation errors as rejection and loop creation/activation as acceptance. It also includes a local `count_content_lines` reference implementation for blank/comment filtering at `tests/robustness/test-plan-file-robustness.sh:129-169`.
- inputs_outputs_state: Inputs are generated `.md` plan files with varied content, size, line endings, binary/null bytes, comments, symlinks, directories, and missing paths. Outputs are pass/fail assertions and temporary `.humanize/rlcr` loop artifacts from setup. The production script under test validates and may create loop state in the temp repo.
- gates_or_invariants: Production validation requires relative safe paths, no spaces/shell metacharacters, no symlink file or symlink parent segment, existing readable file inside project, non-submodule location, correct git tracking policy, at least 5 lines, and at least 3 non-comment content lines at `scripts/setup-rlcr-loop.sh:460-681`.
- dependencies_and_callers: Depends on `scripts/setup-rlcr-loop.sh`, `tests/test-helpers.sh`, `git`, `jq`, and mocked `codex`. The tested production content loop excludes blanks, `#` comments, single-line HTML comments, and multiline HTML comment bodies at `scripts/setup-rlcr-loop.sh:633-669`.
- edge_cases_or_failure_modes: Positive cases include valid plan, minimum 3 content lines, 5KB standard file, rich markdown, large 1MB+ file, and mixed CRLF/LF at `tests/robustness/test-plan-file-robustness.sh:178-366`. Negative/edge cases include empty file, comments-only, whitespace-only, missing file, unreadable file, symlink, directory, null bytes, and disappearing file at `tests/robustness/test-plan-file-robustness.sh:300-550`. Some mid-file tests, such as binary/null byte and long-line handling, validate shell utility behavior rather than running the full production validator.
- validation_or_tests: This file is itself the validation suite and exits through `print_test_summary` at `tests/robustness/test-plan-file-robustness.sh:553-558`. I did not execute it because it writes temp repos/files and the branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `9 item evidence sections; each assigned item heading appears once above`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`