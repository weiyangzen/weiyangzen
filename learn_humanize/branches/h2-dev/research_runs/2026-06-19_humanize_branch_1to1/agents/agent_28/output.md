# agent_28 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-028 `file` `agents/bitlesson-selector.md`
- cursor: `[_]`
- core_role: Prompt/policy specification for selecting BitLesson lessons before executing a task or sub-task. It is a narrow routing agent contract, not executable code.
- algorithmic_behavior: The selector receives a sub-task, related paths, and configured BitLesson content, normally from `.humanize/bitlesson.md`, then emits only lesson IDs that directly match the task scope and failure mode. It explicitly favors precision over recall and returns `NONE` when nothing is relevant. See `agents/bitlesson-selector.md:10-17` and `agents/bitlesson-selector.md:26-30`.
- inputs_outputs_state: Inputs are current sub-task description, related file paths, and BitLesson content. Output is a stable two-line text shape: `LESSON_IDS: ...` and `RATIONALE: ...`, with no extra sections, at `agents/bitlesson-selector.md:32-41`. No persistent state is mutated by this prompt.
- gates_or_invariants: Deterministic output is required so later cross-agent review can validate the selection quickly. Weakly related lessons are excluded. Empty match result must be `NONE`.
- dependencies_and_callers: Runtime execution is delegated to `scripts/bitlesson-select.sh`, which routes to Codex CLI for `gpt-*` models or Claude CLI for Claude-family models based on configured `bitlesson_model`; the selection is consumed by Claude and may be reviewed by Codex later. See `agents/bitlesson-selector.md:19-24`.
- edge_cases_or_failure_modes: The main edge case is no applicable lessons, handled by the mandatory `NONE` output. Another failure mode is over-selection, which the prompt guards by preferring precision.
- validation_or_tests: Validation is prompt-level: downstream reviewers can compare the stable two-line output against the task/path inputs. No direct test file was assigned for this prompt.
- skip_candidate: `no`

### H2_DEV-HZ-058 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: Shell entrypoint for the `humanize` user function, especially terminal/web monitoring of RLCR and skill activity. It implements state parsing, goal-tracker parsing, git status summarization, live log following, terminal UI updates, and dashboard launch routing.
- algorithmic_behavior: Public helpers parse pipe-delimited arrays cross-shell, goal tracker issue/task/AC counts, special git states, and porcelain diff summaries. `humanize_parse_goal_tracker` extracts AC totals from `### Acceptance Criteria`, completed ACs from `### Completed and Verified`, active/completed/deferred task counts, issue counts, and the ultimate goal summary, then returns a pipe-delimited status tuple. See `scripts/humanize.sh:22-153`. `humanize_detect_git_state` classifies not-a-repo, permission error, rebase, merge, shallow, detached, or normal states at `scripts/humanize.sh:156-204`. `humanize_parse_git_status` counts modified/added/deleted/untracked files and insertion/deletion totals at `scripts/humanize.sh:206-252`.
- inputs_outputs_state: Inputs include local `.humanize/rlcr`, state files, goal trackers, cache logs under `${XDG_CACHE_HOME:-$HOME/.cache}/humanize`, terminal dimensions, git repository state, CLI flags, and dashboard project path. Outputs are terminal status UI, live tailed log output, shell return codes, and in web mode a child Python dashboard process. Monitor state is held in shell locals such as `current_session_dir`, `current_file`, `last_size`, `monitor_running`, and `resize_needed`.
- gates_or_invariants: `_humanize_monitor_codex` refuses to start unless `.humanize/rlcr` exists (`scripts/humanize.sh:272-277`) and a session directory is found (`scripts/humanize.sh:798-807`). Log selection is scoped to the current project/session cache and rejects inconsistent run/review ordering where a codex-run round is not strictly before the first codex-review round (`scripts/humanize.sh:291-375`). Terminal monitor requires a minimum height of status bar plus content and blocks in a resize loop until usable (`scripts/humanize.sh:661-688`, `scripts/humanize.sh:815-823`). Web monitor requires the project directory and `.humanize` to exist (`scripts/humanize.sh:1231-1239`), validates daemon launcher presence (`scripts/humanize.sh:1247-1261`), and fails if no dashboard port is available in `18000-18099` (`scripts/humanize.sh:1290-1317`).
- dependencies_and_callers: Sources `scripts/lib/monitor-common.sh`, `hooks/lib/loop-common.sh`, and later `scripts/lib/monitor-skill.sh` (`scripts/humanize.sh:6-16`, `scripts/humanize.sh:1408-1411`). Uses shared monitor helpers `monitor_find_latest_session`, `monitor_find_state_file`, and `monitor_get_file_size`. Web mode depends on `viz/server/app.py`, `viz/static`, `viz/server/requirements.txt`, project-local `.humanize/viz-venv`, and optionally `viz/scripts/viz-start.sh` for tmux daemon mode (`scripts/humanize.sh:1241-1261`). Main caller surface is `humanize monitor <rlcr|skill|codex|gemini|web>` at `scripts/humanize.sh:1350-1406`.
- edge_cases_or_failure_modes: Handles missing session/logs with centered wait messages, session deletion by switching to latest available session, log deletion by switching or waiting, log truncation/rotation by resetting search state, terminal resize races by setting a flag in signal handlers and processing at safe points, and loop directory deletion by graceful cleanup. See `scripts/humanize.sh:883-1008` and `scripts/humanize.sh:1022-1178`. Web mode handles stale requirements markers by retrying dependency refresh without touching the marker on failure (`scripts/humanize.sh:1277-1288`). Non-loopback remote bind without `--auth-token` emits a warning at `scripts/humanize.sh:1320-1322`.
- validation_or_tests: The assigned `tests/test-app-routes-live.sh` exercises web dashboard behavior that this wrapper launches. Assigned `tests/robustness/test-concurrent-state-robustness.sh` covers related state parsing/loop detection functions from shared libraries used by this monitor. `scripts/humanize.sh` itself exposes public parser helpers specifically described as callable for testing at `scripts/humanize.sh:18-20`.
- skip_candidate: `no`

### H2_DEV-HZ-088 `file` `tests/test-app-routes-live.sh`
- cursor: `[_]`
- core_role: Live Flask route-level executable specification for `viz/server/app.py`. It verifies dashboard API behavior with real Flask `test_client` requests, rather than static pattern checks.
- algorithmic_behavior: The script bootstraps or reuses a Python venv with Flask, flask-sock, watchdog, and PyYAML, then runs an embedded Python driver against isolated tmp projects and tmp cache roots. Setup is at `tests/test-app-routes-live.sh:47-75`; fixture helpers create `.humanize/rlcr` sessions and cache logs at `tests/test-app-routes-live.sh:89-118`.
- inputs_outputs_state: Inputs are generated project directories, seeded session state files, seeded goal trackers, cache log files, environment variables such as `XDG_CACHE_HOME` and `CLAUDE_PROJECT_DIR`, bind host/auth token settings, and HTTP requests. Outputs are PASS/FAIL counters and exit status 1 on any failed assertion (`tests/test-app-routes-live.sh:1207-1214`). State transitions under test include active/analyzing/finalizing sessions being renamed to `cancel-state.md`, active SSE sessions becoming terminal, cache log truncation, and parser cache invalidation.
- gates_or_invariants: Health is open in any mode; protected routes require bearer auth in remote mode; legacy project mutation endpoints return 410; missing cancel session ID returns 400; unknown sessions return 404; valid sessions return parsed dicts. These are covered at `tests/test-app-routes-live.sh:160-248` and `tests/test-app-routes-live.sh:248-307`. SSE basenames must match expected round log naming and invalid basenames are 400 (`tests/test-app-routes-live.sh:323-340`). CSRF rejects cross-origin writes but not GETs, supports wildcard bind by comparing against request Host, strips IPv6 brackets, and treats malformed Origin ports as 403 rather than 500 (`tests/test-app-routes-live.sh:471-622`).
- dependencies_and_callers: Imports `viz/server/app.py` directly after adding `viz/server` to `sys.path`, reloads it per test context, and mutates module globals `PROJECT_DIR`, `STATIC_DIR`, `BIND_HOST`, and `AUTH_TOKEN` for deterministic test isolation (`tests/test-app-routes-live.sh:137-155`). Uses `parser.parse_review_result` for verdict parsing at `tests/test-app-routes-live.sh:1078-1105`.
- edge_cases_or_failure_modes: Covers missing cache startup yielding `resync(missing)+eof`, log truncation yielding snapshot then `resync(truncated)` then snapshot then eof, concurrent active session enumeration across active/analyzing/finalizing/cancel states, cancellation of analyzing/finalizing but rejection of terminal sessions, stray `CLAUDE_PROJECT_DIR` not hijacking cancel target, malformed Origin port controlled rejection, path traversal/non-session directory rejection, false-positive `COMPLETE` prose avoidance, and missing methodology report 404. See `tests/test-app-routes-live.sh:374-470`, `tests/test-app-routes-live.sh:623-734`, and `tests/test-app-routes-live.sh:1024-1143`.
- validation_or_tests: This file is itself the validation surface. It asserts criterion parsing for AC/C prefixes, decimal IDs, dashless IDs, multi-ID Completed-Verified cells, and table-form criteria at `tests/test-app-routes-live.sh:735-958`; cache log preservation and latest-round methodology prompt slice at `tests/test-app-routes-live.sh:959-1022`; phase classification for finalize and skip-impl sessions at `tests/test-app-routes-live.sh:798-832` and `tests/test-app-routes-live.sh:1145-1205`.
- skip_candidate: `no`

### H2_DEV-HZ-118 `file` `tests/test-refine-plan.sh`
- cursor: `[_]`
- core_role: Executable specification for the `refine-plan` command, its IO validator, QA template, language variants, atomic write contract, and installation/documentation wiring.
- algorithmic_behavior: The script defines assertion helpers, fixture plan builders, a reference CMT scanner, reference comment classifiers, language normalization, variant path generation, and QA path derivation. Helpers and fixtures start at `tests/test-refine-plan.sh:46-165`; plan fixtures cover valid plans, no-comment plans, missing sections, sections hidden in fences/comments, and mixed real/ignored sections at `tests/test-refine-plan.sh:167-429`.
- inputs_outputs_state: Inputs are repository command/template/config/docs files and generated fixture plans. Outputs are PASS/FAIL counts and process exit 0 or 1. The validator under test receives `--input`, `--output`, `--qa-dir`, `--discussion`, and `--direct`, but must reject unexpected `--alt-language`. The reference extractor mutates in-memory `REFERENCE_COMMENT_COUNT` and `REFERENCE_CLEANED_PLAN` only (`tests/test-refine-plan.sh:432-538`).
- gates_or_invariants: Static gates require command metadata, allowlists, hidden slash-command flag, ultrathink wording, planning-only hard constraint, sequential execution, and exact phases 0 through 7 in order (`tests/test-refine-plan.sh:650-724`). CMT extraction must support inline and multiline markers, ignore markers inside fenced code blocks and HTML comments, preserve surrounding inline text, record location/form/context, and report nested, stray, unterminated, and empty-only comment errors (`tests/test-refine-plan.sh:727-741`). Classification must support question, change_request, research_request, deterministic sub-items, discussion-mode user confirmation, direct-mode assumptions, and dispositions (`tests/test-refine-plan.sh:743-759`).
- dependencies_and_callers: Reads `commands/refine-plan.md`, `prompt-template/plan/refine-plan-qa-template.md`, `scripts/validate-refine-plan-io.sh`, `skills/humanize-refine-plan/SKILL.md`, `scripts/install-skill.sh`, install docs for Claude/Codex/Kimi, `.claude-plugin` metadata, marketplace metadata, and README version. Paths are defined at `tests/test-refine-plan.sh:16-35`.
- edge_cases_or_failure_modes: Validator failures are specified as exit code 1 missing input, 2 empty input, 3 missing/invalid CMT blocks, 4 missing required gen-plan sections, 5 output/input dir not writable, 6 QA path not a writable directory, and 7 invalid args/help/mutually exclusive modes (`tests/test-refine-plan.sh:1054-1295`). It explicitly tests ignored comment markers in HTML comments/fences, empty CMT blocks, unterminated CMT context output, nested CMT parse errors, required sections hidden in ignored regions, and real sections outside ignored regions passing validation.
- validation_or_tests: Reference behavior tests assert CMT extraction, classification precedence of research over change and change over question, language normalization for `zh`, full names, English no-op, unsupported languages, variant filename suffix insertion, and QA path derivation (`tests/test-refine-plan.sh:893-1053`). Static checks verify alt-language precedence/mapping and variant naming (`tests/test-refine-plan.sh:777-800`), atomic temp-file write requirements and primary plan update order (`tests/test-refine-plan.sh:801-813`), QA template sections and metadata (`tests/test-refine-plan.sh:815-829`), AC-7 install wiring and version consistency (`tests/test-refine-plan.sh:830-891`), and successful in-place/new-file validator modes (`tests/test-refine-plan.sh:1296-1354`).
- skip_candidate: `no`

### H2_DEV-HZ-148 `file` `tests/workflow-projection.test.ts`
- cursor: `[_]`
- core_role: Vitest specification for TypeScript workflow projection and RLCR dashboard board rendering. It verifies the workflow engine can turn RLCR cartridge/run state into dashboard flow nodes, boards, and rendered properties views.
- algorithmic_behavior: The first test initializes an RLCR goal tracker board from a markdown implementation plan, extracting goal, plan summary, completion criteria as acceptance criteria, required tests as active tasks, and loop metadata from graph/run inputs (`tests/workflow-projection.test.ts:13-56`). Later tests start a real `WorkflowCoordinator`, load `flow/rlcr/workflow.html`, deliver artifacts through the plan-compliance, quiz, human answer, builder, reviewer, and verdict path, then inspect projected views (`tests/workflow-projection.test.ts:58-244` and `tests/workflow-projection.test.ts:246-368`).
- inputs_outputs_state: Inputs are mocked agent backends, deterministic ID factories, fixed clock, RLCR workflow HTML, delivered artifacts, workflow graph/run records, loop iteration counters, and run events. Outputs are projection objects containing flow nodes, boards, HTML view slots, statuses, and loop status updates. The coordinator state transitions from started workflow, waiting for human input, builder run, reviewer run, stopped/failed run.
- gates_or_invariants: RLCR projection must include top-level nodes `clean-worktree`, `base-detection`, `plan-await`, `code-review-loop`, `finalizer`, `complete-end`, and `stopped` (`tests/workflow-projection.test.ts:126-137`). `code-review-loop` and `implementation-loop` must both be loop nodes with max 42 and iteration 1, but with different counter labels (`Review Round` versus `Round`) (`tests/workflow-projection.test.ts:139-164`). Builder/reviewer agents must expose expected tool/role/input contracts, including optional review artifacts and boards (`tests/workflow-projection.test.ts:165-215`). Branch projections must route `review-verdict.status` revise/complete and `code-review-result.status` revise/complete correctly (`tests/workflow-projection.test.ts:192-226`).
- dependencies_and_callers: Depends on `src/agents/types.js`, `src/hub/runs.js`, `src/service.js`, `src/workflows/coordinator.js`, `src/workflows/projection.js`, `src/workflows/rlcr-board.js`, and `src/workflows/types.js` (`tests/workflow-projection.test.ts:1-11`). It also consumes `flow/rlcr/workflow.html` at runtime.
- edge_cases_or_failure_modes: A failed workflow run with a failed child agent must mark both the containing loop and failed agent node distinctly as failed, using event history and `loopIterations` (`tests/workflow-projection.test.ts:370-468`). Async race risk is controlled by `waitUntil` with a 1.5s timeout and deterministic delayed backends (`tests/workflow-projection.test.ts:471-517`).
- validation_or_tests: This file is itself the test. It validates board values including stage `implementation`, phase `build`, round 1, maxRounds 42, counts, and next action `Await reviewer verdict`; it also validates rendered properties HTML includes RLCR status, round, acceptance criteria, operational panel, next action, and goal text (`tests/workflow-projection.test.ts:318-343`). Stop verdict updates loop status with phase `stopped`, next action `Stop workflow`, and review summary (`tests/workflow-projection.test.ts:344-367`).
- skip_candidate: `no`

### H2_DEV-HZ-178 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Prompt block enforcing a large-file stop gate before an agent exits. It is algorithmic policy text used to redirect completion behavior when files exceed a configured line limit.
- algorithmic_behavior: The block receives templated `{{MAX_LINES}}` and `{{LARGE_FILES}}`, tells the agent it is trying to stop while files exceed the limit, explains maintainability risk, and prescribes split actions by file type. See `prompt-template/block/large-files.md:1-5`.
- inputs_outputs_state: Inputs are the maximum line threshold and rendered list of oversized files. Output is instruction text requiring further work, not a structured data artifact. No persistent state is changed by this file itself.
- gates_or_invariants: Code files must be split into smaller modular files under the line limit, functionality must remain strictly unchanged, module boundaries/interfaces must stay clear, and optional `code-simplifier` review should be used if installed. Documentation files must be split into logical sections/chapters under the line limit, cross-references maintained, narrative flow preserved, and TOC/navigation updated. See `prompt-template/block/large-files.md:11-24`.
- dependencies_and_callers: Depends on the prompt rendering layer that supplies `MAX_LINES` and `LARGE_FILES`. Optionally references the `code-simplifier` plugin invocation forms at `prompt-template/block/large-files.md:16`.
- edge_cases_or_failure_modes: If the oversized file list includes both code and documentation, the block gives separate remediation paths. If `code-simplifier` is unavailable, the instruction is conditional and the split requirements still apply.
- validation_or_tests: Validation is policy-gate oriented: after splitting, the agent must commit changes and attempt exit again (`prompt-template/block/large-files.md:25`). No direct assigned test targets this exact template.
- skip_candidate: `no`

### H2_DEV-HZ-208 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: Claude next-round prompt footer defining anti-cheating and handoff requirements at the end of an RLCR round.
- algorithmic_behavior: It forbids ending the loop by lying, editing loop state files, or running `cancel-rlcr-loop`; then it requires optional `code-simplifier` review if installed, a descriptive commit, and writing a work summary to `@{{NEXT_SUMMARY_FILE}}`. See `prompt-template/claude/next-round-footer.md:4-9`.
- inputs_outputs_state: Input is the rendered `NEXT_SUMMARY_FILE` placeholder. Expected outputs are committed changes and a summary written to the resolved summary file. No direct state mutation happens in the template itself, but it constrains agents not to mutate loop state as an exit shortcut.
- gates_or_invariants: The core invariant is truthful completion through normal workflow evidence, not direct loop-state manipulation. The footer also keeps the summary file as a required handoff surface for the next round.
- dependencies_and_callers: Consumed by Claude prompt composition for next-round execution. Optionally depends on installed `code-simplifier` plugin. The summary file placeholder must be provided by the prompt renderer.
- edge_cases_or_failure_modes: A worker may be tempted to cancel or alter state files to exit; this footer explicitly blocks that path. If `code-simplifier` is not installed, that step is conditional and should not block commit/summary.
- validation_or_tests: Validation is mainly workflow/review enforcement: downstream rounds should find a commit and populated summary file rather than altered loop state. No direct assigned test targets this footer.
- skip_candidate: `no`

### H2_DEV-HZ-238 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Robustness test suite for concurrent RLCR state access, state parsing edge cases, active-loop detection, strict state validation, and permission/staleness handling.
- algorithmic_behavior: The script sets up a temp test dir via `tests/test-helpers.sh`, sources `hooks/lib/loop-common.sh`, creates synthetic `state.md` files, and exercises `get_current_round`, `find_active_loop`, and `parse_state_file_strict`. The helper creates frontmatter with current round, max iterations, branch/model fields, and loop body (`tests/robustness/test-concurrent-state-robustness.sh:15-50`).
- inputs_outputs_state: Inputs are temp directories containing `.md` state files, loop timestamp directories, permission modes, and concurrent reader/writer processes. Outputs are PASS/FAIL summary via `print_test_summary` and process exit status (`tests/robustness/test-concurrent-state-robustness.sh:499-504`). State transitions include atomic temp-to-final `mv` writes, state file permission changes, and background writer lifecycle.
- gates_or_invariants: `get_current_round` should parse normal LF state files, tolerate trailing whitespace, degrade gracefully for leading whitespace/CRLF/empty values, tolerate YAML comments after cleanup, and handle unicode content, zero, and very large round numbers (`tests/robustness/test-concurrent-state-robustness.sh:59-160`, `tests/robustness/test-concurrent-state-robustness.sh:373-387`, `tests/robustness/test-concurrent-state-robustness.sh:439-472`). `find_active_loop` must return the newest active loop only when the newest timestamp directory has active state, treat `finalize-state.md` as active, return empty for missing/empty bases, and not consider `cancel-state.md` active (`tests/robustness/test-concurrent-state-robustness.sh:170-240`, `tests/robustness/test-concurrent-state-robustness.sh:397-429`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh` and `hooks/lib/loop-common.sh`. The functions under test are shared with monitor and loop scripts, so this suite protects state behavior used by the runtime workflow.
- edge_cases_or_failure_modes: Tests zombie-loop protection by making the newest directory empty even when older directories have state (`tests/robustness/test-concurrent-state-robustness.sh:186-201`, `tests/robustness/test-concurrent-state-robustness.sh:397-411`). Concurrency tests spawn 10 simultaneous readers and require all to read the non-default round 5, then run reads while a writer atomically replaces `state.md` 50 times and require 20/20 numeric reads (`tests/robustness/test-concurrent-state-robustness.sh:250-322`). Strict parsing must reject malformed YAML and missing frontmatter (`tests/robustness/test-concurrent-state-robustness.sh:332-371`). Permission denied should return empty or 0 when not running as root (`tests/robustness/test-concurrent-state-robustness.sh:474-497`).
- validation_or_tests: This file is itself an executable validation suite. It encodes graceful-degradation expectations for loose parsing and hard-failure expectations for strict parsing, plus concurrency guarantees around atomic writes and active-loop detection.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8 item evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`