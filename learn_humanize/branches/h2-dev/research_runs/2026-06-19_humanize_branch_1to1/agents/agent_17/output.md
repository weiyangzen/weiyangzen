# agent_17 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-017 `directory` `prompt-template/explore`
- cursor: `[_]`
- core_role: Template bundle for `/humanize:explore-idea`, covering worker dispatch prompts, canonical run report synthesis, and plan-ready final idea synthesis. The directory contains `worker-prompt.md`, `report-template.md`, and `final-idea-template.md`; `scripts/validate-explore-idea-io.sh` validates their presence and emits their paths as `WORKER_PROMPT_TEMPLATE`, `REPORT_TEMPLATE`, and `FINAL_IDEA_TEMPLATE` (`scripts/validate-explore-idea-io.sh:248-260`, `scripts/validate-explore-idea-io.sh:405-407`).
- algorithmic_behavior: `worker-prompt.md` defines the per-direction prototype loop: verify worktree/base commit, create `explore/<RUN_ID>/<DIR_SLUG>`, implement scoped changes, run targeted tests, review via `ask-codex.sh`, commit locally, then emit sentinel-wrapped JSON (`prompt-template/explore/worker-prompt.md:67-160`). `report-template.md` defines two ranking surfaces: Tier 1 product direction quality and Tier 2 implementation readiness (`prompt-template/explore/report-template.md:18-42`). `final-idea-template.md` defines the downstream `/humanize:gen-plan` input artifact with recommendation, evidence, constraints, risks, and learnings (`prompt-template/explore/final-idea-template.md:10-47`).
- inputs_outputs_state: Inputs are validation stdout, directions JSON fields, worker result JSONL, base branch/commit, run paths, and review model settings. Outputs are dispatch prompts under `.humanize/explore/<RUN_ID>/dispatch-prompts/`, `explore-report.md`, and `final-idea.md`; `commands/explore-idea.md` specifies prompt substitution and hashing (`commands/explore-idea.md:137-160`) plus report/final-idea substitution (`commands/explore-idea.md:265-358`). Durable state is external: `manifest.json`, `worker-results.jsonl`, worktrees, branches, commits.
- gates_or_invariants: Workers must stay in their worktree, avoid nested slash commands/agents, never push, use only `ask-codex.sh`, scope Codex via `CLAUDE_PROJECT_DIR="$PWD"`, fail closed if review metadata is not `gpt-5.5` and `xhigh`, and print the result sentinel last (`prompt-template/explore/worker-prompt.md:16-27`). Worker setup hard-gates on `HEAD == <BASE_COMMIT>` before branch creation (`prompt-template/explore/worker-prompt.md:71-85`). Result enum and schema fields are explicit (`prompt-template/explore/worker-prompt.md:132-182`).
- dependencies_and_callers: Primary caller is `commands/explore-idea.md`, which performs IO validation, confirmation, run state initialization, worker dispatch, result collection, and synthesis (`commands/explore-idea.md:41-49`). The validator supplies canonical template paths and fixed Codex review spec (`scripts/validate-explore-idea-io.sh:31-52`, `scripts/validate-explore-idea-io.sh:449-451`). Tests such as `tests/test-explore-command-structure.sh` assert required placeholders and productization flow (`tests/test-explore-command-structure.sh:220-328`).
- edge_cases_or_failure_modes: Direction data is explicitly untrusted and must be escaped before insertion (`commands/explore-idea.md:139-143`, `prompt-template/explore/worker-prompt.md:28-30`). Missing templates are validation exit code `9`. Missing/invalid worker sentinel becomes a coordinator-generated `no_summary` row (`commands/explore-idea.md:232-246`). If all workers fail, `.failed` is written and failure artifacts are still generated (`commands/explore-idea.md:252-257`, `commands/explore-idea.md:368-375`).
- validation_or_tests: `tests/test-explore-command-structure.sh` checks report/final-idea placeholders and clean productization ordering (`tests/test-explore-command-structure.sh:235-328`). `tests/test-explore-manifest.sh` checks existence and manifest/report structure. `tests/test-worker-result-contract.sh` covers the worker result contract.
- skip_candidate: `no`

### H2_DEV-HZ-047 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit hook that preserves RLCR loop plan-file and branch invariants while a loop is active. It validates state schema, prevents branch switching, and enforces whether the loop plan file must remain tracked/clean or untracked/gitignored.
- algorithmic_behavior: Reads hook JSON from stdin, extracts `session_id`, finds the active loop under `.humanize/rlcr`, resolves the active state file, parses it strictly, then gates on schema fields, current branch, and plan-file git state (`hooks/loop-plan-file-validator.sh:27-54`). No active loop exits allow (`hooks/loop-plan-file-validator.sh:37-40`).
- inputs_outputs_state: Inputs are hook stdin JSON, `PROJECT_ROOT`, `.humanize/rlcr/<session>/state.md` or finalize/methodology state, `plan_tracked`, `plan_file`, `start_branch`, and git status. Outputs are Claude hook JSON blocks like `{"decision":"block","reason":...}` for recoverable blocks, plain stderr plus exit `1` for malformed state, or exit `0` to allow (`hooks/loop-plan-file-validator.sh:45-49`, `hooks/loop-plan-file-validator.sh:100-115`, `hooks/loop-plan-file-validator.sh:176-192`).
- gates_or_invariants: Requires `plan_tracked` and `start_branch` in addition to strict state parsing (`hooks/loop-plan-file-validator.sh:60-90`). Blocks if current branch differs from `start_branch` (`hooks/loop-plan-file-validator.sh:96-116`). If `plan_tracked=true`, the file must still be tracked and `git status --porcelain` clean (`hooks/loop-plan-file-validator.sh:124-193`). If `plan_tracked=false`, the file must not be tracked (`hooks/loop-plan-file-validator.sh:194-230`). Git timeout/error cases fail closed with block JSON.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` for `resolve_project_root`, `extract_session_id`, `find_active_loop`, `resolve_active_state_file`, strict state parsing, template loading, and field constants (`hooks/loop-plan-file-validator.sh:13-22`). Uses `scripts/portable-timeout.sh` for bounded git calls (`scripts/portable-timeout.sh:38-76`). Active-loop filtering comes from `find_active_loop` session isolation and zombie-loop rules (`hooks/lib/loop-common.sh:308-425`).
- edge_cases_or_failure_modes: `resolve_project_root` failure exits allow, making non-project hook contexts non-blocking (`hooks/loop-plan-file-validator.sh:18`). Malformed state exits `1` and blocks for safety (`hooks/loop-plan-file-validator.sh:45-49`). Missing `plan_file` is not explicitly part of this hook’s schema check; strict parsing validates core state fields, while this hook separately checks only `plan_tracked` and `start_branch`. Git command exit `124` is treated as timeout and blocked.
- validation_or_tests: Plan-file behavior is covered by setup/hook tests around tracking expectations: tracked file without `--track-plan-file`, untracked file with it, and modified tracked file are rejected (`tests/test-plan-file-validation.sh:328-425`). Broader plan path validation appears in `tests/test-plan-file-validation.sh:88-180`.
- skip_candidate: `no`

### H2_DEV-HZ-077 `file` `tests/hub.test.ts`
- cursor: `[_]`
- core_role: Vitest executable specification for the Humanize2 hub: agent run coordination, JSON-RPC API, workflow handoff/intervention semantics, dashboard shell invariants, workflow view binding, and workflow records.
- algorithmic_behavior: Creates fake and interruptible agent backends that record `AgentRequest`s and emit output events (`tests/hub.test.ts:20-116`). The test suite then exercises `AgentRunCoordinator`, `createHubHttpServer`, `HumanizeService`, and `WorkflowCoordinator` (`tests/hub.test.ts:7-11`).
- inputs_outputs_state: Inputs include mocked backend IDs, prompts, cwd, timeouts, model/effort overrides, HTTP JSON-RPC calls, and inline workflow HTML. Outputs are run records, output event arrays, backend requests, HTTP JSON payloads, workflow records, rendered dashboard HTML, and workflow events.
- gates_or_invariants: Agent defaults must apply to runs and backend requests unless explicit model/effort is provided (`tests/hub.test.ts:168-207`). Parent-child runs must preserve `parentRunId`, `shortName`, timeout, project metadata, output events, and hub env vars (`tests/hub.test.ts:209-252`). `sendMessage` must interrupt a running run, preserve backend session ID, create linked continuation, and pass `resumeSessionId` (`tests/hub.test.ts:254-299`); completed runs can receive continuation without status mutation (`tests/hub.test.ts:301-327`).
- dependencies_and_callers: HTTP layer is validated through `run.create`, `run.wait`, `run.list`, `run.send_message`, `run.spawn_child`, `system.info`, `/api/sessions`, `/api/runs/:id`, and `/api/workflows` (`tests/hub.test.ts:329-516`, `tests/hub.test.ts:877-895`). Workflow tests load H2 workflow HTML and assert agent handoff and artifacts (`tests/hub.test.ts:422-482`).
- edge_cases_or_failure_modes: ID factories throw on exhausted IDs (`tests/hub.test.ts:929-937`). Child-spawn parent inference only passes when exactly one active run matches cwd (`tests/hub.test.ts:484-516`). Unset `var.greeting` in `data-h2-bind` leaves placeholder text rather than fabricating data (`tests/hub.test.ts:746-796`). Dashboard scripts must parse under `vm.Script` (`tests/hub.test.ts:730-744`).
- validation_or_tests: Dashboard shell assertions lock many UI and data-contract invariants, including endpoints, workflow views, sanitized rendering, resizable layout, collapse toggles, transcript/group chat, branding, and removal of legacy labels (`tests/hub.test.ts:519-728`). Workflow view tag allowlist must include spec tags and drop legacy passive tags (`tests/hub.test.ts:798-818`). `workflow.start` cwd must reach spawned agents (`tests/hub.test.ts:820-848`).
- skip_candidate: `no`

### H2_DEV-HZ-107 `file` `tests/test-gen-idea-dual-write.sh`
- cursor: `[_]`
- core_role: Deterministic shell specification for the `/humanize:gen-idea` dual-write contract: every idea draft output has a companion `.directions.json`, and validation prevents partial writes when either target already exists.
- algorithmic_behavior: Builds a mock git repo and mock plugin template root, runs `scripts/validate-gen-idea-io.sh`, inspects `commands/gen-idea.md`, and optionally validates a fixture with `scripts/validate-directions-json.sh` (`tests/test-gen-idea-dual-write.sh:28-40`).
- inputs_outputs_state: Inputs are inline idea text, `--output`, the mock repo, command markdown, valid directions fixture, and `jq` availability. Outputs are test pass/fail lines and a test summary; state changes are confined to the temp test directory.
- gates_or_invariants: Validation must emit `DIRECTIONS_JSON_FILE:` on success (`tests/test-gen-idea-dual-write.sh:45-55`). `commands/gen-idea.md` must reference `DIRECTIONS_JSON_FILE`, contain an `explore-idea` hint, and list `validate-directions-json.sh` in allowed tools (`tests/test-gen-idea-dual-write.sh:57-76`). The valid fixture must pass the schema validator when `jq` exists (`tests/test-gen-idea-dual-write.sh:78-90`).
- dependencies_and_callers: Depends on `tests/test-helpers.sh`, `scripts/validate-gen-idea-io.sh`, `commands/gen-idea.md`, `scripts/validate-directions-json.sh`, and `tests/fixtures/directions/valid.directions.json` (`tests/test-gen-idea-dual-write.sh:15-22`).
- edge_cases_or_failure_modes: Existing draft output must fail before writing (`tests/test-gen-idea-dual-write.sh:96-106`). Existing companion JSON must also fail before either file is written (`tests/test-gen-idea-dual-write.sh:108-118`). Schema validation is skipped if `jq` is unavailable, so that subcheck is environment-dependent (`tests/test-gen-idea-dual-write.sh:78-90`).
- validation_or_tests: This test directly verifies the validator’s companion derivation and no-write guards, which are implemented in `scripts/validate-gen-idea-io.sh` by deriving `${OUTPUT_FILE%.md}.directions.json`, rejecting existing output, rejecting existing companion, and emitting both output paths (`scripts/validate-gen-idea-io.sh:158-181`, `scripts/validate-gen-idea-io.sh:203-211`).
- skip_candidate: `no`

### H2_DEV-HZ-137 `file` `tests/test-viz.sh`
- cursor: `[_]`
- core_role: Executable specification for Humanize Viz/dashboard monitoring behavior: shell helpers, Python parser/analyzer/exporter modules, RLCR session parsing, monitor-web migration, static asset presence, and UI language constraints.
- algorithmic_behavior: Skips if `python3` is unavailable, creates a mock RLCR session under `.humanize/rlcr`, writes state, goal tracker, summaries, and review output, then runs inline Python assertions against `viz/server` modules (`tests/test-viz.sh:28-34`, `tests/test-viz.sh:70-216`).
- inputs_outputs_state: Inputs are generated `state.md`, `goal-tracker.md`, round summaries, review result, Python modules, viz shell scripts, `scripts/humanize.sh`, static assets, requirements, and README. Outputs are pass/fail test counters and a summary. Mock session state models active RLCR state with current round, max iterations, plan file, branch, model, effort, and timestamps (`tests/test-viz.sh:75-87`).
- gates_or_invariants: `viz-start.sh`, `viz-stop.sh`, `viz-status.sh` must pass shell syntax; `parser.py`, `analyzer.py`, `exporter.py`, `app.py`, and `watcher.py` must compile (`tests/test-viz.sh:36-62`). Parser must validate sessions, include canonical round indices through current round, parse verdicts, goal tracker ACs, completed table, and task counts (`tests/test-viz.sh:147-216`).
- dependencies_and_callers: Depends on `viz/scripts/*`, `viz/server/*`, `scripts/humanize.sh`, README, static files, and `tests/test-helpers.sh`. The migration tests assert dashboard access moved to `humanize monitor web` and legacy `/humanize:viz` command/skill are absent (`tests/test-viz.sh:419-463`).
- edge_cases_or_failure_modes: Malformed session without state is skipped (`tests/test-viz.sh:218-235`). Duration regression covers state `current_round` lagging behind summary files on disk; parser must span all on-disk summaries (`tests/test-viz.sh:237-281`). Analyzer must not count unreviewed rounds as `unknown` verdicts (`tests/test-viz.sh:289-332`). Exporter must handle bilingual `{en, zh}` dictionaries without `TypeError` (`tests/test-viz.sh:340-388`).
- validation_or_tests: Additional gates verify opt-in daemon helper executability (`tests/test-viz.sh:390-416`), foreground monitor-web host-sensitive port probing (`tests/test-viz.sh:464-480`), static asset existence, English-only `i18n.js`, and Flask requirement (`tests/test-viz.sh:481-512`).
- skip_candidate: `no`

### H2_DEV-HZ-167 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: User-facing block template for the RLCR Bash validator when a command would stage `.humanize/` local loop state.
- algorithmic_behavior: Static message explains why `.humanize/` must not be committed, gives safe `git add` alternatives, lists blocked broad or direct add commands, and explains how to add `.humanize*` to `.gitignore` without staging runtime state (`prompt-template/block/git-add-humanize.md:1-34`).
- inputs_outputs_state: No dynamic placeholders. Input is the validator decision that a command adds `.humanize`; output is rendered markdown from `git_add_humanize_blocked_message`. It does not mutate state.
- gates_or_invariants: Protects `.humanize/` RLCR state from version control. The template allows specific paths, source directories, and patch mode (`prompt-template/block/git-add-humanize.md:8-15`), while warning against direct `.humanize`, `git add -A`, `git add --all`, `git add .`, and force-add broad scope (`prompt-template/block/git-add-humanize.md:16-25`).
- dependencies_and_callers: Rendered through `hooks/lib/loop-common.sh:git_add_humanize_blocked_message`, which falls back to equivalent inline text if the template is missing (`hooks/lib/loop-common.sh:1383-1420`). Called by `hooks/loop-bash-validator.sh` when `git_adds_humanize` matches a command (`hooks/loop-bash-validator.sh:245-248` from repository reference scan).
- edge_cases_or_failure_modes: The message specifically notes the commit message for adding `.humanize*` to `.gitignore` must not contain the literal string `.humanize`, because the protection can also be triggered by command text (`prompt-template/block/git-add-humanize.md:26-34`). The broader detector, not this template, handles quoted paths, `git -C`, chained commands, force flags, and all-flags.
- validation_or_tests: Detection behavior is covered by `tests/test-humanize-escape.sh`, including direct `.humanize` paths, quoted variants, force adds, `-A`/`--all`, chained commands, `git -C`, and allowed non-humanize paths (`tests/test-humanize-escape.sh:83-197`).
- skip_candidate: `no`

### H2_DEV-HZ-197 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: Block template for stale or wrong-round RLCR file reads. It keeps agents anchored to the current loop round’s prompt/summary instead of reading previous or future round files through the Read tool.
- algorithmic_behavior: Renders the attempted round/file type, current round, current prompt and summary paths, and a fallback explicit `cat` command for intentional access (`prompt-template/block/wrong-round-file.md:1-9`).
- inputs_outputs_state: Inputs are placeholders `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH`. Output is markdown written to stderr by the read validator. It does not mutate loop state.
- gates_or_invariants: `hooks/loop-read-validator.sh` blocks if the requested round differs from current round and the file is not allowlisted (`hooks/loop-read-validator.sh:282-299`). This protects current-round context and avoids accidental reasoning from stale prompt/summary/contract files.
- dependencies_and_callers: Called only through `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-round-file.md"` in `hooks/loop-read-validator.sh` (`hooks/loop-read-validator.sh:286-298`). File type is inferred before rendering (`hooks/loop-read-validator.sh:260-264`).
- edge_cases_or_failure_modes: The template’s “Current round files” lists prompt and summary, but the validator can also classify `contract`; for a wrong-round contract read, the message still omits the current contract path. The “use cat” escape route intentionally shifts explicit access to Bash rather than Read (`prompt-template/block/wrong-round-file.md:5-9`).
- validation_or_tests: Template reference integrity is covered by repository template-reference tests; wrong-round behavior is part of the read-validator surface. Direct caller evidence is `hooks/loop-read-validator.sh:286-298`.
- skip_candidate: `no`

### H2_DEV-HZ-227 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared shell utility library for monitor/status flows, especially RLCR session discovery, state-file status classification, terminal display helpers, and goal-tracker progress parsing.
- algorithmic_behavior: Provides ANSI color helpers (`scripts/lib/monitor-common.sh:14-23`), cross-platform file size (`scripts/lib/monitor-common.sh:32-35`), latest timestamp-named session discovery (`scripts/lib/monitor-common.sh:40-63`), terminal scroll-region setup/restore (`scripts/lib/monitor-common.sh:71-92`), status-to-color mapping (`scripts/lib/monitor-common.sh:130-141`), state-file detection (`scripts/lib/monitor-common.sh:154-192`), YAML frontmatter value extraction (`scripts/lib/monitor-common.sh:201-213`), timestamp/string formatting (`scripts/lib/monitor-common.sh:222-256`), and goal tracker parsing (`scripts/lib/monitor-common.sh:265-383`).
- inputs_outputs_state: Inputs are file paths, session directories, status strings, timestamps, and markdown goal trackers. Outputs are plain strings: latest session path, `state_file|status`, YAML values, display strings, and pipe-delimited progress summaries. Only terminal helpers mutate terminal display state; parsing helpers do not persist state.
- gates_or_invariants: Session names must match `YYYY-MM-DD_HH-MM-SS` for latest-session discovery (`scripts/lib/monitor-common.sh:54-60`). State priority is `methodology-analysis-state.md`, then `state.md`, then terminal `*-state.md` files (`scripts/lib/monitor-common.sh:162-190`). Missing goal tracker returns `0|0|0|0|0|0|No goal tracker` (`scripts/lib/monitor-common.sh:300-304`). Active task count excludes rows whose status is completed or deferred (`scripts/lib/monitor-common.sh:324-345`).
- dependencies_and_callers: Intended to be sourced by `scripts/humanize.sh` rather than executed (`scripts/lib/monitor-common.sh:5-6`). Repository references show monitor flows using `monitor_find_latest_session`, `monitor_find_state_file`, and goal-tracker parsers from the monitor surface (`scripts/humanize.sh:281`, `scripts/humanize.sh:379`, `scripts/humanize.sh:410`). Viz source docs reference parity with `monitor_find_state_file`.
- edge_cases_or_failure_modes: `monitor_find_latest_session` returns empty for absent loop dirs and uses lexical timestamp ordering. `monitor_find_state_file` uses the first `find` result for terminal states, so multiple terminal state files would be nondeterministic (`scripts/lib/monitor-common.sh:172-185`). Goal tracker parsing supports both new Blocking/Queued Side Issues and older Open Issues sections (`scripts/lib/monitor-common.sh:285-294`, `scripts/lib/monitor-common.sh:361-373`). Nested `_count_table_rows` helper definitions are shell-global despite being used as local implementation details.
- validation_or_tests: Viz parser behavior is indirectly checked by `tests/test-viz.sh` through mock RLCR sessions, malformed-session skip, and duration regression (`tests/test-viz.sh:147-281`). Goal tracker robustness is separately covered by `tests/robustness/test-goal-tracker-robustness.sh` per repository references.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `8/8 item evidence sections present; each assigned row is represented once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`