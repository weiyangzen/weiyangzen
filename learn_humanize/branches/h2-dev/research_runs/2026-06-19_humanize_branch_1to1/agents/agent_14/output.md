# agent_14 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 8
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-014 `directory` `prompt-template/block`
- cursor: `[_]`
- core_role: Shared RLCR block-message template catalog. Recursively inspected the directory; it contains 42 Markdown files directly under `prompt-template/block` and no child subdirectories. These templates are the human-facing reason payloads used when validators, stop hooks, review gates, and loop-state guards reject an action or require retry.
- algorithmic_behavior: The directory does not execute code itself; it defines deterministic block/retry contracts rendered by `hooks/lib/template-loader.sh`. The loader resolves the template root as `<plugin_root>/prompt-template` (`get_template_dir`, lines 24-31), loads `block/*.md` by relative name (`load_template`, lines 33-46), and performs single-pass `{{VAR}}` substitution to avoid placeholder injection (`render_template`, lines 48-136). `load_and_render_safe` falls back to embedded messages if a template is missing or empty (lines 185-211), so hooks fail operationally rather than crashing on missing prose.
- inputs_outputs_state: Inputs are template names and `VAR=value` substitutions from hooks. Outputs are rendered Markdown reason strings, usually emitted to stderr by PreToolUse validators or as JSON `reason` fields by stop-hook decisions. Templates encode state-transition explanations for state file protection (`state-file-modification.md`, `finalize-state-file-modification.md`, `methodology-analysis-state-file-modification.md`), round-scoped artifacts (`wrong-round-number.md`, `wrong-round-file.md`, `wrong-contract-location.md`, `wrong-summary-location.md`), git hygiene (`git-not-clean.md`, `git-add-humanize.md`, `git-tracked-humanize.md`, `git-push.md`, `unpushed-commits.md`), stop gates (`incomplete-todos.md`, `work-summary-missing.md`, `round-contract-missing.md`, `large-files.md`), review/retry gates (`codex-review-failed.md`, `mainline-verdict-missing.md`, `mainline-drift-stop.md`), and BitLesson validation (`bitlesson-delta-*.md`).
- gates_or_invariants: The directory preserves the invariant that every hard block has a stable, actionable contract. Examples: prompt files cannot be edited because they are instructions from Codex (`prompt-file-write.md`); `.humanize/` state must not be committed (`git-add-humanize.md`, `git-tracked-humanize.md`); active loop state files are system-managed (`state-file-modification.md` family); round contracts and summaries must live in the active loop directory (`wrong-contract-location.md`, `wrong-summary-location.md`); stop is blocked until tasks, summaries, contracts, git state, review output, and file-size constraints satisfy loop rules.
- dependencies_and_callers: Loaded by `hooks/lib/template-loader.sh` and initialized via `hooks/lib/loop-common.sh`, where `TEMPLATE_DIR` is set and validated. Direct caller families include `hooks/loop-bash-validator.sh` for Bash command blocks, `hooks/loop-write-validator.sh` and `hooks/loop-edit-validator.sh` for path/write validation, `hooks/loop-read-validator.sh` for read-scope validation, `hooks/loop-plan-file-validator.sh` for schema guards, and `hooks/loop-codex-stop-hook.sh` for stop/review/git gates. Sibling directories under `prompt-template` provide `codex`, `claude`, and `plan` prompt bodies; `block` is the rejection/error side of that prompt system.
- edge_cases_or_failure_modes: Missing templates degrade to embedded fallbacks instead of failing the hook (`load_and_render_safe`, lines 197-210). Placeholder values are single-pass rendered, so if a substituted value contains `{{OTHER_VAR}}`, it remains literal and cannot trigger secondary substitution (`template-loader.sh`, lines 52-55). Several templates intentionally include specific corrective paths, so incorrect or absent substitutions can reduce actionability but not alter the gate decision.
- validation_or_tests: Template presence and references are exercised across hook and template tests. The block directory is also indirectly covered by tests that assert stop-hook, read/write/edit/bash, and template-loader behavior. `tests/test-refine-plan.sh` and `tests/test-template-loader.sh` are sibling examples of template contract coverage; `hooks/lib/template-loader.sh` validates that `prompt-template` contains required subdirectories including `block` (lines 213-238).
- skip_candidate: `no`

### H2_DEV-HZ-044 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse validator for Bash commands inside the RLCR loop. It is a central enforcement point for preventing shell-based bypasses of Write/Edit validators, loop-state corruption, unauthorized git operations, direct hook execution, and methodology-analysis phase mutations.
- algorithmic_behavior: Reads hook JSON from stdin (`HOOK_INPUT=$(cat)`, line 22), validates JSON and required fields via `loop-common.sh` (`validate_hook_input`, line 25; `require_tool_input_field`, line 41), ignores non-Bash tools (lines 36-38), lowercases the command (lines 45-46), resolves the project root and active session-scoped loop directory (lines 52-60), then applies ordered gates. If no active RLCR loop exists, commands are allowed after early safety checks (lines 173-175). During methodology analysis, it blocks mutating Bash except a tightly constrained leading `cancel-rlcr-loop.sh` invocation (lines 81-171). In active loops, it parses the state file strictly, derives `CURRENT_ROUND` and `PUSH_EVERY_ROUND`, then blocks disallowed command classes and protected file writes.
- inputs_outputs_state: Input is Claude/Codex hook JSON with `tool_name`, `tool_input.command`, and optional `session_id`. Outputs are process exit statuses: `0` allow, `1` malformed/unsafe hook or state parse error, `2` block with a Markdown reason on stderr. It does not mutate loop state; it enforces state-machine boundaries by observing `.humanize/rlcr/<loop>/state.md`, `finalize-state.md`, or `methodology-analysis-state.md`.
- gates_or_invariants: Key gates include JSON sanity and max nesting depth (`is_deeply_nested "$HOOK_INPUT" 30`, lines 29-32); methodology-analysis no-write mode with shell metacharacter rejection for cancel bypasses (lines 96-105) and blocks for git writes, file manipulation, in-place editing, interpreters, shell scripts, build tools, sourcing, direct scripts, and redirection (lines 106-170); direct execution of stop hook scripts is blocked through wrapper-aware regexes for `loop-codex-stop-hook.sh` and `rlcr-stop-gate.sh` (lines 184-202); git push is blocked unless `push_every_round` is true (lines 225-237); `git add` targeting `.humanize` is blocked (lines 241-249); state/finalize/methodology state modifications are blocked as destination and source paths (lines 271-297 and 428-455); shell-wrapper `sh -c`/`bash -c` state-file moves are blocked (lines 458-488); plan backups, goal tracker, prompt files, summary files, round contracts, and out-of-scope todos writes are blocked (lines 490-571).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh` for project root resolution, active-loop discovery, strict state parsing, cancel authorization, git-add detection, command modification detection, and rendered block messages. `loop-common.sh` provides JSON validation (`validate_hook_input`, lines 91-128), session extraction (`extract_session_id`, lines 251-256), active-loop selection with zombie-loop and session isolation semantics (`find_active_loop`, lines 308-420), strict state schema checks (`parse_state_file_strict`, lines 525-575), `.humanize` git-add detection (`git_adds_humanize`, lines 1264-1381), and command mutation pattern matching (`command_modifies_file`, lines 1487-1515). It is invoked by the hook system as a Bash PreToolUse hook.
- edge_cases_or_failure_modes: The script explicitly handles multiple concurrent sessions by filtering active loops by `session_id` and not falling back to unfiltered search during methodology analysis (lines 54-60 and 77-79). It defends against cancel-command injection with semicolon, pipe, ampersand, backticks, redirection, `$(`, and newline checks (lines 89-99). It protects against command segmentation bypasses using shell operators and redirection placeholders before scanning `mv`/`cp` sources (lines 310-456). It normalizes `/private` path prefixes for macOS when allowing only active-loop `round-[12]-todos.md` paths (lines 561-568). If the active state file is malformed, it fails closed (lines 213-217).
- validation_or_tests: Covered by hook robustness and Bash validator tests such as `tests/test-bash-validator-patterns.sh`, `tests/test-allowlist-validators.sh`, `tests/robustness/test-hook-input-robustness.sh`, `tests/robustness/test-state-file-robustness.sh`, and `tests/robustness/test-state-transition-robustness.sh`. The file itself references the expected blocked classes in comments at lines 3-9, and the helper-level tests exercise many of the parsing and bypass cases.
- skip_candidate: `no`

### H2_DEV-HZ-074 `file` `tests/config.test.ts`
- cursor: `[_]`
- core_role: Executable specification for user-level Humanize config loading, default creation, agent model fallback defaults, parser edge cases, and workflow retry/script allowlist settings.
- algorithmic_behavior: Uses Vitest with temporary home directories (`mkdtemp`, lines 15-19) and cleanup after each test (lines 11-13). It calls `loadHumanizeConfig` from `src/config.ts` and asserts that the loader creates or reads a YAML config, merges defaults, honors environment overrides, and parses nested agent/workflow structures.
- inputs_outputs_state: Inputs are synthetic home dirs, `HUMANIZE2_CONFIG`, `HUMANIZE2_CACHE_DIR`, `HUMANIZE2_DEFAULT_TIMEOUT_MS`, and written YAML config content. Outputs are `HumanizeConfig` objects plus a day-one config file under `<home>/.h2/config.yaml`. State is temporary filesystem state only; the test removes temp dirs afterward.
- gates_or_invariants: The first test requires day-one config creation under `.h2`, default cache dir, default timeout, dark theme, and Codex defaults `model: gpt-5.5` plus `reasoningEffort: xhigh` (lines 21-41). Environment variables must override config path/cache/timeout (lines 43-58). Agent model defaults must be loaded from YAML for `codex` and `claude` (lines 60-94). A regression gate ensures a multiline `extraArgs` list under one agent does not swallow following sibling agent fields (lines 96-130). Another ensures empty `extraArgs: []` still allows later sibling fields under the same agent (lines 132-157). Workflow config must parse `softEnforcement.retryMax` and `scripts.allow` (lines 159-191).
- dependencies_and_callers: Directly depends on `src/config.ts`. There, defaults are defined as `DEFAULT_RUN_TIMEOUT_MS`, `DEFAULT_DASHBOARD_THEME`, and `DEFAULT_AGENT_MODEL_DEFAULTS` with Codex `gpt-5.5:xhigh` and Claude `claude-opus-4-7:xhigh` (lines 7-49). `loadHumanizeConfigSync` resolves path/env overrides and merges file config (lines 65-96). `ensureConfigFile` creates or backfills missing config blocks (lines 98-131). `parseSimpleYaml` implements the hand-rolled parser for top-level config, `agents`, `agents.extraArgs`, and `workflow` sections (lines 155-299). Merge helpers preserve defaults when overrides are absent (lines 301-336).
- edge_cases_or_failure_modes: The test specifically targets parser state leakage from `agents.extraArgs`, where list mode must reset before processing sibling fields (implementation lines 196-200). It also covers empty list syntax and workflow list parsing. Invalid or missing integer/theme values fall back in implementation via `parsePositiveInteger` and `parseTheme` (lines 344-355), though not all fallbacks are asserted here.
- validation_or_tests: This file is itself the validation. It should be run through the repo’s Vitest path, likely as part of `npm test` or targeted Vitest execution. I did not run it because this assignment requested research notes only and the workspace is read-only.
- skip_candidate: `no`

### H2_DEV-HZ-104 `file` `tests/test-explore-manifest.sh`
- cursor: `[_]`
- core_role: Shell executable specification for the `/humanize:explore-idea` run-state contract: manifest schema, run directory layout, worker result schema, write ordering, and final/report template coverage.
- algorithmic_behavior: The test performs static contract checks against `commands/explore-idea.md`, `prompt-template/explore/worker-prompt.md`, `prompt-template/explore/report-template.md`, `prompt-template/explore/final-idea-template.md`, and `scripts/validate-explore-idea-io.sh`. It uses grep-based assertions rather than running a full explore dispatch.
- inputs_outputs_state: Inputs are repository files and `tests/test-helpers.sh` assertion helpers. Outputs are pass/fail test lines and a summary. It does not create explore runs or mutate command artifacts.
- gates_or_invariants: Required files must exist (lines 28-42). `commands/explore-idea.md` must document manifest fields including `run_id`, `created_at`, `directions_json_file`, `draft_path`, `selected_direction_ids`, `base_branch`, `base_commit`, concurrency/timeouts, Codex review settings, report/final paths, expected worker count, runtime spike status, and `workers` (lines 48-76). Per-worker manifest entries must include `direction_id`, `dir_slug`, `prompt_path`, `prompt_hash`, `branch_name`, and `status` (lines 82-97). Run state must live under `.humanize/explore/` as defined by the validator (lines 103-108). The command must document `dispatch-prompts`, `worker-results.jsonl`, `explore-report.md`, `final-idea.md`, and `.failed` sentinel recovery (lines 110-143). Worker result JSONL must document schema/version, run/direction IDs, status, Codex verdict, tests, branch/commit fields, and summary markdown (lines 149-170). Manifest must be written before dispatch (lines 172-181). Report and final-idea templates must support two-tier ranking and plan-ready synthesis sections (lines 183-213).
- dependencies_and_callers: Depends on the command contract in `commands/explore-idea.md`. That command requires no worker dispatch until confirmation, manifest write before dispatch, canonical `explore-report.md` and `final-idea.md`, no nested skills/slash commands in workers, exact worker branch format, detached base-commit anchoring, targeted tests, and Codex review model spec `gpt-5.5:xhigh` (lines 23-35). It defines phases from IO validation through artifact synthesis (lines 41-49), run directory and manifest construction (lines 125-198), worker dispatch batches by `EFFECTIVE_CONCURRENCY` (lines 201-224), result collection into JSONL (lines 228-260), and artifact synthesis (lines 265-388).
- edge_cases_or_failure_modes: The test checks the existence of `.failed` sentinel documentation for error recovery but does not simulate failure. It also checks static schema presence, not JSON validity of a generated manifest. Because it is grep-based, it can pass if documentation text exists but implementation drifts; paired tests like `tests/test-validate-explore-idea-io.sh` cover executable validation behavior.
- validation_or_tests: This file is a test. It validates the explore manifest/run-state contract against documentation and templates. I did not run it in the read-only research pass.
- skip_candidate: `no`

### H2_DEV-HZ-134 `file` `tests/test-validate-explore-idea-io.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/validate-explore-idea-io.sh`, the hard pre-dispatch validator for `/humanize:explore-idea`. It covers input resolution, exit-code mapping, direction selection, caps, git anchoring, dirty-checkout failure, required templates, run ID/path emission, and static worker base-anchor contract.
- algorithmic_behavior: The test creates temporary mock git repos via `tests/test-helpers.sh`, copies a valid directions fixture, commits fixture/draft files, creates a fake plugin root with required templates, and runs the validator through a `run_validate` helper inside the clean mock repo (lines 33-58). It skips entirely if `jq` is unavailable (lines 27-31).
- inputs_outputs_state: Inputs are `.directions.json` or draft `.md` paths, CLI flags (`--directions`, `--concurrency`, `--max-worker-iterations`, bad options), git checkout states, and a plugin root via `CLAUDE_PLUGIN_ROOT`. Outputs are validator exit codes and structured stdout containing keys such as `DIRECTIONS_JSON_FILE`, `RUN_ID`, `RUN_DIR`, `BASE_BRANCH`, `BASE_COMMIT`, selected IDs, caps, Codex review settings, template paths, and `VALIDATION_SUCCESS` (required keys listed at lines 302-324).
- gates_or_invariants: Negative tests assert exact exits: missing input exits 1 (lines 67-74); missing `.directions.json` or draft exits 2 (lines 76-92); missing companion for `.md` exits 3 (lines 94-104); unsupported extension exits 4 (lines 106-116); invalid directions schema exits 5 (lines 118-127); cap/selector/unknown option errors exit 6 (lines 129-172); dirty, non-git, and unborn git checkouts exit 7 (lines 174-254); missing worker/report/final templates exit 9 (lines 256-283). Positive tests require success, all structured keys, `.md` companion resolution, selection by `direction_id` and `source_index`, effective concurrency capped to selected count, explanatory collision-safe run IDs, canonical report/final paths, and static contract text for detached HEAD anchoring and capture-first dirty checks (lines 293-429).
- dependencies_and_callers: Directly tests `scripts/validate-explore-idea-io.sh`. The script defines defaults and caps for selected directions, concurrency, worker iterations, worker timeout, and Codex timeout (lines 60-69), parses arguments (lines 143-170), validates positive integer caps (lines 186-201), resolves `.directions.json` or `.md` companion inputs (lines 207-235), locates plugin templates and schema validator (lines 241-260), validates schema with `jq` and `validate-directions-json.sh` (lines 266-275), selects directions by display order or explicit selectors with pre- and post-resolution duplicate checks (lines 281-364), requires a real git checkout and HEAD commit (lines 378-388), captures dirty files with `git diff --name-only HEAD --` instead of a `grep -q` pipeline (lines 396-403), builds run IDs under `.humanize/explore` (lines 435-446), fixes review model to `gpt-5.5:xhigh` (lines 449-451), and emits structured stdout (lines 457-477).
- edge_cases_or_failure_modes: Regression cases include mixed selector forms resolving to the same direction ID (test lines 156-163; implementation lines 344-349), many dirty files to catch `git|grep` SIGPIPE-style bugs (test lines 190-219; implementation lines 396-401), non-git and unborn repos that cannot anchor worker worktrees (test lines 221-254), metadata-derived slug for direct `.directions.json` and basename-derived slug for draft `.md` (test lines 372-395; implementation lines 414-438), and run directory collision exit 8 in the validator (implementation lines 443-447; documented by script lines 19-29 though not directly exercised here).
- validation_or_tests: This file is itself a targeted validator test. It pairs with `tests/test-explore-manifest.sh` to cover both executable IO validation and static explore command schema. I did not run it due to read-only research constraints.
- skip_candidate: `no`

### H2_DEV-HZ-164 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Block template for the RLCR stop hook when Codex review fails to produce usable output. It converts a review execution failure into a retryable loop-block reason with debug file pointers.
- algorithmic_behavior: The template renders a title, failure statement, exit code, expected review result path, command/stdout/stderr debug file paths, last stderr content, and retry guidance. Placeholders include `{{CODEX_EXIT_CODE}}`, `{{REVIEW_RESULT_FILE}}`, `{{CODEX_CMD_FILE}}`, `{{CODEX_STDOUT_FILE}}`, `{{CODEX_STDERR_FILE}}`, and `{{STDERR_CONTENT}}` (template lines 1-18).
- inputs_outputs_state: Inputs are Codex review failure metadata from the stop hook. Output is Markdown embedded in the stop hook JSON block reason. The intended state transition is not “complete”; the user is blocked and the system will retry review on the next exit attempt, as the template states at line 18.
- gates_or_invariants: Enforces that Codex review failure cannot be silently skipped. The caller wraps the rendered reason in a JSON decision with `"decision": "block"` and `"systemMessage": "Loop: Blocked - Codex review failed, retry required"` (stop hook lines 1662-1669). This preserves the invariant that implementation review must pass or at least produce valid output before the RLCR loop can advance.
- dependencies_and_callers: Called from `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "block/codex-review-failed.md"` (lines 1651-1660). The fallback in that caller includes a longer diagnostic and retry instructions (lines 1620-1649). Rendering behavior depends on `hooks/lib/template-loader.sh` single-pass substitution.
- edge_cases_or_failure_modes: There is an apparent placeholder/caller mismatch: the template expects `CODEX_EXIT_CODE`, `CODEX_STDOUT_FILE`, and `CODEX_STDERR_FILE`, but the observed caller passes `EXIT_CODE`, `CODEX_CMD_FILE`, `CODEX_LOG_FILE`, `STDERR_CONTENT`, `REVIEW_RESULT_FILE`, and other values (stop hook lines 1652-1660). Because missing placeholders are intentionally left as-is by the loader (template-loader lines 119-120), the rendered template may expose unresolved `{{CODEX_EXIT_CODE}}`, `{{CODEX_STDOUT_FILE}}`, and `{{CODEX_STDERR_FILE}}` unless another caller supplies those exact names. The fallback uses `{{EXIT_CODE}}`, so this mismatch is a concrete edge case to verify.
- validation_or_tests: Indirectly covered by stop-hook tests such as `tests/test-stop-gate.sh`, `tests/test-codex-review-merge.sh`, and template-reference tests. A focused assertion should check that the rendered failure block has no unresolved placeholders for a review failure.
- skip_candidate: `no`

### H2_DEV-HZ-194 `file` `prompt-template/block/wrong-contract-location.md`
- cursor: `[_]`
- core_role: Block template for write attempts that create or modify `round-N-contract.md` outside the active RLCR loop directory. It directs the agent to the only valid contract path for the current round.
- algorithmic_behavior: The template is minimal: title, invariant statement, and `{{CORRECT_PATH}}` rendered as the required path (lines 1-5). It is used when a file is recognized as a contract file but is not under the active `.humanize/rlcr/<loop>/` directory.
- inputs_outputs_state: Input is `CORRECT_PATH`, built by the write validator from `ACTIVE_LOOP_DIR` and `CURRENT_ROUND`. Output is Markdown on stderr, followed by exit `2` from the validator. It prevents an invalid contract write from becoming loop state.
- gates_or_invariants: Enforces the invariant “round contract files MUST be in the active loop directory” (template line 3). In `hooks/loop-write-validator.sh`, if `IS_CONTRACT_FILE=true` and `IN_HUMANIZE_LOOP_DIR=false`, the validator computes `"$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-contract.md"`, renders this template, and exits 2 (lines 283-299).
- dependencies_and_callers: Direct caller is `hooks/loop-write-validator.sh` lines 283-290. Related block templates for the same artifact family are `round-contract-bash-write.md` for Bash writes, `wrong-round-number.md` for mismatched round numbers, `round-contract-missing.md` for stop-hook missing-contract gate, and `finalize-contract-access.md` for finalize phase historical contract access.
- edge_cases_or_failure_modes: The template only addresses location, not round-number correctness or Bash bypasses; those are enforced by other validators/templates. If `ACTIVE_LOOP_DIR` or `CURRENT_ROUND` is wrong due to malformed state, upstream strict state parsing and active-loop discovery must fail closed before this template is used.
- validation_or_tests: Covered by write/path validation tests, including `tests/test-plan-file-hooks.sh`, `tests/test-plan-file-validation.sh`, and robustness path/state tests. The caller path and placeholder are directly visible in `hooks/loop-write-validator.sh` lines 283-290.
- skip_candidate: `no`

### H2_DEV-HZ-224 `file` `prompt-template/plan/refine-plan-qa-template.md`
- cursor: `[_]`
- core_role: Output template for `/humanize:refine-plan` QA documentation. It defines the required evidence ledger and disposition record for turning annotated plan comments into a refined plan and an auditable QA artifact.
- algorithmic_behavior: The template organizes refinement evidence into `Summary`, `Comment Ledger`, `Answers`, `Research Findings`, `Plan Changes Applied`, `Remaining Decisions`, and `Refinement Metadata` sections (lines 1-119). It expects every extracted comment to receive a stable `CMT-ID`, classification, location, original-text excerpt, and disposition (lines 7-17). It separates answer-only comments (lines 19-36), research-request findings (lines 38-58), applied plan changes with affected sections and cross-reference updates (lines 60-84), unresolved decisions with options/recommendation/status (lines 86-106), and metadata including input/output/QA paths, counts, modified sections, convergence status, and date (lines 108-119).
- inputs_outputs_state: Inputs are extracted comments from an annotated plan, their classifications (`question`, `change_request`, `research_request`), plan edits, research findings, remaining user decisions, and file paths/counts from the refine-plan workflow. Output is a QA Markdown document, usually alongside the refined plan. It records the refinement state as `converged` or `partially_converged` rather than mutating runtime state.
- gates_or_invariants: The command treats the QA document as mandatory. `commands/refine-plan.md` says to read this template and populate it completely in Phase 6, and that the QA document is not optional (lines 457-459). The broader command requires planning-only refinement, sequential execution, validated IO, deterministic comment extraction/classification, and atomic write behavior before final output replacement.
- dependencies_and_callers: Primary caller is `commands/refine-plan.md`. The command invokes `scripts/validate-refine-plan-io.sh` first (search result lines 36 and 162), keeps `--alt-language` out of the validator invocation because the validator does not accept it, and generates the QA document before atomic write transaction. `tests/test-refine-plan.sh` statically asserts that this template exists and includes all required sections, ledger columns, metadata paths, and convergence status (test assertions around lines 816-828 in the search output).
- edge_cases_or_failure_modes: The template allows `deferred` and `partially_converged`, so unresolved decisions are explicit rather than hidden. However, it is a skeleton; correctness depends on the command fully replacing placeholders and examples. Missing population would produce a syntactically valid Markdown file that is semantically incomplete, so the command/test contract must enforce completeness beyond file existence.
- validation_or_tests: `tests/test-refine-plan.sh` validates the template title, required sections, ledger columns, input/output/QA metadata, and convergence status entries. `scripts/validate-refine-plan-io.sh` validates the input/output/QA path side before generation, while `commands/refine-plan.md` defines the generation and atomic-write phases.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 8 item evidence sections present, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`