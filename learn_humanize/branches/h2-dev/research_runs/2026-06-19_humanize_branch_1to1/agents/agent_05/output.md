# agent_05 h2-dev 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 9
- source_commit: `2da7defbd5e955dbc329a27f1745fa74a0bee3f7`

## Item Evidence

### H2_DEV-HZ-005 `directory` `docs`
- cursor: `[_]`
- core_role: Documentation and contract surface for Humanize planning, RLCR execution, BitLesson knowledge capture, install/runtime modes, and monitor-web streaming behavior. This directory is not just user prose: `docs/streaming-protocol.md` is a frozen event contract, and `docs/plans/viz-monitor-web.md` is a generated implementation plan with acceptance criteria, dependency sequence, task routing, and resolved decisions.
- algorithmic_behavior: `docs/usage.md` defines the top-level RLCR loop as implementation plus Codex review until acceptance criteria and review issues are cleared (`docs/usage.md:5-12`), describes the plan-understanding quiz before loop start (`docs/usage.md:14-41`), documents `gen-idea` directed-diversity outputs and `explore-idea` isolated worker artifacts (`docs/usage.md:72-116`), and documents `gen-plan` auto-start constraints (`docs/usage.md:150-175`). `docs/bitlesson.md` defines the BitLesson selector routing, project KB path, summary delta shape, and strict summary validation (`docs/bitlesson.md:7-23`, `docs/bitlesson.md:27-51`). `docs/streaming-protocol.md` fixes per-session/per-file SSE semantics, offset handling, resync, reconnect replay, latency, and backpressure behavior (`docs/streaming-protocol.md:7-34`). `docs/plans/viz-monitor-web.md` specifies monitor-web architecture: reuse `.humanize/rlcr` metadata and cache logs, enumerate all sessions, enforce remote auth, add session-scoped cancel, remove project switching, and preserve monitor subcommands (`docs/plans/viz-monitor-web.md:13-100`, `docs/plans/viz-monitor-web.md:224-246`).
- inputs_outputs_state: Inputs documented here include draft plans, generated `.directions.json`, `.humanize/rlcr/<session>/` state files, `~/.cache/humanize/<project>/<session>/round-*` logs, config layers, and installed CLI/skill files. Outputs include plan files, QA ledgers, BitLesson deltas, RLCR round summaries/review results, SSE frames, install directories, and monitor views. State surfaces include `.humanize/rlcr/<timestamp>/`, `.humanize/bitlesson.md`, `.humanize/explore/<RUN_ID>/manifest.json`, and cache log directories documented in `docs/usage.md:105-110`, `docs/usage.md:376-398`, and `docs/bitlesson.md:27-35`.
- gates_or_invariants: Key invariants are: no parallel dashboard capture pipeline (`docs/drafts/viz-monitor-web.md:71-74`, `docs/plans/viz-monitor-web.md:22-29`); monitor-web defaults to localhost and requires token for non-localhost exposure (`docs/usage.md:400-410`, `docs/plans/viz-monitor-web.md:55-63`); SSE path names must be basenames, not absolute paths (`docs/streaming-protocol.md:12-13`); silent data loss is forbidden on backpressure (`docs/streaming-protocol.md:30-31`); BitLesson `Action: none` must not cite concrete lesson IDs and `add` or `update` must cite existing IDs (`docs/bitlesson.md:47-51`); generated implementation code must avoid plan workflow markers (`docs/plans/viz-monitor-web.md:96-100`, `docs/plans/viz-monitor-web.md:345-352`).
- dependencies_and_callers: The docs coordinate with `scripts/humanize.sh` monitor dispatch and cache lookup, `scripts/install-skill.sh` and `scripts/install-codex-hooks.sh` install behavior, `hooks/loop-codex-stop-hook.sh` stop-gate behavior, `scripts/bitlesson-select.sh` and `scripts/bitlesson-validate-delta.sh`, plus `viz/server/*` and `viz/static/js/*` described as plan targets. `README.md` embeds `docs/images/rlcr-workflow.svg` and `docs/images/monitor.png`, making the images user-facing workflow documentation.
- edge_cases_or_failure_modes: Directory-level edge cases include stale/legacy install paths (`docs/install-for-codex.md:34`, `docs/install-for-codex.md:117`), Kimi needing gate-based RLCR rather than Codex native hooks (`docs/install-for-kimi.md:56-58`), remote SSE needing token query params because EventSource cannot set arbitrary headers (`docs/usage.md:407-410`, `docs/plans/viz-monitor-web.md:301-337`), startup races with session metadata but missing cache logs (`docs/plans/viz-monitor-web.md:85-87`), log truncation/rotation resync (`docs/streaming-protocol.md:15-19`), and zombie/single-newest terminal monitor behavior that monitor-web must not inherit (`docs/plans/viz-monitor-web.md:30-36`).
- validation_or_tests: Docs point to concrete test expectations: monitor compatibility, cache-path parity, auth enforcement, project isolation, concurrent sessions, and rotation recovery in `docs/plans/viz-monitor-web.md:83-95`; install behavior is covered by `tests/test-codex-hook-install.sh`; BitLesson behavior by `tests/test-bitlesson-select-routing.sh` and `tests/test-bitlesson-validate-delta.sh`; streaming/monitor surfaces by `tests/test-streaming.sh`, `tests/test-monitor-runtime.sh`, and `tests/test-viz.sh` references in the repository inventory.
- skip_candidate: `no`

### H2_DEV-HZ-035 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: Command workflow definition for converting a draft into an implementation plan that can feed the RLCR loop. It is a core algorithm prompt/controller, not a shell implementation, because it defines phase order, gates, Codex/Claude deliberation, plan schema, and optional auto-start behavior.
- algorithmic_behavior: The command enforces “no coding during plan generation” and limits writes before auto-start to the output plan and optional translation (`commands/gen-plan.md:20-29`). It defines strict sequential phases: mode setup, config load, IO validation, relevance check, Codex first-pass analysis, Claude candidate plan, convergence loop, issue resolution, final plan generation, and write/complete (`commands/gen-plan.md:32-45`). It requires a Codex first-pass before Claude synthesis with structured sections such as risks, gaps, alternatives, questions, and candidate criteria (`commands/gen-plan.md:184-207`). A second Codex review loop repeats until no required changes or high-impact disagreements remain, two no-change rounds occur, or max three rounds is reached (`commands/gen-plan.md:255-298`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, `--input`, `--output`, optional `--auto-start-rlcr-if-converged`, `--discussion`, `--direct`, merged Humanize config, the draft file, repo context, Codex analysis outputs, and user decisions. Outputs are the main plan file, optional translated variant, convergence status, pending decision section, task breakdown, and optionally a started RLCR loop. Internal state names include `AUTO_START_RLCR_IF_CONVERGED`, `GEN_PLAN_MODE`, `ALT_PLAN_LANGUAGE`, `ALT_PLAN_LANG_CODE`, `PLAN_CONVERGENCE_STATUS`, and `HUMAN_REVIEW_REQUIRED` (`commands/gen-plan.md:49-128`, `commands/gen-plan.md:295-310`).
- gates_or_invariants: Gates include mutually exclusive `--discussion` and `--direct` (`commands/gen-plan.md:51-57`), side-effect-free IO validation with explicit exit-code handling (`commands/gen-plan.md:132-150`), relevance check before creating the plan file (`commands/gen-plan.md:154-180`), manual review unless discussion mode converged with auto-start enabled (`commands/gen-plan.md:305-312`), no auto-start when pending decisions remain (`commands/gen-plan.md:596-608`), task tags restricted to `coding` or `analyze` (`commands/gen-plan.md:447-457`, `commands/gen-plan.md:529`), AC format and TDD positive/negative tests (`commands/gen-plan.md:384-401`, `commands/gen-plan.md:511-515`), and preserving all original draft content (`commands/gen-plan.md:521-525`, `commands/gen-plan.md:535-543`).
- dependencies_and_callers: Depends on `${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh`, `${CLAUDE_PLUGIN_ROOT}/scripts/lib/config-loader.sh`, `${CLAUDE_PLUGIN_ROOT}/scripts/ask-codex.sh`, `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh`, `prompt-template/plan/gen-plan-template.md`, `agents/draft-relevance-checker.md`, Task/Explore agents, and AskUserQuestion (`commands/gen-plan.md:4-13`, `commands/gen-plan.md:62-138`, `commands/gen-plan.md:160-179`, `commands/gen-plan.md:190-193`, `commands/gen-plan.md:610-614`).
- edge_cases_or_failure_modes: Handles malformed optional config as warnings but required/default config or missing `jq` as fatal (`commands/gen-plan.md:66-75`, `commands/gen-plan.md:126`). Unsupported alternative languages are disabled with warning (`commands/gen-plan.md:115-118`). Codex unavailability prompts retry or Claude-only planning with reduced confidence (`commands/gen-plan.md:208-213`). Direct mode deliberately sets partial convergence and blocks auto-start (`commands/gen-plan.md:255-258`). Mixed-language plans trigger a user question before unification (`commands/gen-plan.md:555-568`). Auto-start failure reports manual command fallback (`commands/gen-plan.md:610-620`).
- validation_or_tests: `tests/test-gen-plan.sh` validates existence, frontmatter, allowed tools, argument hint, Codex-first workflow, auto-start option, direct-mode partial convergence, discussion-mode auto-start requirement, ultrathink instruction, pending decisions, convergence loop, max-round limit, phase ordering, task breakdown, and template sections (`tests/test-gen-plan.sh:55-247`). IO behavior is separately covered by `tests/test-validate-gen-plan-io.sh`.
- skip_candidate: `no`

### H2_DEV-HZ-065 `file` `scripts/validate-directions-json.sh`
- cursor: `[_]`
- core_role: Runtime validation script enforcing the `directions.json` schema version 1 contract used by idea generation and exploration routing.
- algorithmic_behavior: The script takes exactly a `.directions.json` path, checks file existence and `jq`, then runs a single `jq -e` boolean expression that validates the entire object and exits success only when every schema predicate holds (`scripts/validate-directions-json.sh:15-40`, `scripts/validate-directions-json.sh:115-121`).
- inputs_outputs_state: Input is the path argument in `INPUT_FILE` (`scripts/validate-directions-json.sh:22-31`). Output is `VALIDATION_SUCCESS` on stdout with exit 0, or a stderr error and exit 1/2/3 for missing argument/file, missing `jq`, or schema failure (`scripts/validate-directions-json.sh:7-11`, `scripts/validate-directions-json.sh:116-120`). It has no persistent state and no writes.
- gates_or_invariants: Enforces `schema_version == 1`; required top-level string fields `title`, `original_idea`, and `synthesis_notes`; required `metadata` and `directions`; directions length 1 through 10; exactly one primary; unique and regex-safe `direction_id`; unique branch/path-safe `dir_slug`; integer unique `source_index` bounded by `metadata.n_requested`; `direction_id == dir-<source_index padded>-<dir_slug>`; sequential display order; metadata consistency; timestamp format; confidence enum; and required per-direction string/array fields (`scripts/validate-directions-json.sh:51-114`).
- dependencies_and_callers: Depends only on `bash` and `jq`. It is the concrete validator for the directions companion contract documented in `docs/usage.md:78-103` and exercised by `tests/test-directions-json-schema.sh`. It is likely invoked by gen/explore idea IO validators and command workflows that accept `.directions.json`.
- edge_cases_or_failure_modes: Missing input and help both route to usage exit 1 (`scripts/validate-directions-json.sh:22-24`). Invalid JSON is collapsed into schema failure because the `jq` expression is redirected and failure emits the generic `VALIDATION_FAILED` (`scripts/validate-directions-json.sh:115-120`). `source_index` uses `$root.metadata.n_requested`, so malformed/missing metadata causes the expression to fail closed. Numeric integers are checked by equality to `floor`, rejecting strings (`scripts/validate-directions-json.sh:41-43`, `scripts/validate-directions-json.sh:80-83`). Empty/whitespace `direction_id` fails the regex.
- validation_or_tests: `tests/test-directions-json-schema.sh` runs a positive valid fixture and broad negative mutations: missing schema/version/top-level keys, too many directions, zero/two primaries, duplicate IDs/slugs/source indexes, wrong scalar types, uppercase or spaced slugs, invalid confidence, mismatched metadata counts, and non-string array elements (`tests/test-directions-json-schema.sh:43-260`).
- skip_candidate: `no`

### H2_DEV-HZ-095 `file` `tests/test-codex-hook-install.sh`
- cursor: `[_]`
- core_role: Executable specification for Codex-native hook installation, skill bundle sync, Humanize user config seeding, BitLesson shim dispatch, legacy Codex feature rejection, Kimi RLCR compatibility, and shared-skill-dir safety.
- algorithmic_behavior: The test builds fake Codex CLIs and isolated config homes, invokes `scripts/install-skill.sh`, then inspects installed files, `hooks.json`, generated config, feature-enable logs, shim output, target-specific SKILL content, and installer error logs (`tests/test-codex-hook-install.sh:31-80`, `tests/test-codex-hook-install.sh:121-127`).
- inputs_outputs_state: Inputs are temporary `CODEX_HOME`, `XDG_CONFIG_HOME`, fake `codex`, existing `hooks.json`, installer flags, and fake project BitLesson file (`tests/test-codex-hook-install.sh:31-39`, `tests/test-codex-hook-install.sh:82-119`, `tests/test-codex-hook-install.sh:239-254`). Outputs are pass/fail assertions and temp artifacts: skill directories, `hooks.json`, `.hooks-enabled`, config JSON, command shim, feature logs, and install logs.
- gates_or_invariants: Asserts the installer syncs `skills/humanize` and `humanize-rlcr`, writes hooks, enables native hooks, seeds `bitlesson_model` as `gpt-5.5`, writes `provider_mode: codex-only`, adds exactly one managed Humanize Stop hook, preserves unrelated Stop and SessionStart hooks, removes stale Humanize hooks, is idempotent, and enables features on each install (`tests/test-codex-hook-install.sh:129-237`, `tests/test-codex-hook-install.sh:262-300`). It rejects legacy `codex_hooks` config, legacy-only feature builds, builds without native hooks, and builds with hooks but no `--disable` support (`tests/test-codex-hook-install.sh:302-482`). It requires Kimi to get a gate-based `humanize-rlcr/SKILL.md` without native Stop-hook language and with current model docs (`tests/test-codex-hook-install.sh:484-530`). It rejects `--target both` with shared/equivalent skills dirs (`tests/test-codex-hook-install.sh:561-625`).
- dependencies_and_callers: Sources `tests/test-helpers.sh`, runs `scripts/install-skill.sh`, indirectly exercises `scripts/install-codex-hooks.sh`, uses `python3` and `jq`, and creates fake `codex` binaries to model CLI feature surfaces (`tests/test-codex-hook-install.sh:8-27`, `tests/test-codex-hook-install.sh:179-207`).
- edge_cases_or_failure_modes: Covers excessive `codex --help` output while searching for `--disable` (`tests/test-codex-hook-install.sh:44-52`), stale managed hook paths, repeated installs, legacy config key rename, Codex builds exposing only `codex_hooks`, unsupported builds with no `hooks`, native hooks without recursive-disable support, mixed target provider mode, same physical skills dir through path normalization, and Kimi accidentally receiving Codex-native RLCR instructions.
- validation_or_tests: This file is itself validation. It should be run as a shell test from the repo test suite and ends with `print_test_summary "Codex Hook Install Tests"` (`tests/test-codex-hook-install.sh:627`).
- skip_candidate: `no`

### H2_DEV-HZ-125 `file` `tests/test-stop-hook-legacy-compat.sh`
- cursor: `[_]`
- core_role: Executable regression spec for legacy RLCR stop-hook compatibility around untracked Humanize runtime directories, real untracked config files, and legacy BitLesson opt-in semantics.
- algorithmic_behavior: The test creates temporary Git repositories with a minimal RLCR loop directory, state frontmatter, plan, summary, and goal tracker, then runs `hooks/loop-codex-stop-hook.sh` with a fake Codex binary and observes whether the hook reaches Codex or blocks early (`tests/test-stop-hook-legacy-compat.sh:25-39`, `tests/test-stop-hook-legacy-compat.sh:40-136`).
- inputs_outputs_state: Inputs include a fixture repo, `.gitignore`, `plans/test-plan.md`, `.humanize/rlcr/2026-03-01_00-00-00/state.md`, `round-0-summary.md`, `goal-tracker.md`, optional `.humanize/bitlesson.md`, untracked `.humanize-old`, and untracked `.humanizeconfig` (`tests/test-stop-hook-legacy-compat.sh:47-118`). Outputs are hook stdout/stderr, exit code, and a `codex-called.marker` written by the fake Codex when review is reached (`tests/test-stop-hook-legacy-compat.sh:120-136`).
- gates_or_invariants: Test 1 requires untracked `.humanize-*` directories to be ignored by dirty checks and not block review (`tests/test-stop-hook-legacy-compat.sh:145-160`). Test 1b requires `.humanizeconfig` to be treated as a normal untracked file and block before Codex, while emitting both Humanize-runtime and generic untracked-file notes (`tests/test-stop-hook-legacy-compat.sh:162-183`). Test 2 requires old loops whose state omits `bitlesson_required` to keep BitLesson validation disabled even if a KB file exists (`tests/test-stop-hook-legacy-compat.sh:185-197`).
- dependencies_and_callers: Sources `tests/test-helpers.sh`, calls `hooks/loop-codex-stop-hook.sh`, uses test helper `init_test_git_repo`, relies on Git, and mocks `codex` in `PATH`. It validates behavior implemented in `hooks/loop-codex-stop-hook.sh` around dirty status filtering and frontmatter defaults.
- edge_cases_or_failure_modes: Protects against over-broad `.humanize` ignore rules that would let `.humanizeconfig` slip through; protects legacy loops from retroactive BitLesson blocking when the state file predates `bitlesson_required`; and protects user/runtime local directories like `.humanize-old` from preventing normal review.
- validation_or_tests: This file is itself the validation and exits through `print_test_summary "Stop Hook Legacy Compatibility Test Summary"` (`tests/test-stop-hook-legacy-compat.sh:199-200`). The related hook code filters untracked `.humanize/` and `.humanize-*` directories via `HUMANIZE_UNTRACKED_PATTERN='^\?\? \.humanize[-/]'` while keeping similarly named files outside that pattern (`hooks/loop-codex-stop-hook.sh:699-716`).
- skip_candidate: `no`

### H2_DEV-HZ-155 `file` `hooks/lib/methodology-analysis.sh`
- cursor: `[_]`
- core_role: Stop-hook library for the RLCR methodology-analysis phase, inserted before final loop exit unless privacy mode or prior completion suppresses it.
- algorithmic_behavior: Provides `enter_methodology_analysis_phase`, `complete_methodology_analysis`, and `block_methodology_analysis_incomplete`. Entering the phase skips when `PRIVACY_MODE=true`, when `methodology-analysis-state.md` already exists, or when a non-empty done marker indicates prior completion; otherwise it renames the active state file to `methodology-analysis-state.md`, writes `.methodology-exit-reason`, creates an empty done placeholder, renders a methodology-analysis prompt, and returns a block JSON (`hooks/lib/methodology-analysis.sh:38-100`). Completion requires a non-empty done marker, a non-empty report, and a valid saved exit reason (`hooks/lib/methodology-analysis.sh:114-169`). The block helper emits JSON instructing the agent to complete analysis (`hooks/lib/methodology-analysis.sh:179-202`).
- inputs_outputs_state: Inputs are function arguments `exit_reason` and description plus globals `PRIVACY_MODE`, `STATE_FILE`, `LOOP_DIR`, `CURRENT_ROUND`, `MAX_ITERATIONS`, and `TEMPLATE_DIR` (`hooks/lib/methodology-analysis.sh:22-33`). Outputs/state transitions are `STATE_FILE` renamed to `$LOOP_DIR/methodology-analysis-state.md`, `$LOOP_DIR/.methodology-exit-reason`, `$LOOP_DIR/methodology-analysis-done.md`, expected `$LOOP_DIR/methodology-analysis-report.md`, and JSON block decisions to the hook host (`hooks/lib/methodology-analysis.sh:64-97`, `hooks/lib/methodology-analysis.sh:115-147`).
- gates_or_invariants: Privacy mode defaults to skip; re-entry is blocked; an empty done marker is insufficient; the report must exist and contain non-whitespace; exit reason must be exactly `complete`, `stop`, or `maxiter`; final terminal-state rename is intentionally delegated to the caller after the git-clean gate so active state remains until cleanliness is confirmed (`hooks/lib/methodology-analysis.sh:42-62`, `hooks/lib/methodology-analysis.sh:118-168`).
- dependencies_and_callers: Sourced by `hooks/loop-codex-stop-hook.sh` (`hooks/lib/methodology-analysis.sh:10`, `hooks/loop-codex-stop-hook.sh:53-54`). Uses `load_and_render_safe` from `hooks/lib/template-loader.sh`, `jq`, and `prompt-template/claude/methodology-analysis-prompt.md` with a fallback (`hooks/lib/methodology-analysis.sh:74-97`). The stop hook calls `enter_methodology_analysis_phase` on max iteration, completion, Codex-complete-at-max, and stagnation stop paths (`hooks/loop-codex-stop-hook.sh:974-991`, `hooks/loop-codex-stop-hook.sh:1887`, `hooks/loop-codex-stop-hook.sh:1990-1991`) and calls `complete_methodology_analysis` before git-clean finalization (`hooks/loop-codex-stop-hook.sh:609-668`).
- edge_cases_or_failure_modes: Fail-closed on missing `.methodology-exit-reason`, invalid exit reason, empty/whitespace-only done/report files, and missing report. Potential filesystem failure points include `mv`, `touch`, and marker writes under `set -e` caller context. Completion intentionally blocks if methodology artifacts coexist with uncommitted non-Humanize changes; stop hook applies `.humanize` untracked exclusions before final rename (`hooks/loop-codex-stop-hook.sh:618-663`).
- validation_or_tests: Covered by finalization/stop-hook tests in the suite; related references include `tests/test-finalize-phase.sh`, `tests/test-state-exit-naming.sh`, and robustness tests around state transitions/templates. Direct source references in `hooks/loop-codex-stop-hook.sh:609-668` show the integration gate that checks completion before terminal state rename.
- skip_candidate: `no`

### H2_DEV-HZ-185 `file` `prompt-template/block/round-contract-bash-write.md`
- cursor: `[_]`
- core_role: Block prompt template for a validator gate that forbids Bash-based writes to RLCR round contract files and redirects the agent to structured write/edit tools.
- algorithmic_behavior: The template is short but algorithmically significant: it is rendered when a Bash command attempts to modify a round contract path. It tells the agent “Do not use Bash commands to modify round contract files” and provides `{{CORRECT_PATH}}` as the allowed target (`prompt-template/block/round-contract-bash-write.md:1-7`).
- inputs_outputs_state: Input placeholder is `CORRECT_PATH`, supplied by the validator. Output is a rendered block reason shown to the agent. It does not read or write state itself; it supports the validator state machine by preserving the round contract as a controlled artifact.
- gates_or_invariants: Invariant is tool-channel separation: round contract updates must go through Write/Edit rather than arbitrary shell mutation. That matters because round contracts anchor the current RLCR objective and anti-drift controls.
- dependencies_and_callers: Called by `hooks/loop-bash-validator.sh`, which has an inline fallback with the same message and renders this template through `load_and_render_safe` (`hooks/loop-bash-validator.sh:548-552`). Related templates include `prompt-template/block/round-contract-missing.md`, `prompt-template/claude/next-round-prompt.md`, and `prompt-template/claude/review-phase-prompt.md`. Stop hook requires a round contract when anti-drift state is present (`hooks/loop-codex-stop-hook.sh:834-849`).
- edge_cases_or_failure_modes: If the template is missing, caller fallback still blocks. If `CORRECT_PATH` is wrong or absent, the user-facing remediation degrades, but the block still communicates the invariant. Finalize phase has separate round-contract access templates because there is no active round contract then.
- validation_or_tests: `rg` references show coverage in `tests/test-allowlist-validators.sh`, `tests/test-finalize-phase.sh`, `tests/test-humanize-escape.sh`, and `tests/test-plan-file-hooks.sh`. The direct caller is `hooks/loop-bash-validator.sh:548-552`, while stop-hook missing-contract behavior is tested by `tests/test-plan-file-hooks.sh:495-524`.
- skip_candidate: `no`

### H2_DEV-HZ-215 `file` `prompt-template/codex/commit-history-section.md`
- cursor: `[_]`
- core_role: Codex review prompt section template for Integral Context: it injects accumulated commit history and recent round files so Codex can evaluate progress trends, drift, and repeated issues across RLCR rounds.
- algorithmic_behavior: Renders a `## Development History (Integral Context)` section containing `{{COMMIT_HISTORY}}`, followed by recent round file references from `{{RECENT_ROUND_FILES}}`, and instructs Codex to weight recent rounds while watching systemic trends (`prompt-template/codex/commit-history-section.md:1-12`).
- inputs_outputs_state: Inputs are two rendered placeholders: `COMMIT_HISTORY` and `RECENT_ROUND_FILES`. Outputs are prompt text embedded into Codex review. Upstream state feeding it includes `BASE_COMMIT`, current `HEAD`, `CURRENT_ROUND`, `LOOP_TIMESTAMP`, and round summary/review-result files under `.humanize/rlcr/<session>/`.
- gates_or_invariants: The section must preserve oldest-first commit order and include bounded recent round references. It must degrade gracefully to placeholders like no commits or first round rather than crashing. If base commit is unavailable or unrelated, caller annotates fallback recent commit history instead of failing.
- dependencies_and_callers: Rendered in `hooks/loop-codex-stop-hook.sh` using `load_and_render_safe` with a fallback full section (`hooks/loop-codex-stop-hook.sh:1056-1069`). Depends on `hooks/lib/template-loader.sh`. It is part of the Codex review prompt and is directly tested as the “Integral component.”
- edge_cases_or_failure_modes: Round 0 has no prior files; corrupted or unrelated `BASE_COMMIT` must not trip `set -e`; missing template must use fallback; commit history is bounded (`tail -80` in test/caller logic) to avoid runaway prompt growth. If recent round file list is stale, Codex loses trajectory context.
- validation_or_tests: `tests/test-commit-history-section.sh` validates round 0 placeholders, round 3 commits and round file references, corrupted nonexistent base commit fallback, unrelated base commit fallback, and missing-template fallback rendering (`tests/test-commit-history-section.sh:33-61`, `tests/test-commit-history-section.sh:63-114`, `tests/test-commit-history-section.sh:117-180`, `tests/test-commit-history-section.sh:181-220`).
- skip_candidate: `no`

### H2_DEV-HZ-245 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Robustness specification for RLCR session discovery via `find_active_loop`, especially newest-session selection, finalize-state detection, zombie-loop protection, terminal-state ignore behavior, and odd path cases.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh`, builds temporary `.humanize/rlcr`-like directory trees, calls `find_active_loop`, and asserts whether it returns the expected active session directory or empty string under many filesystem arrangements (`tests/robustness/test-session-robustness.sh:14-20`, `tests/robustness/test-session-robustness.sh:33-317`).
- inputs_outputs_state: Inputs are a loop base directory containing timestamp-like or arbitrary child dirs with `state.md`, `finalize-state.md`, `complete-state.md`, or `cancel-state.md`. Output is the path emitted by `find_active_loop` or empty. The production helper resolves active state in priority order `methodology-analysis-state.md`, `finalize-state.md`, then `state.md` (`hooks/lib/loop-common.sh:264-280`).
- gates_or_invariants: Without session filter, production `find_active_loop` checks only the single newest directory and returns it only if it has active state, preserving zombie-loop protection (`hooks/lib/loop-common.sh:308-356`). With a session filter, it scans newest-to-oldest for the matching session id, accepts empty stored session id for backward compatibility, blocks fallback if the newest matching session is terminal, and optionally adopts a `bg-pending.marker` candidate only when the stop hook opts in (`hooks/lib/loop-common.sh:359-424`). The test focuses on the no-filter path and active-state resolution.
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, `tests/test-helpers.sh`, shell utilities, and `date` for a performance sanity check. Production callers include `hooks/loop-codex-stop-hook.sh`, which calls `find_active_loop "$LOOP_BASE_DIR" "$HOOK_SESSION_ID" true` (`hooks/loop-codex-stop-hook.sh:47-64`), plus validators that use stricter session isolation.
- edge_cases_or_failure_modes: Tests cover many sessions and performance under 15 dirs, newest dir lacking state while older has stale state, both `state.md` and `finalize-state.md`, empty/nonexistent/no-subdir base, no state files, unexpected naming, symlinked session dir, spaces in names, deep nested paths that must not be considered, rapid creation, terminal-only complete/cancel states, mixed finished/newest versus older active stale state, and graceful handling of unusual names (`tests/robustness/test-session-robustness.sh:33-317`).
- validation_or_tests: This file is the executable validation and exits through `print_test_summary "Session Robustness Test Summary"` (`tests/robustness/test-session-robustness.sh:321-323`). Its assertions map directly onto `hooks/lib/loop-common.sh:264-425`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: H2_DEV-HZ-005, H2_DEV-HZ-035, H2_DEV-HZ-065, H2_DEV-HZ-095, H2_DEV-HZ-125, H2_DEV-HZ-155, H2_DEV-HZ-185, H2_DEV-HZ-215, H2_DEV-HZ-245
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`