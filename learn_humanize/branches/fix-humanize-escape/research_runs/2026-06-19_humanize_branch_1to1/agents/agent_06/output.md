# agent_06 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-006 `directory` `tests`
- cursor: `[_]`
- core_role: The `tests` directory is the repository’s executable specification suite for the Humanize RLCR loop. It is flat, with 19 executable shell scripts and no child directories. It validates the loop’s state machine, hook gating, template rendering, plan-file integrity, `.humanize/rlcr` path handling, monitor runtime behavior, and shell portability. The ordered orchestrator is `tests/run-all-tests.sh`, which enumerates suites at `tests/run-all-tests.sh:32` and dispatches the zsh-only suite separately at `tests/run-all-tests.sh:53`.
- algorithmic_behavior: The suite exercises the RLCR algorithm as a hook-driven state machine. Key covered roles are: template loading and single-pass placeholder rendering (`test-template-loader.sh`, `test-templates-comprehensive.sh`, `test-template-references.sh`); shell mutation detection (`test-bash-validator-patterns.sh`, `test-cancel-signal-file.sh`); hook path gates for Read/Write/Edit/Bash (`test-allowlist-validators.sh`, `test-plan-file-hooks.sh`); setup-time plan validation (`test-plan-file-validation.sh`); stop-hook review/finalize transitions (`test-finalize-phase.sh`, `test-state-exit-naming.sh`); transcript todo parsing (`test-todo-checker.sh`); `.humanize` escape protection (`test-humanize-escape.sh`); and monitor runtime/zsh safety (`test-monitor-runtime.sh`, `test-monitor-e2e-real.sh`, `test-zsh-monitor-safety.sh`, `manual-monitor-test.sh`).
- inputs_outputs_state: Test inputs are synthetic git repositories, JSON hook payloads, temporary `.humanize/rlcr/<timestamp>` directories, `state.md` or `finalize-state.md` frontmatter, prompt templates, transcript JSONL files, and fake `codex` binaries. Expected outputs are exit codes, stderr/stdout block text, JSON hook decisions, renamed state files such as `complete-state.md`, and generated prompt/review/finalize files. State transitions are centered on `state.md -> finalize-state.md -> complete-state.md`, plus terminal states like `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, and `unexpected-state.md`, covered by `tests/test-state-exit-naming.sh:3` and finalize cases in `tests/test-finalize-phase.sh:6`.
- gates_or_invariants: The directory enforces these invariants: only the newest `.humanize/rlcr` directory with `state.md` or `finalize-state.md` is active (`hooks/lib/loop-common.sh:58`); legacy `.humanize-loop.local` is intentionally not a loop path (`tests/test-state-exit-naming.sh:242`); direct and broad `git add` operations that can stage `.humanize` are blocked (`tests/test-humanize-escape.sh:83`); plan paths must be relative, in-project, non-symlink, shell-safe, and with consistent git tracking (`tests/test-plan-file-validation.sh:86`); protected files cannot be edited through Write/Edit/Bash; todos must use native TodoWrite except narrow allowlisted historical files; and template rendering must be single-pass to prevent placeholder injection (`hooks/lib/template-loader.sh:50`).
- dependencies_and_callers: The tests depend on `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, the five hooks under `hooks/`, `hooks/check-todos-from-transcript.py`, `scripts/setup-rlcr-loop.sh`, `scripts/humanize.sh`, and templates under `prompt-template/`. They also require `bash`, `jq`, `git`, `python3`, and optionally `zsh`; `tests/run-all-tests.sh:86` skips the zsh suite when zsh is missing. The suite coordinates with sibling implementation paths by treating hook scripts as command-line programs fed JSON hook input and using `CLAUDE_PROJECT_DIR` to bind each synthetic project root.
- edge_cases_or_failure_modes: Edge coverage is broad: missing templates and fallback rendering (`tests/test-error-scenarios.sh:43`), ANSI-colored pass/fail parsing (`tests/test-ansi-parsing.sh:42`), zsh glob failures on empty directories (`tests/test-zsh-monitor-safety.sh:295`), state-file source and destination mutation bypasses (`tests/test-cancel-signal-file.sh:5`), shell wrappers and command operators, YAML quote stripping for state fields (`tests/test-plan-file-hooks.sh:338`), committed plan changes with clean git status (`tests/test-plan-file-hooks.sh:655`), stale/old loop directories, same-basename different-root allowlist bypasses (`tests/test-allowlist-validators.sh:368`), and finalize phase continuing to require clean git, summaries, and completed todos (`tests/test-finalize-phase.sh:13`).
- validation_or_tests: `tests/run-all-tests.sh` is itself the suite validator. It captures each suite’s output, strips ANSI escapes, accumulates `Passed:` and `Failed:` counts, and fails if any suite exits nonzero or reports failures (`tests/run-all-tests.sh:82`, `tests/run-all-tests.sh:99`, `tests/run-all-tests.sh:108`). I did not execute the tests because this task requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-036 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: This file is the primary executable spec for plan-file hook behavior during an active RLCR loop. It directly covers `loop-plan-file-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, and `loop-codex-stop-hook.sh`, as stated in its header at `tests/test-plan-file-hooks.sh:3`. Its core role is to prove the original project plan and loop backup plan cannot be mutated, bypassed, or desynchronized once a loop starts.
- algorithmic_behavior: The script constructs temporary git repos and `.humanize/rlcr/2024-01-01_12-00-00` state at `tests/test-plan-file-hooks.sh:37`. It then feeds hook JSON payloads into validators and checks exits/results. The main phases are: UserPromptSubmit schema/branch/tracking validation (`tests/test-plan-file-hooks.sh:88`); Write/Edit/Bash blocking of loop `plan.md` backups (`tests/test-plan-file-hooks.sh:188`, `tests/test-plan-file-hooks.sh:208`, `tests/test-plan-file-hooks.sh:225`); command-injection bypass prevention (`tests/test-plan-file-hooks.sh:269`); YAML quote parsing (`tests/test-plan-file-hooks.sh:338`); stop-hook plan integrity (`tests/test-plan-file-hooks.sh:466`); section-specific goal-tracker placeholder detection (`tests/test-plan-file-hooks.sh:734`); and negative legacy path handling (`tests/test-plan-file-hooks.sh:1013`).
- inputs_outputs_state: Inputs include JSON hook payloads such as `{"tool_name":"Write","tool_input":{"file_path":".../plan.md"}}`, Bash command strings, mutable `state.md` frontmatter, tracked and gitignored plan files, `round-0-summary.md`, and `goal-tracker.md`. Outputs are expected hook exit codes and block messages: most PreToolUse blockers must exit `2`, while UserPromptSubmit and Stop hooks return JSON with `"decision": "block"` and exit `0`. The script mutates only its temp fixtures: it creates backup `plan.md`, changes/deletes source plan files, creates tracked plan scenarios, commits plan changes, and verifies stop-hook decisions.
- gates_or_invariants: Required fields `plan_tracked` and `start_branch` must exist; missing fields block with schema guidance (`tests/test-plan-file-hooks.sh:121`, implementation in `hooks/loop-plan-file-validator.sh:76`). Branch switching is blocked (`tests/test-plan-file-hooks.sh:164`, implementation at `hooks/loop-plan-file-validator.sh:104`). Plan backup writes, edits, removals, redirects, `tee`, globs, braces, `$()` and backticks are blocked (`tests/test-plan-file-hooks.sh:228`, `tests/test-plan-file-hooks.sh:272`). Stop-hook integrity requires `LOOP_DIR/plan.md` to exist, the project plan to exist, tracked plans to be clean, and content to match backup (`hooks/loop-codex-stop-hook.sh:182`, `hooks/loop-codex-stop-hook.sh:214`, `hooks/loop-codex-stop-hook.sh:230`). Legacy `.humanize-loop.local` must not be treated as protected loop state (`tests/test-plan-file-hooks.sh:1016`).
- dependencies_and_callers: The script depends on real `git`, `jq` through the hooks, `diff` via stop-hook integrity, and shared parser behavior in `hooks/lib/loop-common.sh`. The most relevant implementation links are `parse_state_file` quote stripping at `hooks/lib/loop-common.sh:121`, `is_in_humanize_loop_dir` matching only `.humanize/rlcr/` at `hooks/lib/loop-common.sh:415`, Bash plan-backup mutation detection at `hooks/loop-bash-validator.sh:295`, Write plan backup blocking at `hooks/loop-write-validator.sh:132`, Edit plan backup blocking at `hooks/loop-edit-validator.sh:100`, and stop-hook plan comparison at `hooks/loop-codex-stop-hook.sh:230`.
- edge_cases_or_failure_modes: The spec targets known bypass classes: direct `.humanize/rlcr/plan.md` without timestamp (`tests/test-plan-file-hooks.sh:254`), command substitution (`tests/test-plan-file-hooks.sh:272`), glob expansion (`tests/test-plan-file-hooks.sh:285`), brace expansion (`tests/test-plan-file-hooks.sh:298`), piped `tee` (`tests/test-plan-file-hooks.sh:311`), backticks (`tests/test-plan-file-hooks.sh:324`), quoted YAML branch/plan fields (`tests/test-plan-file-hooks.sh:341`), hyphenated plan paths (`tests/test-plan-file-hooks.sh:431`), tracked plan race conditions via uncommitted changes (`tests/test-plan-file-hooks.sh:562`), committed tracked changes that leave git status clean (`tests/test-plan-file-hooks.sh:655`), missing plan backups (`tests/test-plan-file-hooks.sh:543`), and section-specific placeholder false positives (`tests/test-plan-file-hooks.sh:737`).
- validation_or_tests: The file is self-validating: it increments pass/fail counters through helper functions at `tests/test-plan-file-hooks.sh:26`, prints totals at `tests/test-plan-file-hooks.sh:1057`, and exits with `TESTS_FAILED` at `tests/test-plan-file-hooks.sh:1065`. It is included in the full suite order at `tests/run-all-tests.sh:41`. I inspected it directly and did not run it.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-066 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: This prompt block is the stop-hook user-facing contract for the `--push-every-round` gate. It explains that loop exit is blocked when local commits are ahead of the remote and gives the exact required `git push origin {{CURRENT_BRANCH}}` action.
- algorithmic_behavior: The template is rendered by `hooks/loop-codex-stop-hook.sh` only when `push_every_round` is true and `git status -sb` reports an ahead count. The hook extracts `AHEAD_COUNT` from the `ahead N` marker and `CURRENT_BRANCH` from `git rev-parse --abbrev-ref HEAD`, then renders this block through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:486`, `hooks/loop-codex-stop-hook.sh:491`, `hooks/loop-codex-stop-hook.sh:501`). The rendered text becomes the JSON `reason`, with a system message that includes the ahead count (`hooks/loop-codex-stop-hook.sh:505`).
- inputs_outputs_state: Inputs are `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}`, both supplied by the stop hook. Output is Markdown with a required push command. It does not alter state itself; it is an explanatory layer over the stop-hook state gate that blocks exit before review/finalization can proceed. The related state input is `push_every_round`, parsed from state frontmatter by `parse_state_file` via `FIELD_PUSH_EVERY_ROUND` (`hooks/lib/loop-common.sh:21`, `hooks/lib/loop-common.sh:128`).
- gates_or_invariants: The invariant is that when `--push-every-round` is enabled, a clean working tree is not enough; local branch ahead status must be zero before exit. The block states the gate at `prompt-template/block/unpushed-commits.md:3` and mandates `git push origin {{CURRENT_BRANCH}}` at `prompt-template/block/unpushed-commits.md:7`. Bash pushes are also blocked when `push_every_round` is false, which prevents unnecessary remote pushes in the default mode (`hooks/loop-bash-validator.sh:56`).
- dependencies_and_callers: Its direct caller is `loop-codex-stop-hook.sh`; rendering depends on `hooks/lib/template-loader.sh`, whose renderer performs single-pass `{{VAR}}` replacement and preserves missing placeholders (`hooks/lib/template-loader.sh:50`). It is covered by the comprehensive template suite, which renders the block with `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch` and checks for all key terms (`tests/test-templates-comprehensive.sh:517`).
- edge_cases_or_failure_modes: If `git status -sb` output format does not include `ahead N`, this block is not triggered. If branch lookup fails, the hook uses `unknown` as a fallback branch name (`hooks/loop-codex-stop-hook.sh:494`). If either placeholder is missing during rendering, the single-pass renderer leaves it visible rather than failing closed; the template reference and comprehensive tests guard against missing template files and basic render regressions.
- validation_or_tests: `tests/test-templates-comprehensive.sh:517` validates this exact template renders the title, ahead count, and branch. `tests/test-template-references.sh` scans hook references and verifies referenced templates exist, with the reference extraction rules at `tests/test-template-references.sh:67`. I inspected the template directly at `prompt-template/block/unpushed-commits.md:1`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3 unique item evidence headings present, matching the assigned rows in order`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`