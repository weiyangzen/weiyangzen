# agent_22 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-022 `file` `scripts/humanize.sh`
- cursor: `[_]`
- core_role: User-facing shell utility for the Humanize RLCR loop monitor. It exposes `humanize monitor rlcr-loop` and implements a live terminal monitor for the newest `.humanize/rlcr/<timestamp>/` session plus the matching Codex run log in `$HOME/.cache/humanize/...`; main entry is `humanize()` at `scripts/humanize.sh:806-838`, which dispatches to `_humanize_monitor_codex()` at `scripts/humanize.sh:9-803`.
- algorithmic_behavior: The monitor first requires `.humanize/rlcr` to exist (`scripts/humanize.sh:20-25`), finds the newest timestamp-named session using `find` and a strict `YYYY-MM-DD_HH-MM-SS` regex (`scripts/humanize.sh:28-49`), then finds the newest `round-*-codex-run.log` in the project-specific cache (`scripts/humanize.sh:53-109`). It parses loop status by preferring `state.md` as active and otherwise using the first `*-state.md` stop file (`scripts/humanize.sh:116-150`). It continuously redraws a fixed status bar (`scripts/humanize.sh:316-453`) and streams log changes incrementally by comparing file sizes and tailing new bytes (`scripts/humanize.sh:670-715`). It switches to newer sessions/logs as they appear (`scripts/humanize.sh:718-797`).
- inputs_outputs_state: Inputs are current working directory, `.humanize/rlcr`, session subdirectory names, `state.md` or stop-state files, `goal-tracker.md`, git status, terminal dimensions from `tput`, and Codex log files under `$HOME/.cache/humanize/<sanitized-project-path>/<timestamp>/`. The cache path mirrors `hooks/loop-codex-stop-hook.sh:779-791`. Outputs are terminal UI, streamed log text, usage/errors, and exit status. It does not write repository files; state transitions are in-memory: `current_session_dir`, `current_file`, `current_loop_status`, `last_size`, `monitor_running`, and `cleanup_done`.
- gates_or_invariants: Session discovery is bounded to direct children of `.humanize/rlcr` and timestamp regex names (`scripts/humanize.sh:37-47`). zsh compatibility is an explicit invariant: `ksharrays` is enabled locally (`scripts/humanize.sh:10-12`) and directory/log iteration uses `find` rather than unmatched globs. The monitor refuses to start with no loop dir or no valid session (`scripts/humanize.sh:20-25`, `scripts/humanize.sh:521-525`). Terminal cleanup is idempotent through `cleanup_done` and restores the scroll region before reporting stop (`scripts/humanize.sh:465-497`). Deleting `.humanize/rlcr` is treated as graceful completion of monitoring, not a crash (`scripts/humanize.sh:547-552`, `scripts/humanize.sh:682-685`).
- dependencies_and_callers: Called through the shell function `humanize monitor rlcr-loop` (`scripts/humanize.sh:810-816`). Depends on standard tools `find`, `basename`, `grep`, `sed`, `sort`, `wc`, `git`, `stat`, `tail`, `tput`, and `clear`. It depends on the loop producer writing state and cache logs; the cache naming contract is produced by `hooks/loop-codex-stop-hook.sh:779-791`. Tests and references include `tests/test-monitor-e2e-real.sh:59-224` for real bash monitoring, `tests/test-monitor-e2e-real.sh:227-359` for zsh, `tests/test-monitor-runtime.sh:56-323` for cleanup behavior, `tests/test-zsh-monitor-safety.sh:59-324` for glob-safety patterns, and `tests/test-humanize-escape.sh:194-332` for find-based iteration/session detection.
- edge_cases_or_failure_modes: If `.humanize/rlcr` is missing, startup returns failure; if it is deleted during monitoring, cleanup runs and returns success. If no log exists, it displays a waiting/no-log state and keeps polling (`scripts/humanize.sh:568-667`). If a log is truncated or rotated, it clears the log target and searches again (`scripts/humanize.sh:704-714`). If multiple stop-state files exist, `find` order chooses the first one, so status can be nondeterministic. `round_num` is compared numerically, so malformed matching filenames such as `round-foo-codex-run.log` can cause shell comparison errors. Pipe-delimited parser outputs can be corrupted by pipe characters in parsed values. Very narrow terminals can make truncation math fragile because `max_display_len` is derived directly from `tput cols` (`scripts/humanize.sh:371-383`).
- validation_or_tests: I inspected but did not execute tests because this task requested research notes only. Relevant coverage asserts graceful deletion stop, scroll-region restore, no zsh glob errors, bash/zsh end-to-end monitor exit, session discovery, and state-file discovery. The read-only export also did not expose a normal git checkout to `git status`, so repository mutation checks were not run.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-052 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Small but core gate-message template for the RLCR Bash validator. It defines the user-facing block reason when an agent attempts `git push` during a loop that was not started with `--push-every-round`.
- algorithmic_behavior: The template itself is static Markdown (`prompt-template/block/git-push.md:1-9`): title, local-commit policy, and the restart/start example using `/humanize:start-rlcr-loop plan.md --push-every-round`. The algorithmic transition is implemented by `hooks/loop-bash-validator.sh:56-72`: after parsing active loop state, if `push_every_round` is not `true`, commands matching leading `git push` are blocked, this template is rendered to stderr, and the hook exits with code `2`.
- inputs_outputs_state: Inputs are the active loop state parsed by `parse_state_file`, especially `push_every_round` from the state schema (`hooks/lib/loop-common.sh:15-24`, `hooks/lib/loop-common.sh:98-120`), the Bash command string from hook JSON (`hooks/loop-bash-validator.sh:22-30`), and the template loader. Output is a blocking message rendered via `load_and_render_safe` (`hooks/loop-bash-validator.sh:66-71`). The template mutates no state; the state transition is external: the attempted Bash tool action is rejected unless the loop was configured for per-round pushes.
- gates_or_invariants: Default invariant is local-only commits during RLCR unless `push_every_round: true`. The start script sets that flag from `--push-every-round` (`scripts/setup-rlcr-loop.sh:148-150`) and adds the corresponding positive instruction to round prompts (`scripts/setup-rlcr-loop.sh:655-660`). Template loading is guarded by `load_and_render_safe`, so a missing template falls back to inline text rather than allowing the command (`hooks/lib/template-loader.sh:170-203`).
- dependencies_and_callers: Direct caller is `hooks/loop-bash-validator.sh:63-72`. Template loading behavior comes from `hooks/lib/template-loader.sh:36-48` and rendering/fallback from `hooks/lib/template-loader.sh:134-203`. User-visible option documentation appears in `commands/start-rlcr-loop.md:3` and `README.md:96`. Related opposite-side template is `prompt-template/block/unpushed-commits.md`, used when pushing is required after opting in.
- edge_cases_or_failure_modes: The hook regex is anchored to commands beginning with `git push` after whitespace normalization (`hooks/loop-bash-validator.sh:65`), so forms like `git -C repo push`, `command git push`, `env git push`, or chained `cd repo && git push` may not be covered by this specific gate unless another validator catches them. The template hardcodes `plan.md` in its example and has no placeholders, so it cannot echo the actual active plan path. If the template is empty or missing, fallback text still blocks, but the exact wording changes.
- validation_or_tests: Template existence and loader behavior are covered by `tests/test-template-loader.sh:55-65` and `tests/test-template-loader.sh:211-222`; comprehensive template tests also verify that the real template overrides fallback text at `tests/test-templates-comprehensive.sh:456-464`. `tests/test-template-references.sh` scans loader calls to ensure referenced templates exist. I inspected these tests but did not execute them because this run is research-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2 item evidence sections present, matching the two assigned rows`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`