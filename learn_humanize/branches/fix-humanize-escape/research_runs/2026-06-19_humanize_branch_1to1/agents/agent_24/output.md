# agent_24 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-024 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Bootstrap/runtime setup entrypoint for `/humanize:start-rlcr-loop`. It validates a user-provided implementation plan, records loop configuration, creates the loop state directory under `.humanize/rlcr/<timestamp>/`, snapshots the plan, initializes `goal-tracker.md`, generates `round-0-prompt.md`, and prints the first-round instructions. The slash command delegates directly to this script via `commands/start-rlcr-loop.md:4` and `commands/start-rlcr-loop.md:13`.
- algorithmic_behavior: Argument parsing accepts one plan path or `--plan-file`, plus `--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, and `--track-plan-file` in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:104). `--codex-model` splits `MODEL:EFFORT` by colon, defaulting effort when omitted in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:121). The script sources `scripts/portable-timeout.sh` for bounded git probes in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:27); that helper chooses `gtimeout`, GNU `timeout`, Python subprocess timeout, or no-timeout fallback in `scripts/portable-timeout.sh:10`.
- inputs_outputs_state: Inputs are CLI args, `CLAUDE_PROJECT_DIR` or current directory, the plan file, git repository state, current branch, `codex` availability, and current date/time. Outputs are `.humanize/rlcr/<timestamp>/plan.md`, `state.md`, `goal-tracker.md`, `round-0-prompt.md`, and user-facing activation/completion instructions. The state file records `current_round: 0`, max iterations, Codex model/effort/timeout, push policy, plan path/tracking flag, start branch, and UTC start time in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:485). The goal tracker is initialized with immutable goal/criteria and mutable task/evidence tables in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:507).
- gates_or_invariants: The script fails closed unless it is inside a git repo with at least one commit in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:211). Plan paths must be relative, contain no spaces, avoid shell metacharacters, not be symlinks, have an existing parent, be readable, and resolve under the project root in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:227). Submodule-contained plans are rejected when `.gitmodules` exists in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:292). Tracking mode is strict: `--track-plan-file` requires tracked and clean; otherwise the plan must not be tracked in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:331). Content must have at least five physical lines and at least three nonblank, noncomment content lines in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:362). Branch/model/effort values are sanitized for YAML safety in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:435).
- dependencies_and_callers: Called by `commands/start-rlcr-loop.md`, described in `README.md`, and paired with hooks under `hooks/` that police loop files after setup. It depends on `git`, `codex`, shell utilities (`wc`, `grep`, `sed`, `awk`, `date`, `cp`, `mkdir`), and `scripts/portable-timeout.sh`. Runtime consumers include `scripts/humanize.sh`, which monitors `.humanize/rlcr`, parses `state.md`, and reads `goal-tracker.md`; stop/review behavior is implemented by `hooks/loop-codex-stop-hook.sh`, which also expects this state layout. The generated `round-0-prompt.md` explicitly requires goal tracker initialization before implementation in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:602).
- edge_cases_or_failure_modes: Absolute paths, spaces, shell metacharacters, symlink plans, missing parents, unreadable files, path traversal outside root, submodule plans, tracked plans without explicit tracking mode, untracked/dirty tracked plans under explicit tracking mode, too-short or comment-only plans, missing `codex`, unsafe branch names, unsafe model names, unsafe effort names, and timed-out git commands all stop setup. One notable heuristic edge case: lines starting with `#` are treated as comments rather than Markdown headings for content sufficiency, so a plan made only of Markdown headings can fail content validation; this is intentional per comments in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:372). Goal/acceptance extraction is heuristic and may leave placeholders for Round 0 if expected headings are absent in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:527).
- validation_or_tests: `tests/test-plan-file-validation.sh` directly exercises this script. Covered cases include absolute paths, nonexistent files/directories, spaces, metacharacters, and symlinks in `tests/test-plan-file-validation.sh:89`; blank/sparse/comment-only plans and valid content acceptance in `tests/test-plan-file-validation.sh:520`; CLI option merging and conflict rejection in `tests/test-plan-file-validation.sh:667`; and Codex model/effort validation in `tests/test-plan-file-validation.sh:700`. I did not run tests because this assignment requested read-only research notes and the workspace permissions are read-only.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-054 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: Prompt/block template for a validation denial shown when Bash attempts to modify `goal-tracker.md`. It is part of the RLCR guard contract: goal-tracker edits must go through write/edit tools rather than shell writes so validation hooks can see and constrain the mutation.
- algorithmic_behavior: The template is intentionally small and deterministic. It emits a heading, states that Bash must not modify `goal-tracker.md`, renders the allowed target path with `{{CORRECT_PATH}}`, and explains that commands such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks in [prompt-template/block/goal-tracker-bash-write.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/prompt-template/block/goal-tracker-bash-write.md:1).
- inputs_outputs_state: Input is the `CORRECT_PATH` template variable supplied by `goal_tracker_bash_blocked_message`. Output is the block message sent to stderr by the Bash validator. It does not mutate state itself; it redirects the actor toward the active loop’s `goal-tracker.md` path, normally `.humanize/rlcr/<timestamp>/goal-tracker.md`.
- gates_or_invariants: The invariant is “Bash may not be the writer for goal tracker updates.” In Round 0, Bash writes to `goal-tracker.md` are blocked with this template and the correct path is provided; after Round 0, a different goal-tracker modification block directs changes through summary/request flow. The Round 0 branch is in `hooks/loop-bash-validator.sh:308`, which calls `goal_tracker_bash_blocked_message "$ACTIVE_LOOP_DIR/goal-tracker.md"` at `hooks/loop-bash-validator.sh:309`.
- dependencies_and_callers: Rendered by `hooks/lib/loop-common.sh:241`, which calls `load_and_render_safe "$TEMPLATE_DIR" "block/goal-tracker-bash-write.md"` with `CORRECT_PATH` at `hooks/lib/loop-common.sh:247`. Its primary caller is `hooks/loop-bash-validator.sh`, which detects goal tracker modification attempts and exits with denial code after printing the rendered message. The template is referenced as an expected common template in `tests/test-template-references.sh:152`.
- edge_cases_or_failure_modes: If the template is missing or rendering fails, `hooks/lib/loop-common.sh` has a fallback string with the same core instruction in `hooks/lib/loop-common.sh:243`. The detection quality depends on `command_modifies_file`; tests show coverage for redirection, `tee`, in-place editors, file operations, `dd`, `truncate`, and descriptor redirection, while read-only commands are not supposed to be false positives in `tests/test-bash-validator-patterns.sh:75`. The template text names common shell commands but does not enumerate every blocked mutation form; detection lives in the hook, not the template.
- validation_or_tests: `tests/test-template-references.sh:152` verifies this template exists in the common template set. `tests/test-bash-validator-patterns.sh:75` validates command-pattern classification for `goal-tracker.md` Bash modifications and non-modifying reads. `tests/test-template-loader.sh` also exercises template rendering with variables, including `CORRECT_PATH`, based on repository search hits. I did not run tests under the read-only research constraint.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `FIX_HUMANIZE_ESCAPE-HZ-024`, `FIX_HUMANIZE_ESCAPE-HZ-054`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`