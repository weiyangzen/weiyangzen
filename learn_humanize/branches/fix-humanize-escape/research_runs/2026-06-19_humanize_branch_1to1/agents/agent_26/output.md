# agent_26 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-026 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: This is the repository’s aggregate executable validation gate for the Humanize plugin test surface. It defines the ordered suite list, dispatches each suite, normalizes child-suite output, accumulates pass/fail totals, and exits nonzero if any suite fails. It is core algorithm evidence because it encodes which behavioral specifications must pass for the plugin, including the branch-specific escape regression suite at `tests/test-humanize-escape.sh`.

- algorithmic_behavior: The script initializes global counters and a failure array at `tests/run-all-tests.sh:28-30`, then declares an ordered `TEST_SUITES` array at `tests/run-all-tests.sh:32-51`. That order is part of the validation contract: template loading, validator pattern checks, todo/plan validation, template reference coverage, state/exit naming, comprehensive templates, hook behavior, error scenarios, ANSI parsing, allowlist validators, finalize/cancel behavior, humanize escape fixes, zsh monitor safety, monitor runtime, and a real monitor E2E suite. For each suite it builds `suite_path="$SCRIPT_DIR/$suite"` at `tests/run-all-tests.sh:58-60`, skips missing files rather than failing the whole run at `tests/run-all-tests.sh:61-64`, detects whether the suite must run under zsh at `tests/run-all-tests.sh:66-73`, executes it while capturing combined stdout/stderr at `tests/run-all-tests.sh:82-96`, strips ANSI color sequences at `tests/run-all-tests.sh:99-101`, extracts the last reported `Passed:` and `Failed:` counts at `tests/run-all-tests.sh:102-103`, accumulates totals at `tests/run-all-tests.sh:105-106`, records failures when either the suite exit code is nonzero or the parsed failed count is greater than zero at `tests/run-all-tests.sh:108-115`, then emits a final summary and exits `1` or `0` at `tests/run-all-tests.sh:119-137`.

- inputs_outputs_state: Primary input is the set of executable child scripts in the same directory as the runner, discovered by fixed names in `TEST_SUITES`, not by filesystem globbing. Another input is shell availability, especially `zsh` for `test-zsh-monitor-safety.sh`, which is listed in `ZSH_TESTS` at `tests/run-all-tests.sh:53-56`. Child suites must print summary lines matching `Passed:[[:space:]]*[0-9]+` and `Failed:[[:space:]]*[0-9]+`; the aggregate runner derives numeric state from those strings at `tests/run-all-tests.sh:102-103`. Runtime state consists of `TOTAL_PASSED`, `TOTAL_FAILED`, and `FAILED_SUITES`, initialized once and updated after each child suite. Outputs are human-readable pass/fail logs, tail output from failed suites, aggregate totals, and the process exit code. The `ls -l` inspection showed `tests/run-all-tests.sh` and child scripts such as `tests/test-humanize-escape.sh` are executable, which matters because non-zsh suites are invoked directly via `"$suite_path"` at `tests/run-all-tests.sh:94`.

- gates_or_invariants: The runner’s main invariant is that a suite passes only when both its process exit code is zero and its parsed `Failed:` count is zero (`tests/run-all-tests.sh:108`). A missing suite is treated as `SKIP`, not failure (`tests/run-all-tests.sh:61-64`), so presence and executable permissions are external completeness assumptions. `zsh` absence also skips zsh-only suites (`tests/run-all-tests.sh:86-92`), making zsh coverage conditional on tool availability. ANSI-stripping is a portability invariant: it uses `esc=$'\033'` plus `sed "s/${esc}\\[[0-9;]*m//g"` (`tests/run-all-tests.sh:99-101`), and `tests/test-ansi-parsing.sh` explicitly tests that same approach for GNU/Linux and BSD/macOS sed compatibility at `tests/test-ansi-parsing.sh:3-6` and `tests/test-ansi-parsing.sh:38-47`.

- dependencies_and_callers: The runner depends on Bash, standard Unix tools (`sed`, `grep`, `tail`), optional `zsh`, and every listed child suite. It is self-documented as invoked by `./tests/run-all-tests.sh` at `tests/run-all-tests.sh:5`. The branch-specific suite `tests/test-humanize-escape.sh` is included at `tests/run-all-tests.sh:47`; that child sources `hooks/lib/loop-common.sh` at `tests/test-humanize-escape.sh:16-18` and validates the escape fix behaviors around `.humanize` path detection and zsh-safe filesystem iteration. The runner is referenced by `tests/test-ansi-parsing.sh` as the consumer of the ANSI stripping pattern at `tests/test-ansi-parsing.sh:5`.

- edge_cases_or_failure_modes: If a child suite omits or changes its `Passed:`/`Failed:` summary labels, the runner may parse `0` via the `|| echo "0"` fallback at `tests/run-all-tests.sh:102-103`, so the exit code remains the stronger signal. If a suite prints multiple summaries, the runner uses `tail -1`, so the last summary wins. Missing suites and missing zsh are skipped without incrementing failure totals, which can hide absent coverage unless the caller separately validates suite inventory. Because the script uses `set -uo pipefail` but intentionally disables `errexit` around child execution (`tests/run-all-tests.sh:83-97`), failed child suites are captured and summarized rather than aborting the aggregate loop. If a direct child script loses executable permission, the direct invocation at `tests/run-all-tests.sh:94` will fail and be recorded as a failed suite.

- validation_or_tests: The runner itself is validation infrastructure. Its parsing behavior is covered by `tests/test-ansi-parsing.sh`, which tests basic ANSI stripping, multiple color codes, extraction of colored `Passed:` and `Failed:` counts, zero counts, and multiline output parsing at `tests/test-ansi-parsing.sh:41-130`. The included `tests/test-humanize-escape.sh` verifies the branch’s named fixes: blocking `.humanize` path variants, quoted variants, force/all forms, chained commands, `git -C` forms, allowed non-target paths, zsh-safe empty/dotfile directory iteration, `*-state.md` detection, and latest session directory detection. Examples include `.humanize` path blocking assertions at `tests/test-humanize-escape.sh:83-101`, forced broad add blocking at `tests/test-humanize-escape.sh:109-119`, zsh-safe `find` iteration at `tests/test-humanize-escape.sh:204-237`, and session directory selection at `tests/test-humanize-escape.sh:303-332`. I did not execute the runner because this assignment requested research notes only and the workspace is read-only.

- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-056 `file` `prompt-template/block/goal-tracker-not-initialized.md`
- cursor: `[_]`
- core_role: This prompt block is the user-facing block reason emitted when Round 0 exits before the Goal Tracker has replaced required placeholders. It is a transition contract between the stop hook’s validation gate and the agent’s next required action: initialize `goal-tracker.md` before attempting to exit again.

- algorithmic_behavior: The template declares the state at `prompt-template/block/goal-tracker-not-initialized.md:3`: Round 0 and Goal Tracker not initialized. It renders the target file and missing section list through `{{GOAL_TRACKER_FILE}}` and `{{MISSING_ITEMS}}` at `prompt-template/block/goal-tracker-not-initialized.md:5-6`. Its action sequence requires reading the tracker, replacing placeholders with actual Ultimate Goal, 3-7 testable Acceptance Criteria, and Active Tasks mapped to ACs, then writing the updated file at `prompt-template/block/goal-tracker-not-initialized.md:8-14`. It encodes a state transition invariant at `prompt-template/block/goal-tracker-not-initialized.md:16`: the immutable section is settable only in Round 0 and read-only after that. The final instruction at `prompt-template/block/goal-tracker-not-initialized.md:18` allows retrying exit only after this state has been repaired.

- inputs_outputs_state: Inputs are two render variables: `GOAL_TRACKER_FILE`, the concrete path to `.humanize/rlcr/.../goal-tracker.md`, and `MISSING_ITEMS`, a newline list generated by the stop hook. The runtime state inspected by the caller is not inside the template; it is built in `hooks/loop-codex-stop-hook.sh`, where `GOAL_TRACKER_FILE="$LOOP_DIR/goal-tracker.md"` is set at `hooks/loop-codex-stop-hook.sh:560`, the check runs only when not finalizing, `CURRENT_ROUND` is `0`, and the file exists at `hooks/loop-codex-stop-hook.sh:562-563`. The hook extracts `### Ultimate Goal`, `### Acceptance Criteria`, and `#### Active Tasks` sections at `hooks/loop-codex-stop-hook.sh:573-590`, marks each missing if it contains the generic placeholder pattern `[To be ` at `hooks/loop-codex-stop-hook.sh:577-593`, builds the `MISSING_ITEMS` bullet list at `hooks/loop-codex-stop-hook.sh:596-609`, renders this template through `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:611-618`, then outputs JSON with `"decision": "block"` and a system message at `hooks/loop-codex-stop-hook.sh:620-627`.

- gates_or_invariants: The gate is Round 0 only and explicitly skipped during Finalize Phase (`hooks/loop-codex-stop-hook.sh:557-563`). The missing check is section-scoped rather than whole-file, which prevents placeholder examples elsewhere in the file from triggering false positives; the hook comments call out that intent at `hooks/loop-codex-stop-hook.sh:564-567`. The template requires 3-7 acceptance criteria at `prompt-template/block/goal-tracker-not-initialized.md:12`, creating a stronger completion contract than merely removing placeholder text. The immutable-section rule is the key invariant: Ultimate Goal and Acceptance Criteria may be initialized in Round 0, but later flows treat that section as read-only.

- dependencies_and_callers: The direct caller is `hooks/loop-codex-stop-hook.sh`, which renders `block/goal-tracker-not-initialized.md` with a fallback message if the template is missing or empty (`hooks/loop-codex-stop-hook.sh:611-618`). Rendering is provided by `hooks/lib/template-loader.sh`: placeholders use `{{VARIABLE_NAME}}` syntax (`hooks/lib/template-loader.sh:7-13`), `render_template` performs single-pass substitution to avoid placeholder injection (`hooks/lib/template-loader.sh:50-57`), and `load_and_render_safe` falls back when a template is missing or renders empty (`hooks/lib/template-loader.sh:167-203`). Initial goal-tracker placeholders are created by `scripts/setup-rlcr-loop.sh`, including the default Ultimate Goal placeholder at `scripts/setup-rlcr-loop.sh:527-540`, Acceptance Criteria placeholder at `scripts/setup-rlcr-loop.sh:550-557`, and Active Tasks placeholder row at `scripts/setup-rlcr-loop.sh:574-579`. The initial Round 0 prompt also tells the worker to initialize the Goal Tracker first at `scripts/setup-rlcr-loop.sh:605-615`.

- edge_cases_or_failure_modes: If `goal-tracker.md` does not exist, this specific gate does not run because the hook requires `[[ -f "$GOAL_TRACKER_FILE" ]]` (`hooks/loop-codex-stop-hook.sh:563`); missing-file behavior must be handled elsewhere. If placeholders are reworded without the `[To be ` pattern, this hook may not detect them. If a valid user-supplied section literally contains `[To be ` text, the hook may block even if the section is semantically initialized. The `awk` section extraction stops on any `^##` heading for all three sections (`hooks/loop-codex-stop-hook.sh:575`, `hooks/loop-codex-stop-hook.sh:582`, `hooks/loop-codex-stop-hook.sh:590`); malformed heading hierarchy can alter what content is inspected. If this template is missing, the fallback at `hooks/loop-codex-stop-hook.sh:612-615` is much shorter and loses the full action contract, though still blocks.

- validation_or_tests: Section-specific behavior is tested in `tests/test-plan-file-hooks.sh`. Test 14.1 builds a tracker with only Ultimate Goal still placeholdered and asserts only that missing item is reported at `tests/test-plan-file-hooks.sh:779-807`. Test 14.2 covers only Acceptance Criteria missing at `tests/test-plan-file-hooks.sh:848-876`. Test 14.3 covers only Active Tasks missing at `tests/test-plan-file-hooks.sh:917-943`. Test 14.4 covers all three placeholders being present and requires all three missing-item lines at `tests/test-plan-file-hooks.sh:984-1010`. The template syntax and renderer are covered by `tests/test-templates-comprehensive.sh`, which validates placeholder syntax generally at `tests/test-templates-comprehensive.sh:122-191` and renders real templates with dummy placeholder assignments at `tests/test-templates-comprehensive.sh:576-596`. The exact template file also appeared in the `prompt-template/block` inventory and has no executable bit, as expected for a Markdown prompt block.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2 unique Item Evidence sections present; each assigned row is represented once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`