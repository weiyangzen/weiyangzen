# agent_16 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-016 `file` `hooks/loop-bash-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Bash gate for the RLCR loop. It prevents shell-command edits from bypassing the dedicated Read/Write/Edit validators and protects RLCR state, plan, prompt, summary, todo, goal-tracker, and `.humanize` version-control boundaries. It is registered as the Bash PreToolUse hook in `hooks/hooks.json:42-48`.
- algorithmic_behavior: The hook reads JSON hook input from stdin, extracts `.tool_name`, exits immediately unless the tool is `Bash`, then lowercases `.tool_input.command` for pattern matching (`hooks/loop-bash-validator.sh:22-30`). It locates the newest active RLCR loop under `${CLAUDE_PROJECT_DIR:-pwd}/.humanize/rlcr` via `find_active_loop`; without an active loop it permits the command (`hooks/loop-bash-validator.sh:36-43`, helper in `hooks/lib/loop-common.sh:63-81`). It chooses `finalize-state.md` over `state.md` when Finalize Phase is active, parses frontmatter state, and uses `current_round` and `push_every_round` to drive later gates (`hooks/loop-bash-validator.sh:45-63`, parser in `hooks/lib/loop-common.sh:112-139`).
- inputs_outputs_state: Input is Claude hook JSON containing `tool_name` and `tool_input.command`. Output is either silent success with exit `0`, or a rendered blocking message on stderr with exit `2` for command-blocking PreToolUse cases (`hooks/loop-bash-validator.sh:25-27`, `66-72`, `81-84`, `98-115`, `295-300`, `308-317`, `324-327`, `334-337`, `344-350`). State read includes `.humanize/rlcr/<loop>/state.md` or `finalize-state.md`; state variables used here are `STATE_CURRENT_ROUND` and `STATE_PUSH_EVERY_ROUND`. The only intentional allow-state transition it permits is an authorized cancellation rename from active `state.md` or `finalize-state.md` to `cancel-state.md` when `.cancel-requested` exists, delegated to `is_cancel_authorized` (`hooks/loop-bash-validator.sh:98-112`, `246-259`; helper `hooks/lib/loop-common.sh:274-413`).
- gates_or_invariants: Blocks `git push` unless `push_every_round: true` (`hooks/loop-bash-validator.sh:63-72`). Blocks git-add operations that would commit `.humanize`, including direct `.humanize` paths, force-add broad scopes, and `git add -A` when `.humanize` exists (`hooks/loop-bash-validator.sh:81-84`; helper `hooks/lib/loop-common.sh:421-539`). Blocks any detected shell modification to `finalize-state.md` before generic `state.md` matching, because `state\.md` also matches `finalize-state.md` (`hooks/loop-bash-validator.sh:87-115`). It separately blocks `mv`/`cp` when protected state files are the source, not only destination (`hooks/loop-bash-validator.sh:117-263`). It strips leading redirections and splits chained segments so patterns like prefixed redirections, `;`, `&&`, `||`, `|`, `|&`, and background operators are checked per segment while preserving redirection operators with placeholders (`hooks/loop-bash-validator.sh:128-243`). It blocks shell-wrapper bypasses containing `sh -c` or `bash -c` plus `mv`/`cp` of state files (`hooks/loop-bash-validator.sh:265-287`). It blocks writes to `.humanize/rlcr/.../plan.md`, `goal-tracker.md`, `round-*-prompt.md`, `round-*-summary.md`, and non-allowlisted `round-*-todos.md` (`hooks/loop-bash-validator.sh:295-352`). The todos exception requires a full active-loop path and only permits `round-1-todos.md` or `round-2-todos.md` under the active loop path, not same-basename paths elsewhere (`hooks/loop-bash-validator.sh:344-351`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` for active-loop detection, state parsing, lowercasing, template-backed block messages, git-add detection, protected file matching, and cancel authorization (`hooks/loop-bash-validator.sh:14-16`). `loop-common.sh` itself sources `hooks/lib/template-loader.sh`, which gives single-pass template rendering and fallback behavior (`hooks/lib/loop-common.sh:46-55`, `hooks/lib/template-loader.sh:50-58`). The generic modification matcher recognizes redirection, append redirection, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`/`cp` destination writes, `rm`, `dd of=`, `truncate`, `printf >`, and `exec >` patterns (`hooks/lib/loop-common.sh:571-599`). Templates used indirectly include block messages such as `block/git-push.md`, `block/plan-backup-protected.md`, `block/state-file-modification.md`, `block/finalize-state-file-modification.md`, `block/goal-tracker-*`, `block/prompt-file-write.md`, `block/summary-bash-write.md`, and `block/todos-file-access.md`.
- edge_cases_or_failure_modes: The hook intentionally allows all Bash commands when no active loop directory exists, so protection is scoped to the newest active RLCR loop only (`hooks/loop-bash-validator.sh:40-43`). It lowercases the entire command, which normalizes matching but can make case-sensitive path analysis approximate. The source-protection regexes are substantial but still regex-based shell parsing, so exotic shell syntax outside the covered patterns may be a residual risk; the file explicitly covers wrappers, command chaining, leading redirections, quoted targets, ANSI-C/locale quoting, escaped spaces, sudo/env/command prefixes, and source-vs-destination state file moves (`hooks/loop-bash-validator.sh:117-243`). `command_modifies_file` patterns generally match non-whitespace path forms, so quoted paths containing spaces may rely on the special state-source redirection cleanup only for that subset. Cancel authorization is deliberately narrow: signal file required, no command substitution/backticks, no newlines, no shell operators, exactly `mv` with two normalized active-loop arguments, source must be active `state.md` or `finalize-state.md`, destination must be active `cancel-state.md` (`hooks/lib/loop-common.sh:288-412`). Legacy `.humanize-loop.local` paths are not protected by this hook’s current active-loop model; tests assert that legacy path writes are allowed (`tests/test-plan-file-hooks.sh:1016-1028`).
- validation_or_tests: Direct hook coverage appears in `tests/test-plan-file-hooks.sh`, including Bash-blocked plan backup writes, `rm`, direct `.humanize/rlcr/plan.md`, command substitution, glob expansion, brace expansion, pipe-to-tee, and backtick bypass attempts (`tests/test-plan-file-hooks.sh:228-335`). Finalize-state behavior is tested for Bash blocking write, `mv` source, `cp` source, and parsing `finalize-state.md` without error (`tests/test-finalize-phase.sh:317-351`, `620-637`). Todo allowlist path restrictions are tested for active-loop `round-1`/`round-2` allow cases and wrong directory, wrong round, missing full path, old loop, and same-basename different-root blocks (`tests/test-allowlist-validators.sh:288-381`). Cancel-signal behavior is tested for allowed state-to-cancel rename with signal and blocked rename without signal (`tests/test-cancel-signal-file.sh:74-130`). Template reference validation scans this hook and shared libraries for template paths (`tests/test-template-references.sh:56-64`, `83-110`). I inspected tests but did not execute them in this read-only research run.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-046 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: Intended block-message template for the Codex review failure path, used to tell Claude/the RLCR participant that Codex failed to produce review output and that the stop hook will retry review on the next exit. The template contains placeholders for Codex exit code, expected review result file, debug command/stdout/stderr file paths, and a stderr excerpt (`prompt-template/block/codex-review-failed.md:1-18`).
- algorithmic_behavior: As a template, it has no executable logic. Its intended transition role is “Codex review execution failed or produced no review file” -> “block the user/agent exit and request another review attempt.” The text says the system will attempt another review when the user exits (`prompt-template/block/codex-review-failed.md:18`). It is designed to render with `{{CODEX_EXIT_CODE}}`, `{{REVIEW_RESULT_FILE}}`, `{{CODEX_CMD_FILE}}`, `{{CODEX_STDOUT_FILE}}`, `{{CODEX_STDERR_FILE}}`, and `{{STDERR_CONTENT}}` (`prompt-template/block/codex-review-failed.md:5-15`).
- inputs_outputs_state: Inputs are template variables supplied by a caller through the template loader. Expected output is markdown block text beginning `# Codex Review Failed`, with debug file paths and a fenced stderr section. It does not read or mutate RLCR state itself. The relevant runtime state lives in `hooks/loop-codex-stop-hook.sh`: Codex debug paths are built under `$HOME/.cache/humanize/<sanitized-project>/<loop-timestamp>` (`hooks/loop-codex-stop-hook.sh:780-791`), Codex exit code is captured after `codex exec` (`hooks/loop-codex-stop-hook.sh:838-844`), and failures block exit via JSON decision `"block"` (`hooks/loop-codex-stop-hook.sh:850-875`).
- gates_or_invariants: The template’s contract assumes a missing review result file, explicitly labeling `{{REVIEW_RESULT_FILE}} (not created)` (`prompt-template/block/codex-review-failed.md:5-7`). It surfaces diagnostic artifacts rather than authorizing a state transition. In the current hook implementation, equivalent gates are inline: non-zero Codex exit code blocks with stderr details (`hooks/loop-codex-stop-hook.sh:878-895`), missing review result blocks after optional stdout copy fallback (`hooks/loop-codex-stop-hook.sh:897-940`), and empty review result blocks (`hooks/loop-codex-stop-hook.sh:943-950`).
- dependencies_and_callers: I found no active shell caller for `block/codex-review-failed.md`. Repository search shows only this template itself and an inline `# Codex Review Failed` string in `hooks/loop-codex-stop-hook.sh:856`; no `load_template` or `load_and_render_safe` reference uses this path. The template would be rendered safely by `hooks/lib/template-loader.sh` if wired in; that renderer is single-pass and intentionally does not expand placeholders embedded inside variable values (`hooks/lib/template-loader.sh:50-58`). Template-reference tests only scan references used by shell scripts; unreferenced templates may exist without being validated as live call sites (`tests/test-template-references.sh:83-110`).
- edge_cases_or_failure_modes: Because it is currently unreferenced, edits to this template would not affect the observed Codex failure behavior unless a caller is later added. Its wording only covers “failed to produce output”/“not created”; the current stop hook also handles non-zero exit, stdout fallback copy failures, and empty files with more specific inline messages (`hooks/loop-codex-stop-hook.sh:878-950`). It asks for the last 50 stderr lines, while current inline failure handling tails 30 stderr/stdout lines (`hooks/loop-codex-stop-hook.sh:881-894`, `917-940`), so wiring this template directly would change diagnostic volume unless caller logic adapts.
- validation_or_tests: No direct test appears to assert this template’s rendered content. The broader template loader has tests for placeholder behavior, including preserving `{{...}}` inside substituted review content, which would matter if diagnostic content contained template-like braces (`tests/test-template-loader.sh:525-537` from repository search context). Template reference validation would not catch this file as required by a caller because current code does not reference it. I inspected references but did not execute tests.
- skip_candidate: `yes: present as a block template but not currently part of the live RLCR transition path; the active Codex-review-failure behavior is implemented inline in hooks/loop-codex-stop-hook.sh rather than loading this template.`

### FIX_HUMANIZE_ESCAPE-HZ-076 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: Claude-facing prompt template for the “review found issues, continue loop” transition. It tells Claude that work is not finished, reanchors the next round on the original implementation plan, requires Todo creation for all discovered issues, embeds Codex’s review result, and reminds Claude that `goal-tracker.md` is read-only after Round 0 (`prompt-template/claude/next-round-prompt.md:1-31`).
- algorithmic_behavior: The stop hook uses this template when Codex review output does not end in `COMPLETE` or `STOP`. After reading the review result, the hook updates `current_round` in the state file, creates `round-${NEXT_ROUND}-prompt.md` and `round-${NEXT_ROUND}-summary.md`, renders this template with plan, review content, and goal-tracker variables, then appends optional post-alignment instructions, a footer, optional push note, and goal-tracker update request text (`hooks/loop-codex-stop-hook.sh:1053-1108`). Finally it returns a JSON hook response with `"decision": "block"` and the next-round prompt as the block reason, forcing Claude to continue instead of exiting (`hooks/loop-codex-stop-hook.sh:1110-1120`).
- inputs_outputs_state: Inputs are `{{PLAN_FILE}}`, `{{REVIEW_CONTENT}}`, and `{{GOAL_TRACKER_FILE}}` (`prompt-template/claude/next-round-prompt.md:6`, `18`, `24`). The output is markdown written to the next round prompt file (`hooks/loop-codex-stop-hook.sh:1062-1078`). State input is the current loop state parsed earlier by the stop hook; state output is a rewritten `current_round: $NEXT_ROUND` line in `state.md` (`hooks/loop-codex-stop-hook.sh:1057-1060`). The stop hook derives `REVIEW_CONTENT` by reading `round-${CURRENT_ROUND}-review-result.md` after validating the review result exists and is non-empty (`hooks/loop-codex-stop-hook.sh:943-953`).
- gates_or_invariants: The template reinforces the invariant that the original plan remains the authoritative scope: “Before proceeding, review the original plan” and “Ensure your work aligns with this plan” (`prompt-template/claude/next-round-prompt.md:3-8`). It requires Todo coverage for all tasks and explicitly forbids fixing only the highest-priority subset (`prompt-template/claude/next-round-prompt.md:12-14`). It places Codex review content between explicit start/end markers, making the review payload structurally visible (`prompt-template/claude/next-round-prompt.md:16-19`). It requires reading `goal-tracker.md` for ultimate goal, acceptance criteria, active/completed/deferred tasks, plan evolution, and open issues (`prompt-template/claude/next-round-prompt.md:22-29`). It states Claude cannot directly modify `goal-tracker.md` after Round 0 and must request updates through the summary (`prompt-template/claude/next-round-prompt.md:30-31`), aligning with Bash validator blocks on post-Round-0 goal-tracker shell modifications (`hooks/loop-bash-validator.sh:308-317`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe "$TEMPLATE_DIR" "claude/next-round-prompt.md"` with an inline fallback if missing (`hooks/loop-codex-stop-hook.sh:1066-1078`). It depends on `hooks/lib/template-loader.sh` single-pass substitution, important because `{{REVIEW_CONTENT}}` may itself contain `{{...}}` strings that should not be recursively expanded (`hooks/lib/template-loader.sh:50-58`). It coordinates with sibling templates appended after it: `claude/post-alignment-action-items.md` after every fifth-round alignment check condition (`hooks/loop-codex-stop-hook.sh:1080-1086`), `claude/next-round-footer.md` for commit/summary instructions (`hooks/loop-codex-stop-hook.sh:1088-1092`), `claude/push-every-round-note.md` when `push_every_round` is true (`hooks/loop-codex-stop-hook.sh:1094-1101`), and `claude/goal-tracker-update-request.md` (`hooks/loop-codex-stop-hook.sh:1103-1108`).
- edge_cases_or_failure_modes: If the template is missing, the stop hook falls back to a shorter inline prompt with review content and plan/goal-tracker references (`hooks/loop-codex-stop-hook.sh:1066-1078`). If `REVIEW_CONTENT` contains template syntax, the single-pass renderer should preserve it rather than substituting unintended variables; this behavior is explicitly documented in the loader (`hooks/lib/template-loader.sh:54-57`). The template begins with “ultrathink” (`prompt-template/claude/next-round-prompt.md:1`), which is prompt-level guidance rather than an enforceable shell/state gate. The template itself says `goal-tracker.md` is read-only after Round 0, but enforcement is distributed across write/edit/bash validators and the stop hook’s summary/update processing; the template alone cannot prevent edits.
- validation_or_tests: Repository search shows the stop hook call site as the active reference (`hooks/loop-codex-stop-hook.sh:1075-1078`). Template-reference tests scan `loop-codex-stop-hook.sh` and verify referenced templates exist (`tests/test-template-references.sh:56-64`, `83-110`). Template-loader tests include a realistic case where `REVIEW_CONTENT` contains `{{GOAL_TRACKER_FILE}}` and `{{PLAN_FILE}}`, ensuring review text is inserted without recursive placeholder expansion (`tests/test-template-loader.sh:525-537` from repository search context). I inspected references but did not execute tests.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item evidence sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`