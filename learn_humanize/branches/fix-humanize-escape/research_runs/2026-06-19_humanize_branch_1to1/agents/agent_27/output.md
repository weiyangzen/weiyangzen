# agent_27 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-027 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR hook allowlist contract. It verifies that a narrow set of generated loop files may bypass otherwise strict round-file protection: `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` inside the active loop directory only. The file directly exercises `hooks/lib/loop-common.sh:is_allowlisted_file()` and the Read/Write/Edit/Bash hook validators.
- algorithmic_behavior: The script builds a temporary git repo and active loop fixture via `setup_test_loop()` at `tests/test-allowlist-validators.sh:33`. It writes `.humanize/rlcr/2024-01-01_12-00-00/state.md` with `current_round: 5` and related frontmatter at lines 49-61, then exports `CLAUDE_PROJECT_DIR` for hook lookup at line 131. It runs 25 assertions: direct allowlist checks at lines 70-124, Write validator checks at lines 133-183, Edit validator checks at lines 189-225, Read validator checks at lines 232-281, and Bash path-restriction checks at lines 288-380. Each validator receives synthetic Claude hook JSON through stdin, matching the hook API shape `{"tool_name": "...", "tool_input": ...}`.
- inputs_outputs_state: Inputs are hook JSON strings and a generated loop state file. Output is TAP-like console text from `pass()`/`fail()` helpers, with counters `TESTS_PASSED` and `TESTS_FAILED` initialized at lines 23-24 and incremented by helpers at lines 26-27. The final process state is `exit $TESTS_FAILED` at line 391, so zero failures is success and any failure count becomes the exit code. Internal state transitions include creating an active loop directory, setting `ACTIVE_LOOP_DIR="$LOOP_DIR"` at line 68 for direct function tests, and resetting `set +e` around hook invocations to capture nonzero validator exits without aborting the test.
- gates_or_invariants: The direct invariant is implemented in `hooks/lib/loop-common.sh`: `is_allowlisted_file()` compares the requested file path to exact active-loop paths for four filenames only at `hooks/lib/loop-common.sh:170-188`. The test asserts positive cases for the four allowed files at `tests/test-allowlist-validators.sh:70-100`, negative cases for non-allowlisted rounds at lines 102-116, and a wrong-directory negative at lines 118-124. Validator gate expectations are exit `0` for allowed files and exit `2` plus relevant error text for blocked files. Bash-specific gates require a full active-loop path and only allow `round-[12]-todos.md`; generic basenames, old loop directories, `/tmp`, and same basename under a different root are blocked at lines 314-380.
- dependencies_and_callers: The script sources `hooks/lib/loop-common.sh` at line 17. It calls `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh` through `$PROJECT_ROOT` at lines 137, 150, 163, 176, 193, 206, 219, 236, 249, 262, 275, 292, 305, 318, 331, 344, 359, and 374. The hook runtime dependencies include `find_active_loop()` from `hooks/lib/loop-common.sh:63-80`, round-file matching from `is_round_file_type()` at `hooks/lib/loop-common.sh:148-153`, and exact allowlist matching at `hooks/lib/loop-common.sh:170-188`. The test is included in the aggregate suite by `tests/run-all-tests.sh:44`.
- edge_cases_or_failure_modes: The test covers same filename but wrong path (`/other/path/round-1-todos.md`) at lines 118-124, disallowed future/non-bootstrap todo files such as `round-3-todos.md` and `round-4-todos.md` at lines 102-108, 159-170, 215-225, and 258-268, and disallowed historical summary files beyond the allowlist such as `round-2-summary.md` and `round-3-summary.md` at lines 110-116, 172-183, and 271-281. For Bash, it specifically guards against basename-only writes, stale loop directories, and same active-loop basename under another root at lines 340-380. A notable implementation edge is that direct Read/Write/Edit allowlisting uses exact string equality, so path normalization, symlink resolution, relative paths, or case variants are not accepted unless they match exactly.
- validation_or_tests: This file is itself the validation asset. It verifies the shared function and all four hook surfaces. The corresponding runtime gate for Write is at `hooks/loop-write-validator.sh:37-44` and summary wrong-round allowlist bypass is at `hooks/loop-write-validator.sh:181-198`; Read todo blocking and summary/prompt round checks are at `hooks/loop-read-validator.sh:36-44` and `hooks/loop-read-validator.sh:116-129`; Edit todo blocking and summary round checks are at `hooks/loop-edit-validator.sh:36-44` and `hooks/loop-edit-validator.sh:124-150`; Bash todo modification filtering is at `hooks/loop-bash-validator.sh:344-351`. I did not execute the test because this worker run requested research notes only and the workspace is read-only.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-057 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-gate template for the RLCR loop when the agent attempts to finish while TodoWrite still contains incomplete work. It is part of the user-facing control path that blocks premature exit before Codex review.
- algorithmic_behavior: The template defines a rendered block message headed `Incomplete Todos Detected` at `prompt-template/block/incomplete-todos.md:1`. It accepts one placeholder input, `{{INCOMPLETE_LIST}}`, at line 5. The behavior contract is expressed as required actions: complete remaining todos, mark each completed using TodoWrite, and only then write the summary and stop at lines 7-10. Line 12 adds the transition gate: do not proceed to Codex review until all todos are finished.
- inputs_outputs_state: Input is the rendered `INCOMPLETE_LIST` generated by `hooks/loop-codex-stop-hook.sh` from the todo checker output. The stop hook extracts incomplete todo lines with `tail -n +2` at `hooks/loop-codex-stop-hook.sh:287`, renders this template with `load_and_render_safe` at lines 289-295, then emits JSON containing `"decision": "block"` and a `systemMessage` at lines 297-304. The user-visible output is a structured block reason, not a shell exit failure; the hook exits `0` after returning the block decision at line 305.
- gates_or_invariants: This template supports a hard invariant: a stop attempt cannot advance to Codex review while the latest TodoWrite has any todo whose status is not exactly `completed`. The checker implements that status rule at `hooks/check-todos-from-transcript.py:113-126`. The stop hook intentionally runs the todo check before expensive Codex review at `hooks/loop-codex-stop-hook.sh:252-257`, and blocks immediately on checker exit `1` at lines 284-305. The template reinforces that the only valid transition is completing all todos first.
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh`, which names `block/incomplete-todos.md` at `hooks/loop-codex-stop-hook.sh:294`. It depends on `hooks/check-todos-from-transcript.py`, assigned to `TODO_CHECKER` at `hooks/loop-codex-stop-hook.sh:258`. Rendering depends on the shared template loader sourced by `hooks/lib/loop-common.sh:46-51`; the stop hook uses `load_and_render_safe`, which provides a fallback if template loading fails. Related tests include `tests/test-finalize-phase.sh:515-540`, which creates a transcript with an `in_progress` todo and asserts the stop hook blocks without invoking Codex, and `tests/test-todo-checker.sh`, which covers checker parsing/status behavior.
- edge_cases_or_failure_modes: If hook input JSON cannot be parsed by the todo checker, checker exit `2` triggers a separate parse-error block rather than this template at `hooks/loop-codex-stop-hook.sh:265-281`. If no transcript path exists, no TodoWrite call exists, or the latest TodoWrite list is empty, the checker exits `0` at `hooks/check-todos-from-transcript.py:99-111`, so this template is not used. Todo status matching is strict: anything other than `completed`, including `pending`, `in_progress`, empty, or missing status, is formatted as incomplete at `hooks/check-todos-from-transcript.py:115-119`. The template itself assumes `INCOMPLETE_LIST` is safe to display and does not add escaping beyond the renderer’s substitution behavior.
- validation_or_tests: The template is exercised indirectly by stop-hook tests. `tests/test-finalize-phase.sh:521-532` asserts a block decision and todo/task wording when incomplete todos exist, and `tests/test-finalize-phase.sh:535-540` asserts Codex is not called during that early block path. `tests/test-template-references.sh:134-140` scans hook scripts for direct template references, so this template should be detected through the stop hook’s literal `"block/incomplete-todos.md"` reference. I did not execute tests because this worker run requested research notes only and the workspace is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 unique item sections present, matching the 2 assigned rows
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`