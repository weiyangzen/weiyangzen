# agent_13 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-013 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Slash-command workflow definition for starting the RLCR loop. The file is a declarative command wrapper: its frontmatter exposes the command description, argument hint, and allowed tool boundary, then delegates actual setup to `scripts/setup-rlcr-loop.sh` via `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh" $ARGUMENTS` at `commands/start-rlcr-loop.md:1-14`.
- algorithmic_behavior: The command defines the high-level iterative RLCR algorithm: the implementer works from a plan, writes a round summary, Codex reviews on attempted exit, feedback loops continue on issues, and an exact `COMPLETE` result ends the Codex review loop at `commands/start-rlcr-loop.md:16-23`. It also defines the Goal Tracker algorithm: immutable goal/acceptance criteria created in round 0, mutable task/evolution/deferral state updated each round, and full alignment audits every 4 work rounds after rounds 4, 9, 14, etc. at `commands/start-rlcr-loop.md:24-42`.
- inputs_outputs_state: Inputs are the command arguments shown in the hint: `<path/to/plan.md> [--max N] [--codex-model MODEL:EFFORT] [--codex-timeout SECONDS] [--push-every-round]` at `commands/start-rlcr-loop.md:3`. The wrapper itself outputs by invoking the setup script; the setup script creates `.humanize/rlcr/<timestamp>/state.md`, copies the plan to `plan.md`, creates `goal-tracker.md`, and writes `round-0-prompt.md`/`round-0-summary.md` paths at `scripts/setup-rlcr-loop.sh:467-653`. Initial state includes `current_round: 0`, max iterations, Codex model/effort/timeout, push policy, plan path/tracking mode, start branch, and start timestamp at `scripts/setup-rlcr-loop.sh:485-498`.
- gates_or_invariants: The command’s invariant is that users must write summaries, maintain `goal-tracker.md`, include implementation/test details, avoid state-file or cancel-command cheating, and trust Codex feedback at `commands/start-rlcr-loop.md:43-50`. The delegated setup script adds hard input gates: git repo and commit must exist at `scripts/setup-rlcr-loop.sh:211-220`; plan path must be relative, space-free, shell-metacharacter-free, non-symlink, existing, readable, inside the project, and not in a submodule at `scripts/setup-rlcr-loop.sh:227-305`; plan tracking mode must be consistent with `--track-plan-file` at `scripts/setup-rlcr-loop.sh:311-356`; the plan must have at least 5 lines and at least 3 content lines at `scripts/setup-rlcr-loop.sh:362-415`; `codex` must be installed at `scripts/setup-rlcr-loop.sh:417-423`; branch/model/effort strings must be YAML-safe at `scripts/setup-rlcr-loop.sh:429-461`.
- dependencies_and_callers: Direct dependency is `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh`, constrained by the command frontmatter `allowed-tools` at `commands/start-rlcr-loop.md:4` and invoked at `commands/start-rlcr-loop.md:12-14`. Runtime continuation depends on stop/validator hooks, especially `hooks/loop-codex-stop-hook.sh`, which parses the active loop state and enforces summary/todo/git/review gates before allowing exit. The README lists this as the user entry point at `README.md:73-88`.
- edge_cases_or_failure_modes: Startup fails if no plan is provided, multiple plan files are provided, `--plan-file` conflicts with a positional plan, or arguments are malformed at `scripts/setup-rlcr-loop.sh:104-205`. The command advertises stop conditions: maximum iteration count, Codex `COMPLETE`, or `/humanize:cancel-rlcr-loop` at `commands/start-rlcr-loop.md:51-55`. Because the command wrapper passes `$ARGUMENTS` to Bash, the real safety boundary is the setup script’s path and model validation; paths with spaces are explicitly rejected at `scripts/setup-rlcr-loop.sh:233-239`.
- validation_or_tests: Relevant coverage is in `tests/test-plan-file-validation.sh`, which exercises setup-script plan path, tracking, content, and model validation paths. The loop lifecycle is further covered by `tests/test-finalize-phase.sh` and cancel/monitor tests. I did not run tests because this assignment requires research notes only and the branch export is read-only.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-043 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable specification for the monitor’s zsh-safe file discovery behavior. It targets AC-1.2 and AC-1.4 explicitly: no zsh `no matches found` errors, and correct operation under zsh, as stated at `tests/test-zsh-monitor-safety.sh:3-10`.
- algorithmic_behavior: The test constructs isolated loop/cache states and verifies monitor discovery patterns that were historically vulnerable to unmatched globs. It first sources `scripts/humanize.sh` under zsh and fails if stderr/stdout contains `no matches found` at `tests/test-zsh-monitor-safety.sh:61-73`. It then mirrors and exercises `_find_latest_session` against an empty `.humanize/rlcr` directory at `tests/test-zsh-monitor-safety.sh:82-120`, dotfile-only loop contents at `tests/test-zsh-monitor-safety.sh:123-160`, `_find_state_file` when no `*-state.md` exists at `tests/test-zsh-monitor-safety.sh:163-208`, empty Codex cache log discovery at `tests/test-zsh-monitor-safety.sh:211-238`, valid timestamp session ordering at `tests/test-zsh-monitor-safety.sh:243-276`, zsh execution verification at `tests/test-zsh-monitor-safety.sh:279-289`, and an old-glob-vs-new-find safety demonstration at `tests/test-zsh-monitor-safety.sh:292-324`.
- inputs_outputs_state: Inputs are the local repo root, zsh runtime, `scripts/humanize.sh`, `/tmp/test-zsh-humanize-$$`, `.humanize/rlcr` test fixtures, and `$HOME/.cache/humanize/test-project/2026-01-16_10-00-00` at `tests/test-zsh-monitor-safety.sh:15-17`, `tests/test-zsh-monitor-safety.sh:48-56`, and `tests/test-zsh-monitor-safety.sh:217-240`. Outputs are colored `PASS`/`FAIL` lines, accumulated `TESTS_PASSED`/`TESTS_FAILED` counters, and exit `0` only when no failures are recorded at `tests/test-zsh-monitor-safety.sh:24-36` and `tests/test-zsh-monitor-safety.sh:329-344`. The script cleans its temporary test base on exit at `tests/test-zsh-monitor-safety.sh:52-56`.
- gates_or_invariants: The central invariant is that monitor discovery must use `find` loops instead of bare globs so empty directories, dotfile-only directories, and missing matches do not trigger zsh `nomatch` failures. The production implementation uses that same invariant in `_find_latest_session`, `_find_latest_codex_log`, and `_find_state_file` at `scripts/humanize.sh:27-49`, `scripts/humanize.sh:51-109`, and `scripts/humanize.sh:111-150`. Session names must match timestamp format `YYYY-MM-DD_HH-MM-SS`; latest selection is lexicographic over that sortable timestamp shape at `tests/test-zsh-monitor-safety.sh:101-110` and `tests/test-zsh-monitor-safety.sh:258-268`. State-file lookup prioritizes `state.md` as active, then first `*-state.md`, else `|unknown` at `tests/test-zsh-monitor-safety.sh:175-199` and production `scripts/humanize.sh:123-149`.
- dependencies_and_callers: Direct dependency is `scripts/humanize.sh`, sourced in Test 1 at `tests/test-zsh-monitor-safety.sh:68`. The production monitor function `_humanize_monitor_codex` is bash/zsh-compatible by declaration at `scripts/humanize.sh:1-4`, sets `ksharrays` under zsh at `scripts/humanize.sh:9-13`, discovers initial session/log at `scripts/humanize.sh:517-530`, and keeps polling/switching sessions and logs through the same helpers at `scripts/humanize.sh:547-724`. Related broader tests include `tests/test-monitor-e2e-real.sh`, which also checks absence of `no matches found`/`bad pattern` in monitor output.
- edge_cases_or_failure_modes: Covered cases include missing or empty loop directories, dotfile-only loop directories, no matching state files, empty project cache, valid multiple sessions where the newer timestamp must win, and shell mismatch. The test intentionally writes under `/tmp` and `$HOME/.cache/humanize/test-project`; in a read-only research run that side effect is why I inspected but did not execute it. A limitation is that several tests duplicate internal helper logic locally because the helpers are nested inside `_humanize_monitor_codex`, so they verify equivalence to the intended algorithm rather than directly invoking those nested production functions.
- validation_or_tests: This file is itself the validation asset. Pass criteria are eight successful test sections and `TESTS_FAILED == 0`, causing `All zsh runtime safety tests passed!` and exit `0` at `tests/test-zsh-monitor-safety.sh:336-344`. It validates production behavior against the zsh glob-safety acceptance criteria noted in its header.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-073 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Claude prompt template for the RLCR Finalize Phase. It defines the post-Codex-approval transition contract: after Codex review has passed and acceptance criteria are met, Claude gets one final functionality-equivalent simplification/refactor pass before loop completion at `prompt-template/claude/finalize-phase-prompt.md:1-6`.
- algorithmic_behavior: The template tells Claude to invoke the `code-simplifier:code-simplifier` subagent through the Task tool at `prompt-template/claude/finalize-phase-prompt.md:7-14`, restricts the phase to non-behavior-changing cleanup at `prompt-template/claude/finalize-phase-prompt.md:16-24`, focuses that cleanup on RLCR-session changes and maintainability improvements at `prompt-template/claude/finalize-phase-prompt.md:25-34`, supplies reference paths for the original plan and goal tracker at `prompt-template/claude/finalize-phase-prompt.md:35-39`, and requires completed todos, a commit, and a finalize summary before exit at `prompt-template/claude/finalize-phase-prompt.md:40-50`.
- inputs_outputs_state: Template inputs are placeholders `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}` at `prompt-template/claude/finalize-phase-prompt.md:37-44`. These are rendered by `hooks/loop-codex-stop-hook.sh` after an exact final Codex review line of `COMPLETE`: the hook sets `FINALIZE_SUMMARY_FILE="$LOOP_DIR/finalize-summary.md"` and calls `load_and_render_safe` with the three variables at `hooks/loop-codex-stop-hook.sh:983-1008`. The rendered prompt is emitted as a blocking hook response so Claude stays in the loop and performs the finalize work at `hooks/loop-codex-stop-hook.sh:1010-1019`.
- gates_or_invariants: The non-negotiable invariants are no existing functionality changes, no existing test failures, no new bugs, and only functionality-equivalent simplification/cleanup at `prompt-template/claude/finalize-phase-prompt.md:16-24`. The hook enforces the transition state: normal loop uses `state.md`, Finalize Phase uses `finalize-state.md` at `hooks/loop-codex-stop-hook.sh:67-82`; on `COMPLETE`, the hook renames `state.md` to `finalize-state.md` at `hooks/loop-codex-stop-hook.sh:961-981`. Finalize exit skips another Codex review only after earlier checks pass, then renames `finalize-state.md` to `complete-state.md` at `hooks/loop-codex-stop-hook.sh:646-657`.
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh`, which loads this template with a fallback at `hooks/loop-codex-stop-hook.sh:985-1008`. Rendering depends on `hooks/lib/template-loader.sh`, whose placeholder syntax is `{{VARIABLE_NAME}}`, single-pass, preserving missing placeholders at `hooks/lib/template-loader.sh:7-13`; `load_and_render_safe` falls back if the template is missing or renders empty at `hooks/lib/template-loader.sh:167-203`. The template’s summary requirement is enforced by the stop hook selecting `finalize-summary.md` during Finalize Phase at `hooks/loop-codex-stop-hook.sh:522-554`.
- edge_cases_or_failure_modes: If Codex returns `COMPLETE` at or beyond `max_iterations`, Finalize Phase is skipped and the loop terminates as max-iteration state at `hooks/loop-codex-stop-hook.sh:961-969`. If the template file is missing, the fallback prompt still blocks with core finalize instructions at `hooks/loop-codex-stop-hook.sh:985-1008`. If `finalize-summary.md` is missing, exit is blocked at `hooks/loop-codex-stop-hook.sh:522-554`. If transcript todos are incomplete, the todo checker blocks before final exit at `hooks/loop-codex-stop-hook.sh:252-306`. The template asks for test confirmation, but the hook does not itself run the test command; the enforcement is contractual plus summary/git/todo gates.
- validation_or_tests: `tests/test-finalize-phase.sh` verifies that a `COMPLETE` review creates `finalize-state.md`, removes `state.md`, blocks with a prompt mentioning simplification, and that max-iteration completion skips Finalize Phase at `tests/test-finalize-phase.sh:460-493`. It also verifies Finalize Phase blocks on incomplete todos at `tests/test-finalize-phase.sh:499-533`. `tests/test-template-references.sh` scans hook template references and checks referenced template files exist, including templates loaded via `load_and_render_safe`, at `tests/test-template-references.sh:51-115`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `FIX_HUMANIZE_ESCAPE-HZ-013`, `FIX_HUMANIZE_ESCAPE-HZ-043`, `FIX_HUMANIZE_ESCAPE-HZ-073`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`