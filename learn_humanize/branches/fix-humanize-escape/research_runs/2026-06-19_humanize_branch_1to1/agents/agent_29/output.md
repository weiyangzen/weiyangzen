# agent_29 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-029 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for the shared Bash command mutation detector `command_modifies_file`, which is implemented in `hooks/lib/loop-common.sh:571`. This test locks down the regex gate used by RLCR hook validators to decide whether a Bash command attempts to modify protected loop files such as `state.md`, `goal-tracker.md`, `round-*-summary.md`, `round-*-todos.md`, and `.humanize/rlcr/.../plan.md`.
- algorithmic_behavior: The file sources `hooks/lib/loop-common.sh` at `tests/test-bash-validator-patterns.sh:13`, then normalizes commands through `to_lower` before invoking `command_modifies_file` in `assert_modifies` and `assert_not_modifies` at `tests/test-bash-validator-patterns.sh:36` and `tests/test-bash-validator-patterns.sh:50`. Its behavior is table-driven through explicit shell assertions rather than a formal test framework. It validates that the detector catches output redirection, append redirection, `tee`, in-place `sed`, `awk -i inplace`, `perl -i`, `mv`, `cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec fd>` mutations. The underlying implementation iterates over ordered extended-regex patterns and returns success on first match at `hooks/lib/loop-common.sh:578`.
- inputs_outputs_state: Input is a command string plus an optional protected filename regex. Default protected target is `goal-tracker\.md` at `tests/test-bash-validator-patterns.sh:39` and `tests/test-bash-validator-patterns.sh:53`; later groups pass `state\.md` and `round-[0-9]+-summary\.md`. Output is process-level test status: pass/fail counters are held in `TESTS_PASSED` and `TESTS_FAILED` at `tests/test-bash-validator-patterns.sh:20`, then the script exits `0` only if failures are zero at `tests/test-bash-validator-patterns.sh:206`. State transitions are limited to counter increments in `pass` and `fail`; the tested production function itself has no persistent state and returns `0` for “modifies” or `1` for “does not modify.”
- gates_or_invariants: Key invariant is false-negative prevention for common write paths while avoiding false positives on read-only commands. Groups 1-5 require write-like shell forms to match, including uppercase input after lowercase normalization at `tests/test-bash-validator-patterns.sh:80`. Group 6 requires read-only commands such as `cat`, `grep`, `head`, `tail`, `wc`, `less`, `ls`, `file`, `stat`, and `diff` to stay allowed at `tests/test-bash-validator-patterns.sh:142`. Group 7 captures edge redirection forms like bare `> goal-tracker.md` and no-space `>goal-tracker.md` at `tests/test-bash-validator-patterns.sh:161`, while also documenting that variable-expanded paths like `$FILE` are not detected at `tests/test-bash-validator-patterns.sh:170`.
- dependencies_and_callers: Direct dependency is `hooks/lib/loop-common.sh`, especially `to_lower` at `hooks/lib/loop-common.sh:141` and `command_modifies_file` at `hooks/lib/loop-common.sh:571`. Production callers include `hooks/loop-bash-validator.sh`: state/finalize-state destination checks at `hooks/loop-bash-validator.sh:98` and `hooks/loop-bash-validator.sh:108`, plan backup protection at `hooks/loop-bash-validator.sh:295`, goal tracker protection at `hooks/loop-bash-validator.sh:308`, prompt-file protection at `hooks/loop-bash-validator.sh:324`, summary protection at `hooks/loop-bash-validator.sh:334`, and todos protection at `hooks/loop-bash-validator.sh:344`. The suite is part of the aggregate test runner at `tests/run-all-tests.sh:33`.
- edge_cases_or_failure_modes: The test explicitly documents a known limitation: multi-source copy such as `cp file1.md file2.md goal-tracker.md` is not detected because the production regex expects `cp src dest` at `tests/test-bash-validator-patterns.sh:165`. The regex implementation also does not resolve variables or command substitutions; the test expects `echo x > $FILE` to be ignored at `tests/test-bash-validator-patterns.sh:171`. Production matching is regex/string based, so quoted filenames with spaces, shell arrays, here-docs, process substitutions, complex option ordering, or path transformations may need separate tests if the protected surface expands. For `rm`, the implementation only allows a compact option cluster matching `-[rfv]+` before the target at `hooks/lib/loop-common.sh:586`.
- validation_or_tests: I ran `bash -n tests/test-bash-validator-patterns.sh`, `bash -n hooks/lib/loop-common.sh`, `bash -n hooks/loop-bash-validator.sh`, `bash -n hooks/loop-write-validator.sh`, and `bash -n hooks/loop-edit-validator.sh`; all returned success. I also ran `tests/test-bash-validator-patterns.sh`; it reported 48 passed, 0 failed, and exited `0`. A later `git status --short --branch` attempt was not usable in this read-only branch export because the directory has no `.git` metadata and macOS developer tooling attempted to write cache files under `/tmp`, which is blocked by the sandbox.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-059 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Prompt/block template that supplies the user-facing reason when validators block attempts to modify the loop-local `plan.md` backup. The template defines the contract that `.humanize/rlcr/<timestamp>/plan.md` is immutable backup state, not an editable working plan.
- algorithmic_behavior: The template itself is static Markdown: title at `prompt-template/block/plan-backup-protected.md:1`, prohibition at `prompt-template/block/plan-backup-protected.md:3`, integrity rationale at `prompt-template/block/plan-backup-protected.md:5`, and allowed action at `prompt-template/block/plan-backup-protected.md:7`. It participates in algorithmic control flow through `load_and_render_safe`, which loads a template and falls back if missing or empty at `hooks/lib/template-loader.sh:170`. Validators use that rendered text as the block reason, write it to stderr, and exit `2`, so this file is part of the rejection surface for protected plan-backup transitions.
- inputs_outputs_state: Inputs are validator decisions that identify a protected `plan.md` in `.humanize/rlcr/`. For Bash, input is `.tool_input.command` parsed at `hooks/loop-bash-validator.sh:29`; for Write/Edit, input is `.tool_input.file_path` parsed at `hooks/loop-write-validator.sh:30` and `hooks/loop-edit-validator.sh:29`. Output is the rendered template text plus hook exit code `2`. The protected state is the loop backup file copied from the original plan when a loop starts: `scripts/setup-rlcr-loop.sh:475` copies the full plan path to `$LOOP_DIR/plan.md`.
- gates_or_invariants: The invariant is that the loop-local plan backup must remain read-only for the whole RLCR loop. Bash mutation attempts are detected with `command_modifies_file "$COMMAND_LOWER" "\.humanize/rlcr(/[^/]+)?/plan\.md"` at `hooks/loop-bash-validator.sh:295`, then blocked with this template at `hooks/loop-bash-validator.sh:297`. Write blocks basename `plan.md` only when the path is under `/.humanize/rlcr/` at `hooks/loop-write-validator.sh:132`. Edit uses the same basename-plus-loop-dir gate at `hooks/loop-edit-validator.sh:100`. The template’s instruction to read instead of modify is therefore the human-facing expression of a stricter hook invariant.
- dependencies_and_callers: Direct callers are `hooks/loop-bash-validator.sh:297`, `hooks/loop-write-validator.sh:135`, and `hooks/loop-edit-validator.sh:104`. All depend on `hooks/lib/loop-common.sh` for shared path and lowercasing helpers, and on `hooks/lib/template-loader.sh` for `load_and_render_safe`. The backup is created by `scripts/setup-rlcr-loop.sh:476`. Stop-hook integrity validation also treats the backup as required: missing backup is blocked in `tests/test-plan-file-hooks.sh:543`, and production code reports “Plan file backup not found” around `hooks/loop-codex-stop-hook.sh:185`.
- edge_cases_or_failure_modes: The Bash gate requires a literal `.humanize/rlcr` path in the command string, including either direct `.humanize/rlcr/plan.md` or one timestamp segment via `(/[^/]+)?`; commands that mutate a loop backup through a variable, symlink, `cd` plus relative `plan.md`, shell glob expansion not visible in the raw command, or indirect file descriptor reuse may not be caught by this specific plan-backup check. Write/Edit are path based and rely on the provided `file_path` containing `/.humanize/rlcr/`; paths outside that namespace named `plan.md` are not blocked by this template. If the template is missing or empty, behavior still blocks because callers pass fallback strings, but the richer integrity guidance would be lost.
- validation_or_tests: Relevant hook coverage appears in `tests/test-plan-file-hooks.sh`: Write validator blocks loop `plan.md` at lines `194-205`, Edit validator blocks it at `211-222`, Bash redirection blocks it at `228-239`, and Bash `rm` blocks it at `241-249`. The same test file verifies stop-hook behavior when the backup is missing at `543-560`. I ran syntax checks for the three validators that load this template and for `hooks/lib/loop-common.sh`; all passed. I did not run the full `tests/test-plan-file-hooks.sh` suite because it performs broad temp/git setup and the current sandbox is read-only/restricted; the focused static and syntax validation was sufficient for research notes.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 item evidence sections present; each assigned row is represented once as its own section
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`