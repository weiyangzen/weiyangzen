# agent_30 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-030 `file` `tests/test-cancel-signal-file.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR cancel escape hatch. The file verifies that the Bash validator blocks normal `state.md` and `finalize-state.md` mutation while allowing the single intentional cancellation transition: loop-local active state file to loop-local `cancel-state.md`, authorized by a loop-local `.cancel-requested` signal. The test harness is set up at [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-cancel-signal-file.sh:42) and calls the real validator through JSON hook input at [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-cancel-signal-file.sh:61).
- algorithmic_behavior: The suite creates a synthetic active loop under `$TEST_DIR/.humanize/rlcr/2024-01-01_12-00-00`, writes frontmatter-bearing `state.md`, exports `CLAUDE_PROJECT_DIR`, then submits candidate Bash commands to `hooks/loop-bash-validator.sh`. Positive cases require exit `0`; blocked cases require exit `2`; helper-only cases call `is_cancel_authorized` directly. The central allowed transition is `mv ${LOOP_DIR}/state.md ${LOOP_DIR}/cancel-state.md` with `.cancel-requested` present, covered at [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-cancel-signal-file.sh:78), plus path normalization through `/./` at line 102, quoted paths at line 367, and literal `"${loop_dir}state.md"` command syntax at lines 453 and 473.
- inputs_outputs_state: Inputs are generated hook JSON with `tool_name: Bash` and `tool_input.command`, a temporary project root, a loop directory, `state.md`, and optionally `.cancel-requested`. Outputs are validator process statuses and diagnostic text. State transitions under test are not executed by the test itself; they are authorization decisions for commands that would rename `state.md` to `cancel-state.md`. A missing active loop, modeled by removing `.humanize` and omitting `state.md`, must allow the command because no active RLCR guard applies, as checked at [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-cancel-signal-file.sh:1231).
- gates_or_invariants: The invariant is exact authorization: the signal must be a file named `.cancel-requested` inside the active loop directory, not the parent RLCR directory, and the command must be exactly `mv` with two arguments from active-loop `state.md` or `finalize-state.md` to active-loop `cancel-state.md`. The production helper documents and enforces these gates in `is_cancel_authorized`: signal required, no `$(` or backticks, no newline, no shell operators, `$loop_dir` normalization only, no remaining `$`, command starts with `mv`, exactly two parsed args, source matches loop state file, destination matches loop cancel file at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/lib/loop-common.sh:274). The validator calls this helper as the only exception to state-file blocking at [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/loop-bash-validator.sh:98), [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/loop-bash-validator.sh:108), and source-side `mv/cp` checks at [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/loop-bash-validator.sh:117).
- dependencies_and_callers: Direct dependencies are `jq` for hook JSON construction, `hooks/loop-bash-validator.sh`, and `hooks/lib/loop-common.sh` sourced by the test at [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-cancel-signal-file.sh:33). Runtime caller context is Claude’s Bash PreToolUse hook registered in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/hooks.json:42). The command contract being protected is documented in [commands/cancel-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/commands/cancel-rlcr-loop.md:28): create the signal first, then rename state to cancel state. That command also covers Finalize Phase cancellation by renaming `finalize-state.md` to `cancel-state.md` after confirmation at lines 34-45.
- edge_cases_or_failure_modes: The suite is primarily negative/adversarial. It blocks cancellation without signal, signal in wrong directory, `echo > state.md`, `sed -i`, `rm`, wrong destination, arbitrary `/tmp` destination, unrelated `/tmp/state.md`, extra arguments before or after, hidden variables such as `${IFS}`, command substitution, backticks, newlines, `sudo`, `env`, `command --`, shell chaining with `;`, `&&`, `||`, `|`, `|&`, background `&`, `sh -c` and `bash -c` wrappers, and leading redirection forms. Redirection stripping is heavily specified in the validator for patterns such as `2>/tmp/x mv`, `&>`, `>>`, quoted targets, escaped-space targets, and ANSI-C quoting at [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/loop-bash-validator.sh:133); tests for those forms run from roughly [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-cancel-signal-file.sh:846) through line 1204.
- validation_or_tests: This file itself is the validation artifact. It maintains counters via `pass`/`fail`, prints a summary, and exits with `$TESTS_FAILED` at [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-cancel-signal-file.sh:1347). I inspected the test and referenced implementation paths only; I did not execute it because this worker request was research-only and the branch export is read-only. A read-only `git status --short` check also could not complete because this export is not a git checkout and the restricted environment prevented `/tmp` cache writes.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-060 `file` `prompt-template/block/plan-file-modified.md`
- cursor: `[_]`
- core_role: Prompt/block template for the plan integrity gate. It is not algorithmic code, but it defines the user-facing transition contract when the plan file diverges from the loop-start backup: current loop must be cancelled, plan changed outside the loop, then a new loop started. The complete template is only 12 lines at [prompt-template/block/plan-file-modified.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/prompt-template/block/plan-file-modified.md:1).
- algorithmic_behavior: The template is rendered when `hooks/loop-codex-stop-hook.sh` detects that the current project plan file differs from `$LOOP_DIR/plan.md`. The stop hook first ensures the backup exists, ensures the original exists, optionally checks tracked-file git status, then always compares current plan content to backup using `diff -q`. On mismatch, it renders this block template and returns a JSON block decision with system message `"Loop: Blocked - plan file modified"` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/loop-codex-stop-hook.sh:230). The template’s short instruction sequence is: cancel current loop, update plan file, start a new loop with the same plan path.
- inputs_outputs_state: Template inputs are `PLAN_FILE` and `BACKUP_PATH`, substituted into lines 3, 10, and 12. Its output is Markdown included as the `reason` field in a hook response JSON object. It does not mutate state directly. The state transition it prescribes is procedural: active RLCR loop remains blocked until cancelled via `/humanize:cancel-rlcr-loop`; the backup path remains available for recovery or comparison. Rendering is performed via `load_and_render_safe "$TEMPLATE_DIR" "block/plan-file-modified.md" "$FALLBACK" "PLAN_FILE=$PLAN_FILE" "BACKUP_PATH=$BACKUP_PLAN"` in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/loop-codex-stop-hook.sh:244).
- gates_or_invariants: Invariant: plan files cannot change during an active RLCR loop. The hook enforces the invariant by blocking if tracked plans have uncommitted changes or if the current plan content differs from the saved backup. The template reinforces the invariant in human-readable form at [prompt-template/block/plan-file-modified.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/prompt-template/block/plan-file-modified.md:5), where it states that plan modifications are forbidden during an active loop. Template variable rendering is single-pass, keeps missing placeholders intact, and has fallback behavior through `load_and_render_safe`, defined at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/lib/template-loader.sh:50) and [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/lib/template-loader.sh:170).
- dependencies_and_callers: The direct caller is `hooks/loop-codex-stop-hook.sh`, registered as a Stop hook with a 7200 second timeout in [hooks/hooks.json](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/hooks.json:52). It depends on template discovery through `hooks/lib/loop-common.sh`, which initializes `TEMPLATE_DIR` from `hooks/lib/template-loader.sh` at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/lib/loop-common.sh:46). It is also covered indirectly by template reference and rendering tests such as `tests/test-template-references.sh`, `tests/test-template-loader.sh`, and comprehensive template tests found by repository search.
- edge_cases_or_failure_modes: If the backup plan is missing, the stop hook blocks with a different inline reason before this template is reached. If the original plan was deleted, the hook blocks with a deletion-specific reason that includes the backup path. If a tracked plan has uncommitted changes, the hook blocks before the content diff path. If the template file is missing or renders empty, `load_and_render_safe` uses the inline fallback with the same placeholders. Missing variables are not fatal at the template layer; unresolved placeholders are preserved by the loader, which could produce a less useful block message but would not bypass the gate.
- validation_or_tests: The template is validated structurally by template-reference tests that scan `load_template`, `load_and_render`, and `load_and_render_safe` usages and check referenced Markdown files exist; see the scanner setup in [tests/test-template-references.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-template-references.sh:83). Rendering semantics, fallback behavior, and single-pass placeholder handling are covered in `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh` by references found during inspection. I inspected these references only; I did not run tests in the read-only research pass.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 item headings present above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`