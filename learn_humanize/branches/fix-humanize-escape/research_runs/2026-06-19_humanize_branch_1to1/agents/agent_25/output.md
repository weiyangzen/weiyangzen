# agent_25 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-025 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Human-operated executable specification for the RLCR monitor’s deletion/escape behavior. It defines the manual acceptance path for AC-1.1 and AC-1.3: deleting `.humanize/` while `humanize monitor rlcr-loop` is active must produce a clean, user-facing stop message, restore terminal state, and avoid shell glob errors.
- algorithmic_behavior: The script is a three-command dispatcher over `${1:-}` at `tests/manual-monitor-test.sh:23`. `setup` creates a synthetic latest RLCR session under `.humanize/rlcr/2026-01-16_99-99-99`, writes minimal `state.md`, and writes a minimal `goal-tracker.md` (`tests/manual-monitor-test.sh:24-46`). `delete` removes `.humanize` with `rm -rf` and prints the expected monitor observations (`tests/manual-monitor-test.sh:47-57`). `cleanup` removes leftovers (`tests/manual-monitor-test.sh:58-62`). Unknown commands print usage and exit nonzero (`tests/manual-monitor-test.sh:63-71`).
- inputs_outputs_state: Input is the positional command `setup|delete|cleanup`; implicit input is project root because the script resolves `SCRIPT_DIR`, `PROJECT_ROOT`, and `cd`s to root (`tests/manual-monitor-test.sh:19-21`). State created is `.humanize/rlcr/<timestamp>/state.md` with `current_round`, `max_iterations`, model fields, timestamp, and `plan_file` (`tests/manual-monitor-test.sh:26-32`), plus `goal-tracker.md` with immutable goal/AC headings (`tests/manual-monitor-test.sh:33-38`). Output is console guidance and destructive removal of `.humanize` during `delete`/`cleanup`.
- gates_or_invariants: Requires `set -euo pipefail` (`tests/manual-monitor-test.sh:17`). The fixture timestamp matches the monitor’s session-name regex in `_find_latest_session`, which accepts `YYYY-MM-DD_HH-MM-SS` directories (`scripts/humanize.sh:41-47`). The expected stop reason is exactly `Monitoring stopped: .humanize/rlcr directory no longer exists` (`tests/manual-monitor-test.sh:51-53`), matching `_graceful_stop` when the monitor detects missing loop dir (`scripts/humanize.sh:549-552`, `scripts/humanize.sh:682-685`). The terminal restoration invariant maps to `_cleanup -> _restore_terminal -> "Stopped monitoring."` (`scripts/humanize.sh:478-497`).
- dependencies_and_callers: Depends on `scripts/humanize.sh` being sourceable and exposing `humanize monitor rlcr-loop` (`scripts/humanize.sh:805-837`). The monitor depends on `.humanize/rlcr` existence at startup (`scripts/humanize.sh:20-25`), latest session discovery via `find` rather than shell globs (`scripts/humanize.sh:27-49`), state-file lookup (`scripts/humanize.sh:111-127`), and graceful-stop checks in both no-log and log-follow loops (`scripts/humanize.sh:589-592`, `scripts/humanize.sh:682-685`). Related automated coverage exists in `tests/test-monitor-runtime.sh:56-130`, `tests/test-zsh-monitor-safety.sh:59-130`, and real e2e monitor tests referenced by `tests/run-all-tests.sh`.
- edge_cases_or_failure_modes: The script intentionally deletes the whole `.humanize` tree, so running it in a real active project is destructive to local loop artifacts. It creates no cached Codex log, so the manual path exercises the monitor’s “waiting/no log” branch as well as directory deletion. It assumes the user runs the monitor in a second terminal before `delete`; otherwise it only deletes fixture state. Startup fails cleanly if `.humanize/rlcr` is missing before monitor launch (`scripts/humanize.sh:20-25`). Zsh glob failures are specifically guarded in monitor discovery by `find` and existence checks (`scripts/humanize.sh:30-37`, `scripts/humanize.sh:65-72`).
- validation_or_tests: The file is itself manual validation, not an automated assertion harness. Expected observations are printed in `delete`: clean stop message, scroll-region/cursor restoration, and no `zsh/bash 'no matches found'` errors (`tests/manual-monitor-test.sh:51-56`). Automated analogs verify `_graceful_stop` calls cleanup/restoration and emits the reason (`tests/test-monitor-runtime.sh:94-130`) and verify zsh safety on empty/dotfile-only loop dirs (`tests/test-zsh-monitor-safety.sh:59-130`).
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-055 `file` `prompt-template/block/goal-tracker-modification.md`
- cursor: `[_]`
- core_role: Prompt/block template that enforces the post-Round-0 Goal Tracker authority boundary. It is part of the hook validation algorithm: Claude-side Write/Edit/Bash attempts against `goal-tracker.md` are blocked, and the allowed transition is an indirect request in the current round summary for Codex review.
- algorithmic_behavior: The template renders a block headed with `CURRENT_ROUND` (`prompt-template/block/goal-tracker-modification.md:1`), states that only Codex can modify the Goal Tracker after Round 0 (`prompt-template/block/goal-tracker-modification.md:3-5`), and instructs the agent to add a `Goal Tracker Update Request` section in `SUMMARY_FILE` (`prompt-template/block/goal-tracker-modification.md:7-23`). It encodes the algorithmic state transition from “direct mutation requested” to “write a structured request for later Codex adjudication.”
- inputs_outputs_state: Inputs are template variables `{{CURRENT_ROUND}}` and `{{SUMMARY_FILE}}`, supplied by `goal_tracker_blocked_message "$CURRENT_ROUND" "$SUMMARY_FILE"` in `hooks/lib/loop-common.sh:601-612`. Output is rendered Markdown sent to stderr by validators before exiting with status `2` (`hooks/loop-edit-validator.sh:114-117`, `hooks/loop-write-validator.sh:145-148`, `hooks/loop-bash-validator.sh:308-316`). It does not mutate state; it prevents direct state mutation of `goal-tracker.md` and redirects desired changes into `round-N-summary.md`.
- gates_or_invariants: Gate condition for Write/Edit validators is `is_goal_tracker_path "$FILE_PATH_LOWER" && CURRENT_ROUND > 0` (`hooks/loop-edit-validator.sh:114-117`, `hooks/loop-write-validator.sh:145-148`). Bash validation blocks any shell command matching modification patterns for `goal-tracker.md`; Round 0 uses a different “use proper tools” block, while Round > 0 uses this template (`hooks/loop-bash-validator.sh:303-316`). Rendering is single-pass and leaves undefined placeholders intact, preventing placeholder injection through values (`hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-131`). If the template is missing or renders empty, `load_and_render_safe` falls back to an inline message (`hooks/lib/template-loader.sh:167-203`, `hooks/lib/loop-common.sh:606-612`).
- dependencies_and_callers: Loaded through `hooks/lib/loop-common.sh`, which sources `template-loader.sh` and resolves `TEMPLATE_DIR` from the plugin root (`hooks/lib/loop-common.sh:46-55`). Direct callers are edit, write, and bash validators. The bash validator’s detection depends on `command_modifies_file`, which catches redirects, `tee`, in-place `sed/awk/perl`, `mv/cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec >` patterns (`hooks/lib/loop-common.sh:571-599`). The Codex-side counterpart that consumes these requests is referenced by `prompt-template/codex/goal-tracker-update-section.md` and stop-hook prompt assembly (`hooks/loop-codex-stop-hook.sh:677-698`, `hooks/loop-codex-stop-hook.sh:1103-1106` from repository search output).
- edge_cases_or_failure_modes: If `CURRENT_ROUND` is misparsed as `0`, Write/Edit would not use this post-Round-0 block. If command parsing misses an exotic shell mutation form, Bash could bypass the intended authority gate. If `SUMMARY_FILE` points outside the active loop dir, the template would direct the user to the wrong request surface; callers construct it from `ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` (`hooks/loop-edit-validator.sh:115`, `hooks/loop-write-validator.sh:146`, `hooks/loop-bash-validator.sh:313`). Missing template is degraded but not fatal because the inline fallback preserves the key instruction.
- validation_or_tests: Template existence is cross-checked by `tests/test-template-references.sh:149-167`, which includes `block/goal-tracker-modification.md` in common template validation. Rendering behavior is covered indirectly by the template loader tests and comprehensive template tests, including single-pass rendering stress coverage (`tests/test-templates-comprehensive.sh:547-555`) and related Goal Tracker update-section rendering (`tests/test-templates-comprehensive.sh:531-539`). Validator behavior is exercised by hook tests that create `goal-tracker.md` fixtures in `tests/test-plan-file-hooks.sh` and related loop hook suites.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 section headings present; each assigned section is separate and cursor-marked [_]`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`