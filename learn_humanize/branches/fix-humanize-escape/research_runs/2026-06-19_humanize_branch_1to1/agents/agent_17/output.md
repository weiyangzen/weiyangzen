# agent_17 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-017 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: Stop-hook controller for the RLCR loop. It intercepts Claude Code stop attempts, performs cheap local gates first, runs Codex review when needed, and either blocks exit with next-round/finalize instructions or advances the loop state. The hook is registered as the Claude `Stop` hook in `hooks/hooks.json:52-58` with a 7200s timeout.
- algorithmic_behavior: Reads hook JSON from stdin at `hooks/loop-codex-stop-hook.sh:29`, intentionally ignores `stop_hook_active` so repeated blocked exits still re-review at `hooks/loop-codex-stop-hook.sh:31-37`, locates the newest active `.humanize/rlcr/<timestamp>` loop at `hooks/loop-codex-stop-hook.sh:43-64`, then chooses normal `state.md` or finalize `finalize-state.md` at `hooks/loop-codex-stop-hook.sh:72-81`. Normal rounds validate state, todos, git state, summaries, goal-tracker initialization, max-iteration bounds, then build a Codex review prompt at `hooks/loop-codex-stop-hook.sh:671-749`. Codex review runs via `codex exec` with model/effort/timeout settings at `hooks/loop-codex-stop-hook.sh:813-840`, and the last non-empty review line is the decision marker at `hooks/loop-codex-stop-hook.sh:955-960`.
- inputs_outputs_state: Inputs include `CLAUDE_PROJECT_DIR` or cwd, `.humanize/rlcr` state files, `plan.md` backup, configured plan path, current round summary, `goal-tracker.md`, prompt templates, git status, the transcript path from hook input, Python todo checker output, and Codex CLI output. Outputs include JSON hook decisions with `"decision": "block"` for corrective prompts, stderr diagnostics, review prompt/result files under the loop dir, Codex debug command/stdout/stderr files under `$HOME/.cache/humanize/...` at `hooks/loop-codex-stop-hook.sh:779-791`, generated next-round prompts, and state-file renames. State transitions are: no active loop -> exit 0; normal issue round -> increment `current_round` in `state.md` and create `round-N-prompt.md` at `hooks/loop-codex-stop-hook.sh:1057-1123`; Codex `COMPLETE` -> rename `state.md` to `finalize-state.md` and block with finalize prompt at `hooks/loop-codex-stop-hook.sh:961-1021`; finalize success -> rename `finalize-state.md` to `complete-state.md` at `hooks/loop-codex-stop-hook.sh:651-657`; max-iteration completion -> `maxiter-state.md` via `end_loop` at `hooks/loop-codex-stop-hook.sh:639-643` or `hooks/loop-codex-stop-hook.sh:963-968`; `STOP` marker -> `stop-state.md` at `hooks/loop-codex-stop-hook.sh:1023-1051`; invalid model/effort/current round -> `unexpected-state.md` at `hooks/loop-codex-stop-hook.sh:102-119`.
- gates_or_invariants: Requires schema fields `plan_tracked` and `start_branch` at `hooks/loop-codex-stop-hook.sh:131-143`; validates model/effort for YAML-safe characters at `hooks/loop-codex-stop-hook.sh:102-113`; validates numeric `current_round` and defaults malformed `max_iterations` to 42 at `hooks/loop-codex-stop-hook.sh:115-124`; enforces current git branch equals loop start branch at `hooks/loop-codex-stop-hook.sh:145-176`; enforces plan backup exists and plan content has not changed at `hooks/loop-codex-stop-hook.sh:182-250`; blocks incomplete TodoWrite items before Codex at `hooks/loop-codex-stop-hook.sh:252-307`; fails closed when `git status` fails at `hooks/loop-codex-stop-hook.sh:316-338`; blocks changed code/docs files over 2000 lines at `hooks/loop-codex-stop-hook.sh:340-420`; blocks uncommitted changes and, when configured, unpushed commits at `hooks/loop-codex-stop-hook.sh:423-516`; requires summary file presence at `hooks/loop-codex-stop-hook.sh:518-554`; requires round-0 goal-tracker placeholders to be filled at `hooks/loop-codex-stop-hook.sh:556-630`; requires Codex result file exists and is non-empty at `hooks/loop-codex-stop-hook.sh:897-950`; treats `COMPLETE` and `STOP` only when they are the entire last non-empty line at `hooks/loop-codex-stop-hook.sh:955-960`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` for `find_active_loop`, `parse_state_file`, `MARKER_COMPLETE`, `MARKER_STOP`, `to_lower`, and `end_loop` at `hooks/loop-codex-stop-hook.sh:46-52`; `loop-common.sh` defines active-loop detection and finalize-state awareness at `hooks/lib/loop-common.sh:58-81`, state parsing at `hooks/lib/loop-common.sh:98-139`, and terminal state renames at `hooks/lib/loop-common.sh:615-648`. Sources `scripts/portable-timeout.sh` at `hooks/loop-codex-stop-hook.sh:50-55` and again defensively at `hooks/loop-codex-stop-hook.sh:793-811`. Calls `hooks/check-todos-from-transcript.py`, whose contract is exit 0 all done/no todos, exit 1 incomplete todos, exit 2 parse error at `hooks/check-todos-from-transcript.py:7-13` and `hooks/check-todos-from-transcript.py:90-129`. Loads templates from `prompt-template/block/*`, `prompt-template/codex/*`, and `prompt-template/claude/*`, using `load_and_render_safe` from `hooks/lib/template-loader.sh:167-203`.
- edge_cases_or_failure_modes: If no active loop exists or no state file exists, the hook allows exit silently. Missing todo checker means the todo gate is skipped. Todo checker parse errors block. Git branch/status timeouts block rather than allow bypass. Large-file detection only scans paths surfaced by non-`-z` porcelain output and known code/doc extensions, so unusual filenames or untouched oversized files are out of scope. Codex command absence blocks at `hooks/loop-codex-stop-hook.sh:755-775`; nonzero Codex exit, missing result file, stdout-only output copy failures, or empty result all block through `codex_failure_exit` at `hooks/loop-codex-stop-hook.sh:850-950`. In finalize phase, Codex is deliberately skipped after all quick gates pass, so finalize correctness depends on prior `COMPLETE` plus git/summary gates rather than a second Codex review.
- validation_or_tests: Finalize behavior is covered by `tests/test-finalize-phase.sh`: missing finalize summary blocks and Codex is not called at `tests/test-finalize-phase.sh:365-388`, dirty git blocks at `tests/test-finalize-phase.sh:390-407`, and successful finalize renames to `complete-state.md` at `tests/test-finalize-phase.sh:412-430`. Template reference scanning includes this stop hook in `tests/test-template-references.sh:56-64`. Plan-file/gate scenarios invoke the stop hook throughout `tests/test-plan-file-hooks.sh` per repository search results. I inspected these tests but did not execute them in this read-only research pass.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-047 `file` `prompt-template/block/finalize-state-file-modification.md`
- cursor: `[_]`
- core_role: Human-facing block template for protecting `finalize-state.md`, the managed state file for the RLCR Finalize Phase. It is part of the state-machine guard surface rather than executable logic.
- algorithmic_behavior: The template states that `finalize-state.md` cannot be modified because the loop system manages it, then redirects the worker toward finalize-phase duties: run the code-simplifier agent, commit changes, and write `finalize-summary.md` at `prompt-template/block/finalize-state-file-modification.md:1-8`.
- inputs_outputs_state: It has no placeholders and no dynamic inputs. Its output is rendered markdown used as a validator refusal message. It does not mutate state; it preserves the invariant that only loop machinery may move or rewrite `finalize-state.md`.
- gates_or_invariants: Enforces the invariant that `finalize-state.md` is system-owned. `hooks/lib/loop-common.sh` exposes `is_finalize_state_file_path` at `hooks/lib/loop-common.sh:262-266` and `finalize_state_file_blocked_message` at `hooks/lib/loop-common.sh:219-226`, which loads this exact template with a fallback. Write/Edit validators check finalize-state before the broader `state.md` pattern because `state.md` would also match `finalize-state.md`: see `hooks/loop-write-validator.sh:101-112` and `hooks/loop-edit-validator.sh:82-93`.
- dependencies_and_callers: Called through `finalize_state_file_blocked_message` in `hooks/lib/loop-common.sh:219-226`. Used by `hooks/loop-write-validator.sh` for direct writes at `hooks/loop-write-validator.sh:105-108`, by `hooks/loop-edit-validator.sh` for edits at `hooks/loop-edit-validator.sh:86-89`, and by `hooks/loop-bash-validator.sh` for shell modifications at `hooks/loop-bash-validator.sh:98-104`. Bash source-protection also blocks `mv`/`cp` from `finalize-state.md` at `hooks/loop-bash-validator.sh:245-252` and shell-wrapper attempts at `hooks/loop-bash-validator.sh:265-276`.
- edge_cases_or_failure_modes: Because the template is static, rendering cannot adapt paths or current round. If the template is missing, `load_and_render_safe` falls back to a shorter inline message, so protection remains but guidance becomes less specific. The validator intentionally has a cancel exception through `is_cancel_authorized` for controlled `state.md` or `finalize-state.md` to `cancel-state.md` moves when a `.cancel-requested` signal exists; that authorization is constrained at `hooks/lib/loop-common.sh:274-413`.
- validation_or_tests: Helper recognition for `finalize-state.md` is tested at `tests/test-finalize-phase.sh:174-194`, and existence of the message function is checked at `tests/test-finalize-phase.sh:211-215`. Direct validator coverage blocks Write, Edit, redirect writes, `mv` from, and `cp` from `finalize-state.md` at `tests/test-finalize-phase.sh:293-350`.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-077 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Prompt fragment used immediately after a full goal-alignment Codex review. It is a soft control surface that steers the next Claude round toward alignment failures rather than a hard validator.
- algorithmic_behavior: The template declares that the next round follows a Full Goal Alignment Check, then prioritizes three classes of corrective work: forgotten items found by Codex, acceptance criteria marked NOT MET, and unjustified deferrals at `prompt-template/claude/post-alignment-action-items.md:2-7`.
- inputs_outputs_state: It has no placeholders and no direct inputs. Its output is appended verbatim to the next-round prompt file. It does not directly change RLCR state, but it influences the next state transition by shaping the work Claude performs before the next stop-hook review.
- gates_or_invariants: The hard scheduling gate is in the stop hook: full alignment checks are rounds where `CURRENT_ROUND % 5 == 4` at `hooks/loop-codex-stop-hook.sh:683-687`. When Codex does not return `COMPLETE` or `STOP`, the hook advances to `NEXT_ROUND`, writes the next prompt, and appends this template only for post-alignment rounds at `hooks/loop-codex-stop-hook.sh:1057-1086`. The invariant is behavioral: alignment findings must be fed back into the next implementation round.
- dependencies_and_callers: Loaded by `load_template "$TEMPLATE_DIR" "claude/post-alignment-action-items.md"` in `hooks/loop-codex-stop-hook.sh:1080-1085`. The surrounding next-round prompt is rendered from `prompt-template/claude/next-round-prompt.md` at `hooks/loop-codex-stop-hook.sh:1066-1078`, then footer/push/goal-tracker instructions are appended at `hooks/loop-codex-stop-hook.sh:1088-1108`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, the hook silently skips the post-alignment addendum because it uses `load_template` and only appends when non-empty at `hooks/loop-codex-stop-hook.sh:1082-1085`. The block contains no machine-readable checklist or validation rule, so enforcement depends on the next Codex review catching ignored alignment issues. It only applies after issue-producing full-alignment rounds; if Codex returns `COMPLETE`, the hook enters Finalize Phase instead and this template is not appended.
- validation_or_tests: Template-reference coverage scans shell scripts including `hooks/loop-codex-stop-hook.sh` at `tests/test-template-references.sh:56-64` and checks discovered template references exist through the scan logic at `tests/test-template-references.sh:83-90` and `tests/test-template-references.sh:130-141`. I found no direct behavioral test that asserts this exact fragment is appended to a next-round prompt.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item headings present exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`