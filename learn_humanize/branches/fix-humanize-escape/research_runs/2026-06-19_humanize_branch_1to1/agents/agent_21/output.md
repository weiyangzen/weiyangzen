# agent_21 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-021 `file` `hooks/loop-write-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse `Write` gate for the RLCR loop. It prevents Claude from writing protected RLCR control artifacts, enforces the active loop directory, and ensures round-scoped summary writes target the current round. It is registered for `Write` in `hooks/hooks.json:14-21`.
- algorithmic_behavior: Reads hook JSON from stdin, extracts `.tool_name` and `.tool_input.file_path` with `jq`, and exits early for non-`Write` tools (`hooks/loop-write-validator.sh:23-31`). It lowercases the path for pattern checks while preserving the original path for exact active-loop comparisons. It blocks `round-*-todos.md` unless the file is allowlisted in the active loop (`hooks/loop-write-validator.sh:37-44`), blocks all `round-*-prompt.md` writes (`hooks/loop-write-validator.sh:47-50`), classifies summary/finalize-summary/in-loop paths (`hooks/loop-write-validator.sh:56-58`), and exits cleanly for unrelated paths (`hooks/loop-write-validator.sh:60-63`). For `.humanize/rlcr/` paths, it narrows validation to state files, finalize state, goal tracker, plan backup, summaries, and finalize summary while allowing unrelated loop-local files (`hooks/loop-write-validator.sh:65-73`).
- inputs_outputs_state: Input is Claude hook JSON on stdin plus environment `CLAUDE_PROJECT_DIR` when present; fallback project root is `pwd` (`hooks/loop-write-validator.sh:38`, `hooks/loop-write-validator.sh:80`). Shared state comes from the newest active `.humanize/rlcr/<timestamp>` directory located by `find_active_loop` (`hooks/lib/loop-common.sh:63-81`). It parses either `state.md` or `finalize-state.md` via `parse_state_file` to obtain `STATE_CURRENT_ROUND` (`hooks/loop-write-validator.sh:88-98`; parser at `hooks/lib/loop-common.sh:112-139`). Outputs are exit `0` to allow or exit `2` with a rendered blocking message on stderr for denied writes. It does not mutate files.
- gates_or_invariants: Protected invariant: `state.md` and `finalize-state.md` cannot be written by Claude; finalize state is checked first because the generic state matcher also matches it (`hooks/loop-write-validator.sh:101-113`). Finalize summary is allowed only at exactly `$ACTIVE_LOOP_DIR/finalize-summary.md` (`hooks/loop-write-validator.sh:121-125`). `plan.md` inside `.humanize/rlcr/` is blocked as a backup/control artifact (`hooks/loop-write-validator.sh:132-138`). `goal-tracker.md` is blocked after round 0 and instructs updates through the current summary (`hooks/loop-write-validator.sh:145-148`). Round summaries outside `.humanize/rlcr/` are blocked with the correct active-loop path (`hooks/loop-write-validator.sh:155-162`). Summary files in the loop must match `current_round`, except allowlisted historical files (`hooks/loop-write-validator.sh:181-198`). Final exact directory invariant: the requested path must equal `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME` (`hooks/loop-write-validator.sh:205-216`).
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` (`hooks/loop-write-validator.sh:15-17`) for path classifiers, active-loop discovery, allowlist, state parsing, and block-message renderers. `loop-common.sh` sources `hooks/lib/template-loader.sh`, which resolves `prompt-template` and renders `{{VAR}}` placeholders with a single-pass awk substitution (`hooks/lib/template-loader.sh:24-31`, `hooks/lib/template-loader.sh:58-132`). Denial messages use `load_and_render_safe`, which falls back when templates are missing or render empty (`hooks/lib/template-loader.sh:170-203`). Callers/tests invoke the hook directly with hook JSON in `tests/test-allowlist-validators.sh:137`, `tests/test-plan-file-hooks.sh:198`, and `tests/test-finalize-phase.sh:284`.
- edge_cases_or_failure_modes: If there is no active loop, protected summary/state checks generally allow (`hooks/loop-write-validator.sh:84-86`), except todos without an active loop are blocked because the allowlist cannot be established (`hooks/loop-write-validator.sh:37-44`). Legacy `.humanize-loop.local` paths are intentionally not treated as RLCR loop directories because `is_in_humanize_loop_dir` only matches `.humanize/rlcr/` (`hooks/lib/loop-common.sh:415-419`), and tests assert legacy plan writes are allowed (`tests/test-plan-file-hooks.sh:1030-1040`). Case-insensitive pattern detection can identify protected file names, but final exact path comparison is case-sensitive, so case changes in the concrete path can fail directory validation. The current `IS_FINALIZE_PHASE` variable is set but not used directly; behavior is path-based through `finalize-state.md` and `finalize-summary.md`.
- validation_or_tests: Allowlist tests confirm `round-1-todos.md` and historical `round-0-summary.md` are allowed in the active loop, while non-allowlisted `round-3-todos.md` and wrong-round `round-2-summary.md` are blocked (`tests/test-allowlist-validators.sh:133-183`). Plan backup blocking is covered at `tests/test-plan-file-hooks.sh:194-205`. Finalize behavior covers allowing `finalize-summary.md` and blocking `finalize-state.md` (`tests/test-finalize-phase.sh:281-303`). Template-reference tests include this validator in critical scripts scanned for safe template usage (`tests/test-template-references.sh:56-64`, `tests/test-template-references.sh:174-180`). Tests were inspected, not executed.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-051 `file` `prompt-template/block/git-not-clean.md`
- cursor: `[_]`
- core_role: Stop-hook block template for the “git working tree is not clean” gate. It defines the human-facing remediation contract when the RLCR loop tries to stop while uncommitted or untracked changes remain.
- algorithmic_behavior: The template has two dynamic placeholders: `{{GIT_ISSUES}}` for the detected dirty-state summary and `{{SPECIAL_NOTES}}` for contextual notes such as untracked `.humanize*` or other untracked files (`prompt-template/block/git-not-clean.md:3-4`). It then prescribes ordered required actions: optionally run `code-simplifier`, review untracked files and `.gitignore`, stage real changes, and commit with a descriptive message (`prompt-template/block/git-not-clean.md:5-9`). It also encodes commit authorship rules forbidding AI attribution (`prompt-template/block/git-not-clean.md:11-14`) and tells the agent to retry exit after committing (`prompt-template/block/git-not-clean.md:16`).
- inputs_outputs_state: Inputs are provided by `hooks/loop-codex-stop-hook.sh` when `GIT_STATUS_CACHED` is non-empty: `GIT_ISSUES` is set to `uncommitted changes`, and `SPECIAL_NOTES` is assembled from optional local/untracked templates (`hooks/loop-codex-stop-hook.sh:430-458`). Output is rendered markdown assigned to `REASON`, then embedded in a JSON block decision via `jq -n` with `decision: "block"` and a system message (`hooks/loop-codex-stop-hook.sh:461-482`). The template itself does not alter state; the stop hook blocks exit and leaves the active loop intact.
- gates_or_invariants: This template supports the pre-review “git clean” invariant: Codex review or finalize completion should not proceed while the repository has uncommitted changes (`hooks/loop-codex-stop-hook.sh:423-428`, `hooks/loop-codex-stop-hook.sh:461-482`). The commit invariant explicitly says commit messages must follow project conventions and must not include `Co-Authored-By: Claude` or similar AI attribution (`prompt-template/block/git-not-clean.md:11-14`).
- dependencies_and_callers: Consumed by `load_and_render_safe "$TEMPLATE_DIR" "block/git-not-clean.md"` in `hooks/loop-codex-stop-hook.sh:470-472`. Rendering is performed by `hooks/lib/template-loader.sh`, which keeps missing placeholders intact and performs single-pass substitution (`hooks/lib/template-loader.sh:7-13`, `hooks/lib/template-loader.sh:58-132`). Special-note dependencies are sibling templates `block/git-not-clean-humanize-local.md` and `block/git-not-clean-untracked.md`, loaded if matching untracked conditions are found (`hooks/loop-codex-stop-hook.sh:441-457`).
- edge_cases_or_failure_modes: If this template is missing or empty, the stop hook falls back to an inline shorter “Detected: {{GIT_ISSUES}}” message (`hooks/loop-codex-stop-hook.sh:464-472`; fallback behavior at `hooks/lib/template-loader.sh:170-203`). If `SPECIAL_NOTES` is empty, line 4 renders as blank but the required actions remain intact. Since `git add -A` is suggested “if all files should be tracked” (`prompt-template/block/git-not-clean.md:8`), it relies on the separate bash validator’s `.humanize` protection to prevent accidental local RLCR state staging; that protection is implemented in `git_adds_humanize` (`hooks/lib/loop-common.sh:421-539`).
- validation_or_tests: Finalize-phase test `T-NEG-2` creates a dirty file and expects the stop hook to return a block mentioning uncommitted/git/clean (`tests/test-finalize-phase.sh:390-407`). Template loader tests include hook/template reference scanning rather than this template specifically (`tests/test-template-references.sh:52-64`). Tests were inspected, not executed.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-081 `file` `prompt-template/codex/regular-review.md`
- cursor: `[_]`
- core_role: Codex review prompt template for normal RLCR rounds that are not full-alignment rounds. It defines Codex’s review algorithm, goal-alignment checks, output contract, and the `COMPLETE` transition gate.
- algorithmic_behavior: The template starts with the current round heading (`prompt-template/codex/regular-review.md:1`), requires Codex to read the original implementation plan first via `@{{PLAN_FILE}}` (`prompt-template/codex/regular-review.md:3-9`), and frames the review against both that plan and `@{{PROMPT_FILE}}` (`prompt-template/codex/regular-review.md:11`). It injects Claude’s submitted summary between explicit start/end comments (`prompt-template/codex/regular-review.md:13-18`). Part 1 directs a skeptical implementation review focused on gaps between planned design and actual implementation (`prompt-template/codex/regular-review.md:20-31`). Part 2 requires reading `@{{GOAL_TRACKER_FILE}}` and checking AC progress, forgotten items, deferrals, and plan evolution (`prompt-template/codex/regular-review.md:33-45`). Part 3 injects `{{GOAL_TRACKER_UPDATE_SECTION}}`, allowing the stop hook to splice in goal-tracker update instructions (`prompt-template/codex/regular-review.md:47`). Part 4 defines when to write findings to `@{{REVIEW_RESULT_FILE}}` and when, only if everything is complete, to end with `COMPLETE` (`prompt-template/codex/regular-review.md:49-57`).
- inputs_outputs_state: Inputs are variables rendered by `hooks/loop-codex-stop-hook.sh`: `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `GOAL_TRACKER_UPDATE_SECTION`, `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, previous-round numbers, and `REVIEW_RESULT_FILE` (`hooks/loop-codex-stop-hook.sh:736-748`). The generated output file is `$LOOP_DIR/round-${CURRENT_ROUND}-review-prompt.md` (`hooks/loop-codex-stop-hook.sh:671-673`, `hooks/loop-codex-stop-hook.sh:736-748`). Codex must then write its review to `$LOOP_DIR/round-${CURRENT_ROUND}-review-result.md`; the stop hook later reads that result and inspects the last non-empty line for `COMPLETE` or `STOP` (`hooks/loop-codex-stop-hook.sh:953-962`).
- gates_or_invariants: Main invariant: `COMPLETE` is only permitted as the final line if all original plan tasks are fully completed, all ACs are met, and there are no deferred or pending items (`prompt-template/codex/regular-review.md:53-57`). Deferrals are explicitly treated as incomplete and should force review comments plus an actionable implementation plan (`prompt-template/codex/regular-review.md:24-31`, `prompt-template/codex/regular-review.md:53-56`). The goal-alignment summary format `ACs: X/Y addressed | Forgotten items: N | Unjustified deferrals: N` is mandated for every regular review (`prompt-template/codex/regular-review.md:42-45`). The stop hook interprets a last-line `COMPLETE` as the transition into Finalize Phase rather than immediate loop completion (`hooks/loop-codex-stop-hook.sh:70`, `hooks/loop-codex-stop-hook.sh:961-962`).
- dependencies_and_callers: Used only in the normal-review branch when `FULL_ALIGNMENT_CHECK` is false; full alignment rounds use `prompt-template/codex/full-alignment-review.md` instead (`hooks/loop-codex-stop-hook.sh:718-748`). It depends on the summary file having passed earlier stop-hook checks and been read into `SUMMARY_CONTENT` (`hooks/loop-codex-stop-hook.sh:526`, `hooks/loop-codex-stop-hook.sh:675`). It depends on `codex/goal-tracker-update-section.md` or fallback text for Part 3 (`hooks/loop-codex-stop-hook.sh:679-681`). Rendering uses the shared safe template loader, so missing template content degrades to `REGULAR_REVIEW_FALLBACK` (`hooks/loop-codex-stop-hook.sh:707-716`, `hooks/lib/template-loader.sh:170-203`).
- edge_cases_or_failure_modes: If `SUMMARY_CONTENT` includes `{{...}}`, the loader’s single-pass substitution prevents accidental second-order placeholder expansion (`hooks/lib/template-loader.sh:54-57`). If `PLAN_FILE`, `PROMPT_FILE`, or `GOAL_TRACKER_FILE` point to missing files, the template still renders; failure would be behavioral during Codex review rather than render-time. The phrase “The word COMPLETE on the last line will stop Claude” (`prompt-template/codex/regular-review.md:57`) is precise for normal loop blocking behavior, but the stop hook’s actual implementation uses `COMPLETE` to enter Finalize Phase before final completion (`hooks/loop-codex-stop-hook.sh:70`, `hooks/loop-codex-stop-hook.sh:961-962`). Non-final mentions such as “CANNOT COMPLETE” are safe because the hook checks only the last non-empty line exactly (`hooks/loop-codex-stop-hook.sh:955-962`).
- validation_or_tests: `rg` shows the template is referenced by the stop hook at `hooks/loop-codex-stop-hook.sh:736`. Tests around COMPLETE/finalize behavior validate the downstream interpretation of Codex review output, including COMPLETE triggering Finalize Phase and non-COMPLETE reviews blocking with feedback (`tests/test-finalize-phase.sh:443-475`, `tests/test-finalize-phase.sh:547-575`). Template-reference tests scan shell scripts for template references and safe rendering patterns (`tests/test-template-references.sh:52-64`, `tests/test-template-references.sh:172-180`). Tests were inspected, not executed.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item sections above, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`