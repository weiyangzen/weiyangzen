# agent_14 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-014 `file` `hooks/check-todos-from-transcript.py`
- cursor: `[_]`
- core_role: Stop-hook helper that gates RLCR loop exit on Claude Code native TodoWrite completion. It prevents expensive Codex review and round transition when the latest transcript todo list still contains unfinished work.
- algorithmic_behavior: `find_latest_todos()` reads a transcript JSONL file line by line, ignores blank or malformed JSONL entries, and keeps replacing `latest_todos` when it sees a non-empty `TodoWrite` input (`hooks/check-todos-from-transcript.py:20`). It recognizes three transcript shapes: Claude assistant messages with nested tool use (`:51`), alternate `type: message` records (`:65`), and direct `type: tool_use` records (`:78`). `main()` parses hook JSON from stdin, resolves `transcript_path`, loads the latest todo list, and classifies every todo whose `status` is not exactly `completed` as incomplete (`:90`, `:113`).
- inputs_outputs_state: Input is hook JSON on stdin with optional `transcript_path` (`:91`, `:99`). Persistent state is not modified; transient state is only `latest_todos`. Outputs are process exit codes and optional text: exit `0` means allow when there is no transcript, no todos, or all todos are completed (`:100`, `:109`, `:128`); exit `1` prints `INCOMPLETE_TODOS` plus `- [status] content` lines (`:121`); exit `2` prints `PARSE_ERROR` for invalid hook input JSON (`:94`).
- gates_or_invariants: The gate is strict on status spelling: only `completed` passes (`:118`). The latest non-empty TodoWrite call wins, so old pending todos do not block if later replaced by a completed list. Invalid JSONL transcript lines are ignored, but invalid hook input JSON blocks with parse error. Missing `transcript_path` and missing transcript files fail open (`:25`, `:100`).
- dependencies_and_callers: Called by `hooks/loop-codex-stop-hook.sh` before Codex review (`hooks/loop-codex-stop-hook.sh:258`). Exit `2` produces a blocking JSON hook response for checker parse error (`:265`), and exit `1` produces a blocking incomplete-todos response using `prompt-template/block/incomplete-todos.md` fallback rendering (`:284`).
- edge_cases_or_failure_modes: Empty TodoWrite arrays do not replace an earlier latest list because the code updates only when `todos` is truthy (`hooks/check-todos-from-transcript.py:62`, `:75`, `:84`). Missing `status` becomes incomplete with an empty status label. Missing `content` prints an empty task body. Transcript schema drift outside the three supported shapes fails open by producing no todos.
- validation_or_tests: Static inspection only. `tests/test-todo-checker.sh` covers invalid/empty hook JSON, missing transcript path, missing transcript file, completed vs pending/in-progress todos, invalid JSONL lines, latest TodoWrite precedence, direct `tool_use`, and alternate `message` format (`tests/test-todo-checker.sh:51`, `:121`, `:144`, `:179`, `:196`, `:212`, `:227`).
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-044 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared RLCR state-machine and validator library. It centralizes loop discovery, state frontmatter parsing, round-file recognition, protected-file block messages, cancel authorization, `.humanize` git-add protection, shell write-pattern detection, and loop terminal-state renaming.
- algorithmic_behavior: Constants define state frontmatter field names and terminal exit reasons (`hooks/lib/loop-common.sh:15`, `:30`). The library sources `template-loader.sh`, resolves `TEMPLATE_DIR`, and warns on invalid templates while retaining inline fallbacks (`:46`). `find_active_loop()` selects only the newest timestamped loop directory containing `state.md` or `finalize-state.md`, intentionally ignoring older “zombie” loops (`:63`). `parse_state_file()` extracts YAML-like frontmatter into `STATE_*` globals and applies defaults for current round, max iterations, and push policy (`:112`). File helpers classify round files, state/finalize files, goal tracker, finalize summary, and `.humanize/rlcr` containment (`:146`, `:250`, `:256`, `:262`, `:268`, `:415`). Message helpers render template-backed block reasons for todos, prompts, state files, finalize state, summary bash writes, goal tracker bash writes, git-add protection, and goal tracker modification (`:190`, `:201`, `:210`, `:219`, `:228`, `:239`, `:541`, `:601`).
- inputs_outputs_state: Inputs are active loop base paths, state file paths, file paths, lowercased shell commands, and current-round context from parsed state. Outputs are shell return statuses, rendered block text on stdout, parsed `STATE_*` globals, or side effects in `end_loop()`. `end_loop()` is the main state transition: it validates a reason and renames active `state.md` to `<reason>-state.md` for `complete`, `cancel`, `maxiter`, `stop`, or `unexpected` (`:622`).
- gates_or_invariants: Active loop identity is newest-directory-only (`:71`). Todos access is blocked except a narrow allowlist of early generated files in the active loop (`:170`). `is_cancel_authorized()` permits moving `state.md` or `finalize-state.md` to `cancel-state.md` only when `.cancel-requested` exists, command substitution/backticks/newlines/chaining are absent, no hidden `$` variables remain after `$loop_dir` normalization, command is exactly `mv`, exactly two args are present, and normalized source/destination match the active loop (`:274`, `:290`, `:295`, `:305`, `:323`, `:328`, `:378`, `:396`, `:405`). `git_adds_humanize()` blocks direct `.humanize` path variants, quoted paths, chained git-add segments, force-plus-broad-scope, and all-scope adds when `.humanize` exists (`:443`, `:446`, `:480`, `:488`, `:496`, `:511`, `:518`). `command_modifies_file()` is the generic bash write detector for redirection, `tee`, in-place edit, `mv/cp`, `rm`, `dd`, `truncate`, `printf`, and `exec` redirections (`:571`).
- dependencies_and_callers: Sourced by read/write/edit/bash validators and the plan validator (`hooks/loop-read-validator.sh:16`, `hooks/loop-write-validator.sh:17`, `hooks/loop-edit-validator.sh:16`, `hooks/loop-bash-validator.sh:16`, `hooks/loop-plan-file-validator.sh:17`). The read validator uses round-file helpers, active-loop discovery, state parsing, allowlist checks, and directory/round gates (`hooks/loop-read-validator.sh:36`, `:75`, `:116`). Write/edit validators use the same shared helpers to block todos, prompts, state files, plan backups, and post-round-0 goal tracker edits (`hooks/loop-write-validator.sh:37`, `:96`, `:105`, `:145`; `hooks/loop-edit-validator.sh:36`, `:78`, `:86`, `:114`). Bash validator uses git-add, cancel, command-modification, state, plan, goal-tracker, prompt, summary, and todos gates (`hooks/loop-bash-validator.sh:81`, `:98`, `:108`, `:128`, `:295`, `:308`, `:324`, `:334`, `:344`). Codex stop hook uses active-loop discovery, state parsing, and terminal transitions (`hooks/loop-codex-stop-hook.sh:59`, `:89`, `:106`, `:641`, `:1049`).
- edge_cases_or_failure_modes: Parsing is grep/sed frontmatter parsing, not full YAML; repeated fields or complex YAML structures are not modeled. `is_state_file_path()` also matches `finalize-state.md`, so callers must check finalize-state first; the validators explicitly do this. Command analysis is regex-based rather than a shell AST, so extremely unusual quoting or shell syntax may evade or over-trigger checks. Several command gates assume the caller passes lowercased input, which can affect case-sensitive paths. `find_active_loop()` relies on sortable timestamp directory names and ignores older dirs even if they still contain state.
- validation_or_tests: Static inspection only. Related coverage includes allowlist validator tests (`tests/test-allowlist-validators.sh:64`), bash modification pattern tests (`tests/test-bash-validator-patterns.sh:65`), cancel signal and `is_cancel_authorized()` tests (`tests/test-cancel-signal-file.sh:1250`), finalize helpers/state parsing tests (`tests/test-finalize-phase.sh:174`, `:592`), state exit naming and `end_loop()` tests (`tests/test-state-exit-naming.sh:179`), template reference tests for common block templates (`tests/test-template-references.sh:149`), and branch-specific humanize escape tests for `.humanize` git-add path variants, quoted paths, force/all flags, chained commands, and `git -C` variants (`tests/test-humanize-escape.sh:83`, `:96`, `:109`, `:135`, `:153`, `:164`).
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-074 `file` `prompt-template/claude/goal-tracker-update-request.md`
- cursor: `[_]`
- core_role: Claude-facing prompt contract for requesting Goal Tracker state changes indirectly. It is part of the RLCR gate that prevents Claude from directly editing `goal-tracker.md` after Round 0 while still allowing justified goal-state evolution through Codex review.
- algorithmic_behavior: The template instructs Claude to include a specific markdown section only when Goal Tracker updates are needed (`prompt-template/claude/goal-tracker-update-request.md:2`). The required section name is `Goal Tracker Update Request`, with requested changes and justification subsections (`:4`, `:6`, `:12`). Example requested changes include marking tasks completed with evidence, adding open issues, recording plan evolution, and deferring tasks with acceptance-criteria impact (`:7`). The closing rule states Codex will review and apply justified updates (`:16`).
- inputs_outputs_state: No template variables are consumed. Output is appended markdown in the next-round prompt. It does not mutate state directly; it creates a reviewable request in Claude’s summary, which Codex can later translate into `goal-tracker.md` changes.
- gates_or_invariants: The invariant is separation of authority: Claude requests, Codex reviews/applies. This aligns with write/edit/bash gates that block direct goal tracker modification after Round 0 (`hooks/loop-write-validator.sh:145`, `hooks/loop-edit-validator.sh:114`, `hooks/loop-bash-validator.sh:308`). The exact heading matters because Codex prompt sections tell Codex to look for a “Goal Tracker Update Request” section.
- dependencies_and_callers: Loaded and appended by `hooks/loop-codex-stop-hook.sh` while building the next Claude prompt after Codex finds issues (`hooks/loop-codex-stop-hook.sh:1103`). If the file is missing or empty, the hook falls back to a one-line generic instruction (`:1104`). Related prompt text in `prompt-template/claude/next-round-prompt.md` tells Claude to include this section when needed, and `prompt-template/codex/goal-tracker-update-section.md` tells Codex how to process such requests.
- edge_cases_or_failure_modes: Because this is prose, enforcement depends on downstream prompt compliance and Codex recognition of the section heading. There is no schema validation for individual requested changes. If Claude uses a different heading or omits justification, the request may not be detected or may be rejected in review. The fallback instruction is less structured than the template.
- validation_or_tests: Static inspection only. Template/cross-reference coverage exists in `tests/test-templates-comprehensive.sh`, which validates the Codex-side goal-tracker update section renders with a goal tracker path (`tests/test-templates-comprehensive.sh:531`), and `tests/test-template-references.sh` verifies common template references used by shared validators (`tests/test-template-references.sh:149`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item evidence sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`