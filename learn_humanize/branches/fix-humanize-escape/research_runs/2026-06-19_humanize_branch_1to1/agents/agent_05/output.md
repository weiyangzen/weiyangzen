# agent_05 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-005 `directory` `scripts`
- cursor: `[_]`
- core_role: The `scripts` directory is the executable shell layer for starting and monitoring the Humanize RLCR loop. Recursive inspection found three files and no child subdirectories: `scripts/humanize.sh`, `scripts/portable-timeout.sh`, and `scripts/setup-rlcr-loop.sh`. The directory owns loop session bootstrap, portable timeout execution, and terminal monitor behavior for `.humanize/rlcr` sessions.
- algorithmic_behavior: `scripts/humanize.sh` defines `_humanize_monitor_codex()` and the public `humanize monitor rlcr-loop` command path (`scripts/humanize.sh:9`, `scripts/humanize.sh:805-838`). It finds the latest timestamped loop session with `find` instead of shell globs (`scripts/humanize.sh:27-49`), finds the newest Codex log in `$HOME/.cache/humanize/<sanitized-project>/<timestamp>/round-*-codex-run.log` (`scripts/humanize.sh:51-108`), parses state and goal tracker files (`scripts/humanize.sh:116-252`), parses Git working-tree status (`scripts/humanize.sh:254-300`), draws a fixed terminal status bar (`scripts/humanize.sh:316-453`), sets/restores terminal scroll region (`scripts/humanize.sh:455-471`), performs idempotent cleanup and graceful stop (`scripts/humanize.sh:473-512`), then loops over log tailing, log rotation/truncation, deleted sessions, newer sessions, and newer round logs (`scripts/humanize.sh:547-803`).
- inputs_outputs_state: `humanize.sh` reads current working directory, `.humanize/rlcr`, timestamped session directories, `state.md` or `*-state.md`, `goal-tracker.md`, Git status/diff, terminal dimensions, and cache log files. It outputs terminal UI, status messages, and tailed log content. Its main state variables are `current_session_dir`, `current_file`, `current_loop_status`, `last_size`, `last_no_log_status`, `monitor_running`, `cleanup_done`, and `tail_pid`. `scripts/setup-rlcr-loop.sh` takes a relative plan file plus options such as `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, and `--push-every-round` (`scripts/setup-rlcr-loop.sh:104-180`), then writes `.humanize/rlcr/<timestamp>/state.md`, a plan backup, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:467-653`). `scripts/portable-timeout.sh` exposes `TIMEOUT_IMPL` and `run_with_timeout <seconds> <command...>` (`scripts/portable-timeout.sh:10-29`, `scripts/portable-timeout.sh:33-71`).
- gates_or_invariants: `setup-rlcr-loop.sh` gates execution on Git repo presence and at least one commit (`scripts/setup-rlcr-loop.sh:211-221`), relative non-space non-metacharacter plan paths (`scripts/setup-rlcr-loop.sh:227-248`), non-symlink readable plan files inside project root (`scripts/setup-rlcr-loop.sh:250-290`), no submodule plan location (`scripts/setup-rlcr-loop.sh:292-305`), plan tracking policy (`scripts/setup-rlcr-loop.sh:311-356`), at least five lines and at least three content lines (`scripts/setup-rlcr-loop.sh:362-415`), Codex CLI availability (`scripts/setup-rlcr-loop.sh:417-423`), and YAML-safe branch/model/effort values (`scripts/setup-rlcr-loop.sh:429-461`). `humanize.sh` gates monitor startup on `.humanize/rlcr` existence and a valid session directory (`scripts/humanize.sh:20-25`, `scripts/humanize.sh:521-526`), restores terminal state through cleanup on interrupt or deleted loop directory (`scripts/humanize.sh:478-512`, `scripts/humanize.sh:549-552`, `scripts/humanize.sh:682-686`), and uses `find`-based iteration to avoid zsh glob failures (`scripts/humanize.sh:35-47`, `scripts/humanize.sh:71-106`, `scripts/humanize.sh:129-143`).
- dependencies_and_callers: `commands/start-rlcr-loop.md` restricts the slash command to `scripts/setup-rlcr-loop.sh` and invokes it with user arguments (`commands/start-rlcr-loop.md:4`, `commands/start-rlcr-loop.md:12-14`). `hooks/loop-codex-stop-hook.sh` sources `scripts/portable-timeout.sh` (`hooks/loop-codex-stop-hook.sh:46-52`) and writes Codex stdout/stderr logs into the cache layout that `humanize.sh` scans (`hooks/loop-codex-stop-hook.sh:779-840`). The scripts depend on `bash`, `find`, `sed`, `grep`, `git`, `stat`, `tail`, `tput`, `date`, `wc`, `codex`, and optionally `gtimeout`, GNU `timeout`, `python3`, or `python`.
- edge_cases_or_failure_modes: `humanize.sh` explicitly handles missing `.humanize/rlcr`, empty loop directories, dotfile-only directories, absent state files, deleted sessions/logs, new sessions without logs, file truncation/rotation, no Git repo, and zsh array indexing via `setopt localoptions ksharrays` (`scripts/humanize.sh:10-18`, `scripts/humanize.sh:123-150`, `scripts/humanize.sh:670-715`, `scripts/humanize.sh:724-797`). `portable-timeout.sh` falls back to running without a timeout if no implementation exists (`scripts/portable-timeout.sh:64-69`); its Python fallback interpolates the timeout value into inline Python, so callers need numeric validation. `setup-rlcr-loop.sh` rejects many unsafe plan/branch/model forms but writes loop artifacts directly once gates pass, so failures after `mkdir -p "$LOOP_DIR"` can leave a partial loop directory (`scripts/setup-rlcr-loop.sh:467-476`).
- validation_or_tests: `tests/run-all-tests.sh` includes `test-plan-file-validation.sh`, `test-humanize-escape.sh`, `test-zsh-monitor-safety.sh`, `test-monitor-runtime.sh`, and `test-monitor-e2e-real.sh` (`tests/run-all-tests.sh:37-50`). `tests/test-plan-file-validation.sh` covers setup path validation, tracking policy, content validation, and model option validation. `tests/test-humanize-escape.sh` covers `.humanize` git-add blocking and find-based monitor iteration patterns (`tests/test-humanize-escape.sh:1-12`, `tests/test-humanize-escape.sh:195-269`). `tests/test-zsh-monitor-safety.sh` verifies zsh sourcing and empty/dotfile state scans (`tests/test-zsh-monitor-safety.sh:1-10`, `tests/test-zsh-monitor-safety.sh:59-190`). I inspected these tests but did not execute them in this read-only research pass.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-035 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: This is an executable specification for RLCR monitor runtime behavior, focused on graceful stop and terminal restoration. It verifies AC-1.1 clean exit messaging when `.humanize/rlcr` disappears and AC-1.3 terminal restoration through cleanup (`tests/test-monitor-runtime.sh:3-10`).
- algorithmic_behavior: The test uses strict Bash mode, derives `PROJECT_ROOT`, tracks pass/fail counters, creates `/tmp/test-monitor-runtime-$$`, and removes it on exit (`tests/test-monitor-runtime.sh:12-53`). Test 1 builds a loop skeleton, writes a temporary harness that sources `scripts/humanize.sh`, stubs `_restore_terminal`, defines simplified `_cleanup` and `_graceful_stop`, then asserts restore, cleanup, stop message, and reason output (`tests/test-monitor-runtime.sh:61-134`). Test 2 verifies `cleanup_done` prevents repeated cleanup execution (`tests/test-monitor-runtime.sh:143-177`). Test 3 simulates the main loop directory-existence check and confirms deletion triggers graceful stop (`tests/test-monitor-runtime.sh:186-251`). Test 4 verifies terminal restoration by source inspection for `_restore_terminal()`, `printf "\033[r"`, and `_cleanup` calling `_restore_terminal` (`tests/test-monitor-runtime.sh:263-310`). Test 5 verifies `_graceful_stop()` calls `_cleanup` (`tests/test-monitor-runtime.sh:319-323`).
- inputs_outputs_state: Inputs are `scripts/humanize.sh`, temporary `.humanize/rlcr` fixtures, generated mini harness scripts, and shell command output. Outputs are colored PASS/FAIL lines, a summary, and process exit code 0 only when `TESTS_FAILED` remains zero (`tests/test-monitor-runtime.sh:25-34`, `tests/test-monitor-runtime.sh:328-346`). Internal test state includes `TESTS_PASSED`, `TESTS_FAILED`, `cleanup_done`, `monitor_running`, `restore_called`, and `call_count`.
- gates_or_invariants: The core invariants are: graceful stop must call cleanup before emitting user-facing stop text; cleanup must be idempotent; loop-directory deletion must return a graceful stop path; terminal scroll region reset must exist; and `_cleanup -> _restore_terminal` must remain present in `scripts/humanize.sh`. These correspond to assertions at `tests/test-monitor-runtime.sh:112-134`, `tests/test-monitor-runtime.sh:173-177`, `tests/test-monitor-runtime.sh:235-251`, `tests/test-monitor-runtime.sh:293-310`, and `tests/test-monitor-runtime.sh:319-323`.
- dependencies_and_callers: The test depends on Bash, `grep`, `chmod`, `rm`, `/tmp`, and `scripts/humanize.sh`. It is included by the aggregate runner (`tests/run-all-tests.sh:47-50`) and is complemented by `tests/test-monitor-e2e-real.sh`, which runs the real `_humanize_monitor_codex` deletion path under Bash and zsh, and by `tests/test-zsh-monitor-safety.sh` for glob safety.
- edge_cases_or_failure_modes: Tests 1-3 use embedded simplified function copies rather than invoking the nested `_graceful_stop` inside `_humanize_monitor_codex`, because that nested function is not directly accessible after sourcing. This makes the tests useful for intended behavior but partly vulnerable to drift from the real implementation. Test 4 contains a pipeline using `grep -q` before another `grep -q` (`tests/test-monitor-runtime.sh:282`), which cannot emit content to the second grep; the script later performs the real `_cleanup` source check at `tests/test-monitor-runtime.sh:306-310`, so coverage is preserved but that earlier conditional is ineffective.
- validation_or_tests: This file is itself the validation artifact. It ends with explicit success text for AC-1.1 and AC-1.3 when no failures are recorded (`tests/test-monitor-runtime.sh:335-341`). I inspected it directly and did not run it during this read-only research pass.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-065 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: This prompt block is a reusable gate message for blocking direct `round-*-todos.md` file access. Its algorithmic contract is to redirect work tracking from file-based todos to the native `TodoWrite` tool (`prompt-template/block/todos-file-access.md:1-8`).
- algorithmic_behavior: The template has no executable logic by itself; it is loaded by `todos_blocked_message()` in `hooks/lib/loop-common.sh`, with an inline fallback if the template is unavailable (`hooks/lib/loop-common.sh:190-199`). Validators detect todo files using `is_round_file_type`, which matches lowercase paths ending in `round-[0-9]+-todos.md` (`hooks/lib/loop-common.sh:146-153`). When matched and not allowlisted, read/write/edit validators print this block to stderr and exit 2 (`hooks/loop-read-validator.sh:33-43`, `hooks/loop-write-validator.sh:33-45`, `hooks/loop-edit-validator.sh:32-44`). Bash validator also blocks shell commands that modify `round-[0-9]+-todos.md` outside its narrow active-loop exception (`hooks/loop-bash-validator.sh:340-351`).
- inputs_outputs_state: Inputs are the attempted tool action, target path or shell command, active loop directory, and template path. Output is a blocking reason telling the agent not to create or access `round-*-todos.md` and to use `TodoWrite`. The state transition is attempted file-based todo access into a blocked tool result; no loop state file is changed by the template.
- gates_or_invariants: The invariant is that `round-*-todos.md` should not become the task-state source of truth during RLCR operation. `hooks/lib/loop-common.sh` has a small allowlist for selected active-loop files (`hooks/lib/loop-common.sh:170-188`), so the practical gate is “block non-allowlisted todo file access/modification.” The message text itself is intentionally terse and tool-directed (`prompt-template/block/todos-file-access.md:3-8`).
- dependencies_and_callers: The template depends on `hooks/lib/template-loader.sh` via `load_and_render_safe`, through the shared `todos_blocked_message()` helper. It is called by `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh`. `tests/test-template-references.sh` explicitly includes `block/todos-file-access.md` in common template reference validation (`tests/test-template-references.sh:149-154`).
- edge_cases_or_failure_modes: The template says “create or access,” but enforcement varies by hook: Read/Write/Edit match direct file-tool paths, while Bash enforcement shown here targets modifying commands rather than every possible shell read. The active-loop allowlist means `round-1-todos.md` and `round-2-todos.md` can bypass this block in specific paths (`hooks/lib/loop-common.sh:170-188`, `hooks/loop-bash-validator.sh:344-351`), so the block is a policy message for the validator decision rather than a standalone absolute rule.
- validation_or_tests: Reference integrity is covered by `tests/test-template-references.sh`, and broader template loading/fallback behavior is exercised by the template-loader and comprehensive template tests. I inspected the template and its callers directly.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 assigned item sections present once by header
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`