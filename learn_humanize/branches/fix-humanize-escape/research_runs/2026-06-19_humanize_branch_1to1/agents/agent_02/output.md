# agent_02 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-002 `directory` `commands`
- cursor: `[_]`
- core_role: The `commands` directory is the slash-command control surface for the RLCR loop. It does not implement most algorithmic logic itself; it routes `/humanize:start-rlcr-loop` into the setup script and documents the safe manual cancellation flow for normal and finalize phases.
- algorithmic_behavior: `commands/start-rlcr-loop.md` invokes `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh" $ARGUMENTS` through its allowed Bash tool surface at [commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/commands/start-rlcr-loop.md:1). Its behavioral contract is iterative implementation, mandatory summaries, Codex review on exit, feedback continuation, and `COMPLETE`-driven completion/finalization at lines 16-23 and 51-55. `commands/cancel-rlcr-loop.md` defines the operator cancellation algorithm: select newest `.humanize/rlcr/*/`, detect `state.md` versus `finalize-state.md`, create `.cancel-requested`, then rename the active state file to `cancel-state.md`; finalize cancellation requires an explicit AskUserQuestion confirmation at [commands/cancel-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/commands/cancel-rlcr-loop.md:20).
- inputs_outputs_state: Start input is a plan path plus options such as `--max`, `--codex-model`, `--codex-timeout`, and `--push-every-round` from [commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/commands/start-rlcr-loop.md:3). The downstream setup script writes `state.md` with `current_round`, `max_iterations`, Codex config, `push_every_round`, `plan_file`, `plan_tracked`, `start_branch`, and `started_at` at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:485). Cancel output is a preserved terminal state file `cancel-state.md`, not deletion of the loop directory, with `.cancel-requested` as an authorization signal.
- gates_or_invariants: The command templates intentionally constrain allowed tools. Start permits only the setup script at [commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/commands/start-rlcr-loop.md:4). Cancel allows only listing, state reads, signal touch, state renames, `Read`, and `AskUserQuestion` at [commands/cancel-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/commands/cancel-rlcr-loop.md:3). The key invariant is that only the newest timestamped loop directory is active, and only `state.md` or `finalize-state.md` means active at [commands/cancel-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/commands/cancel-rlcr-loop.md:49).
- dependencies_and_callers: `start-rlcr-loop.md` depends directly on `scripts/setup-rlcr-loop.sh`, which creates loop state, goal tracker, and the initial prompt. `cancel-rlcr-loop.md` depends on hook enforcement in `hooks/loop-bash-validator.sh` and shared authorization logic in `hooks/lib/loop-common.sh:is_cancel_authorized`, which requires `.cancel-requested`, rejects command substitution/chaining, and allows only exact `mv state.md|finalize-state.md cancel-state.md` within the active loop at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/lib/loop-common.sh:274).
- edge_cases_or_failure_modes: Start can fail downstream if the plan path/options are invalid, Codex is unavailable, state schema later becomes outdated, or branch/plan tracking checks fail. Cancel handles three active-loop states: no loop directory, newest directory without active state, normal loop, and finalize phase. The finalize branch is intentionally cautious because finalization would otherwise complete without another Codex review; cancellation asks for confirmation before renaming `finalize-state.md` at [commands/cancel-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/commands/cancel-rlcr-loop.md:34).
- validation_or_tests: Cancellation safety is covered by `tests/test-cancel-signal-file.sh`, which asserts `mv state.md cancel-state.md` is allowed only when `.cancel-requested` exists and other state writes remain blocked even with the signal at [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-cancel-signal-file.sh:1) and [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-cancel-signal-file.sh:119). Finalize-aware cancellation is indirectly covered by the finalize-phase active-loop and state-protection tests in `tests/test-finalize-phase.sh`.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-032 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: This is an executable specification for the RLCR Finalize Phase state machine. It proves how `COMPLETE` transitions from normal review into `finalize-state.md`, how finalize completion exits to `complete-state.md`, and how validators treat finalize state as active but protected.
- algorithmic_behavior: The test builds an isolated git repo, mocks `codex`, creates `.humanize/rlcr/<timestamp>/state.md`, and drives hook scripts with JSON hook input. It sources `hooks/lib/loop-common.sh` at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-finalize-phase.sh:44), creates mock Codex binaries at lines 47-81, and builds representative state frontmatter at lines 118-141.
- inputs_outputs_state: Inputs include hook JSON for `Write`, `Edit`, `Bash`, `Read`, and Stop hooks; mock Codex output ending in either `COMPLETE` or `CONTINUE`; transcript JSONL with TodoWrite state; summary files; and git cleanliness. Outputs are hook exit codes, JSON block decisions, created review files, and state file renames among `state.md`, `finalize-state.md`, `complete-state.md`, and `maxiter-state.md`.
- gates_or_invariants: Helper tests assert `is_finalize_state_file_path` only matches `finalize-state.md`, `is_finalize_summary_path` only matches `finalize-summary.md`, and `finalize_state_file_blocked_message` exists at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-finalize-phase.sh:174). Active-loop detection must include `finalize-state.md` but exclude terminal `complete-state.md` at lines 219-256. Write/Edit/Bash validators must block modification and source-copy/move of `finalize-state.md`, while allowing the active loop’s `finalize-summary.md` at lines 281-350.
- dependencies_and_callers: The spec exercises `hooks/lib/loop-common.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/check-todos-from-transcript.py` through stop-hook behavior. It also depends on git being available in the temp repo and on `jq` usage inside the hooks.
- edge_cases_or_failure_modes: Covered edge cases include missing finalize summary blocking exit, dirty git blocking finalize completion, incomplete todos blocking before completion, finalize phase skipping Codex entirely, max iterations bypassing finalize and producing `maxiter-state.md`, normal non-final rounds remaining unaffected, and validator parsing when only `finalize-state.md` exists. The stop-hook assertions for missing summary and no Codex call are at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-finalize-phase.sh:365); dirty git and successful completion are at lines 390-440.
- validation_or_tests: Positive coverage includes `COMPLETE` entering Finalize Phase with `state.md -> finalize-state.md` and a simplification prompt at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-finalize-phase.sh:460), Finalize completion with `finalize-state.md -> complete-state.md` at lines 412-440, and normal non-`COMPLETE` rounds incrementing `current_round` and preserving `state.md` at lines 575-614. Negative coverage includes max-iteration skip at lines 478-496, incomplete todo block at lines 499-540, and terminal `complete-state.md` not being active at lines 238-256. I inspected the test directly but did not execute it in this read-only research run.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-062 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: This block template defines the user-facing validation contract when an active RLCR state file lacks required schema fields introduced by humanize version 1.1.2+. It is part of the guardrail layer for safe state parsing and migration handling.
- algorithmic_behavior: The template renders a block reason headed `State Schema Outdated`, substitutes `{{FIELD_NAME}}`, tells the user the loop was started with an older humanize version, offers cancel/update/restart options, and states the loop will be terminated as `unexpected` to preserve state information at [prompt-template/block/schema-outdated.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/prompt-template/block/schema-outdated.md:1).
- inputs_outputs_state: Input is the missing state field name passed as `FIELD_NAME`. The primary caller is `hooks/loop-plan-file-validator.sh:schema_validation_error`, which renders `block/schema-outdated.md` via `load_and_render_safe` and returns a JSON block decision with the rendered reason at [hooks/loop-plan-file-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/loop-plan-file-validator.sh:56). The validator checks `plan_tracked` and `start_branch` as required fields at lines 76-86.
- gates_or_invariants: The invariant is that active loop state must include `plan_tracked` and `start_branch`; otherwise branch consistency and plan tracking checks are unsafe. Shared parsing extracts these fields from state frontmatter in `parse_state_file` at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/lib/loop-common.sh:112). New loops write both fields in setup at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/scripts/setup-rlcr-loop.sh:485).
- dependencies_and_callers: Rendering depends on `hooks/lib/template-loader.sh`, whose `render_template` does single-pass `{{VAR}}` substitution and leaves missing placeholders intact to avoid recursive placeholder injection at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/lib/template-loader.sh:50). `load_and_render_safe` falls back to inline text if the template is missing or empty at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/lib/template-loader.sh:167).
- edge_cases_or_failure_modes: If the template is missing, the plan-file validator has a fallback with equivalent guidance. If the field value itself contains template syntax, single-pass rendering prevents secondary substitution. The stop hook performs a related schema check, but currently uses an inline combined message for missing `plan_tracked` or `start_branch` rather than this specific template at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/hooks/loop-codex-stop-hook.sh:127).
- validation_or_tests: The assigned finalize test ensures `loop-plan-file-validator.sh` can parse `finalize-state.md` without errors when required schema fields exist at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/fix-humanize-escape/tests/test-finalize-phase.sh:653). Broader state-exit behavior is also exercised by state naming tests, while schema-outdated rendering itself is covered by the validator’s required-field path.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: FIX_HUMANIZE_ESCAPE-HZ-002, FIX_HUMANIZE_ESCAPE-HZ-032, FIX_HUMANIZE_ESCAPE-HZ-062
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`