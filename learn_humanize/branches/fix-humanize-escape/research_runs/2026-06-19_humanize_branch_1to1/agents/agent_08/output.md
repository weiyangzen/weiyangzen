# agent_08 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-008 `directory` `hooks/lib`
- cursor: `[_]`
- core_role: Shared algorithm library for the RLCR hook system. `hooks/lib` contains exactly two files: `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`. Together they provide loop-state discovery, state frontmatter parsing, path classification, terminal state renaming, command-gate helpers, cancel authorization, `.humanize` git-add protection, and safe prompt-template rendering used by the hook validators and stop hook.
- algorithmic_behavior: `loop-common.sh` defines canonical state field names and exit markers at `hooks/lib/loop-common.sh:15` through `hooks/lib/loop-common.sh:40`, sources `template-loader.sh` at `hooks/lib/loop-common.sh:46`, and initializes `TEMPLATE_DIR` from the plugin root at `hooks/lib/loop-common.sh:50`. `find_active_loop` accepts a loop base directory, selects only the lexicographically newest timestamp directory, and returns it only when `state.md` or `finalize-state.md` exists, intentionally ignoring older “zombie” loop directories at `hooks/lib/loop-common.sh:63`. `parse_state_file` extracts YAML-like frontmatter fields into `STATE_*` globals, strips legacy quotes for `start_branch` and `plan_file`, and defaults `current_round=0`, `max_iterations=10`, and `push_every_round=false` at `hooks/lib/loop-common.sh:112`. Filename helpers classify `round-N-summary.md`, `round-N-prompt.md`, and `round-N-todos.md`, extract round numbers, and recognize state, finalize-state, finalize-summary, and goal-tracker paths at `hooks/lib/loop-common.sh:141` through `hooks/lib/loop-common.sh:272`. `is_cancel_authorized` is a gate for the only allowed user-driven state rename: it requires `.cancel-requested`, rejects command substitution, backticks, remaining `$`, newlines, shell chaining, extra arguments, and non-exact source/destination paths, then allows only `state.md` or `finalize-state.md` to `cancel-state.md` at `hooks/lib/loop-common.sh:284`. `git_adds_humanize` blocks direct or broad `git add` commands that would stage `.humanize`, including forced broad adds and `-A/--all` when `.humanize` exists, at `hooks/lib/loop-common.sh:443`. `command_modifies_file` detects shell modification patterns such as redirects, `tee`, `sed -i`, `mv`, `cp`, `rm`, `dd`, `truncate`, and write-oriented `exec` at `hooks/lib/loop-common.sh:574`. `end_loop` validates the terminal reason and renames the active state file to `{reason}-state.md` for `complete`, `cancel`, `maxiter`, `stop`, or `unexpected` at `hooks/lib/loop-common.sh:622`.
- inputs_outputs_state: Main inputs are hook JSON fields passed to caller scripts, filesystem paths under `$CLAUDE_PROJECT_DIR/.humanize/rlcr`, state frontmatter fields, shell command strings, prompt-template variable assignments, and git working tree state for `git check-ignore`. Outputs are stdout path decisions, stderr block messages, shell return codes, exported/global `STATE_*` variables, rendered prompt text, and file state transitions such as `state.md -> complete-state.md` or `state.md -> unexpected-state.md`. `template-loader.sh` resolves `prompt-template` from `hooks/lib` by going two directories up at `hooks/lib/template-loader.sh:26`, loads template files by relative name at `hooks/lib/template-loader.sh:36`, and renders `{{VAR}}` placeholders via a single awk pass using `TMPL_VAR_` environment variables at `hooks/lib/template-loader.sh:58`.
- gates_or_invariants: Only `state.md` and `finalize-state.md` mark an active loop; terminal files such as `complete-state.md`, `stop-state.md`, `maxiter-state.md`, `cancel-state.md`, and `unexpected-state.md` must not revive a loop. Only the newest loop directory is considered active. Template rendering is intentionally single-pass so replacement values containing `{{OTHER_VAR}}` are not recursively expanded, preserving prompt integrity at `hooks/lib/template-loader.sh:54`. Missing templates degrade to fallback messages through `load_and_render_safe` at `hooks/lib/template-loader.sh:170`. Template directory validation requires `block`, `codex`, and `claude` subdirectories at `hooks/lib/template-loader.sh:208`. Path detection is scoped to `.humanize/rlcr/` only, not the legacy `.humanize-loop.local` path, via `is_in_humanize_loop_dir` at `hooks/lib/loop-common.sh:416`. The cancel authorization gate requires exact normalized loop paths and exactly two `mv` arguments at `hooks/lib/loop-common.sh:328` through `hooks/lib/loop-common.sh:412`.
- dependencies_and_callers: `hooks/hooks.json` registers caller scripts as Claude hooks: `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-read-validator.sh`, and `loop-bash-validator.sh` under `PreToolUse`, `loop-plan-file-validator.sh` under `UserPromptSubmit`, and `loop-codex-stop-hook.sh` under `Stop` at `hooks/hooks.json:14` and `hooks/hooks.json:52`. `loop-read-validator.sh` sources `loop-common.sh` at `hooks/loop-read-validator.sh:16`, uses `find_active_loop`, `is_in_humanize_loop_dir`, `parse_state_file`, and template rendering to gate reads. `loop-write-validator.sh` sources it at `hooks/loop-write-validator.sh:17` and blocks wrong summary paths, state writes, prompt writes, plan backup writes, and post-round-0 goal-tracker writes. `loop-edit-validator.sh` sources it at `hooks/loop-edit-validator.sh:16` and applies similar edit gates. `loop-bash-validator.sh` sources it at `hooks/loop-bash-validator.sh:16`, using `git_adds_humanize`, `command_modifies_file`, and `is_cancel_authorized` to prevent shell bypasses. `loop-codex-stop-hook.sh` sources it at `hooks/loop-codex-stop-hook.sh:48`, parses state at `hooks/loop-codex-stop-hook.sh:89`, and uses `end_loop` for unexpected, max-iteration, and stop exits. Test callers include `tests/test-state-exit-naming.sh`, `tests/test-template-loader.sh`, `tests/test-error-scenarios.sh`, `tests/test-humanize-escape.sh`, `tests/test-cancel-signal-file.sh`, `tests/test-finalize-phase.sh`, `tests/test-allowlist-validators.sh`, and `tests/test-template-references.sh`.
- edge_cases_or_failure_modes: `find_active_loop` ignores older directories even if they contain `state.md`, so an older unfinished loop will not revive after a newer terminal directory exists. `parse_state_file` returns failure only when the file is missing, but otherwise tolerates missing fields and supplies defaults, so downstream scripts must separately validate numeric or schema fields. `is_state_file_path` matches `finalize-state.md` because it ends with `state.md`; callers compensate by checking `is_finalize_state_file_path` first, as shown in `hooks/loop-write-validator.sh:103` and `hooks/loop-bash-validator.sh:91`. `git_adds_humanize` depends on the current working directory for `.humanize` existence and `git check-ignore`. `is_cancel_authorized` lowercases command input and expected loop path, which supports case-insensitive command matching but assumes path comparisons remain valid in the intended environment. `end_loop` fails for invalid reasons and missing state files, returning nonzero and writing diagnostics.
- validation_or_tests: `tests/test-state-exit-naming.sh` directly verifies active-loop detection, terminal-state exclusion, valid terminal state renames, missing state handling, and legacy path rejection. `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh` cover template rendering and safe fallback behavior. `tests/test-humanize-escape.sh` targets `.humanize` git-add path variants. `tests/test-cancel-signal-file.sh` heavily exercises `is_cancel_authorized` and shell bypass gates. `tests/test-template-references.sh` checks that template references from hook scripts exist and that critical validators use `load_and_render_safe`. I did not execute tests because the requested task is read-only research notes and no modification was needed.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-038 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Executable behavioral specification for RLCR state-file lifecycle naming and active-loop discovery. It proves that only live state files are treated as active and that terminal loop exits preserve state by renaming `state.md` to a reason-specific terminal file.
- algorithmic_behavior: The test creates a temporary git repository and loop directory under `.humanize/rlcr/2024-01-01_12-00-00`, then sources `hooks/lib/loop-common.sh` at `tests/test-state-exit-naming.sh:64`. Tests 1 through 7 call `find_active_loop` to confirm `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` are not active, while `state.md` is active, and a newer directory with `state.md` takes precedence at `tests/test-state-exit-naming.sh:38` through `tests/test-state-exit-naming.sh:176`. Tests 8 through 10 call `end_loop` to reject invalid reasons, create `{reason}-state.md` for all valid reasons, and fail gracefully when the source state file is missing at `tests/test-state-exit-naming.sh:182` through `tests/test-state-exit-naming.sh:239`. Tests 11 through 13 assert `.humanize/rlcr` is the only supported loop path and that legacy `.humanize-loop.local` is neither detected by `is_in_humanize_loop_dir` nor searched when using the new base path at `tests/test-state-exit-naming.sh:245` through `tests/test-state-exit-naming.sh:287`.
- inputs_outputs_state: Inputs are synthetic loop directories, state frontmatter fixtures, terminal state files, and explicit function calls to `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir`. Outputs are shell PASS/FAIL counters, diagnostic messages, test exit status equal to `$TESTS_FAILED`, and filesystem state transitions caused by `end_loop` renaming `state.md` to `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md`.
- gates_or_invariants: `set -uo pipefail` is enabled at `tests/test-state-exit-naming.sh:13`, but the script intentionally toggles `set +e` around negative `end_loop` cases to inspect exit codes. The key invariant is that `find_active_loop` should not treat terminal files as active-loop indicators; live loop state must be named exactly `state.md` for normal phase. The test also encodes the invariant that active loop lookup is scoped to the provided `.humanize/rlcr` base path, so legacy path artifacts cannot be used accidentally.
- dependencies_and_callers: Depends on `git`, `mktemp`, `bash`, standard shell utilities, and `hooks/lib/loop-common.sh`. It indirectly depends on `hooks/lib/template-loader.sh` because `loop-common.sh` sources it during load. It does not call hook entrypoints; it tests library functions directly.
- edge_cases_or_failure_modes: Covers terminal files that might otherwise be mistaken for active state due to name suffixes. Covers invalid terminal reason strings, missing `state.md`, newer-directory precedence, and coexistence of legacy and new loop directories. It does not test `finalize-state.md`, even though `find_active_loop` treats it as active; finalize behavior is covered elsewhere (`tests/test-finalize-phase.sh` was referenced during research). Because test 7 leaves an older `state.md` in place when creating a newer one, it specifically catches algorithms that scan for any active state rather than only the newest loop directory.
- validation_or_tests: This file is itself the validation artifact. The success criterion is all 13 numbered tests passing and process exit code 0. It validates implementation points in `hooks/lib/loop-common.sh`: `find_active_loop` at `hooks/lib/loop-common.sh:63`, `is_in_humanize_loop_dir` at `hooks/lib/loop-common.sh:416`, and `end_loop` at `hooks/lib/loop-common.sh:622`.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-068 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: Block-message template used when an agent targets a valid round file name in the wrong loop directory. It is part of the hook gate feedback contract for correcting stale or misplaced reads/writes during an active RLCR loop.
- algorithmic_behavior: The template is short and declarative: title, attempted action/path, correct path, and a final hint line at `prompt-template/block/wrong-directory-path.md:1` through `prompt-template/block/wrong-directory-path.md:6`. Placeholders are `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}`. Rendering is handled by `load_and_render_safe` from `template-loader.sh`, which performs single-pass substitution and leaves missing variables unchanged.
- inputs_outputs_state: Inputs are supplied by validators. `loop-read-validator.sh` passes `ACTION=read`, the requested `FILE_PATH`, and the computed `CORRECT_PATH` at `hooks/loop-read-validator.sh:141` through `hooks/loop-read-validator.sh:144`. `loop-write-validator.sh` passes `ACTION=write to`, `FILE_PATH`, and `CORRECT_PATH` at `hooks/loop-write-validator.sh:211` through `hooks/loop-write-validator.sh:214`. The output is rendered Markdown written to stderr before the validator exits with code 2, causing the tool action to be blocked.
- gates_or_invariants: This template is reached only after the validator has already identified an active loop, parsed the current round, classified the file as a relevant round file or loop file, and computed `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"`. The read gate uses it when a requested prompt/summary file is under `.humanize/rlcr` and has the correct round but not the active loop directory at `hooks/loop-read-validator.sh:135` through `hooks/loop-read-validator.sh:145`. The write gate uses it similarly after summary round validation at `hooks/loop-write-validator.sh:205` through `hooks/loop-write-validator.sh:215`. The invariant is that active loop files must be accessed through the active timestamp directory, not a stale session directory with the same filename.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` for placeholder replacement and fallback handling. Direct callers found are `hooks/loop-read-validator.sh` and `hooks/loop-write-validator.sh`. `tests/test-template-references.sh` is a meta-validation caller that checks referenced templates exist, and `tests/test-templates-comprehensive.sh` exercises template loading broadly.
- edge_cases_or_failure_modes: The template itself has no branching. If one of the three variables is omitted, `render_template` leaves the unresolved placeholder in place because missing variables are intentionally preserved. If the template file is missing or empty, callers fall back to inline `# Wrong Directory Path` messages. The final hint says `cat {{FILE_PATH}}`, which points at the attempted path rather than `{{CORRECT_PATH}}`; this may be intentional as a visibility hint or may be confusing because the preceding line names the correct path. It is not a skip candidate because it defines the user-facing contract for a concrete path validation gate.
- validation_or_tests: Reference validation is covered by `tests/test-template-references.sh`, which scans `load_and_render_safe "$TEMPLATE_DIR" ...` calls. Template-loader behavior is covered by `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh`. The gate path is exercised indirectly by hook validator tests that invoke `loop-read-validator.sh` and `loop-write-validator.sh` with wrong active-loop directory paths.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item evidence sections present; item IDs are used only as section headings
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`