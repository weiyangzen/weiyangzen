# agent_01 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root is the Humanize Claude Code plugin implementation for RLCR, an iterative loop where Claude implements a plan and Codex reviews each round. The root coordinates slash commands, setup scripts, tool hooks, prompt templates, runtime monitor utilities, and shell/Python test specifications. Public contract is described in `README.md:7-27`, plugin metadata in `.claude-plugin/plugin.json:1-19`, command entrypoints in `commands/start-rlcr-loop.md:1-14` and `commands/cancel-rlcr-loop.md:1-51`, and hook wiring in `hooks/hooks.json:1-64`.
- algorithmic_behavior: Setup begins in `scripts/setup-rlcr-loop.sh`: validates arguments and plan path, rejects unsafe paths, checks git state, copies the plan into `.humanize/rlcr/<timestamp>/plan.md`, writes `state.md`, creates `goal-tracker.md`, and emits `round-0-prompt.md` plus summary path instructions (`scripts/setup-rlcr-loop.sh:104-180`, `227-356`, `485-653`). Runtime gates are implemented by PreToolUse hooks: Read gates wrong round/location and todos access (`hooks/loop-read-validator.sh:36-44`, `100-146`), Write gates prompt/todos/state/plan/goal-tracker/summary paths (`hooks/loop-write-validator.sh:37-50`, `105-148`, `155-216`), Edit mirrors protected file behavior (`hooks/loop-edit-validator.sh:36-49`, `86-148`), and Bash detects bypasses through redirects, tee, sed/perl/awk, mv/cp/rm/dd/truncate/exec, git push, and `.humanize` git-add attempts (`hooks/loop-bash-validator.sh:63-84`, `98-287`, `295-350`). The Stop hook is the main state machine: it validates schema, branch, plan integrity, TodoWrite completion, git cleanliness, large files, summary presence, goal tracker initialization, max iteration limits, then builds and runs a Codex review prompt (`hooks/loop-codex-stop-hook.sh:59-176`, `182-307`, `316-516`, `522-657`, `671-840`). Review output transitions on exact final markers: `COMPLETE` enters Finalize Phase, `STOP` ends the loop, otherwise the hook increments `current_round` and creates the next prompt (`hooks/loop-codex-stop-hook.sh:955-1021`, `1023-1050`, `1057-1123`).
- inputs_outputs_state: Inputs include slash-command arguments, a relative plan file, Claude hook JSON stdin, transcript JSONL for TodoWrite state, current git branch/status, template files, shell command strings, and Codex review output. Outputs include `.humanize/rlcr/<timestamp>/state.md`, `finalize-state.md`, terminal state files such as `complete-state.md` or reason-specific state names, `goal-tracker.md`, `round-N-prompt.md`, `round-N-summary.md`, `round-N-review-prompt.md`, `round-N-review-result.md`, JSON hook block decisions, and debug logs under `$HOME/.cache/humanize/<sanitized-project>/<timestamp>` (`scripts/setup-rlcr-loop.sh:467-498`, `hooks/loop-codex-stop-hook.sh:779-840`). Shared state parsing and transitions live in `hooks/lib/loop-common.sh:98-139` and `622-648`. The monitor reads loop/cache state and displays status without mutating core state (`scripts/humanize.sh:152-252`, `316-453`, `547-799`).
- gates_or_invariants: Plan files must be relative, inside the project, readable, non-symlink, without spaces or shell metacharacters, and have enough content (`scripts/setup-rlcr-loop.sh:227-290`, `362-415`). Plan tracking mode must remain consistent (`scripts/setup-rlcr-loop.sh:311-356`, `hooks/loop-plan-file-validator.sh:120-226`). Branch must not change during an active loop (`hooks/loop-plan-file-validator.sh:92-112`, `hooks/loop-codex-stop-hook.sh:149-176`). Prompt files are read-only instruction files; state files are loop-managed; goal tracker is immutable after round 0 except by summary request; summaries must target current round and active loop path (`hooks/lib/loop-common.sh:201-247`, `hooks/loop-write-validator.sh:47-50`, `145-216`). Template rendering is single-pass to prevent placeholder injection (`hooks/lib/template-loader.sh:50-57`, `71-129`). `.humanize` must not be git-added, including quoted/direct/forced/broad variants (`hooks/lib/loop-common.sh:421-539`). The branch-specific zsh/glob-safety work is visible in monitor code using `find` rather than shell globs for empty or dotfile-only directories (`scripts/humanize.sh:30-47`, `71-106`, `129-143`).
- dependencies_and_callers: Claude plugin dispatch calls command markdown and hooks through `.claude-plugin/plugin.json` and `hooks/hooks.json`. Hooks depend on `jq`, `git`, `python3`, `codex`, `awk`, `sed`, `grep`, `find`, `stat`, and the portable timeout wrapper (`scripts/portable-timeout.sh`). Shared logic is in `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`; prompt text comes from `prompt-template/block`, `prompt-template/claude`, and `prompt-template/codex`. CI workflows run syntax, template, and plan validation suites (`.github/workflows/shell-syntax-check.yml:19-61`, `.github/workflows/template-test.yml:26-62`, `.github/workflows/plan-file-test.yml:37-60`). `tests/run-all-tests.sh:32-51` orders the local executable specification suite.
- edge_cases_or_failure_modes: Missing template files degrade to empty strings or safe fallbacks (`hooks/lib/template-loader.sh:36-47`, `170-203`). State schema gaps, invalid Codex config, corrupted round numbers, branch mismatch, missing plan backups, changed plans, incomplete todos, dirty git state, oversized files, missing summaries, uninitialized goal tracker, missing Codex CLI, nonzero Codex exit, missing/empty review result, max iterations, and cancellation all have explicit block or end paths (`hooks/loop-codex-stop-hook.sh:102-143`, `182-250`, `258-307`, `325-420`, `461-516`, `529-657`, `755-950`). Cancel is authorized only by a signal file and an exact safe mv shape, rejecting command substitution, extra args, shell operators, and hidden variables (`hooks/lib/loop-common.sh:274-413`). Read-only branch export lacks `.git`; `git status --short --branch` could not run here because this export is not a git checkout and the sandbox forbids Apple toolchain cache files under `/tmp`.
- validation_or_tests: Syntax validation passed for the assigned test and key scripts via `bash -n tests/test-error-scenarios.sh hooks/lib/template-loader.sh hooks/lib/loop-common.sh hooks/loop-bash-validator.sh hooks/loop-write-validator.sh hooks/loop-edit-validator.sh hooks/loop-read-validator.sh hooks/loop-codex-stop-hook.sh scripts/humanize.sh scripts/setup-rlcr-loop.sh`. Runtime test attempts were constrained by the managed read-only sandbox: `bash tests/test-error-scenarios.sh` reached the suite but failed 4 cases after Bash could not create temp files for here-strings used by `render_template`; `bash tests/test-humanize-escape.sh` failed early for the same temp-file issue plus denied `/tmp` fixture creation. Existing tests cover template loading, template references, Bash validator patterns, allowlists, plan validation hooks, cancel signal handling, finalize phase, state exit naming, zsh monitor safety, monitor runtime, and the humanize escape fixes.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-031 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/template-loader.sh` error behavior. It verifies that template loading and rendering fail gracefully, do not crash strict shell callers, preserve malformed placeholders as literals, and use fallback text when templates are missing. It is not branch-specific escape logic, but it is core because every hook block message depends on safe template rendering.
- algorithmic_behavior: The script sources the loader and derives `TEMPLATE_DIR` (`tests/test-error-scenarios.sh:11-15`), maintains pass/fail counters (`22-35`), then runs 12 independent assertions. It checks missing file and missing directory behavior for `load_template` (`45-65`), missing template behavior for `load_and_render` (`70-78`), empty render input (`83-91`), special regex characters in substitution values (`96-106`), survival under a subshell with `set -euo pipefail` (`111-130`), safe fallback rendering and fallback variable substitution (`137-158`), template directory validation (`164-180`), empty placeholder preservation (`186-193`), and unclosed placeholder preservation (`199-206`). Final state is converted to process exit status at `218-226`.
- inputs_outputs_state: Inputs are repository-relative loader code, the computed template directory, missing template names, constructed template strings, fallback strings, and variable assignments such as `PATH=...` or `FILE=test.md`. Outputs are colored PASS/FAIL lines, expected/got diagnostics, total pass/fail counts, and exit code 0 or 1. State is only in `TESTS_PASSED` and `TESTS_FAILED`.
- gates_or_invariants: Missing templates must return an empty string with exit 0 for low-level functions, while `load_and_render_safe` must substitute the fallback (`tests/test-error-scenarios.sh:45-78`, `137-158`). `render_template` must treat special regex characters as literal data, keep `{{}}` and unclosed `{{` text unchanged, and not recursively expand injected placeholders; the underlying loader documents this single-pass invariant at `hooks/lib/template-loader.sh:50-57` and implements it with awk/environment lookup at `74-129`. `validate_template_dir` requires `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:205-221`).
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`. Indirect callers protected by these guarantees include shared block-message helpers in `hooks/lib/loop-common.sh:190-247`, prompt-file blocking through `prompt_write_blocked_message` at `hooks/lib/loop-common.sh:201-208`, and Stop-hook review/fallback templates at `hooks/loop-codex-stop-hook.sh:680-748` and `1005-1108`. The suite is included in the aggregate test order at `tests/run-all-tests.sh:32-51`.
- edge_cases_or_failure_modes: It intentionally covers absent paths, missing templates, empty content, special characters that would break sed-style replacement, strict-mode command substitution, missing fallback templates, invalid template directory, empty placeholder names, and unclosed delimiters. In this read-only execution environment, tests involving `render_template` hit `hooks/lib/template-loader.sh:129` where Bash here-string plumbing attempted to create a temp file and received `Operation not permitted`; this caused empty render outputs in several assertions. That is a sandbox execution failure, not direct evidence that the algorithm fails in a writable shell environment.
- validation_or_tests: `bash -n tests/test-error-scenarios.sh` passed. `bash tests/test-error-scenarios.sh` executed and reported 8 passed, 4 failed; failures were `Special regex characters`, `load_and_render_safe fallback substitution`, `Empty placeholder handling`, and `Unclosed placeholder handling`, each preceded by the sandbox temp-file error from `template-loader.sh:129`.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-061 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: Block-message template for attempts to modify `round-*-prompt.md`. It enforces the RLCR invariant that prompt files are generated instructions from Codex/system flow to Claude and must not be self-modified by the worker.
- algorithmic_behavior: The template has no placeholders and renders as static guidance: title at `prompt-template/block/prompt-file-write.md:1`, hard prohibition at `:3`, rationale at `:5`, required alternate workflow at `:7-10`, and error-reporting route through the summary file at `:12`. It is selected by `prompt_write_blocked_message`, which calls `load_and_render_safe "$TEMPLATE_DIR" "block/prompt-file-write.md"` with an inline fallback (`hooks/lib/loop-common.sh:201-208`).
- inputs_outputs_state: Input is the template path requested by the shared helper. Output is markdown written to stderr by validators that block the tool call. There is no internal state, no variables, and no data-dependent rendering. State transition happens in the caller: hook returns a blocking exit/status after emitting this message.
- gates_or_invariants: The protected invariant is that `round-N-prompt.md` remains authoritative input, not writable output. Write and Edit validators detect prompt filenames via `is_round_file_type "$FILE_PATH_LOWER" "prompt"` and block (`hooks/loop-write-validator.sh:47-50`, `hooks/loop-edit-validator.sh:46-49`). Bash validator detects shell-level file modification against `round-[0-9]+-prompt\.md` and blocks (`hooks/loop-bash-validator.sh:324-326`). The allowed transition is to read the prompt, perform tasks, and write results into the matching summary file, as stated by the template itself (`prompt-template/block/prompt-file-write.md:7-10`).
- dependencies_and_callers: Depends on the template loader for safe static rendering (`hooks/lib/template-loader.sh:170-203`). Called through `prompt_write_blocked_message` in `hooks/lib/loop-common.sh:201-208`, then by Write, Edit, and Bash validators. Template existence is covered by `tests/test-template-references.sh:154`, and `rg` found only those intended runtime references plus the template file itself.
- edge_cases_or_failure_modes: If the template is missing or empty, callers still emit the fallback from `hooks/lib/loop-common.sh:203-205`, so the block remains functional but loses richer wording. Because the template has no placeholders, malformed variable substitution is not a risk. Bash bypasses are handled only to the extent `command_modifies_file` recognizes the mutation shape; that helper covers redirects, tee, in-place edit tools, mv/cp, rm, dd, truncate, printf redirect, and exec redirect (`hooks/lib/loop-common.sh:571-599`).
- validation_or_tests: Direct inspection confirms the full file is 12 lines and static. Syntax validation for the hook callers passed via `bash -n`. Reference discovery confirmed runtime callers at `hooks/loop-write-validator.sh:48`, `hooks/loop-edit-validator.sh:47`, `hooks/loop-bash-validator.sh:324-326`, and shared helper at `hooks/lib/loop-common.sh:201-208`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item evidence headings present; each assigned item section appears once above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`