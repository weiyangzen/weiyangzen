# agent_11 fix-humanize-escape 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `05ff234806d5fe8686b2d9554c00eaa6f3e77dd8`

## Item Evidence

### FIX_HUMANIZE_ESCAPE-HZ-011 `directory` `prompt-template/codex`
- cursor: `[_]`
- core_role: Codex-side review prompt template directory for the RLCR loop. It defines the algorithmic review contracts that convert Claude round summaries into Codex review prompts, including regular review, full alignment audit, and goal-tracker update handling.
- algorithmic_behavior: The directory contains exactly three Markdown templates: `prompt-template/codex/regular-review.md`, `prompt-template/codex/full-alignment-review.md`, and `prompt-template/codex/goal-tracker-update-section.md`. The runtime selection happens in `hooks/loop-codex-stop-hook.sh:677-748`: it renders the shared goal-tracker section first, then selects full alignment when `CURRENT_ROUND % 5 == 4`, otherwise regular review. The generated prompt is written to `round-N-review-prompt.md` and then passed to `codex exec` at `hooks/loop-codex-stop-hook.sh:821-840`.
- inputs_outputs_state: Inputs are loop state fields and derived paths: `CURRENT_ROUND`, `PLAN_FILE`, `PROMPT_FILE`, `SUMMARY_CONTENT`, `GOAL_TRACKER_FILE`, `DOCS_PATH`, `REVIEW_RESULT_FILE`, and for full alignment also `COMPLETED_ITERATIONS`, `LOOP_TIMESTAMP`, `PREV_ROUND`, `PREV_PREV_ROUND`. Outputs are rendered review prompts whose requested output is `round-N-review-result.md`. State transitions are not performed inside the templates, but the templates instruct Codex to end the result with strict markers that the hook consumes: `COMPLETE` enters Finalize Phase (`hooks/loop-codex-stop-hook.sh:961-1021`), `STOP` ends as circuit breaker (`hooks/loop-codex-stop-hook.sh:1023-1050`), anything else advances to the next implementation round (`hooks/loop-codex-stop-hook.sh:1053-1120`).
- gates_or_invariants: `regular-review.md:5-11` requires Codex to read the original plan and compare Claude’s claimed work against both plan and current prompt. `regular-review.md:20-31` forbids accepting deferrals or pending work without detailed corrective plans. `regular-review.md:33-45` requires goal alignment summary. `regular-review.md:51-57` allows `COMPLETE` only when all original plan tasks and acceptance criteria are fully done with no deferrals or pending work. `full-alignment-review.md:19-48` mandates acceptance-criteria audit, forgotten-item detection, deferred-item audit, and completion summary. `full-alignment-review.md:59-85` adds the every-fifth-round stagnation circuit breaker and requires `STOP` as last line if stagnation is detected. `goal-tracker-update-section.md:5-11` requires Codex to evaluate Claude’s requested goal-tracker updates and forbids modifying the immutable section.
- dependencies_and_callers: Rendered through `hooks/lib/template-loader.sh`, whose placeholder contract is uppercase `{{VAR}}`, single-pass replacement, and missing placeholders preserved (`hooks/lib/template-loader.sh:7-14`, `50-57`, `71-129`). `hooks/loop-codex-stop-hook.sh:680-681` renders `goal-tracker-update-section.md`; `hooks/loop-codex-stop-hook.sh:720-731` renders `full-alignment-review.md`; `hooks/loop-codex-stop-hook.sh:736-748` renders `regular-review.md`. The directory depends on sibling `prompt-template/claude` for next-round/finalize prompts and sibling `prompt-template/block` for hook block messages, but this directory owns Codex’s review prompt contract.
- edge_cases_or_failure_modes: Missing template files degrade to inline fallbacks through `load_and_render_safe` (`hooks/loop-codex-stop-hook.sh:696-716`, `720`, `736`). If `SUMMARY_CONTENT` or nested `GOAL_TRACKER_UPDATE_SECTION` contains `{{...}}`, the renderer is intentionally single-pass, so injected placeholder-looking text is not recursively expanded (`hooks/lib/template-loader.sh:54-57`, `116-125`). If Codex does not create or writes an empty review result, the hook blocks and reports a Codex failure (`hooks/loop-codex-stop-hook.sh:897-950`). If `STOP` appears during a non-alignment round, it is still honored but logged as unusual (`hooks/loop-codex-stop-hook.sh:1039-1047`).
- validation_or_tests: `tests/test-templates-comprehensive.sh:74-80` requires `block/`, `codex/`, and `claude/` subdirectories. `tests/test-templates-comprehensive.sh:90-107` loads every Markdown template, including these three. `tests/test-templates-comprehensive.sh:118-194` validates placeholder syntax across all templates; observed read-only run loaded all 36 templates and syntax-validated the three Codex templates with placeholder counts of 19, 1, and 8 respectively before write-restricted runtime rendering failed. `tests/test-templates-comprehensive.sh:531-540` directly renders `codex/goal-tracker-update-section.md`. `bash -n tests/test-templates-comprehensive.sh`, `bash -n hooks/lib/template-loader.sh`, and `bash -n hooks/loop-codex-stop-hook.sh hooks/loop-write-validator.sh hooks/loop-edit-validator.sh` all passed in this export.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-041 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Executable specification for the template-loading and rendering algorithm used by RLCR hook messages and prompt generation. It is not production logic, but it defines expected behavior for template discovery, syntax validation, rendering, fallbacks, real-template integration, and stress behavior.
- algorithmic_behavior: The script sources `hooks/lib/template-loader.sh` at `tests/test-templates-comprehensive.sh:15-17`, resolves the template root with `get_template_dir` at `tests/test-templates-comprehensive.sh:59`, then runs seven sections. It validates directory shape (`64-80`), loads every `*.md` template (`90-107`), detects malformed placeholders (`118-194`, plus targeted malformed tests at `204-259`), checks rendering edge cases (`266-438`), tests fallback behavior (`445-490`), renders real templates (`497-540`), and stress-renders all templates with dummy assignments (`547-606`). It exits 0 only when `TESTS_FAILED == 0` (`619-625`).
- inputs_outputs_state: Inputs are repository-relative templates under `prompt-template/`, template-loader functions, and generated temporary directories for negative tests. Internal state is held in counters `TESTS_PASSED`, `TESTS_FAILED`, and `WARNINGS` (`26-28`) updated by helper functions (`31-50`). Outputs are colored test status lines and final process exit code. It does not modify repository files, but it does create and delete temp directories via `mktemp -d` in malformed and incomplete-directory tests (`205`, `484`, cleanup at `259`, `490`).
- gates_or_invariants: Template root must exist and contain `block`, `codex`, and `claude` (`66-80`). Every Markdown template must load non-empty content or fail/warn (`94-106`). Placeholder syntax must reject extra braces, wrong bracket types, likely unclosed placeholders, spaces inside placeholders, and lowercase placeholders (`128-175`). Valid placeholders follow `{{[A-Z_][A-Z0-9_]*}}` (`122-124`). Fallback behavior must use fallback on missing templates and real templates when available (`446-463`). All placeholders extracted from real templates must render away under dummy values, or leave a warning for remaining placeholders (`575-597`).
- dependencies_and_callers: Depends directly on `hooks/lib/template-loader.sh` functions `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`. It uses `find`, `sort -z`, `grep -oE`, `sed`, `tr`, `wc`, `mktemp`, and Bash process substitution. It is a CI/CD validation script per its header (`1-10`) and is related to narrower tests in `tests/test-template-loader.sh`, especially placeholder injection tests at `tests/test-template-loader.sh:488-540`.
- edge_cases_or_failure_modes: It covers empty content (`266-273`), single and multi-line rendering (`275-297`), empty-line preservation (`299-316`), regex metacharacters (`318-326`), backslashes (`328-336`), quotes (`338-345`), CJK-like input (`347-360`), ASCII status patterns (`362-371`), Markdown formatting (`373-380`), long values (`382-390`), multiline values (`392-402`), variables at line boundaries (`404-420`), repeated variables (`422-429`), adjacent variables (`431-438`), missing templates (`445-452`), invalid/incomplete directories (`474-490`), rapid calls (`548-560`), and all-template rendering (`563-606`). A notable environment failure mode is that `mktemp` and Bash here-strings can fail in a strictly read-only/no-temp sandbox; in this run, later render tests failed with “cannot create temp file for here document: Operation not permitted,” which is an execution-environment limitation rather than evidence that the template files failed syntax/loading.
- validation_or_tests: Direct run of `bash tests/test-templates-comprehensive.sh` in this read-only export partially validated the script: Sections 1-3 loaded all 36 templates with 0 load failures and syntax-validated all templates, then failed during temp-file and here-string-dependent sections because the sandbox denies temp creation. Syntax-only validation passed with `bash -n tests/test-templates-comprehensive.sh`. Related renderer syntax also passed with `bash -n hooks/lib/template-loader.sh`.
- skip_candidate: `no`

### FIX_HUMANIZE_ESCAPE-HZ-071 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Block-message template for enforcing the invariant that Claude writes or edits the summary file for the current RLCR round, not an older/newer `round-N-summary.md`.
- algorithmic_behavior: The template renders a short blocking diagnostic with five placeholders: `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH` (`prompt-template/block/wrong-round-number.md:1-7`). It tells the agent which wrong file was targeted, states the current round, gives the correct path, and explicitly says not to increment the round number manually.
- inputs_outputs_state: Inputs are supplied by write/edit validators after extracting the filename from `.humanize/rlcr/...` paths. `hooks/loop-write-validator.sh:181-197` sets `CLAUDE_ROUND` from `extract_round_number`, compares it to `CURRENT_ROUND`, computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"`, renders the template with `ACTION=write to`, then exits 2. `hooks/loop-edit-validator.sh:124-147` performs the same gate for edit attempts with `ACTION=edit`. Output is a rendered stderr block used as the hook rejection reason; state is not advanced.
- gates_or_invariants: The gate only applies to summary files (`hooks/loop-write-validator.sh:181`, `hooks/loop-edit-validator.sh:124`). It triggers when a round number is present, differs from current round, and the target is not allowlisted (`hooks/loop-write-validator.sh:184`, `hooks/loop-edit-validator.sh:134`). It preserves the loop’s state-machine invariant that only the current round summary is written by the worker; historical and future round files are not self-selected by Claude. The template’s final instruction at line 7 prevents manual round advancement, leaving round progression to `hooks/loop-codex-stop-hook.sh:1057-1060`.
- dependencies_and_callers: Rendered through `load_and_render_safe` in `hooks/loop-write-validator.sh:191-196` and `hooks/loop-edit-validator.sh:141-146`; fallback messages exist in both validators if the template is missing (`hooks/loop-write-validator.sh:186-190`, `hooks/loop-edit-validator.sh:136-140`). It depends on shared path/round helpers from `hooks/lib/loop-common.sh`, including `extract_round_number` and `is_allowlisted_file`. It is directly exercised by comprehensive template integration at `tests/test-templates-comprehensive.sh:497-514` and loader integration at `tests/test-template-loader.sh:147-164`.
- edge_cases_or_failure_modes: If the filename lacks a parseable round number, this specific gate does not fire (`hooks/loop-write-validator.sh:184`, `hooks/loop-edit-validator.sh:134`). If a file is allowlisted under the active loop dir, the wrong-round gate is bypassed. If the template is absent or empty, validators still block with the inline fallback due to `load_and_render_safe`. The template does not quote or escape the rendered path itself; correctness relies on the single-pass renderer preserving literal values and on the hook emitting the message rather than executing it.
- validation_or_tests: `tests/test-templates-comprehensive.sh:498-510` renders the template with `ACTION=edit`, `CLAUDE_ROUND=3`, `FILE_TYPE=summary`, `CURRENT_ROUND=5`, and `CORRECT_PATH=/tmp/round-5-summary.md`, then checks for the title, wrong file, current round, and correct path. `tests/test-template-loader.sh:151-161` performs a similar integration check. In this read-only run, template loading and syntax validation passed, but the render integration part could not execute successfully because the sandbox denied temp files used by Bash here-strings in `render_template`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3/3 item sections present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`