# agent_085 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-085 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: This is a hook-facing block template for the RLCR goal-tracker write gate. It is not executable code, but it supplies the user-visible rejection message when a Bash command attempts to modify `goal-tracker.md` during Round 0. The template title and command warning are at `prompt-template/block/goal-tracker-bash-write.md:1` and `prompt-template/block/goal-tracker-bash-write.md:3`.
- algorithmic_behavior: The template encodes the transition from “Bash attempted to write goal tracker” to “block and redirect to validated write/edit path.” Runtime behavior is wired through `goal_tracker_bash_blocked_message()` in `hooks/lib/loop-common.sh:492`, which renders `block/goal-tracker-bash-write.md` with `CORRECT_PATH` at `hooks/lib/loop-common.sh:500`. The actual gate is in `hooks/loop-bash-validator.sh:349`: if `command_modifies_file "$COMMAND_LOWER" "goal-tracker\.md"` matches and `CURRENT_ROUND` is `0`, the validator sets `GOAL_TRACKER_PATH="$ACTIVE_LOOP_DIR/goal-tracker.md"` and prints this rendered template at `hooks/loop-bash-validator.sh:350` through `hooks/loop-bash-validator.sh:352`, then exits with block code `2` at `hooks/loop-bash-validator.sh:357`.
- inputs_outputs_state: Input is the `{{CORRECT_PATH}}` placeholder, supplied by the caller as the active loop’s canonical goal tracker path. The output is Markdown guidance that says to use the Write or Edit tool and interpolates the correct path at `prompt-template/block/goal-tracker-bash-write.md:5`. State impact is intentionally negative: no goal-tracker mutation should occur through Bash. The validator turns the attempted Bash write into a blocked hook result, preserving the goal tracker for hook-validated mutation paths. This differs by round: Round 0 receives this template, while later rounds use `goal-tracker-modification.md`, which tells the actor to request goal-tracker changes through a summary file instead (`prompt-template/block/goal-tracker-modification.md:3`, `prompt-template/block/goal-tracker-modification.md:9`).
- gates_or_invariants: The invariant is that Bash must not be used to modify `goal-tracker.md`, because shell writes such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks; that rationale is explicit in `prompt-template/block/goal-tracker-bash-write.md:7`. The detector is broader than the examples in the prompt: `command_modifies_file()` checks redirection, `tee`, in-place editors, `mv`, `cp`, `rm`, `dd`, `truncate`, `printf` redirection, and file-descriptor `exec` patterns at `hooks/lib/loop-common.sh:1075` through `hooks/lib/loop-common.sh:1088`. The path-target invariant is basename-oriented for this gate: any command detected as modifying a path ending in or containing the target pattern `goal-tracker.md` is blocked by `hooks/loop-bash-validator.sh:349`.
- dependencies_and_callers: Direct dependency is the template loader/rendering path through `load_and_render_safe` from `goal_tracker_bash_blocked_message()` in `hooks/lib/loop-common.sh:494` through `hooks/lib/loop-common.sh:500`. The direct caller is `hooks/loop-bash-validator.sh`, specifically its “Block Goal Tracker Modifications” section at `hooks/loop-bash-validator.sh:343` through `hooks/loop-bash-validator.sh:358`. Template reference coverage lists this file as a required common template in `tests/test-template-references.sh:152` through `tests/test-template-references.sh:158`. It also mirrors the sibling summary Bash block template, which uses the same `CORRECT_PATH` rendering pattern for summary files at `hooks/lib/loop-common.sh:481` through `hooks/lib/loop-common.sh:489`.
- edge_cases_or_failure_modes: The template only warns; enforcement depends on `loop-bash-validator.sh` and `command_modifies_file()`. The matcher has documented blind spots: `tests/test-bash-validator-patterns.sh:165` through `tests/test-bash-validator-patterns.sh:168` notes that multi-source `cp file1.md file2.md goal-tracker.md` is not detected by the current two-argument `cp` pattern. Variable-expanded destinations such as `echo x > $FILE` are expected not to match at `tests/test-bash-validator-patterns.sh:170` through `tests/test-bash-validator-patterns.sh:172`. The prompt text names `cat`, `echo`, `sed`, and `awk`, but the implementation also blocks destructive operations like `rm`, `truncate`, and `dd`; the message is therefore representative, not exhaustive. If the template file were missing or failed to render, `goal_tracker_bash_blocked_message()` has a fallback string at `hooks/lib/loop-common.sh:496` through `hooks/lib/loop-common.sh:498`, preserving the block behavior but with less formatted guidance.
- validation_or_tests: The template’s existence is validated in the common-template reference set at `tests/test-template-references.sh:152` through `tests/test-template-references.sh:158`. The underlying command-detection behavior for `goal-tracker.md` is covered by `tests/test-bash-validator-patterns.sh`, including redirection cases at lines `75` through `80`, `tee` cases at lines `89` through `93`, in-place editor cases at lines `102` through `107`, file operation cases at lines `116` through `121`, and read-only false-positive guards at lines `142` through `152`. A robustness test confirms that Bash modification after Round 0 is blocked with exit code `2` at `tests/robustness/test-hook-system-robustness.sh:353` through `tests/robustness/test-hook-system-robustness.sh:367`; that test exercises the later-round branch rather than this Round 0-specific template. I attempted `git status --short`, but this read-only branch export is not a Git checkout from the tool’s perspective and the command failed with “not a git repository”; no files were modified.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 evidence sections present; the single assigned item id appears once as the Item Evidence heading`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`