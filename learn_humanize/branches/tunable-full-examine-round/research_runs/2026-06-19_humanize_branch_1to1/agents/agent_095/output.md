# agent_095 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-095 `file` `prompt-template/block/prompt-file-write.md`
- cursor: `[_]`
- core_role: This file is a rendered block-message template for the RLCR/humanize loop guard that rejects attempts to modify round prompt files. Its algorithmic role is policy communication at a write-protection gate: when a mutation command targets `round-*-prompt.md`, the hook emits this template to explain that prompt files are instruction inputs, not writable task outputs. The template itself is not executable, but it is part of the enforcement path because it is loaded by the common hook message function.
- algorithmic_behavior: The template states the blocked target class at `prompt-template/block/prompt-file-write.md:3`, defines the trust boundary at `prompt-template/block/prompt-file-write.md:5`, and gives the permitted workflow at `prompt-template/block/prompt-file-write.md:7` through `prompt-template/block/prompt-file-write.md:12`: read the current round prompt, execute the described tasks, write results to the summary file, and document prompt errors in the summary instead of editing the prompt. In the runtime path, `hooks/loop-bash-validator.sh:365` checks commands against `round-[0-9]+-prompt\.md`; on match, `hooks/loop-bash-validator.sh:366` calls `prompt_write_blocked_message`, and `hooks/loop-bash-validator.sh:367` exits with code `2`. The message loader is `hooks/lib/loop-common.sh:454` through `hooks/lib/loop-common.sh:461`, which renders `block/prompt-file-write.md` and falls back to an inline shorter message if the template cannot be loaded.
- inputs_outputs_state: Inputs are attempted shell commands normalized as `COMMAND_LOWER`, the protected filename pattern `round-[0-9]+-prompt\.md`, and the template path under `prompt-template/block/`. The validator’s observable output is this Markdown block on stderr plus process exit `2`, preventing the command from proceeding. The protected application state is the active loop prompt file, typically `round-N-prompt.md`, which should remain controlled by loop setup/stop-hook code rather than the worker. The normal worker state transition is external to this file: prompt remains read-only, work results move into `round-N-summary.md`. The template explicitly redirects the error-reporting transition to the summary file at `prompt-template/block/prompt-file-write.md:12`.
- gates_or_invariants: The invariant is that prompt files are immutable instructions “FROM Codex TO you” and cannot be modified by the acting worker, stated at `prompt-template/block/prompt-file-write.md:5` through `prompt-template/block/prompt-file-write.md:7`. The Bash gate uses `command_modifies_file` from `hooks/lib/loop-common.sh:1068` through `hooks/lib/loop-common.sh:1096`, which recognizes writes through redirection, append redirection, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`, `cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec fd >`. The RLCR-specific prompt gate is global across rounds, as noted by `hooks/loop-bash-validator.sh:360` through `hooks/loop-bash-validator.sh:364`. A related PR-loop guard separately blocks PR-loop prompt-like files, including `round-[0-9]+-prompt\.md` and `round-[0-9]+-codex-prompt\.md`, at `hooks/loop-bash-validator.sh:417` through `hooks/loop-bash-validator.sh:441`, but that path uses the PR-loop-specific prompt block rather than this exact template.
- dependencies_and_callers: Direct runtime caller: `prompt_write_blocked_message` in `hooks/lib/loop-common.sh:454` through `hooks/lib/loop-common.sh:461`. Direct enforcement caller: `hooks/loop-bash-validator.sh:365` through `hooks/loop-bash-validator.sh:368`. Detection dependency: `command_modifies_file` in `hooks/lib/loop-common.sh:1071` through `hooks/lib/loop-common.sh:1096`. Template loading dependency: `load_and_render_safe` is invoked at `hooks/lib/loop-common.sh:460`, so the template participates in the shared prompt-template rendering mechanism and has an inline fallback. System-generated prompt writers exist outside the worker path: for example, `hooks/loop-codex-stop-hook.sh:1130` through `hooks/loop-codex-stop-hook.sh:1150` writes the next round prompt as part of loop orchestration, which explains why the policy is scoped to worker command validation rather than forbidding all internal prompt generation.
- edge_cases_or_failure_modes: If the template file is missing or unreadable, the guard still emits the fallback from `hooks/lib/loop-common.sh:456` through `hooks/lib/loop-common.sh:458`, so enforcement does not depend solely on this Markdown file. The mutation detector is pattern based; it covers common shell write forms but may not model every possible indirect write hidden behind custom commands, generated scripts, symlinks, or nonstandard tooling. The RLCR prompt gate checks bare `round-[0-9]+-prompt.md` patterns, which catches normal relative and absolute targets but is not a full filesystem policy by itself. The template’s instructions mention “you (Claude)” at `prompt-template/block/prompt-file-write.md:5`, while this branch also contains Codex-facing review prompts; that wording is a role-specific artifact but still conveys the intended trust boundary. The file is therefore core to user-facing enforcement feedback, but not the sole security boundary.
- validation_or_tests: `tests/test-template-references.sh:152` through `tests/test-template-references.sh:155` includes `block/prompt-file-write.md` in the common template existence checks, and `tests/test-template-references.sh:134` through `tests/test-template-references.sh:141` checks whether templates are referenced by hook scripts. The `rg` reference scan confirms this template is directly referenced by `hooks/lib/loop-common.sh:460`. Command-pattern behavior is covered indirectly by tests around `command_modifies_file`, including `tests/test-bash-validator-patterns.sh` as shown by repository references to that helper. I did not run the test suite because the task requested research notes only and the branch export is read-only; an attempted `git status` also failed because this export is not a git checkout and the sandbox could not create `/tmp/xcrun_db-*` cache files.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-095`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`