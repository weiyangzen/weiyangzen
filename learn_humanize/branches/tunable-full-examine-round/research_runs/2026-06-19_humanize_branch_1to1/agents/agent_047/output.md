# agent_047 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-047 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR hook allowlist exception that permits only a small, hard-coded set of round files to bypass otherwise strict `round-*-todos.md` and historical summary protections. The test directly exercises the shared allowlist helper and then verifies that the Read, Write, Edit, and Bash validators all apply the same exception model. See [tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-allowlist-validators.sh:3).
- algorithmic_behavior: The script builds a temporary git-backed project, creates an active loop directory at `.humanize/rlcr/2024-01-01_12-00-00`, writes a synthetic `state.md` with `current_round: 5`, and runs 25 pass/fail assertions. Setup is centralized in `setup_test_loop`, which initializes git, captures the current branch, creates the loop directory, and writes frontmatter fields required by strict state parsing. See [tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-allowlist-validators.sh:29).
- algorithmic_behavior: The first section verifies `is_allowlisted_file(file_path, active_loop_dir)` as a pure path predicate: it allows exactly `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` under the active loop directory, while rejecting `round-3-todos.md`, `round-2-summary.md`, and a valid basename under `/other/path`. The implementation being specified is in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/loop-common.sh:420), where the allowlist is literal and equality-based against `$active_loop_dir/$allowed`.
- algorithmic_behavior: The Write validator section feeds JSON hook events into `hooks/loop-write-validator.sh`. It expects exit `0` for allowlisted todos and historical summary paths, and exit `2` with diagnostic text for non-allowlisted todos or wrong-round summaries. The validator first special-cases todos with `find_active_loop` plus `is_allowlisted_file`, then later applies wrong-round summary checks with the same helper. See [tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-allowlist-validators.sh:129), [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-write-validator.sh:55), and [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-write-validator.sh:228).
- algorithmic_behavior: The Edit validator section mirrors Write for allowlisted `round-2-todos.md` and `round-1-summary.md`, then asserts that `round-4-todos.md` remains blocked. The edit hook uses the same todos early gate and the same wrong-round summary escape check, but unlike Write it does not perform the same JSON/deep-nesting validation preamble. See [tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-allowlist-validators.sh:188) and [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-edit-validator.sh:38).
- algorithmic_behavior: The Read validator section verifies that reading allowlisted todos and historical summaries exits `0`, while reading non-allowlisted todos or wrong-round summaries exits `2`. The read hook normally blocks todos because TodoWrite should be used, but it permits allowlisted paths in the active loop before continuing to summary/prompt round validation. See [tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-allowlist-validators.sh:231) and [hooks/loop-read-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-read-validator.sh:55).
- algorithmic_behavior: The Bash validator section treats shell commands as a bypass surface for file writes. It allows commands that modify `round-1-todos.md` or `round-2-todos.md` only when the command string contains the full active loop directory path. It blocks the same basename in `/tmp`, a bare relative `round-1-todos.md`, an old loop directory, and a same-basename directory under a different root. See [tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-allowlist-validators.sh:287) and [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-bash-validator.sh:381).
- inputs_outputs_state: Inputs are synthetic Claude hook JSON payloads on stdin, environment variable `CLAUDE_PROJECT_DIR="$TEST_DIR"`, a generated `.humanize/rlcr/<timestamp>/state.md`, and command/path strings embedded in `HOOK_INPUT`. Outputs are colored PASS/FAIL lines, aggregate pass/fail counts, and process exit status equal to `TESTS_FAILED`. See [tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-allowlist-validators.sh:132) and [tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-allowlist-validators.sh:385).
- inputs_outputs_state: State transitions inside the test are limited to temporary filesystem setup: `mktemp -d`, git init/first commit, loop directory creation, `state.md` creation, and cleanup via `trap "rm -rf $TEST_DIR" EXIT`. The validators themselves read active-loop state and return gate decisions; the test does not mutate repository source files. See [tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-allowlist-validators.sh:30).
- gates_or_invariants: The central invariant is exact active-loop path equality for non-Bash allowlisting; same filename outside the active loop is denied. The Bash invariant is stricter against bypasses: command text must both look like it modifies a `round-[0-9]+-todos.md` file and contain the escaped lowercase active-loop prefix followed by `round-[12]-todos.md`. See [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/loop-common.sh:427) and [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-bash-validator.sh:385).
- gates_or_invariants: Expected validator outcomes use exit `0` for allowed operations and exit `2` for policy blocks; invalid hook input or malformed state would use exit `1` in the validator logic, though this test feeds valid JSON/state. Failure assertions also check coarse diagnostic content, usually `todos` or `round`, to ensure the block came from the expected policy family. See [tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-allowlist-validators.sh:168).
- dependencies_and_callers: The test sources `hooks/lib/loop-common.sh` directly for the helper-level assertions, and invokes `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh` as subprocesses. Those hooks depend on `jq`, git/Unix shell utilities, `template-loader.sh`, `find_active_loop`, `parse_state_file_strict`, `is_round_file_type`, `extract_round_number`, `todos_blocked_message`, and for Bash mutation detection `command_modifies_file`. See [tests/test-allowlist-validators.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-allowlist-validators.sh:15), [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/loop-common.sh:151), and [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/loop-common.sh:1068).
- dependencies_and_callers: The broader caller is the repository shell test runner, which includes `test-allowlist-validators.sh` in its ordered suite list. See [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/run-all-tests.sh:32).
- edge_cases_or_failure_modes: The test explicitly covers wrong round numbers, wrong directory, historical summary exceptions, bare relative Bash paths, old loop directories, and same timestamp basename under a different root. It does not cover symlinks, quoted active-loop Bash paths with spaces, multiple active loop directories beyond `find_active_loop` choosing the newest timestamp, uppercase filenames beyond lowercasing in validators, or JSON fields with embedded quotes. Bash coverage is regex-based, not AST-based, so behavior is bounded by `command_modifies_file` patterns rather than full shell semantics. See [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/loop-common.sh:1075).
- edge_cases_or_failure_modes: `setup_test_loop` writes `base_branch: main` while `git init` may produce a non-main default current branch for `start_branch`; this is acceptable for the tested path because strict parsing requires the field to exist, not to match an existing branch. The test relies on `jq`, `git`, `sed`, `grep`, and Bash availability; missing dependencies would fail the validator subprocesses or setup rather than indicate allowlist algorithm failure.
- validation_or_tests: This file is itself the validation asset. I inspected it directly with line numbers and followed the invoked validators and shared helper. I also ran `bash -n tests/test-allowlist-validators.sh hooks/loop-read-validator.sh hooks/loop-write-validator.sh hooks/loop-edit-validator.sh hooks/loop-bash-validator.sh hooks/lib/loop-common.sh`, which completed with exit `0` and no output, confirming shell syntax for the inspected files. I did not execute the full test because this branch export is read-only and the test intentionally creates/removes temporary files and initializes a git repository.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 item evidence section present; no repeated item-id strings outside the section header`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`