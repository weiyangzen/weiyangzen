# agent_124 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-124 `file` `prompt-template/pr-loop/round-0-header.md`
- cursor: `[_]`
- core_role: This file is the Round 0 prompt header template for the PR review loop. It establishes the execution mode, identifies the PR context, and introduces the review-comment payload that is appended immediately after the header. The direct caller is `scripts/setup-pr-loop.sh`, which renders this template into `.humanize/pr-loop/<timestamp>/round-0-prompt.md` before appending fetched PR comments and the selected task instructions. See `prompt-template/pr-loop/round-0-header.md:1`, `prompt-template/pr-loop/round-0-header.md:3`, `prompt-template/pr-loop/round-0-header.md:7`, and `scripts/setup-pr-loop.sh:678` through `scripts/setup-pr-loop.sh:684`.

- algorithmic_behavior: The template itself is declarative prompt text rather than executable code, but it is part of the PR-loop state machine’s Round 0 prompt assembly. It tells the worker to “Read and execute below with ultrathink” at `prompt-template/pr-loop/round-0-header.md:1`, labels the session as `PR Review Loop (Round 0)` at line 3, states that the agent is monitoring feedback from remote review bots at line 5, then emits three rendered metadata fields: `PR_NUMBER`, `START_BRANCH`, and `ACTIVE_BOTS_DISPLAY` at lines 8 through 10. It ends with a `Review Comments` section and lead-in sentence at lines 12 through 15, creating the insertion point for the fetched comment markdown. The state transition after this header happens in `scripts/setup-pr-loop.sh`: render header, write it to `round-0-prompt.md`, append `round-0-pr-comment.md`, then append either `round-0-task-no-comments.md` or `round-0-task-has-comments.md` based on comment presence. See `scripts/setup-pr-loop.sh:681`, `scripts/setup-pr-loop.sh:684`, `scripts/setup-pr-loop.sh:687`, `scripts/setup-pr-loop.sh:720`, `scripts/setup-pr-loop.sh:767`, and `scripts/setup-pr-loop.sh:771`.

- inputs_outputs_state: Inputs are shell-rendered template variables supplied by `scripts/setup-pr-loop.sh`: `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, plus extra variables used by sibling task templates: `RESOLVE_PATH` and `BOT_MENTION_STRING`. The relevant variable array is built at `scripts/setup-pr-loop.sh:651` through `scripts/setup-pr-loop.sh:658`. `PR_NUMBER` is detected through GitHub CLI PR lookup and validated numeric at `scripts/setup-pr-loop.sh:306` through `scripts/setup-pr-loop.sh:338`. `START_BRANCH` comes from `git rev-parse --abbrev-ref HEAD` at `scripts/setup-pr-loop.sh:283`. `ACTIVE_BOTS_DISPLAY` is derived from `ACTIVE_BOTS_ARRAY` at `scripts/setup-pr-loop.sh:563` through `scripts/setup-pr-loop.sh:564`. Output is rendered markdown written as the first segment of `$LOOP_DIR/round-0-prompt.md` at `scripts/setup-pr-loop.sh:681`; fetched comments from `$COMMENT_FILE` are then appended at `scripts/setup-pr-loop.sh:684`. The persistent loop state also includes generated files under `.humanize/pr-loop/<timestamp>/`, including `round-0-pr-comment.md`, `round-0-pr-resolve.md`, `goal-tracker.md`, and `round-0-prompt.md`.

- gates_or_invariants: The header depends on setup gates that run before rendering. The setup script requires `gh` authentication at `scripts/setup-pr-loop.sh:263` through `scripts/setup-pr-loop.sh:269`, requires `codex` availability at `scripts/setup-pr-loop.sh:271` through `scripts/setup-pr-loop.sh:277`, requires a current branch at `scripts/setup-pr-loop.sh:283` through `scripts/setup-pr-loop.sh:287`, requires an associated PR at `scripts/setup-pr-loop.sh:326` through `scripts/setup-pr-loop.sh:332`, validates the PR number as numeric at `scripts/setup-pr-loop.sh:334` through `scripts/setup-pr-loop.sh:338`, rejects merged or closed PRs at `scripts/setup-pr-loop.sh:340` through `scripts/setup-pr-loop.sh:349`, and rejects YAML-unsafe branch names at `scripts/setup-pr-loop.sh:365` through `scripts/setup-pr-loop.sh:372`. The template loader performs single-pass substitution, keeps missing placeholders unchanged, and avoids recursive expansion of placeholder-looking values; see `hooks/lib/template-loader.sh:7` through `hooks/lib/template-loader.sh:13` and `hooks/lib/template-loader.sh:54` through `hooks/lib/template-loader.sh:57`. The header is not the gate that decides comment handling; that invariant is enforced by sentinel detection on `*No comments found.*` at `scripts/setup-pr-loop.sh:643` through `scripts/setup-pr-loop.sh:649`.

- dependencies_and_callers: The immediate dependency chain is `scripts/setup-pr-loop.sh` sourcing `hooks/lib/template-loader.sh` at `scripts/setup-pr-loop.sh:37` through `scripts/setup-pr-loop.sh:42`, then invoking `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/round-0-header.md"` at `scripts/setup-pr-loop.sh:678`. The template loader resolves template paths under `prompt-template` via `get_template_dir` at `hooks/lib/template-loader.sh:24` through `hooks/lib/template-loader.sh:31`. Rendering is performed by `render_template`, which scans `{{VAR}}` placeholders and substitutes values from `TMPL_VAR_*` environment entries at `hooks/lib/template-loader.sh:58` through `hooks/lib/template-loader.sh:131`. `load_and_render_safe` provides fallback behavior for missing or empty templates at `hooks/lib/template-loader.sh:170` through `hooks/lib/template-loader.sh:203`. Sibling prompt fragments coordinate with this header: `round-0-task-no-comments.md` handles fresh PRs with no comments, while `round-0-task-has-comments.md` handles PRs with existing review comments. The comment source is generated by `scripts/fetch-pr-comments.sh`, called at `scripts/setup-pr-loop.sh:411` through `scripts/setup-pr-loop.sh:412`.

- edge_cases_or_failure_modes: If `prompt-template/pr-loop/round-0-header.md` is missing or empty, `load_and_render_safe` falls back to the inline `FALLBACK_HEADER` defined at `scripts/setup-pr-loop.sh:660` through `scripts/setup-pr-loop.sh:675`, so prompt creation can continue with equivalent structure. If one of the header placeholders is not supplied, `render_template` keeps it literal as `{{UNDEFINED}}` rather than failing, per `hooks/lib/template-loader.sh:13` and `hooks/lib/template-loader.sh:119` through `hooks/lib/template-loader.sh:121`; this prevents crashes but can leak unresolved placeholders into the generated prompt. If variable values contain `{{...}}`, they are not recursively expanded, which prevents prompt/template injection but means nested template syntax in branch names or bot display names remains literal. If `fetch-pr-comments.sh` produces anything other than the exact no-comments sentinel, the setup treats comments as present because `COMMENT_COUNT` is binary: `0` only for an exact `*No comments found.*`, otherwise `1`; see `scripts/setup-pr-loop.sh:643` through `scripts/setup-pr-loop.sh:649`. A malformed or unsafe branch name is rejected before the template is rendered, but bot display text is not validated by this header itself.

- validation_or_tests: I found integration and robustness references for `setup-pr-loop.sh`, but no narrow test that asserts the exact text of `prompt-template/pr-loop/round-0-header.md`. The repository search shows setup-script tests in `tests/test-pr-loop-system.sh`, including setup failure and goal-tracker integration cases, and script-level tests in `tests/test-pr-loop-scripts.sh`. The search also shows robustness coverage for setup-script argument and environment handling in `tests/robustness/test-setup-scripts-robustness.sh`. Template behavior is indirectly protected by `hooks/lib/template-loader.sh`, especially its documented placeholder and fallback semantics at `hooks/lib/template-loader.sh:7` through `hooks/lib/template-loader.sh:20` and implementation at `hooks/lib/template-loader.sh:170` through `hooks/lib/template-loader.sh:203`. The generated prompt assembly path is visible at `scripts/setup-pr-loop.sh:681`, `scripts/setup-pr-loop.sh:684`, and `scripts/setup-pr-loop.sh:771`.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-124`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`