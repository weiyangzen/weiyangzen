# agent_078 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-078 `file` `prompt-template/block/force-push-detected.md`
- cursor: `[_]`
- core_role: This is the operator-facing block template for the PR loop’s force-push gate. It is rendered when the stop hook detects that the previously tracked PR commit SHA is no longer reachable from the current branch HEAD, meaning PR history was rewritten. The template tells the operator that review state has been reset and that a new bot trigger comment is required before review can continue. See `prompt-template/block/force-push-detected.md:1`.
- algorithmic_behavior: The template itself is declarative Markdown, but it participates in a hard algorithmic transition in `hooks/pr-loop-stop-hook.sh`. The hook parses `latest_commit_sha` and `latest_commit_at` from loop state at `hooks/pr-loop-stop-hook.sh:104`. During force-push detection, it compares stored `PR_LATEST_COMMIT_SHA` with `git rev-parse HEAD`, then runs `git merge-base --is-ancestor "$PR_LATEST_COMMIT_SHA" "$CURRENT_HEAD"` to distinguish a normal descendant commit from a non-fast-forward rewrite at `hooks/pr-loop-stop-hook.sh:370` and `hooks/pr-loop-stop-hook.sh:377`. If the stored SHA is not an ancestor, the hook updates tracking to the new commit, clears stale trigger fields, renders this template, and returns a JSON `decision: block` at `hooks/pr-loop-stop-hook.sh:396` and `hooks/pr-loop-stop-hook.sh:419`.
- inputs_outputs_state: Inputs to the rendered template are `OLD_COMMIT`, `NEW_COMMIT`, and `BOT_MENTION_STRING`, passed through `load_and_render_safe` at `hooks/pr-loop-stop-hook.sh:415`. The template consumes those placeholders in `prompt-template/block/force-push-detected.md:3`, `prompt-template/block/force-push-detected.md:10`, and `prompt-template/block/force-push-detected.md:14`. The output is Markdown embedded in the stop-hook JSON `reason`; the system message is `PR Loop: Force push detected - please re-trigger bots`. The state transition updates `latest_commit_sha` to current HEAD, updates `latest_commit_at` to the latest PR commit timestamp or current UTC fallback, and clears both `last_trigger_at` and `trigger_comment_id` so old trigger comments cannot be reused; see `hooks/pr-loop-stop-hook.sh:398` through `hooks/pr-loop-stop-hook.sh:410`.
- gates_or_invariants: The core invariant is that review may not continue after a history rewrite until a trigger comment newer than the rewritten commit exists. The template states that the commit changed in a non-fast-forward manner and that history rewrite resets review state at `prompt-template/block/force-push-detected.md:3` and `prompt-template/block/force-push-detected.md:5`. The hook enforces the invariant by blocking immediately after state reset at `hooks/pr-loop-stop-hook.sh:419`. Later trigger detection also filters comments by `latest_commit_at`, explicitly preventing pre-force-push comments from satisfying the gate at `hooks/pr-loop-stop-hook.sh:527` and `hooks/pr-loop-stop-hook.sh:564`. The template loader uses single-pass placeholder substitution, so injected `{{...}}` strings inside values are not recursively expanded; see `hooks/lib/template-loader.sh:50` and `hooks/lib/template-loader.sh:54`.
- dependencies_and_callers: Direct caller is `hooks/pr-loop-stop-hook.sh`, which sets `TEMPLATE_DIR="$PLUGIN_ROOT/prompt-template"` at `hooks/pr-loop-stop-hook.sh:56` and renders this file with `load_and_render_safe`. Rendering depends on `hooks/lib/template-loader.sh`: `load_template` reads template files at `hooks/lib/template-loader.sh:36`, `render_template` replaces placeholders at `hooks/lib/template-loader.sh:58`, and `load_and_render_safe` provides fallback behavior for missing or empty templates at `hooks/lib/template-loader.sh:170`. The force-push gate depends on `git rev-parse HEAD`, `git merge-base --is-ancestor`, `gh pr view --json commits`, `sed` state rewrites, and `jq` JSON output. Related trigger enforcement depends on `gh api repos/$PR_BASE_REPO/issues/$pr_num/comments --paginate` and comment filtering in `detect_trigger_comment` at `hooks/pr-loop-stop-hook.sh:530`.
- edge_cases_or_failure_modes: If `gh pr view` cannot return the new commit timestamp, the hook falls back to `date -u +"%Y-%m-%dT%H:%M:%SZ"` at `hooks/pr-loop-stop-hook.sh:391`, preserving the “new trigger after rewrite” rule but using local current UTC as the cutoff. If the template is missing or renders empty, `load_and_render_safe` returns the inline fallback message instead of failing the gate at `hooks/lib/template-loader.sh:179` and `hooks/lib/template-loader.sh:192`; the gate still blocks. If `CURRENT_HEAD` is empty, or equals the stored SHA, the force-push branch is skipped at `hooks/pr-loop-stop-hook.sh:370` through `hooks/pr-loop-stop-hook.sh:375`. If the old commit remains an ancestor of current HEAD, this is treated as a normal forward update rather than a force push. A subtle boundary exists in trigger filtering: comments are accepted with `.created_at >= $after` at `hooks/pr-loop-stop-hook.sh:573`, so exact timestamp equality can pass; this is likely intentional tolerance for API timestamp granularity but is worth noting.
- validation_or_tests: Focused stop-hook tests cover the behavior. `tests/test-pr-loop-stophook.sh:20` defines the old-trigger rejection scenario where `latest_commit_at` is after a trigger comment, and the expected output must require a new trigger at `tests/test-pr-loop-stophook.sh:129`. `tests/test-pr-loop-stophook.sh:538` defines a force-push history-rewrite simulation where mocked `git merge-base --is-ancestor` exits nonzero at `tests/test-pr-loop-stophook.sh:616`, and the expected output must mention force push, history rewrite, or re-trigger at `tests/test-pr-loop-stophook.sh:632`. The broader system runner includes “Force push ancestry check” in the force-push test group at `tests/test-pr-loop-system.sh:1820`. Template infrastructure is also validated: `tests/test-template-references.sh:83` checks referenced templates exist, `tests/test-template-references.sh:172` checks critical rendering uses safe fallbacks, and `tests/test-templates-comprehensive.sh:563` loads and renders all templates with dummy placeholder values.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`