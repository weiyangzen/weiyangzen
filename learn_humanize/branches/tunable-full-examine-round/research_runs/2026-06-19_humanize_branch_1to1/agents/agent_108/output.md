# agent_108 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-108 `file` `prompt-template/claude/finalize-phase-skipped-prompt.md`
- cursor: `[_]`
- core_role: This file is a Claude-facing finalize prompt template for the RLCR loop when automated code review is considered skipped. It is intended to move the loop into a degraded-confidence Finalize Phase: the implementation is not fully validated, so Claude is told to manually review changes, run available tests, check quality, optionally use the `code-simplifier:code-simplifier` subagent, commit, and write a finalize summary. The key role is not direct algorithm execution, but prompt-level control over a terminal loop transition and finalization contract. See `prompt-template/claude/finalize-phase-skipped-prompt.md:1`, `prompt-template/claude/finalize-phase-skipped-prompt.md:3`, and `prompt-template/claude/finalize-phase-skipped-prompt.md:40`.
- algorithmic_behavior: The template defines the skipped-review variant of the finalize instructions. It begins with a warning populated by `{{REVIEW_SKIP_REASON}}`, explicitly states that implementation “could not be fully validated,” then requires manual verification before finalization through three checks: code review, tests, and common quality issues at `prompt-template/claude/finalize-phase-skipped-prompt.md:3-13`. It permits, but does not require, a simplification/refactor pass through a Task-tool subagent at `prompt-template/claude/finalize-phase-skipped-prompt.md:15-24`. It constrains that work to functionality-equivalent cleanup at `prompt-template/claude/finalize-phase-skipped-prompt.md:26-34`. It then references the plan and goal tracker at `prompt-template/claude/finalize-phase-skipped-prompt.md:35-38`, and defines the required exit artifacts at `prompt-template/claude/finalize-phase-skipped-prompt.md:40-50`.
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-codex-stop-hook.sh` through `load_and_render_safe`: `FINALIZE_SUMMARY_FILE`, `PLAN_FILE`, `GOAL_TRACKER_FILE`, `REVIEW_SKIP_REASON`, `BASE_BRANCH`, and `START_BRANCH` at `hooks/loop-codex-stop-hook.sh:1069-1075`. The template consumes those variables at `prompt-template/claude/finalize-phase-skipped-prompt.md:3`, `prompt-template/claude/finalize-phase-skipped-prompt.md:19`, `prompt-template/claude/finalize-phase-skipped-prompt.md:37-38`, and `prompt-template/claude/finalize-phase-skipped-prompt.md:44`. Its output is rendered markdown inserted into a stop-hook JSON block response as the `reason` field, with `decision: "block"` and a separate `systemMessage`, at `hooks/loop-codex-stop-hook.sh:1105-1112`. The surrounding state transition is handled by `enter_finalize_phase()`, which renames the loop state file to `finalize-state.md` at `hooks/loop-codex-stop-hook.sh:1032-1039`, then renders either this skipped template or the normal finalize template depending on whether `skip_reason` is non-empty at `hooks/loop-codex-stop-hook.sh:1042-1076`. The file itself does not mutate state; it is the human/agent instruction payload for the finalized state.
- gates_or_invariants: The prompt-level invariants are strict: do not change existing functionality, do not fail existing tests, do not introduce bugs, and perform only functionality-equivalent changes, as listed at `prompt-template/claude/finalize-phase-skipped-prompt.md:26-34`. The exit contract requires completing todos with TodoWrite, committing changes, and writing the finalize summary to `{{FINALIZE_SUMMARY_FILE}}` at `prompt-template/claude/finalize-phase-skipped-prompt.md:40-44`. The broader hook has stronger review gates: normal review failure is blocking and must not skip to finalize, documented at `hooks/loop-codex-stop-hook.sh:1005-1008` and implemented through `block_review_failure()` at `hooks/loop-codex-stop-hook.sh:1163-1235`. The hook also enforces review-phase consistency by requiring `.review-phase-started` when `review_started=true`, blocking inconsistent state at `hooks/loop-codex-stop-hook.sh:1446-1461`. A notable invariant tension exists: current hook schema blocks missing `base_branch` before reaching later skipped-review logic at `hooks/loop-codex-stop-hook.sh:196-208`, while a later branch still sets `REVIEW_SKIPPED=true` and `REVIEW_SKIP_REASON="No base_branch configured for code review"` at `hooks/loop-codex-stop-hook.sh:1413-1418`.
- dependencies_and_callers: The direct caller is `enter_finalize_phase()` in `hooks/loop-codex-stop-hook.sh`, which uses `load_and_render_safe "$TEMPLATE_DIR" "claude/finalize-phase-skipped-prompt.md"` at `hooks/loop-codex-stop-hook.sh:1069`. The normal sibling template is `prompt-template/claude/finalize-phase-prompt.md`, selected when `skip_reason` is empty at `hooks/loop-codex-stop-hook.sh:1076-1102`; it is structurally similar but assumes review passed, not skipped. Rendering depends on `hooks/lib/template-loader.sh`, whose template syntax is `{{VARIABLE_NAME}}`, missing variables are preserved as placeholders, and substitution is single-pass to prevent placeholder injection at `hooks/lib/template-loader.sh:7-13` and `hooks/lib/template-loader.sh:50-58`. The safe loader falls back if the template is missing or renders empty at `hooks/lib/template-loader.sh:167-203`; the hook also embeds a local fallback copy of the skipped prompt at `hooks/loop-codex-stop-hook.sh:1042-1067`. The broader loop depends on state parsed from `state.md` into `START_BRANCH`, `BASE_BRANCH`, `PLAN_FILE`, `CURRENT_ROUND`, and `REVIEW_STARTED` at `hooks/loop-codex-stop-hook.sh:103-116`.
- edge_cases_or_failure_modes: If the template file is missing or empty, `load_and_render_safe` renders the hardcoded fallback instead, so the stop hook still returns a non-empty finalize instruction at `hooks/lib/template-loader.sh:176-203` and `hooks/loop-codex-stop-hook.sh:1042-1069`. If any required variable is missing, the loader preserves the unresolved `{{VAR}}` placeholder rather than failing, per `hooks/lib/template-loader.sh:12-13` and `hooks/lib/template-loader.sh:115-122`; that could leak placeholder text into the prompt. If a variable value contains another placeholder, it is not recursively expanded, preventing prompt injection or accidental corruption, per `hooks/lib/template-loader.sh:54-57` and tested at `tests/test-template-loader.sh:489-522`. The practical reachability of this skipped template is questionable in the current hook: `enter_finalize_phase` can use it only when called with a non-empty `skip_reason`, but the only observed `enter_finalize_phase` call passes an empty string after review success at `hooks/loop-codex-stop-hook.sh:1024-1027`; the only observed skip-reason assignment at `hooks/loop-codex-stop-hook.sh:1413-1418` is not followed by an `enter_finalize_phase "$REVIEW_SKIP_REASON" ...` call before normal next-round handling continues at `hooks/loop-codex-stop-hook.sh:1501-1571`. Additionally, missing `base_branch` is already blocked earlier as an outdated schema condition at `hooks/loop-codex-stop-hook.sh:196-208`, making that skip path even less reachable in normal current-state execution.
- validation_or_tests: Static template coverage exists. `tests/test-template-references.sh` scans shell scripts for `load_template`, `load_and_render`, and `load_and_render_safe` references, verifies referenced template files exist, and separately checks safe rendering use in critical validators at `tests/test-template-references.sh:56-110` and `tests/test-template-references.sh:170-200`. `tests/test-templates-comprehensive.sh` loads every markdown template, rejects empty/whitespace-only templates, validates placeholder syntax, and attempts rendering with generated placeholder values at `tests/test-templates-comprehensive.sh:83-107`, `tests/test-templates-comprehensive.sh:114-125`, and `tests/test-templates-comprehensive.sh:580-606`. Loader behavior for fallback rendering is covered at `tests/test-template-loader.sh:197-222` and `tests/test-templates-comprehensive.sh:446-463`. I did not run the tests because this assignment is a read-only branch export and the instruction was to produce research notes only without modifying files.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`