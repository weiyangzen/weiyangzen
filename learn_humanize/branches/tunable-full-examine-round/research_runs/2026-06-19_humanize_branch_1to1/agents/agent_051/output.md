# agent_051 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-051 `file` `tests/test-codex-review-merge.sh`
- cursor: `[_]`
- core_role: This file is an executable specification for the Codex review-log issue extraction algorithm implemented by `detect_review_issues()` in `hooks/lib/loop-common.sh`. It validates the review phase gate that decides whether an RLCR loop should continue with review fixes, proceed to finalize, or block because the review artifact is invalid. The file itself is not production logic, but it is core algorithm coverage because it encodes the accepted parser contract for Codex review output.
- algorithmic_behavior: The tested algorithm is documented at `tests/test-codex-review-merge.sh:5` through `tests/test-codex-review-merge.sh:15`: scan the full log from line 1, find the first line where `[P<digit>]` appears within the first 10 characters, extract from that line to EOF, and return no-issue status if no qualifying marker exists. The production implementation matches this with an `awk` first-match scan over `substr($0, 1, 10)` in `hooks/lib/loop-common.sh:364` through `hooks/lib/loop-common.sh:372`, followed by `sed -n "${found_line},$p"` extraction in `hooks/lib/loop-common.sh:377` through `hooks/lib/loop-common.sh:382`. The tests cover marker detection, marker position filtering, whole-file scanning beyond 50 lines, extraction from first marker through final line, common `- [P1]` review format, position-zero markers, and creation of the review result file.
- inputs_outputs_state: Inputs are synthetic `round-N-codex-review.log` files written under `CACHE_DIR`, with `CACHE_DIR` set to `$XDG_CACHE_HOME/humanize/codex-review` by `setup_test_env()` at `tests/test-codex-review-merge.sh:48` through `tests/test-codex-review-merge.sh:53`. The tested function is called as `detect_review_issues <round>` and resolves input and output paths from globals: `CACHE_DIR/round-${round}-codex-review.log` and `LOOP_DIR/round-${round}-review-result.md`, as seen in `hooks/lib/loop-common.sh:349` through `hooks/lib/loop-common.sh:352`. Output on success is Markdown-like stdout prefixed with `## Codex Review Issues`, plus the extracted raw content saved to `round-N-review-result.md` (`hooks/lib/loop-common.sh:381` through `hooks/lib/loop-common.sh:386`). State transitions in the wider loop are driven by the exit code: `0` means continue review loop with the extracted issues, `1` means enter finalize phase, and `2` means block as a hard review failure (`hooks/loop-codex-stop-hook.sh:1011` through `hooks/loop-codex-stop-hook.sh:1027`).
- gates_or_invariants: The key invariant is positional: `[P0-9]` only counts if it appears within the first 10 characters of a line. Test 2 asserts that later occurrences such as “This line has [P1]” do not count and must return `1` (`tests/test-codex-review-merge.sh:83` through `tests/test-codex-review-merge.sh:107`). The scanner must inspect the entire file rather than only a tail window; Test 6 puts `[P1]` at line 55 and Test 7 puts `[P1]` at line 5 in a 70-line file to protect both late and early matches (`tests/test-codex-review-merge.sh:171` through `tests/test-codex-review-merge.sh:227`). Missing or empty logs are hard errors returning `2`, not “no issues” (`tests/test-codex-review-merge.sh:133` through `tests/test-codex-review-merge.sh:169`; implementation at `hooks/lib/loop-common.sh:354` through `hooks/lib/loop-common.sh:358`). The first qualifying marker fixes the extraction start, and all following lines are preserved, including later issue markers and non-issue trailing text (`tests/test-codex-review-merge.sh:229` through `tests/test-codex-review-merge.sh:255`).
- dependencies_and_callers: The test directly sources `hooks/lib/loop-common.sh` at `tests/test-codex-review-merge.sh:41` through `tests/test-codex-review-merge.sh:42`, so it exercises the real function rather than a copy. That library also sources `hooks/lib/template-loader.sh` during load (`hooks/lib/loop-common.sh:139` through `hooks/lib/loop-common.sh:144`), though this particular test uses only the review parser. The production caller is `run_and_handle_code_review()` in `hooks/loop-codex-stop-hook.sh`, which first runs `codex review`, stores combined stdout/stderr in `CACHE_DIR/round-${round}-codex-review.log`, then calls `detect_review_issues "$round"` (`hooks/loop-codex-stop-hook.sh:916` through `hooks/loop-codex-stop-hook.sh:983`, and `hooks/loop-codex-stop-hook.sh:999` through `hooks/loop-codex-stop-hook.sh:1027`). When issues are found, `continue_review_loop_with_issues()` increments `current_round`, writes the next round prompt, and blocks exit so fixes can be made (`hooks/loop-codex-stop-hook.sh:1116` through `hooks/loop-codex-stop-hook.sh:1160`). The repository-wide test runner includes this script as `test-codex-review-merge.sh` in `tests/run-all-tests.sh:46`.
- edge_cases_or_failure_modes: The test explicitly treats absent review logs and zero-byte logs as hard failures because otherwise the loop could falsely finalize after a broken review command. It protects against a historical or plausible tail-only parser by checking logs with more than 50 lines and markers both before and after the tail boundary. It prevents false positives from incidental `[P1]` references embedded later in prose by requiring the marker to be near the start of a line. It also validates that severity digit width is exactly one digit by targeting `[P0]`, `[P1]`, `[P2]`, and `[P3]` style markers, matching the implementation regex `\[P[0-9]\]`. One notable edge not covered here is a marker beginning after character 10 due to deeper indentation; by design that is ignored. Another edge not asserted is the exact stdout prefix, though the result-file side effect and marker-containing output are checked.
- validation_or_tests: The script defines 12 focused tests. Tests 1, 6, 7, 8, 9, 10, and 12 assert positive detection and extraction for qualifying markers (`tests/test-codex-review-merge.sh:56` through `tests/test-codex-review-merge.sh:81`, `tests/test-codex-review-merge.sh:171` through `tests/test-codex-review-merge.sh:303`, and `tests/test-codex-review-merge.sh:332` through `tests/test-codex-review-merge.sh:354`). Tests 2 and 3 assert no-issue return code `1` for non-qualifying or absent markers (`tests/test-codex-review-merge.sh:83` through `tests/test-codex-review-merge.sh:131`). Tests 4 and 5 assert hard error return code `2` for missing and empty logs (`tests/test-codex-review-merge.sh:133` through `tests/test-codex-review-merge.sh:169`). Test 11 asserts creation of `round-11-review-result.md` when issues are found (`tests/test-codex-review-merge.sh:305` through `tests/test-codex-review-merge.sh:330`). The script exits nonzero if any assertion fails (`tests/test-codex-review-merge.sh:356` through `tests/test-codex-review-merge.sh:368`). I performed read-only inspection and `bash -n` syntax validation for the assigned script and `hooks/lib/loop-common.sh`; I did not run the full script because it creates temp/cache files, while this branch export is under a no-modification research instruction.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence section present; assigned checklist row represented once above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`