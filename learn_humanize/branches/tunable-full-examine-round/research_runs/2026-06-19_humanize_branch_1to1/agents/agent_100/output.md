# agent_100 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-100 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: This is a stop-hook blocking message template for the RLCR/PR loop git-push gate. It is not executable by itself, but it defines the user-facing contract when a loop attempts to stop while commits remain local and the workflow requires pushing. The template’s core transition is “exit attempt blocked until remote push is completed,” expressed through the title, variable-filled commit count/branch, required `git push origin {{CURRENT_BRANCH}}`, and retry instruction in [prompt-template/block/unpushed-commits.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/prompt-template/block/unpushed-commits.md:1).
- algorithmic_behavior: The template has two placeholders: `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}`. At render time, stop hooks replace them with the detected number of local commits ahead of the remote and the active branch. The rendered output explains that stopping is disallowed because `--push-every-round` is enabled, then gives the exact command to satisfy the gate: `git push origin <branch>`. The main executable behavior lives in callers: `hooks/loop-codex-stop-hook.sh` detects `PUSH_EVERY_ROUND=true`, parses `git status -sb` for `ahead N`, renders this template, emits JSON with `"decision": "block"`, and exits the hook at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-codex-stop-hook.sh:576). The PR loop stop hook performs a broader ahead-count detection and always enforces push for PR-loop exit at [hooks/pr-loop-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/pr-loop-stop-hook.sh:313).
- inputs_outputs_state: Inputs are the template file content, `AHEAD_COUNT`, and `CURRENT_BRANCH`. For Codex RLCR, `AHEAD_COUNT` comes from `git status -sb | grep -o 'ahead [0-9]*'`, and `CURRENT_BRANCH` comes from `git rev-parse --abbrev-ref HEAD` with fallback `unknown` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-codex-stop-hook.sh:578). For PR loop, branch comes from `git branch --show-current`, local and remote SHAs may be compared, and GitHub PR head is used as a fallback if no upstream or origin branch exists at [hooks/pr-loop-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/pr-loop-stop-hook.sh:314). Output is a rendered Markdown reason string embedded in hook JSON as `reason`; the hook also emits a concise `systemMessage`. State transition: while ahead count is positive, stop remains blocked; after the user pushes and reruns the stop attempt, the ahead count should become zero and this block path is skipped.
- gates_or_invariants: The gate invariant is that a push-required loop cannot exit while local commits are ahead of the remote. In the Codex stop hook, the gate only activates when `PUSH_EVERY_ROUND` is `true`; otherwise local commits may remain unpushed at stop time. In the PR stop hook, the comment says the PR loop always requires push, so any positive ahead count blocks. The template itself preserves the invariant by making the required action explicit and branch-specific. The loader’s invariant is single-pass placeholder substitution: values containing `{{OTHER}}` are not recursively expanded, reducing prompt corruption risk; this is documented and implemented in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/template-loader.sh:50).
- dependencies_and_callers: Direct callers found by repository search are `hooks/loop-codex-stop-hook.sh` and `hooks/pr-loop-stop-hook.sh`, both through `load_and_render_safe "$TEMPLATE_DIR" "block/unpushed-commits.md" ...`. The template depends on the shared loader in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/template-loader.sh:167), which loads the file, renders variables, and falls back to an inline message if the template is missing or empty. The feature is configured through `--push-every-round`, surfaced in command/help paths such as `commands/start-rlcr-loop.md`, `scripts/setup-rlcr-loop.sh`, and `prompt-template/claude/push-every-round-note.md`; those files establish when users are told pushing is required each round.
- edge_cases_or_failure_modes: If the template file is missing or renders empty, both callers use inline fallback messages, so the stop gate still blocks; the failure is degraded wording, not bypass. If upstream tracking is absent, the PR stop hook compares against `origin/<branch>` or GitHub PR head and fail-closes with `AHEAD_COUNT=1` if it cannot fetch PR head, preventing accidental exit with unknown push state. The Codex stop hook only uses the `git status -sb` ahead marker, so if no upstream is configured and `git status` does not report ahead state, this specific template path may not trigger there. If branch detection fails in Codex, the rendered command may use `unknown`; the block still communicates the push requirement but the command is less actionable. If a placeholder variable were omitted, `render_template` would leave the raw placeholder intact rather than failing, per [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/template-loader.sh:119).
- validation_or_tests: Existing validation coverage includes a real-template rendering check in [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-templates-comprehensive.sh:517), rendering `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`, then asserting the output contains “Unpushed Commits,” “3 unpushed,” and the branch name. The shared template-loader tests cover placeholder substitution, missing-template fallback, special characters, single-pass behavior, and unreplaced placeholders. I did not run tests because this assignment requested read-only research notes and no file modifications.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-100`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`