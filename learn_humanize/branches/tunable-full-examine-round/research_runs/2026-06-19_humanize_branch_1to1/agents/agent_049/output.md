# agent_049 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-049 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Executable specification for the Bash hook’s protected-file write detector. The file directly tests `command_modifies_file` from `hooks/lib/loop-common.sh`, which is used by `hooks/loop-bash-validator.sh` to block shell-command bypasses around managed RLCR/PR loop files.
- algorithmic_behavior: The script sources shared hook logic via `PROJECT_ROOT/hooks/lib/loop-common.sh` at `tests/test-bash-validator-patterns.sh:11-13`, lowercases each synthetic Bash command with `to_lower`, then asserts whether `command_modifies_file "$command_lower" "$pattern"` returns true or false at `tests/test-bash-validator-patterns.sh:36-61`. It is table-like executable coverage for regex categories: redirection at `tests/test-bash-validator-patterns.sh:69-80`, `tee` at `tests/test-bash-validator-patterns.sh:82-94`, in-place `sed`/`awk`/`perl` at `tests/test-bash-validator-patterns.sh:95-108`, `mv`/`cp`/`rm` at `tests/test-bash-validator-patterns.sh:109-122`, `dd`/`truncate`/`exec`/`printf` at `tests/test-bash-validator-patterns.sh:123-134`, read-only false positives at `tests/test-bash-validator-patterns.sh:135-153`, edge cases at `tests/test-bash-validator-patterns.sh:154-172`, `state.md` patterns at `tests/test-bash-validator-patterns.sh:174-183`, and `round-N-summary.md` patterns at `tests/test-bash-validator-patterns.sh:185-194`.
- inputs_outputs_state: Inputs are hardcoded command strings plus an optional target regex pattern, defaulting to `goal-tracker\.md` in `assert_modifies` and `assert_not_modifies` at `tests/test-bash-validator-patterns.sh:37-55`. There are no CLI arguments and no intentional filesystem writes. Outputs are colored PASS/FAIL lines and a final summary printed at `tests/test-bash-validator-patterns.sh:197-204`. Internal state is limited to `TESTS_PASSED` and `TESTS_FAILED`, initialized at `tests/test-bash-validator-patterns.sh:20-21` and incremented by `pass`/`fail` at `tests/test-bash-validator-patterns.sh:23-34`. Exit state is binary: exit 0 when failures are zero, exit 1 otherwise at `tests/test-bash-validator-patterns.sh:206-214`.
- gates_or_invariants: The central invariant is the production helper’s contract: `command_modifies_file` returns 0 when a command attempts to modify a path matching the supplied pattern and 1 otherwise, documented and implemented at `hooks/lib/loop-common.sh:1068-1096`. Its pattern array recognizes redirection, append redirection, `tee`, in-place editors, destination-oriented `mv`/`cp`, `rm`, `dd of=`, `truncate`, `printf ... >`, and `exec N>` at `hooks/lib/loop-common.sh:1075-1088`. The test locks in case-insensitive behavior by lowercasing before detection and explicitly testing uppercase input at `tests/test-bash-validator-patterns.sh:40-43` and `tests/test-bash-validator-patterns.sh:80`. It also preserves a false-positive invariant: read-only commands like `cat`, `grep`, `head`, `tail`, `wc`, `less`, `echo`, `ls`, `file`, `stat`, and `diff` must not trip the write gate at `tests/test-bash-validator-patterns.sh:142-152`.
- dependencies_and_callers: Direct dependency is `hooks/lib/loop-common.sh`, especially `to_lower` at `hooks/lib/loop-common.sh:394-397` and `command_modifies_file` at `hooks/lib/loop-common.sh:1071-1096`. The production caller is `hooks/loop-bash-validator.sh`: it parses `tool_input.command`, lowercases it at `hooks/loop-bash-validator.sh:42-49`, then applies `command_modifies_file` to protected RLCR state files at `hooks/loop-bash-validator.sh:139-155`, plan backup at `hooks/loop-bash-validator.sh:336-340`, goal tracker at `hooks/loop-bash-validator.sh:349-357`, prompt/summary/todos files at `hooks/loop-bash-validator.sh:365-390`, and PR-loop protected files at `hooks/loop-bash-validator.sh:407-439`. The helper library also sources `hooks/lib/template-loader.sh` during load at `hooks/lib/loop-common.sh:139-144`, but this specific test only exercises pure string predicates and reporting counters.
- edge_cases_or_failure_modes: The file explicitly documents a known limitation for multiple-source copy commands: `cp file1.md file2.md goal-tracker.md` is not detected because the regex expects a simple `cp src dest` shape at `tests/test-bash-validator-patterns.sh:165-168`. Variable indirection is intentionally not treated as a literal target, so `echo x > $FILE` is expected to be ignored at `tests/test-bash-validator-patterns.sh:170-172`. The underlying regexes are command-text heuristics, not a shell parser; paths with spaces, complex quoting, multiple separated option groups, or shell expansion are outside this test’s positive coverage because production matching uses `[^[:space:]]*` path fragments at `hooks/lib/loop-common.sh:1076-1087`. Source-side `mv/cp state.md /tmp/foo` protection is not covered by this assigned test’s helper assertions; production adds separate segment/source matching for that case at `hooks/loop-bash-validator.sh:158-180`. `finalize-state.md` is also not directly exercised here, while production checks it before the broader `state\.md` pattern because the broader suffix can overlap, noted at `hooks/loop-bash-validator.sh:131-139`.
- validation_or_tests: Ran `bash tests/test-bash-validator-patterns.sh` in the read-only branch export. Result: 48 passed, 0 failed, exit 0. The run confirmed all nine groups and the final zero-failure gate at `tests/test-bash-validator-patterns.sh:206-214`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 evidence section present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`