# agent_052 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-052 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: This file is an executable negative-path specification for the template loading/rendering library used by the hook system. It focuses on graceful failure behavior in [tests/test-error-scenarios.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-error-scenarios.sh:1), especially ensuring missing prompt templates, malformed placeholders, invalid template directories, and strict shell mode do not crash hook flows or emit unsafe output. Its target implementation is [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/template-loader.sh:1).

- algorithmic_behavior: The test sources `template-loader.sh`, computes `PROJECT_ROOT` from the test location, and derives `TEMPLATE_DIR` through `get_template_dir "$PROJECT_ROOT/hooks/lib"` at lines 11-15. It maintains two counters, `TESTS_PASSED` and `TESTS_FAILED`, and uses local `pass`/`fail` helpers to mutate those counters at lines 22-35. The algorithm is a sequential assertion harness: each scenario invokes one loader function, captures output and sometimes exit code, compares against an expected invariant, then increments pass/fail state. Final state transitions happen at lines 218-225: zero failures exits `0`; any failure exits `1`.

  The covered behavior maps directly to loader functions:
  - `load_template` should return empty stdout and exit `0` for a missing file or missing directory, while warnings go to stderr and can be suppressed by callers. The implementation checks `[[ -f "$template_path" ]]`, `cat`s only if present, otherwise emits warning text to stderr plus empty stdout at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/template-loader.sh:36).
  - `load_and_render` should return empty output when `load_template` returns empty, because it only calls `render_template` when `content` is non-empty at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/template-loader.sh:136).
  - `render_template` should do single-pass placeholder substitution with `awk`, replacing `{{VAR}}` values from `TMPL_VAR_*` environment variables and preserving placeholders without supplied variables at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/template-loader.sh:58).
  - `load_and_render_safe` should fall back when a template is missing or rendered content is empty, and should render variables inside the fallback when arguments are supplied at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/template-loader.sh:170).
  - `validate_template_dir` should accept a real template root only when required subdirectories exist and reject invalid/incomplete paths at [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/template-loader.sh:208).

- inputs_outputs_state: Inputs are shell function calls plus controlled template strings and paths. External inputs include the computed `PROJECT_ROOT`, the derived `TEMPLATE_DIR`, literal invalid paths such as `/non/existing/path`, placeholder templates like `Path: {{PATH}}`, fallback strings, and `VAR=value` assignment arguments. Outputs are command stdout captured into `CONTENT`, `RESULT`, or `SCRIPT_OUTPUT`, stderr mostly redirected to `/dev/null` for expected missing-template warnings, pass/fail log lines, counter increments, and the final process exit status. State is local to the script: counters start at `0`, each assertion mutates exactly one counter, and final exit status reflects `TESTS_FAILED`.

- gates_or_invariants: The main invariant is graceful degradation: missing template resources must not be fatal. Tests 1-3 require empty output and exit `0` for missing files/directories or missing `load_and_render` templates at lines 45-78. Test 4 requires empty input to `render_template` to produce empty output and exit `0` at lines 84-91. Test 5 requires variable values containing regex-looking metacharacters such as brackets, parentheses, and asterisks to render literally at lines 97-105, which guards against sed/regex replacement bugs. Test 6 embeds a strict `set -euo pipefail` subshell and requires it to reach `SCRIPT_COMPLETED`, proving missing templates do not abort strict-mode callers at lines 112-130. Tests 7-8 require fallback behavior for `load_and_render_safe`, including fallback variable substitution at lines 137-157. Tests 9-10 assert valid directory acceptance and invalid directory rejection at lines 164-179. Tests 11-12 assert malformed placeholder preservation: `{{}}` and unclosed `{{UNCLOSED` should remain literal at lines 186-205.

- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`, sourced at line 13. That loader is shared by `hooks/lib/loop-common.sh`, which sources it and initializes `TEMPLATE_DIR` with graceful warning-only validation at lines 139-148. Hook callers use `load_and_render_safe` for operator-facing block messages: read validation uses it for wrong file location, wrong round, and wrong directory errors in `hooks/loop-read-validator.sh` lines 122-167; write validation uses it for protected plan backup, wrong summary location, wrong round, and wrong directory errors in `hooks/loop-write-validator.sh` lines 179-262; bash validation uses it for git push and plan backup blocks in `hooks/loop-bash-validator.sh` lines 98-104 and 336-339. `hooks/lib/loop-common.sh` centralizes additional shared block-message wrappers using the same safe fallback path at lines 443-500 and 1098-1109. Companion test coverage includes positive loader behavior in `tests/test-template-loader.sh` and reference completeness/safe-render usage checks in `tests/test-template-references.sh`.

- edge_cases_or_failure_modes: The file intentionally targets missing template files, missing template directories, empty render content, special regex characters in values, strict shell mode with missing templates, fallback messages, fallback substitution, invalid template roots, empty placeholders, and unclosed placeholders. An implementation-sensitive failure mode surfaced during validation in this read-only branch export: `render_template` uses a Bash here-string into `awk` at `template-loader.sh:129`, and this environment cannot create the temporary file needed for that here-string. As a result, render-dependent assertions fail under this sandbox even though the test logic itself is coherent. Another edge implied by the loader implementation is that `load_template` always exits successfully for missing templates because the final command is `echo ""`, making empty stdout the missing-resource sentinel rather than a nonzero status.

- validation_or_tests: I inspected the assigned file directly with line numbers and followed the dependency into `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-bash-validator.sh`, `tests/test-template-loader.sh`, and `tests/test-template-references.sh`. I ran `bash tests/test-error-scenarios.sh` from the branch export. Result in this read-only environment: exit `1`, with 8 passed and 4 failed. The failures were render-path failures caused by `hooks/lib/template-loader.sh:129: cannot create temp file for here document: Operation not permitted`, affecting Test 5 special regex characters, Test 8 fallback substitution, Test 11 empty placeholder preservation, and Test 12 unclosed placeholder preservation. Non-render or no-substitution negative paths passed: missing file, missing directory, missing `load_and_render`, empty content, strict-mode continuation, fallback without variables, and template directory validation.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 of 1 item section present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`