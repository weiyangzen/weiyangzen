# agent_093 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-093 `file` `prompt-template/block/pr-loop-prompt-write.md`
- cursor: `[_]`
- core_role: This is a user-facing guard template for PR-loop generated-file write protection. Its local role is not to implement matching logic itself, but to define the canonical denial message rendered when a hook blocks attempted writes or edits to generated PR-loop round artifacts. The assigned file declares the blocked class at `prompt-template/block/pr-loop-prompt-write.md:1` and names the protected files at `prompt-template/block/pr-loop-prompt-write.md:3`.
- algorithmic_behavior: The block is a static prompt fragment used as the output surface for a validation transition: attempted mutation of system-generated PR-loop files becomes a hook failure with an explanatory message. The message states that `round-*-pr-comment.md` and `round-*-prompt.md` under `.humanize/pr-loop/` are read-only, then identifies their generated sources: PR comments fetched from GitHub and round instructions, at `prompt-template/block/pr-loop-prompt-write.md:5` through `prompt-template/block/pr-loop-prompt-write.md:9`. The actual predicate logic lives in hook code. `hooks/lib/loop-common.sh:898` through `hooks/lib/loop-common.sh:905` defines `pr_loop_prompt_blocked_message`, whose fallback mirrors this template and calls `load_and_render_safe "$TEMPLATE_DIR" "block/pr-loop-prompt-write.md"`.
- inputs_outputs_state: Input to this file is implicit through the renderer: a hook decides a path is a protected PR-loop generated file and asks `pr_loop_prompt_blocked_message` for text. The template has no placeholders and consumes no runtime variables. Its output is Markdown written to stderr by the validators before returning a blocking status. In the write validator, this happens at `hooks/loop-write-validator.sh:83` through `hooks/loop-write-validator.sh:87` for PR-loop read-only files. In the edit validator, the same transition appears at `hooks/loop-edit-validator.sh:66` through `hooks/loop-edit-validator.sh:69`. In the bash validator, shell command attempts are blocked through the PR-loop read-only pattern loop at `hooks/loop-bash-validator.sh:417` through `hooks/loop-bash-validator.sh:441`. The protected state is the generated `.humanize/pr-loop/<timestamp>/round-N-*` files; successful blocking preserves those files unchanged and exits with code `2` in the validators.
- gates_or_invariants: The invariant encoded by this block is that PR-loop generated inputs are read-only to the agent. The narrow text names `round-*-pr-comment.md` and `round-*-prompt.md`, while the actual invariant in hook code is broader: `hooks/lib/loop-common.sh:825` through `hooks/lib/loop-common.sh:835` classifies `pr-comment`, `prompt`, `codex-prompt`, `pr-check`, and `pr-feedback` round files as PR-loop read-only. The validators separately allow the agent-owned resolution file only after round validation: `hooks/loop-write-validator.sh:89` through `hooks/loop-write-validator.sh:93` allows `round-N-pr-resolve.md` when `validate_pr_resolve_round` passes, and `hooks/lib/loop-common.sh:837` through `hooks/lib/loop-common.sh:887` blocks wrong-round `pr-resolve` writes. Another ordering invariant is that generic `round-*-prompt.md` protection runs before the PR-loop-specific branch in direct Write/Edit and Bash validators, so PR-loop prompt-file attempts may receive `block/prompt-file-write.md` rather than this assigned template; see `hooks/loop-write-validator.sh:65` through `hooks/loop-write-validator.sh:68`, `hooks/loop-edit-validator.sh:48` through `hooks/loop-edit-validator.sh:50`, and `hooks/loop-bash-validator.sh:365` through `hooks/loop-bash-validator.sh:367`.
- dependencies_and_callers: Primary caller is `pr_loop_prompt_blocked_message` in `hooks/lib/loop-common.sh:898` through `hooks/lib/loop-common.sh:905`, which depends on the shared template loader `load_and_render_safe`. Execution callers are the PreToolUse validators for `Write`, `Edit`, and `Bash`: `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh`. The protected prompt files are generated by PR-loop setup code: `scripts/setup-pr-loop.sh:641` defines the round-0 resolve path, `scripts/setup-pr-loop.sh:677` through `scripts/setup-pr-loop.sh:684` renders and writes `round-0-prompt.md`, and `scripts/setup-pr-loop.sh:770` through `scripts/setup-pr-loop.sh:771` appends the task section. PR comments are fetched into a comment file and appended to the generated prompt at `scripts/setup-pr-loop.sh:683` through `scripts/setup-pr-loop.sh:684`.
- edge_cases_or_failure_modes: The template text is narrower than the hook predicate: attempts to modify `round-N-codex-prompt.md`, `round-N-pr-check.md`, or `round-N-pr-feedback.md` can render this same message even though those names are not listed in the assigned file; the broader protected set is documented in code comments at `hooks/loop-bash-validator.sh:417` through `hooks/loop-bash-validator.sh:422`. For direct Write/Edit and generic Bash prompt-file writes, `round-N-prompt.md` is intercepted earlier by the generic prompt-file block, so this PR-loop-specific block is most directly observed for `pr-comment`, `codex-prompt`, `pr-check`, and `pr-feedback` attempts. Bash protection has a deliberate relative-path bypass guard: it checks both `.humanize/pr-loop/.../<pattern>` and bare filenames at `hooks/loop-bash-validator.sh:431` through `hooks/loop-bash-validator.sh:441`. Direct Write/Edit path checks rely on `is_in_pr_loop_dir`, which matches `.humanize/pr-loop/` in the provided path at `hooks/lib/loop-common.sh:783` through `hooks/lib/loop-common.sh:787`; unusual path spellings that do not include that normalized segment could bypass the PR-loop-specific branch, though generic prompt blocking still catches `round-N-prompt.md`.
- validation_or_tests: Focused PR-loop hook tests assert the behavioral gate. `tests/test-pr-loop-hooks.sh:102` through `tests/test-pr-loop-hooks.sh:130` creates `.humanize/pr-loop/<timestamp>/round-0-pr-comment.md`, runs the write validator, and expects exit code `2`. The same test file confirms the intended exception for agent-authored resolution files at `tests/test-pr-loop-hooks.sh:135` through `tests/test-pr-loop-hooks.sh:163`, and wrong-round validation for `pr-resolve` at `tests/test-pr-loop-hooks.sh:1431` through `tests/test-pr-loop-hooks.sh:1459`. Template-reference coverage is partial: `tests/test-template-references.sh:152` through `tests/test-template-references.sh:159` lists common block templates but does not include `block/pr-loop-prompt-write.md`, so the assigned block is validated indirectly through the hook code path and `load_and_render_safe` fallback behavior rather than by that common-template inventory.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`