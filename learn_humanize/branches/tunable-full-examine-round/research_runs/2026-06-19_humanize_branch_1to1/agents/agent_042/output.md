# agent_042 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-042 `file` `scripts/validate-gen-plan-io.sh`
- cursor: `[_]`
- core_role: Runtime gate and scaffold writer for the `/humanize:gen-plan` command. Although named as an IO validator, it is part of the command workflow: it validates draft/output paths, locates the plan template, creates the initial output plan file, and appends the original draft for later plan generation. The command contract depends on this file being created before the later “Edit plan content” phase; see `commands/gen-plan.md:30-47` and `commands/gen-plan.md:251-260`.

- algorithmic_behavior: The script uses a fail-fast Bash flow with `set -e` at `scripts/validate-gen-plan-io.sh:14`. It parses only `--input`, `--output`, `-h`, and `--help`; missing option values, values that look like another flag, and unknown options all route through `usage()` and exit `6` at `scripts/validate-gen-plan-io.sh:16-67`. It normalizes the input/output paths with `realpath -m` when available, falling back to the original string, then derives `OUTPUT_DIR` with `dirname` at `scripts/validate-gen-plan-io.sh:69-72`. It performs ordered validation gates: input regular file exists, input is non-empty, output directory exists, output target is not a directory, output target does not already exist, and output directory is writable at `scripts/validate-gen-plan-io.sh:79-124`. After those gates pass, it counts input lines and reports success at `scripts/validate-gen-plan-io.sh:126-131`. It then resolves the template from `CLAUDE_PLUGIN_ROOT/prompt-template/plan/gen-plan-template.md` or from a script-relative fallback at `scripts/validate-gen-plan-io.sh:133-140`, verifies the template exists at `scripts/validate-gen-plan-io.sh:142-146`, copies it to the output path, and appends the original draft between explicit start/end separators at `scripts/validate-gen-plan-io.sh:148-160`.

- inputs_outputs_state: Inputs are shell arguments `--input <draft>` and `--output <plan>`, the optional `CLAUDE_PLUGIN_ROOT` environment variable, the draft file contents, the template file at `prompt-template/plan/gen-plan-template.md`, and filesystem permissions for the output directory. Outputs are status/error messages on stdout, numeric exit codes, and, on success, a newly created output markdown file containing the template plus the original draft. The main state transition is: output target must begin nonexistent, then becomes a scaffolded plan file after `cp` and append operations. There is no persistent scheduler-style state, lock, ledger, or marker file managed by this script.

- gates_or_invariants: The output file is intentionally required to be new; existing files and directories both exit with code `4` at `scripts/validate-gen-plan-io.sh:103-116`. The output parent must already exist; the script tells the user to create it rather than creating it itself at `scripts/validate-gen-plan-io.sh:95-100`. The template must exist before writing begins, otherwise the script exits `7` as a plugin configuration failure at `scripts/validate-gen-plan-io.sh:142-146`. The command-level invariant is that successful IO validation also means the later gen-plan phase has a prepared output file preserving the original draft; `commands/gen-plan.md:253-260` explicitly relies on that side effect.

- dependencies_and_callers: The direct caller is `commands/gen-plan.md`, whose frontmatter allows `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh:*)` at `commands/gen-plan.md:4-11`, invokes the script in Phase 1 at `commands/gen-plan.md:30-36`, and maps exit codes `0` through `7` to command behavior at `commands/gen-plan.md:38-47`. The user-facing README documents `/humanize:gen-plan --input ... --output ...` at `README.md:76-79` and summarizes the workflow at `README.md:122-139`. The template dependency is `prompt-template/plan/gen-plan-template.md`, whose placeholder plan structure begins at `prompt-template/plan/gen-plan-template.md:1-74`. Shell/tool dependencies include Bash, `realpath`, `dirname`, `wc`, `tr`, `cp`, and `cat`.

- edge_cases_or_failure_modes: `--help` exits `6`, the same invalid-argument code used for malformed input, so help is not a success path; tests intentionally expect that behavior at `tests/test-gen-plan.sh:532-538`. A valid filename argument beginning with `--` is rejected by the parser unless expressed in a way that does not start with `--`, because value validation checks `"$2" == --*` at `scripts/validate-gen-plan-io.sh:33-43`. Duplicate `--input` or `--output` flags are not rejected; the later value wins. Path normalization depends on `realpath -m`; platforms without that behavior fall back to the original path string at `scripts/validate-gen-plan-io.sh:69-71`. There is a time-of-check/time-of-use gap between the nonexistence check and `cp`; if another process creates the output path after validation, plain `cp` can overwrite that new regular file. If `cp` succeeds but an append or `cat` later fails, `set -e` stops the script with the underlying command’s exit code and may leave a partial output file; those failures are not remapped into the documented exit-code table. The `-w` directory check can differ from actual write success under ACL, quota, or read-only filesystem conditions. The command invocation in `commands/gen-plan.md:35` expands `$ARGUMENTS` unquoted, so paths containing spaces depend on how the slash-command runtime preserves or shell-quotes user arguments before they reach this script.

- validation_or_tests: The focused shell test block starts at `tests/test-gen-plan.sh:426`. It verifies the script is executable, then covers missing option values, a flag used as a value, unknown option, missing input, empty input, missing output directory, existing output file, output path as directory, a valid success path, and help behavior at `tests/test-gen-plan.sh:431-541`. Current coverage does not exercise no-write-permission exit `5`, missing-template exit `7`, `CLAUDE_PLUGIN_ROOT` resolution, exact scaffold content, duplicate flags, path values beginning with `--`, race conditions, or partial-write cleanup. I ran a read-only syntax validation, `bash -n scripts/validate-gen-plan-io.sh`, and it passed. I did not run the full shell test suite because this assignment requested research notes only and the success-path tests create output files.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`