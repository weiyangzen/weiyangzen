# agent_120 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-120 `file` `prompt-template/pr-loop/codex-goal-tracker-update.md`
- cursor: `[_]`
- core_role: This file is a PR-loop prompt contract fragment injected into Codex’s per-bot PR review validation prompt. It tells Codex how to mutate the PR loop goal tracker after review analysis: append a per-round reviewer summary row, update aggregate issue totals, and add an issue-log entry. It is not executable code itself, but it defines part of the algorithmic state transition for `.humanize/pr-loop/.../goal-tracker.md` during PR review rounds. The stop hook loads it at `hooks/pr-loop-stop-hook.sh:1239-1248` and embeds the rendered instructions in the Codex prompt at `hooks/pr-loop-stop-hook.sh:1288`.

- algorithmic_behavior: The template defines a three-part update algorithm. First, add a row to `## Issue Summary` using `| {{NEXT_ROUND}} | <reviewer_name> | <issues_found> | <issues_resolved> | <status> |`, with allowed statuses listed as `"Issues Found"`, `"All Resolved"`, or `"Approved"` at `prompt-template/pr-loop/codex-goal-tracker-update.md:7-10`. Second, update `## Total Statistics` by incrementing total found and resolved, then recomputing `Remaining = Total Found - Total Resolved` at `prompt-template/pr-loop/codex-goal-tracker-update.md:12-15`. Third, add a `### Round {{NEXT_ROUND}}` issue-log entry with issue or approval details and reviewer name at `prompt-template/pr-loop/codex-goal-tracker-update.md:17-20`. The template also includes an example showing separate reviewer rows for `claude` and `codex` in the same round at `prompt-template/pr-loop/codex-goal-tracker-update.md:24-54`.

- inputs_outputs_state: Inputs are rendered template variables `{{GOAL_TRACKER_FILE}}` and `{{NEXT_ROUND}}`; the stop hook supplies those as `GOAL_TRACKER_FILE=$LOOP_DIR/goal-tracker.md` and `NEXT_ROUND=$NEXT_ROUND` at `hooks/pr-loop-stop-hook.sh:1240-1244`. The template renderer uses single-pass `{{VAR}}` substitution and leaves missing placeholders intact, per `hooks/lib/template-loader.sh:7-13` and `hooks/lib/template-loader.sh:50-57`. Runtime semantic inputs are Codex’s review results: reviewer name, number of issues found, number of issues resolved, and final status. Output is a modified goal tracker file whose schema is initialized by `prompt-template/pr-loop/goal-tracker-initial.md`: PR metadata, ultimate goal, `## Issue Summary`, `## Total Statistics`, and `## Issue Log` at `prompt-template/pr-loop/goal-tracker-initial.md:1-33`. The intended state transition is per round and per reviewer: append one reviewer row, adjust cumulative counters, and append log evidence while preserving earlier tracker state.

- gates_or_invariants: The template’s explicit invariants are structural: keep the markdown structure intact, maintain proper markdown table formatting, update only `Issue Summary`, `Total Statistics`, and `Issue Log`, do not modify `PR Information` or `Ultimate Goal`, append to existing tables instead of replacing them, and use one separate Issue Summary row per reviewer at `prompt-template/pr-loop/codex-goal-tracker-update.md:57-64`. The surrounding stop hook adds a fallback gate: if the template is missing or empty, `load_and_render_safe` falls back to a minimal goal-tracker update instruction at `hooks/pr-loop-stop-hook.sh:1245-1248`. The executable guardrail is `update_pr_goal_tracker`, which treats Codex prompt-based updating as primary but repairs or performs the update when Codex does not update correctly, as documented at `hooks/lib/loop-common.sh:1165-1167`.

- dependencies_and_callers: Direct caller is `hooks/pr-loop-stop-hook.sh`, which builds `round-${NEXT_ROUND}-codex-prompt.md`, includes expected bots and bot review content, specifies the required Codex output sections, and appends this rendered goal-tracker update block at `hooks/pr-loop-stop-hook.sh:1229-1289`. The template depends on the shared template loader’s `load_and_render_safe` path and `{{...}}` substitution behavior in `hooks/lib/template-loader.sh:167-180`. The goal tracker file itself is created by `scripts/setup-pr-loop.sh` through `prompt-template/pr-loop/goal-tracker-initial.md`, and the schema contains the sections this template is allowed to update. The main executable companion is `hooks/lib/loop-common.sh:update_pr_goal_tracker`, which receives `tracker_file`, `round`, and optional JSON `{ "bot": "name", "issues": N, "resolved": N }` at `hooks/lib/loop-common.sh:1151-1159`, checks idempotency by round plus reviewer at `hooks/lib/loop-common.sh:1184-1208`, updates totals at `hooks/lib/loop-common.sh:1220-1265`, inserts the summary row at `hooks/lib/loop-common.sh:1267-1297`, and appends issue-log text at `hooks/lib/loop-common.sh:1299-1313`. The stop hook invokes that helper on all-approved exit with bot `All` at `hooks/pr-loop-stop-hook.sh:1354-1362` and on non-approved rounds after counting list items in the `### Issues Found` section at `hooks/pr-loop-stop-hook.sh:1492-1519`.

- edge_cases_or_failure_modes: There is contract drift between the prompt and helper implementation. The prompt allows status `"All Resolved"` at `prompt-template/pr-loop/codex-goal-tracker-update.md:10`, while `update_pr_goal_tracker` emits `"Resolved"` when `new_resolved > 0` at `hooks/lib/loop-common.sh:1243-1251`. The prompt example writes issue-log entries as bold prose such as `**codex** approved` at `prompt-template/pr-loop/codex-goal-tracker-update.md:49-54`, but the helper’s idempotency check looks for a line whose first token is exactly `Reviewer:` within the round, e.g. `Codex:` at `hooks/lib/loop-common.sh:1195-1203`; if Codex follows the example, the shell helper may treat the log as partially missing and append a second log entry. The prompt says to create `### Round {{NEXT_ROUND}}` for each issue-log entry at `prompt-template/pr-loop/codex-goal-tracker-update.md:17-20`, while multiple reviewers in the same round are expected to get separate summary rows at `prompt-template/pr-loop/codex-goal-tracker-update.md:63-64`; without clearer grouping rules, multiple agents could duplicate `### Round N` headings. The stop hook’s non-approve path counts issue bullets only inside `### Issues Found` using numbered or bullet patterns at `hooks/pr-loop-stop-hook.sh:1497-1503`, so malformed Codex output can undercount. Another drift is resolution accounting: prompt asks Codex to increment resolved issues verified fixed at `prompt-template/pr-loop/codex-goal-tracker-update.md:12-15`, but the stop hook sets resolved count to `0` on non-approve rounds and also records `0` on all-approved exit at `hooks/pr-loop-stop-hook.sh:1358-1362` and `hooks/pr-loop-stop-hook.sh:1505-1511`; depending on intended semantics, prior remaining issues may never be reduced by the shell fallback. If `jq` is unavailable, helper parsing falls back to default values in several command substitutions at `hooks/lib/loop-common.sh:1178-1182` and `hooks/lib/loop-common.sh:1233-1236`, which can silently degrade reviewer or count accuracy.

- validation_or_tests: The goal-tracker schema is validated by `test_goal_tracker_schema`, which checks required sections and total-stat fields at `tests/test-pr-loop-system.sh:642-655`. `test_goal_tracker_update_adds_row` verifies `update_pr_goal_tracker` adds a Round 1 log entry and increments total issues found at `tests/test-pr-loop-system.sh:681-716`. `test_goal_tracker_update_idempotent` verifies duplicate round plus bot updates do not double-count or add duplicate rows at `tests/test-pr-loop-system.sh:722-764`. `test_goal_tracker_row_inside_table` verifies the new summary row appears inside the Issue Summary table before `## Total Statistics` at `tests/test-pr-loop-system.sh:1180-1236`. `test_goal_tracker_partial_update_repair` verifies repair behavior when either the summary row or issue-log entry already exists but the other is missing at `tests/test-pr-loop-system.sh:1242-1309`. `test_stophook_updates_goal_tracker` exercises the PR stop hook path with mocked `gh`, `git`, and `codex`, then verifies a Round 1 codex entry appears in the goal tracker at `tests/test-pr-loop-system.sh:1541-1748`. These tests primarily cover the executable fallback/helper behavior rather than asserting the exact prose template output.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-120`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`