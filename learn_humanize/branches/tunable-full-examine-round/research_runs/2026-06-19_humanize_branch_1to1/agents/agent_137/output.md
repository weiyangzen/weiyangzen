# agent_137 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-137 `file` `tests/robustness/test-pr-loop-api-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the PR review loop’s GitHub API boundary. The file does not implement the PR loop itself; it drives the real scripts and hook entry points with mocked `gh` behavior to assert that the core PR-loop algorithm tolerates empty APIs, failed APIs, bot comment formats, malformed loop state, and JSON output guarantees. It is part of the algorithmic safety net because PR-loop progress depends on interpreting remote bot feedback without crashing or losing state.

- algorithmic_behavior: The test runs under `set -euo pipefail` and derives `PROJECT_ROOT`, then sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` for production helpers and pass/fail counters (`tests/robustness/test-pr-loop-api-robustness.sh:14-21`). Its main mock harness is `create_mock_gh`, which writes a temporary `bin/gh` command that handles `gh repo view`, `gh pr view`, and selected `gh api` behaviors (`tests/robustness/test-pr-loop-api-robustness.sh:32-239`). The mock covers empty arrays, rate limits, network errors, auth failure, Claude approval comments, Codex severity-marker comments, mixed bot comments, Unicode bodies, and very large comment bodies. `create_pr_loop_state` writes `.humanize/pr-loop/<timestamp>/state.md` frontmatter with PR number, configured bots, active bots, startup case, and review status (`tests/robustness/test-pr-loop-api-robustness.sh:241-262`). `init_basic_git_repo` creates isolated Git repos for scripts that expect repository context (`tests/robustness/test-pr-loop-api-robustness.sh:264-276`).

  The first group checks PR-loop state discovery and schema tolerance. Test 1 asserts `find_active_pr_loop` detects a timestamped loop directory with `state.md` (`tests/robustness/test-pr-loop-api-robustness.sh:285-295`), matching the production helper that selects the lexically newest child under `.humanize/pr-loop` and returns it only when `state.md` exists (`hooks/lib/loop-common.sh:795-814`). Test 2 asserts YAML-list `active_bots` is present and accepted as a file format (`tests/robustness/test-pr-loop-api-robustness.sh:297-322`). Test 3 asserts a loop remains discoverable even when `pr_number` is absent (`tests/robustness/test-pr-loop-api-robustness.sh:324-345`), which separates loop-directory activity detection from later state validation.

  The second group exercises `scripts/fetch-pr-comments.sh` end to end. Empty comment APIs must produce an output markdown file containing PR number and repository (`tests/robustness/test-pr-loop-api-robustness.sh:355-377`). Rate-limit failures may either return nonzero or create output with warnings, but must not be silently successful with no file and no diagnostic (`tests/robustness/test-pr-loop-api-robustness.sh:379-406`). Network errors must surface as nonzero or include an error/failure/connection message (`tests/robustness/test-pr-loop-api-robustness.sh:408-425`). The implementation being specified resolves the current/base repository, fetches issue comments, review comments, and reviews with retries (`scripts/fetch-pr-comments.sh:130-219`), converts all three API shapes into a unified JSONL schema (`scripts/fetch-pr-comments.sh:255-323`), filters and sorts comments (`scripts/fetch-pr-comments.sh:325-345`), writes markdown sections (`scripts/fetch-pr-comments.sh:347-442`), and appends an API-failure warning while still exiting successfully after nonfatal endpoint failures (`scripts/fetch-pr-comments.sh:444-451`).

  The bot parsing section asserts that generated markdown preserves bot identity and content needed by the loop’s decision logic: Claude comments include `claude[bot]` and “LGTM” (`tests/robustness/test-pr-loop-api-robustness.sh:435-455`), Codex comments include `chatgpt-codex-connector[bot]` and severity markers such as `[P1]` (`tests/robustness/test-pr-loop-api-robustness.sh:457-478`), and mixed bot responses include both bot authors (`tests/robustness/test-pr-loop-api-robustness.sh:480-500`). This corresponds to `fetch-pr-comments.sh` bot detection and grouping, including the `codex -> chatgpt-codex-connector[bot]` mapping when `--bots` is supplied (`scripts/fetch-pr-comments.sh:380-420`).

  The JSON edge-case section asserts that Unicode comments complete the full fetch-to-markdown path and that a 10,000-character body is not dropped (`tests/robustness/test-pr-loop-api-robustness.sh:511-550`). These tests defend against shell quoting, `jq`, and file-output truncation hazards around comment bodies.

  The stop-hook section checks graceful PR-loop hook behavior at the boundary of state availability. With no active PR loop, `hooks/pr-loop-stop-hook.sh` must exit zero (`tests/robustness/test-pr-loop-api-robustness.sh:560-574`); production code does this immediately when `find_active_pr_loop` returns empty (`hooks/pr-loop-stop-hook.sh:64-70`). With corrupted state content, the hook must not crash with a shell/runtime fatal exit (`tests/robustness/test-pr-loop-api-robustness.sh:576-593`). The hook’s state parser extracts YAML frontmatter and defaults fields, then exits zero on missing `PR_NUMBER` or invalid `current_round` (`hooks/pr-loop-stop-hook.sh:87-174`, `hooks/pr-loop-stop-hook.sh:185-194`). Test 14 verifies the `approve-state.md` path inside the active loop directory is writable, matching the hook’s later state transitions where `state.md` is moved to `approve-state.md` when all bot concerns are approved or timed out.

  The final group exercises `scripts/poll-pr-reviews.sh`. Help must exit zero and show usage text (`tests/robustness/test-pr-loop-api-robustness.sh:619-630`). Missing required args must fail with an error (`tests/robustness/test-pr-loop-api-robustness.sh:632-644`), matching script validation of PR number, `--after`, `--bots`, and numeric PR format (`scripts/poll-pr-reviews.sh:83-102`). With mocked Claude output, the script must emit parseable JSON containing a boolean `has_new_comments` and preferably a `comments` array (`tests/robustness/test-pr-loop-api-robustness.sh:646-675`). A slow API mock must complete and normally report `has_new_comments=false` for empty arrays (`tests/robustness/test-pr-loop-api-robustness.sh:677-738`). On API failure, the contract is strict: exit zero, output valid JSON, and set `has_new_comments` to boolean false, not a string (`tests/robustness/test-pr-loop-api-robustness.sh:740-839`). The implementation’s retry wrapper intentionally returns `[]` and status zero after exhausted retries (`scripts/poll-pr-reviews.sh:199-235`), then builds the final JSON object with `comments`, `bots_responded`, `has_new_comments`, and `comment_count` (`scripts/poll-pr-reviews.sh:306-327`).

- inputs_outputs_state: Inputs are temporary working directories, synthetic `.humanize/pr-loop/<timestamp>/state.md` files, temporary Git repositories, PATH-injected mocked `gh` commands, real project scripts under `scripts/`, and real hook/library code under `hooks/`. Test inputs include PR numbers, output markdown paths, `--after` timestamps, `--bots` lists, `CLAUDE_PROJECT_DIR`, and mocked API payloads. Outputs are pass/fail counter increments via `tests/test-helpers.sh`, generated markdown files like `comments.md`, stdout/stderr from invoked scripts, JSON from `poll-pr-reviews.sh`, and final process status from `print_test_summary` (`tests/robustness/test-pr-loop-api-robustness.sh:841-846`; `tests/test-helpers.sh:58-78`). State transitions are all in disposable temp paths: Git repos are initialized and committed; mock `gh` executables are created; PR-loop state directories are created; `approve-state.md` is probed for writability; hook execution may inspect or move state if it reaches production transitions. The assigned test itself cleans temp state through `setup_test_dir`’s exit trap (`tests/test-helpers.sh:84-89`).

- gates_or_invariants: The file enforces shell strict mode and uses controlled `set +e` blocks around commands expected to fail so the test can inspect exit codes (`tests/robustness/test-pr-loop-api-robustness.sh:361-364`, `386-389`, `415-418`, `621-624`, `802-805`). Core invariants include: active PR-loop discovery depends on newest `state.md`, not complete state validity; fetch output for empty APIs must still include PR/repo metadata; API failures must produce warnings, nonzero exits, or safe empty data rather than silent success; bot author names and severity markers must survive formatting; Unicode and long bodies must not break parsing; stop hook must exit gracefully when no loop or bad state is present; `poll-pr-reviews.sh` must validate required arguments; and API failure polling must still return valid JSON with boolean `has_new_comments:false`.

- dependencies_and_callers: Direct dependencies are `bash`, `git`, `jq`, `mktemp`, standard POSIX-ish shell tools, `hooks/lib/loop-common.sh`, `tests/test-helpers.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, and `hooks/pr-loop-stop-hook.sh`. The test replaces `gh` through PATH-local mocks, so it avoids real GitHub network dependency while exercising the actual script entry points. It is included in the repository’s broader test runner list as `robustness/test-pr-loop-api-robustness.sh` (`tests/run-all-tests.sh`, discovered by reference), and complements other PR-loop script/system tests that exercise setup, monitor, hook, and state behavior.

- edge_cases_or_failure_modes: Covered failures include GitHub API rate limiting, network refusal/unreachable errors, empty API arrays, missing PR-loop `pr_number`, corrupted/non-frontmatter state content, missing poll arguments, slow API calls, Unicode JSON content, very large comment bodies, multiple bots responding, and Codex comments containing priority markers. A notable test-design edge is that Test 2 only verifies the YAML-list key exists rather than invoking the parser; deeper parser behavior is covered by the stop hook and sibling PR-loop tests. Another edge is that fetch API failures are accepted under several outcomes, while poll API failures are strict because polling is expected to keep the PR-loop alive with a safe no-new-comments result.

- validation_or_tests: I inspected the assigned file directly with line numbers and followed its referenced production scripts and helpers. I also ran `bash -n tests/robustness/test-pr-loop-api-robustness.sh`, which returned `syntax-ok`. I did not run the full robustness test because it includes retry sleeps and invokes real scripts in a read-only research context; static inspection plus syntax validation was sufficient for the requested evidence.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-137`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`