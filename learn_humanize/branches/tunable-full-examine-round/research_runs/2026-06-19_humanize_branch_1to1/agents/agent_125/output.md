# agent_125 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-125 `file` `prompt-template/pr-loop/round-0-task-has-comments.md`
- cursor: `[_]`
- core_role: This is the Round 0 PR-loop task contract used when fetched PR comments already exist. It is not executable code, but it is core to the loop algorithm because it defines the worker’s required transition from “comments available” to “fixes pushed, bots re-triggered, resolution summary written, then stop hook polls for approval.” The setup script selects it only for the has-comments branch at `scripts/setup-pr-loop.sh:686` and renders it with `load_and_render_safe` at `scripts/setup-pr-loop.sh:767`.

- algorithmic_behavior: The file imposes a four-step workflow. First, it requires analysis of the already-appended review comments, with human comments taking precedence and bot comments ordered newest first at `prompt-template/pr-loop/round-0-task-has-comments.md:6`. Second, it requires issue remediation by reading relevant code, applying changes, and adding tests where needed at `prompt-template/pr-loop/round-0-task-has-comments.md:10`. Third, it requires commit, push, and an explicit PR trigger comment using rendered `{{PR_NUMBER}}` and `{{BOT_MENTION_STRING}}` placeholders at `prompt-template/pr-loop/round-0-task-has-comments.md:15`. Fourth, it requires a resolution summary at rendered `@{{RESOLVE_PATH}}` with addressed issues, modified files, and tests at `prompt-template/pr-loop/round-0-task-has-comments.md:23`.

- inputs_outputs_state: Inputs are the PR comments appended before this task section into `round-0-prompt.md`; setup writes the header, appends `$COMMENT_FILE`, then appends this task content at `scripts/setup-pr-loop.sh:680`, `scripts/setup-pr-loop.sh:684`, and `scripts/setup-pr-loop.sh:771`. The has-comments branch is chosen by checking whether `round-0-pr-comment.md` lacks the exact sentinel `*No comments found.*`; any non-sentinel content makes `COMMENT_COUNT=1` at `scripts/setup-pr-loop.sh:643`. Template variables used directly by this file are `PR_NUMBER`, `BOT_MENTION_STRING`, and `RESOLVE_PATH`, defined in `TEMPLATE_VARS` at `scripts/setup-pr-loop.sh:651`. Outputs expected by the contract are code/test changes, a descriptive commit, pushed remote branch, PR comment trigger, and a resolution summary at `$LOOP_DIR/round-0-pr-resolve.md`, initialized as `RESOLVE_PATH` at `scripts/setup-pr-loop.sh:641`. State transition is: active PR loop with existing comments -> worker resolves issues -> local git clean and pushed -> trigger comment present when required -> stop hook polls -> either feedback continues the loop or all bot approvals end it, as summarized in `prompt-template/pr-loop/round-0-task-has-comments.md:39`.

- gates_or_invariants: The template’s invariants are stated in “Important Rules”: do not modify `.humanize/pr-loop/` state files, always push fixes, use the correct bot mention format, and address all valid concerns at `prompt-template/pr-loop/round-0-task-has-comments.md:30`. Hook code enforces several of these. The PR stop hook blocks if the resolution summary file is missing at `hooks/pr-loop-stop-hook.sh:262`, blocks on uncommitted non-`.humanize` changes at `hooks/pr-loop-stop-hook.sh:296`, and blocks when local commits are ahead of remote at `hooks/pr-loop-stop-hook.sh:350`. Trigger-comment validation is conditional, but when required the hook blocks if no bot mention trigger exists at `hooks/pr-loop-stop-hook.sh:717`. PR loop state and prompt/comment/check files are protected by path validators: read-only PR loop files include `pr-comment`, `prompt`, `codex-prompt`, `pr-check`, and `pr-feedback` at `hooks/lib/loop-common.sh:825`, while writes to the current round’s `pr-resolve` are allowed after round validation at `hooks/loop-write-validator.sh:89` and `hooks/loop-edit-validator.sh:72`.

- dependencies_and_callers: Direct caller is `scripts/setup-pr-loop.sh`, which loads the template from `prompt-template/pr-loop/round-0-task-has-comments.md` as the nonzero-comment task body at `scripts/setup-pr-loop.sh:767`. Rendering depends on `hooks/lib/template-loader.sh`; its `render_template` function performs single-pass placeholder replacement to prevent injected `{{OTHER_VAR}}` values from being re-expanded at `hooks/lib/template-loader.sh:54`, and `load_and_render_safe` falls back if the template is missing or empty at `hooks/lib/template-loader.sh:170`. Upstream comment content is produced by `scripts/fetch-pr-comments.sh`, whose documented output includes issue comments, review comments, and PR reviews at `scripts/fetch-pr-comments.sh:5`, sorted with human comments before bot comments at `scripts/fetch-pr-comments.sh:68`. The stop-hook consumer is `hooks/pr-loop-stop-hook.sh`, which uses the resolution file and git/PR state to decide whether to block, poll, continue, or finish.

- edge_cases_or_failure_modes: If the comment file contains anything other than the exact no-comments sentinel, the setup script treats it as “has comments,” so malformed or placeholder comment content can route to this remediation workflow at `scripts/setup-pr-loop.sh:643`. If `{{BOT_MENTION_STRING}}` is empty or wrong because active bot flags were misconfigured, the template still renders a trigger command, but the stop hook may fail to find the expected mention later. If the worker writes the summary to a wrong round file, `validate_pr_resolve_round` blocks and points to the correct current-round path at `hooks/lib/loop-common.sh:837`. If the worker fixes code but leaves uncommitted changes or unpushed commits, the stop hook blocks before polling at `hooks/pr-loop-stop-hook.sh:299` and `hooks/pr-loop-stop-hook.sh:350`. If a new trigger is required and missing, polling is blocked at `hooks/pr-loop-stop-hook.sh:717`. For Claude, when trigger verification is required, missing eyes reaction also blocks at `hooks/pr-loop-stop-hook.sh:757`.

- validation_or_tests: This specific markdown template has no direct unit test file that asserts its exact content, but it is covered indirectly by template reference and loader tests. The setup path references `round-0-task-has-comments.md` via `load_and_render_safe`, and template reference tests scan those calls in `tests/test-template-references.sh` according to the repository search evidence. Placeholder rendering behavior is covered by `tests/test-template-loader.sh` and robustness/stress template-loader tests found by `rg`, including cases for special characters, unreplaced variables, and fallback rendering. PR-loop behavioral gates around `round-0-pr-resolve.md` are tested in `tests/test-pr-loop-hooks.sh`, including allowing correct `pr-resolve` writes and blocking wrong-round writes, and stop-hook tests create `round-0-pr-resolve.md` fixtures in `tests/test-pr-loop-stophook.sh`.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-125`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`