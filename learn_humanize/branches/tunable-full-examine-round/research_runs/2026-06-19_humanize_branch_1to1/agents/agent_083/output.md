# agent_083 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-083 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: This file is a user-facing block-message template for the RLCR loop Bash pre-tool validator. Its role is to explain why `git push` is disallowed during normal loop operation and to point the operator to the opt-in mode that permits per-round pushes. The template itself is small markdown content, but it participates in an algorithmic gate: default loop state keeps commits local unless the loop was started with `--push-every-round`. See `prompt-template/block/git-push.md:1-9`.
- algorithmic_behavior: The template has three behavioral sections: a title, `# Git Push Blocked`, at `prompt-template/block/git-push.md:1`; a policy explanation that commits should remain local and the loop handles local commits until completion at `prompt-template/block/git-push.md:3-4`; and the override instruction showing `/humanize:start-rlcr-loop plan.md --push-every-round` at `prompt-template/block/git-push.md:6-9`. The actual branching logic lives in `hooks/loop-bash-validator.sh`: Bash hook input is parsed from JSON, the command is lowered, and active loop directories are detected at `hooks/loop-bash-validator.sh:24-67`. For active RLCR loops, strict state parsing occurs at `hooks/loop-bash-validator.sh:81-85`; then `STATE_PUSH_EVERY_ROUND` is copied into `PUSH_EVERY_ROUND` at `hooks/loop-bash-validator.sh:93-96`. If that value is not `true` and the command regex matches leading `git push`, the hook renders this template and exits with block code `2` at `hooks/loop-bash-validator.sh:96-104`.
- inputs_outputs_state: The template has no `{{VARIABLE}}` placeholders and takes no direct render variables. Its input is indirect: an active RLCR loop state file field `push_every_round`, plus a Bash tool command string. The loop setup script initializes `PUSH_EVERY_ROUND="false"` at `scripts/setup-rlcr-loop.sh:42`, exposes `--push-every-round` in help at `scripts/setup-rlcr-loop.sh:67`, flips the variable to `true` when that flag is parsed at `scripts/setup-rlcr-loop.sh:164-166`, and writes `push_every_round: $PUSH_EVERY_ROUND` into loop state at `scripts/setup-rlcr-loop.sh:709-717`. The state parser reads that field at `hooks/lib/loop-common.sh:227` and defaults missing values to `false` at `hooks/lib/loop-common.sh:237-240`. Output is markdown sent to stderr by `load_and_render_safe "$TEMPLATE_DIR" "block/git-push.md" "$FALLBACK" >&2` in `hooks/loop-bash-validator.sh:99-104`, followed by a blocking exit. The state transition controlled by the broader system is therefore: default loop startup creates a no-push state; `git push` attempts in that state are blocked with this template; startup with `--push-every-round` creates a push-required state where this block template is skipped and companion stop-hook logic instead checks for unpushed commits.
- gates_or_invariants: The key invariant is “commits stay local by default.” The README documents `--push-every-round` as requiring git push after each round, while default behavior keeps commits local at `README.md:104-113`. The validator enforces this as a pre-command gate only when an RLCR loop is active; outside active loops, it exits without blocking at `hooks/loop-bash-validator.sh:64-67`. The regex only gates commands beginning with optional whitespace, `git`, whitespace, then `push` after lowercasing at `hooks/loop-bash-validator.sh:47-48` and `hooks/loop-bash-validator.sh:96-104`. A malformed state file is fail-closed before the push gate, blocking for safety at `hooks/loop-bash-validator.sh:81-84`. Template rendering has a fallback invariant: if the real template is missing or empty, `load_and_render_safe` emits the fallback message instead of failing silently, implemented at `hooks/lib/template-loader.sh:170-203`.
- dependencies_and_callers: Primary caller is `hooks/loop-bash-validator.sh`, specifically the RLCR Bash PreToolUse path at `hooks/loop-bash-validator.sh:70-107`. It depends on `hooks/lib/loop-common.sh` for active loop discovery and strict state parsing, including the `push_every_round` field parsed/defaulted at `hooks/lib/loop-common.sh:227-240`. It depends on `hooks/lib/template-loader.sh` for `load_and_render_safe`; plain template loading is `cat`-based at `hooks/lib/template-loader.sh:36-47`, and safe fallback rendering is at `hooks/lib/template-loader.sh:170-203`. The setup-side producer of the relevant state is `scripts/setup-rlcr-loop.sh`, with CLI option and state write references at `scripts/setup-rlcr-loop.sh:36-42`, `scripts/setup-rlcr-loop.sh:59-68`, `scripts/setup-rlcr-loop.sh:164-166`, and `scripts/setup-rlcr-loop.sh:709-717`. Companion behavior when pushing is enabled appears in `hooks/loop-codex-stop-hook.sh:572-585`, which checks for local commits ahead of remote only when `PUSH_EVERY_ROUND` is true, and in `prompt-template/block/unpushed-commits.md:1-12`.
- edge_cases_or_failure_modes: Because the command detector is anchored to the beginning of the Bash command, direct commands like `git push` are blocked, but wrapped or sequenced forms that do not begin with `git push` may not be caught by this particular regex. The template text says “Current commits should stay local - no need to push to remote,” which is accurate for the default RLCR mode but could be misleading if invoked in an unusual environment where the state file was stale or incorrectly generated. If `prompt-template/block/git-push.md` is missing, the fallback still blocks and reports a shorter message from `hooks/loop-bash-validator.sh:99-104`, so loss of this file degrades UX rather than disabling the gate. If `push_every_round` is omitted from old state, parser defaulting to `false` at `hooks/lib/loop-common.sh:237-240` makes the conservative behavior block pushes. If strict state parsing fails, the hook blocks earlier with a malformed-state error and this template is not rendered.
- validation_or_tests: Template-loading coverage exists. `tests/test-template-loader.sh:55-65` verifies `load_template` can load `block/git-push.md` and that its content contains `Git Push Blocked`. `tests/test-template-loader.sh:211-222` verifies `load_and_render_safe` prefers the real template over fallback for this file. `tests/test-templates-comprehensive.sh:455-463` repeats the safe-render path, checking that the real template appears and fallback text does not. I did not find a direct test that simulates a Bash hook input of `git push` with `push_every_round: false`; the behavioral gate is covered by inspection of `hooks/loop-bash-validator.sh` rather than by a named test discovered in the targeted search.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1; the single assigned item appears in exactly one Item Evidence heading
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`