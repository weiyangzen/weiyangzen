# agent_104 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-104 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: This is a guard-message template for stale RLCR round-file reads. It supports the read validator path that prevents an agent from reading an obsolete `round-N-prompt.md` or `round-N-summary.md` when the active loop has advanced to a different round. The template itself is not the gate implementation; it is the human/agent-facing block text rendered when the gate trips.
- algorithmic_behavior: The template declares the failure class as `Wrong Round File`, identifies the attempted file as `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md`, states the active `{{CURRENT_ROUND}}`, lists the current prompt and summary files under `{{ACTIVE_LOOP_DIR}}`, and gives a direct read command for `{{FILE_PATH}}` if that exact blocked file is still needed. See `prompt-template/block/wrong-round-file.md:1`, `prompt-template/block/wrong-round-file.md:3`, `prompt-template/block/wrong-round-file.md:5`, and `prompt-template/block/wrong-round-file.md:9`.
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-read-validator.sh`: `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH` at `hooks/loop-read-validator.sh:144` to `hooks/loop-read-validator.sh:149`. Output is rendered Markdown written to stderr, followed by validator exit code 2 at `hooks/loop-read-validator.sh:144` to `hooks/loop-read-validator.sh:150`. The template does not mutate repository state; the state transition is hook-level: a `Read` request for a stale round prompt/summary is converted into a blocked tool operation.
- gates_or_invariants: The enclosing read gate only applies to `Read` tool calls with filenames matching round prompt or summary patterns; non-`Read` tools exit early, and non-round prompt/summary paths exit early at `hooks/loop-read-validator.sh:39` to `hooks/loop-read-validator.sh:70`. The current round comes from strict state parsing; malformed state blocks safely at `hooks/loop-read-validator.sh:94` to `hooks/loop-read-validator.sh:99`. The wrong-round condition is `CLAUDE_ROUND != CURRENT_ROUND` plus not allowlisted at `hooks/loop-read-validator.sh:138`. Allowlisted historical files bypass this block via `is_allowlisted_file`, whose fixed allowlist includes early todos and summaries at `hooks/lib/loop-common.sh:423` to `hooks/lib/loop-common.sh:440`.
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh:144`, via `load_and_render_safe`. Rendering behavior comes from `hooks/lib/template-loader.sh`: placeholders use `{{VARIABLE_NAME}}` syntax and missing variables are preserved by design at `hooks/lib/template-loader.sh:7` to `hooks/lib/template-loader.sh:13`; rendering is single-pass to avoid placeholder injection at `hooks/lib/template-loader.sh:50` to `hooks/lib/template-loader.sh:131`; missing or empty templates fall back to inline text at `hooks/lib/template-loader.sh:170` to `hooks/lib/template-loader.sh:203`. Round detection depends on `is_round_file_type` and `extract_round_number` in `hooks/lib/loop-common.sh:399` to `hooks/lib/loop-common.sh:418`.
- edge_cases_or_failure_modes: If no active loop exists, stale-round validation is skipped at `hooks/loop-read-validator.sh:80` to `hooks/loop-read-validator.sh:85`. If `finalize-state.md` exists, it replaces `state.md` as the current-round source at `hooks/loop-read-validator.sh:88` to `hooks/loop-read-validator.sh:92`. If the filename lacks an extractable round number, the hook exits without blocking at `hooks/loop-read-validator.sh:105` to `hooks/loop-read-validator.sh:107`. If the template file is missing or renders empty, `load_and_render_safe` uses the inline fallback, so the gate still blocks with exit code 2. A wording edge case exists: the template’s final line says `cat {{FILE_PATH}}`, while the block context is that normal tool `Read` access is being denied; this appears intended as a manual explicit override hint, not an allow decision.
- validation_or_tests: I found no direct test that renders `block/wrong-round-file.md` by name. Related coverage exists for the read-validator behavior: `tests/test-allowlist-validators.sh:273` to `tests/test-allowlist-validators.sh:284` verifies reading a non-allowlisted stale summary exits 2 and emits a round-related message. Template rendering infrastructure is tested with adjacent wrong-round templates, for example `tests/test-template-loader.sh:151` to `tests/test-template-loader.sh:164` and `tests/test-templates-comprehensive.sh:498` to `tests/test-templates-comprehensive.sh:510`. `rg` shows `wrong-round-file.md` is referenced only by `hooks/loop-read-validator.sh`, so its behavior is tightly scoped to stale prompt/summary reads.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence section present for the single assigned item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`