# agent_071 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-071 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/check-todos-from-transcript.py`, the fast todo-completion gate used by `hooks/loop-codex-stop-hook.sh` before expensive review work. The test file is executable (`-rwxr-xr-x`) and is included in the aggregate suite at `tests/run-all-tests.sh:32-37`. Its purpose is not production logic, but it defines the expected algorithmic contract for transcript parsing, todo state interpretation, and stop-hook blocking behavior.
- algorithmic_behavior: The script runs 16 shell-level cases against `python3 "$TODO_CHECKER"` after resolving `TODO_CHECKER="$PROJECT_ROOT/hooks/check-todos-from-transcript.py"` at `tests/test-todo-checker.sh:11-13`. It uses `set -euo pipefail` globally but temporarily disables `errexit` around expected failures with `set +e` and captures both `RESULT` and `EXIT_CODE` for each checker invocation, for example at `tests/test-todo-checker.sh:53-56`, `126-129`, and `254-257`. It asserts that malformed hook input exits `2`, missing or unusable transcript context exits `0`, latest completed todos exit `0`, and any todo whose `status` is not exactly `completed` exits `1`. It also specifies transcript scanning semantics: invalid JSONL transcript lines are skipped, multiple `TodoWrite` calls use the latest non-empty todos list, and three transcript shapes are accepted: Claude assistant message content blocks, direct `tool_use` entries, and `type: message` entries.
- inputs_outputs_state: Inputs are synthetic hook JSON on stdin plus temporary JSONL transcript files created under `TEST_DIR=$(mktemp -d)` at `tests/test-todo-checker.sh:36-38`. The test writes transcript fixtures with here-docs for completed todos (`108-110`), pending todos (`123-125`), in-progress todos (`146-148`), invalid JSONL mixed with a valid entry (`181-185`), multiple `TodoWrite` calls (`198-201`), direct tool-use format (`214-216`), alternate message format (`229-231`), missing status (`251-253`), empty content (`267-269`), and unicode-labeled content (`282-284`). Outputs are human-readable PASS/FAIL lines from helper functions at `23-34`, counters in `TESTS_PASSED` and `TESTS_FAILED` at `20-21`, and final process status: `0` only when no failures were recorded, `1` otherwise at `tests/test-todo-checker.sh:305-313`. The checker under test returns `0` for no incomplete todos, `1` for incomplete todos, and `2` for hook-input parse errors as documented in `hooks/check-todos-from-transcript.py:7-10`.
- gates_or_invariants: The central invariant is strict completion: only `status == "completed"` is accepted; pending, in-progress, missing status, and empty-content pending todos all block. The corresponding production loop consumes this contract in `hooks/loop-codex-stop-hook.sh:345-393`: exit `2` produces a parse-error block, exit `1` extracts incomplete todo details and returns a block decision without invoking review. The checker’s parser only updates `latest_todos` when a `TodoWrite` input contains a truthy todos array, so empty or absent todos are treated as no gate-relevant state and allow exit (`hooks/check-todos-from-transcript.py:61-63`, `74-76`, `83-85`, `114-116`). The test also establishes a fail-open posture for missing transcript context: empty stdin, no `transcript_path`, and nonexistent transcript file must all return `0` (`tests/test-todo-checker.sh:63-97`).
- dependencies_and_callers: Direct dependency is `python3` plus `hooks/check-todos-from-transcript.py`. The checker uses Python stdlib `json`, `sys`, and `pathlib.Path` (`hooks/check-todos-from-transcript.py:15-17`). Production caller is `hooks/loop-codex-stop-hook.sh`, which invokes the checker with the full hook input and interprets exit codes before Codex review (`hooks/loop-codex-stop-hook.sh:339-393`). The block text for incomplete todos comes from `prompt-template/block/incomplete-todos.md`, which renders `{{INCOMPLETE_LIST}}` and instructs users to finish TodoWrite tasks (`prompt-template/block/incomplete-todos.md:1-12`). Broader integration coverage exists in `tests/test-finalize-phase.sh:715-740`, which creates an incomplete todo transcript and verifies the stop hook blocks and does not call Codex. The aggregate runner includes this file as the third test suite (`tests/run-all-tests.sh:32-37`).
- edge_cases_or_failure_modes: Invalid hook JSON must be a hard parse error with exit `2` (`tests/test-todo-checker.sh:51-61`), while invalid transcript JSONL lines are ignored rather than failing the whole check (`179-194`). A transcript with no lines exits `0` (`166-177`). Nonexistent transcript paths exit `0` because `find_latest_todos` returns an empty list when the file does not exist (`hooks/check-todos-from-transcript.py:25-26`). Multiple TodoWrite calls intentionally discard earlier states in favor of the latest non-empty todo set (`tests/test-todo-checker.sh:196-210`), which means an old pending todo no longer blocks if a later TodoWrite reports all completed. Missing todo status is incomplete because the checker defaults `status` to an empty string and compares it against `completed` (`hooks/check-todos-from-transcript.py:120-124`; tested at `tests/test-todo-checker.sh:249-263`). Empty todo content still blocks when its status is incomplete (`tests/test-todo-checker.sh:265-278`). One notable test-label mismatch: Test 16 is named “Unicode in todo content,” but the fixture content is plain ASCII text `"Task with unicode"` at `tests/test-todo-checker.sh:280-284`; it still verifies ordinary UTF-8 file handling through the checker’s `encoding='utf-8'` open path at `hooks/check-todos-from-transcript.py:30`.
- validation_or_tests: The file is itself the validation harness. It creates isolated temp fixtures, traps cleanup with `rm -rf "$TEST_DIR"` at `tests/test-todo-checker.sh:36-38`, executes the checker per scenario, and records pass/fail counts. It validates input handling, todo detection, transcript format variations, and edge cases across four named groups (`tests/test-todo-checker.sh:45-47`, `99-101`, `159-160`, `242-243`). The final summary prints passed/failed totals and exits nonzero if any assertion failed (`295-313`). I did not run the test script because the assignment requested research notes only and the branch export is read-only; inspection was limited to file reads and related references.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1; the Item Evidence section contains the sole literal assigned item id`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`