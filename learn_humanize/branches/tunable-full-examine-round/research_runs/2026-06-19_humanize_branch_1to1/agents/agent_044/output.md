# agent_044 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-044 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Top-level regression-suite orchestrator for the Humanize plugin. It is not the product algorithm itself; it is the executable validation gate that serializes the repository’s core behavior tests and converts their individual pass/fail summaries into one aggregate exit status. The file declares its contract directly: usage is `./tests/run-all-tests.sh`, success exits `0`, failure exits `1` at `tests/run-all-tests.sh:3-9`.

- algorithmic_behavior: The runner resolves its own directory from `BASH_SOURCE` so it can be invoked from any working directory (`tests/run-all-tests.sh:14`), initializes aggregate counters and a failed-suite array (`tests/run-all-tests.sh:28-30`), then iterates a fixed ordered suite list (`tests/run-all-tests.sh:33-73`). For each suite, it builds `suite_path`, skips missing files (`tests/run-all-tests.sh:80-86`), checks whether the suite must run under zsh (`tests/run-all-tests.sh:88-95`), captures combined stdout/stderr and exit status (`tests/run-all-tests.sh:104-118`), strips ANSI SGR color codes (`tests/run-all-tests.sh:121-123`), extracts the last `Passed:` and `Failed:` numeric summaries (`tests/run-all-tests.sh:124-125`), adds those counts to totals (`tests/run-all-tests.sh:127-128`), and classifies the suite as failed if either its process exit status is nonzero or its parsed failed count is greater than zero (`tests/run-all-tests.sh:130-137`). Failed suites print only the last 30 output lines for diagnostics (`tests/run-all-tests.sh:133-134`). Final status is based on whether `FAILED_SUITES` is nonempty (`tests/run-all-tests.sh:148-159`).

- inputs_outputs_state: Inputs are the hard-coded suite inventory, the executable test files under `tests/` and `tests/robustness/`, the optional `zsh` binary for the zsh-specific monitor safety test, and standard Unix tools including `sed`, `grep`, and `tail`. The runner accepts no meaningful CLI arguments despite inheriting shell environment from the caller. Outputs are colored console progress, aggregate `Total Passed` / `Total Failed` lines (`tests/run-all-tests.sh:141-146`), a failed-suite listing when applicable (`tests/run-all-tests.sh:148-153`), and process exit `0` or `1` (`tests/run-all-tests.sh:155-158`). Internal state transitions are in-memory only: `TOTAL_PASSED`, `TOTAL_FAILED`, and `FAILED_SUITES` start empty/zero, are updated after each captured suite, and are discarded at process exit. The runner itself does not persist files, though child suites may create temporary repositories, fixtures, or mock state.

- gates_or_invariants: The first invariant is ordered execution: suites always run in the array order at `tests/run-all-tests.sh:33-73`, including root suites first and robustness suites afterward. The second invariant is summary-label compatibility: aggregate counts depend on exact `Passed:` and `Failed:` labels after ANSI stripping (`tests/run-all-tests.sh:121-125`). The third invariant is dual failure detection: a suite fails the aggregate if it exits nonzero or reports a parsed failed count greater than zero (`tests/run-all-tests.sh:130`). Missing suite files are explicitly skips, not failures (`tests/run-all-tests.sh:83-85`). The zsh suite is also skipped rather than failed when `zsh` is unavailable (`tests/run-all-tests.sh:106-113`). Non-zsh tests are invoked directly via their path, not through `bash`, so executable bits and shebangs are part of the gate (`tests/run-all-tests.sh:116`). The script starts with `set -uo pipefail` (`tests/run-all-tests.sh:12`) and toggles `set +e` around child execution to capture failing exit codes without aborting (`tests/run-all-tests.sh:105-119`).

- dependencies_and_callers: Direct suite dependencies are the named shell scripts in `TEST_SUITES`, covering template loading, bash validator patterns, todo checking, plan-file validation/hooks, template reference/comprehensive tests, state naming, error scenarios, ANSI parsing, allowlist validators, finalize phase, Codex review merge, cancel signal handling, humanize escape handling, monitor runtime/e2e behavior, generated plans, PR loop tests, and robustness tests (`tests/run-all-tests.sh:33-73`). The zsh interpreter dependency applies only to `test-zsh-monitor-safety.sh` (`tests/run-all-tests.sh:75-78`). The aggregate parser aligns with the shared helper’s `print_test_summary`, which emits `Passed:` and `Failed:` and returns failure when `TESTS_FAILED` is nonzero (`tests/test-helpers.sh:58-77`). The ANSI parsing behavior is itself covered by `tests/test-ansi-parsing.sh`, which explicitly tests the same portable `sed` stripping and count extraction patterns (`tests/test-ansi-parsing.sh:38-125`). Repository search found no direct workflow or script caller for `tests/run-all-tests.sh` beyond comments, so this appears to be a manual/local all-suite gate rather than a CI entry point.

- edge_cases_or_failure_modes: If a suite is absent, it is silently excluded from aggregate success/failure aside from a `SKIP` line, so an incomplete checkout could still exit success if all listed suites were missing. If `zsh` is unavailable, the zsh-only safety suite is skipped and does not affect totals. Any non-zsh test lacking executable permission or a valid shebang will fail at direct invocation. The parser only recognizes summaries shaped like `Passed:` and `Failed:`; `tests/test-pr-loop-system.sh` emits `Tests passed:` and `Tests failed:` at `tests/test-pr-loop-system.sh:1887-1889`, so this runner can undercount that suite even though a nonzero exit still marks it failed. Conversely, a suite with nonstandard failure wording and exit `0` could be reported as passed with `0` parsed tests. The ANSI stripper handles CSI SGR sequences ending in `m`, not arbitrary terminal control codes. Capturing each entire suite output into a shell variable may be memory-heavy for very verbose suites, and failure diagnostics show only the last 30 lines, which can omit the root cause.

- validation_or_tests: Static validation was run with `bash -n tests/run-all-tests.sh` and returned `syntax-ok`. The full all-suite runner was not executed during this research pass because the assignment requested read-only evidence and the child suites may perform broader filesystem/git/mock side effects. Behavioral validation references inspected include `tests/test-ansi-parsing.sh`, which tests the exact parsing strategy used by the runner (`tests/test-ansi-parsing.sh:5-6`, `tests/test-ansi-parsing.sh:38-125`), and `tests/test-helpers.sh`, which defines the standard suite summary contract consumed by the aggregate runner (`tests/test-helpers.sh:58-77`).

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 Item Evidence section present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`