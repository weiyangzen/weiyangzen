# agent_033 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-033 `file` `scripts/cancel-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Runtime cancellation entrypoint for an active RLCR loop. The script performs the sanctioned state transition from an active loop state file to `cancel-state.md`, with a separate confirmation gate for Finalize Phase cancellation. It is exposed by the slash-command wrapper at [commands/cancel-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/commands/cancel-rlcr-loop.md:11), whose key contract is that active RLCR loops are represented by `state.md` or `finalize-state.md` in the newest `.humanize/rlcr/` session directory.
- algorithmic_behavior: The script first parses only `--force` and help flags, treating any other argument as an error with exit code `3` at [scripts/cancel-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/cancel-rlcr-loop.sh:26). It resolves the project root from `CLAUDE_PROJECT_DIR` or `pwd`, targets `.humanize/rlcr`, then selects the newest child directory using reverse lexical sort at [scripts/cancel-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/cancel-rlcr-loop.sh:69). It classifies the loop as normal when `state.md` exists and Finalize Phase when `finalize-state.md` exists, preferring normal state if both files are present at [scripts/cancel-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/cancel-rlcr-loop.sh:85). It extracts `current_round` and `max_iterations` using line-anchored `grep` plus `sed`, defaulting missing values to `?` at [scripts/cancel-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/cancel-rlcr-loop.sh:105). If Finalize Phase is detected without `--force`, it emits `FINALIZE_NEEDS_CONFIRM` and exits `2`; otherwise it creates `.cancel-requested`, moves the active state file to `cancel-state.md`, and emits either `CANCELLED` or `CANCELLED_FINALIZE` at [scripts/cancel-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/cancel-rlcr-loop.sh:117) and [scripts/cancel-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/cancel-rlcr-loop.sh:134).
- inputs_outputs_state: Inputs are CLI flags, `CLAUDE_PROJECT_DIR`, the filesystem layout under `.humanize/rlcr`, and the active state file frontmatter fields `current_round` and `max_iterations`. Outputs are status tokens on stdout, user-facing explanatory lines, exit codes `0`, `1`, `2`, or `3`, a created cancellation signal file at `.cancel-requested`, and an atomic-ish state rename to `cancel-state.md`. The important state transition is `state.md -> cancel-state.md` for a normal loop, or `finalize-state.md -> cancel-state.md` when forced during Finalize Phase. State production originates in setup, which creates `state.md` with round, iteration, model, plan, branch, and review metadata at [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/setup-rlcr-loop.sh:709). Finalize state is produced by the stop hook via `state.md -> finalize-state.md` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-codex-stop-hook.sh:1030), and successful Finalize Phase completion moves it to `complete-state.md` at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-codex-stop-hook.sh:742).
- gates_or_invariants: The script uses `set -euo pipefail` at [scripts/cancel-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/cancel-rlcr-loop.sh:18), so failed `touch`, `mv`, or unset-variable misuse aborts. It refuses to cancel when no newest session directory exists, returning `NO_LOOP`, and refuses when the newest directory lacks both active state files, returning `NO_ACTIVE_LOOP` at [scripts/cancel-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/cancel-rlcr-loop.sh:75) and [scripts/cancel-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/cancel-rlcr-loop.sh:89). The Finalize Phase gate is deliberately two-step: first return `FINALIZE_NEEDS_CONFIRM`, then allow cancellation only via `--force` after the wrapper asks the user at [commands/cancel-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/commands/cancel-rlcr-loop.md:23). The `.cancel-requested` file is also an invariant for hook compatibility: the bash validator blocks arbitrary `state.md` and `finalize-state.md` mutations but allows the exact cancel rename only when `is_cancel_authorized` sees the signal file and validates source, destination, argument count, quoting, shell operators, substitutions, and symlink status at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/loop-common.sh:533) and [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/loop-common.sh:681). This explains why the script creates the signal before running `mv`.
- dependencies_and_callers: Direct caller is `/humanize:cancel-rlcr-loop`, defined in [commands/cancel-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/commands/cancel-rlcr-loop.md:1). The script duplicates the active-loop lookup logic that `find_active_loop` centralizes in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/loop-common.sh:151), where only the newest loop directory is considered active and only `state.md` or `finalize-state.md` count as active indicators. It coordinates with state protection in [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-bash-validator.sh:125), which treats normal and finalize state files as protected except for an authorized cancel rename. Monitor code recognizes terminal `*-state.md` files, including `cancel-state.md`, as non-active session status at [scripts/lib/monitor-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/lib/monitor-common.sh:147). `scripts/cancel-pr-loop.sh` is a sibling implementation for PR loops with the same signal-plus-rename shape but no Finalize Phase gate at [scripts/cancel-pr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/cancel-pr-loop.sh:72).
- edge_cases_or_failure_modes: Newest-directory selection is lexical and checks only the newest child, so an older directory with `state.md` is ignored if a newer directory exists without an active state file; this mirrors `find_active_loop`’s anti-zombie behavior. Directory names containing spaces are likely fragile because `LOOP_DIR=$(ls -1d ... | sort -r | head -1)` captures a trailing slash and later interpolates paths, though state-file operations are quoted after assignment. If both `state.md` and `finalize-state.md` exist, this script selects `state.md` because it checks it first, while the stop hook prefers `finalize-state.md` during phase detection at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-codex-stop-hook.sh:72); that is a rare inconsistent-state edge. If `touch` succeeds and `mv` fails, `.cancel-requested` remains without the terminal rename, leaving a stale authorization signal. Existing `cancel-state.md` would be overwritten by `mv` behavior unless the platform refuses it due permissions. The parser for round metadata is tolerant and not YAML-aware, so quoted, duplicated, or indented fields may produce surprising display values, but cancellation itself does not depend on those fields.
- validation_or_tests: Relevant tests cover the cancellation authorization mechanism rather than this exact script end-to-end. `tests/test-cancel-signal-file.sh` asserts that `mv state.md cancel-state.md` is allowed only when `.cancel-requested` exists, that a wrong-directory signal does not authorize cancellation, and that other state mutations remain blocked at [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-cancel-signal-file.sh:1). The same test verifies literal loop-dir variable normalization through the validator at [tests/test-cancel-signal-file.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-cancel-signal-file.sh:442). Finalize Phase tests assert `finalize-state.md` is active, `complete-state.md` is non-active, and COMPLETE transitions into Finalize Phase at [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-finalize-phase.sh:283) and [tests/test-finalize-phase.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-finalize-phase.sh:524). State-exit naming tests assert `cancel-state.md` is terminal and not detected as active at [tests/test-state-exit-naming.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-state-exit-naming.sh:97), and `end_loop` supports the same `cancel-state.md` naming convention at [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/lib/loop-common.sh:1112). I did not run the test suite because this worker task is read-only research; a `git status` probe also hit macOS sandbox cache-write denial under `/tmp`, so I avoided further commands that might write implicit tool caches.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`