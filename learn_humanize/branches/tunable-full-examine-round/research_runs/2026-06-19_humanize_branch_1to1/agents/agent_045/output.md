# agent_045 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-045 `file` `tests/setup-fixture-mock-gh.sh`
- cursor: `[_]`
- core_role: This file is an executable test fixture generator for the PR-loop review/comment ingestion path. Its role is to synthesize a temporary `gh` CLI binary that returns deterministic GitHub API data, allowing `scripts/fetch-pr-comments.sh` and `scripts/poll-pr-reviews.sh` behavior to be tested without network or real GitHub state. Although it is test code, it is a core executable specification for the branch behavior around issue comments, inline review comments, and approval-only PR reviews.

- algorithmic_behavior: The script starts in strict shell mode with `set -euo pipefail` at `tests/setup-fixture-mock-gh.sh:14`, reads two positional inputs at `tests/setup-fixture-mock-gh.sh:16-17`, validates both are present at `tests/setup-fixture-mock-gh.sh:19-22`, creates the mock binary directory at `tests/setup-fixture-mock-gh.sh:24`, then writes a new bash script to `$MOCK_BIN_DIR/gh` through a heredoc at `tests/setup-fixture-mock-gh.sh:27-100`. The generated mock dispatches on the first `gh` subcommand: `auth status` returns a successful login marker at `tests/setup-fixture-mock-gh.sh:33-39`; `repo view` returns fixed repo metadata for owner/name/parent queries at `tests/setup-fixture-mock-gh.sh:40-53`; `pr view` returns fixed base repository, number, or state data at `tests/setup-fixture-mock-gh.sh:54-66`; `api user` returns `testuser` at `tests/setup-fixture-mock-gh.sh:67-72`; three PR-loop endpoints are routed to fixture files at `tests/setup-fixture-mock-gh.sh:74-90`; other `gh api` calls return an empty JSON array at `tests/setup-fixture-mock-gh.sh:92-94`; non-API unhandled commands fail with stderr and exit 1 at `tests/setup-fixture-mock-gh.sh:98-99`. The wrapper then marks the generated `gh` executable and prints the mock bin directory at `tests/setup-fixture-mock-gh.sh:102-104`.

- inputs_outputs_state: Inputs are `<mock_bin_dir>` and `<fixtures_dir>` from the command line. The expected fixture files are `issue_comments.json`, `review_comments.json`, and `pr_reviews.json`, matching the underscore-named files under `tests/fixtures/`. The durable output is `$MOCK_BIN_DIR/gh`; stdout is the mock bin directory path. The main state transition is filesystem setup: absent or stale mock path to directory present, generated `gh` overwritten, executable bit set. Runtime state is stateless and derived only from CLI arguments plus fixture file contents. The generated mock maps GitHub endpoints to fixture payloads consumed by `fetch-pr-comments.sh` endpoint fetches at `scripts/fetch-pr-comments.sh:209-219` and `poll-pr-reviews.sh` endpoint fetches at `scripts/poll-pr-reviews.sh:239-278`.

- gates_or_invariants: The outer script requires both arguments and exits with usage when either is missing at `tests/setup-fixture-mock-gh.sh:19-22`. It assumes write permission to the supplied mock directory and generated file path. The generated mock assumes callers invoke `gh` with the subcommand in `$1` and endpoint in `$2` for `gh api`. It intentionally recognizes only the PR-loop command subset and fails closed for non-API unknown commands at `tests/setup-fixture-mock-gh.sh:98-99`, while unknown API endpoints succeed with `[]` at `tests/setup-fixture-mock-gh.sh:92-94`. It does not implement real `gh -q` or `--jq` filtering, pagination behavior, auth semantics beyond `auth status`, or posting/comment mutation commands. Its fixture routing invariant is endpoint-shape based: any `$2` containing `/issues/` and `/comments` returns issue comments, any `/pulls/` and `/comments` returns inline review comments, and any `/pulls/` and `/reviews` returns PR reviews.

- dependencies_and_callers: The direct caller found is `setup_fixture_mock_gh` in `tests/test-pr-loop-hooks.sh:1231-1240`, which invokes this script with `$TEST_DIR/mock_bin` and `$SCRIPT_DIR/fixtures`, then prepends that directory to `PATH`. The generated mock is used by four fixture-backed tests: `test_fetch_pr_comments_with_fixtures` at `tests/test-pr-loop-hooks.sh:1242-1285`, `test_fetch_pr_comments_after_filter` at `tests/test-pr-loop-hooks.sh:1287-1320`, `test_poll_pr_reviews_with_fixtures` at `tests/test-pr-loop-hooks.sh:1322-1369`, and `test_poll_pr_reviews_after_filter` at `tests/test-pr-loop-hooks.sh:1371-1419`. These tests exercise `scripts/fetch-pr-comments.sh` and `scripts/poll-pr-reviews.sh`, whose relevant algorithm normalizes issue comments, inline review comments, and PR reviews into a shared comment model. Approval-only review placeholders are created in `fetch-pr-comments.sh` at `scripts/fetch-pr-comments.sh:299-319` and in `poll-pr-reviews.sh` at `scripts/poll-pr-reviews.sh:275-293`. A sibling broader mock exists at `tests/mocks/gh`, but it uses hyphenated fixture names such as `issue-comments.json`; this assigned generator is scoped to the underscore fixture set.

- edge_cases_or_failure_modes: Missing script arguments fail early with usage. A missing or unwritable mock directory fails during `mkdir`, heredoc write, or `chmod`. Missing matched fixture files are not explicitly validated by the generated mock; because the generated mock itself does not enable `set -e`, a failed `cat` can still be followed by `exit 0`, producing empty stdout and an error on stderr. That can make some consumers see an apparently successful but empty API response. The mock ignores `-q`/`--jq`, so calls like `gh repo view --json owner,name -q ...` return raw JSON rather than the real CLI’s filtered string. The production scripts still build endpoint strings that match this mock’s loose endpoint tests, but this means the fixture-backed tests do not fully validate repository-name extraction fidelity. Fixture paths containing embedded double quotes or newlines could break the generated `FIXTURES_DIR="..."` assignment because the heredoc expands the path at generation time. The API default of `[]` can hide unexpected endpoint calls in API-only paths, while unknown non-API commands remain visible failures.

- validation_or_tests: I inspected the assigned file directly with line numbers and verified its shell syntax with `bash -n tests/setup-fixture-mock-gh.sh`, which exited successfully with no output. The fixture-backed hook tests assert that the generated mock enables `fetch-pr-comments.sh` to include a human issue comment, an inline review comment containing `const instead of let`, and approval-only reviews represented by `[Review state: APPROVED]` at `tests/test-pr-loop-hooks.sh:1265-1283`; they also assert `--after` filtering at `tests/test-pr-loop-hooks.sh:1305-1318`. The polling tests assert valid JSON, placeholder inclusion, bot response reporting, and timestamp filtering at `tests/test-pr-loop-hooks.sh:1343-1367` and `tests/test-pr-loop-hooks.sh:1392-1411`.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence section for the single assigned item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`