# agent_022 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-022 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: This file is the Claude Code slash-command definition for starting the RLCR implementation/review loop. It is a thin command workflow entry point, not the loop engine itself. Its frontmatter exposes the command description, argument contract, allowed tool boundary, and hidden slash-command metadata at `commands/start-rlcr-loop.md:1-6`. The executable behavior is a single delegated shell invocation of `"${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh" $ARGUMENTS` at `commands/start-rlcr-loop.md:10-14`. Because it defines how the user enters the plan/RLCR algorithm, it is core workflow surface even though most algorithmic logic lives in referenced scripts and hooks.

- algorithmic_behavior: The documented algorithm is a staged loop: Claude works from a supplied plan, writes a round summary, then a Stop hook runs Codex review; Codex feedback either blocks exit and starts another implementation round or returns the exact terminal signal `COMPLETE`, which transitions into code-review phase; `codex review --base <branch>` then scans for `[P0-9]` issue markers and either loops for fixes or proceeds to finalize when no issues remain (`commands/start-rlcr-loop.md:16-25`). The command also defines a Goal Tracker subsystem to prevent drift: an immutable goal/acceptance-criteria section is initialized in Round 0, mutable tasks and deferrals evolve each round, and configurable full-alignment audits run every N rounds by default every 5 rounds (`commands/start-rlcr-loop.md:27-45`). The setup implementation confirms these behaviors by creating `.humanize/rlcr/<timestamp>/state.md`, a backup `plan.md`, `goal-tracker.md`, and `round-0-prompt.md` (`scripts/setup-rlcr-loop.sh:667-731`, `scripts/setup-rlcr-loop.sh:733-960`). The later Stop hook enforces the loop by building Codex review prompts and running `codex exec` in implementation phase (`hooks/loop-codex-stop-hook.sh:758-847`, `hooks/loop-codex-stop-hook.sh:1238-1273`), then running `codex review --base` in review phase (`hooks/loop-codex-stop-hook.sh:916-984`).

- inputs_outputs_state: Inputs are `$ARGUMENTS` passed through from the slash command, with accepted forms documented as positional plan path or `--plan-file`, plus `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, `--track-plan-file`, `--push-every-round`, `--base-branch`, `--full-review-round`, and `--skip-impl` (`commands/start-rlcr-loop.md:3`, `commands/start-rlcr-loop.md:69-86`). Runtime also depends on `CLAUDE_PLUGIN_ROOT` to locate the setup script (`commands/start-rlcr-loop.md:4`, `commands/start-rlcr-loop.md:12-14`) and on `CLAUDE_PROJECT_DIR` or cwd inside the script (`scripts/setup-rlcr-loop.sh:230`). Outputs from the setup path include a loop directory, YAML-frontmatter `state.md` with round/model/branch/plan/review fields (`scripts/setup-rlcr-loop.sh:709-726`), a copied or placeholder plan (`scripts/setup-rlcr-loop.sh:675-697`), `goal-tracker.md` (`scripts/setup-rlcr-loop.sh:737-856`), initial prompt and summary target (`scripts/setup-rlcr-loop.sh:862-960`), and activation text plus the initial prompt to the current Claude session (`scripts/setup-rlcr-loop.sh:972-1067`). State transitions are: no active loop -> active implementation loop; implementation round N -> blocked next round if Codex reports issues; implementation -> review phase when Codex result’s last non-empty line is exactly `COMPLETE` (`hooks/loop-codex-stop-hook.sh:1388-1402`); review phase -> another review-fix round if `[P0-9]` markers are found (`hooks/loop-codex-stop-hook.sh:999-1023`, `hooks/loop-codex-stop-hook.sh:1116-1161`); review phase -> finalize phase when no priority issues remain (`hooks/loop-codex-stop-hook.sh:1024-1027`, `hooks/loop-codex-stop-hook.sh:1030-1114`); finalize phase -> completed by renaming state to `complete-state.md` after checks pass (`hooks/loop-codex-stop-hook.sh:736-748`).

- gates_or_invariants: The command’s own allowed-tools gate permits only `Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*)` from this command file (`commands/start-rlcr-loop.md:4`). The setup script enforces one active loop at a time across RLCR and PR loops (`scripts/setup-rlcr-loop.sh:240-260`), requires a git repository with at least one commit (`scripts/setup-rlcr-loop.sh:293-307`), validates plan path shape and containment by rejecting absolute paths, spaces, shell metacharacters, symlink traversal, missing/unreadable files, project escapes, and submodule paths (`scripts/setup-rlcr-loop.sh:313-421`), and enforces tracked-vs-gitignored plan policy depending on `--track-plan-file` (`scripts/setup-rlcr-loop.sh:423-474`). It also requires meaningful plan content unless `--skip-impl` without a plan is used (`scripts/setup-rlcr-loop.sh:476-541`), requires `codex` to be installed (`scripts/setup-rlcr-loop.sh:543-549`), validates branch/model/effort fields for YAML safety (`scripts/setup-rlcr-loop.sh:561-587`, `scripts/setup-rlcr-loop.sh:645-651`), and selects a local code-review base branch by user input, remote default, local `main`, then local `master` (`scripts/setup-rlcr-loop.sh:590-660`). During the active loop, hooks enforce branch consistency, plan integrity, no incomplete native todos, git cleanliness, optional push requirement, summary-file presence, Round 0 goal-tracker initialization, max-iteration limits, and Codex output presence (`hooks/loop-codex-stop-hook.sh:223-337`, `hooks/loop-codex-stop-hook.sh:339-394`, `hooks/loop-codex-stop-hook.sh:396-641`, `hooks/loop-codex-stop-hook.sh:643-733`, `hooks/loop-codex-stop-hook.sh:1283-1383`). Hook registration shows the lifecycle integration points: UserPromptSubmit validation, PreToolUse validators, and Stop hook with 7200-second timeout (`hooks/hooks.json:4-68`).

- dependencies_and_callers: Primary dependency is `scripts/setup-rlcr-loop.sh`, called directly by the slash command (`commands/start-rlcr-loop.md:10-14`). That setup script sources `scripts/portable-timeout.sh` and `hooks/lib/loop-common.sh` (`scripts/setup-rlcr-loop.sh:27-30`, `scripts/setup-rlcr-loop.sh:232-234`), uses git and Codex CLI, and emits files consumed by `hooks/loop-codex-stop-hook.sh`. The Stop hook depends on `hooks/lib/loop-common.sh`, `scripts/portable-timeout.sh`, Python todo checker `hooks/check-todos-from-transcript.py` when present, prompt templates under `prompt-template/`, `jq`, `git`, and `codex` (`hooks/loop-codex-stop-hook.sh:46-53`, `hooks/loop-codex-stop-hook.sh:345-394`, `hooks/loop-codex-stop-hook.sh:761-847`, `hooks/loop-codex-stop-hook.sh:855-906`). README documents this command as the main user entry for iterative development at `README.md:80-119`. The command coordinates with sibling `/humanize:cancel-rlcr-loop` as its cancellation path (`commands/start-rlcr-loop.md:54-58`) and with `/humanize:start-pr-loop` through mutual exclusion in setup (`scripts/setup-rlcr-loop.sh:240-260`).

- edge_cases_or_failure_modes: The command inherits setup failure modes for missing plan, conflicting positional plus `--plan-file`, invalid numeric options, unsupported `--full-review-round < 2`, unknown options, and multiple plan files (`scripts/setup-rlcr-loop.sh:120-224`, `scripts/setup-rlcr-loop.sh:263-291`). `--skip-impl` is a special mode where the plan is optional, plan validation is skipped, a placeholder plan is generated, `review_started` starts true, and the first exit attempt goes straight to code review (`commands/start-rlcr-loop.md:69-86`, `scripts/setup-rlcr-loop.sh:272-282`, `scripts/setup-rlcr-loop.sh:675-731`, `scripts/setup-rlcr-loop.sh:864-898`). Base branch detection can fail if only remote branches exist locally absent, because `codex review --base` requires a local ref (`scripts/setup-rlcr-loop.sh:594-642`). The Stop hook fail-closes on malformed or old schema state, changed branches, deleted/modified plan files, incomplete todos, dirty git state, large changed code/doc files over 2000 lines, missing summaries, missing goal-tracker initialization, Codex command failure, missing/empty Codex result, empty review logs, and review-phase command failure (`hooks/loop-codex-stop-hook.sh:118-221`, `hooks/loop-codex-stop-hook.sh:223-337`, `hooks/loop-codex-stop-hook.sh:339-507`, `hooks/loop-codex-stop-hook.sh:510-641`, `hooks/loop-codex-stop-hook.sh:643-733`, `hooks/loop-codex-stop-hook.sh:1163-1236`, `hooks/loop-codex-stop-hook.sh:1283-1383`). Completion matching is deliberately strict: implementation phase only treats the last non-empty review line as complete when it is exactly `COMPLETE`, avoiding false positives such as “CANNOT COMPLETE” (`hooks/loop-codex-stop-hook.sh:1388-1395`). Code review issue detection is also strict: it searches for `[P0-9]` markers within the first 10 characters of log lines, so issues outside that format can be missed by the automated gate (`hooks/lib/loop-common.sh:332-392`).

- validation_or_tests: Direct validation is embedded in the setup and hook gates rather than in this markdown command. README documents command usage and options as user-facing validation expectations (`README.md:99-119`). Hook registration validates that the command’s created state is protected throughout the lifecycle: prompt submit, read/write/edit/bash pre-tool validators, and Stop hooks are all wired in `hooks/hooks.json:4-68`. The setup script validates arguments, repo state, plan path/content/tracking, Codex availability, branch/base selection, and YAML-safe values before creating loop state (`scripts/setup-rlcr-loop.sh:120-660`). Runtime validation includes strict state parsing in shared common code (`hooks/lib/loop-common.sh:246-330`), branch/plan/git/todo/summary/goal-tracker checks in the Stop hook (`hooks/loop-codex-stop-hook.sh:223-733`), and Codex review result validation (`hooks/loop-codex-stop-hook.sh:1238-1420`). Repository tests reference RLCR hook/session behavior under `tests/robustness/`, including active loop detection, malformed state handling, and Stop hook behavior, as found during recursive `rg` inspection.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1 evidence section present for the single assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`