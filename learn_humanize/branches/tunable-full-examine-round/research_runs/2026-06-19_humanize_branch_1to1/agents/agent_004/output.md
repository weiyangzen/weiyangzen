# agent_004 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-004 `directory` `hooks`
- cursor: `[_]`
- core_role: `hooks` is the Claude-plugin enforcement and orchestration layer for two iterative algorithms: RLCR local implementation/review loops and PR-review bot loops. It is core algorithmic code, not incidental glue: `hooks/hooks.json:4-63` wires prompt-submit, pre-tool, and stop hooks; the stop hooks drive loop state transitions, external review invocation, retry/block decisions, and generated next-round prompts. Recursive contents inspected: 11 files, 6232 total lines: `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-plan-file-validator.sh`, `loop-codex-stop-hook.sh`, `pr-loop-stop-hook.sh`, `check-todos-from-transcript.py`, `hooks.json`, `lib/loop-common.sh`, and `lib/template-loader.sh`.

- algorithmic_behavior: The pre-tool validators form a fail-closed access-control layer. `loop-read-validator.sh:25-170` validates hook JSON, rejects deeply nested input, blocks round todos except allowlisted historical files, and enforces current-round/current-directory reads for RLCR prompt and summary files. `loop-write-validator.sh:25-265` blocks writes to todos, generated prompts, state/finalize-state files, plan backups, wrong-round summaries, PR-loop state/read-only files, and goal tracker updates after round 0, while allowing the active `finalize-summary.md` during finalize. `loop-edit-validator.sh:24-181` implements the same broad policy for Edit, including PR-loop protections and wrong-round summary checks. `loop-bash-validator.sh:24-445` prevents shell bypasses: disallowed `git push` when `push_every_round` is false, `git add` of `.humanize`, redirection/tee/sed/perl/mv/cp/rm/dd/truncate edits to protected loop files, shell-wrapper attempts, and PR-loop state/read-only mutations. `loop-plan-file-validator.sh:26-231` runs on user prompt submit and blocks branch drift or plan-file tracking drift.

  The RLCR stop hook is the local implementation/review state machine. `loop-codex-stop-hook.sh:59-82` finds active `state.md` or `finalize-state.md`; `88-220` parses state and schema; `227-337` gates branch and plan integrity; `345-394` blocks incomplete TodoWrite state; `406-603` blocks unverifiable/dirty/unpushed/oversized work; `609-748` requires summaries and completes finalize. It builds Codex review prompts at `761-847`, runs `codex exec` for implementation review at `1248-1387`, interprets final markers at `1388-1498`, transitions into code-review/finalize via `run_and_handle_code_review` and `enter_finalize_phase` at `999-1114`, and creates next-round prompts at `1505-1571`.

  The PR stop hook is the remote bot-review state machine. `pr-loop-stop-hook.sh:87-174` parses scalar state plus YAML bot lists; `204-237` resolves the PR base repo for forks; `244-255` exits on merged/closed PRs; `262-361` requires a resolution file, clean git, and pushed commits; `370-421` detects force-push history rewrites and clears stale triggers; `445-510` supports Codex thumbs-up quick approval; `530-736` detects valid trigger comments after the latest commit; `757-789` verifies Claude eyes reactions when required; `797-1049` polls configured bots with per-bot timeouts and comment deduplication; `1172-1320` formats bot comments and asks local Codex to classify per-bot status; `1355-1399` handles `APPROVE`, `WAITING_FOR_BOTS`, and `USAGE_LIMIT_HIT`; `1406-1623` rebuilds active bots, updates state/goal tracker, and emits next-round feedback.

- inputs_outputs_state: Inputs include Claude hook JSON on stdin, `CLAUDE_PROJECT_DIR`, generated `.humanize/rlcr/<timestamp>/` and `.humanize/pr-loop/<timestamp>/` state files, transcript JSONL paths, git status/branch/history, GitHub CLI PR/comment/reaction responses, Codex CLI output, and prompt templates. Outputs include hook exit codes, JSON `decision: block` payloads, stderr block messages, generated review/prompt/feedback files, and state-file renames. RLCR state mutates `current_round`, `review_started`, `.review-phase-started`, `state.md -> finalize-state.md -> complete-state.md`, and terminal files such as `maxiter-state.md`, `stop-state.md`, or `unexpected-state.md`; shared `end_loop` implements terminal renames at `hooks/lib/loop-common.sh:1112-1145`. PR state mutates `active_bots`, `last_trigger_at`, `trigger_comment_id`, `latest_commit_sha`, `latest_commit_at`, `current_round`, and terminal states such as `approve-state.md`, `merged-state.md`, `closed-state.md`, `usage-limit-state.md`, and `maxiter-state.md`.

- gates_or_invariants: `hooks/lib/loop-common.sh:59-133` validates JSON, null bytes, UTF-8, required fields, and nesting depth for most validators. `parse_state_file_strict` requires frontmatter, `current_round`, `max_iterations`, `review_started`, and `base_branch`, plus numeric/boolean validation at `hooks/lib/loop-common.sh:255-330`. Active loop discovery intentionally picks only the newest live directory and ignores terminal state files at `hooks/lib/loop-common.sh:151-174` and `795-814`. File invariants are enforced by path classifiers and allowlists at `399-441`, PR-loop read-only detection at `816-887`, `.humanize` git-add protection at `930-1066`, command mutation detection at `1068-1096`, and secure cancel authorization at `533-710`. Template rendering is single-pass to avoid placeholder injection at `hooks/lib/template-loader.sh:50-132`, with safe fallbacks at `167-203`.

- dependencies_and_callers: `hooks/hooks.json:4-63` is the immediate caller map for Claude Code hooks. `scripts/setup-rlcr-loop.sh:709-726` creates the RLCR state schema consumed by these hooks; `scripts/setup-pr-loop.sh:535-555` creates the PR-loop state schema. Stop hooks depend on `scripts/portable-timeout.sh:10-71` for bounded `git`, `gh`, and `codex` calls; `prompt-template/**` for block/review/next-round text; `scripts/poll-pr-reviews.sh:239-325` for normalized GitHub issue/review/review-comment JSON; `scripts/check-bot-reactions.sh:149-184` and `261-295` for Codex thumbs-up and Claude eyes reaction detection; and setup/cancel commands under `commands/`. Shared helpers in `hooks/lib/loop-common.sh` coordinate both loop families, including bot-name mapping and PR goal-tracker updates at `722-777` and `1168-1315`.

- edge_cases_or_failure_modes: The code generally fails closed on malformed JSON/state, git timeouts, missing summaries, dirty worktrees, missing Codex, empty Codex output, Codex review failures, stale PR triggers, force pushes, and Claude eyes timeouts. Missing templates degrade to inline fallbacks rather than blocking, by design. PR polling treats repeated GitHub API failures in `poll-pr-reviews.sh` as empty arrays after retries, which can surface later as bot timeout behavior rather than an immediate hard API failure. Bash protection is necessarily regex based; it covers many bypass forms, including wrapper shells and leading redirections, but complex shell indirection remains the inherent risk surface. One notable inconsistency: `loop-edit-validator.sh` does not call the shared JSON/depth validators and does not perform the same final active-directory equality check that Read/Write do, so Edit validation is narrower than `loop-read-validator.sh:157-168` and `loop-write-validator.sh:252-263`.

- validation_or_tests: No tests were run; this was a read-only research pass. Existing coverage is broad: template references in `tests/test-template-references.sh:58-63`, Todo transcript parsing in `tests/test-todo-checker.sh`, finalize/RLCR state transitions and validator behavior in `tests/test-finalize-phase.sh:524-864`, cancel authorization/security in `tests/test-cancel-signal-file.sh` and `tests/robustness/test-cancel-security-robustness.sh`, hook input robustness in `tests/robustness/test-hook-input-robustness.sh`, PR trigger/pagination/approval-only review behavior in `tests/test-pr-loop-hooks.sh:1043-1213`, and PR stop-hook/API robustness in `tests/robustness/test-pr-loop-api-robustness.sh`.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-004`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`