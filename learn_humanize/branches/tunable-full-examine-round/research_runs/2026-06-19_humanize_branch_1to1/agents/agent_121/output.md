# agent_121 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-121 `file` `prompt-template/pr-loop/critical-requirements-has-comments.md`
- cursor: `[_]`
- core_role: This is a PR-loop completion contract template for the “comments exist” path. It is not executable logic by itself, but it defines the worker-facing gate sequence that must happen before the PR loop’s Stop Hook can proceed: commit and push fixes, comment on the PR to trigger re-review, and write a resolution summary. See `prompt-template/pr-loop/critical-requirements-has-comments.md:2` through `prompt-template/pr-loop/critical-requirements-has-comments.md:24`.
- algorithmic_behavior: The template emits a terminal instruction block headed “CRITICAL - Work Completion Requirements” and orders the loop participant through three required actions. First, changes must be committed and pushed (`prompt-template/pr-loop/critical-requirements-has-comments.md:8` through `prompt-template/pr-loop/critical-requirements-has-comments.md:10`). Second, the user must run a rendered `gh pr comment` command to mention the configured review bots (`prompt-template/pr-loop/critical-requirements-has-comments.md:12` through `prompt-template/pr-loop/critical-requirements-has-comments.md:13`). Third, the user must write a resolution summary to the rendered resolve path, including issues addressed, files modified, and tests added (`prompt-template/pr-loop/critical-requirements-has-comments.md:15` through `prompt-template/pr-loop/critical-requirements-has-comments.md:22`). The final line declares the transition into Stop Hook polling (`prompt-template/pr-loop/critical-requirements-has-comments.md:23`).
- inputs_outputs_state: Inputs are template variables rendered by `scripts/setup-pr-loop.sh`: `PR_NUMBER`, `RESOLVE_PATH`, and `BOT_MENTION_STRING` are added to `TEMPLATE_VARS` at `scripts/setup-pr-loop.sh:652` through `scripts/setup-pr-loop.sh:657`. `PR_NUMBER` comes from `gh pr view` discovery and is validated as numeric at `scripts/setup-pr-loop.sh:306` through `scripts/setup-pr-loop.sh:336`. `RESOLVE_PATH` is set to `.humanize/pr-loop/<active-loop>/round-0-pr-resolve.md` via `scripts/setup-pr-loop.sh:641`. `BOT_MENTION_STRING` is built from active bot flags at `scripts/setup-pr-loop.sh:213` through `scripts/setup-pr-loop.sh:214`. Output is rendered prompt text printed after the generated round-0 prompt; `setup-pr-loop.sh` selects this exact template only when `COMMENT_COUNT` is nonzero (`scripts/setup-pr-loop.sh:810` through `scripts/setup-pr-loop.sh:862`). State transition expectation: after the user follows the prompt, the PR branch has new remote commits, the PR contains a bot-mention trigger comment, and the resolve summary file exists for the active round.
- gates_or_invariants: The main invariant is conditional: this template is for PRs with existing comments, not fresh PRs with zero comments. `setup-pr-loop.sh` uses `critical-requirements-no-comments.md` when `COMMENT_COUNT == 0`, and uses this file when comments exist (`scripts/setup-pr-loop.sh:812` through `scripts/setup-pr-loop.sh:860`). The sibling no-comments template explicitly forbids triggering a review comment because bots should review automatically (`prompt-template/pr-loop/critical-requirements-no-comments.md:15` through `prompt-template/pr-loop/critical-requirements-no-comments.md:20`), which makes this file’s trigger-comment requirement part of a branch-specific gate. The Stop Hook enforces part of the contract by blocking if the active round resolve file is missing (`hooks/pr-loop-stop-hook.sh:258` through `hooks/pr-loop-stop-hook.sh:277`) and by requiring a trigger comment before polling in defensive paths (`hooks/pr-loop-stop-hook.sh:838` through `hooks/pr-loop-stop-hook.sh:852`). The hook also handles PR terminal states before polling: merged and closed PRs exit the loop early (`hooks/pr-loop-stop-hook.sh:240` through `hooks/pr-loop-stop-hook.sh:256`).
- dependencies_and_callers: Direct caller is `scripts/setup-pr-loop.sh`, which loads this template with `load_and_render_safe "$TEMPLATE_DIR" "pr-loop/critical-requirements-has-comments.md"` at `scripts/setup-pr-loop.sh:860`. Rendering is implemented by `hooks/lib/template-loader.sh`; placeholders use `{{VARIABLE_NAME}}`, missing variables remain literal, and substitution is single-pass to avoid placeholder injection (`hooks/lib/template-loader.sh:7` through `hooks/lib/template-loader.sh:14`, `hooks/lib/template-loader.sh:50` through `hooks/lib/template-loader.sh:131`). If the file is missing or renders empty, `load_and_render_safe` falls back to an embedded equivalent block (`hooks/lib/template-loader.sh:167` through `hooks/lib/template-loader.sh:203`, with fallback content at `scripts/setup-pr-loop.sh:836` through `scripts/setup-pr-loop.sh:859`). Related prompt `prompt-template/pr-loop/round-0-task-has-comments.md` carries the same operational flow in the main task body, including the stronger “please review the latest changes” comment body (`prompt-template/pr-loop/round-0-task-has-comments.md:15` through `prompt-template/pr-loop/round-0-task-has-comments.md:27`).
- edge_cases_or_failure_modes: If `PR_NUMBER`, `BOT_MENTION_STRING`, or `RESOLVE_PATH` is not supplied, the template loader preserves placeholders literally, which would emit an unusable `gh pr comment {{PR_NUMBER}} ...` or unresolved path instead of failing fast (`hooks/lib/template-loader.sh:119` through `hooks/lib/template-loader.sh:122`). The setup script mitigates `PR_NUMBER` by failing when no PR is found or when the PR number is non-numeric (`scripts/setup-pr-loop.sh:326` through `scripts/setup-pr-loop.sh:336`). A missing template is not fatal because caller fallback text mirrors the assigned template (`scripts/setup-pr-loop.sh:836` through `scripts/setup-pr-loop.sh:860`). The critical template’s command says `please review`, while the round task and Stop Hook examples often use `please review the latest changes`; this is probably accepted because the bot mention is the real trigger, but it is a consistency edge case across prompt surfaces (`prompt-template/pr-loop/critical-requirements-has-comments.md:13`, `prompt-template/pr-loop/round-0-task-has-comments.md:20`, `hooks/pr-loop-stop-hook.sh:848`). The file does not itself validate that commits were pushed; that depends on git state and remote review behavior downstream.
- validation_or_tests: No dedicated unit test for this markdown template was found in the inspected references. Validation is mostly integration-style through `scripts/setup-pr-loop.sh` and `hooks/pr-loop-stop-hook.sh`: setup renders this template based on `COMMENT_COUNT`, and the Stop Hook enforces the resolve-file gate plus PR polling/trigger behavior. I inspected the template directly with line numbers and traced its loader/caller path. A final `git status --short` check could not validate repository cleanliness because this branch export is not a Git checkout in the sandbox and the restricted environment blocked temporary Xcode cache creation; no files were modified.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 heading present
- missing_items: 0
- duplicate_items: 0
- final_worker_status: `complete`