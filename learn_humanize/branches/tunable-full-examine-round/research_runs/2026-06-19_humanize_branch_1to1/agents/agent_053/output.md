# agent_053 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-053 `file` `tests/test-finalize-phase.sh`
- cursor: `[_]`
- core_role: Executable behavioral specification for the RLCR “Finalize Phase” state machine. The file verifies that a normal RLCR loop can transition from implementation completion into a protected finalize phase, then into completed loop state only after local exit gates pass. It also verifies that normal RLCR rounds, max-iteration termination, review failure handling, todo gating, and hook validators continue to behave correctly around the new `finalize-state.md` path. The declared positive and negative coverage appears at `tests/test-finalize-phase.sh:3` through `tests/test-finalize-phase.sh:19`.

- algorithmic_behavior: The test harness creates an isolated temporary git repository, installs mock `codex` executables, creates synthetic RLCR loop directories, and invokes hook scripts with JSON hook input. Setup is centralized in `setup_test_repo` and `setup_loop_dir`, which initialize git, write `plans/test-plan.md`, ignore loop artifacts, and create `.humanize/rlcr/2024-01-01_12-00-00/state.md`, `plan.md`, and `goal-tracker.md`; see `tests/test-finalize-phase.sh:144` through `tests/test-finalize-phase.sh:226`. Mock Codex behavior is controlled by shell shims at `tests/test-finalize-phase.sh:53` through `tests/test-finalize-phase.sh:142`: normal output, call tracking, nonzero `codex review`, and empty review stdout. The test then drives these main algorithm paths: helper path recognition (`tests/test-finalize-phase.sh:238` through `tests/test-finalize-phase.sh:280`), active-loop detection for `finalize-state.md` versus terminal states (`tests/test-finalize-phase.sh:283` through `tests/test-finalize-phase.sh:335`), write/edit/bash validator protection for finalize state and summary files (`tests/test-finalize-phase.sh:337` through `tests/test-finalize-phase.sh:415`), finalize stop-hook gates (`tests/test-finalize-phase.sh:417` through `tests/test-finalize-phase.sh:505`), COMPLETE-to-finalize transition and max-iteration bypass (`tests/test-finalize-phase.sh:506` through `tests/test-finalize-phase.sh:560`), hard failures for broken or empty `codex review` (`tests/test-finalize-phase.sh:562` through `tests/test-finalize-phase.sh:696`), incomplete todo blocking (`tests/test-finalize-phase.sh:698` through `tests/test-finalize-phase.sh:741`), normal non-COMPLETE round feedback (`tests/test-finalize-phase.sh:743` through `tests/test-finalize-phase.sh:814`), and validator parsing of `finalize-state.md` (`tests/test-finalize-phase.sh:816` through `tests/test-finalize-phase.sh:865`). The suite exits with the accumulated failure count at `tests/test-finalize-phase.sh:867` through `tests/test-finalize-phase.sh:876`.

- inputs_outputs_state: Inputs are generated hook JSON payloads, a mock transcript JSONL file, the synthetic loop state frontmatter, mock Codex output, and the temporary git worktree. Hook inputs use `tool_name` and `tool_input.file_path` for validators, or `stop_hook_active`, `transcript`, and `transcript_path` for the stop hook; examples are at `tests/test-finalize-phase.sh:346`, `tests/test-finalize-phase.sh:358`, `tests/test-finalize-phase.sh:382`, `tests/test-finalize-phase.sh:434`, and `tests/test-finalize-phase.sh:722`. Outputs are PASS/FAIL counters, JSON block decisions from hooks, filesystem state changes under `.humanize/rlcr`, review logs under the isolated `XDG_CACHE_HOME`, and mock Codex call markers. The principal state transitions specified are: `state.md` is renamed to `finalize-state.md` when implementation review reaches COMPLETE and `codex review` finds no issues (`tests/test-finalize-phase.sh:524` through `tests/test-finalize-phase.sh:540`); `finalize-state.md` is renamed to `complete-state.md` after finalize summary, todo, git-clean, and other stop-hook gates pass (`tests/test-finalize-phase.sh:476` through `tests/test-finalize-phase.sh:496`); reaching max iterations creates `maxiter-state.md` without entering finalize (`tests/test-finalize-phase.sh:542` through `tests/test-finalize-phase.sh:560`); review failures or empty review output preserve `state.md` with `review_started: true` and do not create finalize or complete state (`tests/test-finalize-phase.sh:588` through `tests/test-finalize-phase.sh:619`, `tests/test-finalize-phase.sh:647` through `tests/test-finalize-phase.sh:696`); normal review feedback increments `current_round` to the next round and keeps `state.md` active (`tests/test-finalize-phase.sh:781` through `tests/test-finalize-phase.sh:797`).

- gates_or_invariants: The executable spec enforces several invariants. `find_active_loop` must treat only `state.md` and `finalize-state.md` as active loop indicators, not `complete-state.md`; the underlying implementation documents this at `hooks/lib/loop-common.sh:151` through `hooks/lib/loop-common.sh:174`, and the test checks it at `tests/test-finalize-phase.sh:293` through `tests/test-finalize-phase.sh:320`. `finalize-state.md` is protected from direct Write/Edit and Bash mutation, including `mv` or `cp` from that file; tests are at `tests/test-finalize-phase.sh:357` through `tests/test-finalize-phase.sh:415`, with implementation hooks at `hooks/loop-write-validator.sh:148` through `hooks/loop-write-validator.sh:172`, `hooks/loop-edit-validator.sh:112` through `hooks/loop-edit-validator.sh:124`, and `hooks/loop-bash-validator.sh:139` through `hooks/loop-bash-validator.sh:172` plus source-file detection at `hooks/loop-bash-validator.sh:286` through `hooks/loop-bash-validator.sh:317`. `finalize-summary.md` must be writable only in the active finalize loop directory; the test covers allow behavior at `tests/test-finalize-phase.sh:345` through `tests/test-finalize-phase.sh:355`, and write validation allows it at `hooks/loop-write-validator.sh:162` through `hooks/loop-write-validator.sh:172`. Finalize completion must not call Codex; tracking assertions are at `tests/test-finalize-phase.sh:446` through `tests/test-finalize-phase.sh:452` and `tests/test-finalize-phase.sh:498` through `tests/test-finalize-phase.sh:504`, while the stop hook explicitly completes finalize before review execution at `hooks/loop-codex-stop-hook.sh:736` through `hooks/loop-codex-stop-hook.sh:748`. Missing summary blocks exit (`tests/test-finalize-phase.sh:429` through `tests/test-finalize-phase.sh:444`), dirty git blocks exit (`tests/test-finalize-phase.sh:454` through `tests/test-finalize-phase.sh:471`), incomplete todos block before Codex review (`tests/test-finalize-phase.sh:721` through `tests/test-finalize-phase.sh:741`), review command failure or empty output is a hard block (`tests/test-finalize-phase.sh:566` through `tests/test-finalize-phase.sh:696`), and max iterations bypass finalize (`tests/test-finalize-phase.sh:542` through `tests/test-finalize-phase.sh:560`). The stop hook’s relevant gate order is todo check at `hooks/loop-codex-stop-hook.sh:342` through `hooks/loop-codex-stop-hook.sh:393`, git status and clean checks at `hooks/loop-codex-stop-hook.sh:396` through `hooks/loop-codex-stop-hook.sh:570`, summary check at `hooks/loop-codex-stop-hook.sh:605` through `hooks/loop-codex-stop-hook.sh:641`, max-iteration check at `hooks/loop-codex-stop-hook.sh:721` through `hooks/loop-codex-stop-hook.sh:734`, and finalize completion at `hooks/loop-codex-stop-hook.sh:736` through `hooks/loop-codex-stop-hook.sh:748`.

- dependencies_and_callers: The test directly sources `hooks/lib/loop-common.sh` for helper functions at `tests/test-finalize-phase.sh:48` through `tests/test-finalize-phase.sh:49`. It calls `find_active_loop`, `is_finalize_state_file_path`, `is_finalize_summary_path`, `finalize_state_file_blocked_message`, and `parse_state_file`; their relevant implementations are in `hooks/lib/loop-common.sh:151` through `hooks/lib/loop-common.sh:174`, `hooks/lib/loop-common.sh:191` through `hooks/lib/loop-common.sh:244`, and `hooks/lib/loop-common.sh:472` through `hooks/lib/loop-common.sh:525`. It invokes these hook entrypoints as system-under-test dependencies: `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. Stop-hook behavior depends on `hooks/check-todos-from-transcript.py`, which finds the latest `TodoWrite` call and returns incomplete todo status; see `hooks/check-todos-from-transcript.py:20` through `hooks/check-todos-from-transcript.py:87` and exit behavior at `hooks/check-todos-from-transcript.py:90` through `hooks/check-todos-from-transcript.py:134`. Review behavior depends on `codex exec` for implementation-phase summary review and `codex review --base` for review phase; the test replaces both with mock `codex` scripts at `tests/test-finalize-phase.sh:53` through `tests/test-finalize-phase.sh:142`. The real stop hook builds review command arguments at `hooks/loop-codex-stop-hook.sh:899` through `hooks/loop-codex-stop-hook.sh:905`, runs `codex review` and writes audit/log files at `hooks/loop-codex-stop-hook.sh:911` through `hooks/loop-codex-stop-hook.sh:984`, analyzes review output via `detect_review_issues` at `hooks/lib/loop-common.sh:332` through `hooks/lib/loop-common.sh:390`, and handles review outcome at `hooks/loop-codex-stop-hook.sh:999` through `hooks/loop-codex-stop-hook.sh:1028`.

- edge_cases_or_failure_modes: The suite specifically protects against false-positive path matching where `state.md` could also match `finalize-state.md`; both write/edit/bash validators check finalize-specific paths first, and the tests exercise direct and source-path mutation attempts (`tests/test-finalize-phase.sh:357` through `tests/test-finalize-phase.sh:415`). It covers terminal-state confusion by proving `complete-state.md` is not active (`tests/test-finalize-phase.sh:302` through `tests/test-finalize-phase.sh:320`) while `finalize-state.md` and normal `state.md` are active (`tests/test-finalize-phase.sh:283` through `tests/test-finalize-phase.sh:335`). It verifies finalize phase is not an opportunity to bypass normal exit hygiene: missing `finalize-summary.md`, dirty git, and incomplete todos all block (`tests/test-finalize-phase.sh:429` through `tests/test-finalize-phase.sh:471`, `tests/test-finalize-phase.sh:698` through `tests/test-finalize-phase.sh:741`). It guards the expensive/retry-sensitive review edge cases where COMPLETE from `codex exec` should not automatically enter finalize if `codex review` fails or emits no usable log; those cases assert no finalize or complete state is created and `review_started: true` remains in `state.md` (`tests/test-finalize-phase.sh:562` through `tests/test-finalize-phase.sh:696`). It also preserves normal loop behavior when review feedback contains issues: the loop blocks with feedback, increments to the next round, and creates `round-3-review-result.md` (`tests/test-finalize-phase.sh:775` through `tests/test-finalize-phase.sh:814`). A subtle numbering invariant is documented in the empty-review test: review logs use `CURRENT_ROUND + 1`, so round 4 implementation review writes `round-5-codex-review.log`; see `tests/test-finalize-phase.sh:659` through `tests/test-finalize-phase.sh:668` and the implementation comment at `hooks/loop-codex-stop-hook.sh:997` through `hooks/loop-codex-stop-hook.sh:999`.

- validation_or_tests: This file is itself a runnable Bash validation suite with 876 lines. It uses isolated temp state via `mktemp -d` and cleans up on exit (`tests/test-finalize-phase.sh:40` through `tests/test-finalize-phase.sh:46`). The suite accumulates pass/fail/skip counters through helper functions at `tests/test-finalize-phase.sh:27` through `tests/test-finalize-phase.sh:38`, then exits with `TESTS_FAILED` at `tests/test-finalize-phase.sh:867` through `tests/test-finalize-phase.sh:876`. It validates behavior by invoking production hook scripts rather than only unit-level helper functions. I inspected the file and directly referenced implementation dependencies, but did not execute the full test suite because this task requested research notes only in a read-only branch export.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-053`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`