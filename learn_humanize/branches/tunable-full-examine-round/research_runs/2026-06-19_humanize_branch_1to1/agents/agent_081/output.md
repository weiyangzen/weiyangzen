# agent_081 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-081 `file` `prompt-template/block/git-not-clean-untracked.md`
- cursor: `[_]`
- core_role: Static prompt-block template that augments the stop-hook “git not clean” block when the repository has non-`.humanize*` untracked files. It does not detect git state itself; it supplies the review contract that tells the agent/operator to classify untracked files as artifacts versus real work and update `.gitignore` where appropriate. The template text is at `prompt-template/block/git-not-clean-untracked.md:2-10`.
- algorithmic_behavior: The stop hook first inspects cached git status output and sets `GIT_ISSUES="uncommitted changes"` when any staged, unstaged, or untracked status exists (`hooks/loop-codex-stop-hook.sh:521-524`). It extracts untracked entries using `grep '^??'` (`hooks/loop-codex-stop-hook.sh:525-527`), handles `.humanize*` entries with a separate note (`hooks/loop-codex-stop-hook.sh:528-535`), then filters remaining untracked paths into `OTHER_UNTRACKED` (`hooks/loop-codex-stop-hook.sh:537-538`). If any remain, it loads this template by name, `block/git-not-clean-untracked.md`, and appends it to `SPECIAL_NOTES` (`hooks/loop-codex-stop-hook.sh:539-545`). That `SPECIAL_NOTES` value is then rendered into the broader `block/git-not-clean.md` prompt via `{{SPECIAL_NOTES}}` (`hooks/loop-codex-stop-hook.sh:557-559`). The final hook output is a JSON block decision with `decision: "block"`, a rendered markdown `reason`, and a system message requiring commit before exit (`hooks/loop-codex-stop-hook.sh:561-568`).
- inputs_outputs_state: The template itself has no placeholders and accepts no direct runtime variables. Its practical input is the hook’s classification that at least one non-`.humanize*` untracked file exists. Its output is markdown guidance headed “Note on Untracked Files” with examples of likely artifact categories: build outputs, dependencies, IDE/editor files, logs, caches, and temp files (`prompt-template/block/git-not-clean-untracked.md:2-8`). The state transition is not a git mutation; it is a stop-hook control transition from “trying to stop” to “blocked until clean/committed,” with `SPECIAL_NOTES` enriched by this static block. If the agent follows the prompt, downstream state may change by adding ignore patterns to `.gitignore`, staging real changes, and committing, but those actions are performed outside this template.
- gates_or_invariants: The hard gate is in the caller: any non-empty `GIT_ISSUES` blocks stop/exit (`hooks/loop-codex-stop-hook.sh:548-568`). This file contributes a sub-gate advisory only when untracked files are present after the `.humanize` exclusion. It preserves the invariant that untracked build/runtime artifacts should generally be ignored rather than committed, explicitly saying they “should typically be added to `.gitignore` rather than committed” (`prompt-template/block/git-not-clean-untracked.md:3-4`). The parent template still requires staging real changes and committing before exit (`prompt-template/block/git-not-clean.md:5-16`), so this note narrows the correct treatment of untracked files but does not relax the clean-worktree requirement.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, which calls `load_template "$TEMPLATE_DIR" "block/git-not-clean-untracked.md"` (`hooks/loop-codex-stop-hook.sh:540`). Loading is provided by `hooks/lib/template-loader.sh`: `load_template` reads the file with `cat` when present and returns empty on missing files (`hooks/lib/template-loader.sh:33-48`). The rendered parent block is `prompt-template/block/git-not-clean.md`, which contains the `{{SPECIAL_NOTES}}` insertion point (`prompt-template/block/git-not-clean.md:3-4`). Template existence is covered generically by `tests/test-template-references.sh`, which scans `load_template`, `load_and_render`, and `load_and_render_safe` references and fails missing template paths (`tests/test-template-references.sh:66-110`); the same test also reports referenced templates by scanning `prompt-template` files (`tests/test-template-references.sh:121-140`).
- edge_cases_or_failure_modes: If this template is missing or empty, the hook falls back to the shorter message “Review untracked files - add to .gitignore or commit them.” (`hooks/loop-codex-stop-hook.sh:540-543`), so the blocking behavior continues but loses category-specific guidance. The `.humanize` filter is substring-based (`grep -v '\.humanize'` at `hooks/loop-codex-stop-hook.sh:538`), so an unrelated untracked path containing `.humanize` could be excluded from this note. Conversely, this template is advisory only: it cannot distinguish a real source file from an artifact, and it cannot enforce that `.gitignore` changes are correct. Because `load_template` returns raw content and this file has no placeholders, there is no variable substitution surface in the block itself; substitution happens later when the parent `git-not-clean.md` is rendered with `SPECIAL_NOTES`.
- validation_or_tests: I inspected the file directly with line numbers and followed its concrete caller. Generic template validation exists: `tests/test-template-references.sh` extracts direct template references from hook scripts and checks the target file exists (`tests/test-template-references.sh:83-110`). The broader loader tests validate `load_template`/`load_and_render_safe` behavior for existing and missing templates, but I did not find a dedicated behavioral test that simulates non-`.humanize*` untracked files and asserts this exact note appears in the stop-hook JSON. The fallback path in `hooks/loop-codex-stop-hook.sh:540-543` gives runtime resilience if the template file is absent.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-081`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`