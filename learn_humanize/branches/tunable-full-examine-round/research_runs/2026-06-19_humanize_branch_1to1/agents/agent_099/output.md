# agent_099 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-099 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: This is a reusable hook-block message template for the RLCR loop’s todo-file access policy. Its role is not to implement matching itself, but to define the user-facing contract emitted when hook validators block access to `round-*-todos.md` files. The template tells the agent not to create or access those files and to use native `TodoWrite` instead (`prompt-template/block/todos-file-access.md:1`, `prompt-template/block/todos-file-access.md:3`, `prompt-template/block/todos-file-access.md:5`).
- algorithmic_behavior: The file has a static three-part message: title, prohibition, and replacement workflow. It contains no placeholders or conditional branches, so rendering is deterministic. The behavioral algorithm around it is: hook receives tool input, validates/parses the tool payload, detects a target path or shell command touching a `round-N-todos.md` file, checks whether it is one of the narrow active-loop allowlist exceptions, and emits this template on stderr before exiting with a blocking status. Read access uses this path in `hooks/loop-read-validator.sh:55-62`; Write uses it in `hooks/loop-write-validator.sh:55-62`; Edit uses it in `hooks/loop-edit-validator.sh:38-45`; Bash modification attempts use it in `hooks/loop-bash-validator.sh:385-392`.
- inputs_outputs_state: Direct template input is only the selected template path, `block/todos-file-access.md`, loaded by `todos_blocked_message()` through `load_and_render_safe` (`hooks/lib/loop-common.sh:443-452`). There are no template variables, so the `action` argument accepted by `todos_blocked_message "Read|Write|Bash"` is currently unused (`hooks/lib/loop-common.sh:444-451`). Operational inputs are hook JSON fields: `tool_input.file_path` for Read/Write/Edit and `tool_input.command` for Bash (`hooks/loop-read-validator.sh:43-49`, `hooks/loop-write-validator.sh:43-49`, `hooks/loop-edit-validator.sh:24-32`, `hooks/loop-bash-validator.sh:42-48`). Output is the rendered block message to stderr and exit code `2` for blocked policy violations (`hooks/loop-read-validator.sh:60-61`, `hooks/loop-write-validator.sh:60-61`, `hooks/loop-edit-validator.sh:43-44`, `hooks/loop-bash-validator.sh:390-391`). The intended state transition is from file-backed round todo tracking to native Claude task state: lines 5-8 explicitly route task management into `TodoWrite` and Claude Code’s task-management system.
- gates_or_invariants: The invariant is that agents should not create or access arbitrary `round-*-todos.md` files; native todo tooling is the accepted state channel (`prompt-template/block/todos-file-access.md:3-8`). The file pattern gate is `round-[0-9]+-todos\.md$` for path-based Read/Write/Edit detection (`hooks/lib/loop-common.sh:399-406`). A special active-loop allowlist permits only `round-1-todos.md` and `round-2-todos.md` in the active loop directory, alongside two summary files, via `is_allowlisted_file()` (`hooks/lib/loop-common.sh:423-440`). Bash has a stricter full-path gate: even allowed todo basenames must appear under the escaped active loop directory as `round-[12]-todos.md`; generic relative paths and wrong-root paths are blocked (`hooks/loop-bash-validator.sh:385-392`). Template loading is fail-soft: if the markdown template is missing or empty, `load_and_render_safe` returns an inline fallback rather than allowing silent policy failure (`hooks/lib/template-loader.sh:167-203`, `hooks/lib/loop-common.sh:447-451`).
- dependencies_and_callers: Primary caller is `todos_blocked_message()` in `hooks/lib/loop-common.sh`, which depends on `hooks/lib/template-loader.sh` for locating and rendering files under `prompt-template` (`hooks/lib/loop-common.sh:140-148`, `hooks/lib/template-loader.sh:24-31`, `hooks/lib/template-loader.sh:170-203`). Runtime callers are `loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, and `loop-bash-validator.sh`. The policy also depends on shared helpers: `validate_hook_input()` and `require_tool_input_field()` for robust JSON/tool-field validation in Read/Write/Bash (`hooks/lib/loop-common.sh:59-114`), `find_active_loop()` for selecting only the newest active RLCR loop (`hooks/lib/loop-common.sh:151-174`), `to_lower()` for case-normalized matching (`hooks/lib/loop-common.sh:394-397`), `is_round_file_type()` for suffix matching (`hooks/lib/loop-common.sh:399-406`), and `command_modifies_file()` for Bash redirection/edit/delete/copy/truncate detection (`hooks/lib/loop-common.sh:1068-1095`).
- edge_cases_or_failure_modes: The template text says “Do NOT create or access `round-*-todos.md` files,” but implementation has explicit compatibility exceptions for `round-1-todos.md` and `round-2-todos.md` in the active loop directory (`hooks/lib/loop-common.sh:427-432`), so the message is broader than the actual gate. If no active loop exists, Read/Write/Edit of todo files are blocked because the allowlist cannot be proven (`hooks/loop-read-validator.sh:55-62`, `hooks/loop-write-validator.sh:55-62`, `hooks/loop-edit-validator.sh:38-45`); Bash, by contrast, allows all commands when neither RLCR nor PR loop is active (`hooks/loop-bash-validator.sh:64-67`). Edit validation is less defensive than Read/Write/Bash because it parses `tool_name` and `file_path` directly with `jq` and does not call the shared JSON/depth/required-field validators in its initial todo gate (`hooks/loop-edit-validator.sh:24-32`). Bash detection is pattern-based and covers common write paths such as redirection, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`, `cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec >` (`hooks/lib/loop-common.sh:1075-1088`), but shell forms outside those patterns may require separate coverage.
- validation_or_tests: `tests/test-template-references.sh` includes `block/todos-file-access.md` in common-template existence checks, so the shared message function has a reference-surface test (`tests/test-template-references.sh:149-166`). `tests/test-allowlist-validators.sh` verifies the allowlist accepts `round-1-todos.md` and `round-2-todos.md`, rejects `round-3-todos.md`, and rejects same-basename files outside the active loop (`tests/test-allowlist-validators.sh:72-107`, `tests/test-allowlist-validators.sh:121-123`). The same test exercises Write allow/block behavior (`tests/test-allowlist-validators.sh:135-171`), Edit allow/block behavior (`tests/test-allowlist-validators.sh:191-228`), Read allow/block behavior (`tests/test-allowlist-validators.sh:234-270`), and Bash full-path/wrong-directory/generic-path blocking (`tests/test-allowlist-validators.sh:290-352`). I did not run tests because this worker was instructed to produce read-only research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section present for the assigned prompt block file
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`