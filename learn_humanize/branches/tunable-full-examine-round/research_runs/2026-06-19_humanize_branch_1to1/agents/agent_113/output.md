# agent_113 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-113 `file` `prompt-template/claude/push-every-round-note.md`
- cursor: `[_]`
- core_role: This is a small Claude prompt fragment used to carry the `--push-every-round` contract into generated loop prompts. The file itself is not an algorithm implementation; its algorithmic role is instructional glue in the round-transition prompt builder. Direct content inspection shows only a blank first line and the note at `prompt-template/claude/push-every-round-note.md:2`: when `--push-every-round` is enabled, commits must be pushed to remote after each round.
- algorithmic_behavior: The template is appended conditionally during next-round prompt construction. In `hooks/loop-codex-stop-hook.sh:1542-1549`, the hook checks `PUSH_EVERY_ROUND == "true"`, loads `claude/push-every-round-note.md`, falls back to `Also push your changes after committing.` if the template is empty, and appends the resulting text to `round-${NEXT_ROUND}-prompt.md`. The same message is duplicated inline for round 0 setup in `scripts/setup-rlcr-loop.sh:962-967`, so this template controls follow-up rounds rather than the initial setup prompt.
- inputs_outputs_state: Input is the persisted loop state field `push_every_round`, parsed from state frontmatter. `hooks/lib/loop-common.sh:23` defines the field name, and `hooks/lib/loop-common.sh:227-239` parses it with a default of `false`. `hooks/loop-codex-stop-hook.sh:99-111` maps `STATE_PUSH_EVERY_ROUND` into `PUSH_EVERY_ROUND`. Output is prompt text appended to the next-round prompt file at `hooks/loop-codex-stop-hook.sh:1511-1549`. It does not mutate git, loop state, or checklist state by itself; the state transition is indirect: enabled flag -> prompt includes push requirement -> worker/agent is instructed to push before the next stop attempt.
- gates_or_invariants: The template is gated by exact string equality against `true` at `hooks/loop-codex-stop-hook.sh:1543`, so values other than lowercase `true` behave as disabled. The actual enforcement gate is separate from this prompt note: when enabled, `hooks/loop-codex-stop-hook.sh:573-590` checks `git status -sb` for an ahead count and renders `prompt-template/block/unpushed-commits.md` if local commits remain unpushed. When disabled, `hooks/loop-bash-validator.sh:88-105` blocks explicit `git push` commands and tells the user to start with `--push-every-round` if pushing is required. The invariant is therefore two-sided: push is forbidden by default, but required before exit when the flag is enabled.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:1544` through `load_template`. The loader behavior is in `hooks/lib/template-loader.sh:36-47`: if the file exists, it is emitted with `cat`; otherwise a warning and empty content are returned. Setup dependencies are `scripts/setup-rlcr-loop.sh:42`, where `PUSH_EVERY_ROUND` defaults to `false`; `scripts/setup-rlcr-loop.sh:164-166`, where `--push-every-round` sets it to `true`; and `scripts/setup-rlcr-loop.sh:709-716`, where it is written to `state.md`. User-facing CLI documentation appears in `README.md:105-113` and `scripts/setup-rlcr-loop.sh:59-68`. Related enforcement copy lives in `prompt-template/block/unpushed-commits.md:1-12` and `prompt-template/block/git-push.md:1-9`.
- edge_cases_or_failure_modes: If this template is missing or empty, the stop hook falls back to a shorter push instruction at `hooks/loop-codex-stop-hook.sh:1545-1547`, so loop operation continues with degraded wording. Because the template has no placeholders, render-time substitution cannot corrupt it, but it also cannot adapt to branch name, remote name, or ahead count. If `push_every_round` is manually written as `True`, `yes`, or another truthy-looking value, the equality gate treats it as disabled because parsing only strips spaces and does not normalize case. The enforcement check for unpushed commits relies on `git status -sb` containing `ahead N`; unusual upstream configuration, detached HEAD, no upstream, or localized git output could reduce detection quality. The prompt says "remote" but does not specify a remote or branch; the blocking template later uses `git push origin {{CURRENT_BRANCH}}`.
- validation_or_tests: No dedicated unit test or golden prompt test was found for this specific template. Validation is mostly integration-style through shell behavior: template loading is handled by `hooks/lib/template-loader.sh:36-47`; next-round prompt assembly is visible in `hooks/loop-codex-stop-hook.sh:1514-1549`; setup stores the flag at `scripts/setup-rlcr-loop.sh:709-716`; and runtime push blocking/required-push behavior is split between `hooks/loop-bash-validator.sh:96-105` and `hooks/loop-codex-stop-hook.sh:576-590`. The file is therefore best validated by a setup/start-loop scenario with `--push-every-round`, followed by next-round prompt generation and an unpushed-commit stop attempt.
- skip_candidate: `yes: the file is a static prompt fragment, not a core algorithm implementation. It is still relevant to the branch’s control contract because it participates in the round-transition prompt path and mirrors the push gate controlled by the loop state.`

## Worker Self-Test
- assigned_items_seen: 1 evidence section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`