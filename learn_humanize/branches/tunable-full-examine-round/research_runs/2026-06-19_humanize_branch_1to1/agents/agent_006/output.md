# agent_006 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-006 `directory` `scripts`
- cursor: `[_]`
- core_role: The `scripts` directory is the shell orchestration layer for the Humanize loop system. It owns command entry-point setup, loop cancellation, PR comment/review retrieval, bot reaction polling, portable timeout handling, monitor display helpers, and gen-plan IO bootstrapping. It does not contain the stop-hook decision engine itself, but it creates and mutates the state files that hooks consume and provides the GitHub/Codex-facing helpers the hooks call. The recursive contents are `scripts/cancel-pr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/check-bot-reactions.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/fetch-pr-comments.sh`, `scripts/humanize.sh`, `scripts/poll-pr-reviews.sh`, `scripts/portable-timeout.sh`, `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/validate-gen-plan-io.sh`, and `scripts/lib/monitor-common.sh`.

- algorithmic_behavior: `setup-rlcr-loop.sh` initializes RLCR sessions under `.humanize/rlcr/<timestamp>` after strict argument, git, plan path, model, and base-branch validation. It enforces one active loop at a time via `find_active_loop`/`find_active_pr_loop`, rejects unsafe plan paths, copies or creates a plan snapshot, records YAML frontmatter in `state.md`, optionally enters skip-implementation review mode, creates `goal-tracker.md`, and renders `round-0-prompt.md` with summary obligations (`scripts/setup-rlcr-loop.sh:120`, `scripts/setup-rlcr-loop.sh:240`, `scripts/setup-rlcr-loop.sh:263`, `scripts/setup-rlcr-loop.sh:313`, `scripts/setup-rlcr-loop.sh:427`, `scripts/setup-rlcr-loop.sh:543`, `scripts/setup-rlcr-loop.sh:594`, `scripts/setup-rlcr-loop.sh:709`, `scripts/setup-rlcr-loop.sh:737`, `scripts/setup-rlcr-loop.sh:862`). `setup-pr-loop.sh` initializes PR review loops under `.humanize/pr-loop/<timestamp>` after bot flag validation, git/`gh`/`codex` checks, PR and fork/base-repo resolution, startup-case detection, optional re-review trigger comment posting, mandatory Claude eyes verification for Case 4/5 with Claude, `state.md` creation, goal tracker rendering, initial PR comment fetch, and round-0 prompt rendering (`scripts/setup-pr-loop.sh:110`, `scripts/setup-pr-loop.sh:179`, `scripts/setup-pr-loop.sh:220`, `scripts/setup-pr-loop.sh:243`, `scripts/setup-pr-loop.sh:283`, `scripts/setup-pr-loop.sh:295`, `scripts/setup-pr-loop.sh:418`, `scripts/setup-pr-loop.sh:443`, `scripts/setup-pr-loop.sh:484`, `scripts/setup-pr-loop.sh:535`, `scripts/setup-pr-loop.sh:561`, `scripts/setup-pr-loop.sh:641`).

  The GitHub helper scripts form a PR-review data pipeline. `fetch-pr-comments.sh` validates PR/output args, resolves the base repo for forks, fetches issue comments, inline review comments, and PR reviews with retries, normalizes them into one JSON stream, deduplicates by ID, optionally filters by timestamp, sorts human comments before bots and newest first, and writes a markdown report (`scripts/fetch-pr-comments.sh:27`, `scripts/fetch-pr-comments.sh:92`, `scripts/fetch-pr-comments.sh:126`, `scripts/fetch-pr-comments.sh:183`, `scripts/fetch-pr-comments.sh:209`, `scripts/fetch-pr-comments.sh:321`, `scripts/fetch-pr-comments.sh:325`, `scripts/fetch-pr-comments.sh:333`, `scripts/fetch-pr-comments.sh:344`). `poll-pr-reviews.sh` returns machine-readable JSON for comments after a trigger timestamp from watched bots; it uses the same fork/base repo strategy, maps `codex` to `chatgpt-codex-connector[bot]`, retries partial API failures as empty arrays, filters by timestamp and bot regex, and emits `comments`, `bots_responded`, `has_new_comments`, and `comment_count` (`scripts/poll-pr-reviews.sh:23`, `scripts/poll-pr-reviews.sh:83`, `scripts/poll-pr-reviews.sh:122`, `scripts/poll-pr-reviews.sh:166`, `scripts/poll-pr-reviews.sh:209`, `scripts/poll-pr-reviews.sh:239`, `scripts/poll-pr-reviews.sh:298`, `scripts/poll-pr-reviews.sh:316`). `check-pr-reviewer-status.sh` classifies PR startup state into cases 1-5 by combining comments/reviews from all three GitHub surfaces, comparing each bot’s newest review timestamp to the latest commit timestamp, and returning JSON with commented/missing reviewers and stale-review status (`scripts/check-pr-reviewer-status.sh:49`, `scripts/check-pr-reviewer-status.sh:94`, `scripts/check-pr-reviewer-status.sh:113`, `scripts/check-pr-reviewer-status.sh:139`, `scripts/check-pr-reviewer-status.sh:150`, `scripts/check-pr-reviewer-status.sh:176`, `scripts/check-pr-reviewer-status.sh:208`, `scripts/check-pr-reviewer-status.sh:256`). `check-bot-reactions.sh` provides two targeted reaction checks: Codex `+1` on the PR issue and Claude `eyes` on a trigger comment, both base-repo aware and paginated; Claude eyes retries before failing (`scripts/check-bot-reactions.sh:86`, `scripts/check-bot-reactions.sh:119`, `scripts/check-bot-reactions.sh:149`, `scripts/check-bot-reactions.sh:161`, `scripts/check-bot-reactions.sh:187`, `scripts/check-bot-reactions.sh:229`, `scripts/check-bot-reactions.sh:259`).

  The cancellation scripts implement state transitions. `cancel-rlcr-loop.sh` finds the newest `.humanize/rlcr/*/` directory, distinguishes `state.md` from `finalize-state.md`, blocks finalize cancellation unless `--force`, writes `.cancel-requested`, and renames the active state to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:69`, `scripts/cancel-rlcr-loop.sh:89`, `scripts/cancel-rlcr-loop.sh:117`, `scripts/cancel-rlcr-loop.sh:134`). `cancel-pr-loop.sh` performs the PR-loop variant, requiring `state.md`, touching `.cancel-requested`, and renaming to `cancel-state.md` (`scripts/cancel-pr-loop.sh:72`, `scripts/cancel-pr-loop.sh:91`, `scripts/cancel-pr-loop.sh:118`). `validate-gen-plan-io.sh` validates and creates a generated plan from a template plus original draft: required `--input`/`--output`, nonempty input, existing output dir, non-existing output target, writable directory, template existence, then copies `prompt-template/plan/gen-plan-template.md` and appends the draft (`scripts/validate-gen-plan-io.sh:29`, `scripts/validate-gen-plan-io.sh:58`, `scripts/validate-gen-plan-io.sh:79`, `scripts/validate-gen-plan-io.sh:95`, `scripts/validate-gen-plan-io.sh:103`, `scripts/validate-gen-plan-io.sh:118`, `scripts/validate-gen-plan-io.sh:133`, `scripts/validate-gen-plan-io.sh:148`).

  The monitoring layer is split between `humanize.sh` and `lib/monitor-common.sh`. `humanize.sh` exposes `humanize monitor rlcr|pr`, parses goal trackers and git status, tails RLCR Codex logs from `$XDG_CACHE_HOME`/`$HOME/.cache/humanize/<sanitized-project>/<timestamp>`, follows PR-loop activity files, redraws fixed status bars, switches to newer sessions/logs, handles truncation/deletion, and restores terminal state on signals (`scripts/humanize.sh:16`, `scripts/humanize.sh:32`, `scripts/humanize.sh:116`, `scripts/humanize.sh:166`, `scripts/humanize.sh:219`, `scripts/humanize.sh:246`, `scripts/humanize.sh:373`, `scripts/humanize.sh:535`, `scripts/humanize.sh:620`, `scripts/humanize.sh:883`, `scripts/humanize.sh:931`, `scripts/humanize.sh:1215`, `scripts/humanize.sh:1303`). `monitor-common.sh` centralizes color helpers, portable file-size reads, latest timestamped session selection, state-file detection, YAML frontmatter lookup, string formatting/truncation, PR-loop phase detection, and goal-tracker parsing (`scripts/lib/monitor-common.sh:32`, `scripts/lib/monitor-common.sh:40`, `scripts/lib/monitor-common.sh:71`, `scripts/lib/monitor-common.sh:154`, `scripts/lib/monitor-common.sh:197`, `scripts/lib/monitor-common.sh:267`, `scripts/lib/monitor-common.sh:390`, `scripts/lib/monitor-common.sh:468`). `portable-timeout.sh` chooses `gtimeout`, GNU `timeout`, Python, or no-timeout fallback, and normalizes timeout expiry to exit code 124 for Python implementations (`scripts/portable-timeout.sh:10`, `scripts/portable-timeout.sh:33`, `scripts/portable-timeout.sh:47`, `scripts/portable-timeout.sh:64`).

- inputs_outputs_state: Primary user inputs arrive through slash-command wrappers in `commands/start-rlcr-loop.md`, `commands/start-pr-loop.md`, `commands/cancel-rlcr-loop.md`, `commands/cancel-pr-loop.md`, and `commands/gen-plan.md`, which call the scripts directly. Setup scripts read CLI flags, `CLAUDE_PROJECT_DIR`, git state, current branch, local/remote base branch refs, plan file content, `gh` PR metadata, and prompt templates. Outputs are local loop directories with managed files: RLCR writes `.humanize/rlcr/<timestamp>/state.md`, `plan.md`, `goal-tracker.md`, `round-0-prompt.md`, and later summary/review paths consumed by hooks; PR loop writes `.humanize/pr-loop/<timestamp>/state.md`, `goal-tracker.md`, `round-0-pr-comment.md`, `round-0-pr-resolve.md` path references, and `round-0-prompt.md`. State transitions are file-name based: active is `state.md`, RLCR finalize is `finalize-state.md`, successful RLCR completion becomes `complete-state.md` in the stop hook, PR approval becomes `approve-state.md`, cancellation becomes `cancel-state.md`, and max-iteration terminal states use `maxiter-state.md`. Cancellation scripts explicitly create `.cancel-requested` and rename active state files. GitHub helpers output either formatted markdown (`fetch-pr-comments.sh`) or JSON (`poll-pr-reviews.sh`, `check-pr-reviewer-status.sh`, `check-bot-reactions.sh`). Monitor scripts output terminal UI only and read state/log files without owning loop decisions.

- gates_or_invariants: The major invariant is single active loop across RLCR and PR-loop modes; both setup scripts query `find_active_loop` and `find_active_pr_loop` and abort if either lane is already active (`scripts/setup-rlcr-loop.sh:240`, `scripts/setup-pr-loop.sh:220`; helper in `hooks/lib/loop-common.sh:798`). RLCR plan validation is strict: one plan path only, optional only in `--skip-impl`; no absolute paths, spaces, shell metacharacters, direct symlinks, parent symlinks, missing/unreadable files, paths outside project, or submodule paths; tracked plan files require `--track-plan-file` and cleanliness, while untracked plan files are required unless tracking is explicit (`scripts/setup-rlcr-loop.sh:263`, `scripts/setup-rlcr-loop.sh:272`, `scripts/setup-rlcr-loop.sh:320`, `scripts/setup-rlcr-loop.sh:326`, `scripts/setup-rlcr-loop.sh:334`, `scripts/setup-rlcr-loop.sh:346`, `scripts/setup-rlcr-loop.sh:359`, `scripts/setup-rlcr-loop.sh:382`, `scripts/setup-rlcr-loop.sh:394`, `scripts/setup-rlcr-loop.sh:408`, `scripts/setup-rlcr-loop.sh:447`). RLCR also requires at least five lines and at least three meaningful non-comment content lines, treating blank lines, shell/YAML comment lines, and HTML comments as non-content (`scripts/setup-rlcr-loop.sh:483`, `scripts/setup-rlcr-loop.sh:493`). YAML safety gates reject unsafe branch/base/model/effort values before state serialization (`scripts/setup-rlcr-loop.sh:561`, `scripts/setup-rlcr-loop.sh:571`, `scripts/setup-rlcr-loop.sh:580`, `scripts/setup-pr-loop.sh:365`, `scripts/setup-pr-loop.sh:374`). Base-branch selection is local-ref only for `codex review`, with user input preferred over remote default, `main`, then `master`, and captures `base_commit` to avoid self-diff drift (`scripts/setup-rlcr-loop.sh:590`, `scripts/setup-rlcr-loop.sh:653`). PR-loop gates require at least one bot flag, numeric PR numbers, installed/authenticated `gh`, installed `codex`, open PR state, safe YAML state fields, and base repo resolution for fork correctness (`scripts/setup-pr-loop.sh:179`, `scripts/setup-pr-loop.sh:255`, `scripts/setup-pr-loop.sh:263`, `scripts/setup-pr-loop.sh:271`, `scripts/setup-pr-loop.sh:326`, `scripts/setup-pr-loop.sh:340`, `scripts/setup-pr-loop.sh:351`). Case 4/5 PR starts must post a trigger comment; if Claude is configured, missing trigger comment ID or absent eyes reaction aborts setup and removes the loop directory (`scripts/setup-pr-loop.sh:443`, `scripts/setup-pr-loop.sh:484`, `scripts/setup-pr-loop.sh:488`, `scripts/setup-pr-loop.sh:505`). Fetch/poll scripts require `gh` and `jq`, numeric PRs, and required timestamp/bot arguments where applicable (`scripts/fetch-pr-comments.sh:103`, `scripts/fetch-pr-comments.sh:112`, `scripts/fetch-pr-comments.sh:117`, `scripts/poll-pr-reviews.sh:83`, `scripts/poll-pr-reviews.sh:108`, `scripts/poll-pr-reviews.sh:113`). Monitor invariants include timestamped session names, state-file priority (`state.md` before `*-state.md`), zsh-safe `find` instead of empty globs, and terminal cleanup on INT/TERM (`scripts/lib/monitor-common.sh:49`, `scripts/lib/monitor-common.sh:55`, `scripts/lib/monitor-common.sh:162`, `scripts/humanize.sh:578`, `scripts/humanize.sh:1174`).

- dependencies_and_callers: Command wrappers call the setup/cancel/gen-plan scripts directly (`commands/start-rlcr-loop.md:13`, `commands/start-pr-loop.md:13`, `commands/gen-plan.md:35`, `commands/cancel-pr-loop.md:14`, `commands/cancel-rlcr-loop.md:14`). `setup-rlcr-loop.sh` and `setup-pr-loop.sh` source `scripts/portable-timeout.sh`; both source `hooks/lib/loop-common.sh`, and PR setup also sources `hooks/lib/template-loader.sh` through the hooks lib path (`scripts/setup-rlcr-loop.sh:27`, `scripts/setup-rlcr-loop.sh:232`, `scripts/setup-pr-loop.sh:33`, `scripts/setup-pr-loop.sh:37`, `scripts/setup-pr-loop.sh:210`). The stop hooks consume these outputs and call back into helpers: `hooks/loop-codex-stop-hook.sh` sources `portable-timeout.sh`, reads `.humanize/rlcr` state, checks plan integrity, todo completion, git status, goal tracker initialization, max iterations, finalize phase, and then builds Codex review prompts (`hooks/loop-codex-stop-hook.sh:43`, `hooks/loop-codex-stop-hook.sh:52`, `hooks/loop-codex-stop-hook.sh:59`, `hooks/loop-codex-stop-hook.sh:340`, `hooks/loop-codex-stop-hook.sh:403`, `hooks/loop-codex-stop-hook.sh:647`, `hooks/loop-codex-stop-hook.sh:725`, `hooks/loop-codex-stop-hook.sh:742`). `hooks/pr-loop-stop-hook.sh` calls `check-bot-reactions.sh` for Codex thumbs-up and Claude eyes, `poll-pr-reviews.sh` for new bot review detection, and `check-pr-reviewer-status.sh` after review cycles (`hooks/pr-loop-stop-hook.sh:459`, `hooks/pr-loop-stop-hook.sh:766`, `hooks/pr-loop-stop-hook.sh:801`, `hooks/pr-loop-stop-hook.sh:985`, `hooks/pr-loop-stop-hook.sh:1534`). Shared bot and YAML helpers used by setup/hooks live in `hooks/lib/loop-common.sh`, including `map_bot_to_author`, `map_author_to_bot`, `build_yaml_list`, and `build_bot_mention_string` (`hooks/lib/loop-common.sh:727`, `hooks/lib/loop-common.sh:741`, `hooks/lib/loop-common.sh:755`, `hooks/lib/loop-common.sh:767`). External command dependencies are `git`, `gh`, `jq`, `codex`, POSIX shell utilities, `stat` variants for Linux/macOS, `tput`, and optional `gtimeout`/GNU `timeout`/Python.

- edge_cases_or_failure_modes: The directory explicitly handles fork PRs by resolving the PR base repo before comments/reactions/reviews, avoiding fork-local PR-number failures in `fetch-pr-comments.sh`, `poll-pr-reviews.sh`, `check-pr-reviewer-status.sh`, `check-bot-reactions.sh`, and `setup-pr-loop.sh`. API fetchers retry three times, but behavior differs: `fetch-pr-comments.sh` records partial markdown warnings after endpoint failures, while `poll-pr-reviews.sh` continues with empty arrays so polling can proceed through transient outages (`scripts/fetch-pr-comments.sh:175`, `scripts/fetch-pr-comments.sh:183`, `scripts/fetch-pr-comments.sh:444`; `scripts/poll-pr-reviews.sh:199`, `scripts/poll-pr-reviews.sh:209`). Lexical ISO timestamp comparison is used throughout for `created_at`/`submitted_at`; this assumes normalized GitHub UTC timestamps. Bot name mapping is hard-coded for Codex and generic `[bot]` for others, so new bot naming patterns may require updates. Plan-file content validation treats markdown `#` headings as comments, so a plan made only of headings is rejected unless it also has non-heading content (`scripts/setup-rlcr-loop.sh:493`, `scripts/setup-rlcr-loop.sh:523`). `validate-gen-plan-io.sh` uses `realpath -m` when available but falls back to the raw path, then writes the output file, so its validations are filesystem-sensitive (`scripts/validate-gen-plan-io.sh:69`, `scripts/validate-gen-plan-io.sh:148`). Cancellation scripts select newest loop with `ls | sort -r`, not the shared timestamp validator; malformed later directory names could affect selection (`scripts/cancel-rlcr-loop.sh:72`, `scripts/cancel-pr-loop.sh:75`). Monitor code detects log truncation/rotation and directory deletion, but it depends on terminal capabilities and can behave poorly in non-interactive contexts; tests mostly inspect source or use `--once` for PR monitor. `portable-timeout.sh` falls back to no timeout when no implementation exists, warning but allowing commands to run unbounded (`scripts/portable-timeout.sh:64`). PR setup’s Case 4/5 trigger path aborts and removes state if trigger ID or Claude eyes cannot be confirmed, which prevents orphaned loops but can discard a freshly created loop on GitHub/API timing issues (`scripts/setup-pr-loop.sh:488`, `scripts/setup-pr-loop.sh:505`, `scripts/setup-pr-loop.sh:516`).

- validation_or_tests: I did not run tests because the branch export is read-only and the assignment requested research notes only; validation here is static inspection of scripts, callers, and test coverage. Existing tests cover the key surfaces. `tests/test-plan-file-validation.sh` validates RLCR plan rejection for absolute paths, missing files/dirs, spaces, shell metacharacters, symlinks, insufficient content, comment-only plans, `--plan-file` conflicts, and Codex model/effort safety (`tests/test-plan-file-validation.sh:89`, `tests/test-plan-file-validation.sh:104`, `tests/test-plan-file-validation.sh:128`, `tests/test-plan-file-validation.sh:169`, `tests/test-plan-file-validation.sh:189`, `tests/test-plan-file-validation.sh:531`, `tests/test-plan-file-validation.sh:567`, `tests/test-plan-file-validation.sh:590`, `tests/test-plan-file-validation.sh:667`, `tests/test-plan-file-validation.sh:701`). `tests/test-pr-loop-scripts.sh` covers PR setup argument parsing, missing/invalid bot flags, max/codex option parsing, PR cancel behavior, and fetch/poll argument validation (`tests/test-pr-loop-scripts.sh:18`, `tests/test-pr-loop-scripts.sh:38`, `tests/test-pr-loop-scripts.sh:117`, `tests/test-pr-loop-scripts.sh:172`, `tests/test-pr-loop-scripts.sh:260`, `tests/test-pr-loop-scripts.sh:328`). `tests/test-pr-loop-system.sh` covers mutual exclusion, reviewer startup cases, Codex thumbs-up, Claude eyes, PR review submissions as valid bot comments, phase detection via `monitor-common.sh`, Case 4/5 stale-review classification, Case 4/5 setup failure cleanup, and PR goal tracker creation (`tests/test-pr-loop-system.sh:120`, `tests/test-pr-loop-system.sh:152`, `tests/test-pr-loop-system.sh:199`, `tests/test-pr-loop-system.sh:223`, `tests/test-pr-loop-system.sh:236`, `tests/test-pr-loop-system.sh:257`, `tests/test-pr-loop-system.sh:1320`, `tests/test-pr-loop-system.sh:1347`, `tests/test-pr-loop-system.sh:1384`, `tests/test-pr-loop-system.sh:1453`). `tests/test-gen-plan.sh` exercises `validate-gen-plan-io.sh` exit codes for missing values, unknown flags, nonexistent/empty input, missing output dir, existing output, and directory output targets (`tests/test-gen-plan.sh:431`, `tests/test-gen-plan.sh:438`, `tests/test-gen-plan.sh:474`, `tests/test-gen-plan.sh:483`, `tests/test-gen-plan.sh:493`, `tests/test-gen-plan.sh:503`). Monitor and timeout coverage appears in `tests/test-monitor-runtime.sh` and `tests/robustness/test-timeout-robustness.sh`, checking terminal restoration/trap setup and timeout detection/exit-code preservation (`tests/test-monitor-runtime.sh:263`, `tests/test-monitor-runtime.sh:305`, `tests/test-monitor-runtime.sh:320`, `tests/test-monitor-runtime.sh:390`, `tests/robustness/test-timeout-robustness.sh:31`, `tests/robustness/test-timeout-robustness.sh:62`, `tests/robustness/test-timeout-robustness.sh:89`).

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`