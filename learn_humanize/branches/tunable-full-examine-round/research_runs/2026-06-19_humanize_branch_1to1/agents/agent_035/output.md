# agent_035 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-035 `file` `scripts/check-pr-reviewer-status.sh`
- cursor: `[_]`
- core_role: Classifies the current GitHub PR reviewer state into startup/re-review cases for the PR loop. It is a runtime decision script, not a passive helper: `scripts/setup-pr-loop.sh` uses its JSON result to choose the initial startup case and decide whether to post bot trigger comments, while `hooks/pr-loop-stop-hook.sh` re-runs it after each round to detect case changes as bot comments arrive.

- algorithmic_behavior: The script runs as a strict Bash program with `set -euo pipefail` at `scripts/check-pr-reviewer-status.sh:29`. It parses one positional PR number plus required `--bots <comma-list>` at `scripts/check-pr-reviewer-status.sh:46-85`. It maps configured bot names to GitHub authors via `map_bot_to_author` at `scripts/check-pr-reviewer-status.sh:94-100`, with a special mapping from `codex` to `chatgpt-codex-connector[bot]` and default `<bot>[bot]` for others. It resolves the PR base repository for fork-aware API calls by first checking the current repo, then parent repo, then falling back to current repo with a warning at `scripts/check-pr-reviewer-status.sh:113-137`. It fetches latest PR commit info at `scripts/check-pr-reviewer-status.sh:140-148`, then fetches three review surfaces: issue comments, inline PR review comments, and PR review submissions at `scripts/check-pr-reviewer-status.sh:150-162`. It combines those surfaces with `jq -s 'add // []'` at `scripts/check-pr-reviewer-status.sh:164-165`. For each requested bot, it filters all combined comments by exact author, records commented vs missing, finds the bot’s newest review timestamp, and marks that bot stale if `latest_commit_at > bot_newest_review_at` at `scripts/check-pr-reviewer-status.sh:176-202`. Classification is then count-based plus stale-aware at `scripts/check-pr-reviewer-status.sh:208-242`: no comments is case 1; partial comments with no stale responders is case 2; all commented and fresh is case 3; all commented with any stale bot is case 4; partial comments with at least one stale bot is case 5. It emits normalized JSON with reviewer arrays, latest commit data, newest review timestamp, and `has_commits_after_reviews` at `scripts/check-pr-reviewer-status.sh:256-272`.

- inputs_outputs_state: Inputs are the PR number, `--bots` comma-separated bot names, the current GitHub repository context from `gh repo view`, PR metadata from `gh pr view`, and comment/review data from `gh api`. It also depends on local `jq`, `gh`, Bash arrays, and `scripts/portable-timeout.sh`, which provides `run_with_timeout` and selects `gtimeout`, GNU `timeout`, Python, or no-timeout fallback at `scripts/portable-timeout.sh:9-76`. Output is a single JSON object on stdout shaped as documented at `scripts/check-pr-reviewer-status.sh:11-20` and emitted at `scripts/check-pr-reviewer-status.sh:256-272`. State is in-memory only: arrays `REVIEWERS_COMMENTED`, `REVIEWERS_MISSING`, `REVIEWERS_STALE`, scalar `NEWEST_REVIEW_AT`, and classification variables `CASE` and `HAS_COMMITS_AFTER_REVIEWS`. The script does not write files or mutate repository state. Its downstream state transitions happen in callers: `scripts/setup-pr-loop.sh:418-428` parses `.case`, latest commit SHA/date, and stale flag into PR loop startup state; `scripts/setup-pr-loop.sh:443-449` posts a trigger comment for case 4 or 5; `hooks/pr-loop-stop-hook.sh:1531-1542` dynamically updates `PR_STARTUP_CASE` if this script reports a new case.

- gates_or_invariants: Required argument gates are enforced before any GitHub API work: missing PR number, missing `--bots`, empty `--bots` value, unknown option, or multiple PR numbers exit with code 1 at `scripts/check-pr-reviewer-status.sh:49-85`. The commit-info fetch is a hard gate: if `gh pr view ... --json headRefOid,commits` fails, the script prints `Error: Failed to fetch PR commit info` and exits at `scripts/check-pr-reviewer-status.sh:140-145`. Comment-surface fetches are soft gates: issue comments, review comments, and PR reviews each fall back to `[]` on API failure at `scripts/check-pr-reviewer-status.sh:153-162`, so partial API failures can bias the classifier toward missing/no-review cases. The classifier invariant is per-bot freshness, not global newest freshness: a bot is stale when that bot’s own newest review precedes the latest commit, even if another bot reviewed later, implemented at `scripts/check-pr-reviewer-status.sh:186-197` and used at `scripts/check-pr-reviewer-status.sh:222-237`. Timestamp ordering assumes ISO-8601 UTC strings so Bash lexicographic `>` matches chronological order.

- dependencies_and_callers: Direct runtime dependencies are `bash`, `gh`, `jq`, and `scripts/portable-timeout.sh` sourced at `scripts/check-pr-reviewer-status.sh:39-40`. GitHub API endpoints used are `repos/<base>/issues/<pr>/comments`, `repos/<base>/pulls/<pr>/comments`, and `repos/<base>/pulls/<pr>/reviews` at `scripts/check-pr-reviewer-status.sh:153-162`. Primary callers are `scripts/setup-pr-loop.sh:418-428`, which defaults to case 1 if this script fails, and `hooks/pr-loop-stop-hook.sh:1531-1542`, which suppresses stderr and ignores failure by leaving status unchanged. Related sibling scripts use similar fork-base resolution patterns, including `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, and `scripts/check-bot-reactions.sh`; this script coordinates with them by using the same base-repo location for comments/reviews on fork PRs.

- edge_cases_or_failure_modes: If base-repo resolution fails and `CURRENT_REPO` is empty, fallback sets `PR_BASE_REPO` to an empty string at `scripts/check-pr-reviewer-status.sh:134-137`, after which the required commit-info fetch should fail and exit. If a comment/review API call fails independently, the failure is silently normalized to no comments for that surface, which can undercount a reviewer while still producing valid JSON. Empty entries in `--bots`, such as a trailing comma, are not trimmed or rejected; they map to `[bot]` and may appear as missing under an empty bot name. Bot matching is exact author equality, so renamed bots or nonstandard GitHub app logins are missed unless covered by `map_bot_to_author`. `PR_REVIEWS` objects use `created_at: .submitted_at` at `scripts/check-pr-reviewer-status.sh:161-162`; if a review submission has null or missing `submitted_at`, the freshness comparison may not behave as intended. Case 5 is an intentional future/distinct branch for partial responders with stale comments, despite the top comment saying case 5 is “like case 4” at `scripts/check-pr-reviewer-status.sh:22-27`. The `newest_review_at` output is global/debugging only; classification does not use it.

- validation_or_tests: System tests directly exercise the classifier. `tests/test-pr-loop-system.sh:152-170` asserts case 1 when all comment sources are empty. `tests/test-pr-loop-system.sh:173-192` asserts case 2 and `reviewers_missing` for partial comments. `tests/test-pr-loop-system.sh:236-249` verifies PR review submissions count as reviewer comments, not only issue/inline comments. `tests/test-pr-loop-system.sh:493-506` asserts case 3 when both configured bots have commented. `tests/test-pr-loop-system.sh:1315-1340` asserts case 4 and `has_commits_after_reviews=true` when all reviewers are stale relative to a newer commit. `tests/test-pr-loop-system.sh:1347-1377` asserts case 5 for partial stale responses. Stop-hook tests mock the transformed `gh --jq` outputs expected by this script at `tests/test-pr-loop-stophook.sh:1191-1305`, including repo/baseRepository and combined API formats. I did not run the test suite because the task is read-only research; inspection commands only were used.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence heading present for the single assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`