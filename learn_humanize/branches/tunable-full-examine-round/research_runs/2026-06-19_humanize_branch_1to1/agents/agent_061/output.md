# agent_061 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-061 `file` `tests/test-pr-loop-hooks.sh`
- cursor: `[_]`
- core_role: `tests/test-pr-loop-hooks.sh` is an executable specification for the PR-loop hook subsystem. It is not a standalone implementation file; it defines `run_hook_tests()` at `tests/test-pr-loop-hooks.sh:15` and is sourced by `tests/test-pr-loop.sh`, which initializes shared test state, sources this file, then calls `run_hook_tests` at `tests/test-pr-loop.sh:18-31`. Within the core algorithm, this file asserts the safety and control-flow contract for PR-loop validators, stop-hook polling, trigger detection, comment aggregation, bot approval tracking, wrong-round protection, and monitor display.
- algorithmic_behavior: The file is organized as ordered test families executed inside `run_hook_tests`: PR-loop validator protections at `tests/test-pr-loop-hooks.sh:26-210`; comment sorting and gate-keeper parsing at `tests/test-pr-loop-hooks.sh:222-466`; stop-hook integration logic at `tests/test-pr-loop-hooks.sh:478-754`; stop-hook runtime tests with mocked `gh` and `git` at `tests/test-pr-loop-hooks.sh:766-1148`; approval-only review handling at `tests/test-pr-loop-hooks.sh:1160-1219`; fixture-backed fetch/poll tests at `tests/test-pr-loop-hooks.sh:1231-1419`; wrong-round validation at `tests/test-pr-loop-hooks.sh:1431-1535`; and monitor display tests at `tests/test-pr-loop-hooks.sh:1547-1621`. The tests combine direct hook execution with inline unit reproductions of the production parsing logic, especially `jq` sort/filter/dedup expressions and YAML-list parsing.
- algorithmic_behavior: Validator behavior is specified by synthetic Claude hook JSON. `Write` attempts to `.humanize/pr-loop/<ts>/state.md` and `round-0-pr-comment.md` must exit `2`, while a correct `round-0-pr-resolve.md` write must exit `0` at `tests/test-pr-loop-hooks.sh:68-172`. Bash-command mutation of PR-loop `state.md` must also be blocked even when no RLCR loop exists at `tests/test-pr-loop-hooks.sh:174-210`. The implementation anchors are `hooks/loop-write-validator.sh:74-93`, `hooks/loop-edit-validator.sh:57-76`, `hooks/loop-bash-validator.sh:403-442`, and shared PR-loop file classifiers in `hooks/lib/loop-common.sh:783-887`.
- algorithmic_behavior: Comment ordering and gate-keeper behavior is specified with `jq`: newest-first sorting uses `fromdateiso8601` at `tests/test-pr-loop-hooks.sh:222-237`; human comments precede bot comments at `tests/test-pr-loop-hooks.sh:239-255`; duplicate comments collapse by `id` at `tests/test-pr-loop-hooks.sh:271-286`; configured bots are parsed from YAML lists at `tests/test-pr-loop-hooks.sh:288-324`; Codex output tables are scanned for `ISSUES`, `APPROVE`, and `WAITING_FOR_BOTS` final markers at `tests/test-pr-loop-hooks.sh:326-457`. Production fetch sorting/dedup mirrors this in `scripts/fetch-pr-comments.sh:321-342`.
- algorithmic_behavior: Stop-hook trigger behavior is modeled and partially run end-to-end. Trigger detection must filter to the current GitHub user and newest bot mention at `tests/test-pr-loop-hooks.sh:538-586`; newer trigger timestamps replace older ones at `tests/test-pr-loop-hooks.sh:588-604`; round `> 0` with no `last_trigger_at` blocks at `tests/test-pr-loop-hooks.sh:606-622`; round `0` must prefer `last_trigger_at` over `started_at`, with `started_at` only as fallback at `tests/test-pr-loop-hooks.sh:624-665`; per-bot timeout is anchored to trigger time, not poll-start time, at `tests/test-pr-loop-hooks.sh:667-696`. The production stop hook parses YAML lists at `hooks/pr-loop-stop-hook.sh:87-174`, detects paginated trigger comments from the current user at `hooks/pr-loop-stop-hook.sh:530-590`, persists newer trigger fields at `hooks/pr-loop-stop-hook.sh:631-670`, requires triggers for later rounds at `hooks/pr-loop-stop-hook.sh:684-736`, and selects the polling `--after` timestamp at `hooks/pr-loop-stop-hook.sh:806-856`.
- algorithmic_behavior: Runtime stop-hook tests create isolated `.humanize/pr-loop/<ts>/state.md` trees, prepend mocked `gh`/`git` binaries to `PATH`, set `CLAUDE_PROJECT_DIR`, and invoke `hooks/pr-loop-stop-hook.sh`. They assert that missing `round-0-pr-resolve.md` blocks exit at `tests/test-pr-loop-hooks.sh:766-843`; trigger detection is reported with a mocked user/comment API at `tests/test-pr-loop-hooks.sh:845-945`; paginated issue comments are aggregated so a trigger on page 2 is found at `tests/test-pr-loop-hooks.sh:947-1050`; and round 0 logs exact use of `last_trigger_at` for `--after` at `tests/test-pr-loop-hooks.sh:1052-1142`.
- algorithmic_behavior: Approval-only review handling is specified by converting empty/null PR review bodies into state placeholders. The inline contract is at `tests/test-pr-loop-hooks.sh:1160-1196`, and bot-pattern matching for approval-only reviews is at `tests/test-pr-loop-hooks.sh:1198-1215`. Production behavior appears in `scripts/fetch-pr-comments.sh:299-319` and `scripts/poll-pr-reviews.sh:275-293`, where empty PR review bodies become `[Review state: <STATE>]`.
- algorithmic_behavior: Fixture-backed tests exercise actual helper scripts through a generated mock `gh`. `setup_fixture_mock_gh` delegates to `tests/setup-fixture-mock-gh.sh` at `tests/test-pr-loop-hooks.sh:1231-1240`; the mock maps GitHub API endpoints to `tests/fixtures/*` at `tests/setup-fixture-mock-gh.sh:74-90`. `fetch-pr-comments.sh` must emit human issue comments, inline review comments, and approval-only review placeholders at `tests/test-pr-loop-hooks.sh:1242-1285`, and respect `--after` filtering at `tests/test-pr-loop-hooks.sh:1287-1320`. `poll-pr-reviews.sh` must return valid JSON with `comments`, `bots_responded`, and filtered late approvals at `tests/test-pr-loop-hooks.sh:1322-1419`; production output shape is built at `scripts/poll-pr-reviews.sh:315-325`.
- algorithmic_behavior: Wrong-round validation establishes the allowed mutable file surface for human/Claude resolution summaries. If `state.md` says `current_round: 2`, writing `round-0-pr-resolve.md` must block, but writing `round-2-pr-resolve.md` must pass at `tests/test-pr-loop-hooks.sh:1431-1497`; editing `round-1-pr-resolve.md` while `current_round: 3` must block at `tests/test-pr-loop-hooks.sh:1499-1535`. The shared round validator reads active PR-loop state and compares the filename round at `hooks/lib/loop-common.sh:837-887`.
- algorithmic_behavior: Monitor tests verify read-only observability of PR-loop state. They create state fixtures with YAML-list bots through `tests/setup-monitor-test-env.sh:20-89`, then source `scripts/humanize.sh` and run `humanize monitor pr --once` at `tests/test-pr-loop-hooks.sh:1547-1621`. Production monitor parsing extracts `configured_bots` and `active_bots` YAML lists at `scripts/humanize.sh:1030-1059` and prints them in once-mode at `scripts/humanize.sh:1232-1261`.
- inputs_outputs_state: Inputs include `TEST_DIR`, `SCRIPT_DIR`, `PROJECT_ROOT`, shell helpers `pass`, `fail`, and `init_test_git_repo` from the sourced harness, JSON hook payloads for `Write`, `Edit`, and `Bash`, mock GitHub API responses, fixture JSON under `tests/fixtures`, PR-loop YAML frontmatter, and external tools `bash`, `jq`, `grep`, `sed`, `gh` mocks, `git` mocks, and production scripts. Outputs are test pass/fail counter increments via `pass`/`fail`, stdout diagnostics, generated temporary state/comment/check/fixture files under `TEST_DIR`, hook JSON decisions from `pr-loop-stop-hook.sh`, markdown output from `fetch-pr-comments.sh`, and JSON output from `poll-pr-reviews.sh`.
- inputs_outputs_state: State transitions under test include creating `.humanize/pr-loop/<timestamp>/state.md`; preserving `configured_bots` while `active_bots` shrinks or becomes empty; writing/persisting `last_trigger_at` and `trigger_comment_id`; requiring `round-N-pr-resolve.md` before stop-hook exit; generating `round-N-pr-comment.md`, `round-N-pr-check.md`, `round-N-pr-feedback.md`, and `round-N-codex-prompt.md` in production stop-hook flow; and recognizing terminal monitor states such as all bots approved. The test file itself leaves cleanup to the parent harness/temp directory rather than maintaining persistent repo state.
- gates_or_invariants: Protected PR-loop state and generated review/comment/prompt/check/feedback files are read-only to Claude writes/edits/Bash mutations. Only the current round’s `round-N-pr-resolve.md` is mutable. `active_bots` and `configured_bots` are YAML lists, with `configured_bots` immutable for full-bot polling and re-add detection. Trigger comments must come from the current GitHub user and mention configured bots. Later rounds require a trigger. Round 0 prefers `last_trigger_at` when available. Bot timeouts are measured from the trigger timestamp. Comments are deduplicated by ID, sorted with humans before bots and newest first, and approval-only PR reviews are not dropped. Monitor output must show configured bots separately from active bots and represent empty active bots as `none` in once-mode.
- dependencies_and_callers: Caller chain is `tests/run-all-tests.sh` includes `test-pr-loop.sh` at `tests/run-all-tests.sh:49-50`; `tests/test-pr-loop.sh` sources `tests/test-pr-loop-hooks.sh` and calls `run_hook_tests`. Direct dependencies are `tests/test-helpers.sh` for pass/fail counters and temp repo setup, `tests/test-pr-loop-lib.sh` for PR-loop test environment initialization, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/pr-loop-stop-hook.sh`, `hooks/lib/loop-common.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/humanize.sh`, `tests/setup-fixture-mock-gh.sh`, `tests/setup-monitor-test-env.sh`, and fixture JSON files under `tests/fixtures`.
- edge_cases_or_failure_modes: Covered edge cases include absent RLCR loop while PR-loop protection is still required; malicious direct writes to `state.md`; direct writes to generated `round-*-pr-comment.md`; missing resolution summary; mixed-user trigger comments; newer trigger replacing old trigger; missing trigger in later rounds; round 0 trigger-vs-start timestamp priority; paginated GitHub comment responses; empty/null PR review body; early comments excluded by `--after`; wrong-round writes and edits; all bots previously approved but a bot later reports issues; and empty `active_bots` monitor display. A notable test limitation is that several gate-keeper checks are inline reproductions of production logic rather than direct calls, so drift between test snippets and implementation remains possible.
- validation_or_tests: This assigned file is itself a validation suite. I inspected it directly and followed its invoked implementation references. I did not execute it because the provided branch export is read-only in this worker profile, while the suite intentionally creates temp repos/files, mock binaries, and PR-loop state. Static inspection confirms it is intended to be run through `tests/test-pr-loop.sh`, not directly, because it only defines `run_hook_tests()` and depends on sourced harness variables/functions.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-061`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`