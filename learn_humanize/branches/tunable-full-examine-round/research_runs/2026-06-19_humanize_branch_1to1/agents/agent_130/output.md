# agent_130 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-130 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR/PR loop state discovery and state parsing under malformed, stale, and concurrent-access conditions. The test validates the behavior of `hooks/lib/loop-common.sh` functions that gate active loop detection, current round extraction, strict state validation, and PR loop discovery. It is core algorithm evidence because hook validators depend on these state readers to decide whether file/tool access should be allowed, blocked, or fail-closed.

- algorithmic_behavior: The script bootstraps a temporary isolated test directory via `setup_test_dir` from `tests/test-helpers.sh:86`, sources `hooks/lib/loop-common.sh`, then runs 22 scenario tests with `pass`/`fail` counters and exits through `print_test_summary` at `tests/robustness/test-concurrent-state-robustness.sh:559`. Its local `create_state_file` helper writes canonical loop frontmatter containing fields such as `current_round`, `max_iterations`, `plan_file`, `start_branch`, `base_branch`, `push_every_round`, and Codex settings at `tests/robustness/test-concurrent-state-robustness.sh:30`. The first section probes tolerant `get_current_round` parsing for whitespace, CRLF, empty values, inline comments, Unicode content, large values, zero, and permission denial. The loop detection section constructs timestamped directories and checks that `find_active_loop` selects only the lexicographically newest directory when it contains `state.md` or `finalize-state.md`, and deliberately ignores older “zombie” loop directories even when they contain state. The concurrency section spawns 10 simultaneous readers against one state file and expects all to read the non-default round `5`, then simulates a writer using temp-file-plus-`mv` atomic replacement while 20 reads occur and requires every read to produce a numeric value. The validation section asserts strict parser rejection for malformed/missing state metadata. The PR section checks `find_active_pr_loop` and YAML list state shape.

- inputs_outputs_state: Inputs are synthetic state files and directories under `$TEST_DIR`, plus functions loaded from `hooks/lib/loop-common.sh`. Key input shapes include canonical frontmatter from `create_state_file` at `tests/robustness/test-concurrent-state-robustness.sh:34`, whitespace variants at lines `62` and `84`, CRLF converted files at line `111`, malformed frontmatter at line `336`, missing-frontmatter content at line `358`, PR loop state at line `401`, and permission-restricted state at line `536`. Outputs are PASS/FAIL lines emitted through `tests/test-helpers.sh:30` and `tests/test-helpers.sh:35`, aggregate counters, and the final shell exit status from `print_test_summary`. State transitions are modeled by file-system state changes: absent loop directory -> empty active loop, newest directory with `state.md` -> active loop, newest directory without state -> no active loop even if older state exists, `finalize-state.md` -> active loop, `cancel-state.md` only -> inactive loop, repeated `state.md.tmp` writes followed by `mv` -> atomically replaced active state.

- gates_or_invariants: `get_current_round` must not crash on bad or unusual input and defaults to `0` when it cannot extract a usable value, matching `hooks/lib/loop-common.sh:179`. The test makes that tolerance explicit for leading whitespace, CRLF, and empty values at `tests/robustness/test-concurrent-state-robustness.sh:91`, `113`, and `134`. Concurrent reads must be deterministic enough that all 10 parallel reads see the real round `5`, not the default `0`, at lines `261` to `278`. Atomic write replacement must keep every observed round numeric during the write window at lines `286` to `323`. Active loop discovery must be newest-directory-only, not newest-valid-directory, matching `hooks/lib/loop-common.sh:151`; this is asserted at lines `186` to `202` and `453` to `467`. `finalize-state.md` counts as active for RLCR at lines `204` to `220`, while `cancel-state.md` does not at lines `469` to `485`. Strict parser malformed/missing-frontmatter cases must return nonzero at lines `343` to `371`.

- dependencies_and_callers: Direct dependency is `tests/test-helpers.sh`, which supplies the temp directory, pass/fail accounting, and final summary. Core implementation dependencies are `get_current_round`, `find_active_loop`, `parse_state_file_strict`, and `find_active_pr_loop` in `hooks/lib/loop-common.sh`. `get_current_round` extracts frontmatter with `sed` and matches `^current_round:` at `hooks/lib/loop-common.sh:182`; `find_active_loop` sorts child directories descending and checks only the newest for `state.md` or `finalize-state.md` at `hooks/lib/loop-common.sh:164`; `parse_state_file_strict` enforces separators, required fields, numeric rounds, and boolean `review_started` at `hooks/lib/loop-common.sh:255`; `find_active_pr_loop` mirrors loop discovery but requires `state.md` at `hooks/lib/loop-common.sh:798`. Main callers include read/write/bash validators, which use active loop detection and strict parsing before blocking wrong-round reads/writes or malformed-state access: `hooks/loop-read-validator.sh:82`, `hooks/loop-write-validator.sh:126`, and `hooks/loop-bash-validator.sh:58`.

- edge_cases_or_failure_modes: Covered edge cases include trailing whitespace, leading whitespace before `current_round`, CRLF separators, empty values, YAML comments, concurrent reads, read-during-atomic-write, malformed YAML-like content, missing frontmatter, Unicode body content, PR state lists, stale/older directories, cancellation state, very large rounds, round `0`, and unreadable state files. A notable specification detail is that “malformed YAML” in Test 13 lacks the newer strict required fields such as `review_started` and `base_branch`, so its nonzero result may be caused by missing required fields rather than true YAML syntax parsing; the test still validates fail-closed behavior. The file header mentions “File locking semantics” at `tests/robustness/test-concurrent-state-robustness.sh:10`, but the body does not exercise `flock` or lock-file coordination directly; concurrency is validated through simultaneous reads and atomic `mv` replacement instead.

- validation_or_tests: This file is itself the validation artifact. It contains 22 checks grouped into state parsing, loop detection, concurrent reads, strict validation, PR loop state, stale loop detection, and miscellaneous edge cases. It is executable (`-rwxr-xr-x`) and intended to be run as a shell test. I did not execute it because this worker assignment requested read-only research notes and no file modification; the script would create temporary files outside the branch export via `mktemp`, but direct execution was not necessary to inspect its algorithmic coverage.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1; the single item evidence header contains the assigned item_id exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`