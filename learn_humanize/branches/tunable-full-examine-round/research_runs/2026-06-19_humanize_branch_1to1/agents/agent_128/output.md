# agent_128 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-128 `file` `tests/robustness/test-base-branch-detection.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR code-review base branch selection. The test isolates and mirrors the auto-detection algorithm used by `scripts/setup-rlcr-loop.sh` so branch detection can be validated without running the full loop setup and its external dependencies; see `tests/robustness/test-base-branch-detection.sh:21-36`.
- algorithmic_behavior: The local `detect_base_branch(project_root, git_timeout)` helper implements a priority chain: remote `origin` default branch from `git remote show origin`, then local `main`, then local `master`, then failure; see `tests/robustness/test-base-branch-detection.sh:37-64`. It wraps git calls with `run_with_timeout` and deliberately appends `|| true` to the remote-default pipeline so `set -euo pipefail` does not abort when `origin` is absent; see `tests/robustness/test-base-branch-detection.sh:41-45`. The real setup script’s broader priority is user input, remote default if locally present, local `main`, local `master`; see `scripts/setup-rlcr-loop.sh:592-643`. The test file’s header mentions user-specified `--base-branch` priority at `tests/robustness/test-base-branch-detection.sh:5-10`, but this particular test harness only exercises the auto-detection branch, not the explicit argument path.
- inputs_outputs_state: Inputs are synthetic git repositories created under `TEST_DIR`, plus a timeout parameter passed to `run_with_timeout`; see `tests/robustness/test-base-branch-detection.sh:24`, `tests/robustness/test-base-branch-detection.sh:38-39`. Output from `detect_base_branch` is the selected branch name on stdout (`main` or `master`) and exit `0`, or no branch output with return `1`; see `tests/robustness/test-base-branch-detection.sh:45-63`. Test state is tracked through shared helper counters `TESTS_PASSED`, `TESTS_FAILED`, and `TESTS_SKIPPED` from `tests/test-helpers.sh:22-24`, with `pass`, `fail`, and `print_test_summary` updating/reporting status at `tests/test-helpers.sh:30-78`. In production, the selected `BASE_BRANCH` is written into RLCR state frontmatter as `base_branch`, alongside the captured `base_commit`; see `scripts/setup-rlcr-loop.sh:653-660` and `scripts/setup-rlcr-loop.sh:709-724`.
- gates_or_invariants: The detection invariant is deterministic priority ordering: remote default only if non-empty and not `(unknown)`, then local `main`, then local `master`, otherwise failure; see `tests/robustness/test-base-branch-detection.sh:41-63`. The production invariant is stricter: explicit `--base-branch` must exist as a local branch, and a remote-only branch is rejected with fetch guidance because `codex review --base` requires a local ref; see `scripts/setup-rlcr-loop.sh:594-611`. Production auto-detection also verifies that the remote default exists locally before accepting it; see `scripts/setup-rlcr-loop.sh:617-623`. Branch names are later gated for YAML safety by rejecting colon, hash, quotes, backticks, or newlines; see `scripts/setup-rlcr-loop.sh:645-650`. Downstream strict state parsing requires `base_branch`; see `hooks/lib/loop-common.sh:246-305`, and the stop hook blocks pre-1.5.0 state files missing it at `hooks/loop-codex-stop-hook.sh:180-208`.
- dependencies_and_callers: The test depends on `tests/test-helpers.sh` for temp directory setup and result reporting, sourced at `tests/robustness/test-base-branch-detection.sh:18`; it depends on `scripts/portable-timeout.sh` for portable timeout execution, sourced at `tests/robustness/test-base-branch-detection.sh:19`. `run_with_timeout` chooses `gtimeout`, GNU `timeout`, Python, or no timeout; see `scripts/portable-timeout.sh:9-76`. The production caller is `/humanize:start-rlcr-loop`, whose command metadata invokes `scripts/setup-rlcr-loop.sh`; see `commands/start-rlcr-loop.md:1-14`. The selected base later feeds `codex review`; the hook prefers fixed `BASE_COMMIT` over `BASE_BRANCH` to avoid a moving branch reference when reviewing work on the base branch itself; see `hooks/loop-codex-stop-hook.sh:921-978`.
- edge_cases_or_failure_modes: Covered edge cases include no `origin` with local `main`, no `origin` with only local `master`, no `origin` and neither `main` nor `master`, and a simulated `origin` remote; see `tests/robustness/test-base-branch-detection.sh:67-191`. The missing-origin case is explicitly protected by the remote-default pipeline guard at `tests/robustness/test-base-branch-detection.sh:41-44`. A repository whose only branch is `develop` must return failure, validated at `tests/robustness/test-base-branch-detection.sh:128-149`. The simulated remote test accepts either remote-default detection or fallback to local `main`, acknowledging that `git remote show origin` can fail or omit a usable default in local bare-remote setups; see `tests/robustness/test-base-branch-detection.sh:181-190`. Important untested production cases are explicit `--base-branch`, explicit branch exists only on remote, remote default exists but is not local, YAML-unsafe branch names, and `base_commit` capture failure.
- validation_or_tests: The file defines four executable scenarios. Test 1 creates a no-remote repo on `main` and expects `main`; see `tests/robustness/test-base-branch-detection.sh:73-93`. Test 2 creates a no-remote repo on `master`, deletes any `main`, and expects `master`; see `tests/robustness/test-base-branch-detection.sh:100-122`. Test 3 creates a `develop`-only repo and expects detection failure; see `tests/robustness/test-base-branch-detection.sh:129-149`. Test 4 creates a bare remote, pushes `main`, and treats any non-failure branch detection as acceptable while specifically expecting `main` when remote/default or local fallback works; see `tests/robustness/test-base-branch-detection.sh:156-190`. I inspected the test and related implementation read-only; I did not execute the test because this branch export is read-only and the harness creates temporary repositories and commits.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-128` appears once as an Item Evidence section.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`