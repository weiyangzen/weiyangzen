# agent_135 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-135 `file` `tests/robustness/test-path-validation-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR plan-file path validation. The file drives the production entrypoint `scripts/setup-rlcr-loop.sh` through isolated throwaway repositories and asserts that unsafe plan paths/content are rejected before loop creation or later setup side effects. It is core algorithm evidence because it encodes the security boundary for user-supplied plan-file inputs.

- algorithmic_behavior: The test initializes a temporary directory, creates a minimal Git repo with one commit, and defines `create_valid_plan` to generate a plan with enough lines/content for the production validator (`tests/robustness/test-path-validation-robustness.sh:16-44`). The central classifier `test_path_validation` removes any active `.humanize/rlcr` state, runs `CLAUDE_PROJECT_DIR="$TEST_DIR" bash "$PROJECT_ROOT/scripts/setup-rlcr-loop.sh" "$plan_path"`, then maps output to accept/reject: known path/content validation errors mean rejection, while reaching `requires codex` or `must be gitignored` means all earlier path/content gates passed (`tests/robustness/test-path-validation-robustness.sh:55-92`). The suite then executes 31 cases: 5 ordinary valid paths, 17 invalid metacharacter/absolute/space cases, 3 symlink cases, 2 long/deep-path allowances, and 5 content/existence/type failures (`tests/robustness/test-path-validation-robustness.sh:101-466`).

- inputs_outputs_state: Input is one candidate plan path string plus files/directories/symlinks created inside `TEST_DIR`. Output is not parsed as structured data; it is reduced to shell success/failure through `pass`, `fail`, and `print_test_summary` from `tests/test-helpers.sh:30-78`. State transitions are local to the temporary repo: setup creates a Git repository and commit (`tests/robustness/test-path-validation-robustness.sh:18-27`), individual cases add candidate plan files or symlinks, and each validation attempt deletes `.humanize/rlcr` first to avoid false “loop already active” failures (`tests/robustness/test-path-validation-robustness.sh:62-67`). The test exits with the accumulated summary code (`tests/robustness/test-path-validation-robustness.sh:461-466`).

- gates_or_invariants: The asserted production invariants are: plan path must be relative (`scripts/setup-rlcr-loop.sh:320-324`); must not contain whitespace (`scripts/setup-rlcr-loop.sh:326-332`); must not contain shell metacharacters such as semicolon, pipe, dollar, backticks, brackets, glob characters, tilde, or backslash (`scripts/setup-rlcr-loop.sh:334-341`); plan file itself must not be a symlink (`scripts/setup-rlcr-loop.sh:346-350`); parent directory must exist (`scripts/setup-rlcr-loop.sh:352-357`); no parent segment may be a symlink (`scripts/setup-rlcr-loop.sh:359-380`); target must be an existing readable regular file (`scripts/setup-rlcr-loop.sh:382-391`); resolved path must remain under `PROJECT_ROOT` (`scripts/setup-rlcr-loop.sh:394-406`); optional submodule exclusion applies when `.gitmodules` exists (`scripts/setup-rlcr-loop.sh:408-421`); content must have at least 5 physical lines and at least 3 non-comment/non-blank content lines (`scripts/setup-rlcr-loop.sh:483-536`). The test also encodes allowed characters/structures: dash, underscore, dots, nested directories, long filenames if the filesystem accepts them, and deep 10-level paths (`tests/robustness/test-path-validation-robustness.sh:120-148`, `366-391`).

- dependencies_and_callers: Direct dependency is `tests/test-helpers.sh`, especially `setup_test_dir`, `pass`, `fail`, and `print_test_summary` (`tests/test-helpers.sh:30-89`). The production dependency under test is `scripts/setup-rlcr-loop.sh`, which itself sources `scripts/portable-timeout.sh` for bounded Git calls (`scripts/setup-rlcr-loop.sh:27-30`; `scripts/portable-timeout.sh:31-71`) and `hooks/lib/loop-common.sh` for active-loop detection (`scripts/setup-rlcr-loop.sh:232-243`; `hooks/lib/loop-common.sh:151-174`). The test assumes `git` is available because it initializes and commits in the temporary repo. It does not require Codex to be installed; reaching the Codex prerequisite failure is treated as proof that path/content validation already passed (`tests/robustness/test-path-validation-robustness.sh:76-79`; `scripts/setup-rlcr-loop.sh:543-549`).

- edge_cases_or_failure_modes: Negative cases cover command-injection and shell-expansion vectors: absolute paths, whitespace, semicolon, pipe, dollar expansion, command substitution backticks, redirection brackets, background ampersand, globs, backslash escapes, tilde expansion, grouping/braces/brackets, hash, and exclamation (`tests/robustness/test-path-validation-robustness.sh:158-311`). Symlink coverage includes direct plan-file symlink, symlinked parent directory traversal, and a symlink chain (`tests/robustness/test-path-validation-robustness.sh:321-356`). Content coverage rejects empty files, comment-only files, files with fewer than 5 lines, nonexistent paths, and directories masquerading as `.md` files (`tests/robustness/test-path-validation-robustness.sh:404-459`). A notable behavioral nuance is that “valid path” cases may still fail later because the plan is not gitignored or Codex is missing; the test intentionally classifies those later failures as path-validation success (`tests/robustness/test-path-validation-robustness.sh:76-84`). Another nuance: the production validator treats markdown headings starting with `#` as comments for content counting, so the fixture relies on body/task lines to reach the 3-content-line threshold (`scripts/setup-rlcr-loop.sh:493-528`).

- validation_or_tests: This file is itself the validation harness. The expected execution is `bash tests/robustness/test-path-validation-robustness.sh` from the repository root. It should report passes for acceptance of safe relative paths and rejection of unsafe or insufficient inputs, then return the summary status from `print_test_summary` (`tests/robustness/test-path-validation-robustness.sh:465-466`). I did not run the full test because the branch export is read-only and the environment showed sandbox-denied Git/Xcode cache writes when probing repository status; I inspected the file and referenced production validator directly instead.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1; the assigned item section appears exactly once`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`