# agent_072 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-072 `file` `tests/test-zsh-monitor-safety.sh`
- cursor: `[_]`
- core_role: Executable zsh-specific safety specification for the Humanize monitor runtime. The file’s role is to prove that monitor discovery logic is safe under zsh’s `nomatch` behavior, especially for empty directories, dotfile-only directories, and absent `*-state.md` or Codex log files. It is explicitly declared as a zsh test at `tests/test-zsh-monitor-safety.sh:1` and documents that it must verify no zsh `"no matches found"` failures at `tests/test-zsh-monitor-safety.sh:5` through `tests/test-zsh-monitor-safety.sh:9`.
- algorithmic_behavior: The test initializes an isolated runtime tree under `/tmp/test-zsh-humanize-$$`, switches into it, redirects cache state through `XDG_CACHE_HOME`, and removes the tree on exit (`tests/test-zsh-monitor-safety.sh:48` through `tests/test-zsh-monitor-safety.sh:60`). It keeps explicit pass/fail counters through `pass()` and `fail()` helpers (`tests/test-zsh-monitor-safety.sh:24` through `tests/test-zsh-monitor-safety.sh:36`) and exits nonzero if any check fails (`tests/test-zsh-monitor-safety.sh:333` through `tests/test-zsh-monitor-safety.sh:340`). Its central algorithmic assertion is that monitor directory scans must use `find`-driven read loops rather than bare globs. The mirrored session scan reads `find "$loop_dir" -mindepth 1 -maxdepth 1 -type d`, skips empty/non-directory rows, accepts only timestamp directory names, and lexicographically selects the latest timestamp (`tests/test-zsh-monitor-safety.sh:99` through `tests/test-zsh-monitor-safety.sh:115`). The state-file scan first prefers `state.md`, then searches `*-state.md` via `find`, derives the stop reason from the filename suffix, and falls back to `|unknown` (`tests/test-zsh-monitor-safety.sh:172` through `tests/test-zsh-monitor-safety.sh:197`). The cache-log scan similarly uses `find "$cache_dir" -maxdepth 1 -name 'round-*-codex-run.log' -type f` and expects zero matches to be a valid empty result (`tests/test-zsh-monitor-safety.sh:221` through `tests/test-zsh-monitor-safety.sh:235`). Test 8 demonstrates the prior hazardous pattern `.humanize/rlcr/*` and contrasts it with the new `find` loop (`tests/test-zsh-monitor-safety.sh:295` through `tests/test-zsh-monitor-safety.sh:321`).
- inputs_outputs_state: Inputs are the repository’s `scripts/humanize.sh`, an executable zsh interpreter, filesystem fixtures created inside the temp test directory, and the `XDG_CACHE_HOME` override (`tests/test-zsh-monitor-safety.sh:52` through `tests/test-zsh-monitor-safety.sh:54`, `tests/test-zsh-monitor-safety.sh:72`). Outputs are console pass/fail lines, accumulated `TESTS_PASSED` and `TESTS_FAILED` counters, and final process status 0 or 1 (`tests/test-zsh-monitor-safety.sh:27` through `tests/test-zsh-monitor-safety.sh:36`, `tests/test-zsh-monitor-safety.sh:326` through `tests/test-zsh-monitor-safety.sh:340`). Persistent state is intentionally avoided: runtime state is confined to `/tmp/test-zsh-humanize-$$` and cleaned via trap (`tests/test-zsh-monitor-safety.sh:48` through `tests/test-zsh-monitor-safety.sh:60`). State transitions covered by fixtures are: missing or empty RLCR session set to empty latest session; dotfile-only RLCR contents still produce no session; session directory without matching state file yields `|unknown`; empty cache yields zero logs; two timestamp sessions select the later timestamp (`tests/test-zsh-monitor-safety.sh:86` through `tests/test-zsh-monitor-safety.sh:124`, `tests/test-zsh-monitor-safety.sh:133` through `tests/test-zsh-monitor-safety.sh:157`, `tests/test-zsh-monitor-safety.sh:166` through `tests/test-zsh-monitor-safety.sh:205`, `tests/test-zsh-monitor-safety.sh:214` through `tests/test-zsh-monitor-safety.sh:235`, `tests/test-zsh-monitor-safety.sh:246` through `tests/test-zsh-monitor-safety.sh:273`).
- gates_or_invariants: The file enforces strict shell error handling with `set -euo pipefail` (`tests/test-zsh-monitor-safety.sh:12` through `tests/test-zsh-monitor-safety.sh:13`). It gates the whole spec on actually running under zsh by requiring a non-empty `ZSH_VERSION` (`tests/test-zsh-monitor-safety.sh:276` through `tests/test-zsh-monitor-safety.sh:286`). Every scan invariant treats empty `find` results as normal, not exceptional. Session names must match the timestamp regex `YYYY-MM-DD_HH-MM-SS` before they participate in latest-session selection (`tests/test-zsh-monitor-safety.sh:109`, `tests/test-zsh-monitor-safety.sh:145`, `tests/test-zsh-monitor-safety.sh:259`). `state.md` has priority over terminal `*-state.md` files (`tests/test-zsh-monitor-safety.sh:177` through `tests/test-zsh-monitor-safety.sh:180`). The test’s failure oracle repeatedly checks captured output for `"no matches found"` so zsh glob expansion errors are treated as regressions (`tests/test-zsh-monitor-safety.sh:73` through `tests/test-zsh-monitor-safety.sh:77`, `tests/test-zsh-monitor-safety.sh:118` through `tests/test-zsh-monitor-safety.sh:124`, `tests/test-zsh-monitor-safety.sh:199` through `tests/test-zsh-monitor-safety.sh:205`).
- dependencies_and_callers: The test depends on `zsh`, core POSIX-style shell tools (`mkdir`, `rm`, `touch`, `find`, `basename`), and `scripts/humanize.sh` (`tests/test-zsh-monitor-safety.sh:72`, `tests/test-zsh-monitor-safety.sh:91` through `tests/test-zsh-monitor-safety.sh:92`). `scripts/humanize.sh` sources shared monitor helpers from `scripts/lib/monitor-common.sh` (`scripts/humanize.sh:6` through `scripts/humanize.sh:10`). The production helper `monitor_find_latest_session` uses the same `find` loop specifically to avoid zsh `"no matches found"` errors (`scripts/lib/monitor-common.sh:37` through `scripts/lib/monitor-common.sh:63`), and production `monitor_find_state_file` implements the same `state.md` then `*-state.md` search contract (`scripts/lib/monitor-common.sh:147` through `scripts/lib/monitor-common.sh:188`). The RLCR monitor wraps those helpers at `scripts/humanize.sh:237` through `scripts/humanize.sh:240` and `scripts/humanize.sh:344` through `scripts/humanize.sh:347`; its Codex log finder also uses `find` for session and cache-log traversal (`scripts/humanize.sh:280` through `scripts/humanize.sh:339`). The suite runner includes this file in `TEST_SUITES` and marks it as zsh-only, invoking `zsh "$suite_path"` when `zsh` is available (`tests/run-all-tests.sh:32` through `tests/run-all-tests.sh:52`, `tests/run-all-tests.sh:75` through `tests/run-all-tests.sh:114`).
- edge_cases_or_failure_modes: Covered edge cases include sourcing `humanize.sh` under zsh without glob errors, an empty `.humanize/rlcr`, dotfile-only RLCR contents, missing `*-state.md`, empty cache log directory, multiple valid timestamp sessions, and explicit confirmation that zsh is the active interpreter (`tests/test-zsh-monitor-safety.sh:63` through `tests/test-zsh-monitor-safety.sh:77`, `tests/test-zsh-monitor-safety.sh:79` through `tests/test-zsh-monitor-safety.sh:124`, `tests/test-zsh-monitor-safety.sh:126` through `tests/test-zsh-monitor-safety.sh:157`, `tests/test-zsh-monitor-safety.sh:159` through `tests/test-zsh-monitor-safety.sh:205`, `tests/test-zsh-monitor-safety.sh:207` through `tests/test-zsh-monitor-safety.sh:235`, `tests/test-zsh-monitor-safety.sh:239` through `tests/test-zsh-monitor-safety.sh:273`, `tests/test-zsh-monitor-safety.sh:276` through `tests/test-zsh-monitor-safety.sh:286`). The main regression mode is any reintroduction of unmatched glob iteration, which would surface as `no matches found`; the test also documents the old unsafe `.humanize/rlcr/*` pattern in a controlled subshell (`tests/test-zsh-monitor-safety.sh:300` through `tests/test-zsh-monitor-safety.sh:321`). A secondary limitation is that several checks duplicate production logic locally rather than calling only the real helper functions, so they protect the intended algorithm shape but can drift if production behavior changes without equivalent test updates.
- validation_or_tests: This file is itself validation. It is executable (`-rwxr-xr-x`) and 10,665 bytes in this branch export. It is part of `tests/run-all-tests.sh` and is dispatched with zsh rather than bash (`tests/run-all-tests.sh:49`, `tests/run-all-tests.sh:75` through `tests/run-all-tests.sh:78`, `tests/run-all-tests.sh:106` through `tests/run-all-tests.sh:114`). I inspected the file directly with line numbers and followed the production references in `scripts/humanize.sh`, `scripts/lib/monitor-common.sh`, and `tests/run-all-tests.sh`. I did not modify files. A read-only `git ls-files -s` metadata check was attempted but was blocked by the sandbox because the macOS developer tooling tried to create cache files under `/tmp`; non-git read checks confirmed file size and executable mode.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 Item Evidence section present for the single assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`