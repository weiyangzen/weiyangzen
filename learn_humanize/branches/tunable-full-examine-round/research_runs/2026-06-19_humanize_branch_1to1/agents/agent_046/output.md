# agent_046 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-046 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: This executable Bash helper is a focused fixture generator for PR monitor tests. It is not production monitor code, but it encodes executable expectations for the monitor’s YAML frontmatter parsing of `configured_bots` and `active_bots`. Its produced state trees are consumed by `tests/test-pr-loop-hooks.sh` monitor tests at lines 1553, 1580, and 1602, then read by `humanize monitor pr --once` through `scripts/humanize.sh`.
- algorithmic_behavior: The script enables strict shell mode with `set -euo pipefail` at `tests/setup-monitor-test-env.sh:10`, reads positional inputs `TEST_DIR` and `TEST_NAME` at lines 12-13, validates that `TEST_DIR` is present at lines 15-18, then dispatches on `TEST_NAME` at lines 20-89. Each recognized case creates exactly one timestamped PR-loop session directory under `$TEST_DIR/.humanize/pr-loop/<timestamp>` and writes a `state.md` file containing YAML frontmatter only. The three cases model distinct monitor parsing states: `yaml_list` writes both configured and active bots as two-item YAML lists at lines 31-36; `configured` writes configured bots as `claude,codex` but active bots as only `codex` at lines 53-57; `empty` writes a key-only `active_bots:` field at lines 74-78 to represent no remaining reviewers. On success it echoes the root test directory at line 91.
- inputs_outputs_state: Input 1 is the target fixture root path (`$1`), required. Input 2 is the fixture variant (`$2`), optional with default `default`, though `default` is intentionally not a valid case and therefore fails unless the caller supplies one of the named variants. Outputs are filesystem side effects: a `.humanize/pr-loop/<session>/state.md` file plus stdout containing `$TEST_DIR`. The generated state fields include `current_round`, `max_iterations`, `pr_number`, `start_branch`, `configured_bots`, `active_bots`, `codex_model`, `codex_effort`, and `started_at` at lines 26-40, 48-61, and 69-81. There is no persistent in-process state beyond `TIMESTAMP`; the only state transition is from no fixture directory to a monitor-discoverable active session because the file is named `state.md`.
- gates_or_invariants: The required directory argument is a hard gate at lines 15-18. The fixture type gate rejects unknown variants and prints the allowed set `yaml_list, configured, empty` at lines 84-87. Generated session names use the monitor’s expected timestamp pattern, e.g. `2026-01-18_16-00-00`, which matches `monitor_find_latest_session`’s regex in `scripts/lib/monitor-common.sh:55`. Each fixture writes `state.md`, which `monitor_find_state_file` treats as an active loop before considering terminal `*-state.md` files in `scripts/lib/monitor-common.sh:162-165`. The YAML list indentation is significant: production monitor parsing only extracts list entries matching `^  - ` under exact key lines `configured_bots:` and `active_bots:` in `scripts/humanize.sh:1042-1043`.
- dependencies_and_callers: Direct callers are the PR-loop hook tests: `test_monitor_yaml_list_parsing`, `test_monitor_configured_bots`, and `test_monitor_empty_active_bots` in `tests/test-pr-loop-hooks.sh:1548-1621`. Those tests create a temporary subdirectory, invoke this helper, `cd` into the generated project root, source `scripts/humanize.sh`, and run `humanize monitor pr --once`. The monitor implementation discovers `.humanize/pr-loop` at `scripts/humanize.sh:955-960`, selects the latest timestamped session via `monitor_find_latest_session` at `scripts/humanize.sh:962-965` and `scripts/lib/monitor-common.sh:40-63`, detects `state.md` via `monitor_find_state_file` at `scripts/lib/monitor-common.sh:154-188`, parses YAML frontmatter in `_pr_parse_state_md` at `scripts/humanize.sh:1030-1059`, and prints `Configured Bots` / `Active Bots` in one-shot mode at `scripts/humanize.sh:1256-1257`. The fixture format mirrors the production state writer in `scripts/setup-pr-loop.sh:535-554`, where `configured_bots` and `active_bots` are stored as YAML lists built from the bot array initialized at `scripts/setup-pr-loop.sh:192-202`.
- edge_cases_or_failure_modes: Calling without `$TEST_DIR` exits with usage and code 1. Calling with no second argument uses `TEST_NAME=default`, which falls into the unknown-case failure path because `default` is not implemented; this makes the second argument effectively required despite the default assignment. If `$TEST_DIR` contains spaces or shell-sensitive characters, the script quotes path expansions around `mkdir` and redirection targets, so the fixture path itself is handled safely. Existing fixture sessions with the same hardcoded timestamps are overwritten because `cat > state.md` truncates files. The `empty` fixture uses a blank YAML key rather than an empty list (`[]`), which exercises the production parser’s fallback from no extracted list items to `active_bots=none` at `scripts/humanize.sh:1054`. One notable test-contract edge: interactive status-bar mode prints empty active bots as `all approved` at `scripts/humanize.sh:1133-1136`, while `--once` prints `${active_bots:-none}` at `scripts/humanize.sh:1257`; the caller test expects `Active Bots:.*none` in `tests/test-pr-loop-hooks.sh:1610-1614`, so this helper specifically supports the one-shot parsing contract rather than the interactive wording.
- validation_or_tests: I inspected the file directly with line numbers and confirmed it is executable ASCII shell (`-rwxr-xr-x`, 2066 bytes). I ran read-only syntax checks: `bash -n tests/setup-monitor-test-env.sh`, `bash -n tests/test-pr-loop-hooks.sh`, `bash -n scripts/humanize.sh`, and `bash -n scripts/lib/monitor-common.sh`; these completed without parser errors. I also searched references with `rg`, confirming the helper’s direct invocations in `tests/test-pr-loop-hooks.sh` and the production monitor parse/display paths in `scripts/humanize.sh` and `scripts/lib/monitor-common.sh`. A `git status --short` check could not complete because this branch export is not a Git checkout in the restricted environment and the toolchain attempted to create denied cache files under `/tmp`; no repository files were modified.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1 item evidence section present above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`