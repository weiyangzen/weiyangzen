# agent_103 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-103 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: This is a block-message template used by the RLCR loop read validator when an agent attempts to read a round prompt or summary file from outside the active loop directory. The template is not executable by itself, but it is part of the enforcement surface for loop-file freshness and location correctness. It turns a failed path-location gate into actionable stderr guidance for the agent.
- algorithmic_behavior: The template renders a “Wrong File Location” message with three substituted variables: `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND`. Its main behavior is to tell the agent that loop files must be read from the active loop directory, then enumerate the current prompt and summary paths as `{{ACTIVE_LOOP_DIR}}/round-{{CURRENT_ROUND}}-prompt.md` and `{{ACTIVE_LOOP_DIR}}/round-{{CURRENT_ROUND}}-summary.md` at `prompt-template/block/wrong-file-location.md:3-7`. It also preserves an escape hatch for intentional manual inspection by saying to use `cat {{FILE_PATH}}` at `prompt-template/block/wrong-file-location.md:9`; this matters because the hook blocks the `Read` tool, not arbitrary shell reads.
- inputs_outputs_state: Inputs are template variables supplied by `hooks/loop-read-validator.sh:127-130`: the requested path, the discovered active loop directory, and the parsed current round. The output is rendered markdown written to stderr by the read validator before it exits with status `2` at `hooks/loop-read-validator.sh:127-131`. The template does not mutate state. State feeding the render comes from `find_active_loop` selecting the newest active `.humanize/rlcr` loop with `state.md` or `finalize-state.md` at `hooks/lib/loop-common.sh:151-174`, then `parse_state_file_strict` reading and validating `current_round` from the chosen state file at `hooks/loop-read-validator.sh:88-99` and `hooks/lib/loop-common.sh:246-329`.
- gates_or_invariants: The associated gate runs only for `Read` tool calls, because `hooks/hooks.json:33-39` registers `hooks/loop-read-validator.sh` for the `Read` matcher and `hooks/loop-read-validator.sh:39-41` exits for other tools. The validator first requires valid hook JSON, rejects excessive nesting, and requires `tool_input.file_path` at `hooks/loop-read-validator.sh:25-48`. The wrong-location template is rendered only after the requested file is recognized as a round `summary` or `prompt` file at `hooks/loop-read-validator.sh:69-71`, an active loop exists at `hooks/loop-read-validator.sh:80-86`, the filename contains an extractable round number at `hooks/loop-read-validator.sh:105-108`, and the path is not inside `.humanize/rlcr/` at `hooks/loop-read-validator.sh:122-131`. The helper defining “inside the loop directory” is a substring check for `.humanize/rlcr/` at `hooks/lib/loop-common.sh:712-716`. Template rendering itself is single-pass, so substituted values containing `{{OTHER_VAR}}` are not recursively expanded; this invariant is documented and implemented in `hooks/lib/template-loader.sh:50-132`.
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh`, specifically `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-file-location.md"` at `hooks/loop-read-validator.sh:127`. The loader comes through `hooks/lib/loop-common.sh`, which sources `hooks/lib/template-loader.sh` and initializes `TEMPLATE_DIR` from the plugin root at `hooks/lib/loop-common.sh:139-148`; `get_template_dir` maps the hooks library location to `prompt-template` at `hooks/lib/template-loader.sh:24-31`. Rendering uses `load_and_render_safe`, which falls back to an inline message if the template is missing or empty at `hooks/lib/template-loader.sh:167-203`. Related sibling templates include `prompt-template/block/wrong-round-file.md` for stale round numbers and `prompt-template/block/wrong-directory-path.md` for path mismatch inside `.humanize/rlcr/`; the read validator invokes those later gates at `hooks/loop-read-validator.sh:138-167`.
- edge_cases_or_failure_modes: If no active loop directory is found, the validator exits success before this template can render at `hooks/loop-read-validator.sh:84-86`. If the state file is malformed, the read is blocked with a generic malformed-state error before template rendering at `hooks/loop-read-validator.sh:94-98`. If a file is named like a prompt or summary but does not include an extractable `round-N` number, validation exits success at `hooks/loop-read-validator.sh:105-108`. The location test is broad: any path containing `.humanize/rlcr/` passes the first location gate, even if it is an old session directory; that later falls through to round-number or exact-directory validation at `hooks/loop-read-validator.sh:138-167`. If this template is absent, empty, or fails to render non-empty output, `load_and_render_safe` emits the inline fallback from `hooks/loop-read-validator.sh:124-126`, so the algorithm still blocks the read but with less detailed guidance. Missing template variables remain as literal placeholders under the loader’s documented missing-variable behavior at `hooks/lib/template-loader.sh:7-14`.
- validation_or_tests: No dedicated test file for `prompt-template/block/wrong-file-location.md` was found by repository text search. Static validation evidence: the file is directly referenced once by `hooks/loop-read-validator.sh:127`, has 9 lines, and its placeholders match the variables supplied by the caller at `hooks/loop-read-validator.sh:128-130`. The shared template loader validates the template directory shape at `hooks/lib/template-loader.sh:205-222` and provides fallback rendering behavior, but this is runtime resilience rather than a unit test.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1 item evidence sections present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`