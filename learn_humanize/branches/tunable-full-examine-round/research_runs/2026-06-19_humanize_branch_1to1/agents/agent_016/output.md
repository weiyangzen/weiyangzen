# agent_016 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-016 `directory` `tests/robustness`
- cursor: `[_]`
- core_role: `tests/robustness` is a recursive robustness validation suite for the repository’s shell-loop algorithms. It is not production runtime code, but it is a core algorithm guardrail because it exercises state-machine parsing, hook authorization, setup argument gates, Git/PR detection, timeout portability, template rendering, monitor behavior, path validation, and cancel security. The directory has no nested subdirectories, only 17 executable shell scripts, and is included by the broader runner in `tests/run-all-tests.sh:56-72`.
- algorithmic_behavior: Each child script is a black-box or semi-black-box harness. It creates an isolated temp workspace with `setup_test_dir` from `tests/test-helpers.sh:61`, synthesizes state files, Git repositories, hook JSON payloads, mock `gh`/`codex`/timeout commands, templates, or plan files, then invokes production scripts/functions and asserts stable behavior through `pass`, `fail`, and `print_test_summary` from `tests/test-helpers.sh:22-54`. The suite deliberately drives malformed inputs and adversarial cases rather than nominal happy paths only.
- inputs_outputs_state: Inputs are generated files under `$TEST_DIR`, synthetic `.humanize/rlcr` and `.humanize/pr-loop` session directories, mock Git repos, JSON hook events on stdin, plan files, template strings/files, mocked GitHub CLI responses, and command-line flags for setup scripts. Outputs are shell exit codes, stdout/stderr text, JSON hook decisions, parsed pipe-delimited status strings, created/renamed state markers such as `state.md`, `finalize-state.md`, `cancel-state.md`, `approve-state.md`, and generated loop artifacts. Persistent branch state is not intended to be modified; temp directories are cleaned via traps from `tests/test-helpers.sh:61-63`.
- gates_or_invariants: The directory encodes invariants that production logic must preserve: invalid hook JSON must not crash validators; direct or traversed edits to protected loop state must be blocked; missing/incomplete state should degrade safely; active loop stop hooks should block exit only when required fields prove a live loop; cancel moves must require a signal file and reject injection/symlink bypass; plan paths must be relative, non-symlink, non-empty, and shell-safe; setup scripts must reject unsafe args and mutually exclusive active loops; timeout helpers must return 124 for real timeouts and preserve command exit codes; template substitution must not recursively expand injected placeholders; Git state parsers must handle detached, merge, rebase, shallow, non-git, and permission cases.
- dependencies_and_callers: The suite depends on `tests/test-helpers.sh` for counters, summaries, temp dirs, and mock repo helpers. Production dependencies include `hooks/lib/loop-common.sh` for state/session/cancel parsing, `hooks/lib/template-loader.sh` for `render_template` and `load_and_render_safe`, hook validators under `hooks/loop-*-validator.sh`, stop hooks under `hooks/loop-codex-stop-hook.sh` and `hooks/pr-loop-stop-hook.sh`, setup scripts under `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh`, API helpers under `scripts/fetch-pr-comments.sh` and `scripts/poll-pr-reviews.sh`, `scripts/portable-timeout.sh`, and monitor/status functions in `scripts/humanize.sh`. The parent runner lists this directory’s scripts explicitly in `tests/run-all-tests.sh:56-72`, so sibling tests coordinate by sequencing these robustness scripts with other integration/runtime suites.
- edge_cases_or_failure_modes: Child coverage includes corrupted YAML/frontmatter, non-numeric and missing state fields, CRLF and binary-mixed state files, concurrent reads during atomic writes, stale/cancelled/finalized loop dirs, malicious cancel commands with `$(...)`, backticks, `;`, `&&`, `||`, pipes, IFS manipulation, mixed quotes, symlinked `state.md`/`finalize-state.md`, absolute or metacharacter plan paths, symlink chains, empty/comment-only plans, Git repos with no commits, detached HEAD, rebase/merge markers, unreadable `.git`, large JSON payloads, deeply nested JSON, null/binary bytes, very narrow/wide terminals, deleted logs, ANSI/binary log content, large templates and variables, malformed placeholders, permission-denied templates, mocked API failures, slow API calls, and invalid/missing setup flags.
- validation_or_tests: The directory is itself validation evidence. Notable child roles are:
  - `test-state-file-robustness.sh` validates tolerant and strict state parsing, `current_round`, required fields, `full_review_round`, malformed/binary/CRLF input; see `tests/robustness/test-state-file-robustness.sh:3-9` and tests beginning at `:33`.
  - `test-state-transition-robustness.sh` validates RLCR state progressions, finalize/cancel precedence, invalid rounds, newest session selection, and wrong-location state rejection; see `tests/robustness/test-state-transition-robustness.sh:3-9`.
  - `test-concurrent-state-robustness.sh` validates concurrent state reads, active loop discovery, atomic-write visibility, stale loop ignoring, PR loop state lists, and permission behavior; see `tests/robustness/test-concurrent-state-robustness.sh:3-10`.
  - `test-session-robustness.sh` validates `find_active_loop` behavior across many timestamped sessions, finalized/cancelled sessions, symlink/special/deep names, and rapid creation; see `tests/robustness/test-session-robustness.sh:3-9`.
  - `test-hook-input-robustness.sh` validates JSON parsing for read/write/bash validators and monitor behavior under terminal/log anomalies; see `tests/robustness/test-hook-input-robustness.sh:3-9`.
  - `test-hook-system-robustness.sh` validates edit/plan/bash validators, stop-hook state handling, protected state/goal-tracker gates, concurrency, and large/nested JSON; see `tests/robustness/test-hook-system-robustness.sh:3-15`.
  - `test-cancel-security-robustness.sh` validates cancel authorization, signal file checks, command parsing, injection rejection, path normalization, and symlink bypass prevention; see `tests/robustness/test-cancel-security-robustness.sh:3-10`.
  - `test-path-validation-robustness.sh` and `test-plan-file-robustness.sh` validate setup-time plan path/content gates, including unsafe characters, symlinks, missing files, directories, empty/comment-only plans, and disappearing files; see `tests/robustness/test-path-validation-robustness.sh:3-7`.
  - `test-setup-scripts-robustness.sh` validates setup argument parsing, YAML-safe values, Git preconditions, mutual exclusion, timeout behavior, `--full-review-round`, and `--skip-impl`; see `tests/robustness/test-setup-scripts-robustness.sh:3-10`.
  - `test-base-branch-detection.sh` validates base-branch detection priority: user value, origin HEAD, local `main`, local `master`, and failure; see `tests/robustness/test-base-branch-detection.sh:3-11`.
  - `test-git-operations-robustness.sh` validates `humanize_parse_git_status` and `humanize_detect_git_state` across clean/dirty/staged/deleted/untracked/binary/renamed/detached/rebase/merge/shallow/non-git states; see `tests/robustness/test-git-operations-robustness.sh:3-10`.
  - `test-goal-tracker-robustness.sh` validates `humanize_parse_goal_tracker` over list/table ACs, completed/deferred/open issue counts, malformed/truncated/binary files, and goal truncation; see `tests/robustness/test-goal-tracker-robustness.sh:3-10`.
  - `test-template-stress-robustness.sh` validates large variable/template substitution, many variables, regex/sed-sensitive characters, placeholder injection prevention, newlines, boundaries, and repeated placeholders; see `tests/robustness/test-template-stress-robustness.sh:3-9`.
  - `test-template-error-robustness.sh` validates fallback loading, malformed placeholders, invalid variable names, BOM/whitespace templates, permission errors, concurrency, subdirectories, and symlinks; see `tests/robustness/test-template-error-robustness.sh:3-10`.
  - `test-timeout-robustness.sh` validates portable timeout implementation detection, exit-code preservation, signal handling, rapid cycles, invalid commands, empty commands, special args, subshells, and `TIMEOUT_IMPL`; see `tests/robustness/test-timeout-robustness.sh:3-9`.
  - `test-pr-loop-api-robustness.sh` validates mock-GitHub PR comment fetching, bot filtering, malformed/large API responses, PR stop-hook degraded states, `approve-state.md`, `poll-pr-reviews` usage, timeout, and API failure JSON contracts; see invocations at `tests/robustness/test-pr-loop-api-robustness.sh:362`, `:566`, `:622`, and `:654`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-016` appears exactly once as an Item Evidence heading.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`