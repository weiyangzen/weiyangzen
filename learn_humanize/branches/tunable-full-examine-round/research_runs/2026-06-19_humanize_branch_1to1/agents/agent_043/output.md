# agent_043 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-043 `file` `tests/manual-monitor-test.sh`
- cursor: `[_]`
- core_role: Manual executable specification for RLCR monitor shutdown behavior. It is not the production monitor algorithm itself; it creates and removes the `.humanize/rlcr` filesystem state that `humanize monitor rlcr` watches, then instructs an operator to verify graceful exit, terminal restoration, and absence of shell glob errors. The stated intent is explicit in [tests/manual-monitor-test.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/manual-monitor-test.sh:5): deletion of `.humanize/` should produce a clean user-facing stop and restore terminal state.
- algorithmic_behavior: The script is a three-command state harness selected by `case "${1:-}"` in [tests/manual-monitor-test.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/manual-monitor-test.sh:23). `setup` creates `.humanize/rlcr/2026-01-16_99-99-99`, writes a minimal `state.md` with monitor-parsed keys, writes a minimal `goal-tracker.md`, and prints the required second-terminal command `source scripts/humanize.sh && humanize monitor rlcr` in [tests/manual-monitor-test.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/manual-monitor-test.sh:24). `delete` removes `.humanize` and prints the expected observation contract, including the message `Monitoring stopped: .humanize/rlcr directory no longer exists`, terminal scroll-region reset, and no `no matches found` errors in [tests/manual-monitor-test.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/manual-monitor-test.sh:47). `cleanup` removes leftover `.humanize` state in [tests/manual-monitor-test.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/manual-monitor-test.sh:58); unknown commands print usage and exit `1`.
- inputs_outputs_state: Inputs are the positional command `setup|delete|cleanup`, the current repository checkout, and an operator-run monitor process in a second terminal. The script resolves `PROJECT_ROOT` from its own location and always `cd`s there before acting, shown in [tests/manual-monitor-test.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/manual-monitor-test.sh:19). Outputs are filesystem mutations plus stdout instructions: `setup` materializes `.humanize/rlcr/<session>/state.md` and `goal-tracker.md`; `delete` transitions the monitor-visible state from “RLCR loop dir exists” to “missing”; `cleanup` returns the checkout to “no `.humanize` fixture”. The expected monitored state transition is implemented by `_humanize_monitor_codex`, whose main loop checks `[[ ! -d "$loop_dir" ]]`, calls `_graceful_stop`, and returns `0` in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:620); the same deletion gate also exists while waiting for logs and while tailing logs in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:662) and [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:755).
- gates_or_invariants: The script runs under `set -euo pipefail` in [tests/manual-monitor-test.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/manual-monitor-test.sh:17), uses `${1:-}` to avoid nounset failure for missing arguments, and only accepts the three command states. The fixture session name is regex-compatible with the monitor’s timestamp-pattern discovery even though the hour/minute/second values are semantically artificial; `monitor_find_latest_session` only validates the `YYYY-MM-DD_HH-MM-SS` shape and picks the lexicographically latest session in [scripts/lib/monitor-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/lib/monitor-common.sh:40). The minimal `state.md` provides exactly the fields `_parse_state_md` greps for: `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `started_at`, and `plan_file` in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:349). The shutdown invariant being tested is `_graceful_stop -> _cleanup -> _restore_terminal`: `_cleanup` is idempotent via `cleanup_done`, sets `monitor_running=false`, resets traps, kills a tail process if present, restores the terminal, and prints `Stopped monitoring.` in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:535); `_graceful_stop` then prints the deletion reason in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:563).
- dependencies_and_callers: The manual flow depends on the public shell dispatcher `humanize monitor rlcr`, which routes to `_humanize_monitor_codex` in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:884). `_humanize_monitor_codex` depends on `scripts/lib/monitor-common.sh`, sourced from [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:6), for safe latest-session discovery, file-size probing, state-file discovery, and terminal helper patterns. The relevant shared utilities are `monitor_get_file_size` in [scripts/lib/monitor-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/lib/monitor-common.sh:32), `monitor_find_latest_session` in [scripts/lib/monitor-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/lib/monitor-common.sh:40), and `monitor_find_state_file` in [scripts/lib/monitor-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/lib/monitor-common.sh:154). The manual script itself is not wired into `tests/run-all-tests.sh`; the automated monitor coverage is listed separately there at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/run-all-tests.sh:49).
- edge_cases_or_failure_modes: `delete` and `cleanup` both run `rm -rf .humanize`, so this is intentionally destructive to local Humanize loop state and should not be run in a checkout with valuable active `.humanize` data. If `setup` is run where real sessions already exist, the artificial `99-99-99` session may be selected as latest because latest-session ordering is lexicographic. If the operator starts `humanize monitor rlcr` before `setup`, `_humanize_monitor_codex` fails early because `.humanize/rlcr` is missing in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:230). The manual fixture creates no cached codex log, so it exercises the monitor’s “no log yet, keep polling” branch; deletion is still detected inside that branch. The shell-glob failure mode called out by the script is mitigated in production by `find`-based iteration rather than raw `*` globbing, for example in [scripts/lib/monitor-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/lib/monitor-common.sh:49) and [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:280).
- validation_or_tests: The manual validation procedure is embedded in the script comments and output: setup in one terminal, start `humanize monitor rlcr` in another, delete `.humanize`, observe clean message and restored terminal, then cleanup in [tests/manual-monitor-test.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/manual-monitor-test.sh:9). Automated coverage mirrors this specification: `tests/test-monitor-runtime.sh` checks graceful-stop message content, cleanup idempotence, deletion detection, scroll reset, `_graceful_stop` calling `_cleanup`, and signal traps in [tests/test-monitor-runtime.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-monitor-runtime.sh:56). `tests/test-monitor-e2e-real.sh` runs the real `_humanize_monitor_codex` under bash, deletes `.humanize/rlcr`, asserts exit, graceful message, no glob errors, cleanup message, scroll reset source, and exit code `0` in [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-monitor-e2e-real.sh:59); it repeats real deletion behavior under zsh in [tests/test-monitor-e2e-real.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-monitor-e2e-real.sh:234). `tests/test-zsh-monitor-safety.sh` specifically validates empty directories, dotfile-only directories, no `*-state.md`, empty cache, latest-session ordering, and old-glob versus new-`find` safety in [tests/test-zsh-monitor-safety.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-zsh-monitor-safety.sh:80). I inspected these validation surfaces but did not execute them in this read-only research pass.
- skip_candidate: `no`; it is a manual/destructive test harness rather than production logic, but it is correctly included as an executable specification for core monitor lifecycle behavior.

## Worker Self-Test
- assigned_items_seen: 1/1 evidence sections; exact item-id occurrence count in this output: 1
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`