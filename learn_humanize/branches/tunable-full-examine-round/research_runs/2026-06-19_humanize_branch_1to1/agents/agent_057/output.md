# agent_057 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-057 `file` `tests/test-monitor-e2e-real.sh`
- cursor: `[_]`
- core_role: This is an executable end-to-end specification for monitor shutdown and terminal cleanup behavior. It validates the real monitor entry points from `scripts/humanize.sh`, not mocks: `_humanize_monitor_codex` is sourced and called directly in generated runner scripts at `tests/test-monitor-e2e-real.sh:148-153`, `314-321`, `464-471`, and `630-635`; the PR monitor is exercised through `humanize monitor pr` at `793-795` and `920-925`. Its core algorithmic role is to prove that active RLCR and PR monitoring loops safely transition from “watching session/log state” to “stopped” when the underlying `.humanize` runtime state disappears or when interrupt signals arrive.

- algorithmic_behavior: The test builds isolated temporary projects under `/tmp/test-monitor-e2e-real-$$` (`48-56`), creates synthetic `.humanize/rlcr/<timestamp>` or `.humanize/pr-loop/<timestamp>` session directories, populates state and goal tracker files, then runs the real monitor functions in background or foreground. For RLCR, it prepares cache logs under a sanitized project path matching production lookup rules (`103-107`, `282-286`, `421-425`, `600-604`) and verifies that deleting `.humanize/rlcr` causes the monitor loop to stop gracefully (`168-185`, `334-350`). For SIGINT scenarios, it starts the real monitor, sends `INT` to the process or process group, waits in bounded polling loops, and applies fallback termination only after timeout (`492-519`, `647-665`, `938-965`). For PR monitoring, it validates both one-shot status mode with `--once` plus session deletion (`784-805`) and continuous poll-loop mode without `--once` plus SIGINT (`920-965`). The production behavior being specified lives mainly in `scripts/humanize.sh`: RLCR setup and session/log discovery at `219-342`, status parsing/rendering at `344-510`, terminal restore and cleanup at `512-587`, and loop state transitions at `620-881`; PR monitor setup, file discovery, state parsing, one-shot mode, and continuous loop are at `931-1364`.

- inputs_outputs_state: Inputs are synthetic filesystem state, shell availability, process signals, terminal command shims, and captured stdout/stderr. RLCR test inputs include `state.md` frontmatter with `current_round`, `max_iterations`, model/effort, start time, plan file, branches, and review status (`69-82`, `248-261`, `387-400`, `566-579`), plus `goal-tracker.md` acceptance/verification sections (`84-97`, `263-276`, `402-415`, `581-594`) and cache log files named `round-1-codex-run.log` (`107`, `286`, `425`, `604`). PR inputs include `.humanize/pr-loop/<timestamp>/state.md` with `pr_number`, bots, model/effort, polling settings, and `goal-tracker.md` issue stats (`697-713`, `716-732`, `838-854`, `856-873`). Outputs are captured monitor text files or command substitution results, pass/fail counter increments, and final process exit status. State transitions under test are: monitor running -> directory deleted -> `_graceful_stop` -> cleanup -> return 0 for RLCR; monitor running -> SIGINT/TERM -> cleanup trap -> clean or tolerated terminated exit; PR `--once` -> status print -> clean exit; PR continuous loop -> poll latest session/file -> SIGINT -> cleanup. Production RLCR state variables include `current_session_dir`, `current_file`, `last_size`, `last_no_log_status`, `monitor_running`, and `cleanup_done` (`224-228`, `530-533`, `615-619`); PR uses `current_session_dir`, `current_file`, `TAIL_PID`, `monitor_running`, `cleanup_done`, and `once_mode` (`935-940`, `1169-1172`).

- gates_or_invariants: The script’s hard gate is `TESTS_FAILED == 0`; otherwise it exits 1 (`1002-1017`). Each scenario uses bounded wait loops capped at 20 half-second sleeps after deletion or SIGINT (`171-176`, `338-342`, `499-503`, `652-656`, `945-949`). Required success signals include a graceful stop phrase (`191-202`, `355-360`), deletion reason text (`198-202`), exit code `0` for directory deletion (`226-231`, `368-372`), cleanup/stop indication for SIGINT (`532-541`, `672-676`, `973-982`), and absence of zsh/bash glob failures matching `no matches found|bad pattern` (`204-209`, `362-366`, `544-549`, `678-682`, `819-824`, `985-990`). A source invariant checks that RLCR terminal cleanup includes scroll-region reset via `printf "\033[r"` in `scripts/humanize.sh` (`219-224`), implemented at `scripts/humanize.sh:523-525`. Production monitor invariants include using `find` rather than shell globs to avoid zsh no-match errors (`scripts/humanize.sh:280-339`, `scripts/lib/monitor-common.sh:49-60`), preventing duplicate cleanup with `cleanup_done` (`scripts/humanize.sh:537-541`, `1176-1180`), and resetting traps after cleanup (`545`, `1183`, `1357-1363`).

- dependencies_and_callers: The test depends on Bash, optional zsh, `sed`, `grep`, `find`, `tail`, `stat`, `pkill`, `kill`, `sleep`, `head`, `chmod`, `mkdir`, `rm`, and terminal utilities shimmed through shell functions. It sources `scripts/humanize.sh`, which in turn sources `scripts/lib/monitor-common.sh` when present (`scripts/humanize.sh:6-10`). RLCR monitor depends on `monitor_find_latest_session`, `monitor_find_state_file`, `monitor_get_file_size`, `humanize_parse_goal_tracker`, `humanize_parse_git_status`, and `humanize_split_to_array` (`scripts/humanize.sh:237-371`, `610-613`). Shared helpers define portable file sizing (`scripts/lib/monitor-common.sh:29-35`), timestamp-session ordering (`37-63`), terminal reset helpers (`84-92`), state-file detection with `state.md` priority over `*-state.md` (`147-188`), PR phase detection (`258-342`), and PR goal tracker issue parsing (`465-498`). PR monitor depends additionally on `get_pr_loop_phase`, `get_pr_loop_phase_display`, and `humanize_parse_pr_goal_tracker` for status display (`scripts/humanize.sh:1097-1147`, `1235-1241`). The external caller surface under test is `humanize monitor <rlcr|pr>` dispatch at `scripts/humanize.sh:883-924`.

- edge_cases_or_failure_modes: The test targets deletion of the entire `.humanize/rlcr` tree during active RLCR monitoring (`168-185`, `334-350`) and deletion of a PR session directory during PR one-shot status generation (`786-793`). It covers bash and zsh differences, especially zsh glob expansion errors and trap behavior (`240-373`, `558-683`). It treats SIGINT delivery as inherently flaky in subprocess/process-group contexts: bash RLCR and PR continuous tests can still pass if the monitor was running but required SIGTERM/SIGKILL fallback after SIGINT did not exit promptly (`505-519`, `951-965`). That makes SIGINT validation more permissive than deletion validation. Other production edge cases surfaced by the referenced code include no initial loop directory returning an error (`scripts/humanize.sh:230-235`, `955-960`), no session directories returning an RLCR error (`595-599`) or PR wait/one-shot error (`1217-1229`, `1305-1311`), missing log files causing a wait display (`641-740`, `1348-1352`), log truncation/rotation resetting search state (`777-787`), deleted current session/log switching to newer available sessions (`693-721`, `797-831`), and PR phase detection writing a transient size cache in `/tmp` (`scripts/lib/monitor-common.sh:297-307`).

- validation_or_tests: This file is itself the validation harness. It defines six scenarios: RLCR deletion in bash (`59-231`), RLCR deletion in zsh when available (`233-373`), RLCR SIGINT in bash (`375-550`), RLCR SIGINT in zsh when available (`551-683`), PR monitor `--once` with deletion (`685-824`), and PR monitor continuous mode with SIGINT (`826-990`). It aggregates pass/fail counts through `pass()` and `fail()` (`28-37`) and prints a final verification summary on success (`992-1017`). I performed a read-only syntax validation with `bash -n tests/test-monitor-e2e-real.sh scripts/humanize.sh scripts/lib/monitor-common.sh`, which completed with exit code 0. I did not run the full e2e script because it creates and removes temporary projects, spawns monitor processes, and sends process signals; the requested output is research notes only and the branch export is read-only.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`