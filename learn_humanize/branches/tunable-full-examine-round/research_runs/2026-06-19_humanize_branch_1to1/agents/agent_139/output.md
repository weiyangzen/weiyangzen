# agent_139 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-139 `file` `tests/robustness/test-setup-scripts-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the setup algorithms behind `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh`. It is not the setup implementation itself, but it defines the expected safety gates, input validation, loop mutual exclusion, timeout behavior, and skip-implementation state initialization for those core setup entrypoints. The script is included by the aggregate runner at `tests/run-all-tests.sh:67`.

- algorithmic_behavior: The test script runs as a strict Bash harness with `set -euo pipefail` at `tests/robustness/test-setup-scripts-robustness.sh:13`, resolves `SCRIPT_DIR` and `PROJECT_ROOT`, then sources `tests/test-helpers.sh` and `scripts/portable-timeout.sh` at lines 15-18. It creates isolated temporary repositories via `setup_test_dir` at line 20 and helper functions at lines 31-87. `create_minimal_plan` writes a valid markdown plan shape with Goal, Acceptance Criteria, and Steps at lines 31-49. `init_basic_git_repo` initializes a committed `main` branch at lines 51-63. `run_rlcr_setup` and `run_pr_setup` execute the real setup scripts from inside each temp repo while setting `CLAUDE_PROJECT_DIR` to isolate loop discovery from the user’s real checkout at lines 67-85.
  
  The suite then runs 45 ordered scenarios. Tests 1-8 validate RLCR argument parsing: help output, missing plan, bad `--max`, empty/negative values, bad `--codex-timeout`, missing `--codex-model`, unknown option, and duplicate positional plus `--plan-file` at lines 96-214. Tests 9-13 validate plan file content and path constraints: comment-only plans, under-5-line plans, spaces, shell metacharacters, and absolute paths at lines 224-341. Tests 14-15 check YAML-safe branch/model values at lines 351-402. Tests 16-18 cover git prerequisites and tracked-plan policy at lines 412-463. Tests 19-23 cover PR-loop argument and git gates at lines 473-524. Tests 24-25 synthesize active `.humanize/rlcr/.../state.md` and `.humanize/pr-loop/.../state.md` directories to assert mutual exclusion at lines 535-595. Tests 26-27 assert direct and parent-directory symlink rejection at lines 605-658. Tests 28-30 confirm valid inputs progress past argument parsing into dependency checks at lines 668-739. Tests 31-35 cover timeout validation and timeout-command simulation at lines 749-866. Tests 36-40 cover `--full-review-round` minimum and type gates at lines 876-965. Tests 41-45 cover `--skip-impl` plan optionality and generated review-mode state artifacts at lines 975-1067. The script exits through `print_test_summary` at lines 1073-1074.

- inputs_outputs_state: Inputs are command-line invocations to `scripts/setup-rlcr-loop.sh` and `scripts/setup-pr-loop.sh`, generated temp git repositories, generated `plan.md` files, synthetic `.gitignore`/tracked-plan states, mocked `codex`, `gh`, `timeout`, and `gtimeout` executables on `PATH`, and synthetic active-loop state files under `.humanize`. Outputs are pass/fail counter updates through `pass`/`fail` from `tests/test-helpers.sh:30-44`, terminal diagnostics, and process exit status from `print_test_summary` in `tests/test-helpers.sh:58-77`. Temporary filesystem state is created under `TEST_DIR`, which is allocated and cleaned by `setup_test_dir` in `tests/test-helpers.sh:86-89`. For successful `--skip-impl` setup, the asserted durable outputs are `.humanize/rlcr/<timestamp>/.review-phase-started`, `state.md` containing `review_started: true`, and `goal-tracker.md` without normal placeholder text at `tests/robustness/test-setup-scripts-robustness.sh:1006-1039`.

- gates_or_invariants: The suite asserts that setup is fail-closed on invalid CLI shape, invalid path shape, weak plan content, unsafe YAML serialization values, missing git repository, missing commits, active competing loops, tracked plan files without `--track-plan-file`, and symlink traversal. It pins RLCR setup behavior implemented in `scripts/setup-rlcr-loop.sh`: numeric parsing for `--max`, `--codex-timeout`, and `--full-review-round` at lines 125-201; unknown option rejection at lines 208-211; mutual exclusion at lines 240-261; duplicate plan-source rejection at lines 263-267; plan optionality only under `--skip-impl` at lines 272-290; git checks through `run_with_timeout` at lines 297-306; path validation for absolute paths, spaces, metacharacters, direct symlinks, parent symlinks, missing/unreadable files, and project containment at lines 320-406; plan tracking policy at lines 427-474; YAML-safe branch/model/effort values at lines 555-587; and `--skip-impl` state creation at lines 675-730 and output banner at lines 980-1004. It also pins PR setup behavior in `scripts/setup-pr-loop.sh`: bot flag requirement at lines 175-180, CLI parsing at lines 110-173, mutual exclusion and git checks at lines 220-253, `gh auth status` failure handling at lines 263-269, and codex availability at lines 271-277. Timeout execution is coupled to `scripts/portable-timeout.sh`, where `run_with_timeout` selects `gtimeout`, GNU `timeout`, Python, or no-timeout fallback at lines 10-27 and executes commands with GNU-compatible timeout exit `124` at lines 33-71.

- dependencies_and_callers: Direct dependencies are `tests/test-helpers.sh` for result accounting and temp-dir cleanup, `scripts/portable-timeout.sh` for timeout abstraction, `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, Git, shell builtins/coreutils, and optionally symlink support. The real setup scripts further source `hooks/lib/loop-common.sh` for active-loop detection, and PR setup sources `hooks/lib/template-loader.sh` before resolving PR context. The test uses mock binaries in per-repo `bin/` directories to isolate dependency branches, especially fake `codex`, fake `gh`, and fake `timeout`/`gtimeout`. Its caller is the full test runner list in `tests/run-all-tests.sh:67`; it can also be executed directly as a standalone robustness script.

- edge_cases_or_failure_modes: The suite intentionally covers parser ambiguities such as `--max=-5` versus `--max -5` at lines 138-152, empty option values at lines 155-164, plan paths that cannot or should not be created such as `plan;.md` at lines 303-321, branch-name behavior that may vary by Git version when creating `test:branch` at lines 364-379, symlink creation support variability at lines 620-629 and 648-657, installed-system `codex` availability changing the expected valid-argument stopping point at lines 682-690, non-negative timeout behavior where `0` is accepted despite the “positive integer” message text at lines 749-765, and timeout-command simulation returning `124` for `git rev-parse` at lines 817-856. One notable harness risk: many tests use `OUTPUT=... || EXIT_CODE=$?` followed by `EXIT_CODE=${EXIT_CODE:-0}` without clearing `EXIT_CODE` between tests, so a previously nonzero value can persist if a later command succeeds. The tests generally inspect output text as the stronger signal, but this shared shell variable is a possible false-positive/false-negative source in success-path cases.

- validation_or_tests: This file is itself the validation asset. It validates by executing the production setup scripts in disposable repositories and checking exit codes plus diagnostic substrings. It covers negative validation, positive “progresses past parser” validation, and material state validation for `--skip-impl`. It also verifies portable timeout assumptions by checking the mock timeout script does not hardcode `/usr/bin/git` at lines 858-866. The final validation gate is `print_test_summary "Setup Scripts Robustness Test Summary"` and `exit $?` at lines 1073-1074, which returns nonzero when any `fail` call occurred.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 evidence section, matching the sole assigned item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`