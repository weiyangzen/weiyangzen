# agent_026 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-026 `file` `hooks/loop-codex-stop-hook.sh`
- cursor: `[_]`
- core_role: This is the RLCR loop Stop hook wired into Claude Code Stop events by `hooks/hooks.json` with a 7200s hook timeout at lines 52-59. Its role is to intercept exit attempts during an active `.humanize/rlcr/<timestamp>/` loop, run layered local validation, invoke Codex as an implementation reviewer or diff reviewer, and either allow exit, terminate the loop with a reason state file, or return Claude-hook JSON `{ "decision": "block", ... }` containing the next instructions. The file describes this contract directly at [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-codex-stop-hook.sh:3), and intentionally does not short-circuit on `stop_hook_active` so each blocked retry is reviewed again at lines 31-37.

- algorithmic_behavior: The algorithm starts by reading the full hook JSON/stdin into `HOOK_INPUT`, resolving `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd`, sourcing shared RLCR helpers, and selecting the newest active loop directory under `.humanize/rlcr` via `find_active_loop` at lines 29-60. No active loop, no `state.md`, or no `finalize-state.md` causes pass-through `exit 0` at lines 61-81. It then chooses normal state or Finalize Phase state at lines 72-80, parses YAML frontmatter with `parse_state_file`, maps state fields into local variables, and validates model/effort syntax plus required schema fields at lines 88-156. The main gate sequence is: schema validation at lines 163-220, branch consistency at lines 227-253, plan integrity unless already in review phase at lines 263-337, incomplete TodoWrite detection from the transcript at lines 345-394, cached git status with fail-closed behavior at lines 403-425, large changed-file detection over code/docs extensions at lines 433-507, clean working tree and optional push-every-round checks at lines 517-603, required summary-file detection at lines 609-641, Round 0 goal tracker initialization at lines 647-719, max iteration enforcement at lines 725-733, Finalize Phase completion at lines 742-747, prompt construction at lines 761-847, Codex availability/cache/argument setup at lines 855-905, then either implementation-phase `codex exec` review at lines 1243-1438 or review-phase `codex review` at lines 1446-1468. If Codex implementation review does not end with a strict final-line marker, the hook advances `current_round`, writes the next prompt, and blocks exit at lines 1505-1571.

- inputs_outputs_state: Inputs include Stop hook stdin JSON, especially `transcript_path` for todo checking; environment variables `CLAUDE_PROJECT_DIR`, `XDG_CACHE_HOME`, and possibly `CODEX_TIMEOUT`; active loop files under `.humanize/rlcr/<timestamp>/`; current git branch/status; the `codex` CLI; and templates from `prompt-template`. The state file fields consumed are `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `full_review_round`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, `base_commit`, and `review_started`, initialized by `scripts/setup-rlcr-loop.sh` at lines 709-725. Outputs are hook JSON blocks on stdout for recoverable gates, stderr audit/debug messages, prompt/result files such as `round-N-review-prompt.md`, `round-N-review-result.md`, `round-N-prompt.md`, `finalize-summary.md`, and cache logs under `$XDG_CACHE_HOME/humanize/<sanitized-project>/<loop-timestamp>/` at lines 877-887 and 1250-1277. State transitions are file-renames: corrupted/terminal loops use shared `end_loop` to rename to `<reason>-state.md` at `hooks/lib/loop-common.sh` lines 1119-1145; implementation `COMPLETE` changes `review_started: true`, creates `.review-phase-started`, and runs `codex review` at lines 1421-1433; passed code review calls `enter_finalize_phase`, moving `state.md` to `finalize-state.md` at lines 1032-1037; the next clean Finalize Phase exit moves `finalize-state.md` to `complete-state.md` at lines 742-747; `STOP` moves to `stop-state.md` at lines 1471-1498; max iterations move to `maxiter-state.md` at lines 730-733 or 1403-1406.

- gates_or_invariants: The hook is fail-closed for malformed loop state, git uncertainty, dirty working trees, missing summaries, missing Codex output, and review failures. It requires `current_round` and `max_iterations` to be physically present in raw frontmatter, not merely defaulted by the tolerant parser, at lines 131-144. It rejects invalid `codex_model` and `codex_effort` characters at lines 118-129, older schema missing `plan_tracked`, `start_branch`, `review_started`, or `base_branch` at lines 163-208, and branch drift from `start_branch` at lines 227-253. It forbids plan drift by checking the backup plan, project plan existence, uncommitted tracked-plan changes, and content diff against backup at lines 267-335. It blocks incomplete todos before invoking Codex at lines 347-393. It blocks changed files over 2000 lines for selected code/docs extensions at lines 433-507. It requires clean git status and, when configured, no unpushed commits at lines 517-603. Completion markers are intentionally strict: only the last non-empty review-result line exactly equal to `COMPLETE` or `STOP` is accepted, avoiding false positives such as “CANNOT COMPLETE” at lines 1388-1392. Full Alignment Check cadence is derived from `current_round % full_review_round == full_review_round - 1`, with invalid values reset to 5 at lines 773-783. Review phase has an anti-tamper invariant: `review_started=true` is invalid unless `.review-phase-started` exists at lines 1446-1462.

- dependencies_and_callers: Direct caller configuration is `hooks/hooks.json`, where this script is one of the Stop hooks. It depends on `hooks/lib/loop-common.sh` for constants (`COMPLETE`, `STOP`, exit reason names), active-loop discovery, state parsing, review issue extraction, lowercase conversion, and `end_loop`; relevant helper definitions are at `hooks/lib/loop-common.sh` lines 15-44, 151-174, 209-244, 349-392, and 1119-1145. It depends on `hooks/lib/template-loader.sh` for safe template resolution/rendering and fallback behavior, especially `load_template`, `render_template`, and `load_and_render_safe` at lines 36-48, 58-132, and 170-203. It depends on `scripts/portable-timeout.sh` for cross-platform `run_with_timeout`, selecting `gtimeout`, GNU `timeout`, Python, or no timeout at lines 10-71. It delegates todo parsing to `hooks/check-todos-from-transcript.py`, which reads `transcript_path`, finds the latest TodoWrite payload, and returns 0/1/2 for complete/incomplete/parse-error at lines 90-134. It is coordinated with setup in `scripts/setup-rlcr-loop.sh`, which captures `base_commit`, writes the state schema, and creates `.review-phase-started` for `--skip-impl` at lines 653-661 and 706-731. It also coordinates with sibling validators (`loop-read-validator.sh`, `loop-write-validator.sh`, `loop-edit-validator.sh`, `loop-bash-validator.sh`, `loop-plan-file-validator.sh`) that guard the same loop state and plan/finalize files before tool use.

- edge_cases_or_failure_modes: With no active loop, the hook allows exit silently; this is explicitly tested in `tests/robustness/test-hook-system-robustness.sh` lines 461-475. A corrupted or incomplete state exits 0 after either producing a block or renaming to `unexpected-state.md`, tested at lines 493-557 of the same file. Git failures/timeouts while resolving branch or status block instead of allowing completion at lines 227-241 and 403-424. Missing `codex` blocks with a Codex-not-found reason at lines 855-874. Nonzero `codex exec`, missing review result, failed stdout copy, or empty result all block through `codex_failure_exit` at lines 1283-1383. `codex review` failures and empty review logs are hard review-phase blocks via `block_review_failure` at lines 1163-1235; tests assert these do not create `finalize-state.md` and keep `review_started: true` in `tests/test-finalize-phase.sh` lines 563-696. `detect_review_issues` only treats `[P0-9]` appearing within the first 10 characters of a log line as a blocking issue and returns 2 for missing/empty logs, which is covered by `tests/test-codex-review-merge.sh` lines 1-16 and 57-169. `STOP` is honored even outside full alignment rounds, but logs it as unusual and still terminates the loop at lines 1471-1498. One dead/legacy-looking path exists: after schema validation, `BASE_BRANCH` should never be empty, so the implementation-complete “skip review because no base_branch” branch at lines 1413-1418 appears practically unreachable.

- validation_or_tests: Direct and related tests are extensive. `tests/test-finalize-phase.sh` covers Finalize Phase missing summary, dirty git blocking, successful `finalize-state.md` to `complete-state.md`, `COMPLETE` transition behavior, max-iteration handling, review failure blocking, empty review output blocking, incomplete todo blocking before Codex, and normal non-COMPLETE round feedback at lines 418-790. `tests/test-plan-file-hooks.sh` covers stop-hook plan-file modification, deletion, missing backup, tracked-plan race detection, old schema JSON blocking, committed plan drift, and section-specific goal-tracker placeholder detection at lines 480-830. `tests/robustness/test-hook-system-robustness.sh` covers no-state pass-through, corrupted/incomplete state behavior, and active-loop blocking at lines 454-613. `tests/test-codex-review-merge.sh` covers the `[P?]` review-log scanner behavior, including full-file scanning and missing/empty hard errors at lines 1-220. `tests/test-template-references.sh` includes this hook in template-reference validation at lines 56-64, reducing missing-template risk for block prompts. I did not run these tests because this task is research-only in a read-only branch export; I inspected their assertions directly.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-026`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`