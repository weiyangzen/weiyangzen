# agent_076 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-076 `file` `prompt-template/block/codex-review-failed.md`
- cursor: `[_]`
- core_role: This file is a block-message template for the RLCR/Codex stop-hook review gate. It is rendered when the Codex review phase cannot produce usable review output, and its operational role is to prevent the loop from finalizing until review can be retried. The template headline and lead text establish the failure state at `prompt-template/block/codex-review-failed.md:1-3`.
- algorithmic_behavior: The template does not execute logic directly; it supplies the structured `reason` payload for a blocking JSON decision emitted by `hooks/loop-codex-stop-hook.sh`. The review runner builds command/debug artifacts, runs `codex review --base`, captures combined stdout/stderr into a log, and records the exit code at `hooks/loop-codex-stop-hook.sh:930-983`. `run_and_handle_code_review` treats a nonzero Codex review command as a hard failure and calls `block_review_failure` at `hooks/loop-codex-stop-hook.sh:1003-1009`; it also treats missing/empty detected review output as a hard error at `hooks/loop-codex-stop-hook.sh:1011-1020`. `block_review_failure` renders this assigned template through `load_and_render_safe` and returns `{"decision":"block"}` with a retry-required system message at `hooks/loop-codex-stop-hook.sh:1163-1235`.
- inputs_outputs_state: Inputs are template variables: `{{CODEX_EXIT_CODE}}` at `prompt-template/block/codex-review-failed.md:5`, `{{REVIEW_RESULT_FILE}}` at `prompt-template/block/codex-review-failed.md:6`, debug-file paths `{{CODEX_CMD_FILE}}`, `{{CODEX_STDOUT_FILE}}`, and `{{CODEX_STDERR_FILE}}` at `prompt-template/block/codex-review-failed.md:8-11`, plus `{{STDERR_CONTENT}}` at `prompt-template/block/codex-review-failed.md:13-16`. The hook currently passes `EXIT_CODE`, `STDERR_CONTENT`, `REVIEW_RESULT_FILE`, `CODEX_CMD_FILE`, and `CODEX_LOG_FILE` to this template at `hooks/loop-codex-stop-hook.sh:1217-1225`; that means `CODEX_EXIT_CODE`, `CODEX_STDOUT_FILE`, and `CODEX_STDERR_FILE` are not populated by the observed caller and would remain literal placeholders under the template loader’s missing-variable rule. Output is Markdown text embedded as the JSON `reason` field at `hooks/loop-codex-stop-hook.sh:1227-1234`. The intended state transition is: review attempt fails or yields no valid output -> stop-hook emits block decision -> loop remains in review/retry state rather than entering finalize/complete. Tests assert no `finalize-state.md` or `complete-state.md` is created after review failure at `tests/test-finalize-phase.sh:588-599`.
- gates_or_invariants: The key invariant is that review failure is blocking and cannot be skipped. This is documented directly in the hook comments at `hooks/loop-codex-stop-hook.sh:1163-1165` and enforced by the `decision: block` output at `hooks/loop-codex-stop-hook.sh:1227-1234`. The upstream gate distinguishes successful clean review, review issues, nonzero command failure, and missing output: only the clean path proceeds to finalize at `hooks/loop-codex-stop-hook.sh:1023-1027`; issues continue the review loop at `hooks/loop-codex-stop-hook.sh:1020-1022`; hard failures call this block template path at `hooks/loop-codex-stop-hook.sh:1007-1009` and `hooks/loop-codex-stop-hook.sh:1017-1020`. Template rendering is single-pass, so inserted stderr/debug values are not recursively expanded, as described and implemented in `hooks/lib/template-loader.sh:50-58` and `hooks/lib/template-loader.sh:71-129`.
- dependencies_and_callers: Direct caller is `block_review_failure` in `hooks/loop-codex-stop-hook.sh:1217-1225`. The template depends on the shared template loader, especially `load_and_render_safe`, which loads the real template when present and falls back only if missing or empty at `hooks/lib/template-loader.sh:167-203`. Template discovery comes from `TEMPLATE_DIR` via `hooks/lib/loop-common.sh` references found by repository search, and template existence is guarded by `tests/test-template-references.sh`, which scans hook references to `load_template`, `load_and_render`, and `load_and_render_safe` and fails missing templates at `tests/test-template-references.sh:83-110`. The broader review flow is described in `prompt-template/codex/code-review-phase.md` and in the hook’s audit prompt generation at `hooks/loop-codex-stop-hook.sh:935-954`, but `codex-review-failed.md` is only used for the failure/block branch.
- edge_cases_or_failure_modes: A concrete mismatch exists between this template’s placeholders and the caller’s variable names. The template expects `CODEX_EXIT_CODE`, `CODEX_STDOUT_FILE`, and `CODEX_STDERR_FILE`, but the caller supplies `EXIT_CODE` and `CODEX_LOG_FILE`; because missing variables remain unchanged under `hooks/lib/template-loader.sh:12-13` and `hooks/lib/template-loader.sh:116-122`, rendered failure messages may expose unresolved placeholders instead of the actual exit code/log path. Another edge case is that `REVIEW_RESULT_FILE` is always shown as “not created” at `prompt-template/block/codex-review-failed.md:6`, which matches the hard-failure scenario but may be less precise if a partial result file exists. Stderr display is bounded to the last 50 lines by both the template section at `prompt-template/block/codex-review-failed.md:13` and the hook’s `tail -50` read at `hooks/loop-codex-stop-hook.sh:1173-1177`, limiting log noise but hiding earlier root-cause lines. If the template is missing or empty, `load_and_render_safe` uses the hook’s longer fallback message at `hooks/loop-codex-stop-hook.sh:1179-1214`, so the block still operates.
- validation_or_tests: Relevant tests cover both the template system and the review-failure gate. `tests/test-finalize-phase.sh` verifies a nonzero Codex review exit blocks instead of finalizing at `tests/test-finalize-phase.sh:566-599`, checks the block message indicates review failure/retry at `tests/test-finalize-phase.sh:601-607`, and verifies state preservation with `review_started: true` at `tests/test-finalize-phase.sh:609-619`. It also tests empty review output as a block condition at `tests/test-finalize-phase.sh:621-657`. Template structure/load validation walks all Markdown templates and fails empty or unloadable templates at `tests/test-templates-comprehensive.sh:90-107`; placeholder syntax checks begin at `tests/test-templates-comprehensive.sh:118-130`. Safe-loader behavior is validated for missing templates and real templates at `tests/test-templates-comprehensive.sh:446-463`. I did not run the tests in this read-only research pass; I inspected the file, caller, loader, and tests directly.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1; the single Item Evidence section above corresponds to the sole assigned item`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`