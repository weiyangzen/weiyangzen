# agent_032 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-032 `file` `scripts/cancel-pr-loop.sh`
- cursor: `[_]`
- core_role: `scripts/cancel-pr-loop.sh` is the user-facing cancellation state-transition script for an active PR loop. It is intentionally scoped to `.humanize/pr-loop/` and does not affect RLCR loop state. Its role is not to poll GitHub or resolve PR feedback; it converts the latest active PR loop from an active state into a cancelled/preserved state by creating a cancellation sentinel and renaming the active state file.

- algorithmic_behavior: The script uses strict Bash mode (`set -euo pipefail`) at `scripts/cancel-pr-loop.sh:18`. It parses only `--force`, `-h`, and `--help` at `scripts/cancel-pr-loop.sh:24-66`; `--force` is accepted but has no behavioral effect beyond setting `FORCE=true`. It chooses the project root from `CLAUDE_PROJECT_DIR` or the current working directory at `scripts/cancel-pr-loop.sh:72`, sets the loop base to `.humanize/pr-loop` at `scripts/cancel-pr-loop.sh:73`, then selects the lexicographically newest child directory with `ls -1d ... | sort -r | head -1` at `scripts/cancel-pr-loop.sh:76`. If no directory exists, it emits `NO_LOOP` and exits `1` at `scripts/cancel-pr-loop.sh:78-82`. If the newest directory exists but lacks `state.md`, it emits `NO_ACTIVE_LOOP` and exits `1` at `scripts/cancel-pr-loop.sh:91-98`. With an active state file, it extracts `current_round`, `max_iterations`, and `pr_number` from top-level YAML-style lines at `scripts/cancel-pr-loop.sh:104-112`, touches `.cancel-requested` at `scripts/cancel-pr-loop.sh:118-119`, moves `state.md` to `cancel-state.md` at `scripts/cancel-pr-loop.sh:121-122`, then prints `CANCELLED` plus a summary at `scripts/cancel-pr-loop.sh:128-130`.

- inputs_outputs_state: Inputs are CLI options, `CLAUDE_PROJECT_DIR`, the caller working directory, and the latest loop directory under `$PROJECT_ROOT/.humanize/pr-loop`. Required active input state is `$LOOP_DIR/state.md`; optional metadata keys are intended to be `current_round`, `max_iterations`, and `pr_number`. Outputs are stdout status tokens (`NO_LOOP`, `NO_ACTIVE_LOOP`, or `CANCELLED`), a created `$LOOP_DIR/.cancel-requested` sentinel, and a filesystem state transition from `$LOOP_DIR/state.md` to `$LOOP_DIR/cancel-state.md`. Exit codes are documented in the script header at `scripts/cancel-pr-loop.sh:11-15`: success `0`, no active loop `1`, future reserved `2`, and other errors `3`. In practice, shell/tool failures under strict mode can also exit with the failing command’s status.

- gates_or_invariants: The primary active-loop invariant is "newest PR loop directory contains `state.md`"; this is enforced at `scripts/cancel-pr-loop.sh:76-98` and also documented in the command wrapper as the key principle at `commands/cancel-pr-loop.md:21`. The script never scans older loop directories after selecting the newest one, so active state is defined by newest-directory semantics, not by any directory containing `state.md`. It requires `state.md` before touching/moving cancellation state. It does not validate that `cancel-state.md` is absent before `mv`, so a pre-existing cancel state can be overwritten by normal `mv` behavior. It does not inspect PR status, bot status, or Git state; cancellation is purely local loop-state mutation. The `--force` flag is explicitly documented as currently having no additional effect in the help text at `scripts/cancel-pr-loop.sh:39-41`.

- dependencies_and_callers: The script depends on standard shell utilities: `ls`, `sort`, `head`, `grep`, `sed`, `tr`, `touch`, and `mv`. It is exposed by `commands/cancel-pr-loop.md`, whose allowed tools permit only `${CLAUDE_PLUGIN_ROOT}/scripts/cancel-pr-loop.sh` and the same script with `--force` at `commands/cancel-pr-loop.md:3`; the wrapper tells the command agent to dispatch based on the first output line at `commands/cancel-pr-loop.md:18-19`. The producer of the expected active state is `scripts/setup-pr-loop.sh`, which writes `current_round`, `max_iterations`, `pr_number`, active/configured bots, Codex settings, polling settings, and trigger metadata into `$LOOP_DIR/state.md` at `scripts/setup-pr-loop.sh:535-555`. The PR loop stop hook observes `.cancel-requested` during polling and exits its poll loop when the signal exists at `hooks/pr-loop-stop-hook.sh:941-945`. Shared loop helpers define matching PR-loop active detection by newest directory plus `state.md` at `hooks/lib/loop-common.sh:795-813`. The Bash validator has a special authorization path for cancellation moves: it requires `.cancel-requested`, only allows a source of `state.md` or `finalize-state.md`, and only allows a destination of `cancel-state.md` at `hooks/lib/loop-common.sh:547-709`; this aligns with the script’s touch-then-move sequence.

- edge_cases_or_failure_modes: Missing metadata keys are intended to default to `?` at `scripts/cancel-pr-loop.sh:109-112`, but because the assignments at `scripts/cancel-pr-loop.sh:105-107` run `grep` pipelines under `set -euo pipefail`, a missing `current_round`, `max_iterations`, or `pr_number` can terminate the script before the defaults are applied. The script’s newest-directory selection assumes timestamp-like directory names sort correctly; if a newer completed/cancelled directory exists without `state.md` while an older directory still has `state.md`, the script reports `NO_ACTIVE_LOOP` and ignores the older active-looking state. The `ls | sort | head` discovery is not robust for loop paths containing newlines, though loop directories appear timestamp-generated. Running the script twice against the same loop returns `NO_ACTIVE_LOOP` on the second run because `state.md` has already been moved. A failed `touch` or `mv` leaves partial state possible, especially if `.cancel-requested` is created but `mv` fails. There is no locking, so a concurrent stop hook or another cancellation/completion transition could race with the `mv`. There is no symlink defense inside this script itself; symlink safety is handled by the separate hook helper authorization path, but direct shell execution outside that hook layer would rely only on filesystem permissions and `mv` behavior.

- validation_or_tests: Focused tests exist in `tests/test-pr-loop-scripts.sh`: the cancel test suite starts around `tests/test-pr-loop-scripts.sh:169`, checks `--help` output around `tests/test-pr-loop-scripts.sh:181-189`, verifies no-loop behavior returns exit `1` and `NO_LOOP` around `tests/test-pr-loop-scripts.sh:193-214`, and verifies active cancellation by creating a mock `.humanize/pr-loop/<timestamp>/state.md`, running the script, and asserting `cancel-state.md` exists while `state.md` is gone around `tests/test-pr-loop-scripts.sh:217-240`. I did not modify files or run the full test suite; this was a read-only research pass. A direct `git status --short` attempt was not useful in this branch export because the path is not a normal git checkout in the restricted environment.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`