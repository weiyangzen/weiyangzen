# agent_144 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-144 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for the repository’s portable timeout abstraction. The file validates `scripts/portable-timeout.sh`, which is sourced by loop hooks and setup scripts to bound git, GitHub CLI, Codex, and other external commands. The test is part of the aggregate test runner via [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/run-all-tests.sh:65).
- algorithmic_behavior: The script initializes under strict Bash mode, resolves `SCRIPT_DIR` and `PROJECT_ROOT`, then sources the timeout implementation and shared test helpers at [tests/robustness/test-timeout-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/robustness/test-timeout-robustness.sh:12). It exercises the timeout selection and execution algorithm in 18 numbered checks. Tests 1-2 validate implementation detection and allowed implementation names: `gtimeout`, `timeout`, `python3`, `python`, or `none` at [tests/robustness/test-timeout-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/robustness/test-timeout-robustness.sh:31). Tests 3-7 validate normal command execution, argument preservation, command exit-code preservation, and shell pipeline support at [tests/robustness/test-timeout-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/robustness/test-timeout-robustness.sh:51). Tests 8-18 cover short and zero timeout values, large output, rapid repeated invocations, missing commands, empty commands, special characters, signal handling, fallback detection, subshell execution, and `TIMEOUT_IMPL` export behavior from [tests/robustness/test-timeout-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/robustness/test-timeout-robustness.sh:120) through [tests/robustness/test-timeout-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/robustness/test-timeout-robustness.sh:259).
- inputs_outputs_state: Inputs are the host executable environment, the sourced `TIMEOUT_IMPL` value, and command/timeout pairs passed into `run_with_timeout`. The test’s command inputs include `echo`, `sleep`, `printf`, `sh -c`, `seq`, an intentionally missing executable, an empty command string, and a shell command with a `TERM` trap. Outputs are printed PASS/FAIL lines plus the final summary from `print_test_summary` at [tests/robustness/test-timeout-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/robustness/test-timeout-robustness.sh:278). State is limited to shell variables such as `RESULT`, `EXIT_CODE`, `START`, `END`, `ELAPSED`, `SUCCESS_COUNT`, `DETECTED`, and helper counters `TESTS_PASSED` / `TESTS_FAILED` / `TESTS_SKIPPED` from [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-helpers.sh:22). The tested implementation sets `TIMEOUT_IMPL=$(detect_timeout_impl)` and exports it at [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/portable-timeout.sh:29) and [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/portable-timeout.sh:76).
- gates_or_invariants: The core invariants are: detection must yield a non-empty known implementation or `none`; quick commands must return output and exit `0`; timed-out commands must return GNU-compatible exit `124` when a timeout implementation exists; successful command non-zero exit codes must be preserved; arguments and special characters must remain unexpanded and uncorrupted; repeated timeout calls must not poison later calls; missing or empty commands must not report false success; and the fallback detector must always return one of the accepted implementation names. The timeout helper’s algorithmic priority is `gtimeout` first, GNU `timeout` second, then `python3`, `python`, and finally no timeout at [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/portable-timeout.sh:6). The Python fallback normalizes `subprocess.TimeoutExpired` to exit `124` at [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/portable-timeout.sh:56).
- dependencies_and_callers: Direct dependencies are [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/portable-timeout.sh:10) for `detect_timeout_impl` / `run_with_timeout` and [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-helpers.sh:30) for `pass`, `fail`, and summary behavior. The aggregate caller is [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/run-all-tests.sh:32), where this file appears in the `TEST_SUITES` list. Repository-wide references show `run_with_timeout` is used by `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, `scripts/check-bot-reactions.sh`, `scripts/setup-pr-loop.sh`, `scripts/setup-rlcr-loop.sh`, and related robustness tests; this makes the assigned file a behavioral guard for broader hook and automation reliability.
- edge_cases_or_failure_modes: The spec explicitly covers no timeout implementation by treating timeout-specific tests as skipped-pass cases when `TIMEOUT_IMPL == none`; very short timeout enforcement; zero timeout variance where either `0` or `124` is acceptable; large stdout via 1000 lines; five rapid calls; nonexistent commands; empty command strings; literal `$HOME & "quotes"` handling; signal-trapping commands that may exit `0` or `124`; subshell use; and export visibility. One notable behavior from the implementation is that the Python fallback does not kill process groups, only the direct subprocess managed by `subprocess.run`; the assigned test validates simple `sh -c` cases but does not deeply assert cleanup of grandchildren. The `none` fallback runs commands without a timeout at [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/portable-timeout.sh:64), so hangs are still possible on hosts without GNU timeout, Homebrew `gtimeout`, or Python.
- validation_or_tests: Read-only validation performed: `bash -n tests/robustness/test-timeout-robustness.sh` exited `0`. Full run performed: `bash tests/robustness/test-timeout-robustness.sh` exited `0` with summary `Passed: 18`, `Failed: 0`. Local run detected `TIMEOUT_IMPL=python3`; macOS developer-tool cache/FSEvents warnings appeared because the read-only sandbox could not create some `/tmp/xcrun_db-*` cache files, but the assigned test still completed successfully.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 item evidence section present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`