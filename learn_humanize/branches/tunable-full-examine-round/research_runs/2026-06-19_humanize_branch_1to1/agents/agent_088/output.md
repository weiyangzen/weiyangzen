# agent_088 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-088 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: This is a blocking prompt template used by the RLCR/Codex stop hook when the latest TodoWrite state still contains unfinished todos. It is part of the loop’s exit-control algorithm: before expensive Codex review is allowed, the hook checks whether the agent has incomplete local todos and, if so, returns a block decision with this rendered message. The template’s core contract is visible at `prompt-template/block/incomplete-todos.md:1-12`: announce incomplete todos, inject the unfinished list, require completion via TodoWrite, and forbid proceeding to Codex review until all todos are finished.
- algorithmic_behavior: The file itself has no executable logic; it defines the human/agent-facing transition rule for an already-detected gate failure. Runtime behavior is implemented in `hooks/loop-codex-stop-hook.sh:343-393`: the hook runs `hooks/check-todos-from-transcript.py`, treats exit code `1` as “incomplete todos found,” extracts the checker output after the first marker line with `tail -n +2`, renders `block/incomplete-todos.md` using `INCOMPLETE_LIST`, and emits JSON with `"decision": "block"`. The template’s line `prompt-template/block/incomplete-todos.md:5` is the only dynamic insertion point: `{{INCOMPLETE_LIST}}`.
- inputs_outputs_state: Input is `INCOMPLETE_LIST`, supplied as a rendered template variable by `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:381-382`. That list is produced from the most recent TodoWrite call in the transcript by `hooks/check-todos-from-transcript.py:20-87` and formats every todo whose `status != "completed"` as `  - [status] content` at `hooks/check-todos-from-transcript.py:118-130`. Output is a markdown reason string embedded in the hook’s JSON block response at `hooks/loop-codex-stop-hook.sh:384-391`. State transition: attempted stop -> todo quick-check -> if any incomplete todo exists, stop is blocked and Codex review is skipped; only after all todos are `completed` can the hook proceed beyond this gate.
- gates_or_invariants: The invariant enforced by this template/caller pair is “no Codex review and no stop summary while TodoWrite contains unfinished work.” Template lines `prompt-template/block/incomplete-todos.md:7-10` encode the required action sequence: complete todos, mark each completed through TodoWrite, then write summary/stop. The final line `prompt-template/block/incomplete-todos.md:12` makes this a pre-review gate rather than a post-review warning. Rendering is single-pass through `hooks/lib/template-loader.sh:50-131`, so placeholder-like text inside `INCOMPLETE_LIST` is not recursively expanded.
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:381`, which loads this template through `load_and_render_safe`. Template discovery comes from `TEMPLATE_DIR`, initialized via `hooks/lib/loop-common.sh` and implemented by `hooks/lib/template-loader.sh:24-31`. The runtime detector dependency is `hooks/check-todos-from-transcript.py`, whose supported transcript shapes include assistant messages, direct `tool_use`, and alternate message format entries at `hooks/check-todos-from-transcript.py:51-85`. `rg` found no other direct caller of `block/incomplete-todos.md`.
- edge_cases_or_failure_modes: If the template file is missing or renders empty, `load_and_render_safe` falls back to an inline message at `hooks/loop-codex-stop-hook.sh:376-382` and `hooks/lib/template-loader.sh:170-203`, so the gate still blocks. If hook input JSON is invalid, the stop hook uses a separate parse-error block path instead of this template (`hooks/loop-codex-stop-hook.sh:351-368`). If no transcript path, empty input, missing transcript, or no TodoWrite entries exist, the checker exits `0` and this template is not used (`hooks/check-todos-from-transcript.py:90-116`). Missing todo status is treated as incomplete because only exact `completed` passes (`hooks/check-todos-from-transcript.py:120-124`). Multiple TodoWrite calls use the latest non-empty todo list, so stale unfinished todos can be superseded by a later completed list (`hooks/check-todos-from-transcript.py:28-87`).
- validation_or_tests: `tests/test-todo-checker.sh:121-158` validates that pending and in-progress todos exit `1` and include task details. `tests/test-todo-checker.sh:196-240` validates latest TodoWrite selection and transcript format variants. `tests/test-todo-checker.sh:249-275` validates edge cases such as missing status and empty content as incomplete. `tests/test-finalize-phase.sh:715-738` validates the stop hook blocks finalize exit when incomplete todos exist and does not invoke Codex on that path. `tests/test-template-references.sh:54-115` scans shell template references and verifies referenced template files exist, covering this template’s direct hook reference. I did not run tests because the task requested research notes only and the branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 Item Evidence section
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`