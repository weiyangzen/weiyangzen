# agent_040 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-040 `file` `scripts/setup-pr-loop.sh`
- cursor: `[_]`
- core_role: Initializes the `/humanize:start-pr-loop` runtime workflow for PR review loops. It is not a passive installer: it validates the local repo and GitHub context, selects monitored review bots, detects the PR and fork/base repository, fetches round-0 review material, classifies startup state, may post a trigger comment for stale reviews, creates `.humanize/pr-loop/<timestamp>/` state and prompt artifacts, and prints the first operator prompt that hands later control to `hooks/pr-loop-stop-hook.sh`. The command surface points directly at this script from `commands/start-pr-loop.md:10-14`.
- algorithmic_behavior: The script is a single top-level bash workflow with one local `show_help` function. It starts with strict shell mode and default runtime constants for Codex, polling, git timeouts, and GitHub CLI timeouts at `scripts/setup-pr-loop.sh:11-31`. Argument parsing accepts `--claude`, `--codex`, `--max`, `--codex-model MODEL:EFFORT`, and `--codex-timeout` at `scripts/setup-pr-loop.sh:110-173`; at least one bot flag is mandatory at `scripts/setup-pr-loop.sh:179-190`. Active bots are stored as canonical short names, `claude` and/or `codex`, at `scripts/setup-pr-loop.sh:192-202`, then converted into mention strings through `build_bot_mention_string` from `hooks/lib/loop-common.sh:764-777`. After prerequisite checks, it resolves the current branch, detects the current GitHub repo, optionally detects a parent repo for fork PRs, finds the PR in current or parent repo, validates numeric PR number, rejects merged/closed PRs, and resolves the PR base repo for comment APIs at `scripts/setup-pr-loop.sh:283-359`. It then validates YAML-safety for branch/model/effort before writing state at `scripts/setup-pr-loop.sh:365-388`. State setup creates `.humanize/pr-loop/<timestamp>` at `scripts/setup-pr-loop.sh:394-400`, fetches initial comments through `scripts/fetch-pr-comments.sh` at `scripts/setup-pr-loop.sh:406-412`, classifies reviewer startup status through `scripts/check-pr-reviewer-status.sh` at `scripts/setup-pr-loop.sh:418-428`, and falls back to local `git rev-parse HEAD` if the API lacks a latest commit SHA at `scripts/setup-pr-loop.sh:430-433`. For startup case 4 or 5, it posts a bot trigger comment and records GitHub’s comment timestamp rather than local time at `scripts/setup-pr-loop.sh:443-482`; for Claude, it also requires a Claude eyes reaction before continuing at `scripts/setup-pr-loop.sh:484-521`. After that, it writes `state.md`, `goal-tracker.md`, `round-0-pr-comment.md`, and `round-0-prompt.md`, then prints activation metadata and critical completion instructions at `scripts/setup-pr-loop.sh:535-865`.
- inputs_outputs_state: Inputs are CLI flags, environment variables `CLAUDE_PROJECT_DIR` and optional `TEMPLATE_DIR`, the current git worktree, GitHub CLI auth/session state, the current branch’s PR state, GitHub API comments/reviews/reactions, prompt templates, and helper scripts. Outputs are filesystem state under `$PROJECT_ROOT/.humanize/pr-loop/<timestamp>/`: `state.md` frontmatter at `scripts/setup-pr-loop.sh:535-555`, `goal-tracker.md` at `scripts/setup-pr-loop.sh:561-633`, `round-0-pr-comment.md` from the fetch script at `scripts/setup-pr-loop.sh:406-412`, and `round-0-prompt.md` at `scripts/setup-pr-loop.sh:677-771`. It also outputs user-facing activation text and the generated initial prompt to stdout at `scripts/setup-pr-loop.sh:780-862`. The state file includes `current_round: 0`, `max_iterations`, `pr_number`, `start_branch`, immutable `configured_bots`, mutable `active_bots`, Codex model/effort/timeout, poll interval/timeout, timestamps, startup case, latest commit metadata, and optional trigger comment metadata at `scripts/setup-pr-loop.sh:535-554`. Later, `hooks/pr-loop-stop-hook.sh` consumes those fields: it parses current round, bot lists, Codex config, polling config, startup case, latest commit SHA/time, and trigger comment ID at `hooks/pr-loop-stop-hook.sh:87-174`; it blocks exit if the current round’s resolve file is missing at `hooks/pr-loop-stop-hook.sh:262-277`; it also uses stored latest commit SHA for force-push detection at `hooks/pr-loop-stop-hook.sh:364-380`. State transitions are: no loop to active loop by creating `state.md`; failed case-4/5 trigger or Claude reaction validation removes the new loop directory at `scripts/setup-pr-loop.sh:457-458`, `scripts/setup-pr-loop.sh:500-502`, and `scripts/setup-pr-loop.sh:516-518`; later stop-hook transitions can rename active state to merged/closed state when the PR closes or merges at `hooks/pr-loop-stop-hook.sh:244-255`.
- gates_or_invariants: Mutual exclusion is enforced before starting: existing RLCR or PR loop state blocks the script at `scripts/setup-pr-loop.sh:222-240`, using `find_active_pr_loop` from `hooks/lib/loop-common.sh:795-814`. Repo gates require a git repo and at least one commit at `scripts/setup-pr-loop.sh:243-253`, `gh` installation and auth at `scripts/setup-pr-loop.sh:255-269`, and `codex` availability at `scripts/setup-pr-loop.sh:271-277`. PR gates require an associated PR, numeric PR number, and non-merged/non-closed state at `scripts/setup-pr-loop.sh:326-349`. Fork support invariant: PR lookup can happen against current or parent repo, while comments/reactions use the PR base repo at `scripts/setup-pr-loop.sh:289-359` and `scripts/setup-pr-loop.sh:447-449`. YAML/state integrity gates reject branch names containing `: # " ' \`` or newline, reject Codex model characters outside `[a-zA-Z0-9._-]`, and reject effort characters outside `[a-zA-Z0-9_-]` at `scripts/setup-pr-loop.sh:365-388`. Trigger-comment invariant: if startup status indicates stale reviews, bots must be explicitly re-triggered; on comment-post failure the script aborts and deletes the newly created loop dir at `scripts/setup-pr-loop.sh:443-458`. Claude-specific invariant: when `--claude` participates in that re-trigger path, setup must retrieve the trigger comment ID and confirm Claude’s eyes reaction, otherwise it aborts and cleans up at `scripts/setup-pr-loop.sh:484-521`. Prompt-state invariant: generated state files are system-managed; validators block direct user modification of PR loop state and generated prompt/comment/check/feedback files through helpers in `hooks/lib/loop-common.sh:825-905`.
- dependencies_and_callers: Direct caller is the slash command definition `commands/start-pr-loop.md:10-14`, with behavior documented there at `commands/start-pr-loop.md:16-23`. Internal sourced dependencies are `scripts/portable-timeout.sh` for cross-platform `run_with_timeout` at `scripts/setup-pr-loop.sh:33-35` and `hooks/lib/template-loader.sh` for safe template loading at `scripts/setup-pr-loop.sh:37-42`; `portable-timeout.sh` chooses `gtimeout`, GNU `timeout`, Python, or no-timeout fallback at `scripts/portable-timeout.sh:9-71`, and `template-loader.sh` uses single-pass `{{VAR}}` rendering to prevent recursive placeholder injection at `hooks/lib/template-loader.sh:50-132`. It sources `hooks/lib/loop-common.sh` for loop discovery, bot mapping, YAML list building, and mention-string construction at `scripts/setup-pr-loop.sh:210-214`; bot mapping and YAML helpers are in `hooks/lib/loop-common.sh:722-777`. Runtime child scripts are `scripts/fetch-pr-comments.sh`, `scripts/check-pr-reviewer-status.sh`, and `scripts/check-bot-reactions.sh`. Comment fetching resolves the PR base repo and fetches issue comments, inline review comments, and PR reviews at `scripts/fetch-pr-comments.sh:123-220`, deduplicates/sorts/formats them at `scripts/fetch-pr-comments.sh:321-451`, and emits the `*No comments found.*` sentinel that setup uses at `scripts/setup-pr-loop.sh:643-649`. Reviewer-status classification maps bots to GitHub authors, gathers comment/review timestamps, and produces cases 1-5 at `scripts/check-pr-reviewer-status.sh:87-100`, `scripts/check-pr-reviewer-status.sh:150-202`, and `scripts/check-pr-reviewer-status.sh:204-272`. Reaction checks detect Codex thumbs-up and Claude eyes reactions through GitHub reactions APIs at `scripts/check-bot-reactions.sh:149-184` and `scripts/check-bot-reactions.sh:259-295`. Downstream consumer is `hooks/pr-loop-stop-hook.sh`, which interprets the created state and manages polling, local Codex validation, blocking, and later round advancement.
- edge_cases_or_failure_modes: `--max` and `--codex-timeout` validation accepts any digit string, including `0`, despite error text saying positive integer; robustness tests document this behavior for timeout zero at `tests/robustness/test-setup-scripts-robustness.sh:749-765`. `--codex-model` without `:` silently uses default effort, while `MODEL:EFFORT` splits on the first colon at `scripts/setup-pr-loop.sh:135-148`; the later YAML-safety regexes reject unsafe characters but do not validate semantic model names or effort levels at `scripts/setup-pr-loop.sh:374-388`. If the branch name includes YAML-unsafe characters, setup fails before state creation at `scripts/setup-pr-loop.sh:365-372`. If GitHub repo/parent/PR discovery fails, setup exits before creating usable state at `scripts/setup-pr-loop.sh:295-332`. If fetching reviewer status fails, the script warns and defaults to case 1 with empty metadata at `scripts/setup-pr-loop.sh:418-422`; this allows startup to proceed but may under-trigger stale-review flows. Comment fetching logs API failures as warnings and writes an incomplete-comments warning instead of failing hard at `scripts/fetch-pr-comments.sh:179-203` and `scripts/fetch-pr-comments.sh:444-449`, so round-0 prompts can be incomplete under partial GitHub API failure. Case 4/5 trigger posting is fail-closed and removes the session directory at `scripts/setup-pr-loop.sh:446-458`. For Claude case 4/5, missing trigger comment ID or missing eyes reaction also removes the session directory at `scripts/setup-pr-loop.sh:488-518`. Timestamp handling deliberately avoids local-time fallback when trigger comment timestamp lookup fails to avoid future-skew filtering by the stop hook at `scripts/setup-pr-loop.sh:479-482`. The `trap 'exit 0' PIPE` near final output at `scripts/setup-pr-loop.sh:777-778` prevents broken-pipe output failures from turning an otherwise-created loop into a nonzero command result.
- validation_or_tests: Argument parsing is covered by `tests/test-pr-loop-scripts.sh:18-166`, including help, missing bot flag, unknown option, bot flag recognition, `--max`, `--codex-model`, and `--codex-timeout`. Robustness tests cover help, missing bot, unknown option, nonnumeric `--max`, non-git rejection, active-loop mutual exclusion, valid setup reaching GitHub auth checks, and nonnumeric PR `--codex-timeout` rejection at `tests/robustness/test-setup-scripts-robustness.sh:473-779`. Broader PR-loop system tests cover setup failure when case 4/5 cannot retrieve a trigger comment ID, including cleanup of the loop directory at `tests/test-pr-loop-system.sh:1384-1447`, and setup creation of `goal-tracker.md` for a mocked open PR at `tests/test-pr-loop-system.sh:1453-1530`. I did not run tests in this worker pass because the branch export is explicitly read-only and these tests create/remove `.humanize/pr-loop` directories and mocked fixtures.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence section present for `scripts/setup-pr-loop.sh`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`