# agent_020 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-020 `file` `commands/gen-plan.md`
- cursor: `[_]`
- core_role: `commands/gen-plan.md` is the Claude slash-command workflow that turns an informal draft into a structured implementation plan for the Humanize RLCR loop. It is a command definition rather than runtime algorithm code, but it is core to the plan-generation surface: it defines how a draft becomes a `plan.md` with goal description, AC-style acceptance criteria, path boundaries, feasibility hints, dependency sequencing, and code-style constraints. The README presents it as the first step before `/humanize:start-rlcr-loop`, which consumes the generated plan (`README.md:76`, `README.md:82`, `README.md:122`).
- algorithmic_behavior: The command is a six-phase pipeline. Phase 1 runs `${CLAUDE_PLUGIN_ROOT}/scripts/validate-gen-plan-io.sh $ARGUMENTS` and branches on exit codes (`commands/gen-plan.md:30`, `commands/gen-plan.md:34`, `commands/gen-plan.md:38`). Phase 2 reads the draft and delegates a lenient repository relevance verdict to `humanize:draft-relevance-checker` on the haiku model (`commands/gen-plan.md:50`, `commands/gen-plan.md:56`, `commands/gen-plan.md:57`). Phase 3 performs deeper draft analysis across clarity, consistency, completeness, and functionality, with Explore agents used to inspect affected code and patterns (`commands/gen-plan.md:76`, `commands/gen-plan.md:80`, `commands/gen-plan.md:101`). Phase 4 resolves analysis issues with `AskUserQuestion` and separately confirms quantitative metrics as either hard requirements or optimization directions (`commands/gen-plan.md:111`, `commands/gen-plan.md:117`, `commands/gen-plan.md:126`). Phase 5 generates the structured plan from a fixed schema and generation rules (`commands/gen-plan.md:142`, `commands/gen-plan.md:148`, `commands/gen-plan.md:225`). Phase 6 updates the already-created output, reviews it for consistency, optionally unifies mixed languages, and reports path, contents summary, AC count, and language status (`commands/gen-plan.md:251`, `commands/gen-plan.md:262`, `commands/gen-plan.md:272`, `commands/gen-plan.md:286`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, expected as `--input <draft.md> --output <plan.md>` (`commands/gen-plan.md:3`, `commands/gen-plan.md:35`); the draft file content; repository context gathered by Read/Glob/Grep/Task; and user clarification answers from `AskUserQuestion`. The validation script normalizes input/output paths with `realpath -m`, derives the output directory, and prints validation metadata (`scripts/validate-gen-plan-io.sh:69`, `scripts/validate-gen-plan-io.sh:74`). On success, it copies `prompt-template/plan/gen-plan-template.md` to the output path and appends the original draft between explicit start/end markers (`scripts/validate-gen-plan-io.sh:133`, `scripts/validate-gen-plan-io.sh:148`, `scripts/validate-gen-plan-io.sh:151`). The command’s intended final output is a completed plan file that still contains the original draft intact at the bottom (`commands/gen-plan.md:253`, `commands/gen-plan.md:257`). Persistent state is limited to the new output plan file; downstream RLCR setup later copies that plan into `.humanize/rlcr/<timestamp>/plan.md`, stores `plan_file` and `plan_tracked` in `state.md`, and extracts goal/AC text into `goal-tracker.md` (`scripts/setup-rlcr-loop.sh:675`, `scripts/setup-rlcr-loop.sh:709`, `scripts/setup-rlcr-loop.sh:785`, `scripts/setup-rlcr-loop.sh:809`).
- gates_or_invariants: IO gates are explicit: exit 1 input missing, 2 input empty, 3 output directory missing, 4 output exists or is a directory, 5 output directory not writable, 6 invalid arguments/help, 7 missing plan template (`commands/gen-plan.md:38`; implemented in `scripts/validate-gen-plan-io.sh:79`, `scripts/validate-gen-plan-io.sh:87`, `scripts/validate-gen-plan-io.sh:95`, `scripts/validate-gen-plan-io.sh:103`, `scripts/validate-gen-plan-io.sh:118`, `scripts/validate-gen-plan-io.sh:142`). Relevance is a stop gate only when the draft is clearly unrelated (`commands/gen-plan.md:67`). Draft preservation is a hard invariant: original human draft content must never be discarded or overwritten by clarifications (`commands/gen-plan.md:113`, `commands/gen-plan.md:247`). Plan-generation invariants include AC-X or AC-X.Y criterion IDs, both positive and negative tests per AC, no time estimates, no code line references, dependency sequencing by relationship rather than time, affirmative path boundaries, and an implementation note banning plan-specific workflow terms in implementation code/comments (`commands/gen-plan.md:225`, `commands/gen-plan.md:229`, `commands/gen-plan.md:239`, `commands/gen-plan.md:241`, `commands/gen-plan.md:245`). Language unification is optional and user-mediated, not automatic (`commands/gen-plan.md:272`).
- dependencies_and_callers: Direct dependencies are declared in frontmatter: the validation script, Read, Glob, Grep, Task, Write, and AskUserQuestion (`commands/gen-plan.md:4`). The concrete script dependency is `scripts/validate-gen-plan-io.sh`, whose template dependency is `prompt-template/plan/gen-plan-template.md` (`scripts/validate-gen-plan-io.sh:135`, `scripts/validate-gen-plan-io.sh:139`). The relevance subagent is `agents/draft-relevance-checker.md`, configured with model `haiku` and Read/Glob/Grep tools (`agents/draft-relevance-checker.md:1`, `agents/draft-relevance-checker.md:4`, `agents/draft-relevance-checker.md:5`). The generated plan is consumed by `/humanize:start-rlcr-loop`, which initializes the implementation loop, goal tracker, and immutable acceptance criteria surface (`commands/start-rlcr-loop.md:16`, `commands/start-rlcr-loop.md:27`, `scripts/setup-rlcr-loop.sh:803`). Plan integrity is later guarded by RLCR hooks that block active-loop plan deletion or modification before review phase (`hooks/loop-codex-stop-hook.sh:257`, `hooks/loop-codex-stop-hook.sh:283`, `hooks/loop-codex-stop-hook.sh:315`) and validate tracked/untracked plan-file status during prompts (`hooks/loop-plan-file-validator.sh:117`, `hooks/loop-plan-file-validator.sh:123`, `hooks/loop-plan-file-validator.sh:193`).
- edge_cases_or_failure_modes: The command spec says Phase 6 should use the Edit tool, but the frontmatter allowed tools include `Write` and not `Edit`; in Claude command execution this can block the prescribed update path or force behavior that contradicts the command text (`commands/gen-plan.md:4`, `commands/gen-plan.md:257`). The validation script creates the output plan before relevance analysis, so a later NOT_RELEVANT stop can leave a template-plus-draft output file behind even though the command did not complete (`scripts/validate-gen-plan-io.sh:148`, `commands/gen-plan.md:67`). Output paths may be absolute or outside the repo because `validate-gen-plan-io.sh` only checks existence/writeability, unlike `start-rlcr-loop` which later requires relative in-project plan paths (`scripts/validate-gen-plan-io.sh:69`, `scripts/setup-rlcr-loop.sh:320`, `scripts/setup-rlcr-loop.sh:394`). `realpath -m` can normalize non-existent path components, but the script still refuses missing output directories (`scripts/validate-gen-plan-io.sh:70`, `scripts/validate-gen-plan-io.sh:95`). Missing `${CLAUDE_PLUGIN_ROOT}` falls back to script-relative template lookup (`scripts/validate-gen-plan-io.sh:133`), while an incorrect set value can force exit 7. Multi-language drafts are supported only after generation via a user choice; if the user declines, mixed-language output is allowed by design (`commands/gen-plan.md:274`, `commands/gen-plan.md:284`).
- validation_or_tests: `tests/test-gen-plan.sh` validates command file existence, non-empty description, allowed-tools, argument hint, relevance-agent existence/name/model/tools, naming rules, minimal YAML/frontmatter, and English-only content for the command and agent (`tests/test-gen-plan.sh:55`, `tests/test-gen-plan.sh:67`, `tests/test-gen-plan.sh:81`, `tests/test-gen-plan.sh:107`, `tests/test-gen-plan.sh:220`, `tests/test-gen-plan.sh:280`, `tests/test-gen-plan.sh:403`). It also exercises `validate-gen-plan-io.sh` edge cases for missing option values, unknown option, missing input, empty input, missing output directory, existing output file, output path as directory, valid paths, and help (`tests/test-gen-plan.sh:426`, `tests/test-gen-plan.sh:438`, `tests/test-gen-plan.sh:474`, `tests/test-gen-plan.sh:483`, `tests/test-gen-plan.sh:493`, `tests/test-gen-plan.sh:503`, `tests/test-gen-plan.sh:513`, `tests/test-gen-plan.sh:523`, `tests/test-gen-plan.sh:532`). These tests cover structural and IO behavior, but they do not validate semantic plan quality, preservation of every draft requirement, correct AC numbering, Explore-agent completeness, quantitative metric confirmation, language unification behavior, or the frontmatter/tool mismatch around Edit vs Write.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-020`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`