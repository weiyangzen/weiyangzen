# agent_068 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-068 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for the repository’s prompt template loading and rendering primitive. The test sources `hooks/lib/template-loader.sh` at `tests/test-template-loader.sh:10-12` and verifies the functions that many hook paths use to produce block messages, review prompts, next-round prompts, and setup content. This is core algorithm coverage because template rendering sits on the hook decision path: validators and stop hooks call `load_and_render_safe` to turn structured state into human-facing block/review messages, for example `hooks/loop-bash-validator.sh:96-104` blocks `git push` by rendering `prompt-template/block/git-push.md`.

- algorithmic_behavior: The script is a black-box test harness around six template-loader behaviors. First, it resolves the template root from a library path via `get_template_dir`, expecting `prompt-template` under the plugin root (`tests/test-template-loader.sh:42-52`, implementation at `hooks/lib/template-loader.sh:26-31`). Second, it verifies file loading for present and missing templates (`tests/test-template-loader.sh:55-78`, implementation at `hooks/lib/template-loader.sh:36-48`). Third, it verifies `render_template` substitutions for one variable, many variables, multiline text, unused variables, and unreplaced placeholders (`tests/test-template-loader.sh:81-194`). The implementation builds `TMPL_VAR_` environment entries, then uses a single `awk` pass that scans for `{{...}}`, replaces only known names, and leaves unknown placeholders intact (`hooks/lib/template-loader.sh:58-132`). Fourth, it tests `load_and_render` on a real prompt template, `prompt-template/block/wrong-round-number.md`, expecting variables like `{{CLAUDE_ROUND}}` and `{{CURRENT_ROUND}}` to be filled (`tests/test-template-loader.sh:147-164`; template at `prompt-template/block/wrong-round-number.md:1-5`). Fifth, it tests `load_and_render_safe` fallback selection for missing templates and normal template selection for existing templates (`tests/test-template-loader.sh:197-222`, implementation at `hooks/lib/template-loader.sh:170-203`). Sixth, it validates template directory gating (`tests/test-template-loader.sh:225-244`, implementation at `hooks/lib/template-loader.sh:208-222`).

- inputs_outputs_state: Inputs are the repository root inferred from `BASH_SOURCE`, the sourced library path, template names such as `block/git-push.md` and `block/wrong-round-number.md`, inline template strings, and `VAR=value` render arguments. Outputs are terminal PASS/FAIL lines plus process exit code: the script increments `TESTS_PASSED` and `TESTS_FAILED` through `pass()` and `fail()` helpers (`tests/test-template-loader.sh:20-34`), prints a summary (`tests/test-template-loader.sh:641-649`), exits `0` only when failures are zero (`tests/test-template-loader.sh:651-654`), and exits `1` otherwise (`tests/test-template-loader.sh:655-658`). State is local to the shell process: counters, `TEMPLATE_DIR`, `CONTENT`, `RESULT`, `EXPECTED`, and test-specific inline strings. The sourced renderer temporarily transfers render variables into environment names prefixed with `TMPL_VAR_` (`hooks/lib/template-loader.sh:62-69`) so `awk` can read them from `ENVIRON` (`hooks/lib/template-loader.sh:75-82`).

- gates_or_invariants: The script runs with `set -euo pipefail` (`tests/test-template-loader.sh:8`), so unexpected unset variables or unhandled command failures can abort the suite, while assertion mismatches are intentionally counted through `fail()`. The renderer contract under test is single-pass substitution: values containing `{{OTHER_VAR}}` must remain literal and must not be recursively expanded (`tests/test-template-loader.sh:488-543`; documented in `hooks/lib/template-loader.sh:50-57`). Missing template variables stay as their original placeholders (`tests/test-template-loader.sh:182-194`; implementation at `hooks/lib/template-loader.sh:119-122`). Values containing shell metacharacters must render literally rather than being interpreted by the shell or by regex replacement machinery; the suite covers ampersand, backslash, dollar signs, backticks, pipes, semicolons, globs, parentheses, redirection characters, quotes, hash, tilde, regex-like strings, JSON, and multiline values (`tests/test-template-loader.sh:247-486`). Directory validation requires the template root plus `block`, `codex`, and `claude` child directories (`hooks/lib/template-loader.sh:211-219`), matching the existing tree that includes those folders and additional `plan` and `pr-loop` template families.

- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`, sourced at `tests/test-template-loader.sh:12`. Template dependencies include `prompt-template/block/git-push.md`, checked for the heading text `Git Push Blocked` (`tests/test-template-loader.sh:55-65`; template at `prompt-template/block/git-push.md:1-9`), and `prompt-template/block/wrong-round-number.md`, checked for round/path substitutions (`tests/test-template-loader.sh:151-160`; template at `prompt-template/block/wrong-round-number.md:1-5`). The same library is loaded centrally by `hooks/lib/loop-common.sh:139-148`, which initializes and validates `TEMPLATE_DIR` for hook consumers. Call sites are broad: `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, read/write/edit/bash validators, setup scripts, and other tests use `load_template`, `load_and_render`, or `load_and_render_safe`. `tests/run-all-tests.sh:32-40` includes this file as the first suite, making it an early gate for the wider Humanize plugin test run. `tests/test-template-references.sh:66-105` separately scans hook call sites for referenced template files, so this assigned file complements reference-existence validation by asserting render semantics.

- edge_cases_or_failure_modes: The suite specifically guards against the old-style `awk gsub` ampersand replacement bug by testing multiple ampersands (`tests/test-template-loader.sh:425-435`). It guards against prompt corruption when externally supplied review content contains template-looking tokens (`tests/test-template-loader.sh:499-543`). It covers empty variable values (`tests/test-template-loader.sh:555-565`), non-ASCII template and value bytes using Bash `$'...'` strings (`tests/test-template-loader.sh:579-615`), underscore-prefixed variable names (`tests/test-template-loader.sh:617-627`), and numbered variable names (`tests/test-template-loader.sh:629-639`). A notable runtime failure mode is environmental: the current read-only sandbox blocks Bash’s here-string backing temp file used by the implementation at `hooks/lib/template-loader.sh:129`, so renderer tests fail here with “cannot create temp file for here document: Operation not permitted.” That is a sandbox artifact of this branch export run, not a semantic assertion failure observed from expected-vs-actual rendering logic. Another implementation edge visible from validation is that calling `render_template` with no render arguments can trip `env_vars[@]: unbound variable` under `set -u` when an existing template is rendered without variables; the assigned test exposes this through Test 12 (`tests/test-template-loader.sh:211-222`) in the current environment.

- validation_or_tests: I inspected the assigned file directly with line numbering, inspected `hooks/lib/template-loader.sh`, inspected the two real prompt templates used by the suite, and checked repository call sites. I also ran `bash tests/test-template-loader.sh` from the assigned read-only branch export. Observed result: 5 passed, 38 failed, exit code `1`. The passed checks were path/file/directory checks that do not invoke the here-string renderer: `get_template_dir`, existing `load_template`, missing `load_template`, valid `validate_template_dir`, and invalid `validate_template_dir`. The failures all route through `render_template` or `load_and_render_safe` rendering paths and report the read-only sandbox temp-file error at `hooks/lib/template-loader.sh:129`, except Test 12, which reports the unset `env_vars[@]` behavior for a no-variable render call.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 item section present; assigned item id appears exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`