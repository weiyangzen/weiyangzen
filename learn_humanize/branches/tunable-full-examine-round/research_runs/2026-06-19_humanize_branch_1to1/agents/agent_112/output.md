# agent_112 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-112 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: This file is a Claude-facing prompt fragment appended after a Full Goal Alignment Check finds issues and the loop continues into another implementation round. Its role is not executable algorithm logic, but it is part of the control prompt contract that turns Codex alignment-review findings into the next Claude work priorities. The block title is `Post-Alignment Check Action Items` at `prompt-template/claude/post-alignment-action-items.md:2`, and the trigger context is stated at `prompt-template/claude/post-alignment-action-items.md:4`.

- algorithmic_behavior: The template encodes a three-priority transition after full alignment review: address forgotten items, prioritize unmet acceptance criteria, and un-defer unjustified deferrals. The bullets at `prompt-template/claude/post-alignment-action-items.md:5`, `prompt-template/claude/post-alignment-action-items.md:6`, and `prompt-template/claude/post-alignment-action-items.md:7` map directly to findings the full alignment review is required to produce. The upstream Codex full-alignment template requires audits for Acceptance Criteria, forgotten items, and deferred items at `prompt-template/codex/full-alignment-review.md:19`, `prompt-template/codex/full-alignment-review.md:30`, and `prompt-template/codex/full-alignment-review.md:36`; it also requires “specific action items for Claude” at `prompt-template/codex/full-alignment-review.md:89`. This file is the downstream reminder that those categories are not ordinary review comments and should steer the next round.

- inputs_outputs_state: Inputs are implicit prompt context from the previous Codex review, especially the rendered `REVIEW_CONTENT` already inserted into `round-N-prompt.md` by `hooks/loop-codex-stop-hook.sh:1523` through `hooks/loop-codex-stop-hook.sh:1526`. The file itself has no placeholders and no dynamic variables. Output is literal Markdown appended to `NEXT_PROMPT_FILE` when `POST_ALIGNMENT` is non-empty, via `hooks/loop-codex-stop-hook.sh:1530` through `hooks/loop-codex-stop-hook.sh:1533`. State transition occurs just before the append: the stop hook increments `current_round` in `state.md` using a temp file and `sed` at `hooks/loop-codex-stop-hook.sh:1505` through `hooks/loop-codex-stop-hook.sh:1508`, then creates `round-${NEXT_ROUND}-prompt.md` at `hooks/loop-codex-stop-hook.sh:1511`.

- gates_or_invariants: The append gate is `FULL_ALIGNMENT_CHECK == "true"` at `hooks/loop-codex-stop-hook.sh:1529`; regular review rounds do not receive this fragment. `FULL_ALIGNMENT_CHECK` is computed from `CURRENT_ROUND % FULL_REVIEW_ROUND == FULL_REVIEW_ROUND - 1` at `hooks/loop-codex-stop-hook.sh:780` through `hooks/loop-codex-stop-hook.sh:782`. `FULL_REVIEW_ROUND` must be a positive integer of at least 2, otherwise it defaults to 5 at `hooks/loop-codex-stop-hook.sh:775` through `hooks/loop-codex-stop-hook.sh:779`. A stronger upstream gate can terminate the loop before this file is appended: if Codex writes `STOP` as the last line, the hook exits through the circuit-breaker path at `hooks/loop-codex-stop-hook.sh:1471` through `hooks/loop-codex-stop-hook.sh:1498`. If Codex writes completion instead, the loop enters completion/finalization behavior rather than creating another implementation prompt, so this fragment is only relevant to full-alignment rounds that found continued work.

- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh`, which sources shared helpers from `hooks/lib/loop-common.sh:47` through `hooks/lib/loop-common.sh:48`; `loop-common` sources the template loader and initializes `TEMPLATE_DIR` at `hooks/lib/loop-common.sh:139` through `hooks/lib/loop-common.sh:144`. The file is loaded with `load_template "$TEMPLATE_DIR" "claude/post-alignment-action-items.md"` at `hooks/loop-codex-stop-hook.sh:1530`. `load_template` resolves the path as `$template_dir/$template_name`, reads it with `cat` when present, and otherwise returns empty content with a warning, as implemented in `hooks/lib/template-loader.sh:36` through `hooks/lib/template-loader.sh:47`. The sibling prompt around it is `prompt-template/claude/next-round-prompt.md`, which inserts the original plan, Codex review result, and goal tracker reference before this post-alignment block; see `prompt-template/claude/next-round-prompt.md:1` through `prompt-template/claude/next-round-prompt.md:31`. The footer and goal-tracker update request are appended after this block at `hooks/loop-codex-stop-hook.sh:1536` through `hooks/loop-codex-stop-hook.sh:1556`.

- edge_cases_or_failure_modes: If this template is missing or empty, the hook silently skips the post-alignment action block because stderr is redirected and the append is guarded by `[[ -n "$POST_ALIGNMENT" ]]` at `hooks/loop-codex-stop-hook.sh:1530` through `hooks/loop-codex-stop-hook.sh:1533`; there is no fallback for this particular fragment. Because the template has no `{{...}}` placeholders and is loaded with `load_template`, not `load_and_render_safe`, no variable rendering occurs and no substitution failure is possible inside this file. If `FULL_REVIEW_ROUND` is corrupted below 2 or non-numeric, the hook defaults to 5, which can shift when this block appears; that behavior is explicit at `hooks/loop-codex-stop-hook.sh:776` through `hooks/loop-codex-stop-hook.sh:778`. If a full alignment review reports `STOP`, the circuit-breaker path prevents next prompt generation, so this file is intentionally bypassed. If the review found issues but failed to include concrete forgotten/unmet/deferred categories, this block still appends generic category reminders, but it cannot itself reconstruct missing review evidence.

- validation_or_tests: No direct unit test targets this exact template file in the inspected references. Related robustness coverage exists for the `full_review_round` state field, including parsing a configured value, defaulting missing values to 5, and accepting minimum value 2 in `tests/robustness/test-state-file-robustness.sh:399` through `tests/robustness/test-state-file-robustness.sh:473`. Template directory validation checks that `block`, `codex`, and `claude` subdirectories exist at `hooks/lib/template-loader.sh:205` through `hooks/lib/template-loader.sh:222`, but it does not validate that this specific optional post-alignment template exists. I validated this item by direct file inspection and focused reference search; no repository files were modified.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 item evidence section present; matches assigned_item_count`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`