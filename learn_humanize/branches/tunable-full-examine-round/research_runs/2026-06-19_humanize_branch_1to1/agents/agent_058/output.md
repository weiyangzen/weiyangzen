# agent_058 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-058 `file` `tests/test-monitor-runtime.sh`
- cursor: `[_]`
- core_role: Runtime executable specification for RLCR monitor shutdown and terminal cleanup behavior. The file verifies the monitor-facing safety contract around deleted `.humanize/rlcr` state, signal cleanup, trap installation, terminal scroll-region restoration, and idempotent cleanup. It is included in the aggregate suite by `tests/run-all-tests.sh:32-51`, so it participates in the repository’s normal validation path.
- algorithmic_behavior: The script is a Bash test harness with `set -euo pipefail` at `tests/test-monitor-runtime.sh:12`, local pass/fail counters at `tests/test-monitor-runtime.sh:22-34`, and a temp workspace rooted at `/tmp/test-monitor-runtime-$$` at `tests/test-monitor-runtime.sh:45-47`. It creates small executable subtests using heredocs, runs them, inspects their output with `grep`, increments counters, and exits `0` only when `TESTS_FAILED == 0` at `tests/test-monitor-runtime.sh:502-508`; otherwise it exits `1` at `tests/test-monitor-runtime.sh:509-512`. The tested behavior maps to `scripts/humanize.sh` monitor logic: `_restore_terminal()` resets the terminal scroll region with `printf "\033[r"` at `scripts/humanize.sh:522-528`; `_cleanup()` guards repeated execution with `cleanup_done`, flips `monitor_running=false`, clears INT/TERM traps, kills any tracked tail process, calls `_restore_terminal`, and prints a stop message at `scripts/humanize.sh:537-560`; `_graceful_stop()` calls `_cleanup` and prints a reason-specific user message at `scripts/humanize.sh:565-575`; the RLCR monitor loop checks `.humanize/rlcr` before and during polling/tailing and returns cleanly through `_graceful_stop` when deleted at `scripts/humanize.sh:621-625`, `scripts/humanize.sh:658-666`, and `scripts/humanize.sh:751-758`.
- inputs_outputs_state: Inputs are the repository root resolved from `SCRIPT_DIR`, the sourced implementation file `scripts/humanize.sh`, temporary `.humanize/rlcr/.../state.md` test data created at `tests/test-monitor-runtime.sh:61-62`, shell availability, and optional `zsh` availability at `tests/test-monitor-runtime.sh:432-490`. Outputs are colored `PASS`/`FAIL` lines, a summary count, and process exit status. Internal state is limited to `TESTS_PASSED`, `TESTS_FAILED`, temp scripts under `$TEST_BASE`, and subtest-local variables such as `cleanup_done`, `monitor_running`, `tail_pid`, `restore_called`, and `cleanup_triggered`. The parent harness cleans `$TEST_BASE` on EXIT via `trap cleanup EXIT` at `tests/test-monitor-runtime.sh:49-53`.
- gates_or_invariants: The main invariant is that monitor cleanup is single-shot: the subtest at `tests/test-monitor-runtime.sh:143-177` calls `_graceful_stop` and `_cleanup` repeatedly and expects `FINAL_COUNT: 1`. Graceful stop must include `_cleanup`, terminal restoration, a `Monitoring stopped:` message, and the deletion reason at `tests/test-monitor-runtime.sh:111-134`. Deleted `.humanize/rlcr` must transition from continued monitoring to graceful stop at `tests/test-monitor-runtime.sh:186-251`. Terminal restoration must be defined and must reset the scroll region at `tests/test-monitor-runtime.sh:263-311`. `_graceful_stop` must call `_cleanup` per the R1.2 compliance check at `tests/test-monitor-runtime.sh:314-324`. Bash INT/TERM handling must invoke cleanup through `trap '_cleanup' INT TERM` at `tests/test-monitor-runtime.sh:333-381` and source verification at `tests/test-monitor-runtime.sh:390-395`. Zsh trap functions `TRAPINT()` and `TRAPTERM()` must exist at `tests/test-monitor-runtime.sh:397-409`, and cleanup must reset traps at `tests/test-monitor-runtime.sh:418-423`.
- dependencies_and_callers: Direct dependency is `scripts/humanize.sh`, sourced by generated test scripts at `tests/test-monitor-runtime.sh:69-70` and `tests/test-monitor-runtime.sh:267-268`, and inspected by `grep` at `tests/test-monitor-runtime.sh:274-285`, `tests/test-monitor-runtime.sh:307`, `tests/test-monitor-runtime.sh:320`, `tests/test-monitor-runtime.sh:391`, `tests/test-monitor-runtime.sh:398`, `tests/test-monitor-runtime.sh:405`, and `tests/test-monitor-runtime.sh:419`. The implementation dependency is specifically the `_humanize_monitor_codex` function starting at `scripts/humanize.sh:219`, which owns `loop_dir=".humanize/rlcr"` at `scripts/humanize.sh:224`, the initial missing-directory error gate at `scripts/humanize.sh:230-235`, log/session discovery helpers at `scripts/humanize.sh:237-345`, cleanup/trap routines at `scripts/humanize.sh:522-587`, and the main deletion checks at `scripts/humanize.sh:621-758`. Caller is the aggregate test runner `tests/run-all-tests.sh`, which lists `test-monitor-runtime.sh` in `TEST_SUITES` at `tests/run-all-tests.sh:32-51`.
- edge_cases_or_failure_modes: Several checks are structural or simulated rather than full runtime coverage. Test 1 sources `scripts/humanize.sh` but then redefines stubbed `_restore_terminal`, `_cleanup`, and `_graceful_stop` in the generated script at `tests/test-monitor-runtime.sh:78-101`, so it validates the intended message sequence more than the actual implementation body. Test 3 similarly simulates only the directory-existence check pattern at `tests/test-monitor-runtime.sh:205-212`. The terminal restore test cannot exercise `tput` in a real interactive terminal and instead checks source text at `tests/test-monitor-runtime.sh:270-285`. There is a likely dead/ineffective pipeline at `tests/test-monitor-runtime.sh:282`: `grep -q '_restore_terminal' file | grep -q '_cleanup'` cannot pass meaningful content because `grep -q` suppresses output; the later stronger check at `tests/test-monitor-runtime.sh:307-311` covers the same invariant. The zsh runtime signal test is conditional and becomes a printed skip, not a failure, when `zsh` is unavailable at `tests/test-monitor-runtime.sh:432-490`. Because the harness uses `/tmp/test-monitor-runtime-$$`, abnormal termination before the EXIT trap could leave temp files outside the repo.
- validation_or_tests: The file itself is validation. It contains nine named checks: graceful-stop output, idempotent cleanup, loop-directory deletion detection, terminal restore source behavior, `_graceful_stop -> _cleanup`, Bash SIGINT cleanup, Bash/zsh signal-handler source setup, cleanup trap reset, and conditional zsh SIGINT behavior. I did not execute the script because this assignment requests research notes only and the test writes temporary scripts/data under `/tmp`; evidence here comes from direct source inspection with line references.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item evidence section, matching the single assigned file item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`