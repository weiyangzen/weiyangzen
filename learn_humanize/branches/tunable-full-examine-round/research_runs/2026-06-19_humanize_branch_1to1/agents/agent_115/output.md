# agent_115 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-115 `file` `prompt-template/codex/code-review-phase.md`
- cursor: `[_]`
- core_role: This is the audit/template contract for the RLCR Code Review Phase after implementation review has reached completion. It is rendered into `round-{{REVIEW_ROUND}}-review-prompt.md`, but it explicitly states that `codex review` does not accept prompt input and instead reviews the git diff directly (`prompt-template/codex/code-review-phase.md:3-4`). Its core role is therefore not to steer Codex with prompt text, but to document the review invocation and the transition contract: run code review, classify `[P0-9]` output, route either back to remediation or forward to Finalize Phase (`prompt-template/codex/code-review-phase.md:12-18`).
- algorithmic_behavior: The template declares a four-step phase algorithm: run `codex review --base {{BASE_BRANCH}}`, scan output for severity markers, return a fix prompt when issues exist, and transition to Finalize Phase when no issues exist (`prompt-template/codex/code-review-phase.md:14-17`). The actual caller is `run_codex_code_review()` in `hooks/loop-codex-stop-hook.sh`, which renders this template via `load_and_render_safe "$TEMPLATE_DIR" "codex/code-review-phase.md"` into the audit prompt file (`hooks/loop-codex-stop-hook.sh:930-955`). The real command then runs from `PROJECT_ROOT` as `codex review --base "$review_base"` with model/review-model config args, capturing combined stdout and stderr to a log (`hooks/loop-codex-stop-hook.sh:973-978`). The review handler treats command failure as blocking, then calls `detect_review_issues` to classify the combined log (`hooks/loop-codex-stop-hook.sh:999-1027`).
- inputs_outputs_state: Template inputs are `{{BASE_BRANCH}}`, `{{REVIEW_ROUND}}`, and `{{TIMESTAMP}}` (`prompt-template/codex/code-review-phase.md:6-10`). The hook also passes `BASE_COMMIT`, `REVIEW_BASE`, and `REVIEW_BASE_TYPE`, but this template does not currently consume those placeholders (`hooks/loop-codex-stop-hook.sh:948-954`). Runtime inputs include `BASE_COMMIT` or `BASE_BRANCH` as the selected review base, with `BASE_COMMIT` preferred when present (`hooks/loop-codex-stop-hook.sh:921-928`), plus `CODEX_MODEL`, `CODEX_EFFORT`, `CODEX_TIMEOUT`, `PROJECT_ROOT`, `LOOP_DIR`, and `CACHE_DIR` through the stop-hook environment. Declared generated files are the audit prompt, review result, command file, stdout capture, and stderr capture (`prompt-template/codex/code-review-phase.md:30-36`). Actual runtime outputs include `round-N-review-prompt.md`, `round-N-codex-review.cmd`, and a combined `round-N-codex-review.log` (`hooks/loop-codex-stop-hook.sh:930-968`). If issues are found, `detect_review_issues` writes extracted findings to `round-N-review-result.md` (`hooks/lib/loop-common.sh:374-387`). State transitions are: issue path updates `current_round` to the review round and builds the next remediation prompt (`hooks/loop-codex-stop-hook.sh:1116-1131`); clean path renames the state file to `finalize-state.md` through `enter_finalize_phase` (`hooks/loop-codex-stop-hook.sh:1024-1037`); Finalize Phase completion later renames finalize state to `complete-state.md` without another Codex review (`hooks/loop-codex-stop-hook.sh:737-747`).
- gates_or_invariants: The review phase requires the `codex` CLI; absence blocks the loop before review execution (`hooks/loop-codex-stop-hook.sh:855-875`). Review execution failure is a blocking error and must not skip to Finalize Phase (`hooks/loop-codex-stop-hook.sh:1005-1009`). The issue gate is not a broad substring scan: `detect_review_issues` scans the full combined log, but only treats `[P0-9]` markers as issues when they appear in the first 10 characters of a line (`hooks/lib/loop-common.sh:332-348`, `hooks/lib/loop-common.sh:364-372`). Missing or empty review logs are hard errors returning code 2, causing review failure handling rather than finalization (`hooks/lib/loop-common.sh:354-358`, `hooks/loop-codex-stop-hook.sh:1017-1019`). The template loader uses single-pass `{{VAR}}` substitution and leaves missing variables unchanged, limiting placeholder injection and making omitted variables visible rather than recursively expanded (`hooks/lib/template-loader.sh:7-14`, `hooks/lib/template-loader.sh:50-57`).
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh` inside `run_codex_code_review()` (`hooks/loop-codex-stop-hook.sh:916-955`). Template loading depends on `hooks/lib/template-loader.sh`, where `get_template_dir` resolves the root `prompt-template` directory and `load_template` reads named template files (`hooks/lib/template-loader.sh:24-47`). The classification dependency is `detect_review_issues()` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:332-392`). Downstream remediation messaging is handled by `prompt-template/claude/review-phase-prompt.md`, which instructs Claude to address all `[P0-9]` findings, commit fixes, and write a summary (`prompt-template/claude/review-phase-prompt.md:1-28`). User-facing command docs describe this phase as `codex review --base <branch>` followed by fix-or-finalize routing (`commands/start-rlcr-loop.md:23-25`, `commands/start-rlcr-loop.md:57-67`; `README.md:65-72`).
- edge_cases_or_failure_modes: The largest audit gap is that the template only records `Base Branch`, while the hook may actually review against `BASE_COMMIT`; the fallback text includes base commit and selected review base, but the existing template suppresses that fallback and does not render those fields (`hooks/loop-codex-stop-hook.sh:921-947`, `hooks/loop-codex-stop-hook.sh:948-954`). This can make audit files less precise when a fixed commit is used to avoid branch self-comparison. The file list in the template also appears stale relative to implementation: it claims separate `round-N-codex-review.out` and `round-N-codex-review.log` captures, while the hook captures both stdout and stderr into one `.log` file (`prompt-template/codex/code-review-phase.md:34-36`, `hooks/loop-codex-stop-hook.sh:930-932`, `hooks/loop-codex-stop-hook.sh:973-978`). Because `codex review` ignores prompt input, changes to this template do not directly change review behavior; behavior changes must happen in the stop hook or classifier (`prompt-template/codex/code-review-phase.md:3-4`, `hooks/loop-codex-stop-hook.sh:990-995`). Severity markers after character 10 are intentionally ignored by the classifier, so an indented or prose-wrapped Codex issue marker could be treated as clean unless the output starts the marker near the line front (`hooks/lib/loop-common.sh:364-372`; `tests/test-codex-review-merge.sh:84-107`). Empty output and nonzero review exit both block finalization and preserve review state for retry, according to finalize-phase tests (`tests/test-finalize-phase.sh:566-619`, `tests/test-finalize-phase.sh:625-696`).
- validation_or_tests: `tests/test-codex-review-merge.sh` directly covers the log analysis algorithm: marker detection in the first 10 characters, full-file scanning, extraction from first issue line onward, no-issue return behavior, and missing/empty log hard errors (`tests/test-codex-review-merge.sh:5-15`, `tests/test-codex-review-merge.sh:57-80`, `tests/test-codex-review-merge.sh:84-107`, `tests/test-codex-review-merge.sh:134-168`, `tests/test-codex-review-merge.sh:171-220`). `tests/test-finalize-phase.sh` covers mocked `codex review` output paths and verifies that review command failure and empty review output block instead of creating `finalize-state.md` or `complete-state.md` (`tests/test-finalize-phase.sh:52-69`, `tests/test-finalize-phase.sh:566-619`, `tests/test-finalize-phase.sh:625-696`). I inspected these tests and implementation paths in read-only mode; I did not execute the test suite because this assignment requested research notes only and the branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1`; the single Item Evidence heading corresponds to the only assigned row
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`