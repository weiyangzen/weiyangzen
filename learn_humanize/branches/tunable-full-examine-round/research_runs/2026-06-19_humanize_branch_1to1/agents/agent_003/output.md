# agent_003 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-003 `directory` `commands`
- cursor: `[_]`
- core_role: The `commands` directory is the plugin's slash-command control surface. It contains five flat Markdown command manifests, with no nested directories: `commands/gen-plan.md`, `commands/start-rlcr-loop.md`, `commands/cancel-rlcr-loop.md`, `commands/start-pr-loop.md`, and `commands/cancel-pr-loop.md`. These files do not implement the heavy algorithmic logic directly; they define command metadata, allowed tools, user-facing workflows, argument shapes, and dispatch into the algorithmic scripts and hook state machines under `scripts/`, `hooks/`, `prompt-template/`, and `agents/`. The directory is therefore core as an entry-point layer: it routes user intent into plan generation, RLCR iterative review loops, PR bot review loops, and cancellation state transitions.
- algorithmic_behavior: `commands/gen-plan.md` defines a six-phase transformation from draft document to implementation plan: validate IO, check repository relevance, analyze draft quality, resolve issues with the user, generate a structured plan, then write and review the output. The command explicitly delegates IO validation and template seeding to `scripts/validate-gen-plan-io.sh` (`commands/gen-plan.md:30`-`47`), then calls a relevance checker agent via Task with expected `RELEVANT` or `NOT_RELEVANT` output (`commands/gen-plan.md:50`-`72`). Its generation algorithm requires AC-format acceptance criteria, positive and negative tests, path boundaries, feasibility hints, dependencies, and code-style constraints (`commands/gen-plan.md:142`-`247`). It also preserves all original draft content as non-replacement input and later asks about language unification if the final plan is multilingual (`commands/gen-plan.md:251`-`292`).
- algorithmic_behavior: `commands/start-rlcr-loop.md` dispatches to `scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:10`-`14`) and describes an iterative implementation loop: user works on a plan, writes a round summary, the Stop hook runs Codex review, issues feed back into the next round, `COMPLETE` enters code review phase, code review issues marked with priority markers feed back into review-phase fixes, and no issues lead to finalize phase (`commands/start-rlcr-loop.md:16`-`25`, `commands/start-rlcr-loop.md:60`-`67`). The command also exposes the Goal Tracker invariants: immutable ultimate goal and acceptance criteria, mutable active/completed/deferred tasks and plan evolution log, and periodic full alignment checks (`commands/start-rlcr-loop.md:27`-`45`). `--skip-impl` bypasses implementation tracking and starts in code-review mode (`commands/start-rlcr-loop.md:69`-`86`).
- algorithmic_behavior: `commands/cancel-rlcr-loop.md` dispatches to `scripts/cancel-rlcr-loop.sh` and interprets first-line status codes (`commands/cancel-rlcr-loop.md:11`-`22`). The command has a finalize-phase gate: if the script reports `FINALIZE_NEEDS_CONFIRM`, the agent must ask the user whether to force-cancel; only a positive answer runs `cancel-rlcr-loop.sh --force` (`commands/cancel-rlcr-loop.md:23`-`34`). Its invariant is that script logic owns cancellation, and an active RLCR loop is defined by `state.md` or `finalize-state.md` in the newest loop directory (`commands/cancel-rlcr-loop.md:36`-`38`).
- algorithmic_behavior: `commands/start-pr-loop.md` dispatches to `scripts/setup-pr-loop.sh` (`commands/start-pr-loop.md:10`-`14`) and defines a remote-bot monitoring loop. Its algorithm detects the PR for the current branch, fetches bot review comments, prioritizes human comments before bots, requires fixes and pushes, triggers re-review with the configured bot mentions, then relies on the Stop hook to poll every 30 seconds with a 15-minute timeout and to run local Codex validation (`commands/start-pr-loop.md:16`-`24`, `commands/start-pr-loop.md:31`-`45`). It requires at least one bot flag, `--claude` or `--codex` (`commands/start-pr-loop.md:25`-`30`).
- algorithmic_behavior: `commands/cancel-pr-loop.md` dispatches to `scripts/cancel-pr-loop.sh`, interprets `NO_LOOP`, `NO_ACTIVE_LOOP`, and `CANCELLED`, and scopes itself strictly to `.humanize/pr-loop/` (`commands/cancel-pr-loop.md:9`-`23`). It explicitly does not affect RLCR loops (`commands/cancel-pr-loop.md:25`).
- inputs_outputs_state: Inputs into `commands/gen-plan.md` are `--input <draft.md>` and `--output <plan.md>` (`commands/gen-plan.md:3`). Its validator parses those arguments, rejects missing values and unknown options (`scripts/validate-gen-plan-io.sh:29`-`67`), resolves input/output paths (`scripts/validate-gen-plan-io.sh:69`-`77`), rejects missing or empty input, missing output directory, existing output target, output target that is a directory, and unwritable output directory (`scripts/validate-gen-plan-io.sh:79`-`124`). On success, it copies `prompt-template/plan/gen-plan-template.md` to the output and appends the original draft under delimiters (`scripts/validate-gen-plan-io.sh:133`-`160`). Outputs are the initialized plan file plus the command's later edits filling plan sections.
- inputs_outputs_state: Inputs into `commands/start-rlcr-loop.md` include a plan file or `--plan-file`, iteration and Codex settings, push policy, base branch, full-review interval, tracking flag, and skip-implementation mode (`commands/start-rlcr-loop.md:3`). `scripts/setup-rlcr-loop.sh` parses these flags and enforces numeric values for iteration count, Codex timeout, and full-review interval (`scripts/setup-rlcr-loop.sh:120`-`224`). It writes `.humanize/rlcr/<timestamp>/state.md` with round, max iteration, Codex config, push policy, full-review interval, plan path/tracking, start branch, base branch, base commit, and `review_started` (`scripts/setup-rlcr-loop.sh:667`-`726`). It copies the plan into the loop directory or creates a skip-impl placeholder (`scripts/setup-rlcr-loop.sh:675`-`697`), creates `goal-tracker.md` (`scripts/setup-rlcr-loop.sh:733`-`856`), and emits `round-0-prompt.md` plus required summary path (`scripts/setup-rlcr-loop.sh:858`-`1067`).
- inputs_outputs_state: RLCR loop runtime state is advanced by `hooks/loop-codex-stop-hook.sh`, registered as a Stop hook in `hooks/hooks.json:52`-`64`. The hook reads active state from `.humanize/rlcr`, accepts either normal `state.md` or finalize `finalize-state.md` (`hooks/loop-codex-stop-hook.sh:59`-`82`), parses frontmatter fields (`hooks/loop-codex-stop-hook.sh:88`-`117`), and blocks or transitions based on git cleanliness, summary presence, goal tracker initialization, max iterations, Codex result markers, and code review results. Normal implementation review writes `round-N-review-prompt.md` and expects `round-N-review-result.md` (`hooks/loop-codex-stop-hook.sh:758`-`847`, `hooks/loop-codex-stop-hook.sh:1248`-`1393`). `COMPLETE` transitions into review phase by setting `review_started: true`, creating `.review-phase-started`, and running `codex review` (`hooks/loop-codex-stop-hook.sh:1394`-`1434`). Passing code review renames state into `finalize-state.md` and returns a finalize prompt; a later finalize summary lets the hook rename state to `complete-state.md` (`hooks/loop-codex-stop-hook.sh:736`-`748`, `hooks/loop-codex-stop-hook.sh:1024`-`1114`).
- inputs_outputs_state: Inputs into `commands/start-pr-loop.md` are bot flags plus max/Codex options (`commands/start-pr-loop.md:3`, `commands/start-pr-loop.md:25`-`30`). `scripts/setup-pr-loop.sh` validates at least one bot flag (`scripts/setup-pr-loop.sh:175`-`190`), builds active bot arrays (`scripts/setup-pr-loop.sh:192`-`203`), resolves GitHub PR context including fork/base repo handling (`scripts/setup-pr-loop.sh:283`-`360`), fetches comments (`scripts/setup-pr-loop.sh:402`-`413`), determines startup case from reviewer status (`scripts/setup-pr-loop.sh:415`-`437`), may post a trigger comment for cases with new commits after reviews (`scripts/setup-pr-loop.sh:438`-`522`), then writes `.humanize/pr-loop/<timestamp>/state.md` with PR number, branch, configured/active bots, Codex config, polling config, startup case, latest commit metadata, and trigger metadata (`scripts/setup-pr-loop.sh:524`-`555`). It also creates a PR goal tracker and initial prompt/resolution path (`scripts/setup-pr-loop.sh:557`-`772`).
- inputs_outputs_state: PR loop runtime state is advanced by `hooks/pr-loop-stop-hook.sh`, also registered as a Stop hook (`hooks/hooks.json:52`-`64`). The hook parses YAML-like PR state including configured and active bot lists (`hooks/pr-loop-stop-hook.sh:78`-`176`), blocks missing resolution summaries (`hooks/pr-loop-stop-hook.sh:258`-`277`), requires committed and pushed work (`hooks/pr-loop-stop-hook.sh:279`-`362`), detects force pushes and clears stale trigger state (`hooks/pr-loop-stop-hook.sh:364`-`424`), enforces max iterations (`hooks/pr-loop-stop-hook.sh:426`-`436`), detects trigger comments from the current user (`hooks/pr-loop-stop-hook.sh:515`-`590`), verifies Claude eyes reaction when required (`hooks/pr-loop-stop-hook.sh:738`-`789`), polls bot reviews per configured bot with per-bot timeout and deduplication (`hooks/pr-loop-stop-hook.sh:791`-`1049`), saves grouped bot comments (`hooks/pr-loop-stop-hook.sh:1168`-`1217`), asks local Codex to classify each bot as approve/issues/no-response/usage-limit (`hooks/pr-loop-stop-hook.sh:1219`-`1353`), then either moves to `approve-state.md`, blocks waiting, terminates on usage limits, or updates `active_bots` and creates feedback for the next round (`hooks/pr-loop-stop-hook.sh:1354`-`1623`).
- gates_or_invariants: The top-level command manifests restrict allowed tools to the relevant scripts and tools. `start-rlcr-loop` and `start-pr-loop` each call only their setup script via Bash (`commands/start-rlcr-loop.md:4`, `commands/start-pr-loop.md:4`); cancellations call only their cancel scripts, with RLCR cancellation also allowing AskUserQuestion for finalize confirmation (`commands/cancel-rlcr-loop.md:3`, `commands/cancel-pr-loop.md:3`); `gen-plan` allows the validator, read/search/task/write/user-question tools (`commands/gen-plan.md:4`-`12`).
- gates_or_invariants: RLCR setup enforces mutual exclusion between RLCR and PR loops (`scripts/setup-rlcr-loop.sh:236`-`261`), git repository and commit presence (`scripts/setup-rlcr-loop.sh:293`-`307`), relative non-space non-metacharacter plan paths (`scripts/setup-rlcr-loop.sh:320`-`341`), no symlink plan file or symlink parent path (`scripts/setup-rlcr-loop.sh:346`-`380`), no submodule plan path (`scripts/setup-rlcr-loop.sh:408`-`421`), plan tracking consistency with `--track-plan-file` (`scripts/setup-rlcr-loop.sh:423`-`472`), minimum plan content (`scripts/setup-rlcr-loop.sh:476`-`541`), Codex CLI availability (`scripts/setup-rlcr-loop.sh:543`-`549`), YAML-safe branch/model/effort values (`scripts/setup-rlcr-loop.sh:561`-`587`), and local base branch availability with captured base commit (`scripts/setup-rlcr-loop.sh:589`-`663`).
- gates_or_invariants: RLCR Stop hook blocks stale or corrupt state, missing required fields, invalid model/effort, branch switching, plan file drift, incomplete todos, large changed files over 2000 lines, uncommitted changes, required unpushed commits when push-every-round is enabled, missing summaries, uninitialized round-zero goal tracker, and review-phase marker spoofing (`hooks/loop-codex-stop-hook.sh:118`-`337`, `hooks/loop-codex-stop-hook.sh:340`-`425`, `hooks/loop-codex-stop-hook.sh:427`-`719`, `hooks/loop-codex-stop-hook.sh:1446`-`1469`). Its review gate treats `codex review` failure or missing output as blocking, detects priority issue markers only when they appear in the first 10 characters of a line, and continues until no issue markers are found (`hooks/lib/loop-common.sh:332`-`392`, `hooks/loop-codex-stop-hook.sh:988`-`1028`).
- gates_or_invariants: PR setup enforces at least one bot, mutual exclusion with RLCR, git repository and commit presence, `gh` authentication, Codex availability, open PR state, numeric PR number, base repository resolution for fork PRs, and YAML-safe branch/model/effort values (`scripts/setup-pr-loop.sh:175`-`190`, `scripts/setup-pr-loop.sh:216`-`278`, `scripts/setup-pr-loop.sh:280`-`389`). For Claude-triggered re-review, setup requires retrieval of the trigger comment ID and confirmation of an eyes reaction, otherwise it removes the loop directory and fails (`scripts/setup-pr-loop.sh:443`-`522`).
- gates_or_invariants: PR Stop hook requires the PR to remain open or it archives state as merged/closed, requires a round resolution file, requires no non-`.humanize` uncommitted changes, requires pushed commits, detects force pushes as needing fresh bot mentions, validates trigger comments after the latest commit, verifies Claude eyes reaction after confirmed trigger when required, treats Codex validation failure/empty output as blocking, and updates active bots only through parsed Codex per-bot status (`hooks/pr-loop-stop-hook.sh:239`-`362`, `hooks/pr-loop-stop-hook.sh:364`-`789`, `hooks/pr-loop-stop-hook.sh:1291`-`1483`).
- gates_or_invariants: PreToolUse and UserPromptSubmit hooks protect the state directories created by these commands. `hooks/loop-write-validator.sh` blocks prompt/todo/state writes, PR loop read-only files, wrong round summaries, plan backup writes, and goal tracker writes after round zero (`hooks/loop-write-validator.sh:51`-`265`). `hooks/loop-edit-validator.sh` applies the corresponding edit protection (`hooks/loop-edit-validator.sh:34`-`183`). `hooks/loop-bash-validator.sh` blocks shell bypasses for state files, plan backups, goal tracker, prompts, summaries, todos, `.humanize` git-add patterns, and PR loop read-only files (`hooks/loop-bash-validator.sh:50`-`445`). `hooks/loop-read-validator.sh` prevents reading stale or wrong-location RLCR prompt/summary/todo files (`hooks/loop-read-validator.sh:51`-`170`). `hooks/loop-plan-file-validator.sh` rechecks schema, branch consistency, and plan tracking on user prompts (`hooks/loop-plan-file-validator.sh:29`-`231`).
- dependencies_and_callers: The command files depend directly on `scripts/validate-gen-plan-io.sh`, `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, and `scripts/cancel-pr-loop.sh` through `CLAUDE_PLUGIN_ROOT` (`commands/gen-plan.md:5`, `commands/start-rlcr-loop.md:4`, `commands/cancel-rlcr-loop.md:3`, `commands/start-pr-loop.md:4`, `commands/cancel-pr-loop.md:3`). Runtime coordination depends on `hooks/hooks.json`, which wires plan validation on `UserPromptSubmit`, Write/Edit/Read/Bash validators on `PreToolUse`, and both RLCR and PR Stop hooks on `Stop` (`hooks/hooks.json:4`-`68`).
- dependencies_and_callers: Shared state and utility functions live in `hooks/lib/loop-common.sh`, including active loop detection, state parsing, review issue detection, path type checks, PR bot mapping, `.humanize` git-add detection, protected-file modification detection, state termination, and PR goal-tracker updates. The active-loop helpers intentionally inspect only the newest timestamped directory and require `state.md` or `finalize-state.md` for RLCR (`hooks/lib/loop-common.sh:151`-`174`), which matches the cancel command’s model of active loops.
- dependencies_and_callers: PR command behavior depends on GitHub helper scripts: `scripts/fetch-pr-comments.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, and `scripts/poll-pr-reviews.sh`, plus `jq`, `gh`, `git`, and `codex`. RLCR behavior depends on `codex exec`, `codex review`, `git`, `jq`, `prompt-template/claude/*`, `prompt-template/codex/*`, `prompt-template/block/*`, and `hooks/check-todos-from-transcript.py`. Plan generation depends on `agents/draft-relevance-checker.md` and `prompt-template/plan/gen-plan-template.md`.
- dependencies_and_callers: Tests cover both command structure and script behavior. `tests/test-gen-plan.sh` validates the gen-plan manifest and exercises `validate-gen-plan-io.sh` exit codes for invalid arguments, missing/empty input, missing directory, existing output, directory output, valid paths, and help (`tests/test-gen-plan.sh:426`-`545`). `tests/test-plan-file-validation.sh` exercises RLCR plan path validation, tracking consistency, content sufficiency, `--plan-file` parsing, and Codex parameter validation (`tests/test-plan-file-validation.sh:93`-`180`, `tests/test-plan-file-validation.sh:340`-`435`, `tests/test-plan-file-validation.sh:627`-`740`). `tests/test-pr-loop-scripts.sh` exercises PR setup/cancel flags and cancel state renaming (`tests/test-pr-loop-scripts.sh:15`-`120`, `tests/test-pr-loop-scripts.sh:169`-`255`). `tests/test-pr-loop-system.sh` covers mutual exclusion and PR goal-tracker creation (`tests/test-pr-loop-system.sh:119`-`146`, `tests/test-pr-loop-system.sh:1454`-`1490`). `tests/test-cancel-signal-file.sh` covers the RLCR cancel signal authorization path and negative bypass cases (`tests/test-cancel-signal-file.sh:1`-`220`).
- edge_cases_or_failure_modes: `commands` can be misread as algorithm implementation because the files are Markdown. The real computation is delegated; however, the command specs are not trivial documentation because they impose tool allowlists, user confirmation gates, exact result-code interpretation, and workflow invariants that downstream scripts assume.
- edge_cases_or_failure_modes: RLCR plan input has many fail-closed conditions: no plan unless `--skip-impl`, absolute path, spaces, shell metacharacters, symlink, missing parent directory, out-of-project path, submodule path, unreadable file, tracked file without tracking mode, untracked file with tracking mode, modified tracked plan, too few lines, too little meaningful content, invalid branch/model/effort YAML characters, no local base branch, no git repo, no Codex CLI, or another active loop (`scripts/setup-rlcr-loop.sh:263`-`663`).
- edge_cases_or_failure_modes: RLCR runtime failure modes include truncated state frontmatter, old schema, invalid numeric round fields, branch changes, plan deletion/modification, incomplete todos, large file changes, dirty git state, missing summaries, uninitialized goal tracker placeholders, Codex nonzero exit, missing or empty Codex output, manual review-phase toggle without `.review-phase-started`, and `STOP` circuit breaker output (`hooks/loop-codex-stop-hook.sh:88`-`221`, `hooks/loop-codex-stop-hook.sh:223`-`337`, `hooks/loop-codex-stop-hook.sh:340`-`748`, `hooks/loop-codex-stop-hook.sh:1271`-`1499`).
- edge_cases_or_failure_modes: PR setup/runtime failure modes include missing bot flags, no `gh` or unauthenticated `gh`, no Codex, no PR for branch, merged/closed PR, invalid PR number, fork PR context ambiguity, trigger-comment post failure, missing trigger ID, missing Claude eyes reaction, force push requiring a fresh trigger, no resolution summary, uncommitted/unpushed changes, PR closed/merged mid-loop, usage/rate-limit marker, no bot responses, per-bot timeouts, and bots previously approved posting new issues and being re-added (`scripts/setup-pr-loop.sh:175`-`389`, `scripts/setup-pr-loop.sh:443`-`522`, `hooks/pr-loop-stop-hook.sh:239`-`424`, `hooks/pr-loop-stop-hook.sh:717`-`789`, `hooks/pr-loop-stop-hook.sh:900`-`1166`, `hooks/pr-loop-stop-hook.sh:1391`-`1483`).
- edge_cases_or_failure_modes: Cancellation preserves loop directories rather than deleting them. PR cancel only recognizes `state.md` in newest `.humanize/pr-loop/<timestamp>/` and moves it to `cancel-state.md` (`scripts/cancel-pr-loop.sh:72`-`132`). RLCR cancel recognizes either normal `state.md` or `finalize-state.md`, blocks unforced finalize cancellation with exit code 2, creates `.cancel-requested`, then moves active state to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:69`-`152`).
- validation_or_tests: I inspected the assigned directory recursively with `rg --files commands` and `find commands -type d -print`; it is flat and contains exactly the five command files listed above. I inspected every command file directly with line numbers, then followed their referenced scripts, hooks, templates/tests where needed. The workspace export is read-only and did not expose a usable `.git` directory for status validation, so no repository mutation or git-based validation was performed.
- validation_or_tests: Existing tests provide coverage for command structure and delegated algorithms: gen-plan validator exit codes (`tests/test-gen-plan.sh:426`-`545`), RLCR setup path/tracking/content/Codex option validation (`tests/test-plan-file-validation.sh:93`-`180`, `tests/test-plan-file-validation.sh:340`-`435`, `tests/test-plan-file-validation.sh:627`-`740`), PR setup/cancel flags and cancel behavior (`tests/test-pr-loop-scripts.sh:15`-`120`, `tests/test-pr-loop-scripts.sh:169`-`255`), PR/RLCR mutual exclusion and PR goal tracker creation (`tests/test-pr-loop-system.sh:119`-`146`, `tests/test-pr-loop-system.sh:1454`-`1490`), and cancel-signal authorization for RLCR state moves (`tests/test-cancel-signal-file.sh:1`-`220`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`