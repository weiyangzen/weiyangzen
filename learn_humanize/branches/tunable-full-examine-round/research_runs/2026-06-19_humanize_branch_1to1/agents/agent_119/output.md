# agent_119 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-119 `file` `prompt-template/plan/gen-plan-template.md`
- cursor: `[_]`
- core_role: Template contract for the `/humanize:gen-plan` plan-generation path. It defines the canonical shape of a generated implementation plan before the model fills it in: title, goal, TDD-style acceptance criteria, path boundaries, feasibility hints, dependency sequencing, and implementation notes. It is a core prompt/template artifact because it constrains the transition from an informal draft into a structured plan that later drives implementation and review loops.
- algorithmic_behavior: The file is a Markdown scaffold, not executable code, but it participates directly in command execution. `scripts/validate-gen-plan-io.sh` locates this exact file through `CLAUDE_PLUGIN_ROOT/prompt-template/plan/gen-plan-template.md` or a script-relative fallback, then copies it to the requested output path before appending the original draft between explicit start/end markers (`scripts/validate-gen-plan-io.sh:133`, `scripts/validate-gen-plan-io.sh:148`, `scripts/validate-gen-plan-io.sh:153`). After that, `commands/gen-plan.md` instructs the agent to replace placeholders while preserving the original draft at the bottom (`commands/gen-plan.md:251`). The template’s own sections encode the generation algorithm: define a goal (`prompt-template/plan/gen-plan-template.md:3`), express ACs with positive and negative tests (`prompt-template/plan/gen-plan-template.md:6`), bound acceptable scope (`prompt-template/plan/gen-plan-template.md:25`), separate suggestions from requirements (`prompt-template/plan/gen-plan-template.md:46`), order dependencies without time estimates (`prompt-template/plan/gen-plan-template.md:57`), and prevent plan vocabulary from leaking into implementation code (`prompt-template/plan/gen-plan-template.md:69`).
- inputs_outputs_state: Input is an existing, non-empty user draft passed to `validate-gen-plan-io.sh` as `--input`; output is a new plan file passed as `--output` (`scripts/validate-gen-plan-io.sh:16`). The intermediate state transition is: validate paths and permissions, copy `gen-plan-template.md`, append draft, then the slash-command agent edits placeholders into a complete plan. The resulting plan is expected to contain both the filled structured plan and the original draft for traceability (`commands/gen-plan.md:253`, `commands/gen-plan.md:259`). The template itself has no persistent state, but it seeds the output document’s initial state and carries placeholder fields that must be resolved by the later generation phase.
- gates_or_invariants: The command-level gates are IO validation and relevance/analysis before final plan generation. The validator rejects missing input, empty input, missing output directory, pre-existing output path, output directory-as-file, unwritable output directory, invalid arguments, and missing template file with explicit exit codes (`scripts/validate-gen-plan-io.sh:4`). The generation contract requires all ACs to use `AC-X` or `AC-X.Y`, each AC to include positive and negative tests, no time estimates, dependency descriptions instead of schedule estimates, affirmative upper/lower path bounds, and preservation of all original draft information (`commands/gen-plan.md:225`). The template-specific invariant at lines 71-74 says implementation code and comments must not contain plan-specific markers such as `AC-`, `Milestone`, `Step`, or `Phase`; those labels are documentation-only.
- dependencies_and_callers: Direct caller is `scripts/validate-gen-plan-io.sh`, which copies the template and appends the draft. The slash command `commands/gen-plan.md` is the higher-level orchestrator and authorizes the validator script in its frontmatter (`commands/gen-plan.md:4`). `commands/gen-plan.md` mirrors the template structure in its Phase 5 plan structure block (`commands/gen-plan.md:142`) and then tells the agent to edit the generated file rather than overwrite it (`commands/gen-plan.md:255`). `agents/draft-relevance-checker.md` participates earlier in the workflow by returning `RELEVANT:` or `NOT_RELEVANT:` before plan generation proceeds (`agents/draft-relevance-checker.md:27`). README documentation exposes the command as a user-facing way to turn a draft into a structured implementation plan (`README.md:122`).
- edge_cases_or_failure_modes: If the template file is missing from either `CLAUDE_PLUGIN_ROOT` or the script-relative fallback, the validator exits with plugin configuration error code 7 (`scripts/validate-gen-plan-io.sh:142`). If the output already exists, the command refuses to overwrite it, so stale generated plans must be removed or a different path chosen (`scripts/validate-gen-plan-io.sh:111`). If the draft is unrelated to the repository, the slash command stops after relevance checking (`commands/gen-plan.md:67`). If the model fills the template but omits draft content, contradicts the draft, leaves placeholders, introduces time estimates, or leaks plan markers into implementation code names/comments, it violates the command/template contract even though the Markdown template itself cannot enforce that mechanically. Deterministic designs are a notable edge case: upper and lower path bounds may collapse to the same fixed choice, and the allowed choices section should state the choice is fixed (`prompt-template/plan/gen-plan-template.md:44`).
- validation_or_tests: `tests/test-gen-plan.sh` validates the command/validator surface, including gen-plan command structure, English-only command content, and validator behavior for argument and path failures. Focused validator tests cover missing argument values, unknown flags, missing input, empty input, missing output directory, existing output file, output path as directory, valid path success, and help behavior (`tests/test-gen-plan.sh:426`). The tests verify the script exits with expected codes but do not appear to assert the full contents of `prompt-template/plan/gen-plan-template.md` or that generated plans satisfy every semantic rule in the template. I inspected tests only; I did not run them because this task is read-only research.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-119`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`