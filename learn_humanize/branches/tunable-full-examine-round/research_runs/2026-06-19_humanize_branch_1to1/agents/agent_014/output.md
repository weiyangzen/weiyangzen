# agent_014 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-014 `directory` `prompt-template/pr-loop`
- cursor: `[_]`
- core_role: This directory is the PR-review-loop prompt/template surface. It does not execute code directly; it supplies the text fragments that initialize round 0, steer the worker’s behavior based on whether PR comments already exist, define completion gates, seed the PR goal tracker, and instruct Codex how to update that tracker during later review validation. The direct children are seven flat files, with no nested subdirectories found:
  - `prompt-template/pr-loop/goal-tracker-initial.md`: initializes `goal-tracker.md` with PR metadata, ultimate goal, issue summary table, total statistics, and round-0 issue log. Key placeholders are `{{PR_NUMBER}}`, `{{START_BRANCH}}`, `{{STARTED_AT}}`, `{{ACTIVE_BOTS_DISPLAY}}`, and `{{STARTUP_CASE}}`; see lines 1-33.
  - `prompt-template/pr-loop/round-0-header.md`: starts the generated round-0 prompt with “PR Review Loop (Round 0)”, PR number, branch, active bots, and a review-comments section; see lines 1-15.
  - `prompt-template/pr-loop/round-0-task-no-comments.md`: zero-comment startup path. It tells the agent not to trigger review manually, to write `@{{RESOLVE_PATH}}`, and to let the Stop Hook poll; see lines 4-14 and 18-30.
  - `prompt-template/pr-loop/round-0-task-has-comments.md`: existing-comment startup path. It orders comment analysis, issue fixing, commit/push, PR bot mention, and resolution summary writing; see lines 6-27 and 30-43.
  - `prompt-template/pr-loop/critical-requirements-no-comments.md`: console-critical reminder for the zero-comment path. It requires only the summary and explicitly forbids a PR trigger comment; see lines 6-20.
  - `prompt-template/pr-loop/critical-requirements-has-comments.md`: console-critical reminder for the existing-comment path. It requires commit, push, PR comment, and resolution summary; see lines 6-23.
  - `prompt-template/pr-loop/codex-goal-tracker-update.md`: injected into Codex’s later per-bot validation prompt. It instructs Codex to add one Issue Summary row per reviewer, update totals, and append an Issue Log entry while preserving header sections; see lines 1-20 and 57-64.
- algorithmic_behavior: `scripts/setup-pr-loop.sh` renders these templates with `load_and_render_safe`, then composes the initial PR-loop state artifacts. It creates `goal-tracker.md` from `pr-loop/goal-tracker-initial.md` at `scripts/setup-pr-loop.sh:561-633`. It detects zero comments only when `round-0-pr-comment.md` contains the exact sentinel `*No comments found.*`; otherwise it treats comments as present (`scripts/setup-pr-loop.sh:641-649`). It renders `round-0-header.md`, appends the fetched comments, then selects either `round-0-task-no-comments.md` or `round-0-task-has-comments.md` (`scripts/setup-pr-loop.sh:677-771`). It separately prints the matching critical-requirements template to the user-facing setup output (`scripts/setup-pr-loop.sh:810-862`). Later, `hooks/pr-loop-stop-hook.sh` loads `pr-loop/codex-goal-tracker-update.md` into the generated Codex validation prompt (`hooks/pr-loop-stop-hook.sh:1239-1289`), so Codex’s review classification and tracker-maintenance instructions travel together.
- inputs_outputs_state: Inputs are template variables: `PR_NUMBER`, `START_BRANCH`, `ACTIVE_BOTS_DISPLAY`, `STARTUP_CASE`, `STARTED_AT`, `RESOLVE_PATH`, `BOT_MENTION_STRING`, `GOAL_TRACKER_FILE`, and `NEXT_ROUND`. Runtime inputs include fetched PR comments, active/configured bot lists, current round, and Codex review output. Outputs are generated `.humanize/pr-loop/<timestamp>/goal-tracker.md`, `round-0-prompt.md`, console critical requirements, and later `round-N-codex-prompt.md`. State transitions encoded by the directory are: initialize tracker as round 0 with zero totals (`goal-tracker-initial.md:15-33`); if no comments, enter passive wait/poll mode without PR comment (`round-0-task-no-comments.md:6-14`, `critical-requirements-no-comments.md:15-20`); if comments exist, enter fix/commit/push/re-review mode (`round-0-task-has-comments.md:10-27`, `critical-requirements-has-comments.md:8-23`); on later Codex validation, update per-round issue totals and logs using the injected instructions (`codex-goal-tracker-update.md:7-20`).
- gates_or_invariants: Template rendering uses the shared loader’s `{{VARIABLE_NAME}}` syntax, single-pass replacement, and “missing variables stay as placeholders” behavior (`hooks/lib/template-loader.sh:7-14`, `hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-131`). `load_and_render_safe` falls back when a template is missing or renders empty (`hooks/lib/template-loader.sh:167-203`), and setup includes inline fallback copies for every PR-loop template call (`scripts/setup-pr-loop.sh:588-632`, `scripts/setup-pr-loop.sh:660-720`, `scripts/setup-pr-loop.sh:723-767`, `scripts/setup-pr-loop.sh:813-860`). Behavioral gates are expressed in prompt text: do not modify `.humanize/pr-loop/` state files (`round-0-task-has-comments.md:30-35`, `round-0-task-no-comments.md:18-23`), always push fixes before requesting re-review when comments exist, and never manually trigger the first review when no comments exist. Goal-tracker invariants require preserving structure, adding rather than replacing table rows, and one Issue Summary row per reviewer (`codex-goal-tracker-update.md:57-64`).
- dependencies_and_callers: Primary callers are `scripts/setup-pr-loop.sh` for round-0 prompt/tracker creation and `hooks/pr-loop-stop-hook.sh` for later Codex validation prompt construction. The stop hook also has a non-template fallback updater, `update_pr_goal_tracker`, which performs idempotency checks by round and reviewer (`hooks/lib/loop-common.sh:1168-1208`), updates totals only when adding a new summary row (`hooks/lib/loop-common.sh:1256-1265`), inserts a new table row (`hooks/lib/loop-common.sh:1267-1297`), and appends a round log entry (`hooks/lib/loop-common.sh:1299-1314`). Sibling/parent coordination comes through the broader `prompt-template` tree and validators: block templates prevent direct PR-loop state/prompt modification, while the PR-loop templates tell the agent which generated files are system-managed. Search found all seven direct references in `scripts/setup-pr-loop.sh` or `hooks/pr-loop-stop-hook.sh`; `tests/test-pr-loop-system.sh:642-655` checks the goal-tracker schema expected by this directory.
- edge_cases_or_failure_modes: The zero-comment branch depends on an exact sentinel line (`scripts/setup-pr-loop.sh:643-649`); altered fetch output could incorrectly route to the “has comments” path and tell the agent to commit/push/comment. Because missing variables remain literal placeholders, malformed variable setup can leak `{{...}}` tokens into prompts rather than failing fast (`hooks/lib/template-loader.sh:11-14`). Missing or empty templates degrade to fallbacks instead of hard failure, preserving loop operability but risking divergence between file templates and embedded fallback text (`hooks/lib/template-loader.sh:179-203`). The stop hook counts issues by list items under `### Issues Found` in Codex output (`hooks/pr-loop-stop-hook.sh:1497-1503`), so nonconforming Codex formatting can undercount or overcount. Tracker helper failures are ignored with `|| true` (`hooks/pr-loop-stop-hook.sh:1513-1519`), making tracker freshness best-effort after Codex output. Idempotency is based on exact round/reviewer patterns (`hooks/lib/loop-common.sh:1189-1203`), so unusual table formatting or reviewer casing can cause partial-update repair or duplicate-looking rows.
- validation_or_tests: Template loader tests cover fallback behavior for missing templates (`tests/test-template-loader.sh:197-222`) and placeholder-injection prevention for values containing `{{VAR}}` (`tests/test-template-loader.sh:489-520`). `tests/test-pr-loop-system.sh:642-655` validates that `goal-tracker-initial.md` contains `Issue Summary`, `Total Statistics`, `Issue Log`, and required total fields. `tests/test-template-references.sh:130-180` performs broader template-reference and `load_and_render_safe` usage checks. I did not run the test suite because this assignment requested read-only research notes only; inspection stayed within the read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-014`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`