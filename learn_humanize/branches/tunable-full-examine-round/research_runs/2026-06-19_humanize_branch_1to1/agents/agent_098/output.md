# agent_098 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-098 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: This is a reusable hook-block message template for the RLCR loop’s Bash validator. It tells an agent that summary files must not be modified through shell commands and must instead be written with the Write or Edit tool, using the rendered `{{CORRECT_PATH}}`; see `prompt-template/block/summary-bash-write.md:1` and `prompt-template/block/summary-bash-write.md:5`.
- algorithmic_behavior: The file itself is declarative Markdown, but it participates in an algorithmic gate. When a Bash tool command is detected as modifying `round-[0-9]+-summary.md`, `hooks/loop-bash-validator.sh` computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"` and emits this template via `summary_bash_blocked_message`; see `hooks/loop-bash-validator.sh:371` through `hooks/loop-bash-validator.sh:378`. The behavioral transition is: Bash summary write attempt -> pattern match by `command_modifies_file` -> render this block -> return hook block exit code `2` -> agent is redirected to the Write/Edit tool path where round/location validators can run.
- inputs_outputs_state: Input is the `CORRECT_PATH` template variable. It is supplied by `summary_bash_blocked_message "$CORRECT_PATH"` in `hooks/loop-bash-validator.sh:377`, passed as `CORRECT_PATH=$correct_path` to `load_and_render_safe` in `hooks/lib/loop-common.sh:483` through `hooks/lib/loop-common.sh:489`, and substituted by `render_template` using `{{VARIABLE_NAME}}` syntax in `hooks/lib/template-loader.sh:7` through `hooks/lib/template-loader.sh:13`. Output is a Markdown error message on stderr, including the correct summary path and the invariant that Bash commands bypass validation hooks. This template does not mutate state. The state it depends on is the active RLCR loop and `CURRENT_ROUND`, parsed before the gate from the active loop state file in `hooks/loop-bash-validator.sh:74` through `hooks/loop-bash-validator.sh:87`.
- gates_or_invariants: The protected invariant is that round summary writes must pass through Write/Edit hook validation rather than shell redirection or in-place shell editors. The template explicitly names the bypass risk at `prompt-template/block/summary-bash-write.md:7` and ties the restriction to correct round-number validation at `prompt-template/block/summary-bash-write.md:8`. The surrounding gate only applies when an active RLCR loop exists; if no active RLCR or PR loop is found, Bash commands are allowed by `hooks/loop-bash-validator.sh:64` through `hooks/loop-bash-validator.sh:67`. The summary-specific gate matches summary modification commands at `hooks/loop-bash-validator.sh:375`, emits this template at `hooks/loop-bash-validator.sh:377`, and exits blocked at `hooks/loop-bash-validator.sh:378`.
- dependencies_and_callers: Direct caller is `summary_bash_blocked_message` in `hooks/lib/loop-common.sh:481` through `hooks/lib/loop-common.sh:489`. That helper is called from `hooks/loop-bash-validator.sh:377`. Rendering depends on `load_and_render_safe`, which loads the template, falls back if missing or empty, and renders variables in `hooks/lib/template-loader.sh:170` through `hooks/lib/template-loader.sh:203`. The detector depends on `command_modifies_file`, whose recognized write forms include redirection, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`, `cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec fd>` in `hooks/lib/loop-common.sh:1071` through `hooks/lib/loop-common.sh:1096`. Template reference coverage includes `tests/test-template-references.sh:152` through `tests/test-template-references.sh:157`, which asserts `block/summary-bash-write.md` exists among common templates.
- edge_cases_or_failure_modes: If the template is missing or empty, `load_and_render_safe` returns the fallback message defined in `hooks/lib/loop-common.sh:485` through `hooks/lib/loop-common.sh:487`, so the block still functions but with shorter wording. Missing template variables are kept as placeholders by the renderer rather than failing, per `hooks/lib/template-loader.sh:11` through `hooks/lib/template-loader.sh:13`, so a caller bug could expose `{{CORRECT_PATH}}` literally. Detection is pattern-based, so it inherits known limitations from `command_modifies_file`; for example, multi-source `cp file1 file2 target` is documented as not detected in `tests/test-bash-validator-patterns.sh:165` through `tests/test-bash-validator-patterns.sh:168`. The summary pattern intentionally ignores read-only shell commands such as `cat round-5-summary.md`, covered by `tests/test-bash-validator-patterns.sh:192` through `tests/test-bash-validator-patterns.sh:194`. Because `COMMAND_LOWER` is used, path matching is case-insensitive for command text, but the message path is generated from active loop state rather than from the attempted path.
- validation_or_tests: I inspected, but did not run, tests because the task requested research notes only and the branch export is read-only. Relevant validation surfaces are `tests/test-template-references.sh:156`, which checks this template exists in the common template set, and `tests/test-bash-validator-patterns.sh:186` through `tests/test-bash-validator-patterns.sh:194`, which verifies summary write patterns are detected while plain reads are ignored. Template-loader fallback behavior is exercised elsewhere in the suite, including `tests/test-template-loader.sh:197` through `tests/test-template-loader.sh:221`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`