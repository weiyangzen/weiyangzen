# agent_127 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-127 `file` `scripts/lib/monitor-common.sh`
- cursor: `[_]`
- core_role: Shared monitor utility library for the `humanize` CLI monitor paths. It is sourced by `scripts/humanize.sh` at startup when present, rather than executed directly, and centralizes reusable terminal, session-discovery, state-file, PR-phase, and tracker-parsing behavior for RLCR and PR loop monitors (`scripts/humanize.sh:6-10`, `scripts/lib/monitor-common.sh:3-6`).
- algorithmic_behavior: The file is a set of shell functions with no top-level workflow loop. It provides color escape emitters (`scripts/lib/monitor-common.sh:14-23`), cross-platform file sizing with GNU/BSD `stat` fallback (`scripts/lib/monitor-common.sh:32-35`), latest timestamped session selection using lexicographic comparison over `YYYY-MM-DD_HH-MM-SS` directory names (`scripts/lib/monitor-common.sh:40-63`), terminal scroll-region setup/restore (`scripts/lib/monitor-common.sh:71-92`), state-file classification (`scripts/lib/monitor-common.sh:154-188`), YAML frontmatter value extraction (`scripts/lib/monitor-common.sh:197-209`), timestamp/string display formatting (`scripts/lib/monitor-common.sh:218-252`), PR loop phase inference (`scripts/lib/monitor-common.sh:267-342`), PR phase display text mapping (`scripts/lib/monitor-common.sh:346-381`), generic goal tracker summarization (`scripts/lib/monitor-common.sh:390-463`), and PR issue tracker summarization (`scripts/lib/monitor-common.sh:468-498`).
- inputs_outputs_state: Inputs are filesystem paths and small scalar display values: loop dirs, session dirs, markdown state/tracker files, timestamps, strings, and phase names. Outputs are shell-printed values, mostly pipe-delimited records such as `state_file|status`, `total_acs|completed_acs|active_tasks|completed_tasks|deferred_tasks|open_issues|goal_summary`, and `total_issues|resolved_issues|remaining_issues|last_reviewer` (`scripts/lib/monitor-common.sh:148-154`, `scripts/lib/monitor-common.sh:387-390`, `scripts/lib/monitor-common.sh:465-468`). Persistent state is minimal but real: `get_pr_loop_phase` writes `/tmp/humanize-phase-${session_name}-${highest_round}.size` to compare current and previous `round-*-pr-check.md` sizes, then uses file growth or mtime under 10 seconds to classify `codex_analyzing` (`scripts/lib/monitor-common.sh:297-317`). Terminal state is also affected by ANSI scroll-region calls in `monitor_setup_terminal` and `monitor_restore_terminal` (`scripts/lib/monitor-common.sh:71-92`).
- gates_or_invariants: Session discovery only accepts direct child directories whose basename exactly matches the timestamp regex, then treats lexicographically greatest timestamp as latest (`scripts/lib/monitor-common.sh:54-60`). State detection prioritizes live `state.md` over terminal `*-state.md` files (`scripts/lib/monitor-common.sh:162-181`). Missing/invalid session dirs return `|unknown`, missing tracker files return zeroed summaries, and missing timestamps return `N/A` (`scripts/lib/monitor-common.sh:157-160`, `scripts/lib/monitor-common.sh:221-224`, `scripts/lib/monitor-common.sh:392-395`, `scripts/lib/monitor-common.sh:470-472`). PR phase detection checks final states before active analysis, so `approve-state.md`, `cancel-state.md`, and `maxiter-state.md` terminate phase inference early (`scripts/lib/monitor-common.sh:272-276`). Active PR waiting state depends on `current_round == 0 && startup_case == 1`; otherwise an active `state.md` is treated as `waiting_reviewer` (`scripts/lib/monitor-common.sh:321-341`).
- dependencies_and_callers: The script depends on POSIX-ish shell utilities available in bash/zsh environments: `stat`, `find`, `basename`, `sed`, `grep`, `sort`, `wc`, `tr`, `head`, `cut`, `date`, `clear`, `printf`, and `tput`. `scripts/humanize.sh` sources it at lines 6-10. RLCR monitor wraps `monitor_find_latest_session`, `monitor_find_state_file`, and `monitor_get_file_size` through local helpers (`scripts/humanize.sh:237-240`, `scripts/humanize.sh:344-347`, `scripts/humanize.sh:612`). PR monitor uses `monitor_find_latest_session` and wraps `monitor_find_state_file` to map raw filename-derived stop reasons such as `approve` into UI names like `approved` (`scripts/humanize.sh:962-965`, `scripts/humanize.sh:1011-1028`). PR monitor status rendering calls `get_pr_loop_phase`, `get_pr_loop_phase_display`, and `humanize_parse_pr_goal_tracker` for live and `--once` output (`scripts/humanize.sh:1097-1147`, `scripts/humanize.sh:1235-1240`). The generic `parse_goal_tracker` in this file is tested directly, while the production RLCR monitor currently calls `humanize_parse_goal_tracker` from `scripts/humanize.sh`, a similarly shaped function with a different public name (`scripts/humanize.sh:32-80`, `scripts/humanize.sh:369-402`).
- edge_cases_or_failure_modes: `monitor_find_state_file` returns the first `*-state.md` from `find` without sorting, so multiple terminal state files in one session could be nondeterministic (`scripts/lib/monitor-common.sh:172-181`). `monitor_get_yaml_value` interpolates the key directly into grep/sed expressions, so regex metacharacters in keys would change matching semantics (`scripts/lib/monitor-common.sh:197-209`). `monitor_truncate_string` assumes `max_len >= 3`; smaller values make the suffix/prefix length negative or zero in shell substring arithmetic (`scripts/lib/monitor-common.sh:233-251`). PR phase size-cache keys use only the session basename and round, so two projects with the same session timestamp could collide in `/tmp` (`scripts/lib/monitor-common.sh:297-306`). The recent-mtime check uses `date +%s` minus file mtime; future mtimes produce negative ages and therefore `codex_analyzing` (`scripts/lib/monitor-common.sh:310-317`). Markdown parsing is regex-based and section-boundary dependent, so malformed tables, repeated headings, or pipe characters inside cells can skew counts (`scripts/lib/monitor-common.sh:397-463`, `scripts/lib/monitor-common.sh:475-497`).
- validation_or_tests: I ran `bash -n scripts/lib/monitor-common.sh`; it passed with no syntax errors. Existing tests source this file directly for PR phase detection, tracker parsing, latest session detection, state-file detection, file sizing, and codex-analyzing cache behavior (`tests/test-pr-loop-system.sh:256-309`, `tests/test-pr-loop-system.sh:315-371`, `tests/test-pr-loop-system.sh:377-415`, `tests/test-pr-loop-system.sh:770-843`, `tests/test-pr-loop-system.sh:849-899`). Monitor output tests exercise the caller path that uses phase/display helpers for approved, waiting, cancelled, and analyzing states (`tests/test-pr-loop-system.sh:947-1106`). Runtime/e2e tests cover the real monitor functions and cleanup behavior that source `scripts/humanize.sh`, which in turn sources this library (`tests/test-monitor-runtime.sh:64-71`, `tests/test-monitor-e2e-real.sh` references real `_humanize_monitor_codex` and `_humanize_monitor_pr` paths).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 item section present; the assigned item id appears only in that section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`