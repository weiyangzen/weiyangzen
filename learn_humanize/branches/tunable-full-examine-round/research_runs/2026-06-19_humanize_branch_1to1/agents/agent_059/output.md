# agent_059 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-059 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR plan-file immutability and hook fail-closed behavior. It validates the hook chain registered in `hooks/hooks.json`: `UserPromptSubmit` uses `hooks/loop-plan-file-validator.sh`, `PreToolUse` uses `loop-write-validator.sh`, `loop-edit-validator.sh`, and `loop-bash-validator.sh`, and `Stop` uses `loop-codex-stop-hook.sh` (`hooks/hooks.json:4`, `hooks/hooks.json:14`, `hooks/hooks.json:52`). It is part of the all-tests suite at `tests/run-all-tests.sh:32-42`.
- algorithmic_behavior: The test creates isolated temporary git repositories and RLCR loop directories, then feeds synthetic hook JSON to validators and asserts exit codes plus message fragments. `setup_test_loop` initializes git, captures default branch, writes a gitignored plan at `plans/test-plan.md`, copies it to `.humanize/rlcr/<timestamp>/plan.md`, and writes `state.md` with `current_round`, `max_iterations`, `plan_file`, `plan_tracked`, `start_branch`, `base_branch`, and `review_started` (`tests/test-plan-file-hooks.sh:37-88`). The first section proves `loop-plan-file-validator.sh` accepts valid state, strips quotes around YAML values, blocks missing v1.5.0 fields, and emits branch-change blocks (`tests/test-plan-file-hooks.sh:90-191`). Write/Edit/Bash sections prove direct writes or edits to the loop backup `plan.md` are blocked (`tests/test-plan-file-hooks.sh:193-245`). The command-injection section asserts bash blocking for direct `.humanize/rlcr/plan.md`, command substitution, glob, brace expansion, pipes through `tee`, and backtick substitution (`tests/test-plan-file-hooks.sh:260-341`). YAML quote tests cover quoted `start_branch`, quoted branch mismatch, quoted stop-hook parsing, and hyphenated `plan_file` paths (`tests/test-plan-file-hooks.sh:343-474`). Stop-hook integrity tests assert plan modification, deletion, missing backup, uncommitted tracked-plan mutation, outdated schema JSON blocks, and committed tracked-plan content drift (`tests/test-plan-file-hooks.sh:480-749`). Placeholder tests assert section-specific goal-tracker placeholder detection for Ultimate Goal, Acceptance Criteria, Active Tasks, and all three together (`tests/test-plan-file-hooks.sh:752-1036`). Negative legacy-path tests assert `.humanize-loop.local/.../plan.md` is allowed and no longer protected as an RLCR loop path (`tests/test-plan-file-hooks.sh:1038-1080`). The script reports counters and exits with `TESTS_FAILED` (`tests/test-plan-file-hooks.sh:1082-1091`).
- inputs_outputs_state: Inputs are hook stdin JSON (`{}` for prompt/stop hooks; `{"tool_name": "...", "tool_input": ...}` for tool hooks), temporary git repository state, RLCR state frontmatter, plan file contents, backup plan contents, branch name, and goal-tracker contents. Outputs are PASS/FAIL/SKIP console lines and process exit status equal to failed test count (`tests/test-plan-file-hooks.sh:26-28`, `tests/test-plan-file-hooks.sh:1082-1091`). The hooks under test output either nothing for allow decisions, JSON `{"decision":"block", ...}` with exit `0` for prompt/stop semantic blocks, or stderr plus exit `2` for PreToolUse blocks. The test relies on mutable shell state: `TEST_DIR`, `LOOP_DIR`, `DEFAULT_BRANCH`, `CURRENT_BRANCH`, `CLAUDE_PROJECT_DIR`, per-test `RESULT`, and `EXIT_CODE` (`tests/test-plan-file-hooks.sh:31-35`, `tests/test-plan-file-hooks.sh:95-101`). The hook implementation state model comes from `.humanize/rlcr/<timestamp>/state.md`; `find_active_loop` selects the newest directory containing `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh:151-174`), while strict parsing extracts and validates fields, including quote stripping for `plan_file`, `start_branch`, and `base_branch` (`hooks/lib/loop-common.sh:255-330`).
- gates_or_invariants: Core invariants are: active RLCR plan source must not change during a loop; backup `.humanize/rlcr/.../plan.md` must not be written, edited, removed, or shell-modified; branch must remain `start_branch`; state schema must include current v1.5.0 fields; and stop-hook exit is blocked if plan integrity cannot be proven. The `UserPromptSubmit` validator parses active state, rejects malformed state, requires `plan_tracked` and `start_branch`, compares current git branch to `start_branch`, and checks tracked/untracked plan status (`hooks/loop-plan-file-validator.sh:29-48`, `hooks/loop-plan-file-validator.sh:79-115`, `hooks/loop-plan-file-validator.sh:121-229`). Write/Edit validators block `plan.md` only when the path is under `.humanize/rlcr/` and permit unrelated/legacy paths (`hooks/loop-write-validator.sh:100-185`, `hooks/loop-edit-validator.sh:83-139`). Bash validation uses `command_modifies_file` patterns for redirection, append, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`, `cp`, `rm`, `dd`, `truncate`, `printf >`, and `exec >` (`hooks/lib/loop-common.sh:1068-1096`), then specifically blocks `.humanize/rlcr(/[^/]+)?/plan.md` (`hooks/loop-bash-validator.sh:330-340`). The stop hook enforces schema, branch, backup presence, original plan existence, tracked-plan uncommitted status, and byte/content equality via `diff -q` (`hooks/loop-codex-stop-hook.sh:158-208`, `hooks/loop-codex-stop-hook.sh:223-337`). Section placeholder detection is scoped by headings and searches each extracted section for `[To be ...]`, preventing cross-section false positives (`hooks/loop-codex-stop-hook.sh:643-718`).
- dependencies_and_callers: Direct dependencies are `bash`, `git`, `jq`, `grep`, `sed`, `awk`, `diff`, `mktemp`, `cp`, `rm`, and the hook scripts under `hooks/`. Hook scripts depend on `hooks/lib/loop-common.sh` for JSON validation, state parsing, active loop discovery, path classifiers, block messages, `git_adds_humanize`, and `command_modifies_file` (`hooks/lib/loop-common.sh:59-133`, `hooks/lib/loop-common.sh:151-174`, `hooks/lib/loop-common.sh:246-330`, `hooks/lib/loop-common.sh:712-716`, `hooks/lib/loop-common.sh:930-1026`, `hooks/lib/loop-common.sh:1071-1096`). Git operations are wrapped by `scripts/portable-timeout.sh`, which picks `gtimeout`, GNU `timeout`, Python, or no-timeout fallback (`scripts/portable-timeout.sh:1-71`). The stop hook optionally invokes `hooks/check-todos-from-transcript.py`, whose exit codes distinguish completed/no todos, incomplete todos, and parse errors (`hooks/check-todos-from-transcript.py:1-13`, `hooks/check-todos-from-transcript.py:90-134`). `tests/run-all-tests.sh` calls this suite as `test-plan-file-hooks.sh` (`tests/run-all-tests.sh:32-42`), and `hooks/hooks.json` is the runtime caller surface.
- edge_cases_or_failure_modes: The test explicitly covers malformed or old state schemas, missing `start_branch`, branch drift, YAML quotes around `plan_file` and `start_branch`, hyphenated plan paths, direct loop-root `.humanize/rlcr/plan.md` without timestamp, shell-command bypass attempts using substitution/globs/braces/pipes/backticks, tracked plan uncommitted changes, tracked plan committed content changes with clean git status, missing backup, deleted original plan, and section-specific placeholder reporting. It also asserts that legacy `.humanize-loop.local` paths are not protected (`tests/test-plan-file-hooks.sh:123-191`, `tests/test-plan-file-hooks.sh:260-341`, `tests/test-plan-file-hooks.sh:347-474`, `tests/test-plan-file-hooks.sh:523-749`, `tests/test-plan-file-hooks.sh:752-1080`). Implementation edge behavior: `is_in_humanize_loop_dir` matches only `.humanize/rlcr/` (`hooks/lib/loop-common.sh:712-716`), explaining the legacy negative tests. `loop-plan-file-validator.sh` returns exit `0` with JSON for semantic blocks but exits nonzero on malformed parse (`hooks/loop-plan-file-validator.sh:44-48`, `hooks/loop-plan-file-validator.sh:95-115`). `loop-write-validator.sh` and `loop-bash-validator.sh` reject invalid JSON or missing tool fields with exit `1`, while protected operations return exit `2` (`hooks/loop-write-validator.sh:25-48`, `hooks/loop-bash-validator.sh:24-47`). A local `git status --short` verification was attempted but this branch export is not a git checkout and the sandbox prevented xcode/git cache creation, so no repository-status validation was available from this export.
- validation_or_tests: The assigned file is itself the validation artifact. It has 1,091 lines and 17 named test groups, ending with `exit $TESTS_FAILED` (`tests/test-plan-file-hooks.sh:1082-1091`). Static inspection confirms it exercises all hook phases wired in `hooks/hooks.json` and is included in `tests/run-all-tests.sh`. I did not execute the full suite because the assignment required research notes only and the environment is read-only/restricted; the script writes only under temp directories, but the local git/toolchain check already showed sandbox friction, so evidence is based on direct file and dependency inspection rather than runtime results.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-059`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`