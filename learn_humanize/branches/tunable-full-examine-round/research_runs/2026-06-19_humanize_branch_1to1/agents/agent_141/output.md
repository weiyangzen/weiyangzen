# agent_141 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-141 `file` `tests/robustness/test-state-transition-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR loop state discovery and state-file parsing. It is not production algorithm code, but it directly exercises production state-machine helpers from `hooks/lib/loop-common.sh`, especially `find_active_loop`, `get_current_round`, and `parse_state_file_strict`. The test is included in the repository-wide robustness suite via `tests/run-all-tests.sh:55-72`, with this file listed at `tests/run-all-tests.sh:71`.
- algorithmic_behavior: The script builds isolated `.humanize/rlcr/<timestamp>/` fixture trees under a temp directory and verifies expected state handling across 16 cases. It creates normal loop states with `state.md` in `create_full_state` at `tests/robustness/test-state-transition-robustness.sh:30-52`, finalize states with `finalize-state.md` at `tests/robustness/test-state-transition-robustness.sh:54-72`, and cancel markers with `cancel-state.md` at `tests/robustness/test-state-transition-robustness.sh:74-89`. It checks valid rounds `0`, `5`, and `42` at `tests/robustness/test-state-transition-robustness.sh:98-138`; finalize detection at `tests/robustness/test-state-transition-robustness.sh:148-172`; cancel exclusion at `tests/robustness/test-state-transition-robustness.sh:182-207`; malformed and boundary round parsing at `tests/robustness/test-state-transition-robustness.sh:217-282`; required schema fields at `tests/robustness/test-state-transition-robustness.sh:292-361`; and directory discovery order/location at `tests/robustness/test-state-transition-robustness.sh:371-417`.
- inputs_outputs_state: Inputs are synthetic YAML-frontmatter markdown files named `state.md`, `finalize-state.md`, or `cancel-state.md`, plus timestamp-like directory names under `.humanize/rlcr`. Outputs are pass/fail counters and process exit status from `print_test_summary` at `tests/robustness/test-state-transition-robustness.sh:423-424`, implemented by `tests/test-helpers.sh:58-78`. The production state extracted by `get_current_round` is a string round value parsed from `current_round` and defaulted to `0` when missing by `hooks/lib/loop-common.sh:179-189`. `parse_state_file_strict` sets shell variables for parsed frontmatter fields and returns specific nonzero failure codes for missing file/frontmatter/fields or malformed numeric/boolean values at `hooks/lib/loop-common.sh:255-330`. `find_active_loop` outputs either the selected loop directory path or an empty string at `hooks/lib/loop-common.sh:156-174`.
- gates_or_invariants: The script runs with `set -euo pipefail` at `tests/robustness/test-state-transition-robustness.sh:12` and temporarily disables `set -e` around expected parser failures. The strict parser requires at least two `---` frontmatter separators, `current_round`, `max_iterations`, `review_started`, and `base_branch`, then requires numeric `current_round` and `max_iterations` and boolean `review_started` at `hooks/lib/loop-common.sh:263-323`. Numeric validation permits negative numbers by regex `^-?[0-9]+$` at `hooks/lib/loop-common.sh:307-315`, so this test explicitly treats negative `current_round` and over-max `current_round` as parseable rather than rejected at `tests/robustness/test-state-transition-robustness.sh:217-257`. Active-loop selection is lexicographic and single-candidate: `find_active_loop` sorts direct child directories descending and inspects only the first/newest child, returning it only if direct `state.md` or `finalize-state.md` exists at `hooks/lib/loop-common.sh:164-170`.
- dependencies_and_callers: The test sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` at `tests/robustness/test-state-transition-robustness.sh:14-17`; temp setup comes from `setup_test_dir` at `tests/test-helpers.sh:86-89`; pass/fail accounting comes from `tests/test-helpers.sh:30-44`. The exercised helpers are shared production hook infrastructure: `hooks/lib/loop-common.sh:5-9` identifies the library as common code for loop read/write/bash validators. Direct production callers found include `hooks/loop-bash-validator.sh:58` and `hooks/loop-bash-validator.sh:82`, `hooks/loop-plan-file-validator.sh:31` and `hooks/loop-plan-file-validator.sh:45`, `hooks/loop-read-validator.sh:58-95`, `hooks/loop-write-validator.sh:58-141`, `hooks/loop-edit-validator.sh:41-106`, and setup scripts such as `scripts/setup-rlcr-loop.sh:242` and `scripts/setup-pr-loop.sh:222`.
- edge_cases_or_failure_modes: The file documents several intentionally permissive or narrow behaviors. `finalize-state.md` is considered an active-loop indicator, but Test 5 only verifies coexistence with `state.md`; it does not prove a caller chooses finalize semantics over normal state after directory selection (`tests/robustness/test-state-transition-robustness.sh:160-172`). A directory containing only `cancel-state.md` is not active because production discovery checks only `state.md` or `finalize-state.md` (`tests/robustness/test-state-transition-robustness.sh:182-192`, `hooks/lib/loop-common.sh:168-170`). Negative and over-max rounds are parsed, not rejected, so range enforcement must happen elsewhere (`tests/robustness/test-state-transition-robustness.sh:217-257`). Directory ordering is lexicographic rather than timestamp-validated, and only the top-level newest child is inspected (`tests/robustness/test-state-transition-robustness.sh:385-402`, `hooks/lib/loop-common.sh:164-170`). A nested `subdir/state.md` is ignored because active-loop discovery looks for direct state files in the selected loop directory (`tests/robustness/test-state-transition-robustness.sh:404-417`).
- validation_or_tests: This assigned file is itself the validation artifact. It defines 16 assertions and exits with the aggregate pass/fail status through `print_test_summary` (`tests/robustness/test-state-transition-robustness.sh:423-424`). It is part of the all-tests runner’s robustness block (`tests/run-all-tests.sh:55-72`). I inspected the file and referenced helper implementations directly; I did not execute the test because this task requested read-only research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 item evidence section present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`