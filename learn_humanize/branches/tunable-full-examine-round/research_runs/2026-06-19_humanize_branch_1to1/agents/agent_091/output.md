# agent_091 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-091 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: This is a reusable block-message template for the RLCR loop’s plan-backup immutability rule. It tells the agent that `.humanize/rlcr/.../plan.md` is a backup of the original plan and must be read-only, preserving plan integrity across the session. The whole file is policy text, not executable logic: [prompt-template/block/plan-backup-protected.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/prompt-template/block/plan-backup-protected.md:1).
- algorithmic_behavior: The template contributes to the algorithm by rendering the denial reason after hook logic detects a forbidden plan-backup mutation. It states three behavioral transitions: `plan.md` in the loop directory is classified as a backup, mutation attempts are blocked, and agents should read it when they need reference material instead of changing it. The operative sentence is short: “cannot be modified” at [prompt-template/block/plan-backup-protected.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/prompt-template/block/plan-backup-protected.md:3). The actual mutation detection lives in hook scripts, not in this template.
- inputs_outputs_state: Inputs are implicit through callers: a validator detects a `plan.md` path under `.humanize/rlcr` or a shell command that modifies such a path, then calls `load_and_render_safe "$TEMPLATE_DIR" "block/plan-backup-protected.md" "$FALLBACK"`. The template has no variables or placeholders, so rendering is deterministic and state-free. Output is plain Markdown written to stderr as the human/agent-facing reason, followed by hook exit status `2` for PreToolUse validators. There is no persistent state transition inside this file; the protected state is the invariant that the loop backup plan remains unchanged.
- gates_or_invariants: The invariant is “loop `plan.md` backup is read-only.” `loop-edit-validator.sh` blocks Edit tool attempts when the basename is `plan.md` and the path contains `/.humanize/rlcr/`, then emits this template and exits `2`: [hooks/loop-edit-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-edit-validator.sh:131). `loop-write-validator.sh` applies the same gate for Write tool attempts after classifying `IS_PLAN_BACKUP`: [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-write-validator.sh:111) and [hooks/loop-write-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-write-validator.sh:179). `loop-bash-validator.sh` blocks shell mutations by matching modifying commands against `\.humanize/rlcr(/[^/]+)?/plan\.md`: [hooks/loop-bash-validator.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-bash-validator.sh:331). In all three cases, the template is the standardized denial surface for the same invariant.
- dependencies_and_callers: Direct callers are `hooks/loop-edit-validator.sh`, `hooks/loop-write-validator.sh`, and `hooks/loop-bash-validator.sh`, each through `load_and_render_safe` from the shared template loader. `TEMPLATE_DIR` is initialized in `hooks/lib/loop-common.sh`, and `load_and_render_safe` is defined in `hooks/lib/template-loader.sh` as the safe path that falls back when a template is unavailable. Related integrity enforcement exists in `hooks/loop-codex-stop-hook.sh`, which checks that the backup exists at `$LOOP_DIR/plan.md` and blocks if missing: [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/hooks/loop-codex-stop-hook.sh:267). That stop hook does not use this template for the missing-backup case, but it enforces the broader same plan-integrity contract.
- edge_cases_or_failure_modes: If the template file is missing or cannot be loaded, the callers provide fallbacks such as “Editing plan.md backup is not allowed during RLCR loop” or “Writing to plan.md backup is not allowed during RLCR loop,” so the gate can still block safely. Path coverage is deliberately limited to `.humanize/rlcr`; tests indicate legacy `.humanize-loop.local/.../plan.md` remains allowed, so this template is not a universal `plan.md` protection message. Bash detection covers direct `.humanize/rlcr/plan.md`, one loop subdirectory, glob/brace/command-substitution cases through `command_modifies_file` tests, but it remains dependent on command-pattern detection rather than filesystem-level ACLs. The template itself has no variables, so it cannot mention the specific attempted path or operation unless callers replace it with fallback text.
- validation_or_tests: `tests/test-plan-file-hooks.sh` explicitly describes coverage for Write, Edit, and Bash validators blocking `plan.md`: [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-plan-file-hooks.sh:5). Test setup creates the original plan and copies it to `$LOOP_DIR/plan.md` as the backup: [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-plan-file-hooks.sh:72). Specific tests cover Write blocking, Edit blocking, Bash redirection blocking, Bash `rm`, direct `.humanize/rlcr/plan.md`, dynamic/glob/brace/pipe cases, and legacy-location allowances; the relevant grep index shows these around lines 200-332 and 1044-1077 in [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-plan-file-hooks.sh:200). Stop-hook integrity validation also tests missing backup behavior at [tests/test-plan-file-hooks.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-plan-file-hooks.sh:557). I did not run tests because this assignment requested read-only research notes and the environment is a read-only branch export.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-091`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`