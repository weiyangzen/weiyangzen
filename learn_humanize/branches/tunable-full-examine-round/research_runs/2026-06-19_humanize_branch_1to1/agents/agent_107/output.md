# agent_107 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-107 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Finalize-phase prompt template for the RLCR loop after Codex review has accepted the implementation. It is not executable code itself, but it defines the human/Claude-side final algorithmic contract for the last blocked stop-hook continuation: run a simplification/refactor pass, preserve behavior, complete todos, commit, and write `finalize-summary.md`. The template is the normal, non-skipped-review variant; the sibling `prompt-template/claude/finalize-phase-skipped-prompt.md` is used when review could not be fully run.

- algorithmic_behavior: The template declares the state transition context: “Codex review has passed” and “implementation is complete” at lines 3-5 of `prompt-template/claude/finalize-phase-prompt.md`. It then instructs Claude to invoke the `code-simplifier:code-simplifier` subagent via Task at lines 7-14. Its behavior contract is intentionally constrained to equivalence-preserving cleanup: no functionality changes, no broken existing tests, no new bugs, and only cleanup/refactoring at lines 16-24. The focus section narrows the refactor scope to recently added or modified code and specifically to branch differences from `{{BASE_BRANCH}}` to `{{START_BRANCH}}` at lines 25-34. The required exit artifacts are todos completed via `TodoWrite`, committed changes, and a summary written to `{{FINALIZE_SUMMARY_FILE}}` at lines 41-51.

- inputs_outputs_state: Inputs are template variables rendered by the stop hook: `{{BASE_BRANCH}}`, `{{START_BRANCH}}`, `{{PLAN_FILE}}`, `{{GOAL_TRACKER_FILE}}`, and `{{FINALIZE_SUMMARY_FILE}}` in `prompt-template/claude/finalize-phase-prompt.md:29`, `:38`, `:39`, and `:45`. The caller passes these variables in `hooks/loop-codex-stop-hook.sh:1097-1102`. The prompt output is a rendered block reason returned to Claude through hook JSON: `jq` emits `"decision": "block"` and `"reason": $finalize_prompt` in `hooks/loop-codex-stop-hook.sh:1105-1110`. The relevant state transition is controlled by `enter_finalize_phase`, which renames `state.md` to `finalize-state.md` before rendering this prompt in `hooks/loop-codex-stop-hook.sh:1032-1039`. On the next valid exit from finalize phase, the stop hook expects `finalize-summary.md` rather than `round-N-summary.md` in `hooks/loop-codex-stop-hook.sh:609-614`, then renames `finalize-state.md` to `complete-state.md` and exits in `hooks/loop-codex-stop-hook.sh:742-747`.

- gates_or_invariants: The template’s invariants are behavior preservation and test preservation: “Must NOT change existing functionality,” “Must NOT fail existing tests,” and “Only perform functionality-equivalent changes” in `prompt-template/claude/finalize-phase-prompt.md:20-24`. System gates around the template enforce that finalize phase is detected only by `finalize-state.md` in `hooks/loop-codex-stop-hook.sh:66-82`; final completion requires `finalize-summary.md` to exist in `hooks/loop-codex-stop-hook.sh:605-641`; finalize completion skips Codex review by design in `hooks/loop-codex-stop-hook.sh:736-747`; max-iteration gating is skipped while already in finalize phase in `hooks/loop-codex-stop-hook.sh:721-730`. The write validators protect state integrity: writes to `finalize-state.md` are blocked before generic state handling in `hooks/loop-write-validator.sh:148-160` and `hooks/loop-edit-validator.sh:112-125`, while writes to the active loop’s `finalize-summary.md` are explicitly allowed in `hooks/loop-write-validator.sh:162-172`.

- dependencies_and_callers: Primary caller is `enter_finalize_phase` in `hooks/loop-codex-stop-hook.sh:1030-1110`. That function chooses this template only when `skip_reason` is empty; otherwise it loads `claude/finalize-phase-skipped-prompt.md` at `hooks/loop-codex-stop-hook.sh:1042-1075`. The normal template is reached after `run_and_handle_code_review` detects no `[P0-9]` issues and calls `enter_finalize_phase "" "$success_msg"` in `hooks/loop-codex-stop-hook.sh:1011-1027`, or from review-phase success in `hooks/loop-codex-stop-hook.sh:1446-1468`. The template rendering dependency is `load_and_render_safe`, referenced by `hooks/loop-codex-stop-hook.sh:1097`; common setup initializes and validates `TEMPLATE_DIR` via `hooks/lib/loop-common.sh:139-148`. The template references external artifacts for Claude context: original plan and goal tracker at `prompt-template/claude/finalize-phase-prompt.md:36-39`.

- edge_cases_or_failure_modes: If the template file is missing or rendering fails, `load_and_render_safe` falls back to an inline prompt embedded in `hooks/loop-codex-stop-hook.sh:1077-1095`; this reduces template-file failure to degraded text rather than a hard stop. If Codex review cannot be run or produces failure/empty output, the flow should block and avoid incorrectly entering normal finalize; failure handling is in `hooks/loop-codex-stop-hook.sh:1007-1023`, and test coverage asserts state preservation after empty output in `tests/test-finalize-phase.sh:686-696`. If `BASE_BRANCH` is empty during implementation COMPLETE handling, the hook uses the skipped-review path instead of this template, recorded in `hooks/loop-codex-stop-hook.sh:1413-1418`. If the loop is at max iterations when COMPLETE is seen, finalize is skipped and `maxiter-state.md` is created instead, controlled by `hooks/loop-codex-stop-hook.sh:1401-1407` and tested in `tests/test-finalize-phase.sh:542-560`. If finalize-phase todos remain incomplete, the stop hook blocks before completion and does not invoke Codex, tested in `tests/test-finalize-phase.sh:698-740`. If `finalize-summary.md` is missing, completion is blocked with a summary message, tested in `tests/test-finalize-phase.sh:429-444`.

- validation_or_tests: `tests/test-finalize-phase.sh` directly validates the finalized behavior surface. COMPLETE causing finalize entry, state rename to `finalize-state.md`, and block output containing simplification guidance are asserted at `tests/test-finalize-phase.sh:524-540`. Max-iteration skip is asserted at `tests/test-finalize-phase.sh:542-560`. Missing summary blocks finalize completion at `tests/test-finalize-phase.sh:429-444`, and finalize-phase Codex bypass is checked at `tests/test-finalize-phase.sh:446-451` and `tests/test-finalize-phase.sh:500-503`. Write/edit/bash validator protection for `finalize-state.md` and write allowance for `finalize-summary.md` are tested at `tests/test-finalize-phase.sh:341-415`. Validator parsing of `finalize-state.md` is tested at `tests/test-finalize-phase.sh:817-850`. I did not run tests because the branch export is read-only and the task requested research notes only.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-107`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`