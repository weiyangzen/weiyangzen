# agent_015 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-015 `directory` `scripts/lib`
- cursor: `[_]`
- core_role: `scripts/lib` is a shared shell-library directory for Humanize monitor algorithms. Recursive inspection found one file: `scripts/lib/monitor-common.sh`, 17,436 bytes and 498 lines. It is not an entrypoint; `scripts/humanize.sh` conditionally sources it at `scripts/humanize.sh:6-10`, then uses its helpers inside RLCR and PR-loop monitor flows. The directory’s responsibility is to centralize reusable monitor state discovery, terminal/status formatting primitives, PR phase inference, and markdown goal-tracker parsing so parent monitor commands do not duplicate this logic.
- algorithmic_behavior: The contained child file `scripts/lib/monitor-common.sh` defines several independent helper algorithms:
  - Color/status helpers at `scripts/lib/monitor-common.sh:14-23` and `scripts/lib/monitor-common.sh:130-141` map semantic monitor statuses such as `active`, `completed`, `failed`, `cancelled`, `timeout`, and unknown values to ANSI color codes.
  - File-size portability is handled by `monitor_get_file_size` at `scripts/lib/monitor-common.sh:32-35`, trying GNU `stat -c%s`, then BSD/macOS `stat -f%z`, then returning `0`.
  - Session discovery is handled by `monitor_find_latest_session` at `scripts/lib/monitor-common.sh:40-63`. It scans immediate child directories, filters names matching `YYYY-MM-DD_HH-MM-SS`, and lexicographically chooses the newest timestamp name. It intentionally uses `find` rather than shell globs to avoid no-match behavior in zsh.
  - Terminal layout helpers at `scripts/lib/monitor-common.sh:71-92` clear the screen, set a scroll region below a fixed header, move the cursor into the scroll region, and restore the scroll region when monitoring stops.
  - State-file detection is handled by `monitor_find_state_file` at `scripts/lib/monitor-common.sh:154-188`. It treats `state.md` as an active loop state with highest priority, otherwise finds a `*-state.md` stop file and derives the stop reason from the filename.
  - YAML frontmatter extraction at `scripts/lib/monitor-common.sh:197-209` extracts frontmatter between delimiter lines and returns a key’s scalar value after removing double quotes.
  - Display helpers at `scripts/lib/monitor-common.sh:218-252` normalize ISO timestamps and truncate long strings from either the start or end.
  - PR-loop phase inference is the most algorithmic section. `get_pr_loop_phase` at `scripts/lib/monitor-common.sh:267-342` first checks terminal state files, then finds the highest numbered `round-*-pr-check.md`, compares current size with a per-session `/tmp/humanize-phase-<session>-<round>.size` cache, and also uses recent mtime under 10 seconds as a fallback signal that Codex is still analyzing. If no active analysis is detected, it parses `state.md` frontmatter and classifies the loop as initial-review wait versus reviewer-poll wait.
  - Phase display mapping at `scripts/lib/monitor-common.sh:346-381` converts internal phase values into user-facing messages, including active bot names when provided.
  - Generic goal tracker parsing at `scripts/lib/monitor-common.sh:390-463` counts acceptance criteria, active/completed/deferred task rows, open issues, and a short ultimate-goal summary from markdown sections.
  - PR goal tracker parsing at `scripts/lib/monitor-common.sh:468-498` extracts total/resolved/remaining issue counts and the last reviewer from the PR tracker’s statistics and issue-summary table.
- inputs_outputs_state: Inputs are shell function arguments and repository/runtime files: loop directories, timestamped session directories, `state.md`, `approve-state.md`, `cancel-state.md`, `maxiter-state.md`, `round-*-pr-check.md`, `goal-tracker.md`, PR goal trackers, terminal dimensions via `tput`, and platform `stat` behavior. Outputs are plain stdout values designed for pipe-delimited parsing, such as `path|status`, `total|completed|active|...`, phase strings, display strings, ANSI escape sequences, or a file byte count. The only persistent state mutation in the library is the phase-size cache written under `/tmp` by `get_pr_loop_phase` at `scripts/lib/monitor-common.sh:297-307`; terminal helpers also mutate terminal scroll-region/cursor state. Parent state consumption is visible in `scripts/humanize.sh`: RLCR monitoring wraps `monitor_find_latest_session` at `scripts/humanize.sh:237-240`, wraps `monitor_find_state_file` at `scripts/humanize.sh:344-347`, uses goal parsing in status-bar assembly at `scripts/humanize.sh:400-409`, and uses `monitor_get_file_size` for incremental log reading at `scripts/humanize.sh:610-613`. PR monitoring uses `monitor_find_latest_session` at `scripts/humanize.sh:962-965`, remaps state-file statuses at `scripts/humanize.sh:1011-1028`, calls phase inference/display at `scripts/humanize.sh:1097-1103`, and parses PR tracker issue stats at `scripts/humanize.sh:1139-1147`.
- gates_or_invariants: Directory traversal is intentionally shallow only for timestamp sessions, not for this assigned research; the implementation’s invariant is that monitor session directories must be immediate children and must match the timestamp regex in `scripts/lib/monitor-common.sh:55` to be considered. `monitor_find_state_file` prioritizes `state.md` over stop-state files at `scripts/lib/monitor-common.sh:162-165`, so an active state masks terminal stop files if both exist. Stop reason is derived mechanically by stripping `-state.md` at `scripts/lib/monitor-common.sh:176-179`, which means callers must normalize names when they need friendlier forms; PR monitor does that for `approve` and `maxiter` at `scripts/humanize.sh:1021-1025`. `get_pr_loop_phase` checks final states before active file-growth detection at `scripts/lib/monitor-common.sh:272-275`, so terminal states win over a fresh `pr-check` file. The Codex-analyzing gate is either file growth compared to cache or mtime younger than 10 seconds at `scripts/lib/monitor-common.sh:308-317`. Goal tracker counters assume recognizable section headings and markdown table/list formats; absent files return zero/default summaries rather than failing at `scripts/lib/monitor-common.sh:392-394` and `scripts/lib/monitor-common.sh:470-472`.
- dependencies_and_callers: Runtime dependencies are standard POSIX/GNU/BSD shell tools: `bash`, `find`, `basename`, `stat`, `sed`, `grep`, `sort`, `wc`, `tr`, `head`, `tail`, `cut`, `date`, `tput`, `printf`, and `/tmp` write access for phase-cache files. The direct production caller is `scripts/humanize.sh`, sourced at `scripts/humanize.sh:6-10`. The call graph from search shows `monitor_find_latest_session`, `monitor_find_state_file`, `monitor_get_file_size`, `get_pr_loop_phase`, `get_pr_loop_phase_display`, and `humanize_parse_pr_goal_tracker` are used by monitor functions in `scripts/humanize.sh`; several other helpers such as color/status/string formatting are exported utility surfaces and currently mostly self-contained. Validation/test callers are concentrated in `tests/test-pr-loop-system.sh`, which sources `scripts/lib/monitor-common.sh` in phase detection tests at `tests/test-pr-loop-system.sh:256-258`, goal parsing at `tests/test-pr-loop-system.sh:315-317`, PR tracker parsing at `tests/test-pr-loop-system.sh:377-379`, state detection at `tests/test-pr-loop-system.sh:423-425`, shared latest-session tests at `tests/test-pr-loop-system.sh:770-782`, shared state-file tests at `tests/test-pr-loop-system.sh:794-819`, file-size tests at `tests/test-pr-loop-system.sh:826-842`, and Codex-analyzing tests at `tests/test-pr-loop-system.sh:849-899`.
- edge_cases_or_failure_modes: Missing session directories return empty or unknown values rather than errors in `monitor_find_latest_session` and `monitor_find_state_file` at `scripts/lib/monitor-common.sh:44-47` and `scripts/lib/monitor-common.sh:157-160`. Nonexistent files return `0` from `monitor_get_file_size` at `scripts/lib/monitor-common.sh:34`. If multiple stop files exist, `monitor_find_state_file` takes the first `find` result at `scripts/lib/monitor-common.sh:172-181`; ordering is filesystem-dependent, so ambiguous terminal states can produce nondeterministic stop reasons. `monitor_get_yaml_value` interpolates the key into a grep regex at `scripts/lib/monitor-common.sh:208`; unusual regex metacharacters in key names could match unexpectedly, though current callers use simple keys. `monitor_truncate_string` subtracts 3 from `max_len` at `scripts/lib/monitor-common.sh:245` and `scripts/lib/monitor-common.sh:249`; very small max lengths could produce odd substring behavior. `get_pr_loop_phase` uses `/tmp` cache names based on only `basename "$session_dir"` and round at `scripts/lib/monitor-common.sh:298-299`, so same-named sessions from different projects can collide in theory. It also uses `touch -d` in the Linux-oriented test path at `tests/test-pr-loop-system.sh:878-880`, while the library itself handles both GNU and BSD `stat`. If `state.md` frontmatter is malformed or absent, current round defaults to `0` and startup case defaults to `1` at `scripts/lib/monitor-common.sh:331-335`, biasing the phase to initial-review wait.
- validation_or_tests: I ran `bash -n scripts/lib/monitor-common.sh`; it exited successfully with no output, confirming shell syntax validity. Existing test coverage includes direct tests for approved/cancelled/max-iteration/waiting phases in `tests/test-pr-loop-system.sh:256-309` and `tests/test-pr-loop-system.sh:459-487`, approve-state consistency in `tests/test-pr-loop-system.sh:616-635`, shared latest-session/state/file-size helpers in `tests/test-pr-loop-system.sh:770-842`, and Codex-analyzing file-growth/mtime behavior in `tests/test-pr-loop-system.sh:849-899`. Goal tracker parsing and PR issue statistics are covered at `tests/test-pr-loop-system.sh:315-370` and `tests/test-pr-loop-system.sh:377-416`. I did not run the full test suite because this assignment requested research notes only and the branch export is read-only; the targeted syntax validation was read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 Item Evidence section for the single assigned row`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`