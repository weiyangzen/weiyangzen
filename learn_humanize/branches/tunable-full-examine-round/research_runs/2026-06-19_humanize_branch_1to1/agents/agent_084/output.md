# agent_084 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-084 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: This is a block prompt template for the RLCR/Codex stop-hook fail-closed path when repository state cannot be verified through `git status`. The template is not an algorithm by itself, but it is part of an algorithmic gate contract: if git status fails or times out, the hook must block progression and produce a human-readable reason instead of continuing with unknown repository state. Direct file content is 326 bytes and spans `prompt-template/block/git-status-failed.md:1-10`.
- algorithmic_behavior: The template defines the rendered Markdown reason headed `# Git Status Failed` at `prompt-template/block/git-status-failed.md:1`. It reports the failed operation and interpolates `{{GIT_STATUS_EXIT}}` at `prompt-template/block/git-status-failed.md:3`, then enumerates likely causes: git lock/contention, invalid repository state, or slow large repository behavior at `prompt-template/block/git-status-failed.md:5-8`. The behavioral action requested from the operator is manual `git status` inspection and retry at `prompt-template/block/git-status-failed.md:10`.
- inputs_outputs_state: Input is a single template variable, `GIT_STATUS_EXIT`, supplied by the Codex stop hook after `run_with_timeout "$GIT_TIMEOUT" git status --porcelain` returns nonzero. The caller initializes `GIT_STATUS_EXIT=0`, captures a nonzero status from the timed command, and passes it into `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:409-420`. Output is a JSON hook response with `"decision": "block"`, rendered `"reason"`, and a system message containing the same exit code at `hooks/loop-codex-stop-hook.sh:421-422`. State transition is from “repository recognized and git status check attempted” to “blocked exit”; the hook exits immediately after emitting the block JSON at `hooks/loop-codex-stop-hook.sh:423`.
- gates_or_invariants: The key invariant is fail-closed repository verification. The hook comment explicitly says cached git status is used by later checks and that git failures must not bypass checks at `hooks/loop-codex-stop-hook.sh:399-401`. The hook only enters this path if `git` exists and `git rev-parse --git-dir` succeeds within the same timeout wrapper at `hooks/loop-codex-stop-hook.sh:406`; once inside, any nonzero `git status --porcelain` result blocks rather than treating status as clean. The timeout budget is `GIT_TIMEOUT=30` seconds in `hooks/loop-codex-stop-hook.sh:54-55`. The template loader uses single-pass `{{VAR}}` substitution, preserving missing placeholders rather than recursively expanding injected placeholder content, documented in `hooks/lib/template-loader.sh:7-13` and implemented around `hooks/lib/template-loader.sh:58-74`.
- dependencies_and_callers: The active direct caller is `hooks/loop-codex-stop-hook.sh`, which sources shared loop functions and the portable timeout helper at `hooks/loop-codex-stop-hook.sh:46-52`. `loop-common.sh` sources `template-loader.sh` and initializes `TEMPLATE_DIR` from `prompt-template` at `hooks/lib/loop-common.sh:139-148`. Rendering is through `load_and_render_safe`, whose fallback behavior is defined at `hooks/lib/template-loader.sh:168-198`. Timeout behavior comes from `scripts/portable-timeout.sh`, which prefers `gtimeout`, GNU `timeout`, Python timeout, or no-timeout fallback at `scripts/portable-timeout.sh:6-27`, and returns timeout exit `124` for Python-backed expiration at `scripts/portable-timeout.sh:53-58`. A related PR-loop hook has a same-title inline failure message but does not load this template; see `hooks/pr-loop-stop-hook.sh:283-293`.
- edge_cases_or_failure_modes: If the template file is missing or empty, `load_and_render_safe` falls back to the inline fallback in `hooks/loop-codex-stop-hook.sh:414-418`, so the block reason is still emitted. If `GIT_STATUS_EXIT` is not supplied, the placeholder remains literal because missing variables are preserved by the renderer. If no timeout implementation is available, `run_with_timeout` runs without a real timeout and warns, so true hangs could persist despite the fail-closed design; this is visible in `scripts/portable-timeout.sh:64-68`. If `git rev-parse --git-dir` fails or times out, this particular template is not used because the enclosing condition is skipped at `hooks/loop-codex-stop-hook.sh:406`; later git-clean gates also do not run because `GIT_IS_REPO` remains false. If `git status` fails due lock contention, invalid repo state, or repository scale, the template intentionally avoids prescribing automatic repair and asks for manual inspection.
- validation_or_tests: I did not modify files or run the test suite. Static validation coverage exists: `tests/test-template-references.sh` scans hook calls to `load_template`, `load_and_render`, and `load_and_render_safe` and fails missing template references, including this direct reference pattern, at `tests/test-template-references.sh:83-107`. It also verifies safe rendering usage for critical validators at `tests/test-template-references.sh:170-200`, though the Codex stop hook is covered by reference existence rather than that critical-validator subset. `tests/test-templates-comprehensive.sh` loads every Markdown template under `prompt-template`, checks non-empty content, and validates placeholder syntax at `tests/test-templates-comprehensive.sh:82-107` and `tests/test-templates-comprehensive.sh:114-194`; this template has one uppercase placeholder, `{{GIT_STATUS_EXIT}}`, matching that syntax rule. A read-only consistency attempt to run `git rev-parse HEAD` failed because this branch export is not a git checkout in the sandbox and macOS temp/cache writes were blocked, so the reported source commit is taken from scheduler metadata.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1/1; the single assigned item id appears exactly once, in the Item Evidence heading.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`