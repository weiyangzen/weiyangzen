# agent_102 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-102 `file` `prompt-template/block/wrong-directory-path.md`
- cursor: `[_]`
- core_role: This is a reusable block prompt template for the RLCR hook validators when a round file is addressed from the wrong active-loop directory path. It is presentation logic for a guard failure, not the guard predicate itself. The template defines the user-facing denial text at `prompt-template/block/wrong-directory-path.md:1` through `prompt-template/block/wrong-directory-path.md:6`.
- algorithmic_behavior: The template renders a short diagnostic with three substituted fields: `{{ACTION}}`, `{{FILE_PATH}}`, and `{{CORRECT_PATH}}`. The generated message says the agent is trying to perform the action on the supplied path, shows the exact correct active-loop path, and suggests `cat {{FILE_PATH}}` if the file is needed. The actual transition is enforced by callers: both read and write validators compute `CORRECT_PATH="$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME"`, compare it with `FILE_PATH`, render this template on mismatch, and exit with code `2`; see `hooks/loop-read-validator.sh:157` through `hooks/loop-read-validator.sh:167` and `hooks/loop-write-validator.sh:252` through `hooks/loop-write-validator.sh:262`.
- inputs_outputs_state: Inputs are template variables supplied by `load_and_render_safe`: `ACTION=read` for the Read hook and `ACTION=write to` for the Write hook, plus the attempted `FILE_PATH` and calculated `CORRECT_PATH`. Output is Markdown written to stderr by the validator before blocking the tool operation. This file does not mutate state; state is read upstream from `state.md` or `finalize-state.md` to derive `CURRENT_ROUND` and `ACTIVE_LOOP_DIR` before the template is used, as shown in `hooks/loop-read-validator.sh:80` through `hooks/loop-read-validator.sh:99` and `hooks/loop-write-validator.sh:123` through `hooks/loop-write-validator.sh:145`.
- gates_or_invariants: The invariant is exact path equality, not just matching filename or being somewhere under `.humanize/rlcr/`. For reads, the file must already be recognized as a round summary or prompt, be in an RLCR loop directory, pass current-round checks unless allowlisted, and then match `ACTIVE_LOOP_DIR/$CLAUDE_FILENAME` exactly. For writes, the relevant summary/finalize/RLCR file passes earlier protections, then the same exact-path check is applied. Helper predicates show the upstream matching rules: round file types are detected by `round-[0-9]+-<type>.md` at `hooks/lib/loop-common.sh:399` through `hooks/lib/loop-common.sh:406`; allowlisted files must be under the active loop dir exactly at `hooks/lib/loop-common.sh:420` through `hooks/lib/loop-common.sh:435`; RLCR membership is a string match for `.humanize/rlcr/` at `hooks/lib/loop-common.sh:712` through `hooks/lib/loop-common.sh:716`.
- dependencies_and_callers: Direct callers are `hooks/loop-read-validator.sh` and `hooks/loop-write-validator.sh`. Both source `hooks/lib/loop-common.sh`, which sources `hooks/lib/template-loader.sh` and initializes `TEMPLATE_DIR`; see `hooks/lib/loop-common.sh:139` through `hooks/lib/loop-common.sh:148`. Rendering is handled by `load_and_render_safe`, which uses the template when present and falls back to inline text if missing or empty; see `hooks/lib/template-loader.sh:167` through `hooks/lib/template-loader.sh:203`. The template is not directly used by `hooks/loop-bash-validator.sh`; bash wrong-directory cases are covered by separate command/file protection paths and tests.
- edge_cases_or_failure_modes: If this template is missing or renders empty, callers still block the operation and emit an inline fallback message, so enforcement does not depend on the file being present. Missing template variables are left as placeholders by the renderer, because `render_template` keeps unknown `{{VAR}}` patterns unchanged and uses single-pass substitution; see `hooks/lib/template-loader.sh:50` through `hooks/lib/template-loader.sh:131`. Because `FILE_PATH` is compared as a raw string against `CORRECT_PATH`, equivalent paths with symlink resolution, relative components, or different normalization may fail the exact-path gate. The template’s final hint uses `cat {{FILE_PATH}}`; in the read-validator context this is a workaround suggestion, while in write-validator context it may be awkward because the blocked action was a write.
- validation_or_tests: No test directly asserts this template’s exact Markdown text. Related coverage validates the underlying wrong-directory invariant: `tests/test-allowlist-validators.sh:120` through `tests/test-allowlist-validators.sh:126` confirms an allowlisted filename in `/other/path` is not allowlisted, and `tests/test-allowlist-validators.sh:316` through `tests/test-allowlist-validators.sh:327` confirms Bash writes to `/tmp/round-1-todos.md` are blocked. The caller fallback paths at `hooks/loop-read-validator.sh:160` through `hooks/loop-read-validator.sh:166` and `hooks/loop-write-validator.sh:255` through `hooks/loop-write-validator.sh:261` provide additional resilience if the template file is unavailable.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section present for the single assigned file
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`