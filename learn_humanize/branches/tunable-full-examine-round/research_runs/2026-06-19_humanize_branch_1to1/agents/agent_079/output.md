# agent_079 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-079 `file` `prompt-template/block/git-add-humanize.md`
- cursor: `[_]`
- core_role: This file is the user-facing block template for the `.humanize/` git-staging protection gate. It is not the detector itself; it is the canonical message emitted when a shell command is classified as a `git add` operation that could stage Humanize loop state. The template states the protected invariant that `.humanize/` contains local loop state and should not be committed at `prompt-template/block/git-add-humanize.md:3-6`.
- algorithmic_behavior: The template defines the recovery contract for a blocked transition from "unstaged local loop state" to "possibly staged version-control content." It directs the operator toward narrow staging commands, including `git add <specific-file>`, `git add src/`, and patch mode at `prompt-template/block/git-add-humanize.md:8-15`. It documents blocked command classes at `prompt-template/block/git-add-humanize.md:16-24`: direct `.humanize` staging, broad all-add staging via `-A`/`--all`, broad `git add .`, and force-add broad scope via `git add -f .`. It also defines a special remediation path for adding ignore coverage: edit `.gitignore`, stage only `.gitignore`, and commit with a message that avoids the literal `.humanize` token at `prompt-template/block/git-add-humanize.md:26-34`.
- inputs_outputs_state: The template has no runtime placeholders and accepts no direct inputs. Its effective inputs come from the caller: a lowercased shell command, the presence of `.humanize/`, gitignore status, and force/all/broad-scope flags evaluated by `git_adds_humanize` in `hooks/lib/loop-common.sh:930-1026`. Its output is markdown rendered by `git_add_humanize_blocked_message`, which calls `load_and_render_safe "$TEMPLATE_DIR" "block/git-add-humanize.md"` at `hooks/lib/loop-common.sh:1028-1065`. The bash validator sends that message to stderr and exits with status `2` when the guard trips at `hooks/loop-bash-validator.sh:109-118`. The template itself does not mutate state; the state transition is a prevented transition: no command execution should proceed after the validator emits this block.
- gates_or_invariants: The central invariant is that `.humanize/` and `.humanize*` local loop artifacts remain uncommitted. The repository `.gitignore` includes `.humanize*` at `.gitignore:4-5`, and this template reinforces that ignore policy. The corresponding detector blocks direct references to `.humanize`, including quoted and path-prefixed variants, while avoiding over-blocking names like `.humanizeconfig` via the direct-reference regex described in `hooks/lib/loop-common.sh:967-972`. Force broad-scope adds are always blocked because they bypass gitignore, as implemented in `hooks/lib/loop-common.sh:975-1003`. Non-force `git add -A`/`--all` is blocked when `.humanize` exists at `hooks/lib/loop-common.sh:1005-1014`. Non-force broad `git add .` or `git add *` is blocked only if `.humanize` is not ignored, using `git check-ignore -q .humanize` at `hooks/lib/loop-common.sh:1016-1021`.
- dependencies_and_callers: The direct caller is `git_add_humanize_blocked_message` in `hooks/lib/loop-common.sh:1028-1065`. That function depends on the shared template loader and has a hardcoded fallback message mirroring this file, so the protection remains explainable if the template is missing. The enforcement caller is `hooks/loop-bash-validator.sh`, which invokes `git_adds_humanize "$COMMAND_LOWER"` and exits before later RLCR state/file protections if the command is unsafe at `hooks/loop-bash-validator.sh:109-118`. The template loader path is resolved through `TEMPLATE_DIR` in `hooks/lib/loop-common.sh:144-147`, and broader template reference tests discover `load_and_render_safe` references, including this one, in `tests/test-template-references.sh`.
- edge_cases_or_failure_modes: A missing or unreadable template should fall back to the embedded message through `load_and_render_safe`, so the gate can still explain the block. The detector receives lowercased input, so uppercase `-A` becomes `-a`; comments in `hooks/lib/loop-common.sh:912-914` explicitly account for that. Chained commands are split on `&&`, `||`, `|`, and `;` at `hooks/lib/loop-common.sh:933-941`, so a hidden `git add .humanize` later in a command chain is still caught. The detector recognizes `git -C <dir> add` and similar git options before `add` at `hooks/lib/loop-common.sh:946-956`. A notable documentation/behavior nuance: the template says `git add .` is blocked "when .humanize exists" at `prompt-template/block/git-add-humanize.md:18-24`, but implementation permits non-force broad scope when `.humanize` is covered by gitignore, as shown in `hooks/lib/loop-common.sh:1016-1021`; this makes the template slightly stricter-sounding than the actual broad-scope non-force gate.
- validation_or_tests: `tests/test-humanize-escape.sh` directly validates the detector behavior that leads to this template. It covers path variants like `./.humanize`, nested paths, and quoted paths at `tests/test-humanize-escape.sh:77-101`; force and broad-scope blocking at `tests/test-humanize-escape.sh:103-119`; `git add -A`/`--all` blocking when `.humanize` exists at `tests/test-humanize-escape.sh:121-144`; chained commands at `tests/test-humanize-escape.sh:147-155`; and `git -C`/`--git-dir` forms at `tests/test-humanize-escape.sh:157-166`. The same test file verifies allowed cases: specific files, `.gitignore`, non-add git commands, patch mode, and similarly named but distinct dotfiles such as `.humanizeconfig` at `tests/test-humanize-escape.sh:169-192`. Template infrastructure is also covered by tests that verify all markdown templates load and critical validators use `load_and_render_safe`, with relevant references in `tests/test-template-references.sh:172-199` and `tests/test-templates-comprehensive.sh`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 Item Evidence section present for the single assigned item
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`