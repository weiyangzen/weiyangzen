# agent_007 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-007 `directory` `tests`
- cursor: `[_]`
- core_role: The `tests` directory is the repository’s executable and fixture-backed validation surface for the Humanize/RLCR and PR-loop algorithms. It is not production code, but it is core algorithm evidence because it validates the loop state machine, hook gating, plan-file invariants, PR-review polling/classification, template rendering, monitor phase detection, timeout behavior, and security bypass handling. Recursive inspection found top-level shell test suites, shared helpers, PR-loop fixtures, a mock `gh` CLI, and robustness suites under `tests/robustness`. The central runner is `tests/run-all-tests.sh`, which orders the suite list at lines 32-73, has a special zsh execution lane at lines 75-118, strips ANSI output for count parsing at lines 121-125, and aggregates pass/fail suite outcomes at lines 127-150.
- algorithmic_behavior: The directory validates several coordinated algorithms. First, the RLCR hook/state algorithm is exercised through tests around `hooks/lib/loop-common.sh`: hook JSON validation rejects null bytes, invalid UTF-8, malformed JSON, and missing tool names (`hooks/lib/loop-common.sh:59`), active-loop discovery chooses the newest timestamped session and accepts `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh:156`), strict state parsing rejects missing frontmatter and required fields (`hooks/lib/loop-common.sh:255`), review issue extraction scans an entire Codex review log for `[P0-9]` markers in the first ten characters of each line (`hooks/lib/loop-common.sh:349`), and exit handling renames state files by reason (`hooks/lib/loop-common.sh:1119`). Second, hook guard tests validate file access policy: `tests/test-plan-file-hooks.sh` creates a loop with quoted YAML state (`tests/test-plan-file-hooks.sh:37`) and checks plan-file validators, write/edit blocking, bash command modification blocking, command substitution/glob/brace/piped bypasses, and legacy path non-matching (`tests/test-plan-file-hooks.sh:93`, `tests/test-plan-file-hooks.sh:200`, `tests/test-plan-file-hooks.sh:234`, `tests/test-plan-file-hooks.sh:278`, `tests/test-plan-file-hooks.sh:1042`). Third, setup validation tests check path safety and CLI parsing: absolute paths, nonexistent parents, metacharacters, symlinks, unreadable path resolution, insufficient plan content, HTML/comment-only files, `--plan-file` exclusivity, and Codex model/effort safety are covered in `tests/test-plan-file-validation.sh:89`, `tests/test-plan-file-validation.sh:180`, `tests/test-plan-file-validation.sh:189`, `tests/test-plan-file-validation.sh:530`, and `tests/test-plan-file-validation.sh:667`. Fourth, PR-loop tests validate mutual exclusion, reviewer status cases, bot reaction detection, startup case classification, goal-tracker creation/update, trigger comment enforcement, and stop-hook behavior in `tests/test-pr-loop-system.sh:110`, `tests/test-pr-loop-system.sh:152`, `tests/test-pr-loop-system.sh:199`, `tests/test-pr-loop-system.sh:493`, `tests/test-pr-loop-system.sh:1315`, `tests/test-pr-loop-system.sh:1384`, and `tests/test-pr-loop-system.sh:1453`. Fifth, template tests validate single-pass rendering, missing-template fallbacks, reference existence, CJK/special-character handling, and syntax constraints; the underlying renderer intentionally does not recursively expand placeholder-looking values (`hooks/lib/template-loader.sh:58`). Sixth, monitor tests validate session discovery, terminal reset, PR phase inference, file-growth detection, bash/zsh behavior, and graceful stop when `.humanize` directories disappear; the shared monitor helpers use `find` instead of shell globs to avoid zsh no-match errors (`scripts/lib/monitor-common.sh:40`) and detect state/terminal phases at `scripts/lib/monitor-common.sh:154` and `scripts/lib/monitor-common.sh:267`.
- inputs_outputs_state: Inputs are shell scripts, JSON hook payloads, YAML-frontmatter `state.md` files, markdown plan/summary/goal-tracker files, git repository state, mocked GitHub API responses, transcript JSONL, terminal dimensions, and environment variables such as `CLAUDE_PROJECT_DIR`, `MOCK_GH_*`, `PATH`, and model/timeout flags. Outputs are exit codes, pass/fail/skip counters, block JSON or block text from validators, generated temporary loop directories, renamed terminal state files such as complete/cancel/maxiter/approve state files, markdown review/comment summaries, parsed JSON from PR status scripts, and monitor display phases. Shared state setup is centralized partly in `tests/test-helpers.sh`, whose counters and summary function are at lines 22-78 and whose temporary git repo initializer is at lines 86-105. The PR fixture layer is split between `tests/fixtures/*.json` and `tests/mocks/gh`: the mock routes `gh pr view`, `gh pr comment`, `gh api`, `gh repo view`, and `gh auth status` to deterministic JSON at `tests/mocks/gh:27`, `tests/mocks/gh:101`, and `tests/mocks/gh:194`. A smaller fixture-backed generator for fetch/poll tests emits a mock `gh` executable into a caller-provided bin directory (`tests/setup-fixture-mock-gh.sh:16`, `tests/setup-fixture-mock-gh.sh:27`). State transitions under test include active RLCR and PR sessions, final states, finalize phase, cancel authorization via signal files, max-iteration handling, PR approval, dynamic PR startup cases after new commits, and goal-tracker idempotent repair. Transcript todo state is parsed by `hooks/check-todos-from-transcript.py`, which scans multiple Claude Code transcript shapes for the latest `TodoWrite` call (`hooks/check-todos-from-transcript.py:20`) and returns incomplete todos as a hard stop (`hooks/check-todos-from-transcript.py:118`).
- gates_or_invariants: The directory enforces these invariants through tests: only relative, in-repo, non-symlink, sufficiently substantive plan files may start an RLCR loop; plan paths and Codex model/effort strings must avoid YAML/shell-unsafe input; RLCR and PR loops are mutually exclusive; `.humanize/rlcr` is the active protected loop namespace while legacy `.humanize-loop.local` is intentionally not protected; plan backups, state files, prompts, summaries, goal trackers, and todos have round/location rules; broad `git add` operations that would commit `.humanize` state are blocked; command patterns that write via redirect, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv`, `cp`, `rm`, `dd`, `truncate`, `printf >`, or `exec >` are considered modification attempts (`hooks/lib/loop-common.sh:1071`); `git_adds_humanize` splits chained shell segments and blocks direct `.humanize`, force broad scope, and all-scope adds when appropriate (`hooks/lib/loop-common.sh:930`); PR bot names are mapped consistently between shorthand and GitHub authors (`hooks/lib/loop-common.sh:727`); PR goal-tracker updates are idempotent by round plus reviewer (`hooks/lib/loop-common.sh:1184`); monitor state-file detection prioritizes active `state.md` over terminal `*-state.md` (`scripts/lib/monitor-common.sh:154`); template rendering is single-pass to prevent placeholder injection (`hooks/lib/template-loader.sh:54`); and test output parsing must work across GNU/BSD sed by stripping ANSI before extracting pass/fail counts (`tests/run-all-tests.sh:121`).
- dependencies_and_callers: The `tests` directory coordinates with `hooks/`, `scripts/`, `prompt-template/`, `commands/`, and `agents/`. Top-level suites invoke production hook validators such as `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `hooks/pr-loop-stop-hook.sh`. They source common libraries `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, and `scripts/lib/monitor-common.sh`. Setup and PR tests call `scripts/setup-rlcr-loop.sh`, `scripts/setup-pr-loop.sh`, `scripts/cancel-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, `scripts/poll-pr-reviews.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, `scripts/validate-gen-plan-io.sh`, `scripts/humanize.sh`, and `scripts/portable-timeout.sh`. Template-reference tests scan prompt templates in `prompt-template/block`, `prompt-template/claude`, `prompt-template/codex`, `prompt-template/plan`, and `prompt-template/pr-loop`; PR goal-tracker schema tests directly inspect `prompt-template/pr-loop/goal-tracker-initial.md` (`tests/test-pr-loop-system.sh:642`). `tests/test-pr-loop.sh` is a module runner that sources `tests/test-pr-loop-lib.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-hooks.sh`, and `tests/test-pr-loop-stophook.sh` (`tests/test-pr-loop.sh:19`). `tests/test-pr-loop-lib.sh` provides shared PR test environment and mock creation, while `tests/test-pr-loop-hooks.sh` and `tests/test-pr-loop-stophook.sh` contain the high-volume hook/stop-hook scenarios.
- edge_cases_or_failure_modes: Edge cases are extensive. Plan validation covers absolute paths, nonexistent files and directories, spaces, shell metacharacters, symlinks, inaccessible path resolution, submodule/outside-repo paths, untracked/modified tracked-plan behavior, blank/comment-only/sparse plans, single-line HTML comments, conflicting positional and `--plan-file` arguments, and unsafe Codex model/effort strings. Hook robustness covers malformed JSON, missing fields, null bytes, invalid UTF-8, deep nesting, binary-like input, path traversal into protected state, command injection, command substitution, glob expansion, brace expansion, piped writes, quoted paths, YAML-quoted fields, missing plan backups, modified/deleted plan files, incomplete goal trackers, missing placeholders, wrong-round PR resolve files, and legacy-path false positives. State robustness covers corrupted YAML, missing `current_round`, missing `max_iterations`, missing `review_started`, non-numeric rounds, invalid booleans, partial writes, concurrent readers, stale sessions, session deletion, permission changes, and rapid create/delete cycles. PR robustness covers API failures, rate limiting, malformed JSON, bot comments from issue comments, inline review comments, formal PR reviews, reaction lookup, after filters, case 1/2/3/4/5 startup states, force-push ancestry, unpushed commits, missing trigger comment IDs, bot timeout removal, codex thumbs-up approval, Claude eyes timeout, fork PR base repo resolution, mixed bot approval, and goal-tracker partial update repair. Monitor robustness covers zsh glob failures, terminal scroll-region restoration, SIGINT/SIGTERM cleanup, deleted `.humanize` directories, no-state sessions, file-growth detection for Codex analysis, and `--once` PR monitor behavior.
- validation_or_tests: Validation is the purpose of this directory. The full suite is orchestrated by `tests/run-all-tests.sh` and includes 38 ordered suites in the runner (`tests/run-all-tests.sh:32`). Important top-level validation groups include template loader/error/reference/comprehensive tests, todo checker tests, plan validation and plan-file hook tests, bash validator pattern tests, allowlist validator tests, state-exit naming tests, finalize phase tests, Codex review issue extraction tests, cancel signal file tests, `.humanize` escape tests, zsh monitor safety tests, monitor runtime and real E2E tests, gen-plan command structure tests, and PR-loop script/hook/system tests. The robustness subdirectory adds focused stress tests for base branch detection, cancel security, concurrent state access, git operations, goal tracker parsing, hook input parsing, hook system behavior, path validation, plan file validation, PR loop API handling, session behavior, setup scripts, state files, state transitions, template errors, template stress, and timeout behavior. I did not execute the suite because this branch export is read-only and the tests intentionally create temporary git repos, mock binaries, `.humanize` trees, fixture mutations, and output files; running them would be environment-incompatible for this research-only assignment.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1` item evidence section above
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`