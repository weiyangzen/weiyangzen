# agent_001 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: The repository root is the full core algorithm surface for the `humanize` Claude Code plugin. It implements two coordinated automation loops: an RLCR loop where Claude implements and Codex reviews until no findings remain, and a PR loop where GitHub bot reviews are triggered, polled, normalized, and locally classified before the loop advances. The directory is not a skip candidate because its root contains the command definitions, hook registrations, shared shell libraries, loop setup scripts, runtime stop hooks, GitHub polling helpers, monitoring command, templates, agents, and validation tests that define the plugin’s algorithmic behavior.

- algorithmic_behavior: RLCR setup begins from slash command and setup script inputs, validates plan/config/path safety, rejects concurrent active loops, resolves a base branch/commit, initializes `.humanize/` loop state, goal tracker, summaries, prompts, and round metadata. The setup path is implemented primarily in `scripts/setup-rlcr-loop.sh`, including option parsing around line 120, mutual exclusion around line 240, plan path rejection for absolute paths/spaces/metacharacters/symlinks around lines 320-346, plan policy validation around line 427, minimum plan content checks around line 483, base branch and base commit capture around lines 594 and 653, state write around line 709, tracker creation around line 737, and round-0 prompt creation around line 862. Runtime RLCR progression is driven by `hooks/loop-codex-stop-hook.sh`: it finds the newest active loop, detects finalization, parses state, validates branch/model/effort, enforces plan integrity, checks TodoWrite completion from transcript, blocks dirty or unpushed work when required, validates summaries and trackers, runs Codex review, parses findings, enters finalize state, increments rounds, or terminates on `COMPLETE`/`STOP` markers. Key sections include active loop and finalize detection at lines 59 and 72, strict state/model validation around lines 88-118, branch consistency at line 227, plan integrity around line 263, todo transcript gate around line 345, git status failure-closed behavior around line 406, large-file changed-line gate around line 433, clean/pushed git gates around lines 517 and 576, summary/tracker gates around lines 609 and 652, finalization around line 742, full alignment cadence around line 773, Codex review invocation around line 916, issue routing around line 999, finalize transition around line 1032, review prompt update around line 1118, `COMPLETE` handling around line 1395, and `STOP` handling around line 1471.

- inputs_outputs_state: Primary inputs are Claude Code slash commands and hook events, user plans, command flags, hook JSON payloads, Git repository state, transcript files, Codex review output, PR metadata, GitHub comments/reviews/reactions, templates, and bot configuration. Primary outputs are `.humanize/` state files, prompt files, summary files, goal trackers, review prompts, grouped PR review summaries, terminal monitor views, and hook allow/block decisions. Hook wiring is declared in `hooks/hooks.json`: `UserPromptSubmit` invokes plan validation around line 4, `PreToolUse` validators cover Write/Edit/Read/Bash around line 14, and Stop hooks invoke `loop-codex-stop-hook.sh` plus `pr-loop-stop-hook.sh` with long timeouts around line 52. Shared state handling is centralized in `hooks/lib/loop-common.sh`, including hook JSON validation around line 59, nested JSON rejection around line 119, newest active RLCR loop selection around line 156, strict YAML state parsing around line 255, Codex finding detection via `[P0-9]` markers around line 349, cancellation authorization around line 551, `.humanize` git-add protection around line 930, protected-file shell modification detection around line 1071, loop terminal state rename around line 1119, and idempotent PR goal tracker updates around line 1168. Template rendering is handled by `hooks/lib/template-loader.sh`, with single-pass `{{VAR}}` substitution around line 58, fallback rendering around line 170, and required-template validation around line 208.

- gates_or_invariants: The root enforces a strict single-active-loop invariant across RLCR and PR workflows, schema-validated state files, branch consistency, fixed review base behavior, protected `.humanize` internals, immutable prompt/state/tracker artifacts except at explicit lifecycle points, clean working tree before Codex/PR advancement, optional push-every-round enforcement, completed TodoWrite state before review, large changed-file limits, plan backup integrity, and review-result parsing that treats recognized findings as continuation and no findings as finalization. Pre-tool validators reinforce these invariants before Claude tools run: `hooks/loop-read-validator.sh` validates input and blocks unauthorized todo/prompt/summary reads around line 25; `hooks/loop-write-validator.sh` blocks todos/prompts/state/finalize-state/goal tracker/summaries around lines 55, 65, 152, 192, and 202; `hooks/loop-edit-validator.sh` applies similar edit protections around line 24; `hooks/loop-bash-validator.sh` applies RLCR checks around line 74, blocks unauthorized pushes around line 96, blocks state edits around lines 139-149, detects move/copy/source bypasses and shell wrappers around line 171, blocks plan backup mutation around line 336, goal tracker writes around line 349, summary writes around line 375, and PR-loop protected files around line 403. Prompt-submit validation in `hooks/loop-plan-file-validator.sh` strict-parses loop state, enforces branch consistency, and checks tracked-plan invariants around lines 44, 95, and 123.

- dependencies_and_callers: Callers are Claude Code command markdown files, Claude Code hook lifecycle events, local shell execution, Codex CLI review/classification runs, Git/GitHub CLI, `jq`, Python helpers, and monitor commands. PR setup in `scripts/setup-pr-loop.sh` parses bot/model/timeouts around line 110, requires at least one bot around line 179, enforces mutual exclusion around line 220, requires git/`gh`/Codex around lines 243-271, resolves fork-aware repository identity around line 295, rejects merged/closed PRs around line 340, fetches startup comments around line 411, determines startup cases around line 418, posts trigger comments and verifies reactions around line 443, writes state around line 535, creates PR goal tracker around line 561, and creates round-0 prompt around line 641. PR runtime logic in `hooks/pr-loop-stop-hook.sh` parses current and legacy state formats around line 87, resolves base repo around line 204, exits on merged/closed PR around line 244, requires round summaries around line 262, checks clean/pushed git around lines 283 and 313, handles force-push ancestry around line 370, verifies Codex and Claude reactions around lines 445 and 757, detects latest trigger comments around line 530, determines trigger requirements around line 684, polls all configured bots around line 795, anchors timeout polling around line 897, auto-removes timed-out bots around line 1057, writes grouped comments around line 1172, invokes local Codex classification around line 1250, handles approval/waiting/usage-limit outcomes around lines 1355-1391, re-adds previously approved bots when new issues appear around line 1411, and rewrites incremented PR state around line 1545. GitHub helper scripts include fork-aware comment fetch/retry/normalization/sorting in `scripts/fetch-pr-comments.sh` around lines 126, 182, 255, 325, and 333; polling and bot-author filtering in `scripts/poll-pr-reviews.sh` around lines 122, 209, 299, and 316; startup case determination in `scripts/check-pr-reviewer-status.sh` around lines 22, 139, 153-161, and 217; reaction checks in `scripts/check-bot-reactions.sh` around lines 87 and 187; and portable timeout selection in `scripts/portable-timeout.sh` around lines 10 and 33.

- edge_cases_or_failure_modes: The implementation explicitly handles malformed hook JSON, null bytes, invalid UTF-8, invalid or deeply nested JSON, missing `tool_name`, malformed YAML state, stale or wrong-round prompt/summary paths, invalid plan paths, symlink escapes, `../` escapes, missing/short plans, concurrent active loops, wrong branch, changed tracked plan, incomplete TodoWrite items, dirty git status, git status command failure, oversized changed files, failed or empty Codex review output, review findings, max rounds/finalization transition, unauthorized cancellation, unauthorized `.humanize` staging, shell redirection or wrapper bypass attempts, protected-file edits through Bash, stale trigger comments, force pushes, fork PR base-repo resolution, missing bot responses, per-bot timeouts, bot usage-limit states, merged/closed PRs, and reaction verification failures. The todo checker in `hooks/check-todos-from-transcript.py` parses the latest `TodoWrite` call and treats any non-`completed` status as incomplete, with parsing and failure behavior centered around lines 20 and 118. Monitoring logic in `scripts/humanize.sh` exposes `humanize monitor rlcr|pr` around line 884, reads cached Codex logs and RLCR state/git/goal context around lines 219, 246, 330, 374, and 620, and displays PR phase/bot/issue status around lines 930, 968, 1031, 1063, and 1215. Shared monitor helpers in `scripts/lib/monitor-common.sh` detect latest sessions, state files, PR phase, goal tracker state, and PR tracker state around lines 40, 154, 267, 390, and 468.

- validation_or_tests: Validation coverage is repository-local and broad, but tests were inspected rather than executed because this branch export is read-only/restricted and git validation was unreliable in the sandbox. `tests/run-all-tests.sh` lists core and robustness suites around line 32. `tests/test-pr-loop-system.sh` covers PR loop mutual exclusion around line 110, reviewer startup cases around line 152, Codex thumbs-up behavior around line 199, Claude eyes reactions around line 223, and additional cases for phase detection, force pushes, idempotent goal tracking, and fork PRs. `tests/test-plan-file-validation.sh` covers absolute paths, missing directories, spaces, metacharacters, symlinks, git state, and tracking behavior around line 89. `tests/test-todo-checker.sh` covers invalid input, empty input, missing transcript, incomplete/in-progress todos, multiple `TodoWrite` calls, and alternate transcript formats around line 51. Gen-plan support is also guarded: `commands/gen-plan.md` defines a six-phase workflow around line 30, `scripts/validate-gen-plan-io.sh` validates input/output/template behavior around lines 79, 88, 96, 111, 119, 142, and 148, and `agents/draft-relevance-checker.md` constrains relevance output to `RELEVANT:` or `NOT_RELEVANT:` around line 27.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 item evidence section above`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`