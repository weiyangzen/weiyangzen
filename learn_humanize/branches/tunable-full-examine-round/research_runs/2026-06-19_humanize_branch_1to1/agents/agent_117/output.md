# agent_117 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-117 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: This file is a Codex prompt block that defines the handoff algorithm for goal-tracker state updates after Round 0. Its purpose is to make Codex, not Claude, the actor responsible for reviewing and applying requested changes to `goal-tracker.md` when Claude includes a "Goal Tracker Update Request" in its summary. The assigned file starts with the role declaration at `prompt-template/codex/goal-tracker-update-section.md:1`, states the post-Round-0 authority boundary at `:3`, and enumerates the allowed update actions at `:5-17`.
- algorithmic_behavior: The block encodes a review-and-transition procedure rather than executable shell logic. The state transition sequence is: detect a Claude summary section named "Goal Tracker Update Request"; evaluate whether the requested change is justified and serves the Ultimate Goal; if approved, update `@{{GOAL_TRACKER_FILE}}`; if rejected, explain the rejection in the review. The concrete approval path is listed at `prompt-template/codex/goal-tracker-update-section.md:5-11`. The approved mutations are constrained to mutable goal-tracker regions: move tasks between Active, Completed, and Deferred sections; append Plan Evolution Log entries with round and justification; add newly discovered Open Issues; and never touch the immutable Ultimate Goal / Acceptance Criteria section (`:6-10`). The common request classes are task completion, new issues, plan changes, and deferrals (`:13-17`).
- inputs_outputs_state: Input is the rendered prompt block plus the current review context. The only template variable in this file is `{{GOAL_TRACKER_FILE}}`, used at `prompt-template/codex/goal-tracker-update-section.md:6` to point Codex at the concrete loop goal tracker. That variable is supplied by `hooks/loop-codex-stop-hook.sh:770-771` when the hook renders `codex/goal-tracker-update-section.md`. The upstream human/Claude-side request format is defined in `prompt-template/claude/goal-tracker-update-request.md:2-16`, where Claude is told to include requested changes and justification in its summary. Output is either a modified goal tracker, if Codex approves the request, or a review note explaining rejection. State transitions target the goal tracker schema created by `scripts/setup-rlcr-loop.sh:765-854`, especially the immutable section (`:779-817`) and mutable sections for Plan Evolution Log, Active Tasks, Completed and Verified, Explicitly Deferred, and Open Issues (`:823-854`).
- gates_or_invariants: The central invariant is that the immutable section must not be modified; the assigned template makes this explicit at `prompt-template/codex/goal-tracker-update-section.md:10`, and the generated goal tracker repeats the invariant in comments at `scripts/setup-rlcr-loop.sh:772-780`. Another invariant is authority separation after Round 0: Claude is not supposed to directly edit `goal-tracker.md`; it must request changes, while Codex evaluates them. This is reinforced by hook gates: edit operations on goal-tracker paths are blocked after Round 0 in `hooks/loop-edit-validator.sh:141-149`, write operations are blocked after Round 0 in `hooks/loop-write-validator.sh:188-196`, and bash modifications are blocked in all rounds with different messages for Round 0 versus later rounds in `hooks/loop-bash-validator.sh:344-358`. Deferrals are treated as a high-friction transition: the template says to allow them only with strong justification at `prompt-template/codex/goal-tracker-update-section.md:17`, and the normal goal tracker schema requires explicit justification for deferred items at `scripts/setup-rlcr-loop.sh:845-848`.
- dependencies_and_callers: The direct caller is `hooks/loop-codex-stop-hook.sh`, which loads this block as `GOAL_TRACKER_UPDATE_SECTION` with `load_and_render_safe` at `:767-771`. The same rendered section is injected into both review prompt paths: full alignment review via `GOAL_TRACKER_UPDATE_SECTION=$GOAL_TRACKER_UPDATE_SECTION` at `hooks/loop-codex-stop-hook.sh:818-829`, and regular review at `:834-846`. The receiving templates place it as Part 3 in `prompt-template/codex/full-alignment-review.md:57` and `prompt-template/codex/regular-review.md:47`. The template renderer dependency is `hooks/lib/template-loader.sh`; it defines `{{VARIABLE_NAME}}` syntax and missing-variable behavior at `:7-14`, performs single-pass replacement to avoid placeholder injection at `:50-58` and `:71-131`, and provides fallback behavior for missing or empty templates through `load_and_render_safe` at `:170-203`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `hooks/loop-codex-stop-hook.sh` supplies a fallback block at `:767-771`; the fallback still tells Codex to apply requested changes to the goal tracker, but it is much less precise than the assigned template because it omits the immutable-section prohibition, allowed mutation categories, and deferral standard. If `GOAL_TRACKER_FILE` is not supplied, the loader keeps unresolved placeholders as-is by design (`hooks/lib/template-loader.sh:12-13` and `:115-122`), so Codex would see a literal `{{GOAL_TRACKER_FILE}}` path reference instead of a usable file target. If Claude does not include a request section, this block imposes no goal-tracker mutation. If Claude requests an unjustified change, the output is rejection rationale, not a state change (`prompt-template/codex/goal-tracker-update-section.md:11`). If Claude requests changes to the immutable section, the template explicitly forbids applying them (`:10`). A further process edge case is that the prompt authorizes Codex to update the goal tracker, but repository hook gates broadly block goal-tracker edits after Round 0 for edit/write tools and shell commands; operationally this relies on the Codex review workflow having an allowed path or bypass context for Codex-controlled updates.
- validation_or_tests: Template rendering coverage exists in `tests/test-templates-comprehensive.sh:531-539`, which loads `codex/goal-tracker-update-section.md` with `GOAL_TRACKER_FILE=.humanize/rlcr/20240101/goal-tracker.md` and asserts that the rendered output includes both "Goal Tracker Update Requests" and the substituted path. General template-loader tests cover fallback behavior for `load_and_render_safe` in `tests/test-template-loader.sh:197-221`, and template-reference tests check safe rendering usage patterns in `tests/test-template-references.sh:172-199`. I did not run the test suite because this branch export is read-only research-only and the requirement was to inspect and report, not modify or validate by execution.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`