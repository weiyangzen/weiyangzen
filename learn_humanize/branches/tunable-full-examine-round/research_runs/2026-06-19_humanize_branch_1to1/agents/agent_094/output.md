# agent_094 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-094 `file` `prompt-template/block/pr-loop-state-modification.md`
- cursor: `[_]`
- core_role: This is a block-message template for PR loop state protection. It is not executable logic itself; it defines the human-facing rejection reason when an agent attempts to modify `.humanize/pr-loop/.../state.md`. The template states that the state file is system-managed and contains the current round, PR number/branch, active bot configuration, Codex configuration, and polling settings, and that modification would corrupt PR loop state at `prompt-template/block/pr-loop-state-modification.md:1` through `prompt-template/block/pr-loop-state-modification.md:12`.
- algorithmic_behavior: The file participates in the hook algorithm as the rendered denial message for a protection gate. `hooks/lib/loop-common.sh` defines `pr_loop_state_blocked_message`, whose normal path loads `block/pr-loop-state-modification.md` through `load_and_render_safe`; if template loading fails, it falls back to an inline shorter message at `hooks/lib/loop-common.sh:889` through `hooks/lib/loop-common.sh:896`. The Write validator detects paths inside `.humanize/pr-loop/`, checks whether the target ends in `state.md`, renders this block, and exits with status 2 at `hooks/loop-write-validator.sh:74` through `hooks/loop-write-validator.sh:80`. The Edit validator follows the same transition at `hooks/loop-edit-validator.sh:57` through `hooks/loop-edit-validator.sh:63`. The Bash validator blocks shell commands that modify PR loop state, including full `.humanize/pr-loop/.../state.md` paths and bare `state.md` commands when an active PR loop exists, at `hooks/loop-bash-validator.sh:403` through `hooks/loop-bash-validator.sh:415`.
- inputs_outputs_state: Inputs are indirect: hook JSON for `Write` or `Edit` supplies `tool_input.file_path`, and Bash hook JSON supplies a shell command inspected by command-pattern logic. The relevant state input is path classification: `is_in_pr_loop_dir` matches paths containing `.humanize/pr-loop/` at `hooks/lib/loop-common.sh:783` through `hooks/lib/loop-common.sh:787`, and `is_state_file_path` matches any lowercase path ending in `state.md` at `hooks/lib/loop-common.sh:509` through `hooks/lib/loop-common.sh:513`. Output is the rendered Markdown block on stderr, paired with hook exit code 2 from the validators. The protected state is not mutated; the algorithmic transition is from an attempted write/edit/bash mutation to a blocked operation.
- gates_or_invariants: The invariant is that PR loop `state.md` remains owned by the PR loop system, not by agent tools. The template documents why: `state.md` stores run-critical fields such as round number, PR identity/branch, bot configuration, Codex settings, and polling settings at `prompt-template/block/pr-loop-state-modification.md:5` through `prompt-template/block/pr-loop-state-modification.md:10`. The gate is fail-closed for direct Write/Edit tool operations under `.humanize/pr-loop/` and for Bash mutation patterns while an active PR loop is detected. Bash detection uses `command_modifies_file`, which recognizes redirects, `tee`, in-place `sed`/`awk`/`perl`, `mv`, `cp`, `rm`, `dd`, and `truncate` patterns at `hooks/lib/loop-common.sh:1068` through `hooks/lib/loop-common.sh:1085`.
- dependencies_and_callers: The template depends on the common template loader path established in `hooks/lib/loop-common.sh`, where `template-loader.sh` is sourced and `TEMPLATE_DIR` is initialized at `hooks/lib/loop-common.sh:139` through `hooks/lib/loop-common.sh:148`. Its direct caller is `pr_loop_state_blocked_message` in `hooks/lib/loop-common.sh:889` through `hooks/lib/loop-common.sh:896`. Indirect callers are `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, and `hooks/loop-bash-validator.sh`. Related PR loop helper functions include `find_active_pr_loop`, which selects the newest PR loop directory containing `state.md`, at `hooks/lib/loop-common.sh:795` through `hooks/lib/loop-common.sh:814`; this matters for Bash protection, which only runs the PR loop state Bash gate when `ACTIVE_PR_LOOP_DIR` is set.
- edge_cases_or_failure_modes: Because `is_state_file_path` matches any path ending in `state.md`, Write/Edit protection inside `.humanize/pr-loop/` also blocks nested or oddly named PR loop paths that end with `state.md`, which is conservative. Bash protection additionally checks bare `state.md` to catch relative-path bypasses such as changing into a PR loop directory before running `sed -i state.md`, as documented at `hooks/loop-bash-validator.sh:405` through `hooks/loop-bash-validator.sh:414`. A possible false-positive edge case exists in Bash mode: with any active PR loop, a shell command modifying an unrelated bare `state.md` may be blocked because the bare-name check is intentionally broad. If the template file is missing or invalid, the fallback in `pr_loop_state_blocked_message` still blocks the operation, but with a less detailed explanation.
- validation_or_tests: `tests/test-pr-loop-hooks.sh` includes `test_pr_loop_state_protected`, which creates `.humanize/pr-loop/<timestamp>/state.md`, invokes the Write validator against that path, and expects exit code 2 plus output matching state/protection wording at `tests/test-pr-loop-hooks.sh:68` through `tests/test-pr-loop-hooks.sh:97`. This validates the caller behavior for the template-backed denial path, though it asserts broadly on message content rather than the exact Markdown body. I did not run the test suite; this research pass was read-only and limited to direct inspection.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 Item Evidence section
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`