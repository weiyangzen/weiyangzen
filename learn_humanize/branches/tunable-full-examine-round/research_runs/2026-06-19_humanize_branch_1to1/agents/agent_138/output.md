# agent_138 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-138 `file` `tests/robustness/test-session-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR session discovery. The file validates the contract of `find_active_loop`, which is implemented in `hooks/lib/loop-common.sh:156` and used by loop setup scripts and hook validators to decide whether an RLCR loop is active. This test is core-adjacent rather than production algorithm code, but it encodes important invariants for active-session selection and zombie-loop prevention.

- algorithmic_behavior: The script is a Bash test harness with strict mode enabled at `tests/robustness/test-session-robustness.sh:12`. It computes `SCRIPT_DIR` and `PROJECT_ROOT`, sources `hooks/lib/loop-common.sh`, sources shared test helpers, then creates an isolated temporary test root through `setup_test_dir` at `tests/robustness/test-session-robustness.sh:14-19`. The core behavior under test is `find_active_loop "$base"`, whose implementation returns an empty string if the base path is missing, otherwise runs `ls -1d "$loop_base_dir"/*/ | sort -r | head -1` to select only the lexicographically newest immediate child directory, then returns that child only when it contains `state.md` or `finalize-state.md` (`hooks/lib/loop-common.sh:156-174`). The test cases build temporary `.humanize/rlcr`-like directory shapes and compare the function output to the expected path or empty string.

- inputs_outputs_state: Inputs are filesystem layouts under `$TEST_DIR/rlcr`, especially child directory names and state marker files. The script creates, deletes, and recreates these layouts repeatedly with `mkdir -p`, `touch`, `rm -rf`, and one `ln -s` symlink case. Outputs are TAP-like console messages via `pass` and `fail`, plus a final process exit code from `print_test_summary` at `tests/robustness/test-session-robustness.sh:322-323`. Test state is held in `TESTS_PASSED`, `TESTS_FAILED`, and `TESTS_SKIPPED` counters from `tests/test-helpers.sh:22-24`; `pass` increments passed count, `fail` increments failed count, and summary returns nonzero if any failure occurred (`tests/test-helpers.sh:30-77`). The temporary directory is registered for cleanup via `trap "rm -rf $TEST_DIR" EXIT` in `tests/test-helpers.sh:86-88`.

- gates_or_invariants: The main invariant is newest-only active-loop selection: older directories are intentionally ignored even if they contain `state.md`, as tested at `tests/robustness/test-session-robustness.sh:89-103` and again with a newer finished directory at `tests/robustness/test-session-robustness.sh:287-302`. This matches the implementation comment that the behavior prevents zombie loops from being revived (`hooks/lib/loop-common.sh:151-155`). Active markers are limited to `state.md` and `finalize-state.md`; `complete-state.md` and `cancel-state.md` are not active markers, tested at `tests/robustness/test-session-robustness.sh:259-285`. Discovery is one-level only, not recursive, tested by creating a deep nested `a/b/c/.../state.md` and accepting empty or shallow output at `tests/robustness/test-session-robustness.sh:224-238`. Directory naming is not semantically validated by `find_active_loop`: non-timestamp names and names with spaces can be selected if they sort newest and contain an active marker, tested at `tests/robustness/test-session-robustness.sh:179-192` and `210-222`. The performance gate requires detection across 15 directories to complete under 1000 ms (`tests/robustness/test-session-robustness.sh:63-87`), which is a coarse regression guard for shell listing behavior.

- dependencies_and_callers: The assigned file depends directly on `hooks/lib/loop-common.sh` for `find_active_loop` (`tests/robustness/test-session-robustness.sh:16`) and on `tests/test-helpers.sh` for temporary-directory setup, pass/fail reporting, and summary exit status (`tests/robustness/test-session-robustness.sh:17-19`). `find_active_loop` itself is a shared primitive for RLCR loop mutual exclusion and hook safety. `scripts/setup-rlcr-loop.sh` sources the shared library and refuses to start a second active RLCR or PR loop when `find_active_loop "$PROJECT_ROOT/.humanize/rlcr"` returns a path (`scripts/setup-rlcr-loop.sh:232-252`). `scripts/setup-pr-loop.sh` performs the same mutual-exclusion check before starting a PR loop (`scripts/setup-pr-loop.sh:210-230`). Hook validators use the active loop path to choose whether to enforce RLCR file and command constraints: `hooks/loop-read-validator.sh:55-63` blocks reads of todos unless allowlisted by the active loop, and `hooks/loop-read-validator.sh:80-99` parses the active loop state file; `hooks/loop-bash-validator.sh:54-85` detects active RLCR and PR loops and fail-closes on malformed active-loop state. Session creation is performed by `scripts/setup-rlcr-loop.sh`, which creates timestamped loop directories and writes the initial `state.md` (`scripts/setup-rlcr-loop.sh:667-725`).

- edge_cases_or_failure_modes: The script covers missing base directories (`tests/robustness/test-session-robustness.sh:140-148`), empty bases (`128-138`), bases with files but no child directories (`150-162`), child directories with no active state files (`164-177`), unexpected directory names (`179-192`), symlinked session directories (`194-208`), spaces in names (`210-222`), deep nested state files outside the one-level search boundary (`224-238`), rapid creation of multiple sessions (`240-257`), finished or cancelled markers only (`259-285`), and a mixed newest-finished/older-active layout (`287-302`). Two notable gaps or inconsistencies: the file header says it tests permission changes at `tests/robustness/test-session-robustness.sh:8`, but the script does not perform `chmod` or unreadable-directory cases; and Test 18 is titled “Unicode characters” but creates `session-test`, not a Unicode directory name (`tests/robustness/test-session-robustness.sh:304-316`). The symlink test only asserts a nonempty result, not which path is returned, so it verifies tolerance more than deterministic symlink semantics (`tests/robustness/test-session-robustness.sh:202-208`). Because the underlying implementation uses `ls | sort -r | head -1`, lexical path ordering is the algorithmic source of truth; timestamp semantics only hold if directory names follow sortable timestamp format.

- validation_or_tests: The assigned file is itself a standalone validation script. It contains 18 numbered test scenarios and exits with the shared summary status. I inspected it directly with line numbers and followed its implementation dependency to `hooks/lib/loop-common.sh:156-174`, plus the helper functions in `tests/test-helpers.sh:30-88`. I did not modify files. I did not run the script because this worker branch export is read-only and the test creates temporary directories and files; the evidence here is static analysis of the executable specification and its referenced implementation.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-138`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`