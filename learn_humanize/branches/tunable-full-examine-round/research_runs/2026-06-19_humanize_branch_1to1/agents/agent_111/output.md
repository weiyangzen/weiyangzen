# agent_111 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-111 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: This file is the Claude next-round prompt template used by the RLCR loop after Codex review finds unresolved work. It is not an executable algorithm itself, but it is part of the loop-control algorithm because it defines the handoff contract from Codex review feedback into the next Claude implementation round. The template tells Claude that work is unfinished, anchors the round to the original implementation plan, injects Codex’s review result, and constrains goal-tracker handling. Direct file evidence: it reads the original plan via `@{{PLAN_FILE}}` at `prompt-template/claude/next-round-prompt.md:3-8`, requires todos for all discovered tasks at `prompt-template/claude/next-round-prompt.md:12-13`, injects `{{REVIEW_CONTENT}}` at `prompt-template/claude/next-round-prompt.md:16-19`, and references the goal tracker as read-only after Round 0 at `prompt-template/claude/next-round-prompt.md:22-31`.

- algorithmic_behavior: The active transition is implemented by the caller, `hooks/loop-codex-stop-hook.sh`. After Codex review output is read from `REVIEW_RESULT_FILE`, the hook checks the last non-empty line for `COMPLETE` or `STOP` markers at `hooks/loop-codex-stop-hook.sh:1385-1392`. If the review does not terminate the loop, the hook increments `current_round` in `state.md` at `hooks/loop-codex-stop-hook.sh:1505-1508`, creates `round-${NEXT_ROUND}-prompt.md` at `hooks/loop-codex-stop-hook.sh:1510-1512`, renders this assigned template into that prompt at `hooks/loop-codex-stop-hook.sh:1514-1526`, appends optional post-alignment instructions at `hooks/loop-codex-stop-hook.sh:1528-1534`, appends the standard Claude footer at `hooks/loop-codex-stop-hook.sh:1536-1540`, appends an optional push note at `hooks/loop-codex-stop-hook.sh:1542-1549`, appends goal-tracker update-request guidance at `hooks/loop-codex-stop-hook.sh:1551-1556`, and finally blocks the stop hook with JSON whose `reason` is the generated next-round prompt at `hooks/loop-codex-stop-hook.sh:1558-1569`. In effect, this template is the first body segment of the generated next-round instruction packet.

- inputs_outputs_state: Inputs are the template variables `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE`, shown directly in the template at `prompt-template/claude/next-round-prompt.md:6`, `prompt-template/claude/next-round-prompt.md:18`, and `prompt-template/claude/next-round-prompt.md:24`. The caller supplies them to `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:1523-1526`. Output is rendered Markdown written to `$LOOP_DIR/round-${NEXT_ROUND}-prompt.md`, after which the full prompt is returned as the blocking reason to Claude. The template itself has no mutable state, but participates in state movement from “Codex found issues” to “next Claude round required”: the caller updates `current_round` before writing the prompt. The rendered prompt also establishes a per-round working state for Claude: read the plan, make todos for every task, use the injected Codex review as required work, read goal-tracker status, and request goal-tracker changes indirectly instead of editing it after Round 0.

- gates_or_invariants: The template encodes several behavioral gates. First, Claude must re-anchor on the original plan before proceeding, preventing review-feedback-only drift (`prompt-template/claude/next-round-prompt.md:3-8`). Second, Claude must create todos for all tasks and is explicitly barred from only fixing the highest-priority issues (`prompt-template/claude/next-round-prompt.md:12-13`). Third, Codex review content is isolated by HTML comment sentinels at `prompt-template/claude/next-round-prompt.md:16-19`, giving downstream readers a stable review-result block. Fourth, the goal tracker is read-only after Round 0, and updates must be requested in the summary rather than applied directly (`prompt-template/claude/next-round-prompt.md:22-31`). The caller adds further gates around this template: `COMPLETE` enters review/finalize behavior rather than generating this prompt (`hooks/loop-codex-stop-hook.sh:1394-1434`), `STOP` terminates the loop (`hooks/loop-codex-stop-hook.sh:1471-1499`), and only unresolved review feedback follows the next-round generation path (`hooks/loop-codex-stop-hook.sh:1501-1569`).

- dependencies_and_callers: The direct caller is `hooks/loop-codex-stop-hook.sh`, which loads `claude/next-round-prompt.md` through `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:1523-1526`. Rendering is provided by `hooks/lib/template-loader.sh`: templates use `{{VARIABLE_NAME}}` syntax (`hooks/lib/template-loader.sh:7-13`), template files are loaded by path under `prompt-template` (`hooks/lib/template-loader.sh:33-47`), and rendering is intentionally single-pass to avoid placeholder injection from values such as review content (`hooks/lib/template-loader.sh:50-57`, `hooks/lib/template-loader.sh:71-129`). The assigned template coordinates with sibling Claude prompt fragments: `prompt-template/claude/next-round-footer.md` supplies anti-exit, commit, optional code-simplifier, and summary instructions at `prompt-template/claude/next-round-footer.md:4-9`; `prompt-template/claude/post-alignment-action-items.md` can be appended after full-alignment checks; `prompt-template/claude/push-every-round-note.md` can be appended when push enforcement is active; and `prompt-template/claude/goal-tracker-update-request.md` is appended to define indirect goal-tracker update mechanics. The hook fallback at `hooks/loop-codex-stop-hook.sh:1515-1522` provides degraded behavior if this template is missing.

- edge_cases_or_failure_modes: If this template is missing, the caller uses `NEXT_ROUND_FALLBACK`, which preserves plan, review, and goal-tracker references but loses the stronger todo and read-only goal-tracker wording from the assigned file (`hooks/loop-codex-stop-hook.sh:1515-1526`). If `REVIEW_CONTENT` contains template-looking strings such as `{{PLAN_FILE}}`, the renderer’s single-pass design prevents them from being recursively substituted, avoiding review-content prompt corruption; this behavior is documented at `hooks/lib/template-loader.sh:54-57` and implemented at `hooks/lib/template-loader.sh:71-129`. If a supplied variable is missing, the placeholder remains literal by template-loader contract (`hooks/lib/template-loader.sh:12-13`), which may leave unusable `@{{PLAN_FILE}}` or `@{{GOAL_TRACKER_FILE}}` references in the generated prompt. If Codex review output is empty or absent, the hook fails earlier instead of rendering this template (`hooks/loop-codex-stop-hook.sh:1360-1383`). If Codex review ends with exact `COMPLETE` or `STOP`, this template is bypassed by marker handling (`hooks/loop-codex-stop-hook.sh:1388-1399`, `hooks/loop-codex-stop-hook.sh:1471-1499`). A subtle semantic edge case is that the template says the goal tracker cannot be directly modified after Round 0, but enforcement depends on hooks and adjacent prompt/footer behavior, not this Markdown file alone.

- validation_or_tests: The template loader has direct tests for placeholder-in-value injection resistance, including a realistic `REVIEW_CONTENT` scenario where `{{GOAL_TRACKER_FILE}}` and `{{PLAN_FILE}}` embedded inside review text must remain literal (`tests/test-template-loader.sh:525-543`). Comprehensive template tests cover real template rendering generally and goal-tracker template substitution, though not this exact next-round template by filename (`tests/test-templates-comprehensive.sh:530-540`). Finalize/normal-round tests validate the surrounding transition behavior: when Codex review is non-`COMPLETE`, the hook blocks exit, keeps `state.md`, increments `current_round`, and creates the review result file (`tests/test-finalize-phase.sh:775-804`). The prompt-file protection surface is also covered by hook logic and tests elsewhere: generated `round-N-prompt.md` files are treated as protected instruction files, with references in `hooks/lib/loop-common.sh:458` and prompt-file write detection in `hooks/loop-bash-validator.sh:365`.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 Item Evidence sections present; the assigned item id is used once as the section heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`