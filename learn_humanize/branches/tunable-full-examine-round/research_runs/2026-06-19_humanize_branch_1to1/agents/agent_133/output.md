# agent_133 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-133 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role:
  - This is an executable robustness specification for the project’s hook-input validation and RLCR monitor behavior. It is core-adjacent but algorithmically meaningful because it asserts the safety contracts that production PreToolUse hooks and monitor utilities must satisfy.
  - The file explicitly tests production validators by piping JSON into `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, and `hooks/loop-bash-validator.sh`, not stand-in mocks (`tests/robustness/test-hook-input-robustness.sh:5`, `tests/robustness/test-hook-input-robustness.sh:39`, `tests/robustness/test-hook-input-robustness.sh:51`, `tests/robustness/test-hook-input-robustness.sh:63`).
  - It also covers terminal/log monitor resilience and production monitor helper functions from `scripts/humanize.sh` (`tests/robustness/test-hook-input-robustness.sh:381`, `tests/robustness/test-hook-input-robustness.sh:438`).

- algorithmic_behavior:
  - The script runs under strict shell mode with `set -euo pipefail`, resolves `SCRIPT_DIR` and `PROJECT_ROOT`, sources shared hook logic plus test helpers, and creates an isolated temp test directory (`tests/robustness/test-hook-input-robustness.sh:12`, `tests/robustness/test-hook-input-robustness.sh:14`, `tests/robustness/test-hook-input-robustness.sh:16`, `tests/robustness/test-hook-input-robustness.sh:17`, `tests/robustness/test-hook-input-robustness.sh:19`).
  - Tests 1-3 establish pass-through behavior for valid JSON events for `Read`, `Write`, and `Bash`, expecting exit `0` when the target path or command is not protected (`tests/robustness/test-hook-input-robustness.sh:35`, `tests/robustness/test-hook-input-robustness.sh:47`, `tests/robustness/test-hook-input-robustness.sh:59`).
  - Tests 4-6 encode fail-closed parsing gates: invalid JSON, missing `tool_name`, and missing required `tool_input.file_path` must return nonzero but below `128`, meaning a graceful rejection rather than a signal crash (`tests/robustness/test-hook-input-robustness.sh:71`, `tests/robustness/test-hook-input-robustness.sh:87`, `tests/robustness/test-hook-input-robustness.sh:102`).
  - Tests 7-10c cover parsing stress and hostile input classes: a 10KB command, shell metacharacters, escaped Unicode path content, an unknown tool that should be ignored, 50-level nested JSON that should be rejected, non-UTF8 bytes that must return exit `1`, and null-byte handling where Bash strips null bytes before hook logic can observe them (`tests/robustness/test-hook-input-robustness.sh:117`, `tests/robustness/test-hook-input-robustness.sh:135`, `tests/robustness/test-hook-input-robustness.sh:149`, `tests/robustness/test-hook-input-robustness.sh:163`, `tests/robustness/test-hook-input-robustness.sh:177`, `tests/robustness/test-hook-input-robustness.sh:201`, `tests/robustness/test-hook-input-robustness.sh:219`).
  - Tests 11-18 model monitor/log primitive behavior: terminal width is positive, log growth is detectable, deleted logs are tolerated, ANSI codes can be stripped, binary logs can still be line-counted, narrow/wide terminal formatting works, and rapid 100-line appends remain observable (`tests/robustness/test-hook-input-robustness.sh:245`, `tests/robustness/test-hook-input-robustness.sh:254`, `tests/robustness/test-hook-input-robustness.sh:268`, `tests/robustness/test-hook-input-robustness.sh:280`, `tests/robustness/test-hook-input-robustness.sh:293`, `tests/robustness/test-hook-input-robustness.sh:305`, `tests/robustness/test-hook-input-robustness.sh:317`, `tests/robustness/test-hook-input-robustness.sh:328`).
  - Tests 19-20 directly exercise `command_modifies_file`, asserting that `sed -i` and output redirection are recognized as file-modifying shell patterns (`tests/robustness/test-hook-input-robustness.sh:351`, `tests/robustness/test-hook-input-robustness.sh:361`).
  - Tests 21-23 source `scripts/humanize.sh` and assert the production parsers are callable and return pipe-delimited numeric summary formats for goal tracker and git status, plus a recognized git state token (`tests/robustness/test-hook-input-robustness.sh:381`, `tests/robustness/test-hook-input-robustness.sh:402`, `tests/robustness/test-hook-input-robustness.sh:416`, `tests/robustness/test-hook-input-robustness.sh:428`).
  - Tests 24-26 create temporary project/session/cache structures and wrapper scripts that source `scripts/humanize.sh` and call `_humanize_monitor_codex` directly. They validate graceful no-session exit, narrow-terminal survival, and ANSI-log survival by requiring wrapper-observed monitor exit codes below `128` (`tests/robustness/test-hook-input-robustness.sh:444`, `tests/robustness/test-hook-input-robustness.sh:488`, `tests/robustness/test-hook-input-robustness.sh:580`).
  - The final state transition is delegated to `print_test_summary`; the script exits with that summary function’s status, so any accumulated `fail` call makes the whole robustness test fail (`tests/robustness/test-hook-input-robustness.sh:670`, `tests/robustness/test-hook-input-robustness.sh:671`).

- inputs_outputs_state:
  - Inputs are shell-generated JSON hook payloads sent on stdin to production hook scripts, filesystem fixtures created in temp directories, fake `HOME` and `XDG_CACHE_HOME` values, synthetic `.humanize/rlcr/<timestamp>` sessions, and log files under the expected cache path.
  - Hook payload structure under test is `{"tool_name": "...", "tool_input": {...}}`; `Read` and `Write` require `tool_input.file_path`, while `Bash` requires `tool_input.command` (`tests/robustness/test-hook-input-robustness.sh:37`, `tests/robustness/test-hook-input-robustness.sh:50`, `tests/robustness/test-hook-input-robustness.sh:62`, `hooks/loop-read-validator.sh:43`, `hooks/loop-write-validator.sh:43`, `hooks/loop-bash-validator.sh:42`).
  - Outputs are test counter mutations through `pass` and `fail`, printed diagnostics, and process exit status via `print_test_summary`. `tests/test-helpers.sh` increments `TESTS_PASSED` and `TESTS_FAILED`, then returns `0` only when failures remain zero (`tests/test-helpers.sh:22`, `tests/test-helpers.sh:30`, `tests/test-helpers.sh:35`, `tests/test-helpers.sh:58`, `tests/test-helpers.sh:71`).
  - State transitions inside the test are mostly local: strict mode is temporarily disabled with `set +e` around expected failures, then restored with `set -e`; `EXIT_CODE` is captured and checked against expected ranges (`tests/robustness/test-hook-input-robustness.sh:76`, `tests/robustness/test-hook-input-robustness.sh:79`, `tests/robustness/test-hook-input-robustness.sh:125`, `tests/robustness/test-hook-input-robustness.sh:128`).
  - The monitor integration fixtures transition from active session state to deleted session directories by backgrounding `sleep 1 && rm -rf "$PROJECT_DIR/.humanize/rlcr"`, then polling the monitor PID for up to roughly 5 seconds (`tests/robustness/test-hook-input-robustness.sh:531`, `tests/robustness/test-hook-input-robustness.sh:537`, `tests/robustness/test-hook-input-robustness.sh:541`, `tests/robustness/test-hook-input-robustness.sh:621`, `tests/robustness/test-hook-input-robustness.sh:626`, `tests/robustness/test-hook-input-robustness.sh:630`).
  - The production monitor itself transitions from session/log discovery to terminal setup, periodic status redraw, incremental log reads, deletion handling, and cleanup. It returns `1` when `.humanize/rlcr` has no sessions, and returns `0` when the loop directory disappears during monitoring (`scripts/humanize.sh:590`, `scripts/humanize.sh:595`, `scripts/humanize.sh:607`, `scripts/humanize.sh:620`, `scripts/humanize.sh:623`, `scripts/humanize.sh:624`, `scripts/humanize.sh:755`, `scripts/humanize.sh:757`).

- gates_or_invariants:
  - JSON validation must reject null bytes if visible to the function, invalid UTF-8 when `iconv` is available, invalid JSON syntax, and missing `tool_name`; it sets `VALIDATED_TOOL_NAME` and `VALIDATED_TOOL_INPUT` only after those checks pass (`hooks/lib/loop-common.sh:59`, `hooks/lib/loop-common.sh:62`, `hooks/lib/loop-common.sh:72`, `hooks/lib/loop-common.sh:79`, `hooks/lib/loop-common.sh:86`, `hooks/lib/loop-common.sh:93`).
  - Depth is capped at 30 by `is_deeply_nested`, which computes maximum JSON path length via `jq '[paths | length] | max // 0'`; the assigned test’s 50-level fixture asserts this DoS guard rejects excessive nesting (`hooks/lib/loop-common.sh:119`, `hooks/lib/loop-common.sh:125`, `hooks/lib/loop-common.sh:127`, `tests/robustness/test-hook-input-robustness.sh:177`).
  - Unknown tools are allowed through by tool-specific validators after generic JSON validation. For example, `loop-read-validator.sh` exits `0` unless `TOOL_NAME` is exactly `Read`; similarly for `Write` and `Bash` (`hooks/loop-read-validator.sh:39`, `hooks/loop-write-validator.sh:39`, `hooks/loop-bash-validator.sh:38`).
  - Required field gates are tool-specific: `Read` and `Write` require `file_path`; `Bash` requires `command` (`hooks/loop-read-validator.sh:43`, `hooks/loop-write-validator.sh:43`, `hooks/loop-bash-validator.sh:42`).
  - Hook rejection behavior distinguishes validation failures from crashes: the test treats exit `1-127` as graceful rejection and exit `>=128` as signal-level failure (`tests/robustness/test-hook-input-robustness.sh:80`, `tests/robustness/test-hook-input-robustness.sh:95`, `tests/robustness/test-hook-input-robustness.sh:110`, `tests/robustness/test-hook-input-robustness.sh:573`, `tests/robustness/test-hook-input-robustness.sh:660`).
  - `command_modifies_file` must detect common modification forms including `>`, `>>`, `tee`, `sed -i`, `awk -i inplace`, `perl -i`, `mv/cp` to destination, `rm`, `dd of=`, `truncate`, `printf >`, and `exec >` (`hooks/lib/loop-common.sh:1071`, `hooks/lib/loop-common.sh:1075`).
  - Monitor invariants include: timestamp-named sessions only, logs stored in `XDG_CACHE_HOME` or `$HOME/.cache/humanize/<sanitized-project>/<timestamp>`, safe `find` usage instead of fragile globs, and defensive rejection of inconsistent run/review log ordering (`scripts/humanize.sh:243`, `scripts/humanize.sh:250`, `scripts/humanize.sh:280`, `scripts/humanize.sh:287`, `scripts/humanize.sh:330`).
  - Terminal cleanup is an invariant for monitor exits: `_cleanup` prevents duplicate cleanup, kills a background tail if present, restores terminal state, and is invoked by `_graceful_stop` when loop directories disappear (`scripts/humanize.sh:535`, `scripts/humanize.sh:548`, `scripts/humanize.sh:558`, `scripts/humanize.sh:563`, `scripts/humanize.sh:570`).

- dependencies_and_callers:
  - Direct dependencies are `hooks/lib/loop-common.sh`, `tests/test-helpers.sh`, `scripts/humanize.sh`, the three production validators, and standard shell utilities such as `jq`, `iconv`, `sed`, `grep`, `wc`, `mktemp`, `chmod`, `tail`, `tput`, `git`, and `find`.
  - The test uses `CLAUDE_PROJECT_DIR="$TEST_DIR"` when invoking validators, forcing hook logic to resolve active loop paths against the temp project instead of the repository root (`tests/robustness/test-hook-input-robustness.sh:39`, `tests/robustness/test-hook-input-robustness.sh:51`, `tests/robustness/test-hook-input-robustness.sh:63`).
  - `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-bash-validator.sh` all source `hooks/lib/loop-common.sh` and call `validate_hook_input`; all three then call `is_deeply_nested` before tool-specific checks (`hooks/loop-read-validator.sh:17`, `hooks/loop-read-validator.sh:28`, `hooks/loop-read-validator.sh:33`, `hooks/loop-write-validator.sh:17`, `hooks/loop-write-validator.sh:28`, `hooks/loop-write-validator.sh:33`, `hooks/loop-bash-validator.sh:16`, `hooks/loop-bash-validator.sh:27`, `hooks/loop-bash-validator.sh:32`).
  - The command-pattern tests depend on `to_lower` and `command_modifies_file` from `loop-common.sh`; `to_lower` is a simple `tr` wrapper, while `command_modifies_file` is the central modification detector used by Bash hook protection (`hooks/lib/loop-common.sh:394`, `hooks/lib/loop-common.sh:1071`, `tests/robustness/test-hook-input-robustness.sh:354`, `tests/robustness/test-hook-input-robustness.sh:355`).
  - The monitor parser tests call `humanize_parse_goal_tracker`, `humanize_parse_git_status`, and `humanize_detect_git_state`; those parse markdown goal tracker sections, summarize git porcelain/diff stats, and classify repository state respectively (`scripts/humanize.sh:32`, `scripts/humanize.sh:116`, `scripts/humanize.sh:166`).
  - `_humanize_monitor_codex` depends on `scripts/lib/monitor-common.sh` through `humanize.sh`; relevant helpers include `monitor_find_latest_session`, `monitor_find_state_file`, and `monitor_get_file_size` (`scripts/humanize.sh:6`, `scripts/lib/monitor-common.sh:32`, `scripts/lib/monitor-common.sh:40`, `scripts/lib/monitor-common.sh:154`).
  - The assigned file is likely called by the broader test harness under `tests/run-all-tests.sh` or manually as an executable robustness test, but the assigned file itself is self-contained enough to run directly as a shell script after dependencies like `jq` are present.

- edge_cases_or_failure_modes:
  - Invalid JSON and empty JSON are expected to reject cleanly. A failure mode would be accepting malformed input, missing `tool_name`, or missing required `tool_input` fields, weakening hook safety (`tests/robustness/test-hook-input-robustness.sh:71`, `tests/robustness/test-hook-input-robustness.sh:87`, `tests/robustness/test-hook-input-robustness.sh:102`).
  - Extremely long commands and shell metacharacters should not break parsing. The test expects a benign long `echo` and a command containing `< > & | ; $` to pass rather than crash or misparse (`tests/robustness/test-hook-input-robustness.sh:117`, `tests/robustness/test-hook-input-robustness.sh:135`).
  - Unicode escaped path content should remain valid input; non-UTF8 raw bytes should be rejected with exit `1` when the production validator sees them and `iconv` is available (`tests/robustness/test-hook-input-robustness.sh:149`, `tests/robustness/test-hook-input-robustness.sh:201`, `hooks/lib/loop-common.sh:69`).
  - Null bytes are a known limitation because Bash command substitution strips them before hook code can inspect the string. The test documents this and only requires non-crashing behavior below signal exit range (`tests/robustness/test-hook-input-robustness.sh:219`, `tests/robustness/test-hook-input-robustness.sh:230`).
  - Monitor file-system edge cases include log deletion, log truncation/rotation, session directory deletion, no session directory, no log file yet, and newer session/log switching (`tests/robustness/test-hook-input-robustness.sh:268`, `tests/robustness/test-hook-input-robustness.sh:444`, `scripts/humanize.sh:641`, `scripts/humanize.sh:693`, `scripts/humanize.sh:777`, `scripts/humanize.sh:797`, `scripts/humanize.sh:834`, `scripts/humanize.sh:858`).
  - Terminal edge cases include very narrow width, very wide width, ANSI-colored logs, and binary log content. The integration tests specifically shim `tput` and `clear` to simulate terminal behavior without depending on a real interactive terminal (`tests/robustness/test-hook-input-robustness.sh:305`, `tests/robustness/test-hook-input-robustness.sh:317`, `tests/robustness/test-hook-input-robustness.sh:580`, `tests/robustness/test-hook-input-robustness.sh:615`).
  - A subtle test-fixture state issue exists: the initial `setup_test_dir` trap is overwritten later by `trap "rm -rf $MONITOR_TEST_DIR $FAKE_HOME_MONITOR" EXIT`, so cleanup responsibility shifts from the original temp test directory to monitor temp directories (`tests/robustness/test-hook-input-robustness.sh:19`, `tests/robustness/test-hook-input-robustness.sh:448`). This is not a behavioral assertion, but it is an observable cleanup edge in the test script.
  - The script creates temporary runner files using heredocs and `chmod +x`; in this read-only branch research pass I did not execute it, because the assignment requests research notes only and the environment forbids modifications. If run normally, those writes occur under `mktemp` directories, not the repository (`tests/robustness/test-hook-input-robustness.sh:455`, `tests/robustness/test-hook-input-robustness.sh:474`, `tests/robustness/test-hook-input-robustness.sh:514`, `tests/robustness/test-hook-input-robustness.sh:562`, `tests/robustness/test-hook-input-robustness.sh:605`, `tests/robustness/test-hook-input-robustness.sh:649`).

- validation_or_tests:
  - The assigned file is itself validation. It defines 26 numbered checks plus subcases 10a-10c, covering hook JSON parsing, monitor primitives, command modification detection, production parser availability, and direct monitor-loop execution.
  - The expected success condition is zero accumulated failures in `TESTS_FAILED`; `print_test_summary` then returns `0`, and the script exits with that result (`tests/test-helpers.sh:58`, `tests/test-helpers.sh:71`, `tests/robustness/test-hook-input-robustness.sh:670`, `tests/robustness/test-hook-input-robustness.sh:671`).
  - The most important validation contracts are: valid hook JSON exits `0`; malformed/missing-field hook JSON exits nonzero below `128`; deep JSON exits nonzero below `128`; non-UTF8 Bash input exits exactly `1`; null-byte-stripped input exits below `128`; monitor no-session exits `0` or `1`; monitor narrow/ANSI cases exit below `128`.
  - I inspected the file directly and followed production references to `hooks/lib/loop-common.sh`, the three validator scripts, `scripts/humanize.sh`, `scripts/lib/monitor-common.sh`, and `tests/test-helpers.sh`. I did not run the test because this worker instruction says not to modify files and the current branch export is read-only.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`