# agent_110 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-110 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: This file is a Claude next-round prompt footer. It is appended after the main next-round review prompt when the Codex stop hook decides the loop must continue because review issues remain. Its role is to enforce the end-of-round handoff contract: do not fake loop exit, optionally run code simplification, commit changes, and write the round summary to the exact generated summary file.
- algorithmic_behavior: The footer is static prompt text with one template variable, `{{NEXT_SUMMARY_FILE}}`. In `hooks/loop-codex-stop-hook.sh`, the loop computes `NEXT_ROUND=$((CURRENT_ROUND + 1))` at lines 721-730, updates `state.md` to that next round at lines 1505-1508, creates `round-${NEXT_ROUND}-prompt.md` and `round-${NEXT_ROUND}-summary.md` at lines 1510-1512, renders `claude/next-round-prompt.md` first at lines 1514-1526, then appends this footer at lines 1536-1540. The footer’s own key sections are: line 4 blocks dishonest loop exit or loop-state tampering; lines 6-9 define the post-work sequence; line 7 adds the optional `code-simplifier` review; line 8 requires a descriptive commit; line 9 requires the summary to be written into `@{{NEXT_SUMMARY_FILE}}`.
- inputs_outputs_state: Input is the caller-provided `NEXT_SUMMARY_FILE` variable from `hooks/loop-codex-stop-hook.sh:1512` and passed into `load_and_render_safe` at lines 1539-1540. Output is rendered Markdown appended to `round-${NEXT_ROUND}-prompt.md`. The state transition is indirect but important: by requiring a commit and summary for the next round, it gives the next stop-hook pass concrete artifacts to validate. The hook has already advanced `current_round` before writing this prompt, so the summary path embedded by this footer points at the newly active round, not the previous one.
- gates_or_invariants: The footer encodes three operator-facing invariants. First, the worker must not terminate the loop by lying, editing loop state files, or running `cancel-rlcr-loop` (`prompt-template/claude/next-round-footer.md:4`). Second, if the optional `code-simplifier` plugin exists, the prompt asks the worker to use it before finalizing (`line 7`). Third, the worker must commit and write the work summary to the exact rendered summary target (`lines 8-9`). Rendering uses the shared template engine, whose comments define single-pass `{{VAR}}` substitution and preserving missing variables as literals in `hooks/lib/template-loader.sh:7-14`; this protects prompt content from recursive placeholder injection.
- dependencies_and_callers: Primary caller is `hooks/loop-codex-stop-hook.sh:1539`, using `load_and_render_safe "$TEMPLATE_DIR" "claude/next-round-footer.md" "$FOOTER_FALLBACK" "NEXT_SUMMARY_FILE=$NEXT_SUMMARY_FILE"`. The renderer is `hooks/lib/template-loader.sh:170-203`; it loads the template, falls back if missing or empty, renders variables, and emits the result. This footer coordinates with `prompt-template/claude/next-round-prompt.md`, which supplies the review content, plan reference, todo requirement, and read-only goal tracker instruction before the footer is appended. It also coordinates with optional `prompt-template/claude/push-every-round-note.md` and `prompt-template/claude/goal-tracker-update-request.md`, which are appended after the footer at `hooks/loop-codex-stop-hook.sh:1542-1556`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, the stop hook falls back to `## Before Exiting / Commit your changes and write summary to {{NEXT_SUMMARY_FILE}}` from `hooks/loop-codex-stop-hook.sh:1537-1539`; this preserves the commit and summary contract but drops the stronger anti-exit-tampering note and optional `code-simplifier` instruction. If `NEXT_SUMMARY_FILE` is not passed or is misspelled, the template loader keeps `{{NEXT_SUMMARY_FILE}}` literal rather than failing, per `hooks/lib/template-loader.sh:11-14` and `116-122`, which would leave an unusable prompt target. If variable values contain `{{...}}`, the renderer intentionally performs only one pass (`hooks/lib/template-loader.sh:54-57`), avoiding accidental substitution inside review content or paths. The footer is not an executable gate by itself; enforcement depends on separate write/summary/state validators and stop-hook checks.
- validation_or_tests: Template existence references are covered by `tests/test-template-references.sh`, which scans `load_template`, `load_and_render`, and `load_and_render_safe` calls and fails missing templates (`lines 66-107`). Safe rendering behavior is covered by `tests/test-template-loader.sh:196-222`, including fallback substitution for missing templates and normal template use when present. Broader robustness tests referenced by search include `tests/test-error-scenarios.sh` and `tests/robustness/test-template-error-robustness.sh` for `load_and_render_safe` missing-template and special-case behavior. I inspected the assigned file directly with line numbers and followed the caller/rendering path; no tests were executed because this task requests read-only research notes.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 Item Evidence sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`