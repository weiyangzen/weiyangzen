# agent_070 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-070 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: Executable specification for the prompt template subsystem used by the Humanize loop hooks. The file validates the real `prompt-template/` tree, the template loader API in `hooks/lib/template-loader.sh`, placeholder syntax rules, fallback behavior, and representative rendered hook messages. It is part of the aggregate suite through `tests/run-all-tests.sh:32-40`, where `test-templates-comprehensive.sh` is listed as an ordered test suite.
- algorithmic_behavior: The script derives `PROJECT_ROOT` from its own location and sources `hooks/lib/template-loader.sh` at `tests/test-templates-comprehensive.sh:15-17`. It then resolves `TEMPLATE_DIR` through `get_template_dir "$PROJECT_ROOT/hooks/lib"` at `tests/test-templates-comprehensive.sh:59`, which maps to `prompt-template` via `hooks/lib/template-loader.sh:26-31`. The script is organized as seven behavioral sections:
  - Section 1 checks required template directory structure: `prompt-template` must exist and must include `block/`, `codex/`, and `claude/` at `tests/test-templates-comprehensive.sh:64-80`.
  - Section 2 recursively discovers every Markdown template with `find "$TEMPLATE_DIR" -name "*.md" -type f -print0 | sort -z`, loads each through `load_template`, and fails empty load results while warning on whitespace-only files at `tests/test-templates-comprehensive.sh:87-111`.
  - Section 3 reads each template directly with `cat` and applies regex-based syntax checks for malformed `{{VAR}}` placeholder forms at `tests/test-templates-comprehensive.sh:118-194`.
  - Section 3.5 self-tests those malformed-placeholder regexes using temporary files and literal strings, covering extra closing braces, extra opening braces, spaces inside placeholders, lowercase placeholders, and valid uppercase placeholders at `tests/test-templates-comprehensive.sh:199-259`.
  - Section 4 exercises `render_template` on empty content, single and multiline values, preserved blank lines, regex-sensitive values, backslashes, quotes, CJK-like text, status patterns, Markdown, long values, multiline values, placeholders at boundaries, repeated placeholders, and adjacent placeholders at `tests/test-templates-comprehensive.sh:264-438`.
  - Section 5 validates safe loading and fallback behavior through `load_and_render_safe` plus `validate_template_dir` success and failure paths at `tests/test-templates-comprehensive.sh:443-490`.
  - Section 6 performs integration assertions against real templates: `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md` at `tests/test-templates-comprehensive.sh:495-540`.
  - Section 7 stress-tests 100 repeated render calls and confirms every discovered template can be loaded, rendered with dummy values for all uppercase placeholders, and left with no unreplaced `{{VAR}}` placeholders except warnings at `tests/test-templates-comprehensive.sh:545-605`.
- inputs_outputs_state: Inputs are the repository checkout, `hooks/lib/template-loader.sh`, the `prompt-template/**/*.md` files, Bash, POSIX-ish shell utilities (`find`, `sort`, `grep`, `sed`, `wc`, `tr`, `mktemp`, `mkdir`, `rm`, `printf`), and the real template content. The script has no external CLI arguments. It maintains in-memory counters `TESTS_PASSED`, `TESTS_FAILED`, and `WARNINGS` initialized at `tests/test-templates-comprehensive.sh:26-28`, mutated by `pass`, `fail`, and `warn` helper functions at `tests/test-templates-comprehensive.sh:31-50`. Section-local state includes `TEMPLATE_COUNT` and `LOAD_FAILURES` at `tests/test-templates-comprehensive.sh:87-111`, `syntax_errors` strings per template at `tests/test-templates-comprehensive.sh:126-184`, temporary directories from `mktemp -d` at `tests/test-templates-comprehensive.sh:205` and `tests/test-templates-comprehensive.sh:484`, render result variables throughout Section 4, and the `all_success` aggregate in Section 7 at `tests/test-templates-comprehensive.sh:563-606`. Outputs are colored PASS/FAIL/WARN lines, section banners, aggregate counts, and process exit status: `0` only when `TESTS_FAILED` is zero, `1` otherwise at `tests/test-templates-comprehensive.sh:611-625`. It creates and removes temporary directories outside the repo during its malformed-placeholder and incomplete-directory checks at `tests/test-templates-comprehensive.sh:205-259` and `tests/test-templates-comprehensive.sh:483-490`.
- gates_or_invariants: The first hard gate is template directory existence; if missing, the script prints a failure and exits immediately at `tests/test-templates-comprehensive.sh:66-72`. Required subdirectories are `block`, `codex`, and `claude` at `tests/test-templates-comprehensive.sh:74-80`, matching `validate_template_dir` in `hooks/lib/template-loader.sh:208-222`. Placeholder syntax invariants are uppercase variable names beginning with `[A-Z_]` and continuing with `[A-Z0-9_]`, with no extra braces, wrong bracket forms, spaces, or lowercase names as checked at `tests/test-templates-comprehensive.sh:122-176`. The rendering invariant is single-pass `{{VAR}}` substitution, supplied by `render_template` in `hooks/lib/template-loader.sh:50-132`; the loader comments explicitly state missing variables remain unchanged and values containing placeholders are not rescanned at `hooks/lib/template-loader.sh:7-13` and `hooks/lib/template-loader.sh:54-57`. Safe rendering must choose fallback content when the template is missing or rendering returns empty, implemented at `hooks/lib/template-loader.sh:170-203` and tested at `tests/test-templates-comprehensive.sh:446-463`. The final suite invariant is strict: any accumulated failure makes the script exit `1` at `tests/test-templates-comprehensive.sh:619-625`; warnings do not affect exit status.
- dependencies_and_callers: Direct dependency is `hooks/lib/template-loader.sh`, sourced at `tests/test-templates-comprehensive.sh:17`. The tested API includes `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir`; their implementations are in `hooks/lib/template-loader.sh:26-222`. The real template dependency set is all Markdown files under `prompt-template`; discovery currently includes `block`, `claude`, `codex`, `plan`, and `pr-loop` files, while only `block`, `codex`, and `claude` are required directory gates. Representative integration templates assert known text from `prompt-template/block/git-push.md:1-9`, `prompt-template/block/wrong-round-number.md:1-7`, `prompt-template/block/unpushed-commits.md:1-12`, and `prompt-template/codex/goal-tracker-update-section.md:1-17`. Production callers of the template API are hook and setup paths such as `hooks/lib/loop-common.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/pr-loop-stop-hook.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `scripts/setup-pr-loop.sh`, found through `load_and_render_safe`, `load_template`, and `load_and_render` references. Related narrower or robustness test suites include `tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-error-scenarios.sh`, `tests/robustness/test-template-stress-robustness.sh`, and `tests/robustness/test-template-error-robustness.sh`.
- edge_cases_or_failure_modes: The script explicitly covers missing template directory, missing required subdirectories, missing template files, whitespace-only templates, malformed placeholder syntax, fallback use for missing templates, invalid template directories, incomplete directories, and render behavior for special characters. Rendering edge cases include empty content at `tests/test-templates-comprehensive.sh:266-273`, preserved empty lines at `tests/test-templates-comprehensive.sh:299-316`, regex-like path characters at `tests/test-templates-comprehensive.sh:318-326`, backslashes at `tests/test-templates-comprehensive.sh:328-336`, quotes at `tests/test-templates-comprehensive.sh:338-345`, long values at `tests/test-templates-comprehensive.sh:382-390`, multiline values at `tests/test-templates-comprehensive.sh:392-402`, repeated variables at `tests/test-templates-comprehensive.sh:422-429`, and adjacent variables at `tests/test-templates-comprehensive.sh:431-438`. Some checks are intentionally loose: backslash, quote, Markdown, CJK-like, and multiline-value checks often assert only that an identifying prefix remains rather than exact output. The script comment says it tests CJK and emoji at `tests/test-templates-comprehensive.sh:8`, but the actual CJK test uses ASCII placeholder values `"Hello World"` and `"Chinese Text Here"` at `tests/test-templates-comprehensive.sh:348-360`, and the emoji case is reduced to ASCII status text at `tests/test-templates-comprehensive.sh:362-370`. The unclosed-placeholder detector at `tests/test-templates-comprehensive.sh:146-158` is heuristic and may under-detect or false-positive because it tokenizes `grep` results through shell word splitting. Another important behavioral limit is that Section 7 treats unreplaced placeholders as warnings, not failures, at `tests/test-templates-comprehensive.sh:590-597`; only load/render emptiness fails the suite there.
- validation_or_tests: This file is itself a validation suite and is invoked by the aggregate runner as `test-templates-comprehensive.sh` at `tests/run-all-tests.sh:32-40`. It validates the loader contract against implementation details in `hooks/lib/template-loader.sh`: direct file loading returns content or empty with warning at `hooks/lib/template-loader.sh:36-48`, rendering uses environment-prefixed values and an AWK scanner at `hooks/lib/template-loader.sh:58-132`, `load_and_render` composes load plus render at `hooks/lib/template-loader.sh:136-147`, `load_and_render_safe` applies fallback behavior at `hooks/lib/template-loader.sh:170-203`, and `validate_template_dir` enforces required directories at `hooks/lib/template-loader.sh:208-222`. I did not execute the test suite during this research pass; evidence is from direct inspection of the assigned file and the referenced loader, templates, call sites, and test runner.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1 Item Evidence section`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`