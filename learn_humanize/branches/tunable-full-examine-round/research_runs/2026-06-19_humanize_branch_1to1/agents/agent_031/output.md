# agent_031 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-031 `file` `hooks/pr-loop-stop-hook.sh`
- cursor: `[_]`
- core_role: `hooks/pr-loop-stop-hook.sh` is a Claude `Stop` hook for the PR-loop/RLCR review state machine. It intercepts exit attempts while an active `.humanize/pr-loop/<timestamp>/state.md` exists, enforces that local changes are committed and pushed, polls configured GitHub review bots, asks local Codex to classify bot feedback, and either allows the session to exit by moving `state.md` to a terminal state file or blocks exit with structured JSON containing the next task. The hook is registered in `hooks/hooks.json:52-64` after `loop-codex-stop-hook.sh`.
- algorithmic_behavior: The script reads hook stdin into `HOOK_INPUT` but does not use it for decisions (`hooks/pr-loop-stop-hook.sh:42`). It locates the newest active PR loop via `find_active_pr_loop "$PROJECT_ROOT/.humanize/pr-loop"` (`hooks/pr-loop-stop-hook.sh:48-66`; helper at `hooks/lib/loop-common.sh:795-814`). It parses frontmatter state, including `current_round`, `max_iterations`, `pr_number`, `configured_bots`, `active_bots`, trigger timestamps, startup case, and Codex/polling settings (`hooks/pr-loop-stop-hook.sh:87-174`). It then executes a gated state-machine sequence: resolve base repo for fork PRs (`hooks/pr-loop-stop-hook.sh:197-237`), terminate on merged/closed PRs (`hooks/pr-loop-stop-hook.sh:239-256`), require the current round resolution summary (`hooks/pr-loop-stop-hook.sh:258-277`), require a clean non-`.humanize` git worktree and no unpushed commits (`hooks/pr-loop-stop-hook.sh:279-362`), detect force-push history rewrites and clear stale triggers (`hooks/pr-loop-stop-hook.sh:364-424`), enforce max iterations (`hooks/pr-loop-stop-hook.sh:426-436`), fast-approve Codex via first-round `+1` reaction (`hooks/pr-loop-stop-hook.sh:438-502`), detect/update trigger comments (`hooks/pr-loop-stop-hook.sh:514-671`), decide whether a trigger is required by round/startup case/new commits (`hooks/pr-loop-stop-hook.sh:673-736`), verify Claude `eyes` reaction when required (`hooks/pr-loop-stop-hook.sh:738-790`), poll bots with per-bot timeout and deduped comment collection (`hooks/pr-loop-stop-hook.sh:791-1049`), save grouped comments (`hooks/pr-loop-stop-hook.sh:1168-1217`), run local Codex review (`hooks/pr-loop-stop-hook.sh:1219-1344`), interpret Codex’s final marker (`hooks/pr-loop-stop-hook.sh:1346-1400`), update active bot state and goal tracker (`hooks/pr-loop-stop-hook.sh:1402-1568`), then either approve or block with next-round feedback (`hooks/pr-loop-stop-hook.sh:1569-1623`).
- inputs_outputs_state: Inputs are the active state file, current round resolve file, local git state, GitHub CLI/API results, bot reactions/comments, templates, and the local Codex CLI. Important state fields are parsed from YAML frontmatter and support both YAML-list and legacy comma-separated bot lists (`hooks/pr-loop-stop-hook.sh:110-164`). Outputs include terminal state moves such as `merged-state.md`, `closed-state.md`, `maxiter-state.md`, `approve-state.md`, and `usage-limit-state.md` (`hooks/pr-loop-stop-hook.sh:246-255`, `432-435`, `1364-1365`, `1391-1399`); rewritten `state.md` with incremented round and updated active bots (`hooks/pr-loop-stop-hook.sh:1545-1568`); generated `round-N-pr-comment.md`, `round-N-codex-prompt.md`, `round-N-pr-check.md`, and `round-N-pr-feedback.md` (`hooks/pr-loop-stop-hook.sh:803-805`, `1223-1230`, `1583-1610`); goal tracker updates through `update_pr_goal_tracker` (`hooks/pr-loop-stop-hook.sh:1358-1362`, `1513-1519`; helper at `hooks/lib/loop-common.sh:1151-1265`); and Claude hook decisions as JSON `{"decision":"block","reason":...,"systemMessage":...}` for recoverable gates such as missing resolve summary, dirty git state, unpushed commits, missing trigger, bot timeout, Codex failure, waiting bots, or remaining issues.
- gates_or_invariants: The hook’s main invariant is that Claude cannot exit an active PR loop until the PR has terminal status, all active bots are removed/approved, max iterations are reached, usage limits terminate the loop, or the loop is otherwise absent. It requires `pr_number` and numeric `current_round` (`hooks/pr-loop-stop-hook.sh:185-194`). It requires a round-specific resolution file before polling (`hooks/pr-loop-stop-hook.sh:262-277`). It rejects dirty non-`.humanize` work and unpushed commits so bots review pushed code (`hooks/pr-loop-stop-hook.sh:283-361`). It rejects stale triggers after new commits or force pushes by clearing `last_trigger_at` and `trigger_comment_id` (`hooks/pr-loop-stop-hook.sh:396-410`, `612-629`). Trigger requirement is round/startup-case dependent: round 0 cases 1/2/3 do not require a new trigger unless new commits are detected, cases 4/5 do, and later rounds always do (`hooks/pr-loop-stop-hook.sh:677-708`). Claude configured with required trigger must show an `eyes` reaction or exit is blocked (`hooks/pr-loop-stop-hook.sh:749-779`). Polling waits until every configured bot has responded or individually timed out, using timeout anchored to the trigger timestamp except historical case 2/3 collection (`hooks/pr-loop-stop-hook.sh:858-891`, `897-939`). Codex output must be nonempty and must end with a recognized marker for clean approval/waiting/usage-limit handling; otherwise the remaining-issues path rewrites state and blocks (`hooks/pr-loop-stop-hook.sh:1319-1344`, `1350-1400`, `1576-1623`).
- dependencies_and_callers: The caller is Claude’s Stop hook configuration in `hooks/hooks.json:52-64`. The hook sources `hooks/lib/loop-common.sh` for PR-loop discovery, bot author mapping, YAML list rendering, mention strings, template loader access, and goal tracker updates (`hooks/pr-loop-stop-hook.sh:51-58`; `hooks/lib/loop-common.sh:726-777`, `795-814`, `1151-1265`). It sources `scripts/portable-timeout.sh` for portable `run_with_timeout` behavior across GNU/macOS/Python fallback (`scripts/portable-timeout.sh:1-76`). It shells out to `gh`, `git`, `jq`, `codex`, `scripts/check-bot-reactions.sh`, `scripts/poll-pr-reviews.sh`, and `scripts/check-pr-reviewer-status.sh` (`hooks/pr-loop-stop-hook.sh:205-244`, `459`, `766`, `801-805`, `951`, `1319-1320`, `1531-1543`). `scripts/poll-pr-reviews.sh` resolves fork base repos, fetches issue/review comments, and returns JSON with `comments` and `bots_responded` (`scripts/poll-pr-reviews.sh:1-10`, `122-158`, `239-260`). `scripts/check-bot-reactions.sh` supports Codex `+1` and Claude `eyes` reaction gates (`scripts/check-bot-reactions.sh:1-17`, `149-184`, `187-240`). `scripts/check-pr-reviewer-status.sh` computes startup case from bot comment freshness relative to latest commit (`scripts/check-pr-reviewer-status.sh:1-28`, `139-165`, `217-240`). Setup and validators are related callers/peers: `scripts/setup-pr-loop.sh` creates PR-loop state and initial artifacts, while PR-loop write/edit validators protect generated state/comment/check/feedback files via helpers in `loop-common.sh`.
- edge_cases_or_failure_modes: The hook has explicit handling for no active loop or missing state file by allowing exit (`hooks/pr-loop-stop-hook.sh:64-76`), missing/invalid PR state by warning and falling back where possible (`hooks/pr-loop-stop-hook.sh:233-237`), merged/closed PRs (`hooks/pr-loop-stop-hook.sh:246-255`), missing resolution summaries (`hooks/pr-loop-stop-hook.sh:264-277`), git status failures (`hooks/pr-loop-stop-hook.sh:283-294`), absent upstream branches with fallback to PR head SHA (`hooks/pr-loop-stop-hook.sh:323-346`), force-push history rewrites (`hooks/pr-loop-stop-hook.sh:370-424`), empty `active_bots` approval (`hooks/pr-loop-stop-hook.sh:504-511`), paginated trigger comments and current-user-only trigger filtering (`hooks/pr-loop-stop-hook.sh:530-590`), new commits invalidating previous triggers (`hooks/pr-loop-stop-hook.sh:599-629`), cancel signal during polling (`hooks/pr-loop-stop-hook.sh:941-945`), poll script transient failures with retry/sleep (`hooks/pr-loop-stop-hook.sh:950-955`), duplicate comment IDs (`hooks/pr-loop-stop-hook.sh:1029-1045`), bot timeout auto-removal including completion if all active bots time out (`hooks/pr-loop-stop-hook.sh:1051-1130`), missing Codex CLI (`hooks/pr-loop-stop-hook.sh:1291-1307`), Codex timeout/failure/empty output (`hooks/pr-loop-stop-hook.sh:1319-1344`), waiting bots without round advancement (`hooks/pr-loop-stop-hook.sh:1368-1389`), and external usage/rate limits terminating the loop (`hooks/pr-loop-stop-hook.sh:1391-1400`). Risk notes: state rewrites are plain `sed`/`awk` temp-file moves without explicit locking; Codex result parsing depends on exact section names/table formatting/final marker; approved-section `grep -qi "$bot"` can match substrings; and the polling-time Codex `+1` guard calls `_map_get BOTS_RESPONDED codex` before confirming Codex is configured (`hooks/pr-loop-stop-hook.sh:974-983`), which may be fragile under `set -u` if no Codex map entry was initialized.
- validation_or_tests: I did not run tests because this assignment is research-only in a read-only branch export. Existing validation coverage is substantial. `tests/test-pr-loop-stophook.sh` directly covers old trigger rejection after force push, startup case 1 trigger exception, approval state creation, unpushed commit blocking, force-push detection, missing trigger blocking for case 4, timeout auto-removal, Codex `+1`, Claude `eyes` timeout, dynamic startup case update, fork PR base repo resolution, and mixed approval/active-bot updates (`tests/test-pr-loop-stophook.sh:20-132`, `140-239`, `247-313`, `453-538`, `539-636`, `642-748`, `754-890`, `896-1002`, `1011-1139`, `1146-1385`, `1396-1510`, `1525-1765`). `tests/test-pr-loop-hooks.sh` covers YAML active-bot format, trigger user filtering, trigger refresh, missing trigger blocking, round-0 trigger priority, trigger-anchored timeout, trigger detection, paginated trigger detection, and runtime trigger priority (`tests/test-pr-loop-hooks.sh:26-62`, `391-464`, `539-752`, `845-1148`). `tests/test-pr-loop-system.sh` covers reaction helper behavior, startup trigger-case logic, setup failure when trigger comment ID cannot be retrieved, and goal tracker update behavior (`tests/test-pr-loop-system.sh:199-229`, `1112-1173`, `1381-1422`, `1548-1716`). Robustness suites include no-state/PR-loop state parsing and API robustness around `find_active_pr_loop`, YAML `active_bots`, and stop-hook invocation (`tests/robustness/test-hook-system-robustness.sh:9`, `483-584`; `tests/robustness/test-pr-loop-api-robustness.sh:285-340`, `566-584`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 Item Evidence section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`