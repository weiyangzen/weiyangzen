# agent_008 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-008 `file` `README.md`
- cursor: `[_]`
- core_role: `README.md` is the public behavior contract for the Humanize Claude Code plugin. It defines the repository’s top-level workflow algorithms: RLCR, plan generation, and PR review loops. The file is not executable itself, but it is behavior-defining documentation because it names the commands, phases, state artifacts, review gates, defaults, and external tool prerequisites that the scripts and hooks implement. Key sections are the RLCR definition and philosophy in `README.md:7`, the two-phase RLCR diagram and phase split in `README.md:58`, quick-start command path in `README.md:74`, command table in `README.md:87`, option contracts in `README.md:97`, gen-plan workflow in `README.md:122`, and PR loop behavior in `README.md:141`.
- algorithmic_behavior: The primary documented algorithm is RLCR: a user supplies an implementation plan, Claude implements and summarizes work, Codex independently reviews the summary, feedback loops back to Claude until Codex emits completion, then a code-review phase runs `codex review --base <branch>` and loops until no `[P0-9]` findings remain (`README.md:60`, `README.md:70`). The setup command described in the README maps to `commands/start-rlcr-loop.md:12`, which invokes `scripts/setup-rlcr-loop.sh`; that script creates `.humanize/rlcr/<timestamp>/` state, plan backup, goal tracker, and round prompt artifacts (`scripts/setup-rlcr-loop.sh:667`, `scripts/setup-rlcr-loop.sh:709`, `scripts/setup-rlcr-loop.sh:737`, `scripts/setup-rlcr-loop.sh:862`). The stop-hook behavior described by the README is implemented in `hooks/loop-codex-stop-hook.sh`: it finds the active loop, parses state, blocks on malformed state or unmet todos, runs Codex summary review during implementation, transitions to review phase on a strict final-line `COMPLETE`, runs `codex review` in review phase, continues when review issues exist, and terminates when review/finalize conditions pass (`hooks/loop-codex-stop-hook.sh:59`, `hooks/loop-codex-stop-hook.sh:88`, `hooks/loop-codex-stop-hook.sh:339`, `hooks/loop-codex-stop-hook.sh:1238`, `hooks/loop-codex-stop-hook.sh:1394`, `hooks/loop-codex-stop-hook.sh:1440`). The README’s `gen-plan` command describes a preprocessing algorithm that validates IO, checks repository relevance, analyzes the draft for clarity/consistency/completeness/functionality, resolves issues with the user, and writes an AC-formatted plan (`README.md:124`, `README.md:133`); the command file expands that into six phases and requires preserving all original draft information (`commands/gen-plan.md:19`, `commands/gen-plan.md:111`, `commands/gen-plan.md:142`, `commands/gen-plan.md:247`). The PR loop algorithm detects the current PR, fetches bot comments, has Claude fix issues, pushes changes, triggers bot re-review, polls for bot feedback, and uses local Codex validation until all monitored bots approve (`README.md:159`); its command delegates to `scripts/setup-pr-loop.sh`, which enforces bot flags, discovers the PR, stores state, fetches comments, creates a goal tracker, and writes the first prompt (`commands/start-pr-loop.md:16`, `scripts/setup-pr-loop.sh:175`, `scripts/setup-pr-loop.sh:280`, `scripts/setup-pr-loop.sh:535`, `scripts/setup-pr-loop.sh:561`, `scripts/setup-pr-loop.sh:641`).
- inputs_outputs_state: Inputs documented by the README include a plan path for `/humanize:start-rlcr-loop`, or `--skip-impl` for review-only mode (`README.md:101`, `README.md:115`); RLCR tunables such as `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, `--push-every-round`, `--base-branch`, and `--full-review-round` (`README.md:104`); draft and output paths for `/humanize:gen-plan` (`README.md:124`); and PR-loop bot flags plus review parameters (`README.md:144`). Outputs and state surfaces include `.humanize/rlcr/<timestamp>/` for RLCR progress monitoring (`README.md:84`), copied plan backup, `state.md`, `goal-tracker.md`, `round-N-prompt.md`, `round-N-summary.md`, review result files, and review-phase marker files from the setup/stop-hook flow (`scripts/setup-rlcr-loop.sh:675`, `scripts/setup-rlcr-loop.sh:709`, `scripts/setup-rlcr-loop.sh:737`, `hooks/loop-codex-stop-hook.sh:72`, `hooks/loop-codex-stop-hook.sh:1427`). For `gen-plan`, the output is a structured plan with goal, AC-X acceptance criteria, path boundaries, feasibility hints, dependencies/sequence, implementation notes, and the original draft appended (`commands/gen-plan.md:146`, `scripts/validate-gen-plan-io.sh:148`). For PR loop, state is written under `.humanize/pr-loop/<timestamp>/` and includes current round, max iterations, PR number, branch, configured/active bots, Codex settings, polling settings, startup case, latest commit metadata, and trigger-comment metadata (`scripts/setup-pr-loop.sh:394`, `scripts/setup-pr-loop.sh:535`). Main state transitions documented by the README are: no active loop -> active implementation phase; implementation phase -> repeated Claude/Codex summary-review rounds; strict `COMPLETE` -> review phase; review phase -> repeated code-review-fix rounds when `[P0-9]` markers are found; no review issues -> done/finalize (`README.md:70`, `README.md:115`). PR-loop state transitions are: PR detected -> comments fetched -> issues fixed/pushed -> trigger comments/polling -> local Codex validation -> continue or finish when bots approve (`README.md:159`).
- gates_or_invariants: README-level gates include requiring Codex CLI for review (`README.md:52`), plan file availability unless `--skip-impl` is used (`README.md:101`, `README.md:117`), code-review phase using `[P0-9]` severity markers (`README.md:72`), base-branch auto-detection priority of remote default then `main` then `master` (`README.md:113`), full alignment check interval minimum and default (`README.md:115`), and PR-loop requirements for GitHub CLI, Codex CLI, and an associated open PR (`README.md:169`). Supporting scripts show stricter invariants behind those docs: only one RLCR or PR loop can be active (`scripts/setup-rlcr-loop.sh:240`, `scripts/setup-pr-loop.sh:220`); RLCR requires a git repo and at least one commit (`scripts/setup-rlcr-loop.sh:297`); normal plan paths must be relative, no spaces, no shell metacharacters, not symlinked, readable, inside the project, and not inside a submodule (`scripts/setup-rlcr-loop.sh:320`, `scripts/setup-rlcr-loop.sh:327`, `scripts/setup-rlcr-loop.sh:334`, `scripts/setup-rlcr-loop.sh:346`, `scripts/setup-rlcr-loop.sh:394`, `scripts/setup-rlcr-loop.sh:408`); `--track-plan-file` requires the plan to be tracked and clean, while untracked mode rejects tracked plans (`scripts/setup-rlcr-loop.sh:447`, `scripts/setup-rlcr-loop.sh:462`); review phase cannot be entered by merely editing state because the hook also requires `.review-phase-started` (`hooks/loop-codex-stop-hook.sh:1446`). Gen-plan IO validation enforces required flags, nonempty existing input, existing writable output directory, nonexisting output file, and a present template (`scripts/validate-gen-plan-io.sh:58`, `scripts/validate-gen-plan-io.sh:79`, `scripts/validate-gen-plan-io.sh:87`, `scripts/validate-gen-plan-io.sh:95`, `scripts/validate-gen-plan-io.sh:103`, `scripts/validate-gen-plan-io.sh:118`, `scripts/validate-gen-plan-io.sh:142`). PR-loop setup enforces at least one bot flag, `gh` installation/authentication, Codex availability, a valid open PR, and YAML-safe branch/model/effort values (`scripts/setup-pr-loop.sh:179`, `scripts/setup-pr-loop.sh:255`, `scripts/setup-pr-loop.sh:263`, `scripts/setup-pr-loop.sh:271`, `scripts/setup-pr-loop.sh:326`, `scripts/setup-pr-loop.sh:361`).
- dependencies_and_callers: README documents user-facing slash commands; concrete command wrappers are `commands/start-rlcr-loop.md`, `commands/cancel-rlcr-loop.md`, `commands/gen-plan.md`, `commands/start-pr-loop.md`, and `commands/cancel-pr-loop.md` as listed in `README.md:87`. RLCR depends on Codex CLI and Claude Code plugin hooks (`README.md:13`, `README.md:52`), command wrapper `commands/start-rlcr-loop.md:12`, setup script `scripts/setup-rlcr-loop.sh`, stop hook `hooks/loop-codex-stop-hook.sh`, shared hook utilities in `hooks/lib/loop-common.sh`, templates under `prompt-template/claude/` and `prompt-template/codex/`, and todo parsing via `hooks/check-todos-from-transcript.py` (`hooks/loop-codex-stop-hook.sh:345`). Gen-plan depends on `scripts/validate-gen-plan-io.sh`, `agents/draft-relevance-checker.md`, Task/Explore agents, user clarification via `AskUserQuestion`, and `prompt-template/plan/gen-plan-template.md` (`commands/gen-plan.md:4`, `commands/gen-plan.md:56`, `commands/gen-plan.md:101`, `scripts/validate-gen-plan-io.sh:133`). PR-loop depends on GitHub CLI, Codex CLI, `scripts/setup-pr-loop.sh`, `scripts/fetch-pr-comments.sh`, `scripts/check-pr-reviewer-status.sh`, `scripts/check-bot-reactions.sh`, PR templates under `prompt-template/pr-loop/`, and the PR stop-hook/polling family (`README.md:169`, `scripts/setup-pr-loop.sh:411`, `scripts/setup-pr-loop.sh:418`, `scripts/setup-pr-loop.sh:507`). Validation coverage is supported by tests listed in the repo inventory, including `tests/test-gen-plan.sh`, `tests/test-pr-loop.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-finalize-phase.sh`, `tests/test-todo-checker.sh`, and robustness tests for plan files, state transitions, hooks, git operations, PR-loop APIs, and timeouts.
- edge_cases_or_failure_modes: The README exposes several user-visible failure modes: missing Codex CLI blocks review use (`README.md:52`), max iteration exhaustion stops loops (`README.md:104`, `README.md:151`), optional skip-implementation mode changes plan requirements and jumps directly to code review (`README.md:117`), PR loop cannot run without `gh`, Codex, and an associated open PR (`README.md:169`), and cancellation is a separate user action (`README.md:85`, `README.md:95`). Supporting implementation expands these into concrete edge cases: duplicate active loops are rejected; missing, absolute, symlinked, submodule, unreadable, dirty, or incorrectly tracked plan files fail setup; base branch detection can fail if remote default/main/master are unavailable; branch names and Codex config with YAML-unsafe characters are rejected; state files missing required fields are treated as corrupted or outdated; manual review-phase toggles are blocked if the marker file is absent; incomplete todos block exit before expensive Codex review; git status failures fail closed; large changed files can be blocked; Codex nonzero exits, empty output, or missing review result files block and ask for retry (`scripts/setup-rlcr-loop.sh:245`, `scripts/setup-rlcr-loop.sh:320`, `scripts/setup-rlcr-loop.sh:633`, `hooks/loop-codex-stop-hook.sh:131`, `hooks/loop-codex-stop-hook.sh:146`, `hooks/loop-codex-stop-hook.sh:182`, `hooks/loop-codex-stop-hook.sh:216`, `hooks/loop-codex-stop-hook.sh:371`, `hooks/loop-codex-stop-hook.sh:403`, `hooks/loop-codex-stop-hook.sh:1311`, `hooks/loop-codex-stop-hook.sh:1348`). PR-loop edge cases include missing bot flags, missing `gh`/auth/Codex, fork PR lookup requiring parent/base repository handling, closed or merged PRs, failed trigger comments, missing trigger-comment IDs, and required Claude eyes reaction verification when monitoring Claude (`scripts/setup-pr-loop.sh:179`, `scripts/setup-pr-loop.sh:255`, `scripts/setup-pr-loop.sh:290`, `scripts/setup-pr-loop.sh:340`, `scripts/setup-pr-loop.sh:443`, `scripts/setup-pr-loop.sh:484`).
- validation_or_tests: README itself is not executable, but it names workflows backed by concrete validators and tests. `gen-plan` explicitly has IO validation exit codes documented in `README.md:127` and implemented in `scripts/validate-gen-plan-io.sh:4`. RLCR validation is mainly hook-driven: state schema checks, branch consistency, plan integrity, todo completion, git status, Codex execution result checks, strict `COMPLETE`/`STOP` marker handling, and review-phase `[P0-9]` detection (`hooks/loop-codex-stop-hook.sh:158`, `hooks/loop-codex-stop-hook.sh:224`, `hooks/loop-codex-stop-hook.sh:257`, `hooks/loop-codex-stop-hook.sh:339`, `hooks/loop-codex-stop-hook.sh:1388`, `hooks/loop-codex-stop-hook.sh:1440`, `hooks/loop-codex-stop-hook.sh:1471`). PR-loop validation includes setup-time PR discovery and bot prerequisite checks plus polling/review validation described in the README (`README.md:159`) and represented in setup state/prompt creation (`scripts/setup-pr-loop.sh:535`, `scripts/setup-pr-loop.sh:641`). Repository test files relevant to this documented behavior include `tests/test-gen-plan.sh`, `tests/test-plan-file-validation.sh`, `tests/test-plan-file-hooks.sh`, `tests/test-todo-checker.sh`, `tests/test-finalize-phase.sh`, `tests/test-pr-loop.sh`, `tests/test-pr-loop-scripts.sh`, `tests/test-pr-loop-stophook.sh`, `tests/test-pr-loop-system.sh`, and robustness tests under `tests/robustness/`. No tests were executed during this research pass because the assignment requested read-only notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1`
- missing_items: `0`
- duplicate_items: `0`
- final_worker_status: `complete`