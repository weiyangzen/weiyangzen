# agent_080 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-080 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: This file is a small prompt block used by the RLCR loop stop-hook git-clean gate. Its role is to append a special-case instruction when local `.humanize*` state is detected as untracked during exit/finalization checks. The direct template text identifies `.humanize/` as created by `humanize:start-rlcr-loop`, says it should not be committed, and instructs adding `.humanize*` to `.gitignore` plus staging only `.gitignore`; see `prompt-template/block/git-not-clean-humanize-local.md:2` through `prompt-template/block/git-not-clean-humanize-local.md:8`.

- algorithmic_behavior: The executable behavior lives in `hooks/loop-codex-stop-hook.sh`, while this file supplies the rendered message fragment. The stop hook first caches `git status --porcelain` with fail-closed handling; if the git status command fails or times out, it blocks immediately instead of bypassing verification, at `hooks/loop-codex-stop-hook.sh:403` through `hooks/loop-codex-stop-hook.sh:424`. Later, if cached status is non-empty, the hook classifies the repo as having `uncommitted changes`, extracts untracked entries with `grep '^??'`, and checks those untracked paths for `.humanize`; see `hooks/loop-codex-stop-hook.sh:521` through `hooks/loop-codex-stop-hook.sh:530`. When the pattern matches, it loads this assigned template via `load_template "$TEMPLATE_DIR" "block/git-not-clean-humanize-local.md"` and appends it into `SPECIAL_NOTES`; if the template is unavailable, the hook falls back to a plain note that `.humanize*` directories are intentionally untracked, at `hooks/loop-codex-stop-hook.sh:530` through `hooks/loop-codex-stop-hook.sh:534`. The final blocking response then renders `prompt-template/block/git-not-clean.md`, injecting `SPECIAL_NOTES`, and returns JSON with `decision: block`; see `hooks/loop-codex-stop-hook.sh:548` through `hooks/loop-codex-stop-hook.sh:569`.

- inputs_outputs_state: The effective inputs are the repository’s `git status --porcelain` output, especially untracked `??` rows, the presence of `.humanize` in those untracked rows, and the template directory resolution used by `load_template`. The assigned file itself has no template variables, so it is loaded as static Markdown. Its output is not a command result; it is appended human-facing guidance inside the larger `# Git Not Clean` block, whose base template has a `{{SPECIAL_NOTES}}` insertion point at `prompt-template/block/git-not-clean.md:3` through `prompt-template/block/git-not-clean.md:5`. The state transition is from “repo has uncommitted/untracked changes” to a blocked stop-hook decision, with this file adding a narrower remediation path for local loop state: update `.gitignore` with `.humanize*` and stage `.gitignore`, not the `.humanize/` contents.

- gates_or_invariants: The key invariant is that `.humanize/` is local loop state and should not enter version control. The assigned template states that invariant directly at `prompt-template/block/git-not-clean-humanize-local.md:2` through `prompt-template/block/git-not-clean-humanize-local.md:4`. The surrounding gate blocks exit whenever `GIT_ISSUES` is non-empty, so this note does not make `.humanize*` acceptable as untracked noise by itself; it tells the agent how to make the repository clean by ignoring the local state. A related command-level protection also blocks broad or forced `git add` forms that would add `.humanize` files; its documented blocked cases include direct `.humanize`, `.humanize/*`, forced adds, and broad `git add -A` or `git add .` when relevant, at `hooks/lib/loop-common.sh:908` through `hooks/lib/loop-common.sh:929`. The prompt copy for that separate protection repeats the safe `.gitignore` path at `prompt-template/block/git-add-humanize.md:26` through `prompt-template/block/git-add-humanize.md:34`.

- dependencies_and_callers: The direct caller is `hooks/loop-codex-stop-hook.sh`, which loads this exact file only from the untracked `.humanize` branch at `hooks/loop-codex-stop-hook.sh:528` through `hooks/loop-codex-stop-hook.sh:534`. Template loading is provided by `hooks/lib/template-loader.sh`; `load_template` returns file content if present and empty output with a warning if missing, at `hooks/lib/template-loader.sh:33` through `hooks/lib/template-loader.sh:48`. The larger git-not-clean template consumes the resulting note through `{{SPECIAL_NOTES}}` at `prompt-template/block/git-not-clean.md:3` through `prompt-template/block/git-not-clean.md:4`. Related sibling behavior exists in `prompt-template/block/git-add-humanize.md`, used by the command validator path represented in `hooks/lib/loop-common.sh:1048` through `hooks/lib/loop-common.sh:1065`.

- edge_cases_or_failure_modes: If `git status --porcelain` fails or times out, the hook never reaches this special-case template; it emits the git-status-failed block and exits with a blocking JSON decision, as shown at `hooks/loop-codex-stop-hook.sh:410` through `hooks/loop-codex-stop-hook.sh:423`. If the assigned template is missing or empty, the hook substitutes a shorter fallback message rather than failing open, at `hooks/loop-codex-stop-hook.sh:530` through `hooks/loop-codex-stop-hook.sh:533`. The detection is substring-based on untracked rows containing `.humanize`, so it intentionally covers `.humanize/` and legacy `.humanize-*` directories per the comment at `hooks/loop-codex-stop-hook.sh:528`, but it can also match any untracked path containing that substring. The note’s suggested `echo '.humanize*' >> .gitignore` is simple and can duplicate an existing ignore rule; tests commonly pre-seed `.humanize*` into `.gitignore`, for example `tests/test-finalize-phase.sh:166` through `tests/test-finalize-phase.sh:176`.

- validation_or_tests: I found coverage for the broader finalize/git-clean behavior rather than a narrow assertion of this exact template text. `tests/test-finalize-phase.sh` sets `.humanize/` and `.humanize*` in `.gitignore` during setup, confirming expected ignore configuration for loop artifacts at `tests/test-finalize-phase.sh:166` through `tests/test-finalize-phase.sh:176`. The same test suite verifies that finalize blocks when the repo is dirty, asserting a block decision and git/clean wording after creating an uncommitted file, at `tests/test-finalize-phase.sh:454` through `tests/test-finalize-phase.sh:470`. Search also found plan-file hook tests that add `.humanize*` to `.gitignore` to avoid local loop state surfacing as uncommitted noise, but no direct test that simulates an untracked `.humanize` directory and asserts `prompt-template/block/git-not-clean-humanize-local.md` is included.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-080` appears once in the Item Evidence section
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`