# agent_106 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-106 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: This is a guard-message template for the RLCR write-path validator. The file itself is not executable logic, but it is part of the algorithmic write contract: if an agent tries to write a `round-N-summary.md` file outside the active `.humanize/rlcr/<loop>/` directory, the validator blocks the write and renders this message. The template defines the user-facing transition instruction from an invalid summary target to the canonical active-loop summary path. Direct content is at `prompt-template/block/wrong-summary-location.md:1-5`; the critical invariant is line 3, “Summary files MUST be in the loop directory,” and the only dynamic output is `{{CORRECT_PATH}}` at line 5.
- algorithmic_behavior: The triggering algorithm lives in `hooks/loop-write-validator.sh`. The hook first accepts only `Write` tool calls and requires `tool_input.file_path` (`hooks/loop-write-validator.sh:37-49`). It classifies round summary files with `is_round_file_type "$FILE_PATH_LOWER" "summary"` (`hooks/loop-write-validator.sh:100`) and detects whether the target is inside `.humanize/rlcr/` (`hooks/loop-write-validator.sh:102`). After finding the active loop and parsing the current round (`hooks/loop-write-validator.sh:123-145`), it reaches the location gate at `hooks/loop-write-validator.sh:202-209`: when `IS_SUMMARY_FILE=true` and `IN_HUMANIZE_LOOP_DIR=false`, it computes `CORRECT_PATH="$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md"`, renders `block/wrong-summary-location.md`, writes the rendered message to stderr, and exits with code `2` to block the write. The template therefore acts as the diagnostic payload for that state transition: invalid external summary write -> blocked write with canonical target path.
- inputs_outputs_state: Input to the template is one variable, `CORRECT_PATH`, supplied by `hooks/loop-write-validator.sh:203` and passed into `load_and_render_safe` at `hooks/loop-write-validator.sh:207-208`. Its value is derived from runtime state: `ACTIVE_LOOP_DIR` from `find_active_loop "$LOOP_BASE_DIR"` (`hooks/loop-write-validator.sh:126`, helper at `hooks/lib/loop-common.sh:156-174`) and `CURRENT_ROUND` from strict state parsing (`hooks/loop-write-validator.sh:140-145`, helper at `hooks/lib/loop-common.sh:255-329`). Output is a Markdown error message on stderr, not a file write. The template does not mutate state; the state transition is enforced by the caller’s exit code `2` at `hooks/loop-write-validator.sh:209`.
- gates_or_invariants: The gate requires summary files matching `round-[0-9]+-summary.md` to live under `.humanize/rlcr/`; path classification is regex-based in `is_round_file_type` (`hooks/lib/loop-common.sh:399-406`) and `is_in_humanize_loop_dir` (`hooks/lib/loop-common.sh:712-716`). The validator has earlier safety gates for valid hook JSON, maximum JSON depth, required `file_path`, active loop discovery, and strict state-file parsing (`hooks/loop-write-validator.sh:25-45`, `hooks/loop-write-validator.sh:123-145`). Finalize summaries are handled separately before this location gate, allowing `$ACTIVE_LOOP_DIR/finalize-summary.md` only when appropriate (`hooks/loop-write-validator.sh:162-173`). Rendering uses the shared template loader with single-pass placeholder substitution and missing-variable placeholders left intact (`hooks/lib/template-loader.sh:7-14`, `hooks/lib/template-loader.sh:58-132`).
- dependencies_and_callers: Direct caller is `hooks/loop-write-validator.sh:207`, via `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-summary-location.md"`. Template directory resolution comes from `hooks/lib/loop-common.sh:139-148`, which sources `hooks/lib/template-loader.sh` and initializes `TEMPLATE_DIR`. Rendering/fallback behavior is implemented in `load_and_render_safe` (`hooks/lib/template-loader.sh:170-203`). The broader sibling template family includes other write/read guard messages such as `prompt-template/block/wrong-directory-path.md`, `prompt-template/block/wrong-round-number.md`, and `prompt-template/block/wrong-file-location.md`; this file is specifically for the “summary file outside loop directory” branch, not the wrong-round or wrong-active-directory branches.
- edge_cases_or_failure_modes: If no active loop exists, the write validator exits early and this template is not rendered (`hooks/loop-write-validator.sh:128-130`). If the active state file is malformed, the hook blocks with a separate malformed-state error before this template (`hooks/loop-write-validator.sh:140-143`). If the template is missing or empty, `load_and_render_safe` falls back to the inline message at `hooks/loop-write-validator.sh:204-206`; that fallback also contains `{{CORRECT_PATH}}`, so the same variable contract remains. If `CORRECT_PATH` is not supplied, the renderer deliberately leaves `{{CORRECT_PATH}}` unchanged rather than failing (`hooks/lib/template-loader.sh:119-122`). Because `is_in_humanize_loop_dir` is a substring regex (`hooks/lib/loop-common.sh:713-716`), this particular gate decides “inside loop directory” by textual path content rather than canonical path resolution; later directory equality checks only apply after a path is already considered inside the loop.
- validation_or_tests: Template reference coverage exists in `tests/test-template-references.sh`: it scans hook calls to `load_template`, `load_and_render`, and `load_and_render_safe`, verifies referenced templates exist, and specifically includes `hooks/loop-write-validator.sh` in the scan (`tests/test-template-references.sh:56-64`, `tests/test-template-references.sh:83-110`). It also requires critical validators to use `load_and_render_safe` rather than unsafe rendering (`tests/test-template-references.sh:172-200`). General template syntax and render coverage checks all `prompt-template/*.md` files for placeholder syntax and dummy rendering (`tests/test-templates-comprehensive.sh:118-194`, `tests/test-templates-comprehensive.sh:566-604`). Nearby write-validator tests cover summary round allowlist/block behavior inside the active loop (`tests/test-allowlist-validators.sh:148-185`), but I did not find a focused test that directly exercises an outside-`.humanize/rlcr/` summary write and asserts this exact `Wrong Summary Location` message.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `TUNABLE_FULL_EXAMINE_ROUND-HZ-106`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`