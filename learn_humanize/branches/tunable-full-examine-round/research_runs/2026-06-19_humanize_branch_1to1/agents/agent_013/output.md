# agent_013 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-013 `directory` `prompt-template/plan`
- cursor: `[_]`
- core_role: `prompt-template/plan` is the template surface for the `/humanize:gen-plan` algorithm. The directory contains one recursive descendant, `prompt-template/plan/gen-plan-template.md`, which defines the initial structured plan skeleton copied into a user-selected output file before the command analyzes and fills it. It is core because the gen-plan IO validator directly depends on this exact path and fails with a plugin configuration error if it is missing: `scripts/validate-gen-plan-io.sh:133-146`.

- algorithmic_behavior: The directory provides a deterministic output scaffold rather than executable code. Its child template defines the plan sections that the command later completes: title, goal description, acceptance criteria, path boundaries, feasibility hints, dependencies/sequence, and implementation notes. The acceptance criteria section requires TDD-oriented positive and negative tests, including nested AC identifiers: `prompt-template/plan/gen-plan-template.md:6-23`. Path boundaries constrain scope using upper/lower bounds and allowed/prohibited choices: `prompt-template/plan/gen-plan-template.md:25-44`. Feasibility hints are explicitly non-prescriptive and reference-oriented: `prompt-template/plan/gen-plan-template.md:46-55`. Dependencies are represented as milestones, phases, and steps without time estimates: `prompt-template/plan/gen-plan-template.md:57-67`. Implementation notes constrain generated implementation code so plan workflow terms do not leak into production names/comments: `prompt-template/plan/gen-plan-template.md:69-74`.

- inputs_outputs_state: Inputs are the rough draft file and requested output path accepted by `/humanize:gen-plan`, documented in `commands/gen-plan.md:1-12` and README usage at `README.md:122-139`. The direct consumer is `scripts/validate-gen-plan-io.sh`, which parses `--input` and `--output`, resolves paths, validates preconditions, then locates this template using `CLAUDE_PLUGIN_ROOT` or a script-relative fallback: `scripts/validate-gen-plan-io.sh:29-72` and `scripts/validate-gen-plan-io.sh:133-140`. On success, the script copies `prompt-template/plan/gen-plan-template.md` to the output file, appends `--- Original Design Draft Start ---`, appends the full input draft, and closes with `--- Original Design Draft End ---`: `scripts/validate-gen-plan-io.sh:148-157`. The state transition is therefore: nonexistent output path -> new plan file containing template plus preserved original draft -> later command phases edit placeholders into complete plan content. The command instructions explicitly say the output already contains the template and draft after IO validation and must be updated with the Edit tool while keeping the original draft intact: `commands/gen-plan.md:251-260`.

- gates_or_invariants: IO validation gates require both arguments, existing non-empty input file, existing output directory, output path not already present and not a directory, and writable output directory: `scripts/validate-gen-plan-io.sh:58-124`. Template existence is a hard invariant; missing `gen-plan-template.md` exits 7 and is treated by the command as “Plan template file not found - plugin configuration error”: `scripts/validate-gen-plan-io.sh:142-146` and `commands/gen-plan.md:38-47`. Plan-generation invariants are duplicated in the command’s embedded plan structure and rules: AC identifiers must use `AC-X` or `AC-X.Y`; each criterion must include positive and negative tests; no line ranges, time estimates, or duration/effort estimates; dependencies describe ordering, not calendar time; deterministic drafts should narrow path boundaries; all draft content must be incorporated without omission: `commands/gen-plan.md:142-248`. The template itself encodes the same major shape in shorter form, especially TDD tests, path-boundary semantics, deterministic-design note, and code-style note: `prompt-template/plan/gen-plan-template.md:6-74`.

- dependencies_and_callers: Primary caller is `commands/gen-plan.md`, whose workflow invokes `scripts/validate-gen-plan-io.sh` first, then performs relevance check, draft analysis, issue resolution, plan generation, and final write/review: `commands/gen-plan.md:19-27` and `commands/gen-plan.md:30-47`. The validation script is the only direct file-level dependency found that copies this directory’s template: `scripts/validate-gen-plan-io.sh:136-149`. The command also invokes the `agents/draft-relevance-checker.md` agent after IO validation; that agent checks README/docs, repository structure, technologies, and semantic relation to the draft, returning `RELEVANT:` or `NOT_RELEVANT:`: `agents/draft-relevance-checker.md:12-36`. Parent/sibling coordination: this directory is part of the broader `prompt-template` tree, but unlike `prompt-template/block`, `prompt-template/claude`, `prompt-template/codex`, and `prompt-template/pr-loop`, it is not loaded through hook template-loader functions; it is consumed by the gen-plan IO validation script as a concrete copied file. Repo search found no other recursive descendants under `prompt-template/plan`.

- edge_cases_or_failure_modes: If the template file is absent or `CLAUDE_PLUGIN_ROOT` points to the wrong plugin root, valid user input still fails with exit 7 before plan generation: `scripts/validate-gen-plan-io.sh:133-146`. If the output path exists, even as a directory, validation exits 4 and the template is not copied: `scripts/validate-gen-plan-io.sh:103-116`. If the output directory lacks write permission, exit 5 prevents partial template creation: `scripts/validate-gen-plan-io.sh:118-124`. Because `set -e` is enabled, a failed `cp`, append, or `cat` after validation would abort the script, potentially after creating a partially written output file; the script does not define rollback cleanup around `scripts/validate-gen-plan-io.sh:148-157`. The command-level edge case for mixed languages is handled after generation by asking whether to unify language and editing if requested: `commands/gen-plan.md:272-285`. Another important behavioral invariant is draft preservation: command instructions state clarifications supplement the original draft and must not replace or discard it: `commands/gen-plan.md:111-139` and `commands/gen-plan.md:247`.

- validation_or_tests: `tests/test-gen-plan.sh` validates the gen-plan command structure, frontmatter, allowed tools, argument hint, relevance agent existence, agent model/tools, naming, YAML shape, content language constraints, and validation-script behavior. Relevant positive coverage includes command existence and metadata at `tests/test-gen-plan.sh:54-104`, relevance agent checks at `tests/test-gen-plan.sh:106-157`, and model/frontmatter/name checks at `tests/test-gen-plan.sh:216-400`. Validation-script tests assert exit 6 for missing/invalid args/help, exit 1 for missing input, exit 2 for empty input, exit 3 for missing output dir, exit 4 for existing output or output directory, and exit 0 for valid paths: `tests/test-gen-plan.sh:426-541`. `tests/test-template-references.sh` scans hook-loaded templates and warns when templates are not directly referenced; this is relevant mostly as sibling-template hygiene and not a direct gen-plan template test because the plan template is consumed by `validate-gen-plan-io.sh`, not by hook loader calls: `tests/test-template-references.sh:51-142`. I did not run tests because this branch export is read-only and the validation-script tests create temporary files; I inspected test logic directly.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 assigned item section present in Item Evidence
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`