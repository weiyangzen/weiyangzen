# agent_029 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-029 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: `hooks/loop-read-validator.sh` is a Claude Code `PreToolUse` guard for the `Read` tool. Its algorithmic role is to keep the RLCR loop’s active-round read state coherent by blocking stale or misplaced `round-*-prompt.md`, `round-*-summary.md`, and most `round-*-todos.md` reads. It is registered under the `Read` matcher in `hooks/hooks.json:33-39`.
- algorithmic_behavior: The script reads hook JSON from stdin, validates structure, rejects deeply nested JSON, and exits early for non-`Read` tools at `hooks/loop-read-validator.sh:25-40`. For `Read`, it requires `tool_input.file_path` at `hooks/loop-read-validator.sh:43-48`. It first applies a todos-file gate: any `round-N-todos.md` path is blocked unless it is allowlisted for the active loop directory at `hooks/loop-read-validator.sh:55-62`. For prompt/summary files, it locates the active RLCR loop, chooses `finalize-state.md` over `state.md` during finalize phase, parses the current round strictly, extracts the requested round from the filename, then validates location, round number, and exact directory path at `hooks/loop-read-validator.sh:69-168`.
- inputs_outputs_state: Input is Claude hook JSON on stdin with `tool_name` and `tool_input.file_path`. The script depends on `CLAUDE_PROJECT_DIR`, `PROJECT_ROOT`, `LOOP_BASE_DIR`, and `LOOP_DIR` when present; otherwise it derives the project root from `pwd` and the loop base as `.humanize/rlcr` at `hooks/loop-read-validator.sh:56-58` and `hooks/loop-read-validator.sh:80-82`. Output is an exit code plus optional stderr block guidance. Exit `0` means allow or not relevant; exit `1` means invalid hook input or malformed state; exit `2` means an intentional policy block with a rendered message. It does not mutate files or loop state. State is read from the newest active loop directory found by `find_active_loop`, which only considers the newest child under `.humanize/rlcr` that contains `state.md` or `finalize-state.md` in `hooks/lib/loop-common.sh:151-174`.
- gates_or_invariants: JSON must be syntactically valid, have `tool_name`, and stay within a max nesting depth of 30, enforced through `validate_hook_input` and `is_deeply_nested` in `hooks/loop-read-validator.sh:27-35` and defined in `hooks/lib/loop-common.sh:59-133`. `Read` calls must include `tool_input.file_path`, enforced by `require_tool_input_field` in `hooks/lib/loop-common.sh:98-114`. Round-file detection only matches lowercase paths ending in `round-[0-9]+-{summary,prompt,todos}.md`, implemented at `hooks/lib/loop-common.sh:394-418`. The state file must have YAML frontmatter and required fields including `current_round`, `max_iterations`, `review_started`, and `base_branch`; malformed state fails closed at `hooks/loop-read-validator.sh:94-99` and `hooks/lib/loop-common.sh:246-330`. Prompt/summary reads must be inside a humanize loop directory, must match the current round unless allowlisted, and must equal the exact active-loop path assembled as `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME` at `hooks/loop-read-validator.sh:122-168`.
- dependencies_and_callers: The direct dependency is `hooks/lib/loop-common.sh`, sourced at `hooks/loop-read-validator.sh:17-20`; that shared library sources `hooks/lib/template-loader.sh` and initializes `TEMPLATE_DIR` from the plugin root at `hooks/lib/loop-common.sh:139-149`. Blocking messages use `load_and_render_safe`, which falls back to inline text when templates are missing at `hooks/lib/template-loader.sh:167-203`. The referenced block templates exist under `prompt-template/block/`: todos access guidance is in `prompt-template/block/todos-file-access.md:1-8`, wrong location guidance in `prompt-template/block/wrong-file-location.md:1-9`, wrong round guidance in `prompt-template/block/wrong-round-file.md:1-9`, and wrong directory guidance in `prompt-template/block/wrong-directory-path.md:1-6`. Callers are the Claude hook system via `hooks/hooks.json:33-39` and test harnesses that pipe JSON into the script, including `tests/test-allowlist-validators.sh:234-283`, `tests/test-finalize-phase.sh:839-850`, and robustness tests under `tests/robustness/`.
- edge_cases_or_failure_modes: If no active loop directory exists, prompt/summary round validation allows the read by exiting `0` at `hooks/loop-read-validator.sh:84-86`; todos reads still block unless allowlisted because the todos gate runs before the active-loop no-op path at `hooks/loop-read-validator.sh:55-63`. If `finalize-state.md` exists, it supersedes `state.md` for current-round parsing at `hooks/loop-read-validator.sh:88-92`, which avoids blocking current-round reads during finalize. State corruption is fail-closed with an error at `hooks/loop-read-validator.sh:94-98`. A static allowlist permits only `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` when they are exactly inside the active loop directory, defined at `hooks/lib/loop-common.sh:420-441`. The exact-path check compares raw strings, so path aliases such as `./`, symlinks, relative forms, or alternate absolute spellings can be rejected even if they resolve to the same file. The template loader is non-fatal: missing template directories warn and use fallbacks rather than breaking the guard, per `hooks/lib/loop-common.sh:146-149` and `hooks/lib/template-loader.sh:179-203`.
- validation_or_tests: `tests/robustness/test-hook-input-robustness.sh:35-115` covers valid JSON passthrough, malformed JSON rejection, missing `tool_name`, and missing `file_path` rejection for this read hook path. The same file covers Unicode paths, unknown tools passing through, deeply nested JSON rejection, non-UTF8 rejection, and null-byte handling at `tests/robustness/test-hook-input-robustness.sh:149-235`. `tests/test-allowlist-validators.sh:234-283` specifically verifies read-validator allowlist behavior: allowed `round-1-todos.md` and historical `round-0-summary.md`, blocked `round-3-todos.md`, and blocked non-allowlisted `round-3-summary.md`. `tests/test-finalize-phase.sh:839-850` verifies the read validator parses `finalize-state.md` and allows the current round summary. `tests/robustness/test-hook-system-robustness.sh:637-665` covers escaped null and nested JSON handling without signal crashes. I did not run the tests because this task is research-only in a read-only branch export; I inspected the test definitions directly.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 of 1; the sole assigned item has exactly one evidence section above.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`