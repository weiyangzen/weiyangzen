# agent_131 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-131 `file` `tests/robustness/test-git-operations-robustness.sh`
- cursor: `[_]`
- core_role: This executable shell test is a robustness specification for the repository’s git-state accounting layer. It directly exercises production functions `humanize_parse_git_status` and `humanize_detect_git_state` from [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:116), using temporary git repositories to verify file-state counts, line-change counts, and special repository-state classification. It is included in the aggregate test runner at [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/run-all-tests.sh:60).
- algorithmic_behavior: The script starts with strict shell mode, resolves `SCRIPT_DIR` and `PROJECT_ROOT`, then sources test helpers, portable timeout support, and production logic at [tests/robustness/test-git-operations-robustness.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/robustness/test-git-operations-robustness.sh:13). It defines `parse_result`, which treats the production status output as a pipe-delimited tuple `modified|added|deleted|untracked|insertions|deletions`, selecting fields with `cut` at lines 36-47. It defines `init_test_repo` at lines 50-62 to create a clean git fixture with configured identity, disabled GPG signing, one tracked file, and one initial commit. The tests then mutate repository state and assert the parser’s numeric output or the detector’s state string.
- inputs_outputs_state: Inputs are current working directory state, git metadata, file mutations, and simulated git internals under each temporary repo’s `.git` directory. Primary output is pass/fail accounting through `pass`, `fail`, and `print_test_summary`; the script exits with the summary status at lines 506-507. The tested parser returns count tuples, with non-repo mode returning a tuple plus a trailing diagnostic from [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:166). The tested detector returns symbolic states: `normal`, `detached`, `rebase`, `merge`, `shallow`, `permission_error`, or `not_a_repo` from [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:116). Test state is isolated under `TEST_DIR`, created and cleaned by `setup_test_dir` from [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-helpers.sh:86).
- gates_or_invariants: Invariants include: clean repo has zero modified/added/untracked files at lines 71-83; default branch must be `master` or `main` at lines 86-95; untracked, modified, staged-added, and deleted states must be counted as expected at lines 98-142 and 218-230; insertion and deletion counts must be at least the number of changed lines at lines 145-157 and 349-365; non-git directories must not crash and must be explicitly identified at lines 168-177 and 466-475. State-detection gates require `normal`, `detached`, `rebase`, `merge`, and `shallow` classifications for corresponding fixture conditions at lines 377-463. The final invariant is `TESTS_FAILED == 0`, enforced by `print_test_summary`.
- dependencies_and_callers: The file depends on local helper functions from [tests/test-helpers.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/tests/test-helpers.sh:30), portable timeout detection from [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/portable-timeout.sh:10), production git parsing/detection functions from [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:116), and the external `git`, `mkdir`, `chmod`, `rm`, `cut`, `grep`, and shell builtins. `portable-timeout.sh` is sourced but not actually used by this test file, so it is an ambient robustness-suite dependency rather than part of the test’s algorithmic path. The likely caller is `tests/run-all-tests.sh`, which lists this file among robustness tests.
- edge_cases_or_failure_modes: The file covers detached HEAD parsing without breaking status counts at lines 181-194; simultaneous modified, added, and untracked files at lines 197-215; empty repositories with no commits at lines 233-245; feature branches at lines 248-262; renamed files, which the production implementation intentionally counts as modified, at lines 265-281; twenty untracked files at lines 284-298; filenames containing spaces at lines 301-313; binary files at lines 316-328; and the same file having both staged and unstaged changes at lines 331-347. It simulates rebase, merge, and shallow states by creating `.git/rebase-merge`, `.git/MERGE_HEAD`, and `.git/shallow` at lines 418-455. Permission handling is deliberately tolerant: an unreadable `.git` directory may yield `permission_error` or `not_a_repo`, both accepted at lines 478-499.
- validation_or_tests: This file is itself the validation asset. It contains 25 labeled tests: 18 status-parser cases and 7 git-state detector cases. The pass/fail mechanism comes from `tests/test-helpers.sh`, where `fail` increments `TESTS_FAILED` and `print_test_summary` returns nonzero when failures exist. The production behavior being validated includes `git status --porcelain` parsing, `git diff --shortstat` parsing, and special `.git` metadata checks in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/tunable-full-examine-round/scripts/humanize.sh:173).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 1 item section present
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`