# agent_028 tunable-full-examine-round 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 1
- source_commit: `67aa7bab09f0d0e36ac403264eed6989b09aada5`

## Item Evidence

### TUNABLE_FULL_EXAMINE_ROUND-HZ-028 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit session guard for active RLCR loops. It does not validate a specific tool call or mutate state; it runs at prompt submission time via `hooks/hooks.json:4-12` and blocks the session when the loop’s branch or plan-file contract no longer matches the state captured at loop start. It is core to the RLCR state machine because it enforces loop invariants continuously between implementation/review rounds rather than waiting for the Stop hook.
- algorithmic_behavior: The hook initializes `SCRIPT_DIR` and `PROJECT_ROOT`, then sources shared loop parsing helpers and a portable timeout wrapper at `hooks/loop-plan-file-validator.sh:13-24`. It consumes stdin into `INPUT` at `hooks/loop-plan-file-validator.sh:26-27`, but the value is not parsed; decisions are driven by repository state and RLCR state files. It locates the newest active loop under `.humanize/rlcr` with `find_active_loop` at `hooks/loop-plan-file-validator.sh:29-35`. If `finalize-state.md` exists, it switches from normal `state.md` to finalize state at `hooks/loop-plan-file-validator.sh:38-42`. It then parses state strictly at `hooks/loop-plan-file-validator.sh:44-48`, maps `STATE_PLAN_TRACKED`, `STATE_PLAN_FILE`, and `STATE_START_BRANCH` into local variables at `hooks/loop-plan-file-validator.sh:50-53`, and runs three guard families: schema presence, branch consistency, and plan tracking consistency.
- inputs_outputs_state: Inputs are stdin hook JSON, `CLAUDE_PROJECT_DIR` or current working directory, `.humanize/rlcr/<timestamp>/state.md` or `finalize-state.md`, the current Git branch, and Git tracking/status for `STATE_PLAN_FILE`. Outputs are either silent `exit 0` allow, JSON `{"decision":"block","reason":...}` with `exit 0` for user-facing hook blocks, or stderr plus `exit 1` only for malformed state parsing at `hooks/loop-plan-file-validator.sh:45-48`. No files are written. State transitions are observational: the hook does not advance rounds or cursors, but it detects invalid loop-state transitions such as branch drift from `start_branch`, tracked-plan becoming untracked, ignored-plan becoming tracked, or tracked-plan becoming dirty.
- gates_or_invariants: Active-loop gate: if no active loop directory exists, allow immediately at `hooks/loop-plan-file-validator.sh:30-35`. Finalize phase gate: prefer `finalize-state.md` over `state.md` when present at `hooks/loop-plan-file-validator.sh:38-42`; tests assert this works in `tests/test-finalize-phase.sh:853-865`. Strict state gate: malformed state blocks for safety at `hooks/loop-plan-file-validator.sh:44-48`; strict parsing itself requires YAML frontmatter, `current_round`, `max_iterations`, `review_started`, and `base_branch`, validates numeric round fields and boolean `review_started` in `hooks/lib/loop-common.sh:246-329`. Schema compatibility gate: `plan_tracked` and `start_branch` must be non-empty at `hooks/loop-plan-file-validator.sh:59-89`, using `prompt-template/block/schema-outdated.md:1-10` when available. Branch invariant: current `git rev-parse --abbrev-ref HEAD` must equal `start_branch`; Git failure or timeout blocks at `hooks/loop-plan-file-validator.sh:95-115`. Plan tracking invariant has two modes. If `plan_tracked=true`, `git ls-files --error-unmatch` must report tracked and `git status --porcelain` must be empty at `hooks/loop-plan-file-validator.sh:123-192`. If `plan_tracked` is not true, the file must not be tracked at `hooks/loop-plan-file-validator.sh:193-229`. Git timeout exit `124` and unexpected Git failures fail closed in both modes.
- dependencies_and_callers: Registered as a Claude/UserPromptSubmit command in `hooks/hooks.json:4-12`. Uses `hooks/lib/loop-common.sh` for field constants, template initialization, active-loop discovery, and strict state parsing; `find_active_loop` chooses only the newest timestamped loop and ignores older zombie loops at `hooks/lib/loop-common.sh:151-174`. Uses `hooks/lib/template-loader.sh` through `loop-common.sh`; `load_and_render_safe` falls back when templates are missing at `hooks/lib/template-loader.sh:167-203`. Uses `scripts/portable-timeout.sh`, whose `run_with_timeout` selects `gtimeout`, GNU `timeout`, Python, or no-timeout fallback at `scripts/portable-timeout.sh:9-71`. The state fields this hook enforces are created by `scripts/setup-rlcr-loop.sh:709-725`. Initial plan path and tracking constraints are validated more deeply during setup, including relative path, no spaces/metacharacters, no symlinks, inside project, not in submodule, and initial tracking mode at `scripts/setup-rlcr-loop.sh:309-470`; this hook rechecks only drift-prone Git/branch invariants during the live loop. Companion Stop-hook behavior performs later content-integrity comparison against loop backup at `hooks/loop-codex-stop-hook.sh:267-335`, so this validator is a prompt-time tracking/branch guard, not the full plan content diff gate.
- edge_cases_or_failure_modes: If no active loop exists, it exits silently and cannot block unrelated sessions. If the newest loop directory lacks valid state/frontmatter, it emits a safety error and exits `1`, unlike user-facing JSON blocks. If `jq` is unavailable, `schema_validation_error` can fail because it uses `jq -Rs` at `hooks/loop-plan-file-validator.sh:67-70`; most block responses elsewhere are raw heredocs. `FULL_PLAN_PATH` is assigned at `hooks/loop-plan-file-validator.sh:121` but unused, so this hook does not directly verify existence/readability/content of the plan file; those checks are owned by setup and stop-hook paths. A missing `plan_file` with otherwise valid strict state is not explicitly included in `REQUIRED_FIELDS` at `hooks/loop-plan-file-validator.sh:79-89`, so subsequent Git checks would operate on an empty path. Any `plan_tracked` value other than literal `true` is treated as untracked/gitignored mode at `hooks/loop-plan-file-validator.sh:123` and `hooks/loop-plan-file-validator.sh:193`, so malformed boolean values are not separately rejected here. The timeout wrapper can degrade to no timeout if no implementation is present, printing a warning but still running Git at `scripts/portable-timeout.sh:64-69`.
- validation_or_tests: `tests/test-plan-file-hooks.sh` directly exercises this hook. It verifies valid state gives exit 0 and no output at `tests/test-plan-file-hooks.sh:93-106`, YAML-quoted `plan_file` parsing at `tests/test-plan-file-hooks.sh:108-121`, malformed/missing schema field blocking at `tests/test-plan-file-hooks.sh:123-163`, branch-change blocking at `tests/test-plan-file-hooks.sh:168-191`, quoted `start_branch` parsing and mismatch detection at `tests/test-plan-file-hooks.sh:347-396`, and hyphenated plan paths at `tests/test-plan-file-hooks.sh:443-474`. Finalize-state parsing is covered at `tests/test-finalize-phase.sh:853-865`. Robustness coverage confirms the hook should not crash on empty JSON and is expected to pass through non-plan-file tool JSON without a block decision at `tests/robustness/test-hook-system-robustness.sh:217-243`, though this hook ignores the tool payload and the pass-through result comes from unchanged global loop invariants.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `1/1 item evidence section present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`