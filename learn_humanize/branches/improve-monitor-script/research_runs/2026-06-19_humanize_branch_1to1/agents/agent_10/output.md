# agent_10 improve-monitor-script 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `5af20b79e6fec323a2d5cb9344a6a584db1c635a`

## Item Evidence

### IMPROVE_MONITOR_SCRIPT-HZ-010 `directory` `prompt-template/claude`
- cursor: `[_]`
- core_role: Follow-up-round Claude prompt fragments for the RLCR loop. This directory is not the initial bootstrap prompt; `scripts/setup-rlcr-loop.sh:602-653` creates round 0 inline. These files are used after Codex review finds more work and the stop hook blocks exit, increments the loop round, and builds the next Claude prompt at `hooks/loop-codex-stop-hook.sh:967-1018`.
- algorithmic_behavior: The directory has no nested subdirectories; recursive inspection found five Markdown fragments: `next-round-prompt.md`, `post-alignment-action-items.md`, `next-round-footer.md`, `push-every-round-note.md`, and `goal-tracker-update-request.md`. The hook first renders `claude/next-round-prompt.md` into `.humanize/rlcr/<timestamp>/round-N-prompt.md` with `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE` substitutions (`hooks/loop-codex-stop-hook.sh:973-988`). On rounds immediately following a full alignment check, it appends `post-alignment-action-items.md` (`hooks/loop-codex-stop-hook.sh:990-995`). It always appends the footer rendered with `NEXT_SUMMARY_FILE` (`hooks/loop-codex-stop-hook.sh:998-1002`). If `push_every_round` is enabled, it appends `push-every-round-note.md` (`hooks/loop-codex-stop-hook.sh:1004-1010`). It finally appends `goal-tracker-update-request.md` or an inline fallback (`hooks/loop-codex-stop-hook.sh:1013-1018`).
- inputs_outputs_state: Inputs are Codex review content, original plan path, goal-tracker path, next summary path, current round number, and `push_every_round` state. Output is the next round prompt file. The state transition is controlled outside the templates: the stop hook updates `state.md` from the current round to `NEXT_ROUND` with `sed` and atomic temp-file move (`hooks/loop-codex-stop-hook.sh:967-970`) before writing the next prompt. `next-round-prompt.md:1-19` tells Claude the work is not finished, includes the original plan via `@{{PLAN_FILE}}`, requires todos for all discovered issues, and embeds Codex review content. `next-round-prompt.md:22-31` makes the goal tracker read-only after round 0 and routes changes through a request section. `next-round-footer.md:4-9` forbids escaping the loop and requires code simplifier review if installed, a commit, and a summary at `@{{NEXT_SUMMARY_FILE}}`. `goal-tracker-update-request.md:2-16` defines the structured summary section Claude must emit when requesting goal-tracker changes.
- gates_or_invariants: The directory encodes loop-continuation invariants rather than executing validation directly. Important invariants are: do not exit by lying/editing loop state/canceling (`next-round-footer.md:4`), commit work and write the required summary (`next-round-footer.md:6-9`), do not directly edit `goal-tracker.md` after round 0 (`next-round-prompt.md:30-31`), and address all discovered issues rather than only the most important (`next-round-prompt.md:12-13`). Post-alignment rounds add targeted gates for forgotten items, acceptance criteria status, and unjustified deferrals (`post-alignment-action-items.md:4-7`). Push behavior is conditional on `push_every_round` and enforced by appended prompt text plus a separate git-ahead gate in the stop hook (`hooks/loop-codex-stop-hook.sh:477-502`).
- dependencies_and_callers: Main caller is `hooks/loop-codex-stop-hook.sh`. Rendering depends on `hooks/lib/template-loader.sh`, where template placeholders use `{{VAR}}`, missing variables are preserved, and substitution is single-pass to avoid accidental prompt corruption (`hooks/lib/template-loader.sh:7-13`, `hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-129`). `load_and_render_safe` supplies fallbacks for `next-round-prompt.md` and `next-round-footer.md` (`hooks/lib/template-loader.sh:167-203`). Plain `load_template` is used for optional append-only fragments, with local fallbacks only for push note and goal tracker update request (`hooks/loop-codex-stop-hook.sh:1006-1017`). Template directory structure is validated as requiring `block`, `codex`, and `claude` subdirectories (`hooks/lib/template-loader.sh:205-221`), and comprehensive tests assert those subdirectories exist and all templates load non-empty (`tests/test-templates-comprehensive.sh:64-107`).
- edge_cases_or_failure_modes: If `next-round-prompt.md` or `next-round-footer.md` is missing or renders empty, safe fallbacks keep loop continuation functional. If `post-alignment-action-items.md` is missing, no fallback is appended, so the loop still runs but loses extra full-alignment remediation emphasis. If `push-every-round-note.md` is missing while push mode is enabled, the hook appends "Also push your changes after committing." If `goal-tracker-update-request.md` is missing, the hook appends a shorter fallback. Because rendering is single-pass, embedded Codex review content containing template-shaped text is not recursively expanded. A subtle documentation/UX edge case is that `next-round-footer.md:7` names several code-simplifier invocation forms; if a plugin name changes, this is prompt-only guidance and not validated by tests.
- validation_or_tests: `tests/test-templates-comprehensive.sh:74-80` checks `claude/` exists as a required template subdirectory. `tests/test-templates-comprehensive.sh:90-107` loads every Markdown template and fails on empty content. `tests/test-template-references.sh:130-141` checks whether templates are directly referenced in hooks, though it treats unreferenced templates as warnings. No dedicated assertion was found for exact rendered Claude next-round prompt composition, post-alignment conditional append, or push-note conditional append.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-040 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Specialized note injected into the git-not-clean block when untracked `.humanize*` paths are detected. It classifies RLCR local state directories as non-commit artifacts and directs the worker to add `.humanize*` to `.gitignore`.
- algorithmic_behavior: The stop hook uses cached git status during its early "Git Clean and Pushed" gate (`hooks/loop-codex-stop-hook.sh:411-450`). If any staged or unstaged status exists, it sets `GIT_ISSUES="uncommitted changes"` (`hooks/loop-codex-stop-hook.sh:422-425`). It extracts untracked lines with `grep '^??'` (`hooks/loop-codex-stop-hook.sh:426-427`). If those untracked lines contain `.humanize`, it loads this template (`hooks/loop-codex-stop-hook.sh:429-435`) and concatenates it into `SPECIAL_NOTES`. The final block message is rendered through `block/git-not-clean.md`, where `{{SPECIAL_NOTES}}` is inserted after the detected git issue (`prompt-template/block/git-not-clean.md:1-5`; hook render at `hooks/loop-codex-stop-hook.sh:452-460`). The JSON hook response blocks exit with `decision: block` and a system message telling the user to commit first (`hooks/loop-codex-stop-hook.sh:462-470`).
- inputs_outputs_state: Inputs are `GIT_STATUS_CACHED`, its untracked subset, `TEMPLATE_DIR`, and the parent `SPECIAL_NOTES` accumulator. The template itself has no variables. Its output is Markdown text with a bash snippet: append `.humanize*` to `.gitignore` and stage `.gitignore` (`prompt-template/block/git-not-clean-humanize-local.md:2-8`). It does not mutate state directly; the state transition is stop-hook control flow from attempted exit to blocked exit. If the agent follows the prompt, repository state transitions from untracked `.humanize*` noise to tracked `.gitignore` coverage plus staged `.gitignore`, then the normal git-clean gate can pass after commit.
- gates_or_invariants: It participates in a hard pre-review gate: Codex review is not run while git status is dirty (`hooks/loop-codex-stop-hook.sh:411-470`). The invariant is that `.humanize/` is generated by `humanize:start-rlcr-loop` and should not be committed (`prompt-template/block/git-not-clean-humanize-local.md:2-4`). It is only advisory within the block; the actual gate remains "no uncommitted changes." The parent template also enforces no AI attribution in commit messages (`prompt-template/block/git-not-clean.md:11-15`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:431`. It depends on `load_template`, which returns empty and warns when missing (`hooks/lib/template-loader.sh:33-48`). If this template is missing, the hook uses an inline fallback: "Note: .humanize* directories are intentionally untracked." (`hooks/loop-codex-stop-hook.sh:431-434`). It coordinates with `prompt-template/block/git-not-clean.md`, which hosts `SPECIAL_NOTES`, and with `prompt-template/block/git-not-clean-untracked.md`, which handles other untracked files (`hooks/loop-codex-stop-hook.sh:438-445`).
- edge_cases_or_failure_modes: Matching uses `grep -q '\.humanize'` over all untracked lines, so any untracked path containing `.humanize` triggers this note, not only the root `.humanize/` directory. If `.humanize*` is already ignored but other tracked changes exist, this note does not appear unless an untracked `.humanize` line remains. The suggested command uses `echo '.humanize*' >> .gitignore`, which can duplicate ignore entries and assumes `.gitignore` is the desired ignore surface. Because the template is loaded without rendering, future placeholders added to it would not be substituted.
- validation_or_tests: There is no dedicated behavior test for `.humanize*` detection or this template’s exact output. It is indirectly covered by all-template load checks (`tests/test-templates-comprehensive.sh:90-107`) and reference checks that search hook calls to templates (`tests/test-template-references.sh:130-141`). The parent git-not-clean and untracked templates have broader render coverage nearby, but this special-case note itself is not asserted in the inspected tests.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-070 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Shared Codex review prompt section defining how Codex must evaluate and apply Claude’s requested goal-tracker changes after round 0. It is a reviewer contract that transfers goal-tracker mutation authority from Claude to Codex/the stop-hook review lane.
- algorithmic_behavior: The stop hook builds this section before choosing full-alignment versus regular Codex review (`hooks/loop-codex-stop-hook.sh:638-642`). It renders `@{{GOAL_TRACKER_FILE}}` into line 6 of the template, producing a concrete target path. The rendered section is then injected as `GOAL_TRACKER_UPDATE_SECTION` into either `prompt-template/codex/full-alignment-review.md` or `prompt-template/codex/regular-review.md` (`hooks/loop-codex-stop-hook.sh:679-709`). Both parent templates place it under "Part 3" (`prompt-template/codex/regular-review.md:47`; `prompt-template/codex/full-alignment-review.md:57`). The section instructs Codex to evaluate whether Claude’s requested changes serve the Ultimate Goal, update the goal tracker if approved, avoid modifying the immutable Ultimate Goal and Acceptance Criteria section, and explain rejected requests (`prompt-template/codex/goal-tracker-update-section.md:3-17`).
- inputs_outputs_state: Input is `GOAL_TRACKER_FILE`, set by the active loop directory as `.humanize/rlcr/<timestamp>/goal-tracker.md` in the stop hook (`hooks/loop-codex-stop-hook.sh:537`, then rendered at `hooks/loop-codex-stop-hook.sh:641-642`). Output is Markdown embedded into the Codex review prompt file at `.humanize/rlcr/<timestamp>/round-N-review-prompt.md` (`hooks/loop-codex-stop-hook.sh:632-634`, `hooks/loop-codex-stop-hook.sh:681-709`). If Codex follows it and approves a request, state changes may include moving tasks among Active, Completed and Verified, Explicitly Deferred, adding Plan Evolution entries, or adding Open Issues (`prompt-template/codex/goal-tracker-update-section.md:5-17`). This complements the Claude-side rule in `prompt-template/claude/next-round-prompt.md:30-31` and `prompt-template/claude/goal-tracker-update-request.md:2-16`, where Claude may request but not directly perform goal-tracker updates after round 0.
- gates_or_invariants: The key invariant is immutability of the goal tracker’s Ultimate Goal and Acceptance Criteria after round 0 (`prompt-template/codex/goal-tracker-update-section.md:10`). The approval gate is semantic: Codex must decide whether a requested change is justified and serves the Ultimate Goal (`prompt-template/codex/goal-tracker-update-section.md:5`). Deferrals require strong justification (`prompt-template/codex/goal-tracker-update-section.md:17`). The parent Codex prompts add completion gates: regular review may only end with COMPLETE when all original plan tasks are complete with no deferrals or pending work (`prompt-template/codex/regular-review.md:51-57`), and full alignment adds acceptance-criteria audit and stagnation circuit breaker behavior (`prompt-template/codex/full-alignment-review.md:19-93`).
- dependencies_and_callers: Direct caller is `hooks/loop-codex-stop-hook.sh:641`. The template depends on `load_and_render_safe`, so missing or empty content falls back to a shorter inline section (`hooks/loop-codex-stop-hook.sh:638-642`; fallback mechanics at `hooks/lib/template-loader.sh:167-203`). It is consumed by both `codex/full-alignment-review.md` and `codex/regular-review.md`. The rendered review prompt is sent to `codex exec` with configured model and effort from state (`hooks/loop-codex-stop-hook.sh:774-801`). The stop hook then validates Codex’s review output exists and is non-empty before reading completion markers (`hooks/loop-codex-stop-hook.sh:858-920`).
- edge_cases_or_failure_modes: The section is injected into parent templates as a heading fragment; the parent line reads `## Part 3: {{GOAL_TRACKER_UPDATE_SECTION}}`, while the fragment itself begins with `## Goal Tracker Update Requests`, resulting in nested or awkward Markdown heading structure after rendering. Because `render_template` is single-pass, if `GOAL_TRACKER_FILE` contains template-shaped text it will be inserted literally rather than recursively expanded. The template delegates correctness of actual goal-tracker edits to Codex review behavior; there is no parser enforcing that the immutable section was untouched at this template layer. If the template is missing, the fallback preserves minimal behavior but loses detailed instructions for moving tasks, plan evolution, open issues, and deferrals.
- validation_or_tests: `tests/test-templates-comprehensive.sh:531-539` directly renders this template with `.humanize/rlcr/20240101/goal-tracker.md` and asserts the output contains "Goal Tracker Update Requests" plus the rendered path. General template syntax/load coverage also applies (`tests/test-templates-comprehensive.sh:90-107`). Template-loader behavior for fallback and single-pass substitution is covered by the loader implementation and related tests in the same suite, but no inspected test validates that Codex actually edits only allowed goal-tracker sections.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: IMPROVE_MONITOR_SCRIPT-HZ-010, IMPROVE_MONITOR_SCRIPT-HZ-040, IMPROVE_MONITOR_SCRIPT-HZ-070
- missing_items: none
- duplicate_items: none in Item Evidence headings
- final_worker_status: `complete`