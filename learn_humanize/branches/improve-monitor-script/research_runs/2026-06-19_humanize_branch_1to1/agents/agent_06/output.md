# agent_06 improve-monitor-script 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `5af20b79e6fec323a2d5cb9344a6a584db1c635a`

## Item Evidence

### IMPROVE_MONITOR_SCRIPT-HZ-006 `directory` `tests`
- cursor: `[_]`
- core_role: The `tests` directory is the executable specification suite for the Humanize/RLCR loop control algorithm. It is not runtime production code, but it encodes the expected behavior for the loop’s validators, prompt/template rendering, plan-file protection, todo completion gate, state lifecycle, allowlist policy, and aggregate test runner behavior. The directory is flat: `find tests -type d` returns only `tests`, and it contains 12 executable shell scripts, all with executable mode in `ls -la tests`.
- algorithmic_behavior: `tests/run-all-tests.sh` is the suite coordinator. It defines the ordered test list at `tests/run-all-tests.sh:33`, runs each script by path at `tests/run-all-tests.sh:58`, captures output and exit code at `tests/run-all-tests.sh:60`, strips ANSI color codes before parsing counts at `tests/run-all-tests.sh:64`, accumulates pass/fail totals at `tests/run-all-tests.sh:70`, and exits nonzero if any suite fails at `tests/run-all-tests.sh:91`. The child tests cover these algorithmic areas:
  - `tests/test-template-loader.sh` sources `hooks/lib/template-loader.sh` at `tests/test-template-loader.sh:12` and verifies template directory resolution, loading, single-pass rendering, fallback rendering, unreplaced placeholders, shell metacharacters, placeholder injection prevention, Unicode-ish values, and variable-name edge cases. The injection regression is explicit around `tests/test-template-loader.sh:489` and the realistic `REVIEW_CONTENT` scenario is at `tests/test-template-loader.sh:525`.
  - `tests/test-bash-validator-patterns.sh` sources `hooks/lib/loop-common.sh` at `tests/test-bash-validator-patterns.sh:13` and unit-tests `command_modifies_file` through `assert_modifies` and `assert_not_modifies` helpers at `tests/test-bash-validator-patterns.sh:37` and `tests/test-bash-validator-patterns.sh:51`. It validates shell write-detection patterns for redirection, `tee`, in-place editors, file operations, `dd`, `truncate`, `exec`, and false positives across groups beginning at `tests/test-bash-validator-patterns.sh:70`.
  - `tests/test-todo-checker.sh` is separately covered below; in the directory suite it is the TodoWrite transcript completion gate spec.
  - `tests/test-plan-file-validation.sh` tests `scripts/setup-rlcr-loop.sh` plan-file admission rules. It rejects absolute paths at `tests/test-plan-file-validation.sh:89`, missing files and dirs at `tests/test-plan-file-validation.sh:104`, spaces at `tests/test-plan-file-validation.sh:128`, shell metacharacters at `tests/test-plan-file-validation.sh:169`, symlinks at `tests/test-plan-file-validation.sh:189`, repository escape at `tests/test-plan-file-validation.sh:245`, non-git and no-commit repos at `tests/test-plan-file-validation.sh:274`, tracked/untracked policy mismatches around `tests/test-plan-file-validation.sh:329`, YAML-unsafe branch/model/effort values around `tests/test-plan-file-validation.sh:432`, low-content plans at `tests/test-plan-file-validation.sh:518`, valid content at `tests/test-plan-file-validation.sh:610`, and `--plan-file` option parsing at `tests/test-plan-file-validation.sh:667`.
  - `tests/test-template-references.sh` scans hardcoded hook scripts for template references, defined at `tests/test-template-references.sh:56`, extracts direct `load_template`, `load_and_render`, and `load_and_render_safe` references at `tests/test-template-references.sh:83`, verifies actual template files at `tests/test-template-references.sh:100`, inventories all template markdown files at `tests/test-template-references.sh:121`, validates core block templates at `tests/test-template-references.sh:152`, and ensures critical validators use safe rendering at `tests/test-template-references.sh:176`.
  - `tests/test-state-exit-naming.sh` validates active-loop discovery and state exit naming. It covers `state.md` as the only active marker at `tests/test-state-exit-naming.sh:38`, inactive renamed state files at `tests/test-state-exit-naming.sh:93`, newest loop precedence at `tests/test-state-exit-naming.sh:157`, `end_loop` invalid reason rejection at `tests/test-state-exit-naming.sh:182`, valid reason file creation at `tests/test-state-exit-naming.sh:202`, missing state handling at `tests/test-state-exit-naming.sh:228`, and legacy path rejection at `tests/test-state-exit-naming.sh:254`.
  - `tests/test-templates-comprehensive.sh` performs broad template-system validation: directory structure at `tests/test-templates-comprehensive.sh:62`, loading every template at `tests/test-templates-comprehensive.sh:83`, placeholder syntax validation at `tests/test-templates-comprehensive.sh:114`, malformed placeholder detection at `tests/test-templates-comprehensive.sh:197`, render edge cases at `tests/test-templates-comprehensive.sh:262`, fallback behavior at `tests/test-templates-comprehensive.sh:441`, real-template integration at `tests/test-templates-comprehensive.sh:493`, and stress tests at `tests/test-templates-comprehensive.sh:543`.
  - `tests/test-plan-file-hooks.sh` is the large integration suite for RLCR hooks. It validates plan-file hook pass/fail state at `tests/test-plan-file-hooks.sh:91`, missing state fields at `tests/test-plan-file-hooks.sh:121`, branch mismatch at `tests/test-plan-file-hooks.sh:164`, write/edit/bash protection of loop plan backup at `tests/test-plan-file-hooks.sh:194`, command-substitution/glob/brace/piped/backtick bypass protections at `tests/test-plan-file-hooks.sh:272`, stop-hook parsing and plan modification/deletion protections at `tests/test-plan-file-hooks.sh:388`, `tests/test-plan-file-hooks.sh:469`, and `tests/test-plan-file-hooks.sh:509`, outdated schema JSON blocking at `tests/test-plan-file-hooks.sh:631`, goal-tracker placeholder reporting at `tests/test-plan-file-hooks.sh:737`, and legacy `.humanize-loop.local` allow behavior at `tests/test-plan-file-hooks.sh:1016`.
  - `tests/test-error-scenarios.sh` is negative-path coverage for template loading/rendering: missing template file at `tests/test-error-scenarios.sh:43`, missing template directory at `tests/test-error-scenarios.sh:55`, missing `load_and_render` behavior at `tests/test-error-scenarios.sh:68`, strict shell continuation around missing templates at `tests/test-error-scenarios.sh:109`, safe fallback at `tests/test-error-scenarios.sh:134`, valid/invalid template dirs at `tests/test-error-scenarios.sh:161`, empty placeholder at `tests/test-error-scenarios.sh:183`, and unclosed placeholder at `tests/test-error-scenarios.sh:196`.
  - `tests/test-ansi-parsing.sh` tests the aggregate runner’s ANSI stripping and count extraction contract. It exercises basic color stripping at `tests/test-ansi-parsing.sh:42`, pass-count extraction at `tests/test-ansi-parsing.sh:70`, fail-count extraction at `tests/test-ansi-parsing.sh:84`, multiline output at `tests/test-ansi-parsing.sh:112`, plain text at `tests/test-ansi-parsing.sh:133`, and bold/color combinations at `tests/test-ansi-parsing.sh:147`.
  - `tests/test-allowlist-validators.sh` validates the small allowlist of round files and how read/write/edit/bash validators honor it. It starts with direct allowlist function checks at `tests/test-allowlist-validators.sh:70`, then write-validator behavior at `tests/test-allowlist-validators.sh:133`, edit-validator behavior at `tests/test-allowlist-validators.sh:189`, read-validator behavior at `tests/test-allowlist-validators.sh:232`, and bash-validator same-basename/different-root protections at `tests/test-allowlist-validators.sh:288`.
- inputs_outputs_state: Inputs are shell test commands, fixture files generated under `mktemp` directories, synthetic hook JSON sent on stdin, temporary git repositories, mock `.humanize/rlcr/<loop>/state.md` trees, template files under `prompt-template`, and production scripts under `hooks` and `scripts`. Outputs are colored PASS/FAIL lines, `Passed:` and `Failed:` summary counters, and script exit codes. State is deliberately local to each test script via `TESTS_PASSED`, `TESTS_FAILED`, temp directories, fake loop directories, and trap cleanup. The top-level runner converts child script output into aggregate totals and failed-suite names.
- gates_or_invariants: Core invariants encoded by the directory include: validators should block with exit code `2` when an operation violates loop file policy; the todo checker should use `0`, `1`, and `2` for allow/incomplete/parse-error states; only the newest `.humanize/rlcr` directory with `state.md` is active; renamed state files are terminal and must not revive a loop; prompt and plan backup files are protected; goal-tracker writes are restricted after Round 0; template variables use uppercase `{{VAR}}` syntax; template rendering is single-pass; missing critical templates must have safe fallbacks; and `run-all-tests.sh` must reliably parse colored child output.
- dependencies_and_callers: The directory depends on `bash`, `python3`, `jq`, `git`, `sed`, `awk`, `grep`, `find`, `mktemp`, and standard Unix file utilities. It directly exercises `hooks/check-todos-from-transcript.py`, `hooks/lib/template-loader.sh`, `hooks/lib/loop-common.sh`, `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, `hooks/loop-codex-stop-hook.sh`, and `scripts/setup-rlcr-loop.sh`. The tests are caller-side validation assets, normally invoked via `tests/run-all-tests.sh`.
- edge_cases_or_failure_modes: Several tests assume a writable temp area and usable local git; in a read-only sandbox those setup phases can fail even if the logic under test is correct. The bash validator coverage is regex-based rather than shell-AST-based, so the suite defends known write forms and bypass patterns but cannot prove complete shell-language coverage. `tests/test-template-references.sh` only extracts direct quoted `$TEMPLATE_DIR` references, so dynamic template references may only produce warnings or be missed. `tests/test-todo-checker.sh` labels one case Unicode, but its fixture content is ASCII text, so that specific test does not prove non-ASCII transcript handling.
- validation_or_tests: The directory is itself the validation suite. I inspected it recursively: 12 executable shell scripts, 5,050 total lines, no nested subdirectories. I did not execute the suite because this assignment is research-only in a read-only branch export, and several tests intentionally create temp repos/files and mutate fixture state.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-036 `file` `tests/test-todo-checker.sh`
- cursor: `[_]`
- core_role: This file is the executable specification for `hooks/check-todos-from-transcript.py`, the hook helper that reads a Claude transcript and blocks continuation when the latest TodoWrite state still contains unfinished todos. It anchors a core loop invariant: a worker/Claude round should not exit cleanly while its native TodoWrite list has pending or in-progress work.
- algorithmic_behavior: The script defines `TODO_CHECKER="$PROJECT_ROOT/hooks/check-todos-from-transcript.py"` at `tests/test-todo-checker.sh:13`, creates a temp fixture directory at `tests/test-todo-checker.sh:37`, and registers cleanup at `tests/test-todo-checker.sh:38`. It uses `pass` and `fail` helpers at `tests/test-todo-checker.sh:24` and `tests/test-todo-checker.sh:29`, then executes 16 assertions in four groups. Group 1 validates hook input handling: invalid JSON and empty stdin must exit `2` at `tests/test-todo-checker.sh:51` and `tests/test-todo-checker.sh:63`, while valid JSON without `transcript_path` and non-existent transcript files must exit `0` at `tests/test-todo-checker.sh:75` and `tests/test-todo-checker.sh:87`. Group 2 validates todo interpretation: all completed todos exit `0` at `tests/test-todo-checker.sh:106`, pending todos exit `1` at `tests/test-todo-checker.sh:121`, incomplete task names appear in output at `tests/test-todo-checker.sh:136`, and `in_progress` exits `1` at `tests/test-todo-checker.sh:144`. Group 3 validates transcript format tolerance: empty transcript files exit `0` at `tests/test-todo-checker.sh:166`, invalid JSONL lines are ignored at `tests/test-todo-checker.sh:179`, the latest TodoWrite call wins at `tests/test-todo-checker.sh:196`, direct `tool_use` entries work at `tests/test-todo-checker.sh:212`, and `type: message` entries work at `tests/test-todo-checker.sh:227`. Group 4 validates todo edge cases: missing `status` is incomplete at `tests/test-todo-checker.sh:249`, empty content is still incomplete at `tests/test-todo-checker.sh:265`, and the labeled Unicode case exits `0` at `tests/test-todo-checker.sh:280`.
- inputs_outputs_state: Inputs are stdin JSON documents passed to the Python checker and temporary JSONL transcript fixtures written under `$TEST_DIR`. The checker’s expected input contract is `{"transcript_path": "..."}`. Outputs are the checker’s exit code plus any stdout/stderr captured into `RESULT`. The shell test state is just `TESTS_PASSED`, `TESTS_FAILED`, temp files, and `EXIT_CODE`. The target Python checker parses stdin JSON at `hooks/check-todos-from-transcript.py:92`, expands `transcript_path` at `hooks/check-todos-from-transcript.py:104`, finds latest todos at `hooks/check-todos-from-transcript.py:107`, builds incomplete entries at `hooks/check-todos-from-transcript.py:114`, prints `INCOMPLETE_TODOS` plus details at `hooks/check-todos-from-transcript.py:124`, and exits at `hooks/check-todos-from-transcript.py:126` or `hooks/check-todos-from-transcript.py:129`.
- gates_or_invariants: The key gate is status equality: only `status == "completed"` is accepted; anything else, including `pending`, `in_progress`, missing status, and empty status, is incomplete. The latest TodoWrite call overrides previous calls, demonstrated by the pending old task followed by a completed new task at `tests/test-todo-checker.sh:198`. Invalid hook JSON is a hard parse failure, but malformed transcript JSONL lines are ignored. Missing `transcript_path`, non-existent transcript files, empty transcript files, and no TodoWrite entries are non-blocking.
- dependencies_and_callers: The test depends on `bash`, `python3`, `grep`, `mktemp`, and the Python target file. The Python target depends only on the standard library: `json`, `sys`, and `pathlib` at `hooks/check-todos-from-transcript.py:15`. This checker is designed as a hook helper; the docstring says it reads a Claude Code transcript JSONL file and finds the most recent TodoWrite call at `hooks/check-todos-from-transcript.py:3`.
- edge_cases_or_failure_modes: The tested transcript formats match the checker’s three parsing paths: assistant message content at `hooks/check-todos-from-transcript.py:51`, alternative message content at `hooks/check-todos-from-transcript.py:65`, and direct tool use at `hooks/check-todos-from-transcript.py:78`. Invalid JSONL lines are deliberately skipped, so corrupted lines cannot block unless all useful TodoWrite state is lost. The suite does not appear to test a malformed `todos` array containing non-object values; the checker assumes each todo supports `.get` at `hooks/check-todos-from-transcript.py:116`. The “Unicode” fixture at `tests/test-todo-checker.sh:280` uses ASCII content, so it is a weak Unicode assertion despite the test name.
- validation_or_tests: The file is a self-contained 16-test shell suite and exits `0` only when `TESTS_FAILED` is zero at `tests/test-todo-checker.sh:305`; otherwise it exits `1` at `tests/test-todo-checker.sh:309`. I inspected it directly in full and traced it to the Python implementation. I did not run it because the branch export is read-only and the assignment asks for research notes only.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-066 `file` `prompt-template/claude/next-round-prompt.md`
- cursor: `[_]`
- core_role: This is the Claude prompt template for the next RLCR round after Codex reviews a round and finds unresolved issues. It defines the algorithmic handoff contract from Codex review to Claude implementation: re-read the original plan, create Todos for all remaining work, address every discovered issue, consume the Codex review payload, and treat the goal tracker as read-only after Round 0.
- algorithmic_behavior: The template starts by telling Claude the work is not finished and to proceed with high reasoning effort at `prompt-template/claude/next-round-prompt.md:1`. It requires reviewing the original implementation plan via `@{{PLAN_FILE}}` at `prompt-template/claude/next-round-prompt.md:5`, and states that the plan contains the full scope at `prompt-template/claude/next-round-prompt.md:8`. It mandates Todo creation for all tasks and prohibits addressing only the most important issues at `prompt-template/claude/next-round-prompt.md:12`. It embeds Codex review feedback inside explicit start/end comments using `{{REVIEW_CONTENT}}` at `prompt-template/claude/next-round-prompt.md:16`. It then requires reading `@{{GOAL_TRACKER_FILE}}` for goal, acceptance criteria, active/completed/deferred tasks, plan evolution, and open issues at `prompt-template/claude/next-round-prompt.md:22`. Finally, it forbids direct `goal-tracker.md` modification after Round 0 and routes requested changes into a summary section at `prompt-template/claude/next-round-prompt.md:30`.
- inputs_outputs_state: Inputs are the template variables `PLAN_FILE`, `REVIEW_CONTENT`, and `GOAL_TRACKER_FILE`. The output is a rendered next-round prompt file, specifically `round-${NEXT_ROUND}-prompt.md`, created by `hooks/loop-codex-stop-hook.sh` after it updates `current_round` in `state.md` at `hooks/loop-codex-stop-hook.sh:967`. The stop hook sets `NEXT_PROMPT_FILE` at `hooks/loop-codex-stop-hook.sh:973` and renders this template through `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:985`. The rendered prompt then becomes the JSON block reason returned to Claude through `jq -n` at `hooks/loop-codex-stop-hook.sh:1024`. The state transition is: Codex review lacks terminal marker, loop increments to next round, next prompt is generated, and the hook blocks exit with the new instructions.
- gates_or_invariants: This template only applies when Codex does not return the terminal `COMPLETE` or `STOP` markers. The stop hook reads review content at `hooks/loop-codex-stop-hook.sh:913`, checks terminal markers at `hooks/loop-codex-stop-hook.sh:916`, exits on `COMPLETE` at `hooks/loop-codex-stop-hook.sh:922`, exits on `STOP` at `hooks/loop-codex-stop-hook.sh:933`, and only then enters the continue-loop path at `hooks/loop-codex-stop-hook.sh:963`. The prompt itself enforces the invariant that Claude must align with the original plan, must create Todos for all discovered issues, and must not directly mutate the goal tracker after Round 0.
- dependencies_and_callers: The direct caller is `hooks/loop-codex-stop-hook.sh`, which renders this file with `load_and_render_safe` and a fallback at `hooks/loop-codex-stop-hook.sh:977`. It depends on `hooks/lib/template-loader.sh` for safe single-pass substitution; the loader documents single-pass substitution and placeholder-injection prevention at `hooks/lib/template-loader.sh:50`, builds `TMPL_VAR_` environment variables at `hooks/lib/template-loader.sh:62`, and performs the awk replacement without rescanning substituted values at `hooks/lib/template-loader.sh:71`. After this template is rendered, the stop hook may append post-alignment instructions at `hooks/loop-codex-stop-hook.sh:990`, a footer at `hooks/loop-codex-stop-hook.sh:998`, an optional push note at `hooks/loop-codex-stop-hook.sh:1004`, and a goal-tracker update request template at `hooks/loop-codex-stop-hook.sh:1013`.
- edge_cases_or_failure_modes: If this template is missing or empty, the stop hook uses the inline `NEXT_ROUND_FALLBACK` at `hooks/loop-codex-stop-hook.sh:977`, so the loop can still provide next-round instructions. Because `REVIEW_CONTENT` is arbitrary generated text, the single-pass renderer is important: review text containing `{{PLAN_FILE}}` or other placeholders should remain literal rather than being accidentally re-expanded. The template itself does not include commit or summary instructions; those are appended later by `claude/next-round-footer.md`, so using this file alone would be incomplete outside the stop-hook assembly path.
- validation_or_tests: Template existence and reference coverage are validated by `tests/test-template-references.sh`, which scans `hooks/loop-codex-stop-hook.sh` and sibling hook files for template references at `tests/test-template-references.sh:56` and verifies referenced files exist at `tests/test-template-references.sh:100`. General template loading, syntax, fallback, and renderability are covered by `tests/test-templates-comprehensive.sh`, especially all-template loading at `tests/test-templates-comprehensive.sh:83`, syntax validation at `tests/test-templates-comprehensive.sh:114`, fallback tests at `tests/test-templates-comprehensive.sh:441`, and render-all stress coverage at `tests/test-templates-comprehensive.sh:563`. Placeholder injection behavior relevant to `REVIEW_CONTENT` is specifically tested in `tests/test-template-loader.sh:525`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 3 item-evidence sections, one per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`