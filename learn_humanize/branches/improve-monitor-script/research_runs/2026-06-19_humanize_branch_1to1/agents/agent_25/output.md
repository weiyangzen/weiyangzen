# agent_25 improve-monitor-script 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `5af20b79e6fec323a2d5cb9344a6a584db1c635a`

## Item Evidence

### IMPROVE_MONITOR_SCRIPT-HZ-025 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Repository-level executable specification and aggregate validation harness for the Humanize plugin’s shell-hook/template behavior. It is not a unit under test itself only; it defines the canonical ordered set of validation suites and the pass/fail aggregation contract used to decide whether the branch’s algorithmic behavior is acceptable.
- algorithmic_behavior: Initializes counters and a failed-suite list, then iterates the `TEST_SUITES` array in a fixed order from `test-template-loader.sh` through `test-allowlist-validators.sh` (`tests/run-all-tests.sh:28-45`). For each suite, it resolves the path relative to its own directory (`tests/run-all-tests.sh:14`, `tests/run-all-tests.sh:48`), skips missing suite files without failing (`tests/run-all-tests.sh:50-53`), captures stdout/stderr and exit status with `set +e` around the child execution (`tests/run-all-tests.sh:58-62`), strips ANSI escape sequences using a portable `$'\033'` sed pattern (`tests/run-all-tests.sh:64-66`), extracts the last `Passed:` and `Failed:` numeric counters (`tests/run-all-tests.sh:67-68`), aggregates totals (`tests/run-all-tests.sh:70-71`), records any suite with nonzero process exit or parsed failed count (`tests/run-all-tests.sh:73-78`), and exits `1` if any suite failed, otherwise `0` (`tests/run-all-tests.sh:91-102`).
- inputs_outputs_state: Input is the set of executable suite scripts in `tests/`, specifically the array entries at `tests/run-all-tests.sh:33-45`; each suite is expected to print parseable `Passed:` and `Failed:` lines and return a meaningful exit code. Output is human-readable terminal status with colored `PASSED`, `FAILED`, `SKIP`, aggregate totals, and optionally the tail of failed suite output. Internal state consists of `TOTAL_PASSED`, `TOTAL_FAILED`, and `FAILED_SUITES` (`tests/run-all-tests.sh:28-30`). The harness’s final state transition is binary: no failed suites means process exit `0`; at least one failed suite means process exit `1`.
- gates_or_invariants: The invariant for suite success is conjunctive: child exit code must be `0` and parsed failed count must be `0` (`tests/run-all-tests.sh:73`). Missing suite files are treated as skipped, not failed (`tests/run-all-tests.sh:50-52`), which keeps the harness tolerant of absent optional suites but means array drift can silently reduce coverage. The parser assumes summary labels match `Passed:[[:space:]]*[0-9]+` and `Failed:[[:space:]]*[0-9]+` after ANSI stripping (`tests/run-all-tests.sh:66-68`). `tests/test-ansi-parsing.sh` explicitly validates this exact ANSI stripping and counter extraction contract, including colored counts and multiline summaries (`tests/test-ansi-parsing.sh:38-48`, `tests/test-ansi-parsing.sh:73-90`, `tests/test-ansi-parsing.sh:116-125`). Most child suites implement the expected counter summary pattern, for example `tests/test-bash-validator-patterns.sh:203-204` and `tests/test-template-loader.sh:648-649`.
- dependencies_and_callers: Depends on Bash, executable child scripts, `sed`, `grep`, `tail`, ANSI-capable output formatting, and child suite adherence to `Passed:`/`Failed:` summary markers. The harness directly coordinates these suites: `test-template-loader.sh`, `test-bash-validator-patterns.sh`, `test-todo-checker.sh`, `test-plan-file-validation.sh`, `test-template-references.sh`, `test-state-exit-naming.sh`, `test-templates-comprehensive.sh`, `test-plan-file-hooks.sh`, `test-error-scenarios.sh`, `test-ansi-parsing.sh`, and `test-allowlist-validators.sh` (`tests/run-all-tests.sh:33-45`). The referenced suites exercise core hook/template algorithms such as template rendering, command modification detection, todo access controls, plan-file validation, state/exit naming, and allowlist validators. `tests/test-template-references.sh` also checks that expected block templates, including `block/summary-bash-write.md`, exist (`tests/test-template-references.sh:152-166`).
- edge_cases_or_failure_modes: Missing suites are only skipped, so a deleted or renamed script can lower validation coverage while still allowing overall success (`tests/run-all-tests.sh:50-53`). A suite that fails before printing a parseable summary contributes `0` passed and `0` failed by parser fallback, but still fails if its exit code is nonzero (`tests/run-all-tests.sh:60-68`, `tests/run-all-tests.sh:73`). A suite that exits `0` while printing a nonzero failed count is still failed (`tests/run-all-tests.sh:73`). Conversely, a broken suite that exits `0` and does not emit a parseable `Failed:` line would be treated as successful with zero tests, because `failed` falls back to `0` (`tests/run-all-tests.sh:68`, `tests/run-all-tests.sh:73-80`). ANSI parsing handles standard SGR sequences of the form `ESC[` plus digits/semicolons plus `m`, but does not remove non-SGR ANSI control sequences (`tests/run-all-tests.sh:65-66`). Only the last parsed pass/fail count is used because of `tail -1`, which is correct for multiline child output but could hide earlier intermediate summaries (`tests/run-all-tests.sh:67-68`).
- validation_or_tests: The harness is itself executable validation. Its internal parsing logic is covered by `tests/test-ansi-parsing.sh`, whose comments state it tests the portable ANSI stripping used in `run-all-tests.sh` (`tests/test-ansi-parsing.sh:38-39`). The suite summary output contract is broadly implemented across child suites, including `tests/test-plan-file-validation.sh:744-749`, `tests/test-plan-file-hooks.sh:1060-1065`, and `tests/test-allowlist-validators.sh:387-391`. I did not execute the harness because the branch export is read-only and the task asks for research notes only, but direct inspection confirms the validation flow and dependencies.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-055 `file` `prompt-template/block/summary-bash-write.md`
- cursor: `[_]`
- core_role: Prompt/block template for an enforcement gate that blocks Bash-based writes to RLCR round summary files and redirects the agent to Write/Edit tooling so summary validation hooks can verify the correct round number and path.
- algorithmic_behavior: The template emits a concise blocking message headed `Bash Write Blocked` (`prompt-template/block/summary-bash-write.md:1`). It instructs that Bash commands must not modify summary files (`prompt-template/block/summary-bash-write.md:3`), renders the allowed target path through the `{{CORRECT_PATH}}` placeholder (`prompt-template/block/summary-bash-write.md:5`), and states the reason: shell commands such as `cat`, `echo`, `sed`, and `awk` bypass validation hooks (`prompt-template/block/summary-bash-write.md:7-8`). At runtime, `hooks/loop-bash-validator.sh` detects Bash commands that modify `round-[0-9]+-summary.md`, computes the active loop’s correct current summary path, calls `summary_bash_blocked_message`, writes the rendered message to stderr, and exits with code `2` (`hooks/loop-bash-validator.sh:126-129`). The helper loads this template with `load_and_render_safe` and substitutes `CORRECT_PATH` (`hooks/lib/loop-common.sh:217-225`).
- inputs_outputs_state: Template input is `CORRECT_PATH`, passed as `CORRECT_PATH=$correct_path` by `summary_bash_blocked_message` (`hooks/lib/loop-common.sh:219-225`). The path originates from active loop state: `loop-bash-validator.sh` finds the active loop under `.humanize/rlcr`, parses `state.md`, sets `CURRENT_ROUND`, and builds `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` (`hooks/loop-bash-validator.sh:36-49`, `hooks/loop-bash-validator.sh:126-128`). Output is Markdown text sent to stderr by the validator, followed by hook exit status `2`, which indicates the tool action is blocked (`hooks/loop-bash-validator.sh:126-129`). The template itself carries no mutable state, but it participates in the loop state transition that prevents an invalid Bash write from mutating the summary file.
- gates_or_invariants: The gate invariant is that summary files must be modified through Write/Edit validators, not Bash, because Write/Edit paths enforce correct round-number and location validation. The template text explicitly encodes that invariant (`prompt-template/block/summary-bash-write.md:3-8`). The runtime invariant is path-pattern based: any Bash command that `command_modifies_file` classifies as modifying `round-[0-9]+-summary.md` triggers this block (`hooks/loop-bash-validator.sh:126-129`). The template-rendering invariant is single-pass `{{VARIABLE_NAME}}` substitution, so `{{CORRECT_PATH}}` is replaced by the provided value and missing placeholders remain literal (`hooks/lib/template-loader.sh:7-13`, `hooks/lib/template-loader.sh:50-58`, `hooks/lib/template-loader.sh:71-129`).
- dependencies_and_callers: Direct caller is `summary_bash_blocked_message` in `hooks/lib/loop-common.sh` (`hooks/lib/loop-common.sh:217-225`). That helper is called by `hooks/loop-bash-validator.sh` when a Bash tool invocation targets a round summary file for modification (`hooks/loop-bash-validator.sh:121-129`). The helper depends on `load_and_render_safe` from `hooks/lib/template-loader.sh`, which provides fallback behavior if a template is missing or empty (`hooks/lib/template-loader.sh:167-170`). `tests/test-template-references.sh` lists `block/summary-bash-write.md` among common templates expected to exist (`tests/test-template-references.sh:152-166`). Pattern-level coverage exists in `tests/test-bash-validator-patterns.sh`, where writes/appends to `round-5-summary.md` and `round-10-summary.md` are detected as modifications while a read-only `cat round-5-summary.md` is not (`tests/test-bash-validator-patterns.sh:186-194`).
- edge_cases_or_failure_modes: If the template is missing or empty, `load_and_render_safe` should fall back to the inline message in `summary_bash_blocked_message`, but the richer wording in this file would be lost (`hooks/lib/loop-common.sh:221-225`). If active loop detection fails, the Bash validator exits before this gate and allows all commands (`hooks/loop-bash-validator.sh:36-43`), so the template is only effective during an active RLCR loop. If `state.md` has an invalid or stale `current_round`, the rendered `CORRECT_PATH` can point to the wrong round even though the block still fires (`hooks/loop-bash-validator.sh:47-49`, `hooks/loop-bash-validator.sh:126-128`). Detection depends on `command_modifies_file`; read-only commands should not be blocked, as validated by `tests/test-bash-validator-patterns.sh:192-194`. The template mentions broad command examples such as `awk`, but actual blocking is determined by validator pattern logic, not by the template text itself.
- validation_or_tests: Existence/reference validation is covered by `tests/test-template-references.sh`, which requires `block/summary-bash-write.md` to exist among common templates (`tests/test-template-references.sh:152-166`). Rendering mechanics are exercised by template-loader integration tests using real templates and `CORRECT_PATH` substitution patterns (`tests/test-template-loader.sh:147-163`, `tests/test-templates-comprehensive.sh:493-510`). The summary-file modification pattern is covered by `tests/test-bash-validator-patterns.sh:186-194`. End-to-end hook behavior is implemented in `hooks/loop-bash-validator.sh:126-129`, which uses this template path via `summary_bash_blocked_message`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `IMPROVE_MONITOR_SCRIPT-HZ-025`, `IMPROVE_MONITOR_SCRIPT-HZ-055`
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`