# agent_02 improve-monitor-script 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `5af20b79e6fec323a2d5cb9344a6a584db1c635a`

## Item Evidence

### IMPROVE_MONITOR_SCRIPT-HZ-002 `directory` `commands`
- cursor: `[_]`
- core_role: Slash-command entrypoint layer for the RLCR loop. The directory contains exactly `commands/start-rlcr-loop.md` and `commands/cancel-rlcr-loop.md`; these are thin command manifests/instructions that route user intent into setup or cancellation behavior rather than implementing the full state machine themselves.
- algorithmic_behavior: `commands/start-rlcr-loop.md:1-5` declares the start command metadata, argument contract, and the only allowed shell execution: `${CLAUDE_PLUGIN_ROOT}/scripts/setup-rlcr-loop.sh:*`. `commands/start-rlcr-loop.md:12-14` executes that setup script with `$ARGUMENTS`. The file then defines the high-level loop algorithm: implement plan, write round summary, run Codex review on attempted exit, continue on issues, stop on `COMPLETE` (`commands/start-rlcr-loop.md:16-23`). It also defines goal-tracker invariants and round cadence, including immutable/mutable tracker sections and full alignment checks after rounds 4, 9, 14, etc. (`commands/start-rlcr-loop.md:24-37`). `commands/cancel-rlcr-loop.md:11-16` selects the newest `.humanize/rlcr/*/` directory, `commands/cancel-rlcr-loop.md:20-31` treats `state.md` presence as the active-loop gate and renames it to `cancel-state.md`.
- inputs_outputs_state: Start input is `<path/to/plan.md>` plus `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, and `--push-every-round` from `commands/start-rlcr-loop.md:3`; output is creation of an active `.humanize/rlcr/<timestamp>/state.md`, prompt, goal tracker, and summary path by the setup script. The setup script writes `state.md` fields such as `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `push_every_round`, `plan_file`, `plan_tracked`, and `start_branch` at `scripts/setup-rlcr-loop.sh:485-498`. Cancel input is the current project’s `.humanize/rlcr` directory; output is terminal state preservation as `cancel-state.md` (`commands/cancel-rlcr-loop.md:28-35`).
- gates_or_invariants: Start forbids bypassing loop controls: summaries must be written, `goal-tracker.md` must stay current, state files/cancel commands must not be used to cheat (`commands/start-rlcr-loop.md:43-49`). Cancellation invariant: newest timestamp directory is authoritative, and active means exactly `state.md` exists (`commands/cancel-rlcr-loop.md:33`). The same active-state invariant is implemented in `hooks/lib/loop-common.sh:58-79`, which checks only the newest directory and ignores older “zombie” directories.
- dependencies_and_callers: The command layer depends on Claude plugin command discovery and tool allowlists. Runtime coordination is with `scripts/setup-rlcr-loop.sh`, which performs validation, state creation, prompt generation, and setup output (`scripts/setup-rlcr-loop.sh:182-423`, `scripts/setup-rlcr-loop.sh:463-498`, `scripts/setup-rlcr-loop.sh:596-719`). Hook registration in `hooks/hooks.json:14-63` binds Write/Edit/Read/Bash validators and the Stop hook to enforce the loop after the start command creates state. Docs register the commands as the public API in `README.md:181-201`.
- edge_cases_or_failure_modes: `cancel-rlcr-loop` uses `ls -1d .humanize/rlcr/*/ | sort -r | head -1`, so malformed timestamp names can affect newest selection; it only checks the newest directory, so an older directory with `state.md` is intentionally ignored. If newest dir exists without `state.md`, cancellation reports no active loop (`commands/cancel-rlcr-loop.md:20-27`). Start’s delegated setup rejects missing plan files, absolute paths, paths with spaces or shell metacharacters, symlink plans, plans outside project, submodule paths, insufficient content, unavailable `codex`, unsafe branch/model/effort names, and non-git repos (`scripts/setup-rlcr-loop.sh:197-423`, `scripts/setup-rlcr-loop.sh:429-461`).
- validation_or_tests: Behavior is exercised indirectly by `tests/test-state-exit-naming.sh`, especially active-loop detection and terminal-state file names. `tests/run-all-tests.sh:32-45` includes `test-state-exit-naming.sh` in the main suite. CI also names this test in `.github/workflows/plan-file-test.yml:50-51`.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-032 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Executable specification for loop lifecycle naming and active-loop detection. It locks down the invariant that only `state.md` means active, while renamed terminal files preserve history without being rediscovered as active loops.
- algorithmic_behavior: The test builds a temp git repository (`tests/test-state-exit-naming.sh:31-47`), creates `.humanize/rlcr/<timestamp>` directories, sources `hooks/lib/loop-common.sh` (`tests/test-state-exit-naming.sh:64-65`), and asserts shared functions. Tests 1-7 verify `find_active_loop`: completed/cancelled/unexpected/maxiter/stop files are not active, `state.md` is active, and a newer directory with `state.md` wins (`tests/test-state-exit-naming.sh:38-176`). Tests 8-10 verify `end_loop`: invalid reasons fail, valid reasons create `${reason}-state.md`, and missing `state.md` returns an error (`tests/test-state-exit-naming.sh:178-239`). Tests 11-13 verify new path detection and legacy path exclusion (`tests/test-state-exit-naming.sh:241-287`).
- inputs_outputs_state: Inputs are synthetic state frontmatter snippets, temp loop directories, and function calls into `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir`. Outputs are PASS/FAIL/SKIP counters and process exit status equal to `$TESTS_FAILED` (`tests/test-state-exit-naming.sh:289-298`). State transitions under test are active `state.md` to terminal `complete-state.md`, `cancel-state.md`, `maxiter-state.md`, `stop-state.md`, and `unexpected-state.md`.
- gates_or_invariants: The tested implementation in `hooks/lib/loop-common.sh:62-79` only checks the newest timestamp directory and requires `${newest_dir}state.md`; this prevents older stale loops from restarting. `end_loop` accepts only `complete|cancel|maxiter|stop|unexpected` and rejects any other reason (`hooks/lib/loop-common.sh:308-321`). It renames the state file to `${reason}-state.md` only if the file exists (`hooks/lib/loop-common.sh:323-333`). `is_in_humanize_loop_dir` matches `.humanize/rlcr/` and intentionally excludes `.humanize-loop.local` (`hooks/lib/loop-common.sh:251-255`).
- dependencies_and_callers: Direct dependency is `hooks/lib/loop-common.sh`. The functions are used by validators and stop hooks: `loop-codex-stop-hook.sh` discovers the active loop at `hooks/loop-codex-stop-hook.sh:59-67`, sends corrupted state to `unexpected-state.md` at `hooks/loop-codex-stop-hook.sh:90-107`, sends max-iteration termination to `maxiter-state.md` at `hooks/loop-codex-stop-hook.sh:612-617`, and sends Codex `COMPLETE`/`STOP` outcomes to `complete-state.md` or `stop-state.md` at `hooks/loop-codex-stop-hook.sh:922-960`. The cancel command separately mirrors the same rename contract in `commands/cancel-rlcr-loop.md:28-31`.
- edge_cases_or_failure_modes: The test covers terminal files being accidentally treated as active, newer-vs-older directory precedence, invalid `end_loop` reason strings, missing state files, and legacy `.humanize-loop.local` false positives. One notable shell concern: the test uses `set -uo pipefail` initially, toggles `set -e` around some negative checks, and exits on the accumulated failure count; this is intentional but means unexpected command failures before counters can still abort once `set -e` is enabled in the function test sections.
- validation_or_tests: This file is itself the validation artifact. It is included in the aggregate runner at `tests/run-all-tests.sh:32-45` and in CI references under `.github/workflows/plan-file-test.yml:14-25` and `.github/workflows/plan-file-test.yml:50-51`.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-062 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: User-facing block template for the round-number gate. It explains why a Write/Edit attempt targeting `round-<wrong>-summary.md` is blocked and points the agent to the authoritative current-round summary path.
- algorithmic_behavior: The template renders five variables: `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH` (`prompt-template/block/wrong-round-number.md:1-7`). Runtime callers load it through `load_and_render_safe`, so the block remains available with a fallback if the template is missing. Write validation calls it when a summary filename’s extracted round differs from `STATE_CURRENT_ROUND` and the file is not allowlisted (`hooks/loop-write-validator.sh:153-170`). Edit validation applies the same check for Edit tool attempts (`hooks/loop-edit-validator.sh:112-136`).
- inputs_outputs_state: Inputs come from hook JSON: `tool_name`, `tool_input.file_path`, active loop path, and parsed `state.md`. `parse_state_file` reads `current_round` from frontmatter and defaults where needed (`hooks/lib/loop-common.sh:110-137`). Output is a stderr block and hook exit code `2`, which blocks the tool call (`hooks/loop-write-validator.sh:163-170`, `hooks/loop-edit-validator.sh:129-136`). The template does not mutate state; it prevents mutation of the wrong round’s summary.
- gates_or_invariants: The invariant is “do not increment the round number yourself” (`prompt-template/block/wrong-round-number.md:7`). The gate relies on `is_round_file_type` matching `round-[0-9]+-summary.md` (`hooks/lib/loop-common.sh:144-151`) and `extract_round_number` extracting the integer (`hooks/lib/loop-common.sh:153-163`). It allows explicitly allowlisted bootstrap files such as `round-0-summary.md` and early todos/summary files in `hooks/lib/loop-common.sh:168-186`; otherwise current state controls the correct path.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh`, where templates use `{{VARIABLE_NAME}}` syntax, single-pass substitution, and missing-variable preservation (`hooks/lib/template-loader.sh:7-14`, `hooks/lib/template-loader.sh:50-132`). `load_and_render_safe` supplies fallback behavior for missing or empty templates (`hooks/lib/template-loader.sh:167-203`). Callers are `hooks/loop-write-validator.sh` and `hooks/loop-edit-validator.sh`.
- edge_cases_or_failure_modes: If no active loop exists, the validators exit without blocking before reaching this template (`hooks/loop-write-validator.sh:83-85`, `hooks/loop-edit-validator.sh:67-69`). If the file path is not a recognized summary filename, the wrong-round gate does not apply. If `CURRENT_ROUND` is stale or corrupted in `state.md`, the template will faithfully point to that parsed value; broader corruption handling is in the stop hook and state validation path, not in the template. Missing template files degrade to inline fallback blocks through `load_and_render_safe`.
- validation_or_tests: `tests/test-template-loader.sh:151-164` renders `block/wrong-round-number.md` and asserts replacement of `round-3-summary.md` and current round `5`. `tests/test-templates-comprehensive.sh:498-510` repeats the integration check and also asserts the rendered correct path appears.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: IMPROVE_MONITOR_SCRIPT-HZ-002, IMPROVE_MONITOR_SCRIPT-HZ-032, IMPROVE_MONITOR_SCRIPT-HZ-062
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`