# agent_01 improve-monitor-script 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `5af20b79e6fec323a2d5cb9344a6a584db1c635a`
- note: researched read-only export only; no files modified. The export is not a live `.git` checkout, so git status was not available, but all assigned paths and referenced runtime/test files were inspected directly.

## Item Evidence

### IMPROVE_MONITOR_SCRIPT-HZ-001 `directory` `.`
- cursor: `[_]`
- core_role: Repository root for the Humanize Claude Code plugin, whose core algorithm is an RLCR loop: initialize a plan-backed loop, force Claude through round summaries, run Codex review on stop attempts, and either end the loop or feed review output into the next round. The high-level flow is documented in `README.md:31-39`, command entry is `commands/start-rlcr-loop.md:10-14`, and hook wiring is `hooks/hooks.json:4-63`.
- algorithmic_behavior: `scripts/setup-rlcr-loop.sh` is the bootstrap state machine. It parses CLI flags, rejects invalid plan inputs, validates git and Codex prerequisites, creates `.humanize/rlcr/<timestamp>/`, writes `state.md`, copies the plan backup, seeds `goal-tracker.md`, and emits `round-0-prompt.md`; key phases are argument parsing `scripts/setup-rlcr-loop.sh:104-180`, path/gate validation `scripts/setup-rlcr-loop.sh:212-423`, state creation `scripts/setup-rlcr-loop.sh:467-498`, goal tracker/prompt generation `scripts/setup-rlcr-loop.sh:504-653`, and completion instructions `scripts/setup-rlcr-loop.sh:667-720`. Runtime enforcement is split across hooks: prompt-submit plan consistency in `hooks/loop-plan-file-validator.sh:29-224`, pretool read/write/edit/bash guards in `hooks/loop-read-validator.sh:36-142`, `hooks/loop-write-validator.sh:37-190`, `hooks/loop-edit-validator.sh:36-140`, and `hooks/loop-bash-validator.sh:59-146`, plus the stop hook loop driver in `hooks/loop-codex-stop-hook.sh:59-1033`.
- inputs_outputs_state: Primary input is a relative markdown plan file plus flags like `--max`, `--codex-model`, `--codex-timeout`, `--track-plan-file`, and `--push-every-round` (`scripts/setup-rlcr-loop.sh:43-64`). Persistent state is YAML-like frontmatter in `.humanize/rlcr/<timestamp>/state.md` with `current_round`, max iterations, Codex settings, plan path, tracking policy, and start branch (`scripts/setup-rlcr-loop.sh:485-498`; parsed by `hooks/lib/loop-common.sh:110-137`). Loop outputs include `plan.md` backup, `goal-tracker.md`, `round-N-prompt.md`, `round-N-summary.md`, `round-N-review-prompt.md`, `round-N-review-result.md`, and terminal/cache logs under `$HOME/.cache/humanize/<sanitized-project>/<timestamp>/` (`README.md:242-267`, `hooks/loop-codex-stop-hook.sh:740-752`). Terminal monitor output is read-only, derived from state/tracker/git/log files in `scripts/humanize.sh:138-285` and displayed in `scripts/humanize.sh:302-440`.
- gates_or_invariants: The loop treats `state.md` presence in the newest `.humanize/rlcr/<timestamp>/` directory as the active-loop cursor (`hooks/lib/loop-common.sh:58-79`); ending the loop renames it to `<reason>-state.md` (`hooks/lib/loop-common.sh:301-334`). Plan files must be relative, inside the project, non-symlink, non-submodule, readable, shell-safe, and either gitignored or explicitly tracked and clean (`scripts/setup-rlcr-loop.sh:227-356`). Plan content must be at least 5 lines and at least 3 non-comment content lines, ignoring blank lines, `#` lines, and HTML comments (`scripts/setup-rlcr-loop.sh:362-415`). Branch names and Codex model/effort are YAML-safe (`scripts/setup-rlcr-loop.sh:435-461`). Runtime hooks preserve invariant ownership: Claude cannot edit state, prompt files, plan backup, stale round summaries, or post-round-0 goal tracker directly (`hooks/loop-write-validator.sh:95-120`, `hooks/loop-edit-validator.sh:79-105`, `hooks/loop-bash-validator.sh:76-129`). Stop hook blocks before Codex on outdated schema, branch changes, plan drift, incomplete todos, dirty git, oversized files, missing summary, and uninitialized round-0 goal tracker (`hooks/loop-codex-stop-hook.sh:119-238`, `hooks/loop-codex-stop-hook.sh:246-531`, `hooks/loop-codex-stop-hook.sh:539-605`).
- dependencies_and_callers: Claude slash commands call shell scripts through restricted allowed tools: start invokes `scripts/setup-rlcr-loop.sh` (`commands/start-rlcr-loop.md:1-14`), cancel renames active state (`commands/cancel-rlcr-loop.md:11-33`). `hooks/hooks.json` registers `loop-plan-file-validator.sh` for `UserPromptSubmit`, pretool validators for Write/Edit/Read/Bash, and `loop-codex-stop-hook.sh` for Stop with a 7200s timeout (`hooks/hooks.json:4-63`). Shared code lives in `hooks/lib/loop-common.sh` and `hooks/lib/template-loader.sh`; template rendering uses single-pass `awk` substitution to prevent placeholder injection (`hooks/lib/template-loader.sh:50-132`). `scripts/portable-timeout.sh` abstracts `gtimeout`, GNU `timeout`, Python, or no-timeout fallback (`scripts/portable-timeout.sh:9-71`). `hooks/check-todos-from-transcript.py` parses Claude transcript JSONL and returns incomplete TodoWrite state to the stop hook (`hooks/check-todos-from-transcript.py:20-129`). CI validates shell syntax, template references/rendering, plan-file behavior, and version bumps through `.github/workflows/*.yml`, especially `plan-file-test.yml:1-51` and `template-test.yml:1-65`.
- edge_cases_or_failure_modes: Path escaping via `..`, spaces, shell metacharacters, symlinks, unreadable parent dirs, submodules, tracked/untracked policy mismatches, no git repo, and no initial commit are all setup-time failures (`scripts/setup-rlcr-loop.sh:212-356`). Runtime failure modes include state schema drift, manual branch switching, plan deletion/modification, missing plan backup, TodoWrite incompletion, git status timeout/failure, dirty/unpushed work, missing summary, Codex absence/failure/no output/empty output, max iterations, and Codex `STOP` circuit breaker (`hooks/loop-codex-stop-hook.sh:90-238`, `hooks/loop-codex-stop-hook.sh:272-293`, `hooks/loop-codex-stop-hook.sh:307-407`, `hooks/loop-codex-stop-hook.sh:417-530`, `hooks/loop-codex-stop-hook.sh:614-617`, `hooks/loop-codex-stop-hook.sh:716-960`). Monitor edge cases include no `.humanize/rlcr`, no latest session, no log yet, stopped sessions without logs, newer sessions appearing, and cross-platform `stat` (`scripts/humanize.sh:16-21`, `scripts/humanize.sh:489-598`, `scripts/humanize.sh:509-512`).
- validation_or_tests: Test runner order is declared in `tests/run-all-tests.sh:32-45`. Major suites cover template loader safety and injection prevention (`tests/test-template-loader.sh`), command-modification regexes (`tests/test-bash-validator-patterns.sh`), TodoWrite transcript parsing (`tests/test-todo-checker.sh`), setup plan validation (`tests/test-plan-file-validation.sh`), template reference completeness (`tests/test-template-references.sh:51-230`), state exit naming (`tests/test-state-exit-naming.sh`), comprehensive template validation (`tests/test-templates-comprehensive.sh`), plan-file runtime hooks (`tests/test-plan-file-hooks.sh`), error scenarios, ANSI parsing, and allowlist behavior (`tests/test-allowlist-validators.sh`). I inspected tests but did not execute them because the task requested research notes only and the branch export is read-only.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-031 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for `scripts/setup-rlcr-loop.sh` setup-time validation. It models the accepted and rejected boundary conditions before the RLCR loop is allowed to create state, which makes it core to the algorithm’s safety contract rather than a peripheral smoke test.
- algorithmic_behavior: The test creates disposable git repositories, mock plan files, and a mock `codex` binary when needed (`tests/test-plan-file-validation.sh:36-84`). It then invokes `scripts/setup-rlcr-loop.sh` with controlled inputs and asserts either nonzero failure plus expected error text or acceptance past the relevant gate. The cases are grouped by path validation `tests/test-plan-file-validation.sh:86-324`, plan tracking validation `tests/test-plan-file-validation.sh:326-426`, branch YAML safety `tests/test-plan-file-validation.sh:428-512`, content validation `tests/test-plan-file-validation.sh:514-661`, CLI option parsing `tests/test-plan-file-validation.sh:663-694`, and Codex parameter validation `tests/test-plan-file-validation.sh:696-738`.
- inputs_outputs_state: Inputs are temporary repo state, plan file paths, plan file contents, branch names, and CLI options. Outputs are captured command stdout/stderr in `RESULT`, exit status in `EXIT_CODE`, and PASS/FAIL/SKIP counters (`tests/test-plan-file-validation.sh:28-34`). The test manipulates temporary git state only: tracked plans, gitignored `plans/`, modified tracked files, no-commit repos, branch creation, and unreadable directories (`tests/test-plan-file-validation.sh:40-71`, `tests/test-plan-file-validation.sh:202-243`, `tests/test-plan-file-validation.sh:329-426`). Final process status is `exit $TESTS_FAILED` (`tests/test-plan-file-validation.sh:740-749`).
- gates_or_invariants: It asserts setup rejects absolute paths, nonexistent files and directories, paths or filenames with spaces, shell metacharacters, symlinks, path-resolution failures, project escapes, non-git repos, and repos without commits (`tests/test-plan-file-validation.sh:89-324`). It enforces the plan tracking invariant: without `--track-plan-file`, tracked plans are rejected; with `--track-plan-file`, the plan must be tracked and clean (`tests/test-plan-file-validation.sh:329-426`). It validates serialization safety for branch names with colon/hash/quotes and Codex model/effort characters (`tests/test-plan-file-validation.sh:432-512`, `tests/test-plan-file-validation.sh:700-738`). It also encodes the content invariant that blank/comment-only/sparse plans fail while sufficient real content passes, including the regression that single-line HTML comments do not poison later content (`tests/test-plan-file-validation.sh:518-661`).
- dependencies_and_callers: Directly depends on `scripts/setup-rlcr-loop.sh`, Git, shell utilities, and optionally the mock `codex` shim (`tests/test-plan-file-validation.sh:16-21`, `tests/test-plan-file-validation.sh:73-84`). It is called by the aggregate runner `tests/run-all-tests.sh:32-45` and by CI in `.github/workflows/plan-file-test.yml:38-42`. Its expected strings map to implementation gates in `scripts/setup-rlcr-loop.sh:189-205`, `scripts/setup-rlcr-loop.sh:212-356`, `scripts/setup-rlcr-loop.sh:362-461`.
- edge_cases_or_failure_modes: The metacharacter loop tests `; & | $ \` < > ( ) { } [ ] ! # ~ * ?` as path-injection blockers (`tests/test-plan-file-validation.sh:169-187`). Permission/path resolution is conditional because some environments may not allow `chmod 000`, so it can skip rather than fail (`tests/test-plan-file-validation.sh:202-243`). Branch tests tolerate Git itself rejecting invalid branch names (`tests/test-plan-file-validation.sh:460-512`). “Accept” tests are scoped to absence of the specific validation error, acknowledging later gates may fail for unrelated environment reasons (`tests/test-plan-file-validation.sh:626-635`, `tests/test-plan-file-validation.sh:672-681`, `tests/test-plan-file-validation.sh:729-738`).
- validation_or_tests: The file is itself validation. It provides 14 numbered major cases plus subcases for path, branch, content, CLI, and Codex-parameter safety. It backs the setup script’s fail-closed posture before loop state is created.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-061 `file` `prompt-template/block/wrong-round-file.md`
- cursor: `[_]`
- core_role: User-facing block template for a read-time round mismatch. It is part of the hook contract that prevents Claude from using stale prompt/summary context from another RLCR round.
- algorithmic_behavior: The template renders the attempted stale file name from `CLAUDE_ROUND` and `FILE_TYPE`, states the authoritative `CURRENT_ROUND`, shows current-round prompt and summary paths under `ACTIVE_LOOP_DIR`, and includes the attempted `FILE_PATH` for explicit access if needed (`prompt-template/block/wrong-round-file.md:1-9`). It is loaded only by the Read validator when a prompt or summary file’s round number differs from state and the file is not allowlisted (`hooks/loop-read-validator.sh:110-122`).
- inputs_outputs_state: Inputs are template variables provided by `hooks/loop-read-validator.sh`: `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, `ACTIVE_LOOP_DIR`, and `FILE_PATH` (`hooks/loop-read-validator.sh:116-121`). Output is rendered markdown sent to stderr, followed by hook exit code 2 to block the Read tool (`hooks/loop-read-validator.sh:116-122`). It does not mutate loop state; it enforces read selection against the already parsed `state.md` current round (`hooks/loop-read-validator.sh:69-88`).
- gates_or_invariants: The invariant is “read current prompt/summary unless explicitly allowlisted.” Round extraction uses `extract_round_number` from `hooks/lib/loop-common.sh:153-163`; prompt/summary classification uses `is_round_file_type` (`hooks/lib/loop-common.sh:144-151`). The mismatch gate excludes allowlisted files such as `round-0-summary.md` and `round-1-summary.md` (`hooks/lib/loop-common.sh:165-186`, `hooks/loop-read-validator.sh:110`). Write/Edit mismatches use `prompt-template/block/wrong-round-number.md`, so this template is specifically read-path guidance, not a general round mismatch template.
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` for single-pass placeholder rendering and fallback behavior (`hooks/lib/template-loader.sh:50-132`, `hooks/lib/template-loader.sh:170-203`). The direct caller is `hooks/loop-read-validator.sh:116`. Template existence is covered by `tests/test-template-references.sh:56-111`; broader template loading/rendering behavior is covered by `tests/test-template-loader.sh` and `tests/test-templates-comprehensive.sh`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, `load_and_render_safe` falls back to an inline wrong-round message (`hooks/loop-read-validator.sh:111-116`, `hooks/lib/template-loader.sh:170-203`). If there is no active loop, the read validator exits without blocking (`hooks/loop-read-validator.sh:61-67`). If the path is outside `.humanize/rlcr`, `wrong-file-location.md` fires before this template (`hooks/loop-read-validator.sh:94-104`). If the same wrong-round basename appears in the wrong session directory, the later directory-path gate can also block (`hooks/loop-read-validator.sh:129-140`).
- validation_or_tests: Direct reference existence is validated by the template reference scanner, which extracts `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-round-file.md"` style calls and checks files exist (`tests/test-template-references.sh:83-111`). Read-validator allowlist and wrong-round behavior is exercised in `tests/test-allowlist-validators.sh:229-282`, including blocking non-allowlisted `round-3-summary.md`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: all 3 assigned item sections are present exactly once above
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`