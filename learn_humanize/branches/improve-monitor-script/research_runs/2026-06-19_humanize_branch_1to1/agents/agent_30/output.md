# agent_30 improve-monitor-script 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `5af20b79e6fec323a2d5cb9344a6a584db1c635a`

## Item Evidence

### IMPROVE_MONITOR_SCRIPT-HZ-030 `file` `tests/test-plan-file-hooks.sh`
- cursor: `[_]`
- core_role: Executable regression/specification suite for RLCR loop hook behavior around plan-file immutability, loop state schema validation, active-branch invariants, command-bypass prevention, stop-hook plan integrity checks, and goal-tracker initialization gating. It is part of the full repository test runner: `tests/run-all-tests.sh` includes `test-plan-file-hooks.sh` in the ordered suite list at lines 32-45.
- algorithmic_behavior: The test creates disposable git repositories and loop directories under a temporary directory, then feeds simulated Claude hook JSON into the real validators. Setup is centralized in `setup_test_loop`, which initializes git, captures the default branch, creates `.humanize/rlcr/<timestamp>/`, writes a gitignored source plan, copies it to the loop backup `plan.md`, and writes `state.md` with `current_round`, `max_iterations`, `plan_file`, `plan_tracked`, and `start_branch` fields at lines 37-86. Tests 1-4 assert `hooks/loop-plan-file-validator.sh` admits a valid state, strips YAML quotes from `plan_file`, blocks missing `plan_tracked`, blocks missing `start_branch`, and blocks branch drift at lines 91-185. Tests 5-8.5 assert Write/Edit/Bash hooks reject writes, edits, removals, redirections, `tee`, glob, brace expansion, `$()` substitution, and backtick substitution against `.humanize/rlcr/.../plan.md` at lines 194-335. Tests 8.6-8.9 assert quote stripping for `start_branch`, stop-hook parsing of quoted fields, branch mismatch detection with quotes, and hyphenated plan paths at lines 341-460. Tests 9-14 assert `loop-codex-stop-hook.sh` detects modified, deleted, missing-backup, tracked-uncommitted, outdated-schema, and tracked-committed-but-content-changed plan states at lines 469-730. Tests 14.1-14.4 assert section-specific placeholder detection in `goal-tracker.md` reports only the sections still containing placeholders at lines 737-1009. Tests 15-17 are negative tests proving legacy `.humanize-loop.local` paths are no longer protected by current RLCR hooks at lines 1012-1054. Final pass/fail counters drive the process exit status at lines 1056-1065.
- inputs_outputs_state: Inputs are synthetic hook payloads on stdin, such as `{"tool_name":"Write","tool_input":{"file_path":"..."}}` and `{"tool_name":"Bash","tool_input":{"command":"..."}}`, plus repository state created in temporary git repos. It exports `CLAUDE_PROJECT_DIR` to point hooks at the test repository, so hook lookup of `.humanize/rlcr` does not use the plugin repository. Outputs are shell test result lines and the final exit code equal to `TESTS_FAILED` at line 1065. State transitions under test include active-loop discovery from `.humanize/rlcr/<timestamp>/state.md`, parser extraction of YAML frontmatter values, git branch comparison against `start_branch`, comparison of the source plan to the loop backup, and stop-hook blocking responses emitted as JSON with `"decision": "block"` for stop-hook gates. The underlying state parser is `parse_state_file` in `hooks/lib/loop-common.sh`, which strips quotes from `start_branch` and `plan_file`, defaults `current_round`, `max_iterations`, and `push_every_round`, and exposes `STATE_*` variables at lines 110-136.
- gates_or_invariants: The suite encodes these invariants: an active loop requires modern state schema fields `plan_tracked` and `start_branch`; the git branch must remain the loop’s starting branch; a gitignored plan must remain untracked, while a tracked plan must remain tracked and clean; the source plan must continue to match the immutable loop backup; `.humanize/rlcr/.../plan.md` cannot be modified via Write, Edit, or shell command bypasses; stop-hook plan integrity failures return blocking JSON rather than silent success; goal-tracker placeholder diagnostics must be section-specific; and legacy `.humanize-loop.local` must not be treated as an active RLCR loop path. The corresponding hook gates are visible in `hooks/loop-plan-file-validator.sh`: schema blocking at lines 52-82, branch consistency at lines 84-108, tracked-plan checks at lines 114-185, and untracked-plan checks at lines 186-221. Write and Edit plan-backup gates live in `hooks/loop-write-validator.sh` lines 101-111 and `hooks/loop-edit-validator.sh` lines 84-96. The Bash bypass gate uses `command_modifies_file "$COMMAND_LOWER" "\.humanize/rlcr(/[^/]+)?/plan\.md"` in `hooks/loop-bash-validator.sh` lines 81-92.
- dependencies_and_callers: The test directly calls `hooks/loop-plan-file-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. Those are wired into Claude hook registration in `hooks/hooks.json`: UserPromptSubmit runs the plan-file validator at lines 4-12; PreToolUse dispatches Write/Edit/Read/Bash validators at lines 14-50; Stop runs `loop-codex-stop-hook.sh` with a 7200-second timeout at lines 52-60. The validators share `hooks/lib/loop-common.sh` for active-loop discovery, state parsing, path classification, allowlist logic, and command-modification regexes. Template rendering goes through `hooks/lib/template-loader.sh`, whose `load_and_render_safe` fallback behavior at lines 167-203 prevents empty block messages if a template is missing. Stop-hook behavior depends on git, `jq`, `diff`, `python3` for optional todo checking, the portable timeout wrapper, and Codex availability for later review execution, but this suite deliberately covers many early gates before actual Codex review.
- edge_cases_or_failure_modes: The suite covers YAML quote handling for both `plan_file` and `start_branch`, branch names differing by current checkout, paths containing hyphens, active-loop path patterns with and without an intermediate timestamp directory, and several command injection/bypass forms: command substitution, glob expansion, brace expansion, pipes to `tee`, and backtick substitution. It also covers tracked-plan race conditions: uncommitted tracked modifications are caught through `git status`, while committed modifications that leave git status clean are caught through content comparison against backup. Missing backup and deleted source plan are both hard stops. Section-specific placeholder tests catch a subtle false-positive class where one placeholder section must not cause all goal-tracker sections to be reported. Legacy path tests guard against over-broad path matching by ensuring `.humanize-loop.local` is allowed. A notable residual fragility is that the suite relies on grep substrings for block reasons, so it verifies behavioral signals more than exact JSON schema shape for some gates.
- validation_or_tests: This file is itself the validation asset. It is run by `tests/run-all-tests.sh` through ordinary shell execution and contributes pass/fail counts parsed from its stdout. Related validation exists in `tests/test-template-references.sh`, which scans hook scripts for referenced templates and fails missing template files at lines 51-115, and `tests/test-templates-comprehensive.sh`, which loads every Markdown template and validates placeholder syntax at lines 82-194. I inspected these references but did not execute the test suite, per the “research notes only / do not modify files” instruction.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-060 `file` `prompt-template/block/wrong-file-location.md`
- cursor: `[_]`
- core_role: Block-message template for the Read validator’s “round file requested from the wrong location” transition. It does not implement the gate itself, but it defines the user-facing remediation contract when a read targets a `round-N-summary.md` or `round-N-prompt.md` file outside the active `.humanize/rlcr/<timestamp>/` loop directory.
- algorithmic_behavior: The template renders a short Markdown block titled “Wrong File Location” and substitutes `FILE_PATH`, `ACTIVE_LOOP_DIR`, and `CURRENT_ROUND`. Its content tells the agent that the attempted read path is not where loop files live, then lists the current round prompt and summary paths derived from the active loop directory at lines 3-7. It also includes a final `cat {{FILE_PATH}}` suggestion at line 9, which is unusual because the gate blocks the current Read action but gives a shell read command for the same path. The actual gate is in `hooks/loop-read-validator.sh`: after confirming the tool is `Read`, it ignores non-round prompt/summary paths, discovers the active loop, parses current round from state, extracts the requested round/file type, and if the file is not under `.humanize/rlcr/`, renders this template through `load_and_render_safe` at lines 90-103.
- inputs_outputs_state: Inputs are template variables supplied by `loop-read-validator.sh`: attempted `FILE_PATH`, discovered `ACTIVE_LOOP_DIR`, and parsed `CURRENT_ROUND`. The active loop comes from `find_active_loop`, which selects the newest timestamp directory under `.humanize/rlcr` only if it contains `state.md` in `hooks/lib/loop-common.sh` lines 62-79. `CURRENT_ROUND` comes from `parse_state_file` via `STATE_CURRENT_ROUND`, with a default of `0` when absent at lines 117-136. Output is Markdown sent to stderr, while the validator exits with status `2` to block the tool call. The template itself has no persistent state mutation.
- gates_or_invariants: This template supports the invariant that prompt/summary round files must be read from the current active loop directory, not from arbitrary project paths or stale external locations. The first read gate rejects `round-*-todos.md` unless allowlisted at `hooks/loop-read-validator.sh` lines 32-44. The wrong-location gate applies only to prompt/summary files where `is_in_humanize_loop_dir "$FILE_PATH"` is false at lines 50-55 and 94-103. If the path is inside a loop directory but points at the wrong round, `wrong-round-file.md` is rendered instead at lines 106-123. If it is inside `.humanize/rlcr` but not the active timestamp directory, `wrong-directory-path.md` is rendered at lines 125-140. This file therefore owns only one branch in a three-stage read-location validation algorithm.
- dependencies_and_callers: Direct caller is `hooks/loop-read-validator.sh` at lines 99-102. Rendering depends on `load_and_render_safe` from `hooks/lib/template-loader.sh`, which loads templates by path and falls back to an inline message if the file is missing or empty at lines 167-203. Template directory resolution comes from `get_template_dir`, which maps the hook library location to `prompt-template` at lines 24-31. Repository-wide template existence is guarded by `tests/test-template-references.sh`, whose script list includes `loop-read-validator.sh` at lines 56-64 and whose reference scan verifies referenced templates exist at lines 83-110. Full template load/syntax coverage is provided by `tests/test-templates-comprehensive.sh`, which loads every template at lines 82-107 and validates uppercase `{{VAR}}` placeholder syntax at lines 114-194.
- edge_cases_or_failure_modes: If there is no active loop, `loop-read-validator.sh` exits successfully before this template can render at lines 61-67, so wrong-location reads are allowed outside active RLCR sessions. If the attempted path is a non-round file, the validator exits early at lines 50-52. If a prompt/summary path is under `.humanize/rlcr` but from an old timestamp directory, this template is not used; the directory-path block takes over. If a placeholder variable is not supplied, `render_template` keeps the raw placeholder by design, as documented in `hooks/lib/template-loader.sh` lines 7-14 and implemented at lines 115-122. The line 9 remediation can be confusing because it suggests `cat {{FILE_PATH}}` even though the blocked read path is not the active loop path; this appears to be a prompt/message contract issue rather than algorithmic state logic.
- validation_or_tests: The template is validated indirectly by template reference and comprehensive template tests, not by `tests/test-plan-file-hooks.sh`. `tests/test-template-references.sh` ensures the `block/wrong-file-location.md` reference in `loop-read-validator.sh` resolves to a real file. `tests/test-templates-comprehensive.sh` ensures the file is loadable and its placeholders use the expected uppercase double-brace syntax. There are no direct assertions in the assigned `test-plan-file-hooks.sh` for the Read validator’s wrong-location branch; that assigned test focuses on plan-file hooks, bash/write/edit protections, and stop-hook integrity.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 item sections present above; identifiers are not repeated here to preserve the exactly-once evidence rule.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`