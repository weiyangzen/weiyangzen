# agent_19 improve-monitor-script 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `5af20b79e6fec323a2d5cb9344a6a584db1c635a`

## Item Evidence

### IMPROVE_MONITOR_SCRIPT-HZ-019 `file` `hooks/loop-plan-file-validator.sh`
- cursor: `[_]`
- core_role: UserPromptSubmit-time RLCR guard that prevents an active loop from continuing when its state schema, git branch, or plan-file tracking mode has drifted. It is registered as the `UserPromptSubmit` command hook in `hooks/hooks.json:4-13`, so it runs before user prompts while an RLCR loop is active.
- algorithmic_behavior: The script starts with fail-fast bash settings at `hooks/loop-plan-file-validator.sh:11`, resolves `SCRIPT_DIR` and `PROJECT_ROOT` from `CLAUDE_PROJECT_DIR` or `pwd` at `hooks/loop-plan-file-validator.sh:13-14`, sources shared loop parsing plus portable git timeout helpers at `hooks/loop-plan-file-validator.sh:16-24`, then consumes hook stdin at `hooks/loop-plan-file-validator.sh:26-27`. It finds only the current active loop via `find_active_loop "$PROJECT_ROOT/.humanize/rlcr"` at `hooks/loop-plan-file-validator.sh:29-35`; if none exists, it silently allows the prompt. For an active loop, it parses `state.md` through `parse_state_file` at `hooks/loop-plan-file-validator.sh:38-46`, then runs three ordered gates: schema fields, branch consistency, and plan-file git tracking consistency.
- inputs_outputs_state: Inputs are hook stdin, `CLAUDE_PROJECT_DIR`, `.humanize/rlcr/<latest>/state.md`, the current git branch, git tracking status for `STATE_PLAN_FILE`, and `git status --porcelain` for that same plan file. The script does not mutate files or loop state. Its successful output is no stdout and exit `0`; blocked states emit JSON with `"decision": "block"` and a human-readable `"reason"`, then exit `0`, matching Claude hook conventions. `FULL_PLAN_PATH` is computed at `hooks/loop-plan-file-validator.sh:114` but is not used by this validator; actual content/backup integrity is handled later by the stop hook.
- gates_or_invariants: Schema invariant: `plan_tracked` and `start_branch` must exist in state frontmatter; missing values call `schema_validation_error` at `hooks/loop-plan-file-validator.sh:52-70` and are checked at `hooks/loop-plan-file-validator.sh:72-82`. Branch invariant: `git rev-parse --abbrev-ref HEAD` must succeed within `GIT_TIMEOUT`, and the current branch must equal `STATE_START_BRANCH`; failures block at `hooks/loop-plan-file-validator.sh:88-108`. Tracked-plan invariant: when `PLAN_TRACKED=true`, `git ls-files --error-unmatch "$PLAN_FILE"` must report tracked, and `git status --porcelain "$PLAN_FILE"` must be empty; timeout/error handling and clean checks are at `hooks/loop-plan-file-validator.sh:116-185`. Untracked-plan invariant: when `PLAN_TRACKED` is not true, the plan file must remain untracked/gitignored; if it becomes tracked, the hook blocks at `hooks/loop-plan-file-validator.sh:186-221`.
- dependencies_and_callers: Direct dependencies are `hooks/lib/loop-common.sh`, which provides field constants, template directory setup, active-loop selection, YAML-ish frontmatter parsing, and quote stripping for `plan_file`/`start_branch` at `hooks/lib/loop-common.sh:16-18` and `hooks/lib/loop-common.sh:84-129`; `scripts/portable-timeout.sh` supplies `run_with_timeout`; `jq` is used for schema-block JSON string escaping at `hooks/loop-plan-file-validator.sh:60-68`; git supplies branch, tracking, and status facts. Setup-side state creation writes the corresponding `plan_file`, `plan_tracked`, and `start_branch` fields in `scripts/setup-rlcr-loop.sh:485-497`, after validating tracking mode in `scripts/setup-rlcr-loop.sh:308-356`. The stop hook repeats related branch and plan integrity checks and adds backup/content diff enforcement in `hooks/loop-codex-stop-hook.sh:112-236`.
- edge_cases_or_failure_modes: No active loop is a no-op allow path. Missing state fields are treated as outdated schema and block. Git branch lookup failure or timeout blocks closed. `ls-files` exit `0` means tracked, exit `1` means not tracked, exit `124` means timeout; any other exit is treated as unexpected git failure and blocks at `hooks/loop-plan-file-validator.sh:120-140` and `hooks/loop-plan-file-validator.sh:190-210`. For tracked plans, uncommitted plan changes block at `hooks/loop-plan-file-validator.sh:177-185`; committed content changes are not detected here, but the stop hook catches plan-vs-backup diffs later at `hooks/loop-codex-stop-hook.sh:229-236`. For untracked plans, only accidental git tracking is checked; deletion/content drift is deferred to the stop hook. `PLAN_TRACKED` is compared only to literal `"true"`, so malformed non-empty values fall into the gitignored/untracked branch.
- validation_or_tests: `tests/test-plan-file-hooks.sh` directly exercises this hook. Covered cases include valid state and quoted `plan_file` parsing at `tests/test-plan-file-hooks.sh:91-119`, missing `plan_tracked` at `tests/test-plan-file-hooks.sh:121-139`, missing `start_branch` at `tests/test-plan-file-hooks.sh:141-159`, branch change blocking at `tests/test-plan-file-hooks.sh:164-185`, quoted `start_branch` handling at `tests/test-plan-file-hooks.sh:341-386`, and hyphenated plan-file paths at `tests/test-plan-file-hooks.sh:431-460`. The broader stop-hook integrity tests cover complementary plan mutation/deletion/backup cases at `tests/test-plan-file-hooks.sh:469-731`. I did not run the test suite because the task requested research-only notes and the branch export is read-only; local `git rev-parse/status` also failed because this export does not expose a usable `.git` repository in the sandbox.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-049 `file` `prompt-template/block/large-files.md`
- cursor: `[_]`
- core_role: Block-message template for the stop-hook large-file gate. It defines the remediation contract shown when changed tracked/new code or documentation files exceed the configured line-count threshold, making it part of the RLCR exit gate rather than executable algorithm code.
- algorithmic_behavior: The template is static markdown with two placeholders: `{{MAX_LINES}}` for the threshold and `{{LARGE_FILES}}` for the computed offending-file list at `prompt-template/block/large-files.md:3-4`. It then explains why oversized files block completion at `prompt-template/block/large-files.md:6-9` and splits required remediation by file class: code-file modularization and behavior preservation at `prompt-template/block/large-files.md:13-17`, documentation splitting and cross-reference maintenance at `prompt-template/block/large-files.md:19-23`, followed by a commit-and-retry instruction at `prompt-template/block/large-files.md:25`.
- inputs_outputs_state: Inputs are rendered variables supplied by `hooks/loop-codex-stop-hook.sh`: `MAX_LINES=2000` from `hooks/loop-codex-stop-hook.sh:334` and `LARGE_FILES`, assembled as markdown bullets containing filename, line count, and file type at `hooks/loop-codex-stop-hook.sh:381-384`. The rendered output becomes the JSON `reason` field returned by the stop hook at `hooks/loop-codex-stop-hook.sh:395-407`. The template itself has no state transition, but it communicates the required state transition for the user/agent: split files below the threshold, commit the changes, then attempt stop again.
- gates_or_invariants: The executable gate that invokes this template runs only when git is available and `git rev-parse --git-dir` succeeds at `hooks/loop-codex-stop-hook.sh:304-326`. It scans cached `git status --porcelain` output, extracts each changed path, follows rename syntax to the destination path, skips deleted/missing files, classifies supported code extensions and documentation extensions, counts lines with `wc -l`, ignores non-numeric counts, and blocks if any supported changed file has line count greater than `MAX_LINES`; this is implemented at `hooks/loop-codex-stop-hook.sh:339-385`. The template reinforces the invariant that code files must be split into modules below the threshold without changing functionality, and docs must be split into logical sections below the threshold with navigation/cross-reference continuity.
- dependencies_and_callers: Called only through `load_and_render_safe "$TEMPLATE_DIR" "block/large-files.md"` in `hooks/loop-codex-stop-hook.sh:395-397`. Rendering uses the shared template loader from `hooks/lib/template-loader.sh`, whose placeholder syntax and single-pass substitution contract are documented at `hooks/lib/template-loader.sh:7-13` and implemented at `hooks/lib/template-loader.sh:50-132`; fallback behavior is at `hooks/lib/template-loader.sh:167-203`. The template depends semantically on the stop hook’s file-type classifier at `hooks/loop-codex-stop-hook.sh:358-373`, because it gives separate instructions for code and documentation files. It also references the optional `code-simplifier` plugin at `prompt-template/block/large-files.md:16`.
- edge_cases_or_failure_modes: If the template is missing or renders empty, the stop hook falls back to a shorter inline message at `hooks/loop-codex-stop-hook.sh:388-397`. Files outside the hard-coded extension lists are ignored even if large. Only files present in git status are inspected, so unchanged oversized files do not trigger this gate. Deleted files are skipped at `hooks/loop-codex-stop-hook.sh:353-356`. Rename lines are normalized to the destination path at `hooks/loop-codex-stop-hook.sh:348-351`. Files exactly equal to `MAX_LINES` pass because the comparison is strictly greater-than at `hooks/loop-codex-stop-hook.sh:381`. The final instruction says to commit before exiting; if the repo is not in a valid git context, this specific large-file gate is skipped, but the preceding git-status gate can block on git failures.
- validation_or_tests: I found no direct large-file gate test in the test suite. Template rendering is covered generically by `tests/test-templates-comprehensive.sh` placeholder checks, and the stop-hook call path is visible in `hooks/loop-codex-stop-hook.sh:328-408`. The related search found no dedicated test that creates a changed file over `MAX_LINES` and asserts the `block/large-files.md` reason. I did not run tests because the requested work is research-only and the branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 item evidence sections present, one for each assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`