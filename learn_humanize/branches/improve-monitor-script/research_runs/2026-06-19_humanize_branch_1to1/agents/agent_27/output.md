# agent_27 improve-monitor-script 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `5af20b79e6fec323a2d5cb9344a6a584db1c635a`

## Item Evidence

### IMPROVE_MONITOR_SCRIPT-HZ-027 `file` `tests/test-ansi-parsing.sh`
- cursor: `[_]`
- core_role: Executable specification for the test aggregator’s ANSI-aware summary parser. It verifies that colored test-suite output can be normalized before extracting numeric `Passed:` and `Failed:` counts. The behavior under test is used by [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/tests/run-all-tests.sh:64), which strips ANSI sequences before aggregating suite totals.
- algorithmic_behavior: The script constructs colored and plain-text inputs, defines `esc=$'\033'`, strips SGR color/control sequences with `sed "s/${esc}\\[[0-9;]*m//g"`, then uses `grep -oE 'Passed:[[:space:]]*[0-9]+'` or `grep -oE 'Failed:[[:space:]]*[0-9]+'` followed by `grep -oE '[0-9]+$'` to extract counts. Its scenarios cover basic color removal, multiple color spans, count extraction, zero values, multi-line summaries with `tail -1`, plain text, and combined bold/color attributes in [tests/test-ansi-parsing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/tests/test-ansi-parsing.sh:38).
- inputs_outputs_state: Inputs are synthetic shell strings containing either ANSI SGR escape sequences or plain `Passed:`/`Failed:` lines. Outputs are human-readable PASS/FAIL lines plus process exit status. State is limited to `TESTS_PASSED` and `TESTS_FAILED` counters initialized at lines 18-19 and incremented by `pass()`/`fail()` at lines 21-31. Successful completion requires `TESTS_FAILED -eq 0`, then exits `0`; otherwise exits `1` in [tests/test-ansi-parsing.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/tests/test-ansi-parsing.sh:170).
- gates_or_invariants: The main invariant is that ANSI color sequences must not interfere with count extraction. The sed pattern intentionally matches only CSI SGR sequences ending in `m` with numeric/semicolon parameters, preserving cross-platform compatibility noted in the comments at lines 5-6 and 38-39. The parser also expects summary labels in the exact `Passed:`/`Failed:` form and decimal counts at the end of the matched fragment.
- dependencies_and_callers: Depends on Bash ANSI-C quoting, `sed`, `grep -E`, `tail`, and normal shell arithmetic. It is listed as a suite in [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/tests/run-all-tests.sh:43). The aggregator repeats the same core stripping/extraction algorithm at lines 64-68, so this file is a regression spec for that aggregation logic rather than a helper library.
- edge_cases_or_failure_modes: Covered edge cases include no ANSI codes, zero failed count, multiple colored spans, bold plus color (`1;32m`), and multi-line output where the final summary should be selected with `tail -1`. Uncovered failure modes include non-SGR ANSI sequences, lowercase/malformed labels, counts not at the end of the `grep` match, and escape parameters containing characters outside `[0-9;]`.
- validation_or_tests: I ran `bash tests/test-ansi-parsing.sh` from the branch export. Result: exit `0`, eight scenarios passed, zero failed. The suite summary printed `Passed: 8` and `Failed: 0`.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-057 `file` `prompt-template/block/unpushed-commits.md`
- cursor: `[_]`
- core_role: Prompt/block template for the stop-hook gate that blocks loop exit when `--push-every-round` is enabled and the current branch has local commits ahead of its remote. The template supplies the user-facing remediation contract for that gate.
- algorithmic_behavior: The template has two placeholders, `{{AHEAD_COUNT}}` and `{{CURRENT_BRANCH}}`, and renders a direct instruction to run `git push origin {{CURRENT_BRANCH}}`. It is loaded by [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/hooks/loop-codex-stop-hook.sh:489) after the hook detects `ahead N` in `git status -sb` at lines 477-481. Rendering is performed through `load_and_render_safe`, which falls back to an inline message if the file is missing or empty.
- inputs_outputs_state: Inputs are `AHEAD_COUNT`, derived from the numeric portion of `git status -sb`’s `ahead` marker, and `CURRENT_BRANCH`, derived from `git rev-parse --abbrev-ref HEAD` in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/hooks/loop-codex-stop-hook.sh:481). Output is Markdown assigned to `REASON`, then embedded into a JSON block decision with `"decision": "block"` and a matching system message at lines 493-500. The template itself has no persistent state; it participates in the stop-hook state machine by preventing exit until the branch is no longer ahead.
- gates_or_invariants: The gate is active only when `PUSH_EVERY_ROUND == "true"` in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/hooks/loop-codex-stop-hook.sh:477). That state comes from `push_every_round` in loop state parsing: [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/hooks/lib/loop-common.sh:126) extracts the field and defaults it to `false` at line 134. Setup writes the field into state in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/scripts/setup-rlcr-loop.sh:492). The invariant is that a loop configured to push every round may not stop with local commits ahead of origin.
- dependencies_and_callers: Depends on the shared template loader in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/hooks/lib/template-loader.sh:170). That loader performs single-pass `{{VAR}}` substitution using environment variables and preserves unknown placeholders, documented at lines 7-13 and implemented at lines 71-131. Direct caller is the Codex stop hook. The template is also covered by the comprehensive template test at [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/tests/test-templates-comprehensive.sh:517), which renders `AHEAD_COUNT=3` and `CURRENT_BRANCH=feature-branch`.
- edge_cases_or_failure_modes: If `git status -sb` does not include an `ahead N` marker, the template is not used. If the remote tracking state is absent or localized differently, the grep-based detector may miss unpushed commits. If branch resolution fails, the hook falls back to branch `unknown`, producing a push command that may not be directly usable. If the template is missing, `load_and_render_safe` uses the fallback text instead of failing closed.
- validation_or_tests: Static template validation exists in `tests/test-templates-comprehensive.sh`, which checks the rendered title, count phrase, and branch name. I did not run the full comprehensive suite because the assigned item is the template and the relevant rendering path was directly inspected; the direct ANSI test above was run because it is itself an executable specification.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `2/2 item evidence headings present; each assigned item appears exactly once as a heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`