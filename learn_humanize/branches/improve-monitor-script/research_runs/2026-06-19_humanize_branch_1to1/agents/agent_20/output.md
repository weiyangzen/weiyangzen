# agent_20 improve-monitor-script 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `5af20b79e6fec323a2d5cb9344a6a584db1c635a`

## Item Evidence

### IMPROVE_MONITOR_SCRIPT-HZ-020 `file` `hooks/loop-read-validator.sh`
- cursor: `[_]`
- core_role: PreToolUse Read guard for RLCR loop artifacts. It protects loop state consistency by preventing Claude from reading stale, wrong-location, wrong-round, or disallowed `round-*-todos.md`, `round-*-summary.md`, and `round-*-prompt.md` files. It is wired as the Read hook in `hooks/hooks.json:33-40`.
- algorithmic_behavior: The script reads hook JSON from stdin, extracts `.tool_name`, and exits successfully for non-Read tools at `hooks/loop-read-validator.sh:22-27`. For Read requests, it extracts `.tool_input.file_path` and normalizes a lowercase copy for pattern checks at `hooks/loop-read-validator.sh:29-30`. It first applies a todos-file gate at `hooks/loop-read-validator.sh:36-44`; most `round-N-todos.md` reads are blocked, except allowlisted files in the active loop. It then ignores non-summary/non-prompt files at `hooks/loop-read-validator.sh:50-52`. For summary/prompt files, it finds the active loop, parses `state.md`, extracts the current round, determines file type, and validates location, round, and exact active-loop path at `hooks/loop-read-validator.sh:54-140`.
- inputs_outputs_state: Inputs are stdin hook JSON, `CLAUDE_PROJECT_DIR` or `pwd`, optional `PROJECT_ROOT`, optional `LOOP_BASE_DIR`, optional `LOOP_DIR`, and the active `.humanize/rlcr/<timestamp>/state.md`. It depends on `jq` for JSON extraction at `hooks/loop-read-validator.sh:23` and `hooks/loop-read-validator.sh:29`. Outputs are process status only plus stderr block messages: `0` allows the read; `2` blocks with a rendered template. It does not mutate files or loop state. It observes `STATE_CURRENT_ROUND` via `parse_state_file "$ACTIVE_LOOP_DIR/state.md"` at `hooks/loop-read-validator.sh:69-71`.
- gates_or_invariants: Key invariant is that current round prompt/summary reads must come from the newest active `.humanize/rlcr/<timestamp>` directory, not copied paths or older sessions. The active loop is selected with `find_active_loop`, which only returns the newest timestamp directory if it has `state.md` in `hooks/lib/loop-common.sh:58-79`. Round file type detection is strict basename-style matching for `round-[0-9]+-{summary|prompt|todos}.md` in `hooks/lib/loop-common.sh:144-151`. Round extraction accepts summary, prompt, and todos forms in `hooks/lib/loop-common.sh:153-163`. The allowlist is path-exact and limited to selected early-loop files in `hooks/lib/loop-common.sh:165-186`. Location is gated in three layers: must be under a `.humanize/rlcr/` path at `hooks/loop-read-validator.sh:94-104`, must match `STATE_CURRENT_ROUND` unless allowlisted at `hooks/loop-read-validator.sh:110-123`, and must equal `$ACTIVE_LOOP_DIR/$CLAUDE_FILENAME` at `hooks/loop-read-validator.sh:129-140`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `hooks/loop-read-validator.sh:14-16`; that library also sources `hooks/lib/template-loader.sh` and initializes `TEMPLATE_DIR` in `hooks/lib/loop-common.sh:46-56`. It renders block templates with safe fallbacks for wrong location, wrong round, wrong directory, and todos access via `load_and_render_safe` calls at `hooks/loop-read-validator.sh:99`, `hooks/loop-read-validator.sh:116`, and `hooks/loop-read-validator.sh:135`; todos uses the shared `todos_blocked_message` wrapper in `hooks/lib/loop-common.sh:188-197`. Caller registration is `hooks/hooks.json:33-40`.
- edge_cases_or_failure_modes: If no active loop exists, summary/prompt reads are allowed at `hooks/loop-read-validator.sh:65-67`, but `round-*-todos.md` reads are blocked because the todos gate treats empty active loop as a block condition at `hooks/loop-read-validator.sh:36-43`. Non-round filenames that only resemble loop files but do not match the exact regex exit successfully at `hooks/loop-read-validator.sh:50-52` or `hooks/loop-read-validator.sh:77-80`. Path checks are string-based: `is_in_humanize_loop_dir` only greps for `.humanize/rlcr/` in `hooks/lib/loop-common.sh:251-255`, and final correctness is exact string equality at `hooks/loop-read-validator.sh:131-140`; relative paths, symlinks, path normalization differences, or equivalent paths with `./` may be blocked. Missing `jq`, missing sourced library, or a race where `state.md` disappears after active-loop discovery can cause non-policy failures under `set -euo pipefail` at `hooks/loop-read-validator.sh:12`.
- validation_or_tests: Existing allowlist tests call the read validator directly. `tests/test-allowlist-validators.sh:232-243` verifies active-loop `round-1-todos.md` is allowed. `tests/test-allowlist-validators.sh:245-256` verifies historical `round-0-summary.md` is allowed. `tests/test-allowlist-validators.sh:258-269` verifies non-allowlisted `round-3-todos.md` is blocked. `tests/test-allowlist-validators.sh:271-282` verifies non-current `round-3-summary.md` is blocked. `tests/test-template-references.sh:56-64` includes the read validator in template-reference scanning, and `tests/test-template-references.sh:176-180` treats it as a critical script that must use safe template rendering. I did not run tests because this task requested read-only research notes only.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-050 `file` `prompt-template/block/plan-backup-protected.md`
- cursor: `[_]`
- core_role: Block-message contract for the RLCR plan-backup immutability gate. The file is not executable logic, but it is part of the user-facing validation behavior used when hooks block modifications to `.humanize/rlcr/.../plan.md`.
- algorithmic_behavior: The template declares that the loop-local `plan.md` is a backup of the original plan and cannot be modified at `prompt-template/block/plan-backup-protected.md:3`. It explains the invariant, “plan integrity throughout the RLCR loop,” at `prompt-template/block/plan-backup-protected.md:5`, and directs the agent to read the backup instead of editing it at `prompt-template/block/plan-backup-protected.md:7`. There are no placeholders or conditional sections, so rendering is static.
- inputs_outputs_state: Inputs are indirect: a validator decides that a tool action targets the plan backup, then calls `load_and_render_safe "$TEMPLATE_DIR" "block/plan-backup-protected.md" "$FALLBACK"`. Output is a Markdown reason emitted to stderr by the calling validator, followed by exit `2`. The template itself reads no state and mutates no state; active-loop and current-round context are handled by callers.
- gates_or_invariants: The invariant is that `.humanize/rlcr/<timestamp>/plan.md` is a read-only backup, not the live editable plan. This gate protects the RLCR loop from self-corrupting its original requirements during iteration. The edit validator applies it to `Edit` operations for `plan.md` under `.humanize/rlcr/` at `hooks/loop-edit-validator.sh:84-95`. The write validator applies it to `Write` operations at `hooks/loop-write-validator.sh:100-110`. The bash validator applies it to shell write/edit/delete patterns targeting `.humanize/rlcr(/[^/]+)?/plan.md` at `hooks/loop-bash-validator.sh:81-91`.
- dependencies_and_callers: Direct callers are `hooks/loop-edit-validator.sh:88-94`, `hooks/loop-write-validator.sh:104-109`, and `hooks/loop-bash-validator.sh:87-91`. All depend on `hooks/lib/loop-common.sh` for template loading setup via `hooks/lib/loop-common.sh:46-56`. The bash caller also depends on `command_modifies_file`, whose redirection, `tee`, in-place edit, copy/move/remove, `dd`, `truncate`, and fd-redirection patterns live in `hooks/lib/loop-common.sh:257-285`.
- edge_cases_or_failure_modes: The template has no dynamic path or action placeholders, so the message is stable but less specific than the caller’s fallback text. If the template is missing or template loading fails, callers use fallback strings such as “Writing to plan.md backup is not allowed during RLCR loop” in `hooks/loop-write-validator.sh:106-108` and `hooks/loop-bash-validator.sh:88-90`. The template cannot enforce anything by itself; protection depends entirely on the validators recognizing the target path. The bash regex intentionally covers both direct `.humanize/rlcr/plan.md` and timestamped `.humanize/rlcr/<dir>/plan.md` paths at `hooks/loop-bash-validator.sh:87`.
- validation_or_tests: Plan backup bash protection is covered in `tests/test-plan-file-hooks.sh:241-266`, including regular `rm` and direct `.humanize/rlcr/plan.md` attempts. Bypass-prevention cases for command substitution, glob expansion, brace expansion, piped `tee`, and backticks are covered in `tests/test-plan-file-hooks.sh:272-335`. Template-reference coverage includes validators that call this template in `tests/test-template-references.sh:56-64`, with safe-rendering checks for critical validators at `tests/test-template-references.sh:176-180`. I did not run tests because this task requested read-only research notes only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2 unique item headings present; item identifiers are intentionally not repeated outside their headings to preserve the single-occurrence evidence invariant.
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`