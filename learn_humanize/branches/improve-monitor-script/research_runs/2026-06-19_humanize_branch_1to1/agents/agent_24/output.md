# agent_24 improve-monitor-script 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `5af20b79e6fec323a2d5cb9344a6a584db1c635a`

## Item Evidence

### IMPROVE_MONITOR_SCRIPT-HZ-024 `file` `scripts/setup-rlcr-loop.sh`
- cursor: `[_]`
- core_role: Initializes an RLCR loop session for `/humanize:start-rlcr-loop`. The command wrapper invokes this script directly via `commands/start-rlcr-loop.md:4` and `commands/start-rlcr-loop.md:13`. Its main responsibility is to validate the requested plan and runtime configuration, create the loop state directory under `.humanize/rlcr/<timestamp>/`, seed `state.md`, copy the plan backup, create the goal tracker, and emit the round-0 prompt that starts the workflow.
- algorithmic_behavior: Argument parsing handles positional plan file, `--plan-file`, `--track-plan-file`, `--max`, `--codex-model MODEL:EFFORT`, `--codex-timeout`, and `--push-every-round` at `scripts/setup-rlcr-loop.sh:104`. The script then validates git repository state, validates the plan path and plan content, verifies Codex availability, records the starting branch, creates a timestamped loop directory, writes state frontmatter, builds `goal-tracker.md`, and renders `round-0-prompt.md`. The round-0 prompt forces goal tracker initialization before implementation and instructs the worker to commit and write `round-0-summary.md` before exit (`scripts/setup-rlcr-loop.sh:602`, `scripts/setup-rlcr-loop.sh:646`, `scripts/setup-rlcr-loop.sh:648`).
- inputs_outputs_state: Inputs are CLI args, `CLAUDE_PROJECT_DIR` or current directory as `PROJECT_ROOT` (`scripts/setup-rlcr-loop.sh:186`), the plan file content, git metadata, the current branch, and Codex availability. Outputs are `.humanize/rlcr/<timestamp>/state.md`, `.humanize/rlcr/<timestamp>/plan.md`, `.humanize/rlcr/<timestamp>/goal-tracker.md`, `.humanize/rlcr/<timestamp>/round-0-prompt.md`, and stdout instructions (`scripts/setup-rlcr-loop.sh:467`, `scripts/setup-rlcr-loop.sh:473`, `scripts/setup-rlcr-loop.sh:476`, `scripts/setup-rlcr-loop.sh:485`, `scripts/setup-rlcr-loop.sh:504`, `scripts/setup-rlcr-loop.sh:602`, `scripts/setup-rlcr-loop.sh:667`). The initialized state sets `current_round: 0`, max iterations, Codex model/effort/timeout, push policy, plan file, plan tracking policy, start branch, and UTC start time (`scripts/setup-rlcr-loop.sh:485`). Later transition ownership is in the stop hook: it parses these fields (`hooks/loop-codex-stop-hook.sh:76`), advances `current_round` by editing `state.md` when Codex returns issues (`hooks/loop-codex-stop-hook.sh:967`), and ends loops by renaming `state.md` through `end_loop` (`hooks/lib/loop-common.sh:301`).
- gates_or_invariants: The script fails closed unless the project is a git repo with at least one commit (`scripts/setup-rlcr-loop.sh:211`, `scripts/setup-rlcr-loop.sh:217`). Plan path gates reject absolute paths, whitespace, shell metacharacters, symlinks, missing parents, missing/unreadable files, paths outside the project, and submodule-contained plan files (`scripts/setup-rlcr-loop.sh:227`, `scripts/setup-rlcr-loop.sh:234`, `scripts/setup-rlcr-loop.sh:243`, `scripts/setup-rlcr-loop.sh:253`, `scripts/setup-rlcr-loop.sh:259`, `scripts/setup-rlcr-loop.sh:266`, `scripts/setup-rlcr-loop.sh:272`, `scripts/setup-rlcr-loop.sh:281`, `scripts/setup-rlcr-loop.sh:292`). Tracking gates require a tracked and clean plan with `--track-plan-file`; otherwise the plan must be untracked, though the code checks only git tracking status rather than proving the file is gitignored (`scripts/setup-rlcr-loop.sh:331`, `scripts/setup-rlcr-loop.sh:347`). Content gates require at least five physical lines and at least three nonblank, non-comment content lines (`scripts/setup-rlcr-loop.sh:362`, `scripts/setup-rlcr-loop.sh:377`, `scripts/setup-rlcr-loop.sh:410`). YAML-safety gates reject unsafe branch, model, and effort characters before writing unquoted state fields (`scripts/setup-rlcr-loop.sh:435`, `scripts/setup-rlcr-loop.sh:445`, `scripts/setup-rlcr-loop.sh:454`).
- dependencies_and_callers: Depends on `scripts/portable-timeout.sh` for bounded git operations (`scripts/setup-rlcr-loop.sh:27`; helper behavior at `scripts/portable-timeout.sh:33`). Downstream hooks depend on the exact state schema: `hooks/lib/loop-common.sh:15` defines field names and `hooks/lib/loop-common.sh:110` parses the state frontmatter. The stop hook enforces branch consistency, schema freshness, plan integrity, summary existence, Codex review, and round advancement using this initialized state (`hooks/loop-codex-stop-hook.sh:90`, `hooks/loop-codex-stop-hook.sh:119`, `hooks/loop-codex-stop-hook.sh:154`, `hooks/loop-codex-stop-hook.sh:170`, `hooks/loop-codex-stop-hook.sh:510`, `hooks/loop-codex-stop-hook.sh:800`). Write/edit/bash validators protect generated loop files and state from user-side mutation (`hooks/loop-write-validator.sh:95`, `hooks/loop-edit-validator.sh:79`, `hooks/loop-bash-validator.sh:76`). The monitor reads active or terminal state files for UI status (`scripts/humanize.sh:98`, `scripts/humanize.sh:138`).
- edge_cases_or_failure_modes: `--max` and `--codex-timeout` accept `0` because validation uses `^[0-9]+$`, despite user-facing messages saying positive integer (`scripts/setup-rlcr-loop.sh:114`, `scripts/setup-rlcr-loop.sh:141`). Markdown headings beginning with `#` are treated as comments for content-line counting, so a plan made mostly of headings can fail as insufficient content (`scripts/setup-rlcr-loop.sh:372`, `scripts/setup-rlcr-loop.sh:402`). Default mode says the plan must be gitignored, but the implementation only rejects tracked files and does not call `git check-ignore` (`scripts/setup-rlcr-loop.sh:347`). Real-path containment compares against raw `PROJECT_ROOT`; symlinked or differently normalized project roots can affect the containment check (`scripts/setup-rlcr-loop.sh:281`, `scripts/setup-rlcr-loop.sh:287`). Goal and acceptance-criteria extraction is heuristic and can leave placeholders for Round 0 when headings do not match the expected patterns (`scripts/setup-rlcr-loop.sh:527`, `scripts/setup-rlcr-loop.sh:550`).
- validation_or_tests: Direct test coverage exists in `tests/test-plan-file-validation.sh` for plan validation, model/effort validation, explicit `--plan-file`, and good-plan success paths. Hook/state behavior around setup-created fields is covered in `tests/test-plan-file-hooks.sh`, `tests/test-state-exit-naming.sh`, `tests/test-allowlist-validators.sh`, and `tests/test-bash-validator-patterns.sh`. I did not run tests because this assignment is research-only and the branch export is read-only.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-054 `file` `prompt-template/block/state-file-modification.md`
- cursor: `[_]`
- core_role: Defines the user-facing block message for attempted `state.md` modification. It is a small but core guard template because `state.md` is the active-loop cursor and configuration file seeded by `scripts/setup-rlcr-loop.sh`.
- algorithmic_behavior: The template states that `state.md` cannot be modified because it is managed by the loop system and contains current round number, max iterations, and Codex configuration (`prompt-template/block/state-file-modification.md:1`). It does not perform logic itself; it is loaded by the hook common helper `state_file_blocked_message` (`hooks/lib/loop-common.sh:208`) through `load_and_render_safe`, which falls back to inline text if the template is missing (`hooks/lib/loop-common.sh:210`, `hooks/lib/template-loader.sh:170`).
- inputs_outputs_state: Inputs are only the template file contents and the hook context that decides a state write/edit/bash mutation was attempted. Output is a denial message printed to stderr by pre-tool validators, causing Claude Code to block the tool use. The state transition it protects is negative: it prevents user/tool mutation of active loop state so only loop hooks can advance rounds or end the loop. System-owned state transitions remain in `hooks/loop-codex-stop-hook.sh:967` for next-round advancement and `hooks/lib/loop-common.sh:301` for terminal state renaming.
- gates_or_invariants: The invariant is that `state.md` is loop-system-managed and should not be user-written. Write tool attempts are blocked by `hooks/loop-write-validator.sh:95`; Edit tool attempts are blocked by `hooks/loop-edit-validator.sh:79`; Bash bypass attempts are blocked by `hooks/loop-bash-validator.sh:76`. The path recognizer treats paths ending in `state.md` as state files (`hooks/lib/loop-common.sh:245`). The bash detector covers redirection, `tee`, in-place edits, copy/move/removal, `dd`, `truncate`, `printf` redirection, and `exec` redirection patterns (`hooks/lib/loop-common.sh:260`).
- dependencies_and_callers: Registered hook entrypoints are in `hooks/hooks.json:14` for PreToolUse and include Write, Edit, Read, and Bash validators; this template is used by Write/Edit/Bash state mutation blockers. It depends on `hooks/lib/template-loader.sh`, whose renderer uses single-pass `{{VAR}}` substitution and keeps missing placeholders unchanged (`hooks/lib/template-loader.sh:50`). Tests reference the template in `tests/test-template-references.sh:155` and validate bash modification pattern behavior for `state.md` in `tests/test-bash-validator-patterns.sh:181`.
- edge_cases_or_failure_modes: The message is generic and does not include the attempted path, active loop directory, or remediation beyond not modifying the file, so diagnosis depends on the surrounding hook message context. Because Bash blocking uses the broad file pattern `state\.md`, an active loop can block shell commands modifying any file named `state.md`, not only files inside `.humanize/rlcr` (`hooks/loop-bash-validator.sh:76`). If the template is absent or empty, the helper uses a shorter inline fallback, so behavior remains blocking but the richer explanation is reduced (`hooks/lib/loop-common.sh:210`).
- validation_or_tests: Covered indirectly by template reference tests and bash validator pattern tests. The Write/Edit validators also call the same helper, but I did not find a dedicated test that asserts the exact rendered text from this template for all tool paths. I did not run tests because this assignment is research-only and the branch export is read-only.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 2/2 item evidence headings present; each assigned item appears once in Item Evidence
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`