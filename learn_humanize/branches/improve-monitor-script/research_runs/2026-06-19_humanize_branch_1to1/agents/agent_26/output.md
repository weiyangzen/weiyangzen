# agent_26 improve-monitor-script 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 2
- source_commit: `5af20b79e6fec323a2d5cb9344a6a584db1c635a`

## Item Evidence

### IMPROVE_MONITOR_SCRIPT-HZ-026 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR hook allowlist behavior. It proves which otherwise-protected loop files may bypass the normal todos/summary gates, and verifies that the bypass is path-scoped to the active loop directory.
- algorithmic_behavior: The test initializes a temporary git-backed loop fixture in `setup_test_loop`, creates `.humanize/rlcr/2024-01-01_12-00-00/state.md`, and sets `current_round: 5` so allowlisted historical files are intentionally non-current (`tests/test-allowlist-validators.sh:33`, `tests/test-allowlist-validators.sh:53`). It directly tests `is_allowlisted_file` from `hooks/lib/loop-common.sh`, then feeds JSON hook payloads to read/write/edit/bash validators over stdin (`tests/test-allowlist-validators.sh:17`, `tests/test-allowlist-validators.sh:135`, `tests/test-allowlist-validators.sh:191`, `tests/test-allowlist-validators.sh:234`, `tests/test-allowlist-validators.sh:290`).
- inputs_outputs_state: Inputs are synthesized Claude hook JSON objects with `tool_name` and `tool_input.file_path` or `tool_input.command`. Expected outputs are exit status `0` for allowed access and exit status `2` plus an explanatory block message for blocked access. Test state is held in `TESTS_PASSED` and `TESTS_FAILED`, and the script exits with the failed count (`tests/test-allowlist-validators.sh:23`, `tests/test-allowlist-validators.sh:26`, `tests/test-allowlist-validators.sh:27`, `tests/test-allowlist-validators.sh:391`).
- gates_or_invariants: The allowlist is exact path equality, not basename matching. The expected allowed set is active-loop `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` (`tests/test-allowlist-validators.sh:70`, `tests/test-allowlist-validators.sh:78`, `tests/test-allowlist-validators.sh:86`, `tests/test-allowlist-validators.sh:94`). The expected blocked set includes later todos, later summaries, allowlisted basenames in the wrong directory, relative todos paths, old loop directories, and same-basename paths under a different root (`tests/test-allowlist-validators.sh:102`, `tests/test-allowlist-validators.sh:110`, `tests/test-allowlist-validators.sh:118`, `tests/test-allowlist-validators.sh:340`, `tests/test-allowlist-validators.sh:353`, `tests/test-allowlist-validators.sh:368`).
- dependencies_and_callers: Depends on `hooks/lib/loop-common.sh`, especially `is_allowlisted_file`, `find_active_loop`, `is_round_file_type`, and `command_modifies_file`. The implementation allowlist is hard-coded in `hooks/lib/loop-common.sh:168`. The read, write, and edit validators all call `is_allowlisted_file` before blocking todos files (`hooks/loop-read-validator.sh:36`, `hooks/loop-write-validator.sh:37`, `hooks/loop-edit-validator.sh:36`). The bash validator separately requires the active loop full path for `round-1-todos.md` or `round-2-todos.md` (`hooks/loop-bash-validator.sh:136`). The suite is included by `tests/run-all-tests.sh:44`.
- edge_cases_or_failure_modes: The tests cover the important bypass risks: a basename match outside the active loop must not pass, an old loop directory must not pass, and a path with the same timestamp basename under `/tmp` must not pass. Bash coverage is path-restricted and command-pattern based, so it validates common write forms such as redirection and `tee`, but does not prove every possible shell mutation syntax. The fixture uses a real `git init` and temporary filesystem writes, so runtime execution is environment-sensitive.
- validation_or_tests: This file is itself the validation suite. It checks 25 cases: 7 direct allowlist checks, 4 write-validator cases, 3 edit-validator cases, 4 read-validator cases, and 7 bash-validator path-restriction cases (`tests/test-allowlist-validators.sh:64`, `tests/test-allowlist-validators.sh:127`, `tests/test-allowlist-validators.sh:186`, `tests/test-allowlist-validators.sh:229`, `tests/test-allowlist-validators.sh:285`). I did not execute it in this read-only research pass because the instruction was to produce notes only and the test creates temporary repos/files.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-056 `file` `prompt-template/block/todos-file-access.md`
- cursor: `[_]`
- core_role: Non-executable block template for the todos-file access gate. It defines the message shown when a validator blocks access to `round-*-todos.md` files and directs the agent to native TodoWrite state instead.
- algorithmic_behavior: The template is static markdown with no placeholders. Its contract is: do not create or access `round-*-todos.md`; use the native TodoWrite tool; native todo tools provide visible state tracking and Claude Code task-management integration (`prompt-template/block/todos-file-access.md:1`, `prompt-template/block/todos-file-access.md:3`, `prompt-template/block/todos-file-access.md:5`, `prompt-template/block/todos-file-access.md:7`).
- inputs_outputs_state: Input is the template lookup from `todos_blocked_message` in `hooks/lib/loop-common.sh`; output is rendered markdown written to stderr by the validator before exit status `2`. There is no local state transition in the template itself. If template loading fails, `todos_blocked_message` has an inline fallback with the same core instruction (`hooks/lib/loop-common.sh:190`, `hooks/lib/loop-common.sh:196`).
- gates_or_invariants: The template encodes the user-facing invariant that todos files are not the normal task-state surface. The executable gate is in the validators: read/write/edit block non-allowlisted todos paths (`hooks/loop-read-validator.sh:36`, `hooks/loop-write-validator.sh:37`, `hooks/loop-edit-validator.sh:36`), and bash blocks command-based todos modification unless the command targets active-loop `round-1-todos.md` or `round-2-todos.md` by full path (`hooks/loop-bash-validator.sh:136`).
- dependencies_and_callers: Called through `todos_blocked_message` in `hooks/lib/loop-common.sh:190`. That function is reached from `hooks/loop-read-validator.sh:41`, `hooks/loop-write-validator.sh:42`, `hooks/loop-edit-validator.sh:41`, and `hooks/loop-bash-validator.sh:141`. Template existence is cross-checked by `tests/test-template-references.sh:152`.
- edge_cases_or_failure_modes: The text says not to create or access all `round-*-todos.md` files, while the implementation intentionally allows a narrow compatibility exception for active-loop `round-1-todos.md` and `round-2-todos.md`. That means the template is accurate for blocked paths but does not document the allowlist exception exercised by `tests/test-allowlist-validators.sh`. Since the template has no variables, the `action` argument passed to `todos_blocked_message` does not affect the rendered message.
- validation_or_tests: `tests/test-template-references.sh` includes `block/todos-file-access.md` in the common template existence list (`tests/test-template-references.sh:152`). The allowlist validator tests assert that blocked todos cases produce an error containing “todos,” which indirectly validates that the todos block path is used (`tests/test-allowlist-validators.sh:166`, `tests/test-allowlist-validators.sh:265`, `tests/test-allowlist-validators.sh:321`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: IMPROVE_MONITOR_SCRIPT-HZ-026, IMPROVE_MONITOR_SCRIPT-HZ-056
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`