# agent_05 improve-monitor-script 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `5af20b79e6fec323a2d5cb9344a6a584db1c635a`

## Item Evidence

### IMPROVE_MONITOR_SCRIPT-HZ-005 `directory` `scripts`
- cursor: `[_]`
- core_role: The `scripts` directory is the shell entry/control surface for RLCR loop setup, timeout portability, and live monitoring. Recursive inspection found exactly three files: `scripts/humanize.sh`, `scripts/portable-timeout.sh`, and `scripts/setup-rlcr-loop.sh`, totaling the assigned 56,399 bytes.
- algorithmic_behavior: `scripts/setup-rlcr-loop.sh` initializes a loop from a plan file: it parses CLI options in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/scripts/setup-rlcr-loop.sh:104), validates the repository and plan, creates `.humanize/rlcr/<timestamp>/`, writes `state.md`, backs up the plan, creates `goal-tracker.md`, and emits `round-0-prompt.md`. `scripts/humanize.sh` implements `humanize monitor rlcr-loop` by finding the latest timestamped session, resolving Codex run logs from `$HOME/.cache/humanize/<sanitized-project-path>/<timestamp>/`, parsing loop state and goal tracker metrics, rendering a fixed terminal status bar, and switching to newer sessions/log files as they appear; the command dispatcher is in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/scripts/humanize.sh:681). `scripts/portable-timeout.sh` abstracts timeout execution across GNU `gtimeout`, GNU `timeout`, Python subprocess timeouts, or an explicit no-timeout fallback in [scripts/portable-timeout.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/scripts/portable-timeout.sh:10).
- inputs_outputs_state: Inputs include CLI args (`plan.md`, `--max`, `--codex-model`, `--codex-timeout`, `--push-every-round`, `--track-plan-file`), `CLAUDE_PROJECT_DIR`, the current Git repository, `codex` availability, plan file content, terminal dimensions, `.humanize/rlcr/*` state directories, and cached Codex logs. Outputs include `.humanize/rlcr/<timestamp>/state.md` with `current_round: 0` and model/plan metadata in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/scripts/setup-rlcr-loop.sh:485), generated `goal-tracker.md` sections in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/scripts/setup-rlcr-loop.sh:507), generated round prompt and summary path in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/scripts/setup-rlcr-loop.sh:600), and terminal monitor output. Monitor state is in-memory: current session dir, current log file, tail offset, loop status, and redraw flags. It does not mutate loop state; it detects active/stopped status from `state.md` or `<reason>-state.md` in [scripts/humanize.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/scripts/humanize.sh:103).
- gates_or_invariants: Setup fails closed unless the project is a Git repo with a commit, the plan path is relative, has no spaces or shell metacharacters, is not a symlink, resolves inside the project, is not inside a submodule, and satisfies tracking policy in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/scripts/setup-rlcr-loop.sh:211). It requires at least five lines and at least three non-comment content lines in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/scripts/setup-rlcr-loop.sh:362), requires `codex`, and rejects YAML-unsafe branch/model/effort values in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/scripts/setup-rlcr-loop.sh:429). Monitor requires `.humanize/rlcr` to exist, recognizes only timestamp-like session names, and treats `state.md` as active while `<reason>-state.md` indicates a stopped loop.
- dependencies_and_callers: `/humanize:start-rlcr-loop` is wired to run `scripts/setup-rlcr-loop.sh` in [commands/start-rlcr-loop.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/commands/start-rlcr-loop.md:12). Hooks consume the same state contract through `hooks/lib/loop-common.sh`, where active loops are detected by the newest directory containing `state.md` in [hooks/lib/loop-common.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/hooks/lib/loop-common.sh:58). `setup-rlcr-loop.sh` sources `portable-timeout.sh` in [scripts/setup-rlcr-loop.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/scripts/setup-rlcr-loop.sh:26); `hooks/loop-codex-stop-hook.sh` also sources it for Codex review execution. README documents the monitor command and state/cache layout in [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/README.md:100) and [README.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/README.md:242).
- edge_cases_or_failure_modes: Monitor handles no session, no log yet, newer session without log, same-session newer round log, and loop status changes while waiting. It may be sensitive to terminal capability (`tput`) and delimiter assumptions because parsed state/tracker fields are pipe-delimited before array splitting. Setup intentionally rejects absolute paths, spaces, metacharacters, submodule paths, symlinked plan files, tracked plan files unless opted in, dirty tracked plan files when opted in, too-thin plans, missing `codex`, and unsafe YAML characters. Timeout fallback returns GNU-like `124` on Python timeout, but runs without a timeout if no supported implementation exists.
- validation_or_tests: The directory is covered by the test harness listed in [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/tests/run-all-tests.sh:32), including plan validation, state exit naming, ANSI parsing, and template tests. I did not execute the full suite because this branch export is read-only and even a simple Git status attempted restricted temp/cache writes in this environment.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-035 `file` `tests/test-templates-comprehensive.sh`
- cursor: `[_]`
- core_role: This file is an executable specification for the prompt-template algorithm used by RLCR hooks. It validates template discovery, loading, placeholder syntax, rendering behavior, fallback behavior, real-template integration, and stress behavior for the whole `prompt-template/` tree.
- algorithmic_behavior: It sources `hooks/lib/template-loader.sh` in [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/tests/test-templates-comprehensive.sh:17), resolves `TEMPLATE_DIR`, then runs seven sections. It verifies required subdirectories `block`, `codex`, and `claude`; loads every markdown template via `find ... -print0`; validates placeholder syntax with regex checks for extra braces, wrong bracket types, unclosed placeholders, spaces, and lowercase placeholders; constructs malformed placeholder examples; exercises `render_template` with empty, multiline, special-character, markdown, long, adjacent, and repeated placeholder values; checks `load_and_render_safe` fallback semantics; smoke-renders real templates; and finally renders every template with dummy values to detect unreplaced placeholders.
- inputs_outputs_state: Inputs are the project root, `hooks/lib/template-loader.sh`, all `prompt-template/**/*.md` files, generated temp directories, and inline template/value cases. Outputs are colored PASS/FAIL/WARN lines, aggregate counters, and exit code `0` only when `TESTS_FAILED` remains zero in [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/tests/test-templates-comprehensive.sh:619). Internal state is limited to `TESTS_PASSED`, `TESTS_FAILED`, `WARNINGS`, per-section locals, and temporary dirs removed after use.
- gates_or_invariants: The template directory must exist and contain `block/`, `codex/`, and `claude/` in [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/tests/test-templates-comprehensive.sh:64). Every template must load and be non-empty or produce at least a warning/failure. Placeholder syntax is constrained to uppercase `{{VAR_NAME}}` shape in [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/tests/test-templates-comprehensive.sh:118). Safe rendering must preserve empty lines, replace repeated/adjacent placeholders, and keep fallback behavior deterministic. The final all-template render gate warns on unreplaced placeholders but fails only on load/render failure.
- dependencies_and_callers: It depends directly on `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` from [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/hooks/lib/template-loader.sh:24). It is included in the aggregate suite in [tests/run-all-tests.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/tests/run-all-tests.sh:33). Real templates used as integration checks include `block/wrong-round-number.md`, `block/unpushed-commits.md`, and `codex/goal-tracker-update-section.md`.
- edge_cases_or_failure_modes: The syntax checker is regex-based, so it may produce conservative warnings or miss complex malformed cases, but it explicitly tests common malformed placeholder shapes. CJK/emoji coverage is intentionally conservative: CJK is represented with ASCII placeholder labels and text values, and emoji-like status patterns are ASCII-only. Backslash, quote, markdown, and multiline value tests mostly assert no crash rather than exact byte-for-byte preservation. Missing templates fall back through `load_and_render_safe`; incomplete template directories are rejected.
- validation_or_tests: This file is itself the validation artifact. Its final summary prints passed, failed, and warning counts, and returns failure when any counted test fails. It also tests real loader semantics documented in `template-loader.sh`, including single-pass substitution that prevents placeholder injection in [hooks/lib/template-loader.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/hooks/lib/template-loader.sh:54). I inspected it directly but did not run it due the read-only branch-export constraint noted above.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-065 `file` `prompt-template/claude/next-round-footer.md`
- cursor: `[_]`
- core_role: This is a prompt footer template that defines the final handoff obligations for a continued RLCR round after Codex has found issues. It is small, but algorithmically relevant because it is appended to generated next-round prompts and tells the worker what artifact must exist before the next exit attempt.
- algorithmic_behavior: The template starts with a separator, warns not to escape the RLCR loop by lying, editing state, or executing `cancel-rlcr-loop`, then requires optional code simplification, a descriptive commit, and a summary written to `@{{NEXT_SUMMARY_FILE}}` in [prompt-template/claude/next-round-footer.md](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/prompt-template/claude/next-round-footer.md:4). It has one placeholder, `NEXT_SUMMARY_FILE`, rendered by the template loader.
- inputs_outputs_state: Input is the `NEXT_SUMMARY_FILE` value supplied by the stop hook after it computes `NEXT_ROUND` and `NEXT_SUMMARY_FILE`. Output is appended prompt text inside `.humanize/rlcr/<timestamp>/round-<next>-prompt.md`. It does not directly mutate repository or loop state, but it establishes the expected next summary file that the exit hook later validates.
- gates_or_invariants: The prompt-level invariants are: do not bypass loop termination rules, commit before exit, and write the round summary to the precise next-round path. The enforcement is indirect: hook validators and the stop hook check state, summaries, and protected files; this template provides the instruction surface used when blocking exit and continuing the loop.
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` appends this template after updating `current_round`, creating `NEXT_PROMPT_FILE`, and setting `NEXT_SUMMARY_FILE` in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/hooks/loop-codex-stop-hook.sh:967). The exact call uses `load_and_render_safe` with a fallback in [hooks/loop-codex-stop-hook.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/hooks/loop-codex-stop-hook.sh:998), then may append push and goal-tracker request templates. Rendering depends on `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: If this template is missing or empty, the stop hook falls back to a shorter “Before Exiting” message that still names `{{NEXT_SUMMARY_FILE}}`. If `NEXT_SUMMARY_FILE` were omitted from render arguments, the placeholder would remain because the loader preserves missing placeholders; the current caller supplies it. The leading blank line plus `---` is intentional as a prompt separator, but consumers that trim leading whitespace could alter formatting.
- validation_or_tests: `tests/test-templates-comprehensive.sh` loads and syntax-validates all markdown templates and stress-renders all placeholders with dummy values, which covers this footer’s loadability, uppercase placeholder syntax, and renderability through the all-template loop in [tests/test-templates-comprehensive.sh](/Users/wangweiyang/GitHub/humanize_branch_worktrees/improve-monitor-script/tests/test-templates-comprehensive.sh:563). There is no dedicated footer-specific assertion beyond the all-template coverage and caller fallback behavior.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3; each assigned item section header is present exactly once`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`