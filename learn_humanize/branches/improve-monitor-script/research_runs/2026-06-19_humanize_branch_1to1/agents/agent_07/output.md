# agent_07 improve-monitor-script 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `5af20b79e6fec323a2d5cb9344a6a584db1c635a`

## Item Evidence

### IMPROVE_MONITOR_SCRIPT-HZ-007 `file` `README.md`
- cursor: `[_]`
- core_role: User-facing behavior contract for the Humanize RLCR loop and monitor workflow. It defines the loop’s purpose, state model, artifact layout, commands, monitor expectations, goal-tracker discipline, and termination states. The README is behavior-defining because the scripts and hooks implement the documented flow: plan input -> Claude implementation -> summary -> Codex review -> continue/complete/stop.
- algorithmic_behavior: Describes the RLCR cycle as a feedback loop where a plan feeds Claude work, Claude writes summaries, Codex reviews, and Codex either returns feedback or terminates with `COMPLETE`/max-iteration completion (`README.md:31-39`). Documents the required plan inputs and minimum content shape: description, acceptance criteria, optional technical approach, and at least 5 lines (`README.md:41-55`), which corresponds to `scripts/setup-rlcr-loop.sh` plan validation. Documents monitor invocation as `humanize monitor rlcr-loop` and the live dashboard outputs for session, round progress, goal/progress summary, git status, plan path, and log output (`README.md:88-110`), implemented by `scripts/humanize.sh` parsing state, goal tracker, git status, and cache logs (`scripts/humanize.sh:138-285`, `scripts/humanize.sh:303-435`, `scripts/humanize.sh:519-674`).
- inputs_outputs_state: Primary input is a markdown plan path supplied to `/humanize:start-rlcr-loop` with options for max iterations, Codex model/effort, timeout, and push behavior (`README.md:57-71`, `README.md:188-201`). Runtime state lives under `.humanize/rlcr/<timestamp>/`, with `state.md` as the active-loop sentinel (`README.md:73-86`, `README.md:242-253`). `state.md` contains current round/config in YAML frontmatter and is created with fields such as `current_round`, `max_iterations`, `codex_model`, `codex_effort`, `codex_timeout`, `push_every_round`, `plan_file`, `plan_tracked`, `start_branch`, and `started_at` (`scripts/setup-rlcr-loop.sh:485-498`). Outputs include per-round prompt, summary, review prompt, review result, Codex cache command/stdout/stderr logs, and exit-state files (`README.md:246-266`).
- gates_or_invariants: Active loop existence is defined solely by `state.md` in the newest timestamped loop directory (`README.md:112-119`). Cancellation is a state transition by renaming `state.md` to `cancel-state.md` (`README.md:120-127`), consistent with `commands/cancel-rlcr-loop.md:28-33`. Goal tracker invariants are documented as immutable ultimate goal/ACs and mutable active/completed/deferred/evolution sections (`README.md:131-143`). Full alignment checks happen at rounds 4, 9, 14, etc. and may emit `STOP` as a stagnation circuit breaker (`README.md:143-152`), implemented as every `CURRENT_ROUND % 5 == 4` in `hooks/loop-codex-stop-hook.sh:644-648`.
- dependencies_and_callers: README points users to `commands/start-rlcr-loop.md`, `commands/cancel-rlcr-loop.md`, hook scripts, `scripts/setup-rlcr-loop.sh`, `scripts/portable-timeout.sh`, and `scripts/humanize.sh` in the documented tree (`README.md:213-239`). `hooks/hooks.json` wires the behavior into Claude Code hook phases: `UserPromptSubmit`, `PreToolUse` for Write/Edit/Read/Bash, and `Stop` for Codex review (`hooks/hooks.json:4-63`). The monitor command documented in the README dispatches through `humanize monitor rlcr-loop` to `_humanize_monitor_codex` (`scripts/humanize.sh:681-714`).
- edge_cases_or_failure_modes: README-defined exit states cover successful completion, cancellation, max iteration exhaustion, circuit-breaker stop, and unexpected schema/integrity failure (`README.md:255-260`). The monitor handles missing `.humanize/rlcr` by returning an error (`scripts/humanize.sh:16-21`), missing sessions by reporting no session directories (`scripts/humanize.sh:493-497`), missing logs with a waiting/no-log message (`scripts/humanize.sh:534-597`), and newer sessions/logs by switching automatically (`scripts/humanize.sh:631-673`). Documentation has a minor naming mismatch: README lists `complete-state.md` (`README.md:256`), while the monitor examples/comments mention statuses like `completed`/`cancelled` in color mapping (`scripts/humanize.sh:395-400`) and the common `end_loop` helper uses `${reason}-state.md` with `complete`/`cancel` reasons (`hooks/lib/loop-common.sh:323-327`).
- validation_or_tests: README itself does not contain tests, but it documents observable validation surfaces: plan setup must pass setup-script checks, summaries and Codex review artifacts can be inspected with `cat`/`tail` (`README.md:73-86`), and live monitor should expose progress/git/log state (`README.md:88-110`). Related validators enforce plan/state/branch checks and protected file access through hooks (`hooks/loop-plan-file-validator.sh:48-130`, `hooks/loop-read-validator.sh:32-142`, `hooks/loop-write-validator.sh:33-190`, `hooks/loop-bash-validator.sh:51-146`).
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-037 `file` `hooks/lib/loop-common.sh`
- cursor: `[_]`
- core_role: Shared shell library for RLCR hook state parsing, active-loop discovery, protected round-file classification, block-message rendering, command-modification detection, and loop termination. It centralizes constants and helper functions used by the loop validators and Codex stop hook.
- algorithmic_behavior: Defines state frontmatter field names (`plan_tracked`, `start_branch`, `plan_file`, `current_round`, `max_iterations`, `push_every_round`, `codex_model`, `codex_effort`, `codex_timeout`) and review/exit markers (`COMPLETE`, `STOP`, `complete`, `cancel`, `maxiter`, `stop`, `unexpected`) (`hooks/lib/loop-common.sh:15-40`). Sources `template-loader.sh`, resolves `TEMPLATE_DIR`, and warns rather than fails if templates are missing, enabling inline fallback messages (`hooks/lib/loop-common.sh:46-56`). `find_active_loop` selects only the newest timestamped child directory under the loop base and returns it only if it has `state.md`, preventing older abnormal directories from being revived (`hooks/lib/loop-common.sh:58-79`). `parse_state_file` extracts YAML-like frontmatter, assigns `STATE_*` globals, strips legacy quotes for selected path/string fields, and defaults current round/max iterations/push flag (`hooks/lib/loop-common.sh:96-137`).
- inputs_outputs_state: Inputs are loop base dirs, `state.md` paths, file paths, lowercase command strings, and template vars. Outputs are stdout values for discovery/parsing helpers and process exit codes for predicates. `parse_state_file` mutates caller-visible shell variables (`STATE_FRONTMATTER`, `STATE_PLAN_TRACKED`, etc.) rather than returning structured data (`hooks/lib/loop-common.sh:96-109`). `end_loop` is the only mutating helper: it renames active `state.md` to `<reason>-state.md` and reports the state transition to stderr (`hooks/lib/loop-common.sh:301-334`).
- gates_or_invariants: Active-loop invariant is newest directory plus `state.md`, not any older directory with state (`hooks/lib/loop-common.sh:58-75`). Round files must match `round-[0-9]+-(summary|prompt|todos).md` and round extraction is limited to those types (`hooks/lib/loop-common.sh:144-163`). The hardcoded allowlist permits only `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in the active loop (`hooks/lib/loop-common.sh:165-186`). Protected file predicates recognize goal tracker, state, and `.humanize/rlcr/` paths (`hooks/lib/loop-common.sh:239-255`). `command_modifies_file` blocks common shell write vectors: redirection, `tee`, in-place `sed`/`awk`/`perl`, `mv`/`cp`, `rm`, `dd of=`, `truncate`, `printf >`, and `exec >` (`hooks/lib/loop-common.sh:257-285`). `end_loop` rejects any reason outside the five defined exit reasons before renaming state (`hooks/lib/loop-common.sh:313-321`).
- dependencies_and_callers: Depends on `hooks/lib/template-loader.sh` for `get_template_dir`, `validate_template_dir`, and `load_and_render_safe` (`hooks/lib/loop-common.sh:46-55`). It is sourced by `hooks/loop-read-validator.sh`, `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-bash-validator.sh`, `hooks/loop-plan-file-validator.sh`, and `hooks/loop-codex-stop-hook.sh`. Callers use it to enforce read/write/edit/bash gates: todos/prompt protection (`hooks/loop-read-validator.sh:36-42`, `hooks/loop-write-validator.sh:37-49`), state/plan backup/goal tracker protection (`hooks/loop-write-validator.sh:95-120`, `hooks/loop-edit-validator.sh:79-105`, `hooks/loop-bash-validator.sh:76-108`), summary current-round matching (`hooks/loop-write-validator.sh:153-170`, `hooks/loop-edit-validator.sh:112-138`), branch/schema state checks (`hooks/loop-plan-file-validator.sh:40-82`), and stop-hook exits (`hooks/loop-codex-stop-hook.sh:92-106`, `hooks/loop-codex-stop-hook.sh:614-617`, `hooks/loop-codex-stop-hook.sh:923-959`).
- edge_cases_or_failure_modes: `find_active_loop` ignores older directories even if they contain `state.md`; if the newest directory lacks `state.md`, no active loop is returned (`hooks/lib/loop-common.sh:58-79`). `parse_state_file` uses grep/sed frontmatter parsing, so duplicate fields or complex YAML are not structurally validated; missing numeric fields default, while some callers validate numeric/model safety later (`hooks/lib/loop-common.sh:117-135`, `hooks/loop-codex-stop-hook.sh:90-112`). `command_modifies_file` is pattern-based and may miss complex shell forms, quoted paths with spaces, command substitutions, or multi-command bypasses not matching its regex list (`hooks/lib/loop-common.sh:264-285`). Template validation warning allows graceful degradation but can hide missing template coverage until a fallback is used (`hooks/lib/loop-common.sh:53-56`).
- validation_or_tests: No direct unit test file for `loop-common.sh` was found in assigned inspection. Functional validation is distributed through hook callers registered in `hooks/hooks.json:4-63`. The plan-file validator checks required state fields and branch consistency using `FIELD_*` constants (`hooks/loop-plan-file-validator.sh:72-108`). The stop hook revalidates model/effort/numeric fields and calls `end_loop` on corrupted state (`hooks/loop-codex-stop-hook.sh:90-112`). Bash/read/write/edit validators exercise path classifiers and command-modification gates in normal hook execution.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-067 `file` `prompt-template/claude/post-alignment-action-items.md`
- cursor: `[_]`
- core_role: Claude prompt addendum inserted immediately after a full alignment check round. It is a small but behavior-defining transition template that biases the next implementation round toward Codex-discovered alignment gaps, failed acceptance criteria, and unjustified deferrals.
- algorithmic_behavior: Contains a section header and three action-item priorities for the post-alignment round: address forgotten items, prioritize acceptance criteria marked not met, and un-defer unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:2-7`). It does not perform computation itself; its algorithmic effect is prompt-level control over the next Claude work cycle after Codex’s full alignment audit.
- inputs_outputs_state: Input is the full-alignment status implied by `CURRENT_ROUND % 5 == 4` in the stop hook (`hooks/loop-codex-stop-hook.sh:644-648`). Output is appended text in the next round prompt file after `claude/next-round-prompt.md` is rendered and before the footer/push/goal-update request sections (`hooks/loop-codex-stop-hook.sh:980-1002`). The template has no variables, so it is loaded raw through `load_template` rather than rendered (`hooks/loop-codex-stop-hook.sh:990-995`).
- gates_or_invariants: The append gate is strict and deterministic: only rounds where the just-reviewed round is a full alignment check append this template (`hooks/loop-codex-stop-hook.sh:990-995`). If the template file is missing or empty, no post-alignment addendum is appended; there is no fallback in this specific call path (`hooks/loop-codex-stop-hook.sh:992-995`). The semantic invariant is that alignment findings must affect the next round’s priorities, especially forgotten work, unmet ACs, and unjustified deferrals (`prompt-template/claude/post-alignment-action-items.md:4-7`).
- dependencies_and_callers: Only direct caller found is `hooks/loop-codex-stop-hook.sh`, which loads `claude/post-alignment-action-items.md` from `TEMPLATE_DIR` (`hooks/loop-codex-stop-hook.sh:990-995`). The template depends on the Codex full-alignment review contract in `prompt-template/codex/full-alignment-review.md`, which includes forgotten-item and deferred-item audit concepts; the README also documents full alignment checks and stagnation STOP behavior (`README.md:143-152`).
- edge_cases_or_failure_modes: Because it has no variable placeholders and no fallback, a missing/empty file silently removes post-alignment guidance. The template also assumes Codex used labels/concepts compatible with “Forgotten Items,” “AC Status,” and “Deferred Items”; if the full-alignment review uses different wording, Claude may need to infer the mapping. It is advisory prompt content, so enforcement relies on subsequent Codex review and goal-tracker gates rather than a parser.
- validation_or_tests: No direct automated test for this template was found. Runtime validation is indirect: the stop hook appends it only in post-full-alignment prompt generation (`hooks/loop-codex-stop-hook.sh:990-995`), and later Codex reviews check goal alignment and may continue or STOP based on unresolved alignment problems (`hooks/loop-codex-stop-hook.sh:923-959`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item evidence sections present`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`