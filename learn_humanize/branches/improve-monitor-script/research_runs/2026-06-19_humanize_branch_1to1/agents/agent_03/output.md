# agent_03 improve-monitor-script 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 3
- source_commit: `5af20b79e6fec323a2d5cb9344a6a584db1c635a`

## Item Evidence

### IMPROVE_MONITOR_SCRIPT-HZ-003 `directory` `hooks`
- cursor: `[_]`
- core_role: `hooks` is the RLCR loop enforcement and orchestration layer for Claude Code. `hooks/hooks.json:4-58` binds `UserPromptSubmit`, `PreToolUse` for `Write`/`Edit`/`Read`/`Bash`, and `Stop` to the directory scripts; `hooks/loop-codex-stop-hook.sh` is the main loop coordinator.
- algorithmic_behavior: The shared library in `hooks/lib/loop-common.sh` defines state field names and markers, finds only the newest active `.humanize/rlcr/<timestamp>/state.md` loop (`62-79`), parses YAML-like frontmatter (`110-137`), recognizes round files (`144-163`), enforces a small allowlist (`168-186`), builds templated block messages (`188-237`, `287-299`), detects shell file mutations by regex (`257-285`), and terminates loops by renaming `state.md` to `<reason>-state.md` (`301-334`).
- algorithmic_behavior: `hooks/lib/template-loader.sh` resolves `prompt-template` from `hooks/lib` (`26-31`), loads template files or returns empty with a warning (`36-48`), performs single-pass `{{VAR}}` substitution with an awk scanner and `TMPL_VAR_` environment values (`58-131`), supports fallback rendering (`170-203`), and validates that `block`, `codex`, and `claude` template subdirectories exist (`208-222`).
- algorithmic_behavior: `hooks/loop-write-validator.sh` reads hook JSON from stdin, allows non-`Write` events, blocks round todos except allowlisted files, blocks prompt writes (`23-50`), finds active loop state (`78-89`), blocks `state.md`, protected `plan.md`, and post-round-0 `goal-tracker.md` writes (`95-120`), blocks summary writes outside `.humanize/rlcr` using `block/wrong-summary-location.md` (`127-134`), rejects wrong summary round numbers (`153-170`), and rejects files in the wrong active loop directory (`177-188`).
- algorithmic_behavior: `hooks/loop-edit-validator.sh` mirrors edit protection for todos/prompts (`22-49`), only applies deeper checks inside `.humanize/rlcr` (`55-73`), blocks state, `plan.md`, and post-round-0 goal tracker edits (`79-105`), and rejects wrong-round summary edits (`112-136`). Unlike the write validator, it exits early for summary-like files outside the loop directory.
- algorithmic_behavior: `hooks/loop-read-validator.sh` blocks reading round todos except allowlisted files (`36-43`), limits summary/prompt checks to round files (`50-52`), requires an active loop (`61-66`), rejects round files outside `.humanize/rlcr` (`94-103`), rejects wrong-round reads (`110-123`), and rejects reads from older loop directories by exact path comparison (`129-139`).
- algorithmic_behavior: `hooks/loop-bash-validator.sh` blocks `git push` unless `push_every_round` is true (`59-68`), blocks shell mutations of `state.md`, loop `plan.md`, `goal-tracker.md`, prompt files, summary files, and most todo files (`76-144`). Summary shell writes are always blocked so the `Write`/`Edit` path validators can enforce exact loop paths.
- algorithmic_behavior: `hooks/loop-plan-file-validator.sh` runs on `UserPromptSubmit`, finds an active loop (`29-36`), validates required state schema fields `plan_tracked` and `start_branch` (`52-82`), blocks if the current git branch differs from the start branch (`88-108`), then enforces plan tracking invariants: tracked plans must still be tracked and clean, while untracked plans must remain untracked (`114-220`).
- algorithmic_behavior: `hooks/check-todos-from-transcript.py` parses the Claude transcript JSONL, supports assistant-message, message, and direct tool-use TodoWrite shapes (`20-87`), returns exit `1` with an `INCOMPLETE_TODOS` list when any latest todo status is not `completed`, and returns exit `2` for invalid hook-input JSON (`90-129`).
- algorithmic_behavior: `hooks/loop-codex-stop-hook.sh` is the round state machine. It loads active state (`59-88`), validates Codex model/effort and numeric state (`90-112`), blocks stale schema, branch changes, missing or modified plan backups (`119-238`), blocks incomplete native todos before Codex review (`246-294`), fails closed on git-status errors (`304-325`), blocks changed code/docs files over 2000 lines (`334-408`), requires clean git state and optional pushed commits (`418-503`), requires the round summary (`510-530`), checks round-0 goal tracker initialization (`537-605`), enforces max iterations (`612-617`), builds regular or full-alignment Codex prompts (`632-710`), runs `codex exec` with timeout and debug logs (`716-837`), validates review output (`860-914`), accepts only a final non-empty line of exactly `COMPLETE` or `STOP` (`916-960`), otherwise increments `current_round`, writes the next prompt, and returns a JSON block to continue (`967-1033`).
- inputs_outputs_state: Inputs are Claude Code hook JSON, `CLAUDE_PROJECT_DIR`, `.humanize/rlcr/<timestamp>/state.md`, plan backup and original plan contents, Claude transcript path, git status, round summary/prompt/review files, and prompt templates. Outputs are exit `0` allows, exit `2` PreToolUse blocks with stderr, JSON `{"decision":"block"}` responses for prompt/stop hooks, updates to `state.md`, generated review and next-round prompt files, review result files, and loop-ending state renames.
- gates_or_invariants: Active loop selection is newest-directory-only; branch must remain unchanged; required state fields must exist; plan file must remain immutable relative to backup; protected loop files cannot be modified directly; round summaries/prompts must be current-round and active-loop scoped; native TodoWrite items must be completed before review; git must be inspectable and clean; large changed files are rejected; pushes are blocked unless configured; Codex completion depends on exact terminal marker lines.
- dependencies_and_callers: Called by Claude Code through `hooks/hooks.json`. Runtime dependencies include `bash`, `jq`, `git`, `diff`, `sed`, `awk`, `grep`, `wc`, `python3`, `codex`, and `scripts/portable-timeout.sh`, whose `run_with_timeout` prefers `gtimeout`, GNU `timeout`, Python subprocess timeout, then no-timeout fallback (`scripts/portable-timeout.sh:10-71`). Message and prompt dependencies are under `prompt-template/block`, `prompt-template/claude`, and `prompt-template/codex`.
- edge_cases_or_failure_modes: No active loop generally allows actions. Git failures in active-loop plan/stop gates block rather than allow. Missing templates degrade to inline fallback messages. `find_active_loop` ignores older active-looking directories, preventing stale loop revival but making timestamp order authoritative. Bash mutation regexes can miss complex shell constructs or produce conservative matches. Edit validation does not block a summary-named file outside `.humanize/rlcr`, while Write validation does. Codex missing, timeout, nonzero exit, missing result, or empty result all block loop exit.
- validation_or_tests: `tests/test-template-loader.sh` directly validates the shared template loader used by all hook messages. No dedicated hook integration test was found in the assigned directory; behavior is specified by script gates and Claude Code hook exit/JSON contracts.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-033 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/template-loader.sh`, which is a core dependency for hook block prompts and Codex/Claude prompt assembly.
- algorithmic_behavior: The test derives `PROJECT_ROOT` from its own location and sources `hooks/lib/template-loader.sh` (`10-12`), keeps pass/fail counters (`20-34`), then runs ordered assertions for path resolution, template loading, rendering, fallback behavior, directory validation, literal special-character rendering, placeholder-injection prevention, and edge cases before exiting according to failure count (`641-659`).
- algorithmic_behavior: Tests 1-14 verify `get_template_dir`, `load_template` existing/missing behavior, single and multiple substitutions, multiline templates, path-like values, `load_and_render` against `block/wrong-round-number.md`, unused variables, unreplaced variables, fallback rendering, existing-template priority, and valid/invalid template directory validation (`41-244`).
- algorithmic_behavior: Tests 15-33 assert shell metacharacters in values render literally, including ampersands, backslashes, dollar signs, backticks, pipes, semicolons, globs, parentheses, redirections, quotes, comments, tildes, combined shell fragments, Windows paths, regex text, JSON text, and multiline values (`246-486`).
- algorithmic_behavior: Tests 34-36 lock down the loader’s single-pass invariant: injected values containing `{{VAR}}` are not recursively substituted, including a realistic `REVIEW_CONTENT` case that mentions `{{GOAL_TRACKER_FILE}}` and `{{PLAN_FILE}}` (`488-543`).
- algorithmic_behavior: Tests 37-41 cover empty variable values, ASCII and byte-escaped non-ASCII template/value content, underscore-prefixed variable names, and numeric variable names (`545-639`).
- inputs_outputs_state: Inputs are the repository layout, `prompt-template/block/git-push.md`, `prompt-template/block/wrong-round-number.md`, inline template strings, and `VAR=value` render arguments. Outputs are colored PASS/FAIL lines, final passed/failed counts, and process exit `0` when `TESTS_FAILED` is zero or `1` otherwise. State is limited to shell variables `TESTS_PASSED` and `TESTS_FAILED`.
- gates_or_invariants: `set -euo pipefail` makes unexpected command failures fatal. Expected render outputs are mostly exact string comparisons. Missing template loading must return empty. Missing variables must remain as placeholders. Values must not be interpreted by shell syntax. Fallbacks must render variables. Template directory validity requires the `block`, `codex`, and `claude` subdirectories, matching `validate_template_dir`.
- dependencies_and_callers: Depends on Bash, grep, ANSI-capable terminal output, the prompt templates named above, and `hooks/lib/template-loader.sh`. It is a standalone test script; no hook registration or CI caller was visible in the assigned paths.
- edge_cases_or_failure_modes: Command substitution can mask trailing newline differences, so final newline preservation is not strongly tested. It does not test malformed placeholders without closing braces, path traversal template names, invalid environment-variable-style names, or NUL bytes. Several tests depend on stable template prose such as “Git Push Blocked” and “current round is **5**”.
- validation_or_tests: The whole file is the validation artifact. It specifically guards the template loader implementation at `hooks/lib/template-loader.sh:58-131`, whose single-pass awk behavior is critical to prevent prompt content such as Codex reviews from being reinterpreted as template syntax.
- skip_candidate: `no`

### IMPROVE_MONITOR_SCRIPT-HZ-063 `file` `prompt-template/block/wrong-summary-location.md`
- cursor: `[_]`
- core_role: Small block-message template for a write-path gate that forces RLCR summary files into the active loop directory.
- algorithmic_behavior: The template defines a Markdown block titled “Wrong Summary Location,” states that summary files must be in the loop directory, and renders the correct destination through `{{CORRECT_PATH}}` (`prompt-template/block/wrong-summary-location.md:1-5`).
- inputs_outputs_state: Input is the `CORRECT_PATH` substitution supplied by `hooks/loop-write-validator.sh`. Output is a rendered Markdown error message written to stderr by the Write hook before blocking. It has no persistent state and no conditional logic of its own.
- gates_or_invariants: The enforced invariant is that a `round-*-summary.md` Write target outside `.humanize/rlcr/` is invalid. The active-loop path is computed as `$ACTIVE_LOOP_DIR/round-${CURRENT_ROUND}-summary.md` immediately before rendering (`hooks/loop-write-validator.sh:127-134`).
- dependencies_and_callers: Called only through `load_and_render_safe "$TEMPLATE_DIR" "block/wrong-summary-location.md"` in `hooks/loop-write-validator.sh:132`. Rendering depends on the single-pass template loader in `hooks/lib/template-loader.sh:58-131`; if this file is missing or empty, the write validator falls back to inline text at `hooks/loop-write-validator.sh:129-131`.
- edge_cases_or_failure_modes: The template does not include the attempted wrong path, current round, or loop directory by itself, so diagnostic specificity depends on `CORRECT_PATH`. If `CORRECT_PATH` is not supplied, the placeholder remains by design. This gate applies to `Write`; `Edit` and `Bash` reach different summary protections.
- validation_or_tests: The exact template is not directly asserted in `tests/test-template-loader.sh`, but the loader behavior it relies on is covered by fallback, real-template, and placeholder substitution tests. The caller has an inline fallback, so missing-template behavior remains covered by loader semantics.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `3/3 item sections present exactly once by heading`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`