# agent_delta_01 oh-my-humanize main incremental research

## Worker Summary
- status: `[_]`
- source_commit_old: `6b3819fad50a89fffae899b240ad1ce065c51d23`
- source_commit_new: `bf4509d4f5a669375b3c88510ba0449e9770884c`
- assigned_item_count: 1

## Item Evidence

### OH_MY_HUMANIZE_MAIN-HZ-1967 `file` `packages/coding-agent/src/cli/workflow-cli.ts`
- cursor: `[_]`
- current_core_role: `workflow-cli.ts` is the headless `omp workflow` command dispatcher for `list`, `freeze`, `start`, `install`, and `uninstall`. For `start`, it resolves a flow by name/path relative to `--cwd` or `getProjectDir()`, requires a frozen `.omhflow` artifact, constructs an in-memory run/lifecycle store, builds a headless runtime host for script/agent nodes, starts `runWorkflow`, and emits either JSON or human-readable run status. Evidence: `handleStart` resolves `cwd`, flow spec, package, start nodes, run/family/attempt IDs, runtime host, lifecycle, and runner options in `packages/coding-agent/src/cli/workflow-cli.ts:185-248`; JSON status is assembled from reconstructed runs/families at `packages/coding-agent/src/cli/workflow-cli.ts:255-302`.
- algorithmic_delta_since_old_commit: The targeted delta is headless JS eval execution context. `handleStart` now passes the resolved workflow `cwd` into `runHeadlessEvalScript`, not just code/language, so JS script nodes run with `process.cwd()` set to the requested workspace. Inside `runHeadlessEvalScript`, current behavior saves `previousCwd` and `originalConsoleLog`, calls `process.chdir(cwd)`, captures `console.log` calls into `capturedOutput`, executes the script via an `AsyncFunction`, appends a non-empty returned value formatted by `formatScriptValue`, then restores both `console.log` and the previous cwd in `finally`. Evidence: caller changed at `packages/coding-agent/src/cli/workflow-cli.ts:200-204`; implementation is `packages/coding-agent/src/cli/workflow-cli.ts:417-447`. This changes outputs too: JS eval output is now newline-joined console output plus optional returned value, whereas before the result value alone was returned. `formatConsoleArgument` preserves strings and JSON-stringifies non-strings through `formatScriptValue` (`packages/coding-agent/src/cli/workflow-cli.ts:515-522`). Related abort/deadline behavior in the current headless start path is that the same `startSignal.signal` is supplied to `runWorkflow` as both scheduler `signal` and `nodeAbortSignal`, and `maxRuntimeMs` is always set from `--max-runtime-ms` or `DEFAULT_WORKFLOW_MAX_RUNTIME_MS` (`packages/coding-agent/src/cli/workflow-cli.ts:242-244`). The runner then combines the workflow signal and timeout signal, also combining node abort signals with the timeout signal, so a max-runtime timeout can stop scheduling and abort active node execution (`packages/coding-agent/src/workflow/runner.ts:148-170`).
- current_inputs_outputs_state: Inputs are `WorkflowCommandArgs` with `action`, positional `args`, and flags including `json`, `force`, `runId`, `familyId`, `startNodeId`, activation limits, `maxRuntimeMs`, and `cwd`. For `start`, the required positional input is `<flow-or-path>`; `cwd` is normalized with `path.resolve`; omitted `--start` means all root nodes are used, falling back to the first node if no root exists (`packages/coding-agent/src/cli/workflow-cli.ts:185-195`, `packages/coding-agent/src/cli/workflow-cli.ts:378-382`). State is held in `InMemoryWorkflowStoreHost`, which appends custom/session store entries and is later reconstructed into workflow runs/families (`packages/coding-agent/src/cli/workflow-cli.ts:249-255`, `packages/coding-agent/src/cli/workflow-cli.ts:531-542`). JSON output includes `flow`, `run.id/status/activation counts/frontier/maxRuntimeMs`, lifecycle `families` with freezes/attempts/checkpoints/change requests, and compact `runs` with activation count and sorted state keys (`packages/coding-agent/src/cli/workflow-cli.ts:261-302`). Human output prints run ID, flow label, status, activation totals, and frontier when non-empty (`packages/coding-agent/src/cli/workflow-cli.ts:305-313`). Headless shell scripts still spawn `sh -c` in the resolved cwd, pipe stdout/stderr, pass `request.signal`, and use `workflowScriptEnvironment` (`packages/coding-agent/src/cli/workflow-cli.ts:449-471`). Headless agent tasks spawn the current CLI invocation with `launch --cwd <cwd>`, optional `--model`, and `-p <assignment>` (`packages/coding-agent/src/cli/workflow-cli.ts:474-503`).
- new_or_changed_gates_or_invariants: Headless `start` remains artifact-only: non-`.omhflow` paths throw `WorkflowPackageError` with the distributable artifact guidance (`packages/coding-agent/src/cli/workflow-cli.ts:362-375`). Runtime binding validation blocks unavailable capabilities before execution unless `--max-activations 0`, which allows binding/lifecycle inspection without launching nodes (`packages/coding-agent/src/cli/workflow-cli.ts:206-218`). Human nodes mark `ask` unavailable and warn that headless CLI cannot answer human nodes (`packages/coding-agent/src/cli/workflow-cli.ts:385-414`). Python eval scripts remain unsupported in headless CLI and return `{ exitCode: 1, output: "", error: "headless workflow CLI does not support py eval scripts", language: "py" }` (`packages/coding-agent/src/cli/workflow-cli.ts:417-424`). JS eval now has a process-global invariant: cwd and `console.log` are temporarily mutated but must be restored in `finally` even on script error (`packages/coding-agent/src/cli/workflow-cli.ts:425-446`). Interrupt handling installs one-shot `SIGINT`/`SIGTERM` listeners, aborts once with `workflow interrupted by <event>`, and `dispose()` removes both listeners after runner completion (`packages/coding-agent/src/cli/workflow-cli.ts:316-335`). For abort/deadline, active node execution receives `context.nodeAbortSignal ?? context.signal`, and abort reasons produce activation-aborted records instead of failed records (`packages/coding-agent/src/workflow/runner.ts:373-385`, `packages/coding-agent/src/workflow/runner.ts:413-421`). `awaitWorkflowNodeExecution` races node work against abort and rejects on abort even if the runtime promise ignores the signal, using a zero-delay timer to let cooperative runtimes settle first (`packages/coding-agent/src/workflow/runner.ts:432-460`).
- dependencies_and_callers: `runWorkflowCommand` is called by the CLI command wrapper in `packages/coding-agent/src/commands/workflow.ts`, while the headless start flow depends on artifact registry/package loading/freezing (`resolveWorkflowFlowSpec`, `loadWorkflowArtifact`, `freezeWorkflowArtifact`), runtime binding checks (`workflowRuntimeBindingUnavailableError`), lifecycle reconstruction (`reconstructWorkflowFamilies`), run reconstruction (`reconstructWorkflowRuns`), and the shared runner (`runWorkflow`). The headless runtime host is produced by `createSessionWorkflowRuntimeHost`, whose script path calls either eval or shell adapters and converts non-zero results into `WorkflowNodeRuntimeError` (`packages/coding-agent/src/workflow/session-runtime.ts:84-148`). The session runtime forwards node `signal` into agent/review and shell script requests (`packages/coding-agent/src/workflow/session-runtime.ts:129-135`, `packages/coding-agent/src/workflow/session-runtime.ts:192-193`); eval requests do not carry an abort signal in the current interface, so headless JS eval cancellation relies on the runner’s wrapper rejecting on abort rather than interrupting synchronous JS execution. Scheduler integration passes both workflow and node abort signals into each activation context and marks activation results as `aborted` when either signal is aborted (`packages/coding-agent/src/workflow/scheduler.ts:121-139`, `packages/coding-agent/src/workflow/scheduler.ts:232-246`).
- edge_cases_or_failure_modes: A JS script that reads relative files now reads from `--cwd`, not from the artifact/resource directory or the process launch directory; packaged script contents are still resolved/materialized by the workflow runner before eval. `console.log` capture is process-global, so concurrently executing headless JS evals in the same process could interfere, although this CLI path runs a single in-memory workflow command. Non-string console arguments are JSON-stringified; `undefined` becomes an empty string, so `console.log(undefined)` contributes an empty segment. Circular/non-JSON values passed to `console.log` or returned by a script can make `JSON.stringify` throw; the eval runner catches that as `{ exitCode: 1, output: "", error }`, dropping any previously captured output (`packages/coding-agent/src/cli/workflow-cli.ts:430-442`). `process.chdir(cwd)` can throw if the requested cwd is missing or inaccessible; restoration still runs only after the `try` begins. SIGINT/SIGTERM stop the workflow and active node because the CLI uses the same abort signal for scheduler and nodes, yielding stopped/checkpointed runs rather than leaving a live attempt. If max runtime elapses, the runner records the timeout reason `workflow max runtime elapsed after <n>ms`, aborts the active node, and checkpoints frontier; if a runtime ignores the signal, the runner still resolves through `awaitWorkflowNodeExecution`. If a node finishes successfully after the scheduler-level signal aborts but before node abort, downstream nodes are not scheduled and eligible children become the checkpoint frontier (`packages/coding-agent/src/workflow/scheduler.ts:172-184`).
- validation_or_tests: Current focused CLI tests cover the delta. `runs headless js workflow scripts from the requested cwd` creates a `.omhflow` whose JS file reads `Bun.file("marker.txt")`, runs `omp workflow start` with `cwd` set to a workspace containing that marker, and asserts completed status plus state key `marker` (`packages/coding-agent/src/cli/__tests__/workflow-cli.test.ts:147-175`, helper script at `packages/coding-agent/src/cli/__tests__/workflow-cli.test.ts:340-386`). `checkpoints headless workflow starts on SIGINT instead of leaving a run alive` injects a fake signal target, emits `SIGINT` while a shell node sleeps, and asserts run status `stopped`, frontier `["hold"]`, attempt status `stopped`, checkpoint frontier `["hold"]`, and listener cleanup (`packages/coding-agent/src/cli/__tests__/workflow-cli.test.ts:178-222`, helper flow/script at `packages/coding-agent/src/cli/__tests__/workflow-cli.test.ts:389-423`). Runner-level regression tests validate node abort/deadline contracts: deadline-aborted lifecycle activations are checkpointed as stopped/aborted (`packages/coding-agent/test/workflow/runner.test.ts:510-561`), ignored aborts still checkpoint within the test race (`packages/coding-agent/test/workflow/runner.test.ts:564-623`), and `maxRuntimeMs` aborts a running node with reason `workflow max runtime elapsed after 1ms` plus checkpoint frontier `["build"]` (`packages/coding-agent/test/workflow/runner.test.ts:626-675`). Slash-command integration tests validate the same runtime/deadline behavior in background workflows, including max-runtime abort (`packages/coding-agent/test/workflow/slash-command.test.ts:3448-3532`), stop deadline abort for live background nodes (`packages/coding-agent/test/workflow/slash-command.test.ts:4057-4137`), and real shell process deadline checkpointing (`packages/coding-agent/test/workflow/slash-command.test.ts:4140-4228`).
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `OH_MY_HUMANIZE_MAIN-HZ-1967`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`