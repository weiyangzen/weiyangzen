# agent_29 main 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 120
- source_commit: `6b3819fad50a89fffae899b240ad1ce065c51d23`

## Item Evidence

### OH_MY_HUMANIZE_MAIN-HZ-029 `directory` `packages/collab-web`
- cursor: `[_]`
- core_role: Browser guest client for live collaboration sessions, encrypted room links, relay socket transport, transcript/agent views, and standalone tool renderers.
- algorithmic_behavior: `src/lib/client.ts` applies ordered `HostFrame` updates into an immutable `GuestSnapshot`; `src/lib/socket.ts` serializes encrypted send/receive chains, reconnects with backoff, and treats fatal close codes/decryption failures as terminal; `src/lib/link.ts` parses/rewrites room links and envelopes; React components project snapshot state into transcript, agent drawer, composer, banners, and tool cards.
- inputs_outputs_state: Inputs are room links, AES room keys, relay WebSocket frames, transcript JSONL, host frames, and optional write tokens. Outputs are guest snapshot state, messages to host, rendered transcript rows, notices, and agent drawer transcript fetches. State includes connection phase, agents, transcript entries, bus frames, notices capped at 50, and per-agent drawer polling.
- gates_or_invariants: 32-byte key or 48-byte key+write-token links; `ws://` allowed only for localhost; welcome timeout is 30s; transcript fetch timeout is 10s; pending socket send queue is capped; AES-GCM payload shape is `[12B IV][ciphertext+tag]`; read-only links hide mutating chat/agent actions.
- dependencies_and_callers: Depends on WebCrypto, browser WebSocket, React, package-local tool-render registry, relay scripts, and collab tests. Parent package is the web guest surface for coding-agent collaboration sessions.
- edge_cases_or_failure_modes: Malformed links, nonlocal plaintext relay URLs, truncated envelopes, decrypt errors, missing welcome, reconnect exhaustion, transcript parse carry-over, and parked/revived agent presentation are explicitly handled.
- validation_or_tests: `packages/collab-web/test/client.test.ts`, `codec.test.ts`, `link.test.ts`, `local-relay.test.ts`, and `markdown.test.tsx`; assigned `link.test.ts` separately covers link/envelope contracts.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-059 `file` `docs/natives-binding-contract.md`
- cursor: `[_]`
- core_role: Architecture contract for direct native binding exports between Rust N-API code and `packages/natives`.
- algorithmic_behavior: Defines that the public API boundary is generated/native `native/index.d.ts`, `native/index.js`, and Rust `#[napi]` exports, not a TypeScript wrapper layer. It codifies sync/async export shape, enum generation, package-version sentinels, and loader expectations.
- inputs_outputs_state: Inputs are Rust N-API declarations, generated TypeScript declarations, generated enum files, native package metadata, and loader package version. Outputs are stable importable native functions/classes/enums.
- gates_or_invariants: Changing sync to async or async to sync is a breaking API change; runtime enum objects must be generated by `scripts/gen-enums.ts`; loader checks the package-version sentinel but not every export; TypeScript facades must not shadow the native contract.
- dependencies_and_callers: Applies to `crates/pi-natives`, `packages/natives/native`, package generation scripts, and downstream packages using native grep/text/image functions.
- edge_cases_or_failure_modes: Wrapper drift, missing generated enum runtime values, version mismatch, native package leaf omissions, and async/sync signature drift.
- validation_or_tests: Supported by native package generation tests and N-API type checks; `packages/natives/test/npm-packages.test.ts` is assigned separately.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-089 `file` `scripts/ci-release-notes.test.ts`
- cursor: `[_]`
- core_role: Regression tests for the release-notes workflow parser/merger.
- algorithmic_behavior: Tests semver numeric ordering, changelog span extraction, package section merging, canonical category order, deduplication of bullets, legacy floor handling, and suppression of blank categories.
- inputs_outputs_state: Inputs are synthetic changelog markdown sections and version floors. Outputs are computed version spans and merged release note sections.
- gates_or_invariants: `[Unreleased]` is skipped; released version spans are selected above the floor; duplicate bullets collapse; category order is canonical; empty categories are not emitted.
- dependencies_and_callers: Validates release note script behavior used by CI/release workflows.
- edge_cases_or_failure_modes: Silent-tag sections above floor, `null` floor legacy extraction, semver lexical-vs-numeric ordering, and duplicate changelog bullets.
- validation_or_tests: The file itself is the regression test; no test run was performed in this read-only pass.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-119 `directory` `crates/pi-iso/src`
- cursor: `[_]`
- core_role: Cross-platform isolation backends and diff extraction for branch/worktree sandboxing.
- algorithmic_behavior: `lib.rs` defines `IsolationBackend` and backend resolution; `diff.rs` computes git-aware or tree-walk diffs; `rcopy.rs` creates fallback copies/worktrees and seeds dirty state; platform backends implement APFS, Btrfs, reflink, overlayfs, ProjFS, Windows block clone, and ZFS strategies.
- inputs_outputs_state: Inputs are source directory, requested backend, filesystem/platform capabilities, and previous/current trees. Outputs are an isolated workspace, backend kind/fallback reason, stopped workspace cleanup, and file diffs with optional unified diff text.
- gates_or_invariants: Backend `probe` must return `Unavailable` for fallback-safe absence; diff skips equal `(size, mtime seconds)` in plain mode; binary files are detected by NUL in the first 8192 bytes; ZFS refuses unrelated dataset destruction; ProjFS disables x64-under-ARM64 emulation.
- dependencies_and_callers: Depends on git CLI for git-mode diff/worktree seeding, filesystem clone/mount APIs, and crate-local diff/resource cleanup paths. Used by higher-level workspace isolation.
- edge_cases_or_failure_modes: Missing backend tools, non-git sources, dirty git state, untracked files, binary diff omission, mount flavor mismatches, stale overlay mounts, and unsafe dataset names.
- validation_or_tests: Crate tests and platform-specific smoke coverage are implied; no tests run here.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-149 `directory` `packages/snapcompact/test`
- cursor: `[_]`
- core_role: Contract tests for snapcompact text normalization, raster shape selection, and PNG/image compaction output.
- algorithmic_behavior: `snapcompact.test.ts` verifies file-list scheme filtering, PNG indexed-output decoding, whitespace/control/ANSI/non-Latin normalization, and shape resolution by API/model family.
- inputs_outputs_state: Inputs are sample text, model/API ids, file list entries, and raster options. Outputs are normalized compaction text, chosen shape variants, and decodable image buffers.
- gates_or_invariants: Control characters and ANSI are normalized; model families map to expected shapes for Claude/Gemini/Kimi/GLM/Qwen-style contexts; scheme filtering keeps relevant paths without leaking irrelevant URL-like forms.
- dependencies_and_callers: Tests the `packages/snapcompact` runtime used by context compaction and shape preview UI.
- edge_cases_or_failure_modes: Indexed PNG decoding, multilingual text, control characters, whitespace collapse, and model-specific shape policy drift.
- validation_or_tests: Directory is test-only; no test run was performed in this pass.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-179 `file` `docs/toolconv/qwen3.md`
- cursor: `[_]`
- core_role: Tool-conversation wire-format specification for Qwen3/Hermes ChatML.
- algorithmic_behavior: Documents system `<tools>` rendering, assistant `<tool_call>` JSON blocks, user `<tool_response>` result turns, `<think>` handling, and vLLM/OpenAI-compatible translation behavior.
- inputs_outputs_state: Inputs are tool schemas, assistant text/thinking/tool calls, and tool result messages. Outputs are ChatML prompt sections and parsed OpenAI-style `tool_calls`.
- gates_or_invariants: Tool call JSON must include nested `arguments`; model-generated ids are not expected; tool results are grouped in user turns; tool definitions live in the system prompt; result and thinking tags must be parsed carefully.
- dependencies_and_callers: Informs `packages/ai/src/dialect/hermes.ts`, compat builders, and tool parser tests.
- edge_cases_or_failure_modes: Malformed JSON inside tool tags, mixed text/tool calls, missing call ids, coalesced tool results, and `<think>` text leaking into final answer.
- validation_or_tests: Covered indirectly by Hermes dialect/tool-conversion tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-209 `file` `python/robomp/entrypoint.sh`
- cursor: `[_]`
- core_role: Container entrypoint for the robomp runtime environment.
- algorithmic_behavior: Creates group/user slots, distinguishes proxy role from agent role, validates `/work/pi/packages/coding-agent`, sets permissions for caches/workspaces, resets staged agent home, and secures SQLite files.
- inputs_outputs_state: Inputs are environment variables, container role, mounted repo path, configured slot counts, and filesystem state. Outputs are prepared users/groups, permissions, HOME/cache directories, and delegated command execution.
- gates_or_invariants: Proxy role bypasses PI_ROOT checks; agent role requires the coding-agent package path; SQLite and cache permissions are tightened; slot users are deterministic.
- dependencies_and_callers: Called as Docker/container entrypoint for `python/robomp`; coordinates with sandbox slot logic and host tools.
- edge_cases_or_failure_modes: Missing checkout, wrong mount path, bad permissions, stale staged home, absent users/groups, and proxy-vs-worker mode confusion.
- validation_or_tests: Covered by robomp integration/sandbox tests; no shell execution was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-239 `directory` `packages/catalog/src/compat`
- cursor: `[_]`
- core_role: Provider compatibility builders for OpenAI-compatible and Anthropic-compatible APIs.
- algorithmic_behavior: `openai.ts` detects/overrides chat completions, Responses, OpenRouter, Z.AI, Qwen/Kimi/DeepSeek/GLM behavior; `anthropic.ts` configures Anthropic wire affordances; `apply.ts` applies sparse override keys only where supported.
- inputs_outputs_state: Inputs are provider/model ids, base URLs, thinking flags, detected metadata, and endpoint family. Outputs are compat flags controlling roles, max-token field, reasoning format, tool replay, strict mode, wire model id, idle timeout, and conditional thinking behavior.
- gates_or_invariants: Official API host/path boundaries are exact; `whenThinking` only applies under active thinking; OpenCode reasoning content is re-enabled only with thinking; `apply.ts` assigns defined keys that already exist in compat.
- dependencies_and_callers: Used by catalog provider descriptors/resolvers and AI request shaping.
- edge_cases_or_failure_modes: NVIDIA Qwen chat-template format, DeepSeek/Kimi thinking markup healing, GLM/Kimi idle timeouts, OpenRouter reasoning variants, developer-role support, and tool-result id requirements.
- validation_or_tests: `packages/catalog/test/issue-2299-repro.test.ts` and provider request-shaping tests cover key regressions.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-269 `directory` `packages/coding-agent/src/lsp`
- cursor: `[_]`
- core_role: Language Server Protocol subsystem for diagnostics, formatting, code actions, symbols, hover, references, rename, and write-through checks.
- algorithmic_behavior: `client.ts` frames JSON-RPC over streams, manages init/request maps/notifications, shutdown, aborts, idle timeouts, and diagnostics; `config.ts` resolves server configs/defaults/plugins/binaries; `edits.ts` applies workspace edits in safe order; `index.ts` exposes the LSP tool and write-through formatting/diagnostics flow.
- inputs_outputs_state: Inputs are file paths, LSP configs, server stdout/stderr, user tool actions, workspace edits, diagnostics, and formatting options. Outputs are rendered diagnostics/actions/symbols/edits, file writes, and deferred diagnostic events. State includes client maps, document versions, diagnostic ledger, negative init cache, locks, and idle timers.
- gates_or_invariants: Recent init failures are cached for 3 minutes; glob diagnostics target max is 20; rename pairs max is 1000; readonly action set is enforced; text edits are reverse-sorted and overlap-validated; resource ops flush text edits before touching subtrees.
- dependencies_and_callers: Used by coding-agent tool routing and write-through flows; depends on LSP servers, Biome/SwiftLint/custom linters, `lspmux`, editorconfig, and TUI renderers.
- edge_cases_or_failure_modes: Framed chunk boundaries, duplicate init/file operations, aborted requests sending `$/cancelRequest`, stale diagnostics, rust-analyzer readiness polling, server crashes, invalid workspace edits, and plugin config containment.
- validation_or_tests: LSP-related tests and tool approval tests validate action exposure; no tests run here.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-299 `directory` `packages/coding-agent/test/eval`
- cursor: `[_]`
- core_role: Regression tests for the JavaScript eval backend and its bridges.
- algorithmic_behavior: Tests console table rendering, display image coercion, agent bridge subprocess options, process stdio capture, and runtime global dispose/reactivation rules.
- inputs_outputs_state: Inputs are eval code snippets, image data in multiple forms, mocked sessions, stdout/stderr writes, and runtime global keys. Outputs are captured eval results, display image blocks, bridge call options, and lifecycle errors.
- gates_or_invariants: Strict base64 and supported binary-like image forms are accepted; invalid image data returns diagnostics; parent agent/session/MCP options propagate to subagents; overlapping global ownership is rejected; stdio bytes are captured exactly.
- dependencies_and_callers: Covers `packages/coding-agent/src/eval/js` and eval bridge modules.
- edge_cases_or_failure_modes: JSON Buffer shapes, decimal CSV image data, invalid base64, same-realm global pollution, and Buffer UTF-8 decoding.
- validation_or_tests: Directory is test-only and directly validates eval contracts; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-329 `directory` `packages/swarm-extension/src/swarm`
- cursor: `[_]`
- core_role: Swarm workflow parser, DAG planner, persistent state store, executor, and renderer.
- algorithmic_behavior: `schema.ts` validates YAML swarm definitions; `dag.ts` derives dependencies from `waits_for`, `reports_to`, and mode order; `state.ts` persists `.swarm_<name>/state/pipeline.json`; `pipeline.ts` runs waves/iterations; `executor.ts` invokes coding-agent subprocesses for each agent.
- inputs_outputs_state: Inputs are swarm YAML, workspace, agent specs, dependency fields, model overrides, and abort/progress callbacks. Outputs are execution waves, agent results, logs/context artifacts, persisted statuses, and rendered progress text.
- gates_or_invariants: Pipeline `target_count` only applies to pipeline mode; refs cannot self/unknown reference; Kahn cycle detection is hard failure; sequential/pipeline implicit deps follow declaration order when explicit deps are absent.
- dependencies_and_callers: Depends on coding-agent subprocess runner, filesystem state/log dirs, and extension command entrypoints.
- edge_cases_or_failure_modes: Cycles, missing workspace/name/agents, abort during wave, fatal agent errors, resumable state load, and model registry/auth propagation.
- validation_or_tests: `src/swarm/__tests__/executor.test.ts` verifies `executeSwarmAgent` auth/model-registry behavior.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-359 `file` `crates/pi-natives/src/grep.rs`
- cursor: `[_]`
- core_role: Rust N-API search/grep engine backed by ripgrep crates.
- algorithmic_behavior: Exports `search`, `has_match`, and async `grep`; compiles regex with brace/parenthesis literal recovery; scans in-memory content or filesystem trees; supports content/count/files-with-matches modes, context lines, offsets, global/per-file limits, glob/type filters, hidden/gitignore flags, callbacks, cancellation, and timeout.
- inputs_outputs_state: Inputs are `SearchOptions` or `GrepOptions`, content bytes/string, path, glob/type filters, abort signal, and timeout. Outputs are matches, counts, files searched, skipped oversized count, truncation flags, and optional callback events.
- gates_or_invariants: Files over 4MiB are skipped and counted; small files read into memory, larger files mmap when possible; binary detection quits on NUL; callbacks fire after aggregation so offset/limit semantics match returned results; concurrent greps fall back to sequential if worker saturation is active.
- dependencies_and_callers: Depends on `grep_regex`, `grep_searcher`, `ignore`, `globset`, `memmap2`, crate task cancellation, and JS callers in native package/tool search paths.
- edge_cases_or_failure_modes: Invalid regexes, literal template braces like `${platform}`, unclosed groups in literal snippets, unreadable files, non-files, node_modules skipping unless glob mentions it, cancellation heartbeat every 128 visits, and UTF-8 lossy line conversion.
- validation_or_tests: Inline Rust tests plus JS native tests cover package exposure; no tests run here.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-389 `file` `packages/agent/src/compaction.ts`
- cursor: `[_]`
- core_role: Barrel export for the agent compaction module.
- algorithmic_behavior: Re-exports `./compaction/index` so package consumers import compaction APIs through a stable path.
- inputs_outputs_state: Input is module resolution; output is the exported compaction API surface.
- gates_or_invariants: No runtime logic beyond preserving the package export contract.
- dependencies_and_callers: Used by consumers importing `@oh-my-pi/pi-agent/compaction`.
- edge_cases_or_failure_modes: Misclassification candidate because the file is only 36 bytes and contains no algorithm itself; export drift would still break callers.
- validation_or_tests: Covered by package type/import checks.
- skip_candidate: `yes: barrel-only file; core behavior lives under packages/agent/src/compaction/`

### OH_MY_HUMANIZE_MAIN-HZ-419 `file` `packages/ai/src/api-registry.ts`
- cursor: `[_]`
- core_role: Runtime registry for extension/custom AI API providers.
- algorithmic_behavior: Rejects names colliding with built-in `KnownApi`, registers custom stream functions and optional wrappers/source IDs, unregisters by source, and clears all custom APIs.
- inputs_outputs_state: Inputs are API name, registration source, `streamSimple`, optional stream wrapper, and source ID. Outputs are registry entries discoverable by AI dispatch.
- gates_or_invariants: Built-in API names cannot be overridden; unregister removes only entries for the matching source; clear removes all custom registrations.
- dependencies_and_callers: Used by extension capability/runtime code and AI provider dispatch.
- edge_cases_or_failure_modes: Source cleanup after extension unload, duplicate provider names, stale custom APIs, and accidental built-in collisions.
- validation_or_tests: `packages/ai/test/api-registry.test.ts` covers collision, unregister, and clear behavior.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-449 `file` `packages/ai/test/anthropic-thinking-immutability.test.ts`
- cursor: `[_]`
- core_role: Regression test for Anthropic signed-thinking replay immutability.
- algorithmic_behavior: Verifies signed thinking blocks preserve raw signatures/content while ordinary text is normalized to well-formed strings and redacted thinking is dropped when appropriate.
- inputs_outputs_state: Inputs are assistant messages with thinking/text blocks, including malformed surrogate content. Outputs are Anthropic request payload blocks.
- gates_or_invariants: Signed thinking must not be string-normalized or mutated; non-thinking content must become well-formed; redacted thinking is excluded from replay in this scenario.
- dependencies_and_callers: Covers Anthropic provider transform/replay code in `packages/ai`.
- edge_cases_or_failure_modes: Invalid Unicode surrogate normalization corrupting signatures, accidental redacted block replay, and cross-block mutation.
- validation_or_tests: The file itself is the test; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-479 `file` `packages/ai/test/auth-storage-credential-origin.test.ts`
- cursor: `[_]`
- core_role: Regression test for credential-origin precedence in auth storage.
- algorithmic_behavior: Asserts how credentials are classified by origin across undefined, environment, OAuth, API key, config, and runtime sources.
- inputs_outputs_state: Inputs are mocked/stored credentials and provider auth sources. Outputs are credential-origin labels used for display/routing decisions.
- gates_or_invariants: Origin precedence is deterministic and does not mislabel runtime/config/env credentials.
- dependencies_and_callers: Covers AI auth storage and provider login flows.
- edge_cases_or_failure_modes: Missing origin metadata, environment fallback, OAuth/API-key overlap, and runtime overrides.
- validation_or_tests: The file itself validates the contract; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-509 `file` `packages/ai/test/github-copilot-login.test.ts`
- cursor: `[_]`
- core_role: Regression tests for GitHub Copilot device-login and model-enable flow.
- algorithmic_behavior: Covers device-code polling, business endpoint detection, structured API-key serialization, enterprise domain validation, abort handling, `slow_down`, timeout, access denied, and silent model enablement failure.
- inputs_outputs_state: Inputs are mocked GitHub/Copilot HTTP responses, device codes, enterprise domains, and abort signals. Outputs are serialized credentials or typed login failures.
- gates_or_invariants: Invalid domains are rejected; empty/denied/timeout flows fail cleanly; `slow_down` changes polling behavior; model enable failure does not poison login when credential acquisition succeeded.
- dependencies_and_callers: Covers GitHub Copilot registry login provider and OAuth/device flow helpers.
- edge_cases_or_failure_modes: Business account endpoint variance, enterprise URL parsing, cancellation races, and polling terminal states.
- validation_or_tests: The file itself validates login behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-539 `file` `packages/ai/test/issue-2080-repro.test.ts`
- cursor: `[_]`
- core_role: Regression test for MiniMax/tool-call streaming argument merge behavior.
- algorithmic_behavior: Verifies object-shaped tool arguments merge across chunks, cumulative chunks preserve earlier keys, deep merge arrays/objects as intended, prototype-pollution keys are ignored, and `toolcall_delta` remains concat-safe.
- inputs_outputs_state: Inputs are streamed tool-call delta fragments. Outputs are final tool-call argument objects/strings.
- gates_or_invariants: Dangerous keys are ignored; later chunks must not erase earlier valid keys unless semantically cumulative; arrays/objects merge predictably.
- dependencies_and_callers: Covers AI streaming delta accumulation and provider-specific MiniMax compatibility.
- edge_cases_or_failure_modes: Prototype pollution, duplicated cumulative payloads, partial nested JSON, and mismatched object/string deltas.
- validation_or_tests: The file itself is the issue repro; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-569 `file` `packages/ai/test/oauth-deepseek.test.ts`
- cursor: `[_]`
- core_role: Regression tests for DeepSeek API-key login validation.
- algorithmic_behavior: Verifies model endpoint validation, Bearer-prefix stripping, empty-key rejection, and credential return shape.
- inputs_outputs_state: Inputs are prompt values and mocked validation HTTP responses. Outputs are stored DeepSeek API key strings or errors.
- gates_or_invariants: Empty input fails; pasted `Bearer ...` is normalized; model-list validation gates successful login.
- dependencies_and_callers: Covers DeepSeek provider registry login and API-key validation helpers.
- edge_cases_or_failure_modes: User pastes header-style key, validation network failures, and blank prompt responses.
- validation_or_tests: The file itself validates the login flow; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-599 `file` `packages/ai/test/openai-responses-stateful.test.ts`
- cursor: `[_]`
- core_role: Regression tests for OpenAI Responses API stateful chaining.
- algorithmic_behavior: Tests `previous_response_id` chaining, delta input, `store:true`, juice scaffolding trailing item, mutation full replay, stale previous-id retry/full replay, repeated stale failure disabling, ZDR disabling, and default official OpenAI chaining.
- inputs_outputs_state: Inputs are message histories, provider state, response IDs, and mocked OpenAI responses/errors. Outputs are request bodies and provider state transitions.
- gates_or_invariants: Stateful chaining only when safe; stale IDs trigger replay; repeated stale failures disable stateful mode; ZDR configurations disable store/chaining.
- dependencies_and_callers: Covers OpenAI provider stream/request code and Responses history utilities.
- edge_cases_or_failure_modes: Mutated history after previous response, stale server state, zero-data-retention policy, and replay scaffolding.
- validation_or_tests: The file itself validates the contract; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-629 `file` `packages/ai/test/service-tier-premium-requests.test.ts`
- cursor: `[_]`
- core_role: Regression tests for service-tier and premium request accounting.
- algorithmic_behavior: Verifies premium accounting and wire `service_tier` behavior for OpenAI, OpenAI Codex, direct Anthropic, scoped tiers, and priority/flex-like tier handling.
- inputs_outputs_state: Inputs are model/provider settings and service-tier selections. Outputs are request body fields and usage/premium request metrics.
- gates_or_invariants: Scoped tiers affect only matching provider families; premium request counts are derived consistently; unsupported providers do not receive inappropriate tier fields.
- dependencies_and_callers: Covers AI provider request shaping and stats accounting.
- edge_cases_or_failure_modes: Provider-scoped tier leakage, Codex vs OpenAI differences, Anthropic priority behavior, and missing usage fields.
- validation_or_tests: The file itself validates the contract; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-659 `file` `packages/catalog/src/fireworks-model-id.ts`
- cursor: `[_]`
- core_role: Model-id translator for Fireworks public IDs and Firepass router wire IDs.
- algorithmic_behavior: Converts model ids between catalog/public forms and Fireworks-compatible wire/router forms, including version separators where digits use `p` versus `.` forms.
- inputs_outputs_state: Inputs are model id strings and provider/router context. Outputs are normalized public or wire model IDs.
- gates_or_invariants: Only Fireworks/Firepass-shaped ids are rewritten; unrelated ids are preserved.
- dependencies_and_callers: Used by catalog provider resolution and AI request wire id selection.
- edge_cases_or_failure_modes: Numeric version separators, already-normalized ids, router prefixes, and non-Fireworks model ids.
- validation_or_tests: Covered indirectly by catalog resolver tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-689 `file` `packages/catalog/test/issue-2299-repro.test.ts`
- cursor: `[_]`
- core_role: Regression tests for NVIDIA NIM Qwen thinking wire format.
- algorithmic_behavior: Asserts NVIDIA-hosted Qwen models resolve to `qwen-chat-template`, non-NVIDIA Qwen stays on top-level `qwen`, and wire requests emit `chat_template_kwargs.enable_thinking` rather than top-level `enable_thinking`.
- inputs_outputs_state: Inputs are provider/model metadata and thinking settings. Outputs are resolved reasoning format and request body.
- gates_or_invariants: NVIDIA Qwen uses chat-template kwargs; non-NVIDIA behavior remains unchanged; top-level `enable_thinking` is forbidden for NVIDIA NIM.
- dependencies_and_callers: Covers `packages/catalog/src/compat/openai.ts` and request serialization.
- edge_cases_or_failure_modes: Provider-specific Qwen id collisions and upstream NIM rejecting top-level thinking flags.
- validation_or_tests: The file itself validates the repro; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-719 `file` `packages/coding-agent/scripts/bundle-dist.ts`
- cursor: `[_]`
- core_role: Bun bundling script for publishing the coding-agent CLI.
- algorithmic_behavior: Cleans owned bundle outputs, temporarily generates stats client bundle, runs `bun build` with external dependency lists and `PI_BUNDLED=true`, resets stats bundle, ensures Bun shebang, and reports output size/time.
- inputs_outputs_state: Inputs are source CLI entry, dist directory, external dependency lists, stats bundle generator, and package version. Outputs are `dist/cli.js`, copied native assets/source maps cleanup, and stdout summary.
- gates_or_invariants: Only removes `cli.js`, `.node`, and `.js.map` outputs from shared `dist`; patched deps must stay bundled; stats dashboard archive is reset in `finally`; shebang is enforced.
- dependencies_and_callers: Uses Bun build/spawn, `packages/stats/scripts/generate-client-bundle.ts`, and release/package workflows.
- edge_cases_or_failure_modes: Build failure still resets stats bundle; missing dist is tolerated in cleanup; externalizing patched deps would load unpatched npm package.
- validation_or_tests: Covered by package build/release smoke workflows; no script execution was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-749 `file` `packages/coding-agent/test/agent-hub-activate.test.ts`
- cursor: `[_]`
- core_role: Regression tests for agent hub focus, roster, and activation behavior.
- algorithmic_behavior: Tests Enter focusing selected agents, focus failure notices, persisted subagent sessions after restart, workflow-agent labels, parked history-only labels, editor focus restoration, and double-left-arrow gating.
- inputs_outputs_state: Inputs are mocked agent registry/session state and keyboard events. Outputs are hub open/close state, focused agent ID, notices, and rendered labels.
- gates_or_invariants: `requireContent` does not open for Main-only roster; explicit hub key can open empty roster; focus failure keeps hub open; Enter returns focus to editor after successful activation.
- dependencies_and_callers: Covers agent hub controller/components in coding-agent TUI.
- edge_cases_or_failure_modes: Restart persistence, parked agents without revivers, empty roster, and focus errors.
- validation_or_tests: The file itself validates the UI/controller contract; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-779 `file` `packages/coding-agent/test/agent-session-retry-cap.test.ts`
- cursor: `[_]`
- core_role: Regression tests for `AgentSession` retry, backoff, and failover policy.
- algorithmic_behavior: Tests retry-after cap bailouts, credential switching for account rate limits, sibling unblock waiting, normal under-cap retries, no retry after complete write tool call timeout, transient socket close retries, HTTP/2 reset, upstream gateway errors, empty aborted turns, disposing sessions, default 502 retry count, and Cloudflare 520 retries.
- inputs_outputs_state: Inputs are simulated provider errors/stream events/session state. Outputs are retry decisions, credential switches, delays, and final session events.
- gates_or_invariants: Delays above `retry.maxDelayMs` fail except account/sibling unblock cases; partial content prevents empty-abort retry; write tool completion suppresses timeout retry; disposing session suppresses retry.
- dependencies_and_callers: Covers core agent session streaming/retry logic.
- edge_cases_or_failure_modes: Rate-limit headers, credential pools, sibling sessions, gateway HTML pages, Bun HTTP/2 resets, partial thinking/text, and abort races.
- validation_or_tests: The file itself validates retry contracts; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-809 `file` `packages/coding-agent/test/available-commands.test.ts`
- cursor: `[_]`
- core_role: Regression tests for advertised slash command metadata.
- algorithmic_behavior: Verifies RPC-safe command metadata, stable sources, file-command loading before advertisement, MCP prompt classification, bundled custom command classification, and legacy fixtures without paths.
- inputs_outputs_state: Inputs are command registries, file commands, MCP prompts, and custom fixtures. Outputs are available command metadata entries.
- gates_or_invariants: Sources must be stable and serializable; file commands must be loaded into session before exposure; MCP prompts are path-classified; legacy no-path commands stay custom.
- dependencies_and_callers: Covers slash-command discovery and RPC metadata used by UI/clients.
- edge_cases_or_failure_modes: Missing command paths, late-loaded file commands, source misclassification, and unsafe metadata objects.
- validation_or_tests: The file itself validates command metadata; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-839 `file` `packages/coding-agent/test/context-consolidation.test.ts`
- cursor: `[_]`
- core_role: Regression tests for context-usage consolidation across branch/session state.
- algorithmic_behavior: Tests branch-local anchors, rollback anchor recovery, speculative mode after compaction, custom messages/summaries in active tails, agreement between `/context`, status line, model selector, and idle compaction, reasoning-signature cache invalidation, and in-flight pending snapshot preference.
- inputs_outputs_state: Inputs are session trees, compaction anchors, provider snapshots, pending requests, and token usage. Outputs are consolidated context usage values and nullable/speculative contracts.
- gates_or_invariants: Sibling branches cannot reuse unsafe anchors; completed in-turn provider anchors win over pending snapshots; first-turn pending state is preserved; outputs must keep numeric nullable-vs-speculative shape.
- dependencies_and_callers: Covers session manager, context status, model selector, compaction scheduler, and status line cache.
- edge_cases_or_failure_modes: Rollbacks, branch switches, custom entries, reasoning signature growth, active request snapshots, and missing assistant snapshots.
- validation_or_tests: The file itself validates context consolidation; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-869 `file` `packages/coding-agent/test/hindsight-backend.test.ts`
- cursor: `[_]`
- core_role: Regression tests for Hindsight memory backend lifecycle and routing.
- algorithmic_behavior: Tests backend no-op when unconfigured, per-session state registration, session-id rekeying, retain every Nth user turn, subagent aliasing, pre-compaction recall blocks, first-turn injection, mental-model ordering/reload, clear behavior, live bank routing, listener coalescing, bank creation, and retain-queue flush/drop on teardown.
- inputs_outputs_state: Inputs are mocked sessions, settings, memory backend responses, agent events, and bank routing changes. Outputs are retained memories, injected developer instructions, registered state, and server calls.
- gates_or_invariants: Server-side mental models are not deleted by `/memory clear`; subagent aliases cannot reload mental models; routing rebuilds only on actual bank/scoping changes; retain queue flushes before pointer clears or drops safely after clear.
- dependencies_and_callers: Covers coding-agent memory backend integration with mnemopi/Hindsight service.
- edge_cases_or_failure_modes: Resume/switch session ids, task-depth subagents, empty recall, bank reset to blank, synchronous routing hooks, and teardown races.
- validation_or_tests: The file itself validates memory behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-899 `file` `packages/coding-agent/test/interactive-mode-loop.test.ts`
- cursor: `[_]`
- core_role: Regression tests for interactive loop auto-submit gating.
- algorithmic_behavior: Verifies the next loop prompt does not resolve while compaction is running, does not recompact when a compact loop turn starts another prompt before resubmitting, and waits for post-prompt background work.
- inputs_outputs_state: Inputs are mocked interactive mode, compaction state, prompt submissions, and background promises. Outputs are prompt resolution timing and compaction invocation count.
- gates_or_invariants: Auto-submit is blocked during compaction and post-prompt work; compact loop turns avoid duplicate compaction.
- dependencies_and_callers: Covers `InteractiveMode` loop and compaction/background-task coordination.
- edge_cases_or_failure_modes: Prompt races, compaction reentrancy, and background work finishing after user input.
- validation_or_tests: The file itself validates loop behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-929 `file` `packages/coding-agent/test/issue-927-repro.test.ts`
- cursor: `[_]`
- core_role: Regression test for optimistic pending spinner cleanup.
- algorithmic_behavior: Ensures the optimistic loading animation clears when prompt handling returns without a model turn.
- inputs_outputs_state: Inputs are prompt execution path with no model response. Outputs are TUI loading/pending state.
- gates_or_invariants: Pending spinner state must be cleared even when no assistant/model turn is produced.
- dependencies_and_callers: Covers interactive mode/TUI pending-state handling.
- edge_cases_or_failure_modes: Early prompt return, no-op prompt, and stale optimistic loader.
- validation_or_tests: The file itself is the issue repro; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-959 `file` `packages/coding-agent/test/mcp-discovered-server-reauth.test.ts`
- cursor: `[_]`
- core_role: Regression tests for MCP discovered-server reauth and config persistence.
- algorithmic_behavior: Verifies manager exposes config/source for discovered but unconnected servers, unknown server lookup returns undefined, colon-namespaced server names validate, and HTTP server OAuth auth blocks round-trip through config writer.
- inputs_outputs_state: Inputs are discovered MCP server configs, namespace names, and update payloads. Outputs are persisted MCP settings and discovery metadata.
- gates_or_invariants: Namespaced plugin server names are allowed; unknown lookup is non-error; OAuth auth blocks persist intact.
- dependencies_and_callers: Covers MCP manager and config writer used by reauth UI/workflows.
- edge_cases_or_failure_modes: Server process exits before connection, plugin names with colon, missing discovered config, and OAuth metadata loss.
- validation_or_tests: The file itself validates MCP reauth contracts; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-989 `file` `packages/coding-agent/test/model-selector-role-badge-thinking.test.ts`
- cursor: `[_]`
- core_role: Regression tests for model selector role badges, thinking display, and refresh behavior.
- algorithmic_behavior: Tests custom role badges from cycle order/model roles, compact auto badges, context-size disabling, blocked menu when all candidates disabled, cached models during offline refresh, highlight preservation after reorder, provider-id refresh for Ollama Cloud, and background spinner on tab switch.
- inputs_outputs_state: Inputs are model catalogs, role metadata, context usage, provider tabs, and refresh promises. Outputs are rendered selector items, disabled states, highlighted model, and refresh calls.
- gates_or_invariants: Built-in metadata can be overridden; unavailable context models are disabled; provider refresh uses provider id rather than label; tab switch is immediate while refresh runs.
- dependencies_and_callers: Covers model selector TUI/controller and catalog role metadata.
- edge_cases_or_failure_modes: Offline refresh, reordered models, all-disabled lists, custom role defaults, and Ollama Cloud label/id mismatch.
- validation_or_tests: The file itself validates model selector behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1019 `file` `packages/coding-agent/test/role-info.test.ts`
- cursor: `[_]`
- core_role: Regression tests for role metadata lookup.
- algorithmic_behavior: Verifies built-in role info, custom roles from `modelTags`, fallback for unknown roles, and configured metadata overriding built-ins while preserving defaults.
- inputs_outputs_state: Inputs are role names and settings metadata. Outputs are display labels/descriptions/badges.
- gates_or_invariants: Unknown roles have safe fallback; overrides merge rather than erase default fields.
- dependencies_and_callers: Covers role info used by model selector and status displays.
- edge_cases_or_failure_modes: Partial custom metadata, unknown role ids, and built-in override merging.
- validation_or_tests: The file itself validates role lookup; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1049 `file` `packages/coding-agent/test/session-manager-close-race.test.ts`
- cursor: `[_]`
- core_role: Regression test for `SessionManager.close()` and append race.
- algorithmic_behavior: Ensures `appendMessage` during an in-flight `close()` does not stash a persistent error.
- inputs_outputs_state: Inputs are session manager close promise and concurrent append. Outputs are persisted/queued session state and error fields.
- gates_or_invariants: Close/append races must not record false `persistError`.
- dependencies_and_callers: Covers session manager persistence lifecycle.
- edge_cases_or_failure_modes: In-flight close, late append, and persistence error contamination.
- validation_or_tests: The file itself validates the race; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1079 `file` `packages/coding-agent/test/status-line-settings-cache.test.ts`
- cursor: `[_]`
- core_role: Regression tests for status line effective-settings cache.
- algorithmic_behavior: Tests cached renders are byte-identical across presets/widths, separator defaults, invalidation on `updateSettings`, segment option merge precedence, custom segment arrays, dynamic plan/hook state without invalidation, no mutation during narrow renders, object reuse, and git probe skipping.
- inputs_outputs_state: Inputs are status settings, render widths, hook/plan state, and git integration flags. Outputs are rendered status strings and cache object identity.
- gates_or_invariants: Preset option siblings survive user overrides; custom segments only apply to custom preset; disabled/nonvisible git-backed segments skip probes; narrow render cannot mutate shared presets.
- dependencies_and_callers: Covers `StatusLineComponent` and footer/status setting integration.
- edge_cases_or_failure_modes: Cache stale after settings update, dynamic state over-invalidation, git probe waste, and shared object mutation.
- validation_or_tests: The file itself validates cache behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1109 `file` `packages/coding-agent/test/tool-choice-queue.test.ts`
- cursor: `[_]`
- core_role: Regression tests for queued forced tool-choice directives.
- algorithmic_behavior: Tests resolve/reject callbacks, requeue/drop rejection policy, repeated requeue retaining callbacks, removal by label, clear rejection, last-served label consumption, in-flight state, and `onInvoked` handling.
- inputs_outputs_state: Inputs are queued directives, labels, rejection policies, and invoked tool names. Outputs are served choices, callback calls, requeued directives, and queue state.
- gates_or_invariants: Requeued directives preserve `onRejected`/`onInvoked`; label removal rejects in-flight matches; `consumeLastServedLabel` returns once; `onInvoked` resolves only after requested tool runs.
- dependencies_and_callers: Covers tool-choice queue used by agent session/tool forcing.
- edge_cases_or_failure_modes: Aborted in-flight directive, replay after rejection, label-targeted cancellation, and invocation race.
- validation_or_tests: The file itself validates queue behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1139 `file` `packages/collab-web/test/link.test.ts`
- cursor: `[_]`
- core_role: Regression tests for collab room link parsing and envelope helpers.
- algorithmic_behavior: Tests bare and legacy room links, scheme inference, localhost plaintext allowance, nonlocal `ws://` rejection, key byte length validation, write-token split, deep links, `%23` mangling, round trips, envelope rewrite, and truncated envelope rejection.
- inputs_outputs_state: Inputs are link strings, host URLs, base64url keys, and envelope buffers. Outputs are parsed room config, formatted links, and decoded/reencoded envelopes.
- gates_or_invariants: Only 32/48 decoded key bytes are valid; custom scheme-less hosts infer `wss`; nonlocal plaintext is rejected; truncated envelopes return null.
- dependencies_and_callers: Covers `packages/collab-web/src/lib/link.ts`.
- edge_cases_or_failure_modes: macOS Foundation fragment re-encoding, dot-joined deep links, legacy `#key` links, and write-token fragments.
- validation_or_tests: The file itself validates link behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1169 `file` `packages/hashline/test/recovery-session-chain.test.ts`
- cursor: `[_]`
- core_role: Regression tests for hashline recovery replay anchor gates.
- algorithmic_behavior: Tests recovery refuses replay when an edit anchor line diverges between snapshot and current file, and replays edits when every anchor line is unchanged.
- inputs_outputs_state: Inputs are edit sessions, snapshots, current file content, and anchor metadata. Outputs are replay success/failure and patched content.
- gates_or_invariants: Anchor content equality is required before replaying session-chain edits.
- dependencies_and_callers: Covers hashline recovery logic used by edit/recovery workflows.
- edge_cases_or_failure_modes: Drifted anchor line, unchanged anchor with surrounding file changes, and unsafe replay prevention.
- validation_or_tests: The file itself validates recovery behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1199 `file` `packages/mnemopi/test/diagnose.test.ts`
- cursor: `[_]`
- core_role: Regression tests for mnemopi database diagnosis helpers.
- algorithmic_behavior: Tests temporary DB schema initialization/inspection, missing schema report on uninitialized in-memory DB, and detection of incomplete required columns without reading memory content.
- inputs_outputs_state: Inputs are SQLite DBs with present/missing/partial schema. Outputs are diagnosis result objects.
- gates_or_invariants: Diagnosis reports schema health without inspecting sensitive memory content; required columns are checked explicitly.
- dependencies_and_callers: Covers mnemopi DB diagnostic utilities.
- edge_cases_or_failure_modes: In-memory DB without init, partial migrations, and privacy-preserving diagnostics.
- validation_or_tests: The file itself validates diagnosis behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1229 `file` `packages/mnemopi/test/query-cache-synonyms.test.ts`
- cursor: `[_]`
- core_role: Regression tests for synonym expansion and query cache tiers.
- algorithmic_behavior: Tests canonical synonym groups, stop-word removal, exact normalized hits/misses, word-order normalization, high-confidence embedding/composite hits, expanded tier overlap, TTL expiry, LRU eviction, SQLite persistence, stats hit rate, and env gate for enhanced recall/query cache.
- inputs_outputs_state: Inputs are query strings, embeddings, cache entries, TTLs, and env flags. Outputs are normalized keys, cache hits/misses, persisted rows, and stats.
- gates_or_invariants: Enhanced recall/cache disabled unless Python env gate is set; exact cache keys normalize case/order; all tiers invalidate on expiry.
- dependencies_and_callers: Covers mnemopi query cache and synonym utilities used by memory recall.
- edge_cases_or_failure_modes: Synonym unknowns, stale entries, max-size eviction, persisted reload, and tier fallback false positives.
- validation_or_tests: The file itself validates query-cache behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1259 `file` `packages/natives/test/npm-packages.test.ts`
- cursor: `[_]`
- core_role: Regression tests for generated native npm leaf packages.
- algorithmic_behavior: Tests x64 leaf manifests expose addon files without package exports, non-x64 leaves use default addon as main entry, generation copies present addon files, and dry runs report missing leaves without writing packages.
- inputs_outputs_state: Inputs are package metadata and addon file layout. Outputs are generated package manifests/files or dry-run diagnostics.
- gates_or_invariants: Leaf packages expose expected native addon entrypoints; dry-run is non-mutating; missing leaves are reported.
- dependencies_and_callers: Covers native package generator and binding contract.
- edge_cases_or_failure_modes: Platform leaf omissions, x64 package export shape, non-x64 main fallback, and dry-run write avoidance.
- validation_or_tests: The file itself validates package generation behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1289 `file` `packages/snapcompact/research/exp17_bestopus.py`
- cursor: `[_]`
- core_role: Research experiment runner for snapcompact image/text compaction evaluation with Claude Opus.
- algorithmic_behavior: Builds document layouts from SQuAD paragraphs, packs pages into units, renders bitmap document conditions with BDF fonts, calls LLM completion with caching, scores QA outputs, aggregates accuracy/cost metrics, and writes CSV/JSON results.
- inputs_outputs_state: Inputs are SQuAD contexts/questions, font configs, length/condition CLI args, provider API keys, caches, and prompt templates. Outputs are rendered PNG units, per-run records, aggregate metrics, and cached model responses.
- gates_or_invariants: Conditions parse to known font configs; cache keys use model/tag/payload; baseline/text ceilings are fixed comparison constants; thread pool runs bounded task batches.
- dependencies_and_callers: Depends on research modules `squad`, `bdf`, `providers`, and `run` caches under `packages/snapcompact/research`.
- edge_cases_or_failure_modes: Missing API keys/fonts, invalid condition strings, rendering capacity overflow, model call failures, and stale cache vs `fresh` mode.
- validation_or_tests: Research script, not a unit test; no execution performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1319 `file` `packages/snapcompact/research/snapcompact_qwen_spotlight_viz.py`
- cursor: `[_]`
- core_role: Visualization generator for Qwen snapcompact spotlight/heatmap experiments.
- algorithmic_behavior: Loads experiment JSON/image data, computes normalized/smoothed attention or score maps, builds answer/distractor bounding boxes, overlays heat colors, crops/pastes panels, computes random-region scores, and draws score cards/generation rows.
- inputs_outputs_state: Inputs are JSON data, image paths, theme/seed CLI args, and generation text. Outputs are a composite visualization image.
- gates_or_invariants: Positive arrays normalize to [0,1]; random score sampling is seeded; crop boxes are padded/clamped; paste-fit preserves aspect ratio.
- dependencies_and_callers: Depends on NumPy and PIL; used only in snapcompact research artifacts.
- edge_cases_or_failure_modes: Missing fonts/images, zero heat maps, invalid indices, out-of-bounds regions, and empty generations.
- validation_or_tests: Research visualization script; no execution performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1349 `file` `packages/stats/src/parser.ts`
- cursor: `[_]`
- core_role: Session JSONL parser for the local stats dashboard.
- algorithmic_behavior: Extracts assistant usage stats, user-message metrics, parent user-message links, folder names, and service-tier state from session JSONL. Uses `Bun.JSONL.parseChunk` leniently, skipping malformed lines and tracking byte offsets.
- inputs_outputs_state: Inputs are session file paths and previous byte offsets. Outputs are `MessageStats[]`, `UserMessageStats[]`, `UserMessageLink[]`, and new offset.
- gates_or_invariants: Legacy message entries without `id` are skipped; synthetic/empty user messages are skipped; service-tier changes before incremental offset are scanned to preserve premium request derivation; missing files return empty result with unchanged offset.
- dependencies_and_callers: Depends on session storage directory utilities, AI usage types, `computeUserMessageMetrics`, and stats sync/import code.
- edge_cases_or_failure_modes: Truncated/malformed JSONL, incremental sync after service-tier change, missing files, legacy entries, and parent links whose user row was inserted in an earlier pass.
- validation_or_tests: Covered by stats parser/sync tests; no tests run here.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1379 `file` `packages/tui/src/latex-to-unicode.ts`
- cursor: `[_]`
- core_role: LaTeX math-to-Unicode/ANSI renderer for terminal text.
- algorithmic_behavior: Maps LaTeX commands to Unicode symbols, scripts, fonts, accents, fractions, radicals, matrices/environments, colors, and inline/block math spans. `LatexParser` parses grouped arguments/commands; `renderMathInText` detects `$...$`, `\(...\)`, `\[...\]`, and bare math lines with anti-currency heuristics.
- inputs_outputs_state: Inputs are bare LaTeX fragments or prose containing math spans. Outputs are Unicode/ANSI-rendered text.
- gates_or_invariants: Superscript/subscript groups fall back when unmappable; color parsing supports named/rgb/cmyk/hsv/wave/mixes; ANSI reset state is restored; bare math environment detection is whitelisted; inline dollar parsing avoids currency-like spans.
- dependencies_and_callers: Uses terminal capability detection; called by markdown/TUI render paths.
- edge_cases_or_failure_modes: Sparse Unicode script alphabets, malformed groups, nested environments, invalid colors, combining marks, ANSI capability differences, and unclosed math delimiters.
- validation_or_tests: Covered by TUI/math rendering tests; no tests run here.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1409 `file` `packages/tui/test/issue-2045-repro.test.ts`
- cursor: `[_]`
- core_role: Regression tests for renderer bounds with oversized zero-width rows.
- algorithmic_behavior: Verifies visible text is preserved after pathological zero-width ANSI prefixes, long combining prefixes, oversized OSC hyperlink prefixes, and OSC 66 text-sizing payloads.
- inputs_outputs_state: Inputs are crafted terminal strings with zero-width/control prefixes. Outputs are bounded rendered rows.
- gates_or_invariants: Width slicing must not drop visible suffix text or miscount zero-width payloads.
- dependencies_and_callers: Covers TUI renderer width/slice utilities.
- edge_cases_or_failure_modes: ANSI prefix overrun, combining mark floods, OSC hyperlink payloads, and text-sizing control sequences.
- validation_or_tests: The file itself validates issue #2045; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1439 `file` `packages/tui/test/render-stress-reducer.test.ts`
- cursor: `[_]`
- core_role: Regression test for render stress-case reducer.
- algorithmic_behavior: Confirms reducer shrinks a failing prefix while preserving the failure condition.
- inputs_outputs_state: Inputs are failing render sequences. Outputs are smaller reproducer sequences.
- gates_or_invariants: Reduction must preserve predicate failure.
- dependencies_and_callers: Covers TUI render debugging/reducer helper.
- edge_cases_or_failure_modes: Over-shrinking that loses failure and failure predicates with prefix dependency.
- validation_or_tests: The file itself validates reducer behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1469 `file` `packages/typescript-edit-benchmark/src/formatter.ts`
- cursor: `[_]`
- core_role: Prettier formatting helper for TypeScript edit benchmark fixtures.
- algorithmic_behavior: Maps file extensions to Prettier parsers, formats supported files with fixed options, returns unchanged content on unsupported extensions or formatting failure, and formats all listed files in a directory.
- inputs_outputs_state: Inputs are file paths, content, and benchmark root directory. Outputs are formatted strings and `didFormat` flags; `formatDirectory` writes changed files.
- gates_or_invariants: JS/JSX use Flow parser to avoid benchmark drift; unsupported files are no-ops; formatter exceptions are swallowed as no-format.
- dependencies_and_callers: Depends on Prettier and benchmark `listFiles`.
- edge_cases_or_failure_modes: Parser errors, unsupported extensions, unchanged formatted output, and benchmark fixture parser drift.
- validation_or_tests: Covered by benchmark workflows; no execution performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1499 `file` `packages/utils/src/path-tree.ts`
- cursor: `[_]`
- core_role: Prefix-folded path grouping utility for compact file listings.
- algorithmic_behavior: Builds a tree from flat path entries, keeps URL-like paths as root leaves, normalizes backslashes, dedupes files/subdirs, folds single-child directory chains during walk, and renders grouped markdown-like path lists.
- inputs_outputs_state: Inputs are paths with `isDir` and optional keys. Outputs are `PathTreeNode`, `GroupedTreeEvent` stream, or formatted grouped text.
- gates_or_invariants: URL-like `scheme://` entries are not split; absolute paths share a folded `/` root; direct files emit before subdirectories; file keys preserve original paths for annotations.
- dependencies_and_callers: Shared by find/grep/ast tool renderers and compaction file-operation lists.
- edge_cases_or_failure_modes: Duplicate paths, trailing-slash directory leaves, Windows backslashes, absolute root segments, and URL-like entries.
- validation_or_tests: Covered by consumers and path-tree tests where present; no tests run here.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1529 `file` `packages/utils/test/peek-file.test.ts`
- cursor: `[_]`
- core_role: Regression tests for bounded head/tail file peeking helpers.
- algorithmic_behavior: Tests async/sync header reads, exact tail reads, short-file whole returns, non-positive budget empty returns, head+tail combined reads, zero-budget no-open behavior, and concurrent reads without buffer corruption.
- inputs_outputs_state: Inputs are temporary files with byte ranges and budgets. Outputs are byte slices from head, tail, or both ends.
- gates_or_invariants: Non-positive budgets return empty; files shorter than budget return whole file; concurrent calls cannot share/corrupt buffers.
- dependencies_and_callers: Covers `@oh-my-pi/pi-utils/peek-file` used by readers/previews.
- edge_cases_or_failure_modes: Pool vs allocated buffer paths, concurrent peeks, zero budgets, and exact EOF boundaries.
- validation_or_tests: The file itself validates peek helpers; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1559 `file` `python/robomp/src/host_tools.py`
- cursor: `[_]`
- core_role: Host-tool suite exposed to robomp agents for GitHub, git publication, reproduction records, issue/PR triage, review, and abort side effects.
- algorithmic_behavior: Builds `HostTool` objects bound to `ToolBindings`; audits every side effect; runs repo commands with scrubbed credentials and slot env; enforces pre-publish `bun run fix`/`bun check`; validates branch/head/author/cleanliness before push; opens PRs with required body sections and closing keywords; handles issue classification, PR review comments, needs-info flow, auto-close scheduling, and abort signaling.
- inputs_outputs_state: Inputs are RPC tool args, GitHub backend, database, workspace, git transport, settings, issue/PR context, and slot uid. Outputs are GitHub comments/labels/PRs/reviews, DB state changes, audit rows, repro files, pushed branches, and abort flags.
- gates_or_invariants: PR review worktrees are read-only; non-bug/doc first PRs require maintainer authorization unless already opened; branch must match workspace; commits must use configured bot identity; dirty worktree blocks push/open; PR body needs Repro/Cause/Fix/Verification and `Fixes #n`.
- dependencies_and_callers: Used by `omp_rpc.host_tool` during robomp issue/PR automation; depends on GitHub backend/client, sandbox git transport, database, persona strings, and slot permission helpers.
- edge_cases_or_failure_modes: Formatter/check timeouts, missing Bun, head drift, GitHub API failures, needs-info label absence, PR-thread no-op triage tools, staged review cleanup, and abort races.
- validation_or_tests: Covered by robomp host-tool/retry/sandbox tests; no Python tests run here.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1589 `file` `python/robomp/tests/test_retry.py`
- cursor: `[_]`
- core_role: Regression tests for robomp event retry/backoff behavior.
- algorithmic_behavior: Tests retry-delay parsing with garbage skipped/defaults, exponential escalation and clamp, jitter bounds, retry scheduling gating claims until available, zero-delay immediate claim, allowed state transitions, manual requeue clearing backoff, and max-retry behavior through worker stubs.
- inputs_outputs_state: Inputs are environment retry settings, database events, and stubbed GitHub/sandbox/transport objects. Outputs are event states, retry timestamps, claimability, and attempt counts.
- gates_or_invariants: Only running/failed events transition into scheduled retry; manual requeue clears retry backoff; delay clamps at configured max; jitter stays in band.
- dependencies_and_callers: Covers robomp database/event loop retry scheduler.
- edge_cases_or_failure_modes: Garbage env values, empty delay config, zero delays, max retries, and stale retry locks.
- validation_or_tests: The file itself validates retry behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1619 `directory` `packages/coding-agent/src/eval/js`
- cursor: `[_]`
- core_role: JavaScript eval backend with persistent worker sessions, import rewriting, local module loading, helper prelude, tool bridge, and output capture.
- algorithmic_behavior: `context-manager.ts` owns worker sessions, resets/disposes contexts, forwards tool calls, and maps worker messages; `worker-core.ts` serializes run/tool protocol; `shared/runtime.ts` runs code with prelude/helpers, stdio/console capture, display image coercion, and global ownership tracking; `rewrite-imports.ts` rewrites static/dynamic imports and wraps final expressions; `local-module-loader.ts` resolves local TS/JS modules.
- inputs_outputs_state: Inputs are eval code, cwd/session id, timeout/abort, local roots, tool session, and imports. Outputs are captured chunks, status events, display outputs, artifacts, exit code, and worker lifecycle state.
- gates_or_invariants: Worker init timeout is 15s; close timeout default is 1s; session ids are namespaced by backend; overlapping global owners are rejected; helper paths are contained under allowed roots; tool errors are normalized.
- dependencies_and_callers: Called by eval tool backend index and tests; depends on Bun Worker/worker-host fallback, Node vm/module APIs, tool bridge modules, and TUI output sinks.
- edge_cases_or_failure_modes: Worker crash, pending run abort, tool-call timeout, import syntax inside comments/templates, local module cache invalidation, invalid image display data, and stdout/stderr patching.
- validation_or_tests: `packages/coding-agent/test/eval`, `core/js-static-import-rewrite.test.ts`, and eval smoke probes.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1649 `directory` `packages/coding-agent/test/web/search`
- cursor: `[_]`
- core_role: Regression suite for web search providers, rendering, provider chain, and timeout/abort handling.
- algorithmic_behavior: Tests Anthropic metadata, Perplexity API/OAuth/anonymous request shapes and fallback, render truncation/markdown, provider exclusion chain, Tavily request body, Codex broker auth, hard-timeout signal composition, abort propagation, and provider fallthrough on empty content.
- inputs_outputs_state: Inputs are mocked auth storage, HTTP responses/SSE streams, search settings, provider exclusions, and abort signals. Outputs are request bodies, rendered search text, selected provider chain, and errors/tool results.
- gates_or_invariants: Caller abort surfaces as `ToolAbortError`; hard timeout composes with caller signal; excluded providers are omitted; anonymous Perplexity is explicit-only; Codex uses central `AuthStorage`.
- dependencies_and_callers: Covers `packages/coding-agent/src/web/search` provider and render modules.
- edge_cases_or_failure_modes: No renderable content, upstream auth failures, SSE parsing, fallback chain exclusions, and metadata differences by auth type.
- validation_or_tests: Directory is test-only; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1679 `file` `packages/agent/src/compaction/openai.ts`
- cursor: `[_]`
- core_role: Remote compaction utilities for OpenAI Responses/Codex compact endpoints and generic compaction endpoints.
- algorithmic_behavior: Gates OpenAI remote compaction by provider, resolves endpoints, builds Responses-native history from LLM messages, preserves previous replacement history, trims compact input to context window, sends compact request with timeout, filters compact output, extracts compaction item, and attaches/removes preserve data.
- inputs_outputs_state: Inputs are model, API key, LLM messages/history, previous preserve data, instructions, abort signal, and fetch override. Outputs are replacement history, `compaction`/`compaction_summary` item, preserveData entry, or generic summary.
- gates_or_invariants: Timeout default is 180s; Codex endpoints add account/beta/originator headers; function call outputs trim with matching call items; only assistant/user/compaction output items are retained; missing compaction item is an error.
- dependencies_and_callers: Depends on `@oh-my-pi/pi-ai` message transforms and Responses history helpers, Codex wire constants, and agent compaction pipeline.
- edge_cases_or_failure_modes: O(N²) history scans avoided via incremental call-id sets, malformed reasoning signatures, different source model ids, image tool results, context overflow trimming, HTTP errors, and missing output body shape.
- validation_or_tests: Covered by OpenAI remote compaction/stateful provider tests; no tests run here.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1709 `file` `packages/ai/src/dialect/hermes.ts`
- cursor: `[_]`
- core_role: Hermes/Qwen ChatML in-band tool-call scanner and renderer.
- algorithmic_behavior: `HermesInbandScanner` buffers streamed text, detects `<tool_call>` and `<think>` regions, emits text/thinking/tool start/end events, attempts partial JSON name extraction, repairs final JSON, handles stringified args, mints ids, and renders ChatML transcript/tool definitions/results.
- inputs_outputs_state: Inputs are streamed model text and messages/tool schemas. Outputs are parsed text/thinking/tool events and rendered ChatML messages.
- gates_or_invariants: Partial tag suffixes are held until disambiguated; tool arguments are normalized to objects where possible; missing ids are generated; tool result turns are rendered as user tool-response blocks.
- dependencies_and_callers: Informed by `docs/toolconv/qwen3.md`; used by Hermes/OpenAI-compatible dialect handling.
- edge_cases_or_failure_modes: Split tags across chunks, malformed JSON, nested/stringified arguments, mixed text and tool calls, and unclosed thinking/tool tags.
- validation_or_tests: Covered by AI dialect/tool conversion tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1739 `file` `packages/ai/src/providers/google-types.ts`
- cursor: `[_]`
- core_role: Local TypeScript mirror of Google/Gemini/Vertex wire types.
- algorithmic_behavior: Defines request/response/content/tool/function-calling type contracts used by Google provider code.
- inputs_outputs_state: Inputs are compile-time provider shapes; outputs are TypeScript type checking only.
- gates_or_invariants: Runtime behavior is not implemented in this file; it preserves local compatibility with upstream Google wire schemas.
- dependencies_and_callers: Imported by Google provider implementation.
- edge_cases_or_failure_modes: Type drift from Google SDK/API and incomplete discriminated unions.
- validation_or_tests: Type checking and provider tests validate usage.
- skip_candidate: `yes: type-only contract file with no runtime algorithm`

### OH_MY_HUMANIZE_MAIN-HZ-1769 `file` `packages/ai/src/registry/api-key-validation.ts`
- cursor: `[_]`
- core_role: Shared API-key validation helpers for provider login flows.
- algorithmic_behavior: Sends minimal OpenAI-compatible chat completion request, Anthropic-compatible messages request, or models endpoint GET with a 15s composed timeout and reports status/body details on failure.
- inputs_outputs_state: Inputs are provider label, API key, base/model/models URL, abort signal, and fetch override. Outputs are success or thrown validation error.
- gates_or_invariants: Anthropic base URL is normalized to remove trailing `/v1`; OpenAI body uses `max_tokens:1`; Anthropic body uses `anthropic-version`; response body text is included when available.
- dependencies_and_callers: Used by API-key login providers such as DeepSeek/ZenMux and OpenAI/Anthropic-compatible flows.
- edge_cases_or_failure_modes: Timeout, caller abort, non-text error bodies, base URL with trailing slash, and provider-specific model access differences.
- validation_or_tests: Covered by provider login tests including DeepSeek and ZenMux-style validation.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1799 `file` `packages/ai/src/registry/ollama-cloud.ts`
- cursor: `[_]`
- core_role: Ollama Cloud API-key login provider.
- algorithmic_behavior: Opens auth/key URL callback, prompts for API key, checks cancellation before and after prompt, trims key, and rejects empty input.
- inputs_outputs_state: Inputs are OAuth/login callbacks and abort signal. Outputs are trimmed API key string or login error.
- gates_or_invariants: Interactive prompt is required; empty key fails; abort is honored.
- dependencies_and_callers: Registered as `ollama-cloud` provider in AI registry.
- edge_cases_or_failure_modes: No prompt callback, user cancellation, whitespace-only key.
- validation_or_tests: Covered by provider login tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1829 `file` `packages/ai/src/registry/zenmux.ts`
- cursor: `[_]`
- core_role: ZenMux API-key login provider declaration.
- algorithmic_behavior: Uses `createApiKeyLogin` with ZenMux key URL, prompt text, placeholder, and models-endpoint validation at `https://zenmux.ai/api/v1/models`.
- inputs_outputs_state: Inputs are login callbacks and pasted API key. Outputs are validated API key string or validation error.
- gates_or_invariants: Models endpoint validation is required; provider id/name are fixed.
- dependencies_and_callers: Depends on shared API-key login helper and registry provider definition.
- edge_cases_or_failure_modes: Empty key, validation failure, and endpoint auth error.
- validation_or_tests: Covered by generic API-key login/validation tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1859 `file` `packages/ai/src/utils/sdk-stream-timeout.ts`
- cursor: `[_]`
- core_role: Maps stream first-event timeout hints into SDK request options.
- algorithmic_behavior: Coerces positive finite `streamFirstEventTimeoutMs` to integer SDK `timeout`; `createSdkStreamRequestOptions` returns `{ signal }` or `{ signal, timeout, maxRetries:0 }`.
- inputs_outputs_state: Inputs are abort signal and optional timeout hint. Outputs are SDK-compatible request options.
- gates_or_invariants: Undefined, non-finite, zero, or negative timeout leaves SDK defaults; setting timeout disables SDK retries to avoid extending explicit deadline.
- dependencies_and_callers: Used by OpenAI/Anthropic/Stainless-style SDK provider calls.
- edge_cases_or_failure_modes: NaN/infinite timeout, fractional values, and retry budget silently exceeding caller deadline.
- validation_or_tests: Covered by stream timeout/default tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1889 `file` `packages/catalog/src/provider-models/descriptors.ts`
- cursor: `[_]`
- core_role: Catalog provider descriptor table and derived provider metadata.
- algorithmic_behavior: Defines provider entries, discovery factories/flags, default models, env vars, and derived `KnownProvider`, `PROVIDER_DESCRIPTORS`, and `DEFAULT_MODEL_PER_PROVIDER` structures while excluding special manager-only providers where needed.
- inputs_outputs_state: Inputs are static provider descriptor definitions. Outputs are catalog provider descriptors and default model maps consumed by model manager/cache.
- gates_or_invariants: Catalog values are imported from `@oh-my-pi/pi-catalog/*`; special managers are excluded from public descriptors; discovery env vars fall back to provider env vars.
- dependencies_and_callers: Used by catalog generation, provider discovery, model selector, and AI registry.
- edge_cases_or_failure_modes: Default model drift, env var mismatch, discovery factory omission, and duplicate/special provider exposure.
- validation_or_tests: Catalog tests and generator checks cover descriptor contracts.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1919 `file` `packages/coding-agent/src/capability/index.ts`
- cursor: `[_]`
- core_role: Capability/provider registry for extensions, built-ins, slash commands, and settings-controlled enablement.
- algorithmic_behavior: Loads providers in priority order, dedupes capability keys, tracks shadowed providers, validates non-shadowed entries, filters disabled extensions/providers, persists enablement settings, exposes introspection, and supports cache reset/invalidation.
- inputs_outputs_state: Inputs are provider definitions, extension state, settings, and load callbacks. Outputs are active capability lists, diagnostics/warnings, and persisted disabled state.
- gates_or_invariants: `_source` is required; first/highest-priority capability wins; disabled extensions are filtered; validation applies only to non-shadowed capabilities.
- dependencies_and_callers: Used by extension runtime, slash command discovery, tool/capability surfaces, and settings UI.
- edge_cases_or_failure_modes: Provider load failures, duplicate keys, stale cache after settings change, shadowed invalid providers, and disabled source filtering.
- validation_or_tests: Covered by discovery/extensibility tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1949 `file` `packages/coding-agent/src/cli/initial-message.ts`
- cursor: `[_]`
- core_role: Initial prompt assembler for CLI stdin, `@file` attachments, images, and positional messages.
- algorithmic_behavior: Combines stdin text, file context, image attachments, and first CLI message; mutates parsed messages by shifting the first message when file context is present; supports image-only starts with empty initial message.
- inputs_outputs_state: Inputs are parsed CLI args/messages, stdin, file refs, and image data. Outputs are initial message text and image list for session start.
- gates_or_invariants: File context is prepended before user message; image-only input does not fabricate text; consumed first positional message is removed from remaining messages.
- dependencies_and_callers: Used by coding-agent CLI startup.
- edge_cases_or_failure_modes: Empty stdin, only images, file context plus multiple messages, and first-message mutation.
- validation_or_tests: Covered by CLI startup/input tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-1979 `file` `packages/coding-agent/src/commands/commit.ts`
- cursor: `[_]`
- core_role: CLI command wrapper for agentic/legacy commit generation.
- algorithmic_behavior: Parses flags for push, dry run, changelog, legacy mode, context, and model; initializes theme; runs commit workflow; exits via `postmortem.quit(0)` to avoid lingering sockets/timers.
- inputs_outputs_state: Inputs are CLI args and environment/session settings. Outputs are commit workflow execution and process exit.
- gates_or_invariants: Does not commit by itself outside workflow; explicit flags control push/dry-run/changelog/legacy behavior.
- dependencies_and_callers: Calls commit workflow/model-selection modules and CLI command registry.
- edge_cases_or_failure_modes: Lingering async handles after command completion, missing model, and dry-run vs push flags.
- validation_or_tests: Covered by commit/CLI tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2009 `file` `packages/coding-agent/src/commit/model-selection.ts`
- cursor: `[_]`
- core_role: Commit-agent model resolver.
- algorithmic_behavior: Resolves commit model from explicit override or roles `[commit, smol, ...]`, verifies API key availability, returns resolver for refresh, and falls back through smol role, priority patterns, then primary model.
- inputs_outputs_state: Inputs are model override, role metadata, registry/cache, API credentials, and primary session model. Outputs are selected model and refresh resolver.
- gates_or_invariants: Selected model must have usable API key; smol fallback follows configured role/priority/default order.
- dependencies_and_callers: Used by commit command/workflow.
- edge_cases_or_failure_modes: Missing API key, absent commit role, stale model registry, and override pointing at unavailable provider.
- validation_or_tests: Covered by commit workflow tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2039 `file` `packages/coding-agent/src/debug/raw-sse-buffer.ts`
- cursor: `[_]`
- core_role: Owner-scoped raw SSE event ring buffer for debugging provider streams.
- algorithmic_behavior: Stores response/event records with caps of 1000 events, 512k total chars, and 64k per event; trims raw lines; tracks dropped counts; supports listeners with eager single-listener and microtask coalescing for many listeners; uses owner buffer symbol or global fallback.
- inputs_outputs_state: Inputs are raw SSE response/event lines and listeners. Outputs are bounded debug records and listener notifications.
- gates_or_invariants: Buffer caps are enforced; dropped counts are recorded; raw event strings are trimmed; listener fanout is coalesced when needed.
- dependencies_and_callers: Used by provider debug tooling and TUI/debug commands.
- edge_cases_or_failure_modes: Huge event lines, listener storms, ownerless contexts, and memory growth from long streams.
- validation_or_tests: Covered by debug/raw SSE tests where present.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2069 `file` `packages/coding-agent/src/edit/file-snapshot-store.ts`
- cursor: `[_]`
- core_role: In-memory file snapshot and seen-line tracker for safe edit validation.
- algorithmic_behavior: Canonicalizes paths via realpath/native helpers, stores LF-normalized snapshots up to a 4MiB cap, parses seen lines from hashline prefixes, records seen lines, and rejects unseen edit anchors.
- inputs_outputs_state: Inputs are file paths, contents, and hashline/seen-line metadata. Outputs are stored snapshots and seen-line validation results.
- gates_or_invariants: Snapshot cap is 4MiB; paths are canonicalized; line endings normalize to LF; unseen lines are rejected.
- dependencies_and_callers: Used by edit tools and hashline recovery/validation.
- edge_cases_or_failure_modes: Deleted paths, symlinks/realpath failures, very large files, CRLF files, and stale seen-line metadata.
- validation_or_tests: Covered by edit/hashline tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2099 `file` `packages/coding-agent/src/extensibility/slash-commands.ts`
- cursor: `[_]`
- core_role: Slash-command loader and renderer for capability and file/template commands.
- algorithmic_behavior: Loads commands from capability providers and embedded templates, extracts frontmatter descriptions or first line, labels sources, parses args, substitutes placeholders with Handlebars, and appends args when no placeholders exist.
- inputs_outputs_state: Inputs are slash command definitions, template markdown, command args, and session context. Outputs are expanded prompt text and command metadata.
- gates_or_invariants: Prompt content comes from static files/templates; source labels are preserved; no-placeholder templates receive raw args as fallback.
- dependencies_and_callers: Used by slash command registry, extensions, and available-commands metadata.
- edge_cases_or_failure_modes: Missing frontmatter, malformed args, no placeholders, duplicate command names, and Handlebars render failures.
- validation_or_tests: Covered by available commands and marketplace/parser tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2129 `file` `packages/coding-agent/src/internal-urls/router.ts`
- cursor: `[_]`
- core_role: Internal URL scheme router for agent-local resources.
- algorithmic_behavior: Registers scheme handlers for `omp`, `agent`, `artifact`, `memory`, `local`, `vault`, `skill`, `rule`, `mcp`, `issue`, `pr`, and `history`; checks support, completes, and resolves URLs with unknown-protocol errors listing supported schemes.
- inputs_outputs_state: Inputs are URL strings and registered handlers. Outputs are completion candidates or resolved resources.
- gates_or_invariants: Default handler map is immutable; unknown protocols produce explicit supported-scheme errors.
- dependencies_and_callers: Used by tools, plan guard, eval helpers, and UI links.
- edge_cases_or_failure_modes: Unknown scheme, malformed URL, handler absence, and protocol completion ambiguity.
- validation_or_tests: Covered by internal URL/tool tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2159 `file` `packages/coding-agent/src/mcp/oauth-flow.ts`
- cursor: `[_]`
- core_role: MCP OAuth/PKCE login and refresh flow.
- algorithmic_behavior: Builds profile-scoped credential IDs, validates OAuth config URLs, performs optional authorization server discovery and dynamic client registration, creates PKCE verifier/challenge, supports resource indicators and consent prompt, exchanges authorization code, stores credentials, and refreshes tokens.
- inputs_outputs_state: Inputs are MCP server config, active profile, redirect/callback ports, auth server metadata, browser/prompt callbacks, and token responses. Outputs are credential records and refreshed OAuth access.
- gates_or_invariants: Redirect/resource URIs reject whitespace, invalid schemes, and fragments; loopback HTTPS requires separate callback port; credential IDs are profile-scoped; refresh uses stored metadata.
- dependencies_and_callers: Used by MCP server auth/reauth workflows and config writer.
- edge_cases_or_failure_modes: Discovery failure, dynamic registration rejection, invalid URI config, callback port conflict, user cancellation, token exchange errors, and refresh expiry.
- validation_or_tests: Covered by MCP OAuth/reauth tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2189 `file` `packages/coding-agent/src/modes/markdown-prose.ts`
- cursor: `[_]`
- core_role: Markdown prose masker for keyword detection outside code/tags.
- algorithmic_behavior: Produces a length-preserving mask of fenced code, inline code, HTML/XML tags and tag content, preserving newlines so prose scanners can ignore non-prose spans while keeping offsets stable.
- inputs_outputs_state: Inputs are markdown strings. Outputs are masked strings with same length/newline layout.
- gates_or_invariants: Newlines are preserved; masked output remains offset-compatible with original text.
- dependencies_and_callers: Used by mode/controllers that detect prose keywords or commands.
- edge_cases_or_failure_modes: Unclosed fences, nested inline code, HTML/XML blocks, and preserving offsets through mixed markdown.
- validation_or_tests: Covered by mode tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2219 `file` `packages/coding-agent/src/session/history-storage.ts`
- cursor: `[_]`
- core_role: SQLite-backed prompt/history storage with FTS search.
- algorithmic_behavior: Initializes SQLite WAL/busy timeout, creates prompt table/FTS5/triggers, migrates schema, batches async writes at 100ms, dedupes last prompt, searches with prefix AND plus escaped substring LIKE fallback, clamps limit to 1000, and dedupes matching sessions by recency.
- inputs_outputs_state: Inputs are session prompt records and search queries. Outputs are stored history rows, FTS index rows, and search results.
- gates_or_invariants: `session_id` migration is applied; limit is clamped; wildcard characters are escaped for LIKE; duplicate last prompt is skipped.
- dependencies_and_callers: Used by coding-agent history UI and session persistence.
- edge_cases_or_failure_modes: Schema migration from `unixepoch`, SQLite busy state, duplicate prompts, special LIKE chars, and cross-session recency dedupe.
- validation_or_tests: Covered by session/history tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2249 `file` `packages/coding-agent/src/ssh/ssh-executor.ts`
- cursor: `[_]`
- core_role: SSH command executor with optional remote mount support.
- algorithmic_behavior: Ensures SSH connection, mounts via sshfs when available, wraps commands for compatibility shell, spawns `ssh`, streams stdout/stderr through `OutputSink`, handles abort/timeout via process tree cleanup, and returns sanitized/truncated output plus artifacts.
- inputs_outputs_state: Inputs are SSH target, command, cwd/env/options, abort signal, and timeout. Outputs are exit status, stdout/stderr previews, artifacts, and cancellation metadata.
- gates_or_invariants: Abort/timeout terminate process tree; output is truncated/sanitized through sink; mount setup is conditional.
- dependencies_and_callers: Used by SSH tool and tested by `test/ssh/ssh-executor.test.ts`.
- edge_cases_or_failure_modes: ControlMaster streams staying open, abort after process exit, sshfs absence, remote shell incompatibility, and output overflow.
- validation_or_tests: Assigned SSH executor test covers abort races.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2279 `file` `packages/coding-agent/src/tiny/dtype.ts`
- cursor: `[_]`
- core_role: Tiny model dtype setting validator and mapper.
- algorithmic_behavior: Validates `PI_TINY_DTYPE`/setting values against allowed transformer dtypes, exposes setting options, and maps default sentinel to `undefined`.
- inputs_outputs_state: Inputs are env/setting dtype strings. Outputs are validated dtype or default/undefined.
- gates_or_invariants: Only allowed dtype values pass; default sentinel means provider default.
- dependencies_and_callers: Used by tiny inference/model subprocess configuration.
- edge_cases_or_failure_modes: Invalid env value, empty value, default sentinel, and unsupported transformer dtype.
- validation_or_tests: Covered by tiny/smoke tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2309 `file` `packages/coding-agent/src/tools/find.ts`
- cursor: `[_]`
- core_role: Find/glob search tool for local and internal URL targets.
- algorithmic_behavior: Validates args with ark schema, expands delimited paths unless custom operations are used, resolves internal URLs, rejects root `/`, clamps limit/timeout, runs custom ops or native glob per target concurrently, streams updates every 200ms, dedupes/sorts matches by mtime, and renders sanitized grouped output with missing-path/truncation notices.
- inputs_outputs_state: Inputs are paths, glob/pattern options, hidden/gitignore flags, limit, timeout, and tool session. Outputs are match list, partial timeout results, render text, and metadata.
- gates_or_invariants: Limit clamp is 1..200; timeout clamp is 0.5..60s; root `/` is forbidden; internal URL resolution only without glob; single missing path errors while multi missing paths are skipped/noted.
- dependencies_and_callers: Uses native glob/search, internal URL router, path tree renderer, and TUI sanitizers.
- edge_cases_or_failure_modes: Timeout partials, missing paths, custom ops with delimited paths, hidden/gitignore defaults, duplicated matches, and long/path-leaking render content.
- validation_or_tests: Covered by find/read/tool tests and TUI sanitization paths.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2339 `file` `packages/coding-agent/src/tools/plan-mode-guard.ts`
- cursor: `[_]`
- core_role: Plan-mode write guard for tool operations.
- algorithmic_behavior: Unwraps strict hashline refs, resolves local/vault/plain paths, classifies write/rename/delete attempts, forbids working-tree writes in plan mode, and allows `local://` artifact sandbox writes after realpath-aware containment checks.
- inputs_outputs_state: Inputs are tool action/path arguments and session path roots. Outputs are allow/deny decisions and guard errors.
- gates_or_invariants: Working tree writes, renames, and deletes are blocked in plan mode; artifact sandbox containment is required; hashline refs are resolved before classification.
- dependencies_and_callers: Used by write/edit/bash-like tools in plan mode.
- edge_cases_or_failure_modes: Symlink containment, vault/local schemes, hashline path wrappers, rename/delete operations, and artifact traversal.
- validation_or_tests: Covered by tool approval/plan-mode tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2369 `file` `packages/coding-agent/src/tui/file-list.ts`
- cursor: `[_]`
- core_role: TUI renderer for file list entries.
- algorithmic_behavior: Renders file entries with icons, metadata, tree/list structure, and terminal hyperlinks.
- inputs_outputs_state: Inputs are file entry objects and render width/theme. Outputs are TUI text rows.
- gates_or_invariants: Paths are shortened/width-safe through TUI helpers; entries remain stable for list rendering.
- dependencies_and_callers: Used by coding-agent file list surfaces.
- edge_cases_or_failure_modes: Long paths, missing metadata, narrow widths, and hyperlink support.
- validation_or_tests: Covered by TUI/file-list snapshots where present.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2399 `file` `packages/coding-agent/src/utils/shell-snapshot.ts`
- cursor: `[_]`
- core_role: Captures shell aliases/functions/options/PATH for command execution context.
- algorithmic_behavior: Runs shell-specific snapshot scripts for bash/zsh, sanitizes `BASH_ENV`/`ENV`, skips Windows, uses a 2s timeout, caches per shell, and registers postmortem cleanup.
- inputs_outputs_state: Inputs are shell path/environment. Outputs are snapshot data for command previews/execution.
- gates_or_invariants: Windows is skipped; timeout is bounded; cache key is shell-specific; dangerous env hooks are cleared.
- dependencies_and_callers: Used by shell/bash tool execution.
- edge_cases_or_failure_modes: Hanging shell startup, user rc files, unsupported shell, poisoned env hooks, and stale cache.
- validation_or_tests: Covered by shell/bash tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2429 `file` `packages/coding-agent/src/workflow/patches.ts`
- cursor: `[_]`
- core_role: Workflow graph patch proposal/apply/validation engine.
- algorithmic_behavior: Clones workflow definitions, previews node/edge/model/prompt/state/branch changes, validates duplicates/references/edge conditions/model context/prompt refs/state permissions, and applies only from authorized supervisor/human contexts while forbidding active-run graph mutation.
- inputs_outputs_state: Inputs are workflow graph, patch operations, caller context, prompt source refs, and branch dispositions. Outputs are previews, validation diagnostics, model impact, or updated definitions.
- gates_or_invariants: Ordinary agents may propose but not apply; active runs cannot be patched directly; model context must specify exactly one role/selector/candidates shape; fallback verdict is review-only; duplicate nodes/edges are rejected.
- dependencies_and_callers: Used by workflow tooling and CLI workflow management.
- edge_cases_or_failure_modes: Dangling node refs, invalid edge conditions, state-scope permission violations, prompt dependency mistakes, and branch disposition mutations.
- validation_or_tests: `packages/coding-agent/test/workflow/patches.test.ts` covers proposal/apply/validation.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2459 `file` `packages/coding-agent/test/core/js-static-import-rewrite.test.ts`
- cursor: `[_]`
- core_role: Regression tests for JavaScript eval import rewriting.
- algorithmic_behavior: Tests static default/named/namespace/combined/side-effect imports, import attributes, dynamic import rewriting, helper/native import fallback, template/comment/string lookalike preservation, unchanged invalid/no-import input, final expression capture, and type-only import stripping.
- inputs_outputs_state: Inputs are JS/TS code snippets. Outputs are rewritten executable code strings.
- gates_or_invariants: Only real top-level imports are rewritten; dynamic imports resolve against session cwd; parser failures return input unchanged; type-only imports are stripped before wrapping.
- dependencies_and_callers: Covers `packages/coding-agent/src/eval/js/shared/rewrite-imports.ts`.
- edge_cases_or_failure_modes: Imports inside template literals/comments/continued strings, nested dynamic imports, nonliteral specifiers, and trailing empty statements.
- validation_or_tests: The file itself validates rewrite behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2489 `file` `packages/coding-agent/test/discovery/agents-monorepo-skills.test.ts`
- cursor: `[_]`
- core_role: Regression tests for project-level `.agents`/`.agent` discovery in monorepos.
- algorithmic_behavior: Tests skills, rules, prompts, commands, `AGENTS.md`, and `SYSTEM.md` discovery from subproject cwd up to repo root, closest-first ordering, `.agent` and `.agents` coexistence, home-dir skip, repo-root stop, and depth values for dedup.
- inputs_outputs_state: Inputs are temporary monorepo directory structures and cwd/home settings. Outputs are discovered resource candidates with order/depth/source.
- gates_or_invariants: Walk-up stops at repo root; project/user candidates do not overlap under home; closest resources precede root resources.
- dependencies_and_callers: Covers coding-agent discovery providers for skills/rules/prompts/context/system.
- edge_cases_or_failure_modes: No repo root, multiple marker dirs at same level, nested subproject, duplicate context files, and home directory pollution.
- validation_or_tests: The file itself validates discovery behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2519 `file` `packages/coding-agent/test/extensibility/typebox-remap.test.ts`
- cursor: `[_]`
- core_role: Regression tests for legacy extension TypeBox import remapping.
- algorithmic_behavior: Verifies bare `@sinclair/typebox` and `typebox` imports inside legacy extensions redirect to the in-repo shim.
- inputs_outputs_state: Inputs are extension modules with bare imports. Outputs are resolved module paths/loaded shim behavior.
- gates_or_invariants: Legacy extension imports must not pull incompatible external TypeBox packages.
- dependencies_and_callers: Covers extension runner/module loading compatibility.
- edge_cases_or_failure_modes: Bare package aliases, legacy extension dependency expectations, and module resolution drift.
- validation_or_tests: The file itself validates remap behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2549 `file` `packages/coding-agent/test/marketplace/slash-install-parser.test.ts`
- cursor: `[_]`
- core_role: Regression tests for marketplace/plugin slash command argument parsers.
- algorithmic_behavior: Tests valid install args with `--force`, `--scope user|project`, positional ordering/whitespace, and errors for empty spec, missing `@`, unknown flags, invalid/missing scope values, multiple positionals, and flags-only input; also tests plugin scope args.
- inputs_outputs_state: Inputs are slash command argument strings. Outputs are parsed spec/force/scope/id or usage errors.
- gates_or_invariants: Scope must be `user` or `project`; exactly one positional spec/id is accepted; unknown flags reject even before positional.
- dependencies_and_callers: Covers marketplace/slash install command parser.
- edge_cases_or_failure_modes: Flag after positional, positional between flags, `--scope` before another flag, extra whitespace, and missing plugin id.
- validation_or_tests: The file itself validates parser behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2579 `file` `packages/coding-agent/test/session-manager/tree-traversal.test.ts`
- cursor: `[_]`
- core_role: Regression tests for session tree append, branch, traversal, and branched context.
- algorithmic_behavior: Tests append operations for messages/thinking/model/compaction/custom entries, leaf pointer movement, `getPath`, `getTree`, branch creation, branch summaries, leaf/entry lookup, branch-local context building, and creating branched sessions.
- inputs_outputs_state: Inputs are in-memory session operations and branch ids. Outputs are tree/path arrays, leaf entries, session contexts, and branched sessions.
- gates_or_invariants: Appends become children of current leaf; branch to unknown id throws; branch context includes current branch only; branch summary advances leaf.
- dependencies_and_callers: Covers `SessionManager` core tree state.
- edge_cases_or_failure_modes: Multiple branches at same point, deep branching, empty session, unknown id, and copying path into new in-memory branched session.
- validation_or_tests: The file itself validates tree behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2609 `file` `packages/coding-agent/test/ssh/ssh-executor.test.ts`
- cursor: `[_]`
- core_role: Regression tests for SSH executor abort races.
- algorithmic_behavior: Tests executor returns promptly when abort races a ControlMaster stream that stays open and reports cancellation when abort unblocks streams after ssh process exits.
- inputs_outputs_state: Inputs are mocked ssh process/streams and abort signals. Outputs are cancellation result timing/status.
- gates_or_invariants: Abort must not hang on still-open streams; cancellation status is preserved.
- dependencies_and_callers: Covers `packages/coding-agent/src/ssh/ssh-executor.ts`.
- edge_cases_or_failure_modes: SSH process exits before stream close, ControlMaster keeps pipe open, and abort-after-exit race.
- validation_or_tests: The file itself validates SSH behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2639 `file` `packages/coding-agent/test/tools/approval.test.ts`
- cursor: `[_]`
- core_role: Regression tests for tool approval policy resolution and prompt formatting.
- algorithmic_behavior: Tests tier/mode policy matrix, yolo override handling, user policy override, MCP fallback approval, prompt origin/reason/details formatting/truncation, Bash dynamic critical pattern classification, benign bash, and LSP/debug readonly action exports.
- inputs_outputs_state: Inputs are tool definitions, modes, user policies, MCP metadata, and command args. Outputs are approval decisions/prompts.
- gates_or_invariants: Invalid user policies are ignored; unannotated MCP tools default to exec tier; yolo ignores override prompts but user policy still controls execution; dynamic tool approvals can classify critical bash patterns.
- dependencies_and_callers: Covers tool execution approval guard.
- edge_cases_or_failure_modes: Missing MCP declarations, write approval in different modes, long details, annotated MCP origin suppression, and dangerous bash patterns.
- validation_or_tests: The file itself validates approval behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2669 `file` `packages/coding-agent/test/tools/fetch-kagi-toggle.test.ts`
- cursor: `[_]`
- core_role: Regression tests for read tool URL fetching, selectors, image handling, provider selection, and cache.
- algorithmic_behavior: Tests raw/line selectors embedded in URLs, image content block emission/resizing, text fallback for unsupported/invalid images, HTML-at-image-path behavior, refetch fallback, rendered page preference over `llms.txt`, section-scoped fallback, Parallel provider preference, repeated URL cache reuse, and offset/limit on cached output.
- inputs_outputs_state: Inputs are mocked fetch/page/image responses, settings, URLs with selectors, and cache state. Outputs are read tool content blocks/text and cache hits.
- gates_or_invariants: HTML at `.png` path is not inline image; failed inline image conversion falls back to text and is not fatal; deep pages prefer rendered content; cached output supports slicing.
- dependencies_and_callers: Covers read/fetch tools and URL provider selection.
- edge_cases_or_failure_modes: Invalid image bytes, unsupported MIME, failed refetch, site-wide vs section `llms.txt`, provider setting changes, and selector parsing.
- validation_or_tests: The file itself validates URL read behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2699 `file` `packages/coding-agent/test/tools/read-pdf-images.test.ts`
- cursor: `[_]`
- core_role: Regression tests for PDF image extraction via read tool.
- algorithmic_behavior: Tests placeholder rewrite into browse handles, line-range placeholder rewrite, inline image block extraction for PDF image members, extraction cache reuse, unknown member errors listing available members, traversal rejection, trailing-colon member listing, and failed conversion not cached.
- inputs_outputs_state: Inputs are PDF conversion/extraction fixtures and read paths with member suffixes. Outputs are text with handles, inline image content blocks, errors, or member listings.
- gates_or_invariants: Member traversal attempts are rejected; failed conversions are not cached; unknown member errors enumerate valid members.
- dependencies_and_callers: Covers read tool PDF/mupdf integration.
- edge_cases_or_failure_modes: Line-range reads, repeated member extraction, conversion failure, and malicious member paths.
- validation_or_tests: The file itself validates PDF image behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2729 `file` `packages/coding-agent/test/tools/web-search-searxng.test.ts`
- cursor: `[_]`
- core_role: Regression tests for SearXNG web search authentication and error handling.
- algorithmic_behavior: Tests RFC7617 Basic auth from settings/config, nested config.yml credentials, Basic-over-bearer precedence, intentionally empty username/password handling, requirement for both values, colon/control-character rejection, bearer fallback, and empty results with upstream failures becoming provider errors.
- inputs_outputs_state: Inputs are SearXNG settings and mocked responses. Outputs are HTTP auth headers or provider errors.
- gates_or_invariants: Basic auth requires both fields but allows intentional empty string for one side; username cannot contain colon/control chars; password cannot contain control chars; Basic auth wins over bearer when both configured.
- dependencies_and_callers: Covers SearXNG provider in web search tool.
- edge_cases_or_failure_modes: Nested config parsing, empty credentials, malformed credentials, and upstream failure metadata.
- validation_or_tests: The file itself validates SearXNG behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2759 `file` `packages/coding-agent/test/workflow/patches.test.ts`
- cursor: `[_]`
- core_role: Regression tests for workflow graph patch API.
- algorithmic_behavior: Tests ordinary contexts can propose but not apply, authorized apply reports graph/model impact, model role updates and empty role rejection, branch dispositions without graph mutation, validation of refs/model context/edge conditions, prompt source dependency preview, and rejection of active-run direct patching.
- inputs_outputs_state: Inputs are workflow definitions, patch operations, and caller auth context. Outputs are previews, diagnostics, updated definitions, and rejection errors.
- gates_or_invariants: Apply requires authorized context; active runs are protected; invalid refs/conditions/model contexts are rejected.
- dependencies_and_callers: Covers `packages/coding-agent/src/workflow/patches.ts`.
- edge_cases_or_failure_modes: Empty model role patches, branch-only changes, prompt dependency changes, and active run mutation attempts.
- validation_or_tests: The file itself validates patch behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2789 `file` `packages/mnemopi/src/core/banks.ts`
- cursor: `[_]`
- core_role: Mnemopi memory-bank filesystem/database manager.
- algorithmic_behavior: Creates, deletes, lists, checks, renames, and stats memory banks; maps default bank to root DB and named banks to `banks/<name>/mnemopi.db`; opens/closes DB on creation; maintains process-global default bank.
- inputs_outputs_state: Inputs are bank names, data dir, and force flag. Outputs are DB paths, bank lists, deletion booleans, stats, and default bank value.
- gates_or_invariants: Bank names must be nonempty, <=64 chars, alphanumeric/hyphen/underscore; default bank cannot be deleted without force or renamed; default bank always exists logically.
- dependencies_and_callers: Depends on mnemopi config and DB open/close helpers; used by memory routing/Hindsight bank logic.
- edge_cases_or_failure_modes: Existing bank create, missing bank delete, invalid name chars, default bank special-case, and rename collisions.
- validation_or_tests: Covered by mnemopi bank/query/diagnose tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2819 `file` `packages/mnemopi/src/core/vector-index.ts`
- cursor: `[_]`
- core_role: Exact cosine vector index for memory search.
- algorithmic_behavior: Filters invalid/null/zero vectors, computes max dimension, stores normalized rows in a compact `Float32Array`, and performs exact cosine dot-product ranking with top-k slicing.
- inputs_outputs_state: Inputs are rows with ids/vectors and query vector/limit. Outputs are sorted `{id, score}` hits.
- gates_or_invariants: Non-finite or zero-norm vectors are skipped; non-finite/zero query returns no hits; limit is truncated and clamped nonnegative; dimensions compare up to min(query,index).
- dependencies_and_callers: Used by mnemopi recall/query cache exact vector paths.
- edge_cases_or_failure_modes: Mixed vector dimensions, NaN/Infinity values, empty query/index, zero limit, and precision loss from Float32 normalization.
- validation_or_tests: Covered by mnemopi query-cache/vector tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2849 `file` `packages/tui/src/components/loader.ts`
- cursor: `[_]`
- core_role: Animated TUI loader/spinner component.
- algorithmic_behavior: Extends `Text`, advances spinner frames every 80ms, optionally redraws animated message colors at 30fps, truncates rendered lines to width by columns, and requests component-scoped rerender when display text changes.
- inputs_outputs_state: Inputs are TUI instance, color functions, message, optional frames, and render width. Outputs are rendered spinner/message rows and component render requests.
- gates_or_invariants: `dispose`/`stop` clear interval idempotently; width clipping uses visible-column slicing; component-scoped rerender avoids full tree repaint.
- dependencies_and_callers: Used by TUI loading states across coding-agent.
- edge_cases_or_failure_modes: Animated color function without spinner tick, narrow width, repeated same message, and leaked intervals.
- validation_or_tests: Covered by TUI render tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2879 `file` `python/robomp/web/src/types.ts`
- cursor: `[_]`
- core_role: Frontend TypeScript contracts for robomp web server JSON responses.
- algorithmic_behavior: Defines event/issue/runtime/log/browse/trigger/cancel response interfaces and ordering constants for terminal states, log levels, and event states.
- inputs_outputs_state: Inputs are backend JSON shapes from `src/server.py`; outputs are frontend type checking and ordering sets.
- gates_or_invariants: Unknown log extras remain `unknown`; terminal issue states are `merged`, `closed`, and `abandoned`; event state order is fixed.
- dependencies_and_callers: Used by robomp web React UI/data formatting.
- edge_cases_or_failure_modes: Backend shape drift and un-narrowed unknown log extras.
- validation_or_tests: Type checking validates consumers.
- skip_candidate: `yes: type/constant contract for web UI, not a runtime algorithm itself`

### OH_MY_HUMANIZE_MAIN-HZ-2909 `file` `crates/pi-shell/src/minimizer/filters/glab.rs`
- cursor: `[_]`
- core_role: Output minimizer for GitLab `glab` CLI commands.
- algorithmic_behavior: Supports `mr`, `issue`, `ci`, `pipeline`, `release`; preserves raw modes for JSON/output/web/API/comments/diff; strips ANSI/section markers; compacts CI traces, release lists/views, and MR/issue markdown body noise.
- inputs_outputs_state: Inputs are minimizer context, raw command output, and exit code. Outputs are passthrough or transformed minimized text.
- gates_or_invariants: Nonzero MR/issue view preserves error context via head/tail; release list emits max 20 rows with omission marker; unclosed HTML comment stripping caps at 50 lines to prevent data loss; raw diff is never modified.
- dependencies_and_callers: Used by pi-shell command output minimizer with primitive filters.
- edge_cases_or_failure_modes: GitLab CI bare ANSI codes, release list without tabs, assets/sources sections, image/badge noise, malformed HTML comments, and user-requested raw formats.
- validation_or_tests: Inline Rust tests cover CI trace, release list/view, MR view, passthrough, omissions, and diff passthrough.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2939 `file` `packages/ai/src/registry/oauth/opencode.ts`
- cursor: `[_]`
- core_role: OpenCode Zen API-key login flow.
- algorithmic_behavior: Opens auth URL, prompts for API key, checks abort after prompt, trims key, and rejects empty input.
- inputs_outputs_state: Inputs are OAuth controller callbacks and user-pasted key. Outputs are API key string or login error.
- gates_or_invariants: Prompt callback is required; cancellation throws `Login cancelled`; empty key fails.
- dependencies_and_callers: Used by AI registry provider login for OpenCode Zen.
- edge_cases_or_failure_modes: Missing prompt callback, user cancellation after auth, whitespace-only key.
- validation_or_tests: Covered by provider login tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2969 `file` `packages/coding-agent/src/cli/__tests__/workflow-cli.test.ts`
- cursor: `[_]`
- core_role: Regression tests for workflow CLI error handling and headless start behavior.
- algorithmic_behavior: Tests ambiguous flow lookup and artifact package errors print without source stack traces, headless starts reject non-artifact workflow packages, frozen data resources pass to shell-script nodes, and SIGINT checkpoints headless starts instead of leaving runs alive.
- inputs_outputs_state: Inputs are mocked CLI args, workflow packages, resources, and process signals. Outputs are CLI output/errors, node resource payloads, checkpoints, and run state.
- gates_or_invariants: User-facing workflow errors omit internal stack traces; headless mode requires artifact packages; SIGINT creates checkpoint.
- dependencies_and_callers: Covers workflow CLI command module.
- edge_cases_or_failure_modes: Ambiguous lookup, bad package type, signal races, and resource freezing.
- validation_or_tests: The file itself validates workflow CLI behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-2999 `file` `packages/coding-agent/src/commit/git/diff.ts`
- cursor: `[_]`
- core_role: Git diff/numstat parser for commit tooling.
- algorithmic_behavior: Parses `git numstat` lines, extracts renamed paths, splits unified diff into file sections, counts additions/deletions excluding file headers, detects binary diffs, parses hunk headers, and returns per-file hunks.
- inputs_outputs_state: Inputs are raw numstat or unified diff text. Outputs are `NumstatEntry[]`, `FileDiff[]`, and `FileHunks[]`.
- gates_or_invariants: Binary files produce empty hunk lists; malformed hunk headers default positions to 0; NaN additions/deletions become 0; brace and `=>` rename syntax resolves to new path.
- dependencies_and_callers: Used by commit overview/hunk tools and agentic commit workflow.
- edge_cases_or_failure_modes: Binary diffs, renamed files with brace syntax, malformed headers, empty diff sections, and lines beginning with `+++`/`---`.
- validation_or_tests: Covered by commit workflow/tool tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3029 `file` `packages/coding-agent/src/eval/js/index.ts`
- cursor: `[_]`
- core_role: JavaScript eval backend registration object.
- algorithmic_behavior: Namespaces session ids with `js:`, declares backend availability, calls `executeJs`, resolves local eval URL roots, and maps JS result fields into generic executor backend result.
- inputs_outputs_state: Inputs are code and executor options. Outputs are backend result with output, exit code, truncation, artifact, line/byte counts, and display outputs.
- gates_or_invariants: Session ids are idempotently prefixed; backend is always available.
- dependencies_and_callers: Used by generic eval backend registry; calls `executor.ts` and backend URL root helpers.
- edge_cases_or_failure_modes: Already-prefixed session id, reset option propagation, local root propagation, and display output passthrough.
- validation_or_tests: Covered by eval backend tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3059 `file` `packages/coding-agent/src/extensibility/extensions/runner.ts`
- cursor: `[_]`
- core_role: Extension lifecycle and event hook runner.
- algorithmic_behavior: Initializes extensions with runtime/actions/UI contexts, buffers credential-disabled events before init, registers commands/shortcuts/contexts, dispatches lifecycle/session/tool/input/provider hooks, supports cancellation/mutation chains, and applies timeouts.
- inputs_outputs_state: Inputs are extension manifests/modules, events, session/tool/provider contexts, and settings. Outputs are diagnostics, registered commands/shortcuts/contexts, transformed inputs/tool results, blocked tool calls, and provider hook changes.
- gates_or_invariants: Generic hook timeout is 30s; session shutdown timeout is 2s; pre-init credential-disabled buffer caps at 32; reserved shortcut chords are filtered; `session_before` can cancel; `tool_call` failure blocks; input handlers short-circuit when handled.
- dependencies_and_callers: Used by extensibility subsystem, capability registry, and extension dashboard.
- edge_cases_or_failure_modes: Extension init failure, no-op UI fallback, shortcut conflicts, buffered events overflow, handler throws/timeouts, context cloning, and tool/provider hook mutation ordering.
- validation_or_tests: Covered by extension tests including TypeBox remap and dashboard behavior.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3089 `file` `packages/coding-agent/src/mcp/transports/index.ts`
- cursor: `[_]`
- core_role: Barrel export for MCP HTTP and stdio transports.
- algorithmic_behavior: Re-exports `./http` and `./stdio`.
- inputs_outputs_state: Input is module resolution; output is transport API surface.
- gates_or_invariants: No runtime logic; keeps transport imports stable.
- dependencies_and_callers: Used by MCP manager/server connection code.
- edge_cases_or_failure_modes: Export drift if transport modules move.
- validation_or_tests: Covered by MCP import/type tests.
- skip_candidate: `yes: barrel-only file; transport algorithms live in sibling modules`

### OH_MY_HUMANIZE_MAIN-HZ-3119 `file` `packages/coding-agent/src/modes/components/footer.ts`
- cursor: `[_]`
- core_role: TUI footer/status component.
- algorithmic_behavior: Renders working directory, git branch, tokens, cost, premium, context usage, model, thinking, and extension statuses; watches git HEAD/reftable; truncates to width using visible-width utilities; sanitizes extension status text.
- inputs_outputs_state: Inputs are session/status settings, cwd, git info, token/cost stats, model/thinking state, and extension statuses. Outputs are footer rows.
- gates_or_invariants: Git watchers/probes depend on visible enabled segments; text is width-safe; extension status content is sanitized/truncated.
- dependencies_and_callers: Used by interactive mode TUI and status line cache tests.
- edge_cases_or_failure_modes: Non-git directories, reftable HEAD, narrow terminal, hidden git segments, long extension status, and stale branch watchers.
- validation_or_tests: Covered by status-line settings cache tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3149 `file` `packages/coding-agent/src/modes/components/snapcompact-shape-preview.ts`
- cursor: `[_]`
- core_role: TUI preview component for snapcompact shape settings.
- algorithmic_behavior: Resolves current snapcompact shape, rasterizes sample text at 128px, nearest-neighbor zooms, limits image dimensions, uses Kitty unicode-placeholder rendering, and caches previews by shape/image id.
- inputs_outputs_state: Inputs are settings/model shape info and sample text. Outputs are rendered preview image rows or fallback text.
- gates_or_invariants: Source frame is 128px; zoom scale is 4; max image bounds are 28 cols by 14 rows; only Kitty unicode-placeholder image path is used.
- dependencies_and_callers: Depends on snapcompact rasterizer and TUI image support; used in settings/mode UI.
- edge_cases_or_failure_modes: Unsupported terminal image protocol, shape resolution failure, cache staleness, and oversized preview.
- validation_or_tests: Covered by snapcompact and TUI component tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3179 `file` `packages/coding-agent/src/modes/controllers/todo-command-controller.ts`
- cursor: `[_]`
- core_role: `/todo` command parser/controller for task list workflows.
- algorithmic_behavior: Tokenizes quoted args; supports show/edit/copy/export/import/append/start/done/drop/rm; fuzzy matches phases/tasks; builds system reminders; reads latest branch todos; commits state changes with custom messages and optional reminders.
- inputs_outputs_state: Inputs are command strings, todo phases/items, branch/session context, clipboard/export/import data. Outputs are updated todo entries, UI messages, copied/exported data, and system reminders.
- gates_or_invariants: Unknown/ambiguous phase/task queries are handled; quoted strings stay grouped; latest branch entry is used for current todos.
- dependencies_and_callers: Used by interactive slash command controller and todo storage.
- edge_cases_or_failure_modes: Empty commands, fuzzy collisions, missing todos, malformed import data, and branch-specific todo history.
- validation_or_tests: Covered by mode/controller tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3209 `file` `packages/coding-agent/src/slash-commands/helpers/stats-dashboard.ts`
- cursor: `[_]`
- core_role: Slash-command helper to start/open/stop the local stats dashboard.
- algorithmic_behavior: Parses `--port`, `-p`, and `--port=` flags, defaults to 3847, syncs sessions, starts singleton stats server, opens URL, and stops server/DB on request.
- inputs_outputs_state: Inputs are slash args and current singleton state. Outputs are server URL/open action or stopped state.
- gates_or_invariants: If server is already running, a different requested port is ignored in favor of existing singleton; DB is closed on stop.
- dependencies_and_callers: Uses stats server/sync modules and slash command runtime.
- edge_cases_or_failure_modes: Invalid port, already-running server, sync failure, browser open failure, and stop without running server.
- validation_or_tests: Covered by stats dashboard helper tests where present.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3239 `file` `packages/coding-agent/src/web/scrapers/discourse.ts`
- cursor: `[_]`
- core_role: Special web scraper for Discourse forum topic/post URLs.
- algorithmic_behavior: Parses `/t/.../<topicId>` and `/posts/<postId>` paths, fetches Discourse JSON API with `include_raw=1`, resolves post to topic when needed, formats topic metadata/category/tags/creator, converts cooked HTML to markdown if raw missing, and emits up to 20 posts.
- inputs_outputs_state: Inputs are URL, timeout, abort signal, and loaded JSON/HTML fields. Outputs are markdown render result with metadata notes.
- gates_or_invariants: Nonmatching paths return null; invalid fetch/JSON/title returns null; requested post is prepended if absent from topic stream; posts cap is 20.
- dependencies_and_callers: Used by web read/scraper special handler chain; depends on `loadPage`, `tryParseJson`, and HTML-to-markdown helper.
- edge_cases_or_failure_modes: Post URLs without topic id, missing raw/cooked body, category variants, malformed JSON, and private/unavailable topics.
- validation_or_tests: Covered by web scraper/read tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3269 `file` `packages/coding-agent/src/web/scrapers/openlibrary.ts`
- cursor: `[_]`
- core_role: Special web scraper for Open Library work/book/ISBN URLs.
- algorithmic_behavior: Detects work, edition, and ISBN paths; fetches Open Library APIs; resolves author names in parallel with cap 5; formats book metadata, covers, descriptions, subjects; ISBN path falls back from books API to search API and to unavailable markdown.
- inputs_outputs_state: Inputs are Open Library URL, timeout, abort signal, and API JSON. Outputs are markdown render result.
- gates_or_invariants: Host must include `openlibrary.org`; author fetch timeout is min(original,5); subjects cap at 20; malformed/unmatched URLs return null.
- dependencies_and_callers: Used by web scraper special handler chain; depends on `loadPage` and `tryParseJson`.
- edge_cases_or_failure_modes: Missing book key, API unavailable, malformed description object, absent author names, ISBN lookup miss, and cover variants.
- validation_or_tests: Covered by web scraper/read tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3299 `file` `packages/coding-agent/src/web/scrapers/wikipedia.ts`
- cursor: `[_]`
- core_role: Special web scraper for Wikipedia article URLs.
- algorithmic_behavior: Matches `*.wikipedia.org/wiki/<title>`, fetches REST summary and mobile HTML, parses sections with `linkedom`, skips reference-like sections, extracts headings/paragraphs, and builds markdown result.
- inputs_outputs_state: Inputs are URL, timeout, abort signal, summary JSON, and mobile HTML. Outputs are markdown render result.
- gates_or_invariants: Only `/wiki/` paths are handled; empty result returns null; known noise sections are skipped; paragraphs shorter than 20 chars are ignored.
- dependencies_and_callers: Used by web read/scraper special handler chain; dynamically imports `linkedom`.
- edge_cases_or_failure_modes: Summary failure but content success, content failure but summary success, non-article paths, localized hostnames, and malformed HTML/JSON.
- validation_or_tests: Covered by web scraper/read tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3329 `file` `packages/coding-agent/test/modes/components/session-selector-scrollbar.test.ts`
- cursor: `[_]`
- core_role: Regression tests for session selector scrollbar rendering.
- algorithmic_behavior: Verifies scrollbar thumb renders when sessions overflow viewport and is omitted when all sessions fit.
- inputs_outputs_state: Inputs are session lists and viewport height. Outputs are rendered selector rows with/without ScrollView thumb.
- gates_or_invariants: Scrollbar visibility is tied to actual overflow, not list existence.
- dependencies_and_callers: Covers session selector component.
- edge_cases_or_failure_modes: Exact-fit list and overflow threshold.
- validation_or_tests: The file itself validates component behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3359 `file` `packages/coding-agent/test/modes/controllers/omfg-controller.test.ts`
- cursor: `[_]`
- core_role: Regression tests for OMFG rule-generation controller.
- algorithmic_behavior: Tests saving a generated matching rule under project rules and registering live, reiterating when first valid rule does not match history, asking before save when validation never confirms, user amendment in save selector, empty/missing-model guards, and Escape abort/clear.
- inputs_outputs_state: Inputs are complaints, generated rule markdown, history, model replies, and key events. Outputs are saved rule files, live registry updates, selector prompts, and aborted requests.
- gates_or_invariants: Rule must match history before automatic save; missing model/empty complaint blocks model calls; Escape aborts inner request.
- dependencies_and_callers: Covers OMFG mode controller, rule registry, and model invocation.
- edge_cases_or_failure_modes: Valid but nonmatching rule, amended rule selection, no validation match, and abort cleanup.
- validation_or_tests: The file itself validates OMFG behavior; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3389 `file` `packages/coding-agent/test/web/search/anthropic.test.ts`
- cursor: `[_]`
- core_role: Regression tests for Anthropic web search request metadata.
- algorithmic_behavior: Verifies API-key auth forwards raw session id as `metadata.user_id`, while OAuth auth builds Claude-Code-shaped metadata user id.
- inputs_outputs_state: Inputs are auth storage variants, session id, and captured fetch body. Outputs are Anthropic request body metadata.
- gates_or_invariants: Metadata shape differs intentionally by auth type; raw session id is preserved for API-key auth.
- dependencies_and_callers: Covers `searchAnthropic` provider.
- edge_cases_or_failure_modes: OAuth vs API-key identity mismatch and missing session id formatting.
- validation_or_tests: The file itself validates Anthropic search request body; no test run was performed.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3419 `file` `packages/collab-web/src/tool-render/tools/irc.tsx`
- cursor: `[_]`
- core_role: Web tool renderer for inter-agent `irc` messaging results.
- algorithmic_behavior: Parses receipts, waited messages, inbox messages, and peer lists from tool result details; sorts peers by status; renders summary badges for send/wait/inbox/list; renders body rows with outcome/status tones and fallback result text.
- inputs_outputs_state: Inputs are tool args/result details. Outputs are React nodes for summary/body.
- gates_or_invariants: Malformed receipt/msg/peer entries are ignored; peer status order is running, idle, parked, then unknown; result errors still show fallback text; message summaries are whitespace-normalized/truncated.
- dependencies_and_callers: Used by collab-web tool-render registry.
- edge_cases_or_failure_modes: Timed-out wait, empty inbox, unread peer counts, unknown statuses, failed receipts, and malformed result detail shapes.
- validation_or_tests: Covered by collab tool render tests/snapshots where present.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3449 `file` `packages/stats/src/client/app/AppLayout.tsx`
- cursor: `[_]`
- core_role: Stats dashboard layout component with desktop/mobile navigation state.
- algorithmic_behavior: Maintains mobile drawer open state, routes section changes through parent callback, closes drawer after navigation, and wires `TopBar` range/sync/menu callbacks around content.
- inputs_outputs_state: Inputs are active section, time range, update timestamp, sync callbacks, and children. Outputs are React layout, nav changes, and mobile drawer state.
- gates_or_invariants: Overlay click closes mobile drawer; drawer content stops propagation; dialog uses accessibility labels; desktop nav stays separate from mobile drawer.
- dependencies_and_callers: Used by stats dashboard client app; depends on `NavRail` and `TopBar`.
- edge_cases_or_failure_modes: Mobile drawer lingering after section change and overlay click propagation.
- validation_or_tests: Covered by stats UI tests if present.
- skip_candidate: `yes: primarily UI layout/state, not core algorithmic runtime behavior`

### OH_MY_HUMANIZE_MAIN-HZ-3479 `file` `packages/stats/src/client/ui/RequestDrawer.tsx`
- cursor: `[_]`
- core_role: Stats dashboard request-detail drawer.
- algorithmic_behavior: Fetches request details by id with abortable effect, manages loading/error/details state, focuses close button on load, restores previous focus on close, closes on Escape or overlay click, and renders usage/duration/error JSON details.
- inputs_outputs_state: Inputs are request id and close callback. Outputs are drawer UI, fetched details state, and focus changes.
- gates_or_invariants: `id === null` renders nothing and clears details; aborted fetch does not set state; Escape listener is removed on cleanup; focus returns to previous active element.
- dependencies_and_callers: Uses stats client API, formatters, `JsonBlock`, `Skeleton`, and `StatusPill`.
- edge_cases_or_failure_modes: Request id changes mid-fetch, fetch error, abort during load, missing duration/TTFT, and focus restoration.
- validation_or_tests: Covered by stats UI tests if present.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3509 `file` `packages/coding-agent/src/commit/agentic/tools/index.ts`
- cursor: `[_]`
- core_role: Tool factory for agentic commit workflow.
- algorithmic_behavior: Creates an ordered custom tool list for commit analysis: overview, file diff, hunk, recent commits, optional analyze, propose changelog, propose commit, and split commit.
- inputs_outputs_state: Inputs are commit workflow context/options. Outputs are tool definitions exposed to the commit agent.
- gates_or_invariants: Tool order is stable; optional analyze tool is included only when configured.
- dependencies_and_callers: Used by agentic commit session runner.
- edge_cases_or_failure_modes: Missing optional analyzer, tool ordering assumptions in prompt/workflow, and context propagation.
- validation_or_tests: Covered by commit workflow tests.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3539 `file` `packages/coding-agent/src/modes/components/extensions/extension-dashboard.ts`
- cursor: `[_]`
- core_role: Interactive TUI dashboard for extension/provider enablement and diagnostics.
- algorithmic_behavior: Loads extension/capability state, presents tabbed provider/extension views, filters/searches, persists toggles to settings, handles resize/fullscreen rendering, skips empty enabled-provider tabs, and processes keyboard navigation/toggle/escape.
- inputs_outputs_state: Inputs are capability/extension registry state, settings, terminal size, search text, and key events. Outputs are dashboard rows, selected tab/index, and persisted enable/disable settings.
- gates_or_invariants: Reserved footer controls are fixed; empty provider tabs can be skipped; settings persistence is the source of truth for toggles.
- dependencies_and_callers: Uses capability registry, settings, TUI container/components, and extension runtime diagnostics.
- edge_cases_or_failure_modes: No providers/extensions, resize while open, search filtering leaving no rows, disabled source state drift, and conflicting shortcuts.
- validation_or_tests: Covered by extension dashboard/controller tests where present.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3569 `file` `packages/coding-agent/src/web/search/providers/codex.ts`
- cursor: `[_]`
- core_role: OpenAI Codex web search provider using ChatGPT/Codex Responses SSE with `web_search`.
- algorithmic_behavior: Resolves Codex OAuth via central `AuthStorage`, derives ChatGPT account id from OAuth access/JWT, chooses configured/default model candidates, sends streaming Responses request with `web_search` tool, parses SSE text/items/annotations/usage, extracts fallback markdown/bare URL sources, rejects image-only placeholder answers, and retries next default model only for known unsupported-model 400s.
- inputs_outputs_state: Inputs are search query, auth storage, session id, limit, system prompt, fetch override, and abort signal. Outputs are `SearchResponse` with answer, sources, usage, model, request id, or provider errors.
- gates_or_invariants: `AuthStorage` is sole refresh authority; `hasCodexSearch` is cheap `hasOAuth`; configured model is tried exactly once; fallback model list comes from bundled openai-codex catalog; source URLs are deduped/normalized.
- dependencies_and_callers: Used by web search provider chain; depends on Codex OAuth helpers, bundled model catalog, SSE JSON reader, and provider error classifier.
- edge_cases_or_failure_modes: Missing account id, refreshed credential differing from seed, unsupported ChatGPT model, no response body, SSE error/failed events, image placeholder prose, missing annotations, and URL punctuation/unbalanced delimiter trimming.
- validation_or_tests: `packages/coding-agent/test/web/search/codex-broker.test.ts` and web search provider tests cover broker auth/SSE behavior.
- skip_candidate: `no`

### OH_MY_HUMANIZE_MAIN-HZ-3599 `file` `packages/utils/src/vendor/mermaid-ascii/class/types.ts`
- cursor: `[_]`
- core_role: Vendored TypeScript interfaces for Mermaid ASCII class diagram parsing/layout.
- algorithmic_behavior: Defines class diagram, node, member, relationship, namespace, and positioned layout types.
- inputs_outputs_state: Inputs are compile-time class diagram structures; outputs are type contracts for parser/layout/render code.
- gates_or_invariants: No runtime logic; relationship/member shape is constrained by TypeScript interfaces.
- dependencies_and_callers: Used by vendored mermaid-ascii class diagram implementation.
- edge_cases_or_failure_modes: Type drift relative to parser/renderer expectations.
- validation_or_tests: Type checking validates consumers.
- skip_candidate: `yes: type-only vendored contract with no runtime algorithm`

## Worker Self-Test
- assigned_items_seen: 120
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`

---

## Incremental Refresh Addendum - oh-my-humanize/main bf4509d4f

# agent_delta_04 oh-my-humanize main incremental research

## Worker Summary
- status: `[_]`
- source_commit_old: `6b3819fad50a89fffae899b240ad1ce065c51d23`
- source_commit_new: `bf4509d4f5a669375b3c88510ba0449e9770884c`
- assigned_item_count: 1

## Item Evidence

### OH_MY_HUMANIZE_MAIN-HZ-2969 `file` `packages/coding-agent/src/cli/__tests__/workflow-cli.test.ts`
- cursor: `[_]`
- current_core_role: Bun test coverage for the workflow CLI entrypoint, especially direct `runWorkflowCommand()` behavior for `freeze` and headless `start`. The file builds temporary `.omhflow` artifact fixtures, resource directories, scripts, mocked stdout/stderr streams, and a fake SIGINT/SIGTERM target to validate user-visible CLI contracts without invoking the full command process.
- algorithmic_delta_since_old_commit: The current file adds the regression test `runs headless js workflow scripts from the requested cwd` at lines 147-176. It creates an artifact at `root/cwd-smoke.omhflow`, a frozen JS script resource at `root/cwd-smoke/scripts/read-cwd.js`, and a separate execution cwd at `root/workspace` containing `marker.txt`. The JS script reads `Bun.file("marker.txt")`, returns a structured activation output with `statePatch: [{ op: "set", path: "/marker", value: marker }]`, and the test expects a completed one-node run with `stateKeys: ["marker"]`. This proves the headless JS eval path uses the requested `flags.cwd`, not the artifact root, resource root, or original process cwd.
- algorithmic_delta_since_old_commit: The implementation path behind the new test is `handleStart()` in `workflow-cli.ts`: it resolves `cwd = path.resolve(command.flags.cwd ?? getProjectDir())`, passes that cwd into `createSessionWorkflowRuntimeHost()`, and binds JS script execution through `runHeadlessEvalScript(cwd, request.code, request.language)`. `runHeadlessEvalScript()` temporarily calls `process.chdir(cwd)`, captures `console.log`, runs the generated async function, serializes any returned value, then restores both `console.log` and the previous cwd in `finally`. That global chdir behavior is the current cwd contract the test pins.
- algorithmic_delta_since_old_commit: Related abort/deadline behavior at current head: `handleStart()` creates a signal controller for SIGINT/SIGTERM and passes the same signal as both `signal` and `nodeAbortSignal` into `runWorkflow()`, plus `maxRuntimeMs` from CLI flags or `DEFAULT_WORKFLOW_MAX_RUNTIME_MS`. `runWorkflow()` combines the user signal with a max-runtime timeout signal; the scheduler stops new activation scheduling on workflow abort, and running activations receive node abort signals. On abort with frontier nodes, lifecycle finish records a stopped attempt and creates a checkpoint.
- current_inputs_outputs_state: For the new cwd smoke, inputs are `action: "start"`, `args: [<absolute cwd-smoke.omhflow>]`, `flags: { cwd: <root>/workspace, json: true, runId: "js-cwd-smoke-run" }`. The script input is a frozen `language: js` file resource with declared write path `/marker`; the workspace state starts empty. Expected stdout is JSON whose `run` object matches `{ status: "completed", completed: 1, failed: 0 }`; persisted reconstructed run state contains key `marker`, sorted as `["marker"]`.
- current_inputs_outputs_state: Existing resource coverage remains for shell scripts: `workflowResourceSmokeFlow()` declares script and data resources, `workflowResourceSmokeScript()` reads `$OMP_WORKFLOW_RESOURCE_DIR/data/message.txt`, and the test expects state key `message`. This complements the JS cwd test by separating two contracts: frozen resource materialization via env for shell scripts, and requested cwd for JS relative file access.
- current_inputs_outputs_state: Existing SIGINT coverage starts a frozen artifact containing a shell script that sleeps for two seconds, emits SIGINT through `FakeWorkflowStartSignalTarget` after 30 ms, and expects JSON status `stopped`, run frontier `["hold"]`, lifecycle attempt status `stopped`, checkpoint frontier `["hold"]`, and no remaining SIGINT/SIGTERM listeners.
- new_or_changed_gates_or_invariants: Headless `workflow start` remains artifact-only. The test `rejects headless starts from non-artifact workflow packages` writes a raw `workflow.yml`, calls `start` on the directory, expects no stdout, and expects the friendly error `Workflow start requires a frozen .omhflow artifact` without leaking `workflow-cli.ts` or `WorkflowPackageError`.
- new_or_changed_gates_or_invariants: Headless JS relative paths are now pinned to the CLI `--cwd` / `flags.cwd` execution directory. If the implementation executes from the artifact directory or materialized resource directory, `Bun.file("marker.txt")` will miss the marker file and the test will fail.
- new_or_changed_gates_or_invariants: Structured script output must validate against declared writes and schema. The JS cwd fixture declares `stateSchema.shape.marker: string` and `writes: ["/marker"]`; the returned state patch is accepted only because it writes `/marker` with a string value.
- new_or_changed_gates_or_invariants: Signal cleanup is part of the contract. `createWorkflowStartSignalController()` registers one-shot SIGINT/SIGTERM listeners and `handleStart()` disposes them in `finally`; the fake signal target asserts both listener counts are zero after completion.
- new_or_changed_gates_or_invariants: Abort/checkpoint behavior distinguishes failure from interruption. A normal node exception becomes a failed activation and failed attempt; an exception while the node/workflow abort signal is active is recorded as an aborted activation, stops scheduling, and contributes the node id to checkpoint frontier.
- dependencies_and_callers: The test imports `runWorkflowCommand` and `WorkflowStartSignalTarget` from `packages/coding-agent/src/cli/workflow-cli.ts`, `TempDir` from `@oh-my-pi/pi-utils`, `path.delimiter` for `OMHFLOW_DIR`, and Bun test mocks for stdout/stderr. The production call chain for the new behavior is `runWorkflowCommand()` -> `handleStart()` -> `resolveWorkflowFlowSpec()` -> `loadWorkflowStartPackage()` -> `freezeWorkflowArtifact()` -> `createSessionWorkflowRuntimeHost()` -> `runWorkflow()` -> scheduler/node runtime -> `runHeadlessEvalScript()`.
- dependencies_and_callers: The JS script file is loaded as a frozen resource by `resolveScriptForExecution()` in `workflow/runner.ts`, not by reading the mutable source path at execution time when frozen resources exist. The workflow runner materializes frozen resources to a temporary directory for runtime resource access, while JS cwd behavior is separately controlled by `process.chdir(cwd)` in the headless eval adapter.
- dependencies_and_callers: Abort/deadline dependencies span `workflow-cli.ts`, `workflow/runner.ts`, `workflow/scheduler.ts`, `workflow/node-runtime.ts`, and `workflow/session-runtime.ts`. Shell and agent runners receive `request.signal` and pass it to `Bun.spawn`; the headless JS eval request currently has no signal field, so JS cancellation is enforced by the runner’s outer promise race and lifecycle bookkeeping rather than by a cooperative signal inside the JS code.
- edge_cases_or_failure_modes: If `marker.txt` is absent from the requested cwd, the JS script throws during `Bun.file("marker.txt").text()`, `runHeadlessEvalScript()` returns `exitCode: 1`, session runtime raises `WorkflowNodeRuntimeError`, and the scheduler records a failed activation unless an abort signal is active.
- edge_cases_or_failure_modes: `runHeadlessEvalScript()` mutates process-global cwd and `console.log` while a JS node runs. It restores them in `finally`, but concurrent headless JS script activations would share those globals; the new test only covers the single-node case.
- edge_cases_or_failure_modes: Max-runtime and SIGINT/SIGTERM aborts are propagated to scheduler and node abort signals. Shell subprocesses can be signaled through `Bun.spawn({ signal })`; headless JS eval is not directly signaled, so long-running async JS may continue until it settles, and synchronous blocking JS can delay abort handling because the event loop cannot run the abort timer.
- edge_cases_or_failure_modes: The fixtures include `checkpoint.stopDeadlineMs: 30000`, but this assigned test asserts stopped status/frontier, not the recorded stop deadline. Current `handleStart()` constructs lifecycle options without passing `freeze.checkpointPolicy.stopDeadlineMs`; in `runner.ts`, stop requests use `lifecycle.stopDeadlineMs ?? 0`, so deadline persistence is a coverage gap in this file.
- edge_cases_or_failure_modes: Headless py eval scripts remain unsupported by the CLI adapter; `runHeadlessEvalScript()` returns an error result for `language === "py"`. The new cwd coverage is specifically for JS workflow scripts.
- validation_or_tests: Current file validation includes friendly error tests for ambiguous flow lookup, package-loader errors, and non-artifact headless start; resource materialization for frozen shell scripts; the new requested-cwd behavior for frozen JS scripts; and SIGINT checkpointing for headless starts. The new test’s decisive assertion is `result.runs[0]?.stateKeys === ["marker"]`, because the only way to set that state is for the JS script to read `marker.txt` from `flags.cwd`.
- validation_or_tests: Related implementation validation is visible in current source: `workflow-cli.ts` lines 185-248 wires cwd, signal, runtime host, frozen resources, max runtime, and lifecycle into `runWorkflow()`; lines 417-447 implement JS cwd switching/restoration; lines 449-471 pass abort signal and resource env to shell scripts. `runner.ts` lines 148-171 create max-runtime abort signals, lines 219-271 create checkpoints for failed/stopped attempts, lines 342-429 persist completed/failed/aborted activations, and lines 432-460 race node execution against abort. `scheduler.ts` lines 100-219 stop scheduling and preserve frontier under abort.
- validation_or_tests: Tests were inspected in the read-only export; no source files were edited. I did not execute the Bun test suite as part of this research note.

- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `OH_MY_HUMANIZE_MAIN-HZ-2969`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`
