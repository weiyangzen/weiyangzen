# agent_dir_08 oh-my-humanize main directory incremental research

## Worker Summary
- status: `[_]`
- source_commit_old: `6b3819fad50a89fffae899b240ad1ce065c51d23`
- source_commit_new: `bf4509d4f5a669375b3c88510ba0449e9770884c`
- assigned_item_count: 1

## Item Evidence

### OH_MY_HUMANIZE_MAIN-HZ-320 `directory` `packages/coding-agent/test/workflow`
- cursor: `[_]`
- current_directory_core_role: `packages/coding-agent/test/workflow` is the workflow subsystem contract suite for the coding-agent package. It contains 26 package-local Bun tests covering workflow definition parsing, DSL compilation, conditions, scheduler behavior, state validation, artifact/freezing/package loading, prompts/resources, run/lifecycle stores, graph patches, inspection, node/session/eval/shell/human runtimes, benchmark/reference scenarios, and runner-level persistence. The changed descendant, `runner.test.ts`, is the directory’s main integration harness for `runWorkflow`: it executes definitions against an in-memory `WorkflowRunStoreHost`, then reconstructs run and lifecycle snapshots to assert externally observable activation, state, checkpoint, and model-audit contracts.
- directory_level_delta_since_old_commit: The directory’s responsibility expands from ordinary workflow execution/persistence coverage into stop/deadline safety coverage. New `runner.test.ts` cases pin that lifecycle attempts are checkpointed, not failed, when node execution is aborted by runtime deadlines, including the edge case where the runtime never resolves after receiving an abort. The broader change set also adds headless CLI cwd coverage outside this directory; within this directory the closest existing cwd/runtime contracts are shell script cwd in `shell-script-runtime.test.ts` and package-local script file loading in `runner.test.ts`. Together the test surface now distinguishes three roots: requested run cwd for headless script execution, workflow package root for loading declared script/resource files, and temporary materialized resource roots for frozen artifacts.
- affected_descendant_algorithms: `runner.test.ts` now exercises `runWorkflow` with `maxRuntimeMs`, `signal`, `nodeAbortSignal`, lifecycle metadata, frozen resources, and restart/checkpoint inputs. The new behavior is driven by `workflowRuntimeSignal()` combining the workflow stop signal and max-runtime timeout with node abort signals; `awaitWorkflowNodeExecution()` racing node execution against abort observation; `executeAndPersistActivation()` recording aborted activations when `context.nodeAbortSignal ?? context.signal` has an abort reason; `finishLifecycleAttempt()` treating aborted frontier work as a stoppable checkpoint; and `workflowCheckpointReason()` turning max-runtime/stop signals into checkpoint reasons. Related headless cwd behavior is in `workflow-cli.ts`: `handleStart()` resolves `--cwd`, builds `createSessionWorkflowRuntimeHost({ cwd, ... })`, runs eval scripts through `runHeadlessEvalScript(cwd, ...)`, shell scripts through `Bun.spawn(..., { cwd })`, and agent tasks through `launch --cwd`.
- current_inputs_outputs_state: Inputs under test include YAML workflow definitions, in-memory custom-entry hosts, model-resolution fixtures, lifecycle options (`familyId`, `attemptId`, `freeze`, `runtimeBindingSnapshot`, optional stop deadline), abort controllers, `maxRuntimeMs`, package roots, frozen resource snapshots, completed activations, and start-parent activation ids. Observable outputs are scheduler activation arrays/statuses/errors, reconstructed run state and activation records, lifecycle attempt status, checkpoint ids, completed/aborted activation ids, frontier node ids, checkpoint state, source mappings, model audit metadata, script inputs (`script`, `scriptLanguage`, `scriptPath`), and CLI JSON summaries for headless starts outside this directory.
- new_or_changed_gates_or_invariants: A node abort signal is distinct from the scheduler stop signal and is passed to node runtimes without causing already-running work to be mistaken for normal failure. When the workflow stop/deadline signal aborts a running node, the activation is persisted as `aborted`, the lifecycle attempt becomes `stopped`, and a checkpoint is created with the aborted activation id and the current frontier node. If a runtime ignores abort and never settles, runner-level abort handling must still return promptly and checkpoint instead of hanging indefinitely. `maxRuntimeMs` uses reason text `workflow max runtime elapsed after <n>ms`, and this reason is preserved on aborted lifecycle activation evidence. Schema-invalid activation output still fails before state patches are persisted; liveness guards still block fake script-only progress; activation-limit and cancellation stops still checkpoint with source mappings, including approved change-request mappings.
- dependencies_and_callers: This test directory depends on `src/workflow/runner.ts`, `scheduler.ts`, `run-store.ts`, `lifecycle.ts`, `node-runtime.ts`, `session-runtime.ts`, `state.ts`, `prompt-source.ts`, `freeze.ts`, `package-loader.ts`, `artifact-registry.ts`, and model resolution helpers from `@oh-my-pi/pi-catalog`. External callers affected by the same contracts include interactive workflow commands and headless `omp workflow start` in `src/cli/workflow-cli.ts`. The new headless cwd caller path is pinned by `src/cli/__tests__/workflow-cli.test.ts`, especially the JS script smoke flow that reads `marker.txt` from the requested `--cwd` while loading the script file from the frozen artifact resource directory.
- edge_cases_or_failure_modes: The critical failure mode now covered is a workflow stop or max-runtime deadline where the node runtime either throws after observing abort or ignores abort forever; both must produce stopped/checkpointed lifecycle evidence instead of a failed attempt or hung command. Other relevant edge cases in the current directory include scheduler cancellation after a completed upstream node, activation limit frontier checkpointing, script-only loops that do not make workflow progress, agent retry loops that legitimately hit activation limits, unavailable package roots for script files, script file path traversal outside package root, missing frozen script resources, unreadable script files, schema-rejected state writes, and review verdicts outside declared gates.
- validation_or_tests: New/changed validation in `runner.test.ts` includes `checkpoints deadline-aborted lifecycle activations instead of failing the attempt`, `checkpoints deadline-aborted lifecycle activations even when the runtime ignores abort`, and `checkpoints lifecycle attempts when max runtime elapses`. Existing nearby tests continue to validate activation-limit checkpointing, cancellation checkpointing, separate node abort signal propagation, restart prompt resolution from checkpointed parent activations, script file loading from package root, state patch persistence, and failure persistence. Related cwd coverage is outside this assigned directory in `src/cli/__tests__/workflow-cli.test.ts`: `runs headless js workflow scripts from the requested cwd` asserts a frozen JS workflow script can read `marker.txt` from `--cwd` and return structured state. Existing `shell-script-runtime.test.ts` inside this directory asserts shell scripts execute in the workflow cwd and preserve stdout for structured parsing.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `OH_MY_HUMANIZE_MAIN-HZ-320`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`