# agent_05 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-005 `directory` `docs`
- cursor: `[_]`
- core_role: Documentation surface for Humanize’s RLCR, planning, installation, monitoring, BitLesson, provider-routing, and operator-facing safety contracts. It is not executable code, but it defines user-visible algorithm contracts that sibling commands, scripts, hooks, config, and tests implement.
- algorithmic_behavior: The directory recursively contains:
  - `docs/usage.md`: canonical user workflow for the two-phase loop: Implementation Phase then Review Phase, where Codex review must reach no issues before completion (`docs/usage.md:5-12`). It also documents the Plan Understanding Quiz, including advisory behavior and skip modes (`docs/usage.md:14-41`), command options (`docs/usage.md:70-100`), gen-plan/refine-plan/ask-codex flows (`docs/usage.md:102-255`), config precedence (`docs/usage.md:256-307`), monitoring (`docs/usage.md:310-323`), and sandbox bypass risk (`docs/usage.md:329-364`).
  - `docs/bitlesson.md`: BitLesson contract for project memory. It defines config hierarchy (`docs/bitlesson.md:5-12`), provider routing from model names to Codex/Claude (`docs/bitlesson.md:14-23`), per-round selection and stop-gate validation workflow (`docs/bitlesson.md:25-34`), and the required summary delta shape plus strict validation rules (`docs/bitlesson.md:36-51`).
  - `docs/install-for-codex.md`: Codex install algorithm and expected installed state: skill sync, runtime bundle copy, native hook install, `codex_hooks` enablement, config seeding, `provider_mode: "codex-only"`, and default `gpt-5.5:high` RLCR model settings (`docs/install-for-codex.md:25-34`). It also defines verification surfaces (`docs/install-for-codex.md:36-81`) and troubleshooting gates (`docs/install-for-codex.md:102-115`).
  - `docs/install-for-kimi.md`: Kimi install algorithm for four skills (`docs/install-for-kimi.md:5-15`), runtime bundle placement (`docs/install-for-kimi.md:26-34`), manual copy steps including `scripts`, `hooks`, `prompt-template`, `templates`, `config`, and `agents` (`docs/install-for-kimi.md:44-64`), runtime-root hydration and frontmatter stripping (`docs/install-for-kimi.md:65-90`), RLCR usage/options (`docs/install-for-kimi.md:142-178`), and output locations (`docs/install-for-kimi.md:259-265`).
  - `docs/install-for-claude.md`: Claude Code installation modes, prerequisites, command verification, and monitor setup (`docs/install-for-claude.md:3-80`).
  - `docs/images/rlcr-workflow.svg`: executable-like visual spec for state flow: Plan to Implement, Implement to Review Summary, `COMPLETE` to Code Review, “No Issues” to Done, and two feedback loops back to implementation (`docs/images/rlcr-workflow.svg:3-17`, `docs/images/rlcr-workflow.svg:71-75`). It also depicts Agent Teams/swarm-style parallel workers (`docs/images/rlcr-workflow.svg:37-55`).
  - `docs/images/monitor.png`: screenshot asset of the RLCR monitor status bar showing session start, round, model/effort, push mode, status, AC/task/issue counts, and git cleanliness.
- inputs_outputs_state: Inputs are operator choices and environment state documented by the guides: target runtime (`Claude`, `Codex`, `Kimi`), `$CODEX_HOME`, `$XDG_CONFIG_HOME`, skill directories, plan file paths, RLCR flags, model config, BitLesson KB state, and monitor state. Outputs are installed skills/runtime bundles/hooks/config, generated `.humanize/rlcr/<timestamp>/` loop state, `.humanize/skill/<timestamp>/` one-shot results, `.humanize/bitlesson.md`, and monitor-visible status fields.
- gates_or_invariants: Important invariants documented here include: one plan enters the RLCR loop; quiz can be skipped but is designed as a comprehension friction point; config precedence is defaults < user < project < CLI (`docs/usage.md:258-263`); Codex installs force BitLesson to Codex/OpenAI path when `provider_mode` is `codex-only`; BitLesson delta must use `Action: none|add|update`; add/update actions require concrete existing IDs; native Codex hooks require Codex CLI `0.114.0+`; sandbox bypass is explicitly dangerous and opt-in.
- dependencies_and_callers: Coordinates with `commands/start-rlcr-loop.md`, `scripts/setup-rlcr-loop.sh`, `scripts/install-skill.sh`, `scripts/install-skills-codex.sh`, `scripts/install-skills-kimi.sh`, `scripts/install-codex-hooks.sh`, `scripts/bitlesson-select.sh`, `scripts/bitlesson-validate-delta.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/loop-bash-validator.sh`, `skills/humanize/SKILL.md`, `config/default_config.json`, and README links. The docs are consumed by humans and encode expected behavior for tests such as install, config, BitLesson, monitor, and agent-team suites.
- edge_cases_or_failure_modes: Documented edge cases include missing provider binary falling back for BitLesson selection (`docs/bitlesson.md:19`), Codex-only installs overriding older Claude defaults (`docs/bitlesson.md:21-23`), quiz bypass through `--skip-quiz` or `--yolo`, Kimi skill reload needing restart, output/cache directories needing write access, and unsafe sandbox bypass allowing unrestricted filesystem access (`docs/usage.md:331-356`).
- validation_or_tests: The docs themselves are not directly run as tests, but their contracts are exercised by `tests/test-codex-hook-install.sh`, `tests/test-config-merge.sh`, `tests/test-config-error-handling.sh`, `tests/test-bitlesson-select-routing.sh`, `tests/test-bitlesson-validate-delta.sh`, `tests/test-skill-monitor.sh`, `tests/test-agent-teams.sh`, and CI via `.github/workflows/run-all-tests.yml`.
- skip_candidate: `no`

### HZ-035 `file` `commands/start-rlcr-loop.md`
- cursor: `[_]`
- core_role: Claude command workflow definition for starting the RLCR loop. It is the pre-flight and user-interaction layer in front of `scripts/setup-rlcr-loop.sh`.
- algorithmic_behavior: The command defines allowed tools and argument surface in frontmatter (`commands/start-rlcr-loop.md:1-9`). Before setup it performs a plan-compliance pre-check unless `--skip-impl` or help is requested (`commands/start-rlcr-loop.md:13-20`). It extracts the plan path from `--plan-file` or the first safe positional argument (`commands/start-rlcr-loop.md:21-27`), applies a basic relative-path/safe-character gate before reading (`commands/start-rlcr-loop.md:28-40`), and invokes `humanize:plan-compliance-checker` to return one of `PASS`, `FAIL_RELEVANCE`, or `FAIL_BRANCH_SWITCH` (`commands/start-rlcr-loop.md:41-56`). It then runs a Plan Understanding Quiz unless skipped by mode/flags or unavailable plan content (`commands/start-rlcr-loop.md:60-70`), invokes `humanize:plan-understanding-quiz` for exactly 13 output fields (`commands/start-rlcr-loop.md:71-87`), asks two multiple-choice questions, and either proceeds or stops based on user choice after wrong answers (`commands/start-rlcr-loop.md:88-106`). If checks pass or are skipped, it runs the setup script with original arguments (`commands/start-rlcr-loop.md:109-115`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`, the referenced plan file content, user answers to `AskUserQuestion`, and flags such as `--skip-impl`, `--yolo`, `--skip-quiz`, `--plan-file`, `--max`, `--codex-model`, `--codex-timeout`, `--base-branch`, `--agent-teams`, and `--push-every-round`. Outputs are either a stop/error message, a user-facing quiz/summary, or execution of `setup-rlcr-loop.sh`. Downstream state is created by `scripts/setup-rlcr-loop.sh`: `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, `round-0-summary.md`, `round-0-contract.md`, `round-0-prompt.md`, and `.humanize/bitlesson.md`.
- gates_or_invariants: The pre-check fails closed on relevance failure, branch-switch instructions, or malformed compliance output (`commands/start-rlcr-loop.md:52-56`). The quiz is advisory, not a hard gate: wrong answers show `PLAN_SUMMARY` and ask whether to proceed (`commands/start-rlcr-loop.md:96-105`). Unsafe paths are not accepted by the markdown pre-read path but are delegated to the setup script’s stronger validator (`commands/start-rlcr-loop.md:28-36`). The command defines the semantic invariant that a “round” means the agent believes the entire plan is done, not a task or milestone boundary (`commands/start-rlcr-loop.md:130-138`). It also requires summaries, goal tracker maintenance, no state-file cheating, and BitLesson delta discipline (`commands/start-rlcr-loop.md:139-180`).
- dependencies_and_callers: Calls `scripts/setup-rlcr-loop.sh`; uses `agents/plan-compliance-checker.md` and `agents/plan-understanding-quiz.md`; downstream setup uses `hooks/lib/loop-common.sh`, `scripts/bitlesson-init.sh`, `prompt-template/claude/agent-teams-core.md`, and hook/runtime templates. The command is documented by `docs/usage.md`, `docs/install-for-claude.md`, `docs/install-for-codex.md`, `docs/install-for-kimi.md`, and `skills/humanize/SKILL.md`.
- edge_cases_or_failure_modes: No plan path means the pre-check is skipped and setup handles the error unless `--skip-impl` is used. Unreadable plan content skips the markdown pre-check but is still rejected by setup. Malformed quiz output only warns and continues (`commands/start-rlcr-loop.md:86-87`), while malformed compliance output stops. `--yolo` skips the quiz and also maps to `--claude-answer-codex` at setup-script parsing. `--skip-impl` bypasses implementation-plan validation and starts review mode.
- validation_or_tests: Setup-script behavior behind this command is covered by `tests/test-plan-file-validation.sh`, `tests/robustness/test-plan-file-robustness.sh`, `tests/robustness/test-setup-scripts-robustness.sh`, `tests/test-agent-teams.sh`, `tests/test-unified-codex-config.sh`, `tests/test-task-tag-routing.sh`, and stop-hook tests. The named agents provide the exact verdict/quiz output contracts in `agents/plan-compliance-checker.md:48-63` and `agents/plan-understanding-quiz.md:57-85`.
- skip_candidate: `no`

### HZ-065 `file` `tests/run-all-tests.sh`
- cursor: `[_]`
- core_role: Repository-wide executable validation gate for Humanize behavior. It orchestrates all shell test suites, including RLCR setup, hooks, templates, config, BitLesson, monitor, skill, agent teams, and robustness tests.
- algorithmic_behavior: The script computes a bounded default parallelism from CPU count, capped at 8 and floored at 1 (`tests/run-all-tests.sh:19-31`), allows `HUMANIZE_TEST_JOBS` override with integer validation (`tests/run-all-tests.sh:33-37`), detects whether `wait -n` is available (`tests/run-all-tests.sh:39-44`), declares the full suite list (`tests/run-all-tests.sh:59-119`), and marks zsh-only tests separately (`tests/run-all-tests.sh:121-124`). It creates a temporary output directory and deletes it on exit (`tests/run-all-tests.sh:126-128`). If `codex` is not installed, it injects a minimal mock binary to satisfy setup tests that only need `command -v codex` (`tests/run-all-tests.sh:130-141`). It launches each suite in the background, records stdout/stderr, exit code, and elapsed time in per-suite files, and throttles active PIDs to `MAX_JOBS` (`tests/run-all-tests.sh:162-219`). It then waits, strips ANSI codes, extracts `Passed:` and `Failed:` counts, classifies failures by exit code or failed count, sorts display lines by elapsed time, prints skipped suites, and exits `1` if any suite failed (`tests/run-all-tests.sh:221-303`).
- inputs_outputs_state: Inputs are executable test files under `tests/`, optional `HUMANIZE_TEST_JOBS`, shell availability (`bash`, `zsh`), `codex` availability, `getconf`, and each suite’s printed pass/fail counters. State is local to associative arrays (`PIDS`, `SKIPPED`), `ACTIVE_PIDS`, and temp files under `OUTPUT_DIR`. Outputs are sorted per-suite status lines, aggregate pass/fail totals, detailed logs for failures, and process exit code `0` or `1`.
- gates_or_invariants: Invalid `HUMANIZE_TEST_JOBS` fails immediately. Missing suites and unavailable `zsh` for zsh-only tests are skipped rather than failed. A suite fails if either its process exit code is nonzero or its parsed `Failed:` count is greater than zero (`tests/run-all-tests.sh:241-255`). ANSI stripping must be portable across GNU/BSD sed (`tests/run-all-tests.sh:246-249`). Parallel execution is assumed safe because each suite should isolate itself in temp directories (`tests/run-all-tests.sh:7-8`).
- dependencies_and_callers: Called by CI workflow `.github/workflows/run-all-tests.yml`, which installs `zsh` and `jq`, chmods the runner, and executes it on push/PR (`.github/workflows/run-all-tests.yml:16-22`). Depends on all listed test scripts, `mktemp`, `sort`, `cut`, `sed`, `grep`, shell job control, and optionally `zsh`/`codex`.
- edge_cases_or_failure_modes: On old Bash without `wait -n`, it waits for the oldest PID (`tests/run-all-tests.sh:213-217`). If a suite omits `Passed:`/`Failed:` counters, parsed counts default toward zero, but nonzero exit still fails. If `codex` is missing, tests requiring only discovery can proceed via mock, but suites needing specific behavior create their own mocks. Missing test files are explicitly skipped and reported.
- validation_or_tests: The runner validates the repository by executing 40+ suites. Its own ANSI parsing contract is covered by `tests/test-ansi-parsing.sh`, which tests color stripping and pass/fail count extraction (`tests/test-ansi-parsing.sh:38-95`). CI exercises the runner on every branch push and PR.
- skip_candidate: `no`

### HZ-095 `file` `tests/test-skill-monitor.sh`
- cursor: `[_]`
- core_role: Executable specification for the skill monitor algorithm, specifically `_humanize_monitor_skill --once`, which summarizes `.humanize/skill` one-shot invocations from `ask-codex` and `ask-gemini`.
- algorithmic_behavior: The test creates an isolated git repo and sources `scripts/humanize.sh`, which in turn sources `scripts/lib/monitor-skill.sh` (`tests/test-skill-monitor.sh:41-54`, `scripts/humanize.sh:1243-1246`). It defines `create_skill_invocation`, which writes timestamped `.humanize/skill/<id>/input.md`, optional `metadata.md`, and optional `output.md` (`tests/test-skill-monitor.sh:56-104`). It then verifies monitor behavior for missing skill directory, empty directory, a single successful invocation, multiple mixed-status invocations, running invocation with no metadata, recent invocation listing, first-line question extraction, empty response display, and ignoring non-timestamp directories (`tests/test-skill-monitor.sh:106-389`). It prints counters and exits nonzero if any failures remain (`tests/test-skill-monitor.sh:391-404`).
- inputs_outputs_state: Inputs are synthetic `.humanize/skill` directories named like `YYYY-MM-DD_HH-MM-SS-...`, `input.md` with `## Question` and config fields, `metadata.md` with YAML-like status/model/effort/duration fields, and `output.md`. Outputs are `PASS`/`FAIL` lines plus final counts; monitored function output includes total/status counts, focused invocation, model, duration, question, watched output, and recent invocation list.
- gates_or_invariants: Valid invocation dirs must start with a timestamp pattern; non-matching dirs are ignored (`scripts/lib/monitor-skill.sh:77-90`). Absence of `metadata.md` means `running` (`scripts/lib/monitor-skill.sh:130-140`). Status counts recognize `success`, `error`, `timeout`, and `empty_response` (`scripts/lib/monitor-skill.sh:121-142`). Focus selection prefers newest invocation with watchable content and falls back to the newest invocation (`scripts/lib/monitor-skill.sh:97-117`). Question extraction returns the first non-empty line after `## Question` before the next `##` (`scripts/lib/monitor-skill.sh:145-152`). `--once` exits `1` when no valid invocations exist and `0` when a focusable invocation exists (`scripts/lib/monitor-skill.sh:339-425`).
- dependencies_and_callers: Directly depends on `scripts/humanize.sh`, `scripts/lib/monitor-skill.sh`, monitor common helpers, `git`, temp dirs, and the local shell. The monitored data is produced by `scripts/ask-codex.sh`, which writes `.humanize/skill/<unique-id>/input.md` and marks `Tool: codex` (`scripts/ask-codex.sh:205-238`), plus analogous Gemini flow.
- edge_cases_or_failure_modes: Missing `.humanize/skill` produces an explicit error (`tests/test-skill-monitor.sh:112-118`). Empty directory produces “No skill invocations found” (`tests/test-skill-monitor.sh:126-133`). Running invocations are metadata-less and still counted (`tests/test-skill-monitor.sh:249-267`). Empty responses must show no-output messaging (`tests/test-skill-monitor.sh:349-369`). Multi-line questions are truncated to the first question line, not later context (`tests/test-skill-monitor.sh:296-346`). Legacy unknown-tool invocations are treated as Codex when filtered by Codex in implementation (`scripts/lib/monitor-skill.sh:66-75`), though this file focuses on unfiltered `--once`.
- validation_or_tests: This file is itself a validation suite and is included in the global runner (`tests/run-all-tests.sh:100-101`). It validates the key non-interactive monitor path; interactive terminal redraw/tail behavior is implemented in `scripts/lib/monitor-skill.sh:428-527` but not covered here.
- skip_candidate: `no`

### HZ-125 `file` `prompt-template/block/git-push.md`
- cursor: `[_]`
- core_role: Block-message template for the RLCR Bash pre-tool validator when a user attempts `git push` while `push_every_round` is false.
- algorithmic_behavior: The template emits a concise block reason: commits should stay local, the loop handles local commits until completion, and pushing requires starting with `--push-every-round` (`prompt-template/block/git-push.md:1-9`). It is selected by `hooks/loop-bash-validator.sh` after parsing active loop state and seeing `push_every_round` is not true (`hooks/loop-bash-validator.sh:220-237`).
- inputs_outputs_state: Inputs are an active RLCR loop, parsed `state.md` field `push_every_round`, and a Bash command matching leading `git push`. The output is the rendered block text to stderr and hook exit `2`. It does not mutate repository state.
- gates_or_invariants: Default invariant is local-only commits unless `--push-every-round` is enabled. The template reinforces the setup-time flag as the only sanctioned transition into per-round push requirements. If the template cannot be loaded, the validator has a hardcoded fallback with the same semantics (`hooks/loop-bash-validator.sh:231-235`).
- dependencies_and_callers: Loaded through `hooks/lib/template-loader.sh` using `load_and_render_safe`, whose fallback behavior is explicitly designed for missing/empty templates (`hooks/lib/template-loader.sh:185-211`). The state value is written by `scripts/setup-rlcr-loop.sh` into `state.md` (`scripts/setup-rlcr-loop.sh:887`). The user-facing flag is documented in `commands/start-rlcr-loop.md:3`, `commands/start-rlcr-loop.md:113-115`, and `docs/usage.md:83`.
- edge_cases_or_failure_modes: If no active loop exists, the Bash validator allows commands before this check. If `push_every_round: true`, push is not blocked here. The detector checks commands beginning with `git push` after lowercasing (`hooks/loop-bash-validator.sh:228-236`), so unusual wrappers may be handled by other shell-safety checks rather than this exact template.
- validation_or_tests: Template loading is directly tested by `tests/test-template-loader.sh`, which loads `block/git-push.md` and expects “Git Push Blocked” (`tests/test-template-loader.sh:55-65`), and tests fallback-vs-template behavior (`tests/test-template-loader.sh:196-222`). Comprehensive template tests also verify the real template wins over fallback (`tests/test-templates-comprehensive.sh:455-463`).
- skip_candidate: `no`

### HZ-155 `file` `prompt-template/claude/agent-teams-core.md`
- cursor: `[_]`
- core_role: Shared Agent Teams prompt contract. It changes the RLCR implementation algorithm from direct implementation by the lead agent to delegation, coordination, conflict avoidance, and teammate review.
- algorithmic_behavior: The template defines the team leader role and forbids direct code/file edits by the leader (`prompt-template/claude/agent-teams-core.md:1-13`). It instructs the leader to split work into independent tasks, spawn team members using the Task tool with `team_name`, assign strict file ownership, sequence overlapping tasks via dependencies, track progress via TaskList, review teammate output, coordinate commits, optionally require plans for high-risk work, and enforce BitLesson discipline (`prompt-template/claude/agent-teams-core.md:14-24`). The Important section reiterates Task tool usage, monitoring/reassignment, merge/conflict handling, and no direct implementation (`prompt-template/claude/agent-teams-core.md:25-31`).
- inputs_outputs_state: Inputs are the RLCR state flag `agent_teams: true`, the implementation plan, current round context, goal tracker, round contract, and Codex feedback on continuation rounds. Outputs are behavioral instructions appended to generated prompts; no files are changed by the template itself. Downstream state effects occur when the leader spawns teams, collects work, commits changes, updates summaries, and moves the loop forward through normal stop-hook review.
- gates_or_invariants: Key invariants are strict leader/delegate separation, file-conflict prevention, cold-start context completeness, review before completion, and no parallel same-file edits. BitLesson selection remains required before each sub-task (`prompt-template/claude/agent-teams-core.md:23`). The setup script gates `--agent-teams` behind `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` (`scripts/setup-rlcr-loop.sh:381-397`).
- dependencies_and_callers: Injected in round 0 by `scripts/setup-rlcr-loop.sh` when `AGENT_TEAMS=true`, together with `agent-teams-instructions.md` (`scripts/setup-rlcr-loop.sh:1370-1379`). Injected in implementation-phase continuation prompts by `hooks/loop-codex-stop-hook.sh`, together with `agent-teams-continue.md`, but only before review phase (`hooks/loop-codex-stop-hook.sh:2183-2193`). Related docs/options are in `docs/usage.md:93-95` and `commands/start-rlcr-loop.md:3`.
- edge_cases_or_failure_modes: Missing core/header templates trigger fallback instructions rather than stopping setup (`scripts/setup-rlcr-loop.sh:1379-1390`, `hooks/loop-codex-stop-hook.sh:2193-2201`). Review phase intentionally does not include Agent Teams continuation even when `agent_teams` is true. The template warns that same-file teammate edits cause silent overwrite rather than merge conflict (`prompt-template/claude/agent-teams-core.md:18`), making ownership boundaries a critical invariant.
- validation_or_tests: `tests/test-agent-teams.sh` validates env-var gating and state recording (`tests/test-agent-teams.sh:31-120`), prompt injection when enabled and absence when disabled (`tests/test-agent-teams.sh:245-328`), existence/content of this core template (`tests/test-agent-teams.sh:346-365`), continuation prompt injection in implementation phase (`tests/test-agent-teams.sh:614-627`), preservation during drift recovery (`tests/test-agent-teams.sh:631-669`), and omission during review phase (`tests/test-agent-teams.sh:703-734`).
- skip_candidate: `no`

### HZ-185 `file` `skills/humanize/SKILL.md`
- cursor: `[_]`
- core_role: Installed skill-level workflow contract for Humanize. It is the user-visible skill instruction that binds RLCR, plan generation/refinement, ask-codex, goal tracking, monitoring, runtime-root hydration, and exit codes into a portable skill runtime.
- algorithmic_behavior: The frontmatter identifies the skill as non-user-invocable and disables model invocation (`skills/humanize/SKILL.md:1-6`). It declares the hydrated runtime root placeholder (`skills/humanize/SKILL.md:12-20`) and explains the core iterative review philosophy (`skills/humanize/SKILL.md:22-29`). It defines RLCR as two phases: implementation reviewed by Codex until `COMPLETE`, then code review using `[P0-9]` findings until no issues remain and Finalize Phase can run (`skills/humanize/SKILL.md:30-49`). It describes Generate Plan, command invocation forms, common options, cancel behavior, ask-codex, expected plan structure, Goal Tracker rules, important loop rules, directory structure, monitoring command, and exit codes (`skills/humanize/SKILL.md:50-229`).
- inputs_outputs_state: Inputs include plan files, rough drafts, annotated plans, CLI flags, Codex model/effort/timeout, base branch, full-review interval, tracking/push/agent-team/privacy flags, and the installed runtime root. Outputs include generated plans, refined QA docs, RLCR loop state under `.humanize/rlcr/<timestamp>/`, one-shot skill results under `.humanize/skill/<timestamp>/`, hook-mediated review prompts, summaries, review results, finalize artifacts, and command exit codes.
- gates_or_invariants: Invariants include: summaries must be written before exit; `goal-tracker.md` must remain up to date; state-file/cancel-command cheating is prohibited; the native Stop hook should be used for Codex exit gating; acceptance criteria anchor tasks; explicit deferrals require justification; full alignment checks occur every configured interval; and Codex CLI is a prerequisite (`skills/humanize/SKILL.md:152-178`). The plan structure requires ACs, positive/negative tests, path boundaries, dependencies, and implementation notes that avoid plan terminology leaking into code (`skills/humanize/SKILL.md:111-150`).
- dependencies_and_callers: Installed by `scripts/install-skill.sh`, which syncs skills and runtime bundle components, hydrates `{{HUMANIZE_RUNTIME_ROOT}}`, and strips Claude-specific frontmatter for target runtimes (`scripts/install-skill.sh:147-250`). It points to `scripts/setup-rlcr-loop.sh`, `scripts/cancel-rlcr-loop.sh`, `scripts/validate-gen-plan-io.sh`, and `scripts/ask-codex.sh` (`skills/humanize/SKILL.md:61-109`). Runtime hooks and scripts depend on the same `.humanize` directory layout documented at `skills/humanize/SKILL.md:180-203`.
- edge_cases_or_failure_modes: If runtime-root hydration is not performed, command examples retain an unusable placeholder. Older Codex builds without native hook support cannot satisfy the documented Stop-hook behavior. `--skip-impl` starts review-only mode without a plan. Model/effort defaults can be overridden by flags/config. `ask-codex.sh` has explicit exit codes for validation and timeout (`skills/humanize/SKILL.md:214-220`).
- validation_or_tests: Install/runtime behavior is covered by `tests/test-codex-hook-install.sh`, `tests/test-refine-plan.sh` bundle checks, and installer logic in `scripts/install-skill.sh`. RLCR behavior referenced by the skill is covered broadly by `tests/run-all-tests.sh`, especially setup, stop-hook, config, plan-file, agent-team, ask-codex, monitor, and robustness suites.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7/7 item sections present above, one section per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`