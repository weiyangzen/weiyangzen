# agent_06 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-006 `directory` `hooks`
- cursor: `[_]`
- core_role: Central RLCR hook runtime. The directory wires Claude/Codex hook events to validators and the stop-loop orchestrator, enforcing file-access boundaries, git cleanliness, branch/plan invariants, task completion gates, Codex review transitions, methodology-analysis transitions, and terminal state naming.
- algorithmic_behavior: `hooks/hooks.json:4-73` registers Claude plugin hooks for `UserPromptSubmit`, `PreToolUse` on `Write`/`Edit`/`Read`/`Bash`, `PostToolUse` on `Bash`, and `Stop`. `hooks/loop-codex-stop-hook.sh:61-83` finds the session’s active loop, then delegates background-task parking before normal stop gates. The stop hook runs schema, branch, plan, task, git, summary, round-contract, BitLesson, max-iteration, Codex-review, review-phase, finalize, methodology-analysis, and drift gates in sequence.
- inputs_outputs_state: Inputs are hook JSON from stdin, `session_id`, `transcript_path`, tool input fields, `.humanize/rlcr/<timestamp>/state.md` or phase state files, git status, Codex CLI output, and prompt templates. Outputs are hook decisions as JSON blocks, stderr block messages, generated prompt/summary/review files under the loop dir, cached Codex logs under XDG cache, and state-file renames such as `finalize-state.md`, `complete-state.md`, `stop-state.md`, `maxiter-state.md`, or `unexpected-state.md`. Shared state parsing lives in `hooks/lib/loop-common.sh:442-599`; active-vs-terminal loop discovery is in `hooks/lib/loop-common.sh:264-425`; terminal rename is `end_loop` at `hooks/lib/loop-common.sh:1533-1566`.
- gates_or_invariants: JSON inputs reject null bytes, invalid UTF-8, invalid JSON, missing `tool_name`, and excessive depth via `validate_hook_input` and `is_deeply_nested` at `hooks/lib/loop-common.sh:91-165`. Path resolution refuses `pwd` drift and canonicalizes project roots via `hooks/lib/project-root.sh:41-53`. Read/write/edit validators enforce active-loop path ownership, current-round files, no prompt writes, no state-file writes, no plan backup writes, and immutable goal-tracker sections after round 0. Bash validator blocks shell bypasses for managed files, direct hook execution, `.humanize` git staging, unauthorized state moves, and write-capable commands during methodology analysis. Stop hook fails closed on malformed state, branch switching, git status failures, dirty tree, incomplete tasks, missing summaries, missing round contracts, missing mainline verdicts, Codex failures, tracked `.humanize` state, and review-phase marker inconsistencies.
- dependencies_and_callers: Hook entrypoints source `hooks/lib/loop-common.sh`, which sources `scripts/lib/config-loader.sh`, `hooks/lib/project-root.sh`, and `hooks/lib/template-loader.sh` (`hooks/lib/loop-common.sh:171-240`). Stop hook depends on `scripts/portable-timeout.sh`, `hooks/lib/methodology-analysis.sh`, `hooks/check-todos-from-transcript.py`, Codex CLI, git, jq, and templates under `prompt-template`. Native Codex config installs only the Stop hook, while Claude plugin config installs all validators. `hooks/lib/loop-bg-tasks.sh` is sourced at `hooks/lib/loop-common.sh:1568-1578`, making background-task helpers available to all consumers.
- edge_cases_or_failure_modes: Background tasks are fail-open for liveness when output file is absent or `lsof` is unavailable (`hooks/lib/loop-bg-tasks.sh:120-141`) and are filtered to loop-start timestamps to avoid pre-loop tasks pinning a loop (`hooks/lib/loop-bg-tasks.sh:179-185`). Cross-session parked loops exit without mutating foreign state (`hooks/lib/loop-bg-tasks.sh:338-367`). Methodology analysis blocks project reads/writes but allows only methodology artifacts (`hooks/loop-read-validator.sh:86-168`, `hooks/loop-write-validator.sh:88-143`, `hooks/loop-edit-validator.sh:71-124`). Symlink and traversal concerns are handled with parent-only canonicalization for authorization (`hooks/lib/project-root.sh:55-109`) and explicit methodology fail-closed fallbacks. Git status timeout is a hard block using `prompt-template/block/git-status-failed.md` through `hooks/loop-codex-stop-hook.sh:495-515`.
- validation_or_tests: Tests in the assigned set cover active/terminal state naming and path matching (`tests/test-state-exit-naming.sh:38-291`) and base-branch detection logic consumed by setup (`tests/robustness/test-base-branch-detection.sh:35-64`). Broader repo tests referenced by search include hook install, disabled nested Codex hooks, plan-file hooks, agent-teams, state transition robustness, and stop-gate behavior.
- skip_candidate: `no`

### HZ-036 `file` `config/codex-hooks.json`
- cursor: `[_]`
- core_role: Native Codex hook configuration template. It is the Codex-side runtime config that routes Stop events into the RLCR stop hook.
- algorithmic_behavior: Defines one `Stop` hook group with a command placeholder pointing at `{{HUMANIZE_RUNTIME_ROOT}}/hooks/loop-codex-stop-hook.sh`, a 7200-second timeout, and status message `humanize RLCR stop hook` (`config/codex-hooks.json:4-12`). It deliberately does not register the Claude-specific pre-tool validators; Codex native integration only needs the Stop gate.
- inputs_outputs_state: Input is the template file plus installer-provided runtime root. Output is a merged Codex `hooks.json` entry after placeholder replacement. `scripts/install-codex-hooks.sh:94-110` JSON-escapes the runtime root before parsing, then `scripts/install-codex-hooks.sh:112-119` shell-quotes command paths to preserve spaces. Existing managed stop hooks are removed before appending the current managed Stop group (`scripts/install-codex-hooks.sh:138-165`).
- gates_or_invariants: Requires valid JSON object shape and a Stop hook command; installer rejects missing template at `scripts/install-codex-hooks.sh:71`. Installer requires Codex CLI and the `codex_hooks` feature (`scripts/install-codex-hooks.sh:75-82`) unless in dry-run flow after validation. Runtime root placeholder must be substituted before installation; leaving it unresolved would point Codex at a nonexistent hook.
- dependencies_and_callers: Consumed by `scripts/install-codex-hooks.sh` via `HOOKS_TEMPLATE="$REPO_ROOT/config/codex-hooks.json"` (`scripts/install-codex-hooks.sh:14`) and invoked during skill/plugin installation paths discovered by `scripts/install-skill.sh`. It routes to `hooks/loop-codex-stop-hook.sh`, which then depends on the hook libraries, templates, git, jq, timeout wrapper, and Codex CLI.
- edge_cases_or_failure_modes: Existing `hooks.json` may have non-object top-level or invalid Stop arrays; installer aborts for these (`scripts/install-codex-hooks.sh:120-137`). Runtime roots containing quotes/backslashes are JSON-escaped before `json.loads` (`scripts/install-codex-hooks.sh:105-110`), and paths with spaces are shell-quoted after parse (`scripts/install-codex-hooks.sh:112-119`). Duplicate old managed hooks are filtered by regex to avoid repeated stop-hook invocations (`scripts/install-codex-hooks.sh:138-156`).
- validation_or_tests: `tests/test-codex-hook-install.sh` is the direct installer test surface found by search; relevant assertions include writing `hooks.json` and enabling `codex_hooks` around `tests/test-codex-hook-install.sh:131-139`, plus unsupported-feature failure messaging around `tests/test-codex-hook-install.sh:326-330`. Nested review calls also disable Codex hooks when supported (`hooks/loop-codex-stop-hook.sh:1169-1181`), covered by `tests/test-disable-nested-codex-hooks.sh` references.
- skip_candidate: `no`

### HZ-066 `file` `tests/setup-monitor-test-env.sh`
- cursor: `[_]`
- core_role: Minimal test-environment helper stub for monitor command tests.
- algorithmic_behavior: Parses `TEST_DIR` as first argument and optional `TEST_NAME` as second (`tests/setup-monitor-test-env.sh:12-13`). If `TEST_DIR` is absent, prints usage and exits failure (`tests/setup-monitor-test-env.sh:15-18`). The only `case` arm rejects every `TEST_NAME` as unknown, prints that no names are available, and exits failure (`tests/setup-monitor-test-env.sh:20-26`). The final `echo "$TEST_DIR"` at `tests/setup-monitor-test-env.sh:28` is currently unreachable for all non-empty test names because there is no accepted case.
- inputs_outputs_state: Inputs are command-line args only. Intended output would be the test directory path; actual current output is usage or unknown-test diagnostics to stderr and exit code 1. It does not create directories, state files, monitor fixtures, or `.humanize/rlcr` artifacts despite the header claiming it sets up monitor test state (`tests/setup-monitor-test-env.sh:3-7`).
- gates_or_invariants: `set -euo pipefail` is active (`tests/setup-monitor-test-env.sh:10`). It requires a non-empty test dir. It has a closed enum of available test names, currently empty.
- dependencies_and_callers: No external dependencies beyond Bash. Search did not reveal current callers in the inspected assignment scope. It is related conceptually to monitor E2E tests but does not implement fixture creation today.
- edge_cases_or_failure_modes: Any invocation with a test name, including default, fails as unknown. Empty test dir fails with usage. The script’s comment is stale relative to implementation, so it is a skip/placeholder candidate for core behavior.
- validation_or_tests: None in this file beyond its own argument validation. It is an executable spec only in the sense that it documents currently unsupported setup cases by failing them.
- skip_candidate: `yes: helper is a placeholder/stub with no accepted monitor fixture scenarios and no current core algorithm behavior beyond argument rejection`

### HZ-096 `file` `tests/test-state-exit-naming.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR loop state-file naming, active-loop discovery, terminal-state inertness, and new `.humanize/rlcr` path boundary.
- algorithmic_behavior: Creates a temporary git repo and loop directory, sources `hooks/lib/loop-common.sh`, then exercises `find_active_loop`, `end_loop`, and `is_in_humanize_loop_dir`. Tests 1-6 verify terminal files such as `complete-state.md`, `cancel-state.md`, `unexpected-state.md`, `maxiter-state.md`, and `stop-state.md` do not count as active, while `state.md` does (`tests/test-state-exit-naming.sh:38-159`). Test 7 verifies the newest directory with active `state.md` wins (`tests/test-state-exit-naming.sh:161-179`). Tests 8-10 verify `end_loop` rejects invalid reasons, creates the correct terminal files for valid reasons, and returns failure for missing state files (`tests/test-state-exit-naming.sh:186-243`). Tests 11-13 verify only `.humanize/rlcr` paths are treated as loop paths and legacy `.humanize-loop.local` is ignored (`tests/test-state-exit-naming.sh:249-291`).
- inputs_outputs_state: Inputs are temporary fixture state files and helper function calls. Outputs are pass/fail counters and final exit code equal to failed test count (`tests/test-state-exit-naming.sh:293-302`). State transitions are simulated by creating/removing `state.md` and calling `end_loop`, which moves `state.md` to `<reason>-state.md`.
- gates_or_invariants: Only active filenames returned by `resolve_active_state_file` should revive loops: `methodology-analysis-state.md`, `finalize-state.md`, or `state.md` in implementation code (`hooks/lib/loop-common.sh:268-280`), with this test emphasizing that terminal names are inert. Valid `end_loop` reasons are exactly `complete`, `cancel`, `maxiter`, `stop`, and `unexpected` (`hooks/lib/loop-common.sh:1545-1553`). Legacy path separation prevents old runtime directories from being treated as active state.
- dependencies_and_callers: Depends on git, Bash, `mktemp`, and `hooks/lib/loop-common.sh` (`tests/test-state-exit-naming.sh:15-17`, `tests/test-state-exit-naming.sh:64-67`). It validates behavior consumed by all validators and the stop hook when they call `find_active_loop` or `end_loop`.
- edge_cases_or_failure_modes: Multiple terminal files in one dir must still not create an active loop. A newer active loop takes precedence. Missing state file in `end_loop` is a handled failure rather than silent success. The test uses its own counters rather than `tests/test-helpers.sh`.
- validation_or_tests: This file is itself the validation asset. It was inspected but not executed in this read-only research run.
- skip_candidate: `no`

### HZ-126 `file` `prompt-template/block/git-status-failed.md`
- cursor: `[_]`
- core_role: Block-message template for the stop hook’s fail-closed git-status gate.
- algorithmic_behavior: Renders a concise failure explanation when `git status --porcelain` fails or times out. Placeholder `{{GIT_STATUS_EXIT}}` is filled with the failing exit code (`prompt-template/block/git-status-failed.md:3`). The message lists likely causes: git lock contention, invalid repo state, or large repo slowness (`prompt-template/block/git-status-failed.md:5-8`) and instructs manual `git status` verification (`prompt-template/block/git-status-failed.md:10`).
- inputs_outputs_state: Input is `GIT_STATUS_EXIT` from `hooks/loop-codex-stop-hook.sh:498-511`. Output is a rendered Markdown block inserted into JSON `decision: block` at `hooks/loop-codex-stop-hook.sh:510-513`. It does not mutate state; it prevents state transition by blocking exit.
- gates_or_invariants: The invariant is fail-closed repository verification. If the hook cannot verify git cleanliness, it must not proceed to Codex review, finalize, or terminal state changes. The hook also calls `cleanup_stale_index_lock` before rendering this block (`hooks/loop-codex-stop-hook.sh:501-505`).
- dependencies_and_callers: Loaded via `load_and_render_safe` in `hooks/loop-codex-stop-hook.sh:510`. Rendering semantics come from `hooks/lib/template-loader.sh:56-136`, which performs single-pass placeholder substitution and keeps unresolved placeholders intact.
- edge_cases_or_failure_modes: Missing template falls back to inline text in the stop hook (`hooks/loop-codex-stop-hook.sh:505-509`). If template rendering fails, `load_and_render_safe` emits fallback content (`hooks/lib/template-loader.sh:188-210`). The template intentionally avoids remediation beyond manual status checking because the hook cannot safely infer repo state.
- validation_or_tests: Indirectly exercised by stop-hook tests that simulate git failures/timeouts; the concrete gate path is in `hooks/loop-codex-stop-hook.sh:495-515`. No direct standalone test for this template was assigned.
- skip_candidate: `no`

### HZ-156 `file` `prompt-template/claude/agent-teams-instructions.md`
- cursor: `[_]`
- core_role: Initial Agent Teams mode prompt header that changes RLCR execution from single-agent implementation to team-leader delegation.
- algorithmic_behavior: Instructs Claude it is the Team Leader in an RLCR cycle (`prompt-template/claude/agent-teams-instructions.md:1-3`). It tells the leader to read the implementation plan before creating the team and identifies required artifacts: plan file, `goal-tracker.md`, and final work summary (`prompt-template/claude/agent-teams-instructions.md:5-8`).
- inputs_outputs_state: Input is the `AGENT_TEAMS` state/config flag. During setup, when enabled, `scripts/setup-rlcr-loop.sh:1370-1378` appends this header plus `agent-teams-core.md` to `round-0-prompt.md`. Output is prompt text that affects task routing and delegation behavior but does not directly mutate loop state.
- gates_or_invariants: The template creates a behavioral invariant: initial team creation must be plan-grounded, goal-tracker-aware, and summary-producing. The stronger "delegate coding, do not implement yourself" enforcement is added by neighboring core/continuation templates and fallback code in setup (`scripts/setup-rlcr-loop.sh:1380-1389`) and later continuation injection in the stop hook (`hooks/loop-codex-stop-hook.sh:2092-2114`, `hooks/loop-codex-stop-hook.sh:2183-2203`).
- dependencies_and_callers: Called by `scripts/setup-rlcr-loop.sh` when `AGENT_TEAMS=true`; existence/content is checked by `tests/test-agent-teams.sh:330-344`. It coordinates with `prompt-template/claude/agent-teams-core.md`, `agent-teams-continue.md`, and the stop hook’s task-tag routing reminder (`hooks/loop-codex-stop-hook.sh:1405-1419`).
- edge_cases_or_failure_modes: If this template or the core template is missing, setup falls back to an inline Agent Teams block (`scripts/setup-rlcr-loop.sh:1374-1390`). If Agent Teams is disabled, tests assert the initial prompt should not contain team-leader instructions (`tests/test-agent-teams.sh:320-328`).
- validation_or_tests: `tests/test-agent-teams.sh:334-343` checks the template exists and is at least 50 bytes. Additional agent-team tests validate absence/presence of team-leader text depending on mode.
- skip_candidate: `no`

### HZ-186 `file` `tests/robustness/test-base-branch-detection.sh`
- cursor: `[_]`
- core_role: Robustness spec for setup-time base branch selection, which feeds later Codex review base selection.
- algorithmic_behavior: Defines `detect_base_branch` as a focused reproduction of setup logic: remote default from `git remote show origin`, then local `main`, then local `master`, otherwise failure (`tests/robustness/test-base-branch-detection.sh:37-64`). It then creates temporary repos to verify fallback to local main with no origin (`tests/robustness/test-base-branch-detection.sh:70-93`), fallback to local master when main is absent (`tests/robustness/test-base-branch-detection.sh:99-122`), failure when only `develop` exists (`tests/robustness/test-base-branch-detection.sh:128-149`), and acceptable detection with an origin remote (`tests/robustness/test-base-branch-detection.sh:155-190`).
- inputs_outputs_state: Inputs are temporary git repos, optional origin remote, and timeout seconds. Output is detected branch name on stdout or nonzero failure, plus test pass/fail counters through `tests/test-helpers.sh`. The selected branch becomes `base_branch` in RLCR state during real setup; real implementation captures `BASE_COMMIT` from that branch at loop start to avoid a moving-base diff (`scripts/setup-rlcr-loop.sh:807-815`).
- gates_or_invariants: Real setup priority is user input, remote default if local, local main, local master, then hard error (`scripts/setup-rlcr-loop.sh:743-797`). User-specified branch must exist locally; remote-only branches trigger a fetch instruction and failure (`scripts/setup-rlcr-loop.sh:748-764`). Branch names reject YAML-unsafe characters and newlines (`scripts/setup-rlcr-loop.sh:799-805`). The test uses `|| true` around remote detection to avoid `pipefail` terminating when origin is missing (`tests/robustness/test-base-branch-detection.sh:41-44`), matching the live implementation at `scripts/setup-rlcr-loop.sh:770-771`.
- dependencies_and_callers: Depends on git, `tests/test-helpers.sh`, and `scripts/portable-timeout.sh` (`tests/robustness/test-base-branch-detection.sh:16-24`). It validates setup behavior consumed later by `hooks/loop-codex-stop-hook.sh`, where `base_branch` is required for modern state schema (`hooks/loop-codex-stop-hook.sh:258-270`) and used for Codex review base selection together with `base_commit` (`hooks/loop-codex-stop-hook.sh:1214-1221`).
- edge_cases_or_failure_modes: Missing origin should not fail detection if local main/master exists. Remote default of `(unknown)` is ignored. Remote default that is not local is ignored in auto-detection and produces a clearer failure in real setup if no fallback exists. The test comment says it replicates older setup line numbers (`tests/robustness/test-base-branch-detection.sh:35`), but the live implementation is currently around `scripts/setup-rlcr-loop.sh:743-815`.
- validation_or_tests: This file is itself the robustness validation. It was inspected but not executed in this read-only research run.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 evidence headers present exactly once
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`