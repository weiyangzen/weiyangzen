# agent_11 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-011 `directory` `tests`
- cursor: `[_]`
- core_role: Recursive executable-spec tree for the Humanize/RLCR state machine, hooks, prompt/template rendering, config merge, monitor behavior, plan validation, stop/finalize phases, task routing, Codex/GitHub integration mocks, and robustness edge cases. Static inventory found 64 files under `tests`, including 16 robustness suites, 8 JSON fixtures, one `gh` mock, shared helpers, a parallel all-suite runner, and many feature-level shell specs.
- algorithmic_behavior: `tests/run-all-tests.sh` is the suite coordinator. It defines the canonical suite list at `tests/run-all-tests.sh:59`, throttles parallel jobs via `HUMANIZE_TEST_JOBS` at `tests/run-all-tests.sh:21`, creates a mock `codex` when unavailable at `tests/run-all-tests.sh:131`, runs zsh-only tests through zsh at `tests/run-all-tests.sh:121`, strips ANSI for pass/fail parsing at `tests/run-all-tests.sh:246`, aggregates totals at `tests/run-all-tests.sh:251`, and prints failing suite logs at `tests/run-all-tests.sh:286`. Shared assertions and temp repo setup live in `tests/test-helpers.sh:30`, `tests/test-helpers.sh:58`, `tests/test-helpers.sh:86`, and `tests/test-helpers.sh:93`.
- inputs_outputs_state: Inputs are shell test files, JSON fixtures, mock CLIs, temporary project directories, generated `.humanize/rlcr` loop states, mocked `codex` and `gh` binaries, and production scripts sourced from `hooks/`, `hooks/lib/`, and `scripts/`. Outputs are per-suite pass/fail counters, process exit status, temp artifacts, and runner aggregate status. Most tests create disposable state directories; no authoritative repo state is intended to persist.
- gates_or_invariants: The suite asserts hook JSON validation, state-file schema strictness, path canonicalization, no direct state/prompt/todos mutation, no Bash bypasses, clean stop/finalize transitions, no tracked `.humanize/` state, safe monitor cleanup, provider routing, and template availability. Robustness files explicitly cover malformed JSON, non-UTF8, symlink traversal, concurrent state reads, stale loop detection, base branch fallback, timeout behavior, and setup-script YAML/path safety.
- dependencies_and_callers: Main dependencies are Bash, zsh for `tests/test-zsh-monitor-safety.sh`, `jq`, `git`, temp dirs, `sed`, `grep`, and project scripts. `tests/mocks/gh` routes `gh pr`, `gh api`, `gh repo`, and `gh auth` commands from `tests/mocks/gh:27` through `tests/mocks/gh:246`, backed by fixtures under `tests/fixtures`. The test tree calls sibling/parent paths including `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, validators in `hooks/`, `scripts/humanize.sh`, setup/cancel scripts, and prompt templates.
- edge_cases_or_failure_modes: Suite-level edge cases include missing test files being skipped, zsh absent causing a zsh suite skip, runner output parse fragility from ANSI codes, missing `codex` handled by a mock, and broad filesystem churn in temp dirs. Algorithm-specific edge cases are distributed: `tests/robustness/test-state-file-robustness.sh` covers malformed or partial frontmatter, `tests/robustness/test-session-robustness.sh` covers multiple/deleted sessions, `tests/robustness/test-hook-input-robustness.sh` covers invalid and hostile hook JSON, and `tests/test-cancel-signal-file.sh` covers cancel authorization bypass attempts.
- validation_or_tests: The directory is itself the validation surface. Static evidence: `find tests -type f | xargs wc -l` reported 29,392 total lines, and `tests/run-all-tests.sh` enumerates the executable suite. I did not execute the full suite because this worker was asked to produce research notes only in a read-only checkout.
- skip_candidate: `no`

### HZ-041 `file` `hooks/hooks.json`
- cursor: `[_]`
- core_role: Hook routing manifest for the RLCR loop. It maps Claude hook events and tool matchers to the validator and stop-hook scripts that enforce the state machine boundaries.
- algorithmic_behavior: `UserPromptSubmit` invokes `loop-plan-file-validator.sh` at `hooks/hooks.json:4` to validate prompt/plan entry. `PreToolUse` dispatches `Write`, `Edit`, `Read`, and `Bash` to their matching validators at `hooks/hooks.json:14`, `hooks/hooks.json:24`, `hooks/hooks.json:33`, and `hooks/hooks.json:42`. `PostToolUse` dispatches Bash results to `loop-post-bash-hook.sh` at `hooks/hooks.json:52`. `Stop` dispatches `loop-codex-stop-hook.sh` at `hooks/hooks.json:63` with a 7200-second timeout at `hooks/hooks.json:69`.
- inputs_outputs_state: Input is Claude hook event JSON routed by event type and matcher. Output is whatever the target script emits: pass-through exit 0, blocking stderr for PreToolUse validators, or JSON block/allow decisions for stop-phase control. State transitions are delegated, especially to the Stop hook, which can advance normal, finalize, methodology-analysis, terminal, or blocked review paths.
- gates_or_invariants: The manifest enforces tool-specific validation before file/tool effects happen. It keeps Write/Edit/Read/Bash validation separate, so each validator can gate path, round, state, prompt, todos, and Bash bypass behavior. The Stop hook is long-running by design because Codex review and methodology analysis may need time.
- dependencies_and_callers: Depends on `CLAUDE_PLUGIN_ROOT` resolving to the plugin root, since every command is `${CLAUDE_PLUGIN_ROOT}/hooks/...` at `hooks/hooks.json:9`, `hooks/hooks.json:20`, `hooks/hooks.json:29`, `hooks/hooks.json:38`, `hooks/hooks.json:47`, `hooks/hooks.json:58`, and `hooks/hooks.json:68`. The called scripts depend on `hooks/lib/loop-common.sh`, `hooks/lib/template-loader.sh`, `scripts/portable-timeout.sh`, and state under `.humanize/rlcr`.
- edge_cases_or_failure_modes: Misconfigured `CLAUDE_PLUGIN_ROOT` disables the entire manifest. Matchers are exact tool names, so renamed tools or new tool surfaces would bypass unless added. `UserPromptSubmit` and `Stop` have no matcher filters, so their scripts must self-detect whether an active RLCR loop applies.
- validation_or_tests: Covered by hook-system and install tests such as `tests/test-codex-hook-install.sh`, `tests/robustness/test-hook-system-robustness.sh`, and validator-specific suites. The assigned `tests/robustness/test-hook-input-robustness.sh` directly pipes JSON into several scripts referenced here.
- skip_candidate: `no`

### HZ-071 `file` `tests/test-bash-validator-patterns.sh`
- cursor: `[_]`
- core_role: Focused executable specification for `command_modifies_file` in `hooks/lib/loop-common.sh`. That helper is the shared Bash-write detector used by `loop-bash-validator.sh` to block shell bypasses of Write/Edit validators.
- algorithmic_behavior: The test lowercases commands through `to_lower` before calling `command_modifies_file` at `tests/test-bash-validator-patterns.sh:40`. Positive cases cover redirection at `tests/test-bash-validator-patterns.sh:75`, `tee` at `tests/test-bash-validator-patterns.sh:89`, in-place `sed`/`awk`/`perl` at `tests/test-bash-validator-patterns.sh:102`, file operations at `tests/test-bash-validator-patterns.sh:116`, and `dd`/`truncate`/`exec` at `tests/test-bash-validator-patterns.sh:130`. Negative cases assert read-only commands are ignored at `tests/test-bash-validator-patterns.sh:142`. State and summary regex variants are checked at `tests/test-bash-validator-patterns.sh:181` and `tests/test-bash-validator-patterns.sh:192`.
- inputs_outputs_state: Inputs are literal shell command strings and target file regexes, defaulting to `goal-tracker\.md` at `tests/test-bash-validator-patterns.sh:39`. Output is a test pass/fail counter and final exit 0 when no failures remain at `tests/test-bash-validator-patterns.sh:206`. The underlying helper itself returns 0 for detected modification and 1 for no modification.
- gates_or_invariants: The key invariant is high recall for write-like shell operations while avoiding false positives for `cat`, `grep`, `head`, `tail`, `wc`, `less`, `echo`, `ls`, `file`, `stat`, and `diff` at `tests/test-bash-validator-patterns.sh:142`. `loop-bash-validator.sh` uses the same helper to block methodology/finalize/state/goal/prompt/summary/contract/todos modifications, for example `loop-bash-validator.sh:271`, `loop-bash-validator.sh:509`, `loop-bash-validator.sh:524`, `loop-bash-validator.sh:534`, and `loop-bash-validator.sh:561`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` at `tests/test-bash-validator-patterns.sh:13`. The implementation patterns live at `hooks/lib/loop-common.sh:1485`, with regex entries for redirects, `tee`, in-place editors, `mv|cp`, `rm`, `dd`, `truncate`, `printf`, and `exec` through `hooks/lib/loop-common.sh:1501`.
- edge_cases_or_failure_modes: The test documents a known limitation: multi-source copy to a single destination, such as `cp file1 file2 goal-tracker.md`, is not detected because the pattern expects `cp src dest` shape at `tests/test-bash-validator-patterns.sh:166`. Variable-only destinations like `$FILE` are intentionally not detected at `tests/test-bash-validator-patterns.sh:171`.
- validation_or_tests: The file is a standalone validator test with local `pass`/`fail` functions at `tests/test-bash-validator-patterns.sh:24` and final summary logic at `tests/test-bash-validator-patterns.sh:199`.
- skip_candidate: `no`

### HZ-101 `file` `tests/test-template-loader.sh`
- cursor: `[_]`
- core_role: Executable specification for `hooks/lib/template-loader.sh`, the shared prompt/block template renderer used by validators and stop/finalize logic.
- algorithmic_behavior: Tests template directory resolution at `tests/test-template-loader.sh:42`, existing and missing template loading at `tests/test-template-loader.sh:55` and `tests/test-template-loader.sh:68`, single and multi-variable rendering at `tests/test-template-loader.sh:81` and `tests/test-template-loader.sh:96`, multiline rendering at `tests/test-template-loader.sh:111`, and integration against `block/wrong-round-number.md` at `tests/test-template-loader.sh:147`. Safe fallback behavior is validated at `tests/test-template-loader.sh:197` and `tests/test-template-loader.sh:211`; directory validation at `tests/test-template-loader.sh:225`.
- inputs_outputs_state: Inputs are template strings, template file names, `VAR=value` assignments, fallback strings, and the resolved `prompt-template` directory. Outputs are rendered strings and suite pass/fail counts. No persistent state transition occurs, but rendered output becomes the user-facing block reason in hook gates.
- gates_or_invariants: The suite locks down single-pass substitution, literal handling of shell metacharacters, missing placeholder preservation, and fallback-on-missing-template behavior. Shell metacharacter cases cover ampersand, backslash, dollar sign, backticks, pipes, semicolons, globs, parentheses, redirection text, quotes, hash, tilde, combined strings, Windows paths, regex text, JSON text, and multiline values from `tests/test-template-loader.sh:247` through `tests/test-template-loader.sh:487`.
- dependencies_and_callers: Sources `hooks/lib/template-loader.sh` at `tests/test-template-loader.sh:12`. The implementation computes `prompt-template` at `hooks/lib/template-loader.sh:26`, loads files at `hooks/lib/template-loader.sh:36`, renders with awk at `hooks/lib/template-loader.sh:56`, wraps load-and-render at `hooks/lib/template-loader.sh:140`, emits fallback content at `hooks/lib/template-loader.sh:188`, and validates required subdirs `block`, `codex`, `claude`, and `plan` at `hooks/lib/template-loader.sh:224`.
- edge_cases_or_failure_modes: Placeholder injection is explicitly blocked: values containing `{{VAR}}` are not re-scanned at `tests/test-template-loader.sh:489`, with realistic `REVIEW_CONTENT` coverage at `tests/test-template-loader.sh:525`. Empty values, Unicode, underscore-prefixed variables, and numeric variable names are covered at `tests/test-template-loader.sh:555`, `tests/test-template-loader.sh:567`, `tests/test-template-loader.sh:617`, and `tests/test-template-loader.sh:629`.
- validation_or_tests: Standalone shell spec with local counters and final exit logic at `tests/test-template-loader.sh:641`. Additional template coverage exists in `tests/test-templates-comprehensive.sh`, `tests/test-template-references.sh`, and robustness stress/error suites.
- skip_candidate: `no`

### HZ-131 `file` `prompt-template/block/incomplete-todos.md`
- cursor: `[_]`
- core_role: Stop-gate block template for unfinished native Task state. It prevents RLCR from entering Codex review while TaskCreate/TaskUpdate work remains incomplete.
- algorithmic_behavior: The template emits a hard block headed `Incomplete Tasks Detected` at `prompt-template/block/incomplete-todos.md:1`, includes the injected `{{INCOMPLETE_LIST}}` at `prompt-template/block/incomplete-todos.md:5`, and instructs completion via TaskUpdate at `prompt-template/block/incomplete-todos.md:7`. It explicitly blocks Codex review until tasks are finished at `prompt-template/block/incomplete-todos.md:12`.
- inputs_outputs_state: Input is `INCOMPLETE_LIST`, produced by the stop hook after transcript/task checking. Output is rendered Markdown used as the stop-hook block reason. The state transition is negative: the Stop hook remains blocked and does not proceed into review, finalize, or terminal exit while task work is incomplete.
- gates_or_invariants: All remaining tasks must be completed with TaskUpdate status `completed` before summary/stop. This preserves the invariant that Claude cannot summarize early and hand work to Codex while its own task list still contains active work.
- dependencies_and_callers: Loaded by `loop-codex-stop-hook.sh` through `load_and_render_safe` at `hooks/loop-codex-stop-hook.sh:444`, using `INCOMPLETE_LIST` from `hooks/loop-codex-stop-hook.sh:437`. The stop hook comments note support for legacy TodoWrite and new TaskCreate/TaskUpdate systems at `hooks/loop-codex-stop-hook.sh:406`. Template rendering depends on `hooks/lib/template-loader.sh`.
- edge_cases_or_failure_modes: If the template is missing or empty, the stop hook falls back to an inline message built at `hooks/loop-codex-stop-hook.sh:439`. If `INCOMPLETE_LIST` contains template-like braces, `render_template` single-pass behavior prevents secondary substitution, as specified by `hooks/lib/template-loader.sh:52`.
- validation_or_tests: Covered by `tests/test-todo-checker.sh`, stop-hook suites, and `tests/test-template-references.sh`, which scans shell template references to avoid missing block messages.
- skip_candidate: `no`

### HZ-161 `file` `prompt-template/claude/methodology-analysis-prompt.md`
- cursor: `[_]`
- core_role: RLCR exit-phase prompt that inserts a sanitized methodology-improvement analysis before the loop finally terminates on completion, stagnation stop, or max-iteration exit.
- algorithmic_behavior: The prompt declares the exit point at `prompt-template/claude/methodology-analysis-prompt.md:3`, renders exit reason and round counts at `prompt-template/claude/methodology-analysis-prompt.md:5`, requires spawning an Opus agent at `prompt-template/claude/methodology-analysis-prompt.md:12`, points that agent at round summaries and review results at `prompt-template/claude/methodology-analysis-prompt.md:16`, and requires writing `methodology-analysis-report.md` at `prompt-template/claude/methodology-analysis-prompt.md:20`. It then requires reading only that report for user-facing content at `prompt-template/claude/methodology-analysis-prompt.md:43`, optionally asking about a GitHub issue at `prompt-template/claude/methodology-analysis-prompt.md:57`, and writing a meaningful done marker at `prompt-template/claude/methodology-analysis-prompt.md:71`.
- inputs_outputs_state: Inputs are `LOOP_DIR`, `EXIT_REASON`, `EXIT_REASON_DESCRIPTION`, `CURRENT_ROUND`, and `MAX_ITERATIONS`, injected by `enter_methodology_analysis_phase` at `hooks/lib/methodology-analysis.sh:82`. Output is a rendered block reason JSON returned at `hooks/lib/methodology-analysis.sh:90`. Entering the phase renames the active state to `methodology-analysis-state.md` at `hooks/lib/methodology-analysis.sh:64`, records `.methodology-exit-reason` at `hooks/lib/methodology-analysis.sh:68`, and touches an initially empty done marker at `hooks/lib/methodology-analysis.sh:71`.
- gates_or_invariants: Sanitization is central. The report must not include paths, identifiers, code, raw errors, URLs, domain terms, or other project-identifying data at `prompt-template/claude/methodology-analysis-prompt.md:22`. Completion requires a non-empty done marker, a non-empty report, a valid original exit reason, and then renames methodology state to the terminal state at `hooks/lib/methodology-analysis.sh:114` through `hooks/lib/methodology-analysis.sh:173`.
- dependencies_and_callers: `hooks/loop-codex-stop-hook.sh` sources `hooks/lib/methodology-analysis.sh` at `hooks/loop-codex-stop-hook.sh:54`. It handles active methodology completion before git-clean and normal review gates at `hooks/loop-codex-stop-hook.sh:607`, calls entry on max iteration at `hooks/loop-codex-stop-hook.sh:968`, completion/finalize paths at `hooks/loop-codex-stop-hook.sh:984` and `hooks/loop-codex-stop-hook.sh:1875`, and STOP at `hooks/loop-codex-stop-hook.sh:1979`.
- edge_cases_or_failure_modes: Privacy mode skips the phase at `hooks/lib/methodology-analysis.sh:42`. Re-entry and already-complete markers are guarded at `hooks/lib/methodology-analysis.sh:48` and `hooks/lib/methodology-analysis.sh:54`. Missing or empty report blocks completion at `hooks/lib/methodology-analysis.sh:131`; missing or invalid exit reason blocks at `hooks/lib/methodology-analysis.sh:145`. During this phase, read/write/Bash validators restrict project access and writes: read allowlist at `hooks/loop-read-validator.sh:130`, write allowlist at `hooks/loop-write-validator.sh:130`, and Bash write restrictions at `hooks/loop-bash-validator.sh:81`.
- validation_or_tests: Covered by stop/finalize/hook robustness suites and template reference/comprehensive tests. Direct static callers and restriction gates confirm this prompt participates in real Stop-hook transition logic, not only documentation.
- skip_candidate: `no`

### HZ-191 `file` `tests/robustness/test-hook-input-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for hook JSON parsing, Bash command edge handling, monitor resilience, and production monitor helper functions.
- algorithmic_behavior: Pipes valid JSON to `loop-read-validator.sh`, `loop-write-validator.sh`, and `loop-bash-validator.sh` at `tests/robustness/test-hook-input-robustness.sh:35`, `tests/robustness/test-hook-input-robustness.sh:47`, and `tests/robustness/test-hook-input-robustness.sh:59`. Rejects invalid JSON, empty JSON, and missing `tool_input.file_path` at `tests/robustness/test-hook-input-robustness.sh:71`, `tests/robustness/test-hook-input-robustness.sh:87`, and `tests/robustness/test-hook-input-robustness.sh:102`. Exercises long commands, special characters, Unicode, unknown tools, deep nesting, non-UTF8, and null-byte behavior at `tests/robustness/test-hook-input-robustness.sh:117` through `tests/robustness/test-hook-input-robustness.sh:235`.
- inputs_outputs_state: Inputs are synthesized hook JSON payloads, temp directories via `setup_test_dir`, fake `.humanize/rlcr` monitor sessions, log files, and shell command strings. Outputs are validator exit codes, pass/fail assertions, monitor wrapper exit codes, and parser outputs. It creates temporary state files for monitor scenarios but does not modify persistent repo state.
- gates_or_invariants: Valid hook JSON for recognized tools should pass when targeting non-loop paths. Invalid JSON, missing required fields, over-depth JSON, and invalid UTF-8 should fail without signal crashes. Unknown tool names are ignored by a non-matching validator at `tests/robustness/test-hook-input-robustness.sh:163`. Monitor behavior must tolerate terminal width, log growth/deletion, ANSI codes, binary log content, narrow/wide terminals, and rapid updates at `tests/robustness/test-hook-input-robustness.sh:245` through `tests/robustness/test-hook-input-robustness.sh:341`.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` and `tests/test-helpers.sh` at `tests/robustness/test-hook-input-robustness.sh:16`. Calls production validators directly and later sources `scripts/humanize.sh` for `humanize_parse_goal_tracker`, `humanize_parse_git_status`, `humanize_detect_git_state`, and `_humanize_monitor_codex` at `tests/robustness/test-hook-input-robustness.sh:384`, `tests/robustness/test-hook-input-robustness.sh:470`, `tests/robustness/test-hook-input-robustness.sh:529`, and `tests/robustness/test-hook-input-robustness.sh:619`.
- edge_cases_or_failure_modes: Deeply nested JSON at 50 levels must reject below signal threshold at `tests/robustness/test-hook-input-robustness.sh:177`. Non-UTF8 binary content must reject with exit 1 at `tests/robustness/test-hook-input-robustness.sh:201`. Null bytes are documented as stripped by Bash command substitution, so the test only requires graceful non-signal behavior at `tests/robustness/test-hook-input-robustness.sh:219`. Monitor subprocess tests delete session directories after startup to verify graceful exit at `tests/robustness/test-hook-input-robustness.sh:531`.
- validation_or_tests: The file is itself a robustness suite with `print_test_summary` at `tests/robustness/test-hook-input-robustness.sh:670`. It validates implementation contracts in `validate_hook_input` and `is_deeply_nested` from `hooks/lib/loop-common.sh:91` and `hooks/lib/loop-common.sh:151`, plus `command_modifies_file` through checks at `tests/robustness/test-hook-input-robustness.sh:351`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7/7 Item Evidence headings present, matching the assigned row count
- missing_items: none
- duplicate_items: none; item identifiers are used only as the seven Item Evidence headings
- final_worker_status: `complete`