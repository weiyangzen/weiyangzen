# agent_02 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-002 `directory` `agents`
- cursor: `[_]`
- core_role: Prompt-spec directory for specialized subagents used by Humanize planning/RLCR workflows. It contains four agent definitions: `agents/bitlesson-selector.md`, `agents/draft-relevance-checker.md`, `agents/plan-compliance-checker.md`, and `agents/plan-understanding-quiz.md`.
- algorithmic_behavior: `bitlesson-selector` maps a sub-task plus related paths plus BitLesson content to exactly `LESSON_IDS` and `RATIONALE`, preferring precision and returning `NONE` when no lesson directly applies (`agents/bitlesson-selector.md:12-39`). `draft-relevance-checker` performs lenient repo relevance classification for draft content and emits exactly `RELEVANT:` or `NOT_RELEVANT:` (`agents/draft-relevance-checker.md:14-36`). `plan-compliance-checker` performs two pre-RLCR gates: repo relevance and branch-switch detection, with safe disambiguations for file checkout, negated branch instructions, branch references, and `--base-branch` (`agents/plan-compliance-checker.md:16-55`). `plan-understanding-quiz` generates exactly two four-option technical comprehension questions plus a plan summary in a strict 13-field format (`agents/plan-understanding-quiz.md:16-85`).
- inputs_outputs_state: Inputs are prompt-provided task/plan/draft text and limited repo exploration via declared tools. Outputs are stable text protocols consumed by command flows. No persistent state is written by files in `agents` themselves; the state transition happens in callers that parse the verdicts or quiz fields. `bitlesson-selector` output feeds later work notes and review discipline; relevance/compliance outputs gate `/humanize:gen-plan` and `/humanize:start-rlcr-loop`; quiz output drives an advisory user knowledge check.
- gates_or_invariants: Each agent enforces a narrow output vocabulary. `bitlesson-selector` forbids extra sections (`agents/bitlesson-selector.md:32-40`). `draft-relevance-checker` leans relevant on doubt (`agents/draft-relevance-checker.md:31-36`). `plan-compliance-checker` must emit exactly one of `PASS`, `FAIL_RELEVANCE`, or `FAIL_BRANCH_SWITCH` and leans PASS on ambiguous relevance/branch cases (`agents/plan-compliance-checker.md:48-62`). `plan-understanding-quiz` requires all 13 fields, answer letters A-D only, and same-language output as the plan (`agents/plan-understanding-quiz.md:57-85`).
- dependencies_and_callers: `commands/gen-plan.md:160-179` invokes `humanize:draft-relevance-checker` before plan generation. `commands/start-rlcr-loop.md:37-56` invokes `humanize:plan-compliance-checker`; `commands/start-rlcr-loop.md:60-86` invokes `humanize:plan-understanding-quiz`. `bitlesson-selector` is referenced by prompt templates and hook prompt generation, including `prompt-template/claude/agent-teams-core.md:23`, `prompt-template/claude/next-round-prompt.md:44`, `hooks/loop-codex-stop-hook.sh:1559-1567`, and `scripts/setup-rlcr-loop.sh:1359`. Runtime routing for BitLesson selection is documented in `agents/bitlesson-selector.md:19-24`, pointing to `scripts/bitlesson-select.sh`.
- edge_cases_or_failure_modes: Malformed agent output is a hard or soft failure depending on caller: malformed compliance output blocks start-loop fail-closed (`commands/start-rlcr-loop.md:52-56`), while malformed quiz output only warns and continues (`commands/start-rlcr-loop.md:86`). `draft-relevance-checker` can false-pass intentionally because it is lenient. `plan-compliance-checker` may miss subtle branch-switch instructions because it avoids false positives. `bitlesson-selector` can return `NONE`, which is valid and must be recorded.
- validation_or_tests: `tests/test-gen-plan.sh` checks `draft-relevance-checker.md` existence, frontmatter name, model, tools, naming, required fields, valid model, and English-only content. `tests/test-bitlesson-select-routing.sh` validates selector routing output for Codex/Claude paths. `tests/test-codex-hook-install.sh` checks installation of a PATH-ready `bitlesson-selector` shim. The command files provide parser expectations for the compliance and quiz agents.
- skip_candidate: `no`

### HZ-032 `file` `commands/gen-idea.md`
- cursor: `[_]`
- core_role: Command workflow definition for generating a repo-grounded idea draft from loose input using directed-diversity exploration and a parallel swarm of read-only `Explore` subagents.
- algorithmic_behavior: The workflow is strictly sequential: parse input, validate IO, generate directions, run parallel exploration, synthesize/write (`commands/gen-idea.md:23-31`). Phase 1 runs `scripts/validate-gen-idea-io.sh` and parses `INPUT_MODE`, `OUTPUT_FILE`, `SLUG`, `TEMPLATE_FILE`, and `N` from success output (`commands/gen-idea.md:46-69`). Phase 2 produces exactly `N` orthogonal directions, retries once if too few are produced, degrades only if at least two directions remain, and otherwise stops (`commands/gen-idea.md:73-104`). Phase 3 launches one `Explore` subagent per direction in a single Task-tool message; each subagent must return `APPROACH_SUMMARY`, `OBJECTIVE_EVIDENCE`, `KNOWN_RISKS`, and `CONFIDENCE` (`commands/gen-idea.md:108-140`). Phase 4 picks the primary by evidence density, repo-pattern fit, surface area, then confidence, fills the template, and writes one output file (`commands/gen-idea.md:146-199`).
- inputs_outputs_state: Inputs are `$ARGUMENTS`: first positional inline idea or `.md` file, optional `--n`, optional `--output` (`commands/gen-idea.md:35-42`). The validator emits either inline body via `=== IDEA_BODY_BEGIN/END ===` or `IDEA_BODY_FILE` for file mode (`scripts/validate-gen-idea-io.sh:189-202`). Output is a single draft file at `OUTPUT_FILE` based on `prompt-template/idea/gen-idea-template.md`, with original idea, primary direction, alternatives, and synthesis notes (`prompt-template/idea/gen-idea-template.md:1-31`). State is held in workflow variables: `IDEA_BODY`, `DIRECTIONS`, surviving proposals, `PRIMARY`, alternatives, and warnings.
- gates_or_invariants: Hard draft-only constraint: no source edits, no feature implementation, no commits; permitted write is only the final draft file plus default directory creation by validation script (`commands/gen-idea.md:17-21`). Direction orthogonality is a hard constraint (`commands/gen-idea.md:87-97`). Exploration is read-only and forbids fabricated repo references; if no evidence exists, the subagent must use the sentinel `exploratory, no concrete precedent` (`commands/gen-idea.md:120-128`). Field parsing drops degraded proposals; fewer than two surviving proposals stops the command (`commands/gen-idea.md:135-140`). If any phase errors, no partial output may be written (`commands/gen-idea.md:203-209`).
- dependencies_and_callers: Depends directly on `scripts/validate-gen-idea-io.sh`, which validates `--n` range 2-10, detects inline vs file input, creates the default `.humanize/ideas` directory, rejects existing output, and resolves the template path (`scripts/validate-gen-idea-io.sh:31-187`). Depends on `prompt-template/idea/gen-idea-template.md` placeholders (`commands/gen-idea.md:162-188`). Uses repo context from `README.md`, `CLAUDE.md`, `.claude/CLAUDE.md`, and top-level `Glob` (`commands/gen-idea.md:77-85`). The generated draft is intended as input to `/humanize:gen-plan` (`commands/gen-idea.md:195-199`).
- edge_cases_or_failure_modes: Validation exit codes 1-7 map to user-facing stop messages (`commands/gen-idea.md:53-62`). `WARNING:` lines before `VALIDATION_SUCCESS` are informational and must be surfaced but not stop execution (`commands/gen-idea.md:63`). Inline ideas under 10 characters produce a warning in the validator (`scripts/validate-gen-idea-io.sh:131-134`). A missing path-like input exits as input-not-found, but whitespace in a nonexistent path makes the validator treat it as inline by design (`scripts/validate-gen-idea-io.sh:89-101`). Direction generation or exploration can degrade to a reduced count but only with at least two items.
- validation_or_tests: Direct search found the command and validator references but no dedicated `tests/test-gen-idea.sh`. The validator has self-documenting exit-code gates in `scripts/validate-gen-idea-io.sh:4-12`. Related coverage exists through template/reference tests for command surfaces, but assigned evidence did not show an executable gen-idea-specific suite.
- skip_candidate: `no`

### HZ-062 `file` `scripts/validate-refine-plan-io.sh`
- cursor: `[_]`
- core_role: Runtime IO validator for `/humanize:refine-plan`; it verifies an annotated gen-plan file is suitable for refinement, checks comment-block syntax and required plan sections, validates output/QA writability, and reports a machine-readable validation result.
- algorithmic_behavior: The script defines exit codes 0-7 for success, missing input, empty input, invalid/no comments, missing gen-plan sections, output/input directory writability, QA directory writability, and invalid arguments (`scripts/validate-refine-plan-io.sh:1-12`). `scan_cmt_blocks()` is an awk state machine that ignores fenced code and HTML comments, recognizes `CMT:/ENDCMT`, `<cmt></cmt>`, and `<comment></comment>`, counts only non-empty comment blocks, and errors on nested/stray/missing markers (`scripts/validate-refine-plan-io.sh:16-316`). `scan_sections()` is a second awk scanner that extracts visible Markdown headings while ignoring fenced code, HTML comments, and custom comment blocks (`scripts/validate-refine-plan-io.sh:318-478`). The main body parses `--input`, optional `--output`, optional `--qa-dir`, and mutually exclusive `--discussion`/`--direct`, defaults output to in-place mode, then runs six checks and prints `VALIDATION_SUCCESS` with mode metadata (`scripts/validate-refine-plan-io.sh:480-692`).
- inputs_outputs_state: Required input is `--input <annotated-plan.md>`; optional state controls are `--output`, `--qa-dir`, `--discussion`, and `--direct` (`scripts/validate-refine-plan-io.sh:480-489`). Defaults are `OUTPUT_FILE=INPUT_FILE`, `QA_DIR=.humanize/plan_qa`, and both mode flags false (`scripts/validate-refine-plan-io.sh:493-558`). Outputs are stdout/stderr diagnostic lines with `VALIDATION_ERROR: ...` or `VALIDATION_SUCCESS`, resolved input/output paths, line/comment counts, mode, QA dir, and exit code. It may create `QA_DIR` if absent (`scripts/validate-refine-plan-io.sh:662-670`).
- gates_or_invariants: `--discussion` and `--direct` cannot both be true (`scripts/validate-refine-plan-io.sh:544-548`). Input must exist and be non-empty (`scripts/validate-refine-plan-io.sh:573-587`). At least one valid non-empty comment block must be found outside ignored regions (`scripts/validate-refine-plan-io.sh:589-606`). Required sections are exactly `## Goal Description`, `## Acceptance Criteria`, `## Path Boundaries`, `## Feasibility Hints`, `## Dependencies and Sequence`, `## Task Breakdown`, `## Claude-Codex Deliberation`, `## Pending User Decisions`, and `## Implementation Notes` (`scripts/validate-refine-plan-io.sh:608-637`). New-file mode requires writable output dir; in-place mode requires writable input dir (`scripts/validate-refine-plan-io.sh:639-660`). QA dir must be creatable/writable (`scripts/validate-refine-plan-io.sh:662-678`).
- dependencies_and_callers: Called by `commands/refine-plan.md`, whose workflow lists IO validation, comment extraction, QA generation, and final write phases; it explicitly says not to pass `--alt-language` to this script (`commands/refine-plan.md:36-76`). Also referenced by `skills/humanize-refine-plan/SKILL.md:22-64`, which mirrors the flow and mode rules. Tests invoke it through `tests/test-refine-plan.sh`.
- edge_cases_or_failure_modes: Markers inside fenced code or HTML comments do not count (`scripts/validate-refine-plan-io.sh:135-158`, `scripts/validate-refine-plan-io.sh:252-256`, `scripts/validate-refine-plan-io.sh:339-357`, `scripts/validate-refine-plan-io.sh:425-460`). Empty blocks do not count because `cmt_has_text` must be true before count increment (`scripts/validate-refine-plan-io.sh:182-214`). Nested starts and wrong/stray end markers produce parse errors with line, column, nearest heading, and context excerpt (`scripts/validate-refine-plan-io.sh:48-60`, `scripts/validate-refine-plan-io.sh:227-236`, `scripts/validate-refine-plan-io.sh:294-297`). Missing end marker at EOF exits as invalid comment blocks (`scripts/validate-refine-plan-io.sh:303-314`). A non-directory `QA_DIR` reaches the not-writable QA gate rather than a separate type gate.
- validation_or_tests: `tests/test-refine-plan.sh` covers executable presence, argument errors, mutual exclusion, help text, missing/empty input, no comments, ignored markers in HTML/code fences, empty/unterminated/nested comments, missing required sections, required sections hidden inside ignored regions, missing/non-writable output dirs, non-writable input dir in in-place mode, QA dir creation/failure, in-place/new-file success modes, and mixed valid/ignored/empty comments (`tests/test-refine-plan.sh:1044-1342`).
- skip_candidate: `no`

### HZ-092 `file` `tests/test-plan-file-validation.sh`
- cursor: `[_]`
- core_role: Executable specification for plan-file validation behavior in `scripts/setup-rlcr-loop.sh`, especially path safety, git/tracking policy, content sufficiency, branch-name YAML safety, CLI parsing, and Codex parameter validation.
- algorithmic_behavior: The test creates temporary git repositories, mocks `codex`, executes `scripts/setup-rlcr-loop.sh` with varied arguments, captures output/exit code, and increments pass/fail/skip counters (`tests/test-plan-file-validation.sh:23-83`). It expects absolute paths to be rejected (`tests/test-plan-file-validation.sh:88-101`), missing files and missing parent directories to fail clearly (`tests/test-plan-file-validation.sh:103-125`), spaces and shell metacharacters to be blocked (`tests/test-plan-file-validation.sh:127-186`), symlinks and path resolution failures to fail (`tests/test-plan-file-validation.sh:188-242`), `../` project escape to be rejected (`tests/test-plan-file-validation.sh:244-271`), non-git or no-commit repos to fail (`tests/test-plan-file-validation.sh:273-322`), tracked/untracked/modified plan state to match `--track-plan-file` semantics (`tests/test-plan-file-validation.sh:328-425`), YAML-unsafe branch names to be rejected or already rejected by git (`tests/test-plan-file-validation.sh:431-511`), weak plan content to fail and substantive content to pass content validation (`tests/test-plan-file-validation.sh:517-660`), `--plan-file` parsing to work and conflict with positional path to fail (`tests/test-plan-file-validation.sh:666-694`), and Codex model/effort validation to reject unsafe values while allowing `gpt-5.5:medium` (`tests/test-plan-file-validation.sh:700-740`).
- inputs_outputs_state: Inputs are generated temp files/repos plus CLI arguments to `setup-rlcr-loop.sh`. Outputs are test result lines and final exit status equal to `TESTS_FAILED` (`tests/test-plan-file-validation.sh:742-751`). State is isolated in `TEST_DIR` from `mktemp -d`, removed by trap (`tests/test-plan-file-validation.sh:36-38`). It unsets `CLAUDE_PROJECT_DIR` so the setup script uses the temp repo rather than the real checkout (`tests/test-plan-file-validation.sh:16-21`).
- gates_or_invariants: The test codifies fail-closed path validation: plan path must be relative, no spaces, no shell metacharacters, no symlink file or symlink parent, existing/readable, within project, and not in a submodule. These map to `scripts/setup-rlcr-loop.sh:455-556`. Git tracking policy is binary: without `--track-plan-file`, a tracked plan is invalid; with `--track-plan-file`, the file must be tracked and clean (`scripts/setup-rlcr-loop.sh:562-607`). Plan content must have at least five lines and at least three non-comment content lines, ignoring blank lines, `#` lines, and HTML comments (`scripts/setup-rlcr-loop.sh:618-671`). Current branch and Codex model/effort must be YAML-safe (`scripts/setup-rlcr-loop.sh:686-716`).
- dependencies_and_callers: Directly depends on `scripts/setup-rlcr-loop.sh` as the system under test and on git/codex availability, with codex mocked by adding a temp `bin/codex` to PATH (`tests/test-plan-file-validation.sh:73-83`). It is likely invoked by the aggregate suite `tests/run-all-tests.sh`, though this assigned file itself is standalone executable.
- edge_cases_or_failure_modes: Some branch-name tests accept either git rejecting the invalid branch or the setup script rejecting it (`tests/test-plan-file-validation.sh:431-511`). Permission-based path-resolution and non-writable tests can be skipped/variable depending on environment capability (`tests/test-plan-file-validation.sh:225-241`). Positive content tests do not require total setup success; they only assert failure is not due to “insufficient content” because later gates may still fail in a mocked environment (`tests/test-plan-file-validation.sh:625-630`, `tests/test-plan-file-validation.sh:651-656`, `tests/test-plan-file-validation.sh:731-736`). Test 2.8 loops over metacharacters but unconditionally calls `pass` after the loop, so a failure inside the loop records a fail but does not prevent the aggregate pass message (`tests/test-plan-file-validation.sh:178-186`).
- validation_or_tests: This file is itself validation coverage. It covers the plan-file branch of `setup-rlcr-loop.sh`; the relevant implementation section spans `scripts/setup-rlcr-loop.sh:400-730`.
- skip_candidate: `no`

### HZ-122 `file` `prompt-template/block/git-not-clean-humanize-local.md`
- cursor: `[_]`
- core_role: Prompt block for a git cleanliness gate special case: when `.humanize/` local runtime artifacts make the working tree dirty, the user/agent should add a gitignore rule rather than commit those artifacts.
- algorithmic_behavior: The template emits a “Special Case” message that identifies `.humanize/` as created by `humanize:start-rlcr-loop`, states it should not be committed, and provides a two-command remediation: append `.humanize*` to `.gitignore`, then stage `.gitignore` (`prompt-template/block/git-not-clean-humanize-local.md:2-8`).
- inputs_outputs_state: Inputs are implicit context from the hook/template loader selecting this block after detecting local `.humanize` artifacts in git status. Output is a remediation prompt block. State transition intended by the block is: dirty `.humanize` runtime paths become ignored via `.gitignore`; only `.gitignore` should become staged.
- gates_or_invariants: `.humanize/` is classified as runtime/local state, not source deliverable. The invariant is “do not commit `.humanize/`”; the only suggested tracked change is `.gitignore`.
- dependencies_and_callers: Related to git cleanliness hooks and templates in `prompt-template/block/git-not-clean.md`, `prompt-template/block/git-tracked-humanize.md`, and hook code that enforces clean working tree before RLCR transitions. It is part of the prompt-template block set used by stop hooks and workflow gates.
- edge_cases_or_failure_modes: The suggested `echo '.humanize*' >> .gitignore` can duplicate an existing pattern if not checked first. The wildcard also covers legacy dash-separated variants, which aligns with setup-loop comments that filter untracked `.humanize/` and `.humanize-*` runtime paths (`scripts/setup-rlcr-loop.sh:729-730`). If `.humanize` content is already tracked, adding `.gitignore` alone does not untrack it; adjacent template `git-tracked-humanize.md` likely covers that case.
- validation_or_tests: Template presence/coverage is likely under comprehensive template tests (`tests/test-template-loader.sh`, `tests/test-template-references.sh`, `tests/test-templates-comprehensive.sh` listed in repo). No direct assigned test output was inspected for this exact small block.
- skip_candidate: `no`

### HZ-152 `file` `prompt-template/block/wrong-round-number.md`
- cursor: `[_]`
- core_role: Prompt block enforcing RLCR round-number state integrity for round-scoped artifacts.
- algorithmic_behavior: The template tells the actor they are trying to perform `{{ACTION}}` on `round-{{CLAUDE_ROUND}}-{{FILE_TYPE}}.md` while the current round is `{{CURRENT_ROUND}}`, then gives `{{CORRECT_PATH}}` and instructs not to increment the round number manually (`prompt-template/block/wrong-round-number.md:1-7`).
- inputs_outputs_state: Inputs are template variables from the hook/runtime state: `ACTION`, `CLAUDE_ROUND`, `FILE_TYPE`, `CURRENT_ROUND`, and `CORRECT_PATH`. Output is a blocking correction prompt. State transition intended is not to mutate round state, but to redirect writes to the currently valid round artifact path.
- gates_or_invariants: Round-scoped files must match the authoritative current round. Agents must not self-increment round numbers; round advancement is controlled by the RLCR loop/hook state machine.
- dependencies_and_callers: Related templates include `wrong-round-file.md`, `wrong-round-file`, `wrong-summary-location.md`, `round-contract-missing.md`, and `wrong-contract-location.md` in the same block directory. Hook/runtime code likely selects this block when an attempted summary/contract/work file path has a stale or future round number.
- edge_cases_or_failure_modes: If `CURRENT_ROUND` or `CORRECT_PATH` is stale, the template can misdirect the user; correctness depends entirely on upstream state extraction. It blocks both lagging and premature round numbers with the same message. It does not explain how to recover missing prior-round artifacts; it only provides the current correct path.
- validation_or_tests: Template reference/comprehensive tests likely cover availability and placeholder compatibility. State-transition robustness tests in the repo list include `tests/robustness/test-state-transition-robustness.sh` and round/file hook behavior appears covered by hook/template tests, but no direct execution of this block was inspected.
- skip_candidate: `no`

### HZ-182 `file` `skills/humanize-gen-plan/SKILL.md`
- cursor: `[_]`
- core_role: Skill instruction defining the non-invocable `humanize-gen-plan` flow: transform a rough draft into a structured implementation plan with goals, AC-formatted acceptance criteria, boundaries, feasibility hints, dependencies, and implementation notes.
- algorithmic_behavior: The mermaid flow runs IO validation through `{{HUMANIZE_RUNTIME_ROOT}}/scripts/validate-gen-plan-io.sh --input <draft> --output <plan>`, stops on validation failure, reads the draft, checks repository relevance, stops on irrelevance, analyzes for clarity/consistency/completeness/functionality, loops through user issue resolution if issues remain, confirms quantitative metrics if present, generates the structured plan, writes it with Edit, reviews/fixes inconsistencies, optionally asks user to unify multiple languages, and reports success (`skills/humanize-gen-plan/SKILL.md:19-47`).
- inputs_outputs_state: Required inputs are `--input <path/to/draft.md>` and `--output <path/to/plan.md>` (`skills/humanize-gen-plan/SKILL.md:49-54`). Output is a plan with sections for title, goal, acceptance criteria, path boundaries, dependencies/sequence, and implementation notes (`skills/humanize-gen-plan/SKILL.md:55-94`). Runtime root is hydrated by installer via `{{HUMANIZE_RUNTIME_ROOT}}` (`skills/humanize-gen-plan/SKILL.md:13-17`). State transitions are validation pass/fail, relevance pass/fail, issue-resolution loop, metric confirmation, plan generation, consistency-fix loop, language unification branch, and success/failure terminal nodes.
- gates_or_invariants: The skill is `type: flow`, `user-invocable: false`, and `disable-model-invocation: true`, so it is an instruction artifact for the installed flow rather than a free-running model tool (`skills/humanize-gen-plan/SKILL.md:1-7`). It requires validation before reading/generating. It requires relevance to the repo before plan creation. It requires inconsistencies to be fixed before success. Plan output should include AC-X acceptance criteria with positive/negative tests and code should not contain plan terminology (`skills/humanize-gen-plan/SKILL.md:65-94`).
- dependencies_and_callers: Depends on `scripts/validate-gen-plan-io.sh`; the exit-code table mirrors that script’s contract (`skills/humanize-gen-plan/SKILL.md:96-108`). Semantically pairs with `commands/gen-plan.md`, which invokes `draft-relevance-checker` and then uses a plan template before Codex/Claude analysis. Installation logic replaces `{{HUMANIZE_RUNTIME_ROOT}}` with an absolute runtime root.
- edge_cases_or_failure_modes: Quantitative metrics trigger a user confirmation branch distinguishing hard requirement vs trend (`skills/humanize-gen-plan/SKILL.md:33-35`). Multiple languages trigger a user unification branch late in the flow (`skills/humanize-gen-plan/SKILL.md:42-45`). If issues remain after user discussion, the flow loops back to analysis rather than proceeding (`skills/humanize-gen-plan/SKILL.md:29-32`). Validation exit 7 indicates missing template/plugin configuration, not user input (`skills/humanize-gen-plan/SKILL.md:96-108`).
- validation_or_tests: `tests/test-gen-plan.sh` covers generated plan command/agent surfaces and validator mode flags according to repository search results. The skill’s validator exit-code contract is explicitly listed in `skills/humanize-gen-plan/SKILL.md:96-108`.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `HZ-002`, `HZ-032`, `HZ-062`, `HZ-092`, `HZ-122`, `HZ-152`, `HZ-182`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`