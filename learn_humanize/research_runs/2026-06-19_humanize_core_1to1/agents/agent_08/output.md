# agent_08 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-008 `directory` `scripts`
- cursor: `[_]`
- core_role: `scripts/` is the repository’s command/runtime layer for Humanize workflows. It creates and cancels RLCR loop state, wraps the stop hook, initializes and validates BitLesson knowledge capture, validates generated-plan IO contracts, runs one-shot model consultations, installs runtime skills/hooks, and provides monitoring/status views. Recursive inventory found 20 files under `scripts/` and `scripts/lib/`, 8154 total lines.
- algorithmic_behavior: `scripts/setup-rlcr-loop.sh` is the main state initializer: parses loop flags, enforces dependency and one-active-loop gates (`scripts/setup-rlcr-loop.sh:188`, `scripts/setup-rlcr-loop.sh:326`, `scripts/setup-rlcr-loop.sh:369`), validates git repo, plan path, tracking mode, branch/model safety, and clean worktree (`scripts/setup-rlcr-loop.sh:433`, `scripts/setup-rlcr-loop.sh:455`, `scripts/setup-rlcr-loop.sh:582`, `scripts/setup-rlcr-loop.sh:686`, `scripts/setup-rlcr-loop.sh:723`), initializes BitLesson (`scripts/setup-rlcr-loop.sh:860`), writes `state.md` with loop fields (`scripts/setup-rlcr-loop.sh:880`), creates `goal-tracker.md`, summary, contract, and prompt files (`scripts/setup-rlcr-loop.sh:929`, `scripts/setup-rlcr-loop.sh:1162`, `scripts/setup-rlcr-loop.sh:1195`, `scripts/setup-rlcr-loop.sh:1290`), and injects BitLesson/task routing rules into prompts (`scripts/setup-rlcr-loop.sh:1354`, `scripts/setup-rlcr-loop.sh:1398`). `scripts/cancel-rlcr-loop.sh` finds the active loop, writes `.cancel-requested`, removes setup/methodology marker leftovers, and renames the active state file to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:91`, `scripts/cancel-rlcr-loop.sh:139`, `scripts/cancel-rlcr-loop.sh:156`, `scripts/cancel-rlcr-loop.sh:166`). `scripts/rlcr-stop-gate.sh` adapts non-hook callers into a Stop-hook JSON event and maps hook block output to exit 10 while allowing no-project/no-output cases (`scripts/rlcr-stop-gate.sh:8`, `scripts/rlcr-stop-gate.sh:81`, `scripts/rlcr-stop-gate.sh:108`, `scripts/rlcr-stop-gate.sh:152`).
- algorithmic_behavior: BitLesson scripts form a capture/selection/validation loop. `bitlesson-init.sh` creates `.humanize/bitlesson.md` from a template without overwriting existing files and rejects absolute or parent-traversing relpaths (`scripts/bitlesson-init.sh:70`, `scripts/bitlesson-init.sh:83`). `bitlesson-select.sh` loads merged config, resolves provider, falls back to Codex on missing provider binary, constructs a two-line selector prompt, runs Codex or Claude with timeout, and normalizes output to `LESSON_IDS` plus `RATIONALE` (`scripts/bitlesson-select.sh:20`, `scripts/bitlesson-select.sh:115`, `scripts/bitlesson-select.sh:126`, `scripts/bitlesson-select.sh:148`, `scripts/bitlesson-select.sh:188`, `scripts/bitlesson-select.sh:242`). `bitlesson-validate-delta.sh` extracts a `## BitLesson Delta` block while ignoring fenced code and HTML comments, then validates action, IDs, notes, file existence, and knowledge-base membership before returning block JSON (`scripts/bitlesson-validate-delta.sh:92`, `scripts/bitlesson-validate-delta.sh:181`, `scripts/bitlesson-validate-delta.sh:202`, `scripts/bitlesson-validate-delta.sh:242`, `scripts/bitlesson-validate-delta.sh:281`, `scripts/bitlesson-validate-delta.sh:312`).
- algorithmic_behavior: IO validators define generation/refinement contracts. `validate-gen-idea-io.sh` distinguishes inline text from path-like inputs, validates `.md` file input, output parent/existence/writeability, template existence, and emits a sentinel-wrapped inline body on success (`scripts/validate-gen-idea-io.sh:89`, `scripts/validate-gen-idea-io.sh:103`, `scripts/validate-gen-idea-io.sh:123`, `scripts/validate-gen-idea-io.sh:151`, `scripts/validate-gen-idea-io.sh:189`). `validate-gen-plan-io.sh` validates input/output paths and mutually exclusive discussion/direct flags (`scripts/validate-gen-plan-io.sh:76`, `scripts/validate-gen-plan-io.sh:108`, `scripts/validate-gen-plan-io.sh:124`, `scripts/validate-gen-plan-io.sh:171`). `validate-refine-plan-io.sh` implements a real parser for `CMT:/ENDCMT`, `<cmt>`, and `<comment>` blocks while ignoring fences/HTML comments, rejects nested/stray/missing markers, enforces required gen-plan sections, and validates output/QA writeability (`scripts/validate-refine-plan-io.sh:16`, `scripts/validate-refine-plan-io.sh:318`, `scripts/validate-refine-plan-io.sh:589`, `scripts/validate-refine-plan-io.sh:608`, `scripts/validate-refine-plan-io.sh:639`).
- algorithmic_behavior: One-shot consultation scripts persist model interactions. `ask-codex.sh` and `ask-gemini.sh` validate CLI/model/timeouts, resolve project roots, create `.humanize/skill/<id>` plus cache logs, run with portable timeout, write metadata for success/error/timeout/empty response, and print clean stdout (`scripts/ask-codex.sh:153`, `scripts/ask-codex.sh:202`, `scripts/ask-codex.sh:244`, `scripts/ask-codex.sh:296`, `scripts/ask-codex.sh:309`, `scripts/ask-gemini.sh:145`, `scripts/ask-gemini.sh:184`, `scripts/ask-gemini.sh:224`, `scripts/ask-gemini.sh:281`, `scripts/ask-gemini.sh:294`). Installer scripts sync skills/runtime, hydrate placeholders, strip Claude-only frontmatter, seed Codex-friendly config, install native hooks, and create a `bitlesson-selector` shim (`scripts/install-skill.sh:75`, `scripts/install-skill.sh:120`, `scripts/install-skill.sh:155`, `scripts/install-skill.sh:167`, `scripts/install-skill.sh:194`, `scripts/install-skill.sh:269`, `scripts/install-skill.sh:334`, `scripts/install-codex-hooks.sh:75`, `scripts/install-codex-hooks.sh:85`). Monitoring/status scripts parse state, goal tracker, git status, skill metadata, and cache logs for display without mutating workflow state (`scripts/humanize.sh:71`, `scripts/humanize.sh:158`, `scripts/humanize.sh:261`, `scripts/humanize.sh:383`, `scripts/humanize.sh:838`, `scripts/lib/monitor-common.sh:154`, `scripts/lib/monitor-skill.sh:18`, `scripts/statusline.sh:67`).
- inputs_outputs_state: Inputs include CLI flags, plan markdown, active git repo state, merged config layers, `.humanize/bitlesson.md`, hook JSON, transcript paths, prompt templates, model binaries, and generated artifacts. Outputs include `.humanize/rlcr/<timestamp>/state.md`, `goal-tracker.md`, `round-N-summary.md`, `round-N-contract.md`, prompts, `.pending-session-id`, `.review-phase-started`, `.humanize/bitlesson.md`, `.humanize/skill/<id>/{input,output,metadata}.md`, cache logs, installed skills, Codex `hooks.json`, and user config. Primary state transitions are no-loop to active `state.md`, active/review/finalize state handling via stop hook wrapper, active to `cancel-state.md`, and one-shot skill invocation from running/no-metadata to terminal metadata.
- gates_or_invariants: Only one active RLCR loop may exist at setup; plan paths must be relative, no spaces/metacharacters, no symlink traversal, within project, not in submodules, and must obey tracked-vs-gitignored mode; git must be clean except ignored `.humanize` runtime paths; branch/model/effort/state fields must be YAML/shell safe; base branch must exist locally; BitLesson delta must match action/ID/notes rules; refine-plan requires at least one non-empty comment block plus required plan sections; model wrappers reject unsafe model names and empty prompts; installer validates source layout and Codex hooks support.
- dependencies_and_callers: `scripts/` is called by slash-command skills and hooks. It depends on sibling `hooks/lib/loop-common.sh`, `hooks/loop-codex-stop-hook.sh`, `hooks/lib/project-root.sh`, `hooks/lib/template-loader.sh`, `prompt-template/`, `templates/`, `config/default_config.json`, `skills/`, `agents/`, and external `git`, `jq`, `codex`, `claude`, `gemini`, `python3`, `rsync` or `cp`, and timeout/date variants.
- edge_cases_or_failure_modes: Skip-implementation mode can run without a user plan and disables BitLesson enforcement for review-only flow (`scripts/setup-rlcr-loop.sh:408`, `scripts/setup-rlcr-loop.sh:875`); missing provider binary falls back to Codex for BitLesson selection (`scripts/bitlesson-select.sh:126`); an empty BitLesson file is fatal but a non-empty file with no lessons returns `NONE` (`scripts/bitlesson-select.sh:99`, `scripts/bitlesson-select.sh:105`); no project root in `rlcr-stop-gate.sh` allows exit benignly (`scripts/rlcr-stop-gate.sh:81`); no timeout implementation runs with warning (`scripts/portable-timeout.sh:64`); cache unwritable falls back to project-local cache (`scripts/ask-codex.sh:214`, `scripts/ask-gemini.sh:195`); malformed optional configs are ignored while malformed required default config is fatal (`scripts/lib/config-loader.sh:40`, `scripts/lib/config-loader.sh:52`).
- validation_or_tests: Relevant executable specs include `tests/test-allowlist-validators.sh`, `tests/test-stop-hook-bg-allow.sh`, and `tests/robustness/test-concurrent-state-robustness.sh`. I did not execute them because this worker was instructed to perform read-only research and the tests create temp git repositories and files.
- skip_candidate: `no`

### HZ-038 `file` `docs/bitlesson.md`
- cursor: `[_]`
- core_role: Behavior-defining documentation for the BitLesson knowledge-capture workflow that is enforced by `scripts/bitlesson-*` and the RLCR stop hook.
- algorithmic_behavior: Defines config lookup order for `bitlesson_model` as default, user, project, then CLI (`docs/bitlesson.md:7`); defines provider routing from model names to Codex or Claude (`docs/bitlesson.md:14`); requires fallback to default Codex model when provider binary is missing (`docs/bitlesson.md:19`); defines `provider_mode: "codex-only"` as a force-to-Codex override before provider resolution (`docs/bitlesson.md:21`); defines workflow: initialize `.humanize/bitlesson.md`, run `scripts/bitlesson-select.sh` per task/sub-task, apply selected lesson IDs or record `NONE`, and stop-gate validate summary delta (`docs/bitlesson.md:27`); defines the required `## BitLesson Delta` summary block (`docs/bitlesson.md:36`).
- inputs_outputs_state: Inputs are merged config values, `.humanize/bitlesson.md`, task/sub-task description, related paths, and round summary content. Outputs are selected lesson IDs or `NONE`, implementation behavior constrained by those lessons, and accepted or blocked round summaries.
- gates_or_invariants: Summary validation is strict: `Action: none` must use `Lesson ID(s): NONE` or blank; `Action: add` and `Action: update` must reference concrete `BL-YYYYMMDD-short-name` IDs that exist in `.humanize/bitlesson.md`; `--require-bitlesson-entry-for-none` can disallow repeated empty-KB `none` reports (`docs/bitlesson.md:47`).
- dependencies_and_callers: Implemented by `scripts/bitlesson-init.sh`, `scripts/bitlesson-select.sh`, `scripts/bitlesson-validate-delta.sh`, `scripts/setup-rlcr-loop.sh`, and `hooks/loop-codex-stop-hook.sh`. Uses `config/default_config.json`, user/project config, and `templates/bitlesson.md`.
- edge_cases_or_failure_modes: Codex-only installs override older Claude defaults like `haiku`; missing provider binaries fall back to Codex; empty knowledge bases can report no matching lessons but can be gated if repeated `none` is disallowed.
- validation_or_tests: The code paths matching this doc are visible in `scripts/bitlesson-select.sh:115`, `scripts/bitlesson-select.sh:126`, `scripts/setup-rlcr-loop.sh:860`, and `scripts/bitlesson-validate-delta.sh:181`.
- skip_candidate: `no`

### HZ-068 `file` `tests/test-allowlist-validators.sh`
- cursor: `[_]`
- core_role: Executable specification for RLCR hook-validator allowlist behavior and anti-bypass protections around loop-managed files.
- algorithmic_behavior: Builds a temp git repo and active loop fixture with `current_round: 5` (`tests/test-allowlist-validators.sh:33`). Directly tests `is_allowlisted_file()` allowing only `round-1-todos.md`, `round-2-todos.md`, `round-0-summary.md`, and `round-1-summary.md` in the active loop directory, while rejecting future/stale files and wrong directories (`tests/test-allowlist-validators.sh:72`, `tests/test-allowlist-validators.sh:104`, `tests/test-allowlist-validators.sh:120`, `tests/test-allowlist-validators.sh:128`). Exercises Write, Edit, and Read validators with hook JSON, allowing historical allowed files and current `round-5-contract.md` but blocking stale/future summaries, todos, and contracts (`tests/test-allowlist-validators.sh:143`, `tests/test-allowlist-validators.sh:225`, `tests/test-allowlist-validators.sh:294`). Exercises Bash validator path restrictions: only exact active-loop todo paths are allowed; current contract writes via Bash are blocked; wrong directory, generic relative path, old loop directory, and same-basename different root are blocked (`tests/test-allowlist-validators.sh:376`, `tests/test-allowlist-validators.sh:402`, `tests/test-allowlist-validators.sh:415`, `tests/test-allowlist-validators.sh:454`, `tests/test-allowlist-validators.sh:469`). Verifies Bash cannot directly execute stop-hook/gate scripts through wrappers such as `bash`, `zsh`, `env`, `command`, assignments, `timeout`, `nice`, `nohup`, `strace`, or `ltrace` (`tests/test-allowlist-validators.sh:488`, `tests/test-allowlist-validators.sh:507`).
- inputs_outputs_state: Inputs are crafted hook JSON events for `Read`, `Write`, `Edit`, and `Bash`, plus an active loop dir and state file. Outputs are validator process exit codes and stderr messages; test pass/fail counters determine final exit status (`tests/test-allowlist-validators.sh:577`).
- gates_or_invariants: File allowlist is exact-path and active-loop scoped; current round contract is accessible through structured file tools but not Bash; Bash access to todos requires the full active-loop path; direct hook execution is always blocked as a bypass attempt.
- dependencies_and_callers: Sources `hooks/lib/loop-common.sh` and calls `hooks/loop-write-validator.sh`, `hooks/loop-edit-validator.sh`, `hooks/loop-read-validator.sh`, and `hooks/loop-bash-validator.sh` (`tests/test-allowlist-validators.sh:17`, `tests/test-allowlist-validators.sh:147`, `tests/test-allowlist-validators.sh:229`, `tests/test-allowlist-validators.sh:298`, `tests/test-allowlist-validators.sh:380`).
- edge_cases_or_failure_modes: Covers old loop directories, same basename under another root, missing full path in Bash, stale contracts, future todos, and wrapper-obfuscated direct execution.
- validation_or_tests: This file is itself the validation suite. Not executed in this read-only research run.
- skip_candidate: `no`

### HZ-098 `file` `tests/test-stop-hook-bg-allow.sh`
- cursor: `[_]`
- core_role: Executable specification for the RLCR stop hook’s background-task short-circuit, parked-loop markers, session isolation, transcript parsing, wrapper behavior, and liveness probes.
- algorithmic_behavior: The file states the core contract: if the current session has unfinished background Agent or Bash work, the stop hook must exit 0 with a user-facing `systemMessage`, skip normal gates/Codex review, and leave loop state unchanged (`tests/test-stop-hook-bg-allow.sh:3`). It enumerates acceptance criteria AC-1 through AC-24 (`tests/test-stop-hook-bg-allow.sh:12`). Fixtures create active or finalize loop states, summaries, and goal trackers sufficient to reach Codex when no background short-circuit applies (`tests/test-stop-hook-bg-allow.sh:101`). Transcript builders emit async Agent launches, background shell launches, legacy queue-operation completions, and SDK task notifications (`tests/test-stop-hook-bg-allow.sh:190`). The runner invokes `hooks/loop-codex-stop-hook.sh` with crafted JSON and a mocked Codex marker (`tests/test-stop-hook-bg-allow.sh:271`).
- algorithmic_behavior: Positive/negative flow tests prove: no background dispatch reaches Codex (`tests/test-stop-hook-bg-allow.sh:354`); pending Agent and Bash launches short-circuit with state unchanged and no Codex (`tests/test-stop-hook-bg-allow.sh:365`, `tests/test-stop-hook-bg-allow.sh:381`); completed Agent launches proceed normally (`tests/test-stop-hook-bg-allow.sh:397`); multiple pending tasks produce a count-aware message (`tests/test-stop-hook-bg-allow.sh:411`); missing, empty, or absent transcript path is fail-closed into normal Codex flow (`tests/test-stop-hook-bg-allow.sh:434`); no active loop exits silently (`tests/test-stop-hook-bg-allow.sh:456`); finalize phase also short-circuits on pending background work (`tests/test-stop-hook-bg-allow.sh:476`); `rlcr-stop-gate.sh` forwards transcript path and returns an `ALLOW` wrapper response (`tests/test-stop-hook-bg-allow.sh:491`).
- algorithmic_behavior: Path/session/marker tests prove tilde transcript paths are expanded both in hook and helper (`tests/test-stop-hook-bg-allow.sh:521`); parked foreign-session loops emit a distinct “parked” message and preserve marker/state (`tests/test-stop-hook-bg-allow.sh:606`); cross-session without marker remains isolated (`tests/test-stop-hook-bg-allow.sh:670`); short-circuit writes `bg-pending.marker` (`tests/test-stop-hook-bg-allow.sh:718`); exact session match wins over newer foreign marker candidates (`tests/test-stop-hook-bg-allow.sh:742`); same-session resume removes stale marker only after authoritative no-pending evidence (`tests/test-stop-hook-bg-allow.sh:793`); foreign sessions preserve marker and stored session id (`tests/test-stop-hook-bg-allow.sh:845`); validators’ default `find_active_loop` call ignores foreign markers while opt-in returns them (`tests/test-stop-hook-bg-allow.sh:1052`).
- algorithmic_behavior: Transcript parsing tests prove SDK `system/task_notification` completions and legacy XML completions both remove pending task IDs (`tests/test-stop-hook-bg-allow.sh:901`, `tests/test-stop-hook-bg-allow.sh:926`); missing/unreadable/nonexistent/malformed transcripts preserve marker/session because the helper fails closed (`tests/test-stop-hook-bg-allow.sh:955`, `tests/test-stop-hook-bg-allow.sh:1008`, `tests/test-stop-hook-bg-allow.sh:1155`); pre-loop background launches are filtered by a loop-start timestamp and timezone conversion is tested for UTC, Tokyo, and Los Angeles (`tests/test-stop-hook-bg-allow.sh:1205`, `tests/test-stop-hook-bg-allow.sh:1242`, `tests/test-stop-hook-bg-allow.sh:1257`, `tests/test-stop-hook-bg-allow.sh:1273`); wrapper calls without session id either write marker on a clean pending case or silently preserve a prior marker (`tests/test-stop-hook-bg-allow.sh:1312`, `tests/test-stop-hook-bg-allow.sh:1353`); liveness probes keep tasks alive when `lsof` reports holders and prune orphaned tasks when `lsof` reports no holders (`tests/test-stop-hook-bg-allow.sh:1410`, `tests/test-stop-hook-bg-allow.sh:1437`).
- inputs_outputs_state: Inputs are hook JSON with `transcript_path` and optional `session_id`, JSONL transcript events, active/finalize loop state files, `bg-pending.marker`, mocked `codex`, and mocked `lsof`. Outputs are hook JSON or silence, wrapper `ALLOW` lines, Codex marker presence/absence, marker creation/removal/preservation, and state-file hash stability.
- gates_or_invariants: Pending background tasks must be checked before git, summary, BitLesson, or Codex gates. Cross-session parked loops cannot be adopted without explicit opt-in. Empty-session callers with existing marker are ambiguous and must not mutate state. Marker cleanup requires a readable, parseable transcript whose pending set is empty. Pre-loop transcript events must not pin a newly started RLCR loop.
- dependencies_and_callers: Exercises `hooks/loop-codex-stop-hook.sh`, `scripts/rlcr-stop-gate.sh`, `hooks/lib/loop-common.sh`, and background helpers in `hooks/lib/loop-bg-tasks.sh` including `list_pending_background_task_ids`, `derive_loop_start_iso_ts`, and marker guard logic (`hooks/lib/loop-bg-tasks.sh:193`, `hooks/lib/loop-bg-tasks.sh:299`).
- edge_cases_or_failure_modes: Covers tilde paths, no active loop, finalize phase, foreign sessions, no-session wrappers, missing/malformed transcripts, legacy vs current transcript completion formats, pre-loop events, timezone offsets, and orphaned background task output files.
- validation_or_tests: This file is itself the validation suite. Not executed in this read-only research run.
- skip_candidate: `no`

### HZ-128 `file` `prompt-template/block/goal-tracker-bash-write.md`
- cursor: `[_]`
- core_role: Block-message template for the gate that prevents Bash-based mutation of `goal-tracker.md`.
- algorithmic_behavior: Renders a short denial message titled “Bash Write Blocked: Use Write or Edit Tool,” instructing the agent not to use Bash commands to modify `goal-tracker.md`, pointing to `{{CORRECT_PATH}}`, and explaining that `cat`, `echo`, `sed`, `awk`, etc. bypass validation hooks (`prompt-template/block/goal-tracker-bash-write.md:1`).
- inputs_outputs_state: Input is the `CORRECT_PATH` render variable supplied by hook helper code; output is a user-facing block reason. No persistent state is changed by the template itself.
- gates_or_invariants: The invariant is that goal-tracker mutation must go through structured Write/Edit tooling so validators can inspect content and preserve immutable sections. Bash mutation is disallowed even if the target path is otherwise correct.
- dependencies_and_callers: Called through `goal_tracker_bash_blocked_message` in `hooks/lib/loop-common.sh`, which loads this template with `CORRECT_PATH` (`hooks/lib/loop-common.sh:903`). It is used by Bash validator paths that detect goal-tracker modification attempts.
- edge_cases_or_failure_modes: If the template is missing, `loop-common.sh` has an inline fallback with the same functional message (`hooks/lib/loop-common.sh:905`). The template intentionally names common Bash write/edit utilities to catch attempts that might appear harmless but bypass validators.
- validation_or_tests: Bash validator behavior around protected loop files is covered broadly by `tests/test-allowlist-validators.sh`; direct line coverage for goal-tracker Bash writes is implied by the validator/helper integration rather than a named test case in the assigned file.
- skip_candidate: `no`

### HZ-158 `file` `prompt-template/claude/finalize-phase-prompt.md`
- cursor: `[_]`
- core_role: Transition prompt for the RLCR Finalize Phase after Codex review passes and implementation acceptance criteria are met.
- algorithmic_behavior: Directs the agent to use the `code-simplifier:code-simplifier` subagent via Task tool (`prompt-template/claude/finalize-phase-prompt.md:7`), constrains the phase to functionality-equivalent simplification only (`prompt-template/claude/finalize-phase-prompt.md:16`), scopes focus to recently changed code and the diff from `{{BASE_BRANCH}}` to `{{START_BRANCH}}` (`prompt-template/claude/finalize-phase-prompt.md:25`), and provides references to `{{PLAN_FILE}}` and `{{GOAL_TRACKER_FILE}}` (`prompt-template/claude/finalize-phase-prompt.md:36`). Before exit, it requires all `[mainline]` and `[blocking]` tasks to be completed, allows `[queued]` only as documented non-blocking follow-up, requires a commit, and requires a finalize summary at `{{FINALIZE_SUMMARY_FILE}}` (`prompt-template/claude/finalize-phase-prompt.md:41`).
- inputs_outputs_state: Inputs are rendered placeholders for base branch, start branch, original plan, goal tracker, and finalize summary path. Outputs are Task updates, simplification commits, test confirmation, and `finalize-summary.md` content. The template participates in the state transition where the stop hook renames active state into `finalize-state.md` and blocks normal round-contract behavior.
- gates_or_invariants: Must not change existing functionality, fail existing tests, introduce bugs, or perform non-equivalent refactoring (`prompt-template/claude/finalize-phase-prompt.md:18`). Blocking/mainline task completion gates remain active; queued work is explicitly non-blocking only.
- dependencies_and_callers: Rendered by `hooks/loop-codex-stop-hook.sh` when entering Finalize Phase after review success; the hook references this template with `FINALIZE_SUMMARY_FILE` and related placeholders (`hooks/loop-codex-stop-hook.sh:1325`, `hooks/loop-codex-stop-hook.sh:1386`).
- edge_cases_or_failure_modes: The prompt assumes the simplifier subagent exists; if unavailable, the surrounding hook fallback text still preserves the finalize contract. Queued work can remain only when documented as non-blocking, preventing finalize from silently skipping blocking defects.
- validation_or_tests: Finalize-state access and cancellation behavior are covered indirectly by assigned tests: `tests/test-stop-hook-bg-allow.sh` checks finalize-phase background short-circuit (`tests/test-stop-hook-bg-allow.sh:476`), and `tests/test-allowlist-validators.sh` covers contract/state access restrictions relevant to phase boundaries.
- skip_candidate: `no`

### HZ-188 `file` `tests/robustness/test-concurrent-state-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for RLCR state parsing, active-loop detection, concurrent reads, atomic write assumptions, stale-loop handling, and edge-case state files.
- algorithmic_behavior: Defines a `create_state_file` helper that writes YAML frontmatter with loop fields and body content (`tests/robustness/test-concurrent-state-robustness.sh:30`). State parsing tests check trailing whitespace, leading whitespace tolerance/defaulting, CRLF graceful degradation, empty `current_round`, inline comments, and Unicode body content (`tests/robustness/test-concurrent-state-robustness.sh:59`, `tests/robustness/test-concurrent-state-robustness.sh:80`, `tests/robustness/test-concurrent-state-robustness.sh:100`, `tests/robustness/test-concurrent-state-robustness.sh:123`, `tests/robustness/test-concurrent-state-robustness.sh:141`, `tests/robustness/test-concurrent-state-robustness.sh:373`). Loop detection tests require newest timestamped active directory, zombie-loop protection when newest has no state, `finalize-state.md` detection, and empty/nonexistent dir safety (`tests/robustness/test-concurrent-state-robustness.sh:170`, `tests/robustness/test-concurrent-state-robustness.sh:186`, `tests/robustness/test-concurrent-state-robustness.sh:203`, `tests/robustness/test-concurrent-state-robustness.sh:221`).
- algorithmic_behavior: Concurrent read tests spawn 10 simultaneous `get_current_round` readers and require all to read the real value, then simulate 50 atomic temp-file-plus-`mv` writes while performing reads and require all reads to remain numeric (`tests/robustness/test-concurrent-state-robustness.sh:250`, `tests/robustness/test-concurrent-state-robustness.sh:279`). Strict parser tests require malformed YAML and missing frontmatter to be rejected (`tests/robustness/test-concurrent-state-robustness.sh:332`, `tests/robustness/test-concurrent-state-robustness.sh:353`). Stale-loop tests require older state not to revive when newer dir is empty and `cancel-state.md` not to count as active (`tests/robustness/test-concurrent-state-robustness.sh:397`, `tests/robustness/test-concurrent-state-robustness.sh:413`). Edge tests cover very large round numbers, round zero, and unreadable state files (`tests/robustness/test-concurrent-state-robustness.sh:439`, `tests/robustness/test-concurrent-state-robustness.sh:456`, `tests/robustness/test-concurrent-state-robustness.sh:474`).
- inputs_outputs_state: Inputs are synthetic state directories/files under the test temp directory and helper functions from `hooks/lib/loop-common.sh`. Outputs are pass/fail records through `test-helpers.sh` and final test summary/exit status (`tests/robustness/test-concurrent-state-robustness.sh:503`).
- gates_or_invariants: `get_current_round` must not crash on malformed-but-readable state; `find_active_loop` must enforce newest-dir zombie protection and active-state-only semantics; state writers are expected to use atomic rename, not partial in-place writes; strict parsing must reject missing/malformed frontmatter.
- dependencies_and_callers: Sources `tests/test-helpers.sh`, then `hooks/lib/loop-common.sh` for `get_current_round`, `find_active_loop`, and `parse_state_file_strict` (`tests/robustness/test-concurrent-state-robustness.sh:17`, `tests/robustness/test-concurrent-state-robustness.sh:72`).
- edge_cases_or_failure_modes: CRLF may parse or default depending on platform tools; leading whitespace on keys may parse or default; unreadable files should return empty/zero rather than crash; root execution skips the permission-denied branch (`tests/robustness/test-concurrent-state-robustness.sh:477`).
- validation_or_tests: This file is itself the validation suite. Not executed in this read-only research run.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7/7 item-evidence headings present, one heading per assigned row
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`