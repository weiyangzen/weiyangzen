# agent_07 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-007 `directory` `prompt-template`
- cursor: `[_]`
- core_role: Declarative prompt/control surface for Humanize RLCR. The directory is recursively organized into `block/`, `claude/`, `codex/`, `idea/`, and `plan/`; `hooks/lib/template-loader.sh` resolves this directory from the plugin root and validates required subdirectories `block`, `codex`, `claude`, and `plan` at `hooks/lib/template-loader.sh:24-31` and `hooks/lib/template-loader.sh:213-238`.
- algorithmic_behavior: Templates are loaded by name and rendered with `{{VAR}}` placeholders through a single-pass renderer, so injected values are not re-scanned for nested placeholders (`hooks/lib/template-loader.sh:48-56`, `hooks/lib/template-loader.sh:69-135`). Missing templates fall back through `load_and_render_safe`, preserving gate behavior even if a template file is absent (`hooks/lib/template-loader.sh:185-211`). `block/` templates define stop/read/write/bash rejection messages and recovery instructions; `claude/` templates drive worker continuation, drift recovery, review phase, finalize phase, methodology analysis, open-question handling, and agent-team behavior; `codex/` templates define Codex review prompts and completion/verdict contracts; `plan/` and `idea/` templates define output schemas for plan and idea generation.
- inputs_outputs_state: Inputs are template names plus substitution variables such as `PLAN_FILE`, `REVIEW_CONTENT`, `GOAL_TRACKER_FILE`, `ROUND_CONTRACT_FILE`, `STALL_COUNT`, and `LAST_MAINLINE_VERDICT`. Outputs are rendered prompt files in `.humanize/rlcr/<timestamp>/round-N-prompt.md`, review prompt/result files, block reasons in hook JSON, and planning artifacts. Setup writes initial prompts and summary scaffolds using prompt content and inlined/fallback template fragments (`scripts/setup-rlcr-loop.sh:1191-1431`). The stop hook writes next-round prompts from `claude/next-round-prompt.md` or `claude/drift-replan-prompt.md` and blocks stop with the generated prompt as JSON reason (`hooks/loop-codex-stop-hook.sh:1990-2221`).
- gates_or_invariants: Template-level invariants include single-pass substitution, fallback-on-missing-template, and required subdir presence. Semantic invariants include: Codex must emit `Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED` in implementation review prompts (`prompt-template/codex/regular-review.md:56-61`, `prompt-template/codex/full-alignment-review.md:59-67`); Claude must maintain one mainline objective and classify `[mainline]`, `[blocking]`, and `[queued]` work (`prompt-template/claude/next-round-prompt.md:12-40`); review phase must not let findings become a new objective (`prompt-template/claude/review-phase-prompt.md:5-24`); finalize must preserve functionality and tests (`prompt-template/claude/finalize-phase-prompt.md:16-23`).
- dependencies_and_callers: `hooks/lib/loop-common.sh` uses `block/git-add-humanize.md`, `block/git-tracked-humanize.md`, state-file modification blocks, and tracker/summary/contract block templates (`hooks/lib/loop-common.sh:1378-1466`, `hooks/lib/loop-common.sh:1012-1019`). `hooks/loop-codex-stop-hook.sh` uses Claude/Codex phase templates for review, drift, next round, footer, push notes, open-question notices, methodology analysis, and agent teams (`hooks/loop-codex-stop-hook.sh:1551-1558`, `hooks/loop-codex-stop-hook.sh:2055-2075`, `hooks/loop-codex-stop-hook.sh:2160-2181`, `hooks/loop-codex-stop-hook.sh:2183-2203`). `scripts/setup-rlcr-loop.sh` injects `claude/agent-teams-instructions.md` and `claude/agent-teams-core.md` into round-0 prompts when enabled (`scripts/setup-rlcr-loop.sh:1370-1391`). `commands/gen-plan.md` and `commands/refine-plan.md` reference `plan/` templates and `config/default_config.json` for generation mode.
- edge_cases_or_failure_modes: Missing template directory causes a warning and inline fallback behavior rather than immediate crash (`hooks/lib/loop-common.sh:242-248`). Missing individual template produces fallback text through `load_and_render_safe`. Missing mainline verdict in Codex output is a hard safety block (`hooks/loop-codex-stop-hook.sh:1823-1829`). Agent-team templates are optional but have inline fallback instructions if absent (`scripts/setup-rlcr-loop.sh:1374-1390`, `hooks/loop-codex-stop-hook.sh:2186-2201`). `block/` includes targeted edge-case messages for tracked `.humanize/`, wrong round files, stale schema, missing summaries/contracts, BitLesson delta failures, large files, direct hook execution, and git cleanliness.
- validation_or_tests: `tests/test-template-references.sh` scans template references. `tests/test-agent-teams.sh` validates agent-team templates exist and contain expected guidance (`tests/test-agent-teams.sh:331-388`). `tests/test-stop-gate.sh` validates `block/git-tracked-humanize.md` behavior through `rlcr-stop-gate` (`tests/test-stop-gate.sh:118-145`). `tests/test-gen-plan.sh` and `tests/test-refine-plan.sh` validate plan templates.
- skip_candidate: `no`

### HZ-037 `file` `config/default_config.json`
- cursor: `[_]`
- core_role: Default runtime configuration layer for model routing, RLCR effort, BitLesson model, agent-teams default, alternative-language plan output, and plan-generation interaction mode.
- algorithmic_behavior: The file provides default JSON object values: `codex_model: gpt-5.5`, `codex_effort: high`, `bitlesson_model: haiku`, `agent_teams: false`, empty `alternative_plan_language`, and `gen_plan_mode: discussion` (`config/default_config.json:1-8`). `scripts/lib/config-loader.sh` requires this file as the mandatory default layer, merges it with optional user and project config using jq object multiplication, and strips `null` values before applying later layers (`scripts/lib/config-loader.sh:63-136`).
- inputs_outputs_state: Inputs are default config plus optional user config at `$XDG_CONFIG_HOME/humanize/config.json` or `~/.config/humanize/config.json`, and optional project config at `.humanize/config.json` or `$HUMANIZE_CONFIG` (`scripts/lib/config-loader.sh:77-88`). Output is merged JSON consumed by `get_config_value` (`scripts/lib/config-loader.sh:139-159`). In `loop-common.sh`, merged values become process defaults: `DEFAULT_CODEX_MODEL`, `DEFAULT_CODEX_EFFORT`, `DEFAULT_BITLESSON_MODEL`, and `DEFAULT_AGENT_TEAMS` (`hooks/lib/loop-common.sh:204-236`).
- gates_or_invariants: Required default config must exist and be a JSON object (`scripts/lib/config-loader.sh:40-60`, `scripts/lib/config-loader.sh:108-111`). `codex_model` must match safe characters and supported prefixes `gpt-` or `o[0-9]`; invalid values warn and fall back (`hooks/lib/loop-common.sh:211-222`). `codex_effort` must be one of `xhigh`, `high`, `medium`, or `low` (`hooks/lib/loop-common.sh:223-230`). `agent_teams` config can enable the feature, but the setup script still requires `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` when effective value is true (`scripts/setup-rlcr-loop.sh:381-397`).
- dependencies_and_callers: `scripts/setup-rlcr-loop.sh` reads default values indirectly through `loop-common.sh`, then writes chosen values to loop state (`scripts/setup-rlcr-loop.sh:29-54`, `scripts/setup-rlcr-loop.sh:880-905`). `scripts/install-skill.sh` uses the default config to seed Codex-friendly user config and derive a default BitLesson model (`scripts/install-skill.sh:269-315`). `commands/gen-plan.md` and `commands/refine-plan.md` use `alternative_plan_language` and `gen_plan_mode` as user-facing planning controls.
- edge_cases_or_failure_modes: Missing or malformed required default config is fatal. Malformed optional user/project configs are ignored with warnings. Unsupported Codex model/effort values fall back rather than corrupting state. `agent_teams: true` from project config can make setup fail if the experimental env var is not exactly `1`, as exercised by `tests/test-agent-teams.sh:156-186`.
- validation_or_tests: `tests/test-config-merge.sh`, `tests/test-config-error-handling.sh`, and `tests/test-unified-codex-config.sh` cover merge and validation behavior. `tests/test-agent-teams.sh` verifies project config can enable `agent_teams` and records `agent_teams: true` in state (`tests/test-agent-teams.sh:156-186`). `tests/run-all-tests.sh` includes config tests at `tests/run-all-tests.sh:86-89`.
- skip_candidate: `no`

### HZ-067 `file` `tests/test-agent-teams.sh`
- cursor: `[_]`
- core_role: Executable specification for `--agent-teams` routing through setup, state parsing, prompt composition, and stop-hook continuation behavior.
- algorithmic_behavior: The test first asserts setup rejects `--agent-teams` unless `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` is exactly `1` (`tests/test-agent-teams.sh:54-79`), then asserts successful setup creates state and records `agent_teams: true` when enabled (`tests/test-agent-teams.sh:82-121`). It verifies default false behavior without the flag (`tests/test-agent-teams.sh:124-153`), project config enablement (`tests/test-agent-teams.sh:156-186`), parsing via `parse_state_file` (`tests/test-agent-teams.sh:189-242`), initial prompt injection (`tests/test-agent-teams.sh:245-328`), template presence/content (`tests/test-agent-teams.sh:331-388`), active loop discovery with `agent_teams` metadata (`tests/test-agent-teams.sh:391-414`), and stop-hook next-round behavior in implementation, drift recovery, disabled, and review-phase cases (`tests/test-agent-teams.sh:416-734`).
- inputs_outputs_state: Inputs are temporary git repos, plan files, `.humanize/config.json`, synthetic `state.md` frontmatter, mocked `codex` binaries, and Stop hook JSON. Outputs are `state.md`, `round-0-prompt.md`, next-round prompts such as `round-4-prompt.md`, hook exit/block behavior, and pass/fail summary. State transitions under test include setup writing `agent_teams`, stop hook reading it into `AGENT_TEAMS`, and next-round prompt generation appending team continuation only when still in implementation phase.
- gates_or_invariants: Effective agent teams requires env var value `1`, not `0`, `false`, `yes`, or `true` (`tests/test-agent-teams.sh:71-79`). State defaults `agent_teams` to false if missing (`tests/test-agent-teams.sh:218-242`). Initial and continuation prompts must contain team-leader/coordination guidance when enabled (`tests/test-agent-teams.sh:271-288`, `tests/test-agent-teams.sh:616-626`) and omit it otherwise (`tests/test-agent-teams.sh:315-328`, `tests/test-agent-teams.sh:692-700`). Review phase must not include agent-team continuation even if `agent_teams: true` (`tests/test-agent-teams.sh:704-734`).
- dependencies_and_callers: Sources `tests/test-helpers.sh` and `hooks/lib/loop-common.sh` (`tests/test-agent-teams.sh:17-22`). Drives `scripts/setup-rlcr-loop.sh` and `hooks/loop-codex-stop-hook.sh` (`tests/test-agent-teams.sh:29`, `tests/test-agent-teams.sh:427-429`). It is listed in the full test runner (`tests/run-all-tests.sh:91-93`). It relies on `prompt-template/claude/agent-teams-instructions.md`, `agent-teams-core.md`, and `agent-teams-continue.md`.
- edge_cases_or_failure_modes: Covers missing env var, bad env var values, absent `agent_teams` field, project config enablement without CLI flag, missing prompt file, drift recovery after repeated stalled verdicts, disabled agent-team prompts, and review-phase exclusion. The test currently uses `|| true` around some setup invocations, then checks output artifacts, so artifact absence rather than exit code is the later failure signal.
- validation_or_tests: This file is itself the validation. It indirectly validates `scripts/setup-rlcr-loop.sh:280-397`, state creation at `scripts/setup-rlcr-loop.sh:880-905`, `parse_state_file` at `hooks/lib/loop-common.sh:471-522`, and stop-hook agent-team insertion at `hooks/loop-codex-stop-hook.sh:2092-2114` and `hooks/loop-codex-stop-hook.sh:2183-2203`.
- skip_candidate: `no`

### HZ-097 `file` `tests/test-stop-gate.sh`
- cursor: `[_]`
- core_role: Executable specification for the `scripts/rlcr-stop-gate.sh` wrapper that exposes Stop-hook RLCR gating outside the native hook environment.
- algorithmic_behavior: The test builds minimal active loop fixtures that should block because required summary state is incomplete (`tests/test-stop-gate.sh:18-59`). It verifies the wrapper uses caller cwd as project root by default (`tests/test-stop-gate.sh:64-89`), accepts `--project-root` from outside the repo (`tests/test-stop-gate.sh:91-116`), blocks tracked `.humanize/` state before normal loop validation (`tests/test-stop-gate.sh:118-145`), ignores `.humanize-backup` and `.humanizeconfig` as non-loop paths (`tests/test-stop-gate.sh:147-178`), allows exit when no active loop exists (`tests/test-stop-gate.sh:180-204`), and preserves `transcript_path` when `session_id` is empty in generated hook JSON (`tests/test-stop-gate.sh:206-287`).
- inputs_outputs_state: Inputs are temp projects, loop directories, `state.md` frontmatter, forced git-index additions under `.humanize/`, wrapper CLI flags, `CLAUDE_PROJECT_DIR`, mock `loop-codex-stop-hook.sh`, and jq-parsed captured JSON. Outputs are wrapper exit codes `0`, `10`, or error codes, stdout lines beginning `ALLOW:` or `BLOCK:`, and hook input JSON containing `session_id: null` plus `transcript_path`.
- gates_or_invariants: Exit `10` means the underlying gate blocked, exit `0` means allow, and exit `20` means wrapper/runtime error (`scripts/rlcr-stop-gate.sh:8-15`). Empty `session_id` and empty `transcript_path` are represented independently as JSON `null`, avoiding jq object-collapse from `select(length > 0)` (`scripts/rlcr-stop-gate.sh:99-123`). Tracked/staged `.humanize/` must block with the dedicated “Tracked Humanize State Blocked” template, while similarly named dotfiles must not (`tests/test-stop-gate.sh:140-178`).
- dependencies_and_callers: Tests `scripts/rlcr-stop-gate.sh`, which sources `hooks/lib/project-root.sh`, builds Stop-event JSON, runs `hooks/loop-codex-stop-hook.sh`, and maps hook JSON decisions to CLI status (`scripts/rlcr-stop-gate.sh:19-178`). The tracked-state behavior depends on `git_has_tracked_humanize_state` and `git_tracked_humanize_blocked_message` in `hooks/lib/loop-common.sh:1418-1466`, and on `prompt-template/block/git-tracked-humanize.md`.
- edge_cases_or_failure_modes: Covers cwd-vs-plugin-root confusion, project-root override, no project root/no active loop benign allow, tracked Humanize state blocking before normal missing-summary validation, false positives for `.humanize-backup` and `.humanizeconfig`, missing `jq`, missing hook executable, hook non-JSON output, hook non-zero exit, and JSON field loss when one optional field is empty.
- validation_or_tests: This file is the validation. It directly exercises wrapper output and exit behavior and includes a mock hook regression test for JSON generation (`tests/test-stop-gate.sh:219-287`).
- skip_candidate: `no`

### HZ-127 `file` `prompt-template/block/git-tracked-humanize.md`
- cursor: `[_]`
- core_role: Dedicated block-message template for the invariant that local Humanize loop state under `.humanize/` must never be tracked, staged, or committed.
- algorithmic_behavior: The template tells the agent/user that tracked or staged `.humanize/` files were detected, instructs removal from the index via `git rm --cached -r .humanize`, and forbids force-adding or committing RLCR trackers, summaries, contracts, and cancel/finalize markers (`prompt-template/block/git-tracked-humanize.md:1-19`). It is rendered by `git_tracked_humanize_blocked_message` with no variables, falling back to equivalent inline text if missing (`hooks/lib/loop-common.sh:1442-1466`).
- inputs_outputs_state: Input is a guard decision from `git_has_tracked_humanize_state`, which runs `git ls-files -- .humanize` against the project root (`hooks/lib/loop-common.sh:1418-1440`). Output is a Markdown block reason surfaced by `rlcr-stop-gate` or hook/validator logic. It does not mutate state; it guides the user to unstage local loop state so the stop gate can proceed.
- gates_or_invariants: The invariant is scoped exactly to `.humanize/`, not `.humanize-backup` or `.humanizeconfig`, matching comments in `git_has_tracked_humanize_state` (`hooks/lib/loop-common.sh:1422-1427`). The required remediation is index-only removal; real project files can remain staged (`prompt-template/block/git-tracked-humanize.md:7-14`).
- dependencies_and_callers: Called by `git_tracked_humanize_blocked_message` (`hooks/lib/loop-common.sh:1442-1466`). Verified through `scripts/rlcr-stop-gate.sh` by `tests/test-stop-gate.sh:118-178`. Related but distinct from `prompt-template/block/git-add-humanize.md`, which blocks new `git add` commands that would add `.humanize/`.
- edge_cases_or_failure_modes: Main edge cases are false positives on similarly named dotfiles and allowing recovery after `git rm --cached` removes `.humanize/` from the index. If the template file is missing, fallback text preserves the block semantics. If `.humanize/` is untracked only, this template should not trigger.
- validation_or_tests: `tests/test-stop-gate.sh` forces `.humanize/rlcr/.../goal-tracker.md` into the git index and expects the dedicated reason (`tests/test-stop-gate.sh:118-145`), then stages `.humanize-backup` and `.humanizeconfig` and expects no tracked-state reason (`tests/test-stop-gate.sh:147-178`).
- skip_candidate: `no`

### HZ-157 `file` `prompt-template/claude/drift-replan-prompt.md`
- cursor: `[_]`
- core_role: Recovery prompt template used when Codex judges consecutive implementation rounds as stalled or regressed and the loop must re-anchor rather than continue ordinary issue-clearing.
- algorithmic_behavior: The template declares “Drift Recovery Mode,” reports `{{STALL_COUNT}}` and `{{LAST_MAINLINE_VERDICT}}`, forces re-reading `@{{PLAN_FILE}}`, `@{{GOAL_TRACKER_FILE}}`, recent summaries/reviews, and rewriting `@{{ROUND_CONTRACT_FILE}}` before code changes (`prompt-template/claude/drift-replan-prompt.md:3-33`). It requires exactly one recovered mainline objective, 1-2 target ACs, a root-cause explanation, blocking/queued split, and success criteria that would restore `ADVANCED` (`prompt-template/claude/drift-replan-prompt.md:25-31`). It embeds Codex review content and adds tracker reconciliation and guardrails (`prompt-template/claude/drift-replan-prompt.md:48-68`).
- inputs_outputs_state: Inputs are rendered values from stop-hook state and review result: `PLAN_FILE`, `REVIEW_CONTENT`, `GOAL_TRACKER_FILE`, `BITLESSON_FILE`, `ROUND_CONTRACT_FILE`, `CURRENT_ROUND`, `STALL_COUNT`, and `LAST_MAINLINE_VERDICT` (`hooks/loop-codex-stop-hook.sh:2055-2064`). Output is `round-N-prompt.md` and a blocking Stop-hook JSON reason that drives the next implementation round (`hooks/loop-codex-stop-hook.sh:2211-2219`).
- gates_or_invariants: Triggered when implementation review verdict is `STALLED` or `REGRESSED` and consecutive stall count reaches at least 2, setting `drift_status: replan_required`; at 3, the mainline drift circuit breaker may stop the loop (`hooks/loop-codex-stop-hook.sh:1831-1848`, `hooks/loop-codex-stop-hook.sh:1947-1950`). The template forbids starting implementation before the recovery contract exists (`prompt-template/claude/drift-replan-prompt.md:33`) and forbids queued cleanup from replacing mainline recovery (`prompt-template/claude/drift-replan-prompt.md:63-68`).
- dependencies_and_callers: Called only by `hooks/loop-codex-stop-hook.sh` when `DRIFT_REPLAN_REQUIRED=true` (`hooks/loop-codex-stop-hook.sh:2055-2065`). It depends on Codex review templates requiring a mainline verdict (`prompt-template/codex/regular-review.md:56-61`) and on `extract_mainline_progress_verdict` normalization (`hooks/lib/loop-common.sh:634-662`). Agent-team continuation can be appended after this template if `agent_teams` is true and review has not started (`hooks/loop-codex-stop-hook.sh:2183-2203`).
- edge_cases_or_failure_modes: If Codex omits or ambiguously includes the verdict, stop hook blocks via missing-verdict handling instead of rendering drift recovery (`hooks/loop-codex-stop-hook.sh:1823-1829`). If this template is missing, the hook uses `DRIFT_REPLAN_FALLBACK` with a narrower recovery prompt (`hooks/loop-codex-stop-hook.sh:2038-2056`). If BitLesson is required but the rendered recovery prompt lacks `bitlesson-selector`, the hook appends a BitLesson section (`hooks/loop-codex-stop-hook.sh:2077-2090`).
- validation_or_tests: `tests/test-agent-teams.sh` simulates repeated stalled verdicts, expects “Drift Recovery Mode,” and verifies agent-team continuation is preserved in drift recovery prompts (`tests/test-agent-teams.sh:632-669`). Drift-state parsing and normalization are exercised by stop-hook robustness and task-tag routing tests.
- skip_candidate: `no`

### HZ-187 `file` `tests/robustness/test-cancel-security-robustness.sh`
- cursor: `[_]`
- core_role: Robustness executable spec for the cancel authorization algorithm that permits only intentional, signal-file-backed loop cancellation and rejects path/injection bypasses.
- algorithmic_behavior: The test sources `hooks/lib/loop-common.sh`, creates a mock active loop with `state.md`, and calls `is_cancel_authorized "$LOOP_DIR" "$COMMAND_LOWER"` directly (`tests/robustness/test-cancel-security-robustness.sh:13-30`). Positive cases accept `mv state.md cancel-state.md`, `mv finalize-state.md cancel-state.md`, single quotes, and unquoted paths when `.cancel-requested` exists (`tests/robustness/test-cancel-security-robustness.sh:39-89`). Negative cases reject missing signal, command substitution, backticks, semicolon/`&&`/pipe/`||`, wrong source/destination, extra args, newline injection, remaining variable expansion, non-`mv`, mixed quotes, IFS manipulation, trailing spaces, non-standard source filename, source symlinks, and paths containing `finalize` that might fool substring checks (`tests/robustness/test-cancel-security-robustness.sh:99-405`).
- inputs_outputs_state: Inputs are loop directory path, a lowercased command string, on-disk `.cancel-requested`, `state.md` or `finalize-state.md`, and symlink fixtures. Output is the return code of `is_cancel_authorized`; success authorizes bash-validator state-file move operations, while non-zero blocks. The real cancel script creates `.cancel-requested` then renames active state to `cancel-state.md` (`scripts/cancel-rlcr-loop.sh:156-167`).
- gates_or_invariants: `is_cancel_authorized` requires the signal file first (`hooks/lib/loop-common.sh:1055-1060`), rejects substitution/newlines/shell operators/trailing-space anomalies (`hooks/lib/loop-common.sh:1062-1080`), normalizes `$loop_dir`/`${loop_dir}` then rejects remaining `$` (`hooks/lib/loop-common.sh:1092-1104`), requires `mv` with exactly two args (`hooks/lib/loop-common.sh:1106-1189`), canonicalizes path prefixes without dereferencing leaf symlinks (`hooks/lib/loop-common.sh:1191-1229`), and rejects symlink source files by exact source classification (`hooks/lib/loop-common.sh:1232-1247`).
- dependencies_and_callers: `hooks/loop-bash-validator.sh` calls `is_cancel_authorized` before blocking state-file modifications, allowing the authorized cancel move while blocking normal edits/moves/copies (`hooks/loop-bash-validator.sh:271-297`, `hooks/loop-bash-validator.sh:428-488`). `scripts/cancel-rlcr-loop.sh` is the intended producer of the `.cancel-requested` signal and state rename (`scripts/cancel-rlcr-loop.sh:103-167`).
- edge_cases_or_failure_modes: Covers parser tricks including mixed quote delimiters, broad shell operators, variable and IFS expansion, multiline commands, extra args, symlink aliases, symlink leaf files, non-standard `state-link.md`, and loop paths containing the substring `finalize` so exact path matching is required (`tests/robustness/test-cancel-security-robustness.sh:369-405`). It currently lowercases commands before validation, matching hook behavior but relying on canonicalized lowercased comparisons for case-insensitive filesystems (`hooks/lib/loop-common.sh:1202-1209`).
- validation_or_tests: This file is direct validation of `is_cancel_authorized`. Related broader coverage exists in `tests/test-cancel-signal-file.sh`, which includes additional helper tests for symlinked prefix paths and destination/source aliases.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: `HZ-007`, `HZ-037`, `HZ-067`, `HZ-097`, `HZ-127`, `HZ-157`, `HZ-187`
- missing_items: `none`
- duplicate_items: `none`
- final_worker_status: `complete`