# agent_21 1:1 Core Algorithm Research

## Worker Summary
- status: `[_]`
- assigned_item_count: 7
- source_commit: `0ec921a36b4365df503511c5567bbd3e02db0df5`

## Item Evidence

### HZ-021 `directory` `skills/ask-gemini`
- cursor: `[_]`
- core_role: Defines the `/humanize:ask-gemini` skill command contract. The directory recursively contains only `skills/ask-gemini/SKILL.md`, so its algorithmic role is command exposure, safe argument handling guidance, output interpretation, and delegation to `scripts/ask-gemini.sh`.
- algorithmic_behavior: Skill frontmatter names the command, argument hint, and allowed runtime script at `skills/ask-gemini/SKILL.md:1-5`. The usage section requires quoting free-form user text and reconstructing `--gemini-model` / `--gemini-timeout` as separate shell arguments before passing the remaining question as one quoted argument (`skills/ask-gemini/SKILL.md:15-39`). Runtime behavior is delegated to `scripts/ask-gemini.sh`, which parses flags (`scripts/ask-gemini.sh:91-136`), validates Gemini CLI presence, nonempty question, and model-name characters (`scripts/ask-gemini.sh:145-168`), resolves project root (`scripts/ask-gemini.sh:174-178`), creates project-local and cache state directories (`scripts/ask-gemini.sh:184-199`), augments the prompt with a Google Search instruction (`scripts/ask-gemini.sh:236-241`), runs Gemini through `run_with_timeout` (`scripts/ask-gemini.sh:281-283`), and classifies timeout, nonzero, empty, and success results (`scripts/ask-gemini.sh:294-388`).
- inputs_outputs_state: Inputs are skill arguments, optional `--gemini-model`, optional `--gemini-timeout`, `CLAUDE_PLUGIN_ROOT`, and runtime env such as `HUMANIZE_GEMINI_YOLO`. Successful stdout is Gemini’s response; stderr carries status and debug paths (`skills/ask-gemini/SKILL.md:41-46`). State is written under `.humanize/skill/<unique-id>/input.md`, `output.md`, and `metadata.md`, with cache/debug artifacts under `~/.cache/humanize/...` or `.humanize/skill/<id>/cache` if home cache is unwritable (`scripts/ask-gemini.sh:187-199`, `scripts/ask-gemini.sh:205-218`, `scripts/ask-gemini.sh:368-382`).
- gates_or_invariants: The skill contract forbids unsafe unquoted `$ARGUMENTS` expansion (`skills/ask-gemini/SKILL.md:33-39`). The runtime blocks missing Gemini CLI, empty question, invalid model characters, timeout exit `124`, nonzero Gemini process exits, and empty stdout (`scripts/ask-gemini.sh:145-168`, `scripts/ask-gemini.sh:294-361`). Default mode uses Gemini sandbox unless `HUMANIZE_GEMINI_YOLO=true|1` is set (`scripts/ask-gemini.sh:226-231`).
- dependencies_and_callers: Depends on `scripts/ask-gemini.sh`, which sources `scripts/portable-timeout.sh` and `hooks/lib/project-root.sh` (`scripts/ask-gemini.sh:27-33`). Monitoring support references ask-gemini invocations through `scripts/lib/monitor-skill.sh:6` and CLI monitor help in `scripts/humanize.sh:1219`.
- edge_cases_or_failure_modes: Free-form questions can contain shell metacharacters, hence strict quoting guidance (`skills/ask-gemini/SKILL.md:17-39`). Timeout must be numeric, though `0` passes the numeric regex and is left to timeout implementation behavior (`scripts/ask-gemini.sh:113-123`). A read-only project checkout would prevent the runtime from creating `.humanize/skill/...`; this directory itself only defines the command, not the storage fallback. Empty Gemini stdout is treated as failure even if Gemini exits `0` (`scripts/ask-gemini.sh:340-361`).
- validation_or_tests: No dedicated test file for the skill directory was found in the assigned set. The runtime behavior is covered indirectly by shared timeout tests and monitor references; output/error semantics are specified in the skill table (`skills/ask-gemini/SKILL.md:48-55`).
- skip_candidate: `no`

### HZ-051 `file` `scripts/bitlesson-init.sh`
- cursor: `[_]`
- core_role: Idempotent initializer for the project BitLesson knowledge base file used by RLCR/BitLesson workflows. It ensures a configured project-local lesson file exists before loop state starts using it.
- algorithmic_behavior: Defines CLI usage and default target `.humanize/bitlesson.md` (`scripts/bitlesson-init.sh:5-20`). Parses `--project-root`, `--template`, and optional `--bitlesson-relpath` (`scripts/bitlesson-init.sh:22-46`). Validates required arguments and that project root and template exist (`scripts/bitlesson-init.sh:48-68`). Rejects absolute or parent-traversing target relpaths (`scripts/bitlesson-init.sh:70-73`). Canonicalizes project root, builds the destination path, rejects pre-existing non-regular targets, creates the parent directory, copies the template only when missing, and prints the resolved destination path (`scripts/bitlesson-init.sh:75-88`).
- inputs_outputs_state: Inputs are `--project-root`, `--template`, and `--bitlesson-relpath`. Output is the absolute BitLesson file path on stdout (`scripts/bitlesson-init.sh:14`, `scripts/bitlesson-init.sh:88`); validation errors go to stderr with exit `1`. State transition is missing target -> created from template; existing regular target -> preserved without overwrite (`scripts/bitlesson-init.sh:78-86`).
- gates_or_invariants: Does not overwrite an existing regular file (`scripts/bitlesson-init.sh:12-13`, `scripts/bitlesson-init.sh:83-86`). Refuses targets outside project scope by rejecting leading `/` and `..` path segments (`scripts/bitlesson-init.sh:70-73`). Requires the source template to be an existing file (`scripts/bitlesson-init.sh:65-68`).
- dependencies_and_callers: Called by RLCR setup with `templates/bitlesson.md` as the source template (`scripts/setup-rlcr-loop.sh:860-866`). Stop-hook BitLesson logic derives and sanitizes `BITLESSON_FILE_REL` before using the same default path family (`hooks/loop-codex-stop-hook.sh:148-158`). The copied template defines strict BitLesson entry fields and the `## Entries` insertion area (`templates/bitlesson.md:1-23`).
- edge_cases_or_failure_modes: An option without its value can fail during `shift 2` under `set -euo pipefail` before the later empty-value validation. Existing directories, device files, or other non-regular targets block initialization (`scripts/bitlesson-init.sh:78-81`). Symlinks to regular files are accepted by `-f`; the script does not canonicalize the template path. The relpath guard rejects path-segment `..` but allows names merely containing two dots as part of a segment.
- validation_or_tests: BitLesson init is exercised by `tests/test-bitlesson-validate-delta.sh:58`. Broader BitLesson routing and stop-hook compatibility tests reference `.humanize/bitlesson.md` in `tests/test-bitlesson-select-routing.sh` and `tests/test-stop-hook-legacy-compat.sh`.
- skip_candidate: `no`

### HZ-081 `file` `tests/test-error-scenarios.sh`
- cursor: `[_]`
- core_role: Executable error-scenario specification for `hooks/lib/template-loader.sh`. It verifies that template rendering failures degrade gracefully instead of crashing hooks or strict-mode scripts.
- algorithmic_behavior: Sources `hooks/lib/template-loader.sh`, computes `TEMPLATE_DIR`, and initializes local pass/fail counters (`tests/test-error-scenarios.sh:11-35`). Test cases assert: missing template file returns empty stdout and exit `0` (`tests/test-error-scenarios.sh:45-52`), missing template directory behaves the same (`tests/test-error-scenarios.sh:58-65`), `load_and_render` with missing template returns empty (`tests/test-error-scenarios.sh:71-78`), empty template content renders empty (`tests/test-error-scenarios.sh:84-91`), special regex-like characters in values are preserved (`tests/test-error-scenarios.sh:97-106`), strict-mode callers continue after missing templates (`tests/test-error-scenarios.sh:112-131`), safe rendering falls back and substitutes fallback variables (`tests/test-error-scenarios.sh:137-158`), template directory validation accepts/rejects expected directories (`tests/test-error-scenarios.sh:164-180`), and malformed placeholders stay literal (`tests/test-error-scenarios.sh:186-206`).
- inputs_outputs_state: Inputs are the repository template directory and inline test templates/values. Outputs are PASS/FAIL lines plus a summary. No persistent repository state is modified. Exit status is `0` only when `TESTS_FAILED` is zero (`tests/test-error-scenarios.sh:211-226`).
- gates_or_invariants: Error paths must return controlled empty output or fallback content rather than aborting the caller. Tests suppress stderr where warnings are expected and assert command-substitution exit codes remain usable (`tests/test-error-scenarios.sh:46-60`, `tests/test-error-scenarios.sh:72-87`).
- dependencies_and_callers: Directly depends on `hooks/lib/template-loader.sh` and its `get_template_dir`, `load_template`, `render_template`, `load_and_render`, `load_and_render_safe`, and `validate_template_dir` functions (`tests/test-error-scenarios.sh:13-15`). Included in the aggregate test runner at `tests/run-all-tests.sh:72`.
- edge_cases_or_failure_modes: The script itself uses `set -uo pipefail`, not global `set -e`, so it can collect multiple failures in one run (`tests/test-error-scenarios.sh:9`). It separately spawns a strict-mode subshell to prove callers using `set -euo pipefail` survive missing templates (`tests/test-error-scenarios.sh:114-126`). It depends on expected empty stdout when stderr is redirected away.
- validation_or_tests: This file is the validation artifact. It complements broader template coverage in `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, and robustness template tests referenced by `tests/robustness/test-template-error-robustness.sh`.
- skip_candidate: `no`

### HZ-111 `file` `hooks/lib/template-loader.sh`
- cursor: `[_]`
- core_role: Central template loading and rendering library for RLCR hooks, validators, and prompt generation. It converts markdown prompt/block templates into runtime messages while preserving hook stability when templates are missing.
- algorithmic_behavior: Documents placeholder syntax and single-pass behavior (`hooks/lib/template-loader.sh:7-21`). `get_template_dir` maps `hooks/lib` to plugin `prompt-template` (`hooks/lib/template-loader.sh:24-31`). `load_template` prints file contents or warns to stderr while returning no stdout for missing files (`hooks/lib/template-loader.sh:33-46`). `render_template` builds `TMPL_VAR_` environment entries from `VAR=value` arguments, then uses awk to scan each line for `{{...}}` placeholders and substitute only names present in the environment (`hooks/lib/template-loader.sh:56-136`). `load_and_render` composes load + render without fallback (`hooks/lib/template-loader.sh:138-151`). `append_template` conditionally appends a second template (`hooks/lib/template-loader.sh:153-168`). `_emit_fallback` and `load_and_render_safe` render fallback text when templates are missing, empty, or render empty (`hooks/lib/template-loader.sh:174-211`). `validate_template_dir` requires the `block`, `codex`, `claude`, and `plan` subdirectories (`hooks/lib/template-loader.sh:213-238`).
- inputs_outputs_state: Inputs are a template directory, template relative name, template content, and `VAR=value` substitution pairs. Outputs are rendered markdown on stdout, warnings/errors on stderr, and shell return codes. It maintains no files itself. Missing variables remain literal placeholders (`hooks/lib/template-loader.sh:12-13`, `hooks/lib/template-loader.sh:118-121`).
- gates_or_invariants: Rendering is single-pass to prevent placeholder injection from values (`hooks/lib/template-loader.sh:52-55`). Missing templates are nonfatal for normal loading and safe loading; safe loading always emits fallback if usable template output is absent (`hooks/lib/template-loader.sh:195-210`). Directory validation is advisory in common hook setup: `loop-common.sh` warns but keeps inline fallbacks available (`hooks/lib/loop-common.sh:240-248`).
- dependencies_and_callers: Sourced by `hooks/lib/loop-common.sh:240`, then used across read/write/edit/bash validators and the Codex stop hook. `hooks/loop-plan-file-validator.sh:66` uses it for schema-outdated blocks. `hooks/loop-codex-stop-hook.sh:1012-1013` uses it for goal-tracker prompt sections. `tests/test-template-references.sh:172-200` enforces safe rendering usage in critical validators.
- edge_cases_or_failure_modes: Variable names are documented as uppercase/numeric/underscore but are not enforced before becoming `TMPL_VAR_...` environment names (`hooks/lib/template-loader.sh:7-13`, `hooks/lib/template-loader.sh:62-67`). Arguments without `=` become name/value pairs with the same string due shell parameter expansion. Empty or unclosed placeholders are preserved unless an exact empty-name variable is provided; assigned tests assert preservation for normal callers. Command substitution can trim trailing newlines from rendered content. Awk failure returns `1` and writes an explicit rendering error (`hooks/lib/template-loader.sh:130-133`).
- validation_or_tests: Covered by `tests/test-error-scenarios.sh`, `tests/test-template-loader.sh`, `tests/test-templates-comprehensive.sh`, `tests/test-template-references.sh`, and robustness template tests. The assigned error-scenario test validates missing files/dirs, strict mode, fallback substitution, invalid directories, empty placeholders, and unclosed placeholders.
- skip_candidate: `no`

### HZ-141 `file` `prompt-template/block/schema-outdated.md`
- cursor: `[_]`
- core_role: Block-message template for RLCR state schema validation when an active loop lacks required v1.1.2+ state fields.
- algorithmic_behavior: Emits a concise markdown block headed `State Schema Outdated`, names the missing `{{FIELD_NAME}}`, tells the user the session was started with an older humanize version, provides recovery options, and states that the session will be terminated as unexpected to preserve state (`prompt-template/block/schema-outdated.md:1-12`).
- inputs_outputs_state: Input is `FIELD_NAME`, supplied by `hooks/loop-plan-file-validator.sh:66` through `load_and_render_safe`. Output becomes the JSON `reason` string for a hook block decision after newline escaping with `jq -Rs` (`hooks/loop-plan-file-validator.sh:60-77`). The immediate hook state transition is allow nothing further and exit after block response when a required field is empty (`hooks/loop-plan-file-validator.sh:80-89`).
- gates_or_invariants: Required fields are `plan_tracked` and `start_branch`, defined in `hooks/lib/loop-common.sh:25-26`, parsed from state frontmatter at `hooks/lib/loop-common.sh:449-450`, and checked in `hooks/loop-plan-file-validator.sh:80-89`. The fallback message has the same basic recovery contract if this template is missing (`hooks/loop-plan-file-validator.sh:61-66`).
- dependencies_and_callers: Called only by `schema_validation_error` in `hooks/loop-plan-file-validator.sh`. That hook first locates an active loop and strict-parses state (`hooks/loop-plan-file-validator.sh:33-49`), then performs schema validation before branch and plan tracking checks.
- edge_cases_or_failure_modes: If the template is missing, `load_and_render_safe` uses an inline fallback that lacks the final “terminated as unexpected” sentence. If `FIELD_NAME` contains unusual markdown, it is inserted verbatim through single-pass rendering. The template itself declares the preservation-oriented termination contract, but actual termination mechanics are outside this template.
- validation_or_tests: Template reference integrity is covered by template-loader and template-reference tests. The schema call site is visible at `hooks/loop-plan-file-validator.sh:66`; required-field definitions are visible at `hooks/lib/loop-common.sh:25-26`.
- skip_candidate: `no`

### HZ-171 `file` `prompt-template/codex/goal-tracker-update-section.md`
- cursor: `[_]`
- core_role: Shared Codex review prompt section that defines how tracker drift and Claude-requested goal-tracker updates are adjudicated during RLCR review.
- algorithmic_behavior: Instructs Codex that if Claude’s summary contains a “Goal Tracker Update Request” or Codex detects tracker drift, Codex must evaluate whether the mutable section of `goal-tracker.md` aligns with the Ultimate Goal and current AC progress (`prompt-template/codex/goal-tracker-update-section.md:1-5`). If correction is needed, it must update `@{{GOAL_TRACKER_FILE}}`, move tasks among active/completed/deferred sections, add Plan Evolution Log entries, and add blocking/queued side issues as appropriate (`prompt-template/codex/goal-tracker-update-section.md:6-10`). Rejected changes require explanation (`prompt-template/codex/goal-tracker-update-section.md:11`). Common request categories are enumerated at `prompt-template/codex/goal-tracker-update-section.md:13-18`.
- inputs_outputs_state: Input is `GOAL_TRACKER_FILE`, rendered by `hooks/loop-codex-stop-hook.sh:1009-1013`. Output is markdown embedded into both full-alignment and regular review prompts via `{{GOAL_TRACKER_UPDATE_SECTION}}` (`prompt-template/codex/full-alignment-review.md:75`, `prompt-template/codex/regular-review.md:65`). The downstream state mutation is a Codex edit to the mutable section of the goal tracker, not a direct shell-side mutation.
- gates_or_invariants: The critical invariant is “NEVER modify the IMMUTABLE SECTION” (`prompt-template/codex/goal-tracker-update-section.md:10`). Deferrals require strong justification (`prompt-template/codex/goal-tracker-update-section.md:18`). The section also requires Codex to explain rejected tracker changes, creating a review accountability gate (`prompt-template/codex/goal-tracker-update-section.md:11`).
- dependencies_and_callers: Generated in the Codex stop hook before full/regular review prompt rendering (`hooks/loop-codex-stop-hook.sh:1009-1013`) and passed into the larger prompt render calls later in the same hook (`hooks/loop-codex-stop-hook.sh:1101`, `hooks/loop-codex-stop-hook.sh:1118`). Falls back to a simpler inline goal-tracker update instruction if the template is unavailable (`hooks/loop-codex-stop-hook.sh:1010-1013`).
- edge_cases_or_failure_modes: Drift detection is prompt-governed and therefore depends on Codex following instructions; it is not a mechanical validation gate in this file. The fallback omits the detailed task-movement, side-issue, and deferral rules, so missing template weakens the review contract. The file intentionally allows updates only to the mutable section, but enforcement depends on surrounding hooks and review behavior.
- validation_or_tests: `tests/test-templates-comprehensive.sh:531-539` renders this real template and asserts both the section heading and rendered goal-tracker path are present.
- skip_candidate: `no`

### HZ-201 `file` `tests/robustness/test-timeout-robustness.sh`
- cursor: `[_]`
- core_role: Executable robustness specification for `scripts/portable-timeout.sh`, which protects RLCR hooks and helper scripts from hanging git, Codex, Claude, Gemini, and selector commands.
- algorithmic_behavior: Sources `scripts/portable-timeout.sh` and shared test helpers (`tests/robustness/test-timeout-robustness.sh:14-17`). Positive tests assert a timeout implementation is detected and valid (`tests/robustness/test-timeout-robustness.sh:31-49`), quick commands complete (`tests/robustness/test-timeout-robustness.sh:51-60`), timeout returns `124` when supported (`tests/robustness/test-timeout-robustness.sh:62-77`), arguments are preserved (`tests/robustness/test-timeout-robustness.sh:79-87`), command exit codes pass through (`tests/robustness/test-timeout-robustness.sh:89-100`), and shell pipelines work through `sh -c` (`tests/robustness/test-timeout-robustness.sh:102-110`). Edge tests cover short timeout elapsed bounds, zero timeout variance, large output, rapid cycles, nonexistent commands, empty command, special characters, signal handling, fallback detection, subshells, and `TIMEOUT_IMPL` visibility (`tests/robustness/test-timeout-robustness.sh:120-272`).
- inputs_outputs_state: Inputs are shell commands passed to `run_with_timeout`; outputs are PASS/FAIL/SKIP-style helper messages and process exit status from `print_test_summary` (`tests/robustness/test-timeout-robustness.sh:278-279`). No repository state is modified.
- gates_or_invariants: `TIMEOUT_IMPL` must be one of `gtimeout`, `timeout`, `python3`, `python`, or `none` (`tests/robustness/test-timeout-robustness.sh:42-49`, `tests/robustness/test-timeout-robustness.sh:239-247`). Timeout-capable implementations should return `124` for timed-out commands (`tests/robustness/test-timeout-robustness.sh:62-77`). Non-timeout command exit codes must be preserved (`tests/robustness/test-timeout-robustness.sh:89-100`).
- dependencies_and_callers: Tests `scripts/portable-timeout.sh`, whose detection priority is `gtimeout` -> GNU `timeout` -> `python3` -> `python` -> `none` (`scripts/portable-timeout.sh:9-29`) and whose `run_with_timeout` dispatches based on that implementation (`scripts/portable-timeout.sh:31-71`). This timeout wrapper is used by RLCR setup, plan validation, Codex stop hook, ask-codex, ask-gemini, and BitLesson selection (`hooks/loop-plan-file-validator.sh:22`, `hooks/loop-codex-stop-hook.sh:51`, `scripts/setup-rlcr-loop.sh:27`, `scripts/ask-gemini.sh:30`, `scripts/bitlesson-select.sh:29`). Included in the aggregate test list at `tests/run-all-tests.sh:112`.
- edge_cases_or_failure_modes: Timeout-specific assertions are skipped as passing when `TIMEOUT_IMPL=none` (`tests/robustness/test-timeout-robustness.sh:65-77`, `tests/robustness/test-timeout-robustness.sh:122-137`, `tests/robustness/test-timeout-robustness.sh:142-155`), so a platform without timeout support does not fail the suite. Zero timeout allows either success or timeout due implementation differences (`tests/robustness/test-timeout-robustness.sh:139-152`). Empty command handling is observed but not treated as a failure (`tests/robustness/test-timeout-robustness.sh:201-209`). Signal-trapping commands may exit `0` or `124` and both are accepted (`tests/robustness/test-timeout-robustness.sh:221-233`).
- validation_or_tests: This file is the validation artifact. It relies on `tests/test-helpers.sh` pass/fail counters and summary behavior (`tests/test-helpers.sh:20-58`) and exercises the runtime timeout wrapper used by core RLCR command paths.
- skip_candidate: `no`

## Worker Self-Test
- assigned_items_seen: 7 unique item sections above; each assigned item id appears once in Item Evidence
- missing_items: none
- duplicate_items: none
- final_worker_status: `complete`